{
  "slug": "stripe.search_customers",
  "service": "dashboard.stripe.com",
  "intentSynonyms": [
    "search customers in stripe",
    "search customers by query in stripe",
    "search customers using stripe search syntax in stripe"
  ],
  "description": "Search customers using Stripe search syntax. Example queries: \"email:'test@example.com'\", \"name:'John'\", \"metadata['key']:'value'\".",
  "actionVerb": "search",
  "sideEffectClass": "read",
  "params": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "Search query using Stripe search syntax"
      },
      "limit": {
        "description": "Number of results to return (default 10, max 100)",
        "type": "integer",
        "minimum": 1,
        "maximum": 100
      }
    },
    "required": [
      "query"
    ],
    "additionalProperties": false
  },
  "backing": "handler",
  "provenance": {
    "source": "opentabs",
    "sha": "4b17021637d2cac12b8d84d21c40e765aa7b85e9",
    "sourcePath": "plugins/stripe/src/tools/search-customers.ts",
    "license": "MIT",
    "signals": {
      "transportHelper": "api",
      "httpMethod": "GET",
      "opNameVerb": "search"
    }
  }
}
