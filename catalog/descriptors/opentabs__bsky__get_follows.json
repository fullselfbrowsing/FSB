{
  "slug": "bsky.get_follows",
  "service": "bsky.app",
  "intentSynonyms": [
    "get follows in bsky",
    "look up follows in bsky",
    "fetch a single follows in bsky",
    "view one specific follows in bsky",
    "get accounts a user follows in bsky"
  ],
  "description": "Get the list of accounts a user follows. Supports cursor-based pagination.",
  "actionVerb": "get",
  "sideEffectClass": "read",
  "params": {
    "type": "object",
    "properties": {
      "actor": {
        "type": "string",
        "description": "DID or handle of the user whose follows to retrieve"
      },
      "cursor": {
        "description": "Pagination cursor from a previous response",
        "type": "string"
      },
      "limit": {
        "description": "Number of follows to return (1-100, default 50)",
        "default": 50,
        "type": "number",
        "minimum": 1,
        "maximum": 100
      }
    },
    "required": [
      "actor"
    ],
    "additionalProperties": false
  },
  "backing": "handler",
  "provenance": {
    "source": "opentabs",
    "sha": "4b17021637d2cac12b8d84d21c40e765aa7b85e9",
    "sourcePath": "plugins/bluesky/src/tools/get-follows.ts",
    "license": "MIT",
    "signals": {
      "transportHelper": "api",
      "httpMethod": "GET",
      "opNameVerb": "get"
    }
  }
}
