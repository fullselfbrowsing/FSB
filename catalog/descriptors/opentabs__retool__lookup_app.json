{
  "slug": "retool.lookup_app",
  "service": "retool.com",
  "intentSynonyms": [
    "lookup an app in retool",
    "lookup app in retool",
    "look up app by url path in retool"
  ],
  "description": "Look up a Retool application by its URL path (e.g., \"fraud/fraud\"). Returns the full app state including components, queries, and configuration. Use this when you know the app path but not its UUID.",
  "actionVerb": "lookup",
  "sideEffectClass": "write",
  "params": {
    "type": "object",
    "properties": {
      "page_path": {
        "type": "string",
        "description": "Page path or name to look up (e.g., \"my-folder/my-app\")"
      }
    },
    "required": [
      "page_path"
    ],
    "additionalProperties": false
  },
  "backing": "dom",
  "provenance": {
    "source": "opentabs",
    "sha": "4b17021637d2cac12b8d84d21c40e765aa7b85e9",
    "sourcePath": "plugins/retool/src/tools/lookup-app.ts",
    "license": "MIT",
    "signals": {
      "transportHelper": "api",
      "httpMethod": "POST",
      "opNameVerb": "lookup"
    }
  }
}
