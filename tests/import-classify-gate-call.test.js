#!/usr/bin/env node
// Phase 36 / Plan 01 (CGEN-01) -- importer calls classifyGate() before emit.
// STUB registered by Plan 01 Task 1; the real assertions land in Plan 01 Task 2
// (aborts on an unclassified sensitive origin; emits on the benign todoist origin).
'use strict';
console.log('PASS (stub) -- filled by Plan 36-01 Task 2');
process.exit(0);
