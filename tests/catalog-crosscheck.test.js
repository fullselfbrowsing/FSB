#!/usr/bin/env node
// Phase 36 / Plan 03 (CGEN-02) -- side-effect derived-vs-declared cross-check test.
// STUB registered by Plan 01 Task 1; the real assertions land in Plan 03
// (void_invoice/delete_customer classify destructive; a GraphQL POST never read).
'use strict';
console.log('PASS (stub) -- filled by Plan 36-03');
process.exit(0);
