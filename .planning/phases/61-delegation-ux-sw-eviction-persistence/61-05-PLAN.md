---
phase: 61-delegation-ux-sw-eviction-persistence
plan: "05"
type: execute
wave: 2
depends_on:
  - "61-03"
files_modified:
  - mcp/src/agent-providers/spawn-supervisor.ts
  - mcp/src/agent-providers/runtime-files.ts
  - mcp/src/agent-providers/serve-delegation.ts
  - mcp/src/index.ts
  - tests/mcp-spawn-supervisor.test.js
  - tests/mcp-agent-orphan-recovery.test.js
  - tests/mcp-reverse-channel-contract.test.js
autonomous: true
requirements:
  - UX-04
  - UX-05
  - LIFE-04
must_haves:
  truths:
    - "Only the Phase 60 supervisor may hold, resume, cancel, settle, or report a live server-minted delegation."
    - "POSIX process-group hold/resume and expiry/cancel/result races settle exactly once; unsupported platforms fail closed."
    - "Restart loss requires both a new daemon generation and a persisted bounded recovery disposition; disconnect alone never qualifies."
  artifacts:
    - path: "mcp/src/agent-providers/spawn-supervisor.ts"
      provides: "Strict hold/resume/status authority on the existing supervised child tree"
      contains: "delegate.hold"
    - path: "mcp/src/agent-providers/runtime-files.ts"
      provides: "Bounded non-secret generation/recovery disposition persistence"
      contains: "generation"
    - path: "tests/mcp-spawn-supervisor.test.js"
      provides: "Schema, POSIX signal, expiry, cancel/final, and tree-absence race evidence"
      contains: "SIGSTOP"
  key_links:
    - from: "mcp/src/agent-providers/serve-delegation.ts"
      to: "mcp/src/agent-providers/spawn-supervisor.ts"
      via: "closed reverse methods route to the one supervisor instance"
      pattern: "delegate\.(hold|resume|status)"
    - from: "mcp/src/agent-providers/runtime-files.ts"
      to: "mcp/src/agent-providers/spawn-supervisor.ts"
      via: "startup cleanup persists a bounded restart-loss disposition for a prior generation"
      pattern: "generation.*recovery|recovery.*generation"
threat_model:
  - id: T61-07
    severity: Critical
    mitigation: "Exact live run check, POSIX group hold/continue confirmation, bounded lease, exact-once races"
    blocking_tests: "mcp-spawn-supervisor hold/resume/expiry race matrix"
  - id: T61-08
    severity: Critical
    mitigation: "Supervisor remains sole child authority; cancel waits for confirmed tree absence"
    blocking_tests: "cancel/result/hold/tree settlement tests"
  - id: T61-09
    severity: High
    mitigation: "Generation plus persisted recovery disposition; ordinary disconnect insufficient"
    blocking_tests: "orphan-recovery generation matrix"
---

<objective>
Add hold/resume/status and restart-loss evidence to the existing daemon supervisor without widening the adapter contract.

Purpose: Make human tab control and service-worker recovery depend on confirmed process authority and honest restart evidence.
Output: Closed reverse methods, POSIX process-group lifecycle handling, bounded generation/recovery status, and deterministic race tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/61-delegation-ux-sw-eviction-persistence/61-CONTEXT.md
@.planning/phases/61-delegation-ux-sw-eviction-persistence/61-RESEARCH.md
@.planning/phases/61-delegation-ux-sw-eviction-persistence/61-PATTERNS.md
@.planning/phases/61-delegation-ux-sw-eviction-persistence/61-VALIDATION.md
@.planning/phases/60-adapter-contract-claude-code-mvp/60-03-SUMMARY.md

<interfaces>
Closed additive reverse requests:

```ts
{ method: 'delegate.hold', payload: { delegationId: string } }
{ method: 'delegate.resume', payload: { delegationId: string } }
{ method: 'delegate.status', payload: {} }

type DelegationStatus = {
  generation: string;
  active: Array<{ delegationId: string; state: 'running'|'held'|'stopping' }>;
  restartLosses: Array<{ delegationId: string; code: 'daemon_restart_lost_run'; recoveredAt: number }>;
};
```

Status arrays are bounded and expose no PID, argv, env, cwd, task, prompt, credentials, or raw provider output. Hold/resume use the supervisor's retained process-group authority and do not add an adapter method.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add strict hold, resume, and status schemas to the supervisor authority</name>
  <files>mcp/src/agent-providers/spawn-supervisor.ts, mcp/src/agent-providers/serve-delegation.ts, mcp/src/index.ts, tests/mcp-spawn-supervisor.test.js, tests/mcp-reverse-channel-contract.test.js</files>
  <read_first>
- mcp/src/agent-providers/spawn-supervisor.ts
- mcp/src/agent-providers/serve-delegation.ts
- mcp/src/index.ts
- mcp/src/agent-providers/adapter.ts
- tests/mcp-spawn-supervisor.test.js
- tests/mcp-reverse-channel-contract.test.js
  </read_first>
  <action>
Implement D-14, D-23, D-26, and D-27. Extend the existing strict reverse-request parser/handler in `serve-delegation.ts` and `index.ts` with only the three `<interfaces>` methods. Reject extra keys, missing/case-varied ids, caller generation/state, unknown methods, and non-empty status payloads using the established Phase 59 error envelope. Route all three to the same `SpawnSupervisor`; do not add `hold/resume/status` to `AgentProviderAdapter` or a sixth adapter.

In `spawn-supervisor.ts`, follow the existing live-run map and server-minted id checks. Hold/resume accept only the exact active record; terminal/unknown/stopping/wrong-generation requests fail typed and perform no signal. Status projects a maximum of 64 active records and 128 restart dispositions through an allowlist, in deterministic order, with a daemon-minted `crypto.randomUUID` generation. It never exposes process handles or sensitive metadata. Extend supervisor and reverse-channel tests in their current built-output/closed-schema style for exact methods/payloads, extra keys, unknown ids/states, deterministic bounds, no sensitive fields, legacy start/cancel parity, and source proof that the five-method adapter remains unchanged.
  </action>
  <acceptance_criteria>
- Only exact `delegate.hold`, `delegate.resume`, and empty-payload `delegate.status` schemas are accepted additively.
- Requests route to the one existing supervisor and never change the five-method adapter interface.
- Status is bounded/deterministic and contains only generation, delegation id/state, and bounded recovery dispositions.
- `npm --prefix mcp run build && node tests/mcp-spawn-supervisor.test.js && node tests/mcp-reverse-channel-contract.test.js` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix mcp run build &amp;&amp; node tests/mcp-spawn-supervisor.test.js &amp;&amp; node tests/mcp-reverse-channel-contract.test.js</automated>
  </verify>
  <done>The daemon exposes a closed lifecycle/status protocol through its sole existing process authority.</done>
</task>

<task type="auto">
  <name>Task 2: Implement confirmed POSIX hold, resume, expiry, and exact-once settlement</name>
  <files>mcp/src/agent-providers/spawn-supervisor.ts, mcp/src/agent-providers/runtime-files.ts, tests/mcp-spawn-supervisor.test.js</files>
  <read_first>
- mcp/src/agent-providers/spawn-supervisor.ts
- mcp/src/agent-providers/runtime-files.ts
- tests/mcp-spawn-supervisor.test.js
- tests/mcp-agent-orphan-recovery.test.js
- .planning/phases/60-adapter-contract-claude-code-mvp/60-RESEARCH.md
  </read_first>
  <action>
Implement D-14, D-15, D-16, D-17, and D-27. Reuse the supervisor's injected child/process/clock/kill-tree dependencies. On supported POSIX platforms only, signal the retained process group with `SIGSTOP`, verify through the injected process-tree/status inspector that the exact tree is stopped, then return held; `SIGCONT` similarly verifies resumed. Unsupported platform, missing process group, signal/inspection failure, or exited child fails closed and converges to cancellation—never reports held/resumed optimistically. Arm the fixed five-minute hold expiry only after confirmed hold.

Coalesce duplicate hold/resume/cancel promises per run. Expiry, explicit cancel, result, error, child exit, hold, and resume races must use the existing settle-once path: cancel signals/kills the exact group, awaits inspector-confirmed tree absence, deletes runtime files, and only then reports success. A result during held/holding still cleans up exactly once. Extend fake-process/fake-clock tests for SIGSTOP/SIGCONT order, inspection delay/failure, unsupported platform, expiry boundary, hold-vs-result, resume-vs-cancel, duplicate operations, signal error, and descendant lingering. Follow existing supervisor test injection patterns; no real process is automated evidence.
  </action>
  <acceptance_criteria>
- Held/resumed is returned only after exact POSIX process-group confirmation; unsupported/unverifiable cases cancel fail closed.
- Five-minute expiry, cancel, result, and signal races settle once, clear timers, and leave zero confirmed descendants.
- No PID/argv/env/task is exposed in status/errors/runtime disposition.
- `npm --prefix mcp run build && node tests/mcp-spawn-supervisor.test.js` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix mcp run build &amp;&amp; node tests/mcp-spawn-supervisor.test.js</automated>
  </verify>
  <done>Process hold/resume is confirmed and bounded, and every lifecycle race converges to one cleaned terminal state.</done>
</task>

<task type="auto">
  <name>Task 3: Persist bounded generation and restart-loss recovery evidence</name>
  <files>mcp/src/agent-providers/spawn-supervisor.ts, mcp/src/agent-providers/runtime-files.ts, mcp/src/agent-providers/serve-delegation.ts, mcp/src/index.ts, tests/mcp-agent-orphan-recovery.test.js, tests/mcp-spawn-supervisor.test.js, tests/mcp-reverse-channel-contract.test.js</files>
  <read_first>
- mcp/src/agent-providers/runtime-files.ts
- mcp/src/agent-providers/spawn-supervisor.ts
- mcp/src/agent-providers/serve-delegation.ts
- tests/mcp-agent-orphan-recovery.test.js
- tests/mcp-spawn-supervisor.test.js
  </read_first>
  <action>
Implement D-23, D-24, and D-27. Extend `runtime-files.ts` by following its existing private-directory, atomic-write, schema-validation, bounded-cleanup pattern. Store the current non-secret daemon generation in new run metadata and, during startup orphan cleanup, emit a bounded `daemon_restart_lost_run` disposition only when a prior-generation nonterminal record is found and its exact process tree has been killed/confirmed absent. Retain at most 128 dispositions with delegation id, code, and timestamp; use restrictive permissions and atomic replace. Never re-adopt/replay a run and never infer restart from WebSocket disconnect.

Expose current generation and drained/non-destructive bounded dispositions through `delegate.status`; repeated status is idempotent and worker reconciliation may query after reconnect. Extend orphan-recovery tests in their existing private-dir/process-inspector style for same-generation active, new-generation orphan, corrupt/missing metadata, already-dead process, failed kill, bounded pruning, repeated startup/status, and disconnect-only absence. Extend supervisor/reverse source pins for no adoption/replay and no sensitive status fields.
  </action>
  <acceptance_criteria>
- Restart loss is emitted only for a prior-generation nonterminal record after confirmed cleanup; disconnect alone emits none.
- Current live runs are never adopted from disk and corrupt/failed-cleanup records fail closed without false success.
- Recovery status is bounded, idempotent, non-secret, and schema-validated.
- `npm --prefix mcp run build && node tests/mcp-agent-orphan-recovery.test.js && node tests/mcp-spawn-supervisor.test.js && node tests/mcp-reverse-channel-contract.test.js` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix mcp run build &amp;&amp; node tests/mcp-agent-orphan-recovery.test.js &amp;&amp; node tests/mcp-spawn-supervisor.test.js &amp;&amp; node tests/mcp-reverse-channel-contract.test.js</automated>
  </verify>
  <done>Daemon restart loss is proven by generation plus cleanup disposition and can never be confused with transport disconnect.</done>
</task>

</tasks>

<threat_model>

| Threat | Severity | Plan 05 mitigation and blocking evidence |
|--------|----------|------------------------------------------|
| T61-07 hold race | Critical | Exact live group, confirm-before-return, fixed expiry, injected POSIX race matrix |
| T61-08 process orphan/false settlement | Critical | Sole supervisor authority, settle-once, confirmed tree absence, runtime-file cleanup tests |
| T61-09 false restart classification | High | Daemon generation plus bounded post-cleanup disposition; disconnect-negative tests |

No High/Critical result is deferrable. Real kernel process-group behavior remains human-needed only at milestone end.
</threat_model>

<verification>
- `npm --prefix mcp run build`
- `node tests/mcp-spawn-supervisor.test.js`
- `node tests/mcp-agent-orphan-recovery.test.js`
- `node tests/mcp-reverse-channel-contract.test.js`
</verification>

<success_criteria>
- Hold/resume/status is strict additive supervisor protocol, not adapter expansion.
- POSIX lifecycle operations are confirmed, bounded, and exact-once under races.
- Restart-loss evidence requires generation and persisted cleanup disposition; disconnect never substitutes.
</success_criteria>

<output>
After completion, create `.planning/phases/61-delegation-ux-sw-eviction-persistence/61-05-SUMMARY.md`.
</output>
