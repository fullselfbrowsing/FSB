---
phase: 61-delegation-ux-sw-eviction-persistence
plan: "04"
type: execute
wave: 2
depends_on:
  - "61-02"
files_modified:
  - mcp/src/agent-scope.ts
  - extension/ws/mcp-tool-dispatcher.js
  - extension/utils/agent-registry.js
  - tests/agent-scope.test.js
  - tests/agent-registry.test.js
  - tests/agent-bridge-routes.test.js
  - tests/open-tab-background-default.test.js
autonomous: true
requirements:
  - UX-04
  - UX-05
must_haves:
  truths:
    - "The server delegation id crosses only an optional registration sidecar and maps to exactly one extension-minted agent id."
    - "The controller proves Take Control eligibility on the exact active owned tab; after confirmed hold the registry atomically seals every exact mapped tab into one unclaimable lease."
    - "Resume/Stop restore or release only the complete unchanged mapping; missing/inconsistent mappings release nothing unrelated."
  artifacts:
    - path: "mcp/src/agent-scope.ts"
      provides: "Optional bounded FSB_DELEGATION_ID registration sidecar"
      contains: "FSB_DELEGATION_ID"
    - path: "extension/utils/agent-registry.js"
      provides: "Delegation mapping and sealed hold lease primitives under the registry lock"
      contains: "holdLease"
    - path: "tests/agent-registry.test.js"
      provides: "Two-agent isolation, conflicts, expiry, restore, and exact cleanup evidence"
      contains: "delegationId"
  key_links:
    - from: "mcp/src/agent-scope.ts"
      to: "extension/ws/mcp-tool-dispatcher.js"
      via: "optional register sidecar carries only the bounded server delegation id"
      pattern: "FSB_DELEGATION_ID|delegationId"
    - from: "extension/ws/mcp-tool-dispatcher.js"
      to: "extension/utils/agent-registry.js"
      via: "an injected controller authorization gate verifies the expected live daemon delegation before registry binding"
      pattern: "bindRegisteredAgent|authorizeDelegation"
threat_model:
  - id: T61-05
    severity: Critical
    mitigation: "Server id accepted only on registration, authorized against the controller's one expected live daemon delegation, and bound to one extension-minted agent"
    blocking_tests: "agent-scope/bridge/registry exact sidecar tests"
  - id: T61-06
    severity: Critical
    mitigation: "All release operations require exact delegation/agent/tab/token mapping"
    blocking_tests: "two-agent adversarial cleanup matrix"
  - id: T61-07
    severity: Critical
    mitigation: "Registry-locked sealed hold lease with confirm-before-convert, conflict denial, expiry cancellation"
    blocking_tests: "fake-clock hold/restore/conflict cases"
---

<objective>
Extend existing agent identity and tab ownership contracts with exact delegation correlation and a sealed human-control lease.

Purpose: Make handoff and cleanup safe under two-agent conflicts without creating a sixth adapter or a second ownership registry.
Output: Optional registration sidecar, exact mapping APIs, hold-lease ownership transitions, and adversarial registry tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/61-delegation-ux-sw-eviction-persistence/61-CONTEXT.md
@.planning/phases/61-delegation-ux-sw-eviction-persistence/61-RESEARCH.md
@.planning/phases/61-delegation-ux-sw-eviction-persistence/61-PATTERNS.md
@.planning/phases/61-delegation-ux-sw-eviction-persistence/61-VALIDATION.md
@.planning/phases/59-reverse-request-channel-security-foundation/59-03-SUMMARY.md

<interfaces>
```ts
// additive optional registration field only
{ agentId, ownershipToken, delegationId?: string }
```
```js
registry.bindDelegation({ delegationId, agentId })
registry.getAgentForDelegation(delegationId)
registry.getDelegationOwnedTabs({ delegationId, agentId })
// Controller has already queried the active tab, proved activeTabId is in ownedTabs,
// and received confirmed supervisor-held state. Registry never queries active-tab UI state.
registry.sealHoldLease({ delegationId, agentId, activeTabId, ownedTabs, expiresAt })
registry.restoreHoldLease({ delegationId, agentId, liveTabIds })
registry.releaseDelegation({ delegationId, agentId })
// -> { ok, code, releasedTabCount }
```

`ownedTabs` is the complete canonical array of `{ tabId, ownershipToken }` returned by the registry immediately before hold. Under one lock, seal compares it set-for-set with the current complete mapping, then moves every mapped tab into one versioned delegation lease recording `activeTabId`, all exact tab/token pairs, issued time, and expiry. The active tab identifies the user's handoff target; it does not reduce the lease to one tab. Default `open_tab` remains background creation; no focus-stealing policy changes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Carry the optional server delegation id into one extension agent mapping</name>
  <files>mcp/src/agent-scope.ts, extension/ws/mcp-tool-dispatcher.js, extension/utils/agent-registry.js, tests/agent-scope.test.js, tests/agent-registry.test.js, tests/agent-bridge-routes.test.js</files>
  <read_first>
- mcp/src/agent-scope.ts
- extension/ws/mcp-tool-dispatcher.js
- extension/utils/agent-registry.js
- tests/agent-scope.test.js
- tests/agent-bridge-routes.test.js
- .planning/phases/59-reverse-request-channel-security-foundation/59-03-SUMMARY.md
  </read_first>
  <action>
Implement D-13, D-26, and D-27. In `agent-scope.ts`, follow the existing injected-environment/registration payload builder: read optional `FSB_DELEGATION_ID`, validate the same bounded server id grammar used by Phase 60, include it only on the initial agent registration sidecar, and never expose it as caller-supplied MCP tool args. Preserve absent-env and all Phase 59 wire/error behavior.

In `mcp-tool-dispatcher.js`, follow its existing `agent.register`/canonical extension-minted identity path: never treat delegation id as agent id or ownership token. When a sidecar is present, pass the freshly minted agent id and exact sidecar to an injected `bindRegisteredAgent`/authorization callback owned by the controller contract from Plan 02. That gate accepts only the single daemon-minted delegation currently expected by an active starting/running controller and consumes the expected registration; a missing gate, pre-registration, stale/unknown/terminal delegation, duplicate delegation-to-different-agent, agent-to-different-delegation, malformed, or case-varied id rolls back the ordinary agent record and maps nothing. Only the authorized controller gate calls the registry's exact binding method. In `agent-registry.js`, extend the existing v1 envelope additively with a delegation map, clone/validate/hydrate it under `withRegistryLock`, remove inconsistent/ghost mappings fail closed, and expose only `<interfaces>` lookups.

Extend `agent-scope.test.js`, `agent-bridge-routes.test.js`, and `agent-registry.test.js` in their current built-output/fake-runtime/storage styles for absent/present/malformed sidecar, caller-arg absence, missing authorization gate, unknown/not-yet-active/stale/terminal delegation, expected-live single consume, rollback on denial, exact one-to-one mapping, duplicate conflicts, hydration, removal, and legacy registration parity.
  </action>
  <acceptance_criteria>
- `FSB_DELEGATION_ID` appears only as an optional validated registration sidecar and never in MCP tool input schemas.
- One controller-expected live server delegation maps once to one extension-minted agent id; pre-registration, stale/unknown/terminal, conflict, and malformed cases fail without partial mutation.
- Phase 59 registration/wire/error behavior is unchanged when the sidecar is absent.
- `npm --prefix mcp run build && node tests/agent-scope.test.js && node tests/agent-registry.test.js && node tests/agent-bridge-routes.test.js` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix mcp run build &amp;&amp; node tests/agent-scope.test.js &amp;&amp; node tests/agent-registry.test.js &amp;&amp; node tests/agent-bridge-routes.test.js</automated>
  </verify>
  <done>Delegation correlation is optional, bounded, one-to-one, and never weakens extension-minted identity.</done>
</task>

<task type="auto">
  <name>Task 2: Seal exact owned-tab state into an unclaimable hold lease</name>
  <files>extension/utils/agent-registry.js, tests/agent-registry.test.js</files>
  <read_first>
- extension/utils/agent-registry.js
- tests/agent-registry.test.js
- extension/utils/agent-tab-context.js
- extension/ui/owner-chip.js
- .planning/phases/61-delegation-ux-sw-eviction-persistence/61-PATTERNS.md
  </read_first>
  <action>
Implement D-14, D-15, and D-27. Extend `agent-registry.js` using its existing ownership-token checks and module-scope registry mutex. The registry never queries the browser's current active tab; Plan 06's controller owns that UI-state eligibility check. After confirmed supervisor hold, `sealHoldLease` must validate the exact delegation/agent mapping, require `activeTabId` to be a member of the caller's complete registry-produced `ownedTabs` snapshot, and compare the entire `{tabId, ownershipToken}` set against the current complete mapping under one lock. Atomically remove every mapped tab from active automation ownership and write one sealed lease containing only delegation/agent/active-tab/all-tab-token/issued/expiry fields. Every held tab remains unavailable to every other agent's bind, claim, release, or open-tab reuse. Use a fixed five-minute lease, session storage, and an injected clock; expiry returns a cancellation-required disposition instead of making any tab free while the process might remain held.

Extend registry tests in their existing two-agent/fake-clock pattern for active id not in the supplied owned set, partial/missing/extra tab sets, duplicate tab ids, any token/mapping change, concurrent seal/claim/release, second-agent claims against each held tab, exact expiry boundary, module reload, and storage write rejection. Source-pin that the registry never calls `chrome.tabs.query` or decides active-tab eligibility. Assert the complete original ownership set remains intact on every validation/persistence failure.
  </action>
  <acceptance_criteria>
- Only a controller-verified active tab can trigger sealing, and the registry then seals the complete exact mapped tab/token set after confirmed supervisor hold.
- Every tab in the sealed lease is unavailable to every other claim/release path and the complete lease survives worker reload for its bounded duration.
- Partial/conflicting/storage failure leaves the complete original ownership set intact; expiry requires cancel and never silently frees any tab.
- `node tests/agent-registry.test.js` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node tests/agent-registry.test.js</automated>
  </verify>
  <done>The human-control window is one sealed complete-delegation ownership lease, not a one-tab or unowned race window.</done>
</task>

<task type="auto">
  <name>Task 3: Restore or release only the exact unchanged delegation mapping</name>
  <files>extension/ws/mcp-tool-dispatcher.js, extension/utils/agent-registry.js, tests/agent-registry.test.js, tests/agent-bridge-routes.test.js, tests/open-tab-background-default.test.js</files>
  <read_first>
- extension/utils/agent-registry.js
- extension/ws/mcp-tool-dispatcher.js
- tests/agent-registry.test.js
- tests/open-tab-background-default.test.js
- tests/agent-bridge-routes.test.js
  </read_first>
  <action>
Implement D-12, D-15, D-16, D-17, D-26, and D-27. Implement `restoreHoldLease` and `releaseDelegation` inside the existing registry lock. Resume succeeds only when delegation/agent/complete tab-token lease are unchanged and the controller-supplied `liveTabIds` proves every held tab identity still exists; atomically restore the complete original active-ownership set before returning success. If any tab/token/identity cannot restore, retain the complete sealed state for controller cancellation and return a typed `resume_ownership_lost` result—never partially restore or bind a substitute tab or agent.

Cleanup accepts the server delegation id plus expected mapped agent, releases only that agent's active tabs and complete held lease, removes the exact mapping/lease, and returns the exact distinct union count. Missing/inconsistent mapping releases zero and emits a typed diagnostic without touching any other record. Keep `open_tab` default-background behavior unchanged by following the existing dispatcher route; Take Control never creates a sixth adapter or changes global tab focus defaults. Extend the adversarial tests for two agents/two delegations, missing/mismatched maps, repeated cleanup, multi-tab held restore, mixed active+held exact distinct count, any stale token/tab closure, partial-restore prohibition, restore race, and background-open parity.
  </action>
  <acceptance_criteria>
- Resume restores the complete exact unchanged lease atomically; failure returns typed cancellation disposition and never partially restores or substitutes ownership.
- Stop cleanup releases exactly mapped active/held tabs once and reports the exact count; missing/inconsistent mapping releases zero.
- A second agent's tabs and default background `open_tab` behavior are unchanged across every adversarial case.
- `node tests/agent-registry.test.js && node tests/agent-bridge-routes.test.js && node tests/open-tab-background-default.test.js` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node tests/agent-registry.test.js &amp;&amp; node tests/agent-bridge-routes.test.js &amp;&amp; node tests/open-tab-background-default.test.js</automated>
  </verify>
  <done>Resume and cleanup are exact, idempotent, countable, and isolated from every unrelated agent/tab.</done>
</task>

</tasks>

<threat_model>

| Threat | Severity | Plan 04 mitigation and blocking evidence |
|--------|----------|------------------------------------------|
| T61-05 delegation confused deputy | Critical | Sidecar-only server id, controller-expected live delegation gate, rollback on denial, extension-minted identity, strict one-to-one mapping tests |
| T61-06 cross-agent release | Critical | Expected delegation+agent+tab+token match and two-agent cleanup isolation |
| T61-07 hold ownership race | Critical | Controller active-tab proof plus confirm-then-revalidate complete-set atomic sealed lease and conflict/expiry/restore matrix |

No High/Critical result is deferrable. Real active-tab handoff remains human-needed only at milestone end.
</threat_model>

<verification>
- `npm --prefix mcp run build`
- `node tests/agent-scope.test.js`
- `node tests/agent-registry.test.js`
- `node tests/agent-bridge-routes.test.js`
- `node tests/open-tab-background-default.test.js`
</verification>

<success_criteria>
- Server delegation ids correlate one-to-one with extension identities without entering caller tool args.
- Hold transitions are exact, confirmed, seal every mapped tab atomically, persist, and remain conflict-safe.
- Resume/cleanup never claim or release an unrelated agent's tab and report honest exact counts.
</success_criteria>

<output>
After completion, create `.planning/phases/61-delegation-ux-sw-eviction-persistence/61-04-SUMMARY.md`.
</output>
