---
phase: 33
plan: 33-01
title: PhantomStream 0.2.1 media-mirroring uptake
requirements: [MEDIA-01, MEDIA-02, MEDIA-03, MEDIA-04]
created: 2026-06-23
status: complete
---

# Plan 33-01 — PhantomStream 0.2.1 Media-Mirroring Uptake

Single plan, consume-the-upgrade. Surgical glue + tests + docs; no capability/MCP/agent-loop change.

## Task 1 — Dependency bump + targeted rebuild (MEDIA-01)
- `package.json`: pin `@full-self-browsing/phantom-stream` `0.1.0 → 0.2.1` (exact); `npm install` (updates `package-lock.json` integrity).
- Rebuild ONLY the three phantom-stream esbuild entries (capture, protocol, viewer) via a direct `esbuild.build` invocation — NOT `buildAll()` — so `dist/offscreen/lattice-host.js` / `stt.js` (user drift) stay byte-identical.
- Verify the media surface landed: protocol bundle exposes `ext:dom-media` + `classifyManifest`; capture bundle carries the media tracker; viewer bundle carries `reconcileMediaDrift`/`MediaPlayer`.

## Task 2 — Entry shims (MEDIA-01)
- `…/phantom-stream-protocol-entry.js`: import + freeze-export `classifyManifest` (`STREAM.MEDIA`/`MEDIA_HINT` ride the existing `STREAM` object).
- `…/phantom-stream-viewer-entry.js`: forward `mediaMode` + `onMediaBlocked`/`onMediaUnavailable`/`mediaReconcileConfig` into `createViewer`.

## Task 3 — Capture-side relay glue (MEDIA-02)
- `dom-stream.js` `forwardCaptureMessage`: add `STREAM.MEDIA` → `domStreamMedia` and `STREAM.MEDIA_HINT` → `domStreamMediaHint` branches (the allowlist otherwise drops them). Forward the `MediaSyncPayload` intact.
- `background.js`: add `domStreamMedia`/`domStreamMediaHint` cases → `fsbWebSocket.send('ext:dom-media'|'ext:dom-media-hint', payload)`.

## Task 4 — Viewer-side dispatch glue (MEDIA-03)
- `showcase/js/dashboard.js`: `handleDOMMedia`/`handleDOMMediaHint` (stale-frame guard + stream-only), inbound router cases for `ext:dom-media`/`-hint`, and `mediaMode: 'reference'` + degrade callbacks on `createDashboardViewer`.
- `…/dashboard-page.component.ts`: the same parity edits (Angular).

## Task 5 — Tests (MEDIA-04)
- New `tests/phantom-stream-media-sync.test.js`: `reconcileMediaDrift` branch coverage (incomplete-remote / seeked / paused / live rejoin+in-band / VOD hold+nudge+hard-seek / loop-wrap / latency compensation) + `classifyManifest` (hls/dash/null) + the `/protocol` media surface. 31 assertions.
- New `tests/phantom-stream-media-wiring.test.js`: source-level lock across all three glue seams (capture/relay/dashboard static+Angular) + the rebuilt bundle media surface (vm-loaded protocol bundle). 24 assertions.
- Update version-pin tests: `phantom-stream-exports.test.js` (`0.2.1`), `tests/helpers/phantom-stream-public-pin.js` (version/integrity/tarball/shasum), `phantom-stream-public-package.test.js` (comment).
- Wire both new tests into `package.json` `scripts.test`.

## Task 6 — Pin + traceability + UAT (MEDIA-01..04)
- `PHANTOMSTREAM-PIN.md`: `0.2.1` version/integrity/tarball/shasum/published_at/last_verified + a media-uptake section.
- `REQUIREMENTS.md` (MEDIA-01..04 + traceability + counts), `ROADMAP.md` (Phase 33), `STATE.md` (pointer), `v0.9.99-MILESTONE-AUDIT.md` (Phase 33 row).
- `33-HUMAN-UAT.md`: live media playback fidelity recorded `human_needed`.

## Gate
Full `npm test` EXIT 0 (the standing milestone gate). Live media UAT is non-blocking deferred debt.
