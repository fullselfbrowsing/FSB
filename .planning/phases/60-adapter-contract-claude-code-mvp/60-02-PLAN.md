---
phase: 60-adapter-contract-claude-code-mvp
plan: "02"
type: execute
wave: 2
depends_on:
  - "60-01"
files_modified:
  - mcp/src/agent-providers/claude-stream.ts
  - mcp/src/agent-providers/claude-code.ts
  - tests/fixtures/agent-streams/claude-code-2.1.177/manifest.json
  - tests/fixtures/agent-streams/claude-code-2.1.177/contract-stream.jsonl
  - tests/mcp-agent-stream-fixture.test.js
  - tests/mcp-claude-code-adapter.test.js
autonomous: true
requirements:
  - ADAPT-01
  - ADAPT-02
  - CLAUDE-01
  - CLAUDE-02
  - CLAUDE-03
  - CLAUDE-04
must_haves:
  truths:
    - "Every accepted Claude JSONL event becomes one strict provider-neutral event, and raw Claude shapes stop at the adapter boundary."
    - "Unknown or malformed event discriminants fail loud as agent_protocol_drift, terminate the affected run through the adapter contract, and never fabricate or replay success."
    - "The parser handles byte/chunk splits, a final unterminated line, bounded individual lines, and more than 200 KiB total incremental output without buffering the stream wholesale."
    - "Fixture provenance is truthful: deterministic schema-derived bytes provisionally cover parser CI now, while CLAUDE-03's recorded-2.1.177 provenance subcriterion remains human_needed until milestone-end capture/corroboration."
  artifacts:
    - path: "mcp/src/agent-providers/claude-stream.ts"
      provides: "Bounded incremental JSONL framing, runtime validation, init attestation, and normalization"
      contains: "agent_protocol_drift"
    - path: "mcp/src/agent-providers/claude-code.ts"
      provides: "Concrete exact-five-method Claude adapter composed from injected detection/parser/kill dependencies"
      contains: "createClaudeCodeAdapter"
    - path: "tests/fixtures/agent-streams/claude-code-2.1.177/manifest.json"
      provides: "Pinned fixture profile, sequence labels, sanitization, and honest provenance metadata"
      contains: "liveCapturePending"
    - path: "tests/mcp-agent-stream-fixture.test.js"
      provides: "Happy path, mutation, framing, bounds, attestation, and no-fabricated-success evidence"
      contains: "agent_protocol_drift"
  key_links:
    - from: "mcp/src/agent-providers/claude-code.ts"
      to: "mcp/src/agent-providers/claude-stream.ts"
      via: "parseEvents delegates all provider-specific JSONL parsing to the bounded normalizer"
      pattern: "parseClaude|parseEvents"
    - from: "mcp/src/agent-providers/claude-stream.ts"
      to: "mcp/src/agent-providers/adapter.ts"
      via: "only strict AgentEvent values cross the adapter boundary"
      pattern: "AgentEvent"
    - from: "tests/mcp-agent-stream-fixture.test.js"
      to: "tests/fixtures/agent-streams/claude-code-2.1.177/manifest.json"
      via: "manifest labels drive exact expected sequences and provenance assertions"
      pattern: "contract-stream|expectedSequence"
---

<objective>
Complete the Claude adapter and pin its provider-stream boundary with a bounded, fail-loud, offline-evaluable JSONL contract.

Purpose: Ensure protocol drift, stream pressure, and provider-specific shapes cannot become silent UI success or leak above the adapter.
Output: Incremental parser/normalizer, concrete Claude adapter factory, truthful contract fixture, deterministic mutations, and focused tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/60-adapter-contract-claude-code-mvp/60-CONTEXT.md
@.planning/phases/60-adapter-contract-claude-code-mvp/60-AI-SPEC.md
@.planning/phases/60-adapter-contract-claude-code-mvp/60-RESEARCH.md
@.planning/phases/60-adapter-contract-claude-code-mvp/60-VALIDATION.md
@.planning/phases/60-adapter-contract-claude-code-mvp/60-01-SUMMARY.md

<interfaces>
Normalized types are the closed provider-neutral set `init`, `assistant`, `assistant_delta`, `user`, `tool_use`, `tool_result`, `retry`, `result`, and `diagnostic`. Every value is strict `{ type, sessionId, payload }`; additional provider fields may survive only inside `payload` after a recognized envelope's required fields validate.

Known baseline raw inputs are `system/init`, `assistant`, `user`, `stream_event`, `system/api_retry`, and `result`. Unknown top-level types, unknown required `system` subtypes, missing required fields, invalid JSON, oversized lines, impossible ordering, and a stream ending without a valid result are typed failures, not ignored noise.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the bounded incremental Claude JSONL normalizer</name>
  <files>mcp/src/agent-providers/claude-stream.ts, tests/mcp-agent-stream-fixture.test.js</files>
  <read_first>
- .planning/phases/60-adapter-contract-claude-code-mvp/60-AI-SPEC.md
- .planning/phases/60-adapter-contract-claude-code-mvp/60-RESEARCH.md
- mcp/src/agent-providers/adapter.ts
- mcp/src/ext-protocol.ts
- mcp/src/types.ts
- mcp/package.json
- tests/mcp-reverse-channel-contract.test.js
  </read_first>
  <action>
Implement an async-iterable parser that consumes `Buffer`/string chunks with one `StringDecoder`-equivalent UTF-8 boundary mechanism, accumulates only the current line, accepts CRLF/LF and one final unterminated JSON object, and throws a typed `AgentProtocolDriftError` when a line exceeds 256 KiB or parsing/validation fails. It must process unlimited total output incrementally and never call whole-stream `readFile`, concatenate every prior line, or treat chunks as line boundaries.

Use Zod already present in `mcp/package.json` for strict normalized output and discriminated provider validation. Define required-field schemas for each known top-level type and `system` subtype while using passthrough only inside recognized provider envelopes. Establish one session id from init and require later session ids to match when present. Validate init before yielding browser-ready state: no built-in tools, exactly the `fsb` MCP server, no loaded plugins/hooks/customization indicators, and no missing FSB server. Capabilities are optional because 2.1.177 predates the documented 2.1.205 array.

Normalize assistant text/content deltas, tool-use blocks, user/tool-result blocks, API retries, and terminal result into the closed set in `<interfaces>`. Preserve validated usage/model/turn/retry fields in payload without assuming billing. Recognize documented pre-init hook/plugin events only to raise typed configuration drift. Track ordering so a result cannot precede init, two terminal results reject, and EOF without result returns non-success. The parser emits no fabricated `result`, never retries, and never logs the raw line.

Start the direct Node test with generated in-memory cases for split JSON, split multibyte UTF-8, CRLF, final unterminated line, invalid/missing/unknown events, init attestation, duplicate/missing result, and 256 KiB boundary behavior.
  </action>
  <acceptance_criteria>
- A valid known stream yields only strict `{type, sessionId, payload}` objects whose types belong to the nine-value normalized set.
- Every byte/chunk split and a split multibyte character produces the same normalized sequence as the unsplit input; a final unterminated valid object is parsed once.
- Unknown top-level/system subtype, malformed JSON, missing required field, unexpected tool/MCP/plugin init surface, duplicate result, missing result, and a line over 256 KiB throw a typed error with code `agent_protocol_drift` and no raw input content.
- More than 200 KiB across valid sub-limit lines drains successfully without a whole-stream accumulator.
- `npm --prefix mcp run build && node tests/mcp-agent-stream-fixture.test.js` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix mcp run build &amp;&amp; node tests/mcp-agent-stream-fixture.test.js</automated>
  </verify>
  <done>The provider JSONL boundary is bounded, incremental, strict, init-attested, and fail-loud.</done>
</task>

<task type="auto">
  <name>Task 2: Add the truthful provisional contract fixture and deterministic mutation matrix</name>
  <files>tests/fixtures/agent-streams/claude-code-2.1.177/manifest.json, tests/fixtures/agent-streams/claude-code-2.1.177/contract-stream.jsonl, tests/mcp-agent-stream-fixture.test.js</files>
  <read_first>
- .planning/phases/60-adapter-contract-claude-code-mvp/60-CONTEXT.md
- .planning/phases/60-adapter-contract-claude-code-mvp/60-AI-SPEC.md
- .planning/phases/60-adapter-contract-claude-code-mvp/60-VALIDATION.md
- mcp/src/agent-providers/claude-stream.ts
- tests/fixtures
- tests/mcp-agent-stream-fixture.test.js
  </read_first>
  <action>
Create a sanitized deterministic JSONL contract fixture for the exact known 2.1.177 shapes needed by Phase 60: init with only the FSB MCP surface, assistant text and tool-use content, partial stream events, user/tool result, API retry progress, and one terminal result with usage. Use synthetic ids/text and no real prompt, user path, browser content, credential, OAuth material, or provider response prose.

Create a strict manifest with `profileVersion: "2.1.177"`, `provenance: "schema-derived-contract"`, `liveCapturePending: true`, `sanitized: true`, the exact expected normalized sequence, terminal label, and source-doc URLs. Do not use the words live recording/captured output for this file. Preserve an explicit milestone-end task to compare a genuine sanitized 2.1.177 capture before changing `liveCapturePending`.

Record explicitly in the manifest and test output that this artifact does **not** satisfy the recorded-provenance portion of CLAUDE-03/D-27. It provides provisional automated parser coverage only; phase verification must report that subcriterion `human_needed`, not complete, until the deferred authenticated capture is performed. Do not mark the whole requirement passed merely because the schema-derived fixture is green.

Drive deterministic mutations from the fixture: split at all significant byte boundaries, final line without newline, compatible extra fields, unknown type, unknown system subtype/pre-init hook, malformed JSON, missing fields, mismatched session, duplicate result, missing result, an oversized line, noisy stderr-equivalent data kept outside stdout parsing, and more than 200 KiB total valid output. Assert exact success/failure labels and zero fabricated terminal success on every negative case.
  </action>
  <acceptance_criteria>
- Manifest has exactly truthful schema-derived provenance, `liveCapturePending: true`, sanitization metadata, 2.1.177 profile, expected sequence, and no claim of a performed live capture.
- Manifest/test reporting states the recorded-provenance portion of CLAUDE-03 is `human_needed` and cannot be promoted by automated schema-derived results.
- Fixture and manifest contain no absolute home path, email, URL token, API key, OAuth data, password, CVV, PAN-like value, or real browser content.
- All EV-01..EV-08 fixture/mutation classes from `60-AI-SPEC.md` have explicit assertions.
- `node tests/mcp-agent-stream-fixture.test.js` exits 0 without invoking `claude`, a model API, Chrome, or a network service.
  </acceptance_criteria>
  <verify>
    <automated>node tests/mcp-agent-stream-fixture.test.js</automated>
  </verify>
  <done>CI has a pinned, sanitized provisional stream contract and complete deterministic drift/framing coverage, while recorded provenance remains explicitly human_needed.</done>
</task>

<task type="auto">
  <name>Task 3: Compose the concrete Claude adapter from the closed profile and parser</name>
  <files>mcp/src/agent-providers/claude-code.ts, tests/mcp-claude-code-adapter.test.js, tests/mcp-agent-stream-fixture.test.js</files>
  <read_first>
- mcp/src/agent-providers/adapter.ts
- mcp/src/agent-providers/registry.ts
- mcp/src/agent-providers/claude-detect.ts
- mcp/src/agent-providers/claude-profile.ts
- mcp/src/agent-providers/claude-stream.ts
- tests/mcp-agent-provider-contract.test.js
- tests/mcp-claude-code-adapter.test.js
- tests/mcp-agent-stream-fixture.test.js
  </read_first>
  <action>
Implement `createClaudeCodeAdapter(dependencies)` as a concrete object with exactly `detect`, `buildSpawn`, `parseEvents`, `kill`, and `caps`. Compose Plan 01 detection/profile and this plan's parser; inject the kill dependency so Plan 03/04 can bind platform tree cleanup without importing a future module or spawning here. `buildSpawn` validates task/context bounds, requires a successful retained detection/profile and owner-only MCP/agent file context, returns frozen declarative data, and performs no filesystem/process/global mutation. `parseEvents` exposes the async iterable without buffering. `caps()` returns task mode true and chat/resume/server false.

Extend adapter tests to enumerate exactly five own callable methods, prove every method delegates to the expected injected component, prove buildSpawn never invokes spawn/exec/shell and never includes task text, and prove parser drift reaches the caller as typed `agent_protocol_drift`. The registry remains a closed injected registry in this wave; the production singleton registration is created only in Plan 04 when the real kill dependency exists.
  </action>
  <acceptance_criteria>
- The concrete adapter has exactly the five required callable keys and implements the shared `AgentProviderAdapter` type.
- `buildSpawn` is pure/declarative and rejects missing detection, task over 64 KiB, invalid context/config path, and mutated/case-varied provider identity.
- `parseEvents` yields the fixture's exact normalized sequence and exposes typed drift without fallback or replay.
- `kill` calls only the injected tree-kill dependency and `caps()` exactly reports task true/chat false/resume false/server false.
- `npm --prefix mcp run build && node tests/mcp-agent-provider-contract.test.js && node tests/mcp-claude-code-adapter.test.js && node tests/mcp-agent-stream-fixture.test.js` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix mcp run build &amp;&amp; node tests/mcp-agent-provider-contract.test.js &amp;&amp; node tests/mcp-claude-code-adapter.test.js &amp;&amp; node tests/mcp-agent-stream-fixture.test.js</automated>
  </verify>
  <done>The concrete Claude adapter is exact-five-method, declarative, provider-bounded, and fully offline-testable.</done>
</task>

</tasks>

<threat_model>

| Threat | Severity | Plan 02 mitigation and blocking evidence |
|--------|----------|------------------------------------------|
| T60-02 inherited tool/config surface | Critical | Init attestation rejects built-ins, non-FSB MCP, plugins/hooks, or missing FSB before browser-ready state |
| T60-04 provider drift/fabricated success | High | Required discriminants/order, unknown-type terminal failure, no retry/replay, exact terminal-result tests |
| T60-05 pipe/resource deadlock | High | Incremental async parsing, 256 KiB line cap, UTF-8/chunk matrix, and >200 KiB drain case |
| T60-09 fixture/provenance drift | High | Strict manifest and sanitization tests; real-capture status remains explicitly pending rather than fabricated |

No High/Critical parser or attestation failure may be deferred. Only real authenticated capture corroboration stays human-needed at milestone end.
</threat_model>

<verification>
- `npm --prefix mcp run build`
- `node tests/mcp-agent-provider-contract.test.js`
- `node tests/mcp-claude-code-adapter.test.js`
- `node tests/mcp-agent-stream-fixture.test.js`
</verification>

<success_criteria>
- Known 2.1.177 shapes normalize to strict provider-neutral events and unknown/malformed inputs fail loud without success/replay.
- Stream parsing is incremental, byte-boundary-safe, final-line-safe, and bounded per line while draining >200 KiB total.
- The concrete adapter implements exactly five methods and keeps process creation/lifecycle injected and outside adapter code.
- Fixture provenance and deferred real-capture status are explicit and truthful; the recorded-provenance portion of CLAUDE-03 is not claimed complete.
</success_criteria>

<output>
After completion, create `.planning/phases/60-adapter-contract-claude-code-mvp/60-02-SUMMARY.md`.
</output>
