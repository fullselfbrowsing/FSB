---
phase: 60-adapter-contract-claude-code-mvp
plan: "04"
type: execute
wave: 3
depends_on:
  - "60-02"
  - "60-03"
files_modified:
  - mcp/src/agent-providers/spawn-supervisor.ts
  - mcp/src/agent-providers/serve-delegation.ts
  - mcp/src/agent-providers/registry.ts
  - mcp/src/index.ts
  - tests/mcp-spawn-supervisor.test.js
  - tests/mcp-reverse-channel-contract.test.js
  - tests/mcp-bridge-topology.test.js
  - scripts/run-phase60-full-tests.mjs
  - package.json
  - .planning/phases/60-adapter-contract-claude-code-mvp/60-HUMAN-UAT.md
autonomous: true
requirements:
  - ADAPT-01
  - ADAPT-02
  - ADAPT-03
  - ADAPT-04
  - ADAPT-05
  - CLAUDE-01
  - CLAUDE-02
  - CLAUDE-03
  - CLAUDE-04
must_haves:
  truths:
    - "Only the intentionally started serve daemon owns a supervisor/agent-spawn handler; stdio and incidental bridge processes retain no spawn authority."
    - "delegate.start/cancel payloads are recursively strict, caller input cannot choose any control-plane value or delegation id, and the task reaches the child only through bounded stdin."
    - "Each start emits its server-minted delegation id before normalized progress, keeps the Phase 59 correlation open through one terminal result, and every exit/error/cancel/drift/shutdown race settles exactly once without replay."
    - "HTTP binds first, orphan recovery finishes second, and only then may the bridge connect/advertise agent-spawn; shutdown stops intake and verifies all trees before closing resources."
    - "All Phase 59 auth, exact-five transport errors, legacy serialization/version parity, and full repository tests remain green."
  artifacts:
    - path: "mcp/src/agent-providers/spawn-supervisor.ts"
      provides: "Strict delegation handler, fixed spawn/stdin/env/drain lifecycle, event fanout, cancel, recovery, and exact-once settlement"
      contains: "delegate.start"
    - path: "mcp/src/agent-providers/serve-delegation.ts"
      provides: "Testable HTTP -> recovery -> capable bridge startup and async shutdown orchestration"
      contains: "agent-spawn"
    - path: "tests/mcp-spawn-supervisor.test.js"
      provides: "Hostile payload, spawn capture, stream/backpressure, race, cancel, recovery, and shutdown matrix"
      contains: "ANTHROPIC_API_KEY"
    - path: "tests/mcp-reverse-channel-contract.test.js"
      provides: "Local/relay early-id events, long-lived terminal response, domain failure, and cancel contract"
      contains: "delegation.started"
    - path: ".planning/phases/60-adapter-contract-claude-code-mvp/60-HUMAN-UAT.md"
      provides: "Preserved milestone-end live checks with no fabricated pass"
      contains: "human_needed"
  key_links:
    - from: "mcp/src/agent-providers/spawn-supervisor.ts"
      to: "mcp/src/bridge.ts"
      via: "ExtRequestHandler emits validated ext:event frames while its promise remains pending until terminal settlement"
      pattern: "ExtRequestHandler|emit"
    - from: "mcp/src/agent-providers/spawn-supervisor.ts"
      to: "mcp/src/agent-providers/claude-code.ts"
      via: "closed registry lookup supplies declarative spawn data and normalized events; supervisor alone calls spawn"
      pattern: "registry.*require|buildSpawn|parseEvents"
    - from: "mcp/src/agent-providers/spawn-supervisor.ts"
      to: "mcp/src/agent-providers/runtime-files.ts"
      via: "private config/journal registration precedes stdin/task authority and cleanup follows verified tree settlement"
      pattern: "journal|runtime"
    - from: "mcp/src/agent-providers/serve-delegation.ts"
      to: "mcp/src/index.ts"
      via: "runHttpMode delegates ordered startup/shutdown only in serve mode"
      pattern: "runHttpMode|startServeDelegation"
---

<objective>
Wire the tested adapter and process substrate into a serve-only supervisor behind Phase 59's authenticated reverse channel, then close the phase with full automated/security evidence and an honest deferred-UAT ledger.

Purpose: Deliver the real daemon-side Claude delegation core without leaking spawn authority into stdio or pulling Phase 61 side-panel behavior forward.
Output: Supervisor, strict delegate handler, ordered serve lifecycle, reverse-channel harness, full regression wiring, and milestone-end live checklist.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/60-adapter-contract-claude-code-mvp/60-CONTEXT.md
@.planning/phases/60-adapter-contract-claude-code-mvp/60-AI-SPEC.md
@.planning/phases/60-adapter-contract-claude-code-mvp/60-RESEARCH.md
@.planning/phases/60-adapter-contract-claude-code-mvp/60-VALIDATION.md
@.planning/phases/59-reverse-request-channel-security-foundation/59-CONTEXT.md
@.planning/phases/59-reverse-request-channel-security-foundation/59-REVIEW.md
@.planning/phases/59-reverse-request-channel-security-foundation/59-VERIFICATION.md
@.planning/phases/60-adapter-contract-claude-code-mvp/60-02-SUMMARY.md
@.planning/phases/60-adapter-contract-claude-code-mvp/60-03-SUMMARY.md

<interfaces>
Only these payloads are accepted, recursively strict:

```ts
type DelegateStartPayload = { adapterId: 'claude-code'; task: string };
type DelegateCancelPayload = { delegationId: string };
```

`delegate.start` first emits `ext:event` event `delegation.started` with `{ delegationId, adapterId, profileVersion }`, then event `delegation.event` with one normalized `AgentEvent`, and resolves once with a domain payload `{ delegationId, status, terminal }`. Adapter-domain failures such as `agent_protocol_drift` stay in events/terminal payloads; the Phase 59 `ExtError` union remains exactly five values.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the strict exact-once SpawnSupervisor</name>
  <files>mcp/src/agent-providers/spawn-supervisor.ts, mcp/src/agent-providers/registry.ts, tests/mcp-spawn-supervisor.test.js</files>
  <read_first>
- .planning/phases/60-adapter-contract-claude-code-mvp/60-CONTEXT.md
- .planning/phases/60-adapter-contract-claude-code-mvp/60-AI-SPEC.md
- .planning/phases/60-adapter-contract-claude-code-mvp/60-RESEARCH.md
- mcp/src/types.ts
- mcp/src/ext-protocol.ts
- mcp/src/bridge.ts
- mcp/src/agent-providers/adapter.ts
- mcp/src/agent-providers/registry.ts
- mcp/src/agent-providers/claude-code.ts
- mcp/src/agent-providers/runtime-files.ts
- mcp/src/agent-providers/process-tree.ts
- tests/mcp-reverse-channel-contract.test.js
  </read_first>
  <action>
Create an injectable supervisor with one active-run map keyed by a cryptographically server-minted bounded delegation id and explicit `created -> spawning -> running -> stopping -> settled` states. Install the concrete Claude adapter into the closed production registry by binding Plan 02 detection/parser/profile and Plan 03 tree-kill dependencies. The supervisor is the only module that imports/calls `spawn`; adapters remain declarative.

Implement an `ExtRequestHandler` accepting only the two exact methods/payloads in `<interfaces>` using recursively `.strict()` Zod schemas. Reject unknown methods/keys, arrays/prototypes, noncanonical adapter ids, caller delegation ids, task not valid UTF-8 text, empty task, and task over 64 KiB before detection/files/spawn. No request may supply command, binary, argv, flags, environment, cwd, config/agent path, permission mode, profile, or identity.

For start, use a durable two-phase authority barrier. Detect, create the private MCP config/run state, build/freeze the spawn spec, scrub `ANTHROPIC_API_KEY`, `OPENAI_API_KEY`, and `GEMINI_API_KEY`, and mint a unique non-secret fingerprint. Before any spawn, durably write a `prepared` journal record containing that fingerprint plus retained binary/profile/argv signature. Then call `spawn(retainedPath, argv, { shell:false, detached:true, windowsHide:true, stdio:['pipe','pipe','pipe'], cwd, env })` with stdin held unwritten. Attach child error/close observers, bounded/redacted 64 KiB stderr drain, and async stdout parser immediately. Resolve the process start identity and durably replace prepared with `active` PID/group/start metadata. Only after that second fsync/rename succeeds may the supervisor emit `delegation.started`, write the task, or otherwise grant browser/task authority. If spawn or activation fails, close stdin, terminate/verify any child tree, retain the journal record until recovery evidence is safe, and fail without task delivery. A daemon crash before spawn leaves only a stale prepared record; between spawn and activation it leaves a prepared fingerprint for narrow recovery while stdin EOF gives the child no task; after activation it leaves a strongly identified active record. Never log/store the task or raw stdout/stderr.

All spawn error, stdin error, non-result exit, valid result, parser drift, cancellation, duplicate cancel, handler route loss, and daemon shutdown races enter one idempotent `settleOnce`. Protocol/config drift emits one typed domain diagnostic, requests verified tree termination, and returns non-success without replay. Start stays pending through validated result, child close, stream drain, and required cleanup. Cancel looks up only the server id, uses the adapter/tree helper, waits for exit/tree absence, settles once, and returns idempotent already-terminal status without signaling twice. `close()` stops new work, cancels all active runs concurrently, awaits every settlement/cleanup, and returns structured content-free counts.

Build a fake spawn/child/stream/env/files/registry/clock matrix for hostile payload canaries, exact options/argv/stdin, provider key scrub, backpressure, >200 KiB stdout plus noisy stderr, spawn/stdin/parser/exit orderings, duplicate terminal/cancel, grace escalation, journal failure, and shutdown. Assert no task/key substring enters args, env, journal, diagnostics, or emitted non-task payload.
  </action>
  <acceptance_criteria>
- `spawn` is imported/called only by supervisor/process helpers, always with exact `shell:false`, `detached:true`, `windowsHide:true`, and three pipes; task canary exists only in captured stdin bytes.
- The three provider API keys are absent while unrelated safe env values remain; no task/key/raw-output canary appears in journal, diagnostics, runtime filenames, events, or terminal payload.
- Every invalid/extra/nested payload rejects before detect/files/spawn, and adapter id/delegation id are never caller-forgeable.
- Ordering tests prove durable prepared journal before spawn, durable active journal after PID/start identity, and no event/task stdin before active commit; injected daemon loss at each transition leaves no unjournaled task-authorized child.
- Every race produces one terminal outcome, one cleanup path, zero replay, and cancel success only after child close plus verified tree absence.
- Stdout/stderr drain concurrently through >200 KiB/stdout and bounded 64 KiB/redacted stderr without deadlock.
- `npm --prefix mcp run build && node tests/mcp-spawn-supervisor.test.js` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix mcp run build &amp;&amp; node tests/mcp-spawn-supervisor.test.js</automated>
  </verify>
  <done>The supervisor is the sole spawn authority and enforces strict input, stdin/env/process, streaming, cancellation, and exact-once lifecycle contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Wire ordered serve-only startup, reverse events, cancellation, and shutdown</name>
  <files>mcp/src/agent-providers/serve-delegation.ts, mcp/src/index.ts, tests/mcp-reverse-channel-contract.test.js, tests/mcp-bridge-topology.test.js</files>
  <read_first>
- mcp/src/index.ts
- mcp/src/http.ts
- mcp/src/runtime.ts
- mcp/src/bridge.ts
- mcp/src/types.ts
- mcp/src/ext-protocol.ts
- mcp/src/agent-providers/spawn-supervisor.ts
- tests/mcp-reverse-channel-contract.test.js
- tests/mcp-bridge-topology.test.js
- tests/mcp-bridge-auth.test.js
- .planning/phases/59-reverse-request-channel-security-foundation/59-REVIEW.md
  </read_first>
  <action>
Create a testable serve lifecycle helper with injected HTTP/bridge/supervisor/process-exit dependencies. Configure a capable `WebSocketBridge` with an `ExtRequestHandler` closure but do not connect it yet; bind Streamable HTTP first to obtain the actual loopback endpoint; construct the supervisor with that endpoint; run journal recovery; assign the ready supervisor; only on `spawnAvailable:true` call `bridge.connect()` so `agent-spawn` can be advertised. On recovery ambiguity/unsettled failure, do not connect/advertise the capable bridge, close safe resources, and return/throw a typed startup failure without killing unrelated processes.

Update only `runHttpMode`/`serve` in `mcp/src/index.ts` to use this lifecycle. `runStdioServer`, doctor/setup/status/wait, and incidental `new WebSocketBridge()` sites must not construct a supervisor, handler, or capability. Preserve Phase 59 secret rotation/auth and inventory push ordering after capable bridge connection.

Route `delegate.start` through the existing local-first/capable-relay handler seam without changing `parseExtFrame`, `MCPMessageType`, the five `ExtError.code` values, auth, origin/host rules, relay default serialization, or route settlement. Extend local and relayed harnesses so the first event carries the server-minted delegation id, multiple normalized events remain routable while the start handler is pending, one terminal response follows, late events drop, cancel uses the minted id, and drift is a domain failed terminal payload rather than a new transport code.

Implement one idempotent async signal shutdown: stop accepting delegation requests; await supervisor close/tree settlement; close HTTP; disconnect bridge; then exit once. Repeated SIGTERM/SIGINT uses the same promise. Do not call `process.exit(0)` before cleanup resolves. Test exact startup order (`HTTP -> supervisor -> recovery -> bridge connect/advertise`) and shutdown order with fakes, including HTTP/bridge/recovery failure paths, stdio source/runtime no-spawn assertions, local vs capable relay routing, handler route loss, and no replay.
  </action>
  <acceptance_criteria>
- Serve startup test records HTTP bind before supervisor construction, recovery before bridge connect, and inventory push after connect; recovery ambiguity records zero capable connect/advertise calls.
- Stdio and every non-serve mode construct no `SpawnSupervisor`, provide no `handleExtRequest`, and serialize no `agent-spawn` capability.
- Local and relayed start emit one early server id, ordered events, and one terminal response; late/topology-lost routes settle/drop exactly once without replay.
- `EXT_ERROR_CODES` remains the exact five Phase 59 values and `agent_protocol_drift` appears only in domain event/terminal payloads.
- Repeated shutdown awaits supervisor/tree cleanup before HTTP close/bridge disconnect/process exit and exits once.
- `npm --prefix mcp run build && node tests/mcp-reverse-channel-contract.test.js && node tests/mcp-bridge-topology.test.js && node tests/mcp-bridge-auth.test.js && node tests/mcp-version-parity.test.js` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix mcp run build &amp;&amp; node tests/mcp-reverse-channel-contract.test.js &amp;&amp; node tests/mcp-bridge-topology.test.js &amp;&amp; node tests/mcp-bridge-auth.test.js &amp;&amp; node tests/mcp-version-parity.test.js</automated>
  </verify>
  <done>The authenticated reverse channel owns a serve-only, recovery-gated supervisor with long-lived events and verified async shutdown.</done>
</task>

<task type="auto">
  <name>Task 3: Add root regression gates and preserve all live checks for milestone end</name>
  <files>package.json, scripts/run-phase60-full-tests.mjs, .planning/phases/60-adapter-contract-claude-code-mvp/60-HUMAN-UAT.md, tests/mcp-agent-provider-contract.test.js, tests/mcp-claude-code-adapter.test.js, tests/mcp-agent-stream-fixture.test.js, tests/mcp-spawn-supervisor.test.js, tests/mcp-agent-orphan-recovery.test.js</files>
  <read_first>
- package.json
- mcp/package.json
- scripts/verify-agent-provider-flags.mjs
- .planning/phases/60-adapter-contract-claude-code-mvp/60-VALIDATION.md
- .planning/phases/59-reverse-request-channel-security-foundation/59-HUMAN-UAT.md
- tests/mcp-agent-provider-contract.test.js
- tests/mcp-claude-code-adapter.test.js
- tests/mcp-agent-stream-fixture.test.js
- tests/mcp-spawn-supervisor.test.js
- tests/mcp-agent-orphan-recovery.test.js
- tests/mcp-version-parity.test.js
  </read_first>
  <action>
Add each of the five new Phase 60 built-output test commands exactly once to root `package.json` immediately after the existing MCP build/security/bridge cluster, preserving every existing command. Keep `mcp` prebuild's permanent forbidden-flag scanner. Run the focused cluster, Phase 59 auth/topology/parity tests, then the full root suite.

Create `scripts/run-phase60-full-tests.mjs` as the exact fail-safe full-suite harness. It must run with no shell interpolation and: (1) capture `git status --short` and `git diff --cached --name-only` before mutation; (2) `lstat`-preflight that the compatibility link is absent as both file and symlink; (3) create only the missing Phase 39 directory and the exact symlink to the archived file; (4) run root `npm test` with inherited stdio; (5) in `finally`, verify the temporary path is still that symlink, unlink only it, remove the directory only if this harness created it and it is empty, and leave all other paths untouched; (6) capture status/staging again, require every pre-existing dirty-status line still exists afterward, require the staged-file list is byte-identical, and require the compatibility link is absent. Propagate the test/cleanup/preservation failure exit code. Never restore, reset, checkout, stage, or commit any path. In particular, preserve `.planning/config.json`, historical phase deletions, generated showcase files, and the pre-existing dirty `mcp/build/index.js` path.

Create `60-HUMAN-UAT.md` with status `human_needed` for: authenticated installed Claude 2.1.177 effective setting/tool/MCP isolation; real sanitized JSONL capture/corroboration; live CLI-to-loopback-MCP agent registration/tool call; POSIX and Windows descendant termination; crash/restart orphan cleanup without collateral kill; and browser ownership/vault/irreversible-action behavior. State these are deferred to the single milestone-end gate by user instruction, include executable steps/expected evidence, and leave every result unchecked/pending. Do not add Phase 61 production side-panel consent/feed/stop/persistence UAT as implemented Phase 60 behavior; cross-reference it as future scope only.
  </action>
  <acceptance_criteria>
- Root `test` contains each Phase 60 test exactly once and retains all prior commands; MCP prebuild still runs the forbidden-flag gate.
- Focused Phase 60 cluster, Phase 59 reverse/auth/topology/parity cluster, and full `npm test` exit 0.
- `node scripts/run-phase60-full-tests.mjs` runs the full suite, removes the link on both success and injected failure, preserves every pre-existing dirty-status entry, and leaves the staged-file list unchanged.
- Temporary Phase 39 link is absent and unstaged after the full run; no user-owned dirty/deleted/generated path is staged or restored.
- `60-HUMAN-UAT.md` lists every required live CLI/auth/fixture/OS/crash/browser check as unchecked `human_needed`, explicitly milestone-end deferred, with no fabricated pass.
- Source scan finds no Phase 61 production extension/background/side-panel implementation in Phase 60 commits.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix mcp run build &amp;&amp; node tests/mcp-agent-provider-contract.test.js &amp;&amp; node tests/mcp-claude-code-adapter.test.js &amp;&amp; node tests/mcp-agent-stream-fixture.test.js &amp;&amp; node tests/mcp-spawn-supervisor.test.js &amp;&amp; node tests/mcp-agent-orphan-recovery.test.js &amp;&amp; node tests/mcp-reverse-channel-contract.test.js &amp;&amp; node tests/mcp-bridge-topology.test.js &amp;&amp; node tests/mcp-bridge-auth.test.js &amp;&amp; node tests/mcp-version-parity.test.js &amp;&amp; node tests/agent-provider-forbidden-flags.test.js &amp;&amp; node scripts/run-phase60-full-tests.mjs</automated>
  </verify>
  <done>Phase 60 is root-suite gated and every live check remains honestly pending for the single final milestone UAT gate.</done>
</task>

</tasks>

<threat_model>

| Threat | Severity | Plan 04 mitigation and blocking evidence |
|--------|----------|------------------------------------------|
| T60-01 control-plane/shell injection | Critical | Recursively strict methods, fixed registry/spec/options, stdin-only canary, sole spawn import |
| T60-02 inherited tools/settings or auto-approval | Critical | Closed profile/static agent plus parser init attestation before browser-ready event |
| T60-03 binary/auth/secret confusion | High | Retained path, env scrub, private files, bounded redaction, leakage-canary matrix |
| T60-04 protocol drift/fabricated success | High | Typed domain failure, tree stop, no replay, exact-one terminal response |
| T60-05 pipe/resource deadlock | High | Listeners/drains before stdin, backpressure, bounded stderr, >200 KiB interleaving tests |
| T60-06 incomplete tree termination | Critical | Cancel/shutdown await close plus verified tree absence and share idempotent settlement |
| T60-07 orphan/collateral process handling | Critical | Recovery before connect, ambiguity withholds capability, unrelated process untouched |
| T60-08 confused-deputy/topology replay | High | Phase 59 auth/routing, server id, long-lived correlation, route-loss exact-once tests |
| T60-09 legacy wire/package regression | High | Exact-five codes, byte/version parity, root test chain, package/static-asset gates |

No High/Critical automated finding may be waived for UAT. Live checks corroborate real environments only at milestone end.
</threat_model>

<verification>
- All five Phase 60 focused built-output tests pass.
- Phase 59 reverse-channel, topology, auth, version parity, and forbidden-flag gates pass unchanged.
- Full root `npm test` passes with the temporary historical symlink removed afterward.
- Source/security review finds no shell fallback, caller flags/env/path/id, secret/task persistence, broad process kill, transport-code drift, replay, early capability, or Phase 61 production UI bleed.
- `60-HUMAN-UAT.md` remains entirely pending/human-needed.
</verification>

<success_criteria>
- Serve owns the only production supervisor and advertises capability only after orphan recovery.
- Strict start/cancel requests drive fixed shell-free spawn, bounded stdin-only task delivery, normalized progress, verified cancel, and one terminal response.
- Shutdown and crash-recovery paths do not leave accepted matching descendants or kill unrelated processes in automated state matrices.
- All automatable/source portions of the nine Phase 60 requirements have blocking evidence; CLAUDE-03 recorded provenance and the named live environment checks remain explicitly human_needed rather than claimed complete.
- All focused and full repository tests pass while unrelated user workspace state remains untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/60-adapter-contract-claude-code-mvp/60-04-SUMMARY.md`.
</output>
