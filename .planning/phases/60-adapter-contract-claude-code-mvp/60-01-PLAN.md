---
phase: 60-adapter-contract-claude-code-mvp
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - mcp/src/agent-providers/adapter.ts
  - mcp/src/agent-providers/registry.ts
  - mcp/src/agent-providers/claude-detect.ts
  - mcp/src/agent-providers/claude-profile.ts
  - mcp/ai/agents/fsb.json
  - tests/mcp-agent-provider-contract.test.js
  - tests/mcp-claude-code-adapter.test.js
autonomous: true
requirements:
  - ADAPT-01
  - ADAPT-02
  - CLAUDE-01
  - CLAUDE-02
  - CLAUDE-04
must_haves:
  truths:
    - "The provider boundary has exactly five methods, with declarative spawn data and no adapter-owned process lifecycle escape hatch."
    - "Claude detection binds one resolved native binary path to one parsed version/profile, and unavailable, old, changed, or unsafe Windows candidates fail closed."
    - "The 2.1.177 profile is a fixed MCP-only, stdin-task contract using the shipped static FSB agent and never a caller-built flag list or dynamic system prompt."
  artifacts:
    - path: "mcp/src/agent-providers/adapter.ts"
      provides: "Exact five-method interface and provider-neutral runtime types"
      contains: "interface AgentProviderAdapter"
    - path: "mcp/src/agent-providers/claude-profile.ts"
      provides: "Closed 2.1.177 compatibility profile and immutable argv construction"
      contains: "--strict-mcp-config"
    - path: "mcp/ai/agents/fsb.json"
      provides: "Reviewed static browser-control persona and tool policy"
      contains: "mcp__fsb"
    - path: "tests/mcp-claude-code-adapter.test.js"
      provides: "Retained-path, version, exact-argv, static-policy, and unsafe-shim evidence"
      contains: "2.1.177"
  key_links:
    - from: "mcp/src/agent-providers/claude-detect.ts"
      to: "mcp/src/agent-providers/claude-profile.ts"
      via: "the retained real path and parsed version select one closed profile"
      pattern: "binary.*version|version.*profile"
    - from: "mcp/src/agent-providers/claude-profile.ts"
      to: "mcp/ai/agents/fsb.json"
      via: "the profile serializes the shipped reviewed agent definition, never task text"
      pattern: "agents.*fsb|fsb.*agent"
    - from: "mcp/src/agent-providers/registry.ts"
      to: "mcp/src/platforms.ts"
      via: "the canonical provider id remains exactly claude-code"
      pattern: "claude-code"
---

<objective>
Freeze the provider-neutral contract and the Claude Code 2.1.177 compatibility policy before any process can be spawned.

Purpose: Make adapter breadth, exact-path detection, MCP-only argv, static agent policy, and version gating mechanically testable independently of lifecycle code.
Output: Contract/registry types, injected Claude detection, closed profile builder, shipped agent JSON, and focused built-output tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/60-adapter-contract-claude-code-mvp/60-CONTEXT.md
@.planning/phases/60-adapter-contract-claude-code-mvp/60-AI-SPEC.md
@.planning/phases/60-adapter-contract-claude-code-mvp/60-RESEARCH.md
@.planning/phases/60-adapter-contract-claude-code-mvp/60-VALIDATION.md
@.planning/phases/59-reverse-request-channel-security-foundation/59-REVIEW.md

<interfaces>
The public contract is exactly:

```ts
export interface AgentProviderAdapter {
  detect(): Promise<AdapterDetection>;
  buildSpawn(task: AgentTask, ctx: SpawnContext): Promise<SpawnSpec>;
  parseEvents(stream: NodeJS.ReadableStream): AsyncIterable<AgentEvent>;
  kill(child: SupervisedChild, options: { grace: number }): Promise<void>;
  caps(): AdapterCapabilities;
}
```

`SpawnSpec` is immutable data containing the retained command path, fixed argv, cwd/private-file references, and fixed non-secret environment additions. It contains no task text. `AdapterCapabilities` reports task mode true and chat/resume/server modes false.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the exact adapter contract and closed registry</name>
  <files>mcp/src/agent-providers/adapter.ts, mcp/src/agent-providers/registry.ts, tests/mcp-agent-provider-contract.test.js</files>
  <read_first>
- .planning/phases/60-adapter-contract-claude-code-mvp/60-CONTEXT.md
- .planning/phases/60-adapter-contract-claude-code-mvp/60-AI-SPEC.md
- .planning/REQUIREMENTS.md
- mcp/src/types.ts
- mcp/src/platforms.ts
- mcp/src/client-inventory.ts
- tests/mcp-client-inventory.test.js
  </read_first>
  <action>
Create `adapter.ts` with exported strict provider-neutral types and the exact five-method interface from `<interfaces>`. Define normalized `AgentEvent` as strict `{ type, sessionId, payload }`, bounded task/context types, `AdapterDetection` with `installed`, `version`, `authState`, and retained `binary`, frozen `SpawnSpec`, and honest capabilities. Do not add `start`, `stop`, `close`, `init`, `dispose`, `spawn`, or any hidden lifecycle callback.

Create `registry.ts` as a closed canonical-id map that accepts exactly one adapter for `claude-code`, rejects duplicate/unknown/missing/case-varied ids with typed adapter-domain errors, and never falls back. Keep the registry factory injectable so Plan 02 can install the concrete Claude adapter without global mutation.

Create a built-output Node test that reads the emitted exports and source, provides a five-method fake adapter, proves lookup only for exact `claude-code`, duplicate/unknown rejection, immutable capability/spawn shapes, and asserts the interface source has exactly the five required method signatures. It must also assert `PLATFORMS` retains the canonical `claude-code` id.
  </action>
  <acceptance_criteria>
- `mcp/src/agent-providers/adapter.ts` contains `detect(`, `buildSpawn(`, `parseEvents(`, `kill(`, and `caps(` exactly once in `AgentProviderAdapter`, and contains no sixth method signature.
- `AdapterCapabilities` expresses `taskMode: true`, `chatMode: false`, `resume: false`, and `serverMode: false` for the Phase 60 fake/Claude contract.
- Registry lookup accepts only lowercase `claude-code`; `Claude-Code`, an empty id, unknown id, and duplicate registration throw typed errors without fallback.
- `npm --prefix mcp run build && node tests/mcp-agent-provider-contract.test.js` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix mcp run build &amp;&amp; node tests/mcp-agent-provider-contract.test.js</automated>
  </verify>
  <done>The exact provider-neutral boundary and closed canonical registry are compile- and runtime-gated.</done>
</task>

<task type="auto">
  <name>Task 2: Implement retained-path detection, the static FSB policy, and the closed Claude profile</name>
  <files>mcp/src/agent-providers/claude-detect.ts, mcp/src/agent-providers/claude-profile.ts, mcp/ai/agents/fsb.json, tests/mcp-claude-code-adapter.test.js</files>
  <read_first>
- .planning/phases/60-adapter-contract-claude-code-mvp/60-CONTEXT.md
- .planning/phases/60-adapter-contract-claude-code-mvp/60-AI-SPEC.md
- .planning/phases/60-adapter-contract-claude-code-mvp/60-RESEARCH.md
- mcp/src/client-inventory.ts
- tests/mcp-client-inventory.test.js
- scripts/verify-agent-provider-flags.mjs
- mcp/package.json
- skills/fsb/SKILL.md
- skills/fsb/references/multi-agent-contract.md
- skills/fsb/references/vault-boundary.md
  </read_first>
  <action>
Implement injected asynchronous detection that resolves one native executable/real path, probes that same path with `['--version']` using `shell:false`, `windowsHide:true`, a three-second timeout, and 64 KiB output bound, parses semver, and selects the minimum 2.1.177 profile. Return `installed:false` plus a secret-free doctor diagnostic for missing, unparseable, below-minimum, changed, or unsafe candidates. Never infer authentication from files/env and never re-resolve PATH after detection. On Windows reject `.cmd`/`.bat` unless an injected deterministic resolver returns a verified fixed native interpreter/entry point; never use `cmd /c` or a shell.

Before building or testing profile serialization, create `mcp/ai/agents/fsb.json` as static product policy derived only from the three FSB skill references. It must define the `fsb` browser-control role, permit only `mcp__fsb`, accept server-minted identity/owned-tab rules, require vault references for credentials, require human handoff for irreversible/consent-required actions, deny Bash/Edit/Write/NotebookEdit/WebFetch/WebSearch, use `dontAsk`, and cap at 40 turns where supported. It contains no task placeholder, user data, credential, dynamic prompt input, or general shell/web authority.

Implement the closed profile builder with the exact ordered values from AI-SPEC: `-p`, `--verbose`, `--output-format stream-json`, `--include-partial-messages`, the version-gated isolation flags including `--setting-sources` with a literal empty following argument, `--disable-slash-commands`, `--no-chrome`, `--strict-mcp-config`, `--mcp-config <private path>`, `--agents <static compact JSON>`, `--agent fsb`, `--permission-mode dontAsk`, `--tools` with a literal empty following argument, `--allowedTools mcp__fsb`, `--disallowedTools Bash,Edit,Write,NotebookEdit,WebFetch,WebSearch`, `--max-turns 40`, and `--no-session-persistence`. The builder accepts only daemon-owned context values, freezes its result, and asserts the bounded user task does not occur in argv, env, paths, or diagnostics. Do not include `--model`, `--bare`, resume, any auto-approval flag, or API credentials.

Test retained path/version behavior, PATH-change resistance, minimum/unparseable cases, native Windows and rejected shim cases, exact argv order including both empty string values, fixed spawn metadata, task metacharacter canary absence, and provider-key absence. Treat effective 2.1.177 setting-source behavior as milestone-end UAT; automated tests prove only the fixed profile plus fail-closed init requirement.
  </action>
  <acceptance_criteria>
- Version `2.1.176` and unparseable output produce `installed:false`; `2.1.177` selects one closed profile using the same retained path for probe and future spawn.
- `mcp/ai/agents/fsb.json` exists and parses before profile tests run; it contains only static FSB MCP/ownership/vault/handoff policy and no task-derived value.
- Exact argv contains `--tools`, `''`, `--allowedTools`, `mcp__fsb`, `--max-turns`, `40`, and `--no-session-persistence` in order and contains no task canary.
- Source contains `shell: false` and no `shell: true`, `cmd /c`, string command construction, `--bare`, `--model`, or forbidden approval flag.
- `npm --prefix mcp run build && node tests/mcp-claude-code-adapter.test.js && node tests/agent-provider-forbidden-flags.test.js` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix mcp run build &amp;&amp; node tests/mcp-claude-code-adapter.test.js &amp;&amp; node tests/agent-provider-forbidden-flags.test.js</automated>
  </verify>
  <done>Claude version detection and argv policy are closed, retained-path, shell-free, stdin-task-ready, and source-gated.</done>
</task>

<task type="auto">
  <name>Task 3: Validate the shipped FSB delegated-agent and package contract</name>
  <files>mcp/ai/agents/fsb.json, tests/mcp-agent-provider-contract.test.js, tests/mcp-claude-code-adapter.test.js</files>
  <read_first>
- skills/fsb/SKILL.md
- skills/fsb/references/multi-agent-contract.md
- skills/fsb/references/vault-boundary.md
- .planning/phases/60-adapter-contract-claude-code-mvp/60-AI-SPEC.md
- mcp/package.json
- mcp/src/prompts/index.ts
- tests/skill-fsb-spec.test.js
  </read_first>
  <action>
Review the Task 2 static JSON against all three source skill references and extend tests to parse it, assert exact key/type bounds, required policy phrases/deny entries, absence of task-derived placeholders and credentials, exact serialization into the profile, and inclusion through `mcp/package.json`'s existing `ai/` published-file entry. Do not recreate or dynamically rewrite the agent at runtime. Do not run `npm pack` if it mutates the workspace; package inclusion may be proven from the manifest and a dry-run listing directed to temporary output.
  </action>
  <acceptance_criteria>
- `mcp/ai/agents/fsb.json` parses as JSON and contains the static name/description/prompt/tool/permission/turn policy expected by the profile.
- Tests find `mcp__fsb`, ownership, vault-reference, and irreversible-action handoff rules and find no dynamic task interpolation marker, provider secret, shell authority, or general web tool permission.
- `mcp/package.json` still includes `ai/` and the profile's `--agents` JSON is derived only from the shipped file.
- `npm --prefix mcp run build && node tests/mcp-agent-provider-contract.test.js && node tests/mcp-claude-code-adapter.test.js` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix mcp run build &amp;&amp; node tests/mcp-agent-provider-contract.test.js &amp;&amp; node tests/mcp-claude-code-adapter.test.js</automated>
  </verify>
  <done>The delegated Claude persona is reviewed static product policy, packaged with MCP, and never prompt-stuffed from user input.</done>
</task>

</tasks>

<threat_model>

| Threat | Severity | Plan 01 mitigation and blocking evidence |
|--------|----------|------------------------------------------|
| T60-01 control-plane/shell injection | Critical | Exact interface, closed registry, immutable fixed argv, task-canary absence, and no-shell source scan |
| T60-02 inherited tools/settings or auto-approval | Critical | Empty built-ins, strict MCP, static agent, explicit denies, setting-source isolation profile, and forbidden-flag gate |
| T60-03 binary/auth/secret confusion | High | Retained real path/version, minimum profile, unsafe-shim rejection, no auth inference, and provider-key absence |
| T60-09 package/policy drift | High | Static JSON schema/source assertions and published `ai/` manifest evidence |

No High/Critical result is deferrable. Only live effective-profile/auth corroboration remains human-needed at milestone end.
</threat_model>

<verification>
- `npm --prefix mcp run build`
- `node tests/mcp-agent-provider-contract.test.js`
- `node tests/mcp-claude-code-adapter.test.js`
- `node tests/agent-provider-forbidden-flags.test.js`
</verification>

<success_criteria>
- The adapter contract exposes exactly five methods and the registry accepts only canonical `claude-code`.
- Detection and spawn policy share one retained path/profile and reject unsafe/unsupported candidates without a shell.
- The exact MCP-only 2.1.177 argv and static shipped agent policy are test-pinned; task text appears only in the future stdin path.
- All focused build/contract/profile/security tests pass without a live CLI invocation.
</success_criteria>

<output>
After completion, create `.planning/phases/60-adapter-contract-claude-code-mvp/60-01-SUMMARY.md`.
</output>
