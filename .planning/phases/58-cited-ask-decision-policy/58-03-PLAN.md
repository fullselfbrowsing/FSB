---
phase: 58-cited-ask-decision-policy
plan: "03"
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - extension/background.js
  - extension/utils/skopeo-hud-schema.js
  - extension/utils/skopeo-hud-projector.js
  - tests/skopeo-hud-runtime.test.js
autonomous: true
requirements:
  - VIEW-06
  - VIEW-07
  - POLICY-01
  - POLICY-02
  - POLICY-03
must_haves:
  truths:
    - "D-02/D-03: Background resolves each opaque explicit scope to a fresh exact source set and independently authorizes every ask; follow-ups, old projections, and old actions carry no authority."
    - "D-04..D-08: Only a schema-valid locally adjudicated cited answer crosses to content, with separate governing/history and answer/clearance states."
    - "D-09..D-17: Stable policy writes, review/open/acknowledge, complex classification, memo proof, blocking, and existing memo navigation are current one-shot background actions; no Drive mutation or memo authoring exists."
  artifacts:
    - path: "extension/background.js"
      provides: "Private ask/policy facades, exact source-scope controller, fresh provider orchestration, and revocable action effects"
      contains: "skopeo:hud-ask"
    - path: "extension/utils/skopeo-hud-schema.js"
      provides: "Content-safe Phase 58 ask/result/policy projection bodies and action vocabulary"
      contains: "answered"
    - path: "extension/utils/skopeo-hud-projector.js"
      provides: "Pure current ask plus deterministic policy projection join"
      contains: "createProjection"
    - path: "tests/skopeo-hud-runtime.test.js"
      provides: "Exact background controller, action, race, privacy, and source/policy effect oracle"
      contains: "skopeo ask controller contract"
  key_links:
    - from: "extension/background.js"
      to: "extension/utils/skopeo-ask-engine.js"
      via: "fresh exact-set `query` operation supplies certified bounded excerpts and current truth proof"
      pattern: "runSkopeoCorpusOperation|FsbSkopeoAskEngine"
    - from: "extension/background.js"
      to: "extension/utils/skopeo-decision-policy.js"
      via: "current partition config, policy proof, controller acknowledgement, and decision digest produce clearance"
      pattern: "FsbSkopeoDecisionPolicy"
    - from: "extension/background.js"
      to: "extension/utils/skopeo-hud-projector.js"
      via: "locally adjudicated answer and policy result are joined then reparsed before publication"
      pattern: "FsbSkopeoHudProjector"
    - from: "extension/background.js"
      to: "runSkopeoCorpusOperation('citation-open')"
      via: "existing answer citation, Document 10 review, and memo navigation reuse fresh one-shot source authorization"
      pattern: "citation-open"
---

<objective>
Wire cited asks and decision safeguards into the existing trusted background HUD controller without widening content authority.

Purpose: Turn the pure schema/engine/policy modules into fresh exact-set operations and replay-safe effects bound to the current Skopeo lifecycle.
Output: Extended HUD projection schema/projector, private background orchestration, one-shot action registry, and integration/security tests.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/58-cited-ask-decision-policy/58-CONTEXT.md
@.planning/phases/58-cited-ask-decision-policy/58-RESEARCH.md
@.planning/phases/58-cited-ask-decision-policy/58-PATTERNS.md
@.planning/phases/58-cited-ask-decision-policy/58-UI-SPEC.md
@.planning/phases/58-cited-ask-decision-policy/58-VALIDATION.md
@extension/utils/skopeo-ask-schema.js
@extension/utils/skopeo-ask-engine.js
@extension/utils/skopeo-decision-policy-store.js
@extension/utils/skopeo-decision-policy.js
@extension/utils/skopeo-hud-schema.js
@extension/utils/skopeo-hud-projector.js
@extension/background.js
@tests/skopeo-hud-runtime.test.js

<interfaces>
Load Phase 58 utility modules after corpus/graph/truth dependencies and before HUD controller construction. Created store/ask/policy facades remain lexical background state.

Extend the HUD projection schema with content-safe `ask` and `answer` modes. The exact authority envelope stays generation/origin/profile/context/entity/request/projection-token bound. The body contains only safe question/scope labels, closed answer/policy states, bounded cited evidence metadata, explicit overflow, and opaque action IDs—never raw source text/provider response/URL/file/account/corpus/revision/storage/graph/truth identity.

Add exact-key controller messages `skopeo:hud-ask`, `skopeo:hud-ask-cancel`, `skopeo:hud-answer-action`, and `skopeo:hud-answer-action-confirm`. Caller chooses no source ID, URL, decision kind, policy identity, complex identity, memo identity, or effect kind; opaque projection-owned action IDs resolve all of those in background.
</interfaces>
</context>

<threat_model>
| Threat | Severity | Mitigation |
|---|---|---|
| T58-01/02 wrong/cross-vendor scope or prefix | High | Opaque scope registry resolves current exact set; `query` operation and completeness/caps authorize engine input. |
| T58-03 replay/fake citation | High | Controller-owned opaque actions, exact current tuple/projection, fresh `citation-open`, one-shot consumption. |
| T58-04 stale provider/controller repaint | High | Per-controller ask epoch/AbortController, pre/post await binding checks, revoke-before-replace. |
| T58-05 model policy authority | High | Background supplies decision kind/policy inputs after answer; projection parser rejects provider policy fields. |
| T58-06/07 identity/action spoofing | High | Stable identities rederived from current binding immediately before local write/open/ack/confirm. |
| T58-08 false memo gap | High | Current explicit complex flag plus complete graph/source proof required. |
| T58-09 content/MCP/log leak | High | Exact minimized projection/messages and static/private-facade assertions. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the HUD runtime harness with a controlled RED controller contract</name>
  <files>tests/skopeo-hud-runtime.test.js</files>
  <read_first>
    - tests/skopeo-hud-runtime.test.js
    - extension/background.js
    - extension/utils/skopeo-hud-schema.js
    - extension/utils/skopeo-hud-projector.js
    - tests/skopeo-ask-engine.test.js
    - tests/skopeo-decision-policy.test.js
    - .planning/phases/58-cited-ask-decision-policy/58-UI-SPEC.md
  </read_first>
  <behavior>
    - Exact current opaque scope becomes the only exact source set passed to one query/provider operation.
    - Late, cancelled, stale, cross-tab, cross-scope, revision/access/provider-drift responses publish nothing.
    - Published answer projection contains no raw authority and retains separate answer outcome and policy clearance.
    - Source, configuration, review, acknowledgement, classification, and removal actions are current/replay-safe and revalidated.
    - Missing/inaccessible Document 10 or required memo blocks clearance but preserves cited informational evidence.
  </behavior>
  <action>
Extend the existing VM/background and content harnesses without weakening Phase 57 markers. Add injected fake ask/policy/store/transport/provider dependencies; exact request/action/confirm matrices; two tabs, vendors, scopes, revisions, access states, and provider completions; and source-level privacy assertions.

Cover exact scope derivation, agreement/vendor/corpus isolation, query certificate set, excerpt/citation join, answer projection keys, cancellation, request replacement, navigation/ABA, account/corpus/source/revision/access/provider drift, late completion, action replay/cross-tab/cross-projection, Document 10 configure/replace/clear/review/open/acknowledge, complex classify/remove, memo on-file/missing/inaccessible/incomplete, routine omission, and no memo authoring/Drive writes. Add `SKOPEO_ASK_EXPECT_CONTROLLER_RED=1` with marker `skopeo ask controller contract: RED`.
  </action>
  <acceptance_criteria>
    - Controlled RED succeeds only before Phase 58 seams exist and preserves all Phase 57 runtime assertions.
    - Every new message/action exact-key shape and every currentness digest is asserted.
    - Content-visible response keys are allowlisted and raw authority/provider/policy-store values are absent.
    - Test syntax and `git diff --check` pass.
  </acceptance_criteria>
  <verify>
    <automated>controller_red=$(SKOPEO_ASK_EXPECT_CONTROLLER_RED=1 node tests/skopeo-hud-runtime.test.js 2&gt;&amp;1); controller_status=$?; test "$controller_status" -eq 0 &amp;&amp; test "$(printf '%s\n' "$controller_red" | grep -Fxc 'skopeo ask controller contract: RED')" -eq 1 &amp;&amp; node --check tests/skopeo-hud-runtime.test.js</automated>
  </verify>
  <done>The runtime harness fixes exact scope, publication, action, policy, race, and privacy contracts before controller implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement current ask orchestration and minimized HUD projection</name>
  <files>extension/background.js, extension/utils/skopeo-hud-schema.js, extension/utils/skopeo-hud-projector.js, tests/skopeo-hud-runtime.test.js</files>
  <read_first>
    - tests/skopeo-hud-runtime.test.js
    - extension/background.js
    - extension/utils/skopeo-hud-schema.js
    - extension/utils/skopeo-hud-projector.js
    - extension/utils/skopeo-ask-engine.js
    - extension/utils/skopeo-decision-policy.js
    - .planning/phases/58-cited-ask-decision-policy/58-PATTERNS.md
  </read_first>
  <action>
Extend Phase 57 schema/projector using their exact in-place analogs. Add ask/answer bodies, caps, safe closed states, action uniqueness, answer/clearance separation, policy blocker/memo omission invariants, and final schema reparse. Keep projector pure and injected.

In `extension/background.js`, import new modules dependency-first, construct private facades, extend the existing HUD controller state with scope/action registries and one current ask AbortController/epoch, and handle exact ask/cancel messages. Resolve scope only from current projection-owned opaque tokens. Under `runSkopeoCorpusOperation('query')`, certify the exact set, read bounded excerpts through existing transport, obtain current truth/graph/citations, invoke the ask engine, recheck binding/digests after every await, evaluate policy locally, build/parse the minimized projection, then publish. Revoke/discard before replacement and on every failure/drift.
  </action>
  <acceptance_criteria>
    - Agreement/vendor/corpus scope never expands or crosses vendor boundaries and content supplies no stable identity/source list.
    - Exactly one current ask operation exists; cancel/replacement/navigation/drift prevents every late publication.
    - Projection parses, is frozen, contains only minimized safe fields/actions, and keeps answer outcome separate from policy clearance.
    - Existing Phase 57 folder/reading/citation tests remain green.
  </acceptance_criteria>
  <verify>
    <automated>node --check extension/background.js &amp;&amp; node --check extension/utils/skopeo-hud-schema.js &amp;&amp; node --check extension/utils/skopeo-hud-projector.js &amp;&amp; node tests/skopeo-hud-schema.test.js &amp;&amp; node tests/skopeo-hud-projector.test.js &amp;&amp; node tests/skopeo-hud-runtime.test.js</automated>
  </verify>
  <done>Fresh exact-set asks produce only current minimized cited/policy projections and stale work has zero effect.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Complete one-shot source and policy effects with deterministic clearance refresh</name>
  <files>extension/background.js, tests/skopeo-hud-runtime.test.js</files>
  <read_first>
    - extension/background.js
    - tests/skopeo-hud-runtime.test.js
    - extension/utils/skopeo-decision-policy-store.js
    - extension/utils/skopeo-decision-policy.js
    - extension/utils/skopeo-consequence-gate.js
    - .planning/phases/58-cited-ask-decision-policy/58-CONTEXT.md
  </read_first>
  <action>
Extend the existing projection-local action registry with typed private bindings while keeping content action IDs opaque. Citation, Document 10 review, and existing memo navigation reuse the fresh `citation-open` authorization sandwich. Review open only enables acknowledgement for the exact current decision/document revision. Acknowledge rechecks every digest then stores controller-local state and rebuilds the projection.

Document 10 configure/replace/clear and agreement complex/routine changes return a bounded consequence model first, then accept one exact confirmation token. Immediately before commit, rederive the current stable file/agreement identity and partition, recheck access/revision/controller authority, perform the local store mutation, revoke old actions/acknowledgement, and rebuild fresh policy state. No operation mutates Drive or authors a memo. Missing/inaccessible/stale policy evidence always blocks while preserving the cited answer.
  </action>
  <acceptance_criteria>
    - Source opens and local policy effects reject replay, cross-tab/projection, revision, access, account, corpus, scope, and decision drift.
    - Acknowledgement is impossible before current review open and disappears on every documented drift.
    - Configuration/classification requires explicit consequence confirmation and persists only rederived stable identity.
    - Routine memo status is omitted and no memo authoring/source mutation path or message exists.
  </acceptance_criteria>
  <verify>
    <automated>node --check extension/background.js &amp;&amp; node tests/skopeo-hud-runtime.test.js &amp;&amp; node tests/skopeo-decision-policy.test.js &amp;&amp; node tests/skopeo-corpus-runtime.test.js &amp;&amp; node scripts/verify-skopeo-storage-boundary.mjs</automated>
  </verify>
  <done>All current source/policy actions are one-shot, freshly authorized, drift-revoked, and unable to create false clearance or source mutation.</done>
</task>

</tasks>

<verification>
- Focused gate: schema/projector/runtime/policy tests in Task 3.
- Regression gate: `npm run test:skopeo-truth-evals &amp;&amp; npm run test:skopeo-hud-evals &amp;&amp; node tests/skopeo-corpus-runtime.test.js &amp;&amp; node scripts/verify-skopeo-storage-boundary.mjs`.
- Static audit: no new manifest permission, MCP/content store surface, raw identity/URL/provider projection, wildcard message, Drive mutation, or memo-authoring action.
</verification>

<success_criteria>
- All Phase 58 requirements and D-02..D-17 are wired through current background authority.
- Provider and policy work cannot outlive the exact current projection/controller.
- Source and policy effects are explicit, one-shot, revalidated, and minimized.
</success_criteria>

<output>
Create `.planning/phases/58-cited-ask-decision-policy/58-03-SUMMARY.md` when done.
</output>
