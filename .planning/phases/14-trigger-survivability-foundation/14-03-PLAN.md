---
phase: 14-trigger-survivability-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 14-01-trigger-survivability-foundation
  - 14-02-trigger-survivability-foundation
files_modified:
  - extension/background.js
autonomous: false
requirements:
  - SURV-01
  - SURV-03
  - LIFE-05
user_setup: []

must_haves:
  truths:
    - "trigger-store.js and trigger-lifecycle.js are importScripts-loaded in background.js in the same region as their templates, with the store loaded BEFORE the lifecycle (lifecycle calls FsbTriggerStore) (D-07 / glue point 0)."
    - "On SW startup/bootstrap, restoreTriggersFromStorage() is called beside restoreVisualSessionLifecyclesFromStorage, guarded by typeof FsbTriggerLifecycle checks and a non-blocking .catch (SURV-03 / D-07.2)."
    - "The single chrome.alarms.onAlarm listener fans out to handleTriggerAlarm when alarm.name.startsWith(FsbTriggerLifecycle.TRIGGER_ALARM_PREFIX), as a new additive branch with an early return (SURV-01 / D-07.1 / D-03)."
    - "A new additive chrome.tabs.onRemoved listener calls handleTriggerTabRemoved(tabId), guarded and non-blocking (LIFE-05 / D-07.3 / D-10c)."
    - "All four edits are ADDITIVE — no existing branch/listener is modified; each new branch mirrors its verified visual-lifecycle sibling and ends in a non-blocking .catch (never throws out of SW glue)."
    - "INV-04 HARD: extension/ai/agent-loop.js is byte-untouched — its setTimeout-chained iterator (lines 2003/2702/2771) is unchanged and grep -c setTimeout still returns 8."
  artifacts:
    - path: "extension/background.js"
      provides: "Four additive trigger glue points: importScripts (store+lifecycle), bootstrap restore call, onAlarm fsbTrigger: branch, tabs.onRemoved trigger listener"
      contains: "trigger-lifecycle.js"
  key_links:
    - from: "background.js importScripts region"
      to: "utils/trigger-store.js + utils/trigger-lifecycle.js"
      via: "importScripts, store BEFORE lifecycle"
      pattern: "importScripts\\('utils/trigger-(store|lifecycle)\\.js'\\)"
    - from: "background.js bootstrap (beside restoreVisualSessionLifecyclesFromStorage)"
      to: "FsbTriggerLifecycle.restoreTriggersFromStorage"
      via: "guarded call + non-blocking .catch"
      pattern: "FsbTriggerLifecycle\\.restoreTriggersFromStorage"
    - from: "background.js chrome.alarms.onAlarm listener"
      to: "FsbTriggerLifecycle.handleTriggerAlarm"
      via: "alarm.name.startsWith(TRIGGER_ALARM_PREFIX) branch with early return"
      pattern: "FsbTriggerLifecycle\\.handleTriggerAlarm"
    - from: "background.js chrome.tabs.onRemoved (new sibling listener)"
      to: "FsbTriggerLifecycle.handleTriggerTabRemoved"
      via: "guarded Promise.resolve(...).catch"
      pattern: "FsbTriggerLifecycle\\.handleTriggerTabRemoved"
---

<objective>
Wire the two new trigger modules into the service worker at EXACTLY four additive glue points in `extension/background.js` (D-07), each mirroring its verified visual-lifecycle sibling: (0) the `importScripts` region — load `trigger-store.js` then `trigger-lifecycle.js`; (1) the bootstrap restore — call `restoreTriggersFromStorage()` beside `restoreVisualSessionLifecyclesFromStorage`; (2) the `chrome.alarms.onAlarm` listener — a new `startsWith('fsbTrigger:')` branch with an early `return`; (3) a new `chrome.tabs.onRemoved` sibling listener calling `handleTriggerTabRemoved(tabId)`.

Purpose: this is the wiring that makes SURV-01 (alarm tick wakes the SW and routes to `handleTriggerAlarm`), SURV-03 (cold-boot bootstrap runs the reconcile + orphan sweep), and LIFE-05 (tab-close reap) actually live in the running service worker. The modules and their logic shipped in Plans 14-01/14-02; this plan is pure additive glue.

Output: a modified `extension/background.js`. NO existing branch or listener is edited. INV-04 is a HARD gate: `extension/ai/agent-loop.js` is byte-untouched. The live-Chrome SW-eviction survival run is the documented manual-only verification (the Node-mock proves the logic deterministically).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-trigger-survivability-foundation/14-CONTEXT.md
@.planning/phases/14-trigger-survivability-foundation/14-PATTERNS.md
@.planning/phases/14-trigger-survivability-foundation/14-VALIDATION.md
@extension/utils/trigger-store.js
@extension/utils/trigger-lifecycle.js

<interfaces>
<!-- The four insertion points were verified at these line numbers on branch automation-worktree this
     session. They may have shifted slightly if background.js changed — the executor MUST anchor on the
     adjacent visual-lifecycle sibling TEXT, not on a stale line number, and confirm with grep. -->

Glue point 0 — importScripts region (verified ~:22-34). The visual lifecycle loads at :23 (`importScripts('utils/mcp-visual-session-lifecycle.js')`); the task store at :34 (`try { importScripts('utils/mcp-task-store.js'); } catch ...`). ADD trigger-store.js then trigger-lifecycle.js (store FIRST — lifecycle calls FsbTriggerStore), each in the `try { importScripts(...); } catch (e) { console.error('[FSB] Failed to load <file>:', e.message); }` form.

Glue point 1 — bootstrap restore (verified ~:2485-2498). Inside the bootstrap async fn, right after `await restorePersistedMcpVisualSessions();` the visual restore sits as:
  `if (typeof MCPVisualSessionLifecycleUtils !== 'undefined' && typeof MCPVisualSessionLifecycleUtils.restoreVisualSessionLifecyclesFromStorage === 'function') { MCPVisualSessionLifecycleUtils.restoreVisualSessionLifecyclesFromStorage().catch((err)=>console.warn(...)); }`
ADD the trigger equivalent beside it (same guard-then-call-then-non-blocking-catch shape).

Glue point 2 — onAlarm prefix branch (verified ~:13284-13301). The single `chrome.alarms.onAlarm.addListener(async (alarm) => { ... })` fans out by `alarm.name.startsWith(<PREFIX>)` with an early `return` per concern; the `MCP_VISUAL_LIFECYCLE_ALARM_PREFIX` branch at :13291-13301 is the exact template. ADD a new branch (place beside the visual branch): `if (typeof FsbTriggerLifecycle !== 'undefined' && alarm && typeof alarm.name === 'string' && alarm.name.startsWith(FsbTriggerLifecycle.TRIGGER_ALARM_PREFIX)) { try { await FsbTriggerLifecycle.handleTriggerAlarm(alarm); } catch (err) { console.warn('[FSB TRG] handleTriggerAlarm failed (non-blocking):', err && err.message); } return; }`

Glue point 3 — new tabs.onRemoved listener (verified ~:13169-13176). FSB registers MULTIPLE independent `chrome.tabs.onRemoved` listeners per concern (siblings at :2691, :2753, :13152, :13169). The visual one at :13169 is the template. ADD a NEW sibling listener (do not fold into an existing one): `chrome.tabs.onRemoved.addListener((tabId) => { if (typeof FsbTriggerLifecycle === 'undefined') return; if (typeof FsbTriggerLifecycle.handleTriggerTabRemoved !== 'function') return; Promise.resolve(FsbTriggerLifecycle.handleTriggerTabRemoved(tabId)).catch((err) => console.warn('[FSB TRG] handleTriggerTabRemoved failed (non-blocking):', err && err.message)); });`

Global names FsbTriggerStore / FsbTriggerLifecycle MUST match what Plans 14-01/14-02 exported via `global.X = exportsObj`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the four additive trigger glue points to background.js</name>
  <files>extension/background.js</files>
  <read_first>
    - extension/background.js importScripts region (~:22-34: visual-lifecycle at :23, task-store at :34) — confirm current line numbers with `grep -n "importScripts" extension/background.js`
    - extension/background.js bootstrap restore (~:2485-2498: `restorePersistedMcpVisualSessions` then the visual restore block) — confirm with `grep -n "restoreVisualSessionLifecyclesFromStorage" extension/background.js`
    - extension/background.js onAlarm listener (~:13284-13301: the `MCP_VISUAL_LIFECYCLE_ALARM_PREFIX` branch) — confirm with `grep -n "chrome.alarms.onAlarm.addListener\|MCP_VISUAL_LIFECYCLE_ALARM_PREFIX" extension/background.js`
    - extension/background.js tabs.onRemoved (~:13169-13176: the visual `handleVisualSessionLifecycleTabRemoved` listener) — confirm with `grep -n "handleVisualSessionLifecycleTabRemoved" extension/background.js`
    - 14-PATTERNS.md "extension/background.js" section — the verbatim sibling excerpts and the exact ADD blocks for all four points
    - 14-CONTEXT.md D-07 (the three glue points + the importScripts ordering) and the INV-04 guard
    - extension/utils/trigger-store.js + extension/utils/trigger-lifecycle.js — confirm the exported global names (FsbTriggerStore / FsbTriggerLifecycle) and the export keys (TRIGGER_ALARM_PREFIX, handleTriggerAlarm, restoreTriggersFromStorage, handleTriggerTabRemoved) the glue references
  </read_first>
  <action>
    Make four ADDITIVE edits to `extension/background.js` using the Edit tool, anchoring each on the adjacent visual-lifecycle sibling TEXT (not on a stale line number — re-confirm with the grep commands in read_first first). Do NOT modify, reorder, or wrap any existing branch, listener, or importScripts line.

      (0) importScripts region: directly after the existing `try { importScripts('utils/mcp-task-store.js'); } catch ... ` line, add two lines — `try { importScripts('utils/trigger-store.js'); } catch (e) { console.error('[FSB] Failed to load trigger-store.js:', e.message); }` THEN `try { importScripts('utils/trigger-lifecycle.js'); } catch (e) { console.error('[FSB] Failed to load trigger-lifecycle.js:', e.message); }`. Store MUST come before lifecycle (the lifecycle module calls FsbTriggerStore at load/runtime).

      (1) Bootstrap restore: directly after the visual `MCPVisualSessionLifecycleUtils.restoreVisualSessionLifecyclesFromStorage()` guarded block, add the trigger equivalent — `if (typeof FsbTriggerLifecycle !== 'undefined' && typeof FsbTriggerLifecycle.restoreTriggersFromStorage === 'function') { FsbTriggerLifecycle.restoreTriggersFromStorage().catch((err) => console.warn('[FSB TRG] restoreTriggersFromStorage failed (non-blocking):', err && err.message)); }`.

      (2) onAlarm branch: inside the existing `chrome.alarms.onAlarm.addListener(async (alarm) => { ... })`, beside the `MCP_VISUAL_LIFECYCLE_ALARM_PREFIX` branch, add a new branch with an early `return` — `if (typeof FsbTriggerLifecycle !== 'undefined' && alarm && typeof alarm.name === 'string' && alarm.name.startsWith(FsbTriggerLifecycle.TRIGGER_ALARM_PREFIX)) { try { await FsbTriggerLifecycle.handleTriggerAlarm(alarm); } catch (err) { console.warn('[FSB TRG] handleTriggerAlarm failed (non-blocking):', err && err.message); } return; }`. The early `return` is required so the fan-out stops at the matched concern (mirroring the visual branch).

      (3) New tabs.onRemoved listener: add a NEW `chrome.tabs.onRemoved.addListener((tabId) => { ... })` sibling beside the visual one — `if (typeof FsbTriggerLifecycle === 'undefined') return; if (typeof FsbTriggerLifecycle.handleTriggerTabRemoved !== 'function') return; Promise.resolve(FsbTriggerLifecycle.handleTriggerTabRemoved(tabId)).catch((err) => console.warn('[FSB TRG] handleTriggerTabRemoved failed (non-blocking):', err && err.message));`. Register it as an independent listener (FSB already uses multiple per-concern onRemoved listeners); do NOT merge into an existing listener body.

    Do NOT touch `extension/ai/agent-loop.js`. Do NOT add any branch logic beyond routing to the FsbTriggerLifecycle functions (no fire-condition logic, no overlay code — those are Phases 15/16).
  </action>
  <acceptance_criteria>
    - SOURCE (glue 0, ordering): `grep -n "importScripts('utils/trigger-store.js')" extension/background.js` and `grep -n "importScripts('utils/trigger-lifecycle.js')" extension/background.js` both match, and the store line number is LESS THAN the lifecycle line number. Verify ordering: `node -e "const s=require('fs').readFileSync('extension/background.js','utf8'); const a=s.indexOf(\"importScripts('utils/trigger-store.js')\"); const b=s.indexOf(\"importScripts('utils/trigger-lifecycle.js')\"); process.exit(a>0 && b>0 && a<b ? 0 : 1)"` exits 0.
    - SOURCE (glue 1): `grep -c "FsbTriggerLifecycle.restoreTriggersFromStorage" extension/background.js` returns 1.
    - SOURCE (glue 2): `grep -c "FsbTriggerLifecycle.handleTriggerAlarm" extension/background.js` returns 1 and `grep -c "FsbTriggerLifecycle.TRIGGER_ALARM_PREFIX" extension/background.js` returns 1.
    - SOURCE (glue 3): `grep -c "FsbTriggerLifecycle.handleTriggerTabRemoved" extension/background.js` returns 1.
    - SOURCE (additive, no existing branch broken): `grep -c "MCPVisualSessionLifecycleUtils.restoreVisualSessionLifecyclesFromStorage\|MCPVisualSessionLifecycleUtils.handleVisualSessionLifecycleAlarm\|MCPVisualSessionLifecycleUtils.handleVisualSessionLifecycleTabRemoved" extension/background.js` is unchanged from before the edit (each visual sibling still present exactly once).
    - SOURCE (INV-04 HARD): `git diff --name-only` does NOT list `extension/ai/agent-loop.js`; `grep -c "setTimeout" extension/ai/agent-loop.js` returns 8 (iterator byte-frozen at 2003/2702/2771).
    - BEHAVIOR (SW parses / no syntax break): `node --check extension/background.js` exits 0.
    - BEHAVIOR (no regression): `npm test` exits 0 (full chain, including the two trigger test files from Plans 14-01/14-02, green).
    - Automated command: `node --check extension/background.js && npm test`
  </acceptance_criteria>
  <verify>
    <automated>node --check extension/background.js && grep -c "setTimeout" extension/ai/agent-loop.js && npm test</automated>
  </verify>
  <done>
    - All four additive glue points are present in background.js, each referencing the correct FsbTriggerLifecycle export and ending in a non-blocking guard/catch.
    - trigger-store.js is imported before trigger-lifecycle.js.
    - No existing branch/listener was modified; the visual-lifecycle siblings are intact.
    - INV-04 holds: agent-loop.js untouched, `grep -c setTimeout` returns 8.
    - `node --check extension/background.js` and `npm test` both pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live Chrome MV3 SW-eviction survival check (manual-only per VALIDATION.md)</name>
  <action>Human-verify checkpoint (no code change). After Task 1 automated gates pass, perform the live-Chrome survival verification in how-to-verify below, OR explicitly defer it to milestone-end Chrome MV3 UAT and record it in STATE.md Deferred Items (matching the v0.10.0 UAT-debt pattern). The Node-mock tests already prove the logic deterministically; this covers only the real MV3 eviction+alarm-wake that a browser-less test cannot.</action>
  <verify><human-check>Live SW-eviction survival confirmed in an unpacked Chrome extension, or explicitly deferred to milestone-end UAT and recorded in STATE.md.</human-check></verify>
  <done>Live survival confirmed (no load error, bootstrap restore ran clean, alarm wakes SW + re-hydrates, tab-close reaps) OR deferral recorded in STATE.md Deferred Items.</done>
  <what-built>
    The four additive trigger glue points are wired into background.js: trigger-store.js + trigger-lifecycle.js are importScripts-loaded (store first), restoreTriggersFromStorage() runs on bootstrap, the onAlarm listener routes fsbTrigger:* alarms to handleTriggerAlarm, and a new tabs.onRemoved listener routes to handleTriggerTabRemoved. All logic is unit-tested deterministically in Node-mock (Plans 14-01/14-02). This checkpoint covers the ONE behavior the Node-mock cannot: a real MV3 service-worker eviction + alarm-wake in an unpacked Chrome extension (per 14-VALIDATION.md "Manual-Only Verifications"). This is consistent with FSB's milestone-end Chrome MV3 UAT pattern; it is acceptable to defer this to milestone-end UAT and record it as a known deferred verification rather than block the phase.
  </what-built>
  <how-to-verify>
    1. Load the unpacked extension in Chrome (chrome://extensions → Developer mode → Load unpacked → select `extension/`).
    2. Open the service worker console and confirm no `[FSB] Failed to load trigger-store.js` / `trigger-lifecycle.js` errors, and that the SW started cleanly (the bootstrap `restoreTriggersFromStorage` ran without throwing — on a fresh profile it should be a no-op returning `{ok:true, restored:0, reaped:0, dropped:0, orphans_cleared:0}`).
    3. (Survival smoke — optional until a Phase-15 arm path exists) In the SW console, manually seed an armed snapshot + alarm via `FsbTriggerLifecycle.armTrigger?.(...)` or `FsbTriggerStore.writeSnapshot(...)` + `chrome.alarms.create('fsbTrigger:test', {when: Date.now()+ <a few minutes>})`, then in chrome://serviceworker-internals stop the SW (or wait >30s idle with devtools closed). Confirm the alarm wakes the SW, `restoreTriggersFromStorage` re-hydrates the registry, and the trigger remains `armed` with no duplicate fire and no orphan alarm.
    4. Close a tab that a seeded trigger is bound to (`target_tab_id`); confirm `handleTriggerTabRemoved` reaped its entry + alarm.
    Note: full end-to-end arming is Phase 15; for Phase 14 the storage/alarm primitives can be exercised directly from the SW console. If deferring to milestone-end UAT, record it in STATE.md Deferred Items (matching the v0.10.0 Chrome MV3 UAT-debt pattern) and approve.
  </how-to-verify>
  <resume-signal>Type "approved" (live survival confirmed OR explicitly deferred to milestone-end Chrome MV3 UAT and recorded in STATE.md), or describe any load error / failed re-hydration / duplicate fire / orphan alarm observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chrome.alarms.onAlarm → SW glue | An alarm event (possibly a stale `fsbTrigger:*` after a code reload) enters the single onAlarm fan-out; must be name-guarded and never throw out of the listener. |
| chrome.tabs.onRemoved → SW glue | A tab-close event with an arbitrary `tabId` enters the new listener; the handler validates and is non-blocking. |
| importScripts load → SW init | A module load failure must not abort SW startup. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-11 | Denial of Service | A throw inside `handleTriggerAlarm` / `handleTriggerTabRemoved` propagating out of the SW listener and disrupting the single onAlarm fan-out or other onRemoved concerns | mitigate | Every glue invocation is wrapped: onAlarm branch in `try/catch` with a non-blocking `console.warn`; onRemoved in `Promise.resolve(...).catch`. The early `return` in the onAlarm branch scopes the trigger concern without affecting sibling branches. The handlers themselves are no-throw (Plan 14-02). |
| T-14-12 | Denial of Service | trigger-store.js / trigger-lifecycle.js failing to load aborting SW startup | mitigate | Each `importScripts` is wrapped in `try { ... } catch (e) { console.error(...) }` (mirroring the existing task-store load); a load failure logs and the `typeof FsbTriggerLifecycle !== 'undefined'` guards at every call site make the glue inert rather than crashing. |
| T-14-13 | Tampering | An edit accidentally touching the load-bearing agent-loop iterator (INV-04) | mitigate | INV-04 is a HARD acceptance gate: `git diff --name-only` must not list `extension/ai/agent-loop.js` and `grep -c setTimeout` must remain 8. The plan's only file_modified is `extension/background.js`. |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero packages installed (additive glue to an existing file). No registry lookup, no postinstall. A dependency need would be a scope violation — escalate. |
</threat_model>

<verification>
- `node --check extension/background.js` — parses (no syntax break from the additive edits).
- `grep -c "FsbTriggerLifecycle.restoreTriggersFromStorage\|FsbTriggerLifecycle.handleTriggerAlarm\|FsbTriggerLifecycle.handleTriggerTabRemoved" extension/background.js` — returns 3 (all three runtime glue points wired).
- `grep -c "setTimeout" extension/ai/agent-loop.js` — returns 8 (INV-04 byte-frozen).
- `git diff --name-only` — does NOT include `extension/ai/agent-loop.js`.
- `npm test` — full chain green (no regression; the two trigger test files included).
- Manual (checkpoint / deferrable to milestone-end Chrome MV3 UAT): live SW-eviction survival in an unpacked extension.
</verification>

<success_criteria>
- SURV-01 / SURV-03 / LIFE-05 are LIVE in the SW: alarm ticks route to `handleTriggerAlarm`, bootstrap runs `restoreTriggersFromStorage`, tab-close routes to `handleTriggerTabRemoved`.
- All four glue points are additive; no existing branch/listener changed; store imported before lifecycle.
- INV-04 holds (agent-loop.js byte-frozen, `setTimeout` count 8).
- `node --check` and the full `npm test` chain pass.
- Live Chrome MV3 survival is confirmed OR explicitly deferred to milestone-end UAT and recorded in STATE.md.
- 14-03-SUMMARY.md can truthfully report Plan 14-03 complete.
</success_criteria>

<output>
Create `.planning/phases/14-trigger-survivability-foundation/14-03-SUMMARY.md` when done.
</output>
