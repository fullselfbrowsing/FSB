---
phase: 37-breadth-a-dev-productivity-least-sensitive
plan: 03
type: execute
wave: 3
depends_on: ["37-02"]
files_modified:
  - vendor/opentabs-snapshot/plugins/gitlab/package.json
  - vendor/opentabs-snapshot/plugins/gitlab/src/sdk-stub.ts
  - vendor/opentabs-snapshot/plugins/gitlab/src/index.ts
  - vendor/opentabs-snapshot/plugins/gitlab/src/gitlab-api.ts
  - vendor/opentabs-snapshot/plugins/gitlab/src/tools/create-issue.ts
  - vendor/opentabs-snapshot/plugins/gitlab/src/tools/list-issues.ts
  - vendor/opentabs-snapshot/plugins/gitlab/src/tools/get-issue.ts
  - vendor/opentabs-snapshot/plugins/gitlab/src/tools/create-merge-request.ts
  - vendor/opentabs-snapshot/plugins/bitbucket/package.json
  - vendor/opentabs-snapshot/plugins/bitbucket/src/sdk-stub.ts
  - vendor/opentabs-snapshot/plugins/bitbucket/src/index.ts
  - vendor/opentabs-snapshot/plugins/bitbucket/src/bitbucket-api.ts
  - vendor/opentabs-snapshot/plugins/bitbucket/src/tools/list-pull-requests.ts
  - vendor/opentabs-snapshot/plugins/bitbucket/src/tools/get-pull-request.ts
  - vendor/opentabs-snapshot/plugins/bitbucket/src/tools/create-pull-request.ts
  - vendor/opentabs-snapshot/plugins/bitbucket/src/tools/list-repositories.ts
  - vendor/opentabs-snapshot/plugins/vercel/package.json
  - vendor/opentabs-snapshot/plugins/vercel/src/sdk-stub.ts
  - vendor/opentabs-snapshot/plugins/vercel/src/index.ts
  - vendor/opentabs-snapshot/plugins/vercel/src/vercel-api.ts
  - vendor/opentabs-snapshot/plugins/vercel/src/tools/list-deployments.ts
  - vendor/opentabs-snapshot/plugins/vercel/src/tools/get-deployment.ts
  - vendor/opentabs-snapshot/plugins/vercel/src/tools/list-projects.ts
  - vendor/opentabs-snapshot/plugins/vercel/src/tools/create-deployment.ts
  - vendor/opentabs-snapshot/plugins/netlify/package.json
  - vendor/opentabs-snapshot/plugins/netlify/src/sdk-stub.ts
  - vendor/opentabs-snapshot/plugins/netlify/src/index.ts
  - vendor/opentabs-snapshot/plugins/netlify/src/netlify-api.ts
  - vendor/opentabs-snapshot/plugins/netlify/src/tools/list-sites.ts
  - vendor/opentabs-snapshot/plugins/netlify/src/tools/get-site.ts
  - vendor/opentabs-snapshot/plugins/netlify/src/tools/list-deploys.ts
  - vendor/opentabs-snapshot/plugins/netlify/src/tools/create-deploy.ts
  - vendor/opentabs-snapshot/PIN.md
  - catalog/descriptors/_fixtures/_provenance.json
  - catalog/descriptors/_fixtures/intent-cases.json
  - extension/catalog/recipe-index.generated.js
  - tests/breadth-search-return.test.js
autonomous: true
requirements: [BRDTH-01]

must_haves:
  truths:
    - "gitlab, bitbucket, vercel, netlify descriptors are imported via the SAME Plan-01 machinery and returned by search with >=3 app-disambiguated synonyms + side-effect class + backing per op"
    - "Importing this sub-batch re-runs the merge-time classifyGate; all four origins (gitlab.com, bitbucket.org, vercel.com, app.netlify.com) classify safe and pass"
    - "gitlab.create_merge_request / bitbucket.create_pull_request / vercel.create_deployment class write (never read); crosscheck stays green"
    - "node scripts/verify-catalog-crosscheck.mjs, node tests/no-dead-entry.test.js, node tests/capability-search-eval.test.js stay exit 0 on the grown corpus"
    - "npm run validate:extension exits 0 (snapshot reconciled)"
  artifacts:
    - path: "vendor/opentabs-snapshot/plugins/vercel/src/index.ts"
      provides: "vendored vercel metadata slice (deployments -- a depth-shortlist read app discoverable from breadth)"
      contains: "VercelPlugin"
    - path: "extension/catalog/recipe-index.generated.js"
      provides: "the snapshot REGENERATED after this sub-batch emit (INV-01 IIFE/djb2 unchanged) so validate:extension reconciles"
  key_links:
    - from: "scripts/import-opentabs-catalog.mjs enumerateBatchApps"
      to: "vendor/opentabs-snapshot/plugins/{gitlab,bitbucket,vercel,netlify}"
      via: "the Plan-01 enumeration imports the new vendored dirs (no importer edit)"
      pattern: "opentabs__(gitlab|bitbucket|vercel|netlify)__"
    - from: "scripts/package-extension.mjs"
      to: "extension/catalog/recipe-index.generated.js"
      via: "regen after emit so validate-extension byte-compare reconciles"
      pattern: "recipe-index.generated|package-extension"
---

<objective>
Vendor the third dev/cloud sub-batch -- gitlab, bitbucket, vercel, netlify -- as DATA only,
reusing the Plan-01 importer machinery WITHOUT touching any machinery file. Each app is a
metadata-only vendored slice; the Plan-01 importer enumerates and emits them; the snapshot is
regenerated and the eval extended so validate:extension stays green.

Purpose: continue growing the breadth corpus (the code-hosting + deploy slice) on the reused
contract.
Output: gitlab/bitbucket/vercel/netlify descriptors; regenerated snapshot; extended eval.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-breadth-a-dev-productivity-least-sensitive/37-CONTEXT.md
@.planning/phases/37-breadth-a-dev-productivity-least-sensitive/37-01-SUMMARY.md
@.planning/phases/37-breadth-a-dev-productivity-least-sensitive/37-02-SUMMARY.md

<interfaces>
<!-- The Plan-01 machinery this plan reuses (do NOT edit it). -->
- scripts/import-opentabs-catalog.mjs: enumerateBatchApps() imports every vendored plugin dir
  except already-head services (github/slack/notion), todoist, and /e2e|prescript|-test$/
  fixtures. Vendoring a new app dir is sufficient -- NO importer edit. synthSynonyms emits
  >=3 app-disambiguated phrases; extractDescriptors stamps backing:'dom'; runImport() aborts
  on any unclassified batch origin via classifyGate.
- The vendored-slice contract (replicate todoist/linear): package.json with
  opentabs.urlPatterns[0]=*://<host>/*; src/sdk-stub.ts (VERBATIM copy); src/<app>-api.ts
  (inert helpers); src/index.ts (<App>Plugin extends OpenTabsPlugin); src/tools/<op>.ts
  (defineTool with a real z.object input + a never-run handle).
- Hosts (authoritative urlPatterns): gitlab -> gitlab.com; bitbucket -> bitbucket.org;
  vercel -> vercel.com; netlify -> app.netlify.com. NONE are in the denylist (all safe).
- crosscheck re-derives the side-effect class from the persisted signals; a write must never
  under-state as read. gitlab/bitbucket are GraphQL/REST mixed -- use the op-name verb the
  shared module recognizes (create/list/get) so the carve-out classes correctly.
</interfaces>

# Reuse references:
@vendor/opentabs-snapshot/plugins/todoist/src/index.ts
@vendor/opentabs-snapshot/plugins/todoist/src/sdk-stub.ts
@vendor/opentabs-snapshot/plugins/todoist/src/tools/create-task.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Vendor gitlab + bitbucket + vercel + netlify metadata slices</name>
  <files>vendor/opentabs-snapshot/plugins/{gitlab,bitbucket,vercel,netlify}/** (each: package.json, src/sdk-stub.ts, src/index.ts, src/<app>-api.ts, src/tools/*.ts), vendor/opentabs-snapshot/PIN.md</files>
  <read_first>
    - vendor/opentabs-snapshot/plugins/todoist/ (the full slice structure to replicate)
    - vendor/opentabs-snapshot/plugins/linear/src/index.ts + src/linear-api.ts (the 37-01 GraphQL slice shape, if gitlab uses GraphQL)
    - vendor/opentabs-snapshot/PIN.md (SHA + MIT pin + per-phase log)
  </read_first>
  <behavior>
    - Each of the four app dirs imports under tsx and exposes a plugin with tools[] and the correct urlPatterns host; no handle() executes.
    - gitlab ops: create_issue, list_issues, get_issue, create_merge_request. bitbucket ops: list_pull_requests, get_pull_request, create_pull_request, list_repositories. vercel ops: list_deployments, get_deployment, list_projects, create_deployment. netlify ops: list_sites, get_site, list_deploys, create_deploy.
    - Each tool input is a real z.object reflecting the op's actual params; z.toJSONSchema yields closed params.
    - The transport stub per app exports the inert helpers the tool handles reference, each a thrower; a comment records the upstream method per op.
  </behavior>
  <action>
    Create the four vendored metadata slices (gitlab, bitbucket, vercel, netlify) mirroring the todoist/linear slice structure EXACTLY (Wall 1: metadata-only, NO dist/, NO executed handle()). For each app: package.json (opentabs.urlPatterns host per the interfaces block), src/sdk-stub.ts (VERBATIM copy of todoist's), src/index.ts (<App>Plugin with the ops in <behavior>), src/<app>-api.ts (inert transport stub matching the app's transport), and src/tools/<op>.ts per op with a real z.object input and a never-run handle whose comment names the upstream method.

    Make the side-effect verbs explicit so crosscheck classes correctly: create_*/create_merge_request/create_pull_request/create_deployment/create_deploy -> write; list_*/get_* -> read. Use op-name verbs the shared side-effect-class.mjs recognizes.

    Append gitlab/bitbucket/vercel/netlify to vendor/opentabs-snapshot/PIN.md's per-phase log (Phase 37 batch A, sub-batch 3). Do NOT vendor any e2e/prescript fixture or denied/sensitive app. Do NOT edit any importer/search/gate file.
  </action>
  <verify>
    <automated>node --import tsx -e "const apps=['gitlab','bitbucket','vercel','netlify'];(async()=>{for(const a of apps){const m=await import('./vendor/opentabs-snapshot/plugins/'+a+'/src/index.ts');const p=m.default;if(!p||!Array.isArray(p.tools)||p.tools.length<4){console.error('bad slice',a);process.exit(1)}console.log(a,p.tools.map(t=>t.name).join(','))}process.exit(0)})().catch(e=>{console.error(e.message);process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - All four slices import under tsx with >=4 ops each, real z.object inputs.
    - No handle() executes during import; inert transport stubs throw only if called.
    - gitlab.create_merge_request, bitbucket.create_pull_request, vercel.create_deployment are present as write ops.
  </acceptance_criteria>
  <done>gitlab/bitbucket/vercel/netlify vendored as hermetic metadata slices importable under tsx; PIN.md per-phase log updated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Import the sub-batch via the Plan-01 importer, regenerate the snapshot, extend the eval</name>
  <files>extension/catalog/recipe-index.generated.js, catalog/descriptors/_fixtures/intent-cases.json, catalog/descriptors/_fixtures/_provenance.json, tests/breadth-search-return.test.js</files>
  <read_first>
    - scripts/import-opentabs-catalog.mjs (the 37-01 enumerateBatchApps + runImport -- run, do NOT edit)
    - scripts/package-extension.mjs + scripts/validate-extension.mjs (regen + byte-compare)
    - tests/capability-search-eval.test.js + tests/breadth-search-return.test.js (the eval harnesses)
    - catalog/descriptors/_fixtures/intent-cases.json (the eval fixtures to grow)
  </read_first>
  <behavior>
    - `node --import tsx ./scripts/import-opentabs-catalog.mjs` emits opentabs__gitlab__*, opentabs__bitbucket__*, opentabs__vercel__*, opentabs__netlify__* (alongside the existing apps) WITHOUT clobbering any existing descriptor.
    - Each emitted descriptor carries >=3 app-disambiguated synonyms + a side-effect class + backing:'dom'.
    - `node scripts/package-extension.mjs` regenerates the snapshot; validate-extension reconciles (exit 0).
    - intent-cases.json gains read/write fixtures for the new apps; capability-search-eval keeps recall@5>=0.9 + wrong-invoke=0.
    - breadth-search-return.test.js asserts the new apps' ops each return with synonyms+class+backing.
  </behavior>
  <action>
    (a) Run `node --import tsx ./scripts/import-opentabs-catalog.mjs` to emit the four new apps via the Plan-01 machinery; confirm the new descriptors appear and existing descriptors are untouched.

    (b) Run `node scripts/package-extension.mjs` to regenerate extension/catalog/recipe-index.generated.js (INV-01 unchanged shape); confirm `node scripts/validate-extension.mjs` exits 0.

    (c) Extend catalog/descriptors/_fixtures/intent-cases.json with intent fixtures for the new apps (at least one read + one write across the four, e.g. "list my vercel deployments" -> vercel.list_deployments; "open a merge request in gitlab" -> gitlab.create_merge_request) with expectedSlug aligned to the emitted real slugs. Extend tests/breadth-search-return.test.js to include the new opentabs__{gitlab,bitbucket,vercel,netlify}__* descriptors. Re-run capability-search-eval.test.js (wrong-invoke=0.000, recall@5>=0.9) and breadth-search-return.test.js.

    Do NOT edit the importer, capability-search.js, or the gates (machinery frozen from Plan 01).
  </action>
  <verify>
    <automated>node --import tsx ./scripts/import-opentabs-catalog.mjs && node scripts/package-extension.mjs && node scripts/validate-extension.mjs && node scripts/verify-catalog-crosscheck.mjs && node tests/no-dead-entry.test.js && node tests/breadth-search-return.test.js && node tests/capability-search-eval.test.js</automated>
  </verify>
  <acceptance_criteria>
    - The four new apps' descriptors are emitted and inlined; validate-extension exits 0 (snapshot reconciled).
    - verify-catalog-crosscheck.mjs exits 0 (create_merge_request/create_pull_request/create_deployment write, never read).
    - no-dead-entry.test.js exits 0; capability-search-eval prints wrong-invoke=0.000, recall@5>=0.9; breadth-search-return passes incl. the new apps.
  </acceptance_criteria>
  <done>gitlab/bitbucket/vercel/netlify imported via the reused machinery; snapshot regenerated; eval extended and green; crosscheck + no-dead-entry stay exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OpenTabs vendored metadata -> FSB descriptor corpus (build-time) | new sub-batch metadata read by the importer |
| descriptor corpus -> search hit -> invoke (runtime) | new write ops (merge-request/pull-request/deployment) must carry the correct side-effect class |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | Elevation of Privilege | the merge-time batch import of an unclassified origin | mitigate | the Plan-01 runImport() classifyGate runs over the new origins and aborts on any unclassified one (the four dev origins classify safe and pass) |
| T-37-06 | Elevation of Privilege | a create-deployment / merge-request write under-stated as read -> ungated mutation under Auto | mitigate | the shared side-effect derivation classes create -> write; verify-catalog-crosscheck.mjs fails the build on under-statement (asserted exit 0 in Task 2) |
| T-37-04 | Tampering | a vendored slice emitting a forbidden field name into shipped params (Wall 1) | mitigate | the importer's recursive forbidden-field pre-scan (UNCHANGED) throws before emit; recipe-path-guard stays green via validate:extension |
| T-37-SC | Tampering | npm/pip/cargo installs | accept | no new packages this plan (data-only vendoring); package-legitimacy gate N/A |
</threat_model>

<verification>
- `node --import tsx ./scripts/import-opentabs-catalog.mjs` emits the four new apps; existing descriptors untouched.
- `node scripts/validate-extension.mjs` exits 0 (snapshot reconciled; gates green).
- `node scripts/verify-catalog-crosscheck.mjs` exits 0.
- `node tests/no-dead-entry.test.js && node tests/breadth-search-return.test.js && node tests/capability-search-eval.test.js` pass.
</verification>

<success_criteria>
- BRDTH-01 (continued): gitlab/bitbucket/vercel/netlify descriptors imported + returned by search with synonyms + side-effect class + backing; contract reused with NO machinery change.
- Regression: crosscheck + no-dead-entry + eval stay green; validate:extension exits 0; the merge-time batch gate still aborts on an unclassified origin.
</success_criteria>

<output>
Create `.planning/phases/37-breadth-a-dev-productivity-least-sensitive/37-03-SUMMARY.md` when done.
</output>
