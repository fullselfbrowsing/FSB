---
phase: 37-breadth-a-dev-productivity-least-sensitive
plan: 02
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - vendor/opentabs-snapshot/plugins/clickup/package.json
  - vendor/opentabs-snapshot/plugins/clickup/src/sdk-stub.ts
  - vendor/opentabs-snapshot/plugins/clickup/src/index.ts
  - vendor/opentabs-snapshot/plugins/clickup/src/clickup-api.ts
  - vendor/opentabs-snapshot/plugins/clickup/src/tools/create-task.ts
  - vendor/opentabs-snapshot/plugins/clickup/src/tools/list-tasks.ts
  - vendor/opentabs-snapshot/plugins/clickup/src/tools/get-task.ts
  - vendor/opentabs-snapshot/plugins/clickup/src/tools/update-task.ts
  - vendor/opentabs-snapshot/plugins/jira/package.json
  - vendor/opentabs-snapshot/plugins/jira/src/sdk-stub.ts
  - vendor/opentabs-snapshot/plugins/jira/src/index.ts
  - vendor/opentabs-snapshot/plugins/jira/src/jira-api.ts
  - vendor/opentabs-snapshot/plugins/jira/src/tools/create-issue.ts
  - vendor/opentabs-snapshot/plugins/jira/src/tools/search-issues.ts
  - vendor/opentabs-snapshot/plugins/jira/src/tools/get-issue.ts
  - vendor/opentabs-snapshot/plugins/jira/src/tools/update-issue.ts
  - vendor/opentabs-snapshot/plugins/jira/src/tools/add-comment.ts
  - vendor/opentabs-snapshot/plugins/confluence/package.json
  - vendor/opentabs-snapshot/plugins/confluence/src/sdk-stub.ts
  - vendor/opentabs-snapshot/plugins/confluence/src/index.ts
  - vendor/opentabs-snapshot/plugins/confluence/src/confluence-api.ts
  - vendor/opentabs-snapshot/plugins/confluence/src/tools/create-page.ts
  - vendor/opentabs-snapshot/plugins/confluence/src/tools/get-page.ts
  - vendor/opentabs-snapshot/plugins/confluence/src/tools/search-pages.ts
  - vendor/opentabs-snapshot/plugins/confluence/src/tools/update-page.ts
  - vendor/opentabs-snapshot/plugins/airtable/package.json
  - vendor/opentabs-snapshot/plugins/airtable/src/sdk-stub.ts
  - vendor/opentabs-snapshot/plugins/airtable/src/index.ts
  - vendor/opentabs-snapshot/plugins/airtable/src/airtable-api.ts
  - vendor/opentabs-snapshot/plugins/airtable/src/tools/list-records.ts
  - vendor/opentabs-snapshot/plugins/airtable/src/tools/get-record.ts
  - vendor/opentabs-snapshot/plugins/airtable/src/tools/create-record.ts
  - vendor/opentabs-snapshot/plugins/airtable/src/tools/update-record.ts
  - vendor/opentabs-snapshot/plugins/airtable/src/tools/delete-record.ts
  - vendor/opentabs-snapshot/PIN.md
  - catalog/descriptors/_fixtures/_provenance.json
  - catalog/descriptors/_fixtures/intent-cases.json
  - extension/catalog/recipe-index.generated.js
  - tests/breadth-search-return.test.js
  - package.json
autonomous: true
requirements: [BRDTH-01]

must_haves:
  truths:
    - "clickup, jira, confluence, airtable descriptors are imported via the SAME Plan-01 machinery and returned by search with >=3 app-disambiguated intent synonyms + a side-effect class + a backing enum per op"
    - "Importing this sub-batch re-runs the merge-time classifyGate; all four origins (clickup.com / app.clickup.com, *.atlassian.net jira+confluence, airtable.com) classify safe and pass; the build still aborts if any were unclassified"
    - "airtable.delete_record classes destructive and jira/confluence writes class write (never read) -- crosscheck stays green"
    - "node scripts/verify-catalog-crosscheck.mjs, node tests/no-dead-entry.test.js, node tests/capability-search-eval.test.js all stay exit 0 on the grown corpus"
    - "npm run validate:extension exits 0 (snapshot reconciled)"
  artifacts:
    - path: "vendor/opentabs-snapshot/plugins/jira/src/index.ts"
      provides: "vendored jira metadata slice (atlassian.net host)"
      contains: "JiraPlugin"
    - path: "vendor/opentabs-snapshot/plugins/airtable/src/tools/delete-record.ts"
      provides: "a destructive op to exercise the crosscheck destructive-class on the grown corpus"
      contains: "delete_record"
    - path: "extension/catalog/recipe-index.generated.js"
      provides: "the snapshot REGENERATED after this sub-batch emit so validate:extension reconciles (INV-01 IIFE/djb2 unchanged)"
  key_links:
    - from: "scripts/import-opentabs-catalog.mjs enumerateBatchApps"
      to: "vendor/opentabs-snapshot/plugins/{clickup,jira,confluence,airtable}"
      via: "the Plan-01 directory enumeration now sees the new vendored dirs and imports them (no importer edit needed)"
      pattern: "opentabs__(clickup|jira|confluence|airtable)__"
    - from: "scripts/package-extension.mjs"
      to: "extension/catalog/recipe-index.generated.js"
      via: "regen after emit so validate-extension byte-compare reconciles"
      pattern: "recipe-index.generated|package-extension"
---

<objective>
Vendor the second dev/PM sub-batch -- clickup, jira, confluence, airtable -- as DATA only,
reusing the Plan-01 importer machinery (directory enumeration + MED-03 synonyms + backing +
merge-time batch gate) WITHOUT touching any machinery file. Each app is a metadata-only
vendored slice; the Plan-01 importer enumerates and emits them; the snapshot is regenerated
and the eval extended so validate:extension stays green.

Purpose: grow the breadth corpus toward full dev/PM/cloud/observability coverage while
proving the contract reuses verbatim for a new sub-batch (the thing 38/39 will rely on).
Output: clickup/jira/confluence/airtable descriptors; regenerated snapshot; extended eval.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-breadth-a-dev-productivity-least-sensitive/37-CONTEXT.md
@.planning/phases/37-breadth-a-dev-productivity-least-sensitive/37-01-SUMMARY.md

<interfaces>
<!-- The Plan-01 machinery this plan reuses (do NOT edit it). -->
- scripts/import-opentabs-catalog.mjs: enumerateBatchApps() (added in 37-01) reads
  vendor/opentabs-snapshot/plugins/ and imports every dir except already-head services
  (github/slack/notion), todoist, and /e2e|prescript|-test$/ fixtures. Vendoring a new
  app dir is sufficient for it to be imported -- NO importer edit. synthSynonyms emits
  >=3 app-disambiguated phrases; extractDescriptors stamps backing:'dom'; runImport()
  aborts on any unclassified batch origin via classifyGate.
- The vendored-slice contract (replicate the todoist/linear/asana shape): package.json with
  opentabs.urlPatterns[0]=*://<host>/*; src/sdk-stub.ts (VERBATIM copy); src/<app>-api.ts
  (inert api/apiVoid/graphql throwers); src/index.ts (<App>Plugin extends OpenTabsPlugin,
  tools[]); src/tools/<op>.ts (defineTool with a real z.object input + a never-run handle).
- Hosts (authoritative urlPatterns): clickup -> app.clickup.com; jira -> *.atlassian.net
  (use a representative host e.g. your-domain.atlassian.net pattern *://*.atlassian.net/*);
  confluence -> *.atlassian.net (confluence path) -- emit a distinct service if OpenTabs
  uses a distinct host, else keep the atlassian host and a confluence service stem;
  airtable -> airtable.com. NONE are in the denylist (all classify safe).
- crosscheck (scripts/verify-catalog-crosscheck.mjs) re-derives side-effect class from the
  persisted signals; a GraphQL/REST write must never under-state as read.
</interfaces>

# Reuse references:
@vendor/opentabs-snapshot/plugins/todoist/src/index.ts
@vendor/opentabs-snapshot/plugins/todoist/src/sdk-stub.ts
@vendor/opentabs-snapshot/plugins/todoist/src/todoist-api.ts
@vendor/opentabs-snapshot/plugins/todoist/src/tools/create-task.ts
@vendor/opentabs-snapshot/plugins/todoist/src/tools/delete-task.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Vendor clickup + jira + confluence + airtable metadata slices</name>
  <files>vendor/opentabs-snapshot/plugins/{clickup,jira,confluence,airtable}/** (each: package.json, src/sdk-stub.ts, src/index.ts, src/<app>-api.ts, src/tools/*.ts), vendor/opentabs-snapshot/PIN.md</files>
  <read_first>
    - vendor/opentabs-snapshot/plugins/todoist/ (the full slice structure to replicate: package.json, src/index.ts, src/sdk-stub.ts, src/todoist-api.ts, src/tools/*.ts)
    - vendor/opentabs-snapshot/plugins/linear/src/index.ts (the 37-01 GraphQL slice, if jira/confluence use GraphQL) -- read only if present after 37-01
    - vendor/opentabs-snapshot/plugins/todoist/src/tools/delete-task.ts (the destructive apiVoid DELETE shape for airtable.delete_record)
    - vendor/opentabs-snapshot/PIN.md (SHA + MIT pin + the per-phase log to append to)
  </read_first>
  <behavior>
    - Each of the four app dirs imports under tsx and exposes a plugin with tools[] and the correct urlPatterns host; no handle() executes.
    - clickup ops: create_task, list_tasks, get_task, update_task. jira ops: create_issue, search_issues, get_issue, update_issue, add_comment. confluence ops: create_page, get_page, search_pages, update_page. airtable ops: list_records, get_record, create_record, update_record, delete_record.
    - Each tool input is a real z.object reflecting the op's actual params; z.toJSONSchema yields closed params.
    - The transport stub per app exports the inert helpers the tool handles reference (api/apiVoid for REST; graphql if the app is GraphQL), each a thrower; a comment records the upstream method per op (so the importer's verb-map + carve-out class each op correctly).
  </behavior>
  <action>
    Create the four vendored metadata slices (clickup, jira, confluence, airtable) mirroring the todoist/linear slice structure EXACTLY (Wall 1: metadata-only, NO dist/, NO executed handle()). For each app: package.json (opentabs.urlPatterns host per the interfaces block), src/sdk-stub.ts (VERBATIM copy of todoist's), src/index.ts (<App>Plugin extends OpenTabsPlugin with the ops listed in <behavior>), src/<app>-api.ts (inert transport stub matching the app's transport -- REST api/apiVoid for clickup/airtable; for jira/confluence use whichever transport OpenTabs uses, exporting inert helpers whose names let the importer's side-effect derivation class each op by its op-name verb + method), and src/tools/<op>.ts per op with a real z.object input and a never-run handle whose comment names the upstream method.

    Make the side-effect-relevant verbs explicit so the crosscheck classes correctly: airtable.delete_record -> destructive (apiVoid DELETE / api {method:'DELETE'}); create_*/update_* -> write; list_*/search_*/get_* -> read. Use op-name verbs the shared side-effect-class.mjs recognizes (create/update/list/search/get/delete/add).

    Append clickup/jira/confluence/airtable to vendor/opentabs-snapshot/PIN.md's per-phase log (Phase 37 batch A, sub-batch 2). Do NOT vendor any e2e/prescript fixture or any denied/sensitive app. Do NOT edit any importer/search/gate file (this is data-only).
  </action>
  <verify>
    <automated>node --import tsx -e "const apps=['clickup','jira','confluence','airtable'];(async()=>{for(const a of apps){const m=await import('./vendor/opentabs-snapshot/plugins/'+a+'/src/index.ts');const p=m.default;if(!p||!Array.isArray(p.tools)||p.tools.length<4){console.error('bad slice',a);process.exit(1)}console.log(a,p.tools.map(t=>t.name).join(','))}process.exit(0)})().catch(e=>{console.error(e.message);process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - All four slices import under tsx; clickup>=4 ops, jira>=5, confluence>=4, airtable>=5, each with real z.object inputs.
    - No handle() executes during import; the inert transport stubs throw only if called.
    - airtable exposes a delete_record op; jira/confluence expose create_*/update_* write ops and search/get read ops.
  </acceptance_criteria>
  <done>clickup/jira/confluence/airtable vendored as hermetic metadata slices importable under tsx; PIN.md per-phase log updated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Import the sub-batch via the Plan-01 importer, regenerate the snapshot, extend the eval</name>
  <files>extension/catalog/recipe-index.generated.js, catalog/descriptors/_fixtures/intent-cases.json, catalog/descriptors/_fixtures/_provenance.json, tests/breadth-search-return.test.js</files>
  <read_first>
    - scripts/import-opentabs-catalog.mjs (the 37-01 enumerateBatchApps + runImport -- run it, do NOT edit it)
    - scripts/package-extension.mjs (the snapshot regen path)
    - scripts/validate-extension.mjs (the stale-snapshot byte-compare)
    - tests/capability-search-eval.test.js + tests/breadth-search-return.test.js (the eval harnesses to extend)
    - catalog/descriptors/_fixtures/intent-cases.json (the eval fixtures to grow)
  </read_first>
  <behavior>
    - `node --import tsx ./scripts/import-opentabs-catalog.mjs` emits opentabs__clickup__*, opentabs__jira__*, opentabs__confluence__*, opentabs__airtable__* descriptors (alongside the existing todoist/linear/asana) WITHOUT clobbering any existing descriptor, and aborts if any new origin were unclassified.
    - Each emitted descriptor carries >=3 app-disambiguated synonyms + a side-effect class + backing:'dom'.
    - `node scripts/package-extension.mjs` regenerates the snapshot; validate-extension reconciles (exit 0).
    - intent-cases.json gains a few read/write/destructive fixtures for the new apps (e.g. "create an issue in jira" -> jira slug; "delete a record in airtable" -> airtable.delete_record) and capability-search-eval keeps recall@5>=0.9 + wrong-invoke=0.
    - breadth-search-return.test.js asserts the new apps' ops each return with synonyms+class+backing.
  </behavior>
  <action>
    (a) Run `node --import tsx ./scripts/import-opentabs-catalog.mjs` to emit the four new apps' descriptors via the Plan-01 machinery. Confirm the new opentabs__{clickup,jira,confluence,airtable}__*.json files appear and the todoist/linear/asana/head descriptors are untouched (idempotent re-emit).

    (b) Run `node scripts/package-extension.mjs` to regenerate extension/catalog/recipe-index.generated.js (INV-01: IIFE/djb2 shape unchanged, only the DATA literal grows). Confirm `node scripts/validate-extension.mjs` exits 0.

    (c) Extend catalog/descriptors/_fixtures/intent-cases.json with a handful of intent fixtures for the new apps (at least one read, one write, one destructive across the four) with expectedSlug aligned to the emitted real slugs. Extend tests/breadth-search-return.test.js so its per-op assertion set includes the new opentabs__{clickup,jira,confluence,airtable}__* descriptors. Re-run capability-search-eval.test.js (wrong-invoke=0.000, recall@5>=0.9) and breadth-search-return.test.js.

    Do NOT edit the importer, capability-search.js, or the gates (machinery is frozen from Plan 01).
  </action>
  <verify>
    <automated>node --import tsx ./scripts/import-opentabs-catalog.mjs && node scripts/package-extension.mjs && node scripts/validate-extension.mjs && node scripts/verify-catalog-crosscheck.mjs && node tests/no-dead-entry.test.js && node tests/breadth-search-return.test.js && node tests/capability-search-eval.test.js</automated>
  </verify>
  <acceptance_criteria>
    - The four new apps' descriptors are emitted and inlined; validate-extension exits 0 (snapshot reconciled).
    - verify-catalog-crosscheck.mjs exits 0 (airtable.delete_record destructive, jira/confluence writes never read).
    - no-dead-entry.test.js exits 0 (new descriptor-only slugs resolve to a non-null seam).
    - capability-search-eval.test.js prints wrong-invoke=0.000, recall@5>=0.9; breadth-search-return.test.js passes including the new apps.
  </acceptance_criteria>
  <done>clickup/jira/confluence/airtable imported via the reused machinery; snapshot regenerated; eval extended and green; crosscheck + no-dead-entry stay exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OpenTabs vendored metadata -> FSB descriptor corpus (build-time) | new sub-batch metadata read by the importer; a wrong origin or forbidden field could enter the catalog |
| descriptor corpus -> search hit -> invoke (runtime) | new write/destructive ops (jira/confluence/airtable) must carry the correct side-effect class so consent gating applies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | Elevation of Privilege | the merge-time batch import of an unclassified origin | mitigate | the Plan-01 runImport() classifyGate runs over the new sub-batch origins too and aborts on any unclassified one (the four dev origins classify safe and pass); no machinery change needed |
| T-37-06 | Elevation of Privilege | airtable.delete_record / jira/confluence writes under-stated as read -> ungated mutation under Auto | mitigate | the shared side-effect derivation classes delete -> destructive and create/update -> write; verify-catalog-crosscheck.mjs fails the build on any under-statement (asserted exit 0 in Task 2) |
| T-37-04 | Tampering | a vendored slice emitting a forbidden field name into shipped params (Wall 1) | mitigate | the importer's recursive forbidden-field pre-scan (UNCHANGED) throws before emit; verify-recipe-path-guard.mjs stays green via validate:extension |
| T-37-SC | Tampering | npm/pip/cargo installs | accept | no new packages this plan (data-only vendoring); package-legitimacy gate N/A |
</threat_model>

<verification>
- `node --import tsx ./scripts/import-opentabs-catalog.mjs` emits the four new apps; head/todoist/linear/asana descriptors untouched.
- `node scripts/validate-extension.mjs` exits 0 (snapshot reconciled; gates green).
- `node scripts/verify-catalog-crosscheck.mjs` exits 0 (no under-stated destructive op).
- `node tests/no-dead-entry.test.js` exits 0.
- `node tests/breadth-search-return.test.js && node tests/capability-search-eval.test.js` pass (recall@5>=0.9, wrong-invoke=0).
</verification>

<success_criteria>
- BRDTH-01 (continued): clickup/jira/confluence/airtable descriptors imported + returned by search with synonyms + side-effect class + backing; the breadth contract reused verbatim for a new sub-batch with NO machinery change.
- Regression: crosscheck + no-dead-entry + eval stay green; validate:extension exits 0; the merge-time batch gate still aborts on an unclassified origin.
</success_criteria>

<output>
Create `.planning/phases/37-breadth-a-dev-productivity-least-sensitive/37-02-SUMMARY.md` when done.
</output>
