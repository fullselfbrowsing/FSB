---
phase: 37-breadth-a-dev-productivity-least-sensitive
plan: 04
type: execute
wave: 4
depends_on: ["37-03"]
files_modified:
  - vendor/opentabs-snapshot/plugins/cloudflare/package.json
  - vendor/opentabs-snapshot/plugins/cloudflare/src/sdk-stub.ts
  - vendor/opentabs-snapshot/plugins/cloudflare/src/index.ts
  - vendor/opentabs-snapshot/plugins/cloudflare/src/cloudflare-api.ts
  - vendor/opentabs-snapshot/plugins/cloudflare/src/tools/list-zones.ts
  - vendor/opentabs-snapshot/plugins/cloudflare/src/tools/get-zone.ts
  - vendor/opentabs-snapshot/plugins/cloudflare/src/tools/list-dns-records.ts
  - vendor/opentabs-snapshot/plugins/cloudflare/src/tools/purge-cache.ts
  - vendor/opentabs-snapshot/plugins/circleci/package.json
  - vendor/opentabs-snapshot/plugins/circleci/src/sdk-stub.ts
  - vendor/opentabs-snapshot/plugins/circleci/src/index.ts
  - vendor/opentabs-snapshot/plugins/circleci/src/circleci-api.ts
  - vendor/opentabs-snapshot/plugins/circleci/src/tools/list-pipelines.ts
  - vendor/opentabs-snapshot/plugins/circleci/src/tools/get-pipeline.ts
  - vendor/opentabs-snapshot/plugins/circleci/src/tools/list-workflows.ts
  - vendor/opentabs-snapshot/plugins/circleci/src/tools/trigger-pipeline.ts
  - vendor/opentabs-snapshot/plugins/datadog/package.json
  - vendor/opentabs-snapshot/plugins/datadog/src/sdk-stub.ts
  - vendor/opentabs-snapshot/plugins/datadog/src/index.ts
  - vendor/opentabs-snapshot/plugins/datadog/src/datadog-api.ts
  - vendor/opentabs-snapshot/plugins/datadog/src/tools/query-metrics.ts
  - vendor/opentabs-snapshot/plugins/datadog/src/tools/list-monitors.ts
  - vendor/opentabs-snapshot/plugins/datadog/src/tools/get-monitor.ts
  - vendor/opentabs-snapshot/plugins/datadog/src/tools/list-dashboards.ts
  - vendor/opentabs-snapshot/plugins/sentry/package.json
  - vendor/opentabs-snapshot/plugins/sentry/src/sdk-stub.ts
  - vendor/opentabs-snapshot/plugins/sentry/src/index.ts
  - vendor/opentabs-snapshot/plugins/sentry/src/sentry-api.ts
  - vendor/opentabs-snapshot/plugins/sentry/src/tools/list-issues.ts
  - vendor/opentabs-snapshot/plugins/sentry/src/tools/get-issue.ts
  - vendor/opentabs-snapshot/plugins/sentry/src/tools/list-projects.ts
  - vendor/opentabs-snapshot/plugins/sentry/src/tools/resolve-issue.ts
  - vendor/opentabs-snapshot/plugins/posthog/package.json
  - vendor/opentabs-snapshot/plugins/posthog/src/sdk-stub.ts
  - vendor/opentabs-snapshot/plugins/posthog/src/index.ts
  - vendor/opentabs-snapshot/plugins/posthog/src/posthog-api.ts
  - vendor/opentabs-snapshot/plugins/posthog/src/tools/list-insights.ts
  - vendor/opentabs-snapshot/plugins/posthog/src/tools/get-insight.ts
  - vendor/opentabs-snapshot/plugins/posthog/src/tools/list-dashboards.ts
  - vendor/opentabs-snapshot/plugins/posthog/src/tools/query-events.ts
  - vendor/opentabs-snapshot/PIN.md
  - catalog/descriptors/_fixtures/_provenance.json
  - catalog/descriptors/_fixtures/intent-cases.json
  - extension/catalog/recipe-index.generated.js
  - tests/breadth-search-return.test.js
  - tests/capability-search-eval.test.js
autonomous: true
requirements: [BRDTH-01]

must_haves:
  truths:
    - "cloudflare, circleci, datadog, sentry, posthog descriptors are imported via the Plan-01 machinery and returned by search with >=3 app-disambiguated synonyms + side-effect class + backing per op -- completing the dev/PM/cloud/observability batch"
    - "cloudflare.purge_cache classes destructive and circleci.trigger_pipeline / sentry.resolve_issue class write (never read); crosscheck stays green over the FULL batch"
    - "After the full batch lands, node tests/no-dead-entry.test.js stays exit 0 (every dev/productivity descriptor-only slug resolves to T3/T2) and the eval keeps wrong-invoke=0.000 incl. the MED-03 cross-app create_* set"
    - "The SW cold-start budget is re-asserted at the grown batch size: the serialized index + loadJSON+first-search stay within the CGEN-04 smoke gates (params NOT indexed/stored), proving the SCALE-01 data-layout still holds before the full-corpus scale gate (Phase 43)"
    - "npm run validate:extension exits 0 and the three breadth tests + the eval are green on the complete batch"
  artifacts:
    - path: "vendor/opentabs-snapshot/plugins/datadog/src/index.ts"
      provides: "vendored datadog metadata slice (read-heavy observability -- a depth-shortlist app discoverable from breadth)"
      contains: "DatadogPlugin"
    - path: "vendor/opentabs-snapshot/plugins/cloudflare/src/tools/purge-cache.ts"
      provides: "a destructive op to exercise the crosscheck destructive-class on the full batch"
      contains: "purge_cache"
    - path: "tests/capability-search-eval.test.js"
      provides: "the cold-start budget re-asserted at the grown batch size (serialized index + loadJSON+first-search within the CGEN-04 smoke gates)"
      contains: "cold-start"
    - path: "extension/catalog/recipe-index.generated.js"
      provides: "the snapshot REGENERATED after the final sub-batch emit (INV-01 IIFE/djb2 unchanged) so validate:extension reconciles over the complete batch"
  key_links:
    - from: "scripts/import-opentabs-catalog.mjs enumerateBatchApps"
      to: "vendor/opentabs-snapshot/plugins/{cloudflare,circleci,datadog,sentry,posthog}"
      via: "the Plan-01 enumeration imports the new vendored dirs (no importer edit)"
      pattern: "opentabs__(cloudflare|circleci|datadog|sentry|posthog)__"
    - from: "tests/capability-search-eval.test.js"
      to: "the serialized index size + loadJSON+first-search timing"
      via: "the CGEN-04 cold-start asserts re-run at the grown batch size (still within budget)"
      pattern: "serialized|loadJSON|cold-start"
---

<objective>
Complete the dev/PM/cloud/observability breadth batch (BRDTH-01) by vendoring the final
observability sub-batch -- cloudflare, circleci, datadog, sentry, posthog -- as DATA only via
the Plan-01 machinery, then re-assert the phase's scale + regression gates over the COMPLETE
grown batch:

1. Vendor + import the five observability apps (data-only; no machinery change).
2. Regenerate the snapshot; validate:extension reconciles.
3. Re-assert success criterion 4: crosscheck + no-dead-entry + the eval (incl. the MED-03
   collision set) stay green on the now-complete batch; the SW cold-start budget is re-asserted
   at the grown batch size (the CGEN-04 smoke gates still hold -- params NOT indexed/stored),
   proving the SCALE-01 data-layout before the full-corpus scale gate in Phase 43.

Purpose: deliver complete descriptor coverage of the least-sensitive dev/productivity category
and prove the breadth contract holds at the grown size -- the foundation Phases 38-39 extend.
Output: cloudflare/circleci/datadog/sentry/posthog descriptors; regenerated snapshot; the
cold-start re-assert at the grown size; all phase gates green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-breadth-a-dev-productivity-least-sensitive/37-CONTEXT.md
@.planning/phases/37-breadth-a-dev-productivity-least-sensitive/37-01-SUMMARY.md
@.planning/phases/37-breadth-a-dev-productivity-least-sensitive/37-03-SUMMARY.md

<interfaces>
<!-- The Plan-01 machinery this plan reuses (do NOT edit it). -->
- scripts/import-opentabs-catalog.mjs: enumerateBatchApps() imports every vendored plugin dir
  except already-head services (github/slack/notion), todoist, and /e2e|prescript|-test$/
  fixtures. Vendoring a new app dir is sufficient -- NO importer edit. synthSynonyms emits
  >=3 app-disambiguated phrases; extractDescriptors stamps backing:'dom'; runImport() aborts
  on any unclassified batch origin via classifyGate.
- The vendored-slice contract (replicate todoist/linear): package.json with
  opentabs.urlPatterns[0]=*://<host>/*; src/sdk-stub.ts (VERBATIM copy); src/<app>-api.ts
  (inert helpers); src/index.ts (<App>Plugin); src/tools/<op>.ts (defineTool with a real
  z.object input + a never-run handle).
- Hosts (authoritative urlPatterns): cloudflare -> dash.cloudflare.com; circleci ->
  app.circleci.com; datadog -> app.datadoghq.com; sentry -> sentry.io; posthog ->
  app.posthog.com. NONE are in the denylist (all classify safe).
- The CGEN-04 cold-start asserts in tests/capability-search-eval.test.js (lines ~121-140):
  serialize the built index with INDEX_OPTIONS, assert serialized length < a budget and
  loadJSON+first-search < a budget. These re-run at the grown batch size here; params are
  schema-on-hit (NOT indexed/stored) so the serialized index stays small. The CGEN-04 smoke
  gates were 50KB / 10ms over the smoke corpus; at the grown batch size keep generous gates
  that still PROVE the layout (raise only if the grown index legitimately exceeds the smoke
  gate while the per-descriptor footprint stays flat -- full-corpus scale is Phase 43).
</interfaces>

# Reuse references:
@vendor/opentabs-snapshot/plugins/todoist/src/index.ts
@vendor/opentabs-snapshot/plugins/todoist/src/sdk-stub.ts
@vendor/opentabs-snapshot/plugins/todoist/src/tools/delete-task.ts
@tests/capability-search-eval.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Vendor cloudflare + circleci + datadog + sentry + posthog metadata slices</name>
  <files>vendor/opentabs-snapshot/plugins/{cloudflare,circleci,datadog,sentry,posthog}/** (each: package.json, src/sdk-stub.ts, src/index.ts, src/<app>-api.ts, src/tools/*.ts), vendor/opentabs-snapshot/PIN.md</files>
  <read_first>
    - vendor/opentabs-snapshot/plugins/todoist/ (the full slice structure to replicate)
    - vendor/opentabs-snapshot/plugins/todoist/src/tools/delete-task.ts (the destructive shape for cloudflare.purge_cache)
    - vendor/opentabs-snapshot/PIN.md (SHA + MIT pin + per-phase log)
  </read_first>
  <behavior>
    - Each of the five app dirs imports under tsx and exposes a plugin with tools[] and the correct urlPatterns host; no handle() executes.
    - cloudflare ops: list_zones, get_zone, list_dns_records, purge_cache. circleci ops: list_pipelines, get_pipeline, list_workflows, trigger_pipeline. datadog ops: query_metrics, list_monitors, get_monitor, list_dashboards. sentry ops: list_issues, get_issue, list_projects, resolve_issue. posthog ops: list_insights, get_insight, list_dashboards, query_events.
    - Each tool input is a real z.object reflecting the op's actual params; z.toJSONSchema yields closed params.
    - The transport stub per app exports the inert helpers the tool handles reference, each a thrower; a comment records the upstream method per op.
  </behavior>
  <action>
    Create the five vendored metadata slices mirroring the todoist slice structure EXACTLY (Wall 1: metadata-only, NO dist/, NO executed handle()). For each app: package.json (opentabs.urlPatterns host per the interfaces block), src/sdk-stub.ts (VERBATIM copy of todoist's), src/index.ts (<App>Plugin with the ops in <behavior>), src/<app>-api.ts (inert transport stub matching the app's transport), and src/tools/<op>.ts per op with a real z.object input and a never-run handle whose comment names the upstream method.

    Make the side-effect verbs explicit so crosscheck classes correctly: cloudflare.purge_cache -> destructive (purge is in the destructive verb set); circleci.trigger_pipeline -> write (trigger is mutating); sentry.resolve_issue -> write; query_metrics/query_events/list_*/get_* -> read. Use op-name verbs the shared side-effect-class.mjs recognizes (purge/trigger/resolve are mapped; if `resolve`/`trigger` are not in the shared verb sets, give the op a {method:'POST'} signal in the stub comment + handle so the method classes it write -- do NOT add a verb to the shared module).

    Append cloudflare/circleci/datadog/sentry/posthog to vendor/opentabs-snapshot/PIN.md's per-phase log (Phase 37 batch A, sub-batch 4, completing the category). Do NOT vendor any e2e/prescript fixture or denied/sensitive app. Do NOT edit any importer/search/gate file.
  </action>
  <verify>
    <automated>node --import tsx -e "const apps=['cloudflare','circleci','datadog','sentry','posthog'];(async()=>{for(const a of apps){const m=await import('./vendor/opentabs-snapshot/plugins/'+a+'/src/index.ts');const p=m.default;if(!p||!Array.isArray(p.tools)||p.tools.length<4){console.error('bad slice',a);process.exit(1)}console.log(a,p.tools.map(t=>t.name).join(','))}process.exit(0)})().catch(e=>{console.error(e.message);process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - All five slices import under tsx with >=4 ops each, real z.object inputs.
    - No handle() executes during import; inert transport stubs throw only if called.
    - cloudflare exposes purge_cache (destructive), circleci exposes trigger_pipeline, sentry exposes resolve_issue (writes).
  </acceptance_criteria>
  <done>cloudflare/circleci/datadog/sentry/posthog vendored as hermetic metadata slices importable under tsx; PIN.md per-phase log updated, category complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Import the final sub-batch, regenerate the snapshot, re-assert scale + regression over the complete batch</name>
  <files>extension/catalog/recipe-index.generated.js, catalog/descriptors/_fixtures/intent-cases.json, catalog/descriptors/_fixtures/_provenance.json, tests/breadth-search-return.test.js, tests/capability-search-eval.test.js</files>
  <read_first>
    - scripts/import-opentabs-catalog.mjs (the 37-01 enumerateBatchApps + runImport -- run, do NOT edit)
    - scripts/package-extension.mjs + scripts/validate-extension.mjs (regen + byte-compare)
    - tests/capability-search-eval.test.js (the eval harness + the CGEN-04 cold-start asserts ~121-140)
    - tests/breadth-search-return.test.js (the per-op breadth assertion to extend)
    - tests/no-dead-entry.test.js (the seam-resolution invariant)
    - catalog/descriptors/_fixtures/intent-cases.json (the eval fixtures to grow)
  </read_first>
  <behavior>
    - `node --import tsx ./scripts/import-opentabs-catalog.mjs` emits opentabs__cloudflare__*, opentabs__circleci__*, opentabs__datadog__*, opentabs__sentry__*, opentabs__posthog__* (alongside all prior apps) WITHOUT clobbering any existing descriptor; the complete dev/productivity batch is now present.
    - `node scripts/package-extension.mjs` regenerates the snapshot; validate-extension reconciles (exit 0).
    - intent-cases.json gains read/write/destructive fixtures for the new apps; the eval keeps recall@5>=0.9 + wrong-invoke=0.000 (incl. the MED-03 create_* collision set from Plan 01).
    - The CGEN-04 cold-start asserts in capability-search-eval re-run at the grown batch size and pass (the serialized index + loadJSON+first-search stay within budget; params NOT indexed/stored).
    - breadth-search-return.test.js asserts every op of the complete batch returns with synonyms+class+backing.
  </behavior>
  <action>
    (a) Run `node --import tsx ./scripts/import-opentabs-catalog.mjs` to emit the five new apps via the Plan-01 machinery; confirm the new descriptors appear, existing descriptors are untouched, and the complete dev/productivity batch (linear/asana/clickup/jira/confluence/airtable/gitlab/bitbucket/vercel/netlify/cloudflare/circleci/datadog/sentry/posthog + the prior todoist) is present.

    (b) Run `node scripts/package-extension.mjs` to regenerate extension/catalog/recipe-index.generated.js (INV-01 unchanged shape); confirm `node scripts/validate-extension.mjs` exits 0.

    (c) Extend catalog/descriptors/_fixtures/intent-cases.json with intent fixtures for the new apps (at least one read + one write + the cloudflare.purge_cache destructive, e.g. "query datadog metrics" -> datadog.query_metrics; "purge the cloudflare cache" -> cloudflare.purge_cache; "trigger a circleci pipeline" -> circleci.trigger_pipeline) with expectedSlug aligned to the emitted real slugs. Extend tests/breadth-search-return.test.js to cover the complete batch.

    (d) Re-assert success criterion 4 over the COMPLETE batch: re-run `node scripts/verify-catalog-crosscheck.mjs` (exit 0 -- purge_cache destructive, trigger/resolve write, queries read), `node tests/no-dead-entry.test.js` (exit 0 -- every descriptor-only slug resolves to a non-null seam), and `node tests/capability-search-eval.test.js` (recall@5>=0.9, wrong-invoke=0.000, AND the CGEN-04 cold-start asserts pass at the grown batch size). If the grown index legitimately exceeds the smoke cold-start gate while the per-descriptor footprint stays flat (params not indexed), widen the assert's budget with a comment noting the grown batch count -- do NOT change the data layout (params stay schema-on-hit) and do NOT defer the full-corpus scale gate (that is Phase 43).

    Do NOT edit the importer, capability-search.js, or the gates (machinery frozen from Plan 01).
  </action>
  <verify>
    <automated>node --import tsx ./scripts/import-opentabs-catalog.mjs && node scripts/package-extension.mjs && npm run validate:extension && node tests/no-dead-entry.test.js && node tests/breadth-search-return.test.js && node tests/capability-search-eval.test.js</automated>
  </verify>
  <acceptance_criteria>
    - The five new apps' descriptors are emitted and inlined; validate:extension exits 0 (snapshot reconciled; classification gate + crosscheck + recipe-path guard green over the complete batch).
    - verify-catalog-crosscheck.mjs exits 0 (cloudflare.purge_cache destructive; circleci.trigger_pipeline / sentry.resolve_issue write; queries read).
    - no-dead-entry.test.js exits 0 over the complete batch.
    - capability-search-eval.test.js prints recall@5>=0.9, wrong-invoke=0.000, and its cold-start asserts pass at the grown batch size.
    - breadth-search-return.test.js passes covering every op of the complete dev/productivity batch.
  </acceptance_criteria>
  <done>The observability sub-batch is imported, the snapshot regenerated, and success criterion 4 re-asserted over the complete dev/productivity batch: crosscheck + no-dead-entry + eval green and the cold-start budget re-asserted at the grown size.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OpenTabs vendored metadata -> FSB descriptor corpus (build-time) | the final sub-batch metadata read by the importer |
| descriptor corpus -> search hit -> invoke (runtime) | observability write/destructive ops (purge_cache/trigger_pipeline/resolve_issue) must carry the correct side-effect class |
| grown index -> SW cold-start (runtime) | the larger serialized index must still restore + answer within budget on SW wake |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | Elevation of Privilege | the merge-time batch import of an unclassified origin | mitigate | the Plan-01 runImport() classifyGate runs over the new origins and aborts on any unclassified one (the five observability origins classify safe and pass) |
| T-37-06 | Elevation of Privilege | cloudflare.purge_cache / trigger_pipeline / resolve_issue under-stated as read -> ungated mutation under Auto | mitigate | the shared side-effect derivation classes purge -> destructive and trigger/resolve (or their POST method) -> write; verify-catalog-crosscheck.mjs fails the build on under-statement (asserted exit 0 in Task 2d) |
| T-37-04 | Tampering | a vendored slice emitting a forbidden field name into shipped params (Wall 1) | mitigate | the importer's recursive forbidden-field pre-scan (UNCHANGED) throws before emit; recipe-path-guard stays green via validate:extension |
| T-37-07 | Denial of Service | the grown serialized index blows the SW cold-start budget (slow/failed wake) | mitigate | the CGEN-04 cold-start asserts re-run at the grown batch size; params stay schema-on-hit (NOT indexed/stored) so the per-descriptor footprint stays flat; full-corpus scale gated in Phase 43 |
| T-37-SC | Tampering | npm/pip/cargo installs | accept | no new packages this plan (data-only vendoring); package-legitimacy gate N/A |
</threat_model>

<verification>
- `node --import tsx ./scripts/import-opentabs-catalog.mjs` emits the five new apps; the complete batch present; existing descriptors untouched.
- `npm run validate:extension` exits 0 (snapshot reconciled; classification gate + crosscheck + recipe-path guard green).
- `node tests/no-dead-entry.test.js` exits 0 over the complete batch.
- `node tests/breadth-search-return.test.js && node tests/capability-search-eval.test.js` pass (recall@5>=0.9, wrong-invoke=0.000, cold-start asserts green at the grown size).
</verification>

<success_criteria>
- BRDTH-01 (complete): the full dev/PM/cloud/observability category is imported + returned by search with synonyms + side-effect class + backing -- descriptor coverage of the least-sensitive category is complete.
- Success criterion 4: the crosscheck CI gate + the eval harness stay green on the grown corpus; the descriptor-only -> T3/T2 fallback is verified for the whole batch; the SW cold-start budget is re-asserted at the larger size (within the SCALE-01 target; full-corpus scale is Phase 43).
- Regression: validate:extension exits 0; the merge-time batch gate still aborts on an unclassified origin.
</success_criteria>

<output>
Create `.planning/phases/37-breadth-a-dev-productivity-least-sensitive/37-04-SUMMARY.md` when done.
</output>
