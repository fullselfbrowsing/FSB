---
phase: 58-providers-panel
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - extension/ui/providers-panel.js
  - extension/ui/control_panel.html
  - tests/providers-panel-logic.test.js
  - package.json
autonomous: true
requirements:
  - PROV-02
  - PROV-04
  - PROV-05
  - PROV-06
must_haves:
  truths:
    - "Provider settings and recommendation evidence are separate pure state domains: evidence can move one advisory recommendation but cannot mutate API or agent selection."
    - "Only the existing seven API ids can become modelProvider values, and only Claude Code, OpenCode, or Codex can become agentProviderId values."
    - "Recommendation is deterministic: live, installed, clicked, then xAI, with fixed agent tie order and historical connected evidence ignored."
    - "Billing definitions use fixed official HTTPS destinations and make subscription inclusion conditional rather than fabricated."
  artifacts:
    - path: "extension/ui/providers-panel.js"
      provides: "Pure closed provider definitions, settings normalization, recommendation, and status derivation"
      contains: "FSBProvidersPanel"
    - path: "tests/providers-panel-logic.test.js"
      provides: "Table-driven recommendation, migration, status, non-mutation, and billing contract proof"
    - path: "package.json"
      provides: "Permanent root-suite execution of the Phase 58 pure contract"
      contains: "providers-panel-logic.test.js"
  key_links:
    - from: "extension/ui/control_panel.html"
      to: "extension/ui/providers-panel.js"
      via: "classic script loads the helper before options.js"
      pattern: "providers-panel.js.*options.js"
    - from: "extension/ui/providers-panel.js"
      to: "extension/ai/universal-provider.js"
      via: "closed API allowlist keeps agent ids out of settings.modelProvider"
      pattern: "API_PROVIDER_IDS"
---

<objective>
Create the pure provider-domain contract before any visible UI wiring.

Purpose: Make kind separation, legacy migration, recommendation priority, status honesty, and billing definitions directly testable without booting the options page.
Output: A CSP-safe classic-script helper, exhaustive Node contract test, deterministic script ordering, and permanent root-suite wiring.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-providers-panel/58-CONTEXT.md
@.planning/phases/58-providers-panel/58-RESEARCH.md
@.planning/phases/58-providers-panel/58-VALIDATION.md
@.planning/phases/58-providers-panel/58-UI-SPEC.md

<interfaces>
Create this classic-script namespace and CommonJS export:

```javascript
globalThis.FSBProvidersPanel = {
  API_PROVIDER_IDS,
  AGENT_PROVIDER_IDS,
  PROVIDER_DEFINITIONS,
  isApiProvider,
  isAgentProvider,
  normalizeSettings,
  getRecommendation,
  getAgentStatus,
  getProviderDefinition
};
```

Normalized settings shape:

```javascript
{
  providerKind: 'api' | 'agent',
  modelProvider: 'xai' | 'gemini' | 'openai' | 'anthropic' | 'openrouter' | 'lmstudio' | 'custom',
  agentProviderId: '' | 'claude-code' | 'opencode' | 'codex'
}
```

Recommendation shape:

```javascript
{
  providerKind: 'api' | 'agent',
  providerId: string,
  reason: 'live' | 'installed' | 'clicked' | 'fallback'
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the closed provider settings, recommendation, status, and billing contracts</name>
  <files>extension/ui/providers-panel.js, tests/providers-panel-logic.test.js</files>
  <read_first>
- .planning/phases/58-providers-panel/58-CONTEXT.md
- .planning/phases/58-providers-panel/58-UI-SPEC.md
- .planning/phases/58-providers-panel/58-RESEARCH.md
- extension/utils/mcp-client-aliases.js
- extension/utils/mcp-agent-providers.js
- tests/mcp-client-merged-view.test.js
- tests/mcp-agent-providers-storage.test.js
  </read_first>
  <action>
Create `extension/ui/providers-panel.js` as a strict-mode IIFE that accepts `globalThis`/`self`/`this`, assigns one frozen `FSBProvidersPanel` namespace, and also assigns `module.exports` when CommonJS exists. It must have no DOM, Chrome API, storage, timer, network, or dynamic-code access.

Define frozen ordered arrays exactly:

```javascript
API_PROVIDER_IDS = ['xai', 'gemini', 'openai', 'anthropic', 'openrouter', 'lmstudio', 'custom'];
AGENT_PROVIDER_IDS = ['claude-code', 'opencode', 'codex'];
```

Define frozen provider records for all ten ids with `id`, `providerKind`, and display name. Agent records additionally carry these exact official links and scoped billing text from the approved UI-SPEC:

- Claude Code: `https://claude.com/pricing`; usage follows the signed-in Claude plan or API configuration.
- OpenCode: `https://opencode.ai/docs/providers/` plus optional Zen URL `https://opencode.ai/docs/zen/`; charges may follow Zen or the configured provider.
- Codex: `https://help.openai.com/en/articles/20001106-codex-rate-card-2`; usage/credits/charges follow the signed-in OpenAI plan or API configuration.

Every agent definition must say FSB does not need the CLI credential. Do not encode a blanket `included`, `free`, `unlimited`, dollar amount, or inferred auth state.

Implement own-allowlist `isApiProvider(id)` and `isAgentProvider(id)` checks. Implement `normalizeSettings(settings)` without mutating input: retain a valid API `modelProvider`, otherwise xAI; use agent kind only when both `providerKind === 'agent'` and `agentProviderId` is allowlisted; use API kind for valid API kind, missing/invalid kind, or invalid agent id; preserve a valid latent agent id even while API kind is selected, otherwise `''`. Evidence is not an input.

Implement `getRecommendation(clients)` without mutating or spreading the untrusted map. For each tier scan only the fixed agent order: row has a non-null object `live`; row has `installed.detected === true`; row has a non-null object `clicked`; otherwise return API xAI fallback. Ignore `connected`, timestamps, saved selection, raw entries, unsupported keys, and object insertion order. Always return one fresh object.

Implement `getAgentStatus(row)` returning booleans `live`, `installed`, `seenBefore`, `clicked`, one primary label, and `checkedAt`. `seenBefore` is true only when connected evidence exists without live; it must never produce `Connected now`. `checkedAt` is the finite numeric `row.installed.checkedAt` when present and otherwise `null`; do not substitute clicked/connected/live timestamps. Auth is always `Not reported` in this phase. A missing/malformed row returns Not installed/status-safe defaults.

Create `tests/providers-panel-logic.test.js` with direct Node assertions. Cover exact arrays and frozen definitions; all valid/invalid migrations; preservation of latent selections; input/result non-mutation; each recommendation tier; same-tier fixed ties; historical-connected-only fallback; installed false; raw/unknown/prototype-like keys; reordered objects; malformed rows; one result every time; status combinations; finite/malformed/missing installed `checkedAt`; and exact HTTPS billing URLs/copy. Assert the helper source has no `chrome.`, `document`, `window`, `fetch`, `eval`, or timer access.

Run `npm test` after the focused test at this first extension implementation gate. Provision missing ignored dependencies rather than treating them as product failures; do not skip, delete, or weaken an existing test.
  </action>
  <acceptance_criteria>
- `extension/ui/providers-panel.js` exposes one frozen `FSBProvidersPanel` global and a CommonJS export with the eight interface members listed above.
- `API_PROVIDER_IDS` contains exactly seven existing BYOK ids in current order; `AGENT_PROVIDER_IDS` contains exactly `claude-code`, `opencode`, `codex` in tie order.
- `normalizeSettings({ modelProvider: 'anthropic' })` returns API/Anthropic, while invalid or missing API values return API/xAI; no evidence argument exists.
- Historical `connected` without `live` cannot win recommendation, and unknown/raw/prototype-like keys cannot be selected or recommended.
- `getAgentStatus` returns only a finite installed `checkedAt` or `null`; malformed and unrelated timestamps never surface as checked time.
- Agent definitions contain the four exact official HTTPS destinations and no blanket subscription/zero-cost promise.
- `node tests/providers-panel-logic.test.js` and `npm test` exit 0.
  </acceptance_criteria>
  <verify>
    <automated>node tests/providers-panel-logic.test.js && npm test</automated>
  </verify>
  <done>The closed provider domain is deterministic, non-mutating, billing-honest, and directly regression-tested.</done>
</task>

<task type="auto">
  <name>Task 2: Load and permanently gate the provider helper without changing runtime behavior</name>
  <files>extension/ui/control_panel.html, tests/providers-panel-logic.test.js, package.json</files>
  <read_first>
- extension/ui/control_panel.html
- extension/ui/providers-panel.js
- package.json
- tests/extension-content-script-files-completeness.test.js
- tests/model-discovery-ui.test.js
- tests/agent-sunset-control-panel.test.js
  </read_first>
  <action>
Add `<script src="providers-panel.js"></script>` at the bottom of `control_panel.html` immediately before `<script src="options.js"></script>`. Do not move, remove, or duplicate any other script and do not add inline JavaScript under MV3 CSP.

Extend `tests/providers-panel-logic.test.js` with a source-order assertion that both script tokens occur exactly once and `providers-panel.js` precedes `options.js`. Add `node tests/providers-panel-logic.test.js` to root `package.json`'s serial `test` command immediately after `node tests/mcp-client-identity-integration.test.js` and before the existing turn/provider tests. Preserve every existing command and its relative order otherwise.

Run the new test plus existing options/model/sunset contracts and then `npm test`. This task must not add provider rows, settings keys, rendering, CSS, storage calls, or change the current visible API Configuration surface; Plan 58-02 owns visible behavior.
  </action>
  <acceptance_criteria>
- `control_panel.html` loads `providers-panel.js` exactly once and before `options.js`, with no inline script added.
- Root `npm test` contains exactly one `node tests/providers-panel-logic.test.js` and drops no pre-existing test command.
- The current seven-value `#modelProvider` and model combobox markup remain present and unchanged in behavior.
- `node tests/providers-panel-logic.test.js`, `node tests/model-discovery-ui.test.js`, `node tests/model-combobox-ui.test.js`, and `node tests/agent-sunset-control-panel.test.js` all exit 0.
- `npm test` exits 0 at this extension-touching task gate.
  </acceptance_criteria>
  <verify>
    <automated>node tests/providers-panel-logic.test.js && node tests/model-discovery-ui.test.js && node tests/model-combobox-ui.test.js && node tests/agent-sunset-control-panel.test.js && npm test</automated>
  </verify>
  <done>The pure helper is available before options boot and permanently enforced without yet altering visible provider behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 57 merged client map → provider helper | Durable/live/raw evidence is observational and may be malformed or attacker-named. |
| Persisted settings → provider helper | Legacy or manually edited storage may contain unsupported provider values. |
| Static provider definitions → external vendor pages | Billing text and destinations affect user financial understanding. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01 | Spoofing | recommendation helper | mitigate | Scan only closed ids in fixed order; raw/unknown/`__proto__` keys never become candidates; Task 1 table-tests all cases. |
| T-58-02 | Tampering | recommendation vs selection | mitigate | Pure recommendation has no settings input/output and returns a fresh object; non-mutation tests freeze inputs. |
| T-58-03 | Elevation of Privilege | settings normalization | mitigate | Closed API/agent allowlists and invalid-agent fail-closed migration keep agent ids out of `modelProvider`. |
| T-58-04 | Information Integrity | billing definitions | mitigate | Exact provider-specific qualified copy; subscription label is not encoded as unconditional state. |
| T-58-05 | Spoofing | external destinations | mitigate | Frozen exact HTTPS official URLs asserted by tests; no runtime URL construction. |
</threat_model>

<verification>
1. `node tests/providers-panel-logic.test.js`
2. `node tests/model-discovery-ui.test.js`
3. `node tests/model-combobox-ui.test.js`
4. `node tests/agent-sunset-control-panel.test.js`
5. Confirm `git diff --check` and that only the four declared files changed for this plan.
</verification>

<success_criteria>
- Pure provider logic is fully testable without DOM or Chrome APIs.
- Recommendation and selected settings have no mutation path between them.
- The API/agent id domains are closed and preserved separately.
- Billing destinations/copy are frozen to the approved honest contract.
- The helper loads under MV3 CSP and the root suite runs its contract permanently.
</success_criteria>

<output>
After completion, create `.planning/phases/58-providers-panel/58-01-SUMMARY.md`.
</output>
