---
phase: 58-providers-panel
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - extension/ui/control_panel.html
  - extension/ui/options.css
  - extension/ui/options.js
  - tests/providers-panel-ui.test.js
  - tests/model-discovery-ui.test.js
  - tests/lattice-provider-bridge-smoke.test.js
  - package.json
autonomous: true
requirements:
  - PROV-01
  - PROV-02
  - PROV-03
  - PROV-04
must_haves:
  truths:
    - "The control panel has one canonical Providers route while legacy #api-config bookmarks normalize to #providers."
    - "Ten native radio rows express explicit api/agent kinds; selected state is user intent and participates in the existing Save/Discard flow."
    - "Agent selection removes model/key/URL/format controls from rendering and keyboard flow, while API selection restores the untouched BYOK configuration."
    - "Loading, saving, discarding, and switching kinds preserve modelProvider as one of the seven API ids and preserve the latent agent selection separately."
  artifacts:
    - path: "extension/ui/control_panel.html"
      provides: "Canonical Providers route, accessible grouped provider radio roster, and separate API/agent detail containers"
      contains: "id=\"providers\""
    - path: "extension/ui/options.css"
      provides: "Theme-aware selected/recommended/status/detail/responsive provider styles"
      contains: ".provider-row"
    - path: "extension/ui/options.js"
      provides: "Hash normalization and kind-aware in-form/load/save/discard behavior"
      contains: "renderProviderKind"
    - path: "tests/providers-panel-ui.test.js"
      provides: "Static and VM contracts for markup, CSS, hashes, selection, visibility, and settings parity"
  key_links:
    - from: "extension/ui/options.js"
      to: "extension/ui/providers-panel.js"
      via: "normalizeSettings validates storage before any radio/model control is updated"
      pattern: "FSBProvidersPanel.*normalizeSettings"
    - from: "provider API radio"
      to: "#modelProvider"
      via: "API-only selection mirrors the id and reuses the existing change/discovery path"
      pattern: "modelProvider"
    - from: "saveSettings"
      to: "extension/ai/universal-provider.js"
      via: "saved modelProvider remains API-only while providerKind/agentProviderId are sibling keys"
      pattern: "providerKind.*agentProviderId"
---

<objective>
Turn the API Configuration surface into an accessible Providers chooser with kind-aware settings that preserve BYOK behavior.

Purpose: Land the visible route, roster, selection semantics, conditional panels, and storage migration before wiring asynchronous Phase 57 evidence.
Output: Providers markup/CSS, canonical+legacy routing, radio/load/save/discard behavior, and focused UI/compatibility tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-providers-panel/58-CONTEXT.md
@.planning/phases/58-providers-panel/58-RESEARCH.md
@.planning/phases/58-providers-panel/58-VALIDATION.md
@.planning/phases/58-providers-panel/58-UI-SPEC.md
@.planning/phases/58-providers-panel/58-01-SUMMARY.md

<interfaces>
The hidden compatibility select keeps exactly these values:

```html
<select id="modelProvider" class="form-select provider-roster__native" tabindex="-1" aria-hidden="true">
  <!-- xai, gemini, openai, anthropic, openrouter, lmstudio, custom -->
</select>
```

In-form provider state remains separate from recommendation:

```javascript
providerPanelState = {
  providerKind: 'api',
  agentProviderId: '',
  recommendation: { providerKind: 'api', providerId: 'xai', reason: 'fallback' },
  clients: Object.create(null),
  evidenceStatus: 'idle'
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the canonical Providers route and accessible static roster using existing design tokens</name>
  <files>extension/ui/control_panel.html, extension/ui/options.css, extension/ui/options.js, tests/providers-panel-ui.test.js</files>
  <read_first>
- .planning/phases/58-providers-panel/58-UI-SPEC.md
- extension/shared/fsb-ui-core.css
- extension/ui/control_panel.html
- extension/ui/options.css
- extension/ui/options.js
- tests/model-discovery-ui.test.js
- tests/agent-cap-ui.test.js
- tests/agent-sunset-control-panel.test.js
  </read_first>
  <action>
Rename the sidebar target/label and content section to canonical `providers` / `Providers`. Use the section description exactly from UI-SPEC: `Choose how FSB runs AI tasks. API providers use keys stored locally; agent CLIs use their existing local sign-in.` Replace the visible provider select area with one named `<fieldset id="providerRoster">` and visible legend `Choose a provider`. Add a secondary `<button type="button" id="refreshProviderStatusBtn">` labeled `Refresh status` with a decorative `aria-hidden="true"` sync icon; its pending contract is visible text `Refreshing…`, disabled state, and `aria-busy="true"`. Inside, render static group headings in fixed order Agent CLIs then API providers and ten native radios sharing `name="fsbProviderSelection"`. Every radio/label carries `data-provider-kind="agent|api"` and the exact closed id. Include separate hidden descendants for selection-independent `Recommended` and evidence badges, but do not populate evidence yet.

Keep the existing `#modelProvider` select with all seven option values in current order. Add class `provider-roster__native`, `tabindex="-1"`, and `aria-hidden="true"`; do not disable it. Wrap the existing model combobox, discovery status, refresh-model action, seven key/server/endpoint groups, and connection-test controls in `#apiProviderDetails`. Add a hidden `#agentEvidenceEmptyState` with exact heading `No agent CLI detected` and body `You can select an agent now and follow its setup guide, or continue with an API provider.` Add a hidden `<details id="otherMcpClientsDisclosure">` with summary slot `Other MCP clients (0)` and a text-only list; it lives after selectable groups and has no radio, Recommended badge, or setup action. Add `#agentProviderDetails` with `hidden`, `aria-live="polite"`, static labeled slots for Installation, Connection, Account, Setup, Usage (Tokens/Turns/Duration), billing copy/link, and an empty state `No delegated runs yet`; Phase 58-03 fills dynamic evidence.

In `options.js`, create `normalizeSectionId(sectionId)` that maps only `api-config` to `providers`. Call it at the top of `switchSection()` and in `initializeSections()` before `document.getElementById`; `history.replaceState` must always write `#providers`. Preserve the special Advanced/Consent routing. Do not create a hidden legacy section id.

Add UI-SPEC-derived styles to `options.css` using existing variables only. Implement two-column groups at `min-width:900px`, one column below; 64px minimum rows, 16px padding, 12px internal gap, native focus ring, selected 2px primary border/background, informational Recommended badge distinct from selection, semantic text badges, full-width compact actions, three-column usage above 640px and stacked below, dark-token inheritance, long-text wrapping, no horizontal overflow, and reduced-motion removal of spin/transform. All new spacing is 4/8/16/24/32/48/64 except inherited shell tokens.

Create `tests/providers-panel-ui.test.js` with static source assertions for canonical nav/section/title/description; the Refresh status button, decorative icon, `Refreshing…`/disabled/`aria-busy` contract hooks; exactly ten same-name radios and exact kind/id order; retained seven-option hidden `#modelProvider`; exact no-agent empty copy; collapsed Other MCP clients disclosure with no selection/recommendation/setup controls; separate API/agent details; no interactive element nested in radio labels; required usage labels/em dashes; CSS token/state/responsive/dark/reduced-motion rules; one helper script before options; and hash alias source behavior. Do not assert dynamic evidence behavior until Task 2/Plan 3.

Run `npm test` after the focused source/DOM tests at this extension-touching task gate.
  </action>
  <acceptance_criteria>
- `control_panel.html` contains `data-section="providers"`, `id="providers"`, visible `Providers`, and no canonical content/nav id or target of `api-config`.
- `normalizeSectionId('api-config')` yields `providers`, and initial `#api-config` navigation ends with canonical `#providers`.
- Exactly ten radios share `name="fsbProviderSelection"`; their agent/API ids and order match `FSBProvidersPanel` definitions.
- `#modelProvider` still contains exactly xai/gemini/openai/anthropic/openrouter/lmstudio/custom and is hidden from interaction rather than removed/disabled.
- Agent details contain Installation, Connection, Account, Tokens, Turns, Duration, and `No delegated runs yet`; API details contain every existing model/key/server control.
- Markup contains `#refreshProviderStatusBtn`, exact `Refresh status`/`Refreshing…` hooks, disabled/`aria-busy` behavior, the exact no-agent heading/body, and a collapsed `Other MCP clients (0)` disclosure that cannot select or recommend a client.
- New CSS uses existing theme variables, has 900px/640px and reduced-motion rules, and visibly distinguishes selected from Recommended.
- `node tests/providers-panel-ui.test.js`, `node tests/agent-sunset-control-panel.test.js`, and `npm test` exit 0.
  </acceptance_criteria>
  <verify>
    <automated>node tests/providers-panel-ui.test.js && node tests/agent-sunset-control-panel.test.js && npm test</automated>
  </verify>
  <done>The old route redirects canonically, and the complete accessible chooser/detail shell exists without changing stored settings yet.</done>
</task>

<task type="auto">
  <name>Task 2: Wire kind-aware selection, migration, load/save/discard, and BYOK-preserving visibility</name>
  <files>extension/ui/options.js, tests/providers-panel-ui.test.js, tests/model-discovery-ui.test.js, tests/lattice-provider-bridge-smoke.test.js, package.json</files>
  <read_first>
- extension/ui/providers-panel.js
- extension/ui/options.js
- extension/ui/control_panel.html
- tests/providers-panel-ui.test.js
- tests/model-discovery-ui.test.js
- tests/model-combobox-ui.test.js
- tests/lattice-provider-bridge-smoke.test.js
- extension/ai/universal-provider.js
  </read_first>
  <action>
Add `providerKind: 'api'` and `agentProviderId: ''` to `defaultSettings`, then create `providerPanelState` with the interface shape above. Cache all new controls. Extend `initFsbSelects()`'s existing native-select skip condition to skip `.provider-roster__native`, preventing a second visual provider dropdown.

Implement these DOM-level functions with no recommendation side effects:

- `setProviderSelection(kind, id, { markDirty = true } = {})`: validate through the helper. For API, assign only a valid API id to `elements.modelProvider.value`, update `providerKind`, and invoke the existing model discovery/options plus `updateApiKeyVisibility` path. For agent, assign only `agentProviderId`; never assign its id to `modelProvider`. Update checked radio and call `renderProviderKind`. Mark unsaved only for user events.
- `renderProviderSelection()`: check exactly the in-form radio and apply selected class/`aria-checked`; it must not inspect recommendation.
- `renderProviderKind()`: set `hidden` on API versus agent details, so inactive controls leave the tab order. For API, retain the existing chosen key/server group and model combobox. For agent, do not run model discovery, key visibility, or `checkApiConnection`.

Wire one delegated radio `change` handler. A row click/Space/arrow-native radio change updates in-form state and the existing Save bar only; do not call storage directly.

Inside `loadSettings()`, call `FSBProvidersPanel.normalizeSettings({ ...defaultSettings, ...data })` before updating controls. Restore the valid API `modelProvider` even while agent kind is active; restore the latent valid `agentProviderId`; apply the saved kind with `{markDirty:false}`. The delayed `checkApiConnection()` call must run only when the normalized kind is API. Preserve the current asynchronous model-discovery and model-name restoration order.

Inside `saveSettings()`, add sibling keys `providerKind` and `agentProviderId` from validated in-form state. Keep `modelProvider: elements.modelProvider?.value || 'xai'` API-only and preserve all current trim calls/fields. The storage callback, Save bar, toast, and log remain unchanged; only invoke API connection testing after save when `providerKind === 'api'`. `discardChanges()` continues to call `loadSettings()` and must restore both kinds without recommendation input.

Expand `tests/providers-panel-ui.test.js` with a focused VM/handcrafted-DOM harness for legacy migration; invalid settings fail-closed; user API and agent changes; save/discard round trips; inactive-kind field preservation; agent hidden API tab flow; API restoration; no storage write until Save; and recommendation placeholder changes not altering selection. Add source assertions that agent ids do not appear in the `modelProvider` option list or API getter/discovery maps.

Update `tests/model-discovery-ui.test.js` and `tests/lattice-provider-bridge-smoke.test.js` only where exact source/markup pins require the new hidden select wrapper or kind guard. Preserve the existing `saveSettings` minimum trim count, lattice test-connection bounce, DOM input reads, and load-order assertions; do not broaden or delete them. Add `node tests/providers-panel-ui.test.js` to root `package.json` immediately after the logic test without dropping any command. Run `npm test` after the focused cluster at this extension-touching task gate.
  </action>
  <acceptance_criteria>
- `defaultSettings` and saved settings contain closed `providerKind` plus separate `agentProviderId`, while `modelProvider` remains one of exactly seven API ids.
- Missing kind with saved Anthropic migrates to API/Anthropic; invalid kind/id fails closed to a valid API selection and never uses evidence.
- Agent selection hides `#apiProviderDetails`, shows `#agentProviderDetails`, marks unsaved, and performs no model discovery/key visibility/connection test.
- API↔agent switching and Save/Discard preserve the inactive model, keys/endpoints, and latent agent id.
- `initFsbSelects()` skips `.provider-roster__native`; no replacement dropdown is created.
- Existing lattice `saveSettings` trim/test-connection/load-order source pins remain meaningful and green.
- Root `npm test` permanently includes both new Phase 58 tests.
- `npm test` exits 0 at this extension-touching task gate.
- Focused UI, discovery, combobox, provider-bridge, and universal-provider tests all exit 0.
  </acceptance_criteria>
  <verify>
    <automated>node tests/providers-panel-ui.test.js && node tests/providers-panel-logic.test.js && node tests/model-discovery-ui.test.js && node tests/model-combobox-ui.test.js && node tests/lattice-provider-bridge-smoke.test.js && node tests/universal-provider-lmstudio.test.js && npm test</automated>
  </verify>
  <done>The Providers chooser now persists explicit kind/agent intent through the existing Save flow while every BYOK provider/model/key path remains intact and isolated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Persisted local settings → options DOM | Legacy, invalid, or manually edited values select visible controls. |
| Provider radios → hidden API compatibility controls | User intent crosses from a new kind-aware chooser into legacy API discovery/save code. |
| Legacy hash → canonical section router | External/bookmarked input selects a control-panel section. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-02 | Tampering | selection render | mitigate | Selection functions never read recommendation; tests mutate recommendation and assert checked radio/settings unchanged. |
| T-58-03 | Elevation of Privilege | radio→modelProvider bridge | mitigate | Closed helper checks, separate ids, invalid-agent fail-closed migration, source tests forbidding agent ids in API maps. |
| T-58-06 | Denial of Service | malformed settings/hash | mitigate | Normalize before DOM writes and map one legacy hash only; unsupported values fall back without throwing. |
</threat_model>

<verification>
1. `node tests/providers-panel-logic.test.js`
2. `node tests/providers-panel-ui.test.js`
3. `node tests/model-discovery-ui.test.js`
4. `node tests/model-combobox-ui.test.js`
5. `node tests/lattice-provider-bridge-smoke.test.js`
6. `node tests/universal-provider-lmstudio.test.js`
7. `node tests/agent-sunset-control-panel.test.js`
8. Confirm `git diff --check` and inspect that no agent id can reach `modelProvider` or UniversalProvider.
</verification>

<success_criteria>
- Providers is the sole canonical route and old bookmarks redirect correctly.
- All ten providers are accessible radio choices with explicit kind.
- Agent/API panels are mutually exclusive in rendering and keyboard flow.
- Save/Discard and legacy migration preserve both configurations independently.
- Existing model discovery, connection test, and UniversalProvider behavior stay green.
</success_criteria>

<output>
After completion, create `.planning/phases/58-providers-panel/58-02-SUMMARY.md`.
</output>
