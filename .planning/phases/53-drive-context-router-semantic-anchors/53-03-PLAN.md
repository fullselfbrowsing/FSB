---
phase: 53-drive-context-router-semantic-anchors
plan: "03"
type: execute
wave: 2
depends_on:
  - "53-01"
  - "53-02"
files_modified:
  - extension/content/skopeo-shell.js
  - tests/skopeo-shell-contract.test.js
  - tests/skopeo-accessibility.test.js
autonomous: true
requirements:
  - HUD-06
  - HUD-09
must_haves:
  truths:
    - "Recognized, uncertain, unsupported, no-target, and withdrawn-target states use only the approved closed copy and never expose page labels or stable IDs."
    - "A validated target shows exactly one pointer-transparent, nonfocusable, aria-hidden 8×8 mark beside its current safe rectangle."
    - "Uncertain identity or geometry synchronously removes every anchor-dependent node from rendering and accessibility, with no gate, focus move, or stale-position residue."
    - "Ambient/context/anchor transitions preserve host focus and interaction and announce at most one final semantic transition, not geometry churn."
  artifacts:
    - path: "extension/content/skopeo-shell.js"
      provides: "Typed context projection, semantic mark geometry, synchronous withdrawal, closed copy, accessibility, and shell-owned projection snapshot"
      exports: ["createShell"]
    - path: "tests/skopeo-shell-contract.test.js"
      provides: "Exact copy, mark geometry/scarcity, hostile-data, scope disposal, focus, and teardown proof"
    - path: "tests/skopeo-accessibility.test.js"
      provides: "Region/live semantics, no focus/tab/alert/gate, reduced-motion, and forced-colors proof"
  key_links:
    - from: "extension/content/skopeo-shell.js"
      to: "extension/content/skopeo-context-router.js"
      via: "closed status/context/reason model consumed without page-derived copy"
      pattern: "projectContext"
    - from: "extension/content/skopeo-shell.js"
      to: "extension/content/skopeo-anchor-registry.js"
      via: "frozen validated targetRect/authority projection and synchronous withdrawal callback"
      pattern: "commitSemanticAnchor|withdrawSemanticAnchor"
---

<objective>
Extend the single Phase 52 Shadow shell with the exact Phase 53 fail-quiet projection and one semantically certified anchor mark—without turning rendering into identity authority.

Purpose: Make HUD-06 and HUD-09 visible, accessible, fail-closed behaviors while preserving Phase 52's one-shell, collision, focus, resource, and teardown contracts.
Output: Narrow public projection methods plus deterministic shell/accessibility coverage for all approved copy and anchor states.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/53-drive-context-router-semantic-anchors/53-CONTEXT.md
@.planning/phases/53-drive-context-router-semantic-anchors/53-RESEARCH.md
@.planning/phases/53-drive-context-router-semantic-anchors/53-UI-SPEC.md
@.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-UI-SPEC.md
@.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-07-SUMMARY.md
@.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-09-SUMMARY.md
@extension/content/skopeo-shell.js
@tests/skopeo-shell-contract.test.js
@tests/skopeo-accessibility.test.js

<interfaces>
Add these instance methods without weakening controlled-fixture `render()`:

```javascript
shell.projectContext({ status, contextKind?, contextEpoch, reason? });
shell.commitSemanticAnchor({
  generation,
  contextEpoch,
  semanticIdentity,
  bindingEpoch,
  targetRect
});
shell.withdrawSemanticAnchor({ contextEpoch, bindingEpoch, reason });
shell.getProjectionSnapshot();
```

`projectContext` accepts only the router's closed values. `commitSemanticAnchor` consumes an already validated registry projection but independently checks shell generation, current context epoch, binding monotonicity, finite target geometry, viewport inset, and host-control/focus/scrollbar clearance before creating a mark. Semantic IDs remain private snapshot metadata and never enter DOM attributes, visible copy, accessible names, or logs.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin the fail-quiet, mark, accessibility, and host-integrity UI contract</name>
  <files>tests/skopeo-shell-contract.test.js, tests/skopeo-accessibility.test.js</files>
  <read_first>
    - .planning/phases/53-drive-context-router-semantic-anchors/53-UI-SPEC.md (all exact copy, 8×8 placement, movement, roles, and QA checkpoints)
    - .planning/phases/53-drive-context-router-semantic-anchors/53-VALIDATION.md (53-W0-04 and T-53-03/T-53-06 proofs)
    - .planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-UI-SPEC.md (inherited tokens, focus, live region, collision, teardown)
    - extension/content/skopeo-shell.js (current `COPY`, render scopes, placement, live cadence, destroy)
    - tests/skopeo-shell-contract.test.js (existing production DOM/clock/resource harness)
    - tests/skopeo-accessibility.test.js (existing shared production harness and `assertAbsent` pattern)
  </read_first>
  <behavior>
    - The four recognized kinds show exactly `Skopeo · Corpus context`, `Skopeo · Vendor folder`, `Skopeo · Agreement view`, or `Skopeo · Focused ask` and their approved polite announcements.
    - Uncertain, unsupported, withdrawn-target, and recognized/no-target show exactly the UI-SPEC copy; hostile names/IDs/HTML/selectors never appear.
    - Fail-quiet removes mark/chip/halo/ghost/focused/gate nodes synchronously and leaves no alert/dialog/modal/focus trap or focus write.
    - A certified target renders one 8×8 mark using top-right, top-left, bottom-right, bottom-left with 8px clearance and 16px viewport inset; unsafe candidates render none.
    - Same-identity geometry moves use zero positional interpolation; withdrawal has no exit animation; reduced motion makes fresh opacity entry 0ms.
    - The mark has `pointer-events:none`, no Tab stop, `aria-hidden=true`; region names switch between `Skopeo ambient HUD` and `Skopeo anchored HUD`; one polite atomic live region deduplicates geometry churn.
  </behavior>
  <action>
Extend the existing test harnesses instead of creating a second shell oracle. Add exact visible/live-copy tables for all router states and recognized context kinds. Feed hostile fields (`name`, `label`, `selector`, `html`, IDs) into models and assert none reaches text nodes, ARIA attributes, logs/test hooks beyond closed metadata, or markup. Record focus/host snapshots before every projection and deep-compare after context, anchor, withdrawal, and teardown transitions.

Add rectangles for every placement candidate, focused-host-control and scrollbar conflicts, 420px/200%-equivalent geometry, same-identity reposition, detached/unsafe withdrawal, stale context/binding epochs, and second-mark replacement. Assert one 8×8 node maximum, exact class/attributes/styles, no leader/chip/halo/ghost/focused/gate, synchronous DOM removal before any live-region timer drains, and exact eleven-key resource plateau/zero. Extend CSS source assertions for forced colors, 120ms opacity-only fresh entry, no positional transition on the mark, and reduced-motion 0ms. Keep Phase 52 controlled-fixture tests byte-behaviorally valid.
  </action>
  <verify>
    <automated>node --check tests/skopeo-shell-contract.test.js &amp;&amp; node --check tests/skopeo-accessibility.test.js &amp;&amp; ! node tests/skopeo-shell-contract.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Both tests contain every exact Phase 53 visible status and polite announcement from `53-UI-SPEC.md`.
    - Contract assertions check an 8×8 mark, 8px target clearance, 16px viewport inset, all four candidate orders, and zero marks when unsafe.
    - Accessibility assertions check `aria-hidden`, no `tabindex`/button role, region-name switching, polite/atomic live region, no alert/dialog/modal, and no focus change.
    - Tests assert withdrawn nodes are removed immediately, not opacity-hidden, and no page-derived label/ID appears in UI.
    - The new tests fail against the pre-Phase 53 shell because the declared projection methods are missing.
  </acceptance_criteria>
  <done>The UI contract is red for the right reasons and directly encodes every user-visible HUD-06/HUD-09 constraint.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement closed ambient projection and one revocable semantic mark</name>
  <files>extension/content/skopeo-shell.js, tests/skopeo-shell-contract.test.js, tests/skopeo-accessibility.test.js</files>
  <read_first>
    - tests/skopeo-shell-contract.test.js (Task 1's exact DOM/geometry/resource contract)
    - tests/skopeo-accessibility.test.js (Task 1's semantic/focus/preference contract)
    - extension/content/skopeo-shell.js (`COPY`, `SHELL_CSS`, `_buildSurfaceScope`, `_disposeSurfaceScope`, `_candidateIsSafe`, `_announce`, `destroy`)
    - extension/content/skopeo-context-router.js (closed statuses/kinds/reasons from Plan 01)
    - extension/content/skopeo-anchor-registry.js (validated projection fields from Plan 02)
    - .planning/phases/53-drive-context-router-semantic-anchors/53-UI-SPEC.md (approved copy and interaction source of truth)
  </read_first>
  <action>
Add frozen Phase 53 copy maps exactly as approved; never accept caller-provided visible or announcement strings. Track `_projectionContextEpoch`, `_projectionBindingEpoch`, `_projectionState`, `_semanticAnchorScope`, and a closed `_projectionSnapshot`. `projectContext` validates exact own keys and positive epoch, rejects lower epochs, synchronously calls `withdrawSemanticAnchor` before applying uncertain/unsupported context, updates only the existing lens label/region name/live region, and maps recognized/fail-quiet/no-target status through runtime-owned copy. It must never invoke controlled-fixture `render`, Focused, Interstitial, or Gate behavior.

Implement an independent anchor surface scope under the one shell so the Ambient lens/rail remain intact. `commitSemanticAnchor` accepts only exact own keys and a projection whose generation equals `this.generation`, context epoch equals the current projection, and binding epoch is a positive monotonic value. Re-normalize a finite nonzero target rect immediately, derive an 8×8 mark rectangle in the exact four-corner order at 8px clearance, require the full mark inside the 16px viewport inset and outside target/required host controls/focused element/scrollbar zone, then create exactly one `span.skopeo-semantic-anchor` with `data-skopeo-primitive="anchor"`, `aria-hidden="true"`, pointer-none, nonfocusable geometry. Store identity only inside the frozen snapshot; never set it as text/data/ARIA.

`withdrawSemanticAnchor` must invalidate binding authority first, dispose the anchor scope synchronously, remove the mark from DOM/accessibility, and apply the exact target fail-quiet copy only when requested by the runtime. Same-identity safe reposition mutates top/left immediately with zero position/transform transition and no live announcement. Fresh validated entry may use 120ms opacity only; reduced motion sets it to 0ms; invalidation has no exit animation. Deduplicate announcements by final semantic transition key, not mutation/scroll/resize/zoom. Extend `destroy()` to dispose anchor scope/snapshot before root removal and preserve exact zero. Do not alter host nodes, focus, selection, scrolling, classes, styles, attributes, or event propagation.
  </action>
  <verify>
    <automated>node tests/skopeo-shell-contract.test.js &amp;&amp; node tests/skopeo-accessibility.test.js &amp;&amp; node tests/helpers/skopeo-resource-ledger.js --self-test &amp;&amp; node --check extension/content/skopeo-shell.js</automated>
  </verify>
  <acceptance_criteria>
    - `extension/content/skopeo-shell.js` exposes all four instance methods in `<interfaces>` and uses only the exact approved Phase 53 copy.
    - `rg -n "innerHTML|insertAdjacentHTML|role.*alert|aria-modal.*true" extension/content/skopeo-shell.js` finds no new Phase 53 sink/modal path; existing controlled-fixture gate remains isolated and its tests pass.
    - The semantic mark is exactly 8×8, pointer-transparent, nonfocusable, aria-hidden, limited to one, and contains no semantic ID/name/label.
    - Context/binding epoch regression, hostile fields, all placement conflicts, same-identity movement, withdrawal timing, announcement dedupe, focus/host integrity, preference CSS, resource plateau, and exact-zero teardown assertions pass.
    - Existing Phase 52 controlled Ambient→Anchored→Focused→Interstitial, collision, focus restoration, and teardown tests remain green.
  </acceptance_criteria>
  <done>The one shared shell visibly distinguishes recognized/fail-quiet states and displays only a currently certified semantic mark with no wrong-target residue or focus/host side effect.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Router model → visible/accessible copy | Status/kind/reason metadata is accepted only through closed maps; page-controlled strings cannot become UI. |
| Registry projection → mark geometry | Identity remains nonvisual metadata; target rectangles and authority fields are revalidated before DOM commit. |
| Shadow shell → host page | Fixed pointer-none UI may read rectangles but cannot mutate, focus, intercept, or obscure required host controls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-03-01 | Spoofing | context/anchor projection | mitigate | Exact status/kind/reason/authority schemas and epoch monotonicity; no caller copy or stable ID in UI. |
| T-53-03-02 | Tampering | stale/wrong mark position | mitigate | Fresh finite geometry, four bounded candidates, current context/binding epochs, one-scope replacement, immediate withdrawal. |
| T-53-03-03 | Information disclosure | text/ARIA/DOM snapshot | mitigate | Closed local copy, textContent only, IDs private to frozen test snapshot, hostile-string negatives. |
| T-53-03-04 | Denial of service | repeated projection churn | mitigate | One mark scope, transition-key announcement dedupe, same-node geometry update, reverse disposal, exact resource plateaus. |
| T-53-03-05 | Elevation of privilege | focus/pointer/modal behavior | mitigate | Mark aria-hidden/nonfocusable/pointer-none; fail quiet uses region/polite status only; no gate or focus movement. |
| T-53-03-06 | Tampering | teardown resurrection | mitigate | Projection epochs invalidated and anchor scope removed before root teardown; stale calls fail against disposed/current state. |
</threat_model>

<verification>
Run the shell, accessibility, and resource-ledger suites. Inspect exact copy, mark dimensions/placement, DOM-removal ordering, focus/host snapshots, and exact-zero teardown; keep the preexisting Phase 52 controlled-fixture suite green.
</verification>

<success_criteria>
- HUD-06 fail-quiet is concise, neutral, exact, non-focus-stealing, and contains no guessed entity/page label.
- HUD-09's visible mark exists only for a current certified target and disappears synchronously when proof is lost.
- No Phase 53 path creates chips, halos, ghost layers, focused cards, gates, extra roots, pointer interception, or host mutation.
- All approved accessibility, reduced-motion, forced-colors, zoom/narrow, announcement, and teardown behaviors are executable.
</success_criteria>

<source_coverage>
- GOAL: fail quietly and bind visible state to verified meaning rather than DOM position.
- REQ: HUD-06, HUD-09.
- CONTEXT: D-07; D-14, D-15, D-16; consumes D-01 result vocabulary and D-05/D-06 projection identity without becoming authority.
- RESEARCH: shell-only visual ownership, typed projection, transactional scope disposal, closed copy, commit-time geometry, and no page-derived HTML/focus.
- UI-SPEC: all copy, state, color/type/spacing, 8×8 mark, placement, motion, focus, live-region, forced-colors, reduced-motion, host-integrity, and teardown decisions.
</source_coverage>

<output>
After completion, create `.planning/phases/53-drive-context-router-semantic-anchors/53-03-SUMMARY.md`.
</output>
