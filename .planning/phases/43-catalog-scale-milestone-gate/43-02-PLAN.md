---
phase: 43-catalog-scale-milestone-gate
plan: 02
type: execute
wave: 2
depends_on: [43-01]
files_modified:
  - scripts/import-opentabs-catalog.mjs
  - catalog/descriptors/_fixtures/seed-descriptors.json
  - extension/catalog/recipe-index.generated.js
  - catalog/descriptors/_fixtures/_provenance.json
  - tests/capability-search-eval.test.js
autonomous: true
requirements: [SCALE-01]

must_haves:
  truths:
    - "The importer's synonym data-map (INTENT_VERB_SYNONYMS + synthSynonyms) is ENRICHED with: a NOUN_SYNONYMS map (colloquial noun -> op-noun, e.g. to-do/to-do-item -> task, groceries -> products), an app-alias synonym emission (a STEM_OVERRIDE'd app also emits phrases carrying the friendly dir-name token, e.g. bluesky for bsky), and a sharpened stem/over-claim guard that trims a sibling op's cross-claiming generic phrases (e.g. outlook/sentry/temporal/chipotle no longer out-claim a stem-bearing curated/sibling descriptor)"
    - "ONLY the synonym DATA-MAP is changed — the importer CORE/LOGIC (emit pipeline, classify gate, crosscheck, stem-override resolution, side-effect inference) is byte-untouched; NO seed descriptor is hand-edited (the seed is RE-EMITTED by re-running the importer)"
    - "After re-import, the full-corpus eval wrong-invoke is driven to 0 (or the documented achievable bar) over the 190-fixture set; recall@5 stays >= 0.9; the curated cross-app collision proofs in breadth-search-return.test.js stay HARD wrong-invoke=0"
    - "capability-search-eval.test.js line ~141-142 is FLIPPED from the RECORDED baseline (wrongRate >= 0) to a HARD assertion at the achieved level (target wrongRate === 0; if an irreducible recall@5-covered residual remains, the HARD bar is the achieved rate + a documented residual list — NOT a fixture overfit)"
    - "No index-shape / params-leak regression: bytes/descriptor stays flat < 700 (currently ~621), the serialized index stays < 2MB, INV-01 catalog IIFE/djb2 unchanged (only the DATA re-emits)"
    - "The WHOLE eval + the WHOLE breadth collision set are re-verified for IDF-shift regression (a fix must not tip a different collision as the corpus re-weights — the 37-04/39-02/39-04 precedent)"
  artifacts:
    - path: "scripts/import-opentabs-catalog.mjs"
      provides: "Enriched synonym data-map (NOUN_SYNONYMS + app-alias emission + sharpened over-claim guard) — data only, importer core unchanged"
      contains: "NOUN_SYNONYMS"
    - path: "catalog/descriptors/_fixtures/seed-descriptors.json"
      provides: "Re-emitted seed (importer output upserted via feedSeedDescriptors) — NOT hand-edited"
    - path: "extension/catalog/recipe-index.generated.js"
      provides: "Re-emitted committed catalog (the runtime-indexed array) with the enriched corpus synonyms"
    - path: "tests/capability-search-eval.test.js"
      provides: "The full-corpus wrong-invoke assertion FLIPPED from RECORDED to HARD at the achieved bar"
      contains: "wrong-invoke"
  key_links:
    - from: "scripts/import-opentabs-catalog.mjs (synthSynonyms)"
      to: "catalog/descriptors/_fixtures/seed-descriptors.json"
      via: "node scripts/import-opentabs-catalog.mjs re-emits + feedSeedDescriptors upserts"
      pattern: "feedSeedDescriptors"
    - from: "tests/capability-search-eval.test.js"
      to: "the achieved full-corpus wrong-invoke"
      via: "check(wrongRate === 0 ...) HARD assertion (or the documented achievable bar)"
      pattern: "wrongRate"
---

<objective>
THE HEADLINE (DEF-39.5-04-A). Drive the full-corpus search wrong-invoke from the recorded
0.079 (15/190 fixtures) toward 0 via ROBUST metadata-source synonym enrichment in the
importer, RE-IMPORT through the frozen machinery, then FLIP the eval's full-corpus
wrong-invoke from a RECORDED baseline to a HARD assertion at the achieved level (target 0).

The mechanism is the proven 37-04/39-02 precedent: enrich the importer's synonym DATA-MAP
at the METADATA SOURCE, then `node scripts/import-opentabs-catalog.mjs` re-emits the
descriptors (and `feedSeedDescriptors` re-upserts the eval-indexed seed). NO importer
core/logic edit beyond the synonym data-map; NO seed-descriptors hand-edit.

This is robust enrichment, NOT fixture overfitting: the enrichment is GENERAL (a noun-
synonym map, an app-alias emission, an over-claim guard — each applies corpus-wide), and
the WHOLE eval + WHOLE breadth collision set are re-verified for the IDF-shift regression a
local fix can trigger as the corpus re-weights.

Purpose: the honest close of DEF-39.5-04-A — the precision frontier the full ~2,383-op
corpus introduced, driven to the hard bar.
Output: enriched importer data-map + re-emitted catalog/seed + the flipped HARD assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-catalog-scale-milestone-gate/43-CONTEXT.md
@.planning/phases/43-catalog-scale-milestone-gate/43-VALIDATION.md

# The synonym data-map to enrich (lines 539-673) + the re-import path (feedSeedDescriptors, 907-982)
@scripts/import-opentabs-catalog.mjs

# The eval to flip (line 141-142 RECORDED -> HARD) + the recall@5 HARD gate (118)
@tests/capability-search-eval.test.js

<the_15_wrong_invoke_cases>
<!-- The EXACT current misses (node tests/capability-search-eval.test.js confirms) + the
     ROOT CAUSE + the importer-data-map LEVER for each. CRITICAL: the eval indexes
     catalog/descriptors/_fixtures/seed-descriptors.json (2318 entries) and boosts
     intentSynonyms:3 ONLY (no owned-origin boostDocument in THIS eval path), so the
     disambiguation MUST come from the query's stem/synonym match in the index. -->

Category A — APP-ALIAS missing (query uses the friendly app name; syns carry only the stem):
  - "post to bluesky" -> want bsky.create_post, got facebook.react_to_post
  - "delete one of my bluesky posts" -> want bsky.delete_post, got mastodon.delete_status
  ROOT: STEM_OVERRIDES maps bluesky->bsky, but synthSynonyms emits only the 'bsky' token,
        never 'bluesky'. The query has no 'bsky' token + no 'bluesky' in any bsky synonym,
        so "post"/"delete post" matches a sibling on the bare verb+noun.
  LEVER: APP-ALIAS emission — when an app has a STEM_OVERRIDE (or a known friendly alias),
         synthSynonyms ALSO emits a small set of alias-tagged phrases ("create a post in
         bluesky") so the friendly name is a first-class stem token. Data-map only.

Category B — NOUN-SYNONYM missing (query uses a colloquial noun for the op-noun):
  - "add a to-do item to my todoist" -> want todoist.create_task, got etsy.add_to_cart
  - "show me my todoist to-do list" -> want todoist.list_tasks, got todoist.list_labels
  - "what tasks do i have in todoist" -> want todoist.list_tasks, got todoist.close_task
  - "file a new issue in linear" -> want linear.create_issue, got linear.create_attachment
  - "search for groceries on instacart" -> want instacart.search_products, got zillow.search_foreclosures
  ROOT: the op-noun ('task'/'issue'/'products') has no colloquial alias ('to-do'/'to-do item',
        'file'=create, 'groceries'=products), so a sibling op or cross-app op wins the verb.
  LEVER: a NOUN_SYNONYMS data-map (op-noun -> colloquial alternates, stem-tagged) woven into
         synthSynonyms the SAME way INTENT_VERB_SYNONYMS is — so todoist.create_task emits
         "add a to-do item in todoist", instacart.search_products emits "search for groceries
         in instacart", etc. The mandatory stem-guard still applies (every phrase carries the
         app stem), so a noun alias never leaks cross-app.

Category C — SAME-APP near-neighbor over-claim (a sibling op's generic phrase out-ranks):
  - "create a page in confluence" / "add a new page in confluence" -> want confluence.create_page,
        got confluence.create_inline_comment
  - "open a chatgpt conversation" -> want chatgpt.get_conversation, got chatgpt.archive_conversation
  ROOT: the sibling op synthesizes a phrase that the query matches as well or better
        ("create an inline comment" shares 'create'; "archive a conversation" shares
        'conversation' + the 'open a new' create-alias collides with archive). Same stem,
        so the stem-guard does not separate them — the VERB/NOUN specificity must.
  LEVER: sharpen the over-claim — trim the sibling's broadest cross-claiming alternate
        (e.g. drop 'open a new' from a non-create op's emission so "open ... " does not
        cross to archive; keep create_page's 'create a page' canonical phrase dominant).
        Data-map (the INTENT_VERB_SYNONYMS alternates / the endsWithDeterminer set) only.
  NOTE: "open a chatgpt/claude conversation" (get vs archive vs create) is a GENUINE
        create-vs-get/archive intent ambiguity — see <residual_policy>.

Category D — CURATED-HEAD cross-domain tie (a curated head descriptor loses to an
importer-owned corpus sibling whose generic synonym cross-claims):
  - "write an email message" -> want email.send (CURATED head), got outlook.send_message
  - "tweet a status update" -> want twitter.post-tweet (CURATED head), got sentry.update_issue
  - "view my schedule for the week" -> want calendar.list-events (CURATED head), got temporal.list_schedules
  ROOT (load-bearing): email.send / twitter.post-tweet / calendar.list-events are CURATED
        HEAD descriptors (kebab/non-opentabs stems) PRESERVED by feedSeedDescriptors — they
        carry HAND-WRITTEN synonyms and are NOT re-emitted by the importer. The WINNERS
        (outlook.send_message / sentry.update_issue / temporal.list_schedules) ARE importer-
        owned (hyphen-free snake_case, RE-EMITTED). outlook's "send an email in outlook" +
        "write a new message in outlook", sentry's "update ... status" matching "status
        update", temporal's "show me my schedules" matching "schedule" — these cross-claim.
  LEVER: trim the COMPETING IMPORTER-OWNED sibling's CROSS-CLAIMING synonym so it stops
        out-ranking the curated head, WITHOUT making the sibling wrong for its OWN intent.
        Concretely: outlook.send_message keeps "send a message in outlook" but its
        cross-domain "send an email in outlook" is the over-claim against email.send — trim
        or stem-bind it; sentry.update_issue's generic phrasing is bound tighter to 'issue'
        (a "status update" is a tweet, not a sentry issue-update); temporal.list_schedules
        is bound to 'temporal schedule' not a bare 'schedule'. All via the importer data-map
        + re-import. (The curated head synonyms in the seed are NOT hand-edited — the fix is
        on the importer-owned competing side, which IS the legitimate lever.)
  RESIDUAL RISK: if trimming the competitor would make it wrong for its own op, the case is
        an irreducible cross-domain tie — see <residual_policy> (recall@5 covers it).

Category E — CROSS-DOMAIN search/find tie:
  - "find restaurants on yelp" -> want yelp.search_businesses, got chipotle.find_restaurants
  ROOT: chipotle.find_restaurants literally owns "find restaurants"; yelp.search_businesses
        does not emit a "restaurants" noun.
  LEVER: NOUN_SYNONYMS for yelp (business -> restaurant/place near a 'yelp' stem) so
        "find restaurants on yelp" carries the yelp stem AND a 'restaurants' match; AND/OR
        bind chipotle.find_restaurants tighter to the 'chipotle' stem (it is a chain-locator,
        not a general restaurant search). Data-map only.
</the_15_wrong_invoke_cases>

<residual_policy>
<!-- The honest-boundary posture (the 39.5 precedent). literal wrong-invoke=0 is the TARGET. -->
Drive to wrong-invoke === 0 if achievable with ROBUST enrichment (no fixture overfit). If a
small irreducible residual remains — specifically the GENUINE same-intent ambiguities where
the RIGHT op IS in top-5 (recall@5 covers it) and disambiguating would require overfitting a
fixture or breaking a competitor's own intent — then:
  1. Assert the ACHIEVED bar HARD (e.g. wrongRate <= 1/190 with the residual enumerated), NOT 0.
  2. DOCUMENT each residual case in a comment block in the eval (query -> top1 vs expected,
     "recall@5-covered, genuine create-vs-get / cross-domain intent ambiguity") and in the
     plan SUMMARY + a deferred-items note, mirroring the 39.5 honest boundary.
  3. recall@5 >= 0.9 stays HARD; the curated breadth collision proofs stay HARD wrong-invoke=0.
The candidate irreducibles (verify during execution — try to close first): "open a
chatgpt/claude conversation" (get vs archive/create) and any Category-D case where trimming
the competitor breaks its own intent. EVERYTHING in Categories A/B/E and the confluence
create-page case is EXPECTED to close to 0 via the general enrichment.
</residual_policy>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enrich the importer synonym DATA-MAP (NOUN_SYNONYMS + app-alias emission + over-claim guard) and re-import</name>
  <files>scripts/import-opentabs-catalog.mjs, catalog/descriptors/_fixtures/seed-descriptors.json, extension/catalog/recipe-index.generated.js, catalog/descriptors/_fixtures/_provenance.json</files>
  <action>
Enrich ONLY the synonym DATA-MAP in scripts/import-opentabs-catalog.mjs (the importer
CORE/LOGIC — the emit pipeline, classifyGate, crosscheck, STEM_OVERRIDES resolution,
inferSideEffect — stays byte-untouched). Three additive, GENERAL data-map changes (NOT
per-fixture special-cases):

(1) NOUN_SYNONYMS map (new const beside INTENT_VERB_SYNONYMS at ~line 539). Key by the
    op-NOUN (the token(s) after the verb in the op name), value = colloquial alternates.
    Seed it with the GENERAL aliases the Category-B/E misses need, expressed as noun
    families that generalize corpus-wide:
      task: ['task', 'to-do', 'to-do item', 'todo'], issue: ['issue', 'ticket', 'bug'],
      products: ['products', 'groceries', 'items'], businesses: ['businesses', 'restaurants', 'places'],
      page: ['page'], conversation: ['conversation', 'chat', 'thread'] (etc — keep SMALL,
      per-noun-distinct, and ONLY where a real colloquial alias exists).
    Weave it into synthSynonyms the SAME way verbAlts is: for each verb-alternate phrase,
    ALSO emit a variant substituting each noun-alternate for the op-noun, stem-tagged
    (push() still REQUIRES the app stem token — the mandatory stem-guard at :565 — so a
    noun alias can NEVER leak cross-app). Respect the existing plural/singular + a/an
    article logic. Stay within the 6-synonym cap (slice at :672) — prioritize the noun
    alternates that close a real miss; let the cap trim the tail.

(2) APP-ALIAS emission. When an app has a STEM_OVERRIDE whose dir-name differs from the
    emitted stem (e.g. bluesky->bsky, docker-hub->dockerhub), synthSynonyms ALSO emits a
    SMALL set of alias-tagged canonical phrases carrying the friendly DIR-NAME token
    ("create a post in bluesky", "delete a post in bluesky"). Pass the dir-name alias into
    synthSynonyms (it already receives serviceStem; add the alias as an extra arg or
    resolve it from the same STEM_OVERRIDES map). The alias phrase MUST still satisfy a
    stem-guard against the ALIAS token (so it is app-bound). This is a DATA-MAP extension
    of the existing STEM_OVERRIDES mechanism — no core logic change.

(3) OVER-CLAIM guard (Categories C/D). Tighten the broadest CROSS-CLAIMING alternates so a
    sibling/cross-domain op stops out-ranking the right op, WITHOUT breaking the
    competitor's own intent:
      - Category C: ensure a non-create op does not emit the create-family 'open a new'
        determiner phrase (so "open a ... " does not cross to archive_conversation); keep
        the canonical 'create a page in confluence' dominant for create_page. This is a
        refinement of INTENT_VERB_SYNONYMS / the endsWithDeterminer set — data only.
      - Category D: trim the IMPORTER-OWNED competitor's cross-domain over-claim
        (outlook.send_message's bare "send an email" cross-claim vs the curated email.send;
        sentry.update_issue / temporal.list_schedules bound tighter to their own op-noun so
        "status update" / "schedule" do not cross-claim). Do this by binding the competitor's
        generic phrase to its own op-noun/stem in the data-map — NEVER by hand-editing the
        curated seed descriptor (email.send / twitter.post-tweet / calendar.list-events are
        PRESERVED untouched; the fix is on the importer-owned competing side).

Then RE-IMPORT through the frozen machinery (the 37-04/39-02 path):
  node scripts/import-opentabs-catalog.mjs
This re-emits the descriptors, re-runs feedSeedDescriptors (re-upserting seed-descriptors.json
— the curated head rows like email.send are PRESERVED by its hyphen-discriminator), and
fills _provenance.json. Then regenerate the committed catalog snapshot the runtime ships:
  node scripts/package-extension.mjs
(so extension/catalog/recipe-index.generated.js carries the enriched corpus synonyms).

Do NOT hand-edit seed-descriptors.json or recipe-index.generated.js — they are GENERATED.
Do NOT touch the index SHAPE: storedFields stay {slug, service, description, sideEffectClass,
backing}; params stay schema-on-hit (the 621B flatness is the leak signal — keep tight).

ITERATE: after each re-import, run the eval (Task 2's command) and inspect the remaining
misses; refine the data-map (more noun alternates / a tighter over-claim bind) and re-import.
Keep the changes GENERAL — if closing a case requires a phrase that ONLY matches that one
fixture's exact wording, that is overfitting: prefer the residual-policy path instead.
  </action>
  <verify>
    <automated>node scripts/import-opentabs-catalog.mjs && grep -q "NOUN_SYNONYMS" scripts/import-opentabs-catalog.mjs && node -e "const g=require('./extension/catalog/recipe-index.generated.js');const ok=g.descriptors.some(d=>(d.intentSynonyms||[]).some(s=>/bluesky/.test(s)));process.exit(ok?0:1)" && echo "DATA-MAP-ENRICHED+REIMPORTED"</automated>
  </verify>
  <done>NOUN_SYNONYMS exists; app-alias phrases (e.g. carrying 'bluesky') are emitted into the corpus; the importer core is unchanged (only the data-map); the seed + committed catalog are RE-EMITTED (not hand-edited); index shape/flatness preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Verify wrong-invoke -> 0 (or the achievable bar) across the WHOLE eval + breadth set (IDF-shift regression watch)</name>
  <files>tests/capability-search-eval.test.js (read-only this task), tests/breadth-search-return.test.js (read-only)</files>
  <action>
Run the FULL eval and the FULL breadth collision set after the re-import and confirm the
re-tune landed WITHOUT an IDF-shift regression. This task asserts via the existing test
output BEFORE the assertion flip in Task 3 (so the flip in Task 3 lands on a measured, green
number):

  node tests/capability-search-eval.test.js   # read the METRICS line: wrong-invoke + recall@5
  node tests/breadth-search-return.test.js     # curated HARD=0 must hold; corpus tier recorded

Confirm:
  - The eval METRICS line shows wrong-invoke=0.000 (target) OR the achievable bar with the
    remaining misses ENUMERATED in the eval's "wrong-invoke cases:" output. Cross-check each
    remaining miss against <residual_policy>: it MUST be a genuine recall@5-covered ambiguity,
    not a Category-A/B/E case that the enrichment should have closed.
  - recall@5 stays >= 0.9 (it was 0.995 — an enrichment that drops recall is a regression;
    the noun/alias additions should HOLD or IMPROVE recall).
  - breadth-search-return.test.js: the CURATED collision proofs stay HARD wrong-invoke=0
    (no IDF-shift tipped a curated pair). The corpus-tier recorded set should IMPROVE or hold.
  - IDF-SHIFT REGRESSION WATCH (the 37-04/39-02/39-04 precedent): scan the WHOLE eval miss
    list + the WHOLE breadth corpus-baseline list — confirm NO NEW miss appeared that was not
    there before (a fix for case X must not tip case Y as the corpus re-weights). If a new
    miss appeared, return to Task 1 and bind the offending synonym tighter, then re-import.

If wrong-invoke is NOT 0 and the residual is NOT a documented recall@5-covered ambiguity,
ITERATE on Task 1 (do not proceed to the flip with an undocumented miss).
  </action>
  <verify>
    <automated>node tests/capability-search-eval.test.js 2>&1 | tee /tmp/43-eval.txt | grep -E "METRICS|wrong-invoke=0.000" ; node tests/breadth-search-return.test.js && grep -q "recall@5=0.9\|recall@5=1.0\|recall@5=0.99" /tmp/43-eval.txt && echo "EVAL+BREADTH-VERIFIED"</automated>
  </verify>
  <done>The eval shows wrong-invoke at the target/achievable bar with any residual matching the residual-policy; recall@5 >= 0.9; the curated breadth proofs stay HARD=0; no NEW IDF-shift miss appeared. The achieved wrong-invoke number is recorded for Task 3's flip.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: FLIP the eval's full-corpus wrong-invoke from RECORDED to a HARD assertion at the achieved bar</name>
  <files>tests/capability-search-eval.test.js</files>
  <behavior>
    - With the enriched corpus (Task 1) re-imported, the eval's full-corpus wrong-invoke check
      becomes HARD: it PASSES at the achieved bar (target wrongRate === 0) and would FAIL if a
      future change re-introduced a wrong-invoke above the bar.
    - recall@5 >= 0.9 stays HARD (unchanged assertion).
    - If an irreducible recall@5-covered residual is documented, the HARD bar is the achieved
      rate (e.g. wrongRate <= 1/190) with the residual enumerated in a comment — NOT a silent 0.
  </behavior>
  <action>
Edit ONLY the full-corpus wrong-invoke assertion in tests/capability-search-eval.test.js
(the RECORDED baseline at ~line 141-142: `check(wrongRate >= 0, '... RECORDED as the
Phase-43 SCALE-01 baseline ...')`). Replace it with a HARD assertion at the bar Task 2
measured:

  - If Task 2 achieved wrong-invoke === 0:
      check(wrongRate === 0, 'SCALE-01 (DEF-39.5-04-A, HARD): full-corpus wrong-invoke === 0
        over the ' + FIXTURES.length + '-fixture paraphrase set -- the re-tune deliverable
        landed (robust intentSynonym/noun-alias/app-alias enrichment, NOT fixture overfit);
        recall@5 stays HARD >= 0.9; the curated collision proofs stay HARD=0 in
        breadth-search-return');
  - If Task 2 achieved an achievable bar with a documented residual:
      define a small `const WRONG_INVOKE_HARD_BAR = <achieved-count>;` with a comment block
      ENUMERATING each residual case (query -> top1 vs expected, "recall@5-covered genuine
      <create-vs-get / cross-domain> intent ambiguity, DEF-39.5-04-A documented residual"),
      then:
      check(wrongInvoke <= WRONG_INVOKE_HARD_BAR, 'SCALE-01 (DEF-39.5-04-A, HARD): full-corpus
        wrong-invoke <= ' + WRONG_INVOKE_HARD_BAR + ' (the achieved bar; the residual is the
        enumerated recall@5-covered same-intent ambiguity, documented above -- NOT overfit)');

Keep the existing grep-able METRICS console.log line intact (the acceptance check greps for
"recall@5=" + "wrong-invoke="). Keep the recall@5 >= 0.9 HARD check unchanged. Update the
surrounding comment to state the baseline is now CLOSED (no longer a Phase-43 deferral).

Use the Edit tool — change ONLY that assertion + its comment; do not rewrite the file.
  </action>
  <verify>
    <automated>node tests/capability-search-eval.test.js && grep -qE "HARD\): full-corpus wrong-invoke" tests/capability-search-eval.test.js && ! grep -q "RECORDED as the Phase-43 SCALE-01 baseline" tests/capability-search-eval.test.js && echo "ASSERTION-FLIPPED-HARD"</automated>
  </verify>
  <done>The eval's full-corpus wrong-invoke is a HARD assertion at the achieved bar (target 0); the RECORDED-baseline language is gone; recall@5 >= 0.9 stays HARD; the eval exits 0; any residual is enumerated in-source.</done>
</task>

</tasks>

<verification>
- `node tests/capability-search-eval.test.js` exits 0 with the full-corpus wrong-invoke HARD
  at the achieved bar (target 0) + recall@5 >= 0.9 HARD.
- `node tests/breadth-search-return.test.js` exits 0; curated collision proofs HARD wrong-invoke=0.
- `node tests/full-corpus-scale.test.js` exits 0 (no index-shape/params-leak regression: bytes/
  descriptor flat < 700, serialized < 2MB).
- `node tests/no-duplicate-stem.test.js` + `node tests/no-dead-entry.test.js` +
  `node tests/catalog-crosscheck.test.js` exit 0 (the re-emit did not break a structural gate).
- `npm run validate:extension` exits 0 (recipe-path-guard / classification-gate / crosscheck /
  no-duplicate-stem / origin-classification / no-orphan-descriptor all green over the re-emit).
- `git diff --stat scripts/import-opentabs-catalog.mjs` shows ONLY data-map additions (NOUN_SYNONYMS
  + app-alias + over-claim trims), not core-logic changes.
</verification>

<success_criteria>
- The importer synonym data-map is enriched (NOUN_SYNONYMS + app-alias emission + over-claim
  guard) — data only, core untouched, seed re-emitted (not hand-edited).
- Full-corpus wrong-invoke is driven to 0 (or the documented achievable bar) and the eval is
  FLIPPED to a HARD assertion at that bar; recall@5 >= 0.9 HARD; curated breadth proofs HARD=0.
- No index-shape/params-leak regression; no NEW IDF-shift miss; all structural gates + validate
  green.
- This wave delivers the precision half of SC1 BECAUSE the full-corpus wrong-invoke is now a HARD
  CI assertion at the hard bar over the complete corpus, closing DEF-39.5-04-A, with the scale gate
  (43-01) holding flat.
</success_criteria>

<output>
Create `.planning/phases/43-catalog-scale-milestone-gate/43-02-SUMMARY.md` when done.
Record: the exact data-map additions (the NOUN_SYNONYMS entries, the app-alias mechanism, each
over-claim trim) with which of the 15 cases each closed; the ACHIEVED wrong-invoke (0 or the bar)
+ any enumerated residual + its recall@5-covered rationale; the before/after recall@5 + bytes/
descriptor; confirmation the importer CORE was untouched and the seed was RE-EMITTED.
</output>
