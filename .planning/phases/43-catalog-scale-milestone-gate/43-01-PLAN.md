---
phase: 43-catalog-scale-milestone-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/relearn-coalescing.test.js
  - tests/rot-recurrence-classify.test.js
  - tests/app-degraded-surfacing.test.js
  - package.json
autonomous: true
requirements: [SCALE-01, SCALE-02]

must_haves:
  truths:
    - "tests/relearn-coalescing.test.js exists and FAILS deterministically (the per-origin re-learn scheduler module/section it requires does not exist yet) — a clean RED that defines the N-rot-on-one-origin -> ONE consent-gated re-learn + exponential-back-off contract"
    - "tests/rot-recurrence-classify.test.js exists and FAILS deterministically — defines the recurrence-counter contract (repeated rot on (origin,slug) -> SYSTEMIC/quarantine+surface; a one-off -> TRANSIENT/retry) layered on the UNCHANGED classifyRecipeBroken verdict"
    - "tests/app-degraded-surfacing.test.js exists and FAILS deterministically — defines the app-level degraded/needs-re-port status accessor contract (a stale/quarantined origin surfaces as degraded, not silent failure)"
    - "All three new test files are registered in the npm test chain in package.json (so the milestone gate runs them)"
    - "full-corpus-scale.test.js is CONFIRMED present in the npm test chain (the authoritative SCALE-01 scale gate) — no edit needed, a grep-verified fact recorded in this plan's SUMMARY"
  artifacts:
    - path: "tests/relearn-coalescing.test.js"
      provides: "RED-first contract: N rot events on ONE origin coalesce into ONE re-learn + exponential back-off; the re-learn still passes the consent gate"
      min_lines: 60
    - path: "tests/rot-recurrence-classify.test.js"
      provides: "RED-first contract: recurrence-based systemic-vs-transient classification atop the unchanged classifyRecipeBroken verdict"
      min_lines: 60
    - path: "tests/app-degraded-surfacing.test.js"
      provides: "RED-first contract: an app-level degraded/needs-re-port status accessor for a stale/quarantined origin"
      min_lines: 50
  key_links:
    - from: "package.json"
      to: "the three new self-heal test files"
      via: "&& node tests/<name>.test.js appended to the test chain"
      pattern: "relearn-coalescing"
    - from: "tests/relearn-coalescing.test.js"
      to: "the (not-yet-existing) per-origin re-learn scheduler"
      via: "require() of the scheduler export the module will expose (FsbRelearnScheduler)"
      pattern: "FsbRelearnScheduler|scheduleRelearn"
---

<objective>
WAVE 0 (the test scaffolds). Author the three SCALE-02 self-heal test files BEFORE
the scheduler/recurrence/surfacing code exists (the proven Phase-32
capability-rot-detector.test.js RED-first precedent), register them in the `npm test`
chain, and CONFIRM `full-corpus-scale.test.js` is already the authoritative SCALE-01
scale gate in that chain.

This plan lands the CONTRACTS. A clean RED on all three new files is the CORRECT and
EXPECTED Wave-0 state: each requires a module/section that Plan 43-03 creates,
so `require()` throws MODULE_NOT_FOUND (or the export is absent), which the
zero-framework harness reports as a single deterministic non-crash failure. Every
`check()` body is the contract those later plans turn GREEN with zero edits here.

Purpose: define the per-origin coalescing/back-off, recurrence, and degraded-surfacing
contracts up front so the implementing plan (43-03) build against a fixed
acceptance shape — no scavenger hunt, no contract drift.
Output: 3 new RED test files + their registration in `npm test` + the confirmed scale gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-catalog-scale-milestone-gate/43-CONTEXT.md
@.planning/phases/43-catalog-scale-milestone-gate/43-VALIDATION.md

# The zero-framework test pattern to mirror (RED-first, passed/failed counters, process.exit)
@tests/capability-rot-detector.test.js

# The seams the three contracts pin (READ these — the contracts must match the real shapes)
@extension/utils/capability-rot-detector.js
@extension/utils/learned-recipe-store.js

<interfaces>
<!-- The real collaborator shapes the three RED tests must pin. Extracted from the codebase. -->
<!-- The scheduler/recurrence/surfacing code (Plan 43-03) implements AGAINST these. -->

From extension/utils/capability-rot-detector.js (UNCHANGED this phase — recurrence layers ON TOP):
  classifyRecipeBroken(result, recipe) -> { broken: boolean, code: string|null, reason: string }
    // broken:true codes: RECIPE_EXPIRED, RECIPE_HTTP_4XX, RECIPE_HTTP_5XX
    // broken:false: RECIPE_LOGGED_OUT (surfaced), null (ok / typed-passthrough)
  global.FsbCapabilityRotDetector / module.exports = { classifyRecipeBroken, validateExpectedShape }

From extension/utils/learned-recipe-store.js (per-origin cap/LRU/quarantine UNCHANGED):
  quarantine(slug, origin) -> Promise<void>   // FLAGS quarantined:true, does NOT delete
  getLearnedSync(slug, origin) -> entry|null  // null when quarantined
  PER_ORIGIN_CAP = 24
  module.exports = { ..., quarantine, getLearnedSync, promote, _reset }

From extension/utils/capability-router.js (the rot hook the scheduler intercepts — Plan 43-03):
  _quarantineAndRelearn(slug, tierLabel, origin, tabId)  // module-internal; calls
    discovery.runDiscovery(origin, { tabId }) FIRE-AND-FORGET on EVERY broken verdict
    -> THIS is the thundering-herd the coalescing scheduler must debounce.

From extension/utils/discovery-session.js (the consent-gated re-learn — UNCHANGED):
  runDiscovery(origin, opts) -> Promise<{ ok:true, promoted, discarded, flaggedForPhase32 }>
                                     | Promise<{ ok:false, reason:'RECIPE_CONSENT_*' }>
    // the consent gate (_runGate) runs INSIDE network-capture.startSession — a denied/
    // default-OFF/sensitive origin re-learns NOTHING. The scheduler must NOT replace it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author tests/relearn-coalescing.test.js (RED-first — the N->1 coalescing + back-off contract)</name>
  <files>tests/relearn-coalescing.test.js</files>
  <action>
Create the RED-first contract test for the per-origin re-learn scheduler (Plan 43-03
creates the implementation). Mirror tests/capability-rot-detector.test.js EXACTLY:
module-level `passed`/`failed` counters, synchronous `check(cond, msg)`, ASCII-only,
NO emojis, `process.exit(failed > 0 ? 1 : 0)`, and a leading header comment block that
NAMES this as a WAVE-0 RED scaffold (requiring the not-yet-created scheduler is the
expected clean RED — report MODULE_NOT_FOUND as one deterministic non-crash failure,
exactly as capability-rot-detector.test.js does at its top).

Require the scheduler module by the export name the implementation will publish:
`FsbRelearnScheduler` (global) / module.exports, with a `scheduleRelearn(origin, fn, opts)`
entry point. Wrap the require in a try/catch that records a single failure on
MODULE_NOT_FOUND (the RED state) and SKIPS the body — so the file exits 1 cleanly now
and runs the full contract once the module lands.

The contract `check()` body (GREEN in Plan 43-03):
  - COALESCING: feed N (e.g. 5) rot events for the SAME origin within the coalescing
    window; assert the underlying re-learn fn (a spy) is invoked EXACTLY ONCE (N->1, no
    thundering-herd of CDP attaches). Two DISTINCT origins each get their own single
    re-learn (keyed by origin).
  - BACK-OFF: after a re-learn fn that RESOLVES ok:false (failed re-learn), assert the
    NEXT scheduled re-learn for that origin is deferred by an EXPONENTIALLY growing delay
    (e.g. base, 2x, 4x — assert the computed schedule via an injectable clock/now hook,
    NOT a real wall-clock sleep; the test must run < 5s with no watch flags).
  - BOUNDED: assert the per-origin scheduler state is bounded (a cap on tracked origins /
    a ceiling on the back-off delay) so it can never grow unbounded — mirror the
    PER_ORIGIN_CAP discipline.
  - CONSENT-PRESERVED: assert the scheduler INVOKES the supplied re-learn fn (it does NOT
    re-implement capture/consent) — the fn IS the consent-gated runDiscovery, so the
    Phase-30 gate still runs inside it. (Spy returns {ok:false,reason:'RECIPE_CONSENT_*'}
    -> the scheduler treats it as a failed attempt for back-off, captures NOTHING itself.)

Use an injectable `now()`/clock + a deterministic flush hook in opts so the back-off
schedule is asserted synchronously (no setTimeout race). Document in a comment that the
real scheduler must expose this test seam.
  </action>
  <verify>
    <automated>node tests/relearn-coalescing.test.js; test $? -eq 1 && echo "EXPECTED-RED-OK"</automated>
  </verify>
  <done>File exists, mirrors the zero-framework pattern, exits 1 (clean RED: scheduler module absent), and its body fully specifies the N->1 + exponential-back-off + bounded + consent-preserved contract.</done>
</task>

<task type="auto">
  <name>Task 2: Author tests/rot-recurrence-classify.test.js (RED-first — systemic-vs-transient recurrence contract)</name>
  <files>tests/rot-recurrence-classify.test.js</files>
  <action>
Create the RED-first contract test for recurrence-based systemic-vs-transient
classification (Plan 43-03 creates the implementation). Same zero-framework pattern +
WAVE-0 RED header as Task 1.

The recurrence layer sits ON TOP of the UNCHANGED `classifyRecipeBroken` verdict: it
does NOT modify the taxonomy — it ADDS a per-(origin,slug) recurrence counter and a
`classifyRecurrence(origin, slug, verdict)` (or equivalent) that returns a
systemic/transient disposition. Require it by the export name the implementation will
publish (e.g. `FsbRotRecurrence` global / module.exports with a `recordRot(origin, slug)`
+ `dispositionFor(origin, slug)` pair). RED via the same try/catch-on-MODULE_NOT_FOUND.

The contract `check()` body (GREEN in Plan 43-03):
  - TRANSIENT: a FIRST broken verdict for (origin,slug) (recurrence count 1, below the
    threshold) classifies TRANSIENT (retry — the existing fire-and-forget re-learn path
    is appropriate, no quarantine escalation). Assert classifyRecipeBroken itself is
    called UNCHANGED to produce the broken verdict that feeds recurrence (the recurrence
    layer consumes the verdict; it never replaces it).
  - SYSTEMIC: repeated broken verdicts for the SAME (origin,slug) crossing a threshold
    (e.g. >= 3 within a bounded window) classify SYSTEMIC (the site changed -> escalate to
    quarantine + surface). Assert the threshold is the documented constant.
  - RESET-ON-SUCCESS: a NON-broken (ok) outcome for (origin,slug) RESETS the recurrence
    counter (a recovered op is no longer trending systemic).
  - BOUNDED: the recurrence store is bounded (a sibling ring / a cap), never unbounded —
    assert via a cap or ring-eviction check.
  - SECURITY-PASSTHROUGH UNCHANGED: a typed RECIPE_* security rejection (broken:false,
    typed-passthrough) is NEVER counted as a rot (recurrence must never escalate a
    security rejection) — mirror the capability-rot-detector.test.js T-32-PASS guard.
  </action>
  <verify>
    <automated>node tests/rot-recurrence-classify.test.js; test $? -eq 1 && echo "EXPECTED-RED-OK"</automated>
  </verify>
  <done>File exists, exits 1 (clean RED: recurrence module absent), and its body specifies transient/systemic threshold, reset-on-success, bounded store, and the security-passthrough never-count guard atop the UNCHANGED classifyRecipeBroken verdict.</done>
</task>

<task type="auto">
  <name>Task 3: Author tests/app-degraded-surfacing.test.js (RED-first) + register all three in npm test + confirm the scale gate</name>
  <files>tests/app-degraded-surfacing.test.js, package.json</files>
  <action>
(a) Create the RED-first contract test for app-level degraded/needs-re-port surfacing
(Plan 43-03 creates the implementation). Same zero-framework pattern + WAVE-0 RED header.

The surfacing is an ADDITIVE status accessor: when an origin has a stale/quarantined
learned-recipe set, a query (a new status accessor and/or an additive field on the
search hit) reports the origin as degraded/needs-re-port instead of silent failure.
Require it by the export name the implementation will publish (e.g. a
`FsbLearnedRecipeStore.getOriginHealth(origin)` ADDITIVE method, or a
`FsbAppDegradedStatus` accessor — Plan 43-03 picks the seam; the test requires the
chosen name). RED via try/catch on the absent export (the method/module does not exist
yet).

The contract `check()` body (GREEN in Plan 43-03):
  - HEALTHY: an origin with live, non-quarantined learned recipes reports healthy
    (degraded:false / status 'ok').
  - DEGRADED: an origin whose learned recipes are ALL quarantined (or whose recurrence
    crossed systemic) reports degraded:true / 'needs-re-port' with the origin identified.
  - VISIBLE-NOT-SILENT: the degraded state is RETRIEVABLE (the accessor returns it, and/or
    the search hit carries an additive degraded marker) — assert the field/accessor is
    present and truthful, so a user/agent SEES "this app needs re-learning".
  - ADDITIVE / NON-BREAKING: assert the existing getLearnedSync / quarantine / promote
    shapes are UNCHANGED (the surfacing is a NEW accessor or a NEW additive field, never a
    breaking change to the store contract). Reuse FsbLearnedRecipeStore._reset between cases.

(b) REGISTER all three new test files in the `npm test` chain in package.json. APPEND
`&& node tests/relearn-coalescing.test.js && node tests/rot-recurrence-classify.test.js
&& node tests/app-degraded-surfacing.test.js` to the existing test script. Place them
near the other capability/self-heal tests (e.g. right after
`node tests/capability-rot-detector.test.js`). Use the Edit tool on package.json — do
NOT rewrite the whole scripts block; surgically extend the one `test` string. Preserve
the rest of the chain byte-for-byte.

NOTE: registering three RED tests in `npm test` means `npm test` now EXITS NON-ZERO until
Plan 43-03 lands. That is the CORRECT Wave-0 posture (the milestone gate is RED
until the work is done). Each per-file verify below runs the file DIRECTLY (not via
npm test) so this Wave-0 plan's own verification is deterministic.

(c) CONFIRM (no edit) that `tests/full-corpus-scale.test.js` is ALREADY in the `npm test`
chain — it is the authoritative SCALE-01 scale gate. Grep package.json for it and record
the confirmed fact (present + position) in this plan's SUMMARY. Run it once to confirm it
passes at the current budget (index < 2MB / cold-start < 100ms / < 700B/descriptor /
> 2000 descriptors — currently ~1.40MB / ~11ms / ~621B / 2314).
  </action>
  <verify>
    <automated>node tests/app-degraded-surfacing.test.js; test $? -eq 1 && grep -q "relearn-coalescing.test.js" package.json && grep -q "rot-recurrence-classify.test.js" package.json && grep -q "app-degraded-surfacing.test.js" package.json && grep -q "full-corpus-scale.test.js" package.json && node tests/full-corpus-scale.test.js && echo "RED+REGISTERED+SCALE-GATE-CONFIRMED"</automated>
  </verify>
  <done>app-degraded-surfacing.test.js exists and exits 1 (clean RED); all three new files AND full-corpus-scale.test.js are present in the npm test chain; full-corpus-scale.test.js passes at the current budget; the scale-gate confirmation is recorded for the SUMMARY.</done>
</task>

</tasks>

<verification>
- Each new test file runs DIRECTLY via `node tests/<name>.test.js` and exits 1 (the clean
  Wave-0 RED — the module/section it requires is created in Plan 43-03).
- `node tests/full-corpus-scale.test.js` exits 0 (the SCALE-01 scale gate holds NOW).
- `grep` confirms all three new files + full-corpus-scale.test.js are in the `npm test` chain.
- No watch flags; every suite < 5s.
</verification>

<success_criteria>
- 3 new RED-first self-heal test files exist, each mirroring the zero-framework pattern and
  fully specifying its contract (coalescing/back-off; recurrence systemic-vs-transient;
  degraded surfacing).
- All three are registered in `npm test`.
- `full-corpus-scale.test.js` is CONFIRMED as the authoritative SCALE-01 scale gate in `npm test`.
- This wave delivers the Wave-0 half of SC1 (the scale-gate confirm) and the Wave-0 half of
  SC2 (the three self-heal contracts) BECAUSE the contracts + the scale gate are now fixed and
  CI-wired, so the implementing plans build against a frozen acceptance shape and the milestone
  gate already runs them.
</success_criteria>

<output>
Create `.planning/phases/43-catalog-scale-milestone-gate/43-01-SUMMARY.md` when done.
Record: the three new test file paths + their npm-test positions; the confirmed
full-corpus-scale.test.js position + its current measured budget numbers; the exact export
names each RED test requires (so Plan 43-03 publishes those names).
</output>
