---
phase: 59-current-user-alerts-release-hardening
plan: "03"
title: Current Alert Status and Owner Mapping HUD
wave: 3
depends_on: [59-02]
requirements: [ALERT-02, ALERT-03, ALERT-05, VERIFY-03]
threats: [T59-01, T59-06, T59-08, T59-09, T59-10, T59-11]
autonomous: true
---

# Plan 59-03 — Current Alert Status and Owner Mapping HUD

## Objective

Replace the neutral notification placeholder with current minimized local-alert status and add explicit mapping/removal confirmation actions inside the existing folder/reading HUD lifecycle.

## Task 1 — Controlled RED projection/content/browser contracts

Extend:

- `tests/skopeo-hud-schema.test.js`
- `tests/skopeo-hud-projector.test.js`
- `tests/skopeo-adaptive-composer.test.js`
- `tests/skopeo-hud-runtime.test.js`
- `tests/skopeo-browser-contract.test.js`

Define exact status/action models and UI-SPEC copy for scheduled, attempted, delivered, failed, missed, not-locally-deliverable, and omitted not-evaluated state; mapping/removal confirmation; hostile labels; stale/replayed actions; focus/live-region/narrow/zoom/preferences/collision/teardown. Use controlled RED markers.

## Task 2 — Extend private projection and one-shot mapping effects

Modify:

- `extension/utils/skopeo-hud-schema.js`
- `extension/utils/skopeo-hud-projector.js`
- `extension/background.js`

Join only the current alert engine public status into current folder/reading projections. Add opaque `map-current-owner` and `remove-current-owner-mapping` actions to the existing controller registry. Both require existing consequence confirmation; background re-derives current exact partition/owner/source authority before store mutation, revokes on drift/replay, and reconciles after success. Public projection contains no owner stable/account/source/alert/alarm/notification identifiers.

**Gate:** schema/projector/background runtime tests green.

## Task 3 — Compose and render the accessible current status

Modify:

- `extension/content/skopeo-adaptive-composer.js`
- `extension/content/skopeo-runtime.js`
- `extension/content/skopeo-shell.js`

Use closed local copy maps, current content action epochs, the existing confirmation model/surface, native buttons/headings/lists, one polite status announcement, text-only sinks, and the existing 384px geometry/resource ledger. A background alert never raises the HUD. Hide/kill removes action authority without deleting durable schedules. Preserve Phase 57/58 views and zero residue.

**Gate:** composer/runtime/session/browser tests and focused alert tests green.

## Must-Haves

- Missing/different/ambiguous owner is explicit without identity leakage or false delivery claims.
- Mapping uses stable background authority and an explicit confirmation; no typed email/name path.
- Alert status words are accessible and never color-only.
- No second shell, host mutation, automatic HUD activation, or durable state in content.
