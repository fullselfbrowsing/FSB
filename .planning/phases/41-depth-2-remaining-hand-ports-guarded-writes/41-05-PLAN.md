---
phase: 41-depth-2-remaining-hand-ports-guarded-writes
plan: 05
type: execute
wave: 4
depends_on: ["41-01", "41-02", "41-03", "41-04"]
files_modified:
  - .planning/phases/41-depth-2-remaining-hand-ports-guarded-writes/41-DEFERRAL.md
autonomous: false
requirements: [DEPTH-02]

must_haves:
  truths:
    - "Full npm test exits 0 with all 7 guarded writes fail-closed, SC2 proven, the CORS-gate green, INV-03 byte-stable, head cap ≤30"
    - "npm run validate:extension exits 0 including the new verify-origin-classification.mjs CORS-gate"
    - "The Wall-2 origin-pin (executeBoundSpec spec.origin !== activeTabOrigin -> RECIPE_ORIGIN_MISMATCH) is UNCHANGED — Pattern-D cross-origin execution was NOT built"
    - "The Pattern-D deferral (linear/datadog/jira demoted to T3-DOM via the CORS-gate) is recorded"
  artifacts:
    - path: ".planning/phases/41-depth-2-remaining-hand-ports-guarded-writes/41-DEFERRAL.md"
      provides: "The recorded Pattern-D / cross-origin-execution deferral + the demote-via-CORS-gate rationale"
      contains: "Pattern-D"
  key_links:
    - from: "npm run validate:extension"
      to: "scripts/verify-origin-classification.mjs"
      via: "the CORS-gate runs in the validate chain"
      pattern: "verify-origin-classification"
---

<objective>
The final battery: prove the whole phase is green and the walls are intact.

1. Run the full headless gate suite and confirm EXIT 0:
   - `npm test` — every existing suite + the new guarded-write-failclosed + the extended
     upgrade/consent/sensitive-write blocks.
   - `npm run validate:extension` — the build gates INCLUDING the new
     verify-origin-classification.mjs CORS-gate.
   - The invariants: INV-01 (frozen registry hash / no new MCP tool — capability-mcp-surface),
     INV-02 (router parity — capability-autopilot-parity), INV-03 (byte-stable typed reasons),
     head cap ≤30 (head-handler-cap), the fail-closed harness, the CORS-gate.
2. Confirm Wall 2 is UNCHANGED: the executeBoundSpec origin-pin (spec.origin !==
   activeTabOrigin → RECIPE_ORIGIN_MISMATCH) was NOT touched — Pattern-D cross-origin
   execution was NOT built. No separate-origin head ships (the CORS-gate enforces it).
3. Record the Pattern-D deferral (41-DEFERRAL.md): linear/datadog/jira/supabase/cloud-consoles
   are SEPARATE-origin and were DEMOTED to T3-DOM via the CORS-gate; the cross-origin
   execution mechanism (the Wall-2 hole) is deferred to a future user-blessed story
   (v2 CLOUD-01 / a linear-CORS spike).
4. The end-of-phase human-verify checkpoint: confirm the live-UAT debt handoff (the 7
   guarded writes ship fail-closed; activation awaits live-capture per 41-HUMAN-UAT.md).

Purpose: the milestone-gate posture — a phase is done only when the full suite is green, the
walls are provably intact, and the honest debt + the deferral are recorded.
Output: 41-DEFERRAL.md + a green full suite + the verified-walls-intact confirmation.

This wave delivers SC1+SC2+SC3+SC4 closure because: SC1 (all writes fail-closed) + SC2 (the
sensitive-origin mutating opt-in) + SC3 (the CORS-gate precedes any separate-origin port;
separate origins demoted) + SC4 (head cap holds, INV-03 byte-stable, depth tests green) are
ALL re-proven green in one battery, and the Pattern-D deferral is the recorded SC3 decision.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-depth-2-remaining-hand-ports-guarded-writes/41-CONTEXT.md
@.planning/phases/41-depth-2-remaining-hand-ports-guarded-writes/41-VALIDATION.md

# The Pattern-D deferral source (the Out-of-scope + Deferred Ideas sections):
@.planning/phases/41-depth-2-remaining-hand-ports-guarded-writes/41-CONTEXT.md

<interfaces>
<!-- The Wall-2 origin-pin to confirm UNCHANGED (do NOT edit — assert it still rejects a
     cross-origin spec): capability-router.js executeBoundSpec is the MAIN-world primitive
     (:530-583); the pin (spec.origin !== activeTabOrigin -> RECIPE_ORIGIN_MISMATCH, no
     executeScript) lives inside that primitive. capability-router.test.js proves it. The
     CORS-gate (build-time) is the SHIPPING gate; the origin-pin (runtime) is the EXECUTION
     gate — both intact, neither extended for cross-origin. -->

<!-- The validate:extension chain after Plan 01 wires the CORS-gate:
     validate-extension.mjs && verify-recipe-path-guard && verify-classification-gate &&
     verify-catalog-crosscheck && verify-no-duplicate-stem && verify-origin-classification &&
     verify-no-orphan-descriptor. EXIT 0 required. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full battery + confirm Wall-2 unchanged</name>
  <files>(read-only verification — no source edit)</files>
  <action>Run the full gate suite and confirm green: `npm test` (EXIT 0), `npm run
validate:extension` (EXIT 0, incl verify-origin-classification.mjs). Then run the focused
invariant checks and confirm each green: node tests/head-handler-cap.test.js (≤30, 4
modules), node tests/head-handler-upgrade.test.js (all read + write slugs T1a, byte-exact),
node tests/guarded-write-failclosed.test.js (all 7 writes fail-closed), node
tests/sensitive-write-import-gate.test.js (SC2 slack block + discord/amazon), node
tests/consent-mutation-gate.test.js (INV-03 incl the new write reason), node
tests/capability-router.test.js (the executeBoundSpec origin-pin / RECIPE_ORIGIN_MISMATCH +
the gate-before-dispatch / INV-02), node tests/capability-autopilot-parity.test.js (INV-02),
node tests/capability-mcp-surface.test.js (INV-01 frozen registry), node
scripts/verify-origin-classification.mjs (the CORS-gate). Confirm Wall 2 is UNCHANGED: grep
capability-router.js for the origin-pin (RECIPE_ORIGIN_MISMATCH) and confirm it is present
and untouched (no cross-origin allowance was added) — the Pattern-D execution mechanism was
NOT built. If any suite reds, STOP and report the failing gate (do not paper over it). Per
the final-wave constraint: full npm test EXIT 0 + validate:extension (incl CORS-gate) +
INV-01/02/03 + head-cap green; Wall-2 origin-pin UNCHANGED.</action>
  <verify>
    <automated>npm test && npm run validate:extension && grep -q "RECIPE_ORIGIN_MISMATCH" extension/utils/capability-router.js && echo "FULL BATTERY GREEN + Wall-2 origin-pin intact"</automated>
  </verify>
  <done>npm test exits 0, validate:extension exits 0 (CORS-gate included), all focused invariant checks green, the Wall-2 origin-pin (RECIPE_ORIGIN_MISMATCH) is present and unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Record the Pattern-D deferral + demote-via-CORS-gate rationale</name>
  <files>.planning/phases/41-depth-2-remaining-hand-ports-guarded-writes/41-DEFERRAL.md</files>
  <action>Create 41-DEFERRAL.md recording the conservative Wall-2-preserving decision: (1)
Pattern-D cross-origin EXECUTION (the actual linear/datadog/jira/supabase/cloud-console ports)
is DEFERRED — porting them requires a CORS-verified cross-origin credentialed fetch that
punches a controlled hole in the NON-NEGOTIABLE Wall-2 origin-pin, a deliberate Wall-2-extension
that needs live-capture (to confirm the cross-origin cookie rides) + user blessing; (2) the
CONSERVATIVE, SC3-compliant choice ("MAY port … or be demoted to T2/T3") is to DEMOTE all
separate-origin apps to T3-DOM this milestone — they remain discoverable + DOM-invocable
breadth descriptors; (3) the CORS-gate (scripts/verify-origin-classification.mjs) is what
ENFORCES the demote: a separate-origin head FAILS the build, so an unverified port can never
silently ship; (4) the deferred follow-on is a future story (v2 CLOUD-01 / a linear-CORS
spike). Reference the verified base-URLs (linear client-api.linear.app; datadog *.datadoghq.com;
jira *.atlassian.net) as the separate-origin evidence. Note this is recorded debt, NOT a gap.
Per D (the Wall-2 integrity decision + the Deferred Ideas section).</action>
  <verify>
    <automated>D=.planning/phases/41-depth-2-remaining-hand-ports-guarded-writes/41-DEFERRAL.md; test -f "$D" && grep -q "Pattern-D" "$D" && grep -q "client-api.linear.app" "$D" && grep -q "CORS" "$D" && echo "deferral recorded"</automated>
  </verify>
  <done>41-DEFERRAL.md records the Pattern-D cross-origin-execution deferral, the demote-to-T3-DOM-via-CORS-gate rationale, the separate-origin evidence, and the future-story handoff.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-of-phase human-verify — full suite green + the live-UAT debt + the Pattern-D deferral</name>
  <action>This is a verification checkpoint (no source edit). The human runs the full suite, reviews the live-UAT debt manifest + the deferral, and confirms the conservative Pattern-D call (or flags a wrongly-excluded same-origin app / a write that should not have shipped). See what-built / how-to-verify / resume-signal below.</action>
  <what-built>The complete Phase 41 depth head: 7 guarded WRITE heads (3 gitlab + 3 notion + 1 slack) all fail-closed to RECIPE_DOM_FALLBACK_PENDING, the CORS / first-party-origin verification gate wired into validate:extension, the SC2 sensitive-origin mutating-opt-in proof on slack.send_message, INV-03 byte-stability extended, head cap ≤30, and the recorded Pattern-D deferral. All headless gates pass; the 7 guarded writes ship INERT (no mutation fires) until a live-captured body activates each.</what-built>
  <how-to-verify>
    1. Run `npm test` and confirm it exits 0 (the full suite, incl guarded-write-failclosed + the SC2 slack block).
    2. Run `npm run validate:extension` and confirm it exits 0 (incl the new CORS-gate).
    3. Review `.planning/phases/41-depth-2-remaining-hand-ports-guarded-writes/41-HUMAN-UAT.md` — confirm the 7 guarded-write live-capture rows are the ONLY outstanding debt (they ship fail-closed; nothing mutates until you capture each body on an authenticated tab).
    4. Review `41-DEFERRAL.md` — confirm the Pattern-D / demote-to-T3-DOM decision is the right conservative call, OR flag if a same-origin app was wrongly excluded (the open question for the plan-checker).
    5. Confirm Wall 2 is intact: the executeBoundSpec origin-pin (RECIPE_ORIGIN_MISMATCH) was NOT changed — no cross-origin execution was built.
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 41, or describe issues (e.g. a same-origin app that should have been ported, or a write that should NOT have shipped even fail-closed).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped extension -> the 7 guarded writes | each is INERT (fail-closed) until live-capture; the milestone gate must confirm none can fire a mutation pre-capture |
| build chain -> shipped head set | validate:extension (incl the CORS-gate) is the last gate before ship; a separate-origin head or a missing invariant must red here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-13 | Elevation | Pattern-D cross-origin execution silently introduced (a Wall-2 hole) | mitigate | the CORS-gate FAILS the build on any separate-origin head; Task 1 confirms the origin-pin (RECIPE_ORIGIN_MISMATCH) is unchanged — no cross-origin execution shipped; the deferral is recorded |
| T-41-14 | Repudiation | a guarded write's live-capture debt being lost / fabricated | mitigate | 41-HUMAN-UAT.md records every [ASSUMED-ENDPOINT] write as human_needed (recorded, not fabricated); the blocking human-verify checkpoint surfaces the debt explicitly |
| T-41-SC | Tampering | npm/pip/cargo installs | accept | NO new packages this phase; no install task exists; slopcheck N/A |
</threat_model>

<verification>
- `npm test` exits 0.
- `npm run validate:extension` exits 0 (incl verify-origin-classification.mjs).
- INV-01 (capability-mcp-surface), INV-02 (capability-autopilot-parity + capability-router gate-before-dispatch), INV-03 (consent-mutation-gate byte-equality incl the new write reason), head cap ≤30 (head-handler-cap), the CORS-gate — all green.
- The Wall-2 origin-pin (RECIPE_ORIGIN_MISMATCH) is present + unchanged in capability-router.js.
- 41-DEFERRAL.md records the Pattern-D deferral.
</verification>

<success_criteria>
- Full npm test + validate:extension green; INV-01/02/03 + head-cap + CORS-gate + fail-closed harness all pass.
- Wall 2 confirmed UNCHANGED (no Pattern-D cross-origin execution built).
- The Pattern-D deferral + the live-UAT debt are recorded; the blocking human-verify checkpoint closes the phase.
</success_criteria>

<output>
Create `.planning/phases/41-depth-2-remaining-hand-ports-guarded-writes/41-05-SUMMARY.md` when done
</output>
