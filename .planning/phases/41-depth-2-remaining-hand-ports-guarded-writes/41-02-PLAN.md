---
phase: 41-depth-2-remaining-hand-ports-guarded-writes
plan: 02
type: execute
wave: 2
depends_on: ["41-01"]
files_modified:
  - catalog/handlers/gitlab.js
  - .planning/phases/41-depth-2-remaining-hand-ports-guarded-writes/41-HUMAN-UAT.md
autonomous: true
requirements: [DEPTH-02]

must_haves:
  truths:
    - "gitlab.create_issue, gitlab.create_merge_request, gitlab.create_note each register as T1a write heads on https://gitlab.com and resolve dom->T1a"
    - "Each gitlab write handle() returns the dual-field RECIPE_DOM_FALLBACK_PENDING and NEVER calls ctx.executeBoundSpec (no mutation fires)"
    - "Each gitlab write carries a closed params schema (additionalProperties:false) + an [ASSUMED-ENDPOINT] marker + a 41-HUMAN-UAT.md row"
  artifacts:
    - path: "catalog/handlers/gitlab.js"
      provides: "3 guarded gitlab write heads appended to the existing READ module"
      contains: "gitlab.create_issue"
    - path: ".planning/phases/41-depth-2-remaining-hand-ports-guarded-writes/41-HUMAN-UAT.md"
      provides: "The live-UAT manifest (human_needed mutation-body capture rows)"
      contains: "gitlab.create_issue"
  key_links:
    - from: "catalog/handlers/gitlab.js"
      to: "catalog/descriptors/opentabs__gitlab__create_issue.json"
      via: "slug-exact dom->T1a upgrade"
      pattern: "gitlab\\.create_issue"
---

<objective>
Append 3 guarded WRITE heads to the existing GitLab handler module (catalog/handlers/
gitlab.js) on the verified same-origin app https://gitlab.com:

- `gitlab.create_issue`        (write) — POST /api/v4/projects/:id/issues
- `gitlab.create_merge_request` (write) — POST /api/v4/projects/:id/merge_requests
- `gitlab.create_note`         (write) — POST /api/v4/projects/:id/issues/:iid/notes

Each ships FAIL-CLOSED exactly like github.issues.create: handle() returns the dual-field
`RECIPE_DOM_FALLBACK_PENDING` (reason `unverified-gitlab-<verb>-mutation`, fellBackToDom:true)
and NEVER calls ctx.executeBoundSpec — no mutation fires. Each carries a closed params
schema (additionalProperties:false), an `[ASSUMED-ENDPOINT]` marker, and registers the EXACT
opentabs write slug so resolve() UPGRADES the breadth descriptor (backing:'dom', write) →
T1a. The VALUE is the scaffolded params+endpoint+gating a single live-capture flips to
executable — without a new hand-port. No new module (writes ADD slugs to the existing gitlab
module; HEAD_HANDLER_MODULES stays 4 ≤ 30).

Purpose: same-origin GitLab writes are the highest-value mutation set the conservative scope
admits (gitlab.com/api/v4 is a PATH on the gitlab.com origin — source-verified same-origin).
Output: 3 guarded write heads + 3 UAT rows.

This wave delivers SC1 because: each write fails closed to RECIPE_DOM_FALLBACK_PENDING until
a live-captured body confirms it (a guarded write never stamps success for an unverified
mutation), each [ASSUMED-ENDPOINT] carries a recorded human_needed live-UAT, and each slug
upgrades its existing opentabs descriptor dom->T1a.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-depth-2-remaining-hand-ports-guarded-writes/41-CONTEXT.md

# The module to EXTEND (READ-only today; the typedRecipeError helper, the registration
# loop, GITLAB_ORIGIN/GITLAB_API_BASE, buildQuery are all already present and reused):
@catalog/handlers/gitlab.js

# The fail-closed write template to copy (github.issues.create, :111-123 — note it does
# NOT call ctx.executeBoundSpec):
@catalog/handlers/github.js

<interfaces>
<!-- Already present in gitlab.js (reuse, do NOT redefine):
       GITLAB_ORIGIN = 'https://gitlab.com'
       GITLAB_API_BASE = GITLAB_ORIGIN + '/api/v4'
       typedRecipeError(code, extra) -> { success:false, code, errorCode, error, ...extra }
       the self-registration loop at the module foot (walks `handlers`, registerHandler
         with tier:'T1a', descriptor.service 'gitlab.com', descriptor.sideEffectClass from
         the handler) — it already carries sideEffectClass through, so a write entry
         registers with sideEffectClass:'write' automatically. -->

<!-- The 3 write descriptors already exist (verified) — register the EXACT dot-form slug:
       opentabs__gitlab__create_issue.json          slug gitlab.create_issue          write/dom
       opentabs__gitlab__create_merge_request.json  slug gitlab.create_merge_request  write/dom
       opentabs__gitlab__create_note.json           slug gitlab.create_note           write/dom -->

<!-- The guarded-write contract (D): each write entry is
       { tier:'T1a', origin:GITLAB_ORIGIN, sideEffectClass:'write', params:<closed schema>,
         async handle(args, ctx){ return typedRecipeError('RECIPE_DOM_FALLBACK_PENDING',
           { slug:'gitlab.create_issue', reason:'unverified-gitlab-create-issue-mutation',
             fellBackToDom:true }); } }
     NO executeBoundSpec call. The params schema mirrors the opentabs descriptor props
     with additionalProperties:false. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Append the 3 guarded gitlab write heads (fail-closed)</name>
  <files>catalog/handlers/gitlab.js</files>
  <behavior>
    - Catalog.resolve('gitlab.create_issue', 'https://gitlab.com').tier === 'T1a' (was T3);
      descriptor.slug byte-exact; descriptor.sideEffectClass === 'write'. Same for
      gitlab.create_merge_request and gitlab.create_note.
    - handle({}, ctx) for each returns code === errorCode === error ===
      'RECIPE_DOM_FALLBACK_PENDING', success:false, fellBackToDom:true, and the recording
      ctx.executeBoundSpec recorder stays EMPTY (no mutation fires).
    - Each write entry's params has additionalProperties:false and the required path props
      (project + title for create_issue; project + source_branch + target_branch + title for
      create_merge_request; project + issue_iid + body for create_note).
  </behavior>
  <action>EDIT catalog/handlers/gitlab.js: inside the existing `var handlers = { ... }`
object, AFTER the 5 read handlers, add 3 closed params schemas (CREATE_ISSUE_PARAMS,
CREATE_MERGE_REQUEST_PARAMS, CREATE_NOTE_PARAMS — each additionalProperties:false, props
mirrored from the opentabs__gitlab__create_*.json descriptor properties, required path
fields set) and 3 write entries. Each write entry is { tier:'T1a', origin:GITLAB_ORIGIN,
sideEffectClass:'write', params:<schema>, async handle(args, ctx){ return
typedRecipeError('RECIPE_DOM_FALLBACK_PENDING', { slug:'<slug>',
reason:'unverified-gitlab-<verb>-mutation', fellBackToDom:true }); } } — copy the
github.issues.create shape EXACTLY (it does NOT call ctx.executeBoundSpec). Add a block
comment above the write section: the writes require the <meta name="csrf-token"> dance + a
live-captured POST body (the gitlab-api.ts mutating path uses the CSRF token), are
[ASSUMED-ENDPOINT], and ship fail-closed until 41-HUMAN-UAT.md is satisfied. Update the
module header docblock to note the 3 guarded writes were added (Phase 41, DEPTH-02). The
existing self-registration loop already carries sideEffectClass through to the descriptor,
so no registration change is needed. NO executeBoundSpec for the [ASSUMED] writes. Per D
(the guarded-WRITE contract): behaviorally identical to its T3-DOM descriptor today (both ->
DOM fallback); the value is the scaffolded params+endpoint+gating.</action>
  <verify>
    <automated>node tests/guarded-write-failclosed.test.js 2>&1 | grep -E "gitlab.create_(issue|merge_request|note)" | grep -v FAIL && node tests/head-handler-upgrade.test.js 2>&1 | grep "gitlab.create_issue" | grep -q "T1a" && node tests/head-handler-cap.test.js</automated>
  </verify>
  <done>The 3 gitlab write slugs pass the fail-closed harness (RECIPE_DOM_FALLBACK_PENDING + empty recorder), resolve dom->T1a in the upgrade harness, and the head cap stays ≤30 (still 4 modules).</done>
</task>

<task type="auto">
  <name>Task 2: Record the gitlab guarded-write live-UAT rows</name>
  <files>.planning/phases/41-depth-2-remaining-hand-ports-guarded-writes/41-HUMAN-UAT.md</files>
  <action>Create 41-HUMAN-UAT.md using the 29-HUMAN-UAT.md tabular format (frontmatter
phase/plan/status:human_needed/created; a "Why human_needed" paragraph; a table with columns
ID | Service | op (slug) | Procedure | Expected | Status). Add 3 rows for the gitlab writes
(gitlab.create_issue / create_merge_request / create_note): Procedure = "on an authenticated
gitlab.com tab, capture the real mutation request (method/path/body incl the CSRF token
placement) via the network-capture path; record the observed shape redacted + date + Chrome
version + commit"; Expected = "the [ASSUMED-ENDPOINT] POST /api/v4/... body is confirmed; a
future flip activates the handler"; Status = "human_needed (writes ship FAIL-CLOSED — nothing
mutates until capture)". State explicitly this is carried-forward user-gated debt, NON-blocking
for CI (the headless gates pass; the writes return RECIPE_DOM_FALLBACK_PENDING). Per D
(Live-UAT recording): recorded, NOT fabricated.</action>
  <verify>
    <automated>test -f .planning/phases/41-depth-2-remaining-hand-ports-guarded-writes/41-HUMAN-UAT.md && grep -c "gitlab.create_" .planning/phases/41-depth-2-remaining-hand-ports-guarded-writes/41-HUMAN-UAT.md | grep -qE "[3-9]" && echo "3 gitlab UAT rows present"</automated>
  </verify>
  <done>41-HUMAN-UAT.md exists with 3 gitlab guarded-write rows in the 29-HUMAN-UAT tabular format, marked human_needed + non-blocking.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI/router -> gitlab write handler | a write to gitlab.com mutates project data (issues/MRs/notes) on the user's account |
| gitlab.com origin -> session cookie | the session cookie attaches only on the first-party origin (same-origin); the public api-host subdomain is forbidden |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-04 | Spoofing | gitlab.create_* stamping success for an unverified mutation | mitigate | fail-closed: handle() returns RECIPE_DOM_FALLBACK_PENDING and never calls executeBoundSpec (proven by guarded-write-failclosed.test.js); endpoint/body are [ASSUMED] until 41-HUMAN-UAT capture |
| T-41-05 | Elevation | a gitlab write running under Auto without consent on a sensitive org | accept (covered) | gitlab.com is NOT in sensitiveOrigins; non-sensitive writes pass under Auto per posture B — but the write is INERT (fail-closed), so no mutation occurs regardless; the consent gate is reused UNCHANGED |
| T-41-06 | Information disclosure | the CSRF token leaking into a log | mitigate | the handler scrapes NO token (it fails closed before any spec build); when a future live-capture activates it, the token must stay only inside the bound spec (the github/gitlab discipline, asserted by the head-handlers no-token-logging scan) |
</threat_model>

<verification>
- `node tests/guarded-write-failclosed.test.js` — the 3 gitlab write rows GREEN (RECIPE_DOM_FALLBACK_PENDING + empty recorder).
- `node tests/head-handler-upgrade.test.js` — gitlab.create_issue/create_merge_request/create_note resolve T1a + write class.
- `node tests/head-handler-cap.test.js` — still 4 modules ≤ 30.
- `node tests/capability-head-handlers.test.js` — no separate-origin api host appears in gitlab.js.
- 41-HUMAN-UAT.md carries the 3 gitlab rows.
</verification>

<success_criteria>
- 3 gitlab guarded write heads on https://gitlab.com, each fail-closed (no executeBoundSpec), closed params, [ASSUMED-ENDPOINT].
- Each slug upgrades its opentabs descriptor dom->T1a (slug-exact); descriptor.sideEffectClass === 'write'.
- Head cap unchanged (4 modules). 41-HUMAN-UAT.md records the 3 live-UAT rows.
</success_criteria>

<output>
Create `.planning/phases/41-depth-2-remaining-hand-ports-guarded-writes/41-02-SUMMARY.md` when done
</output>
