---
phase: 41-depth-2-remaining-hand-ports-guarded-writes
plan: 03
type: execute
wave: 2
depends_on: ["41-01"]
files_modified:
  - catalog/handlers/notion.js
autonomous: true
requirements: [DEPTH-02]

must_haves:
  truths:
    - "notion.create_page, notion.update_page, notion.create_database_item each register as T1a write heads on https://www.notion.so and resolve dom->T1a"
    - "Each notion write handle() returns the dual-field RECIPE_DOM_FALLBACK_PENDING and NEVER calls ctx.executeBoundSpec (no mutation fires)"
    - "Each notion write carries a closed params schema (additionalProperties:false) + an [ASSUMED-ENDPOINT] marker"
  artifacts:
    - path: "catalog/handlers/notion.js"
      provides: "3 guarded notion write heads appended to the existing READ module"
      contains: "notion.create_page"
  key_links:
    - from: "catalog/handlers/notion.js"
      to: "catalog/descriptors/opentabs__notion__create_page.json"
      via: "slug-exact dom->T1a upgrade"
      pattern: "notion\\.create_page"
---

<objective>
Append 3 guarded WRITE heads to the existing Notion handler module (catalog/handlers/
notion.js) on the verified same-origin app https://www.notion.so:

- `notion.create_page`          (write) — POST /api/v3 (RPC: submitTransaction / createPage)
- `notion.update_page`          (write) — POST /api/v3 (RPC: submitTransaction page update)
- `notion.create_database_item` (write) — POST /api/v3 (RPC: submitTransaction row insert)

Each ships FAIL-CLOSED exactly like github.issues.create: handle() returns the dual-field
`RECIPE_DOM_FALLBACK_PENDING` (reason `unverified-notion-<verb>-mutation`, fellBackToDom:true)
and NEVER calls ctx.executeBoundSpec — no mutation fires. Each carries a closed params schema
(additionalProperties:false), an `[ASSUMED-ENDPOINT]` marker, and registers the EXACT opentabs
write slug so resolve() UPGRADES the breadth descriptor (backing:'dom', write) → T1a. No new
module (writes ADD slugs to the existing notion module; HEAD_HANDLER_MODULES stays 4 ≤ 30).

NOTE on the write set: `notion.append_block` was considered but its opentabs descriptor is
sideEffectClass:'read' (verified), so it is NOT a write head — `notion.create_database_item`
(verified write) is used instead to keep 3 genuine writes.

Purpose: same-origin Notion writes (www.notion.so/api/v3 is a PATH on the www.notion.so
origin — source-verified same-origin; the token_v2 HttpOnly cookie authenticates).
Output: 3 guarded write heads. The notion live-UAT rows are added in Plan 04 (which owns the
consolidated UAT manifest after the slack write decision).

This wave delivers SC1 because: each write fails closed to RECIPE_DOM_FALLBACK_PENDING until
a live-captured body confirms it (a guarded write never stamps success for an unverified
mutation), and each slug upgrades its existing opentabs descriptor dom->T1a (slug-exact).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-depth-2-remaining-hand-ports-guarded-writes/41-CONTEXT.md

# The module to EXTEND (READ-only today; typedRecipeError, buildRpcSpec, NOTION_ORIGIN,
# the registration loop are all already present and reused):
@catalog/handlers/notion.js

# The fail-closed write template to copy (github.issues.create, :111-123 — note it does
# NOT call ctx.executeBoundSpec):
@catalog/handlers/github.js

<interfaces>
<!-- Already present in notion.js (reuse, do NOT redefine):
       NOTION_ORIGIN = 'https://www.notion.so'
       buildRpcSpec(op, requestBody) -> POST /api/v3/<op>, token_v2 same-origin-cookie
         (USED ONLY by the reads; the guarded writes do NOT call it — they fail closed)
       typedRecipeError(code, extra) -> { success:false, code, errorCode, error, ...extra }
       the self-registration loop at the foot (descriptor.service 'www.notion.so',
         descriptor.sideEffectClass from the handler — carries 'write' through automatically). -->

<!-- The 3 write descriptors already exist (verified) — register the EXACT dot-form slug:
       opentabs__notion__create_page.json           slug notion.create_page           write/dom
       opentabs__notion__update_page.json           slug notion.update_page           write/dom
       opentabs__notion__create_database_item.json  slug notion.create_database_item  write/dom
     (notion.append_block is read/dom — NOT used as a write.) -->

<!-- The guarded-write contract (D): each write entry is
       { tier:'T1a', origin:NOTION_ORIGIN, sideEffectClass:'write', params:<closed schema>,
         async handle(args, ctx){ return typedRecipeError('RECIPE_DOM_FALLBACK_PENDING',
           { slug:'notion.create_page', reason:'unverified-notion-create-page-mutation',
             fellBackToDom:true }); } }
     NO buildRpcSpec, NO executeBoundSpec call. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Append the 3 guarded notion write heads (fail-closed)</name>
  <files>catalog/handlers/notion.js</files>
  <behavior>
    - Catalog.resolve('notion.create_page', 'https://www.notion.so').tier === 'T1a' (was T3);
      descriptor.slug byte-exact; descriptor.sideEffectClass === 'write'. Same for
      notion.update_page and notion.create_database_item.
    - handle({}, ctx) for each returns code === errorCode === error ===
      'RECIPE_DOM_FALLBACK_PENDING', success:false, fellBackToDom:true, and the recording
      ctx.executeBoundSpec recorder stays EMPTY.
    - Each write entry's params has additionalProperties:false and the required fields
      mirrored from the opentabs descriptor props (e.g. create_page: parent + properties/title;
      update_page: page_id + properties; create_database_item: database_id + properties).
  </behavior>
  <action>EDIT catalog/handlers/notion.js: inside the existing `var handlers = { ... }`
object, AFTER the read handlers, add 3 closed params schemas (CREATE_PAGE_PARAMS,
UPDATE_PAGE_PARAMS, CREATE_DATABASE_ITEM_PARAMS — each additionalProperties:false, props
mirrored from the opentabs__notion__create_page/update_page/create_database_item.json
descriptor properties, required fields set) and 3 write entries. Each write entry is {
tier:'T1a', origin:NOTION_ORIGIN, sideEffectClass:'write', params:<schema>, async
handle(args, ctx){ return typedRecipeError('RECIPE_DOM_FALLBACK_PENDING', { slug:'<slug>',
reason:'unverified-notion-<verb>-mutation', fellBackToDom:true }); } } — copy the
github.issues.create shape EXACTLY (it does NOT call buildRpcSpec or ctx.executeBoundSpec).
Add a block comment above the write section: Notion mutations are POST /api/v3
submitTransaction RPCs whose exact op + body shape are [ASSUMED-ENDPOINT] and require a
live-captured body; they ship fail-closed until 41-HUMAN-UAT.md (the notion rows recorded in
Plan 04) is satisfied. Update the module header docblock to note the 3 guarded writes (Phase
41, DEPTH-02) + that append_block stays a READ. The existing registration loop carries
sideEffectClass through, so no registration change is needed. NO executeBoundSpec for the
[ASSUMED] writes. Per D (the guarded-WRITE contract): behaviorally identical to its T3-DOM
descriptor today; the value is the scaffolded params+endpoint+gating.</action>
  <verify>
    <automated>node tests/guarded-write-failclosed.test.js 2>&1 | grep -E "notion.(create_page|update_page|create_database_item)" | grep -v FAIL && node tests/head-handler-upgrade.test.js 2>&1 | grep "notion.create_page" | grep -q "T1a" && node tests/head-handler-cap.test.js</automated>
  </verify>
  <done>The 3 notion write slugs pass the fail-closed harness (RECIPE_DOM_FALLBACK_PENDING + empty recorder), resolve dom->T1a in the upgrade harness, and the head cap stays ≤30 (still 4 modules).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI/router -> notion write handler | a write to www.notion.so mutates page/database content on the user's workspace |
| www.notion.so origin -> token_v2 cookie | the token_v2 HttpOnly cookie authenticates only on the first-party origin (same-origin RPC) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-07 | Spoofing | notion.create_*/update_page stamping success for an unverified RPC mutation | mitigate | fail-closed: handle() returns RECIPE_DOM_FALLBACK_PENDING and never calls executeBoundSpec/buildRpcSpec (proven by guarded-write-failclosed.test.js); the submitTransaction op + body are [ASSUMED] until live-capture |
| T-41-08 | Elevation | a notion write running under Auto without consent | accept (covered) | www.notion.so is NOT in sensitiveOrigins; the write is INERT (fail-closed) regardless; the consent gate is reused UNCHANGED |
| T-41-09 | Tampering | a closed-vocab smuggle of extra fields into a credentialed RPC | mitigate | additionalProperties:false on every write params schema; and the write never reaches a network call anyway (fail-closed) |
</threat_model>

<verification>
- `node tests/guarded-write-failclosed.test.js` — the 3 notion write rows GREEN.
- `node tests/head-handler-upgrade.test.js` — notion.create_page/update_page/create_database_item resolve T1a + write class.
- `node tests/head-handler-cap.test.js` — still 4 modules ≤ 30.
- `node tests/capability-head-handlers.test.js` — no separate-origin api host appears in notion.js.
</verification>

<success_criteria>
- 3 notion guarded write heads on https://www.notion.so, each fail-closed (no executeBoundSpec/buildRpcSpec), closed params, [ASSUMED-ENDPOINT].
- Each slug upgrades its opentabs descriptor dom->T1a (slug-exact); descriptor.sideEffectClass === 'write'.
- append_block stays a read (not a write head). Head cap unchanged (4 modules).
</success_criteria>

<output>
Create `.planning/phases/41-depth-2-remaining-hand-ports-guarded-writes/41-03-SUMMARY.md` when done
</output>
