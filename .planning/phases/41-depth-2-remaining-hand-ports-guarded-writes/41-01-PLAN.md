---
phase: 41-depth-2-remaining-hand-ports-guarded-writes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/guarded-write-failclosed.test.js
  - scripts/verify-origin-classification.mjs
  - tests/head-handler-upgrade.test.js
  - tests/consent-mutation-gate.test.js
  - package.json
autonomous: true
requirements: [DEPTH-02]

must_haves:
  truths:
    - "A guarded write handler that returns RECIPE_DOM_FALLBACK_PENDING AND never calls ctx.executeBoundSpec passes the fail-closed harness; a handler that fires a mutation for an [ASSUMED] write FAILS it"
    - "verify-origin-classification.mjs classifies every HEAD_HANDLER_MODULES head SAME-ORIGIN with its vendored API base-URL, FAILS the build on a separate-origin head (CORS_SEPARATE_ORIGIN), and is wired into validate:extension"
    - "A synthetic separate-origin head (linear -> client-api.linear.app) FAILS the CORS-gate (negative control)"
    - "The new T1a-write RECIPE_DOM_FALLBACK_PENDING reason is asserted byte-equal code===errorCode===error (INV-03)"
    - "head-handler-upgrade.test.js asserts the Phase-41 write slugs resolve dom->T1a with a write sideEffectClass and a byte-exact descriptor.slug"
  artifacts:
    - path: "tests/guarded-write-failclosed.test.js"
      provides: "The SC1 fail-closed proof harness (RECIPE_DOM_FALLBACK_PENDING + empty executeBoundSpec recorder)"
      min_lines: 90
    - path: "scripts/verify-origin-classification.mjs"
      provides: "The CORS / first-party-origin verification gate + negative-control fixture"
      min_lines: 110
    - path: "tests/head-handler-upgrade.test.js"
      provides: "Extended dom->T1a upgrade assertion covering the write slugs"
      contains: "sideEffectClass"
  key_links:
    - from: "scripts/verify-origin-classification.mjs"
      to: "vendor/opentabs-snapshot/plugins/<app>/src/*-api.ts"
      via: "base-URL extraction"
      pattern: "api\\.ts"
    - from: "package.json validate:extension"
      to: "scripts/verify-origin-classification.mjs"
      via: "npm script chain"
      pattern: "verify-origin-classification"
---

<objective>
Build the Wave 0 test infrastructure + the headline CORS-gate BEFORE any guarded write
handler lands. Three new safety nets + two extensions:

1. `tests/guarded-write-failclosed.test.js` (NEW) — the SC1 security keystone. For every
   guarded write handler shipped this phase, prove `handle()` returns the dual-field
   `RECIPE_DOM_FALLBACK_PENDING` AND a recording `ctx.executeBoundSpec` stub stays EMPTY
   (no mutation fires). A handler that calls executeBoundSpec for an `[ASSUMED]` write
   FAILS. Wave-0 RED is correct (the write handlers do not exist yet); turns GREEN as
   plans 02/03/04 land.
2. `scripts/verify-origin-classification.mjs` (NEW) — the CORS / first-party-origin
   verification gate. Iterate every `HEAD_HANDLER_MODULES` head, read each handler's
   origin + the app's real API base-URL from the vendored `<app>-api.ts`, assert
   SAME-ORIGIN, and FAIL the build (`CORS_SEPARATE_ORIGIN`) on a separate-origin head.
   Include a negative-control fixture (linear -> `client-api.linear.app` MUST fail).
   Wire into `validate:extension`.
3. `tests/head-handler-upgrade.test.js` (EXTEND) — add the Phase-41 write slugs to the
   PORTED set so each is asserted to resolve dom->T1a with a WRITE sideEffectClass and a
   byte-exact descriptor.slug (guarded by existsSync — Wave-0 RED until the handlers land).
4. `tests/consent-mutation-gate.test.js` (EXTEND) — add the INV-03 byte-equality assert
   for the new T1a-write fail-closed `RECIPE_DOM_FALLBACK_PENDING` reason.
5. `package.json` — wire `verify-origin-classification.mjs` into `validate:extension` and
   `guarded-write-failclosed.test.js` into `test`.

Purpose: the fail-closed harness + the CORS-gate are both fail-closed guarantees — a
write can never silently fire a mutation, and a separate-origin port can never silently
ship. Both MUST exist before any write handler.
Output: 2 new files, 3 edits, all gates RED-but-deterministic at Wave 0.

This wave delivers SC1 + SC3 + SC4 scaffolding because: the fail-closed harness is the
mechanical proof of SC1 ("a guarded write never stamps success for an unverified
mutation"); the CORS-gate is the mechanical proof of SC3 ("a per-app CORS gate precedes
any separate-origin port; separate origins are demoted"); the INV-03 byte-equality
extension is the SC4 invariant for the new write reason.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-depth-2-remaining-hand-ports-guarded-writes/41-CONTEXT.md
@.planning/phases/41-depth-2-remaining-hand-ports-guarded-writes/41-VALIDATION.md

# The fail-closed write template the harness validates (github.issues.create, :111-123 —
# returns RECIPE_DOM_FALLBACK_PENDING, NEVER calls ctx.executeBoundSpec):
@catalog/handlers/github.js

# The upgrade harness to EXTEND (the PORTED array, the existsSync Wave-0-RED pattern):
@tests/head-handler-upgrade.test.js

# The cap harness (read-only context — confirms HEAD_HANDLER_MODULES = 4 modules today):
@tests/head-handler-cap.test.js

<interfaces>
<!-- The typedRecipeError dual-field shape every handler + the INV-03 assert rely on.
     From catalog/handlers/github.js:67-75 (identical in gitlab/slack/notion). A guarded
     write returns typedRecipeError('RECIPE_DOM_FALLBACK_PENDING',
       { slug, reason:'unverified-<app>-<verb>-mutation', fellBackToDom:true })
     where typedRecipeError sets out.code === out.errorCode === out.error === code and
     out.success === false. -->

<!-- The ctx contract handler.handle(args, ctx) receives (capability-router.js
     _runHandlerTier:635-647): ctx carries { origin, tabId, executeBoundSpec }. The
     fail-closed harness passes a ctx whose executeBoundSpec is a RECORDING STUB that
     pushes every call; the assertion is the recorder stays EMPTY for a write handler:
     ctx = { origin, tabId, executeBoundSpec(spec, tabId){ recorder.push(...); } } -->

<!-- The vendored API base-URLs the CORS-gate reads (already verified):
     gitlab  vendor/opentabs-snapshot/plugins/gitlab/src/gitlab-api.ts -> https://gitlab.com/api/v4   (SAME-ORIGIN: path on gitlab.com)
     notion  vendor/opentabs-snapshot/plugins/notion/src/notion-api.ts -> https://www.notion.so/api/v3 (SAME-ORIGIN: path on www.notion.so)
     slack   vendor/opentabs-snapshot/plugins/slack/src/slack-api.ts  -> https://app.slack.com/api/<method> (SAME-ORIGIN: app.slack.com; workspaceUrl normalized to https://)
     github  https://github.com (SAME-ORIGIN; no vendored plugin)
     linear  vendor/opentabs-snapshot/plugins/linear/src/linear-api.ts -> https://client-api.linear.app/graphql (SEPARATE subdomain — the negative control) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The fail-closed guarded-write harness (SC1 keystone)</name>
  <files>tests/guarded-write-failclosed.test.js</files>
  <behavior>
    - For each Phase-41 guarded write slug (the WRITE_HEADS list): if its handler file
      exists, require it (fresh), invoke handle({}, ctx) with a RECORDING executeBoundSpec
      stub, and assert: (a) result.code === 'RECIPE_DOM_FALLBACK_PENDING', (b)
      result.errorCode === result.error === result.code (dual-field), (c) result.success
      === false, (d) result.fellBackToDom === true, (e) the executeBoundSpec recorder array
      length === 0 (NO mutation fired).
    - NEGATIVE CONTROL: a synthetic in-test handler that DOES call ctx.executeBoundSpec is
      run through the same path; assert the recorder is NON-empty afterward (proves the
      harness actually distinguishes a mutation-firing write from a fail-closed one).
    - Wave-0 RED: a handler file absent OR the slug not yet added -> a single deterministic
      FAIL per slug (mirror the head-handler-upgrade.test.js existsSync pattern); GREEN
      after plans 02/03/04 add the slugs.
    - WRITE_HEADS (slug | handlerFile | origin): gitlab.create_issue / create_merge_request
      / create_note | gitlab.js | https://gitlab.com ; notion.create_page / update_page /
      create_database_item | notion.js | https://www.notion.so ; slack.send_message |
      slack.js | https://app.slack.com.
  </behavior>
  <action>Create tests/guarded-write-failclosed.test.js using the zero-framework FSB
convention (module-level passed/failed counters, check(cond,msg), process.exit(failed>0?1:0),
ASCII-only, NO emojis). Build makeRecordingCtx(origin) returning an object whose
executeBoundSpec pushes {spec,tabId} onto an exposed recorder array and resolves
{success:true}. For each WRITE_HEADS row guarded by fs.existsSync(handlerFile): delete the
require cache, require the handler module, look up its slug entry; if the entry is absent
emit one deterministic FAIL (Wave-0 RED); else await entry.handle({}, ctx) and check the
five fail-closed conditions plus recorder.length === 0. Add the in-test negative-control
handler that calls ctx.executeBoundSpec and assert recorder.length !== 0 after invoking it.
This is the SC1 keystone and MUST land before any write handler so a mutation-firing
[ASSUMED] write reds CI. Per D (the guarded-WRITE contract): the write NEVER calls
executeBoundSpec.</action>
  <verify>
    <automated>node tests/guarded-write-failclosed.test.js; if [ $? -ne 0 ]; then echo "EXPECTED Wave-0 RED (write handlers not yet present)"; else echo "GREEN"; fi</automated>
  </verify>
  <done>The harness exists, exits non-zero at Wave 0 (handlers absent) with one deterministic FAIL per WRITE_HEADS slug, and the negative-control proves it catches a mutation-firing write.</done>
</task>

<task type="auto">
  <name>Task 2: The CORS / first-party-origin verification gate + negative control + validate:extension wiring</name>
  <files>scripts/verify-origin-classification.mjs, package.json</files>
  <action>Create scripts/verify-origin-classification.mjs as a dual-export ESM module
mirroring scripts/verify-no-duplicate-stem.mjs structure (a checkOriginClassification(...)
export driven by a future test + a CLI on direct invocation; ASCII-only, NO emojis, Wall-1
clean — no run-string-as-code in code or comments). Implement classifyOriginPattern(
handlerOrigin, apiBaseUrl) -> { sameOrigin, separate, apiOrigin, reason }: derive the
origin host of handlerOrigin and of apiBaseUrl using URL() (apiBaseUrl may be a PATH like
https://gitlab.com/api/v4 — new URL(apiBaseUrl).origin yields https://gitlab.com);
sameOrigin === (apiOrigin host EQUALS handlerOrigin host); separate === a different host /
subdomain (client-api.linear.app vs linear.app) or a wildcard host. The gate: (1) parse
HEAD_HANDLER_MODULES from extension/utils/capability-catalog.js source (reuse the
array-literal regex + the origin:'...'/global:'...' field regex from
tests/head-handler-cap.test.js); (2) map each head global -> app slug and read the app's
real API base-URL from vendor/opentabs-snapshot/plugins/<app>/src/<app>-api.ts (gitlab-api.ts
gitlab.com/api/v4; notion-api.ts www.notion.so/api/v3; slack-api.ts app.slack.com; github
has no vendored plugin -> use the documented https://github.com base); (3) assert SAME-ORIGIN
for every head; a separate-origin head returns a FAIL with reason CORS_SEPARATE_ORIGIN naming
the head + both origins; (4) NEGATIVE-CONTROL fixture in the same run: classify a synthetic
head { global:'FsbHandlerLinear', origin:'https://linear.app' } against
https://client-api.linear.app/graphql and assert classifyOriginPattern returns separate:true
(the gate WOULD fail it) — this is the proof the gate enforces the linear/datadog/jira
demote-to-T3. Then EDIT package.json validate:extension to chain
`&& node scripts/verify-origin-classification.mjs` AFTER verify-no-duplicate-stem.mjs and
BEFORE the `node --import tsx scripts/verify-no-orphan-descriptor.mjs` step. Per the CORS-gate
decision (D): it is a SHIPPING GATE (port-eligibility), NOT an executor — it enables no
cross-origin call.</action>
  <verify>
    <automated>node scripts/verify-origin-classification.mjs && grep -q "verify-origin-classification" package.json && echo "CORS-gate green + wired"</automated>
  </verify>
  <done>The gate classifies the 4 shipped heads (github/slack/notion/gitlab) SAME-ORIGIN and exits 0; the linear negative-control fixture is proven to classify separate; the script is chained into validate:extension.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the upgrade harness + the INV-03 byte-equality assert + wire the test into npm test</name>
  <files>tests/head-handler-upgrade.test.js, tests/consent-mutation-gate.test.js, package.json</files>
  <action>EXTEND tests/head-handler-upgrade.test.js: append the Phase-41 write slugs to the
PORTED array (gitlab.create_issue / create_merge_request / create_note -> gitlab.js,
https://gitlab.com; notion.create_page / update_page / create_database_item -> notion.js,
https://www.notion.so; slack.send_message -> slack.js, https://app.slack.com), and for these
write rows ADD a check that res.descriptor.sideEffectClass === 'write' (the write slugs
upgrade dom->T1a AND carry the write class — distinct from the read rows). Keep the existing
existsSync Wave-0-RED guard: the handler files already exist, so each write row resolves the
breadth T3 until its plan adds the slug -> a deterministic FAIL (the correct RED), GREEN once
the slug is registered. EXTEND tests/consent-mutation-gate.test.js: after the existing
byte-equality block (around :180-185), add a block that constructs the new T1a-write
fail-closed reason via the same dual-field typedRecipeError shape (success:false, code ===
errorCode === error === 'RECIPE_DOM_FALLBACK_PENDING') and asserts the three fields are
byte-identical — closing INV-03 coverage for the NEW T1a-write reason (the existing asserts
only cover the CONSENT/import reasons). Finally EDIT package.json test script to add
`&& node tests/guarded-write-failclosed.test.js` adjacent to the existing
head-handler-upgrade.test.js entry. Per D (INV-03): all typed reasons flow through the
central dual-field helper -> byte-stable.</action>
  <verify>
    <automated>node tests/head-handler-upgrade.test.js >/dev/null 2>&1; echo "upgrade exit=$? (Wave-0 RED on new write rows expected)"; grep -q "RECIPE_DOM_FALLBACK_PENDING" tests/consent-mutation-gate.test.js && grep -q "guarded-write-failclosed" package.json && echo "INV-03 write-reason assert + npm-test wiring present"</automated>
  </verify>
  <done>head-handler-upgrade.test.js carries the write rows (sideEffectClass write assertion); consent-mutation-gate.test.js asserts the T1a-write RECIPE_DOM_FALLBACK_PENDING is byte-equal across code/errorCode/error; guarded-write-failclosed.test.js is in npm test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time gate -> shipped head set | the CORS-gate decides which heads are ELIGIBLE to ship; an unverified separate-origin head crossing this boundary would be a silent cross-origin credential-replay surface |
| AI/router -> guarded write handler | an [ASSUMED] write that fired a real mutation would mutate user data without a verified endpoint/body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01 | Tampering | verify-origin-classification.mjs base-URL read | mitigate | read the base-URL from the VENDORED pinned-SHA `<app>-api.ts` (provenance-pinned), not a network fetch; the gate is build tooling, never shipped |
| T-41-02 | Elevation | a separate-origin head silently shipping | mitigate | the CORS-gate FAILS the build (CORS_SEPARATE_ORIGIN) on any non-same-origin head; negative-control fixture proves the failure path fires |
| T-41-03 | Spoofing | a guarded write stamping success for an unverified mutation | mitigate | the fail-closed harness asserts the executeBoundSpec recorder stays EMPTY — a write that fires a mutation reds CI |
| T-41-SC | Tampering | npm/pip/cargo installs | mitigate | NO new packages added this phase (pure Node + existing tsx); slopcheck N/A — no install task exists |
</threat_model>

<verification>
- `node tests/guarded-write-failclosed.test.js` — exits non-zero at Wave 0 (deterministic RED), the negative control passes.
- `node scripts/verify-origin-classification.mjs` — exits 0 (4 heads same-origin), linear negative-control classifies separate.
- `node tests/head-handler-upgrade.test.js` — the new write rows are present (Wave-0 RED until their plans land).
- `grep verify-origin-classification package.json` + `grep guarded-write-failclosed package.json` — both wired.
- INV-03: the new T1a-write byte-equality assert is in consent-mutation-gate.test.js.
</verification>

<success_criteria>
- The fail-closed harness exists and is the SC1 keystone (RECIPE_DOM_FALLBACK_PENDING + empty recorder), with a working negative control.
- The CORS-gate exists, classifies the 4 shipped heads SAME-ORIGIN, FAILS on a separate-origin head, and is wired into validate:extension.
- The upgrade harness covers the write slugs (dom->T1a + write class); INV-03 byte-equality covers the new write reason.
- Both new tests are wired into npm test / validate:extension. No new npm packages.
</success_criteria>

<output>
Create `.planning/phases/41-depth-2-remaining-hand-ports-guarded-writes/41-01-SUMMARY.md` when done
</output>
