---
phase: 62-ci-drift-smoke-gate-doctor-extensions
plan: "06"
type: execute
wave: 4
depends_on:
  - "62-01"
  - "62-02"
  - "62-03"
  - "62-04"
  - "62-05"
files_modified:
  - package.json
  - tests/delegation-phase-contract.test.js
  - .planning/phases/62-ci-drift-smoke-gate-doctor-extensions/62-HUMAN-UAT.md
autonomous: true
requirements:
  - DRIFT-01
  - DRIFT-02
  - DRIFT-03
  - DRIFT-04
must_haves:
  truths:
    - "The root serial suite invokes every new Phase 62 test exactly once and invokes the exact same generalized drift harness as CI."
    - "One phase contract pins all 17 task IDs, four requirements, trust boundaries, schemas, source links, forbidden authorities, and preserved Phase 59-61 contracts."
    - "All genuine live/browser/accessibility checks remain visibly pending human_needed in one milestone-end ledger; no synthetic evidence is promoted to a live UAT pass."
  artifacts:
    - path: package.json
      provides: "Single serial repository-suite entry for Phase 62 gates"
      contains: "node tests/mcp-agent-drift-smoke.test.js"
    - path: tests/delegation-phase-contract.test.js
      provides: "Complete Phase 62 source/requirement/security/ownership contract"
      contains: "62-06-03"
    - path: .planning/phases/62-ci-drift-smoke-gate-doctor-extensions/62-HUMAN-UAT.md
      provides: "Pending milestone-end live, rendered, keyboard, and accessibility evidence ledger"
      contains: "status: human_needed"
  key_links:
    - from: package.json
      to: tests/mcp-agent-drift-smoke.test.js
      via: "root serial suite uses the exact direct script used by CI"
      pattern: "node tests/mcp-agent-drift-smoke.test.js"
    - from: tests/delegation-phase-contract.test.js
      to: .github/workflows/ci.yml
      via: "source contract proves CI and root suite share one harness"
      pattern: "Phase 62 adapter drift smoke"
    - from: .planning/phases/62-ci-drift-smoke-gate-doctor-extensions/62-HUMAN-UAT.md
      to: .planning/phases/62-ci-drift-smoke-gate-doctor-extensions/62-UI-SPEC.md
      via: "only genuinely rendered/live requirements remain pending for milestone-end review"
      pattern: "human_needed"
---

<objective>
Close Phase 62 with one root-suite gate, a complete source/requirement/security contract, and an honest milestone-end human-UAT ledger.

Purpose: DRIFT-01 through DRIFT-04 must remain enforceable after individual plans finish. This final plan prevents CI/root harness divergence, source-contract regression, scope expansion, and fabricated live evidence while preserving the guarded repository-suite workflow.
Output: Exact-once serial test entries, final Phase 62 contract assertions, three pending human UAT scenarios, and a separately guarded full-suite command.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/62-ci-drift-smoke-gate-doctor-extensions/62-CONTEXT.md
@.planning/phases/62-ci-drift-smoke-gate-doctor-extensions/62-RESEARCH.md
@.planning/phases/62-ci-drift-smoke-gate-doctor-extensions/62-VALIDATION.md
@.planning/phases/62-ci-drift-smoke-gate-doctor-extensions/62-UI-SPEC.md
@.planning/phases/61-delegation-ux-sw-eviction-persistence/61-08-SUMMARY.md
@package.json
@tests/delegation-phase-contract.test.js
@scripts/run-phase60-full-tests.mjs
</context>

<interfaces>
The final task ownership map is normative:

```text
62-01-01 matrix/classifier            62-01-02 detector/profile
62-01-03 drift smoke + CI             62-02-01 doctor collection
62-02-02 doctor rendering             62-03-01 authenticated request
62-03-02 storage/freshness            62-03-03 bridge/background refresh
62-04-01 daemon terminal detail       62-04-02 pre-throttled reporter
62-04-03 exact-once background report 62-05-01 pure UI mapping
62-05-02 DOM/refresh/a11y              62-05-03 CSS/responsive
62-06-01 root serial suite            62-06-02 final phase contract
62-06-03 UAT ledger/full-suite guard
```

The root suite and CI both execute the exact literal `node tests/mcp-agent-drift-smoke.test.js`. The human ledger uses unchecked `### [ ] UAT62-*` headings with `status: human_needed`, `result: pending`, and empty evidence until a person performs the milestone-end checks.
</interfaces>

<threat_model>
Boundary: repository scripts and source-pin tests decide whether Phase 62 evidence is accepted, while human observations may later enter the planning ledger.

- T62-01 policy divergence: contract pins one frozen daemon matrix and forbids duplicate detector/doctor/extension version policy.
- T62-02 harness bypass: root and CI must invoke the same production-registry/parser drift script exactly once; no copied parser, live binary, or fixture-provenance relabeling can substitute.
- T62-03 doctor leakage/mutation: contract pins exact local doctor fields, three auth metadata keys, unknown/Not reported auth, offline operation, and secret/path separation from browser data.
- T62-04 reverse-channel authority: contract pins authenticated exact-empty-payload `adapter.compatibility`, safe response keys, and unchanged exact-five transport errors/process authority.
- T62-05 storage/freshness poisoning: contract pins exact plain-object validation, durable serialized replacement, preserved envelope keys, stale-supported downgrade, and fail-closed cache/write failures.
- T62-06 UI injection/state mutation: contract pins closed badge/copy/DOM/a11y/CSS mapping, agent-only rows, no version parsing, and no change to selection/recommendation/preflight/form/focus/storage.
- T62-07 raw drift leakage: contract pins exact `{adapterId, expected, observed}` terminal/report context and excludes raw line/message/issue/path/version/session/task/provider/secret data.
- T62-08 diagnostic flooding/duplication: contract pins exact-once authoritative-final reporting, bounded delegation dedupe, and ten-second per-adapter admission before `rateLimitedWarn`.
- Exactness/idempotence: all 17 IDs and four DRIFT requirements appear once in the contract map; every new root command appears once; re-running contract/UAT generation does not duplicate commands, tests, headings, or claim new evidence.
- Human-evidence integrity: source/DOM/synthetic tests cannot mark live CLI, rendered visual, keyboard, screen-reader, focus, forced-color, or live refresh checks passed. Evidence remains empty/pending until milestone-end human execution.
- Security gate: any unresolved High/Critical T62-01..T62-08 gap, missing requirement/task, duplicate/bypassed harness, forbidden authority, protected-workspace damage, or fabricated UAT pass blocks completion.
</threat_model>

<tasks>

<task type="auto">
  <name>62-06-01: Add every Phase 62 gate exactly once to the root serial suite</name>
  <files>package.json</files>
  <read_first>package.json, .github/workflows/ci.yml, tests/mcp-adapter-compatibility.test.js, tests/mcp-agent-drift-smoke.test.js, tests/agent-protocol-drift-diagnostics.test.js, tests/delegation-phase-contract.test.js, scripts/run-phase60-full-tests.mjs</read_first>
  <action>Edit only the existing root `test` script, preserving its serial `&amp;&amp;` topology, every existing command, and the MCP build boundary. Add each newly created Phase 62 script exactly once: `node tests/mcp-adapter-compatibility.test.js` and `node tests/mcp-agent-drift-smoke.test.js` after `npm --prefix mcp run build` with the adapter/parser block, and `node tests/agent-protocol-drift-diagnostics.test.js` with extension diagnostics/background tests. Existing expanded doctor, supervisor, storage, bridge, Providers, and delegation-contract tests already occur once and must not be duplicated or reordered unnecessarily. The drift entry must be the exact literal `node tests/mcp-agent-drift-smoke.test.js`, identical to the named CI step; do not wrap, rename, shard, skip, or substitute it. Keep `scripts/run-phase60-full-tests.mjs` out of the root test string because it is the separately guarded full-suite runner, not another nested test entry. Use a focused source assertion to prove exact-once counts and preservation before running any tests.</action>
  <verify>
    <automated>node -e "const p=require('./package.json');const s=p.scripts&amp;&amp;p.scripts.test||'';for(const x of ['node tests/mcp-adapter-compatibility.test.js','node tests/mcp-agent-drift-smoke.test.js','node tests/agent-protocol-drift-diagnostics.test.js'])if(s.split(x).length-1!==1)process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - Root `test` contains compatibility, generalized drift, and reporter scripts exactly once each and retains every preexisting command.
    - Root and `.github/workflows/ci.yml` use the exact direct drift command `node tests/mcp-agent-drift-smoke.test.js`.
    - MCP-dependent gates remain after the existing MCP build and the suite remains serial/fail-fast with `&amp;&amp;`.
    - The guarded full-suite runner is not nested into `package.json` and no live provider/browser/native command is added.
  </acceptance_criteria>
  <done>The normal serial repository suite enforces all new Phase 62 automated gates with no duplicate or alternate drift path.</done>
</task>

<task type="auto">
  <name>62-06-02: Pin the complete Phase 62 source, requirement, and security contract</name>
  <files>tests/delegation-phase-contract.test.js</files>
  <read_first>tests/delegation-phase-contract.test.js, .planning/phases/62-ci-drift-smoke-gate-doctor-extensions/62-CONTEXT.md, .planning/phases/62-ci-drift-smoke-gate-doctor-extensions/62-RESEARCH.md, .planning/phases/62-ci-drift-smoke-gate-doctor-extensions/62-VALIDATION.md, .planning/phases/62-ci-drift-smoke-gate-doctor-extensions/62-UI-SPEC.md, .planning/REQUIREMENTS.md, .github/workflows/ci.yml, package.json</read_first>
  <action>Extend the existing delegation phase contract with one Phase 62 section that maps exactly the 17 IDs listed in this plan to exclusive files/behaviors and covers DRIFT-01, DRIFT-02, DRIFT-03, and DRIFT-04. Source-pin the canonical matrix/schema/reasons/range and Claude-only row; manifest path/provenance/liveCapturePending; production registry `parseEvents` smoke; exact CI/root command and CI step name; doctor text/JSON parity, local-only path/version, auth `unknown`/`Not reported`, and exact three bridge-auth metadata keys; separately authenticated `adapter.compatibility`/`{}` safe shape; durable storage/freshness/fail-closed behavior; exact drift terminal/reporter detail, exact-once bounded dedupe, and ten-second pre-throttle; exact UI labels/copy/icons/agent-only DOM/a11y/live-region/token/breakpoint/no-mutation contracts; and T62-01 through T62-08. Preserve and assert the five adapter methods (`detect`, `buildSpawn`, `parseEvents`, `kill`, `caps`), exact-five transport errors (`timeout`, `not_connected`, `send_failed`, `remote_error`, `disconnected`), three agent plus seven API provider roster/order, and existing extension provider parity. Add negative source guards across extension code for compatibility/version constants or semver parsing, binary paths, direct doctor/daemon compatibility calls from UI, shell/process execution, `nativeMessaging`, wake behavior, and new OpenCode/Codex adapters; guard daemon/browser projections for secrets/sessions/prompts/provider output. Assert only Claude is shipped in the Phase 62 matrix. Pin that all UAT headings remain unchecked/pending and that no plan/fixture claims genuine live capture. Keep assertions robust to harmless formatting but exact on security/schema/ownership counts.</action>
  <verify>
    <automated>node tests/delegation-phase-contract.test.js &amp;&amp; node tests/mcp-adapter-compatibility.test.js &amp;&amp; node tests/mcp-reverse-channel-contract.test.js &amp;&amp; node tests/providers-panel-logic.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Contract maps all 17 task IDs exactly once and covers DRIFT-01..04 plus T62-01..08 without assigning final responsibilities to Plan 62-05.
    - Source assertions pin every daemon/doctor/transport/storage/drift/UI/root/UAT schema and key link named above.
    - Existing five adapter methods, exact-five transport errors, provider roster/order, and Phase 59-61 delegation authority remain explicit and green.
    - Negative guards reject extension version policy, native/shell/process/doctor/wake authority, secret/path leakage, OpenCode/Codex shipping, and fabricated live evidence.
  </acceptance_criteria>
  <done>One durable contract makes Phase 62 requirement, ownership, preserved-interface, and threat coverage executable.</done>
</task>

<task type="auto">
  <name>62-06-03: Record all genuine UAT as pending and preserve the guarded full-suite workflow</name>
  <files>.planning/phases/62-ci-drift-smoke-gate-doctor-extensions/62-HUMAN-UAT.md, tests/delegation-phase-contract.test.js</files>
  <read_first>.planning/phases/62-ci-drift-smoke-gate-doctor-extensions/62-UI-SPEC.md, .planning/phases/62-ci-drift-smoke-gate-doctor-extensions/62-VALIDATION.md, .planning/phases/61-delegation-ux-sw-eviction-persistence/61-HUMAN-UAT.md, tests/delegation-phase-contract.test.js, scripts/run-phase60-full-tests.mjs</read_first>
  <action>Create `62-HUMAN-UAT.md` as a milestone-end ledger, not a pass report. Include exactly three unchecked headings and keep each scenario `status: human_needed`, `result: pending`, and `evidence:` empty: (1) `UAT62-01` runs the installed Claude Code/local doctor text and JSON paths with bridge/browser offline and online as applicable, corroborates supported/newer/degraded/unsupported behavior, manually confirms no secret/private auth/provider/task data, captures one genuine sanitized Claude JSONL stream, and compares its required raw `system/init` and `result` fields plus provider-native and normalized sequences against the committed contract fixture; keep fixture provenance `schema-derived-contract` and `liveCapturePending: true` unchanged and pending until a human reviews that genuine-stream comparison at the milestone-end sweep; (2) `UAT62-02` visually compares Supported/Degraded/Unsupported badges and selected details in light/dark, desktop, compact, 641-899px, and at-most-640px layouts, including wrapping/dividers/no overflow and API-row absence; (3) `UAT62-03` exercises keyboard/native radio group, screen reader names/descriptions, focus retention, single live-region success/failure behavior, forced colors, reduced motion, and live refresh projections for fresh/stale/corrupt/absent/failure evidence without selection/form/recommendation mutation. State prominently that all three are deferred to one milestone-end sweep and automated/source/DOM evidence cannot check them off. Extend the phase contract to enforce exact heading count/IDs, unchecked markers, pending values, empty evidence, the UAT62-01 genuine sanitized-stream comparison and retained pending provenance, and absence of pass/approved/screenshots claims. Do not run an installed CLI, browser, accessibility tool, native host, or the full suite in this task.</action>
  <verify>
    <automated>node tests/delegation-phase-contract.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Ledger contains exactly UAT62-01..03 as unchecked headings with human_needed/pending/empty evidence and explicit milestone-end deferral.
    - Scenarios cover genuine local CLI/doctor/security, one genuine sanitized Claude stream compared for required init/result fields and provider-native/normalized sequences while provenance remains pending, rendered badges/layout/theme, and keyboard/screen-reader/focus/live-region/forced-color/live-refresh behavior.
    - Contract fails if any item is marked passed/approved, receives fabricated evidence, loses required coverage, or gains an extra live claim.
    - No live/human UAT or full-suite command is executed during autonomous Phase 62 work.
  </acceptance_criteria>
  <done>All non-automatable evidence is honestly queued for milestone-end human review and cannot be silently promoted to passed.</done>
</task>

</tasks>

<verification>
Run the focused exact-once package assertion and Phase 62 contract/focused scripts above; each command must finish under 60 seconds. Do not treat the guarded full suite or any human UAT as part of these automated task verifications.
</verification>

<guarded_full_suite>node scripts/run-phase60-full-tests.mjs</guarded_full_suite>

The guarded command is a several-minute, final integration check only after focused verification. Its existing workspace-preservation contract must remain intact: protected generated files are hashed before/after, the repository index and user worktree are preserved, temporary link/artifact state is cleaned, and no unrelated dirty file is modified. Do not execute it as planning or human UAT.

<success_criteria>
- DRIFT-01..04 are enforced by one exact root/CI harness path and one complete phase contract.
- All 17 task IDs, T62-01..08, preserved Phase 59-61 interfaces, forbidden authorities, and source links are pinned.
- Three genuine live/rendered/accessibility scenarios remain pending human_needed for the milestone end with empty evidence.
- Focused checks are under 60 seconds; the several-minute full suite remains separately guarded and workspace-safe.
</success_criteria>

<output>
After completion, create `.planning/phases/62-ci-drift-smoke-gate-doctor-extensions/62-06-SUMMARY.md`.
</output>
