---
phase: 18-shared-tool-registry-dispatcher-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extension/ai/tool-definitions.js
  - mcp/ai/tool-definitions.cjs
  - tests/tool-definitions-parity.test.js
  - tests/visual-session-schema-lock.test.js
autonomous: true
requirements: [REG-01, REG-03, REG-04, TRIG-01, LIFE-01, LIFE-02, LIFE-03]
user_setup: []

must_haves:
  truths:
    - "The trigger family is defined exactly once in the shared TOOL_REGISTRY"
    - "The MCP CJS mirror is byte-identical to the extension registry"
    - "Existing tool definitions remain unchanged except for additive trigger-family entries"
    - "Autopilot provider formatting exposes all four trigger tools across xai, openai, anthropic, gemini, openrouter, lmstudio, and custom"
  artifacts:
    - path: "extension/ai/tool-definitions.js"
      provides: "canonical trigger, stop_trigger, get_trigger_status, list_triggers definitions"
      contains: "name: 'trigger'"
    - path: "mcp/ai/tool-definitions.cjs"
      provides: "byte-identical MCP mirror of the shared registry"
      contains: "name: 'stop_trigger'"
    - path: "tests/tool-definitions-parity.test.js"
      provides: "registry additivity, mirror identity, and provider visibility assertions"
      contains: "TRIGGER_TOOL_NAMES"
    - path: "tests/visual-session-schema-lock.test.js"
      provides: "trigger companion bypass/no-visual-field schema lock"
      contains: "stop_trigger"
  key_links:
    - from: "extension/ai/tool-definitions.js"
      to: "mcp/ai/tool-definitions.cjs"
      via: "byte-identical file mirror"
      pattern: "Buffer.compare"
    - from: "extension/ai/agent-loop.js getPublicTools"
      to: "extension/ai/tool-use-adapter.js formatToolsForProvider"
      via: "shared registry output"
      pattern: "formatToolsForProvider"
---

<objective>
Add the four trigger tools once to the canonical shared registry and lock the schema/additivity/provider-parity tests around that surface.

Purpose: Phase 18's INV-01/INV-02 point starts with a single shared schema stack. MCP and autopilot must both consume the same definitions; this plan prevents MCP-only or autopilot-only drift before any dispatcher wiring lands.
Output: additive trigger-family registry entries, byte-identical MCP mirror, and focused parity/schema-lock/provider assertions.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-shared-tool-registry-dispatcher-wiring/18-CONTEXT.md
@.planning/phases/18-shared-tool-registry-dispatcher-wiring/18-RESEARCH.md
@.planning/phases/18-shared-tool-registry-dispatcher-wiring/18-PATTERNS.md
@.planning/phases/18-shared-tool-registry-dispatcher-wiring/18-VALIDATION.md
@extension/ai/tool-definitions.js
@mcp/ai/tool-definitions.cjs
@extension/ai/agent-loop.js
@extension/ai/tool-use-adapter.js
@tests/tool-definitions-parity.test.js
@tests/visual-session-schema-lock.test.js

<interfaces>
Shared registry helpers:
```javascript
const TOOL_REGISTRY = [ /* ToolDefinition[] */ ];
function getToolByName(name) {
  return TOOL_REGISTRY.find(t => t.name === name) || null;
}
function getReadOnlyTools() {
  return TOOL_REGISTRY.filter(t => t._readOnly);
}
function getToolsByRoute(route) {
  return TOOL_REGISTRY.filter(t => t._route === route);
}
```

Autopilot provider visibility:
```javascript
function getPublicTools() {
  return _al_TOOL_REGISTRY.map(t => ({
    name: t.name,
    description: t.description,
    inputSchema: t.inputSchema
  }));
}
function formatToolsForProvider(tools, provider) {
  // anthropic -> input_schema, gemini -> functionDeclarations,
  // default -> OpenAI-compatible function envelope
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Wave 0 registry parity and provider visibility assertions</name>
  <files>tests/tool-definitions-parity.test.js, tests/visual-session-schema-lock.test.js</files>
  <read_first>
    - tests/tool-definitions-parity.test.js current byte-identity and schema sanity checks
    - tests/visual-session-schema-lock.test.js current action/read-only classification locks
    - extension/ai/tool-use-adapter.js formatToolsForProvider provider envelope cases
    - extension/ai/agent-loop.js getPublicTools stripping behavior
    - .planning/phases/18-shared-tool-registry-dispatcher-wiring/18-CONTEXT.md D-01, D-02, D-04, D-06, D-14
    - .planning/phases/18-shared-tool-registry-dispatcher-wiring/18-VALIDATION.md Wave 0 requirements
  </read_first>
  <behavior>
    - Tests fail until all four trigger tools exist in the shared registry and CJS mirror.
    - Tests assert the trigger family appears exactly once and does not remove or mutate existing known tool entries.
    - Tests assert `stop_trigger`, `get_trigger_status`, and `list_triggers` are bypass-class definitions with no visual-session fields.
    - Tests assert all seven provider keys expose all four trigger tool names through the shared registry formatting path.
  </behavior>
  <action>
    Extend `tests/tool-definitions-parity.test.js` with a `TRIGGER_TOOL_NAMES = ['trigger', 'stop_trigger', 'get_trigger_status', 'list_triggers']` constant and assertions that:
    - each name resolves via `td.getToolByName(name)`;
    - each appears exactly once in `td.TOOL_REGISTRY`;
    - every trigger tool has `_route: 'background'`;
    - `trigger._readOnly === false`;
    - `stop_trigger`, `get_trigger_status`, and `list_triggers` each have `_readOnly === true`;
    - no pre-existing tool from the current registry disappears after the edit. Implement this by recording a small locked list already asserted in the file, not by deriving an empty target from the new registry.

    Also import/require `extension/ai/tool-use-adapter.js` and format public registry entries for provider keys `['xai','openai','anthropic','gemini','openrouter','lmstudio','custom']`. Assert each formatted envelope contains all four trigger tool names, using provider-specific extraction for Anthropic, Gemini, and the default OpenAI-compatible shape.

    Extend `tests/visual-session-schema-lock.test.js` by adding the three companion tools to the read-only/no-visual-field checks without adding them to the canonical action-tool field-bundle list. Keep the existing 36 action-tool count intact and update the read-only count from 15 to 18. Add an explicit assertion that `trigger` is not in the visual-session action list and has no `visual_reason`, `client`, or `is_final` schema fields because it is registered through the trigger-specific path planned in Phase 18, not the ordinary manual action path.
  </action>
  <acceptance_criteria>
    - `node tests/tool-definitions-parity.test.js && node tests/visual-session-schema-lock.test.js` fails before the registry entries are added and exits 0 after Task 2.
    - The new tests mention `TRIGGER_TOOL_NAMES` and all four trigger names.
    - `VISUAL_SESSION_ACTION_TOOLS.length` stays 36.
    - `getReadOnlyTools().length` expectation is updated to include the three companion tools.
  </acceptance_criteria>
  <verify>
    <automated>node tests/tool-definitions-parity.test.js && node tests/visual-session-schema-lock.test.js</automated>
  </verify>
  <done>Wave 0 registry/provider/schema tests exist and lock the Phase 18 shared-tool surface before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add trigger-family definitions once and mirror them byte-identically</name>
  <files>extension/ai/tool-definitions.js, mcp/ai/tool-definitions.cjs, tests/tool-definitions-parity.test.js, tests/visual-session-schema-lock.test.js</files>
  <read_first>
    - extension/ai/tool-definitions.js existing background and read-only tool shapes
    - mcp/package.json build script that copies extension/ai/tool-definitions.js to mcp/ai/tool-definitions.cjs
    - extension/utils/trigger-manager.js armTrigger spec comments and condition kinds
    - extension/content/messaging.js triggerRead/triggerObserveStart/triggerPulseStart parameter names
    - .planning/phases/18-shared-tool-registry-dispatcher-wiring/18-CONTEXT.md D-01, D-02, D-03, D-11, D-12, D-13
  </read_first>
  <behavior>
    - `trigger` accepts one selector, one condition object, watch mode, target tab, and extraction/refresh-poll options.
    - `stop_trigger` requires a `trigger_id` and is cancellation-critical bypass-class even though it mutates trigger state.
    - `get_trigger_status` requires a `trigger_id` and is bypass-class.
    - `list_triggers` accepts optional status/include_terminal filters and is bypass-class.
    - Existing registry entries are not wrapped, renamed, reordered for functional reasons, or otherwise changed.
  </behavior>
  <action>
    In `extension/ai/tool-definitions.js`, add a new trigger-family section near the other background/read-only tool groups. Add exactly four plain registry entries, not a second registry:
    - `trigger`: `_route: 'background'`, `_readOnly: false`, `_contentVerb: null`, `_cdpVerb: null`, `_forceForeground: false`, `_emitChangeReport: false`. Its schema must include `selector` string, `condition` object, optional `watch` enum `['live-observe','refresh-poll']`, optional `tab_id` number, optional `target_tab_id` number alias, optional `extract` enum `['text','number','attribute']`, optional `attribute`/`attrName`, optional `poll_interval_ms`, optional `locale`, and optional `decimal_separator`. Required fields: `selector`, `condition`.
    - `stop_trigger`: `_route: 'background'`, `_readOnly: true`, required `trigger_id`, optional `tab_id`.
    - `get_trigger_status`: `_route: 'background'`, `_readOnly: true`, required `trigger_id`, optional `tab_id`.
    - `list_triggers`: `_route: 'background'`, `_readOnly: true`, optional `status`, optional `include_terminal`, optional `tab_id`.

    Do not wrap any trigger-family entry with `withVisualSessionFields`; these tools must not require `visual_reason` or `client`. Keep condition as a JSON Schema object because Phase 15 owns nested condition validation and Phase 18 background handlers must also validate before arming.

    Mirror the final file byte-identically to `mcp/ai/tool-definitions.cjs` using the repository's existing copy pattern or `cp extension/ai/tool-definitions.js mcp/ai/tool-definitions.cjs`. Do not manually diverge the CJS file.
  </action>
  <acceptance_criteria>
    - `Buffer.compare` in `tests/tool-definitions-parity.test.js` passes.
    - `grep -c "name: 'trigger'" extension/ai/tool-definitions.js mcp/ai/tool-definitions.cjs` returns 1 per file.
    - `grep -c "withVisualSessionFields({[[:space:]]*name: 'trigger'" extension/ai/tool-definitions.js` returns 0.
    - `stop_trigger`, `get_trigger_status`, and `list_triggers` are returned by `getReadOnlyTools()`.
  </acceptance_criteria>
  <verify>
    <automated>node tests/tool-definitions-parity.test.js && node tests/visual-session-schema-lock.test.js</automated>
  </verify>
  <done>The trigger-family schemas are additive, shared once, mirrored byte-identically, and visible through all autopilot provider formatting paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared registry -> MCP schema bridge | JSON Schema definitions are converted for MCP registration; drift here changes external tool contracts. |
| shared registry -> autopilot provider formatter | Provider-specific envelopes must be generated from the same definitions, not hand-authored. |
| caller input -> trigger condition schema | Nested trigger conditions cross from untrusted caller input into background validation and Phase 15 manager validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03 | Tampering | `trigger.inputSchema.condition` | mitigate | Keep condition as structured input in the shared schema and require Plan 18-02 background validation before `FsbTriggerManager.armTrigger`; Task 1 adds schema tests so MCP registration cannot omit the condition object. |
| T-18-06 | Tampering / Information Integrity | `extension/ai/tool-definitions.js`, `mcp/ai/tool-definitions.cjs`, provider formatter | mitigate | Byte-identity parity plus provider visibility tests for xai/openai/anthropic/gemini/openrouter/lmstudio/custom. |
| T-18-01 | Denial of Service | companion tool scheduling metadata | mitigate | Mark companions `_readOnly:true` in the canonical registry so `TaskQueue` derives them into the bypass set; Plan 18-03 adds runtime queue-bypass tests. |
</threat_model>

<verification>
Focused:
- `node tests/tool-definitions-parity.test.js && node tests/visual-session-schema-lock.test.js`

Wave-level:
- `npm test && npm --prefix mcp run build && npm run test:mcp-smoke:tools`
</verification>

<success_criteria>
- Four trigger tools exist exactly once in `TOOL_REGISTRY`.
- MCP mirror is byte-identical.
- Existing schemas remain unchanged except for additive trigger-family entries.
- Trigger companions are bypass-class and do not contain visual-session fields.
- Provider formatting exposes all four trigger tools for all seven provider keys.
</success_criteria>

<output>
After completion, create `.planning/phases/18-shared-tool-registry-dispatcher-wiring/18-01-SUMMARY.md`.
</output>
