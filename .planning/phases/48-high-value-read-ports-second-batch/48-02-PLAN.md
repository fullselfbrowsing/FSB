# 48-02 Plan: Extend CircleCI Same-Origin Reads

## Goal

Extend the existing CircleCI handler beyond the Phase 46 seed reads while staying within the same `https://app.circleci.com/api/v2` GET-only path.

## Scope

- Add pipeline, workflow, job, artifact, and test-result read slugs.
- Reuse the existing CircleCI query/path helpers and shape guards.
- Keep CircleCI writes/destructive operations inactive.

## Verification

- Existing CircleCI reads still pass.
- New reads build exact CircleCI API URLs.
- Paginated endpoints map `page_token` to `page-token`.
- Error envelopes return typed DOM fallback errors.
