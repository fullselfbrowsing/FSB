# 48-03 Plan: Wire Gates, Search, and Coverage Evidence

## Goal

Make the new handlers visible to search/invoke and prove they satisfy the existing T1 port contract.

## Scope

- Add `FsbHandlerVercel` to `HEAD_HANDLER_MODULES`.
- Import the Vercel handler from the service worker.
- Add the new slugs to the `T1_READY_SLUGS` search/readiness override.
- Teach the origin-classification gate about Vercel's inline `/api${endpoint}` base.
- Add Vercel to the readiness report loader, recipe-path allowlist, and T1 port contract map.
- Refresh the Phase 44 readiness matrix.

## Verification

- Focused handler, upgrade, readiness, origin-classification, recipe-path, and port-contract tests pass.
- `npm run validate:extension` passes.
- Readiness report shows a measurable increase in ready descriptors and app coverage.
