---
phase: 38-breadth-b-comms-social-content-sensitivity-screened
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extension/config/service-denylist.json
  - docs/LEGAL.md
  - catalog/descriptors/_fixtures/batch-unclassified-social-origin.fixture.json
  - tests/breadth-batch-gate.test.js
  - tests/consent-mutation-gate.test.js
  - tests/dom-only-default.test.js
  - package.json
autonomous: true
requirements: [BRDTH-02, BRDTH-03]

must_haves:
  truths:
    - "Every comms/social/content batch origin Phase 38 will import (chatgpt.com, claude.ai, bsky.app, mastodon.social, threads.net, discord.com, www.reddit.com) is classified denied/sensitive/safe in extension/config/service-denylist.json BEFORE the import plans (02/03) run -- the merge-time classifyGate has zero unclassified social/messaging origins to abort on"
    - "An UNCLASSIFIED social/messaging batch origin (the new fixture, a social brand host absent from the denylist) makes classifyGate return failures.length>0 naming the origin -- the build aborts fail-closed (the per-app screening enforced as a build failure, not a hope)"
    - "A sensitive social/messaging WRITE on a screened sensitive origin (discord.com) WITHOUT the per-origin mutating flag returns decision!='allow' + error.code===RECIPE_CONSENT_MUTATING_REQUIRED (posture B / DENY-04); a READ on the same origin passes under Auto; the per-origin mutating flag elevates the write back to allow"
    - "The DENY-01 fully-denied set (netflix/spotify/twitch/steam/youtube-music/tinder/onlyfans) stays in deniedOrigins and classify() returns denied===true for each -- they are never imported even DOM-only"
    - "A descriptor whose backing field is 'dom' (the conservative default the frozen importer stamps for every data-batch app) returns from search invocable===false / backingStatus==='discovery-pending' -- never a confident invocable hit and never learn-seeded; an AI-chat or social app imported in 02/03 is DOM-only by this default WITHOUT any importer machinery edit"
    - "node scripts/verify-classification-gate.mjs exits 0 (the committed corpus + roster all classify or are benign) and npm run validate:extension exits 0 after the denylist extension"
  artifacts:
    - path: "extension/config/service-denylist.json"
      provides: "the sensitivity-screening roster EXTENDED for the comms/social/content batch: AI-chat (chatgpt.com, claude.ai) + microblog/fediverse + reddit classified per the docs/LEGAL.md ToS axis (sensitive where writes mutate, safe for content reads); the DENY-01 denied set unchanged"
      contains: "sensitiveOrigins"
    - path: "docs/LEGAL.md"
      provides: "the Categorization-Axes ToS-hostile/sensitivity roster doc EXTENDED to name the comms/social/content classification (the per-app screening rationale, defensible against the three axes)"
      contains: "Categorization Axes"
    - path: "catalog/descriptors/_fixtures/batch-unclassified-social-origin.fixture.json"
      provides: "a social/messaging batch origin that trips the social/messaging sensitivity axis but is NOT classified -> the gate fixture proving the merge aborts fail-closed on an unclassified comms/social origin (distinct from the Phase-37 finance fixture)"
      contains: "social"
    - path: "tests/breadth-batch-gate.test.js"
      provides: "the merge-time batch-gate proof EXTENDED: the new unclassified-social fixture aborts the gate; the screened comms/social batch origins (chatgpt.com, claude.ai, discord.com) pass classifyGate"
      contains: "batch-unclassified-social-origin"
    - path: "tests/consent-mutation-gate.test.js"
      provides: "the posture-B re-gate proof EXTENDED to a REAL screened comms/social sensitive origin from this batch (discord.com): a write without the flag -> RECIPE_CONSENT_MUTATING_REQUIRED; a read passes; the flag elevates"
      contains: "discord.com"
    - path: "tests/dom-only-default.test.js"
      provides: "proof the frozen backing:'dom' default makes an imported comms/social/AI descriptor DOM-only (invocable=false, discovery-pending, never confident-invocable) WITHOUT a machinery edit -- the conservative-default screening guarantee"
  key_links:
    - from: "extension/config/service-denylist.json sensitiveOrigins"
      to: "scripts/verify-classification-gate.mjs classifyGate (consumed via service-denylist.js classify)"
      via: "a screened sensitive social/messaging origin classifies sensitive -> classifyGate continues past it (no fail) AND the posture-B re-gate fires on its writes"
      pattern: "sensitiveOrigins|classify"
    - from: "tests/breadth-batch-gate.test.js"
      to: "catalog/descriptors/_fixtures/batch-unclassified-social-origin.fixture.json"
      via: "the gate test loads the unclassified-social fixture and asserts classifyGate aborts naming it"
      pattern: "batch-unclassified-social-origin"
    - from: "tests/consent-mutation-gate.test.js"
      to: "extension/utils/capability-router.js _evaluateConsent (FsbConsentGate.evaluate)"
      via: "a sensitive-origin (discord.com) write without the mutating flag -> RECIPE_CONSENT_MUTATING_REQUIRED through the live step-(3.5) re-gate"
      pattern: "RECIPE_CONSENT_MUTATING_REQUIRED"
    - from: "tests/dom-only-default.test.js"
      to: "extension/utils/capability-search.js search()"
      via: "a backing:'dom' descriptor returns invocable=false / backingStatus='discovery-pending' (the frozen conservative default)"
      pattern: "invocable|discovery-pending"
---

<objective>
Land the per-app SENSITIVITY SCREENING that gates the Phase-38 comms/social/content
import — this plan MUST complete BEFORE the import plans (02/03) because the merge-time
classifyGate fail-closes on any unclassified social/messaging origin. The screening is a
DENYLIST + DOC + PROOF change against the FROZEN Phase-37 machinery (NO importer edit):

1. **Classify every batch origin (BRDTH-02):** extend extension/config/service-denylist.json
   so each comms/social/content origin Phase 38 will import (chatgpt.com, claude.ai, bsky.app,
   mastodon.social, threads.net, www.reddit.com — discord.com is already sensitive) is
   classified denied/sensitive/safe per the docs/LEGAL.md Categorization Axes, defaulting the
   AI-chat + social-write apps to SENSITIVE (conservative) so their writes are posture-B
   re-gated. The DENY-01 fully-denied set stays in deniedOrigins. This makes the merge-time
   classifyGate (which 02/03 run) have zero unclassified social/messaging origins to abort on.

2. **DOC the classification (the three axes):** extend the docs/LEGAL.md Categorization Axes
   roster so the per-app comms/social/content classification is named + defensible against
   finance/gov denial vs ToS-hostility denial vs sensitivity.

3. **Prove the fail-closed gate (BRDTH-02):** a NEW unclassified-social fixture + an extended
   breadth-batch-gate proof: an unclassified social/messaging batch origin aborts classifyGate;
   the screened batch origins pass.

4. **Prove the sensitive-write re-gate (write-sensitivity):** extend consent-mutation-gate to a
   REAL screened sensitive origin from THIS batch (discord.com): a write without the per-origin
   mutating flag -> RECIPE_CONSENT_MUTATING_REQUIRED (posture B / DENY-04); a read passes; the
   flag elevates.

5. **Prove the conservative DOM-only default (BRDTH-03):** the FROZEN importer's
   backingFor() returns 'dom' for EVERY data-batch app, so every comms/social/AI app 02/03
   import is automatically DOM-only (T3, never confident-invocable, never learn-seeded) WITHOUT
   any machinery edit. A new dom-only-default test proves a backing:'dom' descriptor returns
   invocable=false / discovery-pending. (CONTEXT discretion: prefer a metadata-driven marker
   over a machinery edit -- here ZERO edit is needed because DOM-only IS the frozen default.)

Purpose: the screening is the SECURITY contract of this sensitive batch -- a social/messaging
app must never become writable-under-Auto by landing unscreened. The classifyGate merge-gate
(fail-closed) + the conservative DOM-only default + the posture-B sensitive-write re-gate are
the mitigations; this plan classifies the origins + proves all three BEFORE any descriptor
lands.
Output: extended denylist roster + LEGAL doc; the unclassified-social gate fixture + extended
gate proof; the discord.com sensitive-write re-gate proof; the DOM-only-default proof; all
registered in CI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-breadth-b-comms-social-content-sensitivity-screened/38-CONTEXT.md
@.planning/phases/38-breadth-b-comms-social-content-sensitivity-screened/38-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the live codebase. No exploration required. -->

THE FROZEN MACHINERY (do NOT edit; this plan is denylist + doc + tests only):
- scripts/import-opentabs-catalog.mjs backingFor(_app, _service) RETURNS 'dom' for EVERY
  data-batch app (the function ignores its args -- line ~164). So every comms/social/content app
  02/03 import is automatically backing:'dom' = DOM-only (T3) = the conservative default the
  CONTEXT requires. NO importer edit, NO new marker is needed for DOM-only routing.
- scripts/import-opentabs-catalog.mjs runImport() calls classifyGate(gateItemsList) over EVERY
  enumerated batch origin BEFORE writing and THROWS (exit 1) on any unclassified denied/sensitive
  origin. 02/03 run this; THIS plan must classify the origins so it does not abort.

extension/config/service-denylist.json (the screening roster -- the file THIS plan edits):
  { "v":1, "deniedOrigins":[...14...], "sensitiveOrigins":[...22...], "deniedReason":"..." }
  Host-pattern form (LOCKED): 'https://*.<domain>' = apex + any subdomain; a pattern with NO '*'
  = that EXACT origin (scheme+host). EXACT-host forms (music.youtube.com NOT *.youtube.com) keep a
  pattern no broader than OpenTabs' own urlPattern host scope (Phase 35 decision).
  CURRENT roster relevant to this batch: deniedOrigins already has the DENY-01 set
  (*.netflix.com, open.spotify.com, *.twitch.tv, store.steampowered.com, music.youtube.com,
  *.tinder.com, *.onlyfans.com); sensitiveOrigins already has discord.com, *.slack.com,
  *.instagram.com, *.facebook.com, *.tiktok.com, *.x.com, web.whatsapp.com, web.telegram.org,
  teams.*. So discord is ALREADY sensitive; chatgpt/claude/bsky/mastodon/threads/reddit are NOT
  yet classified (classify() returns {denied:false,sensitive:false} for them today).

service-denylist.js classify(origin) -> { sensitive, denied, reason? }:
  denied implies sensitive; a sensitiveOrigins match -> {sensitive:true,denied:false}; no match ->
  {sensitive:false,denied:false}. load() reads ../config/service-denylist.json. The gate +
  the consent re-gate both consume classify(); editing the JSON is the ONLY change needed to
  re-classify an origin.

classifyGate sensitivityHeuristic(host, slug, description) (scripts/verify-classification-gate.mjs)
  -- a fail-closed BACKSTOP, NOT the screening itself. It flags (suspect:true) hosts matching an
  axis RegExp: social/messaging matches /instagram|facebook|tiktok|twitter|whatsapp|telegram|
  messenger|snapchat|discord|linkedin|direct-message|slack|signal/; media matches /netflix|spotify|
  twitch|youtube|steam|hulu|disney|primevideo|hbo/; adult/dating matches /onlyfans|tinder|bumble|
  hinge|grindr|adult|nsfw|dating|escort/. VERIFIED host-only probes:
    discord.com -> suspect (social/messaging); youtube.com -> suspect (media);
    snapchat.com/linkedin.com -> suspect (social/messaging);
    BUT reddit.com / chatgpt.com / claude.ai / bsky.app / mastodon.social / threads.net ->
    suspect:FALSE (no axis token). The heuristic ALONE will NOT force them to be classified.
  -> THE SCREENING RULE for this plan: the CONTEXT framework requires the AI-chat + social-write
  apps to be classified SENSITIVE regardless of whether the heuristic catches them (conservative,
  fail-safe toward less reach). So classify them in the JSON explicitly; do not rely on the
  heuristic to catch chatgpt/claude/bsky/mastodon/threads.

extension/utils/capability-router.js _evaluateConsent (FsbConsentGate.evaluate, posture B / DENY-04):
  reads the LIVE global FsbServiceDenylist at call time. For an origin where classify().sensitive
  ===true and mode==='auto', a WRITE (method in {POST,PUT,PATCH,DELETE} OR a descriptor whose
  sideEffectClass is 'write'/'mutating'/'destructive') WITHOUT the per-origin mutating flag returns
  { decision: (not 'allow'), error: { code/errorCode/error: 'RECIPE_CONSENT_MUTATING_REQUIRED' } }.
  A READ passes (decision 'allow'); setOriginMutating(origin,true) elevates the write to allow.
  consent-mutation-gate.test.js already proves this on dashboard.stripe.com; EXTEND it to discord.com.

capability-search.js search() hit annotation (the BRDTH-03 backing surface, FROZEN from 37-01):
  each hit carries backing (the descriptor enum), backingStatus (display: handler/recipe carry the
  enum; 'dom' -> 'discovery-pending'; 'learn' -> 'learn-pending'), and invocable (boolean, true IFF
  backing is 'handler' or 'recipe'). So a backing:'dom' descriptor returns invocable=false /
  backingStatus='discovery-pending'. buildOrRestore() builds the index from global.FsbRecipeIndex.

THE TEST CONVENTION (zero-framework, ASCII-only, NO emojis): a passed/failed counter +
  check(cond,msg) + process.exit(failed>0?1:0). breadth-batch-gate.test.js + consent-mutation-gate
  .test.js + backing-status-annotation.test.js are the templates to mirror/extend.
</interfaces>

# Reuse (do NOT rebuild) -- the frozen Phase-35/37 screening machinery:
@scripts/verify-classification-gate.mjs
@extension/utils/service-denylist.js
@extension/config/service-denylist.json
@tests/breadth-batch-gate.test.js
@tests/consent-mutation-gate.test.js
@tests/backing-status-annotation.test.js
@catalog/descriptors/_fixtures/batch-unclassified-origin.fixture.json
@docs/LEGAL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Classify the comms/social/content batch origins in service-denylist.json + extend the docs/LEGAL.md Categorization Axes</name>
  <files>extension/config/service-denylist.json, docs/LEGAL.md</files>
  <read_first>
    - extension/config/service-denylist.json (the current 14 denied + 22 sensitive roster + the host-pattern form + the leading _comment; discord.com already sensitive)
    - extension/utils/service-denylist.js (classify() + the host-pattern matcher: '*.<domain>' suffix vs exact-host; confirm the form before adding origins)
    - docs/LEGAL.md (the "### Categorization Axes" section: the three axes + the ToS-hostile roster robinhood..onlyfans + the IG/FB/TikTok/X sensitive-not-denied list)
    - .planning/phases/38-breadth-b-comms-social-content-sensitivity-screened/38-CONTEXT.md (the 4-tier ToS routing + the conservative-default rule)
  </read_first>
  <action>
    Extend extension/config/service-denylist.json sensitiveOrigins to classify the comms/social/
    content batch origins Phase 38 (plans 02/03) will import, per the docs/LEGAL.md Categorization
    Axes + the CONTEXT 4-tier routing (conservative-default: unknown social/AI -> sensitive/DOM-only,
    NOT safe). Add these EXACT-host or apex-suffix patterns to sensitiveOrigins (the WRITE-re-gated
    tier -- reads run under Auto, writes mutating-gated by posture B):
      - "https://chatgpt.com"        (AI-chat; conservative-sensitive, writes re-gated)
      - "https://claude.ai"          (AI-chat; conservative-sensitive, writes re-gated)
      - "https://bsky.app"           (microblog social; conservative-sensitive, writes re-gated)
      - "https://mastodon.social"    (fediverse social; conservative-sensitive, writes re-gated)
      - "https://www.threads.net"    (microblog social; conservative-sensitive, writes re-gated)
    discord.com is ALREADY in sensitiveOrigins (Phase 35) -- do NOT duplicate it.
    www.reddit.com is the SAFE tier (content reads; reddit.inbox already ships backing:'recipe'):
    do NOT add it to sensitiveOrigins or deniedOrigins -- reddit reads run normally, and the
    heuristic does NOT flag reddit.com, so it passes the gate as safe-by-default (correct for the
    content-read tier). Leave the DENY-01 deniedOrigins set UNCHANGED (netflix/spotify/twitch/steam/
    youtube-music/tinder/onlyfans stay denied -- never imported even DOM-only).

    Use the EXACT-host form for chatgpt.com / claude.ai / www.threads.net / bsky.app /
    mastodon.social (each is the precise host, no broadening) -- mirror the Phase-35 exact-host
    discipline (a pattern no broader than the app's own origin). Update the leading "_comment" to
    note the Phase-38 comms/social/content extension (AI-chat + microblog/fediverse classified
    sensitive per the ToS axis; DENY-01 unchanged; reddit content-reads safe), mirroring how the
    comment records the Phase-35 expansion.

    Extend docs/LEGAL.md's "### Categorization Axes" section: under axis 3 (Sensitivity), name the
    Phase-38 comms/social/content additions -- AI-chat (chatgpt, claude) and microblog/fediverse
    (bluesky, mastodon, threads) are classified SENSITIVE (writes re-gated, reads under Auto) as the
    conservative default for this category; reddit content-reads are SAFE; the DENY-01 ToS-hostile
    media/social set (axis 2) stays denied. State the rationale: a social/messaging app must default
    toward LESS reach (DOM-only + sensitive-write gating) because a mis-classification makes it
    writable-under-Auto. Keep it consistent with the existing axis prose (ASCII, no emojis).
  </action>
  <verify>
    <automated>node -e "const D=require('./extension/utils/service-denylist.js');D.load().then(()=>{const sens=['https://chatgpt.com','https://claude.ai','https://bsky.app','https://mastodon.social','https://www.threads.net','https://discord.com'];const den=['https://www.netflix.com','https://open.spotify.com','https://www.twitch.tv','https://music.youtube.com','https://www.tinder.com','https://www.onlyfans.com'];const safe=['https://www.reddit.com'];let ok=true;for(const o of sens){const c=D.classify(o);if(!(c.sensitive===true&&c.denied===false)){console.error('NOT sensitive:',o,c);ok=false}}for(const o of den){const c=D.classify(o);if(c.denied!==true){console.error('NOT denied:',o,c);ok=false}}for(const o of safe){const c=D.classify(o);if(c.sensitive!==false||c.denied!==false){console.error('reddit should be safe:',o,c);ok=false}}if(!ok)process.exit(1);console.log('PASS screening classification: AI-chat+microblog sensitive, DENY-01 denied, reddit safe')})"</automated>
  </verify>
  <acceptance_criteria>
    - classify('https://chatgpt.com'), classify('https://claude.ai'), classify('https://bsky.app'), classify('https://mastodon.social'), classify('https://www.threads.net') each return {sensitive:true, denied:false}; discord.com stays {sensitive:true}.
    - classify() for the DENY-01 set (netflix/spotify/twitch/youtube-music/tinder/onlyfans) returns denied===true (unchanged).
    - classify('https://www.reddit.com') returns {sensitive:false, denied:false} (the safe content-read tier).
    - docs/LEGAL.md Categorization Axes names the Phase-38 comms/social/content classification under the three axes (AI-chat+microblog sensitive; reddit reads safe; DENY-01 unchanged).
    - No DENY-01 deniedOrigins entry was removed; no over-broad '*.' pattern was added for an app that ships an exact host.
  </acceptance_criteria>
  <done>Every comms/social/content batch origin is classified per the ToS axis (AI-chat+microblog sensitive, reddit safe, DENY-01 denied) and the LEGAL doc records the rationale -- the merge-time classifyGate that 02/03 run now has zero unclassified social/messaging origins to abort on.</done>
</task>

<task type="auto">
  <name>Task 2: Fail-closed gate proof + sensitive-write re-gate proof + DOM-only-default proof (extend the breadth/consent suites; add the DOM-only-default test)</name>
  <files>catalog/descriptors/_fixtures/batch-unclassified-social-origin.fixture.json, tests/breadth-batch-gate.test.js, tests/consent-mutation-gate.test.js, tests/dom-only-default.test.js, package.json</files>
  <read_first>
    - tests/breadth-batch-gate.test.js (the (a) fail-closed / (b) safe-batch proof shape to extend; the dynamic-import of the ESM gate + Denylist.load())
    - catalog/descriptors/_fixtures/batch-unclassified-origin.fixture.json (the Phase-37 finance fixture shape to mirror for a SOCIAL one)
    - tests/consent-mutation-gate.test.js (the posture-B block: the SENSITIVE classify stub + the stripe.payouts.create RECIPE_CONSENT_MUTATING_REQUIRED proof + the flag-elevates proof -- the pattern to re-run on a REAL screened origin)
    - tests/backing-status-annotation.test.js (the buildOrRestore + search() invocable/backingStatus assertion pattern to mirror for the DOM-only-default test)
    - scripts/verify-classification-gate.mjs (classifyGate + sensitivityHeuristic: the social/messaging axis the social fixture must trip)
    - extension/utils/capability-router.js (_evaluateConsent step-(3.5) posture-B re-gate -- the live code the discord write flows through; confirm it reads classify() at call time)
    - package.json (the "test" chain ~line 17 -- register the new dom-only-default test after backing-status-annotation.test.js)
  </read_first>
  <action>
    (a) Create catalog/descriptors/_fixtures/batch-unclassified-social-origin.fixture.json mirroring
    batch-unclassified-origin.fixture.json but for a SOCIAL/MESSAGING batch origin that trips the
    social/messaging sensitivity axis and is NOT classified: service a social brand host the
    heuristic catches (e.g. "snapchat.example" or a host containing a social token like "messenger"
    -- pick one whose host trips /instagram|facebook|tiktok|...|snapchat|messenger|.../ AND that is
    absent from service-denylist.json). slug a write op (e.g. "<svc>.post_message"), sideEffectClass
    "write", an _fixture note explaining it is the BRDTH-02 social-origin fail-closed proof and that
    _fixtures/ is excluded by readJsonDir non-recursion so it never ships.

    (b) Extend tests/breadth-batch-gate.test.js: add a block (c) that loads the new
    batch-unclassified-social-origin fixture, runs classifyGate([socialItem]) and asserts
    failures.length>0 AND the failure names the social origin (the social/messaging-axis fail-closed
    proof, distinct from the existing finance-axis (a) block). Add a block (d) that asserts the
    SCREENED comms/social batch origins pass: classifyGate over
    [{origin:'https://chatgpt.com',...},{origin:'https://claude.ai',...},{origin:'https://discord.com',...}]
    returns failures.length===0 (Task-1's classification makes them all governed -> the import will
    not abort). Keep the existing (a)/(b) finance + linear/asana blocks UNCHANGED. (breadth-batch-gate
    is already registered in package.json + chained via verify-classification-gate.mjs in
    validate:extension -- no new registration for it.)

    (c) Extend tests/consent-mutation-gate.test.js posture-B section to add a block that re-runs the
    sensitive-write re-gate on a REAL SCREENED origin from THIS batch -- discord.com (now sensitive
    via Phase 35 + confirmed in Task 1). With the LIVE Denylist loaded (await Denylist.load() against
    the committed roster -- NOT a hand-stubbed classify), set discord.com mode 'auto' + mutating false
    and assert: a READ (method GET, sideEffectClass read) -> decision 'allow'; a WRITE (a discord
    post/DM op, method POST, sideEffectClass 'write') WITHOUT the flag -> decision != 'allow' AND
    error.code === error.errorCode === error.error === 'RECIPE_CONSENT_MUTATING_REQUIRED' (INV-03
    dual-field); then setOriginMutating('https://discord.com',true) -> the same write returns 'allow'.
    This proves the screening's write-gating end-to-end on a real comms/social origin (the existing
    stripe.com / github.com blocks stay UNCHANGED as the cross-batch canaries). Load the REAL committed
    roster — `require('../extension/utils/service-denylist.js')`, `await Denylist.load()`, assign it to
    `globalThis.FsbServiceDenylist`, and assert `classify('https://discord.com').sensitive === true`
    before the gate calls — mirroring the live-roster pattern in `tests/breadth-batch-gate.test.js`
    (NOT the STUBBED `globalThis.FsbServiceDenylist={classify(){...}}` blocks in consent-mutation-gate.test.js,
    which fabricate the verdict). This makes the discord proof exercise the real classification this plan
    landed in Task 1, not a tautology.

    (d) Create tests/dom-only-default.test.js (BRDTH-03, the conservative-default guarantee): plant
    the MiniSearch UMD + a FsbRecipeIndex carrying a backing:'dom' descriptor with a comms/social-
    shaped slug (e.g. service chatgpt.com, slug 'chatgpt.send_message', sideEffectClass 'write',
    backing 'dom') and NO paired recipe; require capability-search.js, buildOrRestore(), search() for
    its synonym, and assert the hit returns invocable===false AND backingStatus==='discovery-pending'
    AND backing==='dom' -- proving an imported comms/social/AI descriptor is DOM-only (T3, never a
    confident invocable hit, never learn-seeded) by the FROZEN backing:'dom' default, WITHOUT any
    importer machinery edit. Add an assertion comment naming the frozen backingFor() default so the
    "no machinery edit needed" decision is recorded in the proof. Register tests/dom-only-default.test.js
    in package.json "test" after node tests/backing-status-annotation.test.js (do NOT remove any
    existing test).
  </action>
  <verify>
    <automated>node tests/breadth-batch-gate.test.js && node tests/consent-mutation-gate.test.js && node tests/dom-only-default.test.js && grep -q dom-only-default package.json && grep -q "batch-unclassified-social-origin" tests/breadth-batch-gate.test.js && grep -q "discord.com" tests/consent-mutation-gate.test.js && echo SCREENING-PROOFS-OK</automated>
  </verify>
  <acceptance_criteria>
    - breadth-batch-gate.test.js passes: the new social-origin fixture makes classifyGate abort (failures>0, names the origin); the screened chatgpt/claude/discord batch passes (failures===0); the existing finance + linear/asana blocks still pass.
    - consent-mutation-gate.test.js passes with the new discord.com block: a read allows; a write without the flag -> RECIPE_CONSENT_MUTATING_REQUIRED (dual-field byte-exact); the per-origin mutating flag elevates the write to allow -- using the LIVE committed roster, not a fabricated classify.
    - dom-only-default.test.js passes: a backing:'dom' comms/social descriptor returns invocable=false / backingStatus='discovery-pending' (the frozen conservative default; no machinery edit).
    - tests/dom-only-default.test.js is registered in package.json "test"; no existing test removed.
  </acceptance_criteria>
  <done>The screening's three security properties are proven: the merge aborts fail-closed on an unclassified social origin (BRDTH-02), a screened sensitive comms/social write is posture-B re-gated (write-sensitivity), and the imported descriptors are DOM-only by the frozen backing:'dom' default with no machinery edit (BRDTH-03) -- all registered in CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| comms/social origin classification -> merge-time classifyGate (build-time) | an unscreened social/messaging origin's descriptor must be refused before it can land in the shipped corpus |
| screened sensitive origin -> invoke consent gate (runtime) | a sensitive social/messaging WRITE must re-enforce the per-origin mutating opt-in (posture B) before it executes on someone's account |
| imported comms/social descriptor -> search hit -> invoke (runtime) | a DOM-only/ToS-hostile descriptor must surface as discovery-pending, never a confident invocable hit, and never be learn-seeded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | Elevation of Privilege | a social/messaging app mis-classified safe/invocable becomes WRITABLE under Auto (post/DM/delete on someone's account) -- THE headline threat | mitigate | the per-app screening classifies every batch origin denied/sensitive (Task 1); the merge-time classifyGate aborts fail-closed on any unclassified social/messaging origin (Task 2b, proven by batch-unclassified-social-origin.fixture.json); writes to the screened sensitive origins are posture-B re-gated (Task 2c, proven on discord.com) |
| T-38-02 | Elevation of Privilege | a sensitive comms/social WRITE (discord post/DM) executes under Auto without the per-origin mutating opt-in | mitigate | classify(origin).sensitive===true (Task 1) triggers the live _evaluateConsent step-(3.5) re-gate -> RECIPE_CONSENT_MUTATING_REQUIRED without the flag; proven end-to-end on discord.com against the committed roster (Task 2c) |
| T-38-03 | Spoofing | a ToS-hostile / AI-chat app surfaced as a confident API-invocable hit (a fabricated API call from guessed auth -- the research anti-pattern) | mitigate | the frozen backingFor() stamps backing:'dom' for every data-batch app -> DOM-only (T3), invocable=false / discovery-pending, never learn-seeded; proven by tests/dom-only-default.test.js (Task 2d). No auto-minted recipe from guessed auth |
| T-38-04 | Tampering | the DENY-01 fully-denied set (netflix/spotify/twitch/steam/youtube-music/tinder/onlyfans) leaking back in as imported descriptors | mitigate | the DENY-01 set stays in deniedOrigins (Task 1, unchanged + asserted denied===true); 02/03 never vendor a denied app, and the merge gate would refuse a denied origin's descriptor |
| T-38-05 | Tampering | a denylist edit silently broadening an app's scope (a '*.' suffix where an exact host was meant) -> over-denial or an unintended apex match | mitigate | exact-host patterns for chatgpt/claude/threads/bsky/mastodon (Task 1, mirroring the Phase-35 exact-host discipline); the classification verify asserts the precise classify() verdict per origin |
| T-38-SC | Tampering | npm/pip/cargo installs | accept | Phase 38 adds NO new packages (denylist JSON + doc + test edits only); no install task -> the package-legitimacy gate is N/A this plan |
</threat_model>

<verification>
- `node -e` classify probe: chatgpt/claude/bsky/mastodon/threads classify sensitive; the DENY-01 set classifies denied; reddit classifies safe.
- `node tests/breadth-batch-gate.test.js` passes: the unclassified-social fixture aborts the gate; the screened chatgpt/claude/discord batch passes.
- `node tests/consent-mutation-gate.test.js` passes: a discord.com write without the flag -> RECIPE_CONSENT_MUTATING_REQUIRED; a read passes; the flag elevates (live committed roster).
- `node tests/dom-only-default.test.js` passes: a backing:'dom' comms/social descriptor returns invocable=false / discovery-pending.
- `node scripts/verify-classification-gate.mjs` exits 0 (committed corpus + roster all classified or benign) and `npm run validate:extension` exits 0.
</verification>

<success_criteria>
- BRDTH-02 (screening): every comms/social/content batch origin is classified denied/sensitive/safe BEFORE 02/03 import; the merge-time classifyGate aborts fail-closed on an unclassified social/messaging origin -- the per-app screening enforced as a build failure.
- BRDTH-03 (conservative default): an imported comms/social/AI descriptor is DOM-only (backing:'dom', invocable=false, discovery-pending) by the frozen default -- never a confident invocable hit, never learn-seeded -- with NO importer machinery edit.
- Write-sensitivity: a screened sensitive comms/social WRITE without the per-origin mutating flag -> RECIPE_CONSENT_MUTATING_REQUIRED (posture B / DENY-04); reads run under Auto; the flag elevates.
- The DENY-01 fully-denied set stays denied (never imported even DOM-only); the docs/LEGAL.md Categorization Axes records the per-app comms/social/content classification defensibly.
- Regression: verify-classification-gate.mjs + validate:extension stay exit 0 after the denylist extension.
</success_criteria>

<output>
Create `.planning/phases/38-breadth-b-comms-social-content-sensitivity-screened/38-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
