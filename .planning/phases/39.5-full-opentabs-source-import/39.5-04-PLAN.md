---
phase: 39.5-full-opentabs-source-import
plan: 04
type: execute
wave: 4
depends_on: [39.5-01, 39.5-02, 39.5-03]
files_modified:
  - catalog/descriptors/
  - catalog/descriptors/_fixtures/_provenance.json
  - catalog/descriptors/_fixtures/seed-descriptors.json
  - extension/catalog/recipe-index.generated.js
  - tests/coverage-report.test.js
autonomous: true
requirements: [BRDTH-01, BRDTH-02, BRDTH-03]
must_haves:
  truths:
    - "The frozen importer emits ~2,374 real descriptors across the 117 real apps with 0 colliding opentabs__<stem>__* filenames"
    - "The 40 overlapping apps are regenerated from real source; the 13 hand-only apps' descriptors remain in the corpus (augment)"
    - "recipe-index.generated.js is regenerated (INV-01 IIFE/djb2 shape intact) inlining the full ~2,400+ descriptor set"
    - "classifyGate + crosscheck (incl the payment-op guard + READ_ONLY_SAFE + the Plan-03 COMMERCE_SENSITIVE_SERVICES commerce-roster backstop) + no-dead-entry + the no-duplicate-stem gate all PASS at full scale"
    - "No payment op is safe-and-API-invocable; the real shipped doordash payment-write proof still holds"
  artifacts:
    - path: "extension/catalog/recipe-index.generated.js"
      provides: "The regenerated build-time catalog inlining ~2,400+ descriptors (the array capability-search indexes)"
      contains: "FsbRecipeIndex"
    - path: "catalog/descriptors/_fixtures/_provenance.json"
      provides: "Per-app provenance for all real apps (filled by the import) + the 13 hand-only markers"
      contains: "opentabs"
  key_links:
    - from: "extension/catalog/recipe-index.generated.js"
      to: "catalog/descriptors/opentabs__*.json"
      via: "package-extension.mjs readJsonDir non-recursive inline"
      pattern: "descriptors"
    - from: "scripts/verify-catalog-crosscheck.mjs payment-op guard"
      to: "the full ~2,400-descriptor corpus"
      via: "checkPaymentOpsNotSafeInvocable over every emitted descriptor"
      pattern: "checkPaymentOpsNotSafeInvocable"
---

<objective>
Run the frozen importer over the full 117-real vendored corpus to emit ~2,374 real descriptors (the 40 overlaps regenerated from real source, the 13 hand-only preserved -- augment), regenerate `recipe-index.generated.js` via package-extension.mjs, and prove the FULL-SCALE gate set is green: classifyGate (merge-time fail-closed), the crosscheck (derived-vs-declared + READ_ONLY_SAFE + the payment-op guard + the Plan-03 COMMERCE_SENSITIVE_SERVICES commerce-roster backstop), no-dead-entry, the no-duplicate-stem gate, and the real shipped doordash payment-write proof. Update the coverage-report test for the ~130-app corpus.

Purpose: This is the actual import -- the moment ~2,374 real descriptors land and the full-scale security/correctness contract must hold. The headline threat (a payment/money-moving op writable-or-invocable under Auto) is enforced by the triple mitigation: breadth descriptors backing:'dom' (not API-invocable) + the payment-op CI guard + commerce/payment origins kept SENSITIVE (Plan 03, now a checked build invariant via the COMMERCE_SENSITIVE_SERVICES backstop). The spike proved 117/117 import cleanly; this plan makes it real and re-asserts every gate at full scale.
Output: ~2,400+ committed descriptors, the regenerated recipe-index.generated.js, filled provenance + seed, and the updated coverage-report test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39.5-full-opentabs-source-import/39.5-CONTEXT.md
@.planning/phases/39-breadth-c-commerce-travel-misc-most-sensitive/FULL-PARITY-SPIKE.md

<read_first>
- FULL-PARITY-SPIKE.md (VERDICT + the metrics table: 117/117 import OK, 0 schema throws, 2,374 ops 1,669 read/534 write/171 destructive; Section 5.3 batching, Section 5.5 augment net corpus ~130 apps)
- scripts/import-opentabs-catalog.mjs runImport() (lines ~665-726 -- the enumerate -> extract -> merge-time classifyGate -> flat write -> fillProvenance -> feedSeedDescriptors flow) + the CLI entry (~800)
- scripts/package-extension.mjs readJsonDir + the recipe-index.generated.js IIFE emit (lines ~41-88 -- non-recursive, inlines { recipes, descriptors }, sets global.FsbRecipeIndex; INV-01 IIFE/djb2 shape)
- scripts/verify-classification-gate.mjs + scripts/verify-catalog-crosscheck.mjs (the gates -- run at full scale; the payment-op guard checkPaymentOpsNotSafeInvocable + READ_ONLY_SAFE + crossCheck + the Plan-03 COMMERCE_SENSITIVE_SERVICES backstop)
- scripts/verify-no-duplicate-stem.mjs (the Plan-02 gate -- must PASS over the full emitted corpus)
- tests/coverage-report.test.js (the realAppCount/PHASE_39_STEMS/HEAD_EXPECTED asserts to UPDATE for ~130 apps; the 39-06 manifest completeness check) + tests/no-dead-entry.test.js (generalized opentabs__* loader -- runs unchanged at full scale)
- tests/sensitive-write-import-gate.test.js (the doordash place_order + discord send_message end-to-end proofs -- must still pass after the real re-import; doordash is in the regenerated set)
- package.json scripts: validate:extension (validate-extension + recipe-path-guard + classifyGate + crosscheck) and package:extension
</read_first>

<interfaces>
<!-- The build sequence (the SAME commands phases 37-39 ran, now at full scale): -->
  node --import tsx ./scripts/import-opentabs-catalog.mjs    # emit ~2,374 flat opentabs__*.json + fill provenance + feed seed
  node scripts/package-extension.mjs                          # regenerate extension/catalog/recipe-index.generated.js (INV-01 IIFE/djb2)
  npm run validate:extension                                  # validate-extension + recipe-path-guard + classifyGate + crosscheck (payment-op guard + READ_ONLY_SAFE + the Plan-03 COMMERCE_SENSITIVE_SERVICES backstop) + no-duplicate-stem (Plan 02 wired it in)

<!-- The augment outcome the import produces: -->
<!--   - the 40 overlapping apps: their opentabs__<app>__*.json are REGENERATED from the real (Plan-01 re-vendored) src -- the real op sets REPLACE the hand-authored descriptors -->
<!--   - the 13 hand-only apps: enumerateBatchApps re-emits them from their PRESERVED hand-authored src (idempotent) -> their descriptors stay in the corpus -->
<!--   - the 77 net-new real apps: emit fresh -->
<!--   - grafana: the hand-authored grafana.com slice emits (real grafana not vendored); sqlpad: skipped (Plan 02) -->
<!--   - EXISTING_HEAD_SERVICES (github.com/app.slack.com/www.notion.so): enumerate-skipped (heads not clobbered, INV-01) -->

<!-- The payment-op guard contract (must PASS at full scale): NO descriptor whose op is a payment op (verbPrefix in PAYMENT_VERBS OR opNameFromSlug in PAYMENT_OP_NAMES) may be backing recipe/handler OR on a safe origin. Every breadth descriptor is backing:'dom' (the frozen backingFor() default) AND every commerce/payment origin is sensitive (Plan 03) -> the guard passes. -->

<!-- The COMMERCE_SENSITIVE_SERVICES backstop contract (Plan 03; also runs in this plan's validate:extension): every enumerated commerce/payment/travel/paid-booking brand classifies sensitive. classifyGate does NOT abort on a READ-ONLY commerce brand (no payment token) and the payment-op guard does NOT key on it (no payment verb) -- so this backstop is the AUTHORITATIVE check for a missed read-only commerce brand. If it fails naming a commerce brand that classifies SAFE, add that brand to BOTH service-denylist.json sensitiveOrigins AND the COMMERCE_SENSITIVE_SERVICES roster (do NOT downgrade it / do NOT remove it from the roster to clear the gate). -->

<!-- coverage-report.test.js updates: the test currently asserts realAppCount > 50 and references the 39-06 manifest (53 vendored). For ~130 apps: RAISE the realAppCount floor to reflect the full corpus (>= ~110 distinct services, leaving margin), KEEP the PHASE_39_STEMS DOM-only assertion + the HEAD_EXPECTED head-bucket assertion + the payment-op-not-invocable assertion. The 39-06 manifest completeness check covers the original 53; the new augment guard (Plan 01 tests/vendor-augment-preserved.test.js) covers the full set, so the coverage test's manifest check can stay as-is for the 53 OR be relaxed to "every manifest VENDORED+IMPORTED stem is still covered" (it is, post-augment). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full import + regenerate the catalog + pass the full-scale gates</name>
  <files>catalog/descriptors/, catalog/descriptors/_fixtures/_provenance.json, catalog/descriptors/_fixtures/seed-descriptors.json, extension/catalog/recipe-index.generated.js</files>
  <read_first>
    - scripts/import-opentabs-catalog.mjs runImport() + CLI entry (the emit flow + the merge-time classifyGate)
    - scripts/package-extension.mjs readJsonDir + the recipe-index.generated.js IIFE emit (INV-01)
    - FULL-PARITY-SPIKE.md metrics table (the expected ~2,374-op outcome) + Section 5.5 (the augment net corpus)
    - scripts/verify-no-duplicate-stem.mjs (the Plan-02 gate that must pass over the emitted corpus)
    - scripts/verify-catalog-crosscheck.mjs (the Plan-03 COMMERCE_SENSITIVE_SERVICES backstop -- the authoritative check for a missed read-only commerce brand)
  </read_first>
  <action>
    Run the frozen importer at full scale: `node --import tsx ./scripts/import-opentabs-catalog.mjs`. It enumerates the full vendored set (Plan 01: 117 real + 13 hand-only + grafana, minus EXISTING_HEAD_SERVICES + fixtures + the Plan-02 sqlpad/empty-origin skip), extracts each op's metadata via the unchanged core path, applies the Plan-02 STEM_OVERRIDES (collision-free filenames) + the target.apply_promo_code carve, runs the merge-time classifyGate (fail-closed over the Plan-03 classify-complete roster -- it must NOT abort), and writes ~2,374 flat opentabs__*.json descriptors + fills _provenance.json (the real apps) + feeds seed-descriptors.json. The 40 overlapping apps' descriptors are regenerated from the real src (replacing the hand-authored ones); the 13 hand-only re-emit from their preserved hand-authored src (their descriptors stay). Then regenerate the catalog: `node scripts/package-extension.mjs` -> extension/catalog/recipe-index.generated.js inlining { recipes, descriptors } with the unchanged IIFE/djb2 shape (INV-01; only the DATA grows). Then run `npm run validate:extension` -- which must PASS at full scale: validate-extension + verify-recipe-path-guard (Wall 1 -- no opentabs runtime shipped) + verify-classification-gate (every emitted origin classified) + verify-catalog-crosscheck (derived-vs-declared + READ_ONLY_SAFE + the payment-op guard -- NO payment op safe-and-invocable -- + the Plan-03 COMMERCE_SENSITIVE_SERVICES backstop -- every commerce brand classifies sensitive) + verify-no-duplicate-stem (0 colliding stems). If classifyGate aborts naming an unclassified origin, that origin was missed in Plan 03 -- add it to service-denylist.json (sensitive for a commerce/payment brand, per the conservative posture) and re-run; do NOT downgrade a commerce brand to clear the gate. NOTE: classifyGate does NOT abort on a READ-ONLY commerce brand (no payment token trips the heuristic), so the AUTHORITATIVE check for a missed read-only commerce brand is the Plan-03 COMMERCE_SENSITIVE_SERVICES backstop in verify-catalog-crosscheck.mjs (part of validate:extension here): if it fails naming a commerce brand that classifies SAFE, add that brand to BOTH service-denylist.json sensitiveOrigins AND the COMMERCE_SENSITIVE_SERVICES roster, then re-run -- do NOT clear it by removing the brand from the roster or downgrading it. If the no-duplicate-stem gate fails, add the missing STEM_OVERRIDE (Plan 02's map) and re-run. Commit the emitted descriptors + the regenerated catalog + the filled provenance/seed.
  </action>
  <verify>
    <automated>node --import tsx ./scripts/import-opentabs-catalog.mjs && node scripts/package-extension.mjs && npm run validate:extension && test $(ls catalog/descriptors/opentabs__*.json 2>/dev/null | wc -l | tr -d ' ') -ge 2000</automated>
  </verify>
  <acceptance_criteria>
    - the importer emits >= ~2,374 descriptors (>= 2000 opentabs__*.json files) with 0 colliding opentabs__<stem>__* filenames (verify-no-duplicate-stem PASS)
    - validate:extension is GREEN at full scale: recipe-path-guard (Wall 1), classifyGate (every origin classified), crosscheck (payment-op guard + READ_ONLY_SAFE + the COMMERCE_SENSITIVE_SERVICES commerce-roster backstop -- no payment op safe-and-API-invocable, every commerce brand sensitive), no-duplicate-stem
    - the 40 overlap apps' descriptors are regenerated from real source; the 13 hand-only apps' descriptors are still present (augment)
    - recipe-index.generated.js is regenerated with the unchanged IIFE/djb2 shape (INV-01); only the descriptor DATA grew
    - _provenance.json carries per-app provenance for the real apps + the 13 hand-only markers
  </acceptance_criteria>
  <done>~2,374 real descriptors emitted (augment-preserving), the catalog regenerated (INV-01 intact), and validate:extension green at full scale (all gates incl payment-op + the COMMERCE_SENSITIVE_SERVICES backstop + no-dup-stem).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update coverage-report test + re-assert no-dead-entry + the doordash payment proof at full scale</name>
  <files>tests/coverage-report.test.js</files>
  <behavior>
    - Test 1: reportCoverage over the regenerated catalog yields totals.dead === 0 (no dead entry over the full ~2,400-descriptor corpus)
    - Test 2: realAppCount reflects the full corpus (>= ~110 distinct services), reported vs the ~117 milestone target
    - Test 3: every Phase-39 commerce stem + every payment op is backing:'dom' (no payment op API-invocable) -- the DOM-only batch assertion holds at full scale
    - Test 4: the head bucket still includes the 3 heads + the hand-authored recipes (HEAD_EXPECTED), unchanged by the augment
  </behavior>
  <read_first>
    - tests/coverage-report.test.js (the existing asserts: realAppCount > 50 [line ~212], PHASE_39_STEMS DOM-only, HEAD_EXPECTED, the payment-op-not-invocable check, the 39-06 manifest completeness)
    - scripts/coverage-report.mjs reportCoverage() (the dual-export the test drives; MILESTONE_APP_TARGET = 117)
    - tests/no-dead-entry.test.js (the generalized opentabs__* loader -- runs unchanged; re-confirm it passes at full scale)
    - tests/sensitive-write-import-gate.test.js (the doordash place_order proof -- doordash is regenerated; confirm the shipped descriptor still gates)
  </read_first>
  <action>
    Update tests/coverage-report.test.js for the ~130-app corpus: RAISE the realAppCount floor from `> 50` to reflect the full corpus (assert realAppCount >= ~110 distinct services -- the 117 real services minus the 3 enumerate-skipped heads, plus the heads' own services + the hand-authored recipe origins; leave margin so the exact count is not brittle) and keep the report-vs-117 milestone-target line. KEEP unchanged: the totals.dead === 0 assertion (the no-dead-entry invariant over the complete corpus), the PHASE_39_STEMS DOM-only assertion (every commerce stem backing:'dom'), the HEAD_EXPECTED head-bucket assertion (the 3 heads + reddit.inbox/github.notifications -- the augment must NOT change the head bucket), and the payment-op-not-invocable assertion (no payment op backing recipe/handler at full scale). The 39-06 manifest completeness check (every VENDORED+IMPORTED manifest stem still covered) stays -- it remains satisfied post-augment (those 53 stems are still in the corpus). Do NOT remove the doordash/discord end-to-end proofs (they live in tests/sensitive-write-import-gate.test.js and must still pass -- doordash's place_order is regenerated from real source; confirm it stays service www.doordash.com + sideEffectClass write + backing dom). Run the three tests to confirm green at full scale.
  </action>
  <verify>
    <automated>node scripts/coverage-report.mjs && node tests/coverage-report.test.js && node tests/no-dead-entry.test.js && node tests/sensitive-write-import-gate.test.js</automated>
  </verify>
  <acceptance_criteria>
    - reportCoverage over the regenerated catalog: totals.dead === 0; realAppCount >= ~110 (reported vs ~117); every payment op + every Phase-39 commerce stem is backing:'dom'
    - the head bucket is unchanged (HEAD_EXPECTED present); the augment did not clobber a head
    - no-dead-entry passes at full scale (every searchable slug resolves to a non-null seam tier)
    - the doordash payment-write proof + the discord write proof still pass on the regenerated shipped descriptors
  </acceptance_criteria>
  <done>coverage-report.test updated for ~130 apps (realAppCount >= ~110, 0 dead, DOM-only commerce, payment-not-invocable); no-dead-entry + the doordash/discord proofs green at full scale.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| emitted descriptor corpus -> the consent gate under Auto | a payment op classed read or backing recipe/handler would run write-capable under Auto = money moved |
| the import merge -> the shipped catalog | an unclassified origin or a colliding stem ships a wrong/writable descriptor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39.5-09 | Elevation of Privilege | a payment/money-moving op writable-or-invocable under Auto across the ~130-app corpus (THE headline) | mitigate | the TRIPLE mitigation re-asserted at full scale: every breadth descriptor backing:'dom' (frozen backingFor()) + the payment-op CI guard (Task 1 crosscheck -- no payment op safe-and-invocable) + commerce/payment origins SENSITIVE (Plan 03, now enforced by the COMMERCE_SENSITIVE_SERVICES build gate in the same crosscheck -- an omitted/downgraded commerce brand fails the build); the real doordash payment-write proof (Task 2) shows the shipped descriptor is posture-B gated |
| T-39.5-10 | Tampering | the augment loses the 13 hand-only apps or clobbers a head | mitigate | enumerateBatchApps re-emits the preserved hand-only src; EXISTING_HEAD_SERVICES skip keeps the heads; the coverage test (Task 2) HEAD_EXPECTED + the Plan-01 augment guard assert both |
| T-39.5-11 | Tampering | a stem collision silently overwrites a descriptor at full scale | mitigate | the no-duplicate-stem gate (Plan 02) runs in validate:extension over the full emitted corpus (Task 1) -- a collision fails the build |
| T-39.5-12 | Information Disclosure | the opentabs runtime (DOM/fetch) ships in the bundle (Wall 1) | mitigate | verify-recipe-path-guard runs in validate:extension (Task 1); the importer ships pure-data descriptors only; no @opentabs-dev/* runtime in extension/ |
| T-39.5-SC | Tampering | npm/pip/cargo installs | accept | NO new packages; the import uses the already-installed pinned @opentabs-dev/plugin-sdk + zod + tsx devDeps (spike-verified); no install task |
</threat_model>

<verification>
- `node --import tsx ./scripts/import-opentabs-catalog.mjs` emits >= ~2,374 descriptors with 0 stem collisions
- `node scripts/package-extension.mjs` regenerates recipe-index.generated.js (INV-01 IIFE/djb2)
- `npm run validate:extension` GREEN at full scale (recipe-path-guard + classifyGate + crosscheck incl payment-op guard + the COMMERCE_SENSITIVE_SERVICES commerce backstop + no-duplicate-stem)
- `node tests/coverage-report.test.js` + `node tests/no-dead-entry.test.js` + `node tests/sensitive-write-import-gate.test.js` all pass at full scale
</verification>

<success_criteria>
SC1 (full real-source import + augment: ~2,374 descriptors, 40 regenerated, 13 hand-only preserved) and the security half of SC3 (payment-op guard + the COMMERCE_SENSITIVE_SERVICES commerce backstop pass at full scale -- no payment op safe-and-invocable, every commerce brand sensitive) are satisfied. The catalog is regenerated with INV-01 intact and Wall 1 holds.
</success_criteria>

<output>
Create `.planning/phases/39.5-full-opentabs-source-import/39.5-04-SUMMARY.md` when done.
</output>
