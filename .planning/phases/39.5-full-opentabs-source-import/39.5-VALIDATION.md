---
phase: 39.5
slug: full-opentabs-source-import
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-25
---

# Phase 39.5 — Validation Strategy

> Full real-source import (~117 plugins / ~2,374 ops) via the proven pipeline. Source of truth:
> FULL-PARITY-SPIKE.md. Continues BRDTH-01/02/03; closes OpenTabs parity.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Standalone Node scripts (`PASS=/FAIL=`, `process.exit(1)`); importer under `tsx` |
| **Quick run command** | `node tests/<name>.test.js` |
| **Full suite command** | `npm test` (CI gate) |
| **Build/import** | `node scripts/import-opentabs-catalog.mjs` → `node scripts/package-extension.mjs` → `npm run validate:extension` |
| **Estimated runtime** | per-suite < ~5s; full `npm test` several minutes |

---

## Sampling Rate

- **After every task/batch commit:** the relevant `node tests/<name>.test.js`
- **After every import batch:** `npm run validate:extension` (classifyGate + crosscheck incl payment-op + recipe-path guard + no-dup-stem) + the eval/scale asserts
- **Before `/gsd-verify-work`:** `npm test` exits 0
- **Max feedback latency:** ~5 s per suite

---

## Per-Task Verification Map

| Requirement / SC | Correct Behavior | Test/Gate | Status |
|------------------|------------------|-----------|--------|
| SC1 — full real-source import + augment | 117 real plugins vendored (real src, no stub) + imported (~2,374 descriptors); 40 overlap regenerated; 13 hand-only PRESERVED; grafana/sqlpad skipped; target.apply_promo_code carved/renamed | `node scripts/import-opentabs-catalog.mjs` + `node scripts/coverage-report.mjs` (realAppCount ≈ 130) | ⬜ pending |
| SC2 — stem collisions | STEM_OVERRIDES complete (~40); a CI no-duplicate-stem assertion; 0 colliding `opentabs__<stem>__*` | `node tests/no-duplicate-stem.test.js` (new) + `npm run validate:extension` | ⬜ pending |
| SC3 — re-screen, no payment-under-Auto | all ~114 origins classified (classifyGate fail-closed); linkedin/youtube classified; commerce/payment brands SENSITIVE; payment-op guard passes at full scale (no payment op safe-and-invocable) | `node scripts/verify-classification-gate.mjs` + `node scripts/verify-catalog-crosscheck.mjs` (payment-op guard) + `node tests/payment-op-guard.test.js` + `node tests/sensitive-write-import-gate.test.js` | ⬜ pending |
| SC4 — scale + invariants | serialized index < ~2MB + loadJSON+first-search < ~100ms at ~2,374 ops; crosscheck + no-dead-entry + eval (recall@5, wrong-invoke=0) + full `npm test` green; Wall 1 (recipe-path-guard); INV-01 (IIFE/djb2) | `node tests/capability-search-eval.test.js` (scale asserts) + `node tests/no-dead-entry.test.js` + `node scripts/verify-recipe-path-guard.mjs` + `npm test` | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] no-duplicate-stem CI test (a synthetic stem collision FAILS the build)
- [ ] scale asserts in the eval harness (<2MB index, <100ms cold-start at full corpus)
- [ ] augment guard: a test asserting the 13 hand-only apps remain in the corpus after the real import

*Reuses the frozen importer + gates; the real source is fetched at execute time.*

---

## Manual-Only Verifications

| Behavior | Why Manual | Test Instructions |
|----------|------------|-------------------|
| (none expected) | Full import = fetch + frozen pipeline + gates → automatable | — |

*All phase behaviors have automated verification (no live-browser UAT in 39.5).*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 5s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
