---
phase: 39.5-full-opentabs-source-import
plan: 03
type: execute
wave: 3
depends_on: [39.5-01, 39.5-02]
files_modified:
  - extension/config/service-denylist.json
  - tests/full-corpus-screen.test.js
  - package.json
autonomous: true
requirements: [BRDTH-02, BRDTH-03]
must_haves:
  truths:
    - "linkedin.com is classified (sensitive, social axis) and youtube.com is classified (denied/sensitive, media axis) -- the 2 classifyGate fail-closed origins"
    - "Every commerce/payment/travel brand origin across the 117 real apps classifies SENSITIVE (conservative posture) -- reads under Auto, writes posture-B gated"
    - "No genuinely-non-commerce read origin is over-restricted, and no commerce/payment brand is downgraded to safe (READ_ONLY_SAFE only for non-commerce reads)"
    - "classifyGate fail-closed over the FULL ~114-origin corpus surfaces NO unclassified sensitive origin"
  artifacts:
    - path: "extension/config/service-denylist.json"
      provides: "linkedin/youtube classified + the conservative commerce roster covering the full corpus"
      contains: "linkedin"
    - path: "tests/full-corpus-screen.test.js"
      provides: "Proof: classifyGate over the full enumerated 117-app origin set has 0 failures; linkedin/youtube classified; every commerce/payment brand classifies sensitive"
      min_lines: 50
  key_links:
    - from: "tests/full-corpus-screen.test.js"
      to: "scripts/verify-classification-gate.mjs classifyGate"
      via: "enumerate every real-app origin -> classifyGate -> assert 0 failures"
      pattern: "classifyGate"
    - from: "extension/config/service-denylist.json"
      to: "linkedin.com / youtube.com"
      via: "sensitiveOrigins / deniedOrigins entry"
      pattern: "linkedin|youtube"
---

<objective>
Re-screen all ~114 distinct origins across the 117 real apps against the fail-closed classifyGate, classifying the EXACTLY 2 surfaced origins (linkedin.com social axis -> sensitive; youtube.com media axis -> denied/sensitive per the ToS axis) and applying the CONSERVATIVE commerce/payment reconciliation: every commerce/payment/travel brand origin stays SENSITIVE even though its REAL ops are largely read-only at this SHA. Add a full-corpus screening test that drives classifyGate over the complete enumerated origin set and asserts zero unclassified-sensitive failures + the conservative posture.

Purpose: THE security task. Under opt-out Auto a sensitive origin runs READS with no friction but GATES any WRITE (posture B / DENY-04). Keeping commerce/payment brands sensitive is fail-safe against a future op-set change silently making a money-mover writable-under-Auto -- without hurting today's read-only UX. The payment-op CI guard + the per-op sideEffectClass enforce that NO payment op is ever safe-and-API-invocable. classifyGate runs merge-time per batch at Plan 04; this plan makes the full-corpus origin set classify-complete first.
Output: the extended service-denylist.json (linkedin/youtube + the full conservative commerce roster) + tests/full-corpus-screen.test.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39.5-full-opentabs-source-import/39.5-CONTEXT.md
@.planning/phases/39-breadth-c-commerce-travel-misc-most-sensitive/FULL-PARITY-SPIKE.md

<read_first>
- FULL-PARITY-SPIKE.md Section 3 (origin-screening scope: the 114 origins, the 10 denied / 19 sensitive / 86 safe buckets, the 2 classifyGate FAILURES linkedin.com + youtube.com, AND the commerce-SAFE policy-gap paragraph -- the reconciliation), Section 5.4 (re-screening approach), TOP RISK 2 (the conservative reconciliation rationale)
- .planning/phases/39.5-full-opentabs-source-import/39.5-CONTEXT.md (<decisions> "Re-screening" -- linkedin->sensitive, youtube->denied/sensitive; "Commerce/payment reconciliation -- CONSERVATIVE": keep commerce/payment/travel brands SENSITIVE, do NOT downgrade)
- extension/config/service-denylist.json (the EXISTING roster + the long _comment audit trail -- extend the deniedOrigins/sensitiveOrigins arrays + append a Phase-39.5 paragraph to _comment; note the EXACT-host vs apex-suffix '*.' discipline)
- scripts/verify-classification-gate.mjs classifyGate + sensitivityHeuristic + the AXES (the social axis already lists 'linkedin'; the media axis already lists 'youtube' -- so an explicit classification is what makes them PASS, the heuristic already flags them)
- extension/utils/service-denylist.js classify()/load() (the dual-export the test loads; classify(origin) -> {sensitive,denied})
- scripts/verify-catalog-crosscheck.mjs READ_ONLY_SAFE_SERVICES (line ~198 -- the set genuinely-non-commerce read origins join; do NOT add a commerce/payment brand here)
</read_first>

<interfaces>
<!-- classifyGate(items) -> { failures } from scripts/verify-classification-gate.mjs. -->
<!-- An item: { origin, service?, slug?, description? }. A commerce/payment origin that is classified sensitive hits the classify()-matched `continue` BEFORE the heuristic; an UNclassified origin that trips an axis is a FAILURE. -->

The 2 classifyGate FAILURES to classify (the ONLY 2 that need a decision today):
  linkedin.com  -> sensitiveOrigins  (social axis -- the 'linkedin' token; CONTEXT: sensitive)
  youtube.com   -> deniedOrigins     (media/ToS axis; CONTEXT: denied/sensitive per the ToS axis; the DENY-01 media set is denied. NOTE music.youtube.com is ALREADY denied -- add the bare youtube.com via an EXACT-host or the '*.youtube.com' wildcard form, Claude's Discretion, NO broader than the app's own origin)

The CONSERVATIVE commerce reconciliation (the established posture -- do NOT re-derive):
  - KEEP every commerce/payment/travel brand origin SENSITIVE. The 39-01..06 roster ALREADY classifies the curated commerce brands (doordash/ubereats/uber/lyft/grubhub/instacart/dominos/chipotle/amazon/ebay/etsy/bestbuy/costco/walmart/target/craigslist/shopify/booking/airbnb/expedia/kayak/opentable/ticketmaster/stubhub/eventbrite). The 117-real corpus ADDS commerce/travel/payment brands not yet in the roster -- any real app whose origin is a commerce/payment/travel/paid-booking brand (e.g. a new retail/food/travel/fintech origin among the 77 net-new) MUST be added to sensitiveOrigins (EXACT-host or apex-suffix '*.' form, no broader than its own origin).
  - Do NOT downgrade a commerce/payment brand to safe just because its current real ops are read-only (real doordash/booking/walmart = list/get, no place_order -- still kept sensitive).
  - READ_ONLY_SAFE (left UNCLASSIFIED/safe + added to crosscheck READ_ONLY_SAFE_SERVICES) is ONLY for genuinely-non-commerce read origins (the existing calendly/yelp/tripadvisor/zillow/grafana pattern). A genuinely-non-commerce real read app (e.g. a docs/analytics/observability read origin) MAY be left safe; a commerce/payment brand MAY NOT.
  - The spike's bucket counts (Section 3): of the 117, ~10 already denied + ~19 already sensitive + ~86 default-safe. The 86 default-safe are overwhelmingly genuinely-non-commerce dev/productivity/cloud/docs origins (airtable/github/linear/notion/datadog/vercel/...) -- those stay safe correctly. The reconciliation TASK is: walk the commerce/payment/travel subset of the 117 and confirm each is sensitive (add any net-new commerce brand the existing roster lacks).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Classify linkedin/youtube + apply the conservative commerce reconciliation</name>
  <files>extension/config/service-denylist.json</files>
  <read_first>
    - extension/config/service-denylist.json (the deniedOrigins/sensitiveOrigins arrays + the _comment audit trail -- extend, do not rewrite; preserve EVERY existing entry)
    - FULL-PARITY-SPIKE.md Section 3 (the 2 surfaced origins + the commerce-SAFE policy gap), Section 5.4 (the re-screening steps)
    - .planning/phases/39.5-full-opentabs-source-import/39.5-CONTEXT.md (<decisions> "Re-screening" -- the conservative posture, do not downgrade)
    - the <interfaces> block above (the EXACT linkedin/youtube classifications + the commerce subset to confirm sensitive)
  </read_first>
  <action>
    Extend extension/config/service-denylist.json WITHOUT removing any existing entry. (1) Add `linkedin.com` to sensitiveOrigins (social axis -- conservative default for a social/professional-network origin; reads under Auto, writes posture-B gated). (2) Add the bare `youtube.com` to deniedOrigins (the ToS-hostile media axis -- consistent with the DENY-01 media set and the already-denied music.youtube.com; use the EXACT-host `https://youtube.com` + `https://www.youtube.com`, OR the `https://*.youtube.com` wildcard form per your discretion, but NO broader than YouTube's own origin and consistent with the existing exact-host discipline -- note the DENY-01 comment kept music.youtube.com EXACT specifically to avoid over-broadening, so prefer the explicit youtube.com/www.youtube.com forms unless the wildcard is clearly justified). (3) Apply the CONSERVATIVE commerce reconciliation: enumerate the commerce/payment/travel/paid-booking brand origins across the 117 real apps (the vendored set from Plan 01) and ensure EACH is in sensitiveOrigins -- the 39-01..06 curated commerce roster is already present; ADD any net-new commerce/payment/travel brand among the 77 net-new real apps that is not yet classified (EXACT-host `https://www.<app>.<tld>` or apex-suffix `https://*.<brand>.<tld>` form, no broader than the app's own origin). Do NOT downgrade any commerce/payment brand to safe. Do NOT add any commerce/payment brand to READ_ONLY_SAFE. Append a Phase-39.5 paragraph to the _comment audit trail documenting: linkedin->sensitive, youtube->denied, the conservative commerce posture (commerce/payment brands kept sensitive despite read-only real ops -- fail-safe against a future op-set change), and the EXACT-host/apex-suffix discipline. ASCII-only.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const d=JSON.parse(fs.readFileSync('extension/config/service-denylist.json','utf8')); const all=[...d.deniedOrigins,...d.sensitiveOrigins].join(' '); if(!/linkedin/.test(all)){console.error('linkedin missing');process.exit(1)} if(!/youtube\.com/.test(all)){console.error('youtube.com missing');process.exit(1)} if(!d.deniedOrigins.some(o=>/youtube\.com/.test(o))){console.error('youtube not denied');process.exit(1)} console.log('OK linkedin+youtube classified')"</automated>
  </verify>
  <acceptance_criteria>
    - linkedin.com is in sensitiveOrigins; youtube.com is in deniedOrigins (bare youtube.com classified, not just music.youtube.com)
    - every commerce/payment/travel brand origin across the 117 real apps is in sensitiveOrigins (no commerce brand left safe; no commerce brand downgraded)
    - no commerce/payment brand was added to READ_ONLY_SAFE
    - every pre-existing denylist entry is preserved; the _comment carries a Phase-39.5 audit paragraph
  </acceptance_criteria>
  <done>service-denylist.json classifies linkedin (sensitive) + youtube (denied) and keeps every commerce/payment/travel brand across the full corpus SENSITIVE (conservative).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Full-corpus screening test (classifyGate over all 117-app origins = 0 failures)</name>
  <files>tests/full-corpus-screen.test.js, package.json</files>
  <behavior>
    - Test 1: classifyGate over EVERY enumerated real-app origin (derived from the vendored set + the importer's readPluginMeta) yields 0 failures (the full origin set is classify-complete / fail-closed satisfied)
    - Test 2: classify(https://linkedin.com).sensitive === true AND classify(https://youtube.com).denied === true (the 2 surfaced origins governed)
    - Test 3: a representative commerce/payment/travel brand set (doordash/amazon/booking/walmart/shopify/+ any net-new) each classify sensitive (the conservative posture holds corpus-wide)
    - Test 4: a representative genuinely-non-commerce read origin (e.g. datadog/airtable) is NOT forced sensitive (no over-restriction)
  </behavior>
  <read_first>
    - scripts/verify-classification-gate.mjs classifyGate (the dual-export the test drives over the full origin set)
    - scripts/import-opentabs-catalog.mjs enumerateBatchApps + readPluginMeta (to derive every real-app origin the SAME way the importer does)
    - extension/utils/service-denylist.js load()/classify() (load the REAL committed roster, NOT a stub -- the live-roster pattern)
    - tests/breadth-batch-gate.test.js (the live-roster classifyGate test convention to mirror) + tests/payment-op-guard.test.js (the dynamic import() of the .mjs dual-export from a CJS test)
  </read_first>
  <action>
    Write tests/full-corpus-screen.test.js as a zero-framework FSB node test (check(cond,msg) counter, PASS=/FAIL=, process.exit(failed>0?1:0), ASCII-only) that: loads the REAL committed service-denylist roster (await Denylist.load(), NOT a hand-stub); enumerates EVERY real-app origin by importing the importer's enumerateBatchApps + readPluginMeta and mapping each app -> `https://<service>` (the SAME origin the importer gates); drives the REAL classifyGate (dynamic import() of scripts/verify-classification-gate.mjs) over that full origin set and asserts failures.length === 0 (the full corpus is classify-complete -- fail-closed satisfied BEFORE the Plan-04 merge-time gate runs); asserts classify('https://linkedin.com').sensitive === true and classify('https://youtube.com').denied === true; asserts a representative commerce/payment/travel brand set each classify sensitive (the conservative posture); and asserts a representative genuinely-non-commerce read origin (datadog/airtable) is NOT sensitive (no over-restriction). Register the test in package.json's `test` chain (append ` && node tests/full-corpus-screen.test.js`).
  </action>
  <verify>
    <automated>node tests/full-corpus-screen.test.js && grep -q "full-corpus-screen.test.js" package.json</automated>
  </verify>
  <acceptance_criteria>
    - classifyGate over the full enumerated 117-app origin set yields 0 failures (fail-closed satisfied corpus-wide)
    - linkedin classifies sensitive; youtube classifies denied
    - every commerce/payment brand in the representative set classifies sensitive; a non-commerce read origin is not over-restricted
    - the test is registered in npm test
  </acceptance_criteria>
  <done>tests/full-corpus-screen.test.js proves the full ~114-origin corpus is classify-complete (0 classifyGate failures), linkedin/youtube governed, commerce brands conservatively sensitive.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| origin classification -> the consent gate (Auto) | an unclassified sensitive origin runs writes under Auto with no friction (the "gap == allow" failure) |
| commerce/payment brand -> a future op-set change | a brand kept safe today becomes write-capable when a re-vendor adds a payment op -> money moves under Auto |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39.5-06 | Elevation of Privilege | an unclassified sensitive origin among the 114 ships writable-under-Auto | mitigate | classifyGate fail-closed runs merge-time per batch (Plan 04) AND the full-corpus screen test (Task 2) asserts 0 failures over EVERY enumerated origin first; linkedin/youtube (the 2 surfaced) classified (Task 1) |
| T-39.5-07 | Elevation of Privilege | a commerce/payment brand kept safe is later made write-capable by an op-set change | mitigate | the CONSERVATIVE reconciliation (Task 1) keeps every commerce/payment/travel brand SENSITIVE so any write is posture-B gated regardless of the current read-only op set; READ_ONLY_SAFE excludes commerce brands |
| T-39.5-08 | Spoofing | an over-broad denylist wildcard captures an unrelated origin | mitigate | the EXACT-host / apex-suffix '*.' discipline (Task 1) keeps each pattern no broader than the app's own origin (youtube prefers exact-host per the DENY-01 precedent) |
| T-39.5-SC | Tampering | npm/pip/cargo installs | accept | NO new packages this plan (denylist JSON + a test only); no install task |
</threat_model>

<verification>
- `node tests/full-corpus-screen.test.js` passes (classifyGate 0 failures corpus-wide; linkedin/youtube governed; commerce sensitive)
- service-denylist.json preserves every prior entry + adds linkedin/youtube + any net-new commerce brand
- the conservative posture is documented in the _comment audit trail
- the test is registered in npm test
</verification>

<success_criteria>
SC3's screening half is satisfied: all ~114 origins classify-complete (classifyGate fail-closed), linkedin/youtube classified, commerce/payment brands kept SENSITIVE (conservative). The payment-op guard PASS at full scale is proven at Plan 04 (the import + crosscheck).
</success_criteria>

<output>
Create `.planning/phases/39.5-full-opentabs-source-import/39.5-03-SUMMARY.md` when done.
</output>
