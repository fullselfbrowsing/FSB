---
phase: 39.5-full-opentabs-source-import
plan: 03
type: execute
wave: 3
depends_on: [39.5-01, 39.5-02]
files_modified:
  - extension/config/service-denylist.json
  - scripts/verify-catalog-crosscheck.mjs
  - tests/full-corpus-screen.test.js
  - package.json
autonomous: true
requirements: [BRDTH-02, BRDTH-03]
must_haves:
  truths:
    - "linkedin.com is classified (sensitive, social axis) and youtube.com is classified (denied/sensitive, media axis) -- the 2 classifyGate fail-closed origins"
    - "Every commerce/payment/travel/paid-booking brand origin across the 117 real apps -- the ENUMERATED COMMERCE_SENSITIVE roster: the existing curated set + the net-new homedepot/priceline/redfin/starbucks/pandaexpress + any further commerce brand walked from the Plan-01 vendored urlPatterns -- classifies SENSITIVE (conservative posture): reads under Auto, writes posture-B gated"
    - "A build-failing backstop (COMMERCE_SENSITIVE_SERVICES + checkCommerceSensitiveClassified in verify-catalog-crosscheck.mjs, run by validate:extension -- the inverse of READ_ONLY_SAFE_SERVICES) asserts EVERY commerce-roster brand classifies sensitive; an omitted or downgraded commerce brand (incl a net-new READ-ONLY one that classifyGate cannot catch) FAILS THE BUILD -- the conservative posture is a checked invariant, not a manual review"
    - "No genuinely-non-commerce read origin is over-restricted, and no commerce/payment brand is downgraded to safe (READ_ONLY_SAFE only for non-commerce reads; the commerce roster is DISJOINT from READ_ONLY_SAFE_SERVICES, asserted)"
    - "classifyGate fail-closed over the FULL ~114-origin corpus surfaces NO unclassified sensitive origin"
  artifacts:
    - path: "extension/config/service-denylist.json"
      provides: "linkedin/youtube classified + the conservative commerce roster covering the full corpus (the net-new homedepot/priceline/redfin/starbucks/pandaexpress + any walked commerce brand added to sensitiveOrigins)"
      contains: "linkedin"
    - path: "scripts/verify-catalog-crosscheck.mjs"
      provides: "The COMMERCE_SENSITIVE_SERVICES roster + checkCommerceSensitiveClassified build gate (the inverse of READ_ONLY_SAFE_SERVICES) -- asserts every commerce brand classifies sensitive AND the roster is disjoint from READ_ONLY_SAFE; merged into runCli so validate:extension fails on an omitted/downgraded commerce brand"
      contains: "COMMERCE_SENSITIVE_SERVICES"
      exports: ["checkCommerceSensitiveClassified"]
    - path: "tests/full-corpus-screen.test.js"
      provides: "Proof: classifyGate over the full enumerated 117-app origin set has 0 failures; linkedin/youtube classified; checkCommerceSensitiveClassified over the full COMMERCE_SENSITIVE_SERVICES roster has 0 failures (drives the exported backstop)"
      min_lines: 50
  key_links:
    - from: "tests/full-corpus-screen.test.js"
      to: "scripts/verify-classification-gate.mjs classifyGate"
      via: "enumerate every real-app origin -> classifyGate -> assert 0 failures"
      pattern: "classifyGate"
    - from: "scripts/verify-catalog-crosscheck.mjs COMMERCE_SENSITIVE_SERVICES"
      to: "extension/utils/service-denylist.js classify()"
      via: "assert each enumerated commerce brand classifies sensitive/denied -- fail the build if safe"
      pattern: "COMMERCE_SENSITIVE_SERVICES"
    - from: "extension/config/service-denylist.json"
      to: "linkedin.com / youtube.com"
      via: "sensitiveOrigins / deniedOrigins entry"
      pattern: "linkedin|youtube"
---

<objective>
Re-screen all ~114 distinct origins across the 117 real apps against the fail-closed classifyGate, classifying the EXACTLY 2 surfaced origins (linkedin.com social axis -> sensitive; youtube.com media axis -> denied/sensitive per the ToS axis) and applying the CONSERVATIVE commerce/payment reconciliation as an ENUMERATED roster: every commerce/payment/travel/paid-booking brand origin stays SENSITIVE even though its REAL ops are largely read-only at this SHA. Add a BUILD-FAILING backstop (COMMERCE_SENSITIVE_SERVICES + checkCommerceSensitiveClassified in verify-catalog-crosscheck.mjs, the inverse of READ_ONLY_SAFE_SERVICES) that asserts every roster brand classifies sensitive, plus a full-corpus screening test that drives classifyGate over the complete enumerated origin set (0 unclassified-sensitive failures) AND drives the commerce backstop over the full roster.

Purpose: THE security task. Under opt-out Auto a sensitive origin runs READS with no friction but GATES any WRITE (posture B / DENY-04). Keeping commerce/payment brands sensitive is fail-safe against a future op-set change silently making a money-mover writable-under-Auto -- without hurting today's read-only UX. CRITICAL GAP this plan closes: classifyGate does NOT abort on a read-only commerce brand (homedepot/priceline/redfin/starbucks/pandaexpress emit only list/get/search ops -- no payment token trips the heuristic) and the payment-op guard keys ONLY on a payment verb/op-name -- so NEITHER existing gate catches a MISSED net-new read-only commerce brand; it would ship SAFE-under-Auto silently. So this plan does TWO things: (1) ENUMERATE the conservative commerce roster EXPLICITLY (the curated set + the net-new homedepot/priceline/redfin/starbucks/pandaexpress + any further commerce brand walked from the Plan-01 vendored urlPatterns) and classify each sensitive, and (2) add a BUILD-FAILING backstop (COMMERCE_SENSITIVE_SERVICES + checkCommerceSensitiveClassified, the inverse of READ_ONLY_SAFE_SERVICES) that asserts every roster brand classifies sensitive -- so an omitted/downgraded commerce brand FAILS validate:extension. This converts the spike's manual reconciliation review into a checked invariant. The payment-op CI guard + the per-op sideEffectClass still enforce that NO payment op is ever safe-and-API-invocable. classifyGate runs merge-time per batch at Plan 04; this plan makes the full-corpus origin set classify-complete first.
Output: the extended service-denylist.json (linkedin/youtube + the full conservative commerce roster) + the COMMERCE_SENSITIVE_SERVICES backstop in verify-catalog-crosscheck.mjs + tests/full-corpus-screen.test.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39.5-full-opentabs-source-import/39.5-CONTEXT.md
@.planning/phases/39-breadth-c-commerce-travel-misc-most-sensitive/FULL-PARITY-SPIKE.md

<read_first>
- FULL-PARITY-SPIKE.md Section 3 (origin-screening scope: the 114 origins, the 10 denied / 19 sensitive / 86 safe buckets, the 2 classifyGate FAILURES linkedin.com + youtube.com, AND the commerce-SAFE policy-gap paragraph naming homedepot/priceline/redfin/starbucks/pandaexpress + doordash/walmart/target/... -- the reconciliation), Section 5.4 (re-screening approach), TOP RISK 2 (the conservative reconciliation rationale)
- .planning/phases/39.5-full-opentabs-source-import/39.5-CONTEXT.md (<decisions> "Re-screening" -- linkedin->sensitive, youtube->denied/sensitive; "Commerce/payment reconciliation -- CONSERVATIVE": keep commerce/payment/travel brands SENSITIVE, do NOT downgrade)
- extension/config/service-denylist.json (the EXISTING roster + the long _comment audit trail -- extend the deniedOrigins/sensitiveOrigins arrays + append a Phase-39.5 paragraph to _comment; note the EXACT-host vs apex-suffix '*.' discipline)
- scripts/verify-classification-gate.mjs classifyGate + sensitivityHeuristic + the AXES (the social axis already lists 'linkedin'; the media axis already lists 'youtube' -- so an explicit classification is what makes them PASS, the heuristic already flags them; but NOTE the heuristic does NOT flag a read-only commerce brand -- see <interfaces>)
- extension/utils/service-denylist.js classify()/load() (the dual-export the test loads; classify(origin) -> {sensitive,denied}; matching is EXACT host OR '*.' apex-suffix, scheme-sensitive)
- scripts/verify-catalog-crosscheck.mjs READ_ONLY_SAFE_SERVICES (line ~198 -- the set genuinely-non-commerce read origins join; do NOT add a commerce/payment brand here) AND checkReadOnlySafeOrigins (lines ~214-236) + the runCli failure-merge (lines ~398-429) -- the PATTERN to MIRROR (inverse direction): this plan ADDS the symmetric COMMERCE_SENSITIVE_SERVICES roster + checkCommerceSensitiveClassified guard here, merged into runCli alongside checkPaymentOpsNotSafeInvocable so validate:extension fails on an unclassified/downgraded commerce brand
</read_first>

<interfaces>
<!-- classifyGate(items) -> { failures } from scripts/verify-classification-gate.mjs. -->
<!-- An item: { origin, service?, slug?, description? }. A commerce/payment origin that is classified sensitive hits the classify()-matched `continue` BEFORE the heuristic; an UNclassified origin that trips an axis is a FAILURE. -->

The 2 classifyGate FAILURES to classify (the ONLY 2 that need a decision today):
  linkedin.com  -> sensitiveOrigins  (social axis -- the 'linkedin' token; CONTEXT: sensitive)
  youtube.com   -> deniedOrigins     (media/ToS axis; CONTEXT: denied/sensitive per the ToS axis; the DENY-01 media set is denied. NOTE music.youtube.com is ALREADY denied -- add the bare youtube.com (apex + www) via an EXACT-host or the '*.youtube.com' wildcard form, Claude's Discretion, NO broader than the app's own origin)

WHY classifyGate is NOT enough for the commerce case (the gap this plan closes -- read this before touching the roster):
  classifyGate only FAILS on an origin whose host/slug/description trips a heuristic AXIS (a finance/payment token, a social brand, etc.). A NET-NEW READ-ONLY commerce brand (homedepot/priceline/redfin/starbucks/pandaexpress) emits only list/get/search ops and its host carries no place-order/checkout/charge token -> it does NOT trip any axis -> classifyGate PASSES it as SAFE. The payment-op guard (checkPaymentOpsNotSafeInvocable) keys ONLY on a payment VERB/op-name, and a read-only app emits none -> the guard never keys on it either. So NEITHER existing gate catches a missed read-only commerce brand: it ships SAFE-under-Auto silently. The fix is an ENUMERATED roster + a build-failing backstop (below) -- NOT a heuristic.

The CONSERVATIVE commerce reconciliation -- the ENUMERATED roster (do NOT re-derive the posture; DO enumerate the members):
  KEEP every commerce/payment/travel/paid-booking brand origin SENSITIVE even when its real ops are read-only. The roster = the union of:
  (a) the EXISTING curated commerce brands already in sensitiveOrigins (food-delivery/rideshare: doordash/ubereats/uber/lyft/grubhub/instacart/dominos/chipotle; retail/marketplace: amazon/ebay/etsy/bestbuy/costco/walmart/target/craigslist/shopify; travel/booking: booking/airbnb/expedia/kayak/opentable; ticketing: ticketmaster/stubhub/eventbrite) -- already classified, KEEP;
  (b) the NET-NEW commerce brands the spike named as SAFE-bucketed-but-money-adjacent (Section 3 policy gap): homedepot, priceline, redfin, starbucks, pandaexpress -- ADD each to sensitiveOrigins;
  (c) ANY FURTHER retail / food-delivery / rideshare / travel / marketplace / fintech / paid-booking / ticketing brand discovered by WALKING the Plan-01 vendored vendor/opentabs-snapshot/plugins/*/package.json urlPatterns[0] host across the 77 net-new real apps -- ADD each (criterion: the brand's primary surface is buying / ordering / booking / paying / marketplace-transacting). Plan 01 (wave 1) vendors before this plan (wave 3) runs, so the vendored dirs ARE present to enumerate.
  Host form: use the EXACT service host the importer emits (the urlPatterns[0] host, lowercased -- e.g. www.homedepot.com / www.priceline.com / www.redfin.com / www.starbucks.com / www.pandaexpress.com; confirm each against the vendored package.json), in EXACT-host (https://www.<app>.<tld>) or apex-suffix (https://*.<brand>.<tld>) form, no broader than the app's own origin. The denylist pattern MUST cover the exact emitted service host (else classifyGate at Plan 04 has a gap).
  Do NOT downgrade any commerce/payment brand to safe; do NOT add any commerce/payment brand to READ_ONLY_SAFE. zillow/grafana/calendly/yelp/tripadvisor/reddit stay safe (READ_ONLY_SAFE, already reconciled in 39-05/06) -- they are genuinely-non-commerce reads and are NOT on the commerce roster; do NOT move them.

The BUILD-FAILING backstop (the security invariant -- MIRROR checkReadOnlySafeOrigins, INVERSE direction) to ADD to scripts/verify-catalog-crosscheck.mjs:
  - COMMERCE_SENSITIVE_SERVICES = new Set([...]) -- the bare service hosts of the enumerated roster (a) + (b) + (c), lowercased (the SAME host-string form as READ_ONLY_SAFE_SERVICES at line ~198).
  - export function checkCommerceSensitiveClassified(classifyFn) -> { failures }:
      for each service in COMMERCE_SENSITIVE_SERVICES: c = classifyFn('https://' + service); if NOT (c.sensitive === true || c.denied === true) push a failure naming the service ("on the COMMERCE_SENSITIVE roster but classifies SAFE -- a commerce/payment brand must be sensitive so its writes are posture-B gated; classify it sensitive in service-denylist.json or remove it from the roster").
      DISJOINTNESS: for each service in COMMERCE_SENSITIVE_SERVICES: if READ_ONLY_SAFE_SERVICES.has(service) push a failure ("on BOTH the commerce-sensitive roster and READ_ONLY_SAFE -- a brand cannot be both"). This blocks the exact downgrade-to-READ_ONLY_SAFE attack.
  - Wire it into runCli alongside checkPaymentOpsNotSafeInvocable: after `const paymentOnly = checkPaymentOpsNotSafeInvocable(...)`, add `const commerceOnly = checkCommerceSensitiveClassified(Denylist.classify);` and concat commerceOnly.failures into allFailures (Denylist.load() is already awaited above). EXPORT both COMMERCE_SENSITIVE_SERVICES and checkCommerceSensitiveClassified so the Task-2 test drives the SAME roster the build gate uses.
  - This runs under `npm run validate:extension` (which chains verify-catalog-crosscheck.mjs) -> an omitted/downgraded commerce brand FAILS THE BUILD. It does NOT depend on the descriptor corpus (pure roster + classify()), so it catches a read-only commerce brand classifyGate cannot -- and it is checkable BEFORE the Plan-04 import.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Classify linkedin/youtube + enumerate the conservative commerce roster + add the build-failing backstop</name>
  <files>extension/config/service-denylist.json, scripts/verify-catalog-crosscheck.mjs</files>
  <read_first>
    - extension/config/service-denylist.json (the deniedOrigins/sensitiveOrigins arrays + the _comment audit trail -- extend, do not rewrite; preserve EVERY existing entry)
    - scripts/verify-catalog-crosscheck.mjs READ_ONLY_SAFE_SERVICES (line ~198) + checkReadOnlySafeOrigins (lines ~214-236) -- the PATTERN to MIRROR (inverse direction) for the new COMMERCE_SENSITIVE_SERVICES roster + checkCommerceSensitiveClassified guard; the runCli failure-merge (lines ~398-429) is where to wire it in (Denylist.load() already awaited; classify already consulted by the payment-op guard)
    - extension/utils/service-denylist.js classify() (EXACT host OR '*.' apex-suffix matching, scheme-sensitive -- so the roster host must be one the denylist pattern covers)
    - FULL-PARITY-SPIKE.md Section 3 (the 2 surfaced origins + the commerce-SAFE policy gap naming homedepot/priceline/redfin/starbucks/pandaexpress), Section 5.4 (the re-screening steps), TOP RISK 2 (the conservative reconciliation)
    - vendor/opentabs-snapshot/plugins/*/package.json urlPatterns[0] (Plan-01 vendored -- WALK these to enumerate any further commerce brand among the 77 net-new)
    - .planning/phases/39.5-full-opentabs-source-import/39.5-CONTEXT.md (<decisions> "Re-screening" -- the conservative posture, do not downgrade)
    - the <interfaces> block above (the EXACT linkedin/youtube classifications + the enumerated commerce roster + the backstop contract)
  </read_first>
  <action>
    Extend extension/config/service-denylist.json AND add the commerce backstop to scripts/verify-catalog-crosscheck.mjs, WITHOUT removing any existing entry.
    (1) Add `linkedin.com` to sensitiveOrigins (social axis -- conservative default; reads under Auto, writes posture-B gated). Cover the emitted service origin (apex + www; an EXACT-host pair or the '*.linkedin.com' apex-suffix form, no broader than the app's own origin).
    (2) Add the bare `youtube.com` to deniedOrigins (the ToS-hostile media axis -- consistent with the DENY-01 media set and the already-denied music.youtube.com; use the EXACT-host `https://youtube.com` + `https://www.youtube.com`, OR `https://*.youtube.com` per your discretion, NO broader than YouTube's own origin -- prefer the explicit youtube.com/www.youtube.com forms per the DENY-01 exact-host precedent unless the wildcard is clearly justified; do NOT merely rely on the pre-existing music.youtube.com entry).
    (3) ENUMERATE + classify the conservative commerce roster: ensure EVERY brand in the <interfaces> roster is in sensitiveOrigins. The (a) existing curated commerce brands are already present -- KEEP them. ADD the (b) net-new spike-named brands: homedepot, priceline, redfin, starbucks, pandaexpress (EXACT emitted service host -- e.g. www.homedepot.com / www.priceline.com / www.redfin.com / www.starbucks.com / www.pandaexpress.com, confirm against the vendored package.json urlPatterns[0]). Then (c) WALK the Plan-01 vendored vendor/opentabs-snapshot/plugins/*/package.json urlPatterns[0] hosts across the 77 net-new real apps and ADD any further retail / food-delivery / rideshare / travel / marketplace / fintech / paid-booking / ticketing brand (criterion: primary surface is buying/ordering/booking/paying/marketplace-transacting). Use EXACT-host or apex-suffix '*.' form, no broader than the app's own origin. Do NOT downgrade any commerce/payment brand to safe; do NOT add any commerce/payment brand to READ_ONLY_SAFE; do NOT move zillow/grafana/calendly/yelp/tripadvisor/reddit (they stay READ_ONLY_SAFE / safe).
    (4) ADD THE BUILD-FAILING BACKSTOP to scripts/verify-catalog-crosscheck.mjs, MIRRORING the existing READ_ONLY_SAFE_SERVICES guard in the INVERSE direction: a `COMMERCE_SENSITIVE_SERVICES` Set of the bare service hosts of the FULL enumerated roster (a)+(b)+(c) (the SAME host-string form as READ_ONLY_SAFE_SERVICES); an exported `checkCommerceSensitiveClassified(classifyFn)` that for each roster host asserts classifyFn('https://'+host).sensitive===true OR .denied===true (else a failure: "on the COMMERCE_SENSITIVE roster but classifies SAFE...") AND asserts no roster host is also in READ_ONLY_SAFE_SERVICES (the disjointness failure). Wire it into runCli alongside checkPaymentOpsNotSafeInvocable (Denylist.load() is already awaited): `const commerceOnly = checkCommerceSensitiveClassified(Denylist.classify);` and concat commerceOnly.failures into allFailures so verify-catalog-crosscheck.mjs (hence `npm run validate:extension`) FAILS on an omitted/downgraded commerce brand. EXPORT COMMERCE_SENSITIVE_SERVICES + checkCommerceSensitiveClassified for the Task-2 test. Keep the module Wall-1-clean (no run-string-as-code / function-from-string / dynamic-module-loader constructs in code OR comments), ASCII-only.
    (5) Append a Phase-39.5 paragraph to the _comment audit trail documenting: linkedin->sensitive, youtube->denied, the conservative commerce posture (commerce/payment brands kept sensitive despite read-only real ops -- fail-safe against a future op-set change), the net-new additions (homedepot/priceline/redfin/starbucks/pandaexpress + any walked), the COMMERCE_SENSITIVE_SERVICES backstop (the manual reconciliation is now a checked BUILD invariant -- classifyGate + the payment-op guard CANNOT catch a missed read-only commerce brand, so the roster + the inverse-direction guard does), and the EXACT-host/apex-suffix discipline. ASCII-only.
  </action>
  <verify>
    <automated>node scripts/verify-catalog-crosscheck.mjs && node -e "(async()=>{const D=require('./extension/utils/service-denylist.js');await D.load();const f=(m)=>{console.error(m);process.exit(1)};if(!(D.classify('https://youtube.com').denied||D.classify('https://www.youtube.com').denied))f('youtube.com app origin not denied (music.youtube.com alone does not count)');if(!(D.classify('https://www.linkedin.com').sensitive||D.classify('https://linkedin.com').sensitive))f('linkedin not sensitive');for(const b of ['www.homedepot.com','www.priceline.com','www.redfin.com','www.starbucks.com','www.pandaexpress.com']){const c=D.classify('https://'+b);if(!(c.sensitive||c.denied))f(b+' net-new commerce brand not sensitive')}console.log('OK youtube denied + linkedin sensitive + net-new commerce sensitive + crosscheck commerce backstop green')})()"</automated>
  </verify>
  <acceptance_criteria>
    - linkedin.com is in sensitiveOrigins; the bare youtube.com (apex + www) is in deniedOrigins (not relying on the pre-existing music.youtube.com)
    - the net-new commerce brands (homedepot/priceline/redfin/starbucks/pandaexpress) + every further commerce brand walked from the vendored urlPatterns are in sensitiveOrigins (no commerce brand left safe; no commerce brand downgraded)
    - scripts/verify-catalog-crosscheck.mjs carries COMMERCE_SENSITIVE_SERVICES + an exported checkCommerceSensitiveClassified, wired into runCli, and `node scripts/verify-catalog-crosscheck.mjs` PASSES (every roster brand classifies sensitive; roster disjoint from READ_ONLY_SAFE) -- and would FAIL if any roster brand were unclassified/downgraded
    - no commerce/payment brand was added to READ_ONLY_SAFE; zillow/grafana/calendly/yelp/tripadvisor/reddit unchanged
    - every pre-existing denylist entry is preserved; the _comment carries a Phase-39.5 audit paragraph
  </acceptance_criteria>
  <done>service-denylist.json classifies linkedin (sensitive) + youtube (denied) + the enumerated commerce roster (net-new homedepot/priceline/redfin/starbucks/pandaexpress + any walked) SENSITIVE, and verify-catalog-crosscheck.mjs carries the COMMERCE_SENSITIVE_SERVICES build-failing backstop (validate:extension fails on an omitted/downgraded commerce brand).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Full-corpus screening test (classifyGate over all 117-app origins = 0 failures + the commerce backstop green)</name>
  <files>tests/full-corpus-screen.test.js, package.json</files>
  <behavior>
    - Test 1: classifyGate over EVERY enumerated real-app origin (derived from the vendored set + the importer's readPluginMeta) yields 0 failures (the full origin set is classify-complete / fail-closed satisfied)
    - Test 2: classify(https://linkedin.com).sensitive === true AND classify(https://youtube.com).denied === true (the 2 surfaced origins governed)
    - Test 3: a representative commerce/payment/travel brand set (doordash/amazon/booking/walmart/shopify + the net-new homedepot/priceline/redfin/starbucks/pandaexpress) each classify sensitive (the conservative posture holds corpus-wide)
    - Test 4: checkCommerceSensitiveClassified(classify) over the FULL COMMERCE_SENSITIVE_SERVICES roster (imported from verify-catalog-crosscheck.mjs) returns 0 failures, and the roster is NON-EMPTY (>= the curated+net-new count) so the assertion cannot pass vacuously -- the build-failing backstop is GREEN against the committed denylist (and would fail if a roster brand were unclassified/downgraded, or if the roster overlapped READ_ONLY_SAFE)
    - Test 5: a representative genuinely-non-commerce read origin (e.g. datadog/airtable) is NOT forced sensitive (no over-restriction)
  </behavior>
  <read_first>
    - scripts/verify-classification-gate.mjs classifyGate (the dual-export the test drives over the full origin set)
    - scripts/verify-catalog-crosscheck.mjs COMMERCE_SENSITIVE_SERVICES + checkCommerceSensitiveClassified (the Task-1 backstop -- import + drive the SAME roster the build gate uses) + READ_ONLY_SAFE_SERVICES (to confirm disjointness)
    - scripts/import-opentabs-catalog.mjs enumerateBatchApps + readPluginMeta (to derive every real-app origin the SAME way the importer does)
    - extension/utils/service-denylist.js load()/classify() (load the REAL committed roster, NOT a stub -- the live-roster pattern)
    - tests/breadth-batch-gate.test.js (the live-roster classifyGate test convention to mirror) + tests/payment-op-guard.test.js (the dynamic import() of the .mjs dual-export from a CJS test)
  </read_first>
  <action>
    Write tests/full-corpus-screen.test.js as a zero-framework FSB node test (check(cond,msg) counter, PASS=/FAIL=, process.exit(failed>0?1:0), ASCII-only) that: loads the REAL committed service-denylist roster (await Denylist.load(), NOT a hand-stub); enumerates EVERY real-app origin by importing the importer's enumerateBatchApps + readPluginMeta and mapping each app -> `https://<service>` (the SAME origin the importer gates); drives the REAL classifyGate (dynamic import() of scripts/verify-classification-gate.mjs) over that full origin set and asserts failures.length === 0 (the full corpus is classify-complete -- fail-closed satisfied BEFORE the Plan-04 merge-time gate runs); asserts classify('https://linkedin.com').sensitive === true and classify('https://youtube.com').denied === true; asserts a representative commerce/payment/travel brand set INCLUDING the net-new homedepot/priceline/redfin/starbucks/pandaexpress each classify sensitive (the conservative posture); IMPORTS COMMERCE_SENSITIVE_SERVICES + checkCommerceSensitiveClassified from scripts/verify-catalog-crosscheck.mjs and asserts checkCommerceSensitiveClassified(Denylist.classify).failures.length === 0 over the FULL roster (the build-failing backstop proven green -- the SAME check validate:extension runs) AND asserts COMMERCE_SENSITIVE_SERVICES.size is non-trivial (>= the curated+net-new count) so the test cannot pass vacuously on an empty roster; and asserts a representative genuinely-non-commerce read origin (datadog/airtable) is NOT sensitive (no over-restriction). Register the test in package.json's `test` chain (append ` && node tests/full-corpus-screen.test.js`).
  </action>
  <verify>
    <automated>node tests/full-corpus-screen.test.js && grep -q "full-corpus-screen.test.js" package.json</automated>
  </verify>
  <acceptance_criteria>
    - classifyGate over the full enumerated 117-app origin set yields 0 failures (fail-closed satisfied corpus-wide)
    - linkedin classifies sensitive; youtube classifies denied
    - every commerce/payment brand in the representative set (incl the net-new 5) classifies sensitive; checkCommerceSensitiveClassified over the FULL COMMERCE_SENSITIVE_SERVICES roster returns 0 failures (the backstop is green) AND the roster is non-empty; a non-commerce read origin is not over-restricted
    - the test is registered in npm test
  </acceptance_criteria>
  <done>tests/full-corpus-screen.test.js proves the full ~114-origin corpus is classify-complete (0 classifyGate failures), linkedin/youtube governed, and the COMMERCE_SENSITIVE_SERVICES backstop green over the full enumerated commerce roster (conservatively sensitive, non-vacuous).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| origin classification -> the consent gate (Auto) | an unclassified sensitive origin runs writes under Auto with no friction (the "gap == allow" failure) |
| commerce/payment brand -> a future op-set change | a brand kept safe today becomes write-capable when a re-vendor adds a payment op -> money moves under Auto |
| a NET-NEW read-only commerce brand -> the existing gates | classifyGate (no payment token) + the payment-op guard (no payment verb) BOTH miss it -> it ships SAFE-under-Auto with no gate catching it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39.5-06 | Elevation of Privilege | an unclassified sensitive origin among the 114 ships writable-under-Auto | mitigate | classifyGate fail-closed runs merge-time per batch (Plan 04) AND the full-corpus screen test (Task 2) asserts 0 failures over EVERY enumerated origin first; linkedin/youtube (the 2 surfaced) classified (Task 1) |
| T-39.5-07 | Elevation of Privilege | a commerce/payment brand kept safe is later made write-capable by an op-set change | mitigate | the CONSERVATIVE reconciliation (Task 1) keeps every commerce/payment/travel brand SENSITIVE so any write is posture-B gated regardless of the current read-only op set; READ_ONLY_SAFE excludes commerce brands (asserted disjoint by the backstop) |
| T-39.5-07b | Elevation of Privilege | a NET-NEW READ-ONLY commerce brand (homedepot/priceline/redfin/starbucks/pandaexpress/...) is missed and ships SAFE-under-Auto -- classifyGate does NOT abort on it (no payment token) and the payment-op guard does NOT key on it (no payment verb), so neither existing gate catches it | mitigate | the ENUMERATED COMMERCE_SENSITIVE_SERVICES roster (Task 1: curated + the spike-named net-new + the vendored-urlPatterns walk) + the build-failing checkCommerceSensitiveClassified backstop in verify-catalog-crosscheck.mjs (run by validate:extension; the Task-2 test drives the SAME roster) -- an omitted/downgraded commerce brand FAILS THE BUILD; the conservative review is a CHECKED invariant, not manual. Residual: a commerce brand nobody enumerated from the vendored dirs (the walk in Task 1 step (3)(c) is what makes the roster complete) |
| T-39.5-08 | Spoofing | an over-broad denylist wildcard captures an unrelated origin | mitigate | the EXACT-host / apex-suffix '*.' discipline (Task 1) keeps each pattern no broader than the app's own origin (youtube prefers exact-host per the DENY-01 precedent) |
| T-39.5-SC | Tampering | npm/pip/cargo installs | accept | NO new packages this plan (denylist JSON + a CI-gate edit + a test only); no install task |
</threat_model>

<verification>
- `node scripts/verify-catalog-crosscheck.mjs` passes with the COMMERCE_SENSITIVE_SERVICES backstop (every enumerated commerce brand classifies sensitive; roster disjoint from READ_ONLY_SAFE)
- `node tests/full-corpus-screen.test.js` passes (classifyGate 0 failures corpus-wide; linkedin/youtube governed; commerce sensitive; checkCommerceSensitiveClassified 0 failures over the full non-empty roster)
- service-denylist.json preserves every prior entry + adds linkedin/youtube + the net-new commerce brands (homedepot/priceline/redfin/starbucks/pandaexpress + any walked)
- the conservative posture + the backstop are documented in the _comment audit trail
- the test is registered in npm test
</verification>

<success_criteria>
SC3's screening half is satisfied: all ~114 origins classify-complete (classifyGate fail-closed), linkedin/youtube classified, every commerce/payment brand kept SENSITIVE (conservative) AND that posture is now a CHECKED BUILD INVARIANT -- the COMMERCE_SENSITIVE_SERVICES backstop fails validate:extension on any omitted/downgraded commerce brand (the gap classifyGate + the payment-op guard cannot catch for a read-only commerce origin). The payment-op guard PASS at full scale is proven at Plan 04 (the import + crosscheck).
</success_criteria>

<output>
Create `.planning/phases/39.5-full-opentabs-source-import/39.5-03-SUMMARY.md` when done.
</output>
