---
phase: 42-discovery-seeding-tail-learn
plan: 03
type: execute
wave: 1
depends_on: [42-01]
files_modified:
  - extension/utils/capability-catalog.js
  - tests/seed-resolve-t2.test.js
autonomous: true
requirements: [DSEED-01]

must_haves:
  truths:
    - "resolve(slug, origin) returns tier:'T2' for a descriptor-only slug whose origin is seeded that would otherwise be T3-DOM"
    - "resolve(slug, origin) still returns tier:'T3' for an UNSEEDED origin's descriptor-only slug (no change)"
    - "The existing LEARN-04 learned-first check is UNCHANGED — a learned recipe still resolves T2 + outranks ahead of the seed branch"
    - "The seed branch is a resolve-time lookup: descriptors are NOT re-stamped and the INV-01 catalog/djb2 shape is untouched"
    - "The seed-resolve branch never fabricates a recipe (T2 carries NO recipe -> the router's RECIPE_LEARN_PENDING leg)"
  artifacts:
    - path: "extension/utils/capability-catalog.js"
      provides: "resolve() seed->T2 branch in the descriptor-only fallback"
      contains: "T2"
    - path: "tests/seed-resolve-t2.test.js"
      provides: "seeded->T2 / unseeded->T3 + learned-first-unchanged + no-recipe-fabrication proof"
      min_lines: 50
  key_links:
    - from: "extension/utils/capability-catalog.js resolve()"
      to: "the seeded-origin lookup (FsbNetworkCapture.getSeedForOrigin or a typeof-guarded seeds accessor)"
      via: "descriptor-only fallback: would-be-T3 + seeded -> T2"
      pattern: "getSeedForOrigin|getSeedsSync|seed"
---

<objective>
Make a seeded-origin descriptor that would resolve T3-DOM resolve T2 (learn-
pending) instead — the minimal `capability-catalog.js resolve()` addition that
upgrades the seeded tail onto the learn path. The hook is the descriptor-only
fallback (capability-catalog.js:352-359): when the fallback would return
`backing:'dom' -> T3` AND the origin is seeded, return T2 (NO recipe) so the
router takes its RECIPE_LEARN_PENDING leg; an unseeded origin stays T3. The
existing LEARN-04 learned-first check (a learned recipe already resolves T2 +
outranks) is UNCHANGED, so once a real captured+replayed call promotes a T2, it
outranks the descriptor on the next visit.

Purpose: This is SC2's resolve half — the seeded tail upgrades from T3-DOM to the
T2 learn-pending seam so the affordance (Plan 04) can surface "open the site to
learn it", and a promoted recipe (the live UAT) then wins.
Output: the resolve seed branch + its falsifiable test (seeded->T2, unseeded->T3,
learned-first untouched, no recipe fabricated).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-discovery-seeding-tail-learn/42-CONTEXT.md
@.planning/phases/42-discovery-seeding-tail-learn/42-01-SUMMARY.md

# The resolve() to extend -- LEARN-04 learned-first :316-332 (UNCHANGED) ; descriptor-only fallback :352-359 (the seed branch hooks HERE) ; _getDescriptor :75-86 ; _getLearned :103-112 :
@extension/utils/capability-catalog.js

# The test harness to mirror (seeded global.FsbLearnedRecipeStore + the real catalog; seeded/unseeded resolve assertions):
@tests/learned-t2-outranking.test.js

<interfaces>
<!-- resolve(slug, origin) -> { tier, handler|recipe, descriptor } | null  (the authoritative tier lookup). -->
<!-- Today's descriptor-only fallback (capability-catalog.js:352-359):
       if (!entry) {
         var desc = _getDescriptor(slug);
         if (desc) { return { tier: (desc.backing==='learn')?'T2':'T3', descriptor: desc }; }
         return null;
       }
-->
<!-- The seed branch: when the fallback would yield T3 (backing!=='learn') AND getSeedForOrigin(origin) is non-null,
     return { tier:'T2', descriptor: desc } -- NO recipe field (never fabricate a credentialed call). -->
<!-- The seeds accessor: FsbNetworkCapture.getSeedForOrigin(origin) (Plan 02) -> { hints, provenance } | null,
     consulted typeof-guarded so the catalog suite degrades to "unseeded" when capture is absent. -->
<!-- INV-01: the catalog IIFE/djb2 data shape is untouched -- the seed branch adds a BRANCH, not a descriptor re-stamp. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the seed->T2 branch to resolve()'s descriptor-only fallback</name>
  <files>extension/utils/capability-catalog.js</files>
  <behavior>
    - resolve('learned.unseededhost.get.X', 'https://unseeded.example.com') for a descriptor with backing!=='learn' returns tier:'T3' (unchanged).
    - resolve(sameKindSlug, 'https://dashboard.stripe.com') for a backing!=='learn' descriptor whose origin IS seeded returns tier:'T2' with the descriptor attached and NO recipe field.
    - The LEARN-04 path is UNCHANGED: when a learned recipe exists for (slug, origin), resolve returns { tier:'T2', recipe, descriptor } from the learned-first check BEFORE the seed branch is ever reached (the seed branch only runs in the no-entry, no-learned descriptor-only fallback).
    - A descriptor with backing:'learn' still returns T2 via the existing leg (the seed branch does not double-handle it).
    - The seed branch never sets a `recipe` field (no fabricated credentialed call).
  </behavior>
  <action>
In `capability-catalog.js resolve()`, modify ONLY the descriptor-only fallback (capability-catalog.js:352-359).
Keep the existing `backing:'learn' -> T2` leg. For the `else -> T3` case, BEFORE returning T3, consult a
typeof-guarded seeds lookup: read `FsbNetworkCapture.getSeedForOrigin(origin)` (or, if you prefer to avoid a hard
capture dependency in the catalog, a thin typeof-guarded `_seedForOrigin(origin)` helper that reads the same
loaded seeds via `globalThis.FsbNetworkCapture`). If the origin is seeded (non-null), return
`{ tier: 'T2', descriptor: desc }` (NO `recipe` field — never fabricate a credentialed call, per CONTEXT). If not
seeded, return the existing `{ tier: 'T3', descriptor: desc }`. The literals 'T2'/'T3' are EXACT (any other string
hits the router default -> RECIPE_NOT_FOUND). Do NOT touch the LEARN-04 learned-first check (:316-332), do NOT
re-stamp descriptors, and do NOT change the REGISTRY/djb2 data shape (INV-01: a branch, not a data-shape change).
Add a `_seedForOrigin` accessor next to `_getLearned`/`_getDescriptor` (typeof-guarded, returns null when capture
or seeds are absent) so the catalog stays loadable standalone in the Node suite.
  </action>
  <verify>
    <automated>node tests/seed-resolve-t2.test.js && node tests/learned-t2-outranking.test.js</automated>
  </verify>
  <done>resolve() returns T2 for a seeded would-be-T3 descriptor and T3 for an unseeded one; the LEARN-04 learned-first path is unchanged (learned-t2-outranking GREEN); no recipe is fabricated; the catalog data shape is untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: seed-resolve-t2 test (seeded->T2, unseeded->T3, learned-first untouched, no fabrication)</name>
  <files>tests/seed-resolve-t2.test.js</files>
  <behavior>
    - Drives the REAL capability-catalog with a seeded global.FsbNetworkCapture.getSeedForOrigin stub (seeded set = {'https://dashboard.stripe.com'}) and a FsbRecipeIndex stub carrying descriptor-only slugs (backing absent / 'dom').
    - Asserts: a descriptor-only slug on the seeded origin resolves tier:'T2' with NO recipe field; the SAME slug on an unseeded origin resolves tier:'T3'.
    - Asserts: with a seeded global.FsbLearnedRecipeStore.getLearnedSync returning a recipe for (slug, seededOrigin), resolve returns { tier:'T2', recipe } from the learned-first path (the seed branch did not shadow it) — and the recipe is the learned one (outranks).
    - Asserts: a genuinely-unknown slug (not in descriptors, not in REGISTRY) still resolves null (RECIPE_NOT_FOUND) even on a seeded origin (the seed branch only fires for a present descriptor).
    - Zero-framework (passed/failed + check + process.exit), mirroring learned-t2-outranking.test.js.
  </behavior>
  <action>
Create `tests/seed-resolve-t2.test.js` mirroring `tests/learned-t2-outranking.test.js`'s catalog-driving idiom:
load the real `capability-catalog.js` with stubbed `globalThis.FsbRecipeIndex` (descriptors array including a
backing:'dom' / backing-absent slug), `globalThis.FsbNetworkCapture` (a `getSeedForOrigin(origin)` stub that
returns `{hints:[...]}` for the seeded origin and null otherwise), and optionally `globalThis.FsbLearnedRecipeStore`
(a `getLearnedSync` stub for the learned-first assertion). Assert the five behaviors above. Make the seeded set and
the descriptor slugs distinctive so a passing test is non-vacuous (the unseeded->T3 case proves the branch is
gated on the seed, not always-T2). This file is registered in package.json by Plan 01 Task 4.
  </action>
  <verify>
    <automated>node tests/seed-resolve-t2.test.js</automated>
  </verify>
  <done>tests/seed-resolve-t2.test.js is GREEN: seeded->T2 (no recipe), unseeded->T3, learned-first still wins with its recipe, unknown slug still null. EXIT 0.</done>
</task>

</tasks>

<verification>
- `node tests/seed-resolve-t2.test.js` GREEN (seeded->T2 / unseeded->T3 / learned-first untouched / no fabrication).
- `node tests/learned-t2-outranking.test.js` GREEN (LEARN-04 path unchanged).
- `node tests/no-dead-entry.test.js` and `node tests/catalog-inline-shape.test.js` GREEN (INV-01 catalog shape intact, no dead entry regressed).
</verification>

<success_criteria>
- A seeded origin's would-be-T3 descriptor resolves T2 (learn-pending) with no recipe; an unseeded origin stays T3.
- The LEARN-04 learned-first check is byte-unchanged and still outranks via resolve order.
- The seed branch is a resolve-time lookup only — descriptors and the catalog/djb2 data shape (INV-01) are untouched.
</success_criteria>

<output>
Create `.planning/phases/42-discovery-seeding-tail-learn/42-03-SUMMARY.md` when done — note the exact resolve()
branch location, the _seedForOrigin accessor, and confirm no recipe is fabricated (so Plan 04's affordance assumes
the T2-no-recipe leg).
</output>
