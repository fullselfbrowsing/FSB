# Phase 42: Discovery Seeding + Tail Learn (DSEED-01/02) — Context

**Gathered:** 2026-06-26
**Status:** Ready for planning
**Mode:** Orchestrator-gathered (the Phase-31 capture→learn→promote machinery + the structural redactor are the shipped substrate, reused UNCHANGED except for reading hints; the conservative consent-gated/structure-only posture is locked). No separate user discuss.

<domain>
## Phase Boundary

Make the non-hand-ported TAIL invocable predictably: seed all OpenTabs origins (+ endpoint hints
harvested from the vendored `*-api.ts`) into the EXISTING Phase-31 network-capture discovery so the tail
LEARNS each origin on the first authenticated visit (consent-gated, promote-after-replay), and EXTEND +
VERIFY the capture-time structural redactor against the full 119-app field universe so NO auth substring
leaks at scale. OWNS **DSEED-01** (seeding the tail) + **DSEED-02** (the redactor no-leak at scale).

**In scope:**
1. **`discovery-seeds.json`** (NEW, `extension/config/`) generated by a build-time harvester from the
   vendored `vendor/opentabs-snapshot/plugins/<app>/src/*-api.ts` (origin → endpoint hints); a loader
   in `network-capture.js` reads it — **NO manifest/permission change** (the discovery path already
   owns its debugger-attach capture permission). A hint only BIASES recognition (the synthesizer's
   confidence), it NEVER executes — execution still requires a real captured + replayed call.
2. **Seed → T2 resolution + ranking:** a descriptor whose origin has a seed resolves **T2 (learn-
   pending)** instead of T3 (DOM); the existing LEARN-04 path makes a learned T2 recipe OUTRANK the
   descriptor-T3 on the next visit (promote-after-replay, consent-gated). Unseeded stays T3-DOM.
3. **`RECIPE_LEARN_PENDING` actionable affordance:** the router T2-no-learned-recipe branch surfaces an
   actionable message ("open the site while logged in to learn it"), not a silent no-op. INV-03 dual-
   field `code===errorCode===error` byte-stable (the extra fields are ADDITIVE, the code unchanged).
4. **The structural redactor 119-app no-leak extension (DSEED-02, the security headline):** extend the
   `AUTH_CARRIER_DENYLIST` to the full 119-app auth-carrier field universe (stripe `session_api_key`,
   instagram `csrftoken`, linear `linear-client-id`, token-shape patterns, …) + EXTEND the no-leak test
   to assert NO auth substring persists into ANY of the 3 sinks (learned-recipe envelope, audit-log
   ring, diagnostic ring) at scale. The structure-only invariant is PRESERVED (capture reads field
   NAMES/structure, never a header value / body / query value).

**Out of scope:**
- The live first-authenticated-visit capture on a REAL logged-in tab → carried-forward `human_needed`
  (the seed→T2 resolve + promote-after-replay + the redactor are ALL fixture-testable headless).
- The authoritative full-corpus scale/test gate + self-heal hardening → Phase 43.
- NO change to the capture→learn→promote CORE (synthesize/interpret/execute/promote), the consent gate,
  or the resolve tier machinery beyond the minimal seed→T2 branch + the affordance fields.
</domain>

<decisions>
## Implementation Decisions (reuse the Phase-31 substrate; conservative structure-only/consent-gated posture)

### discovery-seeds.json + the endpoint-hint harvester (DSEED-01)
- **NEW `scripts/harvest-discovery-seeds.mjs`** (build-time, alongside the importer): reads each vendored
  `plugins/<app>/src/*-api.ts`, extracts the origin + the endpoint hints (op→path/method, base-URL) the
  importer/CORS-gate already parse, and emits `extension/config/discovery-seeds.json`
  `{ "<origin>": { hints: [{ op, method, path }], … } }`. Provenance-stamped, pinned to the SHA.
- **Loader in `network-capture.js`** mirrors the `service-denylist.js` loader (lazy `chrome.runtime
  .getURL('config/discovery-seeds.json')`, fetch-at-startup, no-throw → empty seeds on error). **NO new
  host permission / manifest change** — the seeds feed the synthesizer's recognition, not a fetch right.
- **Hint = recognition bias ONLY (SC1):** a hint raises the synthesizer's confidence when a CAPTURED
  call matches a seeded path; it NEVER triggers a fetch. A seeded origin becomes invocable ONLY after a
  real captured + replayed call promotes a T2 recipe (the existing `discovery-session.js` path,
  consent-gated, fail→DISCARD). The seed is metadata, not an executor.

### Seed → T2 resolution (DSEED-01, SC2) — minimal resolve() addition
- A descriptor whose origin is in `discovery-seeds.json` resolves **T2 (learn-pending)** rather than T3
  (DOM): a MINIMAL `capability-catalog.js resolve()` addition — when the descriptor-only fallback would
  return T3 (`backing:'dom'`, no learned recipe) AND the origin is seeded, return T2 instead. The
  existing LEARN-04 check (`getLearnedSync` FIRST → learned T2 outranks) is UNCHANGED, so once learned,
  the T2 recipe outranks the descriptor on the next visit. Unseeded origins stay T3-DOM. (Claude's
  discretion: seed-origin lookup in resolve vs a `backing:'learn'` re-stamp of seeded descriptors at
  import — default to the resolve-time seed lookup so the descriptors + INV-01 catalog shape are
  untouched.)

### RECIPE_LEARN_PENDING affordance (SC2)
- Extend the router T2-no-learned-recipe branch (`capability-router.js:767`):
  `_err('RECIPE_LEARN_PENDING', { slug, reason:'not-yet-learned', actionable:true, message:'Open
  <origin> while signed in so FSB can learn this capability from your own traffic.' })`. The `code`/
  `errorCode`/`error` stay the byte-stable `RECIPE_LEARN_PENDING` (INV-03); the message/actionable are
  ADDITIVE extra fields the MCP/autopilot surface can render. A test asserts the actionable fields +
  the unchanged dual-field code.

### The structural redactor 119-app no-leak extension (DSEED-02 — the security headline)
- AUDIT all 129 vendored `*-api.ts` for auth-carrier header NAMES + token-shape field patterns not yet
  covered by `network-capture-redactor.js:62 AUTH_CARRIER_DENYLIST`; EXTEND the denylist (+ a token-
  shape pattern set) to cover the full universe (the SC names stripe `session_api_key`, instagram
  `csrftoken`, linear `linear-client-id`). The **structure-only invariant is INVIOLATE**: the redactor
  reads field NAMES/structure and NEVER a header value / body / query value (the `for..in` reads keys;
  `redactRequest`→`{method,path,origin,headerNames}`; `redactResponse`→`{status,mimeType}`; no
  body/postData by construction).
- EXTEND the no-leak proof across ALL 3 sinks at scale: `tests/network-capture-redaction.test.js` (the
  master 7-substring grep → the 119-app field universe), `tests/audit-log-no-secret.test.js` (the ring
  whitelist), + the diagnostic-ring sink (`diagnostics-ring-buffer.js`, error.message via
  `redactForLog`). A captured hostile request carrying every app's auth carrier + a token-shaped value
  must leave ZERO auth substring in the learned-recipe envelope, the audit ring, or the diagnostic ring.

### Invariants
- **NO manifest/permission change** (SC1). The capture→learn→promote core, the consent gate
  (`_runGate`, denied/off/sensitive-unconfirmed → `RECIPE_CONSENT_*`), and the origin-pin are UNCHANGED.
- INV-03 (`RECIPE_LEARN_PENDING` byte-stable). INV-01 (catalog IIFE/djb2 shape; the resolve seed-lookup
  adds a branch, not a data-shape change). Wall 1/2. The learned-store per-origin cap + LRU + quarantine
  (Phase-32 heal) UNCHANGED.

### Claude's Discretion
- The exact harvester extraction (reuse the importer's `*-api.ts` parse) + the discovery-seeds.json shape.
- resolve-time seed lookup vs `backing:'learn'` re-stamp (default: resolve-time lookup).
- The exact affordance message wording.
- Which token-shape patterns to add (keep conservative — name-based denylist is the floor; token-shape
  is defense-in-depth for path/structure substrings).
</decisions>

<code_context>
## Existing Code Insights (reuse the Phase-31 substrate unchanged)
- **Capture + consent gate:** `extension/utils/network-capture.js` — `_runGate` (:221-262, the
  consent boundary, denied/off/sensitive → RECIPE_CONSENT_*), `startSession` (:265-360, reuses the
  Input attachment, no manifest change), `_onCdpEvent` (:174-216, XHR/Fetch + same-origin + redact-at-
  capture), `endSession` (:376-433). Bounds DEFAULT_MAX_MS=30000/COUNT=25.
- **The structural redactor:** `extension/utils/network-capture-redactor.js` — `redactRequest`
  (:99-121, structure-only: method/path/origin/headerNames; the `for..in` reads keys, never values),
  `AUTH_CARRIER_DENYLIST` (:62 — EXTEND here), `redactResponse` (:127-133, status+mimeType only).
- **Seed → T2 resolve:** `extension/utils/capability-catalog.js resolve()` (:316-332 LEARN-04
  getLearnedSync-first; :352-359 descriptor-only fallback backing:'learn'→T2 / 'dom'→T3 — the seed
  branch hooks HERE). `extension/utils/learned-recipe-store.js` (getLearnedSync :311-330, hydrateSyncCache,
  promote, PER_ORIGIN_CAP=24, quarantine).
- **T2 / affordance:** `capability-router.js` T2 branch (:756-768; `_err('RECIPE_LEARN_PENDING',{slug})`
  at :767 — EXTEND with the actionable fields), `_err` (:61-69, dual-field).
- **Promote-after-replay:** `extension/utils/discovery-session.js` (:128-262, synthesize→interpret→
  execute→promote, fail→DISCARD, returns slugs-only). `recipe-synthesizer.js` (the hint-bias consumer).
- **Hint source:** `vendor/opentabs-snapshot/plugins/<app>/src/*-api.ts` (129 apps; op→path extractable
  — the importer/CORS-gate already parse the base-URL). **Seed loader model:** `service-denylist.js`
  (:189-226, lazy fetch + no-throw).
- **The 3 leak-free sinks:** learned-recipe envelope (`learned-recipe-store.js`), audit ring
  (`audit-log.js`), diagnostic ring (`diagnostics-ring-buffer.js`). **Tests to mirror:**
  `tests/network-capture-redaction.test.js` (master 7-substring no-leak), `tests/audit-log-no-secret.test.js`,
  `tests/learned-promote-after-replay.test.js` (CASE A/B/C), `tests/learned-t2-outranking.test.js`.

## Integration Points
- harvester → discovery-seeds.json → network-capture loader (recognition bias) ; seeded origin →
  resolve() T2 (learn-pending) → promote-after-replay learns a T2 → outranks T3 next visit ; redactor
  (structure-only, extended denylist) → all 3 sinks leak-free at 119-app scale.
</code_context>

<specifics>
## Specific Ideas
- THE security headline (DSEED-02): the redactor is STRUCTURE-ONLY by construction (reads names, never
  values) — the 119-app extension widens the name/token-shape denylist + proves ZERO auth substring
  across all 3 sinks at scale. Fail-safe: a value is never read, so it can never leak.
- THE seeding contract (DSEED-01): a hint is recognition BIAS, never an executor — a seeded origin is
  invocable ONLY after a real captured + replayed call promotes a T2 (consent-gated, fail→DISCARD).
- THE affordance (SC2): RECIPE_LEARN_PENDING becomes an actionable "open the site to learn it" prompt,
  not a silent no-op — the seeded tail surfaces a path to invocability from the user's own traffic.
</specifics>

<deferred>
## Deferred Ideas
- The live first-authenticated-visit capture on a real logged-in tab (per seeded origin) → carried-
  forward `human_needed` (the seed→T2 + promote + redactor are fixture-tested headless).
- Recipe-rot self-heal hardening for the 119-app surface + the authoritative full-corpus scale/test
  gate → Phase 43 (SCALE-01/02).
</deferred>
