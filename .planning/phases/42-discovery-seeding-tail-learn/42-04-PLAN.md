---
phase: 42-discovery-seeding-tail-learn
plan: 04
type: execute
wave: 1
depends_on: [42-01]
files_modified:
  - extension/utils/capability-router.js
  - tests/recipe-learn-pending-affordance.test.js
autonomous: true
requirements: [DSEED-01]

must_haves:
  truths:
    - "The router T2-no-learned-recipe branch returns RECIPE_LEARN_PENDING with additive { reason:'not-yet-learned', actionable:true, message } fields"
    - "The message names the origin: 'Open <origin> while signed in so FSB can learn this capability from your own traffic.'"
    - "INV-03 is preserved: code === errorCode === error === 'RECIPE_LEARN_PENDING' byte-stable"
    - "The change is ADDITIVE — the existing T2-with-recipe dispatch leg and the byte-stable code are unchanged"
  artifacts:
    - path: "extension/utils/capability-router.js"
      provides: "RECIPE_LEARN_PENDING actionable affordance (additive fields, byte-stable code)"
      contains: "actionable"
    - path: "tests/recipe-learn-pending-affordance.test.js"
      provides: "actionable-fields + INV-03 dual/triple-field byte-stable proof"
      min_lines: 40
  key_links:
    - from: "extension/utils/capability-router.js T2 branch"
      to: "_err('RECIPE_LEARN_PENDING', { slug, reason, actionable, message })"
      via: "additive extra fields, code unchanged"
      pattern: "RECIPE_LEARN_PENDING"
---

<objective>
Turn the silent `RECIPE_LEARN_PENDING` no-op into an actionable affordance: extend
the router T2-no-learned-recipe branch (capability-router.js:767) with ADDITIVE
`{ reason:'not-yet-learned', actionable:true, message:'Open <origin> while signed
in so FSB can learn this capability from your own traffic.' }` while keeping the
dual/triple-field `code === errorCode === error === 'RECIPE_LEARN_PENDING'`
byte-stable (INV-03). The MCP/autopilot surface can then render a path to
invocability — "open the site to learn it from your own traffic" — instead of a
dead end.

Purpose: SC2's affordance half. A seeded-tail slug resolves T2-no-recipe (Plan 03)
and this is the message the user sees, pointing them at the consent-gated live
capture that promotes the real recipe.
Output: the additive affordance fields + a test asserting both the new fields and
the unchanged byte-stable code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-discovery-seeding-tail-learn/42-CONTEXT.md
@.planning/phases/42-discovery-seeding-tail-learn/42-01-SUMMARY.md

# The router T2 branch :756-768 (the _err('RECIPE_LEARN_PENDING',{slug}) at :767 to EXTEND) ; _err dual-field :61-69 :
@extension/utils/capability-router.js

<interfaces>
<!-- _err(code, extra) -> { success:false, code, errorCode, error, ...extra }  (dual/triple-field; INV-03 load-bearing). -->
<!-- Today (capability-router.js:765-767):
       out = entry.recipe
         ? await _runDeclarativeTier(slug, args, c, 'T2', entry.recipe, { trustedProvenance: 'local' })
         : _err('RECIPE_LEARN_PENDING', { slug: slug });
-->
<!-- Target: the no-recipe leg becomes
       _err('RECIPE_LEARN_PENDING', { slug: slug, reason: 'not-yet-learned', actionable: true,
             message: 'Open ' + <origin> + ' while signed in so FSB can learn this capability from your own traffic.' })
     where <origin> is derived from the resolved descriptor/origin available in this branch (entry.descriptor.origin
     or the call origin `c`); if no origin is resolvable, omit the host from the message gracefully (still actionable).
-->
<!-- INV-03: code === errorCode === error === 'RECIPE_LEARN_PENDING' must stay byte-stable; the new fields are ADDITIVE. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the RECIPE_LEARN_PENDING branch with additive actionable fields</name>
  <files>extension/utils/capability-router.js</files>
  <behavior>
    - The T2 branch with NO learned recipe returns an object with code==='RECIPE_LEARN_PENDING', errorCode==='RECIPE_LEARN_PENDING', error==='RECIPE_LEARN_PENDING' (byte-stable, INV-03), plus reason==='not-yet-learned', actionable===true, and a message string.
    - The message names the origin when one is resolvable in the branch: "Open <origin> while signed in so FSB can learn this capability from your own traffic." When no origin is resolvable, the message is still present + actionable, gracefully omitting the host.
    - The T2-WITH-recipe leg (the declarative dispatch threading trustedProvenance:'local') is UNCHANGED.
    - The existing slug field is still present.
  </behavior>
  <action>
Edit `capability-router.js:767` — extend the no-recipe leg's `_err('RECIPE_LEARN_PENDING', { slug: slug })` to
`_err('RECIPE_LEARN_PENDING', { slug: slug, reason: 'not-yet-learned', actionable: true, message: <msg> })` where
`<msg>` = `'Open ' + origin + ' while signed in so FSB can learn this capability from your own traffic.'` and
`origin` is derived from what the branch already has — prefer `entry.descriptor && entry.descriptor.origin`, fall
back to the active call origin `c` (the same origin variable the surrounding switch uses). If neither yields an
origin, build the message WITHOUT the host (e.g. 'Open the site while signed in so FSB can learn this capability
from your own traffic.') so the field is always present + actionable. Do NOT change the first argument to `_err`
(the code stays the literal 'RECIPE_LEARN_PENDING' — INV-03). Do NOT touch the T2-with-recipe dispatch leg, the
consent gate, or any other branch. The new fields are ADDITIVE keys via `_err`'s extra-merge (capability-router.js:
61-69).
  </action>
  <verify>
    <automated>node tests/recipe-learn-pending-affordance.test.js && node tests/capability-router.test.js</automated>
  </verify>
  <done>The no-recipe T2 branch returns RECIPE_LEARN_PENDING with reason/actionable/message additive fields and a byte-stable code===errorCode===error; the with-recipe leg and the router suite are unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: recipe-learn-pending-affordance test (additive fields + INV-03 byte-stable)</name>
  <files>tests/recipe-learn-pending-affordance.test.js</files>
  <behavior>
    - Drives the router (the router-test injection idiom: stubbed global.FsbCapabilityCatalog resolving a T2 entry with NO recipe + a descriptor carrying a known origin, stubbed FsbConsentGate allowing) through invoke for a T2-no-learned slug.
    - Asserts code==='RECIPE_LEARN_PENDING' && errorCode==='RECIPE_LEARN_PENDING' && error==='RECIPE_LEARN_PENDING' (INV-03 triple-field byte-stable).
    - Asserts reason==='not-yet-learned', actionable===true, typeof message==='string', and message.indexOf(origin) !== -1 (the origin is named) and message contains 'learn'.
    - Asserts the with-recipe path is untouched: a T2 entry WITH a recipe dispatches the declarative tier (no RECIPE_LEARN_PENDING) — reuse the learned-t2-outranking router-half idiom for this control.
    - Zero-framework (passed/failed + check + process.exit).
  </behavior>
  <action>
Create `tests/recipe-learn-pending-affordance.test.js` mirroring the router-driving idiom in
`tests/learned-t2-outranking.test.js` (stubbed `globalThis.FsbCapabilityCatalog` / `FsbConsentGate` /
`FsbCapabilityInterpreter` / `FsbCapabilityFetch`). Drive a T2 entry with NO recipe and a descriptor whose origin is
a known sentinel (e.g. 'https://dashboard.stripe.com'); invoke and assert the additive fields + the INV-03 triple
byte-equality above, including `message.indexOf('https://dashboard.stripe.com') !== -1`. Add the with-recipe control
(a T2 entry WITH a recipe dispatches declaratively, returning no RECIPE_LEARN_PENDING) so the test proves the change
is scoped to the no-recipe leg. This file is registered in package.json by Plan 01 Task 4.
  </action>
  <verify>
    <automated>node tests/recipe-learn-pending-affordance.test.js</automated>
  </verify>
  <done>tests/recipe-learn-pending-affordance.test.js is GREEN: the no-recipe leg carries reason/actionable/message (origin named) with a byte-stable code===errorCode===error, and the with-recipe control still dispatches. EXIT 0.</done>
</task>

</tasks>

<verification>
- `node tests/recipe-learn-pending-affordance.test.js` GREEN (additive fields + INV-03 byte-stable + with-recipe control).
- `node tests/capability-router.test.js` GREEN (router suite unchanged).
- `node tests/learned-t2-outranking.test.js` GREEN (the T2-with-recipe dispatch leg untouched).
</verification>

<success_criteria>
- RECIPE_LEARN_PENDING surfaces an actionable "open <origin> while signed in to learn it" message, not a silent no-op.
- INV-03 holds: code === errorCode === error === 'RECIPE_LEARN_PENDING' byte-stable; the new fields are purely additive.
- The T2-with-recipe dispatch leg and the consent gate are unchanged.
</success_criteria>

<output>
Create `.planning/phases/42-discovery-seeding-tail-learn/42-04-SUMMARY.md` when done — record the exact message
wording and confirm the INV-03 byte-stable triple (so Plan 05's INV-03 battery references it).
</output>
