---
phase: 31-network-capture-discovery-recipe-synthesis-learned-recipes
plan: 04
type: execute
wave: 3
depends_on: ["31-01"]
files_modified:
  - extension/utils/capability-signature.js
  - extension/utils/capability-interpreter.js
autonomous: true
requirements: [LEARN-01]
user_setup: []

must_haves:
  truths:
    - "A 'local'-provenance recipe is EXEMPT from the Ed25519 signature verify (parallel to 'bundled'), so a locally-synthesized recipe binds without a verify call (D-09)"
    - "The exemption is honored ONLY when 'local' is passed via the loader's trustedProvenance argument — a recipe payload self-declaring provenance:'local' is IGNORED and STILL verified (HI-01)"
    - "The exempt short-circuit fires BEFORE any verifyEd25519 call so the exemption is observable as a zero-call (the bundled-exemption test template)"
    - "The bare-core / 'bundled' exempt paths stay byte-identical — the Phase-29 head and existing callers are unaffected"
  artifacts:
    - path: "extension/utils/capability-signature.js"
      provides: "verifyRecipeEnvelope recognizes 'local' alongside 'bundled' as trusted-exempt"
      contains: "'local'"
    - path: "extension/utils/capability-interpreter.js"
      provides: "interpretRecipe exempt short-circuit fires for trustedProvenance === 'local'"
      contains: "'local'"
  key_links:
    - from: "extension/utils/capability-interpreter.js interpretRecipe"
      to: "the exempt short-circuit at :361"
      via: "trustedProvenance === 'bundled' || === 'local' -> bindRecipeCore (no verify)"
      pattern: "trustedProvenance === 'local'"
    - from: "extension/utils/capability-signature.js verifyRecipeEnvelope"
      to: "the resolvedProvenance short-circuit at :251"
      via: "resolvedProvenance === 'bundled' || === 'local' -> { ok:true }"
      pattern: "=== 'local'"
---

<objective>
Add `'local'` as a third trusted-exempt provenance value alongside `'bundled'` in BOTH the signature module (`verifyRecipeEnvelope`) and the interpreter's exempt short-circuit (`interpretRecipe`), so a locally-synthesized learned recipe is exempt from the Phase-30 Ed25519 verify when the LOADER vouches it via `trustedProvenance: 'local'`. Turn the Wave-0 RED suite `learned-local-provenance-exempt.test.js` GREEN.

Purpose: Realize D-09 (learned recipes are trusted-by-local-synthesis — built on THIS device from observed first-party traffic, no remote-delivery tampering vector) WITHOUT weakening HI-01 (provenance is the loader's vouch only; a recipe payload can NEVER self-declare `'local'` in its data to dodge verify). This is a minimal, security-critical 2-line change in each of two files — isolated into its own plan because it touches the signature/verify trust boundary and must be proven by the dedicated zero-call + masquerade test.
Output: 2 additive edits (no new files) wiring one Wave-0 suite GREEN; the existing bundled-exemption and signature suites stay GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-network-capture-discovery-recipe-synthesis-learned-recipes/31-CONTEXT.md
@.planning/phases/31-network-capture-discovery-recipe-synthesis-learned-recipes/31-RESEARCH.md

<interfaces>
<!-- The EXACT current code at the two seams (extracted). The change is additive: add 'local' to the existing 'bundled' check. -->

capability-signature.js verifyRecipeEnvelope (currently :251):
  // resolvedProvenance is resolved from the TRUSTED channel (arguments.length>=2 ? trustedProvenance : envelope.provenance)
  if (resolvedProvenance === 'bundled') {
    return { ok: true };
  }
  // -> becomes: if (resolvedProvenance === 'bundled' || resolvedProvenance === 'local') { return { ok: true }; }

capability-interpreter.js interpretRecipe (currently :361):
  // trustedProvenance is resolved from opts ONLY (opts && typeof opts.trustedProvenance === 'string' ? ... : null) — NEVER the payload
  if (!envelope || trustedProvenance === 'bundled') {
    return bindRecipeCore(recipe, args, authMod);
  }
  // -> becomes: if (!envelope || trustedProvenance === 'bundled' || trustedProvenance === 'local') { return bindRecipeCore(...); }

HI-01 invariant (already enforced — do NOT regress): the interpreter reads trustedProvenance from opts ONLY (interpreter :329-330) and the signature module resolves it from the second ARG when supplied (signature :242-244); the payload's own `provenance` field is consulted ONLY as a legacy fallback when the loader passed NOTHING. Threading `{trustedProvenance:'local'}` from the trusted caller preserves this — a payload self-asserting 'local' with no vouch still gets verified.
</interfaces>

<read_first>
- extension/utils/capability-signature.js:233-311 (verifyRecipeEnvelope — the resolvedProvenance resolution :242-244 + the bundled short-circuit :251 to extend)
- extension/utils/capability-interpreter.js:310-400 (interpretRecipe — the trustedProvenance resolution from opts :329-330 + the exempt short-circuit :361 to extend + the async verify branch :370 that 'local' must short-circuit BEFORE)
- tests/recipe-signature-interpreter-hook.test.js:90-130 (the existing bundled-exemption zero-call test + the payload-masquerade test — the exact template the 'local' suite parallels)
- tests/learned-local-provenance-exempt.test.js (the Wave-0 RED contract: zero verifyEd25519 call on the 'local' path + the HI-01 payload-cannot-self-declare case)
- 31-RESEARCH.md Pattern 5 (the 'local' exemption — the two 2-line edits + the HI-01 preservation note)
</read_first>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Recognize 'local' as a trusted-exempt provenance in capability-signature.js and capability-interpreter.js</name>
  <files>extension/utils/capability-signature.js, extension/utils/capability-interpreter.js</files>
  <behavior>
    - interpretRecipe(localEnvelope, {}, {trustedProvenance:'local'}) binds (success:true) WITHOUT calling verifyEd25519 (zero-call spy)
    - verifyRecipeEnvelope(envelope, 'local') returns {ok:true} without a verify call
    - interpretRecipe(payloadWithProvenanceLocalInData, {}) with NO opts.trustedProvenance STILL verifies (a tampered/unsigned core -> RECIPE_SIGNATURE_INVALID) — the payload cannot self-declare 'local' (HI-01)
    - the existing 'bundled' exemption + bare-core path remain byte-identical (Phase-29 head unaffected)
  </behavior>
  <read_first>
    - extension/utils/capability-signature.js:246-253 (the comment + the bundled short-circuit to extend; keep the comment accurate — note 'local' joins 'bundled' as loader-vouched-trusted)
    - extension/utils/capability-interpreter.js:352-363 (the exempt-path comment + the short-circuit to extend)
    - tests/learned-local-provenance-exempt.test.js + tests/recipe-signature-interpreter-hook.test.js (the contracts — run BOTH; the existing hook suite must stay GREEN)
  </read_first>
  <action>
    In `extension/utils/capability-signature.js` `verifyRecipeEnvelope`, extend the bundled short-circuit (currently `if (resolvedProvenance === 'bundled') { return { ok: true }; }`) to ALSO return `{ ok: true }` when `resolvedProvenance === 'local'`. Update the adjacent comment to state that `'local'` (locally-synthesized, Phase 31) joins `'bundled'` as a trusted-by-provenance value — and reiterate that this fires ONLY for a TRUSTED provenance resolved from the second arg (the loader's vouch), never a payload-asserted one. Do NOT change the resolution logic at :242-244 (the `arguments.length >= 2 ? trustedProvenance : envelope.provenance` is the HI-01 boundary — leave it intact so a self-asserted payload 'local' with no vouch falls through to verify).

    In `extension/utils/capability-interpreter.js` `interpretRecipe`, extend the exempt short-circuit (currently `if (!envelope || trustedProvenance === 'bundled') { return bindRecipeCore(recipe, args, authMod); }`) to ALSO take the exempt synchronous bind when `trustedProvenance === 'local'`. This MUST sit BEFORE the async verify branch (:370) so the exemption is observable as a zero verify call. Update the adjacent comment (:352-360) to add `'local'` as the second loader-vouched exempt value and reaffirm that the payload's own `provenance` is NEVER consulted here (the `trustedProvenance` is resolved from `opts` only at :329-330 — do NOT change that). Do NOT alter the bare-core (`!envelope`) path or the bundled path semantics — they stay byte-identical for the Phase-29 head and existing callers.

    Keep both files dynamic-code-free (they are on RECIPE_PATH_ALLOWLIST). NO EMOJIS. The change is exactly the two boolean-OR additions plus comment updates — no new functions, no signature changes.
  </action>
  <acceptance_criteria>
    - `node tests/learned-local-provenance-exempt.test.js` exits 0 (GREEN): the 'local' path binds with zero verifyEd25519 calls; the payload-self-declare-'local'-without-vouch case is STILL verified (tampered core rejected).
    - `node tests/recipe-signature-interpreter-hook.test.js` exits 0 (GREEN — the existing bundled-exemption + masquerade suite is unregressed).
    - `node tests/capability-interpreter.test.js` exits 0 (the no-network charter + bind behavior unchanged).
    - `grep -c "=== 'local'" extension/utils/capability-signature.js extension/utils/capability-interpreter.js` is >= 2 (one per file).
  </acceptance_criteria>
  <verify>
    <automated>node tests/learned-local-provenance-exempt.test.js && node tests/recipe-signature-interpreter-hook.test.js && node tests/capability-interpreter.test.js</automated>
  </verify>
  <done>'local' is recognized as a trusted-exempt provenance in both the signature module and the interpreter, honored only via the loader's trustedProvenance arg (HI-01 preserved); the dedicated exemption suite plus the existing signature/interpreter suites are GREEN; the bundled/bare-core paths are byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| loader vouch -> verify exemption | The ONLY input that grants the signature-verify exemption is the loader's `trustedProvenance` argument; the recipe payload's own `provenance` field is never trusted for this decision. |
| learned recipe -> interpreter | A locally-synthesized recipe enters the interpreter and must be vouched 'local' by the trusted replay caller to skip verify. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-04-SELFDECLARE | Tampering / Spoofing | verifyRecipeEnvelope + interpretRecipe | mitigate | The exemption is resolved ONLY from the trusted channel (interpreter reads opts.trustedProvenance at :329-330; signature uses the second arg at :242-244). A recipe payload self-asserting `provenance:'local'` with no loader vouch is IGNORED and STILL verified — proven by the HI-01 masquerade case in the Wave-0 suite (the parallel of the existing 'bundled' masquerade test). |
| T-31-04-OVERBROAD | Elevation of Privilege | the exempt short-circuit | mitigate | Only the literal string `'local'` (and the pre-existing `'bundled'`) is exempt; any other provenance value falls through to the fail-closed verify path. The change is two boolean-OR additions — no broadening of the resolution logic. |
| T-31-04-REGRESS | Tampering | the bundled / bare-core paths | accept | The bundled + bare-core exempt paths are left byte-identical; the existing recipe-signature-interpreter-hook + capability-interpreter suites gate against regression. |
| T-31-04-SC | Tampering | npm installs | accept | Zero new packages (RESEARCH audit). |
</threat_model>

<verification>
- `node tests/learned-local-provenance-exempt.test.js` GREEN (the new exemption + HI-01 case).
- `node tests/recipe-signature-interpreter-hook.test.js && node tests/recipe-signature.test.js && node tests/capability-interpreter.test.js` all GREEN (no regression to the Phase-30 signature path).
- `node scripts/verify-recipe-path-guard.mjs` -> PASS (both files remain dynamic-code-free).
</verification>

<success_criteria>
- 'local' is a trusted-exempt provenance in both the signature module and the interpreter, honored only via the loader's trustedProvenance arg.
- HI-01 is preserved: a payload self-declaring 'local' with no vouch is still verified.
- The exemption is observable as a zero verifyEd25519 call.
- The bundled / bare-core paths are byte-identical; no regression.
</success_criteria>

<output>
Create `.planning/phases/31-network-capture-discovery-recipe-synthesis-learned-recipes/31-04-SUMMARY.md` when done.
</output>
