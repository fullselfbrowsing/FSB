---
phase: 31-network-capture-discovery-recipe-synthesis-learned-recipes
plan: 06
type: execute
wave: 4
depends_on: ["31-02", "31-03", "31-04", "31-05"]
files_modified:
  - extension/utils/discovery-session.js
  - extension/background.js
  - extension/ws/mcp-tool-dispatcher.js
  - extension/ws/mcp-bridge-client.js
autonomous: false
requirements: [DISC-01, DISC-02, LEARN-01]
user_setup: []

must_haves:
  truths:
    - "A user-initiated, time-boxed discovery session can be triggered (an internal message route mirroring the capabilities-invoke front door) that starts the consent-gated capture, observes real same-origin XHR/Fetch, and ends on the time/count bound (DISC-01/DISC-02, D-01)"
    - "promote-after-replay: each redacted ObservedCall is synthesized, validateRecipe-gated, replayed through the REAL interpretRecipe->executeBoundSpec path with trustedProvenance:'local', and promoted to the learned store + the search index ONLY on a clean replay (LEARN-01/D-10)"
    - "background.js additively importScripts the new modules + registers the Network-domain onEvent listener; manifest.json is UNCHANGED (the 'debugger' permission already exists, DISC-02)"
    - "The discovery trigger is OUT of TOOL_REGISTRY (INV-01) — the frozen tool-definitions parity hash is unmoved; getPublicTools omits it"
    - "No Phase 28/29/30 invariant breaks: INV-01 hash, INV-02 one-engine, INV-04 iterator byte-untouched, the consent gate, the origin-pin, the signature gate"
  artifacts:
    - path: "extension/utils/discovery-session.js"
      provides: "FsbDiscoverySession.runDiscovery — orchestrates capture -> synthesize -> replay -> promote (promote-after-replay)"
      exports: ["runDiscovery"]
      min_lines: 60
    - path: "extension/background.js"
      provides: "additive importScripts for the new modules + the Network-domain onEvent listener registration + the discovery entry"
      contains: "network-capture.js"
  key_links:
    - from: "extension/utils/discovery-session.js runDiscovery"
      to: "FsbNetworkCapture.startSession + FsbRecipeSynthesizer.synthesize + interpretRecipe->executeBoundSpec + FsbLearnedRecipeStore.promote + FsbCapabilitySearch.addLearnedRecipe"
      via: "the promote-after-replay orchestration"
      pattern: "executeBoundSpec|promote|addLearnedRecipe"
    - from: "extension/ws/mcp-tool-dispatcher.js"
      to: "FsbDiscoverySession.runDiscovery"
      via: "an internal mcp:capabilities-discover route (OUT of TOOL_REGISTRY, INV-01) resolving origin+tabId SW-side"
      pattern: "capabilities-discover"
---

<objective>
Integrate the discovery path end-to-end: a `discovery-session.js` orchestrator that runs the promote-after-replay loop (capture -> synthesize -> validateRecipe -> replay through the REAL `interpretRecipe -> executeBoundSpec` with `trustedProvenance:'local'` -> promote on clean replay + feed the search index), the `background.js` wiring (additive importScripts + the Network-domain `chrome.debugger.onEvent` listener + the discovery entry), and a user-initiated discovery trigger as an internal message route (`mcp:capabilities-discover`) that mirrors the capabilities-invoke front door and stays OUT of `TOOL_REGISTRY` (INV-01). Turn the Wave-0 RED suite `learned-promote-after-replay.test.js` GREEN.

Purpose: Realize DISC-01 (a user-initiated, time-boxed, consent-gated capture session — the trigger), DISC-02 (the Network domain rides the existing debugger attach; no manifest change), and LEARN-01/D-10 (promote ONLY after a successful discovered-and-replayed call through the real credentialed path). This is the integration wave — every Phase-31 module is now wired in without re-architecting the Phase 28/29/30 stack.
Output: 1 new orchestrator module + the background/dispatcher/bridge wiring; one Wave-0 suite GREEN; the full Phase-31 suite GREEN; all INV invariants preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-network-capture-discovery-recipe-synthesis-learned-recipes/31-CONTEXT.md
@.planning/phases/31-network-capture-discovery-recipe-synthesis-learned-recipes/31-RESEARCH.md

<interfaces>
<!-- The seams this plan ties together. All produced by prior Phase-31 plans + existing code. Use directly. -->

From extension/utils/network-capture.js (Plan 02):
  FsbNetworkCapture.startSession(origin, {tabId, maxMs, maxCount, confirmedSensitive}) -> Promise<{ok, reason?, sessionId?}>
  FsbNetworkCapture.endSession(reason) -> ObservedCall[]   // the redacted calls; also returned/collected at end
  FsbNetworkCapture._onCdpEvent(source, method, params)     // the listener background.js registers on chrome.debugger.onEvent

From extension/utils/recipe-synthesizer.js (Plan 03):
  FsbRecipeSynthesizer.synthesize(observedCall) -> { recipe, descriptor, flaggedForPhase32 } | null

From extension/utils/learned-recipe-store.js (Plan 03):
  FsbLearnedRecipeStore.promote(origin, recipe, descriptor) -> Promise<void>

From extension/utils/capability-search.js (Plan 05):
  FsbCapabilitySearch.addLearnedRecipe(recipe, descriptor)

From extension/utils/capability-interpreter.js (Plan 04 — 'local' exempt) + capability-fetch.js (the MAIN-world primitive):
  FsbCapabilityInterpreter.interpretRecipe(recipe, {}, { trustedProvenance: 'local' }) -> Promise<{success, spec?}>
  <fetch primitive>.executeBoundSpec(spec, tabId) -> Promise<{success, ...}>   // re-asserts the active-tab origin-pin

From extension/ws/mcp-tool-dispatcher.js:108-114,2174-2236 (the front-door route idiom to MIRROR):
  MCP route table: 'mcp:capabilities-search' / 'mcp:capabilities-invoke' -> handler; resolve origin+tabId SW-side via chrome.tabs.query({active:true,currentWindow:true}); payload.origin/tab_id are non-authoritative overrides
  These tools register OUTSIDE TOOL_REGISTRY (INV-01) — the discovery route follows the SAME out-of-registry pattern

From extension/background.js:7-120 (the importScripts block) + manifest.json:15 ('debugger' permission ALREADY present — NO change):
  importScripts('utils/<module>.js') with a try/catch wrapper (the established idiom)
</interfaces>

<read_first>
- extension/utils/discovery-session.js (NEW — create)
- extension/background.js:7-120 (the importScripts block — add the new modules in dependency order: redactor, network-capture, recipe-synthesizer, learned-recipe-store, discovery-session AFTER capability-search/catalog/router/interpreter/fetch are already loaded) + the message-handler region where chrome.debugger.onEvent should be registered (search for the existing keyboardEmulator/debugger usage near :13900)
- extension/ws/mcp-tool-dispatcher.js:104-114 (the MCP_PHASE199_MESSAGE_ROUTES table) + :2174-2236 (handleCapabilitiesSearchMessageRoute + handleCapabilitiesInvokeMessageRoute — the EXACT origin/tabId-resolution + route-handler form to mirror for handleCapabilitiesDiscoverMessageRoute)
- extension/ws/mcp-bridge-client.js (the bridge delegate region — add a pure pass-through delegate for the discover wire name, mirroring the capabilities-search/invoke delegates)
- extension/utils/capability-fetch.js:280-300 (executeBoundSpec's SW-side wrapper + the active-tab origin resolution — the replay calls this; the origin-pin holds)
- extension/utils/capability-router.js:345-404 (_runDeclarativeTier — the replay shape to mirror in runDiscovery: interpretRecipe -> executeBoundSpec; thread {trustedProvenance:'local'})
- tests/learned-promote-after-replay.test.js (the Wave-0 RED contract: clean replay -> stored; failed replay -> not stored; opts.trustedProvenance==='local' threaded)
- tests/tool-definitions-parity.test.js + tests/capability-mcp-surface.test.js (the INV-01 frozen-hash + out-of-registry proofs that MUST stay GREEN — the discover route must NOT enter TOOL_REGISTRY)
- 31-RESEARCH.md Pattern 4 (promote-after-replay through the REAL interpret/execute path), §Architectural Responsibility Map (the trigger is a thin front door onto the SW session manager), Pitfall 1/2 (the banner + ownership-safe detach), Open Q1 (auto-promote on clean replay within the consented session)
</read_first>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: discovery-session.js — the promote-after-replay orchestrator</name>
  <files>extension/utils/discovery-session.js</files>
  <behavior>
    - runDiscovery(origin, {tabId, maxMs, maxCount, confirmedSensitive}) starts the consent-gated capture (returns early with the reject reason if the gate denies)
    - for each redacted ObservedCall at session end: synthesize -> if null, skip; else replay via interpretRecipe(recipe,{},{trustedProvenance:'local'}) -> executeBoundSpec(spec, tabId)
    - a CLEAN replay (both success:true) -> promote(origin, recipe, descriptor) + addLearnedRecipe(recipe, descriptor) (stored + findable)
    - a FAILED replay (interpret or execute success!==true) -> the candidate is DISCARDED, never stored (D-10)
    - returns a summary {promoted:[...slugs], discarded:N, flaggedForPhase32:[...]} (no secrets)
  </behavior>
  <read_first>
    - extension/utils/capability-router.js:352-404 (_runDeclarativeTier — the exact interpret->execute replay shape + the falsy-guard fail-closed)
    - extension/utils/capability-fetch.js:280-300 (executeBoundSpec SW wrapper)
    - tests/learned-promote-after-replay.test.js (the contract — note it stubs interpret/execute and asserts opts.trustedProvenance==='local')
    - 31-RESEARCH.md Pattern 4 + Open Q1 (auto-promote on clean replay within the consented session)
  </read_first>
  <action>
    Create `extension/utils/discovery-session.js` as a dynamic-code-free dual-export IIFE (`global.FsbDiscoverySession` + `module.exports`), ASCII-only, NO EMOJIS. (It is NOT recipe-path-adjacent in the bind/execute sense — it orchestrates — but keep it dynamic-code-free regardless; do NOT add it to RECIPE_PATH_ALLOWLIST since it is not on the allowlist and is not auto-globbed.)

    typeof-guarded accessors: `_capture()` -> FsbNetworkCapture, `_synth()` -> FsbRecipeSynthesizer, `_store()` -> FsbLearnedRecipeStore, `_search()` -> FsbCapabilitySearch, `_interp()` -> FsbCapabilityInterpreter, `_primitive()` -> the fetch primitive global (the same one _runDeclarativeTier uses via _fetchPrimitive). Each degrades to null.

    Implement `async runDiscovery(origin, opts)`:
    1. `const started = await _capture().startSession(origin, opts);` — if `!started || started.ok !== true`, return `{ ok:false, reason: started && started.reason }` (the gate denied — surface the reason; no capture happened).
    2. Wait for the session to end (the capture module ends itself on the time/count bound, or `endSession` returns the ObservedCalls). Obtain the ObservedCall[] (the capture module's return at end). [The capture/session coordination: runDiscovery awaits a session-complete signal the capture module exposes — either startSession resolves with the calls after the bound, or runDiscovery polls/awaits an endSession promise. Implement whichever matches network-capture.js's actual surface from Plan 02; prefer awaiting an explicit session-complete promise.]
    3. For each ObservedCall: `const cand = _synth().synthesize(call); if (!cand || !cand.recipe) { discarded++; continue; }`. REPLAY (the promote-after-replay gate, D-10): `const interpreted = await _interp().interpretRecipe(cand.recipe, {}, { trustedProvenance: 'local' }); if (!interpreted || interpreted.success !== true) { discarded++; continue; }` then `const out = await _primitive().executeBoundSpec(interpreted.spec, opts && opts.tabId); if (!out || out.success !== true) { discarded++; continue; }`. On a CLEAN replay: `await _store().promote(origin, cand.recipe, cand.descriptor); if (_search() && _search().addLearnedRecipe) _search().addLearnedRecipe(cand.recipe, cand.descriptor); promoted.push(cand.recipe.id);` and collect cand.flaggedForPhase32 slugs.
    4. Return `{ ok:true, promoted, discarded, flaggedForPhase32 }` — slugs/counts only, NO bodies, NO args, NO secrets.

    The replay MUST thread `{ trustedProvenance: 'local' }` (the loader's vouch, HI-01) so the interpreter takes the exempt bind (Plan 04) — never the payload self-declaring. Awaiting an interpretRecipe result is always safe (the 'local' path returns a plain object; await is a no-op on it).
  </action>
  <acceptance_criteria>
    - `node tests/learned-promote-after-replay.test.js` exits 0 (GREEN): a clean replay promotes (getLearned non-null) + feeds the index; a failed interpret OR a failed executeBoundSpec discards (getLearned null); the interpret stub received `{trustedProvenance:'local'}`.
    - `grep -q "trustedProvenance: 'local'" extension/utils/discovery-session.js` succeeds.
    - `grep -nE "\\beval\\b|new Function|import\\(" extension/utils/discovery-session.js` returns nothing.
    - The return summary carries slugs/counts only (no body/args fields — verified by reading the file).
  </acceptance_criteria>
  <verify>
    <automated>node tests/learned-promote-after-replay.test.js</automated>
  </verify>
  <done>runDiscovery starts the consent-gated capture, synthesizes each ObservedCall, replays it through the real interpretRecipe->executeBoundSpec path with the 'local' vouch, and promotes + indexes ONLY on a clean replay (discarding failures); the promote-after-replay RED suite is GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: background.js wiring (importScripts + Network onEvent listener + discovery entry) and the out-of-registry discover route + bridge delegate</name>
  <files>extension/background.js, extension/ws/mcp-tool-dispatcher.js, extension/ws/mcp-bridge-client.js</files>
  <read_first>
    - extension/background.js:7-120 (the importScripts block + the try/catch idiom) + :13900-14015 (the existing chrome.debugger usage — register the onEvent listener near here, after FsbNetworkCapture loads)
    - extension/ws/mcp-tool-dispatcher.js:104-114 (the route table) + :2174-2236 (the two capabilities handlers to mirror for discover — SW-side origin/tabId resolution; OUT of TOOL_REGISTRY)
    - extension/ws/mcp-bridge-client.js (the capabilities-search/invoke delegates — add a pure pass-through for the discover wire name)
    - tests/tool-definitions-parity.test.js + tests/capability-mcp-surface.test.js (INV-01: the discover route must NOT enter TOOL_REGISTRY; the frozen hash stays unmoved)
    - manifest.json:15 ('debugger' permission — confirm present; do NOT edit the manifest)
  </read_first>
  <action>
    In `extension/background.js`: ADD `importScripts` lines (wrapped in the established `try { importScripts('utils/<m>.js'); } catch (e) { console.error(...) }` idiom) for the new modules, in DEPENDENCY ORDER and AFTER the existing capability stack (capability-recipe-schema/interpreter/signature/search/catalog/router/fetch + redactForLog + the vendored libs are already imported): `utils/network-capture-redactor.js`, `utils/network-capture.js`, `utils/recipe-synthesizer.js`, `utils/learned-recipe-store.js`, `utils/discovery-session.js`. Register the Network-domain event listener ONCE on the EXISTING debugger attachment (D-02): `if (chrome.debugger && chrome.debugger.onEvent && typeof FsbNetworkCapture !== 'undefined') { chrome.debugger.onEvent.addListener(FsbNetworkCapture._onCdpEvent); }` — this is the FIRST onEvent consumer in the extension (RESEARCH-verified), so it adds cleanly; it is method-dispatched so Input sendCommand traffic is unaffected (DISC-02). Do NOT add an onDetach handler here if network-capture.js registers its own per-session (avoid a double teardown); follow whatever Plan 02's endSession contract expects. NO manifest.json edit (the 'debugger' permission is already present — DISC-02, D-02).

    In `extension/ws/mcp-tool-dispatcher.js`: add an internal route `'mcp:capabilities-discover': { routeFamily: 'capabilities', handler: handleCapabilitiesDiscoverMessageRoute }` to the route table (the same const literal that holds capabilities-search/invoke). Add `async function handleCapabilitiesDiscoverMessageRoute({ payload })`: guard `typeof FsbDiscoverySession === 'undefined' || typeof FsbDiscoverySession.runDiscovery !== 'function'` -> a createMcpRouteError. Resolve `tabId` + `origin` SW-side EXACTLY like handleCapabilitiesInvokeMessageRoute (:2212-2234): explicit `payload.tab_id`/`payload.origin` are non-authoritative overrides; else `chrome.tabs.query({active:true,currentWindow:true})` -> the active/owned tab's id + `new URL(tab.url).origin`. Pass through `confirmedSensitive: !!payload.confirmed_sensitive` and the session bounds (maxMs/maxCount with the module defaults if absent). Return `await FsbDiscoverySession.runDiscovery(origin, { tabId, maxMs, maxCount, confirmedSensitive })`. This route is INTERNAL-only and registers via the route table — it is NOT added to TOOL_REGISTRY and NOT to getPublicTools (INV-01): the discovery trigger is a control surface, not an MCP tool schema. Add the wire name to the MCPMessageType union if the codebase requires it (the Rule-3 type wiring the capabilities routes used), with NO TOOL_REGISTRY / tool-definitions edit.

    In `extension/ws/mcp-bridge-client.js`: add a pure pass-through delegate for `mcp:capabilities-discover` mirroring the capabilities-search/invoke delegates (no origin/tab resolution in the bridge — the dispatcher handler is the single authoritative resolution point).

    NO EMOJIS. Do NOT edit manifest.json, tool-definitions.js, or TOOL_REGISTRY. Do NOT touch agent-loop.js (INV-04).
  </action>
  <acceptance_criteria>
    - `node tests/tool-definitions-parity.test.js` exits 0 — the frozen INV-01 tool-definitions hash is UNMOVED (the discover route is out-of-registry).
    - `node tests/capability-mcp-surface.test.js` exits 0 — the registry hash + the two capability tools on the wire are unchanged; the discover route did not enter TOOL_REGISTRY.
    - `node tests/capability-autopilot-parity.test.js && node tests/agent-loop-iterator-guard.test.js` exit 0 — INV-02 one-engine + INV-04 iterator byte-untouched.
    - `grep -q "network-capture.js" extension/background.js` and `grep -q "capabilities-discover" extension/ws/mcp-tool-dispatcher.js` both succeed; `git diff --quiet manifest.json` (no manifest change).
  </acceptance_criteria>
  <verify>
    <automated>node tests/tool-definitions-parity.test.js && node tests/capability-mcp-surface.test.js && node tests/agent-loop-iterator-guard.test.js && git diff --quiet -- manifest.json && echo INV-OK</automated>
  </verify>
  <done>background.js additively imports the new modules and registers the Network onEvent listener (no manifest change); an internal mcp:capabilities-discover route mirrors the invoke front door (SW-side origin/tabId resolution, OUT of TOOL_REGISTRY) with a pure bridge delegate; the INV-01/02/04 proof suites stay GREEN.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3 (checkpoint): Live capture -> synthesize -> promote -> outrank UAT</name>
  <action>Human-gated verification AFTER the automated wiring completes — confirm the live chrome.debugger Network capture -> redact -> synthesize -> credentialed MAIN-world replay -> promote -> outrank loop on a real authenticated origin. Record in 31-HUMAN-UAT.md (human_needed if not run live), matching the Phase 27/28/29/30 live-UAT posture. No code is produced by this task.</action>
  <what-built>The live discovery loop: a real chrome.debugger Network capture on an authenticated origin (the "started debugging this browser" banner appears), a real first-party XHR/Fetch is observed, redacted, synthesized, replayed through the live MAIN-world credentialed fetch, promoted, and outranks the generic recipe on the next visit. This is the irreducibly-LIVE half that cannot run in CI (it needs a real Chrome extension + a real authenticated session + the debugger attach), matching the Phase 27/28/29/30 human_needed UAT posture.</what-built>
  <how-to-verify>
    1. Load the unpacked extension in Chrome.
    2. Sign in to a supported origin (e.g. github.com) so first-party HttpOnly cookies are present.
    3. Trigger a discovery session on that origin (the mcp:capabilities-discover route / control surface). Confirm the "FSB started debugging this browser" banner appears (expected, per Pitfall 2) and clears promptly at session end.
    4. Perform an action on the page that fires a same-origin XHR/Fetch API call within the session bound.
    5. Confirm a learned recipe is synthesized + promoted (no errors), is findable via search_capabilities on the next visit to that origin, and OUTRANKS the generic recipe during routing.
    6. Confirm NO secret/body/query/header-value was persisted (inspect the fsbLearnedRecipes storage key — shape only).
    Record the outcome in `31-HUMAN-UAT.md` (status human_needed if not run live), matching the prior-phase live-UAT ledger.
  </how-to-verify>
  <resume-signal>Type "approved" once the live capture->synthesize->promote->outrank loop is confirmed (or "recorded as human_needed UAT" to log it as deferred live-browser debt, consistent with Phases 27-30).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user trigger -> SW session | The discovery trigger is a user-initiated control surface; the SW owns the capture session (the page is never trusted to start it or report traffic). |
| capture -> synthesize -> replay -> promote | The full promote-after-replay pipeline: a redacted capture must survive synthesis (schema-validated), a real origin-pinned credentialed replay, before it is stored. |
| discover route -> MCP surface | The discover route is internal-only and must NOT enter TOOL_REGISTRY (INV-01) or grow the public tool schema. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-06-HOSTILE | Spoofing / Tampering | runDiscovery pipeline | mitigate | A malicious page cannot inject CDP events (SW-owned attachment); defense-in-depth across the whole pipeline: consent gate (Plan 02) + same-origin filter (Plan 02) + validateRecipe (Plan 03) + promote-after-replay (a hostile candidate that does not replay through the real credentialed path is discarded, D-10) + the executeBoundSpec origin-pin. Five independent gates — a synthesized hostile recipe never reaches the store. |
| T-31-06-INV01 | Tampering | the discover route | mitigate | The route registers via the message-route table OUTSIDE TOOL_REGISTRY (the capabilities-search/invoke precedent); no tool-definitions/getPublicTools edit -> the frozen INV-01 parity hash is unmoved (gated by tool-definitions-parity + capability-mcp-surface suites). |
| T-31-06-MANIFEST | Elevation of Privilege | background.js wiring | mitigate | NO manifest change — the 'debugger' permission is already granted (DISC-02); the wiring is additive importScripts + one onEvent listener; `git diff --quiet manifest.json` is asserted. |
| T-31-06-PROVENANCE | Tampering / Spoofing | the replay vouch | mitigate | runDiscovery threads `trustedProvenance:'local'` (the loader's vouch) so the interpreter takes the exempt bind (Plan 04); the recipe never self-declares 'local' (HI-01). |
| T-31-06-IteratorDoS | Denial of Service | INV-04 iterator | accept | agent-loop.js is NOT touched (discovery is SW-side); the setTimeout iterator stays byte-identical (gated by agent-loop-iterator-guard). |
| T-31-06-LEAK | Information Disclosure | the runDiscovery summary + audit | mitigate | The summary carries slugs/counts only (no body/args); the stored shape is request-shape-only (Plan 03); the live UAT step inspects the fsbLearnedRecipes key for no-secret confirmation (LEARN-02). |
| T-31-06-SC | Tampering | npm installs | accept | Zero new packages (RESEARCH audit). |
</threat_model>

<verification>
- `node tests/learned-promote-after-replay.test.js` GREEN (the orchestrator's clean-vs-failed replay).
- `npm test` GREEN end-to-end: ALL nine Phase-31 suites pass AND the INV-01/02/04 proof suites (tool-definitions-parity, capability-mcp-surface, capability-autopilot-parity, agent-loop-iterator-guard) AND the Phase-30 consent/signature suites are unregressed.
- `node scripts/verify-recipe-path-guard.mjs` -> PASS.
- `git diff --quiet -- manifest.json` (no manifest change).
- Live capture->synthesize->promote->outrank loop: human_needed UAT (recorded in 31-HUMAN-UAT.md), matching the Phase 27/28/29/30 posture.
</verification>

<success_criteria>
- A user-initiated, time-boxed discovery session can be triggered (internal route, out of TOOL_REGISTRY) that runs the consent-gated capture and the promote-after-replay loop.
- promote-after-replay stores + indexes ONLY on a clean real-credentialed replay (failures discarded), threading the 'local' vouch.
- background.js wires the modules + the Network onEvent listener with NO manifest change.
- All INV invariants (INV-01 hash, INV-02 one-engine, INV-04 iterator) and the Phase-30 gates are preserved; the full suite is GREEN.
- The live loop is recorded as human_needed UAT.
</success_criteria>

<output>
Create `.planning/phases/31-network-capture-discovery-recipe-synthesis-learned-recipes/31-06-SUMMARY.md` when done.
</output>
