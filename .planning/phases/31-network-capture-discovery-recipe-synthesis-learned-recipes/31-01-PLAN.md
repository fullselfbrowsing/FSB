---
phase: 31-network-capture-discovery-recipe-synthesis-learned-recipes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/_helpers/cdp-event-driver.js
  - tests/network-capture.test.js
  - tests/network-capture-consent.test.js
  - tests/network-capture-redaction.test.js
  - tests/recipe-synthesizer.test.js
  - tests/learned-promote-after-replay.test.js
  - tests/learned-recipe-store.test.js
  - tests/learned-search-add.test.js
  - tests/learned-t2-outranking.test.js
  - tests/learned-local-provenance-exempt.test.js
  - scripts/verify-recipe-path-guard.mjs
  - package.json
  - .planning/phases/31-network-capture-discovery-recipe-synthesis-learned-recipes/31-VALIDATION.md
autonomous: true
requirements: [DISC-02, DISC-03, DISC-04, LEARN-01, LEARN-02, LEARN-03, LEARN-04]
user_setup: []

must_haves:
  truths:
    - "Nine RED test suites exist and FAIL LOUD (modules absent) so Waves 2-3 turn them GREEN (Nyquist sampling armed)"
    - "A shared chrome.debugger event-driver stub feeds canned onEvent(source,method,params) so capture is testable in Node without a live browser (DISC-02)"
    - "recipe-synthesizer.js + learned-recipe-store.js are on RECIPE_PATH_ALLOWLIST so the CI guard scans them eval-free and Check 4 stays green (the gating CI constraint)"
    - "All nine suites are wired into the package.json scripts.test chain tail so the full suite samples Phase-31 behavior"
  artifacts:
    - path: "tests/_helpers/cdp-event-driver.js"
      provides: "Canned chrome.debugger onEvent feeder + storage stub re-export for the capture suites"
      min_lines: 30
    - path: "tests/network-capture-redaction.test.js"
      provides: "THE security test: no auth substring / no body / no query / no PII survives redaction (DISC-03/LEARN-02)"
      min_lines: 40
    - path: "scripts/verify-recipe-path-guard.mjs"
      provides: "RECIPE_PATH_ALLOWLIST extended with the two recipe-path-adjacent modules"
      contains: "recipe-synthesizer.js"
  key_links:
    - from: "package.json scripts.test"
      to: "tests/network-capture.test.js"
      via: "&& chain tail next to capability-*.test.js"
      pattern: "network-capture.test.js"
    - from: "scripts/verify-recipe-path-guard.mjs"
      to: "extension/utils/learned-recipe-store.js"
      via: "RECIPE_PATH_ALLOWLIST entry"
      pattern: "learned-recipe-store.js"
---

<objective>
Establish the Wave-0 validation contract for Phase 31: nine zero-framework RED test suites authored BEFORE their modules (so Waves 2-3 turn them RED -> GREEN), a NEW shared `chrome.debugger` event-driver stub fixture, the `RECIPE_PATH_ALLOWLIST` extension for the two recipe-path-adjacent net-new modules, and the `package.json` test-chain wiring.

Purpose: Arm the Nyquist sampling (per the 31-VALIDATION.md contract) so every downstream task has an `<automated>` command that fails loud until the behavior lands. The CI-guard allowlist extension is the gating constraint from RESEARCH (`verify-recipe-path-guard.mjs` Check 4 auto-globs `capability-*.js`; the net-new names avoid the glob but the synthesizer + learned-store are recipe-path-adjacent and must be listed AND kept dynamic-code-free).
Output: 9 new `tests/*.test.js` files + 1 shared helper + an extended allowlist + the package.json chain + a finalized 31-VALIDATION.md per-task map.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-network-capture-discovery-recipe-synthesis-learned-recipes/31-CONTEXT.md
@.planning/phases/31-network-capture-discovery-recipe-synthesis-learned-recipes/31-RESEARCH.md
@.planning/phases/31-network-capture-discovery-recipe-synthesis-learned-recipes/31-VALIDATION.md

<interfaces>
<!-- Contracts the RED tests assert against. These modules do NOT exist yet (Waves 2-3 create them). -->
<!-- The RED tests encode the EXACT exported names + return shapes Waves 2-3 must satisfy. -->

The four net-new SW modules (dual-export IIFE: `global.FsbXxx` + `module.exports`), created in Waves 2-3:
- `extension/utils/network-capture.js` -> global `FsbNetworkCapture` with:
    - `startSession(origin, opts) -> Promise<{ ok, reason? , sessionId? }>` (consent-gated; opts: {tabId, maxMs, maxCount, confirmedSensitive})
    - `_onCdpEvent(source, method, params)` (the method-dispatched event handler; testable directly)
    - `_filterResourceType(type) -> bool` (XHR/Fetch only), `endSession(reason)`
- `extension/utils/network-capture-redactor.js` -> global `FsbNetworkCaptureRedactor` with:
    - `redactRequest(request) -> { method, path, origin, headerNames }` (NO query, NO header values, auth carriers removed)
    - `redactResponse(response) -> { status, mimeType }` (NO body, NO getResponseBody)
- `extension/utils/recipe-synthesizer.js` -> global `FsbRecipeSynthesizer` with:
    - `synthesize(observedCall) -> { recipe, descriptor } | null` (validateRecipe-green or null)
- `extension/utils/learned-recipe-store.js` -> global `FsbLearnedRecipeStore` with:
    - `promote(origin, recipe, descriptor) -> Promise<void>`, `getLearned(slug, origin) -> { recipe, descriptor } | null`
    - `quarantine(slug, origin) -> Promise<void>`, `readAll() -> Promise<envelope>`

The four MODIFIED seams (already exist; Waves 2-3 add behavior the tests assert):
- `extension/utils/capability-interpreter.js` `interpretRecipe(recipe, args, opts)` — opts.trustedProvenance gains `'local'` (D-09)
- `extension/utils/capability-signature.js` `verifyRecipeEnvelope(envelope, trustedProvenance)` — `'local'` short-circuits (D-09)
- `extension/utils/capability-search.js` gains `addLearnedRecipe(recipe, descriptor)` (LEARN-03)
- `extension/utils/capability-catalog.js` `resolve(slug, origin)` checks `_getLearned()` first (LEARN-04); `extension/utils/capability-router.js` `case 'T2'` dispatches via `_runDeclarativeTier(..., {trustedProvenance:'local'})`

Proven test idioms to REUSE (read these, do not reinvent):
- `tests/consent-policy-store.test.js:43-72` — `installChromeStorageStub()` Map-backed in-memory chrome.storage.local
- `tests/recipe-signature-interpreter-hook.test.js:97-127` — the `verifyEd25519` spy + zero-call assertion + the payload-self-declare masquerade test
- `tests/capability-fetch.test.js:85-90` — the `vm.runInThisContext` vendored-global loader (cfworker IIFE) so `validateRecipe`/`interpretRecipe` run under Node
- Zero-framework convention: `let passed=0,failed=0; function check(cond,msg){...}; process.exit(failed>0?1:0)`
</interfaces>

<read_first>
- tests/consent-policy-store.test.js:1-75 (the storage stub + check() idiom — copy installChromeStorageStub verbatim into the helper)
- tests/recipe-signature-interpreter-hook.test.js:90-130 (the verifyEd25519 spy + masquerade test pattern for the 'local' exemption suite)
- tests/capability-fetch.test.js:80-95 (the vm.runInThisContext cfworker loader so synthesizer/interpreter tests can run validateRecipe under Node)
- scripts/verify-recipe-path-guard.mjs:90-140 (the RECIPE_PATH_ALLOWLIST array — the exact array to extend, and the "registered ahead of creation; Check 1 skips an absent path, Check 4 fails closed when it lands" precedent)
- package.json:17 (the scripts.test && chain — the capability-*/consent-* tail near tests/agent-loop-iterator-guard.test.js is where the new suites append)
- .planning/phases/31-network-capture-discovery-recipe-synthesis-learned-recipes/31-VALIDATION.md (the per-task map to finalize: replace TBD task IDs with 31-NN, flip nyquist_compliant true)
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the shared chrome.debugger event-driver stub + the three capture RED suites + the synthesizer RED suite</name>
  <files>tests/_helpers/cdp-event-driver.js, tests/network-capture.test.js, tests/network-capture-consent.test.js, tests/network-capture-redaction.test.js, tests/recipe-synthesizer.test.js</files>
  <read_first>
    - tests/consent-policy-store.test.js:43-72 (installChromeStorageStub — re-export it from the helper)
    - tests/capability-fetch.test.js:80-95 (the vm cfworker loader for the synthesizer/redaction suites that call validateRecipe)
    - 31-RESEARCH.md Pattern 1 + Pattern 2 (the onEvent shape: source.tabId, method 'Network.requestWillBeSent'/'Network.responseReceived', params.type ResourceType, params.request {url,method,headers}, params.response {status,mimeType})
    - extension/utils/service-denylist.js:120-145 (isDenied/classify return shapes the consent suite stubs against)
    - extension/utils/consent-policy-store.js:107-179 (readPolicies/getConsentForOrigin the consent suite drives)
  </read_first>
  <action>
    Create `tests/_helpers/cdp-event-driver.js` as a CommonJS helper exporting: (1) `installChromeStorageStub()` lifted verbatim from consent-policy-store.test.js (Map-backed get/set/remove, both callback and promise forms); (2) `makeCdpDriver()` returning `{ listeners:[], addListener(fn), fire(source,method,params), reset() }` modeling `chrome.debugger.onEvent` — `fire` invokes every registered listener with `(source,method,params)`; (3) `cannedRequestEvent({tabId, url, method, headers, type})` and `cannedResponseEvent({tabId, requestId, status, mimeType})` factories producing the exact `requestWillBeSent`/`responseReceived` param shapes. This is the NEW fixture the capture suites share; it mirrors the chrome.scripting recorder idiom in capability-fetch.test.js.

    Write `tests/network-capture.test.js` (DISC-02 dispatch + DISC-04 filter): require the helper; require `extension/utils/network-capture.js`; assert the module loads and exports `FsbNetworkCapture` with `_onCdpEvent`/`_filterResourceType`/`startSession`/`endSession`. Feed via the driver a same-origin XHR event AND a same-origin Document/Image/Stylesheet/Font/Media event AND a cross-origin XHR event; assert ONLY the same-origin XHR/Fetch become ObservedCalls (the Document/Image/CSS/Font/Media and cross-origin are dropped). Assert that firing a `Network.responseReceived` for a tracked requestId attaches a `responseShape` with `status`/`mimeType` and NEVER triggers any `Network.getResponseBody` sendCommand (assert the stubbed sendCommand recorder shows zero getResponseBody calls). Assert an Input `sendCommand` path is unaffected (the event handler is method-dispatched: firing a non-Network method is a no-op).

    Write `tests/network-capture-consent.test.js` (DISC-04 gate): install the storage stub + a FsbConsentPolicyStore/FsbServiceDenylist test double (or drive the real modules with seeded storage). Assert `startSession` is REJECTED (ok:false, reason a RECIPE_CONSENT_* code) on an Off origin and on an `isDenied` origin; ALLOWED on Ask and on Auto; and that a `classify().sensitive` origin is rejected UNLESS `opts.confirmedSensitive === true` (the extra-confirm flag, D-03). Reference D-03/DISC-04 in the test message strings.

    Write `tests/network-capture-redaction.test.js` (THE security test, DISC-03/LEARN-02): require `extension/utils/network-capture-redactor.js`; feed a request carrying `authorization`, `cookie`, `x-csrf-token`, `x-api-key`, a `bearer`-named header, a URL with a `?session=...&token=...` query, and a populated body. Assert the redacted output: has NO `?`/query string (path only, D-06); `headerNames` contains NONE of authorization/cookie/x-csrf/x-api-key/bearer (auth carriers removed, D-07) and NO header VALUES anywhere; has NO body field (D-06); `redactResponse` returns ONLY {status,mimeType} with NO body and NO header values (D-08). Stringify the WHOLE redacted artifact and assert the serialized JSON contains none of the secret substrings (the audit-log-no-secret.test.js discipline). Reference D-05..D-08 in messages.

    Write `tests/recipe-synthesizer.test.js` (LEARN-01): load the cfworker validator via vm (capability-fetch.test.js:85-90) + require capability-recipe-schema.js + recipe-synthesizer.js. Feed a redacted ObservedCall (method GET, path /api/items/{id}-shaped, headerNames, origin) and assert `synthesize()` returns `{recipe, descriptor}` whose recipe passes `FsbCapabilityRecipeSchema.validateRecipe` (success:true) and whose descriptor carries slug/service/intentSynonyms/actionVerb. Assert a token-minted-by-prior-GET / response-CSRF ObservedCall yields a recipe with `authStrategy` of `same-origin-cookie` (NOT csrf with from:'response') and a `flaggedForPhase32` marker (the GATING synthesis cap, D-11 / Pitfall 4). Assert an unsynthesizable capture (e.g. cross-origin or malformed) yields `null`.

    Every suite uses the zero-framework `passed/failed` + `check(cond,msg)` + `process.exit(failed>0?1:0)`. They MUST fail loud now (modules absent -> require throws / undefined exports), which is the RED state Waves 2-3 flip GREEN. NO EMOJIS in any file.
  </action>
  <acceptance_criteria>
    - `node tests/network-capture.test.js` exits NON-ZERO now (module absent) with a clear failure, not a silent pass.
    - `node tests/network-capture-redaction.test.js` exits NON-ZERO now; its assertions encode no-secret/no-body/no-query (verified by reading the file: it stringifies the artifact and greps for authorization/cookie/bearer/token/`?`).
    - `node tests/recipe-synthesizer.test.js` exits NON-ZERO now; it asserts validateRecipe success AND the from:'response' -> same-origin-cookie+flag cap.
    - `tests/_helpers/cdp-event-driver.js` exports `installChromeStorageStub`, `makeCdpDriver`, `cannedRequestEvent`, `cannedResponseEvent`.
    - No file contains an emoji.
  </acceptance_criteria>
  <verify>
    <automated>node tests/network-capture-redaction.test.js; test $? -ne 0 && echo RED-AS-EXPECTED</automated>
  </verify>
  <done>The capture/redaction/synthesizer RED suites + the shared CDP event-driver stub exist, fail loud (modules absent), and encode the DISC-02/03/04 + LEARN-01 contracts including the from:'response' synthesis cap.</done>
</task>

<task type="auto">
  <name>Task 2: Author the five learned-recipe RED suites (store, promote-after-replay, search-add, T2 outranking, 'local' provenance exemption)</name>
  <files>tests/learned-recipe-store.test.js, tests/learned-promote-after-replay.test.js, tests/learned-search-add.test.js, tests/learned-t2-outranking.test.js, tests/learned-local-provenance-exempt.test.js</files>
  <read_first>
    - tests/recipe-signature-interpreter-hook.test.js:97-127 (the verifyEd25519 spy + zero-call + masquerade — the template for the 'local' exemption suite)
    - extension/utils/capability-search.js:48-52,222-234 (INDEX_OPTIONS, getRecipeBySlug, exportsObj — the addLearnedRecipe target surface)
    - extension/utils/capability-catalog.js:217-267 (resolve — the learned-first prepend the outranking suite asserts)
    - extension/utils/capability-router.js:485-512 (the T2 stub at :498-501 the outranking suite asserts becomes a real dispatch)
    - extension/utils/consent-policy-store.js:103-164 (the versioned-envelope read/write idiom the store suite asserts round-trips)
    - extension/utils/capability-interpreter.js:320-399 (interpretRecipe opts.trustedProvenance — the 'local' suite drives this with a spy)
  </read_first>
  <action>
    Write `tests/learned-recipe-store.test.js` (D-13/D-16): install the storage stub; require `extension/utils/learned-recipe-store.js`. Assert a per-origin round-trip: `promote(originA, recipeA, descA)` then `getLearned(recipeA.id, originA)` returns the recipe, and `getLearned(recipeA.id, originB)` returns null (origin-scoped, Pitfall 6). Assert the stored envelope is a versioned `{v, recipes:{[origin]:{[slug]:{recipe,descriptor,capturedAt,lastSuccessAt,successCount,quarantined}}}}` shape. Assert LRU: with a per-origin cap (the module's constant, e.g. 24), promoting past the cap evicts the entry with the oldest `lastSuccessAt`. Assert quarantine: `quarantine(slug, origin)` sets `quarantined:true` (does NOT delete the key) and `getLearned` then returns null for that entry (demoted from routing).

    Write `tests/learned-promote-after-replay.test.js` (LEARN-01/D-10): provide stub `interpretRecipe` (returns `{success:true, spec}`) and stub `executeBoundSpec`. CASE A: a clean replay (executeBoundSpec -> {success:true}) results in the recipe being stored (getLearned non-null). CASE B: a failed replay (executeBoundSpec -> {success:false, code:'RECIPE_ORIGIN_MISMATCH'}) results in NO store (getLearned null) — the candidate is discarded, not stored (D-10). CASE C: a failed interpret ({success:false}) also yields no store. The replay must thread `{trustedProvenance:'local'}` into interpretRecipe — assert the stub received that opts.

    Write `tests/learned-search-add.test.js` (LEARN-03): load MiniSearch (vendored) + require capability-search.js with a built index; call `addLearnedRecipe(recipe, descriptor)`; assert the slug is now findable via `search(query, origin)` AND `getRecipeBySlug(recipe.id)` returns the recipe. Assert the snapshot is re-persisted to `fsbCapabilityIndex` with a BUMPED catalogVersion, and that a simulated restart (`buildOrRestore` against the persisted snapshot, or `MiniSearch.loadJSON(snapshot, INDEX_OPTIONS)`) succeeds WITHOUT a "loadJSON should be given the same options" throw (Pitfall 5 — the index must be the same INDEX_OPTIONS instance mutated, not a fresh one).

    Write `tests/learned-t2-outranking.test.js` (LEARN-04/D-15): seed the learned store with a recipe for `slug+originA`; assert `FsbCapabilityCatalog.resolve(slug, originA)` returns `{tier:'T2', recipe, descriptor}` (learned-first) EVEN WHEN the REGISTRY also has a generic T1b for that slug (the learned outranks by resolve order). Assert `resolve(slug, originB)` does NOT return the learned recipe (origin-scoped). Drive `FsbCapabilityRouter.invoke` (with stubbed interpret/execute) and assert `case 'T2'` DISPATCHES via the declarative tier (interpret/execute called) when a learned recipe is attached, and falls back to the `RECIPE_LEARN_PENDING` stub when no learned recipe exists.

    Write `tests/learned-local-provenance-exempt.test.js` (D-09/HI-01): mirror recipe-signature-interpreter-hook.test.js. (1) Install the verifyEd25519 zero-call spy on FsbCapabilitySignature; call `interpretRecipe(localEnvelope, {}, {trustedProvenance:'local'})`; assert it binds (success:true) AND `verifyCalls === 0` (the 'local' exemption short-circuits before verify). (2) HI-01: build a recipe payload that self-asserts `provenance:'local'` in its OWN data but is passed with NO trusted vouch (opts omitted); assert it is STILL verified (a tampered/unsigned core is rejected with RECIPE_SIGNATURE_INVALID) — a payload cannot self-declare 'local' to dodge verify.

    Zero-framework convention; fail loud now. NO EMOJIS.
  </action>
  <acceptance_criteria>
    - All five suites exit NON-ZERO now (modules/edits absent).
    - `tests/learned-promote-after-replay.test.js` asserts BOTH the store-on-clean-replay and the discard-on-failed-replay branches AND that interpretRecipe received `{trustedProvenance:'local'}`.
    - `tests/learned-local-provenance-exempt.test.js` asserts the zero verifyEd25519 call on the 'local' path AND the payload-cannot-self-declare HI-01 case.
    - `tests/learned-t2-outranking.test.js` asserts resolve returns learned T2 over a generic T1b for the same slug, and the router dispatches vs stubs by learned presence.
    - No file contains an emoji.
  </acceptance_criteria>
  <verify>
    <automated>node tests/learned-promote-after-replay.test.js; test $? -ne 0 && echo RED-AS-EXPECTED</automated>
  </verify>
  <done>The five learned-recipe RED suites exist, fail loud, and encode the D-09/D-10/D-13/D-15/D-16 + LEARN-01/03/04 contracts including the HI-01 payload-cannot-self-declare rule.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the recipe-path-guard allowlist, wire all nine suites into the test chain, finalize 31-VALIDATION.md</name>
  <files>scripts/verify-recipe-path-guard.mjs, package.json, .planning/phases/31-network-capture-discovery-recipe-synthesis-learned-recipes/31-VALIDATION.md</files>
  <read_first>
    - scripts/verify-recipe-path-guard.mjs:90-140 (RECIPE_PATH_ALLOWLIST + the "Phase 30 consent-policy-store does NOT match the capability-* glob, listed explicitly" precedent — the synthesizer + store names are the same non-glob case)
    - package.json:17 (the scripts.test chain; append after tests/agent-loop-iterator-guard.test.js or in the capability-*/consent-* cluster)
    - 31-VALIDATION.md (replace each `TBD` task ID with 31-01/02/03/04/05/06 per the requirement->plan mapping; set nyquist_compliant:true, wave_0_complete:true)
  </read_first>
  <action>
    In `scripts/verify-recipe-path-guard.mjs`, add two entries to `RECIPE_PATH_ALLOWLIST`: `'extension/utils/recipe-synthesizer.js'` and `'extension/utils/learned-recipe-store.js'`, with a comment matching the established Phase-30 precedent (these do NOT match the `capability-*.js` Check-4 glob, so they MUST be listed explicitly; Check 1 scans them for eval/new Function/import even in comments; registered ahead of creation — Check 1 skips an absent path via the existsSync pre-check and Check 4's drift check is not in play for a non-glob name). Do NOT add the redactor or network-capture modules (they are not recipe-path-adjacent — they never bind/execute a recipe).

    In `package.json`, append the nine new suites to the `scripts.test` `&&` chain TAIL, adjacent to the existing `capability-*`/`consent-*` cluster (after `tests/agent-loop-iterator-guard.test.js` is acceptable). Order so the leaf-module suites (network-capture, network-capture-redaction, recipe-synthesizer, learned-recipe-store) precede the wiring suites (learned-search-add, learned-t2-outranking, learned-promote-after-replay, learned-local-provenance-exempt); network-capture-consent next to network-capture. Do NOT reorder or remove any existing entry.

    Finalize `31-VALIDATION.md`: replace every `TBD` Task ID in the Per-Task Verification Map with the real `31-NN` plan id per this distribution — DISC-02/04 capture rows -> 31-02; DISC-03/LEARN-02 redaction row -> 31-02; LEARN-01 synthesizer + D-10 promote rows -> 31-03; D-13/D-16 store row -> 31-03; LEARN-03 search-add -> 31-05; LEARN-04/D-15 outranking -> 31-05; D-09 provenance -> 31-04; the guard row -> 31-01. Set `nyquist_compliant: true` and `wave_0_complete: true` in the frontmatter, and check the Validation Sign-Off boxes that now hold.
  </action>
  <acceptance_criteria>
    - `node scripts/verify-recipe-path-guard.mjs` exits 0 (PASS) — the two added allowlist entries are absent-on-disk now (Check 1 existsSync skip) and do not trip Check 4.
    - `grep -c "network-capture.test.js\|recipe-synthesizer.test.js\|learned-recipe-store.test.js\|learned-local-provenance-exempt.test.js" package.json` returns 4 (all wired).
    - `RECIPE_PATH_ALLOWLIST` contains both `recipe-synthesizer.js` and `learned-recipe-store.js`.
    - 31-VALIDATION.md has zero remaining `TBD` task IDs and `nyquist_compliant: true`.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/verify-recipe-path-guard.mjs && grep -q "recipe-synthesizer.js" scripts/verify-recipe-path-guard.mjs && grep -q "learned-recipe-store.test.js" package.json && echo WIRED</automated>
  </verify>
  <done>Both new recipe-path-adjacent modules are allowlisted (guard still PASS), all nine suites are in the test chain, and 31-VALIDATION.md is finalized with real task IDs and nyquist_compliant:true.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> CI guard | The new modules cross the recipe-path CI guard; an un-allowlisted recipe-path-adjacent module would fail CI closed (the gating constraint). |
| RED test fixtures -> downstream modules | The RED suites are the executable contract Waves 2-3 implement against; a too-weak assertion lets an insecure implementation pass. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01-CI | Tampering | recipe-synthesizer.js + learned-recipe-store.js on the recipe path | mitigate | Allowlist BOTH in `verify-recipe-path-guard.mjs` in THIS wave so Check 1 scans them eval-free; the guard fails closed if a later wave adds dynamic code (eval/new Function/import). |
| T-31-01-LEAK | Information Disclosure | the redaction RED test (network-capture-redaction.test.js) | mitigate | The security test stringifies the WHOLE redacted artifact and asserts NO auth substring / NO body / NO query survives — it is the executable guarantee that the Wave-2 redactor cannot leak (DISC-03/LEARN-02). |
| T-31-01-PROV | Tampering / Spoofing | the 'local' provenance RED test | mitigate | The exemption suite asserts a payload self-declaring `provenance:'local'` with no trusted vouch is STILL verified (HI-01) — encoding the rule before the Wave-2 edit lands. |
| T-31-01-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase (RESEARCH Package Legitimacy Audit: no installs). No supply-chain checkpoint required. |
</threat_model>

<verification>
- `npm test` runs (the new RED suites are in the chain) and the Phase-31 suites are RED (modules absent), every prior suite still GREEN. (Run before Wave 2 to confirm the chain wiring; the RED state is expected.)
- `node scripts/verify-recipe-path-guard.mjs` -> PASS (allowlist extended, no on-disk recipe-path file missing).
</verification>

<success_criteria>
- Nine RED suites + the shared CDP event-driver stub exist and fail loud.
- The two recipe-path-adjacent modules are on RECIPE_PATH_ALLOWLIST; the guard PASSES.
- All nine suites are wired into the package.json test chain tail.
- 31-VALIDATION.md is finalized (no TBD task IDs; nyquist_compliant:true).
- No emojis in any created/modified file.
</success_criteria>

<output>
Create `.planning/phases/31-network-capture-discovery-recipe-synthesis-learned-recipes/31-01-SUMMARY.md` when done.
</output>
