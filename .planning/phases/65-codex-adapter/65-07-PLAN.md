---
phase: 65-codex-adapter
plan: "07"
type: execute
wave: 7
depends_on:
  - "65-06"
files_modified:
  - extension/utils/delegation-controller.js
  - extension/utils/delegation-event-store.js
  - extension/ui/delegation-feed.js
  - extension/ui/sidepanel.js
  - extension/ui/sidepanel.css
  - tests/delegation-controller.test.js
  - tests/delegation-event-store.test.js
  - tests/delegation-sidepanel-ui.test.js
  - tests/provider-parity.test.js
  - tests/providers-panel-ui.test.js
autonomous: true
requirements:
  - MULTI-04
  - MULTI-05
  - MULTI-06
must_haves:
  truths:
    - "Real controller-produced Claude Code, OpenCode, and Codex snapshots retain non-null profileVersion internally while no adapter renders a visible Profile term or value."
    - "Codex ChatGPT and API-key runs persist the accepted auth/billing identity through streaming, terminal settlement, and hydration without consulting current settings."
    - "Completed Codex summaries show tokens, turns, duration, and the exact accepted billing caption while `usd` remains null and no dollar formatter runs."
    - "Every shared delegated action, including fixed delegated Stop and <=350px full-width actions, is at least 44px in each dimension required by its shape."
    - "Feed, focus, live-region, light/dark/forced-colors/reduced-motion, narrow-layout, and hydration-silence contracts remain provider-neutral."
  artifacts:
    - path: "extension/ui/delegation-feed.js"
      provides: "Shared accepted-identity billing summary with no visible Profile row"
    - path: "extension/ui/sidepanel.css"
      provides: "Minimum 44px delegated action targets in normal and narrow layouts"
    - path: "tests/delegation-sidepanel-ui.test.js"
      provides: "Three-provider real-profile DOM parity, billing, terminal, accessibility, and target-size contracts"
  key_links:
    - from: "extension/utils/delegation-event-store.js"
      to: "extension/ui/delegation-feed.js"
      via: "persisted accepted authState/billingKind with usd null"
      pattern: "authState.*billingKind.*usd"
    - from: "extension/utils/delegation-controller.js"
      to: "extension/ui/sidepanel.js"
      via: "real controller snapshots and silent hydration"
      pattern: "profileVersion"
    - from: "extension/ui/sidepanel.css"
      to: "tests/delegation-sidepanel-ui.test.js"
      via: "normal and narrow delegated target-size source assertions"
      pattern: "44px"
---

<objective>
Close durable Codex run presentation and the shared Phase 64 UI advisories without a provider-specific renderer.

Purpose: Accepted auth/billing truth must survive rehydration and render honestly, while internal compatibility profiles and undersized controls remain invisible/fixed across every adapter.
Output: Provider-neutral durable Codex summaries, no visible Profile row, real-profile parity fixtures, and 44px delegated controls with preserved accessibility/theme behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/65-codex-adapter/65-CONTEXT.md
@.planning/phases/65-codex-adapter/65-RESEARCH.md
@.planning/phases/65-codex-adapter/65-UI-SPEC.md
@.planning/phases/65-codex-adapter/65-VALIDATION.md
@.planning/phases/65-codex-adapter/65-06-SUMMARY.md
@extension/utils/delegation-controller.js
@extension/utils/delegation-event-store.js
@extension/ui/delegation-feed.js
@extension/ui/sidepanel.js
@extension/ui/sidepanel.css

<contracts>
- Accepted run identity is exactly providerId, label, profileVersion, authState, billingKind and is immutable through hydration.
- Codex `usd` is always null for both billing modes; visible captions come from accepted identity, not current provider metadata.
- Profile presentation is removed generically while profileVersion remains required internally.
- UI changes reuse the existing DOM/classes; no Codex branch, component, renderer, or markup row is permitted.
</contracts>
</context>

<tasks>

<task id="65-07-01" type="auto">
  <name>Render durable Codex identity and honest summaries without a visible Profile row</name>
  <files>extension/utils/delegation-controller.js, extension/utils/delegation-event-store.js, extension/ui/delegation-feed.js, extension/ui/sidepanel.js, tests/delegation-controller.test.js, tests/delegation-event-store.test.js, tests/delegation-sidepanel-ui.test.js, tests/provider-parity.test.js</files>
  <read_first>65-UI-SPEC.md Delegated side panel, copywriting, consent/lifecycle, feed/summary, accessibility, and security locks; 65-01 and 65-06 summaries for accepted identity; current controller/store init schema, feed `_definition`/summary rendering, side-panel challenge recovery, and controller-produced parity fixture helper</read_first>
  <action>Complete any schema migration needed for the exact five-field init/run identity, deriving legacy identity only for existing Claude/OpenCode records and never fabricating Codex auth/billing. Hydrate and render from the persisted accepted identity without current metadata re-derivation; reject provider/auth/billing/profile drift, accessors, prototypes, symbols, extra keys, and event-provided USD. Keep Codex `usd:null` in ChatGPT and API-key modes. In the shared feed remove only `_definition(initList, 'Profile', ...)` so every adapter retains internal non-null profileVersion but no visible/orphaned term/value. Render exact accepted billing captions, tokens, turns, duration, and existing tool breakdown through the generic summary path; never invoke the generic dollar formatter for Codex API-key billing. On immediate auth mismatch, clear stale consent and render the approved refresh/review recovery rather than redisplaying reusable consent. Replace profile-null test fixtures with real controller snapshots for all three adapters and assert identical provider-neutral DOM shape, silent hydration, bounded native-data absence, and no success before the authoritative terminal barrier. Commit this task atomically.</action>
  <acceptance_criteria>All three adapters round-trip real non-null internal profiles with no visible Profile row, Codex summaries preserve accepted auth/billing with USD null and exact captions across hydration, hostile/drifted identities fail closed, and no Codex-specific renderer or premature success exists.</acceptance_criteria>
  <verify>
    <automated>node tests/delegation-controller.test.js --section codex-accepted-identity &amp;&amp; node tests/delegation-event-store.test.js --section codex-accepted-identity &amp;&amp; node tests/delegation-sidepanel-ui.test.js --section codex-shared-feed &amp;&amp; node tests/provider-parity.test.js --section delegated-agent-parity</automated>
  </verify>
  <done>The acceptance criteria hold, all focused suites pass, and the task is committed atomically.</done>
</task>

<task id="65-07-02" type="auto">
  <name>Raise every shared delegated action target to 44px and preserve responsive accessibility contracts</name>
  <files>extension/ui/sidepanel.css, tests/delegation-sidepanel-ui.test.js, tests/providers-panel-ui.test.js</files>
  <read_first>65-UI-SPEC.md Spacing Scale, Responsive/Theme/Motion, Accessibility, and deterministic verification items 8-10; Phase 64 UI review advisories; existing `.delegation-action`, fixed `.stop-btn[data-delegation-action="stop"]`, <=350px rules, forced-colors, reduced-motion, and focus-visible source tests</read_first>
  <action>Set every shared `.delegation-action` minimum height to at least 44px without changing padding, labels, DOM order, or focus order. Add a narrowly scoped delegated-state override making fixed `.stop-btn[data-delegation-action="stop"]` at least 44px by 44px while leaving unrelated composer behavior unchanged. Preserve <=350px full-width stacking with the same minimum height and no overlap. Update source/DOM contracts to cover Allow/Back, recovery actions, Take control, Resume with agent, in-card Stop, fixed Stop, disabled/pending semantics, visible focus, keyboard order, single live announcements, hydration silence, light/dark tokens, forced-colors redundancy, reduced-motion suppression, <=640px provider wrapping, and <=350px delegated layout. Update the intentional CSS hash only; keep visible HTML/source hashes unchanged where the established test contract expects it. Commit this task atomically.</action>
  <acceptance_criteria>Every delegated rectangular action is at least 44px high, fixed delegated Stop is at least 44px square, narrow layouts retain full-width nonoverlapping controls, and all focus/live-region/theme/forced-colors/reduced-motion/provider-row contracts remain green without markup or provider branches.</acceptance_criteria>
  <verify>
    <automated>node tests/delegation-sidepanel-ui.test.js --section delegated-targets-and-a11y &amp;&amp; node tests/providers-panel-ui.test.js --section responsive-accessibility</automated>
  </verify>
  <done>The acceptance criteria hold, both UI suites pass, and the task is committed atomically.</done>
</task>

</tasks>

<threat_model>
  <block_on>HIGH,CRITICAL</block_on>
  <trust_boundaries>
    <boundary id="B65-07-A">Persisted accepted identity re-enters controller and DOM after service-worker eviction.</boundary>
    <boundary id="B65-07-B">Normalized native lifecycle becomes visible feed and billing summary.</boundary>
    <boundary id="B65-07-C">Shared CSS controls mediate the human kill switch.</boundary>
  </trust_boundaries>
  <threat id="T65-02" category="Information Disclosure" component="feed presentation" severity="CRITICAL" disposition="mitigate"><mitigation>Render only normalized safe identity/events; native status, key, profile/version, reasoning, args/results remain absent.</mitigation><verification>DOM/state sentinel scans and real-profile no-Profile assertions.</verification></threat>
  <threat id="T65-06" category="Information Disclosure" component="native lifecycle presentation" severity="HIGH" disposition="mitigate"><mitigation>Shared feed accepts only normalized messages/tool pairs/authoritative terminal and never reasoning/todo/native errors.</mitigation><verification>Forbidden-event, drift, and raw-content UI negatives.</verification></threat>
  <threat id="T65-08" category="Tampering" component="visible terminal" severity="CRITICAL" disposition="mitigate"><mitigation>No result/summary before daemon-authoritative settlement.</mitigation><verification>Nonzero, signal, missing candidate, cleanup, and unsettled-tree UI cases.</verification></threat>
  <threat id="T65-09" category="Tampering" component="hydrated billing" severity="HIGH" disposition="mitigate"><mitigation>Persist exact accepted auth/billing; reject drift; force USD null; never use current static metadata.</mitigation><verification>Reload, cross-field mutation, dollar-formatter, and event-USD negatives.</verification></threat>
  <threat id="T65-10" category="Elevation of Privilege" component="shared UI" severity="HIGH" disposition="mitigate"><mitigation>Use one provider-neutral DOM/rendering path and preserve the explicit 44px human stop control.</mitigation><verification>No Codex branch/markup plus focus/target-size/parity tests.</verification></threat>
</threat_model>

<verification>
- All tests are deterministic source/VM/DOM checks; no genuine browser, screenshot, account, or assistive technology is used.
- Real accepted identity, not nulled test data, drives three-adapter parity.
- CSS and DOM changes close both Phase 64 advisories without changing provider markup/order.
</verification>

<success_criteria>
- Codex uses the same durable feed and summary experience as Claude Code/OpenCode with honest auth-specific billing and no USD.
- Profile compatibility remains internal and all shared delegated actions meet the approved target size.
- Accessibility, theme, motion, responsive, focus, announcement, and hydration contracts remain intact.
</success_criteria>

<output>
Create `.planning/phases/65-codex-adapter/65-07-SUMMARY.md` when complete.
</output>
