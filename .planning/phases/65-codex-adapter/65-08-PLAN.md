---
phase: 65-codex-adapter
plan: "08"
type: execute
wave: 8
depends_on:
  - "65-07"
files_modified:
  - .planning/phases/65-codex-adapter/65-HUMAN-UAT.md
  - .planning/phases/65-codex-adapter/65-VALIDATION.md
  - scripts/run-phase65-full-tests.mjs
  - scripts/run-phase64-full-tests.mjs
  - tests/phase65-full-tests-harness.test.js
  - tests/phase64-full-tests-harness.test.js
  - tests/delegation-phase-contract.test.js
  - tests/agent-provider-forbidden-flags.test.js
  - package.json
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - MULTI-04
  - MULTI-05
  - MULTI-06
must_haves:
  truths:
    - "Exactly eight Phase 65 plans and sixteen unique implementation tasks map every requirement, accepted decision, UI lock, and T65-01 through T65-12 to deterministic evidence."
    - "Exactly three Phase 65 UAT rows remain unchecked human_needed, pending, and evidence-empty; no automated result can promote them."
    - "One preservation-safe Phase 65 runner executes the focused adapter/authority/persistence/UI/source matrix and guarded root baseline while restoring dirty, staged, untracked, generated, index, mode, and symlink state on success, failure, spawn error, and signal."
    - "Root and CI contain the Codex adapter/harness exactly once in protected order, retain one Linux root invocation, and coordinate the former Phase 64 CI-name assertion."
    - "The final validation map has one row per task, no sampling gap, exact requirement/threat ownership, and no live/paid/browser/account/accessibility claim."
  artifacts:
    - path: ".planning/phases/65-codex-adapter/65-HUMAN-UAT.md"
      provides: "Exact three-row pending milestone-end human evidence ledger"
    - path: "scripts/run-phase65-full-tests.mjs"
      provides: "Authoritative workspace-preserving Phase 65 automated gate"
    - path: "tests/phase65-full-tests-harness.test.js"
      provides: "Success/failure/spawn-error/signal preservation and command-order proof"
    - path: ".planning/phases/65-codex-adapter/65-VALIDATION.md"
      provides: "Exact 8-plan/16-task Nyquist, requirement, decision, and threat map"
  key_links:
    - from: "scripts/run-phase65-full-tests.mjs"
      to: "package.json"
      via: "exact protected root command and Phase 65 harness slots"
      pattern: "phase65-full-tests-harness|mcp-codex-adapter"
    - from: "scripts/run-phase65-full-tests.mjs"
      to: ".github/workflows/ci.yml"
      via: "one sole Linux root invocation with coordinated Phase 65 step name"
      pattern: "Phase 65 Codex contract"
    - from: "tests/delegation-phase-contract.test.js"
      to: ".planning/phases/65-codex-adapter/65-VALIDATION.md"
      via: "exact plan/task/requirement/threat/UAT coverage"
      pattern: "phase65-validation"
---

<objective>
Create the preservation-safe automated closure gate and honest pending human-evidence ledger for Phase 65.

Purpose: Codex cannot be declared complete unless every native, authority, persistence, UI, provenance, and workspace-safety contract runs in protected order while genuine external evidence stays explicitly pending.
Output: Exact UAT/validation contracts, focused/full runner and harness, coordinated root/CI wiring, and blocking source/security coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/65-codex-adapter/65-CONTEXT.md
@.planning/phases/65-codex-adapter/65-RESEARCH.md
@.planning/phases/65-codex-adapter/65-UI-SPEC.md
@.planning/phases/65-codex-adapter/65-VALIDATION.md
@.planning/phases/65-codex-adapter/65-07-SUMMARY.md
@scripts/run-phase64-full-tests.mjs
@tests/phase64-full-tests-harness.test.js
@tests/delegation-phase-contract.test.js
@package.json
@.github/workflows/ci.yml

<contracts>
- Phase graph is exactly 8 plans and 16 tasks: 01(2), 02(2), 03(2), 04(3), 05(1), 06(2), 07(2), 08(2).
- The atomic exposure task is exactly 65-05-01 and remains one commit.
- Human UAT is exactly UAT65-01, UAT65-02, UAT65-03 with unchecked/human_needed/pending/evidence-empty state.
- The local full gate must preserve the user's pre-existing generated-file modifications and old planning deletions; it may never restore unrelated files from Git.
</contracts>
</context>

<tasks>

<task id="65-08-01" type="auto">
  <name>Lock exact Phase 65 validation coverage and the three pending human-UAT rows</name>
  <files>.planning/phases/65-codex-adapter/65-HUMAN-UAT.md, .planning/phases/65-codex-adapter/65-VALIDATION.md, tests/delegation-phase-contract.test.js, tests/agent-provider-forbidden-flags.test.js</files>
  <read_first>65-CONTEXT.md exact UAT/automated closure decisions; 65-RESEARCH.md Validation Architecture, Security Domain, and T65-01..12; 65-UI-SPEC.md Deferred Human Evidence; all eight Phase 65 PLAN files; Phase 64 UAT/validation and delegation-phase-contract formats</read_first>
  <action>Create `65-HUMAN-UAT.md` with exactly UAT65-01 genuine three-state auth matrix, UAT65-02 genuine Codex-to-browser/cancellation/cleanup/summary, and UAT65-03 keyboard/screen-reader/theme/motion/zoom/narrow/44px/no-Profile evidence. Every row must be unchecked, `human_needed`, `pending`, and evidence-empty, with sanitization constraints and milestone-end sweep routing. Verify the already-approved 16-task Nyquist map against the executed plans and record task evidence/status without changing its command, requirement, decision, or T65 ownership. Add source gates for the eight-plan graph, sole atomic 65-05-01 exposure, forbidden flags/authority, schema-derived/liveCapturePending provenance, exact copy/USD null/no-Profile/44px locks, and zero automated UAT promotion. The contract test must fail on any missing/duplicate task, requirement, threat, decision, command, or manual row. Commit this task atomically.</action>
  <acceptance_criteria>The pre-execution validation design still enumerates exactly 8 plans/16 tasks once with MULTI-04/05/06 and T65-01..12 fully owned, execution evidence is recorded without weakening it, source/security locks are mechanical, and the UAT ledger contains exactly three unchecked human_needed/pending/evidence-empty scenarios with no fabricated evidence.</acceptance_criteria>
  <verify>
    <automated>node tests/delegation-phase-contract.test.js --section phase65-validation &amp;&amp; node tests/delegation-phase-contract.test.js --section phase65-uat-ledger &amp;&amp; node tests/agent-provider-forbidden-flags.test.js</automated>
  </verify>
  <done>The acceptance criteria hold, all contract/source suites pass, and the task is committed atomically.</done>
</task>

<task id="65-08-02" type="auto">
  <name>Wire the preservation-safe Phase 65 focused, root, and CI closure gate</name>
  <files>.planning/phases/65-codex-adapter/65-VALIDATION.md, scripts/run-phase65-full-tests.mjs, scripts/run-phase64-full-tests.mjs, tests/phase65-full-tests-harness.test.js, tests/phase64-full-tests-harness.test.js, package.json, .github/workflows/ci.yml</files>
  <read_first>65-VALIDATION focused/full command inventory; 65-08-01 exact phase contract; Phase 64 runner/harness preservation implementation and its CI step-name assertions; root package test ordering around Phase 60/64 harness, MCP build, adapter/drift/supervisor, providers/UI; CI sole-root invocation</read_first>
  <action>Create `run-phase65-full-tests.mjs` from the established bounded argv-only preservation pattern. Its exact focused matrix must include the generic foundation sections, complete atomic Codex adapter/fixture/drift/compatibility/version/diagnostic/inventory suites, supervisor/runtime/recovery, safe storage/routing/consent/background, controller/store, Providers/delegated UI/parity, phase validation/UAT/source-security, extension validation, and a guarded root baseline without recursion. Snapshot and restore raw Git index bytes/mode/entries, dirty and staged bytes, symlinks/modes, generated MCP graph, untracked listing, and status on success, command failure, spawn error, SIGINT, and SIGTERM; reject injected commands outside bounded test mode. Add a Phase 65 harness covering every settlement path and exact command order/count. Add the Phase 65 harness and complete Codex adapter suite exactly once to root `npm test` in protected slots. Rename the sole Linux root CI step to `Phase 65 Codex contract (sole Linux root invocation)` while keeping exactly one `run: npm test`; update Phase 64 runner/harness assertions to accept the coordinated new name without weakening their preservation or root-order contract. Run the new harness and full runner, then update the final 65-VALIDATION row/status to green/complete while leaving all three UAT rows pending. Commit this task atomically.</action>
  <acceptance_criteria>The Phase 65 harness proves exact bounded commands and byte-for-byte workspace identity restoration on every settlement path; the full Phase 65 matrix, extension validation, guarded root baseline, source/security contracts, and sole CI invocation pass; validation records all 16 automated tasks green while exactly three UAT rows remain pending.</acceptance_criteria>
  <verify>
    <automated>node tests/phase65-full-tests-harness.test.js &amp;&amp; node scripts/run-phase65-full-tests.mjs</automated>
  </verify>
  <done>The acceptance criteria hold, both authoritative commands pass with workspace identity preserved, final validation is updated, and the task is committed atomically.</done>
</task>

</tasks>

<threat_model>
  <block_on>HIGH,CRITICAL</block_on>
  <trust_boundaries>
    <boundary id="B65-08-A">Planning claims and automated test output become phase-completion evidence.</boundary>
    <boundary id="B65-08-B">Build/root commands run inside a dirty shared workspace with user-owned generated and planning changes.</boundary>
    <boundary id="B65-08-C">Root/CI command wiring decides whether the complete Codex contract is enforced continuously.</boundary>
  </trust_boundaries>
  <threat id="T65-01" category="Information Disclosure" component="closure logs" severity="CRITICAL" disposition="mitigate"><mitigation>Run secret canaries/source scans and bounded output through the focused gate.</mitigation><verification>Adapter/auth/environment/source suites in the exact runner.</verification></threat>
  <threat id="T65-02" category="Information Disclosure" component="evidence artifacts" severity="CRITICAL" disposition="mitigate"><mitigation>UAT/validation/log contracts forbid raw status, credentials, task, native payloads, endpoint, and paths.</mitigation><verification>Phase-contract and canary scans.</verification></threat>
  <threat id="T65-03" category="Spoofing" component="start authority evidence" severity="CRITICAL" disposition="mitigate"><mitigation>Include consent/reprobe/background mismatch matrix in the authoritative runner.</mitigation><verification>Exact command occurrence and green suites.</verification></threat>
  <threat id="T65-04" category="Elevation of Privilege" component="MCP authority" severity="CRITICAL" disposition="mitigate"><mitigation>Include endpoint/effective-roster/native-authority isolation tests and source gates.</mitigation><verification>Atomic adapter, supervisor, runtime, and forbidden-source suites.</verification></threat>
  <threat id="T65-05" category="Elevation of Privilege" component="inherited tools/config" severity="CRITICAL" disposition="mitigate"><mitigation>Pin exact profile/features/flags and fail runner on drift.</mitigation><verification>Codex adapter and provider-forbidden-flags suites.</verification></threat>
  <threat id="T65-06" category="Information Disclosure" component="native drift" severity="HIGH" disposition="mitigate"><mitigation>Run full native negative corpus and safe UI/state scans.</mitigation><verification>Parser/fixture/drift/UI commands.</verification></threat>
  <threat id="T65-07" category="Tampering" component="MCP event identity" severity="HIGH" disposition="mitigate"><mitigation>Run exact parser/order/diagnostic tests.</mitigation><verification>Adapter, drift, and diagnostic commands.</verification></threat>
  <threat id="T65-08" category="Tampering" component="terminal evidence" severity="CRITICAL" disposition="mitigate"><mitigation>Run supervisor/tree/runtime and UI premature-success matrices.</mitigation><verification>Supervisor/recovery/controller/feed commands.</verification></threat>
  <threat id="T65-09" category="Tampering" component="billing evidence" severity="HIGH" disposition="mitigate"><mitigation>Run immutable store/controller/UI USD-null matrix.</mitigation><verification>Store/controller/provider parity/UI commands.</verification></threat>
  <threat id="T65-10" category="Elevation of Privilege" component="selection authority" severity="HIGH" disposition="mitigate"><mitigation>Run provider-free request, compatibility-observational, refresh, and API-provider invariance tests.</mitigation><verification>Routing/consent/background/Providers commands.</verification></threat>
  <threat id="T65-11" category="Tampering" component="provenance" severity="HIGH" disposition="mitigate"><mitigation>Phase contract pins schema-derived 0.142.5/live pending and 0.144.6 degraded.</mitigation><verification>Version/manifest/phase-contract suites.</verification></threat>
  <threat id="T65-12" category="Elevation of Privilege" component="partial exposure and CI omission" severity="HIGH" disposition="mitigate"><mitigation>Atomic sentinel plus exact root/CI command occurrence and ordering.</mitigation><verification>Phase contract, runner self-check, root, and CI source assertions.</verification></threat>
</threat_model>

<verification>
- The authoritative runner never calls a live Codex model, browser, login/logout, credential store, or assistive technology.
- Workspace preservation is verified on success, nonzero child exit, spawn error, SIGINT, and SIGTERM.
- Exactly three genuine UAT rows remain pending after every automated gate passes.
</verification>

<success_criteria>
- Phase 65 has complete deterministic native, authority, persistence, UI, provenance, security, root, and CI coverage.
- The dirty shared workspace is byte-for-byte preserved around every build/root path.
- Automated closure is honest and cannot substitute for the three deferred external scenarios.
</success_criteria>

<output>
Create `.planning/phases/65-codex-adapter/65-08-SUMMARY.md` when complete.
</output>
