---
phase: 65-codex-adapter
plan: "03"
type: execute
wave: 3
depends_on:
  - "65-02"
files_modified:
  - mcp/src/agent-providers/accepted-identity.ts
  - mcp/src/agent-providers/spawn-supervisor.ts
  - extension/background.js
  - tests/mcp-spawn-supervisor.test.js
  - tests/mcp-bridge-background-dispatch.test.js
  - tests/delegation-routing.test.js
autonomous: true
requirements:
  - MULTI-05
must_haves:
  truths:
    - "The authenticated delegate.start request carries only the exact identity returned by one-time consent consumption, never side-panel authority."
    - "The supervisor derives an identity from its own executeRun detection and compares all five fields before runtime preparation, journal append, child creation, or task stdin."
    - "The daemon started event echoes the exact identity that authorized process creation, and background compares it again before controller persistence or task UI creation."
    - "Any identity mismatch fails once before task stdin, controller creation, journal append, or browser-visible run state."
    - "Client messages still cannot select a provider or provide auth/billing authority."
    - "The production agent roster remains Claude Code/OpenCode only."
  artifacts:
    - path: "mcp/src/agent-providers/accepted-identity.ts"
      provides: "Daemon-owned closed accepted-identity validation and detection projection"
    - path: "mcp/src/agent-providers/spawn-supervisor.ts"
      provides: "Authenticated start binding checked before every runtime side effect and echoed on acceptance"
    - path: "extension/background.js"
      provides: "Consumed challenge identity transport and daemon-echo comparison before run persistence"
  key_links:
    - from: "extension/utils/delegation-consent.js"
      to: "mcp/src/agent-providers/spawn-supervisor.ts"
      via: "background-only authenticated delegate.start acceptedIdentity"
      pattern: "acceptedIdentity"
    - from: "mcp/src/agent-providers/spawn-supervisor.ts"
      to: "extension/background.js"
      via: "validated delegation.started acceptedIdentity echo"
      pattern: "acceptedIdentity"
    - from: "extension/background.js"
      to: "extension/utils/delegation-controller.js"
      via: "controller starts only after exact identity equality"
      pattern: "providerId.*authState.*billingKind"
---

<objective>
Complete the generic consent-to-supervisor immediate-start identity boundary while the production roster is still two adapters.

Purpose: The supervisor must reject changed auth/billing before it can prepare runtime state or write task stdin, and background must persist only the echoed daemon-confirmed identity.
Output: Closed daemon identity derivation, authenticated start binding, exact daemon echo, browser-side comparison, and fail-before-side-effect TOCTOU tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/65-codex-adapter/65-CONTEXT.md
@.planning/phases/65-codex-adapter/65-RESEARCH.md
@.planning/phases/65-codex-adapter/65-02-SUMMARY.md
@mcp/src/agent-providers/adapter.ts
@mcp/src/agent-providers/spawn-supervisor.ts
@extension/utils/delegation-consent.js
@extension/background.js
@tests/mcp-bridge-background-dispatch.test.js
@tests/delegation-routing.test.js
</context>

<tasks>

<task id="65-03-01" type="auto">
  <name>Define the daemon-owned accepted-identity validator and detection projection</name>
  <files>mcp/src/agent-providers/accepted-identity.ts, tests/mcp-spawn-supervisor.test.js</files>
  <read_first>65-RESEARCH.md Pattern 6 and Architectural Responsibility Map; 65-02-SUMMARY.md consent-bound identity contract; adapter.ts AdapterDetection contract; exact Claude Code/OpenCode compatibility labels/profile versions and current auth behavior</read_first>
  <action>Create one daemon-owned exact five-field accepted-identity type, hostile-record-safe validator, equality helper, and `acceptedIdentityFromDetection(adapterId, detection)` projection. Require plain own enumerable data with exactly providerId, label, profileVersion, authState, billingKind; reject symbols, accessors, prototypes, extras, partials, coercion, invalid profile versions, and disallowed pairs. For the still-two-provider roster, derive Claude Code as unknown/subscription and OpenCode as unknown/unknown while taking profileVersion only from the validated detection that will authorize execution. Mirror the extension policy mechanically in tests without importing browser authority. Do not add Codex, change the registry, or wire the start transport in this task. Commit this foundation atomically.</action>
  <acceptance_criteria>The daemon can derive and validate one exact immutable identity only from its own current detection for either shipped provider, with exact parity to the extension contract and no Codex production exposure.</acceptance_criteria>
  <verify>
    <automated>node scripts/run-mcp-build-preserving-workspace.mjs --commands-json '[["node","tests/mcp-spawn-supervisor.test.js","--section","accepted-identity-foundation"]]'</automated>
  </verify>
  <done>The acceptance criteria hold, the preservation-wrapped compiled test passes, and the task is committed atomically.</done>
</task>

<task id="65-03-02" type="auto">
  <name>Bind consumed consent to supervisor detection before every start side effect</name>
  <files>mcp/src/agent-providers/spawn-supervisor.ts, extension/background.js, tests/mcp-spawn-supervisor.test.js, tests/mcp-bridge-background-dispatch.test.js, tests/delegation-routing.test.js</files>
  <read_first>65-CONTEXT.md fail-before-spawn/stdin identity mismatch decision; 65-RESEARCH.md Pattern 6/T65-03; 65-02-SUMMARY.md consumed challenge return contract; 65-03-01 daemon identity helpers; existing authenticated ext:request parsing, executeRun detection/runtime preparation/journal/spawn/stdin order, background preflight/consent consumption/controller creation, and failure routing</read_first>
  <action>After one-time challenge consumption, exact-compare its stored acceptedIdentity with the second background preflight and send that identity—not any side-panel field—in the exact authenticated `delegate.start` payload. Reject provider/auth/billing/profile fields on the side-panel request as before. In spawn-supervisor, require and hostile-record-validate that wire identity, select the adapter only from its providerId, retain it on the run, derive the current daemon identity from the exact `executeRun` detection, and exact-compare all five fields immediately after detection but before runtime ids/paths, buildSpawn, prepareRun/journal, process spawn, or task stdin. Any mismatch must settle once as a bounded start rejection with zero runtime preparation, journal, child, event, or stdin. On success, echo that exact derived frozen identity in `delegation.started`; background exact-compares the echo with the consumed identity before controller/run/feed persistence. Add one-field mutation, hostile-record, forged-wire, and ordering spies proving every mismatch has zero side effects and no automatic replay. Preserve existing Claude Code/OpenCode success paths. Commit this cross-boundary switch atomically.</action>
  <acceptance_criteria>Only an exact consent-bound identity matching the supervisor's current detection can cross the pre-runtime barrier; every mismatch has zero journal, child, stdin, controller, or visible state; the echoed identity matches before persistence; side-panel requests remain provider-free.</acceptance_criteria>
  <verify>
    <automated>node scripts/run-mcp-build-preserving-workspace.mjs --commands-json '[["node","tests/mcp-spawn-supervisor.test.js","--section","consent-bound-start"],["node","tests/mcp-bridge-background-dispatch.test.js","--section","accepted-identity"],["node","tests/delegation-routing.test.js","--section","immediate-start-identity"]]'</automated>
  </verify>
  <done>The acceptance criteria hold, the focused suite passes, and the task is committed atomically.</done>
</task>

</tasks>

<threat_model>
  <block_on>HIGH,CRITICAL</block_on>
  <trust_boundaries>
    <boundary id="B65-03-A">Consent-bound background authority crosses the authenticated native bridge into the spawn supervisor.</boundary>
    <boundary id="B65-03-B">Daemon start evidence crosses back into extension background state and becomes a persisted controller run.</boundary>
  </trust_boundaries>
  <threat id="T65-03" category="Spoofing" component="immediate start identity" severity="CRITICAL" disposition="mitigate"><mitigation>Exact five-field supervisor comparison before runtime preparation, journal, child, or stdin, plus echoed comparison before controller/UI state.</mitigation><verification>One-field-at-a-time request/detection/echo mismatches with zero-side-effect ordering spies.</verification></threat>
  <threat id="T65-09" category="Tampering" component="accepted run" severity="HIGH" disposition="mitigate"><mitigation>Persist only the exact daemon-confirmed frozen identity.</mitigation><verification>No state on mismatch and post-accept mutation tests.</verification></threat>
  <threat id="T65-10" category="Elevation of Privilege" component="routing" severity="HIGH" disposition="mitigate"><mitigation>Side-panel messages remain provider/auth/billing-free; only background-consumed consent enters the authenticated daemon request, and compatibility never grants start.</mitigation><verification>Exact request-shape, consumed-record provenance, and saved-settings authority tests.</verification></threat>
</threat_model>

<verification>
- The MCP consumer runs only through `scripts/run-mcp-build-preserving-workspace.mjs`.
- Supervisor ordering tests prove mismatch happens before runtime preparation, journal, child, event, and stdin; background tests prove no visible/persisted run state.
- The shipped roster is still exactly Claude Code/OpenCode after Wave 3.
</verification>

<success_criteria>
- The generic consent-to-daemon identity chain is complete before runtime or Codex work.
- A later auth re-probe can fail safely without relabeling an accepted run.
</success_criteria>

<output>
Create `.planning/phases/65-codex-adapter/65-03-SUMMARY.md` when complete.
</output>
