---
phase: 65-codex-adapter
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - extension/utils/delegation-providers.js
  - extension/utils/delegation-controller.js
  - extension/utils/delegation-event-store.js
  - tests/provider-parity.test.js
  - tests/delegation-controller.test.js
  - tests/delegation-event-store.test.js
autonomous: true
requirements:
  - MULTI-05
must_haves:
  truths:
    - "Accepted delegated runs carry one immutable exact identity with providerId, label, profileVersion, authState, and billingKind."
    - "A provider/auth pair resolves through one closed provider-neutral auth-to-billing contract; Claude Code and OpenCode retain their current unknown-auth behavior."
    - "Events, current settings, and hydration cannot alter an accepted run's provider, auth, billing, label, or profile."
    - "The shipped agent roster remains exactly Claude Code and OpenCode throughout this plan; Codex production exposure is absent."
  artifacts:
    - path: "extension/utils/delegation-providers.js"
      provides: "Closed auth, billing, and accepted-identity validation helpers"
    - path: "extension/utils/delegation-controller.js"
      provides: "Controller snapshots carrying the exact five-field accepted identity"
    - path: "extension/utils/delegation-event-store.js"
      provides: "Durable exact validation and hydration of accepted identity"
  key_links:
    - from: "extension/utils/delegation-providers.js"
      to: "extension/utils/delegation-controller.js"
      via: "validateAcceptedAgentIdentity and resolveAgentBillingKind"
      pattern: "providerId.*label.*profileVersion.*authState.*billingKind"
    - from: "extension/utils/delegation-controller.js"
      to: "extension/utils/delegation-event-store.js"
      via: "append-before-fanout immutable run snapshots"
      pattern: "acceptedIdentity"
---

<objective>
Create the provider-neutral accepted-auth/billing identity foundation without exposing Codex.

Purpose: Auth class and billing bucket must become accepted run authority before a provider with two legitimate billing modes can be promoted.
Output: One closed mapping/validator plus exact controller and durable-store support for the five-field identity, with current-provider regressions locked.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/65-codex-adapter/65-CONTEXT.md
@.planning/phases/65-codex-adapter/65-RESEARCH.md
@.planning/phases/65-codex-adapter/65-VALIDATION.md
@extension/utils/delegation-providers.js
@extension/utils/delegation-controller.js
@extension/utils/delegation-event-store.js
@tests/provider-parity.test.js

<contracts>
- Exact accepted identity keys, in order-neutral own-data validation: providerId, label, profileVersion, authState, billingKind.
- Closed safe auth vocabulary: chatgpt, api_key, unauthenticated, unknown.
- Closed billing vocabulary: subscription, api, unknown; no USD value is derived here.
- Current production roster at this boundary: claude-code and opencode only.
</contracts>
</context>

<tasks>

<task id="65-01-01" type="auto">
  <name>Define the closed provider-neutral auth, billing, and accepted-identity contract</name>
  <files>extension/utils/delegation-providers.js, tests/provider-parity.test.js</files>
  <read_first>65-CONTEXT.md Auth and Billing Truth plus Product Integration decisions; 65-RESEARCH.md Pattern 6 and T65-03/T65-09/T65-10; existing delegation-providers exports and provider-parity hostile-record cases</read_first>
  <action>Extend the shared provider helper with exact constants `AGENT_AUTH_STATES` and `AGENT_BILLING_KINDS`, one frozen metadata-owned allowed auth-to-billing table, `resolveAgentBillingKind(providerId, authState)`, and `validateAcceptedAgentIdentity(value)`. The validator must accept only own enumerable data properties on a plain null-or-Object-prototype record, require exactly `providerId`, `label`, `profileVersion`, `authState`, and `billingKind`, and reject extra keys, accessors, symbol keys, prototypes, coercion, and invalid provider/auth/billing combinations. Preserve the existing Claude Code subscription and OpenCode unknown billing results and their runnable unknown-auth behavior. Add mutation cases before changing the helper. Do not add `codex`, `CODEX_ADAPTER_ID`, a third shipped metadata row, or a USD mapping. Commit only this task's files as one atomic executor commit.</action>
  <acceptance_criteria>The helper exposes one exact identity validator and one closed metadata-driven auth-to-billing resolver; all hostile shapes fail closed; Claude Code and OpenCode produce byte-for-byte equivalent public metadata; no Codex production symbol or roster row appears.</acceptance_criteria>
  <verify>
    <automated>node tests/provider-parity.test.js --section accepted-identity-foundation</automated>
  </verify>
  <done>The acceptance criteria hold, the focused command passes, and the task is committed atomically.</done>
</task>

<task id="65-01-02" type="auto">
  <name>Persist and hydrate the exact five-field identity through controller and event store</name>
  <files>extension/utils/delegation-controller.js, extension/utils/delegation-event-store.js, tests/delegation-controller.test.js, tests/delegation-event-store.test.js</files>
  <read_first>65-CONTEXT.md immutable identity and real-snapshot decisions; 65-RESEARCH.md Pattern 6 and Runtime State Inventory; the validator created by 65-01-01; existing append-before-fanout, sequence, terminal, and hydration contracts</read_first>
  <action>Require a validated accepted identity when a run is accepted, clone/freeze it once, and carry it unchanged through controller snapshots, streamed event application, terminal settlement, persistence, and hydration. Store the five fields as the run's immutable provider identity and never reconstruct billing from current provider settings or an incoming event. Reject provider, label, profileVersion, authState, or billingKind drift; reject partial/legacy hostile records, extra keys, accessors, polluted prototypes, symbol-key payloads, and event attempts to introduce `usd`. Preserve existing sequence, exact-once terminal, and stale-run handling. Tests must use real non-null profileVersion values for both existing adapters and prove mutation after acceptance cannot affect snapshots. Do not expose a visible Profile definition and do not add Codex to the shipped roster. Commit this task as one atomic executor commit.</action>
  <acceptance_criteria>Controller and store round-trip the exact frozen five-field identity with non-null profile versions, reject every cross-field or hostile mutation, preserve existing provider behavior, and derive neither provider nor billing from current settings or events.</acceptance_criteria>
  <verify>
    <automated>node tests/delegation-controller.test.js --section accepted-identity-foundation &amp;&amp; node tests/delegation-event-store.test.js --section accepted-identity-foundation</automated>
  </verify>
  <done>The acceptance criteria hold, both focused suites pass, and the task is committed atomically.</done>
</task>

</tasks>

<threat_model>
  <block_on>HIGH,CRITICAL</block_on>
  <trust_boundaries>
    <boundary id="B65-01-A">Provider metadata becomes accepted billing authority.</boundary>
    <boundary id="B65-01-B">Untrusted event-store records re-enter controller/UI state during hydration.</boundary>
  </trust_boundaries>
  <threat id="T65-03" category="Tampering" component="accepted identity" severity="CRITICAL" disposition="mitigate"><mitigation>Bind auth and billing in one exact frozen identity instead of recomputing them.</mitigation><verification>Cross-field mutation and current-settings-drift tests.</verification></threat>
  <threat id="T65-09" category="Tampering" component="event persistence" severity="HIGH" disposition="mitigate"><mitigation>Exact own-data validation plus immutable five-field snapshots; no event-provided USD.</mitigation><verification>Accessor, prototype, extra-key, sequence, hydration, and billing-mutation negatives.</verification></threat>
  <threat id="T65-10" category="Elevation of Privilege" component="provider metadata" severity="HIGH" disposition="mitigate"><mitigation>Metadata maps allowed pairs but neither selects a provider nor authorizes start.</mitigation><verification>Provider-parity authority and roster tests.</verification></threat>
</threat_model>

<verification>
- Both focused suites pass without compiling MCP output.
- Source assertions prove the production roster remains Claude Code/OpenCode and accepted identity has exactly five fields.
- Every task produces one atomic commit and no checkpoint is introduced.
</verification>

<success_criteria>
- Auth/billing identity is canonical, immutable, exact, and durable before Codex promotion.
- Existing adapter behavior and all API-provider behavior remain unchanged.
- No Codex production exposure occurs in Wave 1.
</success_criteria>

<output>
Create `.planning/phases/65-codex-adapter/65-01-SUMMARY.md` when complete.
</output>
