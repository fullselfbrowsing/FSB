---
phase: 65-codex-adapter
plan: "02"
type: execute
wave: 2
depends_on:
  - "65-01"
files_modified:
  - extension/utils/delegation-preflight.js
  - extension/utils/delegation-consent.js
  - tests/delegation-routing.test.js
  - tests/delegation-consent.test.js
autonomous: true
requirements:
  - MULTI-05
must_haves:
  truths:
    - "Preflight produces one validated five-field provider identity from background-owned provider evidence."
    - "Consent binds the complete identity, not only provider id and task text."
    - "Provider/auth/billing/profile changes consume stale consent and require a fresh preflight."
    - "Side-panel preflight and consent requests remain provider-, auth-, and billing-free."
    - "Claude Code and OpenCode retain existing start behavior and remain the only shipped agent roster entries."
  artifacts:
    - path: "extension/utils/delegation-preflight.js"
      provides: "Provider-neutral preflight result carrying exact accepted identity"
    - path: "extension/utils/delegation-consent.js"
      provides: "Challenge binding over task and all five identity fields"
  key_links:
    - from: "extension/utils/delegation-preflight.js"
      to: "extension/utils/delegation-consent.js"
      via: "validated acceptedIdentity included in the challenge digest"
      pattern: "authState.*billingKind"
    - from: "tests/delegation-routing.test.js"
      to: "extension/utils/delegation-preflight.js"
      via: "provider-free client request assertions"
      pattern: "providerId|authState|billingKind"
---

<objective>
Bind provider-neutral auth and billing truth through preflight and consent before any Codex production symbol exists.

Purpose: A later immediate auth re-probe can only detect TOCTOU safely if the user-approved identity is exact and immutable.
Output: Exact preflight identity validation, five-field consent challenges, mismatch invalidation, and provider-free request regressions.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/65-codex-adapter/65-CONTEXT.md
@.planning/phases/65-codex-adapter/65-RESEARCH.md
@.planning/phases/65-codex-adapter/65-VALIDATION.md
@.planning/phases/65-codex-adapter/65-01-SUMMARY.md
@extension/utils/delegation-preflight.js
@extension/utils/delegation-consent.js
@tests/delegation-routing.test.js
@tests/delegation-consent.test.js
</context>

<tasks>

<task id="65-02-01" type="auto">
  <name>Make preflight establish one exact accepted provider identity</name>
  <files>extension/utils/delegation-preflight.js, tests/delegation-routing.test.js</files>
  <read_first>65-CONTEXT.md Auth and Billing Truth plus Product Integration decisions; 65-RESEARCH.md Pattern 6; 65-01-SUMMARY.md exact validators; existing provider-free routing and preflight result schemas</read_first>
  <action>Change the shared preflight result to accept only a `validateAcceptedAgentIdentity`-approved identity from background-owned saved-provider evidence. Determine runnable auth through the metadata-owned policy: preserve current Claude Code/OpenCode unknown-auth behavior, while allowing a later provider row to mark unauthenticated and unknown non-runnable. Keep compatibility observational and separate from selection/start authority. Reject missing, stale, malformed, partial, accessor, prototype, extra-key, or disallowed auth/billing evidence without creating an optimistic task, run, feed entry, or announcement. Extend request-shape negatives so side-panel send/preflight data contains no providerId, label, profileVersion, authState, billingKind, compatibility choice, or billing override. Do not add Codex metadata or a third shipped roster entry. Commit only this task as one atomic executor commit.</action>
  <acceptance_criteria>Preflight returns one exact immutable five-field identity from background authority, blocks invalid evidence before optimistic state, preserves existing providers, and proves all browser requests remain provider/auth/billing-free.</acceptance_criteria>
  <verify>
    <automated>node tests/delegation-routing.test.js --section accepted-identity-preflight</automated>
  </verify>
  <done>The acceptance criteria hold, the focused suite passes, and the task is committed atomically.</done>
</task>

<task id="65-02-02" type="auto">
  <name>Bind all five accepted identity fields into shared consent challenges</name>
  <files>extension/utils/delegation-consent.js, tests/delegation-consent.test.js</files>
  <read_first>65-CONTEXT.md immediate re-probe and consent decisions; 65-RESEARCH.md Pattern 6 and T65-03; 65-01-SUMMARY.md validator contract; existing consent challenge issuance, trust, expiry, and consumption paths</read_first>
  <action>Include the exact accepted identity in consent challenge creation, digest/comparison, trust evaluation, consumption, expiry, and retry handling. An otherwise identical task with a changed providerId, label, profileVersion, authState, or billingKind must not reuse consent; mismatches consume/clear stale authority and return a bounded provider-status-refresh outcome. Preserve task scoping, expiration, one-time consumption, existing trusted-provider semantics, and no-replay behavior, but ensure trust never bypasses identity change or non-runnable auth. Reject hostile accepted-identity records through the shared validator. Keep the production roster Claude Code/OpenCode only and commit the task as one atomic executor commit.</action>
  <acceptance_criteria>Consent is reusable only for the exact task and exact five-field identity; successful one-time consumption returns that stored validated identity for the authenticated daemon-start boundary; each single-field mutation, stale state, hostile record, replay, or expiry fails closed while current-provider behavior remains green.</acceptance_criteria>
  <verify>
    <automated>node tests/delegation-consent.test.js --section accepted-identity-binding</automated>
  </verify>
  <done>The acceptance criteria hold, the focused suite passes, and the task is committed atomically.</done>
</task>

</tasks>

<threat_model>
  <block_on>HIGH,CRITICAL</block_on>
  <trust_boundaries>
    <boundary id="B65-02-A">Background provider evidence crosses into preflight and user consent.</boundary>
    <boundary id="B65-02-B">Untrusted side-panel messages request a delegated start.</boundary>
  </trust_boundaries>
  <threat id="T65-03" category="Spoofing" component="consent identity" severity="CRITICAL" disposition="mitigate"><mitigation>Bind task plus all five accepted identity fields and invalidate every mismatch.</mitigation><verification>Single-field TOCTOU, replay, trust, expiry, and hostile-shape matrix.</verification></threat>
  <threat id="T65-09" category="Tampering" component="auth/billing challenge" severity="HIGH" disposition="mitigate"><mitigation>Validate exact allowed auth/billing pairs before challenge issuance and comparison.</mitigation><verification>Invalid-pair and mutation negatives.</verification></threat>
  <threat id="T65-10" category="Elevation of Privilege" component="browser request" severity="HIGH" disposition="mitigate"><mitigation>Keep provider selection in saved background settings and all client requests provider-free.</mitigation><verification>Exact request-key and compatibility-nonauthority assertions.</verification></threat>
</threat_model>

<verification>
- Focused preflight/routing and consent suites pass.
- No Codex production symbol, roster row, fixture, or registry change exists after Wave 2.
- No checkpoint or live provider process is used.
</verification>

<success_criteria>
- Consent is an exact auth/billing authority boundary suitable for immediate pre-spawn comparison.
- Compatibility remains observational and provider-free browser requests remain enforced.
</success_criteria>

<output>
Create `.planning/phases/65-codex-adapter/65-02-SUMMARY.md` when complete.
</output>
