---
phase: 65-codex-adapter
plan: "06"
type: execute
wave: 6
depends_on:
  - "65-05"
files_modified:
  - mcp/src/agent-providers/compatibility.ts
  - mcp/src/agent-providers/serve-delegation.ts
  - extension/utils/mcp-agent-providers.js
  - extension/utils/delegation-providers.js
  - extension/utils/delegation-preflight.js
  - extension/utils/delegation-consent.js
  - extension/background.js
  - extension/ui/providers-panel.js
  - extension/ui/options.js
  - tests/mcp-agent-providers-storage.test.js
  - tests/mcp-adapter-compatibility.test.js
  - tests/mcp-bridge-topology.test.js
  - tests/delegation-routing.test.js
  - tests/delegation-consent.test.js
  - tests/mcp-bridge-background-dispatch.test.js
  - tests/providers-panel-logic.test.js
  - tests/providers-panel-ui.test.js
autonomous: true
requirements:
  - MULTI-04
  - MULTI-05
  - MULTI-06
must_haves:
  truths:
    - "The safe daemon/browser roster is exactly Claude Code, OpenCode, Codex in stable order and contains only bounded compatibility plus the four-state auth classification."
    - "Fresh Codex chatgpt and api_key evidence is runnable with the matching canonical billing kind; unauthenticated and unknown fail before consent/start."
    - "Stale, malformed, missing, or failed auth evidence projects to unknown and never preserves an earlier runnable billing claim."
    - "Background-owned saved settings remain provider authority; side-panel preflight, consent, and start messages remain provider-, auth-, and billing-free."
    - "The existing third Providers row becomes functional without new markup, row order, recommendation behavior, API-provider mutation, or UI-side version parsing."
  artifacts:
    - path: "extension/utils/mcp-agent-providers.js"
      provides: "Exact three-row safe compatibility/auth snapshot and stale projection"
    - path: "extension/utils/delegation-providers.js"
      provides: "Canonical Codex auth-to-billing policy in the shared provider table"
    - path: "extension/ui/providers-panel.js"
      provides: "Existing third-row projection driven by safe evidence"
    - path: "extension/ui/options.js"
      provides: "Selected Codex Account/Auth and Billing facts without new markup"
  key_links:
    - from: "mcp/src/agent-providers/compatibility.ts"
      to: "extension/utils/mcp-agent-providers.js"
      via: "exact safe schema-v2 three-row compatibility/auth snapshot"
      pattern: "schemaVersion.*authState"
    - from: "extension/utils/mcp-agent-providers.js"
      to: "extension/utils/delegation-preflight.js"
      via: "fresh validated authState carried with compatibility evidence"
      pattern: "authState"
    - from: "extension/utils/delegation-providers.js"
      to: "extension/background.js"
      via: "canonical accepted identity and auth-to-billing resolution"
      pattern: "chatgpt.*subscription|api_key.*api"
    - from: "extension/ui/providers-panel.js"
      to: "extension/ui/options.js"
      via: "selected safe row drives shared Account/Auth and Billing details"
      pattern: "getAgentAuthDisplay|getBillingLabel"
---

<objective>
Promote Codex through the safe browser projection, authority chain, and existing Providers row.

Purpose: The complete native adapter must become selectable only through fresh bounded auth evidence and background-owned provider authority, with the billing bucket visible before start.
Output: Exact safe three-row storage, canonical Codex policy, fail-closed preflight/consent/background routing, and existing-row Account/Auth/Billing presentation.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/65-codex-adapter/65-CONTEXT.md
@.planning/phases/65-codex-adapter/65-RESEARCH.md
@.planning/phases/65-codex-adapter/65-UI-SPEC.md
@.planning/phases/65-codex-adapter/65-VALIDATION.md
@.planning/phases/65-codex-adapter/65-05-SUMMARY.md
@mcp/src/agent-providers/compatibility.ts
@mcp/src/agent-providers/serve-delegation.ts
@extension/utils/mcp-agent-providers.js
@extension/utils/delegation-providers.js
@extension/ui/providers-panel.js
@extension/ui/options.js

<contracts>
- Safe auth vocabulary is exactly chatgpt, api_key, unauthenticated, unknown.
- Codex billing mapping is exactly chatgpt→subscription and api_key→api; unauthenticated/unknown cannot run.
- Visible copy is exactly the approved 65-UI-SPEC copy and never includes a version, path, raw login status, key fragment, or dollar estimate.
- `control_panel.html` and the seven API-provider request/storage contracts remain unchanged.
</contracts>
</context>

<tasks>

<task id="65-06-01" type="auto">
  <name>Promote safe Codex evidence through canonical preflight, consent, and background authority</name>
  <files>mcp/src/agent-providers/compatibility.ts, mcp/src/agent-providers/serve-delegation.ts, extension/utils/mcp-agent-providers.js, extension/utils/delegation-providers.js, extension/utils/delegation-preflight.js, extension/utils/delegation-consent.js, extension/background.js, tests/mcp-adapter-compatibility.test.js, tests/mcp-bridge-topology.test.js, tests/mcp-agent-providers-storage.test.js, tests/delegation-routing.test.js, tests/delegation-consent.test.js, tests/mcp-bridge-background-dispatch.test.js</files>
  <read_first>65-CONTEXT.md Auth and Billing Truth plus Product Integration decisions; 65-RESEARCH.md Patterns 6-7 and T65-02/T65-03/T65-09/T65-10; 65-05-SUMMARY.md exact native auth/compatibility projection; 65-01 through 65-03 summaries for accepted identity and consent/start authority; daemon SafeCompatibilitySnapshot producer/serve serialization; existing safe snapshot exact-key, legacy migration, and stale projection tests</read_first>
  <action>Bump only the browser-safe wire snapshot to schema v2 while leaving the compatibility matrix at v1. Produce an exact ordered three-row roster `claude-code`, `opencode`, `codex`; each v2 row has exactly `adapterId`, `displayLabel`, `status`, `reason`, `authState`, while snapshot-level `checkedAt` remains bounded. Serve must own-data-read each detection's authState, validate only `chatgpt|api_key|unauthenticated|unknown`, and reduce any detection error, hostile record, missing value, or roster mismatch to canonical safe rows with `unknown`; never serialize raw version/path/status/config/credential data, billingKind, profileVersion, or acceptedIdentity. In extension storage, accept live replacement only from exact v2/three-row evidence. Treat only the historical exact two-row v1 shape as read-only migration input: normalize in memory to v2 with auth unknown, supported/newer-degraded rows forced to degraded/evidence_stale, synthesize Codex unsupported/matrix_invalid, produce no acceptedIdentity, and do not rewrite merely on read. Any stale/failed v2 projection forces auth unknown, strips acceptedIdentity, and converts runnable compatibility to degraded/evidence_stale. Extend the internal metadata-owned policy with canonical non-public profile versions and Codex `chatgpt→subscription` / `api_key→api`, keeping the public get/list metadata byte shape unchanged; unauthenticated/unknown are non-runnable. Only fresh supported/within-range or degraded/newer-than-tested rows may derive the exact five-field acceptedIdentity through the shared validator. Feed that identity through preflight/consent/start comparison; auth change clears the challenge and requires a fresh status/consent cycle before task stdin. Keep every side-panel request provider/auth/billing-free and compatibility observational. Add hostile producer/snapshot, stale/legacy, all four auth states, one-field identity mutation, trust/replay, leak-canary, and seven-API-provider invariance tests. Commit this task atomically.</action>
  <acceptance_criteria>The daemon emits and browser accepts an exact schema-v2 three-row safe roster; legacy/stale/failed evidence has auth unknown and no acceptedIdentity; only fresh approved Codex auth/billing pairs can start; every mismatch fails before run state/stdin; no sensitive/version/billing/identity detail crosses the wire; compatibility and client messages grant no authority.</acceptance_criteria>
  <verify>
    <automated>node scripts/run-mcp-build-preserving-workspace.mjs --commands-json '[["node","tests/mcp-adapter-compatibility.test.js"],["node","tests/mcp-bridge-topology.test.js"]]' &amp;&amp; node tests/mcp-agent-providers-storage.test.js &amp;&amp; node tests/delegation-routing.test.js --section codex-auth-preflight &amp;&amp; node tests/delegation-consent.test.js --section codex-auth-binding &amp;&amp; node tests/mcp-bridge-background-dispatch.test.js --section codex-start-authority</automated>
  </verify>
  <done>The acceptance criteria hold, all focused suites pass, and the task is committed atomically.</done>
</task>

<task id="65-06-02" type="auto">
  <name>Drive the existing third Providers row from exact Codex auth and billing evidence</name>
  <files>extension/ui/providers-panel.js, extension/ui/options.js, tests/providers-panel-logic.test.js, tests/providers-panel-ui.test.js</files>
  <read_first>65-UI-SPEC.md Providers panel, copywriting, state matrix, accessibility, and deterministic verification sections; 65-06-01 safe row contract; existing dormant Codex row mapper, selected-agent detail renderer, refresh preservation snapshot, and DOM/source hash tests</read_first>
  <action>Remove only the unshipped-Codex presentation fallback so the existing third row consumes the shipped safe row. Make selected-agent Account/Auth and Billing helpers receive the current safe evidence rather than undefined/static billing. Render exact values: chatgpt `ChatGPT` / `Codex is signed in with ChatGPT.` / `Included with your ChatGPT plan`; api_key `API key` / `Codex is signed in with an API key stored by Codex.` / `Billed to the API key stored by Codex; dollar amount not reported.`; unauthenticated `Not signed in` / `Sign in to Codex first.`; unknown `Status unavailable` / refresh guidance / `Billing not reported`. Preserve compatibility as a separate fact, stable row/DOM/focus order, recommendation, checked/dirty state, refresh semantics, provider kind/id, all API values, storage writes, links, themes, and no UI-side version comparison. Do not edit `control_panel.html`, add markup, add a Codex renderer, or show a version/profile/dollar amount. Commit this task atomically.</action>
  <acceptance_criteria>Existing Codex row/details show the exact four-state auth/billing matrix from safe evidence, the row remains third with unchanged markup/focus/recommendation/API behavior, stale evidence is non-runnable unknown, and no native/version/credential/dollar data is visible.</acceptance_criteria>
  <verify>
    <automated>node tests/providers-panel-logic.test.js --section codex-safe-evidence &amp;&amp; node tests/providers-panel-ui.test.js --section codex-existing-row</automated>
  </verify>
  <done>The acceptance criteria hold, both UI suites pass, and the task is committed atomically.</done>
</task>

</tasks>

<threat_model>
  <block_on>HIGH,CRITICAL</block_on>
  <trust_boundaries>
    <boundary id="B65-06-A">Daemon compatibility/auth evidence crosses into extension storage and presentation.</boundary>
    <boundary id="B65-06-B">Saved provider selection and consent cross into daemon start authority.</boundary>
    <boundary id="B65-06-C">Stale persisted evidence is projected after daemon or service-worker unavailability.</boundary>
  </trust_boundaries>
  <threat id="T65-02" category="Information Disclosure" component="safe auth projection" severity="CRITICAL" disposition="mitigate"><mitigation>Project only the four-state enum and reject every raw status/path/version/config/credential field.</mitigation><verification>Canary and hostile-record storage/UI scans.</verification></threat>
  <threat id="T65-03" category="Spoofing" component="consent/start auth" severity="CRITICAL" disposition="mitigate"><mitigation>Bind the exact accepted identity and compare daemon evidence before controller/stdin.</mitigation><verification>ChatGPT↔API-key mismatch, stale challenge, trust, and replay cases.</verification></threat>
  <threat id="T65-09" category="Tampering" component="billing presentation" severity="HIGH" disposition="mitigate"><mitigation>Resolve only metadata-approved auth/billing pairs and carry the accepted immutable pair.</mitigation><verification>Invalid pair, event mutation, and static re-derivation negatives.</verification></threat>
  <threat id="T65-10" category="Elevation of Privilege" component="compatibility and browser requests" severity="HIGH" disposition="mitigate"><mitigation>Compatibility stays observational; saved background settings select; client requests carry no provider identity.</mitigation><verification>Exact request keys, refresh invariants, and recommendation/selection tests.</verification></threat>
  <threat id="T65-11" category="Tampering" component="compatibility display" severity="HIGH" disposition="mitigate"><mitigation>Use daemon classification only and never expose/rebase the tested version in UI.</mitigation><verification>Supported/Degraded fixtures and absence of visible version strings.</verification></threat>
</threat_model>

<verification>
- Safe storage/authority and existing-row UI suites pass without a live Codex process or browser.
- `control_panel.html` remains byte-identical and all seven API providers preserve behavior.
- No task, status bytes, key fragments, local path, profile/version, endpoint, or dollar estimate reaches storage or DOM.
</verification>

<success_criteria>
- MULTI-05 is complete at the pre-start product boundary with exact auth/billing disclosure.
- Codex is the stable third selectable agent through shared abstractions only.
- Existing provider authority, refresh, focus, recommendation, and BYOK behavior remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/65-codex-adapter/65-06-SUMMARY.md` when complete.
</output>
