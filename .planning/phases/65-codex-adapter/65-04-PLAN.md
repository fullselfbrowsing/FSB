---
phase: 65-codex-adapter
plan: "04"
type: execute
wave: 4
depends_on:
  - "65-03"
files_modified:
  - mcp/src/agent-providers/adapter.ts
  - mcp/src/agent-providers/spawn-environment.ts
  - mcp/src/agent-providers/process-probe.ts
  - mcp/src/agent-providers/effective-authority.ts
  - mcp/src/agent-providers/runtime-files.ts
  - mcp/src/agent-providers/spawn-supervisor.ts
  - mcp/src/agent-providers/serve-delegation.ts
  - tests/mcp-codex-adapter.test.js
  - tests/runtime-contracts.test.js
  - tests/mcp-spawn-supervisor.test.js
  - tests/mcp-agent-orphan-recovery.test.js
autonomous: true
requirements:
  - MULTI-04
  - MULTI-05
must_haves:
  truths:
    - "Detection, authority attestation, and task spawn share one source-pinned environment sanitizer whose fixed values cannot restore stripped keys."
    - "A direct adapter receives only a validated serve-owned loopback runtime reference and materializes no browser- or task-supplied endpoint."
    - "A bounded byte-process primitive enforces argv arrays, shell false, timeout/signal/output caps, and caller-owned zeroable Buffer results without logging or stringifying raw bytes."
    - "The supervisor consumes generic pre-spawn identity-reprobe and effective-authority descriptors without an adapter-id branch or sixth adapter method."
    - "Direct delegations use role-aware empty scratch space and cannot succeed until tree settlement and scratch removal complete."
    - "The production roster remains exactly Claude Code/OpenCode; no Codex adapter id, constructor, registry row, or fixture exists after this plan."
  artifacts:
    - path: "mcp/src/agent-providers/spawn-environment.ts"
      provides: "Shared source-pinned environment sanitizer"
    - path: "mcp/src/agent-providers/process-probe.ts"
      provides: "Bounded byte-only child probe primitive"
    - path: "mcp/src/agent-providers/effective-authority.ts"
      provides: "Closed pre-spawn identity and effective-authority descriptor validation"
    - path: "mcp/src/agent-providers/runtime-files.ts"
      provides: "Validated direct runtime reference and role-aware empty scratch lifecycle"
    - path: "mcp/src/agent-providers/spawn-supervisor.ts"
      provides: "Generic pre-spawn probe/attestation and cleanup barriers"
  key_links:
    - from: "mcp/src/agent-providers/serve-delegation.ts"
      to: "mcp/src/agent-providers/runtime-files.ts"
      via: "serve-owned validated loopback direct runtime reference"
      pattern: "DirectRuntimeReference"
    - from: "mcp/src/agent-providers/spawn-supervisor.ts"
      to: "mcp/src/agent-providers/process-probe.ts"
      via: "generic byte-bounded pre-spawn probes"
      pattern: "runBoundedProcessProbe"
    - from: "mcp/src/agent-providers/spawn-supervisor.ts"
      to: "mcp/src/agent-providers/effective-authority.ts"
      via: "closed identity-reprobe and effective-authority descriptors"
      pattern: "PreSpawnIdentityProbe|EffectiveAuthorityAttestation"
---

<objective>
Build the provider-neutral direct-runtime, byte-probe, authority-attestation, sanitizer, and cleanup substrate before Codex is exposed.

Purpose: The eventual Codex adapter must remain declarative while the serve-owned supervisor alone controls endpoint materialization, pre-spawn identity truth, environment authority, child lifecycle, and cleanup.
Output: Generic direct runtime references, bounded byte probes, closed attestation descriptors, shared environment sanitation, identity-reprobe seam, and empty-scratch terminal barrier.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/65-codex-adapter/65-CONTEXT.md
@.planning/phases/65-codex-adapter/65-RESEARCH.md
@.planning/phases/65-codex-adapter/65-VALIDATION.md
@.planning/phases/65-codex-adapter/65-03-SUMMARY.md
@mcp/src/agent-providers/adapter.ts
@mcp/src/agent-providers/runtime-files.ts
@mcp/src/agent-providers/spawn-supervisor.ts
@mcp/src/agent-providers/serve-delegation.ts
@tests/runtime-contracts.test.js
@tests/mcp-spawn-supervisor.test.js

<contracts>
- Preserve exactly five adapter methods: detect, capabilities, buildSpawn, parseStream, normalizeError.
- New private spawn data is declarative and exact: one direct runtime reference, one pre-spawn identity descriptor, and one effective-authority descriptor.
- This wave contains no `CODEX_ADAPTER_ID`, `createCodexAdapter`, Codex registry/matrix row, or Codex fixture.
</contracts>
</context>

<tasks>

<task id="65-04-01" type="auto">
  <name>Create the shared sanitizer and bounded byte-process primitive</name>
  <files>mcp/src/agent-providers/spawn-environment.ts, mcp/src/agent-providers/process-probe.ts, tests/mcp-codex-adapter.test.js, tests/mcp-spawn-supervisor.test.js</files>
  <read_first>65-RESEARCH.md Patterns 2-3, Runtime State Inventory, and T65-01/T65-02; existing supervisor environment construction and injected child-process fakes; 65-VALIDATION isolation/auth negative corpus</read_first>
  <action>Create `buildSanitizedAgentEnvironment(sourceEnv, fixedEnv, policy)` with an exact frozen policy containing inherited allow rules, source-pinned stripped-key names, and forced values. Strip before applying fixed values and reject any fixedEnv attempt to restore a stripped key. Preserve the complete existing Claude/OpenCode credential roster but add no Codex-specific key in this wave. Create `runBoundedProcessProbe(descriptor)` around an absolute retained command with argv array, `shell:false`, sanitized env, explicit cwd, timeout, abort/signal handling, stdout/stderr byte caps, and exact exit metadata. Return fresh Buffer ownership only; never call `toString`, interpolate, serialize, log, or retain raw bytes, and expose an idempotent zeroization path used on success and every failure. Add generic isolation and canary tests under the new Codex test harness without importing/constructing a Codex adapter. Commit this task as one atomic executor commit.</action>
  <acceptance_criteria>The shared sanitizer rejects fixed-env credential restoration, existing provider behavior is unchanged, the probe bounds every channel/lifecycle outcome and zeroes all byte buffers, and no raw sentinel reaches errors/logs/state; no Codex production symbol exists.</acceptance_criteria>
  <verify>
    <automated>node scripts/run-mcp-build-preserving-workspace.mjs --commands-json '[["node","tests/mcp-codex-adapter.test.js","--section","generic-probe"],["node","tests/mcp-spawn-supervisor.test.js","--section","shared-environment"]]'</automated>
  </verify>
  <done>The acceptance criteria hold, the preservation-wrapped command passes, and the task is committed atomically.</done>
</task>

<task id="65-04-02" type="auto">
  <name>Add validated direct runtime references and generic effective-authority descriptors</name>
  <files>mcp/src/agent-providers/adapter.ts, mcp/src/agent-providers/effective-authority.ts, mcp/src/agent-providers/runtime-files.ts, mcp/src/agent-providers/serve-delegation.ts, tests/runtime-contracts.test.js, tests/mcp-codex-adapter.test.js</files>
  <read_first>65-RESEARCH.md Patterns 1 and 4 plus Architectural Responsibility Map; current SpawnPrivateArtifact/runtime-files exact validators; serve-owned FSB endpoint creation; exact five-method adapter contract</read_first>
  <action>Add exact frozen private descriptor types `DirectRuntimeReference`, `PreSpawnIdentityProbe`, and `EffectiveAuthorityAttestation` without adding an adapter method. Materialize `DirectRuntimeReference` only in serve-delegation from the already-owned FSB loopback endpoint; validate scheme, loopback host, explicit port, path, absence of credentials/query/fragment, and generation ownership before buildSpawn resolution. Add `validateEffectiveAuthorityAttestation` for exact expected server name, endpoint binding, required/enabled flags, exact enabled-tool allowlist, local approval policy, and no headers/env/bearer material; the descriptor describes a generic byte-probe classifier and retains only safe equality booleans/reason codes. Reject extra keys, accessors, prototypes, ambiguous/duplicate values, and task/browser-supplied endpoints. Keep the production adapter roster and compatibility matrix Claude/OpenCode only. Commit this task as one atomic executor commit.</action>
  <acceptance_criteria>Only a serve-owned validated loopback reference can reach a direct spawn description; exact generic identity/authority descriptors are available to the supervisor while the adapter surface stays five methods and Codex remains unregistered and fixture-free.</acceptance_criteria>
  <verify>
    <automated>node scripts/run-mcp-build-preserving-workspace.mjs --commands-json '[["node","tests/runtime-contracts.test.js"],["node","tests/mcp-codex-adapter.test.js","--section","generic-authority"]]'</automated>
  </verify>
  <done>The acceptance criteria hold, the preservation-wrapped command passes, and the task is committed atomically.</done>
</task>

<task id="65-04-03" type="auto">
  <name>Execute generic identity re-probe, authority attestation, and empty-scratch cleanup barriers</name>
  <files>mcp/src/agent-providers/runtime-files.ts, mcp/src/agent-providers/spawn-supervisor.ts, tests/mcp-spawn-supervisor.test.js, tests/mcp-agent-orphan-recovery.test.js, tests/runtime-contracts.test.js</files>
  <read_first>65-RESEARCH.md Patterns 4-5, T65-03/T65-04/T65-08, and terminal ordering; 65-04-01/02 contracts; current candidate/exit/tree/journal/runtime cleanup and orphan recovery state machine</read_first>
  <action>Teach the generic supervisor to run the descriptor-owned identity re-probe and effective-authority probe immediately before child creation and before task stdin, both under the exact same sanitized environment and retained binary identity intended for spawn. Compare only safe classifier outputs to the accepted identity/descriptor, zero probe bytes in `finally`, and return closed diagnostics on timeout, signal, overflow, malformed, mismatch, or uncertainty. Do not add an adapter-id conditional. Extend runtime-files with a direct role that creates an empty daemon-owned per-run scratch directory, writes no task/config/auth artifact, and removes it on every outcome. Success requires a parser candidate, exit code zero, no signal, settled process tree, successful journal state, and completed scratch cleanup; cleanup failure or unsettled tree converts a candidate to failure. Preserve existing Claude/OpenCode paths. Commit this task as one atomic executor commit.</action>
  <acceptance_criteria>Generic pre-spawn probes run before child/stdin with byte zeroization, mismatch blocks without a task process, direct scratch remains empty and is removed, and no result is emitted until clean exit/tree/journal/runtime cleanup; no provider-id branch or Codex exposure is introduced.</acceptance_criteria>
  <verify>
    <automated>node scripts/run-mcp-build-preserving-workspace.mjs --commands-json '[["node","tests/mcp-spawn-supervisor.test.js","--section","pre-spawn-authority"],["node","tests/mcp-agent-orphan-recovery.test.js"],["node","tests/runtime-contracts.test.js"]]'</automated>
  </verify>
  <done>The acceptance criteria hold, the preservation-wrapped command passes, and the task is committed atomically.</done>
</task>

</tasks>

<threat_model>
  <block_on>HIGH,CRITICAL</block_on>
  <trust_boundaries>
    <boundary id="B65-04-A">Ambient process environment enters detection, probes, and task spawn.</boundary>
    <boundary id="B65-04-B">Serve-owned endpoint state becomes native child configuration.</boundary>
    <boundary id="B65-04-C">Secret-bearing native probe bytes cross into classification.</boundary>
    <boundary id="B65-04-D">A native terminal candidate crosses process-tree and runtime cleanup.</boundary>
  </trust_boundaries>
  <threat id="T65-01" category="Information Disclosure" component="environment" severity="CRITICAL" disposition="mitigate"><mitigation>One source-pinned sanitizer used by detection, attestation, and spawn; fixedEnv cannot restore stripped keys.</mitigation><verification>Ambient/fixed-env canary matrix.</verification></threat>
  <threat id="T65-02" category="Information Disclosure" component="process probe" severity="CRITICAL" disposition="mitigate"><mitigation>Bounded byte-only channels, no stringify/logging, and finally-path zeroization.</mitigation><verification>Success/error/timeout/signal/overflow byte canaries.</verification></threat>
  <threat id="T65-03" category="Spoofing" component="identity re-probe seam" severity="CRITICAL" disposition="mitigate"><mitigation>Exact immediate classifier comparison before child creation and stdin.</mitigation><verification>Mismatch proves zero spawn/stdin/journal.</verification></threat>
  <threat id="T65-04" category="Elevation of Privilege" component="runtime endpoint and authority" severity="CRITICAL" disposition="mitigate"><mitigation>Serve-owned loopback reference plus exact effective-authority descriptor and fail-closed proof.</mitigation><verification>Endpoint injection, extra/ambiguous authority, header/env, and allowlist negatives.</verification></threat>
  <threat id="T65-05" category="Elevation of Privilege" component="native authority" severity="CRITICAL" disposition="mitigate"><mitigation>Declarative closed descriptors consumed only by the generic supervisor; no provider branch or sixth method.</mitigation><verification>Provider-contract and source-structure assertions.</verification></threat>
  <threat id="T65-08" category="Tampering" component="terminal success" severity="CRITICAL" disposition="mitigate"><mitigation>Candidate plus exit-zero/no-signal/tree/journal/scratch-cleanup barrier.</mitigation><verification>Every missing settlement gate yields failure and no result.</verification></threat>
</threat_model>

<verification>
- Every build or compiled-output test is invoked through `scripts/run-mcp-build-preserving-workspace.mjs`.
- The exact adapter surface remains five methods and the shipped roster remains two entries.
- Source tests prove the forbidden Codex exposure symbols and fixture are absent through Wave 4.
</verification>

<success_criteria>
- All high-risk runtime primitives exist and are green before the first Codex production exposure.
- No task/browser input can choose endpoint, auth, billing, or native authority.
- Empty direct scratch and process settlement are part of authoritative success.
</success_criteria>

<output>
Create `.planning/phases/65-codex-adapter/65-04-SUMMARY.md` when complete.
</output>
