---
phase: 63-native-messaging-host
plan: "06"
type: execute
wave: 6
depends_on:
  - "63-05"
files_modified:
  - mcp/src/install.ts
  - mcp/src/index.ts
  - tests/mcp-install-platforms.test.js
  - tests/mcp-native-host-install.test.js
autonomous: true
requirements:
  - NATIVE-01
  - NATIVE-04
must_haves:
  truths:
    - "Only explicit `install --native-host [--extension-id <id>]` and `uninstall --native-host` invoke native registration; ordinary install/uninstall, `--all`, serve, doctor, package startup, and extension load never do."
    - "CLI native targets accept only their closed flags, print bounded factual results, and direct mismatched/foreign state to the existing doctor command without overwriting it."
    - "The existing 21 MCP-client install/uninstall paths and counts remain byte/behavior compatible and never include the native host."
  artifacts:
    - path: "mcp/src/install.ts"
      provides: "Explicit native target branch before broad MCP-client expansion"
      exports: ["runInstall", "runUninstall"]
    - path: "mcp/src/index.ts"
      provides: "Help and bounded output/error composition for explicit native commands"
    - path: "tests/mcp-install-platforms.test.js"
      provides: "CLI flag isolation and ordinary/--all non-mutation proof"
  key_links:
    - from: "mcp/src/install.ts"
      to: "mcp/src/native-host-install/index.ts"
      via: "early exact native-host branch"
      pattern: "native-host"
    - from: "mcp/src/index.ts"
      to: "mcp/src/install.ts"
      via: "existing install/uninstall router only"
      pattern: "runInstall|runUninstall"
---

<objective>
Expose the exact-owned installer through explicit CLI targets without contaminating ordinary MCP-client setup or the serve/doctor routers.

Purpose: Native registration must be opt-in, never implicit or included in broad existing install behavior.
Output: Closed native CLI parsing/help/results and compatibility tests for every existing installer path.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/63-native-messaging-host/63-CONTEXT.md
@.planning/phases/63-native-messaging-host/63-PATTERNS.md
@.planning/phases/63-native-messaging-host/63-05-SUMMARY.md
@mcp/src/install.ts
@mcp/src/index.ts
@tests/mcp-install-platforms.test.js
@scripts/run-mcp-build-preserving-workspace.mjs

<decision_trace>
- D63-07: explicit separate native install/uninstall targets, never ordinary install/startup/load.
- D63-09: default published id; unpacked development requires explicit validated id.
- D63-10/D63-11: exact idempotent install/uninstall and foreign-state guidance.
</decision_trace>

<interfaces>
```typescript
export async function runInstall(flags: InstallFlags): Promise&lt;void&gt;;
export async function runUninstall(flags: InstallFlags): Promise&lt;void&gt;;
```

Closed syntax:
```text
fsb-mcp-server install --native-host [--extension-id <32 lowercase a-p chars>]
fsb-mcp-server uninstall --native-host
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>63-06-01: Route the explicit native target before list/all/client expansion</name>
  <files>mcp/src/install.ts, tests/mcp-install-platforms.test.js, tests/mcp-native-host-install.test.js</files>
  <read_first>mcp/src/install.ts runInstall/runUninstall and broad `--all` expansion; Plan 05 install API/results; 63-PATTERNS.md explicit CLI branch</read_first>
  <behavior>
    - "install native-host accepts only native-host plus optional extension-id; uninstall accepts only native-host."
    - "native branch returns before list/all/PLATFORMS/Claude CLI logic and does not change the existing platform count."
    - "ordinary, list, all, dry-run, every client flag, serve/startup, and combinations never call native install/uninstall."
  </behavior>
  <action>Implement D63-07/D63-09 and mitigate T63-08. At the top of `runInstall`, detect the exact native target before `--list` and `--all`; validate the flag key set and the optional exact development id, then call `installNativeHost` once and return. At the top of `runUninstall`, accept only the native target and call `uninstallNativeHost` once. Reject `--all`, `--list`, `--dry-run`, client flags, extra extension ids, booleans/arrays, malformed ids, and mixed native/client operations with stable usage errors and zero mutation. Do not add the host to `PLATFORMS`, ordinary target matching, setup sections, totals, or Claude CLI delegation. Preserve all legacy outputs/tests.</action>
  <acceptance_criteria>The injected call-count matrix proves native methods are reachable only through the two exact commands, every mixed/unknown flag fails before mutation, and all existing installer platform assertions remain unchanged.</acceptance_criteria>
  <verify>
    <automated>node scripts/run-mcp-build-preserving-workspace.mjs --commands-json '[["node","tests/mcp-install-platforms.test.js"],["node","tests/mcp-native-host-install.test.js","--section","cli-routing"]]'</automated>
  </verify>
  <done>The acceptance criteria above are satisfied and the automated verification passes.</done>
</task>

<task type="auto" tdd="true">
  <name>63-06-02: Add bounded factual help, success, refusal, and uninstall output</name>
  <files>mcp/src/index.ts, mcp/src/install.ts, tests/mcp-install-platforms.test.js, tests/mcp-native-host-install.test.js</files>
  <read_first>mcp/src/index.ts printHelp/main install router and current fatal formatting; approved doctor/install copy constraints in 63-UI-SPEC.md; Plan 05 stable result codes</read_first>
  <behavior>
    - "Installed/already-installed/removed/not-installed results name the native host and bounded expected location; they never claim pairing/provider/delegation success."
    - "Foreign/mismatch/invalid/shadow/unavailable says `Native messaging host was not changed: <stable_reason>` and `Run fsb-mcp-server doctor for repair details.` with exitCode 1."
    - "No raw manifest, registry value, exception, environment, username, child output, secret, or task reaches stdout/stderr."
  </behavior>
  <action>Continue the required sequential ownership of `mcp/src/index.ts` after Plan 02. Add the two exact help lines and the optional extension-id syntax. Format the closed installer receipts in `install.ts` or a pure helper: factual state, bounded expected location, and sole origin for successful install; factual removal/count for uninstall; stable content-free reason and exact doctor guidance on refusal. Set a nonzero exit code on refusal/unavailable without throwing raw errors into the top-level fatal formatter. Do not add an interactive prompt, implicit repair, wake, pair, broad deletion, new command, or second doctor surface. Pin that `serve`, `doctor`, and no-argument startup never call installer code.</action>
  <acceptance_criteria>Subprocess tests prove exact syntax/copy/exit codes, no sensitive sentinel serialization, no mutation from non-native commands, and unchanged existing install/uninstall help and behavior.</acceptance_criteria>
  <verify>
    <automated>node scripts/run-mcp-build-preserving-workspace.mjs --commands-json '[["node","tests/mcp-install-platforms.test.js"],["node","tests/mcp-native-host-install.test.js","--section","cli-output"]]'</automated>
  </verify>
  <done>The acceptance criteria above are satisfied and the automated verification passes.</done>
</task>

</tasks>

<threat_model>
  <block_on>HIGH,CRITICAL</block_on>
  <residual_human_evidence_boundary>Real user command execution and installed Chrome discovery remain pending UAT; deterministic CLI tests prove routing and output only.</residual_human_evidence_boundary>
  <threat id="T63-06" category="Tampering" component="CLI exposure of durable runtime materialization" severity="HIGH" disposition="mitigate" asvs="V12">
    <mitigation>CLI reaches only the Plan 05 exact bundled/offline runtime transaction and reports a typed refusal for missing or invalid payload evidence.</mitigation>
    <verification>Native-target call counters, runtime result fixtures, and zero mutation on refusal.</verification>
    <residual_risk>Real installed interpreter/runtime reachability remains pending OS UAT.</residual_risk>
  </threat>
  <threat id="T63-07" category="Elevation of Privilege" component="CLI-selected Windows native artifact" severity="CRITICAL" disposition="avoid" asvs="V14">
    <mitigation>The CLI accepts no launcher selector and delegates only to the validated version-bound PE selection contract.</mitigation>
    <verification>Closed flag matrix and packaged-artifact refusal fixtures.</verification>
    <residual_risk>Real packaged PE launch remains pending Windows UAT.</residual_risk>
  </threat>
  <threat id="T63-08" category="Tampering" component="native CLI target beside broad legacy install/uninstall" severity="HIGH" disposition="mitigate" asvs="V4">
    <mitigation>Route an exact early native branch, exclude it from PLATFORMS/list/all/client expansion, reject every mixed or unknown flag, and permit no implicit repair.</mitigation>
    <verification>Injected subprocess/call-count matrix, mixed-flag negatives, legacy 21-platform parity, and ordinary/serve/doctor zero-mutation assertions.</verification>
    <residual_risk>User-invoked explicit commands still require real OS UAT for Chrome discovery.</residual_risk>
  </threat>
  <threat id="T63-09" category="Information Disclosure" component="install/uninstall terminal output" severity="HIGH" disposition="mitigate" asvs="V7">
    <mitigation>Format closed typed receipts and stable content-free reasons only; never serialize manifest, registry, exception, environment, username, child output, secret, session, or task data.</mitigation>
    <verification>Sensitive-sentinel output/exit snapshots and raw-error negative fixtures.</verification>
    <residual_risk>The bounded expected local location is intentionally CLI-visible per NATIVE-04 and never browser-projected.</residual_risk>
  </threat>
</threat_model>

<verification>
- Focused native install and existing platform suites pass after build.
- `mcp/src/index.ts` has only this sequential change after Plan 02; doctor edits remain for Plan 07.
- No ordinary install/uninstall/`--all` path reaches native mutation.
</verification>

<success_criteria>
- Native host installation is explicit and opt-in.
- Development ids are exact, validated, and never discovered from Chrome profiles.
- Existing MCP-client installers remain unchanged and isolated.
</success_criteria>

<output>
Create `.planning/phases/63-native-messaging-host/63-06-SUMMARY.md` when complete.
</output>
