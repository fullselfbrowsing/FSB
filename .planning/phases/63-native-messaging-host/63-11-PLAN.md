---
phase: 63-native-messaging-host
plan: "11"
type: execute
wave: 11
depends_on:
  - "63-10"
files_modified:
  - .planning/phases/63-native-messaging-host/63-REVIEW.md
  - .planning/phases/63-native-messaging-host/63-SECURITY.md
  - .planning/phases/63-native-messaging-host/63-UI-REVIEW.md
  - scripts/verify-phase63-review-artifacts.mjs
autonomous: true
requirements:
  - NATIVE-01
  - NATIVE-02
  - NATIVE-03
  - NATIVE-04
must_haves:
  truths:
    - "Independent code, security, and UI reviews inspect the completed Phase 63 implementation and cite concrete source and test evidence."
    - "No review artifact may pass with an unresolved HIGH/CRITICAL finding, a missing required section, an unverified claim, or a fabricated live-UAT result."
    - "The security review maps every T63-01..T63-12 threat and confirms that native wake remains a bounded availability hint outside agent authority."
    - "The ASVS L1 review explicitly covers applicable V2, V3, V4, V5, V7, V12, V13, and V14 control themes."
    - "The UI review checks the exact checking-copy sequence, intent fencing, accessibility, responsive behavior, and the prohibition on new controls or live regions."
  artifacts:
    - path: ".planning/phases/63-native-messaging-host/63-REVIEW.md"
      provides: "Evidence-backed implementation and regression review"
    - path: ".planning/phases/63-native-messaging-host/63-SECURITY.md"
      provides: "ASVS L1 and T63 threat disposition"
    - path: ".planning/phases/63-native-messaging-host/63-UI-REVIEW.md"
      provides: "Six-pillar UI and accessibility disposition"
    - path: "scripts/verify-phase63-review-artifacts.mjs"
      provides: "Deterministic review completeness and honesty gate"
  key_links:
    - from: ".planning/phases/63-native-messaging-host/63-SECURITY.md"
      to: ".planning/phases/63-native-messaging-host/63-VALIDATION.md"
      via: "T63-01..T63-12 identifiers and blocking verification commands"
      pattern: "T63-12"
    - from: ".planning/phases/63-native-messaging-host/63-UI-REVIEW.md"
      to: ".planning/phases/63-native-messaging-host/63-UI-SPEC.md"
      via: "exact state, copy, token, responsive, and accessibility criteria"
      pattern: "Checking local agent service"
    - from: "scripts/run-mcp-build-preserving-workspace.mjs"
      to: "scripts/verify-native-host-boundary.mjs and native-host protocol/install suites"
      via: "one closed compiled-check commands-json sequence after the security review artifact verifier passes"
      pattern: "commands-json"
---

<objective>
Apply independent blocking code, security, and UI review gates to the completed native-host slice before the final full-suite claim.

Purpose: Catch implementation drift, authority expansion, threat gaps, regressions, and presentation/accessibility defects without substituting review judgment for deferred real-platform UAT.
Output: Three evidence-backed review artifacts and one deterministic verifier that rejects incomplete or falsely passing reviews.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/63-native-messaging-host/63-CONTEXT.md
@.planning/phases/63-native-messaging-host/63-RESEARCH.md
@.planning/phases/63-native-messaging-host/63-PATTERNS.md
@.planning/phases/63-native-messaging-host/63-UI-SPEC.md
@.planning/phases/63-native-messaging-host/63-VALIDATION.md
@.planning/phases/63-native-messaging-host/63-HUMAN-UAT.md
@.planning/phases/59-reverse-request-channel-security-foundation/59-CONTEXT.md
@.planning/phases/61-delegation-ux-sw-eviction-persistence/61-CONTEXT.md
@.planning/phases/62-ci-drift-smoke-gate-doctor-extensions/62-CONTEXT.md
@scripts/run-phase63-focused-tests.mjs
@scripts/run-mcp-build-preserving-workspace.mjs

<decision_trace>
- D63-01..D63-25: reviews verify every locked behavior against source, tests, package artifacts, and phase contracts.
- D63-20..D63-24: diagnostics, security boundaries, package proof, and review gates remain blocking.
- D63-25: reviews may inspect the pending ledger but cannot promote, check, or populate human evidence.
</decision_trace>
</context>

<tasks>

<task type="auto">
  <name>63-11-01: Perform the blocking Phase 63 code and regression review</name>
  <files>.planning/phases/63-native-messaging-host/63-REVIEW.md, scripts/verify-phase63-review-artifacts.mjs</files>
  <read_first>All 63-01..10 SUMMARY files and their changed-source manifests; git diff for Phase 63-owned source/tests/workflows; 63-CONTEXT locked decisions; 63-VALIDATION matrix; focused workspace-preserving runner; existing Phase 59–62 contracts</read_first>
  <action>First create `scripts/verify-phase63-review-artifacts.mjs` and freeze one canonical implementation identity from the Phase 63 start/end commits recorded by 63-01..10 SUMMARY files: an ordered manifest of only their implementation source/test/package/workflow files, canonical `git diff --binary --no-ext-diff` bytes for that manifest, SHA-256, and the current worktree/index fingerprint. Exclude review artifacts, summaries, validation evidence, generated `mcp/build/**`, and the verifier itself from the implementation diff while recording them separately as review infrastructure. Then dispatch exactly one dedicated `gsd-code-reviewer` agent—not the interactive `gsd-code-review` skill/workflow—with a fixed Phase 63 prompt whose mandatory clauses are: read `$HOME/.codex/agents/gsd-code-reviewer.md`; noninteractive; source-only/no browser; inputs limited to the frozen implementation patch+manifest+hash, 63-01..10 SUMMARY files, 63-CONTEXT, 63-RESEARCH, 63-PATTERNS, 63-VALIDATION, 63-HUMAN-UAT, and the exact Phase 59/61/62 context files listed above; inspect correctness, race/lifecycle behavior, framing/size limits, platform/registry semantics, atomic ownership, bundled-offline package closure, workspace preservation, one-flight/no-replay behavior, error normalization, test strength, root/CI ordering, compatibility, and maintainability; do not run commands, tests, skills, or live UAT; do not fix, stage, commit, mutate implementation, prompt, or ask questions; write only `.planning/phases/63-native-messaging-host/63-REVIEW.md`. Require the artifact to record agent name, fixed prompt version `phase63-code-v1`, exact input manifest/hash, cited source lines and precomputed Plan 10 test evidence, NATIVE-01..04/D63-01..25 coverage, findings normalized only to `CRITICAL|HIGH|MEDIUM|LOW|INFO`, status/resolution, and explicit residual human-evidence boundary. Any CRITICAL/HIGH finding remains blocking and returns to its owning implementation plan; the reviewer never fixes it. The verifier must reject a changed implementation hash/index, any output other than the one scoped artifact, missing/extra input, absent section/identifier/evidence, placeholder, unknown/downgraded severity, unresolved CRITICAL/HIGH, claimed live UAT, or reviewer mutation. Define CLI modes exactly: `--kind code`, `--kind security`, and `--kind ui` validate one artifact; no arguments validates code then security then UI, requires all three to share the exact implementation identity, and exits nonzero if any kind fails; every other flag/arity fails closed. On every invocation run deterministic fixture negatives for no-argument all-kinds dispatch, missing kind/artifact, hash mismatch, extra output, unknown severity, unresolved HIGH, and falsely promoted UAT.</action>
  <acceptance_criteria>The code review covers all four requirements and 25 decisions, cites concrete evidence, records every finding disposition, has no unresolved HIGH/CRITICAL issue, and passes the deterministic artifact verifier.</acceptance_criteria>
  <verify>
    <automated>node scripts/run-phase63-focused-tests.mjs &amp;&amp; node scripts/verify-phase63-review-artifacts.mjs --kind code</automated>
  </verify>
  <done>The acceptance criteria above are satisfied and the automated verification passes.</done>
</task>

<task type="auto">
  <name>63-11-02: Perform the ASVS L1 native-channel security review</name>
  <files>.planning/phases/63-native-messaging-host/63-SECURITY.md, scripts/verify-phase63-review-artifacts.mjs</files>
  <read_first>63-RESEARCH security architecture/threats; 63-VALIDATION T63 table; native host/daemon/install/doctor/background implementation and tests; Phase 59 CHAN and secret-redaction invariants; packed artifact and boundary reports; 63-01-PLAN Task 63-01-01 complete build-wrapper and raw-index restoration contract</read_first>
  <action>Dispatch exactly one dedicated `gsd-security-auditor` agent—not `gsd-secure-phase` or another generic/interactive workflow—with fixed prompt version `phase63-security-v1`. Its mandatory clauses are: read `$HOME/.codex/agents/gsd-security-auditor.md`; noninteractive; source-only/no browser; inputs limited to the same frozen implementation patch+manifest+hash, 63-01..10 SUMMARY files, 63-CONTEXT, 63-RESEARCH security/resolved decisions, 63-PATTERNS, 63-VALIDATION, 63-HUMAN-UAT, and exact Phase 59/61/62 contexts; trace Chrome framing through packaged launcher, exact bundled/offline runtime, daemon readiness, bridge auth, agent authority, installer ownership, diagnostics, and browser projection; disposition T63-01..T63-12 individually with exploit path, mitigation, cited source/test evidence, residual risk, and applicable V2/V3/V4/V5/V7/V12/V13/V14; consume only precomputed Plan 10 test/package/workflow evidence and do not run commands/tests/skills/live UAT; do not fix, stage, commit, mutate implementation, prompt, or ask questions; write only `.planning/phases/63-native-messaging-host/63-SECURITY.md`. Normalize findings only to `CRITICAL|HIGH|MEDIUM|LOW|INFO`; any CRITICAL/HIGH blocks and returns to the owning plan for correction and fresh affected reviews. Require explicit checks for malformed/oversize/multi-frame input, product/protocol mismatch, dependency bundle/receipt tampering and online fallback, path/registry shadowing, symlink/reparse escape, foreign preservation, stale lock races, secret/path leakage, wake flooding, response correlation, direct-agent bypass, CHAN relaxation, and workspace/index mutation. Never copy secrets, auth headers, raw local paths, or live registry values. The verifier's `--kind security` mode must require all twelve threats, all eight ASVS identifiers, exact shared diff identity, normalized severity, residual risk, sanitized evidence, no false UAT promotion, and no unscoped output. After the review artifact is written, run its `--kind security` verifier first; only after that passes, run the boundary, protocol, and install checks together in one closed `--commands-json` sequence through the Plan 01 wrapper while fresh compiled output exists. That outer wrapper owns complete `mcp/build/**` graph and raw Git index restoration after the compiled checks settle.</action>
  <acceptance_criteria>Every T63 threat and each applicable V2/V3/V4/V5/V7/V12/V13/V14 theme has a concrete evidence-backed disposition, CHAN and agent-authority invariants remain unchanged, artifacts contain no secrets or unsanitized local paths, and no HIGH/CRITICAL security finding remains unresolved. The artifact verifier passes before the three compiled checks run in one Plan 01 wrapper lifecycle, and the complete build graph and raw Git index are restored afterward.</acceptance_criteria>
  <verify>
    <automated>node scripts/verify-phase63-review-artifacts.mjs --kind security &amp;&amp; node scripts/run-mcp-build-preserving-workspace.mjs --commands-json '[["node","scripts/verify-native-host-boundary.mjs"],["node","tests/mcp-native-host-protocol.test.js"],["node","tests/mcp-native-host-install.test.js"]]'</automated>
  </verify>
  <done>The acceptance criteria above are satisfied and the automated verification passes.</done>
</task>

<task type="auto">
  <name>63-11-03: Perform the six-pillar UI and accessibility review</name>
  <files>.planning/phases/63-native-messaging-host/63-UI-REVIEW.md, scripts/verify-phase63-review-artifacts.mjs</files>
  <read_first>63-UI-SPEC complete; sidepanel/background implementation diff; delegation-sidepanel-ui and background-wake suites; pending visual/accessibility sections of 63-HUMAN-UAT; Phase 61 status-row authority</read_first>
  <action>Dispatch exactly one dedicated `gsd-ui-auditor` agent—not `gsd-ui-review` or another generic/interactive workflow—with fixed prompt version `phase63-ui-v1`. Its mandatory clauses are: read `$HOME/.codex/agents/gsd-ui-auditor.md`; noninteractive; source-only/no browser; inputs limited to the same frozen implementation patch+manifest+hash, 63-01..10 SUMMARY files, 63-CONTEXT, complete 63-UI-SPEC, 63-PATTERNS, 63-VALIDATION, 63-HUMAN-UAT, and relevant Phase 61 context; audit visual hierarchy, interaction, responsiveness, accessibility, resilience, and design-system fidelity from source and precomputed DOM-test evidence only; do not run commands/tests/skills/live browser/screen-reader UAT; do not fix, stage, commit, mutate implementation, prompt, or ask questions; write only `.planning/phases/63-native-messaging-host/63-UI-REVIEW.md`. Verify the exact checking heading/body/announcement/header copy, info semantics, raw text plus monotonic edit revision, edit-then-revert invalidation, intent/attempt fencing, stale suppression, one decorative spinner, no new page/card/CTA/progress control/live region/focus move, token-only color, narrow/zoom source contract, forced colors, reduced motion, and unchanged ready/unpaired/offline flows. Normalize findings only to `CRITICAL|HIGH|MEDIUM|LOW|INFO`; CRITICAL/HIGH blocks and returns to Plan 09 or the actual owning plan. Rendered browser, screen-reader, focus, zoom, and OS-theme evidence must remain pending references to 63-HUMAN-UAT and never review-pass evidence. The verifier's `--kind ui` mode must require all six pillars, every UI-SPEC acceptance criterion, exact shared diff identity, normalized severity, residual boundary, no false UAT promotion, and no unscoped output.</action>
  <acceptance_criteria>The review covers all six pillars and every UI-SPEC criterion, the exact single-row checking state is source/test proven, all genuine rendered/accessibility evidence remains pending, and no HIGH/CRITICAL UI finding remains unresolved.</acceptance_criteria>
  <verify>
    <automated>node tests/delegation-sidepanel-ui.test.js &amp;&amp; node tests/native-host-background-wake.test.js &amp;&amp; node scripts/verify-phase63-review-artifacts.mjs --kind ui</automated>
  </verify>
  <done>The acceptance criteria above are satisfied and the automated verification passes.</done>
</task>

</tasks>

<threat_model>
  <block_on>HIGH,CRITICAL</block_on>
  <residual_human_evidence_boundary>Real OS install/registry, Chrome behavior, visual rendering, focus, and screen-reader evidence remains exclusively pending in 63-HUMAN-UAT.md; source-only reviews cannot promote it.</residual_human_evidence_boundary>
  <threat id="T63-01" category="Denial of Service" component="review of native protocol rejection boundary" severity="HIGH" disposition="mitigate" asvs="V5">
    <mitigation>Dedicated reviewers must trace cap/one-frame/schema/origin/argv controls before side effects.</mitigation>
    <verification>Code/security artifacts cite exact parser tests and source lines; verifier requires T63-01.</verification>
    <residual_risk>Real Chrome framing remains pending UAT.</residual_risk>
  </threat>
  <threat id="T63-02" category="Elevation of Privilege" component="review of serve-only host authority" severity="CRITICAL" disposition="mitigate" asvs="V4">
    <mitigation>Reviewers inspect frozen source/compiled graph evidence and exact serve tuple with no agent/task/shell authority.</mitigation>
    <verification>Required T63-02 evidence row, boundary report, and source citations.</verification>
    <residual_risk>Real child launch remains pending UAT.</residual_risk>
  </threat>
  <threat id="T63-03" category="Spoofing" component="review of daemon health identity" severity="HIGH" disposition="mitigate" asvs="V13">
    <mitigation>Review exact product/protocol/readiness and wrong-responder refusal.</mitigation>
    <verification>Required source/test evidence and normalized disposition.</verification>
    <residual_risk>Real port contention remains pending UAT.</residual_risk>
  </threat>
  <threat id="T63-04" category="Tampering" component="review of bridge secret ordering" severity="HIGH" disposition="mitigate" asvs="V2">
    <mitigation>Review post-bind auth hook and zero-side-effect bind loser against Phase 59.</mitigation>
    <verification>Call-order evidence and CHAN contract citations.</verification>
    <residual_risk>Real scheduler behavior remains pending UAT.</residual_risk>
  </threat>
  <threat id="T63-05" category="Denial of Service" component="review of lock/stale recovery" severity="HIGH" disposition="mitigate" asvs="V12">
    <mitigation>Review tokened locking, health recheck, quarantine, bounds, and no PID kill.</mitigation>
    <verification>Concurrency/stale test citations and residual-risk entry.</verification>
    <residual_risk>Real scheduling remains pending UAT.</residual_risk>
  </threat>
  <threat id="T63-06" category="Tampering" component="review of bundled runtime and workspace preservation" severity="HIGH" disposition="mitigate" asvs="V12">
    <mitigation>Review exact dependency pins, bundle closure, lock receipt, offline proof, stable runtime, and build/index restoration.</mitigation>
    <verification>Package/workflow/wrapper evidence and hash-bound review artifacts.</verification>
    <residual_risk>Installed Node relocation remains operator evidence.</residual_risk>
  </threat>
  <threat id="T63-07" category="Elevation of Privilege" component="review of Windows PE launcher" severity="CRITICAL" disposition="avoid" asvs="V14">
    <mitigation>Require version-bound PE artifacts and reject every command-interpreter fallback.</mitigation>
    <verification>PE/package/workflow citations and historical-shim blacklist.</verification>
    <residual_risk>Real Windows/Chrome execution remains pending UAT.</residual_risk>
  </threat>
  <threat id="T63-08" category="Tampering" component="review of exact-owned registration mutation" severity="HIGH" disposition="mitigate" asvs="V12">
    <mitigation>Review symlink/foreign/view/atomicity/idempotence/uninstall preservation controls.</mitigation>
    <verification>Install test evidence and exact registry/path source citations.</verification>
    <residual_risk>Genuine registry/filesystem behavior remains pending UAT.</residual_risk>
  </threat>
  <threat id="T63-09" category="Information Disclosure" component="review of diagnostic and UI projections" severity="HIGH" disposition="mitigate" asvs="V7">
    <mitigation>Review closed local output, five-key browser projection, sentinel redaction, and fixed checking copy.</mitigation>
    <verification>Diagnostics/UI artifact evidence with no raw local/private values.</verification>
    <residual_risk>CLI expected location remains intentionally local-only.</residual_risk>
  </threat>
  <threat id="T63-10" category="Elevation of Privilege" component="review of boot/offline native authority" severity="HIGH" disposition="mitigate" asvs="V14">
    <mitigation>Review silent boot, offline-only wake, background-only API, and one permission.</mitigation>
    <verification>Background/manifest evidence and source authority citations.</verification>
    <residual_risk>Real service-worker boot remains pending UAT.</residual_risk>
  </threat>
  <threat id="T63-11" category="Tampering" component="review of async intent replay fences" severity="HIGH" disposition="mitigate" asvs="V3">
    <mitigation>Review token/timeout/late-ignore, one rerun, current intent, and edit invalidation.</mitigation>
    <verification>Background/UI race evidence and zero-mutation citations.</verification>
    <residual_risk>Real eviction timing remains pending UAT.</residual_risk>
  </threat>
  <threat id="T63-12" category="Elevation of Privilege" component="review of lifecycle/delegation authority separation" severity="CRITICAL" disposition="mitigate" asvs="V3">
    <mitigation>Review authenticated bridge/preflight authority and prohibit native pairing/provider/task/start inference.</mitigation>
    <verification>Boundary, preflight, state-mutation, and exact response evidence.</verification>
    <residual_risk>Live ready/unpaired convergence remains pending UAT.</residual_risk>
  </threat>
  <threat id="T63-R01" category="Repudiation" component="review artifact identity and severity disposition" severity="HIGH" disposition="mitigate" asvs="V7">
    <mitigation>Use dedicated noninteractive source-only agents, fixed prompts/scopes, canonical diff hash, output allowlist, normalized severities, no-fix/no-commit rules, and block unresolved CRITICAL/HIGH.</mitigation>
    <verification>Per-kind verifier plus no-argument all-kinds negative fixtures for stale hash, extra output, missing artifact, unknown severity, unresolved HIGH, and false UAT.</verification>
    <residual_risk>Reviewer judgment on MEDIUM/LOW/INFO remains recorded and inspectable rather than silently fixed.</residual_risk>
  </threat>
</threat_model>

<verification>
- Each review names the exact implementation diff and cites source/test/package evidence.
- NATIVE-01..04, D63-01..25, T63-01..12, and all UI-SPEC criteria have review owners.
- Review verifier rejects placeholders, missing evidence, unresolved blocking findings, and false UAT promotion.
- Focused security and UI suites still pass after review.
</verification>

<success_criteria>
- Code, security, and UI reviews independently pass with no unresolved HIGH/CRITICAL findings.
- Review artifacts are mechanically complete, evidence-backed, sanitized, and bound to the final implementation.
- Deferred human evidence remains untouched and honestly pending.
</success_criteria>

<output>
Create `.planning/phases/63-native-messaging-host/63-11-SUMMARY.md` when complete.
</output>
