---
phase: 63-native-messaging-host
plan: "07"
type: execute
wave: 7
depends_on:
  - "63-06"
files_modified:
  - mcp/src/diagnostics.ts
  - mcp/src/index.ts
  - tests/mcp-diagnostics-status.test.js
  - tests/mcp-version-parity.test.js
autonomous: true
requirements:
  - NATIVE-04
must_haves:
  truths:
    - "The existing doctor command reports one closed native-host snapshot and exact human section without installing, repairing, waking, starting, pairing, or uninstalling anything."
    - "CLI text/JSON may include the bounded expected local location and stable reason required for repair; browser-safe projection reconstructs only installState, registration, allowlist, launcher, and daemon enums, omitting location, reason, and all registry/path/raw-value detail."
    - "Optional host absence does not change the historical overall doctor layer or healthy/unhealthy exit semantics."
  artifacts:
    - path: "mcp/src/diagnostics.ts"
      provides: "Closed read-only nativeHost snapshot and browser-safe projection"
      exports: ["collectBridgeDiagnostics", "projectNativeHostBrowserStatus"]
    - path: "mcp/src/index.ts"
      provides: "Exact Native messaging host doctor text section"
      exports: ["formatDoctor"]
    - path: "tests/mcp-diagnostics-status.test.js"
      provides: "Read-only, projection, order, enum, and sentinel non-leak proof"
  key_links:
    - from: "mcp/src/diagnostics.ts"
      to: "mcp/src/native-host-registration.ts"
      via: "injected read-only registration inspector only"
      pattern: "inspectNativeHost"
    - from: "mcp/src/index.ts"
      to: "Bridge auth: -> Native messaging host: -> Install paths:"
      via: "single snapshot formatter order"
      pattern: "Native messaging host:"
---

<objective>
Extend the existing doctor snapshot and text/JSON projections with bounded, read-only native-host diagnostics while keeping local paths out of browser-safe data.

Purpose: Operators need exact installation/allowlist/launcher/reachability facts, but doctor must never become repair/wake authority or leak local/private values into the extension.
Output: Closed nativeHost diagnostics model, safe projection, exact text placement, and deterministic non-mutation/non-disclosure tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/63-native-messaging-host/63-CONTEXT.md
@.planning/phases/63-native-messaging-host/63-UI-SPEC.md
@.planning/phases/63-native-messaging-host/63-PATTERNS.md
@.planning/phases/62-ci-drift-smoke-gate-doctor-extensions/62-CONTEXT.md
@.planning/phases/63-native-messaging-host/63-06-SUMMARY.md
@mcp/src/diagnostics.ts
@mcp/src/index.ts
@tests/mcp-diagnostics-status.test.js
@scripts/run-mcp-build-preserving-workspace.mjs

<decision_trace>
- D63-20: extend collectBridgeDiagnostics and its existing text/JSON projections with one closed native section.
- D63-21: doctor is offline/read-only; CLI path allowed, browser-safe path/registry detail forbidden.
- D63-22: bounded stable enums/reasons only; no raw manifest/registry/env/secret/task/child/error values.
</decision_trace>

<interfaces>
```typescript
type NativeHostDoctor = Readonly&lt;{
  installState: 'installed' | 'not_installed' | 'invalid' | 'unavailable';
  expectedLocation: string;
  registration: 'valid' | 'missing' | 'invalid' | 'unavailable';
  allowlist: 'matches' | 'mismatch' | 'not_reported';
  launcher: 'reachable' | 'missing' | 'invalid' | 'unavailable';
  daemon: 'reachable' | 'offline' | 'unavailable';
  reason: NativeHostDoctorReason;
}&gt;;
```

Closed `NativeHostDoctorReason` vocabulary and first-failure precedence:
```text
ok | platform_unsupported | inspection_unavailable | not_installed |
registration_missing | registration_invalid | registration_shadowed |
allowlist_mismatch | runtime_missing | runtime_invalid |
launcher_missing | launcher_invalid | daemon_identity_mismatch |
daemon_protocol_mismatch | daemon_offline

platform -> inspection -> complete absence -> registration/shadow -> allowlist ->
runtime -> launcher -> daemon identity/protocol/reachability -> ok
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>63-07-01: Collect one bounded read-only native-host snapshot and safe browser projection</name>
  <files>mcp/src/diagnostics.ts, tests/mcp-diagnostics-status.test.js</files>
  <read_first>diagnostics BridgeDiagnostics/Dependencies and collectBridgeDiagnostics; native-host-registration pure inspector; 63-UI-SPEC doctor projection; T63-09</read_first>
  <behavior>
    - "One injected inspector call produces the exact nativeHost keys/enums/reason; invalid/prototype/overlong/raw values collapse to bounded invalid/unavailable facts."
    - "Collector remains offline-capable and never invokes installer, wake, spawn, secret rotation, pair, repair, or uninstall dependencies."
    - "projectNativeHostBrowserStatus constructs exactly `{installState,registration,allowlist,launcher,daemon}` and cannot represent expectedLocation, reason, registry value/view, launcher path, manifest contents, raw error, username, environment, secret, session, child output, or task."
  </behavior>
  <action>Implement D63-20/D63-21/D63-22 and T63-09. Extend `BridgeDiagnostics` and injected dependencies with exactly one read-only native registration/launcher/daemon inspector. Normalize only ordinary own-data records, bounded expected location for local CLI, closed enums, and the exact reason vocabulary/first-failure precedence frozen above; unknown or contradictory facts collapse to `inspection_unavailable`, never a caller string. Run the inspector once before bridge probes so doctor works offline, but do not let optional host absence add a probe note or change `classifyDoctorLayer`. Export an explicit browser-safe projector that reconstructs exactly `{installState,registration,allowlist,launcher,daemon}` and omits expectedLocation and reason as well as every registry/path detail; never spread or pass through unknown keys. Add one case per reason/precedence boundary plus mutation spies and sentinel secrets/paths/errors to text, local JSON, and browser projection tests.</action>
  <acceptance_criteria>Text/JSON share one normalized snapshot and exact deterministic reason; browser projection has exactly five approved enum keys with no location/reason/registry/path detail; all forbidden sentinels are absent; every reason/precedence branch is tested; inspector call count is one; and every mutation-capable spy is zero.</acceptance_criteria>
  <verify>
    <automated>node scripts/run-mcp-build-preserving-workspace.mjs --commands-json '[["node","tests/mcp-diagnostics-status.test.js","--section","native-host-snapshot"]]'</automated>
  </verify>
  <done>The acceptance criteria above are satisfied and the automated verification passes.</done>
</task>

<task type="auto" tdd="true">
  <name>63-07-02: Format the exact doctor section without changing overall semantics</name>
  <files>mcp/src/index.ts, tests/mcp-diagnostics-status.test.js, tests/mcp-version-parity.test.js</files>
  <read_first>mcp/src/index.ts formatDoctor/runDoctor after Plan 06; exact doctor block in 63-UI-SPEC.md; Phase 62 doctor parity/exit contract</read_first>
  <behavior>
    - "Human section appears exactly after Bridge auth and before Install paths with seven approved labels and title-cased values."
    - "doctor --json serializes the same local snapshot; status/doctor exit layer remains historical and host-not-installed alone is non-failing."
    - "No second doctor command or native install/wake/repair action exists."
  </behavior>
  <action>Continue sequential `mcp/src/index.ts` ownership after Plans 02 and 06. Render exactly the approved `Native messaging host:` block and labels: Install state, Expected location, Manifest/registry, Chrome allowlist, Launcher, Daemon, Reason. Use only closed mappings and bounded snapshot values. Preserve all existing adapter/auth/install-path output, JSON projection, diagnostic layer, and exit code semantics. Do not surface raw manifest, registry value/view, exception, environment, username, child output, secret, session, or task. Extend version parity to pin section order and prove browser-safe native facts are exactly the five enum keys and exclude expectedLocation, reason, and registry/path detail.</action>
  <acceptance_criteria>Exact text order/copy and local JSON pass; optional absence leaves a previously healthy snapshot healthy; no doctor path calls a mutation or wake API; browser-safe projection remains exactly five-key, reason-free, path-free, and registry-detail-free.</acceptance_criteria>
  <verify>
    <automated>node scripts/run-mcp-build-preserving-workspace.mjs --commands-json '[["node","tests/mcp-diagnostics-status.test.js"],["node","tests/mcp-version-parity.test.js"]]'</automated>
  </verify>
  <done>The acceptance criteria above are satisfied and the automated verification passes.</done>
</task>

</tasks>

<threat_model>
  <block_on>HIGH,CRITICAL</block_on>
  <residual_human_evidence_boundary>Actual paths, registry contents, and daemon state on real macOS/Linux/Windows are pending UAT; synthetic facts validate projection semantics only.</residual_human_evidence_boundary>
  <threat id="T63-03" category="Spoofing" component="doctor daemon reachability classification" severity="HIGH" disposition="mitigate" asvs="V13">
    <mitigation>Classify reachable only from the same bounded exact product/protocol/readiness health contract as the host.</mitigation>
    <verification>Wrong-service, protocol mismatch, malformed, offline, and ready cases with exact precedence.</verification>
    <residual_risk>Real local daemon state remains pending OS/browser UAT.</residual_risk>
  </threat>
  <threat id="T63-09" category="Information Disclosure" component="native diagnostics snapshot, CLI projection, and browser-safe projection" severity="HIGH" disposition="mitigate" asvs="V7">
    <mitigation>Use one read-only injected inspector, exact own-data normalization, closed enums/reasons, bounded CLI-only location, and an explicit five-key reconstructive browser projector.</mitigation>
    <verification>Mutation spies, sentinel leakage matrix, exact-key snapshots, one-call assertion, text/JSON order, browser omission, and unchanged layer/exit tests.</verification>
    <residual_risk>The expected location is intentionally exposed only to local CLI output as required by NATIVE-04.</residual_risk>
  </threat>
</threat_model>

<verification>
- Diagnostics and version parity suites pass after the build.
- Doctor is mechanically read-only and optional absence is not global failure.
- Every browser-safe projection omits local path and registry details.
</verification>

<success_criteria>
- NATIVE-04 has exact local install/path/allowlist/launcher/reachability diagnosis.
- Doctor remains one command, one snapshot, read-only, and offline-capable.
- Sensitive/local detail never crosses into browser-safe data.
</success_criteria>

<output>
Create `.planning/phases/63-native-messaging-host/63-07-SUMMARY.md` when complete.
</output>
