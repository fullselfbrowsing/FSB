---
phase: 63-native-messaging-host
plan: "12"
type: execute
wave: 12
depends_on:
  - "63-11"
files_modified:
  - .planning/phases/63-native-messaging-host/63-VALIDATION.md
autonomous: true
requirements:
  - NATIVE-01
  - NATIVE-02
  - NATIVE-03
  - NATIVE-04
must_haves:
  truths:
    - "The final focused, boundary, package, contract, and review gates pass against the reviewed implementation before the repository-wide suite starts."
    - "The existing Phase 60 repository-wide harness runs inside the Plan 01 outer build-preserving wrapper; the outer wrapper restores the complete ignored mcp/build graph and raw Git index, while the inner harness runs the repository-wide suite and retains its existing dirty-path checks."
    - "Automated completion records exact commands and exit status while leaving every genuine human-UAT item pending."
    - "A full-suite failure blocks Phase 63 completion and is corrected at the owning plan before this gate is rerun."
  artifacts:
    - path: ".planning/phases/63-native-messaging-host/63-VALIDATION.md"
      provides: "Final focused/full-suite evidence with explicit pending-UAT boundary"
    - path: "scripts/run-phase63-focused-tests.mjs"
      provides: "Reviewed Phase 63 focused and artifact gate"
    - path: "scripts/run-phase60-full-tests.mjs"
      provides: "Inner repository-wide regression harness and existing dirty-path checks"
    - path: "scripts/run-mcp-build-preserving-workspace.mjs"
      provides: "Outer complete mcp/build graph and raw Git index snapshot/restoration boundary"
  key_links:
    - from: ".planning/phases/63-native-messaging-host/63-VALIDATION.md"
      to: "scripts/verify-phase63-review-artifacts.mjs"
      via: "the final focused evidence records the separate blocking review verifier"
      pattern: "verify-phase63-review-artifacts"
    - from: ".planning/phases/63-native-messaging-host/63-VALIDATION.md"
      to: ".planning/phases/63-native-messaging-host/63-HUMAN-UAT.md"
      via: "automation result never promotes pending human evidence"
      pattern: "human_needed"
    - from: "scripts/run-mcp-build-preserving-workspace.mjs"
      to: "scripts/run-phase60-full-tests.mjs"
      via: "the full-suite harness runs as one commands-json child inside the outer Plan 01 restoration boundary"
      pattern: "run-phase60-full-tests"
---

<objective>
Run the reviewed Phase 63 focused matrix and the guarded repository-wide regression suite as the final automated completion gate.

Purpose: Prove that the complete native-host slice and its package/review contracts pass together without regressing earlier behavior or mutating the protected workspace.
Output: Exact final automated evidence in the validation ledger, with human UAT still explicitly pending for the milestone-end sweep.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/63-native-messaging-host/63-VALIDATION.md
@.planning/phases/63-native-messaging-host/63-HUMAN-UAT.md
@.planning/phases/63-native-messaging-host/63-REVIEW.md
@.planning/phases/63-native-messaging-host/63-SECURITY.md
@.planning/phases/63-native-messaging-host/63-UI-REVIEW.md
@scripts/run-phase63-focused-tests.mjs
@scripts/verify-phase63-review-artifacts.mjs
@scripts/run-phase60-full-tests.mjs
@scripts/run-mcp-build-preserving-workspace.mjs
@tests/mcp-native-host-packaging.test.js
@package.json

<decision_trace>
- D63-01..D63-24: final focused/full gates verify the implemented native host, distribution, diagnostics, extension wake, UI, tests, and reviews as one slice.
- D63-25: final automation records no genuine OS/browser/accessibility result and leaves the single human ledger pending.
</decision_trace>
</context>

<tasks>

<task type="auto">
  <name>63-12-01: Run the final reviewed Phase 63 focused and artifact gates</name>
  <files>.planning/phases/63-native-messaging-host/63-VALIDATION.md</files>
  <read_first>63-VALIDATION final task map; 63-01..11 SUMMARY verification evidence; three review artifacts; focused runner source; package/boundary/review verifier source</read_first>
  <action>Capture the current commit/diff identity and protected-workspace fingerprint, then run the Phase 63 focused wrapper exactly once against the reviewed tree, followed by the separate review verifier exactly once with no arguments. The no-argument form is normative all-kinds mode defined in Plan 11: it validates `code`, then `security`, then `ui`; requires all three artifacts, fixed prompt/agent identities, normalized severities, scoped outputs, and one shared implementation manifest/hash; runs its negative fixtures; blocks any missing/stale/extra/falsely promoted artifact or unresolved CRITICAL/HIGH; and exits nonzero on the first failing kind while reporting all checked kinds deterministically. Do not substitute an implicit default or an unrecognized flag. Require the focused wrapper to execute build, four native-host focused suites, every extended seam, the build lifecycle's one source-mode and one compiled-mode boundary pass, packed-artifact checks, phase/version contracts, and UAT-honesty parser in the declared serial order with no redundant all-mode boundary invocation; require the no-argument review verifier to bind all three clean review artifacts to the same diff. Record both exact commands, timestamp, sanitized exit status, suite counts, artifact identities/hashes already produced by the package gate, and unchanged workspace/index proof in `63-VALIDATION.md`; do not paste secrets, raw local install paths, or registry contents. If either command fails or the reviewed diff changes, stop, route the issue to its owning plan, rerun affected review(s), and restart this task from a fresh fingerprint.</action>
  <acceptance_criteria>The focused wrapper and every blocking subgate exit 0 against the exact reviewed diff, protected bytes/index are unchanged, evidence is sanitized and reproducible, and no human-UAT result changes.</acceptance_criteria>
  <verify>
    <automated>node scripts/run-phase63-focused-tests.mjs &amp;&amp; node scripts/verify-phase63-review-artifacts.mjs</automated>
  </verify>
  <done>The acceptance criteria above are satisfied and the automated verification passes.</done>
</task>

<task type="auto">
  <name>63-12-02: Run the guarded repository-wide suite and close automated validation</name>
  <files>.planning/phases/63-native-messaging-host/63-VALIDATION.md</files>
  <read_first>63-01-PLAN Task 63-01-01 complete build-wrapper and raw-index restoration contract; tests/mcp-native-host-packaging.test.js existing `workspace-preserving-build` failure/signal/generated-file fixture matrix; run-phase60-full-tests inner harness and its existing dirty-path assertions; root package test chain; final focused evidence from Task 1; HUMAN-UAT pending schema</read_first>
  <action>First run the existing Plan 01 `workspace-preserving-build` packaging fixture matrix and require all of its success, nonzero-child, signal, exception, initially-absent/generated-file, build-entry type/mode/symlink, and raw-index restoration cases to pass. Only then invoke `scripts/run-phase60-full-tests.mjs` as the sole child in one closed `--commands-json` sequence through `scripts/run-mcp-build-preserving-workspace.mjs` from the same reviewed tree. The outer Plan 01 wrapper—not the inner Phase 60 harness—owns snapshot and unconditional restoration of the complete ignored `mcp/build/**` graph and raw Git index for the full-suite lifecycle. The inner Phase 60 harness remains responsible for executing the repository-wide root suite serially and enforcing its existing tracked dirty-byte, dirty-path/status, staged-entry, untracked-listing, and temporary compatibility-path checks. Do not broaden or modify `scripts/run-phase60-full-tests.mjs`. Record both exact commands, sanitized exit status, duration/suite summary, final commit/diff identity, outer build/index restoration proof, and inner dirty-path proof in `63-VALIDATION.md`. Mark only Phase 63 automated validation complete. Preserve every `63-HUMAN-UAT.md` checkbox as unchecked with `human_needed`, `pending`, and empty evidence; state that milestone completion still requires the separate final UAT ledger sweep. Any fixture or regression failure is blocking and returns to the source-owning plan plus affected review before both Plan 12 tasks rerun.</action>
  <acceptance_criteria>The existing Plan 01 failure/signal/generated-file fixture matrix passes before the full suite starts; the Phase 60 repository-wide suite then exits 0 as a child of the Plan 01 wrapper on the exact reviewed diff. The outer wrapper restores the complete ignored build graph and raw Git index, the inner harness's existing dirty-path checks pass unchanged, validation contains complete sanitized evidence, and the human ledger remains wholly pending.</acceptance_criteria>
  <verify>
    <automated>node tests/mcp-native-host-packaging.test.js --section workspace-preserving-build &amp;&amp; node scripts/run-mcp-build-preserving-workspace.mjs --commands-json '[["node","scripts/run-phase60-full-tests.mjs"]]'</automated>
  </verify>
  <done>The acceptance criteria above are satisfied and the automated verification passes.</done>
</task>

</tasks>

<threat_model>
  <block_on>HIGH,CRITICAL</block_on>
  <residual_human_evidence_boundary>Real macOS/Linux/Windows installs, Chrome outcomes, rendered themes/layout, keyboard focus, and screen-reader behavior remain pending until the final milestone ledger sweep; automated completion cannot promote them.</residual_human_evidence_boundary>
  <threat id="T63-01" category="Denial of Service" component="final protocol gate" severity="HIGH" disposition="mitigate" asvs="V5">
    <mitigation>Require the reviewed focused protocol matrix before full-suite execution.</mitigation>
    <verification>Focused wrapper and validation command receipt.</verification>
    <residual_risk>Real Chrome framing remains pending UAT.</residual_risk>
  </threat>
  <threat id="T63-02" category="Elevation of Privilege" component="final host authority gate" severity="CRITICAL" disposition="mitigate" asvs="V4">
    <mitigation>Require source/compiled boundary and exact serve tuple against the reviewed diff.</mitigation>
    <verification>Boundary receipt, review identity, and full-suite result.</verification>
    <residual_risk>Real process execution remains pending UAT.</residual_risk>
  </threat>
  <threat id="T63-03" category="Spoofing" component="final health identity gate" severity="HIGH" disposition="mitigate" asvs="V13">
    <mitigation>Require exact product/protocol/readiness tests and reviews.</mitigation>
    <verification>Daemon/topology receipts bound to the reviewed diff.</verification>
    <residual_risk>Real port contention remains pending UAT.</residual_risk>
  </threat>
  <threat id="T63-04" category="Tampering" component="final bridge secret ordering gate" severity="HIGH" disposition="mitigate" asvs="V2">
    <mitigation>Require bind-loser regression and Phase 59 invariant review.</mitigation>
    <verification>Topology and security-review receipts.</verification>
    <residual_risk>Real scheduling remains pending UAT.</residual_risk>
  </threat>
  <threat id="T63-05" category="Denial of Service" component="final wake lock gate" severity="HIGH" disposition="mitigate" asvs="V12">
    <mitigation>Require concurrency, stale quarantine, bounded timeout, and no-kill coverage.</mitigation>
    <verification>Daemon suite and code/security review receipts.</verification>
    <residual_risk>Real scheduler behavior remains pending UAT.</residual_risk>
  </threat>
  <threat id="T63-06" category="Tampering" component="final runtime/package/workspace gate" severity="HIGH" disposition="mitigate" asvs="V12">
    <mitigation>Require bundled offline payload, integrity receipt, source/cache durability, and exact protected workspace/index restoration.</mitigation>
    <verification>Package/workflow/focused/full-wrapper receipts and fingerprints.</verification>
    <residual_risk>Installed Node relocation remains operator evidence.</residual_risk>
  </threat>
  <threat id="T63-07" category="Elevation of Privilege" component="final Windows launcher gate" severity="CRITICAL" disposition="avoid" asvs="V14">
    <mitigation>Require version-bound PE evidence and command-interpreter blacklist.</mitigation>
    <verification>Windows artifact metadata and package/review receipts.</verification>
    <residual_risk>Real Windows/Chrome launch remains pending UAT.</residual_risk>
  </threat>
  <threat id="T63-08" category="Tampering" component="final registration ownership gate" severity="HIGH" disposition="mitigate" asvs="V12">
    <mitigation>Require exact-owner cross-platform install/uninstall and foreign/shadow/symlink refusal.</mitigation>
    <verification>Install/CLI/review receipts.</verification>
    <residual_risk>Genuine filesystem/registry behavior remains pending UAT.</residual_risk>
  </threat>
  <threat id="T63-09" category="Information Disclosure" component="final diagnostics/UI disclosure gate" severity="HIGH" disposition="mitigate" asvs="V7">
    <mitigation>Require sentinel redaction, browser-safe projection, fixed UI copy, and sanitized review/validation evidence.</mitigation>
    <verification>Diagnostics/UI/review verifier receipts.</verification>
    <residual_risk>CLI expected location remains intentionally local-only.</residual_risk>
  </threat>
  <threat id="T63-10" category="Elevation of Privilege" component="final boot/offline wake gate" severity="HIGH" disposition="mitigate" asvs="V14">
    <mitigation>Require silent boot, offline-only wake, background-only authority, and exact manifest delta.</mitigation>
    <verification>Background/manifest/review receipts.</verification>
    <residual_risk>Real service-worker boot remains pending UAT.</residual_risk>
  </threat>
  <threat id="T63-11" category="Tampering" component="final replay fence gate" severity="HIGH" disposition="mitigate" asvs="V3">
    <mitigation>Require timeout/late/concurrency/current-intent/edit fences and zero replay.</mitigation>
    <verification>Background/UI/review receipts.</verification>
    <residual_risk>Real eviction timing remains pending UAT.</residual_risk>
  </threat>
  <threat id="T63-12" category="Elevation of Privilege" component="final lifecycle authority separation gate" severity="CRITICAL" disposition="mitigate" asvs="V3">
    <mitigation>Require authenticated bridge/preflight authority and no optimistic pairing/provider/task/start mutation.</mitigation>
    <verification>Boundary/background/UI/phase-contract/review receipts.</verification>
    <residual_risk>Live ready/unpaired convergence remains pending UAT.</residual_risk>
  </threat>
  <threat id="T63-G01" category="Repudiation" component="final review/test identity and validation ledger" severity="HIGH" disposition="mitigate" asvs="V7">
    <mitigation>Use exact commit/diff fingerprints, no-argument all-kinds review verification, serial one-pass wrappers, sanitized receipts, correction-and-rereview loop, and immutable pending-UAT schema.</mitigation>
    <verification>No-arg verifier negative fixtures, focused/full workspace fingerprints, and validation/UAT contract audit.</verification>
    <residual_risk>Manual ledger evidence remains deliberately incomplete until the user-directed sweep.</residual_risk>
  </threat>
</threat_model>

<verification>
- Final focused and review/artifact gates pass on the exact reviewed diff.
- Plan 01 workspace-preservation fixtures pass, then the repository-wide suite passes inside the outer Plan 01 wrapper without complete build-graph or raw-index mutation; the inner Phase 60 dirty-path checks remain green.
- Validation records exact sanitized evidence and no skipped/duplicated gate.
- HUMAN-UAT remains entirely unchecked, human_needed, pending, and evidence-empty.
</verification>

<success_criteria>
- All Phase 63 automated gates and the repository-wide suite pass on the reviewed implementation.
- No Phase 59–62 invariant regresses and no user-owned workspace state changes.
- Automated Phase 63 work is complete while genuine human UAT remains explicitly deferred.
</success_criteria>

<output>
Create `.planning/phases/63-native-messaging-host/63-12-SUMMARY.md` when complete.
</output>
