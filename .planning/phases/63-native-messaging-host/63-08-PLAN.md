---
phase: 63-native-messaging-host
plan: "08"
type: execute
wave: 8
depends_on:
  - "63-07"
files_modified:
  - extension/utils/native-host-wake.js
  - extension/background.js
  - extension/manifest.json
  - tests/native-host-background-wake.test.js
  - tests/mcp-bridge-background-dispatch.test.js
autonomous: true
requirements:
  - NATIVE-02
  - NATIVE-03
must_haves:
  truths:
    - "Service-worker boot performs one bounded connectNative presence probe with no message, no wake, no UI event, and only an in-memory present/absent/unknown advisory fact."
    - "Only an existing authoritative `agent_offline` preflight may call sendNativeMessage; ready, API, unpaired, unsupported, refresh, hydration, setup, copy, and boot paths never wake."
    - "Concurrent offline callers share one timed wake operation but each current intent receives one attempt-scoped checking event."
    - "Only exact correlated already_running/started proceeds to authenticated bridge wait and exactly one direct preflight rerun; no delegate.start, message, gesture, challenge, session, feed, tab, or replay is created."
    - "The manifest adds only nativeMessaging and preserves every other permission/host permission byte-for-byte."
  artifacts:
    - path: "extension/utils/native-host-wake.js"
      provides: "Background-only silent probe and one-flight closed wake controller"
      exports: ["globalThis.FsbNativeHostWake"]
    - path: "extension/background.js"
      provides: "Offline-preflight wake composition and one exact rerun"
    - path: "extension/manifest.json"
      provides: "Single additive nativeMessaging permission"
    - path: "tests/native-host-background-wake.test.js"
      provides: "Boot/offline/concurrency/timeout/bridge/rerun/no-replay VM matrix"
  key_links:
    - from: "extension/background.js"
      to: "extension/utils/native-host-wake.js"
      via: "agent_offline-only ensureWake call"
      pattern: "agent_offline"
    - from: "extension/background.js"
      to: "fsbDelegationPreflightResult"
      via: "one initial call plus at most one post-bridge rerun"
      pattern: "fsbDelegationPreflightResult"
---

<objective>
Add the single manifest permission and background-owned silent probe/offline wake orchestration while preserving Phase 61 preflight and Phase 59 authority.

Purpose: Native lifecycle success must only make the daemon reachable; the authenticated bridge and unchanged preflight remain the sole pairing/provider/delegation authority.
Output: Closed wake controller, background integration, exact permission delta, and VM/source-contract tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/63-native-messaging-host/63-CONTEXT.md
@.planning/phases/63-native-messaging-host/63-RESEARCH.md
@.planning/phases/63-native-messaging-host/63-UI-SPEC.md
@.planning/phases/63-native-messaging-host/63-PATTERNS.md
@.planning/phases/61-delegation-ux-sw-eviction-persistence/61-01-SUMMARY.md
@.planning/phases/61-delegation-ux-sw-eviction-persistence/61-06-SUMMARY.md
@extension/background.js
@extension/utils/delegation-preflight.js
@extension/ws/mcp-bridge-client.js
@extension/manifest.json

<decision_trace>
- D63-13: add exactly one permission, nativeMessaging.
- D63-14: background/helper alone owns native API, bridge state, retry, persistence.
- D63-15: boot probe is no-message/no-wake; actual wake is authoritative offline-preflight only.
- D63-16: one-flight timeout/backoff, bridge wait, one preflight rerun, no replay.
- D63-17/D63-18: every failure returns exact offline fallback; reachable unpaired remains unpaired; no secret transport.
</decision_trace>

<interfaces>
Existing pure authority:
```javascript
async function fsbDelegationPreflightResult() {
  const config = await fsbReadAuthoritativeProviderConfig();
  const result = globalThis.FsbDelegationPreflight.check({ ... });
  return { config, result };
}
```

New browser-safe checking event only:
```javascript
{ type: 'FSB_NATIVE_WAKE_CHECKING', attemptId, intentId }
```
No task, outcome, reason, path, host name, platform, secret, or response payload is broadcast.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>63-08-01: Build the closed background-only silent probe and one-flight wake controller</name>
  <files>extension/utils/native-host-wake.js, tests/native-host-background-wake.test.js</files>
  <read_first>63-RESEARCH.md Patterns 6 and 7; Phase 61 background one-flight pattern; exact protocol constants/reasons from Plan 03; Chrome runtime fake patterns in mcp-bridge-background-dispatch.test.js</read_first>
  <behavior>
    - "probePresence calls connectNative, installs listeners immediately, sends no postMessage, closes on a short timer, emits nothing, and caches only present/absent/unknown in memory."
    - "ensureWake creates one safe correlation, one attempt token, one sendNativeMessage, a 12 s timeout, late-settlement fence, and 5 s failure cooldown; concurrent callers receive the same work promise."
    - "Only exact v1 response keys, matching correlation, approved outcome/reason pair, and no prototype/extra fields are accepted; only already_running/started are positive reachability facts."
  </behavior>
  <action>Implement D63-14/D63-15/D63-16 and T63-10/T63-11/T63-12 in a classic-script helper exposed only as `globalThis.FsbNativeHostWake`. Freeze the exact host name and protocol parity with MCP. Boot probe uses `connectNative` with no message and no UI callback; runtime.lastError becomes a content-free advisory state. Actual `ensureWake` uses `sendNativeMessage`, crypto-safe correlation/attempt ids, one module-scoped promise, 12 s timeout, 5 s session-only cooldown after failure, exact response validation, and token-checked cleanup/late ignore. Never persist presence/start success, never pass task/pairing/provider data, and never expose native APIs to UI modules. Write the fake port/runtime/timer/concurrency response matrix first.</action>
  <acceptance_criteria>Boot probe has zero postMessage/sendNativeMessage/UI calls; concurrent wake calls issue one native message; timeout/lastError/malformed/correlation/reason/late cases fail closed; the controller cannot start delegation or read/write secrets.</acceptance_criteria>
  <verify>
    <automated>node tests/native-host-background-wake.test.js --section controller</automated>
  </verify>
  <done>The acceptance criteria above are satisfied and the automated verification passes.</done>
</task>

<task type="auto" tdd="true">
  <name>63-08-02: Integrate wake only inside offline preflight and rerun existing preflight once</name>
  <files>extension/background.js, tests/native-host-background-wake.test.js, tests/mcp-bridge-background-dispatch.test.js</files>
  <read_first>background fsbDelegationPreflightResult/Command and bootstrap path; Phase 61 authoritative start ordering; bridge client state/getState; same-context dispatch invariant</read_first>
  <behavior>
    - "Preflight ready/API/unpaired/unsupported returns unchanged with zero native calls."
    - "Initial agent_offline emits one `{type,attemptId,intentId}` checking event for each valid pending caller, joins one wake, and keeps the command Promise open."
    - "Positive native fact waits up to 5 s for current bridge connectivity, calls fsbDelegationPreflightResult exactly once more, and returns that exact existing result."
    - "Any native/readiness/rerun failure returns the original exact offline result and calls no start/consent/message/session/tab/feed APIs."
  </behavior>
  <action>Implement D63-15–D63-18 and T63-10/T63-11/T63-12. Load the helper in background and start the silent boot probe only after native listeners/dependencies exist; do not await it for UI/bootstrap. Extend the internal preflight request with an optional exact safe `intentId` used only for the checking event while retaining the exact preflight response shape. Call the existing pure preflight once; only exact `agent_offline` may broadcast the closed checking event and join `ensureWake`. On already_running/started, wait for existing bridge connection/pairing facts (no native secret), then invoke `fsbDelegationPreflightResult` directly once—never recurse into the command. Return the rerun's ready/unpaired/offline/unsupported result unchanged. Do not call `delegate.start`, consent, self-send dispatch, replay, chat, storage, controller, registry, or tab APIs. Use normal runtime fanout only for the UI event, not background self-dispatch.</action>
  <acceptance_criteria>VM counters prove boot/non-offline zero-wake, one shared native request, one event per current intent, one bridge wait, at most two total preflight evaluations, exact convergence, and zero replay/optimistic mutation across every terminal case.</acceptance_criteria>
  <verify>
    <automated>node tests/native-host-background-wake.test.js &amp;&amp; node tests/mcp-bridge-background-dispatch.test.js</automated>
  </verify>
  <done>The acceptance criteria above are satisfied and the automated verification passes.</done>
</task>

<task type="auto">
  <name>63-08-03: Add only nativeMessaging and pin background-only native authority</name>
  <files>extension/manifest.json, tests/native-host-background-wake.test.js, tests/mcp-bridge-background-dispatch.test.js</files>
  <read_first>extension/manifest.json exact permission/host arrays; 63-UI-SPEC implementation boundary; Phase 61 Chrome 116/no-native source pins to replace precisely</read_first>
  <action>Implement D63-13 and T63-10. Add exactly one `nativeMessaging` string to the current permission array; preserve all other permissions, host_permissions, minimum Chrome version, worker path, web-accessible resources, and content scripts byte-for-byte. Replace prior no-native assertions with exact authority: only `background.js` and the one background helper may reference `connectNative`, `sendNativeMessage`, or the host name; sidepanel, preflight utility, bridge client, options/providers UI, content scripts, and all other extension modules remain native-free. Pin that no boot/provider refresh/setup/doctor-copy/tab activation path calls wake.</action>
  <acceptance_criteria>The manifest diff is exactly one permission; source scans find native APIs only in the two approved background-owned files; all Phase 59–62 permission and load-order assertions remain green.</acceptance_criteria>
  <verify>
    <automated>node tests/native-host-background-wake.test.js --section manifest-and-authority &amp;&amp; node tests/mcp-bridge-background-dispatch.test.js</automated>
  </verify>
  <done>The acceptance criteria above are satisfied and the automated verification passes.</done>
</task>

</tasks>

<threat_model>
  <block_on>HIGH,CRITICAL</block_on>
  <residual_human_evidence_boundary>Real Chrome native API error timing, service-worker boot, and live paired/unpaired reconnect behavior remain pending UAT; VM tests do not claim browser execution.</residual_human_evidence_boundary>
  <threat id="T63-01" category="Tampering" component="native response parser in background" severity="HIGH" disposition="mitigate" asvs="V5">
    <mitigation>Accept only exact ordinary own-data v1 response keys, matching correlation, and approved outcome/reason pairs; ignore every malformed or late settlement.</mitigation>
    <verification>Malformed/prototype/extra-key/correlation/reason/late response matrix with zero continuation.</verification>
    <residual_risk>Real Chrome serialization behavior remains pending browser UAT.</residual_risk>
  </threat>
  <threat id="T63-10" category="Elevation of Privilege" component="service-worker boot and delegation preflight wake routing" severity="HIGH" disposition="mitigate" asvs="V14">
    <mitigation>Boot uses a no-message advisory probe; only an exact existing agent_offline result can call sendNativeMessage; one permission is added and native APIs stay background-owned.</mitigation>
    <verification>Boot/non-offline call counters, exact manifest delta, and source authority scans.</verification>
    <residual_risk>Boot still starts a no-op host process by Chrome design; live behavior remains pending UAT.</residual_risk>
  </threat>
  <threat id="T63-11" category="Tampering" component="one-flight wake attempt and current Send intent" severity="HIGH" disposition="mitigate" asvs="V3">
    <mitigation>Use crypto-safe attempt/correlation ids, one shared promise, bounded timeout/cooldown, token-checked cleanup, late-ignore, one event per current intent, and no replay.</mitigation>
    <verification>Fake timers/concurrency/late races, checking-event counts, bridge wait/preflight counts, and zero start/message/session/tab/feed mutations.</verification>
    <residual_risk>Real service-worker eviction/timer scheduling remains pending Chrome UAT.</residual_risk>
  </threat>
  <threat id="T63-12" category="Elevation of Privilege" component="native lifecycle fact entering authenticated delegation preflight" severity="CRITICAL" disposition="mitigate" asvs="V3">
    <mitigation>Positive native facts wait for authenticated bridge state and trigger exactly one direct existing preflight rerun; no task, provider, pairing, secret, or start authority is transported.</mitigation>
    <verification>Ready/unpaired/offline convergence, exact preflight shape, one rerun, source scans, and no-replay counters.</verification>
    <residual_risk>Live paired/unpaired reconnect convergence remains pending UAT.</residual_risk>
  </threat>
</threat_model>

<verification>
- Both background-focused suites pass with exact call counts and no replay.
- Manifest diff contains only `nativeMessaging`.
- Existing preflight response and Phase 59/61 authority contracts remain exact.
</verification>

<success_criteria>
- Boot probe is silent and cannot wake.
- Actual wake is offline-preflight-only and coalesced.
- Native success reruns existing preflight exactly once and never replays user intent.
</success_criteria>

<output>
Create `.planning/phases/63-native-messaging-host/63-08-SUMMARY.md` when complete.
</output>
