---
phase: 63-native-messaging-host
plan: "10"
type: execute
wave: 10
depends_on:
  - "63-09"
files_modified:
  - .planning/phases/63-native-messaging-host/63-HUMAN-UAT.md
  - .planning/phases/63-native-messaging-host/63-VALIDATION.md
  - scripts/run-phase63-focused-tests.mjs
  - package.json
  - .github/workflows/ci.yml
  - tests/mcp-version-parity.test.js
  - tests/delegation-phase-contract.test.js
autonomous: true
requirements:
  - NATIVE-01
  - NATIVE-02
  - NATIVE-03
  - NATIVE-04
must_haves:
  truths:
    - "Every genuine macOS/Linux/Windows/Chrome/accessibility scenario is present exactly once as unchecked, human_needed, pending, and evidence-empty; automation never promotes it."
    - "All four new focused suites, every extended seam, package/artifact checks, and the UAT-honesty gate run serially in the root/CI contracts exactly once; the MCP build supplies one source-mode and one compiled-mode boundary pass with no redundant all-mode root invocation."
    - "A workspace-preserving focused wrapper runs the complete Phase 63 automated matrix without altering protected pre-existing dirty bytes or the Git index."
    - "The phase contract mechanically maps NATIVE-01..04, D63-01..25, T63-01..12, UI rules, architecture links, forbidden patterns, and every final task id."
  artifacts:
    - path: ".planning/phases/63-native-messaging-host/63-HUMAN-UAT.md"
      provides: "Single honest end-of-milestone Phase 63 UAT ledger"
    - path: "scripts/run-phase63-focused-tests.mjs"
      provides: "Workspace-preserving serial focused gate"
    - path: "tests/delegation-phase-contract.test.js"
      provides: "Full Phase 63 requirement/decision/threat/authority/UAT contract"
    - path: ".planning/phases/63-native-messaging-host/63-VALIDATION.md"
      provides: "Actual plan/task/wave mapping for all Nyquist slices"
  key_links:
    - from: "package.json"
      to: "tests/mcp-native-host-*.test.js"
      via: "each focused test appears once after MCP build"
      pattern: "mcp-native-host"
    - from: "tests/delegation-phase-contract.test.js"
      to: ".planning/phases/63-native-messaging-host/63-HUMAN-UAT.md"
      via: "unchecked human_needed/pending/empty evidence parser"
      pattern: "63-HUMAN-UAT"
    - from: "scripts/run-mcp-build-preserving-workspace.mjs"
      to: "tests/delegation-phase-contract.test.js and tests/mcp-version-parity.test.js"
      via: "one closed commands-json sequence while fresh compiled output exists, followed by complete mcp/build graph and raw Git index restoration"
      pattern: "commands-json"
---

<objective>
Close Phase 63's deterministic contract, root/CI wiring, Nyquist map, and honest final human-UAT ledger before reviews and the guarded full suite.

Purpose: Source/platform fakes and packed artifacts must block automated completion, but none may impersonate the deferred real OS/browser/accessibility evidence.
Output: One pending UAT ledger, complete focused runner, serial root/CI gates, parity/phase contract, and reconciled validation task map.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/63-native-messaging-host/63-CONTEXT.md
@.planning/phases/63-native-messaging-host/63-RESEARCH.md
@.planning/phases/63-native-messaging-host/63-UI-SPEC.md
@.planning/phases/63-native-messaging-host/63-VALIDATION.md
@.planning/phases/61-delegation-ux-sw-eviction-persistence/61-08-SUMMARY.md
@package.json
@scripts/run-phase60-full-tests.mjs
@scripts/run-mcp-build-preserving-workspace.mjs
@tests/mcp-version-parity.test.js
@tests/delegation-phase-contract.test.js

<decision_trace>
- D63-01..D63-25: this plan installs the mechanical coverage audit for every locked Phase 63 bullet.
- D63-23/D63-24: focused/full/package/source/review gates are blocking.
- D63-25: genuine installed-host/browser/accessibility evidence stays pending in one ledger.
</decision_trace>
</context>

<tasks>

<task type="auto">
  <name>63-10-01: Create the single final pending human-UAT ledger without running it</name>
  <files>.planning/phases/63-native-messaging-host/63-HUMAN-UAT.md, tests/delegation-phase-contract.test.js</files>
  <read_first>63-RESEARCH.md Deferred UAT Ledger Entries and Manual-Only Verifications; 63-UI-SPEC Deferred Human Evidence; Phase 61 HUMAN-UAT/contract pattern</read_first>
  <action>Implement D63-25 exactly. Create one ledger with unchecked headings and explicit `status: human_needed`, `result: pending`, and empty `evidence:` for every genuine scenario: macOS published-id install/boot-probe/offline wake/already-running attach/doctor/uninstall; macOS unpacked explicit-id and allowlist-mismatch/refusal; Linux Google Chrome install/wake/attach/doctor/uninstall; Windows x64 HKCU 32/64-view shadow plus packaged `.exe` wake/attach/doctor/uninstall; Windows arm64 packaged-artifact/bootstrap behavior; published/unpacked Chrome missing/malformed/timeout/wrong-product/ready/unpaired/no-replay flows; rendered light/dark/narrow/zoom/forced-colors/reduced-motion transitions; and keyboard focus/screen-reader announcement order. Include sanitized prerequisites/steps/expected results, but execute none and attach no synthetic/source evidence. Extend the phase contract to reject checked boxes, any result other than pending, nonempty evidence, missing scenario, or wording that treats mocks/adapters/pack checks as live UAT.</action>
  <acceptance_criteria>All genuine scenarios are present, unchecked, human_needed, pending, evidence-empty, and explicitly deferred to the single v0.9.91 milestone-end sweep; no blocking checkpoint exists in any Phase 63 plan.</acceptance_criteria>
  <verify>
    <automated>node tests/delegation-phase-contract.test.js --section phase63-uat-ledger</automated>
  </verify>
  <done>The acceptance criteria above are satisfied and the automated verification passes.</done>
</task>

<task type="auto">
  <name>63-10-02: Wire the focused matrix, package gates, and exact root/CI serial order</name>
  <files>scripts/run-phase63-focused-tests.mjs, package.json, .github/workflows/ci.yml, tests/mcp-version-parity.test.js, tests/delegation-phase-contract.test.js</files>
  <read_first>63-VALIDATION sampling/test matrix; package.json serial test command; run-phase60-full-tests workspace preservation; CI mcp-smoke and Windows job from Plan 01</read_first>
  <action>Implement D63-23/D63-24 and all T63 verification slices. Create a stronger outer workspace-preserving wrapper using the Phase 60 contract: snapshot every pre-existing tracked dirty byte/mode/symlink, exact Git index bytes/entries, untracked listing/bytes, status set, and the protected `mcp/build/**` graph before any child; install idempotent cleanup for success, nonzero exit, child signal/spawn exception, wrapper exception, SIGINT, and SIGTERM; restore snapshotted state; remove only paths created by the run; and fail on any unrelated/index drift. It must never stage, normalize, check out, or hand-edit protected `mcp/build/index.js`. For the MCP build and every compiled-output-dependent test/package/boundary command, invoke the Plan 01 `scripts/run-mcp-build-preserving-workspace.mjs` exactly once with one closed `--commands-json` sequence; do not invoke `npm --prefix mcp run build` directly. Run background/diagnostics/install/UI/topology/version/phase-contract seams in the declared serial order within the outer guard. Treat the inner MCP build's prebuild `--source` and postbuild `--compiled` invocations as the two required halves of one boundary gate; assert both occur exactly once and do not add a redundant default/`--all` boundary call to the root or focused chain. In root `test`, add each of the four new suites and the pack gate exactly once after the existing MCP build and before dependent extended suites without dropping/reordering unrelated commands; that root build is permitted only because root/full execution is itself guarded by this stronger wrapper or `run-phase60-full-tests.mjs`. CI's existing extension job already invokes root `npm test`; rename that single step to identify the Phase 63 contract and source-pin that it remains the sole Linux invocation instead of adding a duplicate focused-wrapper step. Retain the separate required Windows artifact and macOS/Linux offline-payload jobs from Plan 01. Expand version parity for protocol/health/host name/default id/manifest permission/exact dependency pins/bundle roster/lock receipt/package artifacts and historical-shim absence. Expand the phase contract for exact source authority links, no CHAN relaxation, no forbidden launcher/host/UI patterns, and all four NATIVE ids.</action>
  <acceptance_criteria>The focused wrapper exits 0 and proves exact protected build-tree/dirty-byte/mode/symlink/index/untracked identity after success plus injected failure/signal/exception cleanup; every new suite/pack gate appears once in the root chain and therefore once in the existing Linux CI test step; the guarded inner MCP build runs one source and one compiled boundary mode with no redundant root all-mode call; no duplicate Phase 63 CI invocation exists; offline bundle, artifact, workspace, and boundary failures are blocking; existing Phase 59–62 contracts remain unchanged.</acceptance_criteria>
  <verify>
    <automated>node scripts/run-phase63-focused-tests.mjs</automated>
  </verify>
  <done>The acceptance criteria above are satisfied and the automated verification passes.</done>
</task>

<task type="auto">
  <name>63-10-03: Reconcile the Nyquist task map and mechanically audit every source decision and threat</name>
  <files>.planning/phases/63-native-messaging-host/63-VALIDATION.md, tests/delegation-phase-contract.test.js, tests/mcp-version-parity.test.js</files>
  <read_first>All 63-01..12 PLAN files and completed 63-01..09 SUMMARY files; 63-01-PLAN Task 63-01-01 complete build-wrapper and raw-index restoration contract; provisional 63-VALIDATION rows; CONTEXT locked bullets; RESEARCH plan decomposition/threats; UI-SPEC acceptance criteria</read_first>
  <action>Replace and expand the provisional `63-TBD-*` table into one exact row for every task in Plans 63-01 through 63-12, with its actual plan/task/wave id, requirements, threat owner, blocking command, file-existence state, and honest pending/green status; future review/full-suite rows remain pending until those plans execute. Mark only harness existence/plan mapping complete, not implementation results that have not run. Add a mechanical phase contract covering: roadmap goal; NATIVE-01..04; derived D63-01..25 in source order; T63-01..12; applicable ASVS V2/V3/V4/V5/V7/V12/V13/V14 themes; four focused suites; exact architecture/import links; Windows package proof; atomic platform install/uninstall; doctor projection; one permission; offline-only no-replay wake; exact UI copy/a11y; review tasks; full-suite task; and honest UAT ledger. Fail on any missing/duplicate task row, deferred idea, `.bat`/`.cmd`/SEA/cache/worktree registration, direct host agent authority, side-panel native authority, secret/path projection, second page/CTA/live region, or relaxed Phase 59 invariant. Run the delegation contract and version-parity check together in one closed `--commands-json` sequence through the Plan 01 workspace-preserving build wrapper so both consume the same fresh compiled graph before cleanup; the outer wrapper owns unconditional restoration of the complete pre-existing `mcp/build/**` graph and raw Git index after the sequence.</action>
  <acceptance_criteria>Validation has no TBD plan/task/wave values, all 30 tasks map exactly once to an automated slice, every requirement/decision/threat has a concrete source/test owner, and the contract fails for an intentionally removed or duplicated mapping. Both checks pass inside one Plan 01 build-wrapper lifecycle, after which the complete protected build graph and raw Git index match their pre-run state.</acceptance_criteria>
  <verify>
    <automated>node scripts/run-mcp-build-preserving-workspace.mjs --commands-json '[["node","tests/delegation-phase-contract.test.js"],["node","tests/mcp-version-parity.test.js"]]'</automated>
  </verify>
  <done>The acceptance criteria above are satisfied and the automated verification passes.</done>
</task>

</tasks>

<threat_model>
  <block_on>HIGH,CRITICAL</block_on>
  <residual_human_evidence_boundary>Every ledger scenario remains residual `human_needed`; this plan verifies only complete, unchecked, pending, evidence-empty deferral and never promotes source/mock/package results.</residual_human_evidence_boundary>
  <threat id="T63-01" category="Denial of Service" component="protocol focused and root gates" severity="HIGH" disposition="mitigate" asvs="V5">
    <mitigation>Require the complete malformed/oversize/multi-frame/own-data protocol suite in focused, root, and CI chains exactly once.</mitigation>
    <verification>Occurrence-count contract, protocol negative controls, and focused runner result.</verification>
    <residual_risk>Real Chrome framing remains human evidence.</residual_risk>
  </threat>
  <threat id="T63-02" category="Elevation of Privilege" component="host authority source/package gate" severity="CRITICAL" disposition="mitigate" asvs="V4">
    <mitigation>Require source and compiled transitive boundary passes plus exact serve tuple and historical-shim exclusion.</mitigation>
    <verification>Boundary occurrence counts, malicious fixture, phase contract, and package inspection.</verification>
    <residual_risk>Real process execution remains human evidence.</residual_risk>
  </threat>
  <threat id="T63-03" category="Spoofing" component="health identity/readiness gate" severity="HIGH" disposition="mitigate" asvs="V13">
    <mitigation>Require wrong-product/protocol/readiness tests and exact health/version parity.</mitigation>
    <verification>Daemon suite, topology suite, and version contract.</verification>
    <residual_risk>Real local port contention remains human evidence.</residual_risk>
  </threat>
  <threat id="T63-04" category="Tampering" component="serve secret lifecycle regression gate" severity="HIGH" disposition="mitigate" asvs="V2">
    <mitigation>Keep bind-loser zero-auth regression and Phase 59 CHAN contract blocking.</mitigation>
    <verification>Topology call order, bind-race section, and no-CHAN-relaxation source contract.</verification>
    <residual_risk>Real scheduler interleavings remain human evidence.</residual_risk>
  </threat>
  <threat id="T63-05" category="Denial of Service" component="wake coalescing and stale-lock gate" severity="HIGH" disposition="mitigate" asvs="V12">
    <mitigation>Require one-spawn concurrency, tokened stale recovery, bounded timeout, and no-kill assertions.</mitigation>
    <verification>Daemon fake-clock/filesystem matrix and focused/root inclusion.</verification>
    <residual_risk>Real OS scheduling remains human evidence.</residual_risk>
  </threat>
  <threat id="T63-06" category="Tampering" component="bundled runtime, package integrity, and workspace output" severity="HIGH" disposition="mitigate" asvs="V12">
    <mitigation>Require exact pins/bundle closure/lock receipt/offline install, protected workspace wrapper, and source/cache-removal durability.</mitigation>
    <verification>Mac/Linux offline workflow, package parity, wrapper failure/signal tests, and protected fingerprint checks.</verification>
    <residual_risk>Installed Node relocation remains human/operator evidence.</residual_risk>
  </threat>
  <threat id="T63-07" category="Elevation of Privilege" component="Windows PE package gate" severity="CRITICAL" disposition="avoid" asvs="V14">
    <mitigation>Block on version-bound x64/arm64 PE artifacts and forbid batch/shell/SEA fallbacks.</mitigation>
    <verification>Required Windows workflow, PE metadata, pack blacklist, and CI dependency assertions.</verification>
    <residual_risk>Real Chrome/Windows execution remains human evidence.</residual_risk>
  </threat>
  <threat id="T63-08" category="Tampering" component="install/uninstall ownership gate" severity="HIGH" disposition="mitigate" asvs="V12">
    <mitigation>Require three-platform exact-owner, symlink, foreign, shadow, idempotence, rollback, and adjacent-preservation tests.</mitigation>
    <verification>Install suite, CLI routing suite, and phase contract.</verification>
    <residual_risk>Genuine filesystem/registry behavior remains human evidence.</residual_risk>
  </threat>
  <threat id="T63-09" category="Information Disclosure" component="doctor and UI projection gates" severity="HIGH" disposition="mitigate" asvs="V7">
    <mitigation>Require one read-only snapshot, sentinel redaction, five-key browser projection, fixed UI copy, and no native/local details.</mitigation>
    <verification>Diagnostics/UI suites and exact source projection contract.</verification>
    <residual_risk>CLI expected location is intentionally local-only.</residual_risk>
  </threat>
  <threat id="T63-10" category="Elevation of Privilege" component="boot/offline wake authority gate" severity="HIGH" disposition="mitigate" asvs="V14">
    <mitigation>Require silent no-message boot probe, offline-only wake, background-only native APIs, and exact one-permission delta.</mitigation>
    <verification>Background VM, manifest/source scans, and root occurrence contract.</verification>
    <residual_risk>Real service-worker boot remains human evidence.</residual_risk>
  </threat>
  <threat id="T63-11" category="Tampering" component="attempt and intent replay gate" severity="HIGH" disposition="mitigate" asvs="V3">
    <mitigation>Require timeout/late/concurrency fencing, one rerun, edit invalidation, and zero replay/optimistic mutation.</mitigation>
    <verification>Background and side-panel race/state matrices.</verification>
    <residual_risk>Real eviction and panel lifecycle timing remains human evidence.</residual_risk>
  </threat>
  <threat id="T63-12" category="Elevation of Privilege" component="lifecycle-to-delegation authority gate" severity="CRITICAL" disposition="mitigate" asvs="V3">
    <mitigation>Mechanically pin authenticated bridge plus existing preflight as sole authority and forbid native task/pairing/provider/start projection.</mitigation>
    <verification>Phase contract, boundary scan, background/UI zero-mutation suites, and exact response/preflight schemas.</verification>
    <residual_risk>Live ready/unpaired/delegation convergence remains human evidence.</residual_risk>
  </threat>
</threat_model>

<verification>
- Focused wrapper exits 0 and preserves the dirty workspace contract.
- Root and CI include every Phase 63 automated gate once.
- Validation contains actual plan/task/wave ids and all task commands.
- UAT ledger stays wholly unchecked/pending/evidence-empty.
</verification>

<success_criteria>
- Automated coverage is complete, serial, source/artifact aware, and workspace safe.
- All NATIVE requirements, locked decisions, and T63 threats are mechanically traced.
- No synthetic adapter/source/pack result is represented as live UAT.
</success_criteria>

<output>
Create `.planning/phases/63-native-messaging-host/63-10-SUMMARY.md` when complete.
</output>
