---
phase: 63-native-messaging-host
plan: "09"
type: execute
wave: 9
depends_on:
  - "63-08"
files_modified:
  - extension/ui/sidepanel.js
  - extension/ui/sidepanel.css
  - tests/delegation-sidepanel-ui.test.js
autonomous: true
requirements:
  - NATIVE-02
must_haves:
  truths:
    - "Only the current pending Send intent can render one exact checking card and one polite announcement; boot/presence/late/stale facts render nothing."
    - "Checking preserves composer bytes and focus, suppresses duplicate Send, creates no message/feed/session/tab/challenge/start state, and uses no action row, alert, new card, or new live region."
    - "Any composer input event during checking irreversibly invalidates that intent—even if text is later restored byte-for-byte; neither captured nor edited text starts without a fresh explicit Send."
    - "Failure and unpaired results reuse the exact Phase 61 copy/actions/focus behavior; ready continues existing consent/start flow with no native success toast."
    - "The approved info tone, narrow wrapping, forced-colors cue, and reduced-motion spinner suppression use existing tokens."
  artifacts:
    - path: "extension/ui/sidepanel.js"
      provides: "Attempt-scoped checking renderer and stale-text continuation fence"
    - path: "extension/ui/sidepanel.css"
      provides: "Token-only info spinner, narrow, forced-color, and reduced-motion rules"
    - path: "tests/delegation-sidepanel-ui.test.js"
      provides: "Exact copy/DOM/focus/mutation/convergence/accessibility contract"
  key_links:
    - from: "extension/ui/sidepanel.js"
      to: "FSB_NATIVE_WAKE_CHECKING"
      via: "current intentId/attemptId event gate"
      pattern: "FSB_NATIVE_WAKE_CHECKING"
    - from: "extension/ui/sidepanel.js"
      to: "_renderDelegationPreflightFailure"
      via: "existing exact offline/unpaired fallback"
      pattern: "_renderDelegationPreflightFailure"
---

<objective>
Render the approved transient checking state in the existing delegation card while preserving intent, focus, accessibility, and exact fallback behavior.

Purpose: A bounded wake attempt must be visible and truthful without becoming optimistic UI authority or adding a new recovery surface.
Output: Exact checking presentation, edit-invalidated continuation, token-only CSS, and deterministic DOM/source tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/63-native-messaging-host/63-UI-SPEC.md
@.planning/phases/63-native-messaging-host/63-CONTEXT.md
@.planning/phases/63-native-messaging-host/63-PATTERNS.md
@.planning/phases/61-delegation-ux-sw-eviction-persistence/61-07-SUMMARY.md
@.planning/phases/63-native-messaging-host/63-08-SUMMARY.md
@extension/ui/sidepanel.html
@extension/ui/sidepanel.js
@extension/ui/sidepanel.css
@tests/delegation-sidepanel-ui.test.js

<decision_trace>
- D63-16: current intent joins one checking attempt; no replay after reconnect.
- D63-17: failure restores exact offline doctor/setup state without optimistic mutation.
- D63-18: reachable unpaired uses the existing pairing state.
- D63-19: exact Phase 61 card/live-region/focus/tokens/layout/theme/motion are reused.
</decision_trace>

<interfaces>
Exact copy from the approved UI contract:
```text
Heading: Checking local agent service
Body: FSB is trying to make the local agent service available. Your message has not been sent.
Announcement: Checking local agent service. Your message has not been sent.
Header: Checking agent service
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>63-09-01: Render one attempt-scoped checking state and fence edited intent</name>
  <files>extension/ui/sidepanel.js, tests/delegation-sidepanel-ui.test.js</files>
  <read_first>63-UI-SPEC closed state matrix and semantic contract; sidepanel handleSendMessage/pendingPreflight/runtime listener; existing mount/heading/announcer/failure helpers</read_first>
  <behavior>
    - "Current preflight request carries a safe intentId and snapshots trimmed task, exact raw composer text, and the monotonic composer edit revision."
    - "Matching checking event while pending renders one info-tone h2/body/spinner in the existing state card, run aria-busy true, one polite announcement, header copy, empty feed, and no focus/action/alert."
    - "Duplicate, late, wrong-intent, boot, restored-panel, or post-settlement events do nothing and do not announce."
    - "Any post-Send input revision or raw composer mismatch at settlement clears pending/checking without consent/start and requires a new Send, including edit-then-revert."
  </behavior>
  <action>Implement D63-16/D63-19 and T63-11/T63-12. Maintain a monotonic composer edit revision in the existing input listener. At explicit Send, generate a safe intent id and snapshot the revision plus `chatInput.textContent` byte-for-byte in addition to the trimmed task; include only the id in the preflight request. Add a strict listener for the exact `{attemptId,intentId,type}` checking event and render only when it matches the current pending intent. Reuse `_ensureDelegationMount`, text-only element helpers, semantic `h2`, existing run region, and existing announcer; add no static `sidepanel.html` node, second mount/card, or new live region. Set `aria-busy=true`, leave feed/control empty/hidden, keep composer editable and Send gated, and do not call focus. Announce once per intent. Before any ready continuation, require both the unchanged edit revision and byte-identical raw text; any input event permanently invalidates that pending intent even if the user later restores identical bytes. On invalidation, clear pending presentation and require a fresh Send without invoking consent/start for either value. Snapshot every optimistic state surface in tests.</action>
  <acceptance_criteria>DOM tests prove exact copy/one-card/one-spinner/one-announcement, no focus move/actions/alert/new region, exact composer bytes, duplicate/late fences, edit and edit-then-revert invalidation, and zero message/session/feed/tab/challenge/start/storage mutation through pending/failure.</acceptance_criteria>
  <verify>
    <automated>node tests/delegation-sidepanel-ui.test.js --section native-wake-checking</automated>
  </verify>
  <done>The acceptance criteria above are satisfied and the automated verification passes.</done>
</task>

<task type="auto" tdd="true">
  <name>63-09-02: Converge on existing ready/unpaired/offline states with approved responsive and a11y styling</name>
  <files>extension/ui/sidepanel.js, extension/ui/sidepanel.css, tests/delegation-sidepanel-ui.test.js</files>
  <read_first>63-UI-SPEC dimensions 2–5 and deterministic verification; existing `_renderDelegationPreflightFailure`; sidepanel CSS delegation card/tokens/reduced-motion ranges</read_first>
  <behavior>
    - "started/already_running plus ready response enters existing consent/trusted-start only for unchanged current intent; no success toast/card/announcement/focus stop."
    - "unpaired uses exact existing pair heading/body/setup action; all missing/error/malformed/timeout/readiness/rerun-offline cases use exact Agent offline card/doctor/setup/alert/focus."
    - "Checking uses var(--fsb-info), existing card typography/spacing, inline aria-hidden spinner, <=350px wrapping, forced-colors border/icon/text, and no spinner transition/transform under reduced motion."
  </behavior>
  <action>Implement D63-17–D63-19 and T63-11/T63-12. Route the final unchanged preflight result through the existing branches only: ready to existing consent/trusted start, unpaired and offline to `_renderDelegationPreflightFailure`. Do not introduce Retry/Install/Repair/Wake/Restart actions, native success copy, success green, primary orange, countdown, platform/path/reason text, success toast, second card, or second live region. Extend existing CSS blocks with token-only info border/spinner and exact narrow/forced-color/reduced-motion behavior; `sidepanel.html` remains unchanged. Pin UI source against native APIs, process/shell/platform branches, host names, manifest/registry/binary paths, secret/task response fields, or native reason parsing.</action>
  <acceptance_criteria>Ready/unpaired/all-failure table tests converge on exact existing states, UI source contains no native authority/detail, and CSS/DOM source pins satisfy light/dark/narrow/forced-colors/reduced-motion/focus/live-region contracts without claiming rendered UAT.</acceptance_criteria>
  <verify>
    <automated>node tests/delegation-sidepanel-ui.test.js &amp;&amp; node tests/native-host-background-wake.test.js</automated>
  </verify>
  <done>The acceptance criteria above are satisfied and the automated verification passes.</done>
</task>

</tasks>

<threat_model>
  <block_on>HIGH,CRITICAL</block_on>
  <residual_human_evidence_boundary>Actual layout, theme, zoom, screen reader, focus, forced-colors, and reduced-motion behavior in Chrome remains `human_needed`; source/DOM harnesses are not live UAT.</residual_human_evidence_boundary>
  <threat id="T63-09" category="Information Disclosure" component="checking DOM, announcement, and browser-safe UI source" severity="HIGH" disposition="mitigate" asvs="V7">
    <mitigation>Render exact fixed text only, accept only attempt/intent ids, expose no native response/reason/path/platform/registry/secret/task detail, and reuse one announcer.</mitigation>
    <verification>DOM/source sentinel scans, exact copy/key assertions, one-announcement count, and native-authority/detail blacklist.</verification>
    <residual_risk>Screen-reader phrasing/order on real Chrome remains pending UAT.</residual_risk>
  </threat>
  <threat id="T63-11" category="Tampering" component="pending Send intent and mutable composer" severity="HIGH" disposition="mitigate" asvs="V3">
    <mitigation>Gate on current intent/attempt, snapshot raw text and monotonic edit revision, permanently invalidate any edited pending intent including edit-then-revert, and require a fresh explicit Send.</mitigation>
    <verification>Duplicate/late/wrong-intent/restored-panel races, edit and edit-then-revert tests, and zero start/message/storage/session/feed/tab mutation snapshots.</verification>
    <residual_risk>Real service-worker/panel restoration scheduling remains pending Chrome UAT.</residual_risk>
  </threat>
  <threat id="T63-12" category="Elevation of Privilege" component="checking settlement to consent/start presentation" severity="CRITICAL" disposition="mitigate" asvs="V3">
    <mitigation>Route only the unchanged existing final preflight result through existing ready, unpaired, or offline branches; native success creates no optimistic state or UI authority.</mitigation>
    <verification>Closed convergence table, no success toast/card/focus stop, existing branch call counts, and background authority regression.</verification>
    <residual_risk>Live consent and pairing transitions remain pending browser UAT.</residual_risk>
  </threat>
</threat_model>

<verification>
- Dedicated side-panel UI suite passes exact copy, state, mutation, and accessibility contracts.
- Background wake suite stays green, proving UI did not acquire native authority.
- `extension/ui/sidepanel.html` remains unchanged.
</verification>

<success_criteria>
- User sees truthful checking only for their current offline Send intent.
- Task bytes and focus are preserved; edits can never auto-continue.
- Existing offline/unpaired/consent behavior remains exact, with no new page/CTA/live region.
</success_criteria>

<output>
Create `.planning/phases/63-native-messaging-host/63-09-SUMMARY.md` when complete.
</output>
