---
phase: 63-native-messaging-host
plan: "02"
type: execute
wave: 2
depends_on:
  - "63-01"
files_modified:
  - mcp/src/http.ts
  - mcp/src/agent-providers/serve-delegation.ts
  - mcp/src/index.ts
  - tests/mcp-native-host-daemon.test.js
  - tests/mcp-bridge-topology.test.js
autonomous: true
requirements:
  - NATIVE-03
must_haves:
  truths:
    - "A process that loses the loopback HTTP bind cannot rotate the live daemon's bridge secret, connect the bridge, push inventory, or advertise serve readiness."
    - "The health endpoint distinguishes FSB protocol/readiness from a generic open port and remains false until recovery, auth preparation, bridge connection, and inventory push finish."
    - "Secret rotation happens exactly once after successful bind and recovery and before authenticated bridge connect."
  artifacts:
    - path: "mcp/src/http.ts"
      provides: "Product-specific bounded health and explicit serveReady transition"
      exports: ["startHttpServer"]
    - path: "mcp/src/agent-providers/serve-delegation.ts"
      provides: "Post-bind prepareBridgeAuth hook and exact readiness ordering"
      exports: ["startServeDelegation"]
    - path: "tests/mcp-bridge-topology.test.js"
      provides: "Bind-winner/bind-loser secret-rotation regression"
  key_links:
    - from: "mcp/src/index.ts"
      to: "mcp/src/agent-providers/serve-delegation.ts"
      via: "injected prepareBridgeAuth callback"
      pattern: "prepareBridgeAuth.*rotateBridgeSessionSecret"
    - from: "mcp/src/agent-providers/serve-delegation.ts"
      to: "mcp/src/http.ts"
      via: "markServeReady only after inventory push"
      pattern: "pushInventory[\\s\\S]*markServeReady"
---

<objective>
Correct the current pre-bind secret-rotation race and make product-specific serve readiness authoritative before native wake concurrency is introduced.

Purpose: A losing concurrent `serve` must be inert; otherwise a native wake storm can invalidate the already-running daemon's authenticated extension session.
Output: Safe bind/recover/auth/connect/inventory/readiness ordering and a two-lifecycle regression harness.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/63-native-messaging-host/63-CONTEXT.md
@.planning/phases/63-native-messaging-host/63-RESEARCH.md
@.planning/phases/63-native-messaging-host/63-PATTERNS.md
@.planning/phases/59-reverse-request-channel-security-foundation/59-CONTEXT.md
@mcp/src/http.ts
@mcp/src/agent-providers/serve-delegation.ts
@mcp/src/index.ts
@tests/mcp-bridge-topology.test.js
@scripts/run-mcp-build-preserving-workspace.mjs

<decision_trace>
- D63-04: loopback binding and every CHAN Origin/Host/secret/pairing invariant remain unchanged.
- D63-05: concurrent starts coalesce safely and losers cannot disturb an independent daemon.
- D63-06: readiness is a daemon reachability fact only.
</decision_trace>

<interfaces>
Current lifecycle interface to extend:
```typescript
export interface ServeDelegationHttpServer {
  readonly endpoint: string;
  readonly healthEndpoint: string;
  close(): Promise&lt;void&gt;;
}

export interface ServeDelegationDependencies {
  readonly startHttp?: (...) =&gt; Promise&lt;ServeDelegationHttpServer&gt;;
  readonly pushInventory?: (bridge: ServeDelegationBridge) =&gt; Promise&lt;void&gt;;
}
```

Unsafe current composition to remove:
```typescript
rotateBridgeSessionSecret();
const lifecycle = await startServeDelegation({ host, port });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>63-02-01: Add product-specific health identity and an explicit readiness latch</name>
  <files>mcp/src/http.ts, mcp/src/agent-providers/serve-delegation.ts, tests/mcp-native-host-daemon.test.js, tests/mcp-bridge-topology.test.js</files>
  <read_first>mcp/src/http.ts health route and RunningHttpServer type; serve-delegation lifecycle interfaces/order; 63-RESEARCH.md Patterns 2 and 3; T63-03/T63-04</read_first>
  <behavior>
    - "Fresh HTTP listener returns HTTP 200 with `service:'fsb-mcp-server'`, bounded canonical `version`, `nativeHostProtocol:1`, and `serveReady:false`."
    - "Only the lifecycle owner can make `serveReady` true, once, after the full startup barrier; every cleanup/failure remains false."
    - "Existing health topology fields and the 127.0.0.1:7226 endpoint remain additive and compatible."
  </behavior>
  <action>Implement D63-04/D63-06 and T63-03. Extend the health payload additively with the exact service marker, canonical bounded MCP version, native-host health protocol `1`, and a closure-owned `serveReady` boolean initialized false. Extend `RunningHttpServer`/`ServeDelegationHttpServer` with an idempotent `markServeReady()` authority; do not expose a caller-supplied readiness option. Preserve all existing topology/queue/session fields and ports. Add tests for initial false, exact identity/protocol/version, true only after the setter, false on every lifecycle failure, body bounds expected by the later host probe, and no new port or CHAN surface.</action>
  <acceptance_criteria>A generic or partially initialized listener is mechanically distinguishable from a ready FSB serve daemon, and no code path outside the serve lifecycle can set readiness.</acceptance_criteria>
  <verify>
    <automated>node scripts/run-mcp-build-preserving-workspace.mjs --commands-json '[["node","tests/mcp-native-host-daemon.test.js","--section","health"],["node","tests/mcp-bridge-topology.test.js"]]'</automated>
  </verify>
  <done>The acceptance criteria above are satisfied and the automated verification passes.</done>
</task>

<task type="auto" tdd="true">
  <name>63-02-02: Move secret rotation behind bind and prove bind losers are inert</name>
  <files>mcp/src/agent-providers/serve-delegation.ts, mcp/src/index.ts, tests/mcp-native-host-daemon.test.js, tests/mcp-bridge-topology.test.js</files>
  <read_first>mcp/src/index.ts runHttpMode; serve-delegation startServeDelegation; Phase 59 pairing/rotation contract; 63-PATTERNS.md conflict map and required ordering</read_first>
  <behavior>
    - "Startup order is exactly bind -> recover -> prepareBridgeAuth -> connect -> pushInventory -> markServeReady."
    - "A bind loser calls none of createSupervisor/recover/prepareBridgeAuth/connect/pushInventory/markServeReady and closes only resources it owns."
    - "Failures after bind never advertise ready; rotation occurs no more than once and is never rolled back or repeated by cleanup."
  </behavior>
  <action>Mitigate T63-04 before any host start path. Add an injected `prepareBridgeAuth(): void | Promise&lt;void&gt;` dependency and call it only after the existing supervisor recovery reports spawn available, then connect, push inventory, and mark ready. In `runHttpMode`, remove the pre-bind `rotateBridgeSessionSecret()` call and pass it solely as the lifecycle hook. Keep `mcp/src/index.ts` ownership sequential for all later plans. Extend the topology harness with two simultaneous lifecycle attempts on one port and exact counters/call order; assert the loser cannot rotate, connect, push, set ready, or expose `agent-spawn`. Preserve Phase 59's per-daemon rotation, explicit pairing, Origin/Host checks, bridge protocol, and error union byte-for-byte.</action>
  <acceptance_criteria>The concurrency regression fails against the old ordering and passes only when the bind winner alone rotates/authenticates/advertises readiness; all existing topology promotion and Phase 59 tests remain green.</acceptance_criteria>
  <verify>
    <automated>node scripts/run-mcp-build-preserving-workspace.mjs --commands-json '[["node","tests/mcp-native-host-daemon.test.js","--section","bind-race"],["node","tests/mcp-bridge-topology.test.js"]]'</automated>
  </verify>
  <done>The acceptance criteria above are satisfied and the automated verification passes.</done>
</task>

</tasks>

<threat_model>
  <block_on>HIGH,CRITICAL</block_on>
  <residual_human_evidence_boundary>Real OS scheduling with simultaneous native processes and a live paired Chrome connection remains pending UAT; deterministic bind fakes prove authority order only.</residual_human_evidence_boundary>
  <threat id="T63-03" category="Spoofing" component="loopback health identity and readiness" severity="HIGH" disposition="mitigate" asvs="V13">
    <mitigation>Require exact service marker, dedicated native-host health protocol, bounded canonical version, HTTP/body limits, and a closure-owned false-by-default serveReady latch.</mitigation>
    <verification>Wrong/partial responder matrix and exact health transition tests.</verification>
    <residual_risk>A genuine local process can still deny the fixed port; it cannot be accepted or spawned over as FSB.</residual_risk>
  </threat>
  <threat id="T63-04" category="Tampering" component="bridge session secret lifecycle" severity="HIGH" disposition="mitigate" asvs="V2">
    <mitigation>Move secret preparation after successful bind and recovery and before authenticated connect; bind losers own no auth/bridge/inventory/readiness side effect.</mitigation>
    <verification>Two-attempt bind race with exact call order and zero loser counters plus Phase 59 topology/auth regression tests.</verification>
    <residual_risk>Real scheduler interleavings remain pending platform/browser UAT.</residual_risk>
  </threat>
  <threat id="T63-12" category="Elevation of Privilege" component="health readiness projection" severity="CRITICAL" disposition="mitigate" asvs="V3">
    <mitigation>Readiness reports daemon reachability only and never enters pairing, provider, task, or delegation contracts.</mitigation>
    <verification>Closed health keys, unchanged Phase 59 channel contracts, and no native lifecycle field in bridge/preflight schemas.</verification>
    <residual_risk>Live paired/unpaired convergence is human evidence, not inferred from health tests.</residual_risk>
  </threat>
</threat_model>

<verification>
- The Plan 01 workspace-preserving wrapper completes the MCP build and both focused child commands while restoring protected `mcp/build/**` and Git-index identity.
- `node tests/mcp-native-host-daemon.test.js` proves exact health/readiness and bind-loss behavior.
- `node tests/mcp-bridge-topology.test.js` retains all existing Phase 59–62 assertions and adds the no-rotation loser regression.
</verification>

<success_criteria>
- Secret rotation is impossible before a successful bind and recovery.
- A bind loser has zero auth/bridge/inventory/readiness side effects.
- Health cannot call a generic or half-started process ready.
</success_criteria>

<output>
Create `.planning/phases/63-native-messaging-host/63-02-SUMMARY.md` when complete.
</output>
