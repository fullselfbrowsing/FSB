---
phase: 63-native-messaging-host
plan: "05"
type: execute
wave: 5
depends_on:
  - "63-04"
files_modified:
  - mcp/src/native-host-registration.ts
  - mcp/src/native-host-install/types.ts
  - mcp/src/native-host-install/platform.ts
  - mcp/src/native-host-install/runtime.ts
  - mcp/src/native-host-install/index.ts
  - tests/mcp-native-host-install.test.js
autonomous: true
requirements:
  - NATIVE-01
  - NATIVE-04
must_haves:
  truths:
    - "macOS, Linux, and Windows resolve only the requirement-defined Chrome user-scope registration and one stable owned runtime; Chrome profiles, HKLM, system scope, and other browsers are never scanned or mutated."
    - "Runtime publication repacks only the exact installed package and its lock-bound bundled production closure, installs from a new empty cache in offline mode with zero registry requests, and has no online fallback."
    - "Install publishes registration last, is idempotent only for one exact owned configuration, and refuses symlink, foreign, mismatched, invalid, or shadowed registry state without changing it."
    - "Windows writes the canonical HKCU 32-bit view that Chrome reads first and rejects any conflicting 64-bit shadow; uninstall removes only the exact canonical owned value and runtime."
    - "Uninstall removes registration first and then only marker-proved exact FSB artifacts while preserving adjacent hosts, Chrome directories, broad keys, and foreign files."
  artifacts:
    - path: "mcp/src/native-host-registration.ts"
      provides: "Pure manifest/marker/registration validation and closed read-only facts"
      exports: ["inspectNativeHostRegistration", "validateNativeHostManifest"]
    - path: "mcp/src/native-host-install/platform.ts"
      provides: "Injectable macOS/Linux/Windows filesystem and registry-view adapters"
    - path: "mcp/src/native-host-install/runtime.ts"
      provides: "Exact-package staging, artifact verification, atomic publish, and rollback"
    - path: "mcp/src/native-host-install/index.ts"
      provides: "Closed install/uninstall transaction"
      exports: ["installNativeHost", "uninstallNativeHost"]
    - path: "tests/mcp-native-host-install.test.js"
      provides: "Three-platform path/view/ownership/atomicity matrix"
  key_links:
    - from: "mcp/src/native-host-install/index.ts"
      to: "mcp/src/native-host-install/runtime.ts"
      via: "runtime is fully validated before registration publish"
      pattern: "publishRuntime"
    - from: "mcp/src/native-host-install/platform.ts"
      to: "HKCU\\Software\\Google\\Chrome\\NativeMessagingHosts"
      via: "typed 32-bit canonical and 64-bit shadow inspection"
      pattern: "registry32|registry64"
    - from: "mcp/src/native-host-install/runtime.ts"
      to: "mcp/native-host/runtime-integrity.json"
      via: "offline tarball materialization validates the lock-derived bundled production closure before publish"
      pattern: "runtime-integrity|bundleDependencies|offline"
---

<objective>
Implement exact-owned atomic install/uninstall across macOS, Linux, and Windows behind typed injectable adapters.

Purpose: Native registration is a persistent local code-execution boundary; foreign state, symlinks, transient runtimes, and Windows view shadowing must fail closed.
Output: Pure registration inspector, platform adapters, stable runtime transaction, exact install/uninstall orchestrator, and deterministic three-OS suite.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/63-native-messaging-host/63-CONTEXT.md
@.planning/phases/63-native-messaging-host/63-RESEARCH.md
@.planning/phases/63-native-messaging-host/63-PATTERNS.md
@.planning/phases/63-native-messaging-host/63-01-SUMMARY.md
@.planning/phases/63-native-messaging-host/63-04-SUMMARY.md
@mcp/src/native-host/constants.ts
@mcp/src/native-host/runtime-layout.ts
@mcp/src/config-writer.ts
@scripts/run-mcp-build-preserving-workspace.mjs

<decision_trace>
- D63-07: native install/uninstall are explicit targets only.
- D63-08: requirement-defined Chrome user locations, stable host name, absolute owned launcher.
- D63-09: exactly one validated Chrome extension origin.
- D63-10: real-path/type/symlink validation, atomic restrictive publication, exact idempotence, foreign refusal.
- D63-11: exact-owned uninstall only.
- D63-12: injectable typed platform behavior.
</decision_trace>

<interfaces>
Closed install state model:
```typescript
type NativeHostOwnedState =
  | 'absent'
  | 'exact'
  | 'foreign'
  | 'mismatched'
  | 'invalid'
  | 'unavailable';
```

Exact Chrome manifest keys:
```json
{"name":"io.github.fullselfbrowsing.fsb_native_host","description":"FSB local agent service wake host","path":"<absolute-owned-launcher>","type":"stdio","allowed_origins":["chrome-extension://<one-id>/"]}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>63-05-01: Define pure registration facts and typed macOS, Linux, and Windows adapters</name>
  <files>mcp/src/native-host-registration.ts, mcp/src/native-host-install/types.ts, mcp/src/native-host-install/platform.ts, tests/mcp-native-host-install.test.js</files>
  <read_first>63-RESEARCH.md Pattern 8 and Windows view guidance; 63-PATTERNS.md platform paths and no-analog registry contract; Plan 01 runtime layout</read_first>
  <behavior>
    - "darwin path is ~/Library/Application Support/Google/Chrome/NativeMessagingHosts/io.github.fullselfbrowsing.fsb_native_host.json; linux path is ~/.config/google-chrome/NativeMessagingHosts/io.github.fullselfbrowsing.fsb_native_host.json."
    - "win32 uses HKCU Software\\Google\\Chrome\\NativeMessagingHosts\\io.github.fullselfbrowsing.fsb_native_host default REG_SZ; reads 32-bit then 64-bit facts, writes/deletes 32-bit only, and classifies any 64-bit value as shadow/mismatch unless the adapter reports that view unavailable."
    - "Manifest has exactly five keys and one exact allowed origin; marker and manifest accept only ordinary own-data objects and bounded strings."
  </behavior>
  <action>Implement D63-08/D63-09/D63-12 and T63-07/T63-08. Keep `native-host-registration.ts` a data-only leaf usable later by doctor: exact path/manifest/marker parsing and closed facts, no mutation methods. Define injected filesystem, permission, process-materializer, and typed registry adapters; registry operations accept explicit `user/32` or `user/64` views and return structured values/errors rather than parsing localized shell output. Canonical Windows mutation is HKCU 32-bit because Chrome reads it first; inspect the 64-bit view before every install/uninstall to detect shadowing, never inspect/write/delete HKLM. Validate extension id `^[a-p]{32}$`, exactly one `chrome-extension://id/`, exact host/description/type, absolute launcher, bounded real paths, parent types, symlink absence, and view availability. Write the negative/positive adapter tables before implementation.</action>
  <acceptance_criteria>One-platform CI deterministically proves all three location/view contracts, exact manifest/marker schemas, default/development origin handling, HKCU-only behavior, 32/64 shadow classification, and zero mutation from the read-only inspector.</acceptance_criteria>
  <verify>
    <automated>node scripts/run-mcp-build-preserving-workspace.mjs --commands-json '[["node","tests/mcp-native-host-install.test.js","--section","platform-and-registration"]]'</automated>
  </verify>
  <done>The acceptance criteria above are satisfied and the automated verification passes.</done>
</task>

<task type="auto" tdd="true">
  <name>63-05-02: Materialize and atomically publish the exact package-owned runtime</name>
  <files>mcp/src/native-host-install/runtime.ts, mcp/src/native-host-install/types.ts, tests/mcp-native-host-install.test.js</files>
  <read_first>Plan 01 runtime-layout, exact bundleDependencies, package-lock, runtime-integrity receipt, and offline materialization contract; 63-PATTERNS.md atomic filesystem pattern; packaged Windows artifact metadata and POSIX template</read_first>
  <behavior>
    - "A tokened stage repacks only the exact currently installed package and already bundled dependency tree through absolute Node + validated npm-cli.js, records tarball SHA-512, then installs that absolute tarball with a new empty cache, `--offline`, lifecycle scripts disabled, unreachable registry/proxy sentinels, and zero network requests."
    - "Before rename, validation proves package name/version, exact direct pins, bundleDependencies roster, lock-derived integrity receipt, complete production closure with no missing/extra/dev package, build entry, platform launcher, and artifact checksums; any registry resolution attempt or online fallback is a stable refusal."
    - "POSIX directories/launcher are 0700 and marker/manifest are 0600 where supported; pre-existing Chrome directories are never recursively chmodded."
    - "Any pack/install/validation/fsync/rename/permission failure removes only the current tokened stage and leaves prior runtime/registration untouched."
  </behavior>
  <action>Implement D63-10 and T63-06/T63-07. Use exclusive creation, `lstat`/`realpath`, `O_NOFOLLOW` where available, fsync, tokened staging, and atomic rename. Run the exact Plan 01 recipe with explicit executable/argv and `shell:false`: validate the absolute current Node and exact npm-cli; verify the invoking package is `fsb-mcp-server@FSB_MCP_VERSION` with the exact direct dependency pins, canonical `bundleDependencies`, packaged lock-derived receipt, and complete bundled production closure; `npm pack` only that absolute current package into the tokened stage; parse the bounded JSON receipt and record tarball SHA-512; remove access to the invoking source/original cache in integration fixtures; then install the absolute tarball into the staged prefix with `--offline --ignore-scripts --omit=dev --no-audit --no-fund --package-lock=false`, a newly created empty cache, unreachable registry, poisoned HTTP(S) proxy/NO_PROXY settings, and a zero-connection sentinel. Never resolve a registry dependency, retry without `--offline`, copy node_modules recursively, accept an unbundled/plain tarball, or register the invoking package path. Cap captured package-manager stdout/stderr, never forward child output, and collapse process/error details to stable content-free results. After installation, recompute and compare the receipt/closure, root/direct/bundle declarations, build entry, POSIX template or selected Windows `.exe`, PE/package metadata, and artifact checksums before rename. Generate the POSIX launcher with one safe absolute Node shebang; on Windows select only x64/arm64 `.exe` and write its bounded sibling config. Build the closed separate owner marker. Registration publication is not part of this module and cannot occur until this function returns an exact validated receipt.</action>
  <acceptance_criteria>Trace assertions prove stage-before-rename, validation-before-publish, restrictive modes, exact bundled closure/lock receipt/tarball integrity, new-empty-cache offline install with zero network access on macOS/Linux fixtures, no lifecycle scripts/shell/cache path/online fallback, bounded non-forwarded child output, stable error collapse, removal of only failed stages, and a final receipt whose launcher remains reachable after source and original cache deletion.</acceptance_criteria>
  <verify>
    <automated>node scripts/run-mcp-build-preserving-workspace.mjs --commands-json '[["node","tests/mcp-native-host-install.test.js","--section","runtime-transaction"]]'</automated>
  </verify>
  <done>The acceptance criteria above are satisfied and the automated verification passes.</done>
</task>

<task type="auto" tdd="true">
  <name>63-05-03: Implement idempotent exact install and exact-owned uninstall transactions</name>
  <files>mcp/src/native-host-install/index.ts, mcp/src/native-host-install/types.ts, tests/mcp-native-host-install.test.js</files>
  <read_first>registration facts from Task 1; runtime receipt from Task 2; D63-10/D63-11; current package artifact contract</read_first>
  <behavior>
    - "Absent state -> stage/validate runtime -> publish registration last -> installed; exact state -> unchanged idempotent receipt with zero writes."
    - "Foreign/mismatched/invalid/symlink/shadow/unavailable state -> stable refusal and zero writes/deletes."
    - "Exact uninstall -> remove canonical registration first -> remove only exact marker/launcher/config/runtime artifacts -> removed; absent -> unchanged; any ownership mismatch -> zero deletes."
  </behavior>
  <action>Implement D63-10/D63-11 and T63-08 as one serialized transaction per stable root. Classify both runtime and registration before mutation. Installation may proceed only when both are absent or both are the exact same current marker/origin/version/artifact/layout; split, old-version, or foreign state fails with a stable reason and doctor guidance rather than becoming implicit repair/upgrade. Recheck parent types/realpaths immediately before publication; publish the exact manifest file or 32-bit HKCU default REG_SZ only after the runtime receipt. Uninstall requires a supported exact marker schema/owner/host/origin/platform/install token, exact registration path/value, exact launcher/checksum, and exact stable root. Verify the marker-declared installed version against the installed package and artifact metadata; it need not equal the currently invoking package version, so an intact older FSB-owned runtime remains removable. Reject unknown schemas or any internal mismatch. Remove registration first, then only the enumerated owned files and empty owned directories. On Windows delete the exact owned canonical default value and remove the exact host-name subkey only if the adapter proves it is then empty; any extra value/subkey is foreign mismatch and causes zero deletion. Never delete adjacent manifests, Chrome directories, the `NativeMessagingHosts` parent/broad registry keys, a 64-bit shadow, or a mismatched runtime. Test injected failures at every operation boundary, current and older exact-owned uninstalls, Windows empty/nonempty subkeys, and repeat calls.</action>
  <acceptance_criteria>All three platform traces prove exact current-version idempotence, safe uninstall of internally consistent older owned versions, foreign/symlink/shadow/unknown-schema refusal, atomic publication/rollback, registration-first exact-owned removal, adjacent preservation, and stable content-free result codes.</acceptance_criteria>
  <verify>
    <automated>node scripts/run-mcp-build-preserving-workspace.mjs --commands-json '[["node","tests/mcp-native-host-install.test.js"]]'</automated>
  </verify>
  <done>The acceptance criteria above are satisfied and the automated verification passes.</done>
</task>

</tasks>

<threat_model>
  <block_on>HIGH,CRITICAL</block_on>
  <residual_human_evidence_boundary>Real POSIX ownership/permissions, actual HKCU/WOW64 behavior, Chrome discovery, and installed artifact execution remain pending human OS UAT; synthetic adapters and offline package tests cannot claim those passes.</residual_human_evidence_boundary>
  <threat id="T63-06" category="Tampering" component="bundled package materialization and stable runtime" severity="HIGH" disposition="mitigate" asvs="V12">
    <mitigation>Exact current tarball only, complete bundleDependencies closure, lock-derived receipt, offline new-cache install, tarball/artifact checksums, stable roots, and registration publication last.</mitigation>
    <verification>Mac/Linux offline zero-network matrix, missing/altered bundle negatives, source/cache removal, operation trace, and runtime receipt validation.</verification>
    <residual_risk>The independently installed absolute Node interpreter may later disappear and is reported by doctor rather than repaired.</residual_risk>
  </threat>
  <threat id="T63-07" category="Elevation of Privilege" component="Windows launcher and sibling config" severity="CRITICAL" disposition="avoid" asvs="V14">
    <mitigation>Select only version/checksum-bound x64 or arm64 PE artifacts and exact bounded sibling config; no batch, shell, SEA, or registry-selected command.</mitigation>
    <verification>Package metadata checks, typed platform matrix, config negatives, and Windows artifact workflow evidence.</verification>
    <residual_risk>Real Chrome/Windows bootstrap execution remains pending UAT.</residual_risk>
  </threat>
  <threat id="T63-08" category="Tampering" component="filesystem and HKCU registration install/uninstall transaction" severity="HIGH" disposition="mitigate" asvs="V12">
    <mitigation>Exact own-data manifest/marker, lstat/realpath/no-follow, restrictive atomic stage, registration-last/removal-first, canonical HKCU 32-bit mutation, both-view inspection, shadow refusal, and exact enumeration only.</mitigation>
    <verification>Injected three-platform view matrix, symlink/foreign/shadow/unknown-schema cases, fault injection, idempotence, older exact-owned uninstall, and adjacent preservation.</verification>
    <residual_risk>Genuine WOW64 and Chrome lookup order remains explicitly pending Windows UAT.</residual_risk>
  </threat>
</threat_model>

<verification>
- `node tests/mcp-native-host-install.test.js` covers VALIDATION rows 04–06.
- The Plan 01 workspace-preserving wrapper builds while retaining the native boundary and CHAN-07 gates and restores the protected build/index state afterward.
- No ordinary installer or CLI router is modified yet; this plan exposes a closed API only.
</verification>

<success_criteria>
- All three supported OS contracts are typed and deterministic.
- Install and uninstall mutate only exact FSB-owned state in the required order.
- Windows registry view shadowing is detected and never silently overwritten.
</success_criteria>

<output>
Create `.planning/phases/63-native-messaging-host/63-05-SUMMARY.md` when complete.
</output>
