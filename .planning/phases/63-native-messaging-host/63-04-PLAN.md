---
phase: 63-native-messaging-host
plan: "04"
type: execute
wave: 4
depends_on:
  - "63-03"
files_modified:
  - mcp/src/native-host/platform.ts
  - mcp/src/native-host/daemon.ts
  - mcp/src/native-host/entry.ts
  - tests/mcp-native-host-daemon.test.js
  - tests/mcp-native-host-protocol.test.js
  - scripts/verify-native-host-boundary.mjs
autonomous: true
requirements:
  - NATIVE-01
  - NATIVE-03
must_haves:
  truths:
    - "A valid wake probes only the exact FSB v1 health endpoint; ready returns already_running, a wrong/incompatible responder returns unavailable without spawning, and offline alone may enter the wake lock."
    - "Concurrent native hosts create at most one exact shell-free `serve --host 127.0.0.1 --port 7226` child; contenders poll readiness and never signal or kill any process."
    - "The lock recovers only by tokened stale-directory quarantine after a health recheck, and owner release requires the exact token."
    - "A host returns started only after product-specific serveReady and then exits; timeout never kills an independently running daemon."
  artifacts:
    - path: "mcp/src/native-host/platform.ts"
      provides: "Core-only injected clock/fs/health/spawn/runtime facts"
      exports: ["NativeHostDaemonDependencies", "NativeHostRuntimeConfig"]
    - path: "mcp/src/native-host/daemon.ts"
      provides: "Bounded health, tokened lock, exact serve launch, and readiness state machine"
      exports: ["wakeServeDaemon"]
    - path: "tests/mcp-native-host-daemon.test.js"
      provides: "Wrong-port, spawn tuple, concurrency, stale lock, and no-kill matrix"
  key_links:
    - from: "mcp/src/native-host/entry.ts"
      to: "mcp/src/native-host/daemon.ts"
      via: "validated request invokes wakeServeDaemon once"
      pattern: "wakeServeDaemon"
    - from: "mcp/src/native-host/daemon.ts"
      to: "mcp/build/index.js"
      via: "constant-owned exact Node argv"
      pattern: "serve.*--host.*127\\.0\\.0\\.1.*--port.*7226"
---

<objective>
Implement the one-purpose host core: exact FSB readiness probing, tokened cross-process wake coalescing, one shell-free serve launch, and bounded handoff.

Purpose: Native messaging may only make the existing serve daemon reachable; it cannot become a broker, process manager, or agent-spawn authority.
Output: Injected host platform seam, deterministic daemon state machine, entry composition, and security boundary proof.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/63-native-messaging-host/63-CONTEXT.md
@.planning/phases/63-native-messaging-host/63-RESEARCH.md
@.planning/phases/63-native-messaging-host/63-PATTERNS.md
@.planning/phases/63-native-messaging-host/63-02-SUMMARY.md
@.planning/phases/63-native-messaging-host/63-03-SUMMARY.md
@mcp/src/native-host/constants.ts
@mcp/src/native-host/protocol.ts
@mcp/src/native-host/entry.ts
@mcp/src/http.ts
@scripts/run-mcp-build-preserving-workspace.mjs

<decision_trace>
- D63-02: one-shot host probes, optionally starts serve, waits bounded readiness, responds once, exits.
- D63-03: no adapter/supervisor/task/browser/secret authority.
- D63-04: absolute executable/argv, shell false, loopback only, CHAN unchanged.
- D63-05: bounded cross-process coalescing and safe stale-lock recovery.
- D63-06: success means reachability only.
</decision_trace>

<interfaces>
Exact launch tuple:
```text
absoluteNode,
[absoluteStableBuildIndex, 'serve', '--host', '127.0.0.1', '--port', '7226'],
{ cwd: stableRuntimeRoot, shell: false, detached: true, stdio: 'ignore', windowsHide: true }
```

Exact health acceptance:
```text
HTTP 200; <=4096 bytes; JSON own object;
ok === true; service === 'fsb-mcp-server'; nativeHostProtocol === 1;
bounded version; serveReady === true
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>63-04-01: Implement exact health classification, tokened lock, and shell-free serve launch</name>
  <files>mcp/src/native-host/platform.ts, mcp/src/native-host/daemon.ts, tests/mcp-native-host-daemon.test.js</files>
  <read_first>63-RESEARCH.md Patterns 2, 4, and 5; 63-PATTERNS.md patterns 5–7; runtime-layout/constants from Plan 01; health contract from Plan 02</read_first>
  <behavior>
    - "ready exact FSB response -> already_running/daemon_already_ready with zero lock/spawn calls."
    - "wrong product/protocol, malformed/oversize body, non-200, or incompatible response -> unavailable with zero spawn calls."
    - "offline winner obtains atomic wake.lock, spawns exactly once, polls to ready -> started/daemon_started_ready; contender never spawns and polls to already_running."
    - "expired lock is health-rechecked, atomically renamed to a tokened quarantine, and only that quarantine is removed; no PID is trusted or signaled."
  </behavior>
  <action>Implement D63-04/D63-05 and mitigate T63-02/T63-03/T63-05. Define a leaf-only injected dependency interface for bounded loopback HTTP, fs operations, clock/wait/random token, runtime config, and explicit spawn. Probe `http://127.0.0.1:7226/health` with a 500 ms request timeout and 4096-byte response cap. Use the frozen 10 s readiness window, 100 ms poll, and 30 s stale-lock TTL. The lock is an atomic directory under the owned runtime, contains only schema/token/createdAt, and never a PID; release checks the exact token. Before quarantining stale state, re-probe readiness. Spawn the exact tuple only after offline + owner lock; strip `NODE_OPTIONS` and `NODE_PATH`, accept no request/origin fields in command/cwd/env, await spawn/error, then unref. Never kill on timeout. Map all exceptions to the frozen content-free reasons.</action>
  <acceptance_criteria>The fake-clock/fs/http/spawn matrix proves one spawn under concurrency, no spawn over a wrong responder, safe stale recovery, exact env/argv/cwd/options, owner-only release, no signal/kill API, and deterministic timeout settlement.</acceptance_criteria>
  <verify>
    <automated>node scripts/run-mcp-build-preserving-workspace.mjs --commands-json '[["node","tests/mcp-native-host-daemon.test.js"]]'</automated>
  </verify>
  <done>The acceptance criteria above are satisfied and the automated verification passes.</done>
</task>

<task type="auto" tdd="true">
  <name>63-04-02: Compose the production one-shot host without widening its import or response authority</name>
  <files>mcp/src/native-host/entry.ts, tests/mcp-native-host-protocol.test.js, tests/mcp-native-host-daemon.test.js, scripts/verify-native-host-boundary.mjs</files>
  <read_first>entry/protocol contracts from Plan 03; daemon exports from Task 1; runtime owner-marker contract from Plan 01; boundary verifier rules</read_first>
  <behavior>
    - "Valid framed wake with exact owned origin invokes wakeServeDaemon once and returns its exact lifecycle fact."
    - "already_running/started does not contain paired/provider/task/agent/browser/session/secret fields."
    - "entry exits after the single response and cannot instantiate registry/supervisor or invoke any executable other than exact serve."
  </behavior>
  <action>Wire the production entry to read and strictly validate only the closed owner marker/runtime config through the data-only `runtime-layout.ts` leaf, independently compare Chrome argv origin with the marker's sole origin, then invoke `wakeServeDaemon`. Do not import `index.ts`; the stable compiled entry is the native executable target and the only child it may create is `build/index.js serve`. Preserve no-frame boot EOF. Extend protocol and daemon suites with end-to-end injected entry cases for ready, started, wrong product, lock timeout, spawn error, readiness timeout, correlation preservation, exactly one frame, and handler exit. Tighten the transitive verifier's source, compiled, and all modes to allow only constants, protocol, runtime-layout, platform, and daemon leaves, positively pin the exact daemon tuple, and reject all Phase 59/60/61 privileged imports or historical shim tokens; source mode remains the prebuild gate so stale compiled output cannot deadlock rebuilding, while compiled mode runs immediately after TypeScript.</action>
  <acceptance_criteria>The compiled host entry passes the boundary graph, returns only v1 lifecycle facts, never leaks marker contents/paths/child output, and exits after one request; CHAN and agent-provider sources are byte/behavior unchanged.</acceptance_criteria>
  <verify>
    <automated>node scripts/run-mcp-build-preserving-workspace.mjs --commands-json '[["node","tests/mcp-native-host-protocol.test.js"],["node","tests/mcp-native-host-daemon.test.js"],["node","scripts/verify-native-host-boundary.mjs","--all"]]'</automated>
  </verify>
  <done>The acceptance criteria above are satisfied and the automated verification passes.</done>
</task>

</tasks>

<threat_model>
  <block_on>HIGH,CRITICAL</block_on>
  <residual_human_evidence_boundary>Real detached-process behavior and scheduling on each OS, Chrome host lifetime, and live daemon attach/wake remain pending in 63-HUMAN-UAT.md.</residual_human_evidence_boundary>
  <threat id="T63-02" category="Elevation of Privilege" component="daemon spawn and production host import graph" severity="CRITICAL" disposition="mitigate" asvs="V4">
    <mitigation>Permit one constant-owned absolute Node/build-index serve tuple, sanitize environment, use shell:false, and reject every agent/task/auth/installer/router import.</mitigation>
    <verification>Exact spawn snapshots, transitive source/compiled boundary gate, forbidden import fixtures, and no extra child-process edge.</verification>
    <residual_risk>Detached process semantics on real operating systems remain human evidence.</residual_risk>
  </threat>
  <threat id="T63-03" category="Spoofing" component="loopback daemon health responder" severity="HIGH" disposition="mitigate" asvs="V13">
    <mitigation>Bound response time/bytes and require exact HTTP status, service identity, native-host protocol, bounded version, and serveReady before success.</mitigation>
    <verification>Wrong product/protocol/malformed/oversize/non-200/partial readiness matrix with zero spawn.</verification>
    <residual_risk>A wrong local process can deny availability but cannot be treated as FSB or overwritten.</residual_risk>
  </threat>
  <threat id="T63-05" category="Denial of Service" component="cross-process wake lock and readiness polling" severity="HIGH" disposition="mitigate" asvs="V12">
    <mitigation>Atomic tokened directory lock, contender polling, health recheck before tokened quarantine, owner-token release, bounded timers, and no PID signal/kill.</mitigation>
    <verification>Fake-clock/filesystem concurrency, stale-lock, exact-token release, spawn-count, timeout, and no-kill tests.</verification>
    <residual_risk>Real scheduler timing remains pending platform UAT.</residual_risk>
  </threat>
  <threat id="T63-12" category="Elevation of Privilege" component="native lifecycle outcome to delegation boundary" severity="CRITICAL" disposition="mitigate" asvs="V3">
    <mitigation>Return bounded reachability facts only; preserve authenticated bridge and existing preflight as the sole pairing/provider/delegation authority.</mitigation>
    <verification>End-to-end one-shot cases, exact response schema, boundary scan, and unchanged CHAN tests.</verification>
    <residual_risk>Live paired/unpaired extension behavior remains human evidence.</residual_risk>
  </threat>
</threat_model>

<verification>
- Both new MCP focused suites pass after a strict TypeScript build.
- Boundary verifier proves no forbidden authority in source or compiled graph.
- The old pre-bind secret-race regression from Plan 02 stays green under concurrent host tests.
</verification>

<success_criteria>
- Native host can only observe/start the existing loopback serve daemon.
- Concurrent invocations cause at most one exact spawn and never kill unrelated work.
- Successful native response remains a reachability fact, not pairing/provider/delegation authority.
</success_criteria>

<output>
Create `.planning/phases/63-native-messaging-host/63-04-SUMMARY.md` when complete.
</output>
