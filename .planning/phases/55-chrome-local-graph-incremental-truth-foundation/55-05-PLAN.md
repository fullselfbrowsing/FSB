---
phase: 55-chrome-local-graph-incremental-truth-foundation
plan: "05"
type: execute
wave: 4
depends_on:
  - "55-02"
  - "55-03"
  - "55-04"
files_modified:
  - extension/utils/skopeo-graph-engine.js
  - extension/background.js
  - scripts/verify-skopeo-storage-boundary.mjs
  - tests/skopeo-graph-runtime.test.js
  - tests/skopeo-graph-evals.test.js
  - tests/fixtures/skopeo-graph-evals/manifest.json
  - tests/fixtures/skopeo-graph-evals/cases.json
  - package.json
  - README.md
autonomous: true
requirements:
  - LOCAL-01
  - LOCAL-02
  - LOCAL-03
  - LOCAL-04
  - LOCAL-05
  - LOCAL-06
  - LOCAL-07
  - TRUTH-01
  - TRUTH-05
  - TRUTH-10
must_haves:
  truths:
    - "D-02: The production facade includes one closed `replaceCandidateRelations` ingestion path. A nonempty complete set requires fresh exact-set authority for proposer plus all targets and derives endpoint/evidence-bound relation and overlay generations; target advance/revocation removes old incoming influence. Exact empty replacement requires only fresh proposer authority and clears without target lookup or existence leakage. Model output cannot name another source/record, and no link means equivalence, effectiveness, precedence, governing status, or adjudication."
    - "D-07/D-08/D-10/D-12: The production build/update path withdraws old truth before provider work; its session binds only invariant partition/account/source/content-fingerprint/schema/prompt/provider/model fields; preparation, provider/repair, stage, and final steps each consume distinct fresh Phase 54 operations/certificates. Every effectful provider-only ingestion call returns through the existing `publisher.publish(effect)` acknowledgement as exact `{ status: 'provider-no-storage', durableEffect: false, prepared }`, performs zero durable mutation, discards raw response before a later fresh staging operation, and publishes one complete replacement pointer-last or leaves the source withheld."
    - "D-13/D-14: One background-only engine facade wraps build/update/candidate-link replacement, exact lookup, lexical search, bounded neighbors, provenance, and status through exact-source/set Phase 54 operations; content receives no graph/store/provider authority. Validated labels may contain party/person/vendor names only in authoritative record/source-owned lexical state and freshly authorized bounded query/provenance projections. Query caches hydrate lazily only inside a freshly authorized exact scope, never at boot or outside ownership."
    - "D-15: The extension boots, builds, updates, queries, and inspects with MCP absent and adds no MCP registration, Skopeo server, daemon, tool family, or duplicated graph owner."
    - "D-16/LOCAL-07: README records the Graphify repository URL, exact reviewed commit, MIT license, `Copyright 2026 Safi Shamsi`, reviewed upstream file inventory, conceptual-only influence, empty copied-code inventory, and no runtime dependency; static gates prove no Graphify runtime/import/network dependency."
    - "All 37 P/Q/A/I/L/R/B fixtures execute against production modules and every deterministic structural/security threshold passes. Each fixture carries `review_status: pending|approved`; until real experts approve the domain labels, the domain-fidelity/gold gate reports `human_needed` and is never claimed as Critical PASS."
  artifacts:
    - path: "extension/utils/skopeo-graph-engine.js"
      provides: "Closed background engine facade coordinating Phase 54 authority, extraction, store replacement, queries, and status"
      contains: "FsbSkopeoGraphEngine"
    - path: "extension/background.js"
      provides: "Private import, trusted boot/recovery, real purge registration, and MCP-independent facade initialization"
      contains: "fsbSkopeoGraphEngineFacade"
    - path: "scripts/verify-skopeo-storage-boundary.mjs"
      provides: "Static graph-module content closure, no-runtime, no-dynamic-query, and no-new-host/process gate"
      contains: "skopeo-graph-engine.js"
    - path: "tests/skopeo-graph-runtime.test.js"
      provides: "Boot, durable recovery, lazy authorized cache, authority, participant bridge, provider binding, candidate-link, content closure, restart, and MCP-absence integration oracle"
      contains: "skopeo graph runtime contract"
    - path: "tests/fixtures/skopeo-graph-evals/cases.json"
      provides: "Complete 37-case synthetic/redacted deterministic corpus"
      contains: "B03"
    - path: "tests/skopeo-graph-evals.test.js"
      provides: "37-case deterministic structural/security gate plus explicit expert-review `human_needed|approved` status without fabricated adjudication"
      contains: "37"
    - path: "README.md"
      provides: "Exact Graphify repository/commit/MIT/copyright/reviewed-file conceptual provenance and explicit empty copied-code inventory"
      contains: "abff1b1ca4052fcf9d955c5f6a034088723f4536"
  key_links:
    - from: "extension/utils/skopeo-graph-engine.js"
      to: "extension/background.js"
      via: "one initialized `fsbSkopeoGraphEngineFacade` that calls the existing exact-source/set corpus operation authority"
      pattern: "runSkopeoCorpusOperation"
    - from: "extension/utils/skopeo-graph-engine.js"
      to: "extension/utils/skopeo-graph-extractor.js"
      via: "one fresh certified operation per prepare, provider-binding check, normal/repair batch, binding-matched staging effect, and final publication effect"
      pattern: "prepareSource|verifyProviderBinding|nextBatch|repairBatch|finalize"
    - from: "extension/utils/skopeo-graph-engine.js"
      to: "extension/utils/skopeo-graph-store.js"
      via: "withdraw/purge/absence → provider-bound invisible staging → complete shard validation → pointer-last publication, plus atomic proposing-source candidate-overlay replacement"
      pattern: "beginReplacement|stageBatch|sealStaging|publishReplacement|replaceCandidateRelations"
    - from: "extension/utils/skopeo-graph-engine.js"
      to: "extension/utils/skopeo-graph-query.js"
      via: "fresh exact-source/set authority mints one opaque query scope, lazily hydrates only its exact cache, and releases it after final currentness"
      pattern: "createScope|ensureScopeCache|releaseScope"
    - from: "extension/utils/skopeo-corpus-store.js"
      to: "extension/utils/skopeo-graph-store.js"
      via: "registration-private verifier authenticates one-call participant capabilities by name/mode/request; the foreign corpus mutation guard never crosses the seam"
      pattern: "registerAuthorizedPurgeParticipant|verifyParticipantAuthorization|getPurgeParticipant"
    - from: "package.json"
      to: "tests/skopeo-graph-evals.test.js"
      via: "one `test:skopeo-graph-evals` aggregate registered exactly once in normal `npm test`"
      pattern: "test:skopeo-graph-evals"
---

<objective>
Integrate the source-owned graph substrate into trusted extension boot and Phase 54 authority, then close the phase with static/runtime boundaries, pinned conceptual provenance, and the complete deterministic evaluation corpus.

Purpose: Prove the graph engine works end to end inside Chrome with atomic replacement and bounded queries, while remaining private, permission-scoped, provider-minimized, MCP-independent, and free of upstream/runtime/package expansion.
Output: Production engine/facade wiring, strengthened static verification, 37-fixture release gate, package registration, and provenance documentation.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/55-chrome-local-graph-incremental-truth-foundation/55-CONTEXT.md
@.planning/phases/55-chrome-local-graph-incremental-truth-foundation/55-RESEARCH.md
@.planning/phases/55-chrome-local-graph-incremental-truth-foundation/55-AI-SPEC.md
@.planning/phases/55-chrome-local-graph-incremental-truth-foundation/55-VALIDATION.md
@.planning/phases/55-chrome-local-graph-incremental-truth-foundation/55-PATTERNS.md
@.planning/phases/55-chrome-local-graph-incremental-truth-foundation/55-02-SUMMARY.md
@.planning/phases/55-chrome-local-graph-incremental-truth-foundation/55-03-SUMMARY.md
@.planning/phases/55-chrome-local-graph-incremental-truth-foundation/55-04-SUMMARY.md
@extension/utils/skopeo-graph-schema.js
@extension/utils/skopeo-graph-store.js
@extension/utils/skopeo-graph-extractor.js
@extension/utils/skopeo-graph-query.js
@extension/utils/skopeo-drive-reconciler.js
@extension/background.js
@scripts/verify-skopeo-storage-boundary.mjs
@tests/skopeo-corpus-runtime.test.js
@tests/skopeo-browser-contract.test.js
@package.json
@README.md

<interfaces>
Create `globalThis.FsbSkopeoGraphEngine` with `VERSION` and `create({ graphSchema, graphStore, graphExtractor, graphQuery, corpusTransport, runCorpusOperation, readSettings, providerFactory, now })`.

The created facade exposes only `buildSource(exactTuple, { sourceFileId })`, `updateSource(exactTuple, { sourceFileId })`, `replaceCandidateRelations(exactTuple, selection, input)`, `getById(exactTuple, selection, input)`, `searchLexical(exactTuple, selection, input)`, `neighbors(exactTuple, selection, input)`, `inspectProvenance(exactTuple, selection, input)`, and `inspectStatus(exactTuple, { sourceFileId })`. For nonempty `input.relations`, `replaceCandidateRelations` requires a duplicate-free exact bounded source set containing the proposer and every target; for exact `input.relations: []`, it requires the exact single-source selection `{ sourceFileId: proposingSourceFileId }` and must not accept, request, or probe any target. Input is the complete replacement intent set parsed by Plan 01, never a provider response. Every argument is an exact-key plain record. The facade stores no tab, certificate, operation ID/epoch, signal, provider, storage, raw response, or query scope outside the explicitly bounded operation/session lifetime.

The existing `runSkopeoCorpusOperation(kind, exactTuple, sourceSelection, callback, commitCallback)` remains the sole Phase 54 authority entry. Build/update and `replaceCandidateRelations` are ingestion mutations; the four D-14 query primitives remain exactly get-by-ID, lexical search, neighbors, and provenance. Query/display are read-only. No message or MCP handler calls graph modules directly.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Trusted boot → corpus/graph stores | Wrong import/recovery/participant order can expose storage before trusted access or leave no-op graph purge ownership. |
| Phase 54 operation → engine build/query | A stale tuple, source selection, certificate, or late callback can cross account/corpus/source authority. |
| Settings/extractor → staged/published store | Provider/model drift, a partial/invalid batch, or failed absence proof can become visible or mix provenance. |
| Trusted candidate intent → multi-source relation overlay | A model-authored/cross-authority/stale endpoint or unsupported semantic claim can masquerade as cross-document truth. |
| Graph facade → content/MCP/runtime | Raw graph/storage/provider handles or a new generic server/tool surface can bypass the closed authority path. |
| Fixture/provenance/package changes → release evidence | Missing categories, fabricated expert approval, permissive thresholds, copied upstream code, or duplicate test registration can create false closure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T55-01 | Tampering / Elevation of privilege | engine extraction path | mitigate | Production engine accepts only extractor-validated batches; I01/I02 and static no-tool/no-dynamic-code gates require zero durable effect. |
| T55-02 | Spoofing / Information disclosure | exact tuple/source facade and candidate links | mitigate | Nonempty replacement enters exact-set authority for proposer plus every target and derives relation/overlay IDs from both endpoint versions/generations; target-only drift removes old influence. Empty replacement enters proposer-only authority, performs no target read, and returns an existence-independent clear acknowledgement. |
| T55-03 | Information disclosure | provider envelope/config and labels | mitigate | One provider/model snapshot binds invariant-only session/staging/batches; settings are rechecked before every call/stage; raw response exists only in the certified no-storage operation result; location-aware scans allow a name-bearing validated label only in authoritative/lexical/exact-scope query paths. |
| T55-04 | Tampering / Denial of service | cancellation, certificate reuse, and late completion | mitigate | Preparation/provider/repair/stage/final each consume a distinct fresh certified operation, session equality excludes certificate/operation identity/epoch, propagated signal plus one-use checks reject reuse/expiry, and R01-R03 prove zero retry/late write. |
| T55-05 | Tampering / Repudiation | atomic replacement and participant bridge | mitigate | Four real adapters bind corpus-owned one-call capabilities before recovery; R04-R07 prove authorization, old truth absent, and source/candidate pointers last. |
| T55-06 | Denial of service | boot/recovery/quota/corruption | mitigate | Trusted boot stays closed on durable recovery failure; no fingerprint/cache hydration occurs unauthenticated, lazy scoped caches rebuild finitely, and affected truth remains withheld. |
| T55-07 | Tampering | evidence/relations | mitigate | P/Q/A/L fixtures assert exact spans, endpoint matrix, qualifier coverage, abstention, and no unsupported publication. |
| T55-08 | Information disclosure / Denial of service | query/status facade | mitigate | Exact four queries plus bounded status return minimized projections only; validated labels are emitted only from a fresh authorized exact scope, while diagnostics/errors/unrelated/unauthorized/closed/cache-outside-ownership paths reject their markers; runtime/static scans reject raw handles and over-cap output. |
| T55-09 | Tampering | upstream/runtime/package/review surface | mitigate | Exact Graphify provenance inventory and copyright, empty copied-code inventory, explicit fixture review status, no fabricated approval, no new dependency, and static rejection of Graphify/Python/process/database/MCP/runtime imports. |
| T55-SC | Tampering | package install surface | mitigate | No package install or lockfile edit; package.json adds test commands only. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify trusted boot, fresh-operation orchestration, facade closure, and MCP independence in RED</name>
  <files>tests/skopeo-graph-runtime.test.js</files>
  <read_first>
    - tests/skopeo-graph-runtime.test.js
    - extension/utils/skopeo-graph-engine.js
    - extension/background.js
    - scripts/verify-skopeo-storage-boundary.mjs
    - .planning/phases/55-chrome-local-graph-incremental-truth-foundation/55-CONTEXT.md
    - .planning/phases/55-chrome-local-graph-incremental-truth-foundation/55-AI-SPEC.md
    - .planning/phases/55-chrome-local-graph-incremental-truth-foundation/55-PATTERNS.md
    - extension/utils/skopeo-graph-store.js
    - extension/utils/skopeo-graph-extractor.js
    - extension/utils/skopeo-graph-query.js
    - extension/utils/skopeo-corpus-store.js
    - extension/utils/skopeo-drive-reconciler.js
    - extension/utils/skopeo-drive-authority.js
    - tests/skopeo-corpus-runtime.test.js
    - tests/skopeo-drive-authority.test.js
  </read_first>
  <behavior>
    - "Graph modules load privately after validator/MiniSearch and the Phase 54 corpus chain; four real participant binders register with the corpus-owned authorization verifier before corpus recovery, bounded durable graph recovery finishes before facade availability, and boot performs zero current-fingerprint/cache hydration."
    - "Build/update opens a distinct fresh exact-source ingestion operation/certificate for withdrawal, content preparation, every normal/repair provider call, each binding-matched staging effect, and final publication. The session compares only invariant partition/account/source/content-fingerprint/schema/prompt/provider/model fields; matching invariants under a new operation ID/epoch succeed, certificate reuse/expiry fails, and drift withholds before the next effect."
    - "Every provider-only ingestion operation supplies the mandatory Phase 54 commit callback, returns the opaque value from `publisher.publish(effect)`, and yields exact admitted value `{ status: 'provider-no-storage', durableEffect: false, prepared }`; the effect performs zero durable/cache mutation, raw response is confined to `prepared` until immediate discard, and staging occurs only in a later fresh operation."
    - "Nonempty `replaceCandidateRelations` uses a fresh exact set containing proposer and every target, derives exact current endpoint/version IDs plus proposing evidence and a canonical complete-set overlay generation, replaces one source-owned overlay pointer-last, and rejects model output or legal-effect claims. Empty replacement instead uses exact proposer-only selection, reads no target, clears old influence after target revoke/advance, and returns a fixed existence-independent acknowledgement."
    - "Query/inspect opens a fresh exact-source/set query or display operation, mints a scope only for final certified generations, lazily hydrates that scope's cache, releases it after use, and discards output after tuple/source drift."
    - "Boot and all engine methods work when MCP globals/processes are absent; no new message, server, daemon, tool registration, content injection, UI, or remote runtime is created."
  </behavior>
  <action>
Create a VM/fake-Chrome runtime oracle from the Phase 54 corpus integration test before production changes. Supply production graph modules, the real revised corpus participant binder contract, fake configured provider/settings drift, exact corpus authority, fake trusted local storage, MiniSearch, validator, abort controllers, and deterministic content. Adapt the reconciler's preparation-versus-effect ordering from `extension/utils/skopeo-drive-reconciler.js` and the exact publisher/acknowledgement assertions from `tests/skopeo-drive-authority.test.js`; do not carry its certificate across an operation. Confirm RED names the missing graph engine/import/participant/facade seams.

Assert literal import order `minisearch/cfworker → corpus schema/store/transport/authority/controller/reconciler → graph schema/store/extractor/query/engine`; trusted access before either store; graph store plus query cache owner registration before the seven corpus participant binders; real graph binders for `fragments`, `indexes`, `relationships`, `result-cache`; exact empty binders only for `citations`, `counts`, `alerts`; corpus recovery; graph durable journal/orphan recovery; then facade availability. Assert boot performs zero Phase 54 current-fingerprint, graph-shard, MiniSearch-build, or candidate-overlay reads. Seed failure at each dependency, binder, registration, and durable recovery step and require closed state; lazy cache failure is instead tested inside a fresh query operation.

Specify exact build/update ordering and one distinct fresh authority operation/certificate for preparation, every normal/repair provider call, every stage, and final publication. Use certificates with equal invariant partition/account/source/content-fingerprint data but different identity/operation ID/epoch for successive accepted steps; replay one consumed certificate and expire another signal to prove zero next provider/effect. Inspect session snapshots to prove they bind only partition/account/source/content fingerprint/schema/prompt/provider/model, never certificate identity, operation ID/epoch, or signal. Preserve exact provider/model binding across session/staging/batches, exact-fingerprint reuse, provider-unavailable/drift pending/withheld behavior, changed/deleted/revoked/cancelled/worker-restart outcomes, all eight facade methods, candidate-overlay replacement, defensive projections, content dependency closure, and zero MCP/server/process dependence.

For every normal and repair provider call made via effectful `runSkopeoCorpusOperation('ingestion', ...)`, assert the five-argument form and exact current interfaces: provider callback prepares the bounded response; `commitCallback(prepared, publisher, operationSignal)` returns the opaque capability from `publisher.publish(async effectGuard => ...)` with no `bindingValues`; the effect revalidates `effectGuard`, calls no graph store, storage area, cache, or mutation method, and returns recursively frozen `{ status: 'provider-no-storage', durableEffect: false, prepared }`. Assert Phase 54 returns `{ decision: 'admitted', value: <that envelope> }`, never accepts a direct commit return, and the raw response occurs only at `value.prepared.rawResponse`. The engine immediately takes the closed validated batch/failure, releases every reference to the raw response/envelope, and starts staging only through a new ingestion operation/certificate. Add spies proving zero durable provider-step effect and no raw bytes in stage, storage, logs, errors, diagnostics, or cache.

Require a positive two-source candidate relation with proposing-source evidence and exact current endpoints. Keep the proposer unchanged while advancing the target: incoming influence from the old relation/overlay must disappear, and an authorized replacement must have distinct deterministic relation-version and overlay-generation IDs. Then revoke/delete the target and call empty replacement with exact proposer-only selection; assert no target authority/read/existence probe, fixed `{ ok: true, status: 'cleared' }` for present/absent old overlay and present/absent target, and zero target-existence marker. Nonempty missing-target-in-selection, model-cross-source, stale-endpoint, governing/effectiveness, and purge cases reject. Seed a schema-valid synthetic party/person/vendor name in a derived label; require lexical discovery and bounded query/provenance projection only under its fresh exact scope, with zero occurrence in diagnostics/errors/raw provider/prompt persistence/unrelated partitions/unauthorized or closed output/cache outside source ownership. Mutation tests must fail if a graph module enters `CONTENT_SCRIPT_FILES`, `SKOPEO_INJECTION_FILES`, manifest content scripts, a generic message/storage bridge, MCP registration, or a remote import/host.
  </action>
  <acceptance_criteria>
    - Controlled RED exits nonzero only for missing graph runtime/integration behavior.
    - The oracle asserts exact import/boot/durable-recovery/participant-binder/facade order, zero boot cache/current-fingerprint work, and fail-closed behavior at every seeded boot step.
    - Build/update traces show withdrawal before the first provider call, invariant-only session plus one provider/model binding across staging/batches, distinct fresh certificates for preparation/provider/repair/stage/final, accepted cross-operation ID/epoch changes, rejected reuse/expiry, settings recheck before every call/stage, and pointer-last final publication.
    - Provider-only traces prove mandatory `commitCallback` → opaque `publisher.publish(effect)` acknowledgement, exact admitted `{ status: 'provider-no-storage', durableEffect: false, prepared }`, zero durable effect, raw response only in that operation result, immediate discard, and a later distinct staging operation.
    - Candidate-link traces show fresh exact-set authority for nonempty replacement, locally derived endpoint/relation/overlay versions, target-only advance/revoke suppression, and proposer-only empty clear with fixed acknowledgement/no existence leakage; proposing evidence/ownership, positive bounded query visibility, and zero model cross-source/adjudication path remain proven.
    - A schema-valid name-bearing label is lexically discoverable only under fresh exact scope and location-aware scans restrict it to authoritative record/lexical, exact-owned cache, and bounded authorized projections.
    - Query traces show exact-source/set certification, one lazily initialized and released scope, final tuple/currentness check, bounded projections, and zero content/raw handle exposure.
    - A runtime with MCP globals absent still completes synthetic build/query/restart; scans find no new MCP/message/UI/server/daemon/process surface.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set +e; out=$(node tests/skopeo-graph-runtime.test.js 2&gt;&amp;1); code=$?; set -e; test $code -ne 0; printf "%s\n" "$out" | rg "skopeo-graph-engine|SKOPEO_GRAPH|FsbSkopeoGraphEngine"'</automated>
  </verify>
  <done>The RED runtime oracle fixes the trusted boot order, fresh authority choreography, facade surface, and no-MCP/content/runtime boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the private engine, real purge ownership, recovery, and static boundary</name>
  <files>extension/utils/skopeo-graph-engine.js, extension/background.js, scripts/verify-skopeo-storage-boundary.mjs, tests/skopeo-graph-runtime.test.js</files>
  <read_first>
    - extension/utils/skopeo-graph-engine.js
    - extension/background.js
    - scripts/verify-skopeo-storage-boundary.mjs
    - tests/skopeo-graph-runtime.test.js
    - extension/utils/skopeo-corpus-store.js
    - tests/skopeo-corpus-store.test.js
    - extension/utils/skopeo-drive-authority.js
    - extension/utils/skopeo-drive-corpus-transport.js
    - extension/utils/skopeo-drive-reconciler.js
    - extension/utils/skopeo-graph-schema.js
    - extension/utils/skopeo-graph-store.js
    - extension/utils/skopeo-graph-extractor.js
    - extension/utils/skopeo-graph-query.js
    - tests/skopeo-corpus-runtime.test.js
  </read_first>
  <action>
Implement the exact frozen graph engine facade, adapting the reconciler's plain preparation and separately guarded publisher effect without preserving a certificate between operations. `buildSource` and `updateSource` both use the same replacement state machine; build admits absent source and update admits published/withheld source, while exact-fingerprint/full reuse-key equality after fresh authority and current provider/model equality returns `reused` with zero provider calls. The extractor session stores only `(partitionKey, accountPermissionId, sourceFileId, contentFingerprint, graphSchemaVersion, promptVersion, providerId, modelId)` and never certificate identity, operation ID/epoch, signal, or proof metadata. Every engine step supplies a new operation/certificate identity; the extractor consumes it once, permits operation ID/epoch to differ when invariants match, and rejects reuse or expiry before provider/effect.

For changed work: use a certified ingestion effect to close the graph pointer and purge/prove all four categories before content/provider work; use a distinct fresh certified content/preparation operation to create `{session, providerBinding}`; use another fresh certified effect that calls `verifyProviderBinding` and opens a fingerprint/schema/prompt/**provider/model-bound** staging manifest. Use one distinct fresh certified ingestion operation for each normal or repair provider step and another distinct fresh effect that rereads settings, verifies the session/batch/staging binding, and only then stages that validated batch. Use a final fresh certified effect that consumes another fresh certificate, rechecks settings/binding, finalizes coverage, derives/verifies shards, seals staging, and publishes pointer-last. Retain each Phase 54 mutation/effect guard through terminal rollback/repair. On `provider-binding-changed` or any fixed failure, perform zero next provider/stage work, discard session/staging, keep the source withheld, and require a whole-source retry under a newly captured binding; never mix batches.

Implement the provider-only seam against the existing Phase 54 five-argument function exactly. Name the private effect-value record `ProviderNoStorageResult`; its exact recursively frozen shape is `{ status: 'provider-no-storage', durableEffect: false, prepared }`. The normal/repair callback returns frozen `prepared` containing the bounded raw provider response and closed validated-batch/failure outcome. Its mandatory `commitCallback(prepared, publisher, operationSignal)` requires `operationSignal === publisher.signal` and a live signal, then returns `publisher.publish(async function providerNoStorageEffect(effectGuard) { if (effectGuard.signal !== operationSignal || !await effectGuard.validate()) throw new Error('stale-provider-step'); return deepFreeze({ status: 'provider-no-storage', durableEffect: false, prepared }); })` with no binding-values argument. Do not return `prepared` or `ProviderNoStorageResult` directly from the commit callback: Phase 54 requires the opaque acknowledgement capability returned by `publisher.publish`, then unwraps the effect value as the admitted operation's `value`. The method name `publisher.publish` is only the existing Phase 54 acknowledgement handoff here: `providerNoStorageEffect` performs exactly zero graph-store/storage/cache mutation and publishes no raw response or record durably; raw response bytes exist only at `result.value.prepared.rawResponse`. Immediately extract the validated batch/fixed failure and drop all references to the raw/envelope. A later distinct fresh certified ingestion effect receives only the validated batch and performs staging. Use the same acknowledgement discipline for any other ingestion step with no durable write, but reserve `ProviderNoStorageResult` for normal/repair calls and test its key set, freeze, `durableEffect: false`, zero binding values, and zero durable-effect spy.

Implement `replaceCandidateRelations` as a non-model ingestion operation with a descriptor-safe structural branch before any store/target lookup. For nonempty `relations`, require a dense duplicate-free exact set containing the proposing source and every unique target (2–32 sources), parse the complete intent set through Plan 01, and enter a fresh Phase 54 exact-set operation. Inside it, load only current fragments for that certified set; resolve stable endpoint IDs to exact current record-version IDs/generations; require `from` and every evidence locator to belong to the proposing source; apply the closed cross-document predicate/endpoint matrix; derive each relation-version ID from exact partition/relation kind (the allowlisted predicate)/stable relation/proposer record-version+generation/target record-version+generation/canonical evidence identity; derive the overlay-generation ID from the canonical sorted complete relation set including both endpoint versions/generations; and call Plan 02 `replaceCandidateRelations` once as a guarded pointer-last effect. Recheck every selected source/generation before and after the effect. Provider responses, candidateRefs/handles, labels, similarity, filenames, dates, and normalized text cannot supply endpoint identity.

For exact `relations: []`, require exact proposer-only source selection `{ sourceFileId: proposingSourceFileId }`, reject an extraneous set/target field, and enter a fresh Phase 54 exact-source ingestion operation. Do not load, certify, resolve, enumerate, or test any former target; call the store once with exact empty relations/target-generations and return frozen `{ ok: true, status: 'cleared' }` regardless of prior overlay/target existence. This clear removes all proposer-owned relation/adjacency/cache influence even after a target is revoked or advances, and exposes no target name/ID/existence or existence-dependent reason/result shape. For a target-only advance with proposer unchanged, all readers reject the old overlay immediately; a later authorized nonempty replacement derives distinct deterministic relation-version and overlay-generation IDs. Every output/state remains `cross-document-candidate` and contains no equality, same-as, supersession, effectiveness, precedence, governing, deadline, confidence, clearance, or adjudication claim.

Wrap `getById`, `searchLexical`, `neighbors`, and `inspectProvenance` in `query` or `display` Phase 54 exact-source/set operations. After certification, compare active fragment generations, mint one query scope, await `ensureScopeCache(scope)` to hydrate only that certified set, recheck authority, execute one closed method, release the scope in `finally`, and return output only after the existing final tuple/source currentness checks. Candidate edges additionally require both endpoint sources/generations in the selection. Schema-validated labels, including party/person/vendor names, may be indexed and returned only from authoritative records/source-owned lexical shards through that live exact-owned cache and bounded authorized projection; they are absent from diagnostics/errors/raw provider or prompt persistence, unrelated partitions, rejected/closed/unauthorized output, status, and cache entries outside matching source/generation ownership. `inspectStatus` returns only `absent|withheld|staging|published|purging|repairing` plus fixed reason/version/count fields under exact-source display authority; it returns no source ID/name/fingerprint/derived label/raw key outside authorized operation state.

Add graph imports after the Phase 54 chain. During the existing trusted boundary boot, construct graph store/query/extractor, register the query cache owner without hydrating it, and register all four graph participant binders plus three exact empty reserved binders through `registerAuthorizedPurgeParticipant`; the final graph boot block must not call legacy `registerPurgeParticipant`. Run corpus recovery, run bounded **durable-only** graph journal/orphan recovery, then expose one frozen `fsbSkopeoGraphEngineFacade`; any failure leaves it null and marks the graph boundary closed. The participant binder/verifier owns capability authentication: do not pass the raw corpus mutation guard into graph code. Boot must not request source fingerprints, infer current endpoint generations, read graph shards for query, or build MiniSearch/adjacency caches. Do not append graph modules to injection lists or add a message/MCP handler.

Extend the existing storage-boundary verifier's literal dependency/closure analysis. Require every graph module background-only and exact ordered; require all seven final graph-boundary participant registrations to use `registerAuthorizedPurgeParticipant` and reject legacy `registerPurgeParticipant` within that marked boot block; reject content/injection reachability, direct generic storage bridges, new remote hosts, Graphify imports/runtime code, Python/process/database/IndexedDB/server/daemon/MCP registration, eval/new Function/dynamic import, JMESPath/arbitrary graph expressions, embeddings/vectors, and runtime-loaded code. Keep fixed line-number/path diagnostics only.
  </action>
  <acceptance_criteria>
    - `node tests/skopeo-graph-runtime.test.js` exits 0 for build, exact reuse, update, provider/model mid-generation drift, candidate-link replacement/query/purge, query, provenance, provider unavailable, cancellation, account/source drift, deletion/revocation, worker restart, and every boot failure.
    - `node scripts/verify-skopeo-storage-boundary.mjs` exits 0 and its mutation fixtures detect each forbidden content/storage/runtime/MCP/Graphify/dynamic-query path.
    - Four real graph participant binders are registered before recovery through corpus-owned one-call capability verifiers; only citations/counts/alerts use exact empty binders; graph facade is unavailable until corpus and durable graph recovery succeed.
    - Boot performs zero current-source/fingerprint/shard/cache hydration; the first query's fresh exact scope is the only cache-init authority.
    - Preparation, every normal/repair provider call, every stage, and final publication have distinct fresh Phase 54 certifications; matching invariants across different operation IDs/epochs succeed, reused/expired certificates fail, provider/model checks precede applicable work, old truth is absent before the first changed-source provider call, and active pointers are last.
    - Every effectful provider-only call uses the mandatory opaque publisher acknowledgement and exact admitted `{ status: 'provider-no-storage', durableEffect: false, prepared }`; runtime spies prove zero durable provider effect, raw response only in that result then discarded, and staging under a later fresh certificate.
    - Positive candidate-link runtime coverage proves fresh exact-set authority for nonempty replacement, exact endpoint-bound relation/overlay generations, target-only advance/revoke suppression, proposing-source evidence/ownership, bounded query projection, and proposer-only clear with no target read/existence leakage; model cross-source refs and effect/governing claims reject.
    - Location-aware marker coverage proves a valid name-bearing label remains searchable/projectable only through authoritative record/lexical and fresh exact-owned cache/output paths, never diagnostics/errors/raw provider/prompt persistence/unrelated/unauthorized/closed/cache-outside-ownership paths.
    - No graph module is content-injected or exposed through a generic message, storage, MCP, UI, server, daemon, database, Python, Graphify, or remote-host surface.
  </acceptance_criteria>
  <verify>
    <automated>node --check extension/utils/skopeo-graph-engine.js &amp;&amp; node --check extension/background.js &amp;&amp; node tests/skopeo-graph-runtime.test.js &amp;&amp; node tests/skopeo-corpus-runtime.test.js &amp;&amp; node scripts/verify-skopeo-storage-boundary.mjs</automated>
  </verify>
  <done>The production extension owns one trusted, durably recovered, background-only graph engine with authorized purge participation, provider-bound atomic source updates, engine-only candidate links, and lazily initialized closed reads.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Land all 37 deterministic fixtures, package/full-suite gates, and pinned conceptual provenance</name>
  <files>tests/fixtures/skopeo-graph-evals/manifest.json, tests/fixtures/skopeo-graph-evals/cases.json, tests/skopeo-graph-evals.test.js, tests/skopeo-graph-runtime.test.js, package.json, README.md</files>
  <read_first>
    - tests/fixtures/skopeo-graph-evals/manifest.json
    - tests/fixtures/skopeo-graph-evals/cases.json
    - tests/skopeo-graph-evals.test.js
    - tests/skopeo-graph-runtime.test.js
    - package.json
    - README.md
    - .planning/phases/55-chrome-local-graph-incremental-truth-foundation/55-AI-SPEC.md
    - .planning/phases/55-chrome-local-graph-incremental-truth-foundation/55-VALIDATION.md
    - .planning/phases/55-chrome-local-graph-incremental-truth-foundation/55-RESEARCH.md
    - tests/capability-search-eval.test.js
    - tests/skopeo-corpus-store.test.js
    - catalog/descriptors/_fixtures/
  </read_first>
  <action>
Following the stable manifest/fixture loading pattern in `catalog/descriptors/_fixtures/`, create a versioned manifest and exactly 37 immutable synthetic/irreversibly-redacted case objects in `cases.json`: P01–P06, Q01–Q06, A01–A07, I01–I05, L01–L03, R01–R07, and B01–B03. Each case must include ordered excerpts, excerpt/call budgets, authority transition script, recorded raw provider response and usage, expected publish/withhold state and fixed reason, provisional expected records/relations/spans, expected durable keys, query/absence proof, `label_version`, nullable `gold_label_version`, `review_status: 'pending'|'approved'`, `required_reviewer_roles`, `approved_reviewer_roles`, and a nullable nonidentifying `review_record_ref`. Pending cases require `gold_label_version: null`, an empty approved-role list, and no review record; approved cases require exact role coverage, matching gold/label versions, and a genuine review record.

Assign the remaining security choreography explicitly without changing the 37 IDs or category counts. P01 includes distinct synthetic party/person/vendor name markers solely in valid derived `record.label` fields and expects lexical search plus bounded authorized provenance projection. P06 is the positive two-source `cross-document-candidate` path and records the exact relation-version tuple and canonical overlay generation. I03 proves model cross-source refs reject. I05 combines a fallback attempt with provider/model drift after one validated batch and asserts the provider call used the exact `provider-no-storage` acknowledgement, zero durable provider effect, raw response confined to the admitted operation result, and zero fallback/next-provider/stage effect. R03 covers authority loss after response plus expired/reused certificate rejection. R06 keeps the proposer generation unchanged while advancing only the target, expects old incoming influence absent/non-current, and expects a deterministic distinct replacement relation-version/overlay-generation pair. R07 revokes/deletes the target and then performs proposer-only empty replacement, expecting no target authority/read/existence signal and fixed `{ ok: true, status: 'cleared' }`. B03 proves exact reuse only with a distinct fresh certificate matching the invariant session tuple and rejects a reused certificate identity. Do not put customer text, credentials, live identifiers, or real identifying names in fixtures; the P01 name-shaped markers are clearly synthetic derived labels and remain subject to location-aware privacy assertions.

Do not fabricate counsel, legal-operations, privacy/security, or evaluation review. Initialize every unadjudicated case as `review_status: 'pending'` with null gold version/review record and no approved roles. The executable expert-review gate returns `human_needed` while any required case is pending, role coverage is absent, versions disagree, or the recorded approval artifact is missing. Only a real external adjudication may populate approved roles/gold version/review record and change status to `approved`; deterministic code cannot self-approve. Pending expected sets may drive a clearly labeled provisional regression comparison, but they are not gold, do not make domain-fidelity thresholds release-authoritative, and must never be reported as a Critical domain-fidelity PASS.

Implement the eval harness against production schema/store/extractor/query/engine modules and fake Phase 54 authority/storage/provider. Enforce every deterministic non-domain structural/security gate with no averaging: zero unsupported/partial publications relative to closed schema; 100% invalid/max-plus-one rejection before effect; zero model cross-source/partition acceptance; zero model-selected tool/network effects; zero provider/model mixing or late writes/stale hits; complete authorized participant absence; deterministic query snapshots before/after MV3 recreation; candidate-only positive cross-document traversal with exact endpoint versions/proposing evidence and canonical relation/overlay IDs; target-only advance/revoke suppression plus proposer-only no-leak clear; distinct fresh-operation certificate acceptance and reuse/expiry rejection; exact provider no-storage publisher acknowledgement with zero durable mutation/raw persistence; all exact-max/max-plus-one budgets; and no confidence/clearance/equivalence/effectiveness/precedence/governing/deadline claims.

Use location-aware marker classes instead of a global party-name ban. Raw source-excerpt/full-text, filename, prompt, provider-output, credential, and URL markers must occur zero times after their explicitly bounded transient input/operation-result lifetime. Synthetic party/person/vendor label markers are allowed only in schema-validated authoritative record labels, matching source-owned lexical shards, exact source/generation-owned cache entries, and freshly authorized bounded query/provenance projections. The immutable synthetic fixture and fake-provider response are test inputs, not production persistence; scans begin at the production operation result and separately prove that its raw envelope is released. Require zero such label marker in production diagnostics/errors, persisted raw provider/prompt values, control/journal/adjacency/candidate records, unrelated partitions, unauthorized/closed/status output, or cache outside ownership. The harness must enumerate all 37 IDs from the manifest and fail on missing, duplicate, extra, category-count drift, or invalid review metadata. It may compare pending expected sets only as `provisional_regression`; it must separately emit/assert `domain_fidelity: human_needed` until every required approval is genuine. It remains network-free and uses no LLM judge.

Add `test:skopeo-graph-evals` to package scripts so it runs, exactly once and in dependency order, `skopeo-graph-schema`, `universal-provider-cancellation`, `skopeo-graph-store`, `skopeo-graph-extractor`, `skopeo-graph-query`, `skopeo-graph-runtime`, and `skopeo-graph-evals`. Add that aggregate exactly once to the normal `npm test` chain; do not duplicate individual Phase 55 commands there. Keep `validate:extension` ownership of the strengthened storage verifier.

Add one README Acknowledgements entry naming Graphify Labs/Graphify and requiring all provenance fields literally: repository URL `https://github.com/Graphify-Labs/graphify`; exact reviewed commit `abff1b1ca4052fcf9d955c5f6a034088723f4536`; MIT license; `Copyright 2026 Safi Shamsi`; reviewed upstream file inventory `LICENSE`, `docs/how-it-works.md`, `ARCHITECTURE.md`, `graphify/ids.py`, `graphify/validate.py`, and `graphify/cache.py`; conceptual-only influences (bounded extraction, validate-before-build, evidence-bearing records/relations, fingerprints, traversal); exact statements `no copied code` and `no runtime dependency`; and an explicitly empty copied-code inventory. Add runtime/static assertions for every field and for zero Graphify file/import/dependency. Do not create a vendor snapshot or license copy because no code is copied.

Run the focused aggregate, storage verifier, extension validation, and full `npm test`. Record deterministic structural/security gates independently from `domain_fidelity: human_needed`; never summarize the pending expert gate as PASS. Treat optional live configured-provider qualification as unrun unless separately authorized; it may use only these synthetic cases and cannot weaken/replace deterministic admission or expert adjudication.
  </action>
  <acceptance_criteria>
    - Manifest and harness enumerate exactly 37 unique IDs with category counts 6/6/7/5/3/7/3 and complete review metadata; P01 covers authorized name-bearing label search/privacy, P06 exact candidate relation/overlay identity, I03 model-cross-source rejection, I05 provider no-storage/drift, R03 certificate expiry/reuse, R06 target-only advance, R07 proposer-only clear, and B03 distinct-certificate reuse-key behavior.
    - `npm run test:skopeo-graph-evals` exits 0, remains network-free, enforces every deterministic structural/security threshold including location-aware label privacy, exact provider acknowledgement/zero storage, fresh-certificate choreography, relation/overlay generation replacement, and no-leak clear; it reports provisional comparisons separately, emits `domain_fidelity: human_needed` for pending cases, and completes within the validation strategy's 30-second focused target.
    - `package.json` registers the aggregate exactly once in normal `npm test`; no dependency/lockfile/provider/host/MCP script is added.
    - `node scripts/verify-skopeo-storage-boundary.mjs` and `npm run validate:extension` exit 0.
    - Full `npm test` exits 0; any generated date-only showcase artifact is left byte-identical to its pre-run state before commit.
    - README contains the exact repository URL, commit, MIT, `Copyright 2026 Safi Shamsi`, six-file reviewed inventory, conceptual-only influence, `no copied code`, `no runtime dependency`, and empty copied-code inventory; static scan finds zero Graphify runtime/import files.
    - Expert domain review remains `human_needed` until genuine required-role approvals exist; neither provisional expected-set comparison nor optional live-provider output is recorded as a Critical domain-fidelity PASS.
    - Optional live-provider qualification remains unrun unless explicitly authorized and is never recorded as deterministic or expert-review PASS.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:skopeo-graph-evals &amp;&amp; node scripts/verify-skopeo-storage-boundary.mjs &amp;&amp; npm run validate:extension</automated>
  </verify>
  <done>The complete executable corpus, deterministic trusted-boundary gates, full regression closure, and exact conceptual provenance prove the Phase 55 structural/security requirements; domain-fidelity adjudication is honestly recorded as `human_needed` until real expert approval.</done>
</task>

</tasks>

<verification>
Run `npm run test:skopeo-graph-evals && node scripts/verify-skopeo-storage-boundary.mjs && npm run validate:extension && npm test`. Confirm no unexpected worktree changes remain after test side effects. Record deterministic structural/security status separately from the expert domain-fidelity `human_needed|approved` gate. Optional live-provider and local-Chrome qualification are separately human-authorized/non-blocking evidence and cannot alter either status.
</verification>

<success_criteria>
- Users can build, update, query, and inspect source-owned contract graph intelligence entirely inside the bundled Chrome extension.
- Every model call and query is freshly permission-scoped, bounded, source-attributable, and fail closed through cancellation/replacement/restart.
- Four real graph participant categories remove stale truth before replacement; all 37 fixtures prove exact deterministic safety/structure thresholds, while domain-fidelity remains `human_needed` until genuine review approval.
- Content, MCP, and external processes are not graph authority or runtime dependencies.
- Graphify is pinned with exact repository/commit/license/copyright/file inventory and attributed as conceptual influence only, with no copied code, runtime import, package, service, database, daemon, or Python process.
</success_criteria>

<output>
After completion, create `.planning/phases/55-chrome-local-graph-incremental-truth-foundation/55-05-SUMMARY.md` with separate deterministic structural/security results and expert domain-fidelity status; write `human_needed` unless genuine approved review metadata exists.
</output>
