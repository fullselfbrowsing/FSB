---
phase: 59-reverse-request-channel-security-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - mcp/src/types.ts
  - mcp/src/ext-protocol.ts
  - extension/utils/redactForLog.js
  - extension/utils/diagnostics-ring-buffer.js
  - scripts/verify-agent-provider-flags.mjs
  - tests/mcp-reverse-channel-contract.test.js
  - tests/redact-for-log.test.js
  - tests/diagnostics-ring-buffer.test.js
  - tests/agent-provider-forbidden-flags.test.js
  - mcp/package.json
  - package.json
autonomous: true
requirements:
  - CHAN-01
  - CHAN-05
  - CHAN-07
must_haves:
  truths:
    - "The reverse-request family is separate from MCPMessageType, and every legacy MCP type, response union, tool hash, agent envelope, and default relay serialization remains byte-identical."
    - "Every FSB bridge credential-shaped substring is removed both by caller-level redaction and again at the diagnostic sink."
    - "Every MCP build recursively rejects forbidden agent-provider flags before TypeScript compiles, with no exemption inside the scanned directory."
  artifacts:
    - path: "mcp/src/ext-protocol.ts"
      provides: "Strict ext frame constructors/validators and stable typed error codes"
      contains: "parseExtFrame"
    - path: "tests/mcp-reverse-channel-contract.test.js"
      provides: "INV-01 byte freeze plus malformed-frame proof"
      contains: "legacy relay serialization"
    - path: "scripts/verify-agent-provider-flags.mjs"
      provides: "Recursive CHAN-07 prebuild scanner"
      contains: "mcp/src/agent-providers"
    - path: "extension/utils/diagnostics-ring-buffer.js"
      provides: "Sink-level shared-secret sanitization"
      contains: "REDACTED_FSB_BRIDGE_SECRET"
  key_links:
    - from: "mcp/src/types.ts"
      to: "mcp/src/ext-protocol.ts"
      via: "runtime validators consume separate ExtMessage interfaces"
      pattern: "ExtRequest|ExtResponse|ExtEvent"
    - from: "mcp/package.json"
      to: "scripts/verify-agent-provider-flags.mjs"
      via: "prebuild runs scanner before tsc"
      pattern: "prebuild.*verify-agent-provider-flags"
    - from: "extension/utils/redactForLog.js"
      to: "extension/utils/diagnostics-ring-buffer.js"
      via: "shared pure string scrubber is used at caller and sink boundaries"
      pattern: "redactBridgeSecretsInString"
---

<objective>
Freeze the additive reverse-frame contract and land the permanent secret/flag gates before any bridge routing or pairing behavior exists.

Purpose: Make INV-01, CHAN-05, and CHAN-07 mechanically enforceable from the first security-channel commit.
Output: Separate ext types/validators, byte-freeze tests, two-layer secret redaction, and a recursive MCP prebuild scanner.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-reverse-request-channel-security-foundation/59-CONTEXT.md
@.planning/phases/59-reverse-request-channel-security-foundation/59-RESEARCH.md
@.planning/phases/59-reverse-request-channel-security-foundation/59-PATTERNS.md
@.planning/phases/59-reverse-request-channel-security-foundation/59-VALIDATION.md

<interfaces>
Create these separate bridge interfaces without adding any member to `MCPMessageType`:

```ts
export type BridgeCapability = 'agent-spawn';

export interface ExtRequest {
  id: string;
  type: 'ext:request';
  method: string;
  payload: Record<string, unknown>;
}

export interface ExtError {
  code: 'agent_provider_offline' | 'bridge_topology_changed' |
    'ext_unauthorized' | 'invalid_ext_request' | 'ext_request_timeout';
  message: string;
  retryable: boolean;
}

export type ExtResponse =
  | { id: string; type: 'ext:response'; payload: Record<string, unknown>; error?: never }
  | { id: string; type: 'ext:response'; error: ExtError; payload?: never };

export interface ExtEvent {
  id: string;
  type: 'ext:event';
  event: string;
  payload: Record<string, unknown>;
}

export type ExtMessage = ExtRequest | ExtResponse | ExtEvent;
```

`ext-protocol.ts` exports `parseExtFrame(raw: unknown): ExtMessage | null`, `makeExtError(id, code, message, retryable)`, and stable limits for ID/method/event strings and plain-object payloads. It never accepts or emits a `secret`, `token`, URL, CLI flags, or spawn-specific payload.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the separate ext contract and byte-freeze fixtures</name>
  <files>mcp/src/types.ts, mcp/src/ext-protocol.ts, tests/mcp-reverse-channel-contract.test.js, package.json</files>
  <read_first>
- .planning/phases/59-reverse-request-channel-security-foundation/59-CONTEXT.md
- .planning/phases/59-reverse-request-channel-security-foundation/59-RESEARCH.md
- .planning/phases/59-reverse-request-channel-security-foundation/59-PATTERNS.md
- mcp/src/types.ts
- mcp/src/bridge.ts
- tests/mcp-version-parity.test.js
- extension/ai/tool-definitions.js
- mcp/ai/tool-definitions.cjs
- package.json
  </read_first>
  <action>
Create `mcp/src/ext-protocol.ts` and add only the separate type exports shown in `<interfaces>` to `mcp/src/types.ts`. Do not add `ext:*` to `MCPMessageType`, change `MCPResponse`, reorder any existing union member, or alter any existing relay field. Add optional `capabilities?: BridgeCapability[]` to `RelayHello`, `RelayWelcome`, and `RelayState`, but all later default constructors must omit the field rather than serialize `[]`.

Implement strict runtime validation in `parseExtFrame`: require a plain object; `id` must be a non-empty string no longer than 200 characters; request `method` and event `event` must match `^[a-z][a-z0-9_.:-]{0,127}$`; payload must be a non-array plain record; response must contain exactly one of a plain payload or an error with an allowlisted code, non-empty message <=300 chars, and boolean `retryable`; reject unknown top-level keys that imply control-plane input (`secret`, `token`, `flags`, `argv`, `command`, `cwd`, `env`). Return `null` rather than throwing or logging raw input. `makeExtError` returns the response error union exactly.

Create `tests/mcp-reverse-channel-contract.test.js` using direct Node assertions and the compiled `mcp/build` exports. It must prove all three valid frames round-trip, all malformed/ambiguous/oversize/control-plane frames reject, and error codes remain the exact five-value set. Copy the legacy relay fixture objects and assert `JSON.stringify({type:'relay:hello',instanceId:'fixture-relay'})`, welcome, and state produce exact frozen strings when capabilities are absent; assert an `agent-spawn` capability is additive only when explicitly supplied. Invoke the unchanged `tests/mcp-version-parity.test.js` in focused verification rather than weakening or editing its historical assertions.

Add `node tests/mcp-reverse-channel-contract.test.js` once to root `package.json`'s serial test command immediately after the MCP build/bridge lifecycle cluster. Preserve every existing command and order otherwise.
  </action>
  <acceptance_criteria>
- `mcp/src/types.ts` exports `BridgeCapability`, `ExtRequest`, `ExtResponse`, `ExtEvent`, and `ExtMessage`, while `extractMcpMessageTypes()` in the existing parity test still yields its current exact list.
- `mcp/src/ext-protocol.ts` contains `parseExtFrame` and `makeExtError`; its source contains no `child_process`, `spawn(`, or production handler.
- A response containing both `payload` and `error`, or any request containing `secret`, `token`, `flags`, `argv`, `command`, `cwd`, or `env`, returns null.
- Legacy hello/welcome/state JSON strings are byte-identical without capability fields; explicit capability serialization contains exactly `"capabilities":["agent-spawn"]`.
- `npm --prefix mcp run build`, `node tests/mcp-version-parity.test.js`, and `node tests/mcp-reverse-channel-contract.test.js` exit 0.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix mcp run build && node tests/mcp-version-parity.test.js && node tests/mcp-reverse-channel-contract.test.js</automated>
  </verify>
  <done>The reverse family is runtime-validated and additive while historical MCP/relay bytes remain frozen.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce caller-level and sink-level bridge-secret redaction</name>
  <files>extension/utils/redactForLog.js, extension/utils/diagnostics-ring-buffer.js, tests/redact-for-log.test.js, tests/diagnostics-ring-buffer.test.js</files>
  <read_first>
- .planning/phases/59-reverse-request-channel-security-foundation/59-RESEARCH.md
- extension/utils/redactForLog.js
- extension/utils/diagnostics-ring-buffer.js
- tests/redact-for-log.test.js
- tests/diagnostics-ring-buffer.test.js
- tests/audit-log-no-secret.test.js
  </read_first>
  <action>
In `redactForLog.js`, add a pure `redactBridgeSecretsInString(value)` that replaces every `fsb-auth.` token followed by exactly 43 base64url characters (`[A-Za-z0-9_-]{43}`) with `[REDACTED_FSB_BRIDGE_SECRET]`. It must also catch the token when embedded after `Sec-WebSocket-Protocol:`, `token=`, `?token=`, or inside an Error message because the common token matcher is global. Apply it to `Error.message`; preserve ordinary string shape-only behavior and every existing global/CommonJS export, adding the scrubber to both export surfaces.

In `diagnostics-ring-buffer.js`, add a bounded local sanitizer used after the explicit safe-field whitelist and before both `_inMemoryRing.push` and persistent storage. It sanitizes every string in `prefix`, `category`, `message`, and nested `redactedContext` through maximum depth 4, maximum 20 object keys per level, and maximum 100 array items; unsupported values become existing shape-safe values rather than passing raw content. Reuse `globalThis.redactBridgeSecretsInString` when loaded, with an identical private token-regex fallback because `background.js` currently imports the ring before `redactForLog.js`.

Extend both direct tests with the same full fixture `fsb-auth.AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA` (43 `A`s), plus header/query/Error/nested-object wrappers. Assert neither the full token nor the distinctive 16-character interior substring exists in `JSON.stringify` of redactor output, in-memory ring output, or mocked `chrome.storage.local` output. Preserve existing FIFO, field whitelist, rate-limit, URL-origin, and shape assertions.
  </action>
  <acceptance_criteria>
- `redactBridgeSecretsInString` is available on `globalThis` and CommonJS exports and replaces the full token with exactly `[REDACTED_FSB_BRIDGE_SECRET]`.
- `redactForLog(new Error('failed fsb-auth.' + 'A'.repeat(43)))` contains no 16-character run from the credential.
- Diagnostic `message`, `category`, `prefix`, and nested `redactedContext` are sanitized before memory and storage writes even when `redactForLog.js` has not loaded.
- Existing non-secret redaction shapes and the 100-entry FIFO contract remain green.
- `node tests/redact-for-log.test.js`, `node tests/diagnostics-ring-buffer.test.js`, and `node tests/audit-log-no-secret.test.js` exit 0.
  </acceptance_criteria>
  <verify>
    <automated>node tests/redact-for-log.test.js && node tests/diagnostics-ring-buffer.test.js && node tests/audit-log-no-secret.test.js</automated>
  </verify>
  <done>Bridge credential patterns are removed at both caller and tracked diagnostic-sink boundaries.</done>
</task>

<task type="auto">
  <name>Task 3: Add the permanent forbidden-agent-flag prebuild gate</name>
  <files>scripts/verify-agent-provider-flags.mjs, tests/agent-provider-forbidden-flags.test.js, mcp/package.json, package.json</files>
  <read_first>
- .planning/REQUIREMENTS.md
- scripts/verify-classification-gate.mjs
- scripts/verify-recipe-path-guard.mjs
- mcp/package.json
- package.json
- tests/recipe-path-guard.test.js
  </read_first>
  <action>
Create `scripts/verify-agent-provider-flags.mjs`. Resolve the scan root relative to the repository by default and accept `--root <path>` only for tests. Recursively scan every regular file and symlink-resolved regular file under `mcp/src/agent-providers`; a missing directory is clean until Phase 60 creates it. Fail closed on directory/read errors. Detect the exact three CHAN-07 literals defined in an array in this external script, print one deterministic `relative/path:line: forbidden agent-provider flag` diagnostic per match without printing surrounding source, and exit 1 when any match exists. Do not implement ignore comments, allowlists, path exclusions, or extension filters.

Create `tests/agent-provider-forbidden-flags.test.js` with `mkdtempSync` fixtures proving: missing directory exits 0; empty/clean nested files exit 0; each forbidden literal in `.ts`, `.js`, `.json`, and extensionless files exits 1 with relative path and line; multiple matches all report; unreadable/read-error behavior fails on platforms where permissions can be enforced. Remove fixtures in `finally`.

Add `"prebuild": "node ../scripts/verify-agent-provider-flags.mjs"` to `mcp/package.json`, so root tests, MCP builds, and prepublish all gate before `tsc`. Add the scanner unit test once to root `package.json` immediately before the MCP build. Do not create `mcp/src/agent-providers` in this phase merely for the gate.

Run the full root suite using the workspace-only temporary Phase 39 fixture: create the deleted parent directory and symlink to the archived file, install a shell trap that removes the symlink and empty directory, run `npm test`, then confirm the link is absent and unstaged. Do not stage/commit the fixture or alter the user's historical deletions.
  </action>
  <acceptance_criteria>
- The scanner recursively checks all regular files under exactly `mcp/src/agent-providers` with no exemption mechanism.
- Missing scan directory exits 0; a fixture containing any one of the three CHAN-07 literals exits 1.
- `mcp/package.json` runs the scanner in `prebuild`, before the existing clean/tsc/copy build body.
- Root `npm test` contains exactly one scanner unit test and one reverse-channel contract test without dropping any existing command.
- Focused scanner tests, MCP build, and full `npm test` exit 0; the temporary Phase 39 link is absent afterward.
  </acceptance_criteria>
  <verify>
    <automated>node tests/agent-provider-forbidden-flags.test.js && npm --prefix mcp run build && test ! -e .planning/phases/39-breadth-c-commerce-travel-misc-most-sensitive/39-06-REMAINING-APPS.md</automated>
  </verify>
  <done>The future adapter directory is permanently scanned before every MCP build, and Plan 01's complete contract/redaction cluster is root-suite gated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Parsed JSON → reverse frame types | Browser/relay input can be malformed, ambiguous, oversized, or attempt to smuggle control-plane fields. |
| Runtime values → logs/diagnostic storage | Error strings and nested context may contain the session credential. |
| Future adapter source → MCP build | Later code may introduce an auto-approval flag that converts prompt injection into silent RCE. |

## Blocking Threat Register

| Threat | Severity | Mitigation | Blocking evidence |
|--------|----------|------------|-------------------|
| T59-04 secret disclosure | High | exact token scrubber at caller and sink; raw-input validators never log | full and interior substrings absent from Error/ring/storage fixtures |
| T59-06 wire drift | High | separate ext union, optional omission, historical hash/order tests unchanged | version parity + exact relay JSON fixtures |
| T59-07 forbidden future flags | Critical | recursive external prebuild scanner with no local exemptions | positive fixtures fail; MCP prebuild invokes scanner |
| Malformed frame control smuggling | High | strict plain-object/field/key validation and closed error codes | malformed/ambiguous/control-field table rejects |

All High/Critical evidence is automated. Any failed byte freeze, raw-token assertion, or scanner positive fixture blocks the plan.
</threat_model>

<verification>
1. `npm --prefix mcp run build`
2. `node tests/mcp-version-parity.test.js && node tests/mcp-reverse-channel-contract.test.js`
3. `node tests/redact-for-log.test.js && node tests/diagnostics-ring-buffer.test.js && node tests/audit-log-no-secret.test.js`
4. `node tests/agent-provider-forbidden-flags.test.js`
5. Run `npm test` with the temporary Phase 39 fixture/trap; remove fixture.
6. `git diff --check` and confirm no `child_process`, spawn implementation, or production capability advertisement was added.
</verification>

<success_criteria>
- CHAN-01, CHAN-05, and CHAN-07 have permanent automated gates.
- Every legacy MCP/tool/default-relay byte contract stays unchanged.
- Raw bridge credentials cannot survive either logging boundary.
- Future agent-provider files cannot compile when a forbidden flag is present.
- The full root suite is green and the historical workspace fixture is absent.
</success_criteria>

<output>
After completion, create `.planning/phases/59-reverse-request-channel-security-foundation/59-01-SUMMARY.md`.
</output>
