---
phase: 20-integration-cap-ui-docs-edge-cases
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - extension/ui/control_panel.html
  - extension/ui/options.js
  - extension/ui/cap-counter-helpers.js
  - tests/trigger-cap-settings-ui.test.js
autonomous: true
requirements: ["Integration/composition"]
user_setup: []
must_haves:
  truths:
    - "The control panel shows a Trigger Concurrency settings-card immediately after Agent Concurrency."
    - "The trigger cap UI reads and writes chrome.storage.local.fsbTriggerCap with default 8, minimum 1, maximum 64, and step 1."
    - "The active trigger counter renders 'N of M active' from chrome.storage.session.fsbTriggerRegistry.records and counts only armed, needs_attention, and blocked snapshots."
  artifacts:
    - path: "extension/ui/control_panel.html"
      provides: "Trigger Concurrency card"
      contains: "fsbTriggerCap"
    - path: "extension/ui/options.js"
      provides: "Trigger cap cache/load/input/reset/save/listener wiring"
      contains: "refreshActiveTriggerCount"
    - path: "extension/ui/cap-counter-helpers.js"
      provides: "Pure active-trigger counter helper"
      contains: "computeActiveTriggerCount"
    - path: "tests/trigger-cap-settings-ui.test.js"
      provides: "Source-shape and helper coverage for the Trigger Concurrency card"
  key_links:
    - from: "extension/ui/options.js"
      to: "chrome.storage.local.fsbTriggerCap"
      via: "loadSettings/saveSettings/defaultSettings"
      pattern: "fsbTriggerCap"
    - from: "extension/ui/options.js"
      to: "chrome.storage.session.fsbTriggerRegistry"
      via: "refreshActiveTriggerCount"
      pattern: "fsbTriggerRegistry"
    - from: "extension/ui/cap-counter-helpers.js"
      to: "extension/ui/options.js"
      via: "globalThis.computeActiveTriggerCount"
      pattern: "computeActiveTriggerCount"
---

<objective>
Create the Trigger Concurrency control panel surface, the active-trigger counter helper, and focused source-shape tests.

Purpose: Phase 15 already enforces the trigger cap at runtime; Phase 20 must expose that same `fsbTriggerCap` setting in the UI with the approved card copy and active-watch context.
Output: `extension/ui/control_panel.html`, `extension/ui/options.js`, `extension/ui/cap-counter-helpers.js`, and `tests/trigger-cap-settings-ui.test.js` updated for D-01 through D-05.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-integration-cap-ui-docs-edge-cases/20-CONTEXT.md
@.planning/phases/20-integration-cap-ui-docs-edge-cases/20-UI-SPEC.md
@.planning/phases/20-integration-cap-ui-docs-edge-cases/20-RESEARCH.md
@.planning/phases/20-integration-cap-ui-docs-edge-cases/20-VALIDATION.md
@.planning/phases/20-integration-cap-ui-docs-edge-cases/20-PATTERNS.md
@extension/ui/control_panel.html
@extension/ui/options.js
@extension/ui/cap-counter-helpers.js
@tests/cap-counter-live.test.js
@tests/agent-cap-ui.test.js
@tests/change-report-settings-ui.test.js
@tests/trigger-cap.test.js

<interfaces>
From `20-UI-SPEC.md`, the new card must use these exact IDs and values:
```text
fsbTriggerCap
fsbTriggerCapDisplay
fsbTriggerCapReset
fsbTriggerCapValidation
fsbTriggerCapCurrentActive
default 8, min 1, max 64, debounce 100ms
```

From `extension/ui/cap-counter-helpers.js`, preserve the dual export shape:
```javascript
if (typeof module !== 'undefined' && module.exports) { module.exports = { ... }; }
if (typeof globalThis !== 'undefined') { globalThis.computeActiveAgentCount = computeActiveAgentCount; }
```

From `20-CONTEXT.md` D-03, the UI active statuses are exactly:
```text
include: armed, needs_attention, blocked
exclude: fired, timed_out, stopped, malformed records
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Trigger Concurrency source-shape tests</name>
  <files>tests/trigger-cap-settings-ui.test.js</files>
  <read_first>
    - tests/cap-counter-live.test.js
    - tests/agent-cap-ui.test.js
    - tests/change-report-settings-ui.test.js
    - .planning/phases/20-integration-cap-ui-docs-edge-cases/20-UI-SPEC.md
  </read_first>
  <action>Create `tests/trigger-cap-settings-ui.test.js` using the existing plain Node assertion style. Cover D-01 through D-05 explicitly: card placement after Agent Concurrency inside `advanced-settings-grid`, exact IDs `fsbTriggerCap`, `fsbTriggerCapDisplay`, `fsbTriggerCapReset`, `fsbTriggerCapValidation`, `fsbTriggerCapCurrentActive`, card copy `Trigger Concurrency`, subtitle `Maximum simultaneous trigger watches`, helper copy beginning `Default 8. Range 1 to 64.`, reset copy `Reset to default (8)`, validation copy `Must be between 1 and 64`, default/min/max/step `8/1/64/1`, defaultSettings `fsbTriggerCap: 8`, cacheElements mappings, input/reset/load/save clamp logic, `chrome.storage.onChanged` handling for `session/fsbTriggerRegistry` and `local/fsbTriggerCap`, `100ms` debounce, and `computeActiveTriggerCount` behavior for `armed`, `needs_attention`, `blocked`, `fired`, `timed_out`, `stopped`, and malformed records. Do not add a browser-driven UI harness.</action>
  <acceptance_criteria>
    - The test file exists and uses Node's built-in `assert`.
    - The test names mention the locked behavior they protect: card shape, storage key, clamp range, active statuses, and debounce.
    - The file does not require browser automation or a DOM test framework.
  </acceptance_criteria>
  <verify>
    <automated>node --check tests/trigger-cap-settings-ui.test.js</automated>
  </verify>
  <done>`tests/trigger-cap-settings-ui.test.js` is syntactically valid and contains assertions for every D-01..D-05 UI/counter contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Trigger Concurrency card, persistence, and active counter</name>
  <files>extension/ui/control_panel.html, extension/ui/options.js, extension/ui/cap-counter-helpers.js</files>
  <read_first>
    - extension/ui/control_panel.html
    - extension/ui/options.js
    - extension/ui/cap-counter-helpers.js
    - .planning/phases/20-integration-cap-ui-docs-edge-cases/20-UI-SPEC.md
    - .planning/phases/20-integration-cap-ui-docs-edge-cases/20-PATTERNS.md
  </read_first>
  <action>Implement the approved UI per D-01..D-04. In `control_panel.html`, clone the existing Agent Concurrency `settings-card` immediately after it inside `advanced-settings-grid`; use icon class `fas fa-bolt`, title `Trigger Concurrency`, subtitle `Maximum simultaneous trigger watches`, label `Concurrency Cap`, input ID `fsbTriggerCap`, display ID `fsbTriggerCapDisplay`, reset ID `fsbTriggerCapReset`, validation ID `fsbTriggerCapValidation`, counter ID `fsbTriggerCapCurrentActive`, default value `8`, min `1`, max `64`, step `1`, reset text `Reset to default (8)`, helper text exactly from the UI-SPEC, validation text `Must be between 1 and 64`, and initial counter text `0 of 8 active`. In `cap-counter-helpers.js`, add `computeActiveTriggerCount(envelope)` in the same dual CommonJS/global export style; it must return `0` for non-object envelopes and count only record objects whose `status` is `armed`, `needs_attention`, or `blocked`. In `options.js`, add `fsbTriggerCap: 8` to `defaultSettings`; cache all five new elements; add input handling that shows validation with `isCapInputInvalid(rawValue)`, clamps immediately to integer `1..64`, updates `fsbTriggerCapDisplay`, calls `refreshActiveTriggerCount()`, and marks unsaved changes; add reset handling that sets input/display to `8`, hides validation, refreshes the counter, and marks unsaved changes; clamp again during load and save; subscribe to `chrome.storage.onChanged` for `session/fsbTriggerRegistry` and `local/fsbTriggerCap`; debounce `scheduleRefreshActiveTriggerCount()` at `100ms`; and make `refreshActiveTriggerCount()` read `chrome.storage.session.get('fsbTriggerRegistry')` inside try/catch without throwing or toasting on errors.</action>
  <acceptance_criteria>
    - The Trigger Concurrency card appears adjacent to Agent Concurrency and is not `full-width`.
    - `fsbTriggerCap` persists through `chrome.storage.local` and is clamped on input, load, and save.
    - `fsbTriggerCapCurrentActive` displays `N of M active` and includes attention states without counting terminal snapshots.
    - Counter storage failures are swallowed because the counter is informational.
  </acceptance_criteria>
  <verify>
    <automated>node tests/trigger-cap-settings-ui.test.js</automated>
  </verify>
  <done>The focused Trigger Concurrency UI test passes and the UI matches the approved UI-SPEC exactly.</done>
</task>

<task type="auto">
  <name>Task 3: Run cap and settings regressions</name>
  <files>extension/ui/control_panel.html, extension/ui/options.js, extension/ui/cap-counter-helpers.js, tests/trigger-cap-settings-ui.test.js</files>
  <read_first>
    - tests/trigger-cap.test.js
    - tests/cap-counter-live.test.js
    - tests/agent-cap-ui.test.js
  </read_first>
  <action>Run the focused regression set for cap behavior and UI source-shape. If a regression reveals that the new trigger helper names collide with the existing agent helper globals, keep the existing `computeActiveAgentCount`, `formatCounterText`, and `isCapInputInvalid` behavior unchanged and add trigger-specific names only where needed (`computeActiveTriggerCount`, `refreshActiveTriggerCount`, `scheduleRefreshActiveTriggerCount`). Do not alter runtime cap enforcement in `extension/utils/trigger-manager.js`; this plan only exposes the already shipped `fsbTriggerCap` setting.</action>
  <acceptance_criteria>
    - Existing Agent Concurrency tests still pass.
    - Existing trigger cap runtime tests still pass.
    - The new trigger UI test passes in the same source-shape style as existing settings tests.
  </acceptance_criteria>
  <verify>
    <automated>node tests/trigger-cap-settings-ui.test.js && node tests/trigger-cap.test.js && node tests/cap-counter-live.test.js && node tests/agent-cap-ui.test.js</automated>
  </verify>
  <done>Trigger cap UI coverage is green without regressing existing agent cap or runtime trigger cap tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| options page -> chrome.storage.local | User-controlled settings input writes `fsbTriggerCap`. |
| chrome.storage.session -> options page | Session registry records may be absent, stale, malformed, or tampered with by extension code paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01-01 | Tampering | `extension/ui/options.js` cap persistence | mitigate | Clamp `fsbTriggerCap` to integer `1..64` on input, load, and save; default to `8` for non-finite values. |
| T-20-01-02 | Denial of Service | `refreshActiveTriggerCount()` | mitigate | Wrap session storage reads in try/catch and return silently on `chrome.runtime.lastError`; never throw into the options page. |
| T-20-01-03 | Information Disclosure | `fsbTriggerCapCurrentActive` | accept | The counter shows only aggregate active count, not trigger IDs, selectors, URLs, owners, or values. |
</threat_model>

<verification>
Run:

```bash
node tests/trigger-cap-settings-ui.test.js
node tests/trigger-cap.test.js
node tests/cap-counter-live.test.js
node tests/agent-cap-ui.test.js
```
</verification>

<success_criteria>
- D-01 through D-05 are implemented exactly.
- The card copy, IDs, range, debounce, and active status semantics match `20-UI-SPEC.md`.
- No source file outside the declared UI/helper/test files is modified.
</success_criteria>

<output>
After completion, create `.planning/phases/20-integration-cap-ui-docs-edge-cases/20-01-SUMMARY.md`.
</output>
