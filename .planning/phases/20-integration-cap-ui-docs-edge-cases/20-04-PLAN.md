---
phase: 20-integration-cap-ui-docs-edge-cases
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - mcp/package.json
  - mcp/package-lock.json
  - mcp/src/version.ts
  - mcp/build/version.js
  - mcp/server.json
  - tests/mcp-version-parity.test.js
  - mcp/CHANGELOG.md
  - mcp/README.md
  - README.md
autonomous: true
requirements: ["Integration/composition"]
user_setup: []
must_haves:
  truths:
    - "MCP package metadata, runtime version, build output, server metadata, lockfile metadata, and version parity test agree on 0.10.0."
    - "MCP docs document trigger, stop_trigger, get_trigger_status, and list_triggers with blocking/detached guidance and local browser-open notify-only limits."
    - "Existing tool schemas remain additive and shared-registry driven; schema/parity/smoke gates pass."
  artifacts:
    - path: "mcp/package.json"
      provides: "MCP package version"
      contains: "\"version\": \"0.10.0\""
    - path: "mcp/src/version.ts"
      provides: "Runtime MCP version constant"
      contains: "FSB_MCP_VERSION = '0.10.0'"
    - path: "mcp/server.json"
      provides: "MCP server registry metadata"
      contains: "\"version\": \"0.10.0\""
    - path: "mcp/CHANGELOG.md"
      provides: "0.10.0 release notes"
      contains: "## 0.10.0"
    - path: "mcp/README.md"
      provides: "Public Trigger Watchers documentation"
      contains: "Trigger Watchers"
  key_links:
    - from: "mcp/package.json"
      to: "mcp/src/version.ts"
      via: "tests/mcp-version-parity.test.js canonicalVersion"
      pattern: "canonicalVersion = '0.10.0'"
    - from: "mcp/src/version.ts"
      to: "mcp/build/version.js"
      via: "npm --prefix mcp run build"
      pattern: "FSB_MCP_VERSION"
    - from: "extension/ai/tool-definitions.js"
      to: "mcp/README.md"
      via: "docs describe shared trigger tools without schema edits"
      pattern: "trigger"
---

<objective>
Prepare `fsb-mcp-server@0.10.0` metadata and public docs for the trigger tool family, then prove version/schema parity.

Purpose: The trigger tools are an additive public MCP minor feature; release artifacts must agree on `0.10.0` while dependencies and existing tool schemas remain unchanged.
Output: MCP version metadata, lockfile metadata, build version artifact, CHANGELOG, MCP README, root README, and version parity test updated for D-11 through D-15.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-integration-cap-ui-docs-edge-cases/20-CONTEXT.md
@.planning/phases/20-integration-cap-ui-docs-edge-cases/20-RESEARCH.md
@.planning/phases/20-integration-cap-ui-docs-edge-cases/20-VALIDATION.md
@.planning/phases/20-integration-cap-ui-docs-edge-cases/20-PATTERNS.md
@.planning/phases/19-mcp-tools-blocking-detached-reporting/19-03-SUMMARY.md
@mcp/package.json
@mcp/package-lock.json
@mcp/src/version.ts
@mcp/server.json
@tests/mcp-version-parity.test.js
@mcp/src/tools/triggers.ts
@extension/ai/tool-definitions.js
@mcp/ai/tool-definitions.cjs
@mcp/CHANGELOG.md
@mcp/README.md
@README.md

<interfaces>
MCP trigger tool names from `mcp/src/tools/triggers.ts`:
```typescript
trigger
stop_trigger
get_trigger_status
list_triggers
```

Phase 19 shipped semantics that docs must describe, not redefine:
```text
blocking default
detached:true
30s heartbeat/progress
120s default timeout
240s safety auto-detach
fired and timed_out outcomes
rearm_on_fire with hysteresis
owner cleanup and TTL cleanup
```

Target version:
```text
0.10.0
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update MCP version metadata and parity target to 0.10.0</name>
  <files>mcp/package.json, mcp/package-lock.json, mcp/src/version.ts, mcp/build/version.js, mcp/server.json, tests/mcp-version-parity.test.js</files>
  <read_first>
    - mcp/package.json
    - mcp/package-lock.json
    - mcp/src/version.ts
    - mcp/server.json
    - tests/mcp-version-parity.test.js
    - .planning/phases/20-integration-cap-ui-docs-edge-cases/20-RESEARCH.md
  </read_first>
  <action>Implement D-11. Set `mcp/package.json` version to `0.10.0`. Set `mcp/src/version.ts` `FSB_MCP_VERSION` to `'0.10.0'`. Set `mcp/server.json` top-level `version` and `packages[0].version` to `0.10.0`. Set `tests/mcp-version-parity.test.js` `canonicalVersion` to `'0.10.0'`. Update `mcp/package-lock.json` package metadata to `0.10.0` without changing dependency versions, dependency ranges, resolved package versions, integrity fields, or the dependency block. Run `npm --prefix mcp run build` so `mcp/build/version.js` reports `0.10.0`; if the build copies `extension/ai/tool-definitions.js` into `mcp/ai/tool-definitions.cjs` with no content change, do not include unrelated churn. Do not change any MCP dependency versions.</action>
  <acceptance_criteria>
    - All MCP version metadata reports `0.10.0`.
    - `mcp/package-lock.json` has only version metadata changes for the root package if touched.
    - `npm --prefix mcp run build` regenerates `mcp/build/version.js` to `0.10.0`.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix mcp run build && node tests/mcp-version-parity.test.js</automated>
  </verify>
  <done>Version parity passes with canonical MCP version `0.10.0` and no dependency upgrades.</done>
</task>

<task type="auto">
  <name>Task 2: Document Trigger Watchers in CHANGELOG and READMEs</name>
  <files>mcp/CHANGELOG.md, mcp/README.md, README.md, tests/mcp-version-parity.test.js</files>
  <read_first>
    - mcp/CHANGELOG.md
    - mcp/README.md
    - README.md
    - mcp/src/tools/triggers.ts
    - extension/ai/tool-definitions.js
    - .planning/phases/19-mcp-tools-blocking-detached-reporting/19-03-SUMMARY.md
  </read_first>
  <action>Implement D-12 through D-15. Add a `0.10.0` entry to `mcp/CHANGELOG.md` for the trigger family: `trigger`, `stop_trigger`, `get_trigger_status`, `list_triggers`, blocking default with 30s progress, detached mode, safety auto-detach, `fired` and `timed_out` outcomes, `rearm_on_fire`, hysteresis, owner/TTL cleanup, trigger cap UI, cross-watch conflict, refresh-poll coalescing, and anti-scope. In `mcp/README.md`, update public tool counts if the file has a count, then add a clear `Trigger Watchers` section explaining: one-element watching, when to choose `live-observe` versus `refresh-poll`, blocking versus detached behavior, `rearm_on_fire` and hysteresis, notify-only output where the caller/AI decides follow-up, browser-must-be-open/session-only limits, restricted-tab/page-blocked behavior, `TRIGGER_TAB_WATCH_CONFLICT`, coalesced same-tab refresh-poll reloads, cancellation with `stop_trigger`, status with `get_trigger_status`, listing with `list_triggers`, and the cap setting `fsbTriggerCap` default `8` range `1..64`. In root `README.md`, add only concise overview text that helps a new user discover Trigger Watchers and the local/browser-open limitation; keep detailed tool semantics in `mcp/README.md`. Do not promise desktop/browser push notifications, email/SMS/Slack delivery, server-side monitoring, auto-act workflows, cross-browser-restart auto-resume, screenshot diffing, compound multi-element conditions, or any publish/tag action.</action>
  <acceptance_criteria>
    - `mcp/CHANGELOG.md` has a `0.10.0` entry with trigger tools, reporting semantics, lifecycle cleanup, UI, and anti-scope.
    - `mcp/README.md` has a Trigger Watchers section covering choice of watch mode, blocking/detached use, limitations, and companion tools.
    - Root `README.md` stays concise and points readers to MCP docs for details.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix mcp run build && node tests/mcp-version-parity.test.js</automated>
  </verify>
  <done>Public docs and version parity agree on the `0.10.0` trigger surface without overclaiming excluded features.</done>
</task>

<task type="auto">
  <name>Task 3: Run MCP smoke, parity, schema, and tool gates</name>
  <files>mcp/package.json, mcp/package-lock.json, mcp/src/version.ts, mcp/build/version.js, mcp/server.json, tests/mcp-version-parity.test.js, mcp/CHANGELOG.md, mcp/README.md, README.md</files>
  <read_first>
    - tests/mcp-tool-smoke.test.js
    - tests/tool-definitions-parity.test.js
    - tests/visual-session-schema-lock.test.js
    - package.json
    - mcp/package.json
  </read_first>
  <action>Run the MCP release-readiness gates for D-15. Fix only docs/version/package metadata issues in this plan. If a gate indicates a shared tool schema drift, inspect the diff; the expected target is no schema change beyond already shipped additive trigger tools from Phase 18/19. Do not edit `extension/ai/tool-definitions.js` or `mcp/ai/tool-definitions.cjs` for docs-only wording. Do not run `npm publish`, push git tags, or publish ClawHub artifacts.</action>
  <acceptance_criteria>
    - MCP version parity passes after build.
    - MCP smoke, tool-definition parity, visual-session schema lock, and MCP smoke tools pass.
    - No dependency version changes are present in `mcp/package.json` or `mcp/package-lock.json`.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix mcp run build && node tests/mcp-version-parity.test.js && node tests/mcp-tool-smoke.test.js && node tests/tool-definitions-parity.test.js && node tests/visual-session-schema-lock.test.js && npm run test:mcp-smoke:tools</automated>
  </verify>
  <done>MCP metadata/docs are release-ready at `0.10.0`, and parity/smoke/schema gates are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/version metadata -> public package consumers | Public documentation and package metadata drive user expectations and install behavior. |
| shared tool definitions -> MCP docs | Docs describe behavior shipped through shared schemas and runtime code. |
| build output -> version parity tests | Generated MCP build output must match source metadata. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04-01 | Repudiation | MCP version metadata | mitigate | Update package, lockfile metadata, runtime constant, server metadata, build output, and parity test together to `0.10.0`. |
| T-20-04-02 | Tampering | `mcp/package-lock.json` | mitigate | Change only root package version metadata; do not alter dependency versions or integrity fields. |
| T-20-04-03 | Spoofing | public docs | mitigate | Document notify-only and browser-open/session-only limits; do not imply server monitoring, push delivery, or auto-act behavior. |
| T-20-04-04 | Tampering | MCP tool schemas | mitigate | Run smoke/parity/schema-lock gates and keep docs aligned with shared registry behavior. |
</threat_model>

<verification>
Run:

```bash
npm --prefix mcp run build && node tests/mcp-version-parity.test.js
node tests/mcp-tool-smoke.test.js && node tests/tool-definitions-parity.test.js && node tests/visual-session-schema-lock.test.js
npm run test:mcp-smoke:tools
```
</verification>

<success_criteria>
- D-11 through D-15 are implemented exactly.
- `fsb-mcp-server` is prepared at `0.10.0`; dependencies are unchanged.
- Docs describe trigger behavior honestly and exclude out-of-scope push, auto-act, server-side monitoring, screenshot diffing, multi-element compounds, cross-browser-restart auto-resume, and publish/tag actions.
</success_criteria>

<output>
After completion, create `.planning/phases/20-integration-cap-ui-docs-edge-cases/20-04-SUMMARY.md`.
</output>
