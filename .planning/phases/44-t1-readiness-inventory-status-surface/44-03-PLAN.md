---
phase: 44-t1-readiness-inventory-status-surface
plan: 03
type: execute
wave: 2
depends_on: [44-01, 44-02]
files_modified:
  - scripts/verify-t1-readiness-gate.mjs
  - tests/t1-readiness-gate.test.js
  - package.json
  - .planning/phases/44-t1-readiness-inventory-status-surface/44-SUMMARY.md
autonomous: true
requirements: [T1R-03]

must_haves:
  truths:
    - "validate:extension fails if a descriptor is marked T1-ready without handler/recipe proof"
    - "The gate uses the same classifier/report logic as 44-01 instead of a second divergent implementation"
    - "Negative controls prove fake T1-ready and fake handler-backed descriptors fail"
    - "The final phase summary records before/after counts and next Phase 45 inputs"
  artifacts:
    - path: "scripts/verify-t1-readiness-gate.mjs"
      provides: "CI guard for T1 readiness claims"
      min_lines: 80
    - path: "tests/t1-readiness-gate.test.js"
      provides: "Positive and negative control coverage for the guard"
      min_lines: 80
    - path: ".planning/phases/44-t1-readiness-inventory-status-surface/44-SUMMARY.md"
      provides: "Phase completion summary and handoff to Phase 45"
      contains: "Phase 44"
  key_links:
    - from: "package.json"
      to: "scripts/verify-t1-readiness-gate.mjs"
      via: "validate:extension chain"
      pattern: "verify-t1-readiness-gate"
---

<objective>
Convert Phase 44's readiness definition into a CI rule so future catalog work cannot overclaim direct API readiness.
</objective>

<execution_context>
@.planning/phases/44-t1-readiness-inventory-status-surface/44-CONTEXT.md
@.planning/phases/44-t1-readiness-inventory-status-surface/44-T1-READINESS.md
@scripts/report-t1-readiness.mjs
@package.json
@scripts/verify-catalog-crosscheck.mjs
@scripts/verify-origin-classification.mjs
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Add the readiness CI gate</name>
  <files>scripts/verify-t1-readiness-gate.mjs</files>
  <action>
Create a Node-builtins-only dual-export verification script.

The gate should import or reuse the report classifier from scripts/report-t1-readiness.mjs and fail when:
- a descriptor/status claims t1-ready but does not resolve to T0/T1a/T1b with handler/recipe proof;
- a descriptor has backing handler/recipe but resolve() does not reach the matching executable tier;
- a fail-closed guarded write is classified as executable ready;
- the generated report omits descriptors or has duplicate slugs;
- the status labels fall outside the allowed enum.

The CLI should print PASS with row counts on success and print actionable failures on error.
  </action>
  <verify>
    <automated>node scripts/verify-t1-readiness-gate.mjs</automated>
  </verify>
  <done>The gate enforces Phase 44 readiness semantics over the committed catalog.</done>
</task>

<task type="auto">
  <name>Task 2: Add negative-control tests for the gate</name>
  <files>tests/t1-readiness-gate.test.js</files>
  <action>
Create a zero-framework Node test that imports the gate helpers and drives positive plus negative controls.

Required controls:
- current committed catalog passes;
- a synthetic descriptor marked t1-ready without handler/recipe proof fails;
- a synthetic handler-backed descriptor with no matching registered handler fails;
- a guarded write mislabeled t1-ready fails;
- duplicate slug rows fail.
  </action>
  <verify>
    <automated>node tests/t1-readiness-gate.test.js</automated>
  </verify>
  <done>The CI guard is non-vacuous and fails on the exact overclaim class Phase 44 exists to prevent.</done>
</task>

<task type="auto">
  <name>Task 3: Wire validate:extension and close Phase 44</name>
  <files>package.json, .planning/phases/44-t1-readiness-inventory-status-surface/44-SUMMARY.md</files>
  <action>
Add the new gate to `npm run validate:extension` after the existing origin/no-orphan gates.

Write the Phase 44 summary with:
- final readiness totals;
- exact tests run;
- any known caveats;
- handoff requirements for Phase 45.

Do not mark Phase 44 complete unless the verification commands pass.
  </action>
  <verify>
    <automated>node tests/t1-readiness-report.test.js</automated>
    <automated>node tests/t1-readiness-gate.test.js</automated>
    <automated>node scripts/verify-t1-readiness-gate.mjs</automated>
    <automated>npm run validate:extension</automated>
  </verify>
  <done>Phase 44 has an enforced CI guard and a summary suitable for Phase 45 planning.</done>
</task>

</tasks>

<verification>
Run:
- `node tests/t1-readiness-report.test.js`
- `node tests/t1-readiness-gate.test.js`
- `node scripts/verify-t1-readiness-gate.mjs`
- `npm run validate:extension`
- `git diff --check scripts/verify-t1-readiness-gate.mjs tests/t1-readiness-gate.test.js package.json .planning/phases/44-t1-readiness-inventory-status-surface/44-SUMMARY.md`
</verification>
