---
phase: 44-t1-readiness-inventory-status-surface
plan: 02
type: execute
wave: 1
depends_on: [44-01]
files_modified:
  - extension/utils/capability-search.js
  - tests/backing-status-annotation.test.js
  - tests/breadth-search-return.test.js
  - README.md
  - mcp/README.md
autonomous: true
requirements: [T1R-02]

must_haves:
  truths:
    - "Search/API result wording distinguishes t1-ready, t1-guarded-fail-closed, learn-pending, and discovery-pending"
    - "Catalog support is not described as all-app direct API execution"
    - "Sensitive origins are documented as flagged/audited for invoke, with extra confirmation scoped to network-capture discovery"
    - "No public MCP tool schema, storage key, payload version, or consent runtime behavior changes"
  artifacts:
    - path: "tests/backing-status-annotation.test.js"
      provides: "Search-result status labels cannot regress to overclaiming DOM/learn descriptors"
      contains: "discovery-pending"
    - path: "tests/breadth-search-return.test.js"
      provides: "Breadth search still returns catalog tail results without claiming direct execution"
      contains: "backing"
  key_links:
    - from: "extension/utils/capability-search.js"
      to: "tests/backing-status-annotation.test.js"
      via: "status fields returned on search hits"
      pattern: "backingStatus|invocable"
---

<objective>
Update the status surface so users and agents see the same truth the readiness matrix generated: T1-ready is a proof state, DOM/catalog support is discovery-pending, T2 is learn-pending, and guarded writes are fail-closed until UAT.
</objective>

<execution_context>
@.planning/phases/44-t1-readiness-inventory-status-surface/44-CONTEXT.md
@.planning/phases/44-t1-readiness-inventory-status-surface/44-T1-READINESS.md
@extension/utils/capability-search.js
@tests/backing-status-annotation.test.js
@tests/breadth-search-return.test.js
@README.md
@mcp/README.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Audit search hit fields and wording</name>
  <files>extension/utils/capability-search.js, tests/backing-status-annotation.test.js</files>
  <action>
Inspect the current search result shape. If it already has enough fields, preserve wire compatibility and only tighten labels/comments/tests. If an additive field is needed, keep it additive and backwards-compatible.

Required display semantics:
- handler/recipe executable rows: `invocable:true`, status label direct/executable.
- fail-closed guarded writes: visible as guarded/fail-closed if the readiness report can identify them; do not call them executable.
- learn rows: learn-pending.
- dom/absent rows: discovery-pending.

Avoid storage or MCP schema changes. Do not remove existing fields.
  </action>
  <verify>
    <automated>node tests/backing-status-annotation.test.js</automated>
  </verify>
  <done>Search hit status labels are precise and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Update breadth/search tests against overclaiming</name>
  <files>tests/breadth-search-return.test.js</files>
  <action>
Add or update assertions so breadth DOM-tail search hits still return for discoverability but are not treated as direct API-ready.

Include at least one known DOM-only app and one known T1-ready app so the test proves the contrast.
  </action>
  <verify>
    <automated>node tests/breadth-search-return.test.js</automated>
  </verify>
  <done>Breadth search remains useful while status labels stay honest.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh README and MCP wording</name>
  <files>README.md, mcp/README.md</files>
  <action>
Update docs to match the Phase 44 status model.

Required language:
- "catalog/search supported" means searchable/routable, not necessarily direct API execution.
- `invoke_capability` supports direct execution for verified T1/T1b capabilities and fail-closed/pending responses for the catalog tail.
- Non-denied origins are allowed under Auto for ordinary invoke.
- Sensitive origins are flagged/audited; network-capture discovery still requires extra confirmation.
- Denylisted origins remain blocked.

Remove any stale phrase that implies all 128 apps are direct API-ready today.
  </action>
  <verify>
    <automated>rg -n "all apps.*direct|128 apps.*direct|default-off|sensitive Auto|per-origin consent" README.md mcp/README.md || true</automated>
    <automated>git diff --check README.md mcp/README.md</automated>
  </verify>
  <done>Docs no longer overclaim the current T1 surface.</done>
</task>

</tasks>

<verification>
Run:
- `node tests/backing-status-annotation.test.js`
- `node tests/breadth-search-return.test.js`
- `rg -n "all apps.*direct|128 apps.*direct|default-off|sensitive Auto|per-origin consent" README.md mcp/README.md || true`
- `git diff --check extension/utils/capability-search.js tests/backing-status-annotation.test.js tests/breadth-search-return.test.js README.md mcp/README.md`
</verification>
