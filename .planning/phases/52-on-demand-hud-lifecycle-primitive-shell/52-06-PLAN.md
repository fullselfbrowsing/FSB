---
phase: 52-on-demand-hud-lifecycle-primitive-shell
plan: 06
type: execute
wave: 5
depends_on:
  - 01
  - 02
  - 03
  - 04
  - 05
files_modified:
  - .planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-UAT.md
autonomous: false
requirements:
  - HUD-01
  - HUD-02
  - HUD-03
  - HUD-04
  - HUD-05
  - HUD-07
  - HUD-08
user_setup: []

must_haves:
  truths:
    - "All Phase 52 focused suites, adjacent overlay/side-panel regressions, extension validation, and the complete default test chain pass before live review begins."
    - "In real Chrome, explicit command/side-panel invocation, cancellation, kill, navigation, service-worker sleep, and reinvocation leave no stale or cross-tab shell."
    - "Drive and Docs remain usable through overlay geometry, collision fallback, zoom, preferences, and every controlled attention state."
    - "Keyboard and VoiceOver expose the exact roles/names/focus order, and final teardown returns the eleven-category resource ledger—including popover top-layer ownership—and host snapshots to baseline."
  artifacts:
    - path: ".planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-UAT.md"
      provides: "Timestamped automated results and completed live-Chrome evidence matrix for all requirements, decisions, and threats"
      contains: "T-52-01"
      min_lines: 100
  key_links:
    - from: ".planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-UAT.md"
      to: "tests/skopeo-session-lifecycle.test.js, tests/skopeo-shell-contract.test.js, tests/skopeo-sidepanel-command.test.js, tests/skopeo-accessibility.test.js"
      via: "recorded commands and zero-failure outputs"
      pattern: 'skopeo-session-lifecycle'
    - from: ".planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-UAT.md"
      to: "live unpacked Chrome Drive/Docs sessions"
      via: "observer-recorded screenshots, focus/interaction checks, and before/after resource snapshots"
      pattern: 'Drive|Docs|VoiceOver'
---

<objective>
Close Phase 52 only after the complete automated regression surface and a live Chrome accessibility/coexistence/lifecycle review prove explicit invocation, shared HUD grammar, and zero host residue.

Purpose: Validate browser-only behavior that Node mocks cannot establish, including command assignment, Chrome top-layer hit testing, Drive/Docs interaction, assistive technology, zoom/preferences, and MV3 suspension.

Output: A completed evidence ledger and a blocking human approval checkpoint.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-CONTEXT.md
@.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-RESEARCH.md
@.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-VALIDATION.md
@.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-UI-SPEC.md
@.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-01-SUMMARY.md
@.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-02-SUMMARY.md
@.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-03-SUMMARY.md
@.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-04-SUMMARY.md
@.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run focused, adjacent, extension, and full regressions and record evidence</name>
  <read_first>
    - .planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-UAT.md (new modified file; create the evidence sections below if absent, preserve prior failure notes on rerun)
    - .planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-VALIDATION.md (exact quick, integration, full-regression, and manual-only matrices)
    - .planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-UI-SPEC.md (all observable acceptance checkpoints and exact copy)
    - .planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-CONTEXT.md (D-01 through D-14 acceptance traceability)
    - tests/helpers/skopeo-resource-ledger.js (non-vacuous negative control and zero-baseline report)
    - tests/skopeo-session-lifecycle.test.js (T-52-01 generation/late-work suite)
    - tests/skopeo-shell-contract.test.js (T-52-02/T-52-03 host/pointer/text suite)
    - tests/skopeo-sidepanel-command.test.js (command/tab/restricted-page suite)
    - tests/skopeo-accessibility.test.js (roles/focus/preferences suite)
    - package.json (registered full `npm test` chain)
  </read_first>
  <files>.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-UAT.md</files>
  <action>
    Create an evidence document with metadata (date, commit, and a Chrome-build field completed during Task 2), an automated command table, requirement coverage HUD-01/02/03/04/05/07/08, decision coverage D-01 through D-14, threat coverage T-52-01 through T-52-07, before/active/after snapshots for all eleven resource-ledger categories (roots, listeners, observers, timeouts, intervals, animationFrames, animations, focusHooks, pointerSurfaces, pendingRenders, and `popoverTopLayer`), and the live matrix used by Task 2. Record the ledger's listener and `popoverTopLayer` leak negative controls plus the supported-popover 0 -> 1 -> 0 open/hide assertion. Run, in order: the ledger self-test; the exact six-command quick suite from `52-VALIDATION.md`; `node tests/overlay-stability-cadence.test.js`; `node tests/sidepanel-tab-aware-smoke.test.js`; `node tests/extension-content-script-files-completeness.test.js`; `npm run validate:extension`; and full `npm test`. Use no watch mode. Record exact command, exit status, pass/fail counts where emitted, and a concise failure excerpt; never mark an unrun or failed command PASS. If any command fails, stop before the human checkpoint and report the exact gate for correction.

    Explicitly record blocking status for T-52-01 stale resurrection, T-52-02 host interception/obscuration, and T-52-03 executable host text. The document may say READY FOR LIVE UAT only when all three and the full regression are green. This automated gate covers all requirements and D-01 through D-14 at contract level; Task 2 supplies browser evidence.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const p='.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-UAT.md'; const s=fs.readFileSync(p,'utf8'); for (const x of ['READY FOR LIVE UAT','npm run validate:extension','npm test','T-52-01','T-52-02','T-52-03','HUD-08','D-14','popoverTopLayer','0 -> 1 -> 0']) if (!s.includes(x)) throw new Error('missing evidence '+x); if (/UNRUN|FAILED/.test(s)) throw new Error('automated gate is not green');"</automated>
  </verify>
  <acceptance_criteria>
    - Every focused, adjacent, validation, and full-regression command is recorded with a real zero exit status.
    - The non-vacuous listener and `popoverTopLayer` leak controls, the popover open/hide 0 -> 1 -> 0 assertion, and all three blocking threats are explicitly green.
    - No manual row is pre-approved; the live matrix remains clearly pending for Task 2.
  </acceptance_criteria>
  <done>Complete automated evidence is green and the document is honestly marked ready for the blocking live Chrome review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify explicit lifecycle, Drive/Docs coexistence, primitives, accessibility, and zero residue in live Chrome</name>
  <files>.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-UAT.md</files>
  <action>Perform the live checks below only after Task 1 is green. Record Chrome version, OS, tested URLs/tabs, assigned/remapped shortcut, screenshots or observation notes, resource snapshots, and PASS/FAIL for every row in `52-UAT.md`. A failure in any blocking threat, required host interaction, accessibility path, or residue check stops approval and must be reported with reproduction steps.</action>
  <verify><human-check>All live rows below are completed and approved with no unresolved failure, especially T-52-01/T-52-02/T-52-03 and the eleven-category zero-resource teardown ledger including `popoverTopLayer`.</human-check></verify>
  <done>Live unpacked Chrome proves all seven HUD requirements and D-01 through D-14; `52-UAT.md` contains no pending/failed row and records final approval.</done>
  <what-built>
    A dedicated side-panel switch and configurable current-tab Chrome command dynamically prepare with zero roots, make the generation authoritative ACTIVE, then commit one isolated Ambient Skopeo shell. One generation-aware runtime owns the exact six primitives and four attention policies, exposes a read-only generation probe for MV3 rehydration, and uses abort-first terminal teardown with accessible focus behavior, collision-safe pointer isolation, and an eleven-category resource ledger covering roots/listeners/observers/timers/animations/focus/pointers/pending renders/`popoverTopLayer`. The normal path exposes only Ambient; the richer grammar is available only through the controlled non-production fixture hook.
  </what-built>
  <how-to-verify>
    1. Load `extension/` unpacked in a clean Chrome profile. Confirm the extension loads with no manifest/service-worker error and the toolbar still opens the existing FSB side panel.
    2. Open ordinary Web, Drive, and Docs tabs before invoking Skopeo. Confirm no Skopeo root, launcher, listener effect, rail, style, accessibility node, or automatic activation appears (D-01/HUD-01).
    3. Inspect `chrome://extensions/shortcuts`: confirm `Toggle Skopeo in current tab` is present, observe assignment/collision honestly, invoke its assigned key, remap it, and invoke again. Confirm the side-panel hint follows the actual key. The command and switch must target the selected tab only; Tab A state must never appear on Tab B.
    4. Invoke from the side-panel switch. Confirm exact Off -> Starting -> `On · Ambient` copy, focus stays on the switch, prepare/STARTING has zero page roots and zero `popoverTopLayer`, cancellation before prepared or between prepared and ready leaves no host, and only the matching post-ACTIVE commit creates exactly one ambient lens/rail. Repeated same-generation prepare/commit must not duplicate or advance the shell; toggle-off is immediate.
    5. Test unsupported Chrome/internal/store pages and force one injection failure. Confirm exact Unsupported/Error copy, no page flash, unchecked/non-active status, and zero partial session/root.
    6. On Drive, exercise row selection/open, menus, scrollbar, and native controls through empty envelope/rail geometry. On Docs, edit/select text, use native Escape, menus, and scrolling. Confirm no layout shift, no host style/attribute/inert/aria-hidden/scroll diff, and no unrelated FSB automation change (blocking T-52-02/HUD-04).
    7. Force each ambient corner to collide with visible controls. Confirm deterministic alternate corner, then 88x40 compact lens, then exact unsafe-layout fail-closed copy; Skopeo never covers the focused/required host control.
    8. In the extension service-worker console, use `chrome.scripting.executeScript` in the extension isolated world to set the runtime's documented test flag and call `window.__FSB_SKOPEO_RUNTIME__.activateControlledFixtureForTest()`. Confirm this hook is absent from ordinary UI/messages. Walk Ambient -> Anchored -> Focused -> Interstitial and back: exactly six primitive types, legal attention combinations, at most one labelled anomaly halo, temporary pointer-transparent ghost, and one consequence gate with exact demo copy/safe action first.
    9. Keyboard-test every visible control. One Escape backs one level; two non-repeated Escapes within 600ms kill. Repeated/composing Escape and unrelated host keys pass through. Ambient/Anchored do not steal focus; Focused/Gate follow the declared order; restoration uses the true origin without scrolling or detached/body fallback.
    10. With VoiceOver, verify exact switch/region/rail/demo/gate names and roles, one meaningful polite live region, alertdialog behavior only at the gate, and no hidden primitive after back/kill. Feed the hostile fixture string and confirm it is displayed literally with no created image/script/event execution (blocking T-52-03).
    11. Repeat at 200% zoom and below 480 CSS px, with reduced motion, forced colors, and increased contrast. Confirm no horizontal scroll from Skopeo, controls remain visible, gate actions reflow safe-first, focus is visible, all motion becomes static/0ms, and meaning survives without glow/shadow/color alone.
    12. Start the documented delayed controlled fixture operation, turn Skopeo off before it resolves, allow it to resolve, and confirm no root or primitive returns. Repeat with Escape Escape, navigation/reload, service-worker sleep/wake, runtime reinjection/replacement, and later explicit reinvocation. During sleep/wake, confirm the exact generation probe is observational only: it neither remounts nor changes focus/attention; a missing/stale runtime normalizes Off, while a matching active probe preserves the existing one shell. The new explicit generation must dynamically reinject and start visually fresh in Ambient (blocking T-52-01/HUD-02).
    13. Run Skopeo simultaneously in two tabs, kill one, and confirm the other plus unrelated FSB automation remains unchanged. Navigate and close tabs and confirm no silent reinvocation.
    14. Capture the shell's before/active/after eleven-category resource snapshots. On a browser with popover support, require `popoverTopLayer` to read 0 before mount, 1 only while the popover host is open, and 0 after `hidePopover()` and host removal; verify the fallback remains 0. After close, kill, navigation, failed start, and a second idempotent teardown, require zero roots, listeners, observers, timeouts, intervals, animation frames, animations, focus hooks, pointer surfaces, pending renders, and `popoverTopLayer`; confirm no page visual/toast/badge remains (HUD-03/D-14).
    15. Fill every `52-UAT.md` row with evidence and final PASS/FAIL. Approve only when all rows are PASS and no blocking/high threat remains.
  </how-to-verify>
  <resume-signal>Type "approved" after every `52-UAT.md` live row is PASS, or describe the exact failed row, tab/URL, generation, observed residue or interaction, and reproduction steps.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Node/mock evidence to real Chrome | Mocks cannot prove browser hit testing, top-layer behavior, command assignment, AT output, or MV3 eviction. |
| Skopeo viewport UI to live Drive/Docs | Complex host controls and virtualized/editing surfaces must remain operable and unchanged. |
| Controlled fixture to ordinary product path | Rich grammar verification must not become an ordinary production advancement control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-01 | Tampering | delayed work across kill/reinvoke/SW sleep | mitigate (blocking) | Automated deferred-work tests plus live delayed fixture, navigation, runtime replacement, sleep/wake, and fresh-generation observations. |
| T-52-02 | Denial of service | live overlay hit testing/collision/top-layer | mitigate (blocking) | Drive/Docs pointer-through, focused-control collision, layout/attribute diff, zoom, unsafe fallback, and explicit popover open/hide plus `popoverTopLayer` 0 -> 1 -> 0 review. |
| T-52-03 | Elevation of privilege | hostile fixture text | mitigate (blocking) | Automated hostile payload test plus live literal-text/no-node execution confirmation. |
| T-52-04 | Denial of service | keyboard/VoiceOver focus | mitigate | Full ladder, trap boundary, detach fallback, focus restoration, and native host Escape checks. |
| T-52-05 | Denial of service | duplicate shell/resources | mitigate | Repeated invoke/reinjection/second teardown plus before/active/after eleven-category ledger including `popoverTopLayer`. |
| T-52-06 | Denial of service | restricted/failed injection | mitigate | Live restricted pages and forced failure must expose stable error copy and zero partial state. |
| T-52-07 | Spoofing | multi-tab command/status race | mitigate | Assigned/remapped command, out-of-order tab switches, simultaneous sessions, and one-tab kill isolation review. |
</threat_model>

<verification>
Task 1 must make every automated gate green. Task 2 must complete every UI-SPEC live acceptance row and every `52-VALIDATION.md` manual-only row in `52-UAT.md`; T-52-01/T-52-02/T-52-03 are blocking.
</verification>

<success_criteria>
- Automated focused, adjacent, extension-validation, and full regression gates all pass.
- Real Chrome proves explicit current-tab invocation, honest command assignment, safe cancellation/kill/reinvoke, and tab isolation.
- Drive/Docs, keyboard, VoiceOver, zoom, contrast, forced colors, reduced motion, collision, and controlled grammar checks all pass.
- Every teardown path returns the eleven-category resource ledger, including `popoverTopLayer`, and host snapshot to baseline with no stale resurrection.
</success_criteria>

<output>
After approval, create `.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-06-SUMMARY.md` with automated outputs, Chrome/OS details, UAT evidence, requirement/decision/threat coverage, and final approval.
</output>
