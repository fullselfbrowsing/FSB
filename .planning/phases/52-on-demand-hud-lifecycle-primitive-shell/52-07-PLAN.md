---
phase: 52-on-demand-hud-lifecycle-primitive-shell
plan: 07
type: execute
wave: 6
depends_on:
  - 02
  - 03
  - 06
files_modified:
  - extension/content/skopeo-shell.js
  - tests/skopeo-shell-contract.test.js
  - tests/skopeo-accessibility.test.js
  - tests/skopeo-browser-contract.test.js
autonomous: true
gap_closure: true
requirements:
  - HUD-03
  - HUD-04
  - HUD-05
  - HUD-07
  - HUD-08
user_setup: []

must_haves:
  truths:
    - "Real Chrome computes the one Skopeo host as viewport-fixed, inset-zero, maximum-z, and pointer-transparent in both popover and fallback paths; inline style text is not used as the browser oracle."
    - "Shadow-root focus is resolved through the deep active element, ordinary Gate Tab order remains traversable, boundary wraps occur once, and one-level back/destroy restores the exact eligible origin or no forced focus."
    - "Focused and Interstitial transitions measure their real proposed surface before commit and fail closed with the approved unsafe-view announcement when they would cover the required host control or leave the viewport."
    - "Every attention render owns a releasable resource scope: repeated grammar cycles return to the first per-state eleven-key snapshot and final destroy returns every category to exact zero."
  artifacts:
    - path: "extension/content/skopeo-shell.js"
      provides: "Cascade-safe host geometry, browser-faithful focus handling, atomic richer-state collision checks, and attention-scoped resource ownership"
      contains: "_deepActiveElement"
      min_lines: 1200
    - path: "tests/skopeo-shell-contract.test.js"
      provides: "Mock-DOM negative controls for computed-host CSS source, richer-state collision rollback, and repeated resource plateaus"
      contains: "unsafe view"
      min_lines: 1000
    - path: "tests/skopeo-accessibility.test.js"
      provides: "Shadow-retargeted focus, Gate traversal, verified focus postcondition, and exact restoration regressions"
      contains: "shadowRoot.activeElement"
      min_lines: 460
    - path: "tests/skopeo-browser-contract.test.js"
      provides: "Zero-dependency local/headless Chrome contract for computed CSS, Shadow focus, collision rollback, and top-layer lifecycle"
      contains: "CHROME_BIN"
      min_lines: 180
  key_links:
    - from: "extension/content/skopeo-shell.js"
      to: "tests/skopeo-browser-contract.test.js"
      via: "the test loads the production classic script into a temporary local Chrome fixture and asserts computed/rendered behavior"
      pattern: 'skopeo-shell\.js'
    - from: "extension/content/skopeo-shell.js"
      to: "tests/skopeo-accessibility.test.js"
      via: "the corrected mock exposes document host retargeting plus shadowRoot.activeElement to the production focus helpers"
      pattern: 'activeElement'
    - from: "extension/content/skopeo-shell.js"
      to: "tests/skopeo-shell-contract.test.js"
      via: "attention scopes and staged collision candidates are observed through complete eleven-category snapshots and exact DOM rollback"
      pattern: 'pointerSurfaces|listeners|COPY\.unsafeView'
---

<objective>
Close the browser-facing shell defects found after Phase 52 execution: make host geometry true in the CSS cascade, make focus logic true under Shadow DOM retargeting, make Focused/Gate placement fail closed around required host controls, and stop attention-state handles from accumulating.

Purpose: Restore the host-coexistence, keyboard, and shared-grammar guarantees without adding semantic anchors, host-specific selectors, or another runtime.

Output: Corrected shell behavior, faithful fast tests, and one local-Chrome mechanics gate that proves the browser facts the prior mocks hid.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-CONTEXT.md
@.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-RESEARCH.md
@.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-PATTERNS.md
@.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-UI-SPEC.md
@.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-VALIDATION.md
@.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-VERIFICATION.md
@.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-REVIEW.md
@.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-02-SUMMARY.md
@.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-03-SUMMARY.md
@extension/content/skopeo-shell.js
@tests/skopeo-shell-contract.test.js
@tests/skopeo-accessibility.test.js

<gap_coverage>
This plan is the sole owner of WR-01, WR-02, WR-05, and advisory WR-04. Plan 52-08 exclusively owns WR-06 and WR-03. Live UAT rows L01-L15 remain deferred and are not edited or represented as passed here.
</gap_coverage>

<interfaces>
Keep the existing public `FSBSkopeoShell` API and exact six primitive/four attention enums. Add or refactor only shell-private mechanics around these explicit responsibilities:

```javascript
_deepActiveElement() {}                 // shadowRoot.activeElement || document.activeElement
_safeFocus(node) {}                     // true only when focus postcondition resolves exactly to node
_requiredHostControl() {}               // current eligible non-Skopeo control, else remembered host origin
_createSurfaceScope(attention) {}       // { attention, nodes, handles, disposed }
_suspendSurfaceScope() {}
_restoreSurfaceScope(scope) {}
_disposeSurfaceScope(scope) {}
_stageAttention(level) {}               // build/measure without changing authoritative attention
_candidateIsSafe(candidateRect, hostRect) {}
_commitStagedAttention(staged) {}
```

The cascade-critical host rule is exact in effect:

```css
:host {
  all: initial !important;
  display: block !important;
  position: fixed !important;
  inset: 0 !important;
  z-index: 2147483647 !important;
  pointer-events: none !important;
  margin: 0 !important;
  padding: 0 !important;
  border: 0 !important;
  background: transparent !important;
}
```

The browser test is standalone Node/CommonJS. It resolves Chrome from `CHROME_BIN` first and then documented local executable paths, creates an isolated temporary user-data directory and local HTML fixture, launches only that local Chrome with `--headless=new --dump-dom`, parses a JSON result node, and deletes the temporary directory in `finally`. It must add no package, server, daemon, network call, or browser-automation framework.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin browser-computed host geometry, then repair the cascade</name>
  <read_first>
    - tests/skopeo-browser-contract.test.js (new modified file; create the zero-dependency Chrome runner and WR-01 negative control in this task)
    - tests/skopeo-shell-contract.test.js (modified file; preserve every existing one-owner, top-layer, pointer, host-snapshot, and hostile-text assertion)
    - extension/content/skopeo-shell.js (modified file; inspect `SHELL_CSS`, `mountAmbient`, inline host assignments, and popover fallback before editing)
    - extension/content/visual-feedback.js lines 350-410 (local precedent for cascade-critical `!important` overlay host geometry)
    - .planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-REVIEW.md (WR-01 reproduction and required computed values)
    - .planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-PATTERNS.md (real-Chrome runner and computed-style contract)
    - .planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-UI-SPEC.md (Shadow host/envelope geometry and pointer policy)
  </read_first>
  <files>tests/skopeo-browser-contract.test.js, tests/skopeo-shell-contract.test.js, extension/content/skopeo-shell.js</files>
  <action>
    Start with a failing WR-01 control. In `tests/skopeo-shell-contract.test.js`, mount the real exported shell and require the generated `:host` rule—not `host.style.*`—to contain `position:fixed`, zero inset, maximum z-index, pointer-none, zero margin/padding/border, and transparent background at the same `!important` priority as `all:initial`. Retain the inline-style assertions only as secondary defense.

    Create `tests/skopeo-browser-contract.test.js` using only `node:assert/strict`, `node:child_process`, `node:fs`, `node:os`, `node:path`, and `node:url`. Resolve an executable from `CHROME_BIN`, the macOS Google Chrome/Canary/Chromium paths, and standard Linux Chrome/Chromium paths; when this required test is run and none exists, exit nonzero with the searched paths rather than skipping. Create a temporary local fixture that loads the production `extension/content/skopeo-shell.js`, mounts one supported-popover shell and one forced-fallback/throwing-popover shell, writes a single escaped JSON result into a known DOM node, and is dumped by local headless Chrome under a unique temporary profile. Capture before/open/destroy resource snapshots and `getComputedStyle(host)` for both paths. First run the test against the unfixed source and record that its production assertion detects WR-01.

    Then replace the one-line `:host` reset in `SHELL_CSS` with the exact important rule from this plan's interface. Keep the existing defensive inline assignments but do not rely on them, add another host, change `body`/`html`, or alter popover ownership. Require the browser result to report `position:fixed`, four computed zero inset edges, `pointer-events:none`, and `z-index:2147483647` in supported and fallback modes. Require supported `popoverTopLayer` to be 0 before, 1 only while open, and 0 after destroy; fallback stays 0. Delete the temp fixture/profile even on assertion or Chrome failure.
  </action>
  <verify>
    <automated>node tests/skopeo-shell-contract.test.js &amp;&amp; node tests/skopeo-browser-contract.test.js &amp;&amp; node --check extension/content/skopeo-shell.js &amp;&amp; node --check tests/skopeo-browser-contract.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `tests/skopeo-browser-contract.test.js` exits 0 only after asserting production `getComputedStyle` values for both supported-popover and forced-fallback hosts.
    - The supported top-layer ledger is exactly `0 -> 1 -> 0`; fallback is exactly `0 -> 0 -> 0`.
    - `tests/skopeo-shell-contract.test.js` rejects a source fixture where the important host properties are removed even if inline styles still say fixed/pointer-none.
    - `rg -n "puppeteer|playwright|selenium|webdriver" package.json tests/skopeo-browser-contract.test.js` returns no dependency/import use.
  </acceptance_criteria>
  <done>WR-01 is closed by a real computed-style oracle, and both top-layer and fallback hosts have the same non-intercepting viewport boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Model Shadow focus retargeting and fix Gate traversal/restoration</name>
  <read_first>
    - tests/skopeo-accessibility.test.js (modified file; existing Focused/Gate order, back, detach, disable, and Escape contracts)
    - tests/skopeo-shell-contract.test.js lines 250-390 and 620-650 (modified file; `MockElement.focus`, `MockShadowRoot`, blur/removal, and `dispatchKey` behavior)
    - tests/skopeo-browser-contract.test.js (modified file; extend the Task 1 real-Chrome fixture rather than creating another runner)
    - extension/content/skopeo-shell.js (modified file; `_safeFocus`, `_trapGateTab`, `render`, `back`, `_restoreFocusForDestroy`, and every `document.activeElement` read)
    - .planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-REVIEW.md (WR-02 real-browser reproduction)
    - .planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-PATTERNS.md (deep-active and verified focus-postcondition pattern)
    - .planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-UI-SPEC.md (exact Focused/Gate order and restoration fallbacks)
  </read_first>
  <files>tests/skopeo-shell-contract.test.js, tests/skopeo-accessibility.test.js, tests/skopeo-browser-contract.test.js, extension/content/skopeo-shell.js</files>
  <action>
    Correct the shared mock before the production fix. When a `MockElement` inside `MockShadowRoot` receives focus, set `shadowRoot.activeElement` to the element and `document.activeElement` to `shadowRoot.host`; when focus moves outside, blur/removal occurs, or nodes are permanently disposed, clear the prior root's active element consistently. Make `dispatchKey()` target `shadowRoot.activeElement || document.activeElement`. Run the strengthened accessibility assertions against the unfixed production shell and record that the first Gate action is incorrectly trapped and origins are recorded as the host.

    Add `_deepActiveElement()` to `SkopeoShell`, returning the current shadow child when present and otherwise the document active element. Replace `document.activeElement` reads used by Focused origin capture, Gate origin capture, Gate Tab trapping, owned-focus cleanup, and richer-state focus decisions with this helper. Keep document-level host-control discovery outside the shell from treating the retargeted Skopeo host as a page control. Change `_safeFocus(node)` so a nonthrowing `focus({preventScroll:true})` is not enough: after the call, inspect `node.getRootNode().activeElement` for Shadow/Document roots (falling back to `_deepActiveElement`) and return true only when the exact node owns focus. Failed postconditions must execute the already-declared preceding-surface fallback and must never force `body`.

    Extend `tests/skopeo-accessibility.test.js` to assert the browser-shaped pair for every managed state: `document.activeElement === host` and `shadowRoot.activeElement === expected Skopeo control`. From Gate safe action and middle action, forward Tab is not prevented; from the final action forward Tab wraps once to safe; Shift+Tab from safe wraps once to the final action. Gate back restores the exact Focused trigger, Focused back restores the exact Anchored trigger, Ambient/Anchored still do not steal host focus, and a deliberately no-op `focus()` makes `_safeFocus` detect failure and choose the declared fallback/no-forced-focus outcome.

    Extend the existing Chrome fixture with the same production sequence. Synthetic Tab is used only to observe the shell's boundary handling (not to claim browser default traversal): first/middle events remain unprevented, manually focused last wraps to safe, and back calls restore the true shadow origins. Assert the actual browser relation between `document.activeElement`, `shadowRoot.activeElement`, and the focused nodes after each transition. Do not label this VoiceOver or live-UAT evidence.
  </action>
  <verify>
    <automated>node tests/skopeo-accessibility.test.js &amp;&amp; node tests/skopeo-shell-contract.test.js &amp;&amp; node tests/skopeo-browser-contract.test.js</automated>
  </verify>
  <acceptance_criteria>
    - The mock exposes browser retargeting and would fail if a shadow child were assigned directly to `document.activeElement`.
    - Forward Tab from the first and middle Gate actions is not prevented; only last-to-first and Shift+first-to-last boundaries are consumed.
    - Focused/Gate entry and both one-level backs assert exact shadow active elements, and failed focus postconditions cannot suppress fallback.
    - The local-Chrome result independently reports the same focus/trap/restoration outcomes with the production shell.
  </acceptance_criteria>
  <done>WR-02 is closed under browser-faithful Shadow focus semantics without weakening the Gate-only trap or host no-focus-steal policy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make richer transitions atomic and release attention-owned handles</name>
  <read_first>
    - tests/skopeo-shell-contract.test.js (modified file; existing Ambient collision matrix, suspended-node identity checks, and complete ledger snapshots)
    - tests/skopeo-accessibility.test.js (modified file; exact prior-state focus and back behavior must survive staged rollback)
    - tests/skopeo-browser-contract.test.js (modified file; add actual Focused/Gate rectangle and narrow viewport cases to the same runner)
    - extension/content/skopeo-shell.js (modified file; `_button`, `_listen`, `_pointerSurface`, `_clearSurface`, `_suspendSurface`, `_restoreSurface`, `_render*`, `render`, `back`, and `destroy`)
    - tests/helpers/skopeo-resource-ledger.js (exact eleven-key snapshot oracle; do not change its category list)
    - .planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-REVIEW.md (WR-05 blocker and WR-04 `3/1 -> 13/13 -> 17/17` reproduction)
    - .planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-PATTERNS.md (atomic stage/measure/commit and render-scope patterns)
    - .planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-UI-SPEC.md (8px host clearance, narrow reflow, unsafe-view copy, and prior-level behavior)
  </read_first>
  <files>tests/skopeo-shell-contract.test.js, tests/skopeo-accessibility.test.js, tests/skopeo-browser-contract.test.js, extension/content/skopeo-shell.js</files>
  <action>
    Add red-first cases for WR-05 and WR-04. Place a visible eligible host control beneath the real proposed Focused card and Gate rectangles, including top/center controls and a viewport below 480 CSS px. Snapshot the exact attention attribute, surface node identities, deep focus, primitive set, live-region text, and all eleven resource counts before each transition. The unfixed shell must demonstrate that it commits over the control. Add two complete Ambient -> Anchored -> Focused -> Interstitial -> Focused -> Anchored -> Ambient cycles and show that current listener/pointer counts grow before the implementation change.

    Refactor render ownership into explicit surface scopes. A scope owns the nodes plus every per-render listener, pointer-surface, focus-hook, pending-render, and optional animation handle acquired while building that attention state. `_button`, `_listen`, and `_pointerSurface` must register per-node handles with the currently building scope; stable shell-wide resize/keydown/live-region handles remain session-owned. `_clearSurface` permanently disposes the outgoing active scope in reverse handle order. `_suspendSurfaceScope` detaches `{nodes, handles}` together without releasing it for a legitimate one-level back; `_restoreSurfaceScope` reattaches that same scope and identities; `_disposeSurfaceScope` releases active or suspended scopes exactly once. `destroy()` disposes active and every suspended scope before its final session-wide reverse cleanup. Remove the untracked CSS halo `animation` declaration and keyframes so the static labelled halo remains truthful while the `animations` category stays zero; do not retain an uncounted CSS animation.

    Before changing `_attention`, host attributes, suspended stacks, origins, or focus, build a proposed Focused/Interstitial scope as hidden shell-owned staging nodes, append it only long enough for the production Shadow CSS to yield a real `.skopeo-focused-card`/`.skopeo-gate` `getBoundingClientRect()`, and resolve `_requiredHostControl()`. That helper selects a connected/visible eligible document control outside `_host`; if focus is currently inside Skopeo, it falls back only to the remembered eligible host origin. Reject a candidate outside the viewport inset or intersecting the required host rectangle with `HOST_CLEARANCE` 8. On rejection, remove the staging nodes, dispose their scope to the pre-stage eleven-key snapshot, keep prior attention/DOM/suspended scopes/focus unchanged, write exact `COPY.unsafeView` to the one live region, and return false. On success, atomically suspend/dispose the preceding scope as appropriate, commit the staged scope, capture the true deep origin, and then focus. Do not query Drive/Docs selectors, mutate a host element, or invent semantic target identity.

    Require exact plateau evidence: each attention level's second stable snapshot deep-equals its first; Ambient after cycles 1 and 2 equals initial Ambient; a rejected richer transition equals the prior resource snapshot; all hidden-state handles are absent; final and repeated destroy equal the exact eleven-key zero snapshot. Extend the Chrome fixture to prove a safe real rectangle commits and a colliding Focused/Gate rectangle rolls back at normal and narrow viewport sizes. The fixture may use shell-owned/ordinary local controls only and is not Drive/Docs recognition or live UAT.
  </action>
  <verify>
    <automated>node tests/helpers/skopeo-resource-ledger.js --self-test &amp;&amp; node tests/skopeo-shell-contract.test.js &amp;&amp; node tests/skopeo-accessibility.test.js &amp;&amp; node tests/skopeo-browser-contract.test.js &amp;&amp; node tests/overlay-stability-cadence.test.js &amp;&amp; node tests/overlay-content-audit.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Unsafe Focused/Gate attempts preserve prior node identities, attention, deep focus, primitive allowlist, and complete resource snapshot while announcing exact `COPY.unsafeView`.
    - Safe proposed rectangles commit in Node and Chrome; colliding top/center and narrow-viewport rectangles fail closed with 8px clearance.
    - Repeated attention cycles plateau at complete per-state snapshots, and `listeners`/`pointerSurfaces` no longer grow from `3/1` to `13/13` or `17/17`.
    - `rg -n "animation:\\s*skopeo-halo-bloom|@keyframes skopeo-halo-bloom" extension/content/skopeo-shell.js` returns no match, while the labelled static halo and reduced-motion/forced-colors tests remain green.
    - No host mutation, Drive/Docs recognition, semantic anchor, Graphify, AI/MCP, server, daemon, or new dependency is introduced.
  </acceptance_criteria>
  <done>WR-05 and WR-04 are closed: richer states commit only when safe, rollback is exact, and active-session resources remain bounded before terminal zero cleanup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skopeo Shadow/top-layer host to the page | Browser cascade and hit testing decide whether the nominally transparent overlay actually intercepts the untrusted host. |
| Managed focus to host and Shadow controls | Browser retargeting and nonthrowing failed focus calls can deny traversal or lose the true restoration target. |
| Proposed attention surface to current page control | A staged Focused/Gate surface is untrusted until its real rectangle is proven safe. |
| Attention render to resource ledger | Detached nodes and callbacks must not accumulate or outlive their owning attention scope. |

## ASVS L1 Threat Register

| Threat | Severity | Disposition | Concrete mitigation and blocking evidence |
|--------|----------|-------------|-------------------------------------------|
| Host interaction interception | High | mitigate in Task 1/3 | Important computed host boundary, pointer-none geometry, actual rectangle collision gate, exact rollback, and local-Chrome popover/fallback assertions. |
| Focus denial or trap | High | mitigate in Task 2 | Deep active element, postcondition-verified focus, Gate boundary-only consumption, exact back/destroy restoration in corrected mock and Chrome. |
| Wrong-tab spoofing | High | preserve / owned by Plan 52-08 | This plan accepts no tab identity and changes no invocation routing; existing explicit generation/runtime tests remain in the verification chain, while Plan 52-08 closes outer tab authority. |
| Stale resurrection | High | mitigate at staged-render boundary | No authoritative state changes before safe commit; rejected staged scopes are synchronously disposed, and every render/destroy path retains existing disposed/generation admission checks. |
| Teardown-certificate spoofing or coercion | High | preserve / owned by Plan 52-08 | This plan returns exact numeric shell snapshots only; it does not loosen the message envelope. Plan 52-08 makes combined runtime certificates exact and authoritative. |
| Resource accumulation | Medium | mitigate in Task 3 | Per-attention scopes, reverse release, two-cycle per-state plateau, no untracked CSS animation, and exact eleven-key final zero. |
</threat_model>

<verification>
Run `node tests/helpers/skopeo-resource-ledger.js --self-test &amp;&amp; node tests/skopeo-shell-contract.test.js &amp;&amp; node tests/skopeo-accessibility.test.js &amp;&amp; node tests/skopeo-browser-contract.test.js &amp;&amp; node tests/overlay-stability-cadence.test.js &amp;&amp; node tests/overlay-content-audit.test.js`. WR-01, WR-02, WR-05, host interception, focus denial, and stale staged-render proofs are blocking. The browser test must execute rather than skip.
</verification>

<success_criteria>
- Chrome—not inline style inspection—proves fixed, inset-zero, maximum-z, pointer-none host geometry in top-layer and fallback paths.
- Shadow focus traversal/restoration matches real retargeting and never traps ordinary forward Gate order or forces body focus.
- Focused/Gate transitions either commit a measured safe scope or leave the exact prior state with the approved announcement.
- Attention resources plateau across repeated cycles and final destroy returns all eleven categories to numeric zero.
- Normal invocation remains Ambient-only under one dynamic shell, with no later-phase product scope or extra runtime/dependency.
</success_criteria>

<output>
After completion, create `.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-07-SUMMARY.md` with the computed-style values, real Shadow focus results, safe/unsafe rectangle matrix, per-state resource plateaus, test commands, and explicit statement that live UAT remains deferred.
</output>
