---
phase: 52-on-demand-hud-lifecycle-primitive-shell
plan: 08
type: execute
wave: 7
depends_on:
  - 03
  - 04
  - 05
  - 07
files_modified:
  - extension/ui/sidepanel.js
  - extension/content/skopeo-runtime.js
  - extension/background.js
  - tests/skopeo-sidepanel-command.test.js
  - tests/skopeo-session-lifecycle.test.js
  - tests/helpers/skopeo-resource-ledger.js
  - package.json
autonomous: true
gap_closure: true
requirements:
  - HUD-01
  - HUD-02
  - HUD-03
user_setup: []

must_haves:
  truths:
    - "Boot hydration, tabs.onActivated, and windows.onFocusChanged share one monotonic tab-authority epoch, so an older query or event can never overwrite or activate after a newer selected tab."
    - "The runtime reports shell and runtime-owned listeners/timeouts in one exact eleven-key inventory and emits teardown-complete only after fixture work, active listeners, runtime listener, hook, flag, and sentinel are removed."
    - "The service worker accepts a teardown certificate only when its key set is exactly the eleven declared resource categories and every value is a finite JavaScript number equal to zero; missing, extra, coerced, stale, or wrong-sender input is rejected."
    - "The local-Chrome mechanics gate and all focused, adjacent, extension-validation, and default regression gates run without a new package/runtime, while deferred Drive/Docs/VoiceOver UAT remains explicitly unclaimed."
  artifacts:
    - path: "extension/ui/sidepanel.js"
      provides: "One monotonic authority epoch spanning boot, tab activation, and window-focus tab resolution"
      contains: "_tabAuthorityEpoch"
      min_lines: 4080
    - path: "extension/content/skopeo-runtime.js"
      provides: "Combined runtime/shell resource accounting and post-cleanup one-shot teardown acknowledgment"
      contains: "combinedResourceSnapshot"
      min_lines: 440
    - path: "extension/background.js"
      provides: "Exact eleven-key finite numeric zero certificate validation bound to sender tab and generation"
      contains: "resourcesAreZero"
      min_lines: 16980
    - path: "tests/skopeo-sidepanel-command.test.js"
      provides: "Reverse-resolution outer tab-authority races and resource-certificate coercion negative controls"
      contains: "window focus authority"
      min_lines: 1450
    - path: "tests/skopeo-session-lifecycle.test.js"
      provides: "Runtime-owned resource counts, cleanup-before-ack order, one-shot certificate, and stale callback regressions"
      contains: "unregister-runtime-listener"
      min_lines: 1600
    - path: "tests/helpers/skopeo-resource-ledger.js"
      provides: "Shared exact eleven-key finite numeric zero oracle for independent background/runtime tests"
      contains: "isExactZeroSnapshot"
      min_lines: 220
    - path: "package.json"
      provides: "Mandatory default-chain registration of the local Chrome browser mechanics contract"
      contains: "skopeo-browser-contract.test.js"
  key_links:
    - from: "extension/ui/sidepanel.js"
      to: "tests/skopeo-sidepanel-command.test.js"
      via: "the test executes the actual boot, activation, and window-focus authority paths with reversed deferred resolutions"
      pattern: '_tabAuthorityEpoch|windows\.onFocusChanged|tabs\.onActivated'
    - from: "extension/content/skopeo-runtime.js"
      to: "extension/background.js"
      via: "one sender-bound `skopeo:teardown-complete` envelope carries the exact post-cleanup combined resource snapshot"
      pattern: 'skopeo:teardown-complete'
    - from: "tests/helpers/skopeo-resource-ledger.js"
      to: "tests/skopeo-session-lifecycle.test.js, tests/skopeo-sidepanel-command.test.js"
      via: "both suites consume the same independent exact-shape oracle and run missing/extra/coercible-value negative controls"
      pattern: 'isExactZeroSnapshot|zeroSnapshot'
    - from: "package.json"
      to: "tests/skopeo-browser-contract.test.js"
      via: "the browser mechanics test appears exactly once beside the existing Phase 52 suites in `npm test`"
      pattern: 'skopeo-browser-contract\.test\.js'
---

<objective>
Close the remaining current-tab authority blocker and harden teardown evidence so neither stale outer side-panel work nor a partial/coercible certificate can claim authority.

Purpose: Make explicit current-tab invocation and zero-residue acknowledgment trustworthy across asynchronous tab changes, runtime cleanup, and MV3 controller messaging, then put the new browser mechanics test into ordinary regression.

Output: Monotonic side-panel tab authority, exact combined resource certificates, reverse-race/coercion regressions, and complete automated gate registration.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-CONTEXT.md
@.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-RESEARCH.md
@.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-PATTERNS.md
@.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-UI-SPEC.md
@.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-VALIDATION.md
@.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-VERIFICATION.md
@.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-REVIEW.md
@.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-03-SUMMARY.md
@.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-04-SUMMARY.md
@.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-05-SUMMARY.md
@.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-07-SUMMARY.md
@extension/ui/sidepanel.js
@extension/content/skopeo-runtime.js
@extension/background.js
@tests/skopeo-sidepanel-command.test.js
@tests/skopeo-session-lifecycle.test.js
@tests/helpers/skopeo-resource-ledger.js

<gap_coverage>
This plan is the sole owner of WR-06 and advisory WR-03. Plan 52-07 exclusively owns WR-01, WR-02, WR-05, and WR-04. Task 3 verifies all Phase 52 automated mechanics but does not edit `52-UAT.md` or convert any deferred live row into PASS.
</gap_coverage>

<interfaces>
Use one side-panel-page-lifetime authority counter and three exact private helpers:

```javascript
var _tabAuthorityEpoch = 0;
function _claimTabAuthority() { return ++_tabAuthorityEpoch; }
function _tabAuthorityIsCurrent(epoch) { return epoch === _tabAuthorityEpoch; }
function _commitAuthoritativeTab(tabId, epoch) {
  // only positive tab id + current epoch may assign snapshot/activate Skopeo
}
```

`WINDOW_ID_NONE` and malformed focus events return before `_claimTabAuthority()`. `tabs.onActivated` claims synchronously from its explicit `incomingTabId`; boot and window-focus paths claim before starting their tab query and check the captured epoch after every await that can outlive a newer authority event.

The exact resource categories remain, in this order:

```javascript
[
  'roots', 'listeners', 'observers', 'timeouts', 'intervals',
  'animationFrames', 'animations', 'focusHooks', 'pointerSurfaces',
  'pendingRenders', 'popoverTopLayer'
]
```

Production runtime helpers have these concrete responsibilities:

```javascript
function bumpRuntimeResource(category, delta) {}
function combinedResourceSnapshot(shellSnapshot) {}
function resourcesAreExactZero(snapshot) {}
```

The terminal order becomes:

```text
terminal -> abort -> clear-fixture-timeout -> destroy-shell
-> unregister-key/pagehide-listeners -> unregister-runtime-listener
-> delete-fixture-hook/flag -> delete-sentinel
-> compute exact combined zero -> teardown-complete once -> freeze final snapshot
```

The worker's `resourcesAreZero` must use exact key-set equality and this value predicate for every category:

```javascript
typeof value === 'number' &amp;&amp; Number.isFinite(value) &amp;&amp; value === 0
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Serialize boot, activation, and window-focus tab authority</name>
  <read_first>
    - tests/skopeo-sidepanel-command.test.js (modified file; preserve the bounded controller tests and add an outer-event authority harness)
    - extension/ui/sidepanel.js (modified file; `_activeTabIdSnapshot`, `initTabConversationStore`, `tabs.onActivated`, `windows.onFocusChanged`, DOMContentLoaded, and bounded Skopeo controller)
    - tests/sidepanel-tab-scoping-fix-redo-smoke.test.js (actual listener-body brace-walk and explicit incoming-tab precedent)
    - tests/sidepanel-tab-aware-smoke.test.js (tab-query/window mocks and adjacent selected-tab behavior)
    - .planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-REVIEW.md (WR-06 reproduction)
    - .planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-PATTERNS.md (monotonic authority epoch and exact reversed-resolution cases)
    - .planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-CONTEXT.md (D-01 through D-03 and current-tab-only invariant)
  </read_first>
  <files>tests/skopeo-sidepanel-command.test.js, extension/ui/sidepanel.js</files>
  <action>
    Build a test-first outer authority harness around the actual production source, not a mirrored tab oracle. Reuse the suite's VM/deferred helpers and brace-walk/extraction precedent to register or execute the real `initTabConversationStore`, `tabs.onActivated`, and `windows.onFocusChanged` paths with mocked Chrome events, `refreshOwnerChip`, conversation migration/swap, and the real bounded `FSBSkopeoSidepanelController`. Instrument `_activeTabIdSnapshot`, controller activations, Skopeo row text writes, focus, and runtime `skopeo:get-status`/`skopeo:toggle-tab` messages. First demonstrate the current WR-06 failure by resolving Window B before older Window A and by resolving an old boot query after Tab B activation.

    Add `_tabAuthorityEpoch`, `_claimTabAuthority`, `_tabAuthorityIsCurrent`, and `_commitAuthoritativeTab` near `_activeTabIdSnapshot`. The commit helper accepts only a positive tab ID and the current captured epoch, assigns `_activeTabIdSnapshot`, and synchronously calls `FSBSkopeoSidepanelController.activateTab(tabId)`; stale calls perform no assignment, activation, DOM write, or focus move. Do not put a second active-tab query inside the bounded Skopeo controller.

    In `tabs.onActivated`, validate `incomingTabId`, claim the epoch synchronously before any owner/history await, persist the outgoing intent, and commit the explicit incoming ID immediately. After `refreshOwnerChip` and `swapToTabConversation`, recheck both the epoch and incoming ID before authority-dependent running-state/status-intent work. An older callback may finish unrelated best-effort data work but cannot reassign or repaint Skopeo.

    In `initTabConversationStore`, claim before starting `chrome.tabs.query({active:true,currentWindow:true})`; the query result may participate in idempotent storage migration, but it may assign `_activeTabIdSnapshot`, select `conversationId`, or activate Skopeo only if its captured epoch remains current. If a newer activation claimed authority while the boot query/migration awaited, preserve that newer snapshot and presentation. DOMContentLoaded must hydrate the controller only from the still-current `_activeTabIdSnapshot`.

    In `windows.onFocusChanged`, return for malformed/negative/`WINDOW_ID_NONE` before claiming. For a real window, claim immediately and start `chrome.tabs.query({active:true,windowId})` before awaiting `refreshOwnerChip`; a slow owner-chip refresh must not delay tab resolution. Commit/activate the result only while its epoch remains current, and recheck before any conversation/status continuation. Do not let Window A completion overwrite a newer Window B or explicit tab activation.

    Require these reversed cases with exact final authority and payloads: `focus A -> focus B -> resolve B -> resolve A` leaves B; `boot query -> activate B -> resolve boot A` leaves B; `focus A query -> activate B -> resolve A` leaves B; `focus A -> focus B -> resolve A first -> resolve B` leaves B; and `WINDOW_ID_NONE` performs no query, epoch increment, assignment, activation, focus move, or row write. After each race, an actual controller refresh/toggle must carry only the winning positive tab ID.
  </action>
  <verify>
    <automated>node tests/skopeo-sidepanel-command.test.js &amp;&amp; node tests/sidepanel-tab-scoping-fix-redo-smoke.test.js &amp;&amp; node tests/sidepanel-tab-aware-smoke.test.js &amp;&amp; node --check extension/ui/sidepanel.js</automated>
  </verify>
  <acceptance_criteria>
    - All four reverse-resolution cases end with Tab B in `_activeTabIdSnapshot`, last controller activation, last get-status payload, and next toggle payload.
    - Late Window A/boot work produces zero Skopeo row text writes and no focus move after Tab B becomes authoritative.
    - `WINDOW_ID_NONE` leaves the captured epoch and every authority side effect unchanged.
    - `rg -n "_tabAuthorityEpoch|_claimTabAuthority|_tabAuthorityIsCurrent|_commitAuthoritativeTab" extension/ui/sidepanel.js` finds one shared authority implementation used by all three outer paths.
    - No toolbar, command, Drive/Docs, or page-runtime behavior is added or changed.
  </acceptance_criteria>
  <done>WR-06 is closed: only the newest boot/activation/window-focus identity can select or control Skopeo for the side panel.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the eleven-key teardown certificate exact and post-cleanup</name>
  <read_first>
    - tests/helpers/skopeo-resource-ledger.js (modified file; exact category list and zero snapshot are the independent test authority)
    - tests/skopeo-session-lifecycle.test.js (modified file; runtime VM, teardown order, saved-listener/timer/promise, replacement, and zero snapshots)
    - tests/skopeo-sidepanel-command.test.js (modified file; replace the three-key fixture and extend real background validator negatives)
    - extension/content/skopeo-runtime.js (modified file; `zeroResources`, `currentSnapshot`, listener/timer acquisition, `terminateOwner`, replacement, and one-shot outbound messages)
    - extension/background.js lines 592-660 and 1120-1160 (modified file; `RESOURCE_KEYS`, `exactKeys`, `resourcesAreZero`, and sender/generation-bound `handleTeardownComplete`)
    - .planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-REVIEW.md (WR-03 coercion and premature acknowledgment finding)
    - .planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-PATTERNS.md (combined runtime inventory and cleanup-before-ack order)
    - .planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-CONTEXT.md (D-09 terminal boundary and D-14 exact cleanup)
  </read_first>
  <files>tests/helpers/skopeo-resource-ledger.js, tests/skopeo-session-lifecycle.test.js, tests/skopeo-sidepanel-command.test.js, extension/content/skopeo-runtime.js, extension/background.js</files>
  <action>
    Add and export `isExactZeroSnapshot(value)` beside `CATEGORIES`/`zeroSnapshot()` in the test helper. It accepts only a non-array object whose own key set exactly equals `CATEGORIES` and whose values each satisfy `typeof === 'number'`, `Number.isFinite`, and `=== 0`. Extend the helper self-test with one valid snapshot plus rejection of every individually missing key, one extra key, `null`, `false`, empty string, numeric string, `NaN`, `Infinity`, `-Infinity`, non-object, and array. This helper is a test oracle only; production must not import it.

    Replace the three-key `ZERO_RESOURCES` in `tests/skopeo-sidepanel-command.test.js` with `zeroSnapshot()` from the helper. Before production changes, add background-controller negative controls proving the current validator incorrectly accepts a partial/coercible certificate. After the fix, require rejection for every missing category, an extra category, all coercible/non-finite values above, payload `tabId`, wrong sender tab, stale generation, and malformed outer keys. A complete exact numeric-zero snapshot from the current sender/generation remains the only accepted certificate.

    In `extension/background.js`, change `RESOURCE_KEYS` to one immutable ordered eleven-name array (use a companion Set only if another membership check requires it) and implement `resourcesAreZero` as `exactKeys(resources, RESOURCE_KEYS)` plus the exact finite-number predicate. Preserve `handleTeardownComplete`'s exact outer envelope, `sender.tab.id` authority, current-generation check, and terminal reducer transitions; do not accept a page-provided tab ID or make certificate validation advisory.

    In `extension/content/skopeo-runtime.js`, retain the same category names/order and add a small owner-local numeric inventory plus `bumpRuntimeResource`, `combinedResourceSnapshot`, and `resourcesAreExactZero`. Count the Chrome runtime message listener as one `listeners` resource from installation until actual removal; count keydown and pagehide as two additional listeners only while installed; count the controlled-fixture timer as one `timeouts` resource from scheduling until callback entry or explicit clear. `currentSnapshot()` must add those runtime counts to the shell's exact snapshot, so an active runtime never falsely reports shell-only counts.

    Rewrite `terminateOwner` in the exact terminal order declared by this plan. Set terminal/disposed and abort first; clear and decrement the fixture timer; destroy the shell and retain its returned exact snapshot; remove/decrement keydown and pagehide; remove/decrement the Chrome runtime listener; delete the fixture hook/flag and sentinel only when owned; then compute the combined snapshot. Emit at most one `skopeo:teardown-complete` only if the combined snapshot is exact eleven-key numeric zero, and only after all removals. If destroy returns malformed/nonzero resources or a runtime counter remains nonzero, keep the owner terminal and return its diagnostic final snapshot but emit no zero certificate. Freeze/cache the final snapshot so a saved listener, cleared timer callback, Promise continuation, second terminate, or replacement cannot send another acknowledgment or recreate a resource.

    Update `tests/skopeo-session-lifecycle.test.js` to assert active combined counts (runtime listener always, active key/page listeners only after commit, fixture timeout only while pending), abort before shell destroy, every removal before acknowledgment, exact eleven finite zeros at acknowledgment, and one acknowledgment total. Delete the old expected order that placed acknowledgment before listener removal. Add negative shells/counters that remain nonzero or malformed and prove no certificate is sent. Release saved listener/timer/Promise/replacement work after termination and require the cached exact terminal state, zero new messages, zero sentinel/hook, and no resource resurrection.
  </action>
  <verify>
    <automated>node tests/helpers/skopeo-resource-ledger.js --self-test &amp;&amp; node tests/skopeo-session-lifecycle.test.js &amp;&amp; node tests/skopeo-sidepanel-command.test.js &amp;&amp; node --check extension/content/skopeo-runtime.js &amp;&amp; node --check extension/background.js</automated>
  </verify>
  <acceptance_criteria>
    - Helper, runtime, and background tests agree on exactly eleven own keys and finite numeric zero values; partial, extra, coerced, and non-finite certificates all fail.
    - Active runtime snapshots include one runtime message listener, two active page listeners after commit, and one timeout only while the fixture callback is pending.
    - Test operation order places active/runtime listener removal, hook/flag deletion, and sentinel deletion before the sole `teardown-complete` send.
    - A malformed or nonzero final resource snapshot sends no teardown certificate, while the owner remains terminal and stale callbacks remain inert.
    - Exact sender tab, generation, terminal-first reducer behavior, and no-resurrection invariants remain green.
  </acceptance_criteria>
  <done>WR-03 is closed: the worker can no longer be convinced by a partial/coerced snapshot, and a valid certificate describes the fully cleaned runtime rather than only the shell.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Register the browser mechanics gate and run complete automated closure</name>
  <read_first>
    - package.json (modified file; preserve the entire existing `scripts.test` chain and add only one local command)
    - tests/skopeo-browser-contract.test.js (Plan 52-07 required local-Chrome mechanics gate)
    - tests/skopeo-session-lifecycle.test.js (Task 2 runtime/certificate regressions)
    - tests/skopeo-shell-contract.test.js (Plan 52-07 geometry/collision/resource regressions)
    - tests/skopeo-sidepanel-command.test.js (Tasks 1-2 authority/certificate regressions)
    - tests/skopeo-accessibility.test.js (Plan 52-07 Shadow focus regressions)
    - tests/extension-content-script-files-completeness.test.js (dynamic-only Skopeo injection invariant)
    - .planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-UAT.md (read only; live rows remain deferred and must not be edited or marked PASS)
    - .planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-VERIFICATION.md (all blockers/residuals that this automated closure must make re-verifiable)
  </read_first>
  <files>package.json</files>
  <action>
    Add `node tests/skopeo-browser-contract.test.js` exactly once in `package.json`'s existing `scripts.test` chain immediately after the four current Phase 52 suites (or directly after `skopeo-accessibility.test.js`). Preserve every existing command and ordering outside that one insertion. Do not add a dependency, alternate browser script, watch mode, server, daemon, environment download, or skip-on-missing-Chrome PASS path.

    Run the focused closure chain in this order: ledger self-test; lifecycle; shell; side-panel/command; accessibility; browser contract; overlay state; overlay content audit; overlay cadence; sidepanel tab-aware smoke; sidepanel tab-scoping smoke; and injection completeness. Then run `npm run validate:extension` followed by full `npm test`. Record any generated-file diff, restore only deterministic test-generated date stamps if they are the sole unrelated changes, and leave user/unrelated work untouched. Run `git diff --check` and confirm the only intended source/test/package changes belong to Plans 52-07/08.

    Do not edit `52-UAT.md`, insert Chrome/Drive/Docs/VoiceOver observations, or change any live row from DEFERRED. The local Chrome runner proves deterministic DOM/browser mechanics only. It does not satisfy command remapping, MV3 sleep/wake, Drive/Docs hit testing, VoiceOver, OS preference, or full live resource evidence.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const f='skopeo-browser-contract.test.js'; if ((p.scripts.test.match(new RegExp(f.replace(/[.]/g,'\\.'),'g'))||[]).length !== 1) throw new Error(f+' must occur once')" &amp;&amp; node tests/helpers/skopeo-resource-ledger.js --self-test &amp;&amp; node tests/skopeo-session-lifecycle.test.js &amp;&amp; node tests/skopeo-shell-contract.test.js &amp;&amp; node tests/skopeo-sidepanel-command.test.js &amp;&amp; node tests/skopeo-accessibility.test.js &amp;&amp; node tests/skopeo-browser-contract.test.js &amp;&amp; node tests/test-overlay-state.js &amp;&amp; node tests/overlay-content-audit.test.js &amp;&amp; node tests/overlay-stability-cadence.test.js &amp;&amp; node tests/sidepanel-tab-aware-smoke.test.js &amp;&amp; node tests/sidepanel-tab-scoping-fix-redo-smoke.test.js &amp;&amp; node tests/extension-content-script-files-completeness.test.js &amp;&amp; npm run validate:extension &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` parses, the browser contract appears exactly once, and no existing test command is removed.
    - Every focused/adjacent command, `npm run validate:extension`, and full `npm test` exits zero with the required local Chrome test actually executed.
    - `git diff --check` exits zero and no new package, lockfile change, static Skopeo injection, server, daemon, AI/MCP, Graphify, or Drive/Docs recognition appears.
    - `52-UAT.md` remains byte-for-byte unchanged with L01-L15 deferred and zero live rows passed.
  </acceptance_criteria>
  <done>All six Phase 52 code-review findings have automated closure evidence in the default regression chain without misrepresenting deferred live UAT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Asynchronous boot/window/tab work to selected-tab state | Older query results are untrusted once a newer authority event occurs. |
| Document runtime to service-worker terminal state | A page acknowledgment can be stale, wrong-tab, partial, malformed, or coercible. |
| Shell/runtime resources to teardown evidence | An acknowledgment is trustworthy only after every owner has actually released its resource. |
| Automated browser result to live product claims | Local deterministic mechanics cannot be promoted into Drive/Docs/VoiceOver UAT evidence. |

## ASVS L1 Threat Register

| Threat | Severity | Disposition | Concrete mitigation and blocking evidence |
|--------|----------|-------------|-------------------------------------------|
| Host interaction interception | High | preserve from Plan 52-07 | Default-chain Chrome computed-style/collision gate and existing pointer/host-integrity suites must remain green; this plan changes no shell geometry. |
| Focus denial or trap | High | preserve from Plan 52-07 | Browser-faithful focus suite and local-Chrome Gate traversal run in Task 3; this plan changes no focus policy. |
| Wrong-tab spoofing | High | mitigate in Task 1 | Shared monotonic authority epoch, explicit event tab, reverse-resolution A/B and boot/activation races, and winning-tab payload assertions. |
| Stale resurrection | High | mitigate in Tasks 1-2 | Stale authority work cannot reactivate an old tab; runtime stays terminal before cleanup and saved callbacks cannot send a second certificate or recreate state. |
| Teardown-certificate spoofing or coercion | High | mitigate in Task 2 | Sender/generation binding, exact outer envelope, exact eleven-key set, finite numeric zeros, cleanup-before-send, and missing/extra/coercion negative controls. |
| Resource accumulation | Medium | mitigate/preserve across Plans 52-07/08 | Runtime listeners/timeouts join the combined inventory; Plan 52-07 attention scopes plateau; full eleven-key zero is required before acknowledgment. |
</threat_model>

<verification>
Run the complete Task 3 command. Wrong-tab spoofing, stale resurrection, certificate spoof/coercion, host interception, and focus denial are blocking. `tests/skopeo-browser-contract.test.js` must execute local Chrome rather than skip, while all live UAT fields remain deferred.
</verification>

<success_criteria>
- Only the newest boot/tab/window event can assign the side-panel current tab or issue Skopeo status/toggle work.
- Runtime-owned listeners and fixture timeout are visible while active and removed before the one exact teardown acknowledgment.
- Background rejects every non-exact, non-finite, coerced, stale, or wrong-sender resource certificate.
- Browser mechanics, focused suites, adjacent regressions, extension validation, and full default tests all pass with no extra runtime/dependency.
- Phase 52 becomes eligible for re-verification, but its live Chrome/Drive/Docs/VoiceOver debt remains honest and deferred.
</success_criteria>

<output>
After completion, create `.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-08-SUMMARY.md` with authority race outcomes, active/terminal combined resource snapshots, certificate rejection matrix, full command results, and explicit deferred-UAT statement.
</output>
