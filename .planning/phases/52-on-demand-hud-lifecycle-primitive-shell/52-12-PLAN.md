---
phase: 52-on-demand-hud-lifecycle-primitive-shell
plan: 12
type: execute
wave: 9
depends_on:
  - 10
files_modified:
  - extension/ui/sidepanel.js
  - tests/skopeo-sidepanel-command.test.js
autonomous: true
gap_closure: true
requirements:
  - HUD-01
  - HUD-02
user_setup: []

must_haves:
  truths:
    - "Status and toggle keep independent request-lane side-effect authority but share one lifecycle-presentation authority, so an accepted live status event makes every older status/toggle presentation capture unable to write."
    - "For one positive generation, lifecycle presentation moves only forward through Starting, Active, and terminal; duplicates are admissible, while Active cannot regress to Starting and terminal cannot resurrect to Active or Starting."
    - "Tab, activation, lane, presentation, generation, and lifecycle admission all occur before state/global/DOM writes; rejected events/responses leave the current row, focus, chat state, and authority records unchanged."
  artifacts:
    - path: "extension/ui/sidepanel.js"
      provides: "Shared cross-lane presentation tokens and ordered same-generation lifecycle admission"
      contains: "FSBSkopeoSidepanelController"
      min_lines: 4205
    - path: "tests/skopeo-sidepanel-command.test.js"
      provides: "Live-event/late-response races, duplicate admission, and forward-only lifecycle matrix"
      contains: "assertPerTabGenerationFloors"
      min_lines: 3046
  key_links:
    - from: "extension/ui/sidepanel.js"
      to: "tests/skopeo-sidepanel-command.test.js"
      via: "the extracted production controller resolves held status/toggle responses around accepted live status events"
      pattern: "captureRequest|handleSkopeoStatusEvent|renderSkopeoState"
    - from: "extension/ui/sidepanel.js"
      to: "extension/background.js"
      via: "existing explicit tab IDs and worker-issued positive generations feed the side-panel presentation arbiter without protocol changes"
      pattern: "skopeo:get-status|skopeo:toggle-tab|skopeo:status-changed|generation"
---

<objective>
Close WR-09 by serializing lifecycle presentation across status responses, toggle responses, and live status events while enforcing actual same-generation transition order.

Purpose: Prevent an older equal-generation completion from repainting Starting or Active after the worker has already presented Active or Off for the selected tab.

Output: Shared presentation captures, a forward-only per-tab lifecycle presentation record, and deterministic extracted-controller race coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-CONTEXT.md
@.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-RESEARCH.md
@.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-PATTERNS.md
@.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-UI-SPEC.md
@.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-VERIFICATION.md
@.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-08-SUMMARY.md
@.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-10-SUMMARY.md
@extension/ui/sidepanel.js
@extension/background.js
@extension/utils/skopeo-session-state.js
@tests/skopeo-sidepanel-command.test.js

<gap_coverage>
Plans 52-01 through 52-10 retain coverage of the Phase 52 goal, all seven HUD requirements, and D-01 through D-14. This plan claims only the HUD-01/HUD-02 current-tab lifecycle-presentation repair required by WR-09. Plan 52-11 owns WR-07/WR-10 and may execute in the same wave because its files are disjoint. Drive/Docs routing, corpus work, Graphify/contracts, extra capability packs, and live UAT remain excluded; `52-UAT.md` is read-only and zero-live-PASS.

## Must Haves: Decision Preservation Coverage

| Locked decision | Existing executed implementation | What 52-12 preserves or re-verifies |
|---|---|---|
| D-01: | Plans 52-03 through 52-05 established explicit dynamic invocation, no page root during off/prepare, no static Skopeo bundle, and the side-panel row as the sole off-state affordance. | Change only admitted side-panel presentation ordering; retain the explicit worker envelopes and registered command/static-bundle checks so no auto-activation, dormant page launcher, or off-state page residue is introduced. |
| D-02: | Plans 52-04 and 52-05 implemented the dedicated side-panel switch and configurable `toggle-skopeo-current-tab` command while leaving the toolbar action opening the FSB side panel. | Keep the command, shortcut hint, switch semantics, toolbar contract, and `{action, tabId}` payloads unchanged; controller tests re-verify that the repair adds no query, listener, protocol, or alternate invocation surface. |
| D-03: | Plans 52-01, 52-04, 52-05, 52-08, and 52-10 implemented explicit current-tab IDs, command-tab authority, outer activation epochs, same-tab ABA tokens, independent request lanes, and per-tab generation floors. | Preserve admission order as tab -> activation -> lane before shared presentation/generation/lifecycle checks, and re-run wrong-tab plus A1 -> B -> A2 races proving only the selected activation can write. |
| D-08: | Plans 52-01 and 52-03 through 52-05 implemented cancellable Starting/Active toggle-off, double-Escape current-tab kill, abort-first teardown, and isolation from other tabs/unrelated FSB work. | Do not dispatch or repeat kill side effects; prove each explicit toggle envelope occurs once and that accepted terminal presentation makes late Starting/Active completions zero-write while focus and unrelated chat state remain unchanged. |
| D-09: | Plans 52-01, 52-03, 52-04, 52-08, and 52-10 implemented terminal generation tombstones, stale continuation rejection, exact cleanup certificates, same-tab ABA authority, and retained positive generation floors. | Keep those floors and terminal boundaries authoritative, then re-verify that equal-generation presentation moves only Starting -> Active -> terminal, lower/unverifiable work is rejected, and a later generation alone may begin a fresh lifecycle. |
</gap_coverage>

<interfaces>
Keep `_activeTabIdSnapshot`, the outer tab epoch, per-activation tokens, independent `status`/`toggle`/`shortcut` request lanes, and per-tab positive generation floors from Plan 52-10. Add a presentation layer only inside `FSB_SKOPEO_SIDEPANEL_CONTROLLER_START/END`:

```javascript
var _presentationSerial = 0;
var _latestPresentation = null; // { tabId, activationToken, token }
var _lifecyclePresentationByTab = new Map(); // tab -> { generation, stage }

function claimPresentation(capture) {}
// Status/toggle captures receive a presentation token; shortcut captures do not.

function presentationIsCurrent(capture) {}
// Current selected tab + activation + shared presentation token.

function lifecycleStage(response) {}
// starting -> 1, active -> 2, off/error/unsupported/unsafe terminal -> 3.

function acceptLifecyclePresentation(tabId, response, options) {}
// Preserve positive generation floors. Higher generation resets the stage;
// equal generation permits only nondecreasing stage and exact-stage repeats.
```

Local optimistic Starting may render under the current toggle presentation token but must not fabricate or advance a worker generation record. Accepted worker responses/events advance the lifecycle record immediately before render. No timer, second tab query, worker/storage field, listener, dependency, or new public controller method is added. Preserve D-02, D-03, D-07, D-08, D-09, and D-14.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Share lifecycle presentation authority across status, toggle, and live events</name>
  <read_first>
    - extension/ui/sidepanel.js (modified file; read the entire bounded controller, especially `captureRequest`, `requestIsCurrent`, `renderSkopeoState`, `refreshSkopeoControl`, `handleSkopeoToggle`, and `handleSkopeoStatusEvent`)
    - tests/skopeo-sidepanel-command.test.js (modified file; read `instrumentSidepanelControllerAuthority`, `bootSidepanelController`, deferred helpers, `captureSidepanelPresentation`, same-tab ABA tests, and status-event tests)
    - .planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-VERIFICATION.md (WR-09 production probe and required race matrix)
    - .planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-10-SUMMARY.md (independent request-lane and generation-floor decisions that must remain intact)
    - extension/background.js (read-only source for get-status/toggle/status-changed response order and fields)
  </read_first>
  <files>extension/ui/sidepanel.js, tests/skopeo-sidepanel-command.test.js</files>
  <action>
    Add a red-first `assertLifecyclePresentationAuthority` suite against the extracted production controller. Hold a toggle response for tab 11/generation 2 at Starting, accept a live generation-2 Active event, snapshot the complete presentation/write log, then release the older toggle response; prove the current code repaints Starting. Add held status/toggle variants in which a live Off event is accepted first and older generation-equal Active or Starting responses arrive afterward. Assert each late promise returns false, performs zero additional state/text/attribute/hint writes, cannot advance generation/presentation records, preserves switch focus and unrelated chat state, and never repeats the original toggle side effect.

    Add `_presentationSerial` and one latest presentation capture scoped to the current activation. `captureRequest` must continue to advance/check its existing independent lane token, but `status` and `toggle` captures also claim a new shared presentation token; `shortcut` remains independent and presentation-neutral. Add `presentationIsCurrent(capture)` and require it together with `requestIsCurrent(capture)` after every status/toggle await, before error fallback, before `acceptGeneration`, and immediately before `renderSkopeoState` writes. A newer lifecycle request may supersede only the older request's ability to paint; it must not repeat/cancel an already-dispatched toggle side effect or make shortcut work stale.

    Treat live `skopeo:status-changed` as a candidate before it owns anything. First validate selected tab, activation, generation, and (after Task 2) lifecycle transition without advancing state. Only an accepted event claims a new status-lane token plus shared presentation token, then rechecks and renders/advances synchronously. A rejected event must leave both prior tokens untouched, preserving Plan 52-10's pending valid status request. Every accepted event—including a duplicate—must invalidate all older status/toggle presentation captures even though their independent lane tokens may remain current.

    Extend the authority instrumentation with presentation serial/latest-token state so tests prove monotonic claims and zero mutation on rejection. Keep the controller export frozen and unchanged, keep message payloads exactly `{action, tabId}`, and retain one runtime status listener.
  </action>
  <verify>
    <automated>node tests/skopeo-sidepanel-command.test.js &amp;&amp; node tests/sidepanel-tab-scoping-fix-redo-smoke.test.js &amp;&amp; node tests/sidepanel-tab-aware-smoke.test.js &amp;&amp; node --check extension/ui/sidepanel.js</automated>
  </verify>
  <acceptance_criteria>
    - Status and toggle retain independent request-lane tokens, while every lifecycle-producing capture also compares one shared presentation token before writing.
    - An accepted live Active event makes an older held Starting response return false with zero writes; an accepted live Off event does the same to older Active and Starting responses.
    - Rejected live events do not claim/advance status-lane or presentation authority and do not invalidate an already pending valid request.
    - Accepted duplicate live events claim newer presentation authority but changed-value rendering does not repeat atomic live copy.
    - Shortcut completion, explicit `{action, tabId}` side effects, same-tab ABA guards, outer tab authority, focus, and unrelated chat state remain unchanged.
  </acceptance_criteria>
  <done>Worker-accepted live status and newer lifecycle requests own one shared presentation timeline, so older status/toggle completions cannot repaint the row.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce forward-only equal-generation lifecycle presentation</name>
  <read_first>
    - extension/ui/sidepanel.js (modified file; read `acceptGeneration`, `localStateCopy`, `renderSkopeoState`, the Task 1 presentation helpers, and all generation-bearing callers)
    - tests/skopeo-sidepanel-command.test.js (modified file; read `assertPerTabGenerationFloors`, especially the current equal-generation Off -> Active allowance, then the Task 1 race suite)
    - extension/utils/skopeo-session-state.js (read-only source of STARTING/ACTIVE/TERMINATING/OFF generation lifecycle semantics)
    - extension/background.js (read-only public status projection and terminal error/unsupported/off messages)
    - .planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-CONTEXT.md (D-07 through D-09 terminal and kill behavior)
    - .planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-VERIFICATION.md (WR-09 duplicates and legitimate-forward acceptance criteria)
  </read_first>
  <files>extension/ui/sidepanel.js, tests/skopeo-sidepanel-command.test.js</files>
  <action>
    Replace the existing test that intentionally permits equal-generation Off -> Active reversal. Add a current-authority matrix, independent of stale presentation tokens: Starting -> Active and Active -> Off at the same positive generation are accepted; Starting -> Starting, Active -> Active, and Off -> Off duplicates are accepted without duplicate text/live writes; Active -> Starting is rejected; and terminal Off/Error/Unsupported -> Active or Starting is rejected. Include the exact WR-09 Active-event -> late Starting and Off-event -> late Active/Starting response races from Task 1 so both shared authority and transition order are blocking.

    Add one page-lifetime `_lifecyclePresentationByTab` record and a closed stage classifier. `starting` is stage 1, `active` is stage 2, and `off`, `error`, `unsupported`, unsafe-layout, or any other fail-closed local terminal projection is stage 3. For a positive safe-integer generation above the retained floor, accept any first observed stage and replace the tab record; this supports panel hydration directly into Active or Off. For the same generation, accept only a stage equal to or greater than the stored stage. Never allow a lower stage. Terminal-to-terminal changes may remain stage-equal and fail closed, but no terminal state may return to Active/Starting without a strictly newer generation.

    Route all generation-bearing get-status responses, toggle responses, and accepted live events through `acceptLifecyclePresentation`. Admission order is tab -> activation -> request lane -> shared presentation -> positive generation floor -> lifecycle order -> record advance -> DOM write. A preflight `{advance:false}` performs no map/floor/presentation mutation; `{advance:true}` updates the generation floor and lifecycle record immediately before render. Generationless legacy/error behavior before a positive floor remains as Plan 52-10 specified and never creates a lifecycle record; after a floor exists it remains rejected. Local optimistic Starting has no generation and must not overwrite the last accepted worker lifecycle record.

    Assert the lifecycle map is per tab, retained across A -> B -> A re-entry, never advanced by wrong-tab/expired/malformed/rejected input, and bounded to tabs encountered in the side-panel page. Static checks should find exactly one presentation serial/latest authority and one lifecycle map, while the existing one activation serial, request serial, lane map, and generation-floor map remain exactly once.

    Finish with focused/adjacent suites and extension validation. Preserve local text-only copy, switch semantics, command hint behavior, zero second `chrome.tabs.query`, and every existing payload shape.
  </action>
  <verify>
    <automated>node tests/skopeo-sidepanel-command.test.js &amp;&amp; node tests/sidepanel-tab-scoping-fix-redo-smoke.test.js &amp;&amp; node tests/lattice-provider-bridge-smoke.test.js &amp;&amp; node tests/skopeo-session-lifecycle.test.js &amp;&amp; npm run validate:extension</automated>
  </verify>
  <acceptance_criteria>
    - Equal-generation Starting -> Active and Active -> terminal are admitted; equal-generation duplicates are admitted without repeated live-copy writes.
    - Equal-generation Active -> Starting and terminal -> Active/Starting are rejected before lifecycle records, generation floors, global state, or DOM mutate.
    - The exact Active event -> late Starting and Off event -> late Active/Starting races return false for late completions and leave the accepted row byte-for-byte unchanged.
    - A strictly newer positive generation may hydrate directly to Starting, Active, or terminal and begins a fresh ordered lifecycle; lower/unverifiable generations remain rejected under Plan 52-10 rules.
    - Controller authority/map declarations occur exactly once, explicit worker envelopes are unchanged, and `git hash-object .planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-UAT.md` remains `a9fa6926c909d322fe45d8d959d37a24f4cafd80`.
  </acceptance_criteria>
  <done>Equal-generation presentation follows the worker lifecycle rather than arbitrary response arrival, fully closing WR-09.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Concurrent status/toggle responses to one row | Independent request lanes may both be current for side effects but cannot both own final lifecycle presentation. |
| Live worker event to held async capture | An accepted event is newer presentation evidence and must revoke older response write authority. |
| Equal generation to lifecycle state | Numeric equality does not authorize a backwards lifecycle transition or terminal resurrection. |
| Skopeo controller to unrelated side-panel UI | Rejected lifecycle work must not alter chat state, focus, hint state, or outer tab authority. |

## ASVS L1 Threat Register

| Threat | Severity | Disposition | Concrete mitigation and blocking evidence |
|--------|----------|-------------|-------------------------------------------|
| Stale resurrection / lifecycle regression | High | mitigate in Tasks 1-2 | Shared presentation tokens plus nondecreasing same-generation stages block late Starting/Active after accepted Active/Off. |
| Wrong-tab or ABA presentation | High | preserve/extend in Tasks 1-2 | Existing tab/activation/lane guards execute before presentation and generation admission; A1/B/A2 suites remain blocking. |
| User-control deception | High | mitigate in Tasks 1-2 | Row checked/busy/status/action copy follows one admitted lifecycle authority; zero-write race snapshots prevent false Starting/Active resurrection. |
| Focus denial / unrelated UI corruption | High | preserve | Rejected response/event matrices assert exact focus, chat, hint, and mutation-log preservation. |
| Resource accumulation | Medium | preserve | Scalar serial/current capture plus one per-tab lifecycle map add no listener, timer, root, request, or worker field and reuse the existing page-lifetime controller. |
| Host interaction interception | High | preserve / Plan 52-11 boundary | This plan changes no page shell; registered shell/local-Chrome geometry gates remain green. |
</threat_model>

<verification>
Run both focused task commands, confirm `tests/skopeo-sidepanel-command.test.js` occurs exactly once in `package.json`'s `test` script, then run `npm test` and `git diff --check -- extension/ui/sidepanel.js tests/skopeo-sidepanel-command.test.js`. WR-09, any late lifecycle repaint, backwards equal-generation transition, invalidation of a pending request by a rejected event, wrong-tab/ABA regression, new protocol/query/listener, or `52-UAT.md` mutation is blocking. Inspect and restore only known generated date-stamp noise if the registered full chain produces it; do not alter unrelated user changes.
</verification>

<success_criteria>
- One shared presentation authority spans lifecycle-producing status/toggle requests and accepted live events without removing independent request-lane side-effect guards.
- Accepted live Active/Off events invalidate older captures, and late Starting/Active responses perform zero writes.
- Same-generation lifecycle presentation is forward-only with duplicates and legitimate Starting -> Active -> terminal transitions preserved.
- Existing outer/ABA/generation authority, text-only accessibility, explicit-tab payloads, registered validation, and unrelated side-panel behavior remain green.
- D-01 through D-14 and deferred scope remain unchanged; no worker protocol, storage field, tab query, dependency, server, daemon, Graphify work, or live-UAT claim is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-12-SUMMARY.md` with the presentation-token trace, live-event/late-response matrix, equal-generation lifecycle table, focused/registered command results, and explicit statement that L01-L15 remain deferred.
</output>
