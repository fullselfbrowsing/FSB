---
phase: 52-on-demand-hud-lifecycle-primitive-shell
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - extension/content/skopeo-shell.js
  - tests/helpers/skopeo-resource-ledger.js
  - tests/skopeo-shell-contract.test.js
  - tests/skopeo-accessibility.test.js
autonomous: true
requirements:
  - HUD-03
  - HUD-04
  - HUD-05
  - HUD-07
  - HUD-08
user_setup: []

must_haves:
  truths:
    - "Normal invocation creates one Shadow shell containing only the Ambient lens and rail; richer attention states are reachable only through a shell-owned test/dev fixture."
    - "Exactly six primitive names share one placement, focus, motion, pointer, color, and accessibility contract."
    - "The page envelope and visual-only geometry are pointer-transparent, host DOM/accessibility/layout state is never mutated, and unsafe placement fails closed."
    - "Destroy is synchronous and idempotent, restores eligible focus, hides/releases any open popover top-layer state, and returns the non-vacuous eleven-category resource ledger to zero."
  artifacts:
    - path: "extension/content/skopeo-shell.js"
      provides: "One isolated Shadow shell implementing Ambient plus the controlled four-level/six-primitive grammar"
      contains: "SkopeoShell"
      min_lines: 500
    - path: "tests/helpers/skopeo-resource-ledger.js"
      provides: "Reusable eleven-category root/listener/observer/timer/animation/focus/pointer/render/popover-top-layer acquisition and release ledger with negative controls"
      contains: "assertEmpty"
      min_lines: 90
    - path: "tests/skopeo-shell-contract.test.js"
      provides: "Executable shell count, primitive policy, placement, pointer, host-integrity, hostile-text, and teardown checks"
      contains: "six primitives"
      min_lines: 260
    - path: "tests/skopeo-accessibility.test.js"
      provides: "Executable names, roles, focus order/restoration, Escape ladder, preferences, and hidden-node checks"
      contains: "alertdialog"
      min_lines: 220
  key_links:
    - from: "extension/content/skopeo-shell.js"
      to: "tests/helpers/skopeo-resource-ledger.js"
      via: "test instrumentation observes every owned resource category and requires a zero post-destroy snapshot"
      pattern: 'listeners|observers|timeouts|animations|focusHooks|roots|popoverTopLayer'
    - from: "extension/content/skopeo-shell.js"
      to: "extension/content/skopeo-runtime.js"
      via: "global `FSBSkopeoShell.createShell` factory consumed by Plan 03"
      pattern: 'globalThis\.FSBSkopeoShell'
---

<objective>
Build and prove the isolated visual shell: one dynamically mounted Shadow owner, one shared six-primitive grammar, four attention policies, accessible focus behavior, collision-safe placement, and complete resource teardown.

Purpose: Make the host-page surface safe and reusable before any extension invocation wiring can create it.

Output: The shell, a non-vacuous resource ledger, and focused shell/accessibility contract suites.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-CONTEXT.md
@.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-RESEARCH.md
@.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-VALIDATION.md
@.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-UI-SPEC.md
@extension/content/visual-feedback.js
@extension/content/lifecycle.js
@extension/content/accessibility.js
@tests/overlay-content-audit.test.js
@tests/overlay-stability-cadence.test.js

<interfaces>
Create this dual-export shell surface:

```javascript
const PRIMITIVES = Object.freeze(['anchor', 'chip', 'halo', 'rail', 'ghost', 'gate']);
const ATTENTION = Object.freeze({
  AMBIENT: 'ambient', ANCHORED: 'anchored',
  FOCUSED: 'focused', INTERSTITIAL: 'interstitial'
});
const ATTENTION_POLICY = Object.freeze({
  ambient: ['rail'],
  anchored: ['anchor', 'chip', 'rail', 'halo'],
  focused: ['anchor', 'chip', 'ghost'],
  interstitial: ['gate']
});

function createShell(options) {} // returns a SkopeoShell
class SkopeoShell {
  prepareAmbient() {} // collision-checks and returns an opaque prepared placement without inserting a host
  mountAmbient(preparedPlacement) {} // consumes the exact prepared placement after worker commit
  render(level, typedModel) {}
  back() {}
  destroy(reason) {}
  getResourceSnapshot() {}
  enableControlledFixture(token) {}
}

globalThis.FSBSkopeoShell = api;
if (typeof module !== 'undefined' && module.exports) module.exports = api;
```

`createShell` requires injected `document`, `window`, numeric `generation`, and callbacks `onRequestClose`/`onRequestKill`. Ordinary callers never receive a fixture token. The test/dev fixture token is an opaque object created by the test harness and enables only shell-owned sample nodes and the exact UI-SPEC fixture copy.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create the non-vacuous shell, resource, and accessibility test contracts</name>
  <read_first>
    - tests/helpers/skopeo-resource-ledger.js (new modified file; establish the ledger interface from this task if absent)
    - tests/skopeo-shell-contract.test.js (new modified file; establish the shell contract from this plan if absent)
    - tests/skopeo-accessibility.test.js (new modified file; establish the accessibility contract from this plan if absent)
    - .planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-VALIDATION.md (Wave 0 ledger categories, quick suite, and T-52-02/T-52-03 gates)
    - .planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-UI-SPEC.md (all component geometry, exact copy, focus, roles, motion, collision, forced-colors, and reduced-motion values)
    - .planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-CONTEXT.md (D-04 through D-14)
    - tests/overlay-content-audit.test.js (real VM/DOM stub and hostile display-data assertions)
    - tests/overlay-stability-cadence.test.js (listener tracking, Shadow stub, deterministic timers, coalescing, and destroy checks)
    - extension/content/visual-feedback.js (single Shadow/top-layer and explicit destroy precedent)
    - extension/content/lifecycle.js (page lifecycle cleanup precedent)
    - extension/content/accessibility.js (accessibility helper and focus conventions)
  </read_first>
  <files>tests/helpers/skopeo-resource-ledger.js, tests/skopeo-shell-contract.test.js, tests/skopeo-accessibility.test.js</files>
  <action>
    Create a reusable Node resource ledger whose categories are exactly roots, listeners, observers, timeouts, intervals, animationFrames, animations, focusHooks, pointerSurfaces, pendingRenders, and `popoverTopLayer`. It must expose acquire/release, snapshot/diff, releaseAll, and assertEmpty, reject unknown categories and double-release, and include executable negative controls that deliberately leak one listener and one acquired `popoverTopLayer` handle, prove `assertEmpty` throws for each leak, then release them and prove the ledger returns to zero. This makes the top-layer state required by `52-VALIDATION.md` non-vacuous rather than inferring it from root removal.

    Create VM/DOM-based shell tests modeled on the two overlay analogs. The shell suite must pin: zero root before preparation; `prepareAmbient()` collision-checks and returns an opaque one-use placement while root and `popoverTopLayer` counts remain zero; only `mountAmbient(preparedPlacement)` inserts exactly one root/Shadow host; stale, reused, or foreign prepared placements fail closed; repeated mount still one; successful `showPopover()` acquires exactly one `popoverTopLayer` entry and exposes an open top-layer snapshot; fallback without popover support acquires none; the literal six-name registry with no seventh primitive; Ambient shows only lens+rail; fixture attention policies enforce halo scarcity, temporary ghosting, and one gate; pointer transparency for envelope/rail/halo/ghost/empty space; pointer enablement only on visible controls; deterministic corner order, 16px inset, 8px host-control clearance, 240x40 then 88x40 fallback, and fail-closed unsafe layout; no host body/html class/style/attribute/inert/aria-hidden/overflow/padding/position/transform/filter/selection/scroll diff; hostile strings such as `<img src=x onerror=...>` remain literal text with no executable node; and destroy/second destroy calls `hidePopover()` before host removal, proves the popover is no longer open, and returns all eleven ledger categories to baseline.

    The accessibility suite must pin every exact name/role/copy in UI-SPEC, one polite atomic live region, no hidden primitive in DOM or accessibility traversal, 500ms latest-wins live coalescing with terminal bypass, no focus move entering Ambient/Anchored, declared Focused/Gate order and alertdialog semantics, safe `focus({preventScroll:true})` restoration/fallback, one-level back, 600ms non-repeat/non-composing Escape rules at the shell boundary, 2px orange focus outline, 200%/narrow reflow, system-color forced-colors rules, and zero-duration reduced motion. Normal test mode must require production exports and never skip; `--self-test` may validate only the stubs, ledger negative control, and hostile-text oracle before production exists.

    These tests bind D-04/D-06 primitive policy, D-05 Ambient-only invocation, D-07 back behavior, D-10 one owner, D-11 pointer/host integrity, D-12 focus policy, D-13 accessibility/preferences, and D-14 teardown. Host interception (T-52-02) and executable page text (T-52-03) are blocking.
  </action>
  <verify>
    <automated>node tests/helpers/skopeo-resource-ledger.js --self-test &amp;&amp; node tests/skopeo-shell-contract.test.js --self-test &amp;&amp; node tests/skopeo-accessibility.test.js --self-test &amp;&amp; node --check tests/skopeo-shell-contract.test.js &amp;&amp; node --check tests/skopeo-accessibility.test.js</automated>
  </verify>
  <acceptance_criteria>
    - The deliberate listener and `popoverTopLayer` leak negative controls each throw and then return to zero.
    - Normal test paths require production shell exports and contain no skip/todo/placeholder success.
    - The host snapshot, hostile-text, pointer hit-testing, attention scarcity, focus, preferences, popover open/hide ordering, and second-destroy cases are concrete assertions.
  </acceptance_criteria>
  <done>Wave 0 provides executable negative controls and complete contracts for the shell's safety, grammar, accessibility, and resource ownership.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement one Shadow owner, Ambient surface, placement engine, and six-primitive policy</name>
  <read_first>
    - extension/content/skopeo-shell.js (new modified file; create the declared dual-export API if absent)
    - tests/skopeo-shell-contract.test.js (Task 1 shell contract)
    - tests/helpers/skopeo-resource-ledger.js (observable resource categories and negative controls)
    - extension/content/visual-feedback.js (ProgressOverlay Shadow root, top-layer/popover feature detection, pointer isolation, textContent updates, and destroy pattern)
    - extension/content/accessibility.js (existing semantic/focus helper conventions)
    - tests/overlay-content-audit.test.js (DOM stubs and text-only assertions)
    - .planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-UI-SPEC.md (Spacing through Placement, Pointer Policy, copy tables, and controlled fixture)
    - .planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-CONTEXT.md (D-04, D-05, D-06, D-10, D-11)
  </read_first>
  <files>extension/content/skopeo-shell.js</files>
  <action>
    Implement the declared classic-script IIFE. `prepareAmbient()` must read geometry, choose the 240x40/88x40 safe placement, and return a shell-owned opaque one-use placement without inserting a host, attaching a Shadow root, opening a popover, registering active listeners, or moving focus. `mountAmbient(preparedPlacement)` must reject stale/reused/foreign placements and only then insert one identifiable fixed `inset:0` host with one open Shadow root at the already validated placement; use a top-layer/manual-popover capability when supported without ever creating another owner. Acquire `popoverTopLayer` only after `showPopover()` succeeds, never for the z-index fallback, and retain the exact handle needed to hide and release that state during destroy. Keep the host and all geometry pointer-none and opt only visible controls into pointer-auto. Put all trusted static CSS inside the Shadow root; use only UI-SPEC spacing (4/8/16/24/32/48/64), type sizes/line heights (11/16, 12/16, 14/20, 16/24), colors, dimensions, 120ms/240ms timings, media queries, and exact copy. Render all variable/untrusted values through `textContent`/typed scalar setters, never `innerHTML`, `insertAdjacentHTML`, scriptable URLs, event attributes, or host-provided markup.

    Committed mount creates the 240x40 (or safe 88x40 compact) `Skopeo · Ambient` region, 32x32 `Turn off Skopeo` control, rail/status group, and one polite atomic live region only. Use candidate order top-right/top-left/bottom-right/bottom-left, 16px edge inset, 8px clearance, the specified interactive selectors plus focused element and scrollbar zone, reevaluation on resize/zoom, and fail preparation without inserting a root when neither lens fits. The shared renderer owns exactly anchor/chip/halo/rail/ghost/gate and enforces the UI-SPEC attention allowlists. Richer states require `enableControlledFixture` with an opaque test/dev token and use only shell-owned sample nodes/exact fixture copy; ordinary mount exposes no production advance control.

    This implements D-04 one contract, D-05 Ambient-only normal entry, D-06 scarcity by attention, D-10 one dynamic Shadow owner, and D-11 no host mutation/pointer interception. It blocks T-52-02 with fail-closed placement and T-52-03 with text-only sinks.
  </action>
  <verify>
    <automated>node tests/skopeo-shell-contract.test.js</automated>
  </verify>
  <acceptance_criteria>
    - The shell suite proves prepare leaves root/top-layer counts at zero, commit consumes only the matching prepared placement, and exact owner/primitive counts, Ambient-only normal mount, attention allowlists, collision fallback, and unsafe-layout refusal all hold.
    - Host snapshots and hostile-text assertions pass.
    - No Skopeo symbol/file is added to static or automation content-script bundles.
  </acceptance_criteria>
  <done>The host-safe shared shell renders only permitted primitives and cannot cover a required host control or execute host-controlled display text.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Complete focus, preference, Escape, and owned-resource teardown behavior</name>
  <read_first>
    - extension/content/skopeo-shell.js (modified in this task; review all resource acquisition and render paths from Task 2)
    - tests/skopeo-accessibility.test.js (Task 1 accessibility contract)
    - tests/skopeo-shell-contract.test.js (host integrity and teardown contract)
    - tests/helpers/skopeo-resource-ledger.js (all categories that must balance)
    - extension/content/visual-feedback.js (named listener removal, timer cancellation, focus-safe destroy precedent)
    - extension/content/lifecycle.js (pagehide/navigation cleanup precedent)
    - extension/content/accessibility.js (screen-reader and focusability conventions)
    - tests/overlay-stability-cadence.test.js (listener identity, pending timeout, and destroy regression patterns)
    - .planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-UI-SPEC.md (Keyboard/Focus, Roles/Names, Forced Colors, Motion, Teardown, exact fixture copy)
    - .planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-CONTEXT.md (D-07, D-12, D-13, D-14)
  </read_first>
  <files>extension/content/skopeo-shell.js</files>
  <action>
    Finish the shell-owned attention stack and resource registry. A visible close or one applicable Escape calls `back()`: Interstitial to Focused, Focused to Anchored, Anchored to Ambient, and Ambient requests complete shutdown. Ignore composing/repeated Escape and do not suppress any host key unless a visible Skopeo state actually consumes it. Let Plan 03 own the second-Escape universal session kill; the shell reports consumed timestamp/state without installing a second global listener.

    Ambient/Anchored never steal focus. Before Focused/Interstitial store the actual origin; Focused moves to its `tabindex=-1` title and orders back/action/turn-off; Interstitial is one `role=alertdialog`/`aria-modal=true` gate with safe return first and traps Tab only while visible. Restore with `focus({preventScroll:true})` only when connected, visible, enabled, and focusable; otherwise use the preceding Skopeo trigger and never body/host scrolling. Remove hidden primitives from DOM, coalesce live updates at 500ms except terminal/error, implement exact focus-visible, zoom, forced-color, contrast, and reduced-motion rules, and never use color/glow alone for meaning.

    Centralize acquisition wrappers for roots, exact listener functions/options, observers, timers, intervals, animation frames, Web Animations, focus hooks, pointer-enabled surfaces, pending renders, and `popoverTopLayer`. `destroy()` first marks the instance disposed, synchronously cancels pending work/animations, calls `hidePopover()` for an acquired/open top-layer host before removing it, verifies/releases the `popoverTopLayer` handle even when hide throws or removal is the fallback, removes every other acquired resource, restores eligible focus, removes the sole host/live node, clears references, and returns an immutable eleven-category zero snapshot. A second call is a no-op and cannot double-release the popover handle. D-07/D-12/D-13/D-14 are implemented here; T-52-02, T-52-04, and T-52-05 are mitigated by the executable ledger and focus tests.
  </action>
  <verify>
    <automated>node tests/skopeo-accessibility.test.js &amp;&amp; node tests/skopeo-shell-contract.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Exact roles, names, copy, focus order/restoration, live-region cadence, preferences, and back ladder pass.
    - Destroy and repeated destroy leave all eleven ledger categories, including `popoverTopLayer`, at baseline and no host DOM/accessibility/style diff.
    - No endless animation, broad host event suppression, host `inert`/`aria-hidden`, or focus-to-body fallback exists.
  </acceptance_criteria>
  <done>The shell is keyboard/screen-reader operable and its synchronous idempotent destroy leaves zero owned or host residue.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shadow shell to host page | Extension UI shares the viewport with untrusted, interactive host content. |
| Host/display payload to shell renderer | Page-derived strings may contain HTML/script payloads. |
| Skopeo focus manager to document focus | Temporary focused/gate states may move focus but must restore safely. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-01 | Tampering | shell pending renders | mitigate (blocking) | Every queued render checks `disposed` and generation; destroy clears pending renders before root removal. |
| T-52-02 | Denial of service | envelope, placement, pointer policy | mitigate (blocking) | Pointer-none geometry, visible-control opt-in, collision rejection, host snapshots, and unsafe-layout fail-closed tests. |
| T-52-03 | Elevation of privilege | shell text renderer | mitigate (blocking) | Typed model plus `textContent` only; hostile HTML/event payload test proves no executable node. |
| T-52-04 | Denial of service | focused/gate focus handling | mitigate | Trap only the visible gate, validate restoration target, preserve scroll, and test detach/disable fallbacks. |
| T-52-05 | Denial of service | root/listener/resource ownership | mitigate | One owner plus non-vacuous eleven-category ledger, explicit popover open/hide/release evidence, and idempotent destroy assertions. |
</threat_model>

<verification>
Run `node tests/helpers/skopeo-resource-ledger.js --self-test &amp;&amp; node tests/skopeo-shell-contract.test.js &amp;&amp; node tests/skopeo-accessibility.test.js`. T-52-02 and T-52-03 are blocking and must report zero failures; the shell contract must observe `popoverTopLayer` transition 0 -> 1 -> 0 on a supported mock and remain 0 on fallback.
</verification>

<success_criteria>
- Ordinary invocation has exactly one Ambient shell with lens and rail.
- The controlled fixture proves the exact six primitives and four attention policies without touching host nodes.
- Pointer, hostile-text, accessibility, preference, focus, and ledger tests all pass.
</success_criteria>

<output>
After completion, create `.planning/phases/52-on-demand-hud-lifecycle-primitive-shell/52-02-SUMMARY.md` with the shell API, primitive/attention matrix, resource ledger, UI-SPEC evidence, and blocking threat results.
</output>
