---
phase: 30-consent-governance-recipe-signature-verification-audit-legal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/consent-policy-store.test.js
  - tests/consent-gate.test.js
  - tests/consent-mutation-gate.test.js
  - tests/consent-chokepoint.test.js
  - tests/audit-log.test.js
  - tests/audit-log-no-secret.test.js
  - tests/recipe-signature.test.js
  - tests/recipe-signature-interpreter-hook.test.js
  - tests/service-denylist.test.js
  - tests/consent-audit-settings-ui.test.js
  - catalog/recipes/_fixtures/signature/sign-fixtures.mjs
  - catalog/recipes/_fixtures/signature/fixture-public-key.json
  - catalog/recipes/_fixtures/signature/signed-recipe.json
  - catalog/recipes/_fixtures/signature/tampered-recipe.json
  - scripts/verify-recipe-path-guard.mjs
  - extension/background.js
  - package.json
autonomous: true
requirements: [GOV-01, GOV-02, GOV-03, GOV-04, GOV-05, GOV-06, GOV-07, GOV-08, SIGN-01, SIGN-02]
must_haves:
  truths:
    - "Ten Phase-30 test files exist and run under `node tests/<name>.test.js` (each fails or asserts MISSING-module until its Wave-1/2 module lands), establishing the Nyquist contract before any production code"
    - "A fixture Ed25519 keypair exists; a fixture recipe is signed (verify-pass) and a one-byte-tampered copy is produced (verify-fail), generated reproducibly by a committed script (D-08)"
    - "RECIPE_PATH_ALLOWLIST contains capability-signature.js, consent-policy-store.js, and audit-log.js so the recipe-path guard Check 4/5 stays green and fails closed the moment those modules land"
    - "background.js importScripts the three new modules (pre-armed, try/catch-guarded) and the denylist load runs at SW startup"
    - "All ten new tests are wired into the package.json `scripts.test` chain at the tail next to capability-*.test.js"
  artifacts:
    - path: "tests/consent-gate.test.js"
      provides: "GOV-01 default-OFF blocks; chokepoint placement (RED until Plan 02)"
    - path: "tests/recipe-signature.test.js"
      provides: "SIGN-01 + D-07 fixture sign/verify/tamper + provenance exemption (RED until Plan 03)"
    - path: "catalog/recipes/_fixtures/signature/fixture-public-key.json"
      provides: "fixture trusted public key for the signature tests (D-08)"
    - path: "scripts/verify-recipe-path-guard.mjs"
      provides: "allowlist pre-arm for the three new recipe-path modules"
  key_links:
    - from: "package.json scripts.test"
      to: "tests/consent-*.test.js + tests/audit-*.test.js + tests/recipe-signature*.test.js + tests/service-denylist.test.js + tests/consent-audit-settings-ui.test.js"
      via: "&& chain append at the tail"
      pattern: "consent-gate\\.test\\.js"
    - from: "scripts/verify-recipe-path-guard.mjs RECIPE_PATH_ALLOWLIST"
      to: "extension/utils/capability-signature.js"
      via: "allowlist array entry"
      pattern: "capability-signature\\.js"
---

<objective>
Wave 0: lay the full validation + safety scaffold for Phase 30 BEFORE any production code, so every later wave is sampled. Create the ten Phase-30 test files (each currently RED or asserting its target module is MISSING), the signed/tampered recipe fixtures + a fixture Ed25519 public key, pre-arm RECIPE_PATH_ALLOWLIST with the three new recipe-path modules, pre-arm background.js importScripts for those modules + the denylist load, and wire all ten tests into the npm test chain.

Purpose: This is the Nyquist contract for the security phase (per 30-VALIDATION.md). Wave 0 must exist first so Plans 02/03/04 turn RED tests GREEN rather than authoring tests after the fact. The allowlist + background.js pre-arm follows the established Phase-27/28/29 "register ahead of creation" precedent so the recipe-path guard stays green now and fails closed the instant a not-yet-eval-free module lands.
Output: 10 test files, a fixture-signing script + 3 fixture artifacts, an updated recipe-path guard allowlist, updated background.js wiring, an updated package.json test chain.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-consent-governance-recipe-signature-verification-audit-legal/30-VALIDATION.md
@.planning/phases/30-consent-governance-recipe-signature-verification-audit-legal/30-RESEARCH.md
@.planning/phases/30-consent-governance-recipe-signature-verification-audit-legal/30-UI-SPEC.md

<interfaces>
<!-- The contracts the later-wave modules WILL export. Tests target these now so they go RED until the module lands, then GREEN when Plans 02/03/04 implement to this exact surface. Names under Claude's-discretion are LOCKED here so all four plans agree. -->

consent-policy-store.js (Plan 02) — CommonJS + globalThis.FsbConsentPolicyStore:
  STORAGE_KEY = 'fsbConsentPolicies'   PAYLOAD_VERSION = 1
  envelope shape: { v:1, defaultMode:'off', policies: { [origin]: { mode:'off'|'ask'|'auto', mutating:boolean } } }
  getConsentForOrigin(envelope, origin) -> { mode, mutating }   // DEFAULT mode 'off', mutating false
  readPolicies() -> Promise<envelope>                            // chrome.storage.local; null-safe
  setOriginMode(origin, mode) -> Promise<void>                   // mode in off|ask|auto
  setOriginMutating(origin, allowed) -> Promise<void>
  _reset()                                                       // test hook

audit-log.js (Plan 02) — CommonJS + globalThis.FsbAuditLog:
  STORAGE_KEY = 'fsbAuditLog'   PAYLOAD_VERSION = 1   MAX_ENTRIES = 200
  append(entry) -> Promise<void>      // field-WHITELIST: { ts, origin, slug, method, sideEffectClass, consentDecision, outcome, error? }
  getEntries(opts) -> Promise<{ entries, clearedAt? }>   // opts.clear === true clears
  _reset()

FsbConsentGate (Plan 02; lives in capability-router.js or a sibling reached by invoke):
  async evaluate({ origin, slug, method, entry }) ->
    { decision:'allow' } | { decision:'off'|'ask'|'mutating'|'blocked'|'sensitive', method, sideEffectClass, error }
  where error is the dual-field RECIPE_CONSENT_* object (see error codes below)

capability-signature.js (Plan 03) — CommonJS + globalThis.FsbCapabilitySignature:
  async verifyEd25519(pubKeyRaw:Uint8Array, sigBytes:Uint8Array, msgBytes:Uint8Array) -> Promise<boolean>   // native-first + feature-detect, fail-closed
  jcsCanonicalize(value) -> string                          // RFC 8785 closed-shape serializer; throws on non-integer number
  async verifyRecipeEnvelope(envelope) -> Promise<{ ok:boolean, reason? }>
    // envelope = { recipe:<schema-valid core>, provenance:'bundled'|'server'|'learned', signature?, capturedAt?, schemaHash? }
    // provenance==='bundled' -> { ok:true } (EXEMPT, D-07); else canonicalize(recipe core + capturedAt + schemaHash, MINUS signature) and Ed25519-verify
  TRUSTED_PUBLIC_KEYS  // array of raw base64 keys shipped in the bundle (fixture key injected in tests)

service-denylist.js (Plan 03) — CommonJS + globalThis.FsbServiceDenylist:
  isDenied(origin) -> { denied:boolean, reason? }
  load() -> Promise<void>   // reads extension/config/service-denylist.json

Error codes (LOCKED — RESEARCH Pitfall 4 Option A; all match /^RECIPE_.+$/ so they surface verbatim with ZERO errors.ts edit):
  RECIPE_CONSENT_REQUIRED            // origin Off or Ask-not-yet-granted
  RECIPE_CONSENT_BLOCKED             // denylisted origin (non-enableable)
  RECIPE_CONSENT_MUTATING_REQUIRED   // POST/PUT/PATCH/DELETE on a read-only-consented origin
  RECIPE_SIGNATURE_INVALID           // non-bundled recipe failed/absent signature verify
</interfaces>

# Analog test patterns to clone (read for structure, not content):
@tests/diagnostics-ring-buffer.test.js
@tests/change-report-settings-ui.test.js
@tests/capability-interpreter.test.js
@scripts/verify-recipe-path-guard.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the fixture-signing script and the three signature fixtures</name>
  <read_first>
    - extension/utils/capability-recipe-schema.js:79-148 (the locked recipe core schema — additionalProperties:false; required schemaVersion/id/origin/endpoint/method/authStrategy — the fixture recipe core MUST validate against this)
    - catalog/recipes/_fixtures/valid-github-notifications.json (an existing valid recipe core to base the fixture on)
    - .planning/phases/30-.../30-RESEARCH.md §Code Examples + Pitfall 3 + Open Question 1 (the signed-payload scope: recipe core + capturedAt + schemaHash, MINUS the signature field; canonicalize via JCS)
    - .planning/phases/30-.../30-RESEARCH.md "Test-harness note" (Node globalThis.crypto.subtle supports Ed25519 generateKey/sign/verify natively — no chrome stub needed for crypto)
  </read_first>
  <action>
    Create catalog/recipes/_fixtures/signature/sign-fixtures.mjs — a reproducible Node ESM script that: (1) generates an Ed25519 keypair via globalThis.crypto.subtle.generateKey({name:'Ed25519'}, true, ['sign','verify']); (2) builds the signed ENVELOPE shape `{ recipe:<schema-valid core>, provenance:'server', capturedAt:<ISO string>, schemaHash:<sha-256 hex of the canonical recipe core> }`; (3) canonicalizes the signed payload (the recipe core plus capturedAt and schemaHash, EXCLUDING the signature field) using a local copy of the same JCS serializer logic Plan 03 ships, so the fixture and the runtime serializer agree byte-for-byte; (4) signs the canonical bytes and writes the raw public key (base64) to fixture-public-key.json, the full signed envelope (signature base64 included) to signed-recipe.json, and a tampered copy to tampered-recipe.json where exactly ONE byte of one signed field (e.g. flip a character in recipe.endpoint) is changed while the signature is left intact. The recipe core MUST be a valid recipe per capability-recipe-schema.js (base it on valid-github-notifications.json: schemaVersion 1, a same-origin-cookie GET). Add a header comment naming the signed-payload scope verbatim (recipe core + capturedAt + schemaHash, minus signature) so Plan 03 binds to the identical contract. Run the script once to emit the three JSON artifacts and commit them. The script is test tooling under catalog/recipes/_fixtures/ — it is NOT shipped runtime and NOT on the recipe-path allowlist (it is an .mjs in the fixtures tree, like the existing reject-*.json fixtures are test data).
  </action>
  <acceptance_criteria>
    - `node catalog/recipes/_fixtures/signature/sign-fixtures.mjs` exits 0 and (re)writes fixture-public-key.json, signed-recipe.json, tampered-recipe.json.
    - signed-recipe.json parses as `{ recipe, provenance:'server', capturedAt, schemaHash, signature }`; its `recipe` validates against the recipe schema (no extra fields inside `recipe`).
    - tampered-recipe.json differs from signed-recipe.json in exactly one signed-field character, signature byte-identical.
    - fixture-public-key.json contains a base64 raw Ed25519 public key.
  </acceptance_criteria>
  <verify>
    <automated>node catalog/recipes/_fixtures/signature/sign-fixtures.mjs && node -e "const s=require('./catalog/recipes/_fixtures/signature/signed-recipe.json'); const t=require('./catalog/recipes/_fixtures/signature/tampered-recipe.json'); const k=require('./catalog/recipes/_fixtures/signature/fixture-public-key.json'); if(!s.signature||!s.recipe||s.provenance!=='server') throw new Error('signed envelope shape'); if(s.signature!==t.signature) throw new Error('signature must be identical between signed and tampered'); if(JSON.stringify(s.recipe)===JSON.stringify(t.recipe)) throw new Error('tampered recipe core must differ'); if(typeof k.publicKey!=='string') throw new Error('public key'); console.log('fixtures OK');"</automated>
  </verify>
  <done>The signing script runs reproducibly and emits a signed envelope, a one-byte-tampered envelope (signature intact), and a fixture public key; the recipe core validates against the locked schema.</done>
</task>

<task type="auto">
  <name>Task 2: Create the ten Phase-30 test files (RED / MISSING-asserting)</name>
  <read_first>
    - .planning/phases/30-.../30-VALIDATION.md §Per-Task Verification Map + §Wave 0 Requirements (the ten files + the exact behavior each asserts, keyed by requirement)
    - .planning/phases/30-.../30-UI-SPEC.md §Acceptance Criteria (the machine-checkable strings/ids the consent-audit-settings-ui.test.js asserts)
    - tests/diagnostics-ring-buffer.test.js (the in-memory chrome.storage.local stub + passed/failed counter idiom to clone for the store/audit tests)
    - tests/change-report-settings-ui.test.js (the source-text `ok(/regex/.test(html|opts))` idiom to clone for consent-audit-settings-ui.test.js)
    - tests/capability-interpreter.test.js:40-138 (the vm.runInThisContext + require() module-load idiom + interpretRecipe call shape for the signature-hook test)
    - extension/utils/capability-fetch.js:228 (MUTATING_METHODS — the set the mutation-gate test references)
  </read_first>
  <action>
    Create all ten test files listed in 30-VALIDATION.md §Wave 0, each a zero-framework Node script (passed/failed counters, check/ok helper, process.exit(failed>0?1:0)), wired to the LOCKED interfaces in this plan's <context>. Because the production modules do not exist yet, each test must FAIL LOUDLY or assert-MISSING in a way that turns GREEN only when its Wave-1/2 module lands — never silently pass. Concretely: (1) tests/consent-policy-store.test.js — require FsbConsentPolicyStore; assert getConsentForOrigin on an empty envelope returns {mode:'off',mutating:false} (default-OFF, GOV-02); Off/Ask/Auto round-trip through setOriginMode + readPolicies using the diagnostics-ring chrome stub; assert there is NO global enable key. (2) tests/consent-gate.test.js — drive FsbConsentGate.evaluate for an unseen origin and assert decision!=='allow' with error.code==='RECIPE_CONSENT_REQUIRED' and that no executeBoundSpec ran (GOV-01). (3) tests/consent-mutation-gate.test.js — on an origin set to read-Auto, a POST slug yields RECIPE_CONSENT_MUTATING_REQUIRED; after setOriginMutating(origin,true) it is allowed (GOV-03, read-Auto != write-Auto via MUTATING_METHODS). (4) tests/consent-chokepoint.test.js — assert the gate runs once inside router.invoke AFTER ownership (an ownership-denied path never reaches consent) and that BOTH front-door call shapes (the {origin,tabId} and {origin,tabId,source:'autopilot'} ctx) reach the SAME gate (parity, in the capability-autopilot-parity.test.js spirit). (5) tests/audit-log.test.js — append an entry; assert the persisted record has EXACTLY the keys {ts,origin,slug,method,sideEffectClass,consentDecision,outcome} (+ optional error) and FIFO-trims at MAX_ENTRIES (GOV-05). (6) tests/audit-log-no-secret.test.js — append entries whose source objects carry cookie/token/csrf/xoxc/_gh_sess/bearer/authorization substrings in non-whitelisted fields; assert NONE of those substrings appears anywhere in JSON.stringify(getEntries()) (GOV-06 security assertion). (7) tests/recipe-signature.test.js — load FsbCapabilitySignature; inject the fixture public key into TRUSTED_PUBLIC_KEYS; verifyRecipeEnvelope(signed-recipe.json) resolves ok:true; verifyRecipeEnvelope(tampered-recipe.json) resolves ok:false; an envelope with provenance:'bundled' returns ok:true WITHOUT calling verify (D-07 exemption, assert via a verify spy/counter); a non-bundled envelope with an absent signature returns ok:false (fail-closed). (8) tests/recipe-signature-interpreter-hook.test.js — assert interpretRecipe rejects a non-bundled tampered envelope with RECIPE_SIGNATURE_INVALID, and that the hook fires AFTER schema-validate and BEFORE bind (a schema-invalid recipe still returns its schema error, not the signature error; a bundled recipe binds without a verify call) (SIGN-02). (9) tests/service-denylist.test.js — assert extension/config/service-denylist.json has the {v,deniedOrigins,deniedReason} shape with a non-empty conservative seed; FsbServiceDenylist.isDenied(a-seeded-origin).denied===true; and the gate returns RECIPE_CONSENT_BLOCKED for a denylisted origin BEFORE any per-origin policy is consulted (GOV-08, checked-first); also assert docs/LEGAL.md exists and contains retention + consent sections. (10) tests/consent-audit-settings-ui.test.js — read control_panel.html + options.js as text and assert the full 30-UI-SPEC §Acceptance Criteria surface (data-section="consent-audit"; ids consentOriginList/pendingRequestList/auditLogTable/auditExportBtn/auditClearBtn; the seven audit column headers; the Off/Ask/Auto data-mode buttons; the "Allow mutating (write) calls" toggle; the load-bearing copy strings; the NO-secret-column string-absence assertion for args/body/cookie/token/csrf/authorization/bearer; the options.js cacheElements + click-handler wiring; no-emoji). Each test must run standalone today; the ones whose module is absent FAIL (require throws or the assertion fails) — that is the intended RED state, recorded in the SUMMARY.
  </action>
  <acceptance_criteria>
    - All ten files exist and are syntactically valid Node (`node --check` passes on each).
    - tests/recipe-signature.test.js passes TODAY (its dependency, the fixtures + Node crypto, exist) once Plan 03 lands; until then it fails on the missing FsbCapabilitySignature require — never silently passes.
    - Each store/gate/audit/signature/denylist test references the LOCKED interface names from this plan's <context> (FsbConsentPolicyStore, FsbAuditLog, FsbConsentGate, FsbCapabilitySignature, FsbServiceDenylist) and the LOCKED error codes (RECIPE_CONSENT_* / RECIPE_SIGNATURE_INVALID).
    - consent-audit-settings-ui.test.js asserts every bullet in 30-UI-SPEC §Acceptance Criteria.
  </acceptance_criteria>
  <verify>
    <automated>for f in consent-policy-store consent-gate consent-mutation-gate consent-chokepoint audit-log audit-log-no-secret recipe-signature recipe-signature-interpreter-hook service-denylist consent-audit-settings-ui; do node --check "tests/$f.test.js" || exit 1; done && echo "all 10 test files parse"</automated>
  </verify>
  <done>Ten zero-framework test files exist, parse, and encode the GOV-01..08 + SIGN-01/02 contracts against the locked interfaces; modules-not-yet-present tests are RED (fail loudly), not silently green.</done>
</task>

<task type="auto">
  <name>Task 3: Pre-arm the allowlist, background.js wiring, and the package.json test chain</name>
  <read_first>
    - scripts/verify-recipe-path-guard.mjs:90-130 (RECIPE_PATH_ALLOWLIST array + the Phase-27/28/29 "registered ahead of creation" comments — Check 1 existsSync-skips an absent path, Check 4 fails closed on a disk capability-*.js NOT on the list)
    - extension/background.js:122-174 (the capability-* importScripts block — every load is wrapped in try/catch so a not-yet-existent module logs and is skipped; this is what makes pre-arm safe)
    - package.json:17 (the `scripts.test` && chain; the tail is ...capability-head-handlers.test.js && node tests/agent-loop-iterator-guard.test.js)
  </read_first>
  <action>
    Three additive edits. (1) In scripts/verify-recipe-path-guard.mjs, add 'extension/utils/capability-signature.js', 'extension/utils/consent-policy-store.js', and 'extension/utils/audit-log.js' to RECIPE_PATH_ALLOWLIST, each with a one-line comment citing Phase 30 D-05/D-02/D-09 and the "registered ahead of creation; Check 1 skips an absent path, Check 4 fails closed when it lands" precedent. capability-signature.js is named capability-* so Check 4's disk glob auto-covers it (RESEARCH Pitfall 5); consent-policy-store.js and audit-log.js do NOT match the glob so they MUST be listed explicitly (RESEARCH Pitfall 6). Keep the allowlist comment block free of the literal forbidden tokens. (2) In extension/background.js, after the capability-router.js importScripts (line ~174), add three try/catch-guarded importScripts in dependency order: utils/consent-policy-store.js, utils/audit-log.js, utils/capability-signature.js — and add the denylist load (utils/service-denylist.js then a call to its load()) so the denylist is read at SW startup BEFORE the gate evaluates (D-15). Each importScripts mirrors the existing `try { importScripts('utils/x.js'); } catch (e) { console.error('[FSB] Failed to load x.js:', e.message); }` shape; a missing module (until Plans 02/03 land) logs and is skipped — the SW still boots. (3) In package.json `scripts.test`, append the ten new tests to the && chain at the tail, AFTER capability-head-handlers.test.js and BEFORE/AFTER agent-loop-iterator-guard.test.js (keep the iterator guard runnable), in this order: consent-policy-store, consent-gate, consent-mutation-gate, consent-chokepoint, audit-log, audit-log-no-secret, recipe-signature, recipe-signature-interpreter-hook, service-denylist, consent-audit-settings-ui. Do NOT reorder or remove any existing entry.
  </action>
  <acceptance_criteria>
    - `node scripts/verify-recipe-path-guard.mjs` still exits 0 (the three pre-armed paths are absent on disk, so Check 1 existsSync-skips them; Check 4 finds no un-allowlisted capability-*.js).
    - RECIPE_PATH_ALLOWLIST contains all three new module paths.
    - background.js contains try/catch importScripts for consent-policy-store.js, audit-log.js, capability-signature.js, and service-denylist.js + its load() call.
    - package.json `scripts.test` contains all ten new test filenames; the chain still parses (`node -e "require('./package.json').scripts.test"`).
  </acceptance_criteria>
  <verify>
    <automated>node scripts/verify-recipe-path-guard.mjs && node -e "const g=require('fs').readFileSync('scripts/verify-recipe-path-guard.mjs','utf8'); ['capability-signature.js','consent-policy-store.js','audit-log.js'].forEach(m=>{if(g.indexOf(m)===-1)throw new Error('allowlist missing '+m)}); const t=require('./package.json').scripts.test; ['consent-policy-store','consent-gate','consent-mutation-gate','consent-chokepoint','audit-log','audit-log-no-secret','recipe-signature','recipe-signature-interpreter-hook','service-denylist','consent-audit-settings-ui'].forEach(n=>{if(t.indexOf('tests/'+n+'.test.js')===-1)throw new Error('test chain missing '+n)}); const bg=require('fs').readFileSync('extension/background.js','utf8'); ['consent-policy-store.js','audit-log.js','capability-signature.js','service-denylist.js'].forEach(m=>{if(bg.indexOf(m)===-1)throw new Error('background.js missing importScripts '+m)}); console.log('pre-arm OK');"</automated>
  </verify>
  <done>The recipe-path guard is green with the three modules pre-armed; background.js loads them + the denylist at startup (guarded); all ten tests are in the npm test chain with no existing entry disturbed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → runtime verify | The fixture keypair must NEVER become a trusted production key. fixture-public-key.json is test-only; the bundled trusted key ships separately in Plan 03 and is injected/replaced in tests. |
| recipe-path module set → CI guard | Any new module reached from interpretRecipe/invoke must be eval-free + allowlisted, or the build must fail closed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Tampering | recipe-path modules (capability-signature/consent-policy-store/audit-log) | mitigate | Pre-arm RECIPE_PATH_ALLOWLIST in this wave so Check 1 eval-scans them and Check 4 fails closed the moment they land (RESEARCH Pitfall 5/6). |
| T-30-02 | Spoofing | fixture keypair leaking into production trust | accept | Fixture key lives only under catalog/recipes/_fixtures/signature/; tests inject it; the bundled trusted key (Plan 03) is a distinct artifact. No production code references the fixture path. |
| T-30-03 | Repudiation | tests silently passing before modules exist | mitigate | Every module-dependent test fails loudly (require throws / assertion fails) until its Wave-1/2 module lands — the RED state is recorded; no `== 0`-on-unfiltered gate. |
| T-30-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs ZERO packages (native Web Crypto + in-house JCS; the noble fallback is deferred per RESEARCH Open Question 2). No [ASSUMED]/[SUS] package vendoring occurs in Wave 0. |
</threat_model>

<verification>
- `node catalog/recipes/_fixtures/signature/sign-fixtures.mjs` emits the three fixtures reproducibly.
- All ten test files `node --check` clean.
- `node scripts/verify-recipe-path-guard.mjs` exits 0 with the three modules pre-armed.
- `node tests/recipe-signature.test.js` is the canary: it depends only on the fixtures + Node crypto + (Plan 03's) FsbCapabilitySignature, so it flips GREEN as soon as Plan 03 lands.
- package.json `scripts.test` parses and contains all ten new tests.
</verification>

<success_criteria>
- Ten Phase-30 test files exist, parse, and encode the GOV-01..08 + SIGN-01/02 contracts against the locked interface names + error codes.
- A reproducible signing script + a signed envelope, a one-byte-tampered envelope (signature intact), and a fixture public key exist.
- RECIPE_PATH_ALLOWLIST is pre-armed with capability-signature.js + consent-policy-store.js + audit-log.js; the guard stays green.
- background.js pre-arms the three importScripts + the denylist load.
- All ten tests are in the npm test chain; no existing chain entry is reordered or removed.
</success_criteria>

<output>
Create `.planning/phases/30-consent-governance-recipe-signature-verification-audit-legal/30-01-SUMMARY.md` when done.
</output>
