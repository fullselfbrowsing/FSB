---
phase: 30-consent-governance-recipe-signature-verification-audit-legal
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - extension/utils/consent-policy-store.js
  - extension/utils/audit-log.js
  - extension/utils/capability-router.js
autonomous: true
requirements: [GOV-01, GOV-02, GOV-03, GOV-04, GOV-05, GOV-06, GOV-07]
must_haves:
  truths:
    - "Capability execution is default-OFF per origin: an origin with no stored policy returns RECIPE_CONSENT_REQUIRED from invoke and runs NO executeBoundSpec (GOV-01, D-02)"
    - "Per-origin consent is Off / Ask / Auto, Auto is an explicit per-origin opt-in with no global enable key (GOV-02, D-02)"
    - "A POST/PUT/PATCH/DELETE slug on a read-Auto origin returns RECIPE_CONSENT_MUTATING_REQUIRED until a SEPARATE elevated per-origin mutating opt-in is set (GOV-03, D-04)"
    - "A SENSITIVE origin (per FsbServiceDenylist.classify) under stored mode 'auto' is downgraded to ask at the GATE: invoke returns RECIPE_CONSENT_REQUIRED with consentDecision 'sensitive' and runs NO executeBoundSpec — Auto NEVER silently executes against a sensitive origin (GOV-07, D-14)"
    - "The consent gate runs at the single FsbCapabilityRouter.invoke chokepoint, AFTER the upstream ownership gate, reached identically by both front doors (GOV-04, D-01) — INV-02 one-gate-both-doors preserved"
    - "Every invoke outcome appends an append-only audit entry with EXACTLY {ts,origin,slug,method,sideEffectClass,consentDecision,outcome,error?} and never args/tokens/bodies (GOV-05, D-10)"
    - "Every audit field passes through redactForLog and no auth substring (cookie/token/csrf/xoxc/_gh_sess/bearer/authorization) survives in the persisted ring (GOV-06, D-11)"
    - "The gate sits ABOVE executeBoundSpec — the two-point origin-pin and INV-01/INV-04 are untouched"
  artifacts:
    - path: "extension/utils/consent-policy-store.js"
      provides: "Per-origin Off/Ask/Auto + elevated mutating envelope over chrome.storage.local; default OFF"
      exports: ["getConsentForOrigin", "readPolicies", "setOriginMode", "setOriginMutating"]
      contains: "defaultMode"
      min_lines: 80
    - path: "extension/utils/audit-log.js"
      provides: "Append-only redacted ring; field-whitelist entry schema; export/clear"
      exports: ["append", "getEntries"]
      contains: "fsbAuditLog"
      min_lines: 80
    - path: "extension/utils/capability-router.js"
      provides: "invoke wrapped by the consent + sensitive + mutation gate (FsbConsentGate.evaluate) + audit append on every outcome"
      contains: "RECIPE_CONSENT_REQUIRED"
  key_links:
    - from: "extension/utils/capability-router.js invoke()"
      to: "FsbConsentGate.evaluate"
      via: "gate call after catalog.resolve, before switch(entry.tier)"
      pattern: "evaluate\\("
    - from: "extension/utils/capability-router.js"
      to: "FsbServiceDenylist.isDenied + FsbServiceDenylist.classify"
      via: "typeof-guarded global read; isDenied checked BEFORE per-origin policy (block); classify().sensitive consumed at step 4 for the sensitive+Auto downgrade (degrade to not-denied/not-sensitive when absent)"
      pattern: "isDenied|classify"
    - from: "extension/utils/capability-router.js"
      to: "FsbAuditLog.append"
      via: "append on every invoke outcome (blocked + allowed)"
      pattern: "append\\("
    - from: "extension/utils/audit-log.js"
      to: "globalThis.redactForLog"
      via: "every field redacted before persistence"
      pattern: "redactForLog"
---

<objective>
Wave 1 leaf + wiring (consent half): ship the per-origin consent policy store (default-OFF Off/Ask/Auto + a separate elevated mutating opt-in), the append-only secret-free audit ring, and wrap FsbCapabilityRouter.invoke in the consent + sensitive + mutation gate so the single chokepoint both front doors reach enforces consent immediately after the existing ownership gate. Every invoke outcome — blocked or allowed — appends a redacted audit entry.

Purpose: This is the credential-replay safety spine (D-01..D-04, D-09..D-12, D-14). Default-OFF per origin means nothing runs against an origin until the user explicitly enables it; mutating needs a second elevated opt-in; and a SENSITIVE origin (bank/email/gov per the Plan-03 classifier) under Auto is downgraded to ask AT THE GATE so Auto never silently fires a credentialed call against a sensitive service (GOV-07/D-14 enforced where credential-replay is actually prevented, not just in the UI). The audit log records the decision/outcome with zero secrets. The gate is a wrapper that preserves the invoke return contract (typed RECIPE_CONSENT_* dual-field errors that surface verbatim), so callers, the errors.ts passthrough, and the Phase-29 origin-pin are all unchanged.
Output: extension/utils/consent-policy-store.js (NEW), extension/utils/audit-log.js (NEW), extension/utils/capability-router.js (MODIFIED — gate wrap + sensitive downgrade + audit).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-consent-governance-recipe-signature-verification-audit-legal/30-RESEARCH.md
@.planning/phases/30-consent-governance-recipe-signature-verification-audit-legal/30-CONTEXT.md

<interfaces>
<!-- LOCKED contracts (must match 30-01-PLAN.md <interfaces> exactly — the Wave-0 tests target these). -->

consent-policy-store.js — CommonJS module.exports + globalThis.FsbConsentPolicyStore:
  STORAGE_KEY = 'fsbConsentPolicies'   PAYLOAD_VERSION = 1
  envelope: { v:1, defaultMode:'off', policies: { [origin]: { mode:'off'|'ask'|'auto', mutating:boolean } } }
  getConsentForOrigin(envelope, origin) -> { mode, mutating }   // pure; DEFAULT mode 'off', mutating false
  readPolicies() -> Promise<envelope>          // chrome.storage.local get; null-safe; returns a default-OFF envelope when absent
  setOriginMode(origin, mode) -> Promise<void> // mode in off|ask|auto
  setOriginMutating(origin, allowed) -> Promise<void>
  _reset()

audit-log.js — CommonJS module.exports + globalThis.FsbAuditLog:
  STORAGE_KEY = 'fsbAuditLog'   PAYLOAD_VERSION = 1   MAX_ENTRIES = 200
  append(entry) -> Promise<void>   // field-WHITELIST { ts, origin, slug, method, sideEffectClass, consentDecision, outcome, error? }
  getEntries(opts) -> Promise<{ entries, clearedAt? }>
  _reset()

FsbConsentGate.evaluate({ origin, slug, method, entry }) ->
  { decision:'allow' } | { decision, method, sideEffectClass, error }
  // LOCKED decision order (BLOCKER fix): denylist(blocked) -> default-OFF/Off(off) -> ask -> sensitive+Auto(sensitive, downgrade to ask) -> mutation(mutating) -> allow
  // 'sensitive' is a real gate decision (NOT a UI-only flag): a classify(origin).sensitive origin with stored mode 'auto' is downgraded to ask here
  // error is a dual-field RECIPE_CONSENT_* object built with the router's _err helper

Error codes (LOCKED; all match /^RECIPE_.+$/ -> verbatim, ZERO errors.ts edit):
  RECIPE_CONSENT_REQUIRED · RECIPE_CONSENT_BLOCKED · RECIPE_CONSENT_MUTATING_REQUIRED
  // 'sensitive' decision surfaces as RECIPE_CONSENT_REQUIRED (consentDecision:'sensitive') — the downgrade-to-ask path; no new error code needed

Collaborators read via typeof-guarded globals (degrade gracefully, NEVER throw), mirroring _catalog()/_search():
  FsbServiceDenylist.isDenied(origin) -> { denied, reason? }            // Plan 03; absent -> treat as not-denied
  FsbServiceDenylist.classify(origin) -> { sensitive, denied, reason? }  // Plan 03; absent -> treat as not-sensitive (the single source of truth for sensitivity, D-14)
  FsbConsentPolicyStore (this plan)
  FsbAuditLog (this plan)
</interfaces>

# Analogs to clone (read for the exact storage/envelope/redaction/error patterns):
@extension/utils/diagnostics-ring-buffer.js
@extension/utils/redactForLog.js
@extension/utils/capability-router.js
@extension/utils/capability-fetch.js
@extension/utils/agent-registry.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: consent-policy-store.js — default-OFF Off/Ask/Auto + elevated mutating envelope</name>
  <read_first>
    - extension/utils/agent-registry.js:80-138 (the versioned-envelope read/write clone: _getChrome lazy global, swallow-to-null, remove-on-empty, v:1 envelope — the exact storage pattern, but use chrome.storage.LOCAL here per D-02, not session)
    - extension/utils/diagnostics-ring-buffer.js:19-25 (the _hasChromeStorage local-storage guard shape)
    - .planning/phases/30-.../30-RESEARCH.md §Code Examples "Default-OFF consent envelope (D-02)" (getConsentForOrigin default-OFF logic) + §Common Pitfalls Pitfall 7 (optional withRegistryLock mutex)
    - .planning/phases/30-.../30-CONTEXT.md D-02, D-04 (envelope shape; mutating is a separate elevated per-origin opt-in)
  </read_first>
  <behavior>
    - getConsentForOrigin({}, 'https://x.com') -> { mode:'off', mutating:false } (default-OFF, GOV-01/02)
    - getConsentForOrigin({policies:{'https://x.com':{mode:'auto',mutating:false}}}, 'https://x.com') -> { mode:'auto', mutating:false }
    - setOriginMode then readPolicies round-trips the mode; setOriginMutating(origin,true) sets only the mutating flag, leaving mode unchanged
    - no global "enable-all" key exists in the envelope (Auto is per-origin only, GOV-02)
    - chrome absent (Node) -> readPolicies resolves a default-OFF envelope, setters resolve without throwing
  </behavior>
  <action>
    Create extension/utils/consent-policy-store.js as a dual-export IIFE (globalThis.FsbConsentPolicyStore + module.exports), cloning agent-registry.js's lazy-chrome + swallow-to-null storage helpers but over chrome.storage.local (D-02) with STORAGE_KEY 'fsbConsentPolicies' and PAYLOAD_VERSION 1. Implement getConsentForOrigin(envelope, origin) as a PURE function returning { mode, mutating } that falls back to envelope.defaultMode then 'off' for an unknown origin and false for mutating (the RESEARCH Code Example). readPolicies() reads the versioned envelope (mismatched v -> a fresh default-OFF envelope). setOriginMode(origin, mode) validates mode against {off,ask,auto} and writes policies[origin].mode (creating the per-origin record); setOriginMutating(origin, allowed) writes policies[origin].mutating as a boolean WITHOUT touching mode (read-Auto != write-Auto, D-04). Use an in-flight promise-chain mutex (the agent-registry withRegistryLock 4-line idiom) so two near-simultaneous setters serialize (Pitfall 7). Provide _reset() for tests. NO eval/new Function/import even in comments (the module is on RECIPE_PATH_ALLOWLIST, Check 1 greps it). No emojis.
  </action>
  <acceptance_criteria>
    - `node tests/consent-policy-store.test.js` PASSES (default-OFF, Off/Ask/Auto round-trip, no global enable key).
    - The module exports getConsentForOrigin/readPolicies/setOriginMode/setOriginMutating on both globalThis.FsbConsentPolicyStore and module.exports.
    - `node scripts/verify-recipe-path-guard.mjs` still exits 0 (module is eval-free + already allowlisted by Plan 01).
  </acceptance_criteria>
  <verify>
    <automated>node tests/consent-policy-store.test.js && node scripts/verify-recipe-path-guard.mjs</automated>
  </verify>
  <done>The consent store round-trips per-origin Off/Ask/Auto + a separate mutating flag, defaults OFF for unknown origins, has no global switch, and is eval-free.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: audit-log.js — append-only secret-free redacted ring</name>
  <read_first>
    - extension/utils/diagnostics-ring-buffer.js:27-122 (the FULL append-only ring to clone: _inMemoryRing shadow, field-whitelist defensive copy on append, FIFO trim at MAX_ENTRIES, chrome.storage.local get/push/trim/set, getEntries({clear}), _reset — clone its structure verbatim, swap the entry schema)
    - extension/utils/redactForLog.js:29-63 (the shape-only redactor: URL->origin, string->{kind,length}, error->{name,message}; reused for every audit field)
    - .planning/phases/30-.../30-RESEARCH.md §Code Examples "Cloning the audit ring (entry schema D-10, redaction D-11)" (the exact safe-field construction: origin via redact(origin).origin, error via redact(error).message)
    - .planning/phases/30-.../30-CONTEXT.md D-09, D-10, D-11 (envelope, entry schema, redaction mandate)
  </read_first>
  <behavior>
    - append({ts,origin,slug,method,sideEffectClass,consentDecision,outcome}) persists a record with EXACTLY those keys (+ optional error); any extra field on the input (e.g. args, body, token) is NOT read or stored
    - getEntries() returns the appended entries newest-last; FIFO-trims at MAX_ENTRIES
    - getEntries({clear:true}) returns then empties the ring
    - no cookie/token/csrf/xoxc/_gh_sess/bearer/authorization substring survives in JSON.stringify(getEntries()) even when those appear in the input's non-whitelisted fields
    - origin is reduced to its origin (redactForLog), error to name+message only (no stack, no body)
  </behavior>
  <action>
    Create extension/utils/audit-log.js as a dual-export IIFE (globalThis.FsbAuditLog + module.exports) cloning diagnostics-ring-buffer.js verbatim in structure, with STORAGE_KEY 'fsbAuditLog', PAYLOAD_VERSION 1, MAX_ENTRIES 200. append(entry) builds the safe record by a strict FIELD WHITELIST (D-10) — read ONLY ts, origin, slug, method, sideEffectClass, consentDecision, outcome, error — and route each through globalThis.redactForLog (lazy global, like redactForLog references fsbDiagnostics): origin -> redact(origin).origin || '' (URL collapses to origin), slug/method/sideEffectClass/consentDecision/outcome -> String() coerced, error -> redact(error).message || '' when present (name+message only, never a body/stack). args/body/headers/cookies/tokens are NEVER referenced in the code path (the schema excludes them by construction). Persist via the in-memory shadow + chrome.storage.local get/push/FIFO-trim/set exactly as the diagnostics ring does (Pitfall 7 ordering). getEntries({clear}) and _reset() mirror the diagnostics ring. NO eval/new Function/import even in comments (allowlisted module, Check 1). No emojis.
  </action>
  <acceptance_criteria>
    - `node tests/audit-log.test.js` PASSES (exact entry schema + FIFO trim at MAX_ENTRIES).
    - `node tests/audit-log-no-secret.test.js` PASSES (no auth substring survives — the GOV-06 security assertion).
    - `node scripts/verify-recipe-path-guard.mjs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node tests/audit-log.test.js && node tests/audit-log-no-secret.test.js && node scripts/verify-recipe-path-guard.mjs</automated>
  </verify>
  <done>The audit ring appends only the whitelisted fields, redacts every one through redactForLog, FIFO-trims, supports export/clear, and provably leaks no auth substring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wrap FsbCapabilityRouter.invoke in the consent + sensitive + mutation gate with audit append</name>
  <read_first>
    - extension/utils/capability-router.js:55-69 (the _err dual-field helper to reuse for RECIPE_CONSENT_* errors), :75-86 (the typeof-guarded collaborator-accessor idiom to copy for _consentStore/_auditLog/_denylist), :182-216 (invoke — insert the gate AFTER catalog.resolve(entry) and BEFORE switch(entry.tier); audit on every return)
    - extension/utils/capability-catalog.js:104-116, :183-186 (entry.descriptor.sideEffectClass + entry.recipe.method — the inputs the gate classifies the side-effect from)
    - extension/utils/capability-fetch.js:228 (MUTATING_METHODS {POST,PUT,PATCH,DELETE} — the read-vs-mutating classifier; duplicate the small set locally OR read it via a typeof-guarded FsbCapabilityFetch accessor)
    - extension/ws/mcp-tool-dispatcher.js:181-260 (the Phase-240 ownership gate that runs UPSTREAM at the front door — consent is the NEXT gate, never duplicated; do NOT add ownership logic here), :2208-2236 + extension/ai/tool-executor.js:672-716 (both doors resolve {origin,tabId} and call invoke — the gate must treat a null origin as fail-closed)
    - .planning/phases/30-.../30-RESEARCH.md §Architecture Patterns Pattern 3 (the gate-as-wrapper that preserves the invoke return contract) + the System Architecture Diagram (the gate's denylist -> default-OFF -> ask -> mutation -> sensitive checks; this plan ENFORCES sensitive BEFORE mutation per the locked BLOCKER order so Auto can never reach a mutation decision on a sensitive origin without first downgrading to ask)
    - .planning/phases/30-.../30-CONTEXT.md D-01, D-03, D-04, D-14, D-15 (one gate both doors; Ask returns a typed reason, no modal; mutating separate opt-in; sensitive origins force Ask even under Auto; denylist checked first)
  </read_first>
  <behavior>
    - invoke for an origin with no policy -> returns RECIPE_CONSENT_REQUIRED (dual-field), appends an audit entry with consentDecision:'off' outcome:'blocked', and NEVER calls executeBoundSpec (GOV-01)
    - invoke for a denylisted origin -> RECIPE_CONSENT_BLOCKED, audit consentDecision:'blocked', checked BEFORE the per-origin policy (GOV-08/D-15)
    - invoke for a GET on a read-Auto, NON-sensitive origin -> proceeds to the existing tier dispatch; audit outcome reflects the dispatched result
    - invoke for a GET on a SENSITIVE origin set to Auto -> RECIPE_CONSENT_REQUIRED with consentDecision:'sensitive', audit outcome:'blocked', NEVER calls executeBoundSpec (GOV-07/D-14) — Auto is downgraded to ask at the gate, NOT in the UI
    - invoke for a POST on a read-Auto origin (mutating false, non-sensitive) -> RECIPE_CONSENT_MUTATING_REQUIRED, audit consentDecision:'mutating' (GOV-03); after setOriginMutating(origin,true) the same POST proceeds
    - an Ask-mode origin -> RECIPE_CONSENT_REQUIRED (no synchronous modal; the pending surfacing is the UI's job in Plan 04), audit consentDecision:'ask'
    - a null/absent origin -> fail closed (RECIPE_CONSENT_REQUIRED), never defaults to allow
    - the gate runs ONCE, inside invoke, identically for the {origin,tabId} and {origin,tabId,source:'autopilot'} ctx shapes (INV-02 one gate both doors)
  </behavior>
  <action>
    Modify extension/utils/capability-router.js. Add typeof-guarded accessors _consentStore()/_auditLog()/_denylist() mirroring _catalog()/_search() (each returns the global or null; a null collaborator degrades safely — a null consent store fails CLOSED to RECIPE_CONSENT_REQUIRED, a null denylist treats nothing as denied AND nothing as sensitive, a null audit log skips the append). Add an async gate function FsbConsentGate.evaluate({origin, slug, method, entry}) implementing the LOCKED decision order (BLOCKER): (1) DENYLIST FIRST — _denylist().isDenied(origin).denied -> { decision:'blocked', error:_err('RECIPE_CONSENT_BLOCKED', {origin, slug, reason}) }; (2) classify sideEffectClass from method via the local MUTATING_METHODS set AND entry.descriptor.sideEffectClass (mutating wins if either says mutating); (3) read the consent envelope via _consentStore().readPolicies() + getConsentForOrigin -> if mode==='off' (or origin null/store absent) -> { decision:'off', error:_err('RECIPE_CONSENT_REQUIRED', {origin, slug}) }; if mode==='ask' -> { decision:'ask', error:_err('RECIPE_CONSENT_REQUIRED', {origin, slug}) }; (4) SENSITIVE+AUTO DOWNGRADE — if mode==='auto' AND _denylist().classify(origin).sensitive is true -> { decision:'sensitive', error:_err('RECIPE_CONSENT_REQUIRED', {origin, slug}) } (Auto is downgraded to ask at the gate; D-14; classify is the single source of truth — do NOT hardcode sensitive hosts here); (5) MUTATION — if sideEffectClass is mutating AND the mutating opt-in is false -> { decision:'mutating', error:_err('RECIPE_CONSENT_MUTATING_REQUIRED', {origin, slug}) }; (6) otherwise { decision:'allow' }. (Sensitive is enforced at step 4, BEFORE mutation at step 5, so a sensitive Auto origin can NEVER reach allow or a mutation decision without first being downgraded to ask.) In invoke (after catalog.resolve yields entry, before the switch), call await FsbConsentGate.evaluate(...) using the method derived from entry.recipe.method or the handler descriptor; on decision!=='allow', append the blocked audit entry ({ts,origin,slug,method,sideEffectClass,consentDecision:decision,outcome:'blocked'}) and RETURN gate.error verbatim. On allow, run the existing tier dispatch unchanged, then append an audit entry with the dispatched outcome ('ok' on success, 'error' otherwise; error name via the result's code when present). Export FsbConsentGate alongside invoke on the router global so the chokepoint/parity/sensitive tests can drive it. The gate sits ABOVE the tier bodies — executeBoundSpec, the origin-pin, INV-01/INV-04 are all untouched. NO eval/new Function/import even in comments. No emojis.
  </action>
  <acceptance_criteria>
    - `node tests/consent-gate.test.js`, `node tests/consent-mutation-gate.test.js`, `node tests/consent-chokepoint.test.js` all PASS — including the sensitive+Auto downgrade case (a classify-sensitive origin with mode:'auto' yields a NON-allow decision RECIPE_CONSENT_REQUIRED / consentDecision:'sensitive').
    - `node tests/capability-router.test.js` still PASSES (the Phase-29 router behavior — allowed paths still dispatch to the same tier bodies; only a gate now precedes them).
    - `node tests/capability-autopilot-parity.test.js` still PASSES (both doors reach the one gate).
    - `node scripts/verify-recipe-path-guard.mjs` exits 0 and `node tests/agent-loop-iterator-guard.test.js` PASSES (INV-04 untouched).
  </acceptance_criteria>
  <verify>
    <automated>node tests/consent-gate.test.js && node tests/consent-mutation-gate.test.js && node tests/consent-chokepoint.test.js && node tests/capability-router.test.js && node tests/capability-autopilot-parity.test.js && node tests/agent-loop-iterator-guard.test.js && node scripts/verify-recipe-path-guard.mjs</automated>
  </verify>
  <done>invoke enforces default-OFF + Ask + sensitive+Auto-downgrade + mutation gating + denylist-first at the single chokepoint after ownership, both doors hit the same gate, a sensitive Auto origin is downgraded to ask (never silently executed), every outcome is audited, and the Phase-29 router/parity/iterator/guard all stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| front door (MCP / autopilot) → invoke | Untrusted slug + model-supplied origin cross here; the gate is the authorization layer. A null/spoofed origin must fail closed. |
| invoke gate → chrome.storage.local | Consent policy + audit ring persist here; the audit ring must carry no secrets. |
| gate decision → audit append | The audit entry must contain only the whitelisted, redacted fields — never the args/recipe body that flow through invoke. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-03 | Elevation | invoke on an unapproved origin (credential replay) | mitigate | Default-OFF per origin (GOV-01): unknown/null origin or mode 'off' -> RECIPE_CONSENT_REQUIRED before any tier dispatch; Auto is per-origin only (GOV-02). |
| T-30-04 | Tampering / Elevation | mutating call under read consent | mitigate | Separate elevated mutating opt-in (GOV-03/D-04); MUTATING_METHODS + descriptor.sideEffectClass classify; mutating-false -> RECIPE_CONSENT_MUTATING_REQUIRED. |
| T-30-04b | Elevation | Auto silently executing a credentialed call against a sensitive origin (bank/email/gov) | mitigate | Step-4 sensitive+Auto downgrade at the GATE (GOV-07/D-14): classify(origin).sensitive + mode 'auto' -> RECIPE_CONSENT_REQUIRED (consentDecision:'sensitive'), enforced BEFORE the mutation/allow steps, so Auto can never reach executeBoundSpec on a sensitive origin; classify (Plan 03) is the single source of truth, not the UI button. |
| T-30-05 | Elevation | gate bypass via the autopilot front door | mitigate | One gate wrapping the single invoke chokepoint (D-01/INV-02); both doors resolve {origin} and reach the same FsbConsentGate.evaluate; no parallel path; chokepoint + parity tests assert it. |
| T-30-06 | Information disclosure | secrets leaking into the audit ring | mitigate | Field-whitelist entry schema (D-10) + redactForLog on every field (D-11) + the no-auth-substring test (GOV-06); args/body/headers never referenced in the append path. |
| T-30-07 | Elevation / legal | automating a denylisted (bank/gov/email) service | mitigate | Denylist checked FIRST in the gate (D-15), before per-origin policy -> RECIPE_CONSENT_BLOCKED; the denylist global is supplied by Plan 03 (absent -> not-denied, but Plan 03 ships in the same wave). |
| T-30-08 | Repudiation | storage write race dropping audit entries | mitigate | Clone the diagnostics-ring in-memory shadow + reconcile-on-write; single-threaded SW; consent store uses the agent-registry promise-chain mutex (Pitfall 7). |
| T-30-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed; the two new modules are pure clones of in-repo patterns, eval-free, and on RECIPE_PATH_ALLOWLIST. |
</threat_model>

<verification>
- The consent/audit tests pass: consent-policy-store, consent-gate (incl. the sensitive+Auto downgrade case), consent-mutation-gate, consent-chokepoint, audit-log, audit-log-no-secret.
- The Phase-29 regression set passes: capability-router, capability-autopilot-parity, agent-loop-iterator-guard.
- `node scripts/verify-recipe-path-guard.mjs` exits 0 (both new modules eval-free + allowlisted).
- No errors.ts edit (the RECIPE_CONSENT_* codes match the existing /^RECIPE_.+$/ passthrough; the 'sensitive' decision reuses RECIPE_CONSENT_REQUIRED — verified indirectly via the chokepoint/gate test driving the dual-field shape).
</verification>

<success_criteria>
- Default-OFF per origin enforced at invoke; Off/Ask/Auto with no global switch; mutating needs a second elevated opt-in.
- A sensitive origin under Auto is downgraded to ask AT THE GATE (consentDecision:'sensitive' -> RECIPE_CONSENT_REQUIRED), enforced before mutation/allow; Auto never silently executes against a sensitive origin (GOV-07/D-14).
- The gate runs once at the single chokepoint after ownership, reached identically by both front doors.
- Every invoke outcome appends an audit entry with exactly the whitelisted, redacted fields; no auth substring survives.
- The two-point origin-pin, INV-01, and INV-04 are untouched; Phase-29 router/parity/iterator tests stay green.
</success_criteria>

<output>
Create `.planning/phases/30-consent-governance-recipe-signature-verification-audit-legal/30-02-SUMMARY.md` when done.
</output>
</content>
