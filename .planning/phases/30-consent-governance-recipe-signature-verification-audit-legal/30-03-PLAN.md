---
phase: 30-consent-governance-recipe-signature-verification-audit-legal
plan: 03
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - extension/utils/capability-signature.js
  - extension/utils/capability-interpreter.js
  - extension/utils/service-denylist.js
  - extension/config/service-denylist.json
  - docs/LEGAL.md
autonomous: true
requirements: [SIGN-01, SIGN-02, GOV-08]
must_haves:
  truths:
    - "A non-bundled recipe envelope with a valid Ed25519 signature over its JCS canonical form verifies; a one-byte-tampered or unsigned non-bundled envelope is rejected with RECIPE_SIGNATURE_INVALID (SIGN-01, D-05/D-06)"
    - "The signature verify hook fires INSIDE interpretRecipe AFTER schema-validate and BEFORE bind; the verified payload is the recipe core + capturedAt + schemaHash, excluding the signature field (SIGN-02, D-06)"
    - "First-party bundled recipes are EXEMPT from the verify gate (verify is not called) because _runDeclarativeTier passes NO non-bundled integrity meta, so the absent/bundled-meta default path skips verify — the path is still exercised on a non-bundled fixture (D-07)"
    - "Ed25519 verify uses native crypto.subtle with a one-time runtime feature-detect and FAILS CLOSED when native Ed25519 is absent (RESEARCH Pattern 1, Pitfall 1)"
    - "service-denylist.js is the SINGLE source of truth for origin sensitivity: FsbServiceDenylist.classify(origin) -> { sensitive, denied, reason } over a SEEDED banking/primary-email/*.gov host-pattern list + the denylist; checked BEFORE per-origin policy, renders denylisted origins non-enableable AND feeds the gate's sensitive+Auto downgrade (GOV-08, D-14/D-15/D-16)"
    - "docs/LEGAL.md documents the ToS/legal posture, audit retention, and consent model (GOV-08, D-15)"
  artifacts:
    - path: "extension/utils/capability-signature.js"
      provides: "native-first Ed25519 verify + in-house JCS canonicalizer + verifyRecipeEnvelope (provenance-aware, fail-closed)"
      exports: ["verifyEd25519", "jcsCanonicalize", "verifyRecipeEnvelope"]
      min_lines: 90
    - path: "extension/utils/capability-interpreter.js"
      provides: "signature-verify hook after schema-validate, before bind; RECIPE_SIGNATURE_INVALID on failure"
      contains: "RECIPE_SIGNATURE_INVALID"
    - path: "extension/config/service-denylist.json"
      provides: "{ v, deniedOrigins, sensitiveOrigins, deniedReason } conservative category seed"
      contains: "deniedOrigins"
    - path: "extension/utils/service-denylist.js"
      provides: "FsbServiceDenylist.isDenied(origin) + classify(origin) (sensitive-origin source of truth, D-14) + load() at SW startup"
      exports: ["isDenied", "classify", "load"]
    - path: "docs/LEGAL.md"
      provides: "legal/ToS posture + audit retention + consent model"
      contains: "Retention"
  key_links:
    - from: "extension/utils/capability-interpreter.js interpretRecipe"
      to: "FsbCapabilitySignature.verifyRecipeEnvelope"
      via: "hook after schema-validate (step 1), before bindAuthStrategy (step 6); only when meta is present AND meta.provenance !== 'bundled'"
      pattern: "verifyRecipeEnvelope|RECIPE_SIGNATURE_INVALID"
    - from: "extension/utils/capability-signature.js verifyEd25519"
      to: "globalThis.crypto.subtle"
      via: "native-first feature-detect; fail-closed when absent"
      pattern: "subtle"
    - from: "extension/utils/service-denylist.js"
      to: "extension/config/service-denylist.json"
      via: "load() reads the bundled JSON (deniedOrigins + sensitiveOrigins) at SW startup"
      pattern: "service-denylist\\.json"
    - from: "extension/utils/service-denylist.js classify"
      to: "FsbConsentGate.evaluate (Plan 02, step 4 sensitive+Auto downgrade)"
      via: "classify(origin).sensitive consumed by the gate as the authoritative sensitive-origin signal (D-14)"
      pattern: "classify"
---

<objective>
Wave 1 leaf + wiring (integrity + legal half): ship the Ed25519/JCS signature verifier (native crypto.subtle first, feature-detected, fail-closed; in-house RFC-8785 canonicalizer for the closed recipe shape), hook it into interpretRecipe after schema-validate and before bind so non-bundled recipes are verified (bundled recipes exempt because no non-bundled meta is threaded), and ship the service denylist + sensitive-origin classifier + docs/LEGAL.md legal posture.

Purpose: SIGN-01/02 + GOV-08 (D-05..D-08, D-14/D-15/D-16). The verify path is the integrity gate for the Phase-31 server/learned recipes — Phase 30 ships and PROVES it on a fixture keypair while leaving the working Phase-29 bundled head untouched (exempt because _runDeclarativeTier passes no non-bundled integrity meta). The denylist module is BOTH the hard, checked-first deny that makes prohibited services non-enableable AND — per D-14 (heuristic + denylist live together) — the SINGLE source of truth for "is this origin sensitive": its classify(origin) is consumed by the Plan-02 gate's step-4 sensitive+Auto downgrade, so the sensitive-origin control is a real security boundary at the gate, not just a disabled UI button. Runs as DATA + a fixed verifier: no eval, no remotely-hosted key, native crypto preferred (zero new packages — the noble fallback is deferred to Phase 31 per RESEARCH Open Question 2).
Output: extension/utils/capability-signature.js (NEW), extension/utils/capability-interpreter.js (MODIFIED — verify hook), extension/utils/service-denylist.js (NEW — isDenied + classify), extension/config/service-denylist.json (NEW — deniedOrigins + sensitiveOrigins), docs/LEGAL.md (NEW).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-consent-governance-recipe-signature-verification-audit-legal/30-RESEARCH.md
@.planning/phases/30-consent-governance-recipe-signature-verification-audit-legal/30-CONTEXT.md

<interfaces>
<!-- LOCKED contracts (must match 30-01-PLAN.md <interfaces> exactly — the Wave-0 tests + the fixture signer target these). -->

capability-signature.js — CommonJS module.exports + globalThis.FsbCapabilitySignature:
  async verifyEd25519(pubKeyRaw:Uint8Array, sigBytes:Uint8Array, msgBytes:Uint8Array) -> Promise<boolean>
  jcsCanonicalize(value) -> string   // RFC 8785 closed-shape serializer; UTF-16 key sort; THROWS on a non-integer number
  async verifyRecipeEnvelope(envelope) -> Promise<{ ok:boolean, reason? }>
  TRUSTED_PUBLIC_KEYS  // array of base64 raw Ed25519 public keys shipped in the bundle; tests push the fixture key
  _setTrustedKeysForTest(keys)  // test hook to inject the fixture public key

  Envelope (LOCKED — the "Lattice receipt" provenance shape, D-05/D-07):
    { recipe:<schema-valid recipe core>, provenance:'bundled'|'server'|'learned', signature?:base64, capturedAt?:string, schemaHash?:string }
  Signed payload (LOCKED — RESEARCH Pitfall 3 + Open Question 1): jcsCanonicalize of an object containing the recipe core PLUS capturedAt and schemaHash, EXCLUDING the signature field. This is byte-identical to what catalog/recipes/_fixtures/signature/sign-fixtures.mjs signed.
    provenance==='bundled' -> { ok:true } WITHOUT calling verifyEd25519 (D-07 exemption)
    else: missing signature/key OR verify false -> { ok:false, reason } (FAIL CLOSED)

service-denylist.js — CommonJS module.exports + globalThis.FsbServiceDenylist (the SINGLE source of truth for "is this origin denied / sensitive", D-14/D-15: heuristic + denylist live together):
  isDenied(origin) -> { denied:boolean, reason? }   // matches deniedOrigins (supports a trailing-host-suffix / wildcard form)
  classify(origin) -> { sensitive:boolean, denied:boolean, reason? }   // D-14: denied implies sensitive; sensitive ALSO true on a SEEDED sensitive host-pattern match (banking / primary-email / *.gov per RESEARCH Open Question 3) even when not denied. Consumed by the Plan-02 gate step-4 sensitive+Auto downgrade.
  async load() -> Promise<void>   // reads extension/config/service-denylist.json (deniedOrigins + sensitiveOrigins) into memory at SW startup
  _setForTest(config)

Error code (LOCKED; matches /^RECIPE_.+$/ -> verbatim, ZERO errors.ts edit):
  RECIPE_SIGNATURE_INVALID

interpretRecipe signature change (LOCKED, additive + backward-compatible):
  interpretRecipe(recipe, args, meta?) — meta carries { provenance, signature, capturedAt, schemaHash } when the caller has an envelope.
  When meta is absent OR meta.provenance === 'bundled' -> NO verify (today's callers + the bundled head are unchanged; this is the absent/bundled-meta default path that keeps the head exempt — _runDeclarativeTier passes NO non-bundled meta).
  When meta.provenance is non-bundled -> build the envelope from (recipe, meta) and call verifyRecipeEnvelope; ok:false -> return createRecipeError('RECIPE_SIGNATURE_INVALID', ...).
</interfaces>

# Analogs / anchors to read:
@extension/utils/capability-interpreter.js
@extension/utils/capability-recipe-schema.js
@extension/config/secure-config.js
@scripts/verify-recipe-path-guard.mjs
@catalog/recipes/_fixtures/signature/sign-fixtures.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: capability-signature.js — native-first Ed25519 verify + in-house JCS + verifyRecipeEnvelope</name>
  <read_first>
    - .planning/phases/30-.../30-RESEARCH.md §Architecture Patterns Pattern 1 (the tri-state feature-detect + native verify + fail-closed structure — implement this verbatim minus the deferred noble branch) and Pattern 2 (the ~40-line jcsCanonicalize with the UTF-16 key sort + integer-only number throw)
    - catalog/recipes/_fixtures/signature/sign-fixtures.mjs (the fixture signer — the runtime jcsCanonicalize + signed-payload scope MUST match it byte-for-byte; read its header comment for the locked scope)
    - .planning/phases/30-.../30-RESEARCH.md §Common Pitfalls Pitfall 1 (Ed25519 absent -> feature-detect, never crash), Pitfall 3 (the exact signed-payload scope), Pitfall 5/6 (named capability-* so Check 4 auto-globs; eval-free)
    - extension/config/secure-config.js (the bundled-config precedent for where the TRUSTED_PUBLIC_KEYS constant lives — a bundled value, never remotely fetched, Wall-1)
    - .planning/phases/30-.../30-CONTEXT.md D-05, D-07, D-08 (Web Crypto Ed25519 over JCS; bundled exempt; trusted public key in the bundle + fixture keypair for tests)
  </read_first>
  <behavior>
    - jcsCanonicalize sorts object keys by UTF-16 code unit (default Array.sort), emits JSON.stringify for string/boolean/integer, recurses arrays/objects, and THROWS on a non-integer/non-finite number (the closed-vocab tripwire)
    - jcsCanonicalize of the fixture's signed payload === the bytes sign-fixtures.mjs signed (round-trip: verify passes on signed-recipe.json)
    - verifyEd25519 returns a boolean; with native crypto.subtle Ed25519 present it imports the raw key and verifies; with it absent it returns false (fail closed), never throws
    - verifyRecipeEnvelope(signed-recipe.json with the fixture key trusted) -> { ok:true }
    - verifyRecipeEnvelope(tampered-recipe.json) -> { ok:false }
    - verifyRecipeEnvelope({provenance:'bundled', recipe}) -> { ok:true } without invoking verifyEd25519
    - verifyRecipeEnvelope({provenance:'server', recipe, /* no signature */}) -> { ok:false } (fail closed)
  </behavior>
  <action>
    Create extension/utils/capability-signature.js as a dual-export IIFE (globalThis.FsbCapabilitySignature + module.exports). Implement jcsCanonicalize exactly per RESEARCH Pattern 2 — the closed-shape RFC-8785 serializer with the UTF-16 key sort and the integer-only number throw — and ensure its output matches sign-fixtures.mjs's canonical bytes (the two MUST agree; if the fixture script and this differ, the round-trip test fails). Implement the tri-state native feature-detect + verifyEd25519 per RESEARCH Pattern 1 using globalThis.crypto.subtle.importKey('raw', key, {name:'Ed25519'}, false, ['verify']) + verify({name:'Ed25519'}, ...); on a missing/throwing subtle, set the cached flag false and return false (FAIL CLOSED) — do NOT add the noble fallback (deferred to Phase 31 per Open Question 2; leave a one-line comment marking the seam). Implement verifyRecipeEnvelope(envelope): if provenance==='bundled' return {ok:true} WITHOUT calling verify (D-07); else require envelope.signature + a trusted key, reconstruct the signed payload = the recipe core plus capturedAt + schemaHash MINUS the signature field (the LOCKED scope), jcsCanonicalize it to bytes (TextEncoder), base64-decode the signature, and try each key in TRUSTED_PUBLIC_KEYS via verifyEd25519 — return {ok:true} on the first pass else {ok:false, reason}. TRUSTED_PUBLIC_KEYS is a bundled constant (empty or a real publish key placeholder for now; Phase 31 ships the real signer); _setTrustedKeysForTest injects the fixture key. NO eval/new Function/import even in comments (named capability-*, Check 1+4). No emojis.
  </action>
  <acceptance_criteria>
    - `node tests/recipe-signature.test.js` PASSES: fixture sign->verify-pass, tamper->fail, bundled-exempt (no verify call), unsigned-non-bundled->fail.
    - jcsCanonicalize throws on a non-integer number (the tripwire) — assertable in the test.
    - `node scripts/verify-recipe-path-guard.mjs` exits 0 (capability-signature.js eval-free + allowlisted by Plan 01; Check 4 disk-glob now finds it on the list).
  </acceptance_criteria>
  <verify>
    <automated>node tests/recipe-signature.test.js && node scripts/verify-recipe-path-guard.mjs</automated>
  </verify>
  <done>The signature module verifies the fixture, rejects the tamper + the unsigned non-bundled case, exempts bundled provenance, fails closed when native Ed25519 is absent, and its canonicalizer agrees byte-for-byte with the fixture signer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hook signature verify into interpretRecipe (after schema-validate, before bind)</name>
  <read_first>
    - extension/utils/capability-interpreter.js:85-93 (createRecipeError — reuse for RECIPE_SIGNATURE_INVALID), :236-251 (interpretRecipe step 1 schema-validate — the hook goes immediately AFTER this returns success), :363-369 (step 6 bindAuthStrategy — the hook goes BEFORE this), :374-385 (the export object to extend if needed)
    - extension/utils/capability-router.js:95-130 (_runDeclarativeTier — the caller of interpretRecipe; it receives entryRecipe, NOT the catalog entry object, so there is no provenance field on what it passes; the bundled head therefore travels with NO non-bundled meta and stays exempt via the absent/bundled-meta default path)
    - .planning/phases/30-.../30-RESEARCH.md §Common Pitfalls Pitfall 3 (signed-payload scope) + the System Architecture Diagram "SIGNATURE VERIFY HOOK" box (provenance==='bundled' -> EXEMPT; else canonicalize+verify -> RECIPE_SIGNATURE_INVALID)
    - .planning/phases/30-.../30-CONTEXT.md D-06, D-07 (hook placement; bundled exemption; the recipe schema's additionalProperties:false means integrity metadata travels in the ENVELOPE/meta, NOT inside the schema-validated recipe core)
  </read_first>
  <behavior>
    - interpretRecipe(recipe, args) with no meta -> behaves EXACTLY as today (no verify; all existing capability-interpreter.test.js cases stay green) — backward-compatible
    - interpretRecipe(recipe, args, {provenance:'bundled', ...}) -> binds without calling verifyRecipeEnvelope (D-07)
    - interpretRecipe(<tampered recipe core>, args, {provenance:'server', signature, capturedAt, schemaHash}) -> returns RECIPE_SIGNATURE_INVALID (dual-field)
    - the hook fires AFTER schema-validate: a schema-invalid recipe with a bad signature still returns its SCHEMA error first (verify is not reached)
    - the hook fires BEFORE bind: a non-bundled recipe that fails verify never reaches bindAuthStrategy / never produces a bound spec
  </behavior>
  <action>
    Modify extension/utils/capability-interpreter.js. Extend interpretRecipe to accept an optional third meta arg (additive — default undefined keeps every current caller behaving identically). Insert the verify hook AFTER step 1 (the schema-validate success at ~line 251) and BEFORE step 6 bind (~line 363) — placement after the args-validate/template/buildRequest steps is fine as long as it is before bindAuthStrategy and after the recipe schema gate. The hook: if meta is absent OR meta.provenance === 'bundled', skip verify (continue to bind) — this is the absent/bundled-meta default path that keeps every current caller and the Phase-29 head exempt WITHOUT introducing a provenance field on the recipe core. Otherwise, read globalThis.FsbCapabilitySignature via a typeof-guard (absent -> fail CLOSED with RECIPE_SIGNATURE_INVALID, since a non-bundled recipe must not bind without a verifier); construct the envelope { recipe:<the schema-valid recipe object>, provenance:meta.provenance, signature:meta.signature, capturedAt:meta.capturedAt, schemaHash:meta.schemaHash }; await verifyRecipeEnvelope(envelope); on { ok:false } return createRecipeError('RECIPE_SIGNATURE_INVALID', { reason }). Because interpretRecipe is currently synchronous and verify is async, make interpretRecipe async (or add an internal async verify branch) — but PRESERVE the synchronous-success contract for the no-meta/bundled path so the existing tests and the router's call still work: the cleanest path per RESEARCH is to make interpretRecipe async and have the router await it (the router already awaits the tier body; confirm _runDeclarativeTier is async and can await). Update the router call site in _runDeclarativeTier to await interp.interpretRecipe(entryRecipe, args) — the Phase-29 head passes NO meta argument, so it stays on the exempt default path (no provenance field is invented and entryRecipe stays the unwrapped recipe). Keep createRecipeError dual-field so RECIPE_SIGNATURE_INVALID surfaces verbatim (no errors.ts edit). NO eval/new Function/import even in comments. No emojis.
  </action>
  <acceptance_criteria>
    - `node tests/recipe-signature-interpreter-hook.test.js` PASSES: tampered non-bundled -> RECIPE_SIGNATURE_INVALID; hook after schema-validate (schema error wins); bundled/no-meta binds without verify.
    - `node tests/capability-interpreter.test.js` still PASSES (backward-compatible no-meta path).
    - `node tests/capability-router.test.js` + `node tests/capability-head-handlers.test.js` still PASS (the bundled head stays exempt because _runDeclarativeTier passes no meta; the router awaits interpretRecipe).
    - `node scripts/verify-recipe-path-guard.mjs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node tests/recipe-signature-interpreter-hook.test.js && node tests/capability-interpreter.test.js && node tests/capability-router.test.js && node tests/capability-head-handlers.test.js && node scripts/verify-recipe-path-guard.mjs</automated>
  </verify>
  <done>interpretRecipe verifies non-bundled recipes after schema-validate and before bind, exempts the no-meta/bundled default path (no provenance field invented; entryRecipe stays unwrapped), rejects tampers with RECIPE_SIGNATURE_INVALID, and stays backward-compatible for every current caller.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: service-denylist.json + service-denylist.js (isDenied + classify) + docs/LEGAL.md</name>
  <read_first>
    - .planning/phases/30-.../30-RESEARCH.md §Code Examples "Service denylist checked BEFORE policy (D-15)" (the { v, deniedOrigins, deniedReason } shape + checked-first semantics) + §Open Questions 3 (sensitive-origin/denylist host-pattern guidance: a small curated host-pattern list seeded alongside the denylist — banking / known primary-email hosts / *.gov — kept conservative and user-visible; the UI flags matches with the amber Sensitive badge)
    - extension/utils/agent-registry.js:87-105 (the lazy-chrome + swallow-to-null read idiom to mirror for load(); the denylist is a bundled JSON read at startup, not chrome.storage)
    - .planning/phases/30-.../30-CONTEXT.md D-14, D-15, D-16 (sensitive-origin classifier = heuristic + denylist together, forces Ask even under Auto; NEW docs/LEGAL.md + service-denylist.json; conservative category seed with an automation-prohibited reason; user-extensible; NOT empty, NOT aggressive; linked from control panel + showcase privacy page)
    - showcase/angular/src/app/pages/privacy/privacy-page.component.html (the privacy page LEGAL.md will be cross-linked from — read only to confirm the link target convention; the actual privacy-page edit is Plan 04)
  </read_first>
  <behavior>
    - service-denylist.json parses as { v:1, deniedOrigins:[...non-empty conservative seed...], sensitiveOrigins:[...non-empty conservative seed: banking / primary-email / *.gov patterns...], deniedReason:'...automation prohibited...' }
    - FsbServiceDenylist.isDenied for a seeded denied origin (e.g. a representative bank/gov/primary-email host pattern) -> { denied:true, reason }
    - FsbServiceDenylist.isDenied for an arbitrary non-seeded origin (e.g. https://github.com) -> { denied:false }
    - FsbServiceDenylist.classify for a denied origin -> { sensitive:true, denied:true, reason } (denied implies sensitive)
    - FsbServiceDenylist.classify for a SEEDED sensitive-but-not-denied origin (e.g. a *.gov or known primary-email host on sensitiveOrigins) -> { sensitive:true, denied:false, reason }
    - FsbServiceDenylist.classify for https://github.com -> { sensitive:false, denied:false }
    - load() reads the bundled JSON; absent file -> empty deny + empty sensitive set (degrade, never throw)
    - docs/LEGAL.md contains a Retention section + a Consent model section + a service-denylist reference
  </behavior>
  <action>
    Create three artifacts. (1) extension/config/service-denylist.json — a { v:1, deniedOrigins:[...], sensitiveOrigins:[...], deniedReason:"Automation prohibited for this service category." } seed. deniedOrigins is a CONSERVATIVE category seed (D-16): a small set of representative banking / government / primary-email host patterns (support a host-suffix/wildcard match form, e.g. a leading-dot suffix like ".gov" or a host glob the matcher understands — pick ONE form and implement the matcher to it). sensitiveOrigins is a SEPARATE conservative seed (D-14, RESEARCH Open Question 3): banking / known primary-email host / *.gov patterns that force friction even under Auto but are not necessarily hard-denied (e.g. a major webmail host, *.gov). NOT empty, NOT an aggressive broad blocklist; comment the file as user-extensible. (2) extension/utils/service-denylist.js — a dual-export IIFE (globalThis.FsbServiceDenylist + module.exports), the SINGLE source of truth for origin sensitivity (D-14 — heuristic + denylist live together): isDenied(origin) parses the origin host and matches it against the loaded deniedOrigins using the chosen suffix/wildcard form, returning { denied, reason }; classify(origin) returns { sensitive, denied, reason } where denied:true implies sensitive:true (a denied origin is always sensitive) AND sensitive:true also when the host matches the loaded sensitiveOrigins seed even if not denied; load() reads extension/config/service-denylist.json (both deniedOrigins and sensitiveOrigins) in the SW via fetch(chrome.runtime.getURL(...)) OR a bundled require under Node — guard both; absent -> empty sets, never throw; _setForTest(config) injects a config for the unit test. This module is read by the Plan-02 gate via the FsbServiceDenylist global: isDenied is checked FIRST (denylist-block, step 1) and classify(origin).sensitive is consumed at step 4 (sensitive+Auto downgrade) — classify is therefore the authoritative sensitive-origin signal the gate enforces, NOT a UI-only flag. Keep it eval-free. (3) docs/LEGAL.md — a markdown legal/ToS posture documenting: FSB's automation posture (supervised, local, default-OFF per origin), the consent model (Off/Ask/Auto + elevated mutating + sensitive-origin friction even under Auto), audit-log RETENTION (bounded ring + user export-&-clear, secret-free by construction), and the service-denylist policy (categories blocked + sensitive categories flagged + that it is user-extensible). No emojis anywhere (global rule; the UI test and reviewers assert emoji-absence).
  </action>
  <acceptance_criteria>
    - `node tests/service-denylist.test.js` PASSES: JSON shape valid + non-empty deniedOrigins + non-empty sensitiveOrigins seed; isDenied true for a seeded denied pattern, false for github.com; classify returns sensitive:true for a denied origin AND for a seeded sensitive-but-not-denied origin, sensitive:false for github.com; checked-first integration (the gate returns RECIPE_CONSENT_BLOCKED for a denylisted origin — asserted via the Plan-02 gate + this module); docs/LEGAL.md exists with Retention + Consent sections.
    - service-denylist.json is valid JSON with the { v, deniedOrigins, sensitiveOrigins, deniedReason } shape.
    - `node scripts/verify-recipe-path-guard.mjs` exits 0 (service-denylist.js is eval-free and allowlisted by Plan 01's Wave-0 pre-arm).
  </acceptance_criteria>
  <verify>
    <automated>node -e "const d=require('./extension/config/service-denylist.json'); if(!Array.isArray(d.deniedOrigins)||d.deniedOrigins.length===0||!Array.isArray(d.sensitiveOrigins)||d.sensitiveOrigins.length===0||!d.deniedReason)throw new Error('denylist shape/seed'); console.log('denylist OK')" && node -e "const fs=require('fs'); const l=fs.readFileSync('docs/LEGAL.md','utf8'); if(!/Retention/i.test(l)||!/Consent/i.test(l))throw new Error('LEGAL.md sections'); console.log('LEGAL OK')" && node tests/service-denylist.test.js && node scripts/verify-recipe-path-guard.mjs</automated>
  </verify>
  <done>A conservative non-empty service denylist + sensitive-origin seed (JSON + matcher module with isDenied + classify, the single source of truth for sensitivity, checked-first) and docs/LEGAL.md (posture + retention + consent model) exist; denylisted origins are non-enableable via the gate; classify(origin).sensitive feeds the gate's sensitive+Auto downgrade; github.com is neither denied nor sensitive.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server/learned recipe → interpretRecipe | A non-bundled recipe is untrusted DATA; it must be signature-verified over a deterministic canonical form before it can bind to a credentialed call. |
| bundled trusted public key → verify | The key is bundled (Wall-1), never remotely fetched; a forged key cannot be injected at runtime. The fixture key is test-only. |
| denylist + sensitive config → gate | The denylist is a hard, checked-first deny (a denylisted origin must be non-enableable regardless of stored policy); the sensitive seed feeds the gate's authoritative sensitive+Auto downgrade — both live in this single source of truth, not in the UI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-09 | Tampering / Elevation | forged/tampered server recipe executes a credentialed call | mitigate | Ed25519 verify over the JCS canonical form before bind (SIGN-01/02); tamper -> RECIPE_SIGNATURE_INVALID; only non-bundled provenance is gated (bundled stays on the no-meta exempt default path, D-07). |
| T-30-10 | Tampering | signature malleability via JSON re-serialization | mitigate | RFC-8785 JCS canonicalization (deterministic byte string, UTF-16 key sort, integer-only number rule) before sign/verify; the signed-payload scope excludes the signature field (Pitfall 3). |
| T-30-11 | Spoofing / Elevation | silent "assume valid" when native Ed25519 absent | mitigate | verifyEd25519 + verifyRecipeEnvelope FAIL CLOSED (missing verifier/key/signature -> false/ok:false); the feature-detect never defaults to valid (Pitfall 1). |
| T-30-12 | Elevation / legal | automating a prohibited service | mitigate | service-denylist.json conservative deniedOrigins seed (D-16) consumed by the Plan-02 gate, checked BEFORE per-origin policy (D-15), non-enableable; LEGAL.md documents the posture. |
| T-30-12b | Elevation | Auto silently executing against a sensitive origin (bank/email/gov) | mitigate | classify(origin) is the single source of truth (D-14): the sensitiveOrigins seed + denied-implies-sensitive rule feeds the Plan-02 gate step-4 sensitive+Auto downgrade -> RECIPE_CONSENT_REQUIRED / consentDecision:'sensitive'; the control is a gate boundary, not a UI-only disabled button. |
| T-30-13 | Tampering | reintroducing dynamic code on the recipe path (the new signature module) | mitigate | capability-signature.js named capability-* (Check 4 disk-glob) + allowlisted (Plan 01) + eval-free; build reds on any eval/new Function/import. |
| T-30-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed — native Web Crypto + in-house JCS only; the noble fallback is explicitly DEFERRED to Phase 31 (RESEARCH Open Question 2). No [ASSUMED] package vendoring; the RESEARCH §Package Legitimacy Audit checkpoint is therefore not triggered this phase. |
</threat_model>

<verification>
- recipe-signature + recipe-signature-interpreter-hook tests pass (fixture sign/verify/tamper, provenance exemption via the no-meta default path, hook order).
- capability-interpreter + capability-router + capability-head-handlers stay green (backward-compatible; bundled head exempt because _runDeclarativeTier passes no meta; router awaits interpretRecipe).
- service-denylist test passes; service-denylist.json valid (deniedOrigins + sensitiveOrigins); classify returns the right sensitive/denied combination; docs/LEGAL.md has Retention + Consent sections.
- `node scripts/verify-recipe-path-guard.mjs` exits 0 (capability-signature.js + service-denylist.js eval-free + allowlisted).
- No errors.ts edit (RECIPE_SIGNATURE_INVALID matches the existing /^RECIPE_.+$/ passthrough).
</verification>

<success_criteria>
- Non-bundled recipes are Ed25519/JCS signature-verified after schema-validate and before bind; tampers/unsigned -> RECIPE_SIGNATURE_INVALID; bundled recipes exempt via the no-meta default path (no provenance field invented).
- Verify uses native crypto.subtle with a feature-detect and fails closed; zero new packages.
- service-denylist.js is the single source of truth for origin sensitivity: isDenied (checked first, non-enableable) + classify (feeds the gate's sensitive+Auto downgrade, D-14); the seed is conservative and non-empty; docs/LEGAL.md documents posture + retention + consent.
- The Phase-26..29 interpreter/router/head tests stay green (backward-compatible).
</success_criteria>

<output>
Create `.planning/phases/30-consent-governance-recipe-signature-verification-audit-legal/30-03-SUMMARY.md` when done.
</output>
</content>
</invoke>
