---
phase: 35-denylist-expansion-import-time-classification-gate-lands-fir
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - vendor/opentabs-snapshot/PIN.md
  - vendor/opentabs-snapshot/_provenance.json
  - catalog/descriptors/_fixtures/_provenance.json
  - docs/LEGAL.md
  - tests/provenance-scaffold.test.js
  - package.json
autonomous: true
requirements: [DENY-01]
must_haves:
  truths:
    - "vendor/opentabs-snapshot/PIN.md exists, pins OpenTabs commit SHA 4b17021637d2cac12b8d84d21c40e765aa7b85e9, and embeds the verbatim MIT license text + copyright line"
    - "A _provenance.json scaffold exists recording {source:opentabs, sha, license:MIT, apps:[]} for Phase 36 to populate"
    - "docs/LEGAL.md gains a Categorization Axes subsection naming the 3 distinct criteria: (1) finance/gov denial, (2) ToS-hostility denial, (3) sensitivity (Ask / mutating-gated)"
    - "No OpenTabs dist/ or handle() runtime is vendored — only metadata/provenance scaffolding (Wall 1)"
    - "The brokerage + ToS-hostile denied roster apps are named in the LEGAL.md ToS-hostility axis"
  artifacts:
    - path: "vendor/opentabs-snapshot/PIN.md"
      provides: "OpenTabs metadata provenance pin (SHA + MIT license), mirroring .planning/LATTICE-PIN.md discipline"
      contains: "4b17021637d2cac12b8d84d21c40e765aa7b85e9"
    - path: "catalog/descriptors/_fixtures/_provenance.json"
      provides: "Provenance scaffold (under _fixtures/ so readJsonDir never inlines it into the shipped catalog), populated by the Phase-36 importer"
      contains: "4b17021637d2cac12b8d84d21c40e765aa7b85e9"
    - path: "docs/LEGAL.md"
      provides: "Categorization Axes subsection (finance/gov denial vs ToS-hostility denial vs sensitivity)"
      contains: "Categorization Axes"
    - path: "tests/provenance-scaffold.test.js"
      provides: "Asserts PIN.md + _provenance.json presence + SHA match + LEGAL.md axes naming"
      min_lines: 30
  key_links:
    - from: "catalog/descriptors/_fixtures/_provenance.json"
      to: "vendor/opentabs-snapshot/PIN.md"
      via: "both pin the same OpenTabs SHA 4b17021637...; the test asserts they match"
      pattern: "4b17021637d2cac12b8d84d21c40e765aa7b85e9"
    - from: "tests/provenance-scaffold.test.js"
      to: "docs/LEGAL.md"
      via: "asserts the Categorization Axes subsection + the three axis names"
      pattern: "Categorization Axes"
---

<objective>
Land the OpenTabs provenance/legal scaffold (criterion 5): `vendor/opentabs-snapshot/PIN.md` pinning the OpenTabs commit SHA `4b17021637d2cac12b8d84d21c40e765aa7b85e9` + the verbatim MIT license, a `_provenance.json` scaffold for the Phase-36 importer to populate, and a `docs/LEGAL.md` "Categorization Axes" subsection naming the three distinct denial/sensitivity criteria. Add a small `tests/provenance-scaffold.test.js` asserting presence + SHA match + axes naming.

Purpose: Phase 36 vendors OpenTabs metadata and emits provenance-stamped descriptors. This plan lands the pin + license + scaffold so that work is hermetic/offline and auditable, WITHOUT shipping any OpenTabs runtime (Wall 1 — only metadata/provenance, never dist/ or handle()). The LEGAL.md axes subsection makes the ToS-hostility criterion (distinct from finance sensitivity) an explicit, documented basis for the denied roster Plan 01 lands.

Output: PIN.md, two `_provenance.json` scaffolds, the LEGAL.md subsection, and the scaffold test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-denylist-expansion-import-time-classification-gate-lands-fir/35-CONTEXT.md
@.planning/phases/35-denylist-expansion-import-time-classification-gate-lands-fir/35-RESEARCH.md
@.planning/LATTICE-PIN.md

<interfaces>
<!-- The PIN.md discipline to mirror + the verified OpenTabs facts (offline-hermetic; no live gh fetch needed). -->
<!-- Sources: .planning/LATTICE-PIN.md (format), 35-RESEARCH.md Q1 (verified SHA + license + copyright), docs/LEGAL.md -->

LATTICE-PIN.md format: YAML frontmatter (current_* keys: source/repo/sha/ref/pinned_at/license + schema_version)
  + a prose body (Current source / License lines) + a Per-FSB-Phase Log table.

VERIFIED OpenTabs facts (35-RESEARCH Q1, read live via gh api against the pinned SHA — embed these, do NOT re-fetch):
  repo:        github.com/opentabs-dev/opentabs
  commit SHA:  4b17021637d2cac12b8d84d21c40e765aa7b85e9
  commit date: 2026-06-21T21:54:20Z   (subject: "chore: auto-publish plugin version bumps [skip ci]")
  license:     MIT  (spdx_id MIT, path LICENSE)
  copyright:   Copyright (c) 2026-present OpenTabs Contributors

docs/LEGAL.md (verified headings): "## Service Denylist" is at line 107; the Categorization Axes
  subsection belongs as a "### Categorization Axes" under (or immediately after) ## Service Denylist.

Denied roster to name in the ToS-hostility axis (from CONTEXT, the Plan-01 deniedOrigins set):
  brokerage/trading: robinhood, fidelity, carta
  ToS-hostile media/social: netflix, spotify, twitch, steam, youtube-music, tinder, onlyfans

readJsonDir is NON-RECURSIVE and reads ONLY catalog/descriptors/*.json top-level. catalog/descriptors/_provenance.json
  IS a top-level .json -> it WOULD be inlined by readJsonDir as a "descriptor". Mitigation in Task 2.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create vendor/opentabs-snapshot/PIN.md + _provenance.json (SHA + verbatim MIT)</name>
  <files>vendor/opentabs-snapshot/PIN.md, vendor/opentabs-snapshot/_provenance.json</files>
  <read_first>
    - .planning/LATTICE-PIN.md (the PIN discipline to mirror: YAML frontmatter current_* keys + prose body + a Per-Phase Log table)
    - .planning/phases/35-denylist-expansion-import-time-classification-gate-lands-fir/35-RESEARCH.md (Q1 — the verified SHA, license, copyright line, and the recommended PIN.md skeleton)
  </read_first>
  <action>
    Create the new directory `vendor/opentabs-snapshot/` and write `PIN.md` mirroring the .planning/LATTICE-PIN.md discipline. YAML frontmatter keys: `current_opentabs_source: github`, `current_opentabs_repo: github.com/opentabs-dev/opentabs`, `current_opentabs_sha: 4b17021637d2cac12b8d84d21c40e765aa7b85e9`, `current_opentabs_ref: main`, `current_opentabs_pinned_at: "2026-06-21T21:54:20Z"`, `current_opentabs_license: MIT`, `schema_version: 1`. Body: a title "# OpenTabs Snapshot Pin -- FSB <-> OpenTabs metadata provenance", a "**Current source:**" line (repo @ SHA (main, 2026-06-21)), a "**License:**" line (MIT -- Copyright (c) 2026-present OpenTabs Contributors), a "## MIT License (verbatim)" section embedding the FULL MIT license text with the exact copyright line `Copyright (c) 2026-present OpenTabs Contributors` (the standard MIT body: permission grant, the "as is" warranty disclaimer, the liability clause), and a "## Per-FSB-Phase Log" table with one row: `| Phase 35 | 2026-06-24 | 4b170216... | (none yet -- scaffold only; metadata files land in Phase 36) | PIN + license scaffold |`. State explicitly in the body that ONLY OpenTabs metadata is ever vendored here and that its dist/ / handle() runtime is never shipped (Wall 1). Also create `vendor/opentabs-snapshot/_provenance.json` as a minimal scaffold: `{ "source": "opentabs", "repo": "github.com/opentabs-dev/opentabs", "sha": "4b17021637d2cac12b8d84d21c40e765aa7b85e9", "license": "MIT", "pinnedAt": "2026-06-21T21:54:20Z", "apps": [] }` (Phase 36 populates `apps`). Do NOT vendor any OpenTabs source/dist/plugin code — this directory holds ONLY PIN.md + _provenance.json in Phase 35. ASCII-only.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const pin=fs.readFileSync('vendor/opentabs-snapshot/PIN.md','utf8'); const prov=require('./vendor/opentabs-snapshot/_provenance.json'); const SHA='4b17021637d2cac12b8d84d21c40e765aa7b85e9'; if(!pin.includes(SHA)){console.error('PIN.md missing SHA');process.exit(1)} if(!/MIT/.test(pin)||!/OpenTabs Contributors/.test(pin)){console.error('PIN.md missing MIT/copyright');process.exit(1)} if(prov.sha!==SHA||prov.license!=='MIT'||!Array.isArray(prov.apps)){console.error('_provenance.json scaffold bad');process.exit(1)} console.log('vendor pin OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `vendor/opentabs-snapshot/PIN.md` exists, contains the SHA `4b17021637d2cac12b8d84d21c40e765aa7b85e9`, the verbatim MIT license body, and the copyright line `Copyright (c) 2026-present OpenTabs Contributors`.
    - `vendor/opentabs-snapshot/_provenance.json` is valid JSON with `sha` == the pinned SHA, `license` == "MIT", and `apps` == `[]`.
    - The `vendor/opentabs-snapshot/` directory contains ONLY PIN.md + _provenance.json (no .js / dist / plugin code): `ls vendor/opentabs-snapshot/ | grep -vE '^(PIN\\.md|_provenance\\.json)$'` produces no output.
  </acceptance_criteria>
  <done>The OpenTabs pin + verbatim MIT license + provenance scaffold exist; no runtime is vendored (Wall 1).</done>
</task>

<task type="auto">
  <name>Task 2: Add catalog/descriptors/_provenance.json scaffold without polluting the shipped catalog</name>
  <files>catalog/descriptors/_fixtures/_provenance.json</files>
  <read_first>
    - scripts/validate-extension.mjs (readJsonDir at lines 80-86 — it reads catalog/descriptors/*.json top-level and the snapshot cross-check compares against catalog/recipe-index.generated.js)
    - scripts/package-extension.mjs (readJsonDir at line 51 + the descriptors read at line 68 — confirm whether a non-descriptor .json placed in catalog/descriptors/ would be inlined)
    - .planning/phases/35-denylist-expansion-import-time-classification-gate-lands-fir/35-RESEARCH.md (Q3/Pitfall 4 — fixture/non-descriptor isolation from the shipped corpus)
  </read_first>
  <action>
    The RESEARCH recommends a `catalog/descriptors/_provenance.json` scaffold (Phase 36 extends it). But `readJsonDir` reads EVERY top-level `catalog/descriptors/*.json` and `package-extension.mjs` inlines them into `recipe-index.generated.js` as descriptors — a `_provenance.json` placed directly there WOULD be inlined as a bogus descriptor and would also break the validate-extension snapshot cross-check. Resolve this WITHOUT weakening the snapshot: place the catalog-side provenance scaffold at `catalog/descriptors/_fixtures/_provenance.json` (the `_fixtures/` sibling is NOT read by the non-recursive readJsonDir — same isolation the Plan-02 gate fixture uses), OR, if you place it at the literal `catalog/descriptors/_provenance.json` path the RESEARCH names, you MUST first read package-extension.mjs's readJsonDir and confirm it inlines it; if it does, prefer the `_fixtures/` location and note the deviation in the SUMMARY. Recommended: write `catalog/descriptors/_fixtures/_provenance.json` with the same scaffold body as Task 1's vendor provenance (`{ source, repo, sha:"4b17021637...", license:"MIT", pinnedAt, apps:[] }`). After creating it, run `node scripts/package-extension.mjs` (or the package:extension npm script) and then `node scripts/validate-extension.mjs` to PROVE the shipped catalog snapshot is unchanged (the provenance scaffold did not leak into recipe-index.generated.js). If recipe-index.generated.js changed, the scaffold landed in the wrong place — move it under `_fixtures/`. ASCII-only, valid JSON.
  </action>
  <verify>
    <automated>node scripts/validate-extension.mjs && node -e "const p=require('./catalog/descriptors/_fixtures/_provenance.json'); if(p.sha!=='4b17021637d2cac12b8d84d21c40e765aa7b85e9'){process.exit(1)} const fs=require('fs'); const idx=fs.readFileSync('extension/catalog/recipe-index.generated.js','utf8'); if(idx.includes('4b17021637')||idx.includes('opentabs') && idx.includes('_provenance')){console.error('provenance leaked into shipped catalog');process.exit(1)} console.log('catalog provenance scaffold isolated OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The catalog-side provenance scaffold exists as valid JSON with `sha` == the pinned SHA and `apps` == `[]`.
    - `node scripts/validate-extension.mjs` exits 0 (the descriptor snapshot cross-check passes — the scaffold did not enter the shipped corpus).
    - `extension/catalog/recipe-index.generated.js` does NOT contain the OpenTabs SHA or a `_provenance` entry (the scaffold is isolated from the shipped index).
  </acceptance_criteria>
  <done>The catalog-side provenance scaffold exists and is provably excluded from the shipped/validated descriptor corpus.</done>
</task>

<task type="auto">
  <name>Task 3: Add the Categorization Axes subsection to docs/LEGAL.md + the scaffold test</name>
  <files>docs/LEGAL.md, tests/provenance-scaffold.test.js, package.json</files>
  <read_first>
    - docs/LEGAL.md (the file being modified — the "## Service Denylist" section at line 107 already describes deniedOrigins/sensitiveOrigins under the opt-out posture committed in 68ceea90; ADD the axes subsection under/after it, do not rewrite the existing posture text)
    - tests/service-denylist.test.js (the existing LEGAL.md assertions — retention + consent sections; the new test must not conflict)
    - .planning/phases/35-denylist-expansion-import-time-classification-gate-lands-fir/35-CONTEXT.md (Area 3 — the three axes wording: finance/gov denial, ToS-hostility denial, sensitivity)
  </read_first>
  <action>
    Add a `### Categorization Axes` subsection to docs/LEGAL.md, placed under the existing `## Service Denylist` section (after the current deniedOrigins/sensitiveOrigins bullets, before `## Recipe Integrity`). Name the THREE distinct criteria as separate, clearly-labeled items: (1) **Finance / government denial** — categorically-prohibited financial and government origins where automation is denied outright (the existing banking/gov seed); (2) **ToS-hostility denial** — origins denied because automation violates the site's Terms of Service, NAMING the roster: brokerage/trading (robinhood, fidelity, carta) and ToS-hostile media/social (netflix, spotify, twitch, steam, youtube-music, tinder, onlyfans); (3) **Sensitivity (Ask / mutating-gated)** — allowed-but-sensitive origins (payments, budgeting, social, messaging) where reads run under Auto but a WRITE re-enforces the per-origin mutating opt-in (posture B / DENY-04), naming the distinction that instagram/facebook/tiktok/x are sensitive (writes gated) rather than denied. State that these axes are distinct: an origin can be denied for finance/gov reasons OR for ToS-hostility reasons, and sensitivity is a separate, softer tier. Keep the existing opt-out-posture text intact. Then create `tests/provenance-scaffold.test.js` (zero-framework: check(cond,msg) counter, PASS=/FAIL= summary, process.exit(1) on fail) asserting: PIN.md exists and contains the SHA + "MIT" + "OpenTabs Contributors"; vendor _provenance.json and the catalog provenance scaffold both have sha == the pinned SHA (they MATCH); docs/LEGAL.md contains "Categorization Axes" AND the three axis labels AND at least one named denied roster app (e.g. "robinhood" and "onlyfans"); vendor/opentabs-snapshot/ contains no .js file (Wall-1 no-runtime guard via fs.readdirSync filter). Append ` && node tests/provenance-scaffold.test.js` to the `test` npm script chain (next to service-denylist.test.js). ASCII-only.
  </action>
  <verify>
    <automated>node tests/provenance-scaffold.test.js && node tests/service-denylist.test.js && node -e "const s=require('./package.json').scripts; if(!/provenance-scaffold\.test\.js/.test(s.test)){process.exit(1)} console.log('provenance test wired')"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/LEGAL.md` contains `### Categorization Axes`, the three axis labels (finance/government denial, ToS-hostility denial, sensitivity), and the named denied roster apps (robinhood ... onlyfans).
    - `node tests/provenance-scaffold.test.js` exits 0 with FAIL=0 (PIN/_provenance/SHA-match/LEGAL-axes/no-runtime all assert true).
    - `node tests/service-denylist.test.js` still exits 0 (the existing LEGAL.md retention/consent assertions are unbroken by the added subsection).
    - `package.json` `test` script contains `node tests/provenance-scaffold.test.js`.
  </acceptance_criteria>
  <done>LEGAL.md names the three categorization axes (incl. the ToS-hostility roster); the scaffold test asserts the pin, provenance SHA-match, axes, and the Wall-1 no-runtime guarantee.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OpenTabs source -> FSB repo | Only metadata/provenance crosses this boundary; OpenTabs dist/ / handle() runtime must NEVER be vendored or shipped (Wall 1 / MV3 no remotely-hosted code) |
| provenance scaffold -> shipped catalog | A non-descriptor .json placed in catalog/descriptors/ would be inlined into the shipped index by readJsonDir — it must be isolated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-13 | Tampering / provenance (Wall 1) | vendor/opentabs-snapshot/ | mitigate | Phase 35 vendors ONLY PIN.md + _provenance.json; the scaffold test asserts vendor/opentabs-snapshot/ contains no .js file, so no OpenTabs runtime can slip in; PIN.md pins the exact SHA for an auditable, hermetic Phase-36 build |
| T-35-14 | Tampering | provenance scaffold leaking into the shipped catalog | mitigate | The catalog-side scaffold is placed under _fixtures/ (or proven isolated); Task 2 runs validate-extension to confirm recipe-index.generated.js is unchanged (no leak) |
| T-35-15 | Repudiation | unattributed / unpinned OpenTabs use | mitigate | PIN.md records the SHA + verbatim MIT license + copyright line mirroring the LATTICE-PIN discipline; the Per-Phase Log table makes each phase's metadata vendoring auditable |
| T-35-16 | Information disclosure | LEGAL.md misstates the denial basis | accept | The Categorization Axes subsection documents the three distinct criteria (finance/gov vs ToS-hostility vs sensitivity) and names the roster; a documentation-only artifact with no runtime trust surface, asserted present by the scaffold test |
</threat_model>

<verification>
- `node tests/provenance-scaffold.test.js` exits 0 (PIN + provenance SHA-match + LEGAL axes + no-runtime guard).
- `node tests/service-denylist.test.js` exits 0 (existing LEGAL assertions intact).
- `node scripts/validate-extension.mjs` exits 0 (the provenance scaffold did not leak into the shipped catalog).
- `vendor/opentabs-snapshot/` contains exactly PIN.md + _provenance.json (no runtime code).
</verification>

<success_criteria>
- PIN.md pins OpenTabs SHA `4b17021637d2cac12b8d84d21c40e765aa7b85e9` + embeds the verbatim MIT license.
- A `_provenance.json` scaffold exists for the Phase-36 importer (vendor-side + catalog-side, SHA-matched).
- docs/LEGAL.md names the three categorization axes incl. the ToS-hostility roster.
- No OpenTabs runtime is vendored (Wall 1).
</success_criteria>

<output>
Create `.planning/phases/35-denylist-expansion-import-time-classification-gate-lands-fir/35-04-SUMMARY.md` when done.
</output>
