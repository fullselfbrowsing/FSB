---
phase: 54-permission-scoped-drive-corpus-boundary
plan: "06"
type: execute
wave: 4
depends_on:
  - "54-01"
  - "54-03"
  - "54-04"
  - "54-05"
files_modified:
  - extension/utils/skopeo-drive-reconciler.js
  - tests/skopeo-drive-reconciler.test.js
autonomous: true
requirements:
  - CORPUS-01
  - CORPUS-03
  - CORPUS-05
  - CORPUS-06
must_haves:
  truths:
    - "D-54-13: Enrollment performs a complete bounded recursive physical inventory bracketed by a start change token; later change pages are hints requiring fresh targeted ancestry/access proof, and uncertain/token-invalid recovery performs a bounded full rescan."
    - "D-54-02/D-54-03: Reconciliation includes only the root's physical descendant tree, assigns nearest direct-child vendor scope, treats shortcuts as leaves, and supports shared-drive pagination without broad search membership."
    - "D-54-10: Opaque 404 and removed/lost-access hints become inaccessible tombstones; only a complete authoritative inventory can mark a source missing."
    - "D-54-14: Stable file identity, metadata/membership fingerprint, and content fingerprint drive separate actions; rename/move updates metadata/membership without re-extraction when exact content identity is unchanged."
    - "D-54-15/D-54-16: Reconciliation stages invisibly, tombstones/purges before checkpoint/pointer publication, is idempotent across restarts, and never persists full source content."
  artifacts:
    - path: "extension/utils/skopeo-drive-reconciler.js"
      provides: "Initial inventory, change-hint reconciliation, fingerprint transitions, and bounded rescan recovery"
      contains: "FsbSkopeoDriveReconciler"
    - path: "tests/skopeo-drive-reconciler.test.js"
      provides: "Pagination/race/restart/shortcut/shared-drive/missing/fingerprint adversarial oracle"
      contains: "newStartPageToken"
  key_links:
    - from: "extension/utils/skopeo-drive-reconciler.js"
      to: "extension/utils/skopeo-drive-authority.js"
      via: "fresh account/root/source/ancestry proof for every hinted source"
      pattern: "beginOperation|certifySource"
    - from: "extension/utils/skopeo-drive-reconciler.js"
      to: "extension/utils/skopeo-corpus-store.js"
      via: "invisible staging, tombstone-first purge, and final checkpoint/manifest publication"
      pattern: "stageSource|purgeSource|commitInventory"
---

<objective>
Build a restart-safe Drive inventory/reconciliation engine that treats change feeds as hints and physical ancestry as the only membership authority.

Purpose: Keep source state and fingerprints current without broad scans on every event, false deletion, shortcut traversal, or stale derivative influence.
Output: A bounded reconciler and deterministic adversarial test suite.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/54-permission-scoped-drive-corpus-boundary/54-CONTEXT.md
@.planning/phases/54-permission-scoped-drive-corpus-boundary/54-RESEARCH.md
@.planning/phases/54-permission-scoped-drive-corpus-boundary/54-VALIDATION.md
@.planning/phases/54-permission-scoped-drive-corpus-boundary/54-PATTERNS.md
@.planning/phases/54-permission-scoped-drive-corpus-boundary/54-03-SUMMARY.md
@.planning/phases/54-permission-scoped-drive-corpus-boundary/54-04-SUMMARY.md
@.planning/phases/54-permission-scoped-drive-corpus-boundary/54-05-SUMMARY.md
@extension/utils/skopeo-corpus-schema.js
@extension/utils/skopeo-corpus-store.js
@extension/utils/skopeo-drive-corpus-transport.js
@extension/utils/skopeo-drive-authority.js

<interfaces>
Expose `create({schema,store,transport,authority,limits})` with `buildInitialInventory(exactContext)`, `reconcileChanges(exactContext)`, `reconcileSource(exactContext, sourceFileId, hint)`, `resume(exactContext)`, and `abort(reason)`. It stores only bounded metadata/state/fingerprints/checkpoints. A content-read callback may compute a content fingerprint/hand operation-local text to a registered future ingestion sink, but bytes/text never enter reconciler records.
</interfaces>
</context>

<threat_model>
| Threat ID | Severity | Category | Disposition | Mitigation and automated negative proof |
|---|---:|---|---|---|
| T-54-02 | High | Elevation of privilege | mitigate | Physical parent traversal and nearest direct-child scope are re-proven; shortcuts never confer target membership. Tests use sibling/shared/shortcut/multiparent graphs. |
| T-54-05 | High | Information disclosure | mitigate | Hints/removals/404 cannot publish missing or stale fields; tombstone and purge precede any visible checkpoint. Tests inspect visibility at every await. |
| T-54-06 | High | Tampering | mitigate | Baseline token, complete scan, post-scan drain, staging manifest, journals, and pointer-last commit make startup/restart idempotent. Failures are injected on every page/source/store step. |
| T-54-09 | High | Denial of service | mitigate | Bound pages/items/depth/requests/time, detect cycles/repeated tokens, abort on incomplete search, and schedule bounded rescan rather than spin. Tests exhaust every limit. |
| T-54-10 | High | Tampering / Repudiation | mitigate | Separate fingerprint comparisons choose metadata-only, membership change, or content replacement; byte hash is never inferred from rename/version alone. Tests independently mutate each dimension. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write RED baseline/change/recovery and fingerprint decision oracles</name>
  <files>tests/skopeo-drive-reconciler.test.js</files>
  <read_first>
    - extension/utils/skopeo-corpus-schema.js
    - extension/utils/skopeo-corpus-store.js
    - extension/utils/skopeo-drive-corpus-transport.js
    - extension/utils/skopeo-drive-authority.js
    - tests/skopeo-drive-authority.test.js
    - tests/skopeo-drive-corpus-transport.test.js
    - .planning/phases/54-permission-scoped-drive-corpus-boundary/54-PATTERNS.md
  </read_first>
  <action>
Create deterministic graph/page/change/store fakes. Specify initial ordering: fresh identity/root proof; get baseline token; recursively list physical children with shared-drive pagination and visited set; certify each item; stage all records invisibly; drain changes since baseline with targeted reproof; publish inventory and checkpoint last. Cover root files, direct-child vendor folders, nested inheritance, shortcut leaf/no target read, siblings, multiple parents, trashed items, resource keys, duplicate files/pages, pagination, incomplete search, cycles, bounds, identity/root drift, and change races during scan.

Specify later change processing as hints only: re-fetch file and ancestry before update; removed/404 becomes inaccessible, never missing; transient becomes pending; complete authoritative rescan absence becomes missing. Invalid/expired/mismatched tokens, incomplete pages, uncertain ancestry, and corrupt checkpoints schedule bounded full rescan while visible stale data is withheld as required. Fingerprint cases must show rename-only, move/vendor-only, content revision/checksum/hash change, download denial, unsupported type, and exact-content-no-change behavior. Assert source purge precedes checkpoint, duplicates/reordered pages are idempotent, restart at every await converges, and no full text/bytes/log/raw errors persist. Initial run fails only for absent reconciler.
  </action>
  <acceptance_criteria>
    - `rg -n "baseline|newStartPageToken|nextPageToken|incomplete|shortcut|vendor|missing|inaccessible|rename|move|checksum|restart" tests/skopeo-drive-reconciler.test.js` finds the complete matrix.
    - Controlled RED names `FsbSkopeoDriveReconciler`.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set +e; out=$(node tests/skopeo-drive-reconciler.test.js 2&gt;&amp;1); code=$?; set -e; test $code -ne 0; printf "%s\n" "$out" | rg "FsbSkopeoDriveReconciler|skopeo-drive-reconciler"'</automated>
  </verify>
  <done>The RED oracle fixes the scan/change ordering, membership rule, six-state decisions, fingerprint actions, all bounds, and restart semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement bounded inventory, targeted change reproof, and idempotent recovery</name>
  <files>extension/utils/skopeo-drive-reconciler.js, tests/skopeo-drive-reconciler.test.js</files>
  <read_first>
    - tests/skopeo-drive-reconciler.test.js
    - extension/utils/skopeo-corpus-schema.js
    - extension/utils/skopeo-corpus-store.js
    - extension/utils/skopeo-drive-corpus-transport.js
    - extension/utils/skopeo-drive-authority.js
  </read_first>
  <action>
Implement a frozen classic-global/CommonJS reconciler using only injected dependencies and explicit limits. Follow the tested baseline-token/recursive-scan/drain/pointer-last sequence. Model traversal as physical IDs/parents with visited sets; never enqueue shortcut targets. Compute vendor scope from the first physical child below root. Require complete pagination and fresh authority; uncertainty leaves staging closed or withholds affected active records. Store a bounded durable resume cursor/checkpoint through Plan 03, but never authority certificates or source bodies.

For changes, deduplicate hints, then re-fetch/certify source and ancestry; do not infer membership from the event. Apply separate fingerprint decisions so metadata/membership-only updates avoid content reads/extraction, while exact content changes invoke the operation-local read/ingestion seam and stage replacement. Tombstone/purge before state/checkpoint advancement. Map opaque 404 to inaccessible; only a complete rescan's absence set can transition to missing. On invalid tokens, incomplete search, corrupt cursor, cycle/bound exhaustion, or account/root epoch drift, close affected visibility and request one bounded full rescan. Make all operations abortable/idempotent and every RED case GREEN without implementing Phase 55 extraction/index/graph logic.
  </action>
  <acceptance_criteria>
    - `node --check extension/utils/skopeo-drive-reconciler.js` exits 0.
    - `node tests/skopeo-drive-reconciler.test.js` exits 0.
    - `rg -n "FsbSkopeoDriveReconciler|buildInitialInventory|reconcileChanges|reconcileSource|resume|newStartPageToken|commitInventory" extension/utils/skopeo-drive-reconciler.js` finds the complete surface.
    - Tests prove zero shortcut-target reads, missing only after complete rescan, and zero content reads for rename/move with unchanged content fingerprint.
  </acceptance_criteria>
  <verify>
    <automated>node --check extension/utils/skopeo-drive-reconciler.js &amp;&amp; node tests/skopeo-corpus-schema.test.js &amp;&amp; node tests/skopeo-corpus-store.test.js &amp;&amp; node tests/skopeo-drive-corpus-transport.test.js &amp;&amp; node tests/skopeo-drive-authority.test.js &amp;&amp; node tests/skopeo-drive-reconciler.test.js</automated>
  </verify>
  <done>Inventory and changes converge through fresh physical membership proof, closed staging/purge ordering, distinct fingerprints, bounded recovery, and no durable source bodies.</done>
</task>

</tasks>

<verification>
Run the five-test dependency chain through reconciler twice, including its injected crash matrix. Inspect transport calls for no shortcut target reads and store traces for tombstone/purge-before-checkpoint order.
</verification>

<success_criteria>
- Initial inventory is complete, bracketed by change-token replay, and pointer-last.
- Change events are hints; fresh ancestry/access remains authoritative.
- Shared-drive, pagination, shortcut, race, and bound cases fail closed.
- Missing is authoritative-only; opaque 404 remains inaccessible.
- Fingerprint decisions avoid false re-extraction and no source body persists.
</success_criteria>

<output>
After completion, create `.planning/phases/54-permission-scoped-drive-corpus-boundary/54-06-SUMMARY.md`.
</output>
