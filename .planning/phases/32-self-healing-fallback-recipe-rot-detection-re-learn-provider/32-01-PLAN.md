---
phase: 32-self-healing-fallback-recipe-rot-detection-re-learn-provider
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/capability-rot-detector.test.js
  - tests/provider-parity.test.js
  - tests/recipe-schema-lock.test.js
  - tests/capability-router.test.js
  - tests/capability-autopilot-parity.test.js
  - scripts/verify-recipe-path-guard.mjs
  - package.json
autonomous: true
requirements: [HEAL-01, HEAL-02, HEAL-03, HEAL-04, HEAL-05]
user_setup: []

must_haves:
  truths:
    - "node tests/capability-rot-detector.test.js exists and runs (RED today: the module it requires does not exist yet)"
    - "node tests/provider-parity.test.js exists and runs (asserts the capability+fallback decision is equivalent across all 7 PROVIDER_KEYS)"
    - "node tests/recipe-schema-lock.test.js exists and runs (freezes the v2 RECIPE_SCHEMA hash + re-asserts the frozen tool registry hash ad6efb8c...)"
    - "tests/capability-router.test.js carries new RED assertions: a broken recipe routes to RECIPE_DOM_FALLBACK_PENDING; quarantine fires on the rot path; runDiscovery re-learn trigger is wired"
    - "tests/capability-autopilot-parity.test.js carries a new RED assertion: the autopilot door surfaces the typed reason / fellBackToDom in makeResult"
    - "scripts/verify-recipe-path-guard.mjs lists extension/utils/capability-rot-detector.js in RECIPE_PATH_ALLOWLIST (pre-armed ahead of file creation)"
    - "package.json test chain appends the 3 new test files after learned-local-provenance-exempt.test.js"
    - "node tests/agent-loop-iterator-guard.test.js stays GREEN (INV-04: iterator byte-untouched)"
  artifacts:
    - path: "tests/capability-rot-detector.test.js"
      provides: "The HEAL-02/HEAL-04 taxonomy + expectedShape RED suite (calls classifyRecipeBroken with synthetic executeBoundSpec results)"
      min_lines: 80
    - path: "tests/provider-parity.test.js"
      provides: "HEAL-05/INV-03 provider-parity RED suite (7 PROVIDER_KEYS x capability+fallback equivalence)"
      min_lines: 60
    - path: "tests/recipe-schema-lock.test.js"
      provides: "HEAL-05/INV-01 schema-lock RED suite (frozen v2 RECIPE_SCHEMA hash + frozen tool registry hash)"
      min_lines: 50
    - path: "scripts/verify-recipe-path-guard.mjs"
      provides: "RECIPE_PATH_ALLOWLIST entry for capability-rot-detector.js"
      contains: "capability-rot-detector.js"
    - path: "package.json"
      provides: "Test-chain wiring for the 3 new suites"
      contains: "capability-rot-detector.test.js"
  key_links:
    - from: "package.json scripts.test"
      to: "tests/capability-rot-detector.test.js"
      via: "&& chain append after learned-local-provenance-exempt.test.js"
      pattern: "capability-rot-detector\\.test\\.js"
    - from: "scripts/verify-recipe-path-guard.mjs RECIPE_PATH_ALLOWLIST"
      to: "extension/utils/capability-rot-detector.js"
      via: "allowlist array entry"
      pattern: "capability-rot-detector\\.js"
---

<objective>
Wave 0 validation contract for Phase 32. Author the three NEW zero-framework test suites (the rot-detector taxonomy, the 7-provider parity gate, the schema-lock gate), extend the two existing capability suites with the new fallback/quarantine assertions, pre-arm the recipe-path CI guard allowlist for the not-yet-created rot-detector module, and wire the three new files into the npm test chain. These tests are RED today (the modules/edits they exercise land in Plans 02-04) and turn GREEN as later waves implement against them. The INV-04 iterator guard must stay GREEN throughout.

Purpose: Nyquist-arm the phase so every later task is sampled by a fast automated check before it is built (D-13, D-14, D-15, D-16; HEAL-01..05).
Output: 3 new test files, 2 extended test files, 1 allowlist edit, 1 package.json edit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-self-healing-fallback-recipe-rot-detection-re-learn-provider/32-CONTEXT.md
@.planning/phases/32-self-healing-fallback-recipe-rot-detection-re-learn-provider/32-RESEARCH.md
@.planning/phases/32-self-healing-fallback-recipe-rot-detection-re-learn-provider/32-VALIDATION.md

<interfaces>
<!-- Extracted from the codebase. Use these directly; no exploration needed. -->

The zero-framework FSB test convention (every tests/*.test.js):
```
let passed = 0, failed = 0;
function check(cond, msg) { if (cond) { passed++; console.log('  PASS:', msg); } else { failed++; console.error('  FAIL:', msg); } }
// ... assertions ...
console.log('  passed:', passed); console.log('  failed:', failed);
process.exit(failed > 0 ? 1 : 0);
```

The frozen-hash harness to clone (tests/tool-definitions-parity.test.js:37,52-69):
```javascript
const PROVIDER_KEYS = ['xai', 'openai', 'anthropic', 'gemini', 'openrouter', 'lmstudio', 'custom'];
const EXPECTED_NON_TRIGGER_REGISTRY_HASH = 'ad6efb8cc3275d964488b67222129b1c0278c5c3b69c64888d926beb89a3926b';
function stable(value) { /* recursive key-sort */ }
function registryHash(tools) { return crypto.createHash('sha256').update(JSON.stringify(tools.map(stable))).digest('hex'); }
function formattedToolNames(formatted, provider) { /* gemini functionDeclarations / anthropic flat / OpenAI function.name */ }
// const { formatToolsForProvider } = require('../extension/ai/tool-use-adapter.js');
// const agentLoop = require('../extension/ai/agent-loop.js');  // agentLoop.getPublicTools()
```

The router test harness idiom (tests/capability-router.test.js:48-130):
```javascript
const harness = require('./fixtures/run-task-harness');   // installChromeMock({ tabs:[...] }) -> recorder
const ROUTER = require(ROUTER_PATH);                       // require() loads the IIFE; sets globalThis.FsbCapabilityRouter
function installCatalog(entriesBySlug, originBiasFn) { /* stubs globalThis.FsbCapabilityCatalog.resolve */ }
// Existing CAT-05 assertion at :203-213: installCatalog({ 'some.dom.slug': { tier:'T3' } });
//   then check dom.code === 'RECIPE_DOM_FALLBACK_PENDING' AND the recorder fired NO executeScript AND executeTool was NOT called.
```

The autopilot-parity spy idiom (tests/capability-autopilot-parity.test.js:14-26): a spy installed on
globalThis.FsbCapabilityRouter.invoke is hit by both front doors; the autopilot wrapper returns
makeResult-shaped { success, hadEffect, error, navigationTriggered, result } whose result is the router response.

The executeBoundSpec normalized result shapes the rot-detector taxonomy keys off (capability-fetch.js):
- success (:377-384): { success:true, status, finalUrl, redirected, data, text }
- failure (:336-339 / :356 / :359): { success:false, error:'...' }
- pin mismatch (:293): dual-field { success:false, code:'RECIPE_ORIGIN_MISMATCH', errorCode:'RECIPE_ORIGIN_MISMATCH', ... }

The RECIPE_SCHEMA + version live in extension/utils/capability-recipe-schema.js (FSB_RECIPE_SCHEMA_VERSION:59, RECIPE_SCHEMA:79-148). Node loads it via require() (it is a dual-export IIFE). cfworker must be preloaded; the existing tests/capability-recipe-schema.test.js shows the load idiom.

The npm test chain currently ENDS with: ... && node tests/learned-local-provenance-exempt.test.js
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the rot-detector taxonomy RED suite + pre-arm the recipe-path allowlist</name>
  <files>tests/capability-rot-detector.test.js, scripts/verify-recipe-path-guard.mjs</files>
  <read_first>
    - WRITE tests/capability-rot-detector.test.js (new)
    - MODIFY scripts/verify-recipe-path-guard.mjs (RECIPE_PATH_ALLOWLIST array at :90-117 — add one entry; follow the array, NOT the stale "EXACTLY the six recipe-path files" header comment at :85)
    - Analog (zero-framework + require-the-IIFE + jmespath global): tests/capability-recipe-schema.test.js (the cfworker/jmespath preload idiom) + tests/tool-definitions-parity.test.js:54-63 (the check() + counters skeleton)
    - Analog (the result shapes to synthesize): extension/utils/capability-fetch.js:336-384 (success :377-384 carries {success,status,redirected,data}; failures :336/:356/:359; pin mismatch :293)
    - Source of truth for the taxonomy + the JMESPath validation contract: 32-RESEARCH.md Pattern 2 (decision table), Pattern 3 (validateExpectedShape predicate), Open Question 1 (the present-container predicate)
  </read_first>
  <action>
    Create tests/capability-rot-detector.test.js using the zero-framework convention (passed/failed counters, check(cond,msg), process.exit(failed>0?1:0); ASCII-only, NO emojis). require('../extension/utils/capability-rot-detector.js') to obtain FsbCapabilityRotDetector with classifyRecipeBroken(result, recipe) and validateExpectedShape(data, expectedShape). This require FAILS today (the module lands in Plan 02) — that is the correct Wave 0 RED state. Set globalThis.jmespath = require of the vendored jmespath path (mirror capability-router.test.js:84) so validateExpectedShape can reach the engine; also require the interpreter so getFSBJmespath resolves if the detector uses it.

    Assert the full HEAL-04 taxonomy by calling classifyRecipeBroken directly with synthetic executeBoundSpec result objects (per D-01, the decision table in RESEARCH Pattern 2). Each row is a check():
    - { success:true, status:404 } and { success:true, status:503 } -> broken === true (HTTP 4xx/5xx -> fallback).
    - { success:true, redirected:true } -> broken === false AND code === 'RECIPE_LOGGED_OUT' (a 302->login is surfaced, NOT healed — the load-bearing logged-out line, D-01).
    - { success:true, status:200, data:[] } with a recipe carrying expectedShape:'@' -> broken === false (a present-but-empty container of the expected kind is a REAL empty result — the load-bearing HEAL-04 "never mask a real no-results" line; assert "0 results passes" explicitly per RESEARCH Open Question 1 / Pitfall 2).
    - { success:true, status:200, data:'<html>login</html>' } (or data:null) with a recipe whose expectedShape requires an object/array path that the HTML/null fails -> broken === true AND code === 'RECIPE_EXPIRED' (a login-HTML / wrong-kind body fails expectedShape -> rot; assert "login-HTML fails" explicitly).
    - { success:false, error:'no result from page fetch' } -> broken === true (fetch-failed -> RECIPE_EXPIRED).
    - { success:false, code:'RECIPE_ORIGIN_MISMATCH', errorCode:'RECIPE_ORIGIN_MISMATCH' } -> broken === false, reason a typed-passthrough (a SECURITY rejection is NOT a rot and MUST NOT trigger fallback — Pitfall 3, threat T-32-PASS). Do the same passthrough assertion for a synthetic RECIPE_CONSENT_REQUIRED dual-field result.
    Add direct validateExpectedShape(data, expectedShape) assertions: a present non-empty value passes; a present empty array/object of the expected kind passes (conservative D-06); a null / missing path fails. Confirm NO assertion logs result.data / result.text raw (V7 redaction posture — the suite reads fields but never console.logs a body).

    Then MODIFY scripts/verify-recipe-path-guard.mjs: add the string 'extension/utils/capability-rot-detector.js' to the RECIPE_PATH_ALLOWLIST array (after the Phase-29 capability-catalog.js entry at :113), with a one-line ASCII comment noting it is pre-armed ahead of its Plan-02 creation (the Phase-27/28/29 "pre-arm in the same milestone" precedent — Check 1 skips a not-yet-existent path via existsSync; Check 4 fails CLOSED the moment the file lands not on this allowlist). Do NOT touch the stale header comment; the array is authoritative.
  </action>
  <verify>
    <automated>node scripts/verify-recipe-path-guard.mjs && node -e "require('fs').readFileSync('tests/capability-rot-detector.test.js','utf8')"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-recipe-path-guard.mjs` exits 0 (the allowlist entry for a not-yet-existent file is skipped by the existsSync pre-check; no disk-drift failure).
    - `node tests/capability-rot-detector.test.js` exits NON-zero today with a module-not-found / undefined-FsbCapabilityRotDetector failure (correct RED state — the module is Plan 02). The file parses as valid Node (no syntax error).
    - The new test file contains an explicit "0 results passes" assertion AND an explicit "login-HTML fails" assertion AND a "RECIPE_ORIGIN_MISMATCH passthrough (not broken)" assertion (grep the source for those three behaviors).
  </acceptance_criteria>
  <done>tests/capability-rot-detector.test.js exists with the full taxonomy + expectedShape assertions (RED today); capability-rot-detector.js is on RECIPE_PATH_ALLOWLIST; the recipe-path guard stays green.</done>
</task>

<task type="auto">
  <name>Task 2: Author the provider-parity + schema-lock RED suites</name>
  <files>tests/provider-parity.test.js, tests/recipe-schema-lock.test.js</files>
  <read_first>
    - WRITE tests/provider-parity.test.js (new)
    - WRITE tests/recipe-schema-lock.test.js (new)
    - Analog (harness pieces — reuse verbatim): tests/tool-definitions-parity.test.js (PROVIDER_KEYS :37, EXPECTED_NON_TRIGGER_REGISTRY_HASH :52, stable/registryHash :54-69, formattedToolNames :75-84, the formatToolsForProvider + agentLoop.getPublicTools requires :120-122)
    - Analog (second schema-lock clone template): tests/visual-session-schema-lock.test.js
    - Analog (the router-invoke spy for the fallback-decision equivalence): tests/capability-autopilot-parity.test.js:14-26 (spy on globalThis.FsbCapabilityRouter.invoke)
    - Source of truth: 32-RESEARCH.md Pattern 5 (Provider Parity + Schema-Lock) + D-13 / D-14
  </read_first>
  <action>
    Create tests/provider-parity.test.js (zero-framework; ASCII-only). Reuse PROVIDER_KEYS (the 7: xai/openai/anthropic/gemini/openrouter/lmstudio/custom), formatToolsForProvider (from ../extension/ai/tool-use-adapter.js), agentLoop.getPublicTools() (from ../extension/ai/agent-loop.js), and formattedToolNames — copied verbatim from tool-definitions-parity.test.js. For EACH of the 7 providers assert: (a) the public tools format without error and the two capability tools (invoke_capability, search_capabilities) are NOT present in the formatted output (they are out-of-registry, INV-01 — getPublicTools maps only the registry), i.e. the capability surface is provider-independent; and (b) the FALLBACK DECISION is equivalent across providers — install a spy on globalThis.FsbCapabilityRouter.invoke that returns a synthetic broken result { success:false, code:'RECIPE_DOM_FALLBACK_PENDING', errorCode:'RECIPE_DOM_FALLBACK_PENDING' }, then assert the typed reason the front door surfaces is identical regardless of which provider's formatted tools are in play (the router sits BELOW the provider layer and never branches on provider, so the assertion is byte-equal typed reason across all 7). This is RED until Plan 04 confirms the wiring end-to-end; some assertions may pass today (the format half) while the fallback-decision half depends on the Plan 03 router emit — keep both so the suite reds until the phase is complete (INV-03, HEAL-05).

    Create tests/recipe-schema-lock.test.js (zero-framework; ASCII-only). Clone stable() + a schemaHash(schema) = crypto.createHash('sha256').update(JSON.stringify(stable(schema))).digest('hex'). require('../extension/utils/capability-recipe-schema.js') to obtain FsbCapabilityRecipeSchema (preload cfworker the same way capability-recipe-schema.test.js does if the module needs it at load). Assert: (1) FSB_RECIPE_SCHEMA_VERSION === 2 (D-08 — RED today; it is 1 until Plan 02); (2) schemaHash(RECIPE_SCHEMA) === a FROZEN_RECIPE_SCHEMA_V2_HASH constant — leave the constant as a CLEARLY-MARKED placeholder string (e.g. 'TBD-FROZEN-IN-PLAN-04') with an ASCII comment that Plan 04 computes-once-and-pastes the real hash at first green (the schema does not exist in v2 form yet, so the hash cannot be known now); (3) re-assert registryHash(nonTriggerTools) === 'ad6efb8cc3275d964488b67222129b1c0278c5c3b69c64888d926beb89a3926b' (the frozen tool registry hash is unmoved — this half can pass today). The suite reds until both the v2 bump (Plan 02) and the frozen-hash paste (Plan 04) land.
  </action>
  <verify>
    <automated>node -e "require('fs').readFileSync('tests/provider-parity.test.js','utf8'); require('fs').readFileSync('tests/recipe-schema-lock.test.js','utf8')"</automated>
  </verify>
  <acceptance_criteria>
    - Both files parse as valid Node and run via `node tests/provider-parity.test.js` and `node tests/recipe-schema-lock.test.js`.
    - tests/recipe-schema-lock.test.js exits NON-zero today (FSB_RECIPE_SCHEMA_VERSION is still 1 and/or the frozen v2 hash placeholder mismatches — correct RED state).
    - tests/recipe-schema-lock.test.js re-asserts the literal frozen tool registry hash `ad6efb8cc3275d964488b67222129b1c0278c5c3b69c64888d926beb89a3926b` (grep confirms).
    - tests/provider-parity.test.js iterates all 7 PROVIDER_KEYS (grep confirms the array of 7 provider names).
  </acceptance_criteria>
  <done>provider-parity.test.js + recipe-schema-lock.test.js exist as RED suites cloning the frozen-hash + provider-format harness; the frozen-tool-hash half is asserted; the v2-schema-hash is a marked placeholder for Plan 04.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the router + autopilot suites for the fallback/quarantine assertions; wire the npm test chain; confirm INV-04 guard green</name>
  <files>tests/capability-router.test.js, tests/capability-autopilot-parity.test.js, package.json</files>
  <read_first>
    - MODIFY tests/capability-router.test.js (the CAT-05 T3 block at :192-213 is the analog — the new broken-recipe assertions sit alongside it; installCatalog at :130, installChromeMock at :197/:224, the T1b GITHUB_RECIPE fixture at :237)
    - MODIFY tests/capability-autopilot-parity.test.js (the router-invoke spy + makeResult-shape assertions at :14-26 are the analog — add the typed-reason/fellBackToDom surfacing assertion)
    - MODIFY package.json (scripts.test — the && chain currently ENDS at `node tests/learned-local-provenance-exempt.test.js`; append the 3 new files there)
    - Analog (the INV-04 guard that MUST stay green): tests/agent-loop-iterator-guard.test.js (byte-checks the 4 setTimeout lines at agent-loop.js:2026/2725/2794/2804 + asserts exactly 4 schedule matches)
    - Source of truth: 32-VALIDATION.md Wave 0 Requirements + 32-RESEARCH.md Pattern 1 (the typed-reason emit) / Pattern 4 (quarantine + runDiscovery wiring)
  </read_first>
  <action>
    Extend tests/capability-router.test.js with new assertions (RED until Plan 03 realizes the router classify-hook + quarantine + re-learn wiring; ASCII-only, NO emojis):
    - A T1b (or T2) recipe whose executeBoundSpec returns a BROKEN result (synthesize a 4xx, or an expectedShape-mismatch via a recipe carrying expectedShape + an empty/wrong-kind data) routes through classifyRecipeBroken and the router returns the dual-field RECIPE_DOM_FALLBACK_PENDING (both .code and .errorCode), carrying the underlying reason (e.g. RECIPE_EXPIRED) in an extra field — extending the existing CAT-05 T3 contract to fire after a broken fetch, not only on a pre-declared T3 tier (D-01, HEAL-01). Reuse installCatalog + the run-task-harness executeScript recorder to drive the broken result shape; stub globalThis.FsbCapabilityRotDetector if needed so the assertion targets the router's HANDLING of a broken verdict.
    - On the rot path, the router calls quarantine: for a T2 learned slug it invokes FsbLearnedRecipeStore.quarantine(slug, origin) (spy it); for a bundled slug it invokes the catalog's quarantineBundled(slug) (spy it). Assert the quarantine call fired (HEAL-03, D-09).
    - The post-fallback re-learn trigger is WIRED: assert FsbDiscoverySession.runDiscovery is CALLABLE on the rot path (spy it; the CI half asserts wiring — the live re-learn-after-real-fallback is UAT per D-10/A5). Place this assertion where Plan 03 will wire the trigger (router or autopilot door — note in a comment that the exact trigger point is Plan 03's, and the CI assertion is "the trigger is reachable/wired", not "a real fallback completed").
    - A LEGITIMATE no-results (200 + valid shape + empty set) and a LOGGED-OUT (redirected:true -> RECIPE_LOGGED_OUT) each return their REAL/typed result and do NOT route to RECIPE_DOM_FALLBACK_PENDING and fire NO quarantine (HEAL-04 never-mask + the logged-out-not-healed line).

    Extend tests/capability-autopilot-parity.test.js: when the spied router.invoke returns a broken result carrying RECIPE_DOM_FALLBACK_PENDING (or a fellBackToDom marker), assert executeCapabilityToolForAutopilot surfaces the typed reason in the makeResult shape — i.e. makeResult.result is the router response carrying the typed code, and makeResult.error reflects response.error || response.errorCode (the autopilot front door surfaces the reason so the model sees it next iteration — HEAL-01 CI half, D-02). RED until Plan 03 ensures the marker is present.

    MODIFY package.json scripts.test: append `&& node tests/provider-parity.test.js && node tests/recipe-schema-lock.test.js && node tests/capability-rot-detector.test.js` to the END of the && chain, AFTER `node tests/learned-local-provenance-exempt.test.js` (the 3 new files run last; the capability/learned cluster precedes them). Do NOT reorder or remove any existing entry.

    Run `node tests/agent-loop-iterator-guard.test.js` and confirm it stays GREEN (this plan touches NO production code, so the iterator is untouched — INV-04 baseline established for the phase).
  </action>
  <verify>
    <automated>node tests/agent-loop-iterator-guard.test.js && node -e "const t=require('./package.json').scripts.test; process.exit((t.includes('capability-rot-detector.test.js') && t.includes('provider-parity.test.js') && t.includes('recipe-schema-lock.test.js') && t.indexOf('learned-local-provenance-exempt.test.js') < t.indexOf('capability-rot-detector.test.js')) ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/agent-loop-iterator-guard.test.js` exits 0 (INV-04 baseline green; this plan touched no production code).
    - `node -e "const t=require('./package.json').scripts.test; process.exit((t.includes('capability-rot-detector.test.js') && t.includes('provider-parity.test.js') && t.includes('recipe-schema-lock.test.js') && t.indexOf('learned-local-provenance-exempt.test.js') < t.indexOf('capability-rot-detector.test.js')) ? 0 : 1)"` exits 0 (all 3 appended AFTER the learned suite).
    - tests/capability-router.test.js now contains a broken-recipe -> RECIPE_DOM_FALLBACK_PENDING assertion AND a quarantine-fired assertion AND a runDiscovery-wired assertion AND a legitimate-no-results-NOT-healed assertion (grep the source for these).
    - The extended router + autopilot suites exit NON-zero today (RED — the router classify-hook + autopilot marker land in Plan 03), while every UNRELATED pre-existing assertion in those files is preserved (no deletions).
  </acceptance_criteria>
  <done>The router + autopilot suites carry the new fallback/quarantine/re-learn RED assertions; the 3 new test files are wired into npm test after the learned cluster; the INV-04 iterator guard is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> production modules under test | Tests require() production IIFEs; a test must not weaken a production invariant (the frozen hashes, the iterator) to "go green" |
| synthetic result objects -> classifyRecipeBroken | The taxonomy is exercised with hand-built result shapes; the test must cover the security-passthrough rows (RECIPE_ORIGIN_MISMATCH / RECIPE_CONSENT_*) so a later impl cannot silently treat a security rejection as a rot |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-MASK | Tampering/Repudiation | tests/capability-rot-detector.test.js | mitigate | The suite asserts BOTH "0 results passes (returned verbatim)" AND "login-HTML fails (rot)" so a future classifier change cannot start masking a real empty outcome (HEAL-04) without reddening this gate. |
| T-32-PASS | Elevation of Privilege | tests/capability-rot-detector.test.js | mitigate | Explicit assertions that RECIPE_ORIGIN_MISMATCH and RECIPE_CONSENT_* dual-field results classify as NOT-broken (no fallback) — a security rejection must never be healed away (Pitfall 3). |
| T-32-LOCK | Tampering | tests/recipe-schema-lock.test.js | mitigate | Re-asserts the frozen tool registry hash ad6efb8c... so any accidental tool-schema drift reds the build (INV-01). |
| T-32-LEAK | Information Disclosure | tests/capability-rot-detector.test.js | mitigate | The suite reads result fields but never console.logs result.data / result.text raw, modeling the V7 no-body-in-logs posture the Plan-02 detector must follow. |
| T-32-IT | Tampering | tests/agent-loop-iterator-guard.test.js | accept | This plan touches no production code; the guard's green baseline is recorded and re-run. No iterator edit possible here. |
| T-32-SC | Tampering | npm/pip/cargo installs | accept | Phase 32 installs NO external packages (RESEARCH Package Legitimacy Audit: N/A). No slopcheck surface. |
</threat_model>

<verification>
- `node scripts/verify-recipe-path-guard.mjs` exits 0 (allowlist pre-arm safe).
- `node tests/agent-loop-iterator-guard.test.js` exits 0 (INV-04 baseline).
- The 3 new suites exist and parse; the rot-detector + recipe-schema-lock + extended router/autopilot suites are RED today (correct Wave 0 state — their targets land in Plans 02-04).
- package.json test chain appends the 3 new files after the learned cluster, no reorder.
</verification>

<success_criteria>
- 3 new test files created (capability-rot-detector, provider-parity, recipe-schema-lock).
- 2 existing test files extended (capability-router, capability-autopilot-parity) with the fallback/quarantine/re-learn assertions, no pre-existing assertion deleted.
- capability-rot-detector.js pre-armed on RECIPE_PATH_ALLOWLIST.
- npm test chain wired.
- INV-04 iterator guard green.
</success_criteria>

<output>
Create `.planning/phases/32-self-healing-fallback-recipe-rot-detection-re-learn-provider/32-01-SUMMARY.md` when done
</output>
