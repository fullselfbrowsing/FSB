---
phase: 32-self-healing-fallback-recipe-rot-detection-re-learn-provider
plan: 05
type: execute
wave: 5
depends_on: ["32-04"]
files_modified:
  - .planning/phases/32-self-healing-fallback-recipe-rot-detection-re-learn-provider/32-HUMAN-UAT.md
autonomous: false
requirements: [HEAL-01, HEAL-03]
user_setup: []

must_haves:
  truths:
    - "32-HUMAN-UAT.md records the live self-healing scenario (a real broken/rotted recipe on a real authenticated site -> the autopilot completes the SAME task via DOM tools -> the recipe is quarantined -> a re-learn is offered/triggered) as human_needed UAT, matching the Phase 27/28/29/30/31 posture"
    - "The doc states clearly the CI/automated half (the taxonomy + the router fallback emit + the autopilot surfacing + quarantine + the wired re-learn trigger) is GREEN (Plans 02-04), and ONLY the irreducibly-live end-to-end completion is human_needed — NOT a fabricated pass (D-15)"
    - "The checkpoint pauses for the human; in auto-mode it is recorded as live-browser UAT debt joining the v0.9.99 ledger"
  artifacts:
    - path: ".planning/phases/32-self-healing-fallback-recipe-rot-detection-re-learn-provider/32-HUMAN-UAT.md"
      provides: "the human_needed live self-healing UAT record (UAT-32-01) + the exact manual steps"
      min_lines: 30
  key_links:
    - from: "32-HUMAN-UAT.md"
      to: "the green CI half (Plans 02-04)"
      via: "explicit statement that only the live half is human_needed (not fabricated)"
      pattern: "human_needed"
---

<objective>
Author the human_needed live-UAT record for the one irreducibly-live Phase-32 behavior — self-healing on a REAL broken recipe against a REAL authenticated site, the autopilot completing the SAME task via DOM, the recipe quarantined, and a re-learn offered — and gate a checkpoint for it. This matches the Phase 27/28/29/30/31 posture: the automated/CI half is green (Plans 02-04 prove the taxonomy, the router fallback emit, the autopilot surfacing, quarantine, and the wired re-learn trigger headlessly); the live end-to-end completion cannot run in CI (it needs a live Chrome extension + a real authenticated origin + a genuinely-broken recipe, and a real credential is forbidden in CI per GOV-06). This is recorded debt, NOT a fabricated pass (D-15).

Purpose: HEAL-01 / HEAL-03 live half — the supervised, live-browser proof of the self-healing flow, deferred as human_needed UAT.
Output: 1 UAT doc + a gated checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-self-healing-fallback-recipe-rot-detection-re-learn-provider/32-CONTEXT.md
@.planning/phases/32-self-healing-fallback-recipe-rot-detection-re-learn-provider/32-VALIDATION.md
@.planning/phases/32-self-healing-fallback-recipe-rot-detection-re-learn-provider/32-04-SUMMARY.md

<interfaces>
<!-- The established human_needed UAT doc shape to mirror. -->

The Phase 27/29/30 UAT precedent (27-HUMAN-UAT.md UAT-27-01, 29-HUMAN-UAT.md, 30-HUMAN-UAT.md UAT-30-01): a markdown doc with a status (human_needed), the scenario, the exact manual steps, the pass/fail criteria, and an explicit statement that the CI/automated half is green and only the live half is deferred (NOT a fabricated pass). The 32-VALIDATION.md "Manual-Only Verifications" table already specifies the scenario + the test instructions for UAT-32-01 — author the doc from it.

The scenario (32-VALIDATION.md Manual-Only Verifications): "Live self-healing: a real recipe that has rotted (or is forced to 4xx/empty) on a real authenticated site is detected (RECIPE_EXPIRED), the autopilot falls back to the DOM tools and STILL completes the task, the recipe is quarantined, and a re-learn is offered/triggered." Test instructions: load the unpacked extension, sign in, invoke a capability whose recipe is broken, confirm the autopilot completes the task via DOM (not an error), the recipe is quarantined, and a re-learn is offered.
</interfaces>

<read_first_global>
Per ./CLAUDE.md: NO emojis anywhere in the doc. This plan creates ONLY a planning markdown file (no code, no test, no production change).
</read_first_global>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 32-HUMAN-UAT.md (UAT-32-01, human_needed live self-healing)</name>
  <files>.planning/phases/32-self-healing-fallback-recipe-rot-detection-re-learn-provider/32-HUMAN-UAT.md</files>
  <read_first>
    - WRITE .planning/phases/32-self-healing-fallback-recipe-rot-detection-re-learn-provider/32-HUMAN-UAT.md (new)
    - Analog (the doc shape + the "CI green, live deferred, not fabricated" framing): .planning/phases/27-...resume-s/27-HUMAN-UAT.md (UAT-27-01) and .planning/phases/30-...legal/30-HUMAN-UAT.md (UAT-30-01)
    - Source of truth: 32-VALIDATION.md "Manual-Only Verifications" (the scenario + test instructions for UAT-32-01) + D-15 (the live self-healing is human_needed UAT, not a CI gate)
  </read_first>
  <action>
    Create 32-HUMAN-UAT.md with frontmatter/status human_needed and a single UAT-32-01 entry. Include: (1) the scenario (live self-healing end-to-end — a real rotted/broken recipe on a real authenticated origin is detected as RECIPE_EXPIRED, the autopilot completes the SAME task via the DOM tools, the recipe is quarantined, and a re-learn is offered/triggered); (2) the exact manual steps (load the unpacked extension; sign in to the target origin; invoke a capability whose recipe is broken — or force it to 4xx/empty; confirm the autopilot completes the task via DOM, NOT a hard error; confirm the recipe is quarantined/demoted from routing; confirm a consent-gated re-learn is offered/triggered); (3) explicit pass/fail criteria; (4) a clear statement that the CI/automated half is GREEN (Plans 02-04: the classifyRecipeBroken taxonomy, the router RECIPE_DOM_FALLBACK_PENDING emit, the autopilot makeResult surfacing, the learned/bundled quarantine, and the wired consent-gated runDiscovery re-learn trigger all proven headlessly) and that ONLY the irreducibly-live end-to-end completion is human_needed — this is recorded debt, NOT a fabricated pass, and it cannot run in CI without a real credential (GOV-06); (5) a note that it joins the v0.9.99 live-browser UAT ledger alongside UAT-27-01 / the Phase 29/30/31 live items. ASCII-only, NO emojis.
  </action>
  <verify>
    <automated>test -f .planning/phases/32-self-healing-fallback-recipe-rot-detection-re-learn-provider/32-HUMAN-UAT.md && grep -q "human_needed" .planning/phases/32-self-healing-fallback-recipe-rot-detection-re-learn-provider/32-HUMAN-UAT.md && grep -q "UAT-32-01" .planning/phases/32-self-healing-fallback-recipe-rot-detection-re-learn-provider/32-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - The file .planning/phases/32-self-healing-fallback-recipe-rot-detection-re-learn-provider/32-HUMAN-UAT.md exists.
    - `grep -c "human_needed" .planning/phases/32-self-healing-fallback-recipe-rot-detection-re-learn-provider/32-HUMAN-UAT.md` is >= 1.
    - The doc names UAT-32-01 and states the CI/automated half is green (the taxonomy + router emit + autopilot surfacing + quarantine + re-learn trigger) and only the live half is deferred (grep for "UAT-32-01" and a "not a fabricated pass" / equivalent statement).
    - No emojis in the file.
  </acceptance_criteria>
  <done>32-HUMAN-UAT.md records UAT-32-01 (live self-healing) as human_needed with the manual steps + the CI-green/live-deferred framing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live self-healing UAT checkpoint (human_needed)</name>
  <what-built>The full self-healing stack is implemented and the CI half is green (Plans 01-04: the rot-detector taxonomy, the v2 schema, the router RECIPE_DOM_FALLBACK_PENDING emit + quarantine + the wired re-learn trigger, the autopilot DOM-completion surfacing + the strengthened prompt hint, the 7-provider parity gate, and the schema-lock gate — the full npm test is green). 32-HUMAN-UAT.md (UAT-32-01) records the irreducibly-live end-to-end self-healing scenario as human_needed.</what-built>
  <how-to-verify>
    Optional live verification (auto-mode: record as live-browser UAT debt, do NOT block the milestone close):
    1. Build + load the unpacked extension in Chrome.
    2. Sign in to a target authenticated origin that has a capability recipe.
    3. Invoke that capability with a recipe forced/known to be broken (4xx, empty, or shape-mismatch) so classifyRecipeBroken returns RECIPE_EXPIRED / a broken verdict.
    4. Confirm the autopilot does NOT hard-fail: it falls back to the DOM tools (click/type_text/read_page/get_site_guide/run_task) and STILL completes the SAME task.
    5. Confirm the broken recipe is quarantined/demoted from routing (it is skipped on the next resolve).
    6. Confirm a consent-gated re-learn (runDiscovery) is offered/triggered for that origin (NOT silent auto-capture).
    7. Confirm a legitimate no-results (a genuinely empty result) and a logged-out (302->login) are NOT healed away — the real outcome is surfaced.
    Record the outcome in 32-HUMAN-UAT.md.
  </how-to-verify>
  <resume-signal>Type "approved" to close the phase (the CI gate HEAL-05 is the milestone criterion; the live UAT is recorded debt), or describe issues to address.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live browser session -> capability + DOM fallback | The live UAT exercises a real authenticated session; it is supervised and local (no CI credential — GOV-06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-FAB | Repudiation | 32-HUMAN-UAT.md | mitigate | The doc explicitly states the CI half is green and only the live half is human_needed — it is recorded debt, NOT a fabricated pass (D-15), matching the Phase 27-31 posture; the milestone close does not claim a live pass it did not run. |
| T-32-LIVE | Information Disclosure | live UAT session | accept | The live run uses a real authenticated session under the user's supervision; no credential is captured to CI (GOV-06); capture-time redaction (Phase-31) governs any re-learn. |
| T-32-SC | Tampering | npm/pip/cargo installs | accept | No external packages installed (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- 32-HUMAN-UAT.md exists with UAT-32-01, status human_needed, the manual steps, and the CI-green/live-deferred/not-fabricated statement.
- The checkpoint is gated (autonomous:false); in auto-mode the live UAT is recorded debt and does not block the HEAL-05 milestone close.
</verification>

<success_criteria>
- 32-HUMAN-UAT.md records the live self-healing scenario as human_needed (not fabricated), matching the Phase 27-31 posture.
- The checkpoint pauses for the human; the CI gate (HEAL-05, Plan 04) remains the milestone completion criterion.
</success_criteria>

<output>
Create `.planning/phases/32-self-healing-fallback-recipe-rot-detection-re-learn-provider/32-05-SUMMARY.md` when done
</output>
