---
phase: 32-self-healing-fallback-recipe-rot-detection-re-learn-provider
plan: 04
type: execute
wave: 4
depends_on: ["32-02", "32-03"]
files_modified:
  - tests/recipe-schema-lock.test.js
  - tests/provider-parity.test.js
autonomous: true
requirements: [HEAL-05]
user_setup: []

must_haves:
  truths:
    - "tests/recipe-schema-lock.test.js freezes the v2 RECIPE_SCHEMA hash (the computed-once SHA256 over stable(schema)) AND re-asserts the frozen tool registry hash ad6efb8c... — both green (HEAL-05/INV-01, D-14)"
    - "tests/provider-parity.test.js is green: the capability surface formats identically across all 7 PROVIDER_KEYS AND the fallback decision (the typed RECIPE_DOM_FALLBACK_PENDING reason) is byte-equal across all 7 providers (HEAL-05/INV-03, D-13)"
    - "FSB_RECIPE_SCHEMA_VERSION === 2 is asserted and the frozen v2 hash placeholder from Plan 01 is replaced with the real computed hash"
    - "The full `npm test` chain is GREEN — capability + fallback + 7-provider parity + schema-lock + every prior suite — the v0.9.99 milestone completion gate (HEAL-05, D-15)"
    - "INV-01 holds: the tool-definitions byte-identity + the frozen tool registry hash are unmoved (no tool schema was touched this phase)"
    - "INV-04 holds: the agent-loop iterator guard is green inside the full run"
  artifacts:
    - path: "tests/recipe-schema-lock.test.js"
      provides: "the frozen v2 RECIPE_SCHEMA hash (computed + pasted) + the re-asserted frozen tool hash (D-14)"
      contains: "FROZEN_RECIPE_SCHEMA_V2_HASH"
    - path: "tests/provider-parity.test.js"
      provides: "the green 7-provider capability+fallback parity gate (D-13)"
      contains: "PROVIDER_KEYS"
  key_links:
    - from: "tests/recipe-schema-lock.test.js FROZEN_RECIPE_SCHEMA_V2_HASH"
      to: "extension/utils/capability-recipe-schema.js RECIPE_SCHEMA (v2)"
      via: "schemaHash(RECIPE_SCHEMA) === the frozen constant"
      pattern: "FROZEN_RECIPE_SCHEMA_V2_HASH|schemaHash"
    - from: "tests/provider-parity.test.js"
      to: "the 7 universal-provider PROVIDER_KEYS + the router fallback decision"
      via: "formatToolsForProvider + a router-invoke spy asserting equal typed reason"
      pattern: "PROVIDER_KEYS|RECIPE_DOM_FALLBACK_PENDING"
---

<objective>
Close the HEAL-05 milestone gate. Now that the v2 schema (Plan 02) and the router fallback emit (Plan 03) exist, compute-and-freeze the v2 RECIPE_SCHEMA hash in the schema-lock test (replacing the Plan-01 placeholder), confirm both parity suites are green, and run the full `npm test` chain — the v0.9.99 milestone completion criterion (capability + fallback + 7-provider parity + schema-lock + all prior suites). This plan adds NO production code; it finalizes the two frozen-hash gates and proves the milestone green.

Purpose: HEAL-05 (the 7-provider parity gate + the schema-lock parity gate + the full-suite milestone gate) (D-13, D-14, D-15; INV-01, INV-03).
Output: 2 test finalizations + the green milestone run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-self-healing-fallback-recipe-rot-detection-re-learn-provider/32-CONTEXT.md
@.planning/phases/32-self-healing-fallback-recipe-rot-detection-re-learn-provider/32-RESEARCH.md
@.planning/phases/32-self-healing-fallback-recipe-rot-detection-re-learn-provider/32-VALIDATION.md
@.planning/phases/32-self-healing-fallback-recipe-rot-detection-re-learn-provider/32-02-SUMMARY.md
@.planning/phases/32-self-healing-fallback-recipe-rot-detection-re-learn-provider/32-03-SUMMARY.md

<interfaces>
<!-- Extracted from the codebase. Use these directly; no exploration needed. -->

tests/recipe-schema-lock.test.js (created RED in Plan 01) carries:
- a stable() + schemaHash(schema) = crypto.createHash('sha256').update(JSON.stringify(stable(schema))).digest('hex') clone
- a placeholder const FROZEN_RECIPE_SCHEMA_V2_HASH = 'TBD-FROZEN-IN-PLAN-04' (or similar) — replace with the real value
- check(FSB_RECIPE_SCHEMA_VERSION === 2) and check(registryHash(nonTriggerTools) === 'ad6efb8cc3275d964488b67222129b1c0278c5c3b69c64888d926beb89a3926b')

tests/provider-parity.test.js (created RED in Plan 01) iterates PROVIDER_KEYS = ['xai','openai','anthropic','gemini','openrouter','lmstudio','custom'] and asserts (a) the capability surface formats identically per provider (the two capability tools are out-of-registry, never in the formatted output) and (b) a spied globalThis.FsbCapabilityRouter.invoke returning a broken RECIPE_DOM_FALLBACK_PENDING result yields a byte-equal typed reason across all 7 providers.

The full chain (package.json scripts.test) ends with the 3 new files appended (Plan 01): ... && node tests/provider-parity.test.js && node tests/recipe-schema-lock.test.js && node tests/capability-rot-detector.test.js. The chain runs `npm --prefix mcp run build` early (the router/schema tests dynamic-import mcp/build/errors.js).

The hash-compute idiom: run node to print schemaHash(RECIPE_SCHEMA) of the v2 schema once, paste the hex into the constant.
</interfaces>

<read_first_global>
Per ./CLAUDE.md: NO emojis in code/comments. This plan touches ONLY test files (no production code, no schema change — the v2 schema is already final from Plan 02). The frozen hash is computed from the EXISTING v2 schema; if the schema were to change again the hash would move (that is the lock's purpose).
</read_first_global>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compute + freeze the v2 RECIPE_SCHEMA hash; confirm the schema-lock gate green</name>
  <files>tests/recipe-schema-lock.test.js</files>
  <read_first>
    - MODIFY tests/recipe-schema-lock.test.js (the FROZEN_RECIPE_SCHEMA_V2_HASH placeholder + the version + frozen-tool-hash assertions from Plan 01)
    - Analog (the compute-once-and-paste-at-first-green pattern): tests/tool-definitions-parity.test.js:52,65-69 (EXPECTED_NON_TRIGGER_REGISTRY_HASH + registryHash) and tests/visual-session-schema-lock.test.js
    - The v2 schema source: extension/utils/capability-recipe-schema.js (RECIPE_SCHEMA + FSB_RECIPE_SCHEMA_VERSION===2 after Plan 02)
    - Source of truth: D-14 (freeze v2 RECIPE_SCHEMA hash + re-assert the tool hash), 32-RESEARCH.md Pattern 5 (Schema-lock)
  </read_first>
  <action>
    Compute the v2 RECIPE_SCHEMA hash once: run a node one-liner that requires extension/utils/capability-recipe-schema.js, applies the SAME stable() + sha256 schemaHash used in the test, and prints the hex digest of RECIPE_SCHEMA. Paste that hex into the FROZEN_RECIPE_SCHEMA_V2_HASH constant in tests/recipe-schema-lock.test.js (replacing the Plan-01 placeholder). Add an ASCII comment noting the hash was computed at first green from the v2 schema and that any drift to the recipe schema's closed vocab will red this gate (the schema is the contract between bundled recipes, learned recipes, and the interpreter — D-14).

    Confirm the test also asserts FSB_RECIPE_SCHEMA_VERSION === 2 and re-asserts registryHash(nonTriggerTools) === 'ad6efb8cc3275d964488b67222129b1c0278c5c3b69c64888d926beb89a3926b' (the frozen tool registry hash is unmoved — INV-01; no tool schema was touched this phase). Keep ASCII-only.
  </action>
  <verify>
    <automated>node tests/recipe-schema-lock.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/recipe-schema-lock.test.js` exits 0 (the v2 schema hash matches the frozen constant; version is 2; the frozen tool hash is unmoved).
    - The FROZEN_RECIPE_SCHEMA_V2_HASH constant is a real 64-char lowercase hex string (not the placeholder) — grep confirms no 'TBD' remains.
    - The test still re-asserts the literal `ad6efb8cc3275d964488b67222129b1c0278c5c3b69c64888d926beb89a3926b` (INV-01).
  </acceptance_criteria>
  <done>The v2 RECIPE_SCHEMA hash is computed + frozen; the schema-lock gate (v2 hash + version + frozen tool hash) is green.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm the 7-provider parity gate green, then run the full npm test milestone gate</name>
  <files>tests/provider-parity.test.js</files>
  <read_first>
    - MODIFY (only if needed to go green) tests/provider-parity.test.js — the 7-PROVIDER_KEYS capability+fallback parity suite from Plan 01
    - Analog (provider format harness): tests/tool-definitions-parity.test.js (PROVIDER_KEYS, formatToolsForProvider, formattedToolNames, getPublicTools)
    - Analog (router-invoke spy): tests/capability-autopilot-parity.test.js:14-26
    - The milestone gate: package.json scripts.test (the full && chain; HEAL-05/D-15)
    - Source of truth: D-13 (provider parity), D-15 (full npm test = milestone gate), 32-RESEARCH.md Pattern 5
  </read_first>
  <action>
    Run `node tests/provider-parity.test.js`. It should now be green (the v2 schema + the Plan-03 router fallback emit exist). If any assertion is red because of an over-tight expectation authored in Plan 01 (e.g. a formatting nuance for gemini functionDeclarations vs anthropic flat that the harness already normalizes via formattedToolNames), correct ONLY the test expectation to match the ACTUAL provider-independent behavior — do NOT relax the core invariant (the capability surface + the fallback decision MUST be equivalent across all 7 providers; if they genuinely differ, that is a real INV-03 failure to escalate, not a test to loosen). The router sits below the provider layer and never branches on provider, so the fallback decision is provider-independent by construction — assert exactly that.

    Then run the FULL milestone gate: `npm test`. This builds the mcp module and runs the entire && chain — every prior suite + the capability/consent/learned cluster + the 3 new Phase-32 suites + the extended router/autopilot suites + the INV-04 iterator guard. The whole chain MUST exit 0 (HEAL-05 = the v0.9.99 milestone completion criterion, D-15). If a PRE-EXISTING unrelated suite reds due to environmental drift (mirroring the Phase-28 stale-baseline fix precedent), record it precisely in the SUMMARY; the Phase-32 gate is the capability+fallback+parity+schema-lock cluster green within the full run.
  </action>
  <verify>
    <automated>node tests/provider-parity.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/provider-parity.test.js` exits 0 (capability surface + fallback decision equivalent across all 7 PROVIDER_KEYS — INV-03).
    - `npm test` exits 0 (the full chain green — the v0.9.99 milestone gate; HEAL-05/D-15).
    - Within the run: `node tests/agent-loop-iterator-guard.test.js` (INV-04), `node tests/tool-definitions-parity.test.js` (INV-01 byte-identity + frozen hash), `node tests/recipe-schema-lock.test.js`, `node tests/capability-rot-detector.test.js`, `node tests/capability-router.test.js`, `node tests/capability-autopilot-parity.test.js`, and `node tests/capability-recipe-schema.test.js` all pass.
  </acceptance_criteria>
  <done>The 7-provider parity gate is green; the full `npm test` chain is green — the v0.9.99 milestone completion gate (HEAL-05) closes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frozen-hash gate -> schema/tool contract | The hashes are the build-time contract; a test must compute them from the real artifact (not a stale copy) so drift is actually caught |
| provider parity -> router fallback decision | The assertion that the fallback decision is provider-independent must reflect real behavior, not be loosened to pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-LOCK | Tampering | tests/recipe-schema-lock.test.js | mitigate | The frozen v2 RECIPE_SCHEMA hash is computed from the actual v2 schema and pasted; any future drift to the closed vocab reds the gate (D-14). The frozen tool registry hash ad6efb8c... is re-asserted (INV-01). |
| T-32-PARITY | Spoofing/Tampering | tests/provider-parity.test.js | mitigate | The fallback decision is asserted byte-equal across all 7 providers; the test is corrected to match real provider-independent behavior, never loosened to hide a genuine INV-03 divergence (D-13). |
| T-32-IT | Tampering | the full-suite run | mitigate | The milestone gate includes the INV-04 iterator guard + the INV-01 tool byte-identity + frozen hash, so the milestone cannot close with a silently-moved invariant. |
| T-32-SC | Tampering | npm/pip/cargo installs | accept | No external packages installed (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `node tests/recipe-schema-lock.test.js` exits 0 (v2 hash frozen + version 2 + tool hash unmoved).
- `node tests/provider-parity.test.js` exits 0 (7-provider capability+fallback parity).
- `npm test` exits 0 (the v0.9.99 milestone gate — HEAL-05/D-15).
</verification>

<success_criteria>
- The v2 RECIPE_SCHEMA hash is frozen + the frozen tool hash re-asserted (INV-01).
- The 7-provider parity gate is green (INV-03).
- The full `npm test` chain is green — the v0.9.99 milestone completion gate (HEAL-05).
</success_criteria>

<output>
Create `.planning/phases/32-self-healing-fallback-recipe-rot-detection-re-learn-provider/32-04-SUMMARY.md` when done
</output>
