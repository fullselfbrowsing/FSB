---
phase: 32-self-healing-fallback-recipe-rot-detection-re-learn-provider
plan: 03
type: execute
wave: 3
depends_on: ["32-02"]
files_modified:
  - extension/utils/capability-router.js
  - extension/utils/capability-catalog.js
  - extension/ai/tool-executor.js
  - extension/ai/agent-loop.js
autonomous: true
requirements: [HEAL-01, HEAL-03, HEAL-04]
user_setup: []

must_haves:
  truths:
    - "After executeBoundSpec in _runDeclarativeTier AND _runHandlerTier, the router calls classifyRecipeBroken(out, recipe); a broken verdict returns the dual-field RECIPE_DOM_FALLBACK_PENDING carrying the underlying code (e.g. RECIPE_EXPIRED) — the T3 seam contract now fires after a broken T1a/T1b/T2 fetch too (HEAL-01)"
    - "A non-broken result (success or legitimate no-results) is returned VERBATIM (never masked); a logged-out (RECIPE_LOGGED_OUT) is surfaced, NOT healed; a typed RECIPE_ORIGIN_MISMATCH / RECIPE_CONSENT_* passes through and does NOT fall back (HEAL-04, Pitfall 3)"
    - "On a broken verdict the router quarantines: a T2 learned slug via FsbLearnedRecipeStore.quarantine(slug, origin) (persisted, demoted); a bundled slug via catalog.quarantineBundled(slug) (session-only Set) — best-effort, fire-and-forget (HEAL-03, D-09)"
    - "capability-catalog.js has a session-only in-memory quarantinedBundledSlugs Set + quarantineBundled/clearBundledQuarantine; resolve SKIPS a quarantined bundled slug (returns null -> router falls through); the REGISTRY data is never mutated and the Set is never persisted (D-09/D-11/D-12)"
    - "The post-fallback runDiscovery(origin,{tabId}) re-learn trigger is WIRED on the rot path (consent-gated inside startSession; CI asserts it is reachable, the real re-learn-after-fallback is UAT) (HEAL-03, D-10)"
    - "executeCapabilityToolForAutopilot surfaces the typed RECIPE_DOM_FALLBACK_PENDING reason (and any fellBackToDom marker) in makeResult so the model sees it next iteration (HEAL-01 CI half, D-02/D-04)"
    - "buildSystemPrompt's capability->DOM hint (agent-loop.js:731) is STRENGTHENED so a RECIPE_DOM_FALLBACK_PENDING / RECIPE_EXPIRED reason explicitly tells the model to complete the SAME task via DOM tools — and the agent-loop setTimeout iterator (lines 2026/2725/2794/2804) stays BYTE-UNTOUCHED (INV-04)"
    - "The fallback inherits the Phase-30 consent gate (already passed above the tier dispatch) and the two-point origin-pin — NO bypass (D-03)"
  artifacts:
    - path: "extension/utils/capability-router.js"
      provides: "post-executeBoundSpec classify hook in _runDeclarativeTier + _runHandlerTier; T3-realization emit; quarantine + re-learn wiring (D-01/D-16)"
      contains: "classifyRecipeBroken"
    - path: "extension/utils/capability-catalog.js"
      provides: "quarantinedBundledSlugs session Set + quarantineBundled + resolve-skip (D-09/D-12)"
      contains: "quarantinedBundledSlugs"
    - path: "extension/ai/tool-executor.js"
      provides: "typed reason / fellBackToDom surfaced in the autopilot makeResult (D-02/D-04)"
      contains: "RECIPE_DOM_FALLBACK_PENDING"
    - path: "extension/ai/agent-loop.js"
      provides: "strengthened buildSystemPrompt DOM-fallback hint (prompt text ONLY; iterator untouched)"
      contains: "RECIPE_DOM_FALLBACK_PENDING"
  key_links:
    - from: "extension/utils/capability-router.js _runDeclarativeTier (~:401) + _runHandlerTier (~:433)"
      to: "FsbCapabilityRotDetector.classifyRecipeBroken"
      via: "post-executeBoundSpec verdict; broken -> _err('RECIPE_DOM_FALLBACK_PENDING', ...)"
      pattern: "classifyRecipeBroken|RECIPE_DOM_FALLBACK_PENDING"
    - from: "extension/utils/capability-catalog.js resolve"
      to: "quarantinedBundledSlugs"
      via: "skip-quarantined check before returning a REGISTRY entry"
      pattern: "quarantinedBundledSlugs"
    - from: "extension/utils/capability-router.js (rot path)"
      to: "FsbLearnedRecipeStore.quarantine + FsbDiscoverySession.runDiscovery"
      via: "best-effort quarantine + the wired re-learn trigger"
      pattern: "quarantine|runDiscovery"
---

<objective>
Realize the DOM-fallback signal and the quarantine/re-learn wiring in the shared engine, and strengthen the autopilot's prompt-driven DOM completion. Hook classifyRecipeBroken into the router immediately after executeBoundSpec in BOTH declarative and handler tiers; on a broken verdict emit the dual-field RECIPE_DOM_FALLBACK_PENDING (carrying the underlying code), quarantine the rotted recipe (learned -> persisted store.quarantine; bundled -> a new session-only catalog Set), and wire the consent-gated runDiscovery re-learn trigger. Surface the typed reason in the autopilot makeResult and strengthen the buildSystemPrompt hint so the model completes the SAME task via the EXISTING DOM tools on the next iteration. The agent-loop setTimeout iterator stays byte-untouched (INV-04); no parallel fallback stack (INV-02); consent + origin-pin inherited (D-03).

Purpose: HEAL-01 (broken recipe -> DOM completion), HEAL-03 (quarantine + opportunistic re-learn), HEAL-04 (never mask / surface-not-heal) (D-01..D-04, D-09..D-12, D-16).
Output: 4 edits — router (the classify hook + emit + quarantine + re-learn), catalog (the bundled quarantine Set + resolve-skip), tool-executor (surface the reason), agent-loop (prompt text only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-self-healing-fallback-recipe-rot-detection-re-learn-provider/32-CONTEXT.md
@.planning/phases/32-self-healing-fallback-recipe-rot-detection-re-learn-provider/32-RESEARCH.md
@.planning/phases/32-self-healing-fallback-recipe-rot-detection-re-learn-provider/32-VALIDATION.md
@.planning/phases/32-self-healing-fallback-recipe-rot-detection-re-learn-provider/32-02-SUMMARY.md

<interfaces>
<!-- Extracted from the codebase. Use these directly; no exploration needed. -->

The router classify-hook insertion point (extension/utils/capability-router.js):
- _runDeclarativeTier(slug,args,ctx,tierLabel,entryRecipe,interpretOpts):352-409. After `var out = await primitive.executeBoundSpec(interpreted.spec, ctx && ctx.tabId)` (:401) and BEFORE the `out.tier = tierLabel` stamp (:402-405). The `recipe` is in scope (:363-365).
- _runHandlerTier(slug,args,ctx,entry):420-440. After `var out = await handler.handle(args || {}, handlerCtx)` (:433) and before the `out.tier='T1a'` stamp (:434-436). NOTE: the T1a entry's recipe is not directly in scope here; pass the handler result + the entry's descriptor/recipe-if-present, or classify on `out` shape alone (4xx/5xx/redirect/fetch-failed do not need a recipe; expectedShape needs one — a T1a handler may not carry expectedShape, so the status/redirect/fetch-failed rows still apply).
- The _err dual-field helper (:61-69): _err(code, extra) -> { success:false, code, errorCode:code, ...extra }. The existing T3 case (:517-521) already returns _err('RECIPE_DOM_FALLBACK_PENDING', { slug }).
- The consent gate runs ABOVE the tier dispatch (:460-475) and is UNCHANGED — the fallback inherits it.
- _audit(origin,slug,method,sideEffectClass,decision,outcome,code) (:528-532) whitelists fields; pass only the typed CODE, never a body.
- typeof-guarded global accessors precedent: _search()/_interp()/_fetchPrimitive()/_catalog()/_gate() (:330-343 region). Reach FsbCapabilityRotDetector, FsbLearnedRecipeStore, FsbDiscoverySession the SAME typeof-guarded way (degrade to no-op if absent).

The catalog resolve (extension/utils/capability-catalog.js resolve:250-310): learned-first (:259-266) -> REGISTRY entry (:268) -> T1b/T0 recipe attach (:284-291) / T1a handler (:294-301) / T2-T3 verbatim (:303-309). The export block is :312-319 (exportsObj). Add the Set + the two functions + the resolve-skip.

The autopilot door (extension/ai/tool-executor.js executeCapabilityToolForAutopilot:672-716): already returns makeResult({ success, hadEffect, error: success?null:(response?.error||response?.errorCode||null), result: response }) at :709-715. response is the router result — when it carries RECIPE_DOM_FALLBACK_PENDING the error already reflects errorCode; ensure the result (carrying the typed code + any fellBackToDom) is preserved verbatim so the model sees it.

The buildSystemPrompt hint (extension/ai/agent-loop.js:731, a STRING inside buildSystemPrompt — NOT code): "Two capability tools exist for authenticated first-party actions: search_capabilities ... and invoke_capability .... Prefer them over DOM tools for first-party authenticated actions on the current origin; fall back to the DOM tools (click, type_text, read_page, etc.) when no capability matches." STRENGTHEN this string only.

The byte-locked iterator (DO NOT TOUCH — tests/agent-loop-iterator-guard.test.js): the 4 lines `session._nextIterationTimer = setTimeout(function() { runAgentIteration(sessionId, options); }, <100|100|5000|2000>);` at agent-loop.js:2026/2725/2794/2804. Exactly 4 schedule matches must remain.

The learned quarantine API (extension/utils/learned-recipe-store.js quarantine:455-476): quarantine(slug, origin) -> Promise; flags quarantined:true (persists + mirrors so getLearnedSync returns null -> resolve demotes). Fire-and-forget on the rot path.

The re-learn entry (extension/utils/discovery-session.js runDiscovery:128-261): runDiscovery(origin, { tabId }) -> Promise<{ok, ...}>; the Phase-30 consent gate runs INSIDE startSession (:137-154) before any debugger attach, so a default-OFF / denied / sensitive-unconfirmed origin re-learns nothing.
</interfaces>

<read_first_global>
Per ./CLAUDE.md: NO emojis in code/comments. The router + catalog are on the recipe-path allowlist (eval-free, comment-scanned) — keep them free of eval/new Function/import( even in comments. INV-04: edit ONLY the buildSystemPrompt string in agent-loop.js; touch NOTHING near lines 2026/2725/2794/2804.
</read_first_global>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bundled quarantine Set + resolve-skip in capability-catalog.js</name>
  <files>extension/utils/capability-catalog.js</files>
  <read_first>
    - MODIFY extension/utils/capability-catalog.js (resolve:250-310 — the REGISTRY-entry path at :268; the export block :312-319)
    - Analog (the persisted learned quarantine this mirrors for bundled): extension/utils/learned-recipe-store.js:455-476 (flag-not-delete) + getLearnedSync quarantine-null :324
    - Analog (session-only in-memory state in a capability IIFE): the module-scope var pattern already used in capability-catalog.js (REGISTRY + the learned mirror accessor)
    - Source of truth: 32-RESEARCH.md Pattern 4 (Bundled quarantine), Pitfall 4 (do NOT mutate REGISTRY / do NOT persist), D-09/D-11/D-12
  </read_first>
  <action>
    Add a module-scope session-only `var quarantinedBundledSlugs = Object.create(null)` (a null-proto map used as a Set) inside the capability-catalog IIFE. Add two functions: quarantineBundled(slug) sets quarantinedBundledSlugs[slug] = true (string-guard the slug; no-op otherwise); clearBundledQuarantine(slug) deletes the key. Neither persists (D-12 — session-only, re-evaluated next SW session) and neither touches the REGISTRY object (D-09 — the bundled catalog data is immutable here, Pitfall 4).

    In resolve(slug, origin): AFTER the learned-first check (:259-266) and the REGISTRY lookup (:268), BEFORE returning a REGISTRY-derived entry, consult the Set: if Object.prototype.hasOwnProperty.call(quarantinedBundledSlugs, slug) is true, return null (skip the quarantined bundled slug so the router falls through to the next tier / the DOM fallback — D-11). Place the skip so it covers the T1a / T1b / T0 / explicit-seam returns (it must demote the bundled recipe regardless of tier). Do NOT skip a LEARNED hit (a fresh re-learned recipe replaces the quarantined bundled one — the learned-first check already returned above; the bundled skip only applies to the REGISTRY entry).

    Export quarantineBundled + clearBundledQuarantine on exportsObj (:312-319) so the router (and a maintainer/test) can flag a bundled slug. Keep ASCII-only, eval-free.
  </action>
  <verify>
    <automated>node tests/capability-router.test.js && node scripts/verify-recipe-path-guard.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-recipe-path-guard.mjs` exits 0 (catalog stays eval-free on the allowlist).
    - `node -e "const c=require('./extension/utils/capability-catalog.js'); process.exit(typeof c.quarantineBundled==='function' && typeof c.clearBundledQuarantine==='function' ? 0 : 1)"` exits 0.
    - A direct check: after registering a T1b slug and calling quarantineBundled(slug), resolve(slug, origin) returns null; clearBundledQuarantine(slug) restores it (the router test or a focused node check confirms the skip + the REGISTRY object is unmutated).
  </acceptance_criteria>
  <done>capability-catalog.js has a session-only quarantinedBundledSlugs Set + quarantineBundled/clearBundledQuarantine; resolve skips a quarantined bundled slug without mutating or persisting the REGISTRY; the recipe-path guard passes.</done>
</task>

<task type="auto">
  <name>Task 2: Router classify hook + T3-realization emit + quarantine + re-learn wiring</name>
  <files>extension/utils/capability-router.js</files>
  <read_first>
    - MODIFY extension/utils/capability-router.js (_runDeclarativeTier:352-409, hook after :401; _runHandlerTier:420-440, hook after :433; _err :61-69; the typeof-guarded accessors region ~:330-343; the audit call :528-532)
    - Analog (the exact insertion + the broken-verdict return): 32-RESEARCH.md "Classify hook insertion" at RESEARCH :430-453 (the skeleton) + Pattern 1 (the typed-reason return) + Pattern 4 (quarantine + runDiscovery)
    - Analog (the existing T3 seam contract being extended): capability-router.js:517-521 (the T3 case _err('RECIPE_DOM_FALLBACK_PENDING', {slug}))
    - Analog (reach a global the typeof-guarded way): the existing _search/_interp/_fetchPrimitive/_catalog/_gate accessors
    - The RED assertions this turns GREEN: tests/capability-router.test.js (Plan 01: broken->RECIPE_DOM_FALLBACK_PENDING; quarantine-fired; runDiscovery-wired; legitimate-no-results NOT healed)
    - Source of truth: D-01, D-04, D-09, D-10, D-11, D-16; Pitfall 3 (passthrough security failures); Open Question 2 (the re-learn trigger is asserted WIRED in CI, real re-learn is UAT)
  </read_first>
  <action>
    Add a typeof-guarded accessor for FsbCapabilityRotDetector (mirror _search at ~:330-343); similarly reach FsbLearnedRecipeStore and FsbDiscoverySession the same way (each degrades to a no-op when absent, so the head/no-store contexts are unaffected).

    In _runDeclarativeTier, immediately after `var out = await primitive.executeBoundSpec(interpreted.spec, ctx && ctx.tabId)` (:401) and BEFORE the `if (out && out.success === true) { out.tier = tierLabel; return out; }` stamp: call the detector. If a detector is present and classifyRecipeBroken(out, recipe) returns { broken:true, code, reason }, then (a) QUARANTINE best-effort: if tierLabel === 'T2' call FsbLearnedRecipeStore.quarantine(slug, ctx && ctx.origin) (fire-and-forget — do not await-block the return; swallow rejection); else (a bundled T0/T1b) call catalog.quarantineBundled(slug); (b) fire the re-learn trigger best-effort on the rot path: call FsbDiscoverySession.runDiscovery(ctx && ctx.origin, { tabId: ctx && ctx.tabId }) fire-and-forget (it self-enforces consent inside startSession; a default-OFF origin re-learns nothing — D-10); (c) return _err('RECIPE_DOM_FALLBACK_PENDING', { slug, reason: verdict.code, recipeBrokenReason: verdict.reason }) carrying the underlying code so both front doors surface it. If the verdict is NOT broken (success, legitimate no-results, or a logged-out/typed passthrough), fall through to the EXISTING stamp+return UNCHANGED (the success path stamps tier; a logged-out verdict's code RECIPE_LOGGED_OUT is informational — return `out` verbatim so a 200-opaque-redirect surfaces as-is, NEVER a fallback). Do NOT mask a real result.

    Apply the analogous hook in _runHandlerTier after `var out = await handler.handle(...)` (:433): classify on the handler `out` (the status/redirect/fetch-failed rows apply even without a recipe; pass the entry's recipe if present for the expectedShape row, else null). On broken: quarantine the bundled slug via catalog.quarantineBundled(slug) + fire runDiscovery + return RECIPE_DOM_FALLBACK_PENDING; else preserve the existing tier:'T1a' stamp+return.

    CRITICAL passthrough (Pitfall 3): the detector already classifies a dual-field RECIPE_ORIGIN_MISMATCH / RECIPE_CONSENT_* as not-broken — so those flow to the existing verbatim return and NEVER fall back; do not add a separate broken branch that catches them.

    Keep the consent gate (:460-475) and the audit call (:528-532) UNCHANGED — the fallback inherits the gate; the dispatched outcome is still audited (a RECIPE_DOM_FALLBACK_PENDING return is audited as an 'error'/code outcome by the existing _audit at :531-532, carrying only the typed CODE, never a body — V7). ASCII-only, eval-free.
  </action>
  <verify>
    <automated>node tests/capability-router.test.js && node tests/agent-loop-iterator-guard.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/capability-router.test.js` exits 0 (the broken->RECIPE_DOM_FALLBACK_PENDING, quarantine-fired, runDiscovery-wired, and legitimate-no-results-NOT-healed assertions from Plan 01 are GREEN).
    - `node tests/agent-loop-iterator-guard.test.js` exits 0 (this task does not touch agent-loop.js).
    - `node scripts/verify-recipe-path-guard.mjs` exits 0 (router stays eval-free on the allowlist).
    - A RECIPE_ORIGIN_MISMATCH result returned by executeBoundSpec is surfaced verbatim (NOT converted to RECIPE_DOM_FALLBACK_PENDING) and fires NO quarantine (the router test's passthrough assertion).
  </acceptance_criteria>
  <done>The router classifies after executeBoundSpec in both tiers, emits RECIPE_DOM_FALLBACK_PENDING on a broken verdict (carrying the underlying code), quarantines (learned/bundled) + fires the consent-gated runDiscovery re-learn trigger, and passes through security failures + real results verbatim; the router suite is green and the iterator guard stays green.</done>
</task>

<task type="auto">
  <name>Task 3: Surface the typed reason in the autopilot makeResult + strengthen the buildSystemPrompt DOM-fallback hint (iterator untouched)</name>
  <files>extension/ai/tool-executor.js, extension/ai/agent-loop.js</files>
  <read_first>
    - MODIFY extension/ai/tool-executor.js (executeCapabilityToolForAutopilot:672-716 — the makeResult return at :709-715; makeResult factory :45-53)
    - MODIFY extension/ai/agent-loop.js (the buildSystemPrompt capability->DOM hint STRING at :731 ONLY)
    - Analog (the makeResult contract): tool-executor.js makeResult:45-53 ({ success, hadEffect, error, navigationTriggered, result })
    - Analog / GUARD (the byte-locked region to NOT touch): tests/agent-loop-iterator-guard.test.js — the 4 setTimeout lines at agent-loop.js:2026/2725/2794/2804 (exactly 4 schedule matches)
    - The RED assertions this turns GREEN: tests/capability-autopilot-parity.test.js (Plan 01: the autopilot door surfaces the typed reason / fellBackToDom in makeResult)
    - Source of truth: D-02 (model-driven DOM completion), D-04 (fellBackToDom marker), 32-RESEARCH.md Pattern 1 (the model acts on the reason), Pitfall 1 (line 731 is a prompt string, NOT the iterator)
  </read_first>
  <action>
    In executeCapabilityToolForAutopilot (tool-executor.js:709-715): ensure the router response carrying RECIPE_DOM_FALLBACK_PENDING (and any fellBackToDom marker the router/audit attaches) is surfaced in the returned makeResult so the model sees it on the next iteration. The existing return already sets error: success?null:(response?.error||response?.errorCode||null) and result: response — confirm the typed code reaches error via errorCode and that result preserves the full response verbatim (the typed code + reason + recipeBrokenReason). If a fellBackToDom marker convention is introduced (D-04), pass it through in result (do not synthesize a fake success — a RECIPE_DOM_FALLBACK_PENDING is success:false at the capability layer; the COMPLETION happens via the model's next DOM tool call, not here). Do NOT call executeTool / run_task from inside this function (that would build a parallel stack — INV-02; the completion is model-driven).

    In agent-loop.js buildSystemPrompt (the STRING at :731): STRENGTHEN the existing capability->DOM hint so it explicitly instructs the model that when an invoke_capability result returns a RECIPE_DOM_FALLBACK_PENDING or RECIPE_EXPIRED reason (the capability path is broken/rotted), it MUST complete the SAME task using the DOM tools (click, type_text, read_page, get_page_snapshot, get_site_guide, run_task) on the current origin — do NOT report failure, do NOT abandon the task. Keep it a single appended/expanded instruction in the same bullet region; ASCII-only, NO emojis. This is the ONLY change to agent-loop.js.

    DO NOT edit anything near lines 2026/2725/2794/2804 (the setTimeout iterator). After editing, the iterator guard must still find the 4 byte-identical schedule lines (INV-04, Pitfall 1).
  </action>
  <verify>
    <automated>node tests/capability-autopilot-parity.test.js && node tests/agent-loop-iterator-guard.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/agent-loop-iterator-guard.test.js` exits 0 (the 4 setTimeout iterator lines are byte-unchanged; exactly 4 schedule matches — INV-04 holds after the prompt edit).
    - `node tests/capability-autopilot-parity.test.js` exits 0 (the autopilot door surfaces the typed reason / fellBackToDom in makeResult; the one-engine-two-doors + frozen-hash assertions still pass).
    - `grep -n "RECIPE_DOM_FALLBACK_PENDING\|RECIPE_EXPIRED" extension/ai/agent-loop.js` shows the reason referenced ONLY inside the buildSystemPrompt string region (near :731), NOT near the iterator.
    - The only agent-loop.js diff is within buildSystemPrompt (the prompt string); no diff in the iterator region.
  </acceptance_criteria>
  <done>The autopilot makeResult surfaces the typed RECIPE_DOM_FALLBACK_PENDING reason (and fellBackToDom) so the model completes via DOM next iteration; the buildSystemPrompt hint explicitly instructs DOM completion on a broken/rotted capability; the agent-loop iterator is byte-untouched and its guard is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executeBoundSpec result -> router classify + audit | A rotted response body (error HTML, 4xx JSON, auth-bearing redirect) reaches the router; only the typed CODE may be audited/surfaced, never the body |
| router -> autopilot door -> model | The typed reason crosses to the model via makeResult; the model then acts in the same owned-tab consented session (the fallback is not a new privilege) |
| router -> runDiscovery (re-learn) | Re-capture must re-enforce consent independently (it does, inside startSession) so the rot path cannot trigger uconsented capture |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-MASK | Tampering/Repudiation | router classify hook (_runDeclarativeTier/_runHandlerTier) | mitigate | A non-broken verdict (success / legitimate no-results) returns `out` verbatim and a logged-out returns RECIPE_LOGGED_OUT — the broken branch fires ONLY on classifyRecipeBroken broken:true, so a real outcome is never healed away (HEAL-04, D-04). Asserted by the router suite's legitimate-no-results-NOT-healed + logged-out rows. |
| T-32-PASS | Elevation of Privilege | router classify hook | mitigate | RECIPE_ORIGIN_MISMATCH / RECIPE_CONSENT_* classify as not-broken (the detector's typed-passthrough) and flow to the verbatim return — the fallback never bypasses a security/consent rejection (Pitfall 3, D-03). |
| T-32-LEAK | Information Disclosure | router _audit on the rot path | mitigate | The existing _audit (:531-532) records only the typed CODE on the fallback return (no body, no args); the classifier never logs result.data/text raw; any diagnostic routes through redactForLog (V7). |
| T-32-PIN | Elevation of Privilege/Spoofing | the DOM fallback completion | mitigate | The completion is normal DOM automation on the ACTIVE owned tab via the existing tools — the two-point origin-pin governs the capability path and the DOM tools act only on the owned tab; the fallback is NOT a credentialed cross-origin re-target (D-03). |
| T-32-CONSENT | Elevation of Privilege | re-learn runDiscovery trigger | mitigate | The original invoke already passed the consent gate (router :460-475); runDiscovery re-enforces consent INSIDE startSession before any capture, so a default-OFF / sensitive origin re-learns nothing (D-10). |
| T-32-DOS | Denial of Service | bundled quarantine | mitigate | Bundled quarantine is session-only in-memory (Plan-01 catalog Task 1); a transient blip never permanently demotes a first-party bundled recipe (D-12). |
| T-32-IT | Tampering | agent-loop.js iterator (INV-04) | mitigate | Only the buildSystemPrompt STRING changes; tests/agent-loop-iterator-guard.test.js re-asserts the 4 byte-identical setTimeout lines (Pitfall 1). The grep acceptance check confirms the reason appears only in the prompt region. |
| T-32-PSTACK | Elevation of Privilege | autopilot door | mitigate | executeCapabilityToolForAutopilot does NOT call executeTool/run_task — no parallel healer stack (INV-02); the completion is the model's next tool selection. |
| T-32-SC | Tampering | npm/pip/cargo installs | accept | No external packages installed (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `node tests/capability-router.test.js` exits 0 (broken->RECIPE_DOM_FALLBACK_PENDING + quarantine + re-learn-wired + never-mask).
- `node tests/capability-autopilot-parity.test.js` exits 0 (typed reason surfaced; one-engine + frozen hash intact).
- `node tests/agent-loop-iterator-guard.test.js` exits 0 (INV-04 — iterator byte-untouched after the prompt edit).
- `node scripts/verify-recipe-path-guard.mjs` exits 0 (router + catalog eval-free).
</verification>

<success_criteria>
- The router classifies after executeBoundSpec in both tiers and emits RECIPE_DOM_FALLBACK_PENDING on a broken verdict, carrying the underlying code.
- Quarantine fires (learned -> store.quarantine; bundled -> catalog Set); the consent-gated runDiscovery re-learn trigger is wired.
- Security failures + real results pass through verbatim (never masked).
- The autopilot door surfaces the typed reason; the prompt hint instructs DOM completion.
- INV-04: the agent-loop iterator is byte-untouched (guard green); INV-02: no parallel stack; D-03: consent + pin inherited.
</success_criteria>

<output>
Create `.planning/phases/32-self-healing-fallback-recipe-rot-detection-re-learn-provider/32-03-SUMMARY.md` when done
</output>
