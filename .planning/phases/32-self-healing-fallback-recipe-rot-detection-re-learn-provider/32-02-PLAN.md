---
phase: 32-self-healing-fallback-recipe-rot-detection-re-learn-provider
plan: 02
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - extension/utils/capability-rot-detector.js
  - extension/utils/capability-recipe-schema.js
  - extension/utils/capability-interpreter.js
  - extension/utils/recipe-synthesizer.js
  - tests/capability-recipe-schema.test.js
  - catalog/recipes/_fixtures/valid-recipe.json
autonomous: true
requirements: [HEAL-02, HEAL-04]
user_setup: []

must_haves:
  truths:
    - "classifyRecipeBroken(result, recipe) returns the HEAL-04 taxonomy: broken (4xx/5xx, empty-when-extract-expects-data, expectedShape-mismatch, fetch-failed) vs logged-out (redirected:true -> RECIPE_LOGGED_OUT, surfaced not healed) vs legitimate-no-results (200+valid+empty -> NOT broken, returned verbatim)"
    - "A dual-field RECIPE_* security failure (RECIPE_ORIGIN_MISMATCH / RECIPE_CONSENT_*) classifies as NOT broken (typed passthrough — never a fallback)"
    - "validateExpectedShape reuses the SAME jmespath engine the extract field uses (getFSBJmespath); a present container of the expected kind (incl. empty) passes; a missing path / null / wrong-kind body fails; absent engine degrades to shape-passes (conservative, no false RECIPE_EXPIRED)"
    - "FSB_RECIPE_SCHEMA_VERSION is 2 (the CURRENT stamp for NEW recipes); RECIPE_SCHEMA.schemaVersion is enum:[1,2] (NOT const) so a persisted/bundled schemaVersion:1 recipe STILL validates at runtime; RECIPE_SCHEMA carries OPTIONAL capturedAt + expectedShape (NOT in required); additionalProperties:false preserved — D-08 'v1 stays valid' is literally true at runtime"
    - "The interpreter carries expectedShape + capturedAt from the recipe into the bound spec, mirroring the extract carry"
    - "The recipe-synthesizer stamps a conservative expectedShape ('@', shape-only) + capturedAt on synthesized learned recipes, and the synthesized schemaVersion is the current stamp (2)"
    - "The in-chain regression gate tests/capability-recipe-schema.test.js is MIGRATED for the enum bump: asserts FSB_RECIPE_SCHEMA_VERSION===2; the v1 valid-recipe.json fixture STILL validates success:true (proves backward-compat); an OUT-OF-ENUM schemaVersion (3 or 0) -> RECIPE_SCHEMA_INVALID; a schemaVersion:2 recipe with the new optional fields -> valid"
    - "capability-rot-detector.js is eval-free (no eval/new Function/import( even in comments) and passes the recipe-path CI guard"
  artifacts:
    - path: "extension/utils/capability-rot-detector.js"
      provides: "classifyRecipeBroken + validateExpectedShape (the pure eval-free rot classifier, D-01/D-16)"
      min_lines: 70
    - path: "extension/utils/capability-recipe-schema.js"
      provides: "v2 schema: schemaVersion enum:[1,2] (backward-compat) + optional capturedAt + expectedShape (D-05/D-08)"
      contains: "expectedShape"
    - path: "extension/utils/capability-interpreter.js"
      provides: "expectedShape + capturedAt carry into the bound spec (D-05)"
      contains: "expectedShape"
    - path: "extension/utils/recipe-synthesizer.js"
      provides: "conservative synthesized expectedShape + capturedAt (D-07)"
      contains: "expectedShape"
    - path: "tests/capability-recipe-schema.test.js"
      provides: "migrated regression gate for the additive enum bump (v1 still valid; out-of-enum rejected; v2-with-optional-fields valid)"
      contains: "FSB_RECIPE_SCHEMA_VERSION === 2"
  key_links:
    - from: "extension/utils/capability-rot-detector.js validateExpectedShape"
      to: "the vendored jmespath engine"
      via: "FsbCapabilityInterpreter.getFSBJmespath().search(data, expectedShape)"
      pattern: "getFSBJmespath|jmespath"
    - from: "extension/utils/capability-recipe-schema.js RECIPE_SCHEMA.properties"
      to: "the closed vocab"
      via: "schemaVersion enum:[1,2] + optional expectedShape + capturedAt, additionalProperties:false unchanged"
      pattern: "expectedShape"
    - from: "tests/capability-recipe-schema.test.js"
      to: "RECIPE_SCHEMA (v2, enum-versioned)"
      via: "v1 fixture validates + out-of-enum rejected + v2 optional-fields validate"
      pattern: "FSB_RECIPE_SCHEMA_VERSION === 2"
---

<objective>
Build the single genuinely-new module of Phase 32 — the eval-free capability-rot-detector.js exporting classifyRecipeBroken(result, recipe) and validateExpectedShape(data, expectedShape) — and make the additive, BACKWARD-COMPATIBLE v1->v2 recipe-schema change (schemaVersion becomes enum:[1,2] so persisted v1 recipes still validate, plus optional capturedAt + expectedShape), threading expectedShape+capturedAt through the interpreter into the bound spec, emitting a conservative expectedShape from the Phase-31 synthesizer, and migrating the in-`npm test`-chain schema regression gate to the enum bump. This is the leaf layer: pure data classification + a closed-vocab schema bump, with no router/autopilot wiring yet (that is Plan 03).

Purpose: HEAL-02 (rot detection via expectedShape -> RECIPE_EXPIRED) + HEAL-04 (the broken/no-results/logged-out taxonomy that never masks a real outcome), reusing the exact jmespath engine the extract field already runs (D-01, D-05, D-06, D-07, D-08, D-16).
Output: 1 new module + 3 additive production edits + 1 migrated regression test (+ optional v2 accept fixture).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-self-healing-fallback-recipe-rot-detection-re-learn-provider/32-CONTEXT.md
@.planning/phases/32-self-healing-fallback-recipe-rot-detection-re-learn-provider/32-RESEARCH.md
@.planning/phases/32-self-healing-fallback-recipe-rot-detection-re-learn-provider/32-VALIDATION.md

<interfaces>
<!-- Extracted from the codebase. Use these directly; no exploration needed. -->

The executeBoundSpec normalized result the classifier inspects (extension/utils/capability-fetch.js):
- success (:377-384): { success:true, status, finalUrl, redirected, data, text }   (redirected:true when opaqueredirect OR status 300-399 — the logged-out signal, redirect:'manual' at :177/189)
- executeScript failed (:336-339): { success:false, error:'executeScript failed: ...' }
- no page result (:356): { success:false, error:'no result from page fetch' }
- page-thrown (:359): { success:false, error:<message> }
- origin-pin mismatch (:293): { success:false, code:'RECIPE_ORIGIN_MISMATCH', errorCode:'RECIPE_ORIGIN_MISMATCH', ... }  (typed; NOT a rot)

The jmespath engine accessor (extension/utils/capability-interpreter.js getFSBJmespath ~:87-89): a typeof-guarded accessor returning the vendored jmespath global; the extract run at capability-fetch.js:362-375 is `jp.search(data, recipe.extract)`. Reuse the SAME path.

The dual-export IIFE shape to mirror (extension/utils/capability-catalog.js:312-319 / capability-interpreter.js export block): assign to a module-scope exportsObj and publish on globalThis + module.exports (typeof-guarded), so require() in Node and importScripts in the SW both resolve FsbCapabilityRotDetector.

The schema today (extension/utils/capability-recipe-schema.js):
- FSB_RECIPE_SCHEMA_VERSION = 1   (:59 — bump to 2; this is the CURRENT stamp for NEW recipes)
- RECIPE_SCHEMA (:79-148): additionalProperties:false; required:['schemaVersion','id','origin','endpoint','method','authStrategy']; schemaVersion:{ const: FSB_RECIPE_SCHEMA_VERSION } (:85 — change const -> enum:[1,2]); extract:{ type:'string' } (:127)

The regression gate that runs inside `npm test` (tests/capability-recipe-schema.test.js — MUST be migrated; it is NOT a Wave-0 RED suite, it is an existing GREEN gate the const->enum bump breaks):
- :65 asserts `M.FSB_RECIPE_SCHEMA_VERSION === 1`  -> migrate to `=== 2`
- :67-71 reads catalog/recipes/_fixtures/valid-recipe.json (schemaVersion:1) and asserts validateRecipe success:true -> KEEP AS-IS (the v1 fixture STILL validating under enum:[1,2] is the backward-compat proof; do NOT migrate the fixture to v2)
- :109-114 builds `Object.assign({}, valid, { schemaVersion: 2 })` and asserts RECIPE_SCHEMA_INVALID -> now INVERTED (2 is valid). Replace with an OUT-OF-ENUM version (schemaVersion 3 or 0) -> RECIPE_SCHEMA_INVALID, AND add a positive `schemaVersion:2` + the new optional fields -> success:true assertion.
- :116-120 (missing schemaVersion -> RECIPE_SCHEMA_INVALID) stays valid; required still lists schemaVersion.

The v1 accept fixture (catalog/recipes/_fixtures/valid-recipe.json): schemaVersion:1, id, origin, endpoint:'/api/{id}', method:'GET', authStrategy:'same-origin-cookie', params/request/extract. It MUST keep schemaVersion:1 (the backward-compat fixture). Optionally ADD a sibling v2 accept fixture (e.g. valid-recipe-v2.json) carrying schemaVersion:2 + capturedAt + expectedShape to prove the v2-with-optional-fields path.

The interpreter bound-spec build (extension/utils/capability-interpreter.js:472-481): the spec object carries `extract: (typeof recipe.extract === 'string') ? recipe.extract : null` — add the two new carries alongside it.

The synthesizer recipe-core build (extension/utils/recipe-synthesizer.js:285-293): builds a closed-vocab core with schemaVersion: SCHEMA_VERSION (a module const SCHEMA_VERSION=1 at :71 — this must follow the bump to 2), extract:'@'. The store already records capturedAt bookkeeping (learned-recipe-store.js:393); D-05 wants it ON the recipe core.

The recipe-path CI guard (scripts/verify-recipe-path-guard.mjs) auto-globs extension/utils/capability-*.js (Check 4) and scans for eval/new Function/import( INCLUDING in comments. capability-rot-detector.js was added to RECIPE_PATH_ALLOWLIST in Plan 01.
</interfaces>

<read_first_global>
Per ./CLAUDE.md: NO emojis anywhere in code or comments. capability-rot-detector.js MUST be ASCII-only and free of eval / new Function / import( even in comments/strings (recipe-path allowlisted, comment-scanned — Pitfall 5).
</read_first_global>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create capability-rot-detector.js (classifyRecipeBroken + validateExpectedShape)</name>
  <files>extension/utils/capability-rot-detector.js</files>
  <read_first>
    - WRITE extension/utils/capability-rot-detector.js (new, eval-free, on the allowlist from Plan 01)
    - Analog (dual-export IIFE shape + typeof-guarded globals): extension/utils/capability-catalog.js:312-319 (exportsObj + publish) and extension/utils/capability-interpreter.js export block + getFSBJmespath ~:87-89
    - Analog (the result shapes to branch on): extension/utils/capability-fetch.js:293,336-339,356,359,377-384
    - Analog (the extract jmespath run to mirror): extension/utils/capability-fetch.js:362-375
    - The RED suite this turns GREEN: tests/capability-rot-detector.test.js (Plan 01) — the decision table + the validateExpectedShape predicate
    - Source of truth: 32-RESEARCH.md Pattern 2 (decision table), Pattern 3 (validation), the classifyRecipeBroken skeleton at RESEARCH :455-487, Open Question 1 (present-container predicate)
  </read_first>
  <behavior>
    - classifyRecipeBroken(non-object) -> { broken:true, code:'RECIPE_EXPIRED', reason:'no-result' }
    - success:false WITH a /^RECIPE_/ code (RECIPE_ORIGIN_MISMATCH, RECIPE_CONSENT_*) -> { broken:false, ..., reason:'typed-passthrough' } (security rejection, never a rot)
    - success:false without a typed code -> { broken:true, code:'RECIPE_EXPIRED', reason:'fetch-failed' }
    - success:true, redirected:true -> { broken:false, code:'RECIPE_LOGGED_OUT', reason:'redirect-to-login' } (surfaced, NOT healed)
    - success:true, status>=400 -> { broken:true, code: status>=500?'RECIPE_HTTP_5XX':'RECIPE_HTTP_4XX' }
    - success:true, status 200, recipe has expectedShape, validateExpectedShape FAILS -> { broken:true, code:'RECIPE_EXPIRED', reason:'expectedShape-mismatch' }
    - success:true, status 200, expectedShape passes OR no expectedShape -> { broken:false, reason:'ok' } (success or legitimate no-results)
    - validateExpectedShape: present non-null/undefined value of the expected kind (INCLUDING an empty array/object) -> true; missing path / null / wrong-kind (e.g. HTML string where an object path was expected) -> false; absent jmespath engine -> true (conservative, no false RECIPE_EXPIRED)
  </behavior>
  <action>
    Author the module as a dual-export IIFE (mirror capability-catalog.js:312-319): a top IIFE that builds exportsObj = { classifyRecipeBroken, validateExpectedShape } and publishes it as globalThis.FsbCapabilityRotDetector and module.exports (each typeof-guarded). ASCII-only; NO emojis; ZERO eval/new Function/import( even in comments.

    Implement classifyRecipeBroken(result, recipe) exactly per the <behavior> decision table (D-01, RESEARCH Pattern 2). The ORDER matters: guard non-object first; then the typed-passthrough (success:false AND /^RECIPE_/ on result.code) BEFORE the generic success:false fetch-failed branch (Pitfall 3 — a pin/consent rejection must short-circuit as not-broken); then redirected:true -> RECIPE_LOGGED_OUT; then status>=400; then the expectedShape gate; else not-broken. Return the { broken, code, reason } shape the router reads.

    Implement validateExpectedShape(data, expectedShape) reusing the SAME engine the extract field runs: obtain the jmespath engine via FsbCapabilityInterpreter.getFSBJmespath() when available (typeof-guarded; mirror capability-fetch.js:362-375), else fall back to a globalThis.jmespath check; if NO engine is reachable, return true (conservative degrade — never a false RECIPE_EXPIRED, D-06). Run jp.search(data, expectedShape); define the "present" predicate per Open Question 1: resolved !== null && resolved !== undefined; an empty array or empty object of the EXPECTED kind is PRESENT (returns true — a real empty result); a null/undefined/missing path returns false. Document the predicate in an ASCII comment (this is the load-bearing HEAL-04 line). NEVER log data/result.text (V7).
  </action>
  <verify>
    <automated>node tests/capability-rot-detector.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/capability-rot-detector.test.js` exits 0 (the full taxonomy + expectedShape suite from Plan 01 is GREEN).
    - `node scripts/verify-recipe-path-guard.mjs` exits 0 (the new module is on the allowlist AND is eval-free — Check 4 disk-glob + the eval/new Function/import scan pass, comments included).
    - The "0 results passes (returned verbatim)" and "login-HTML fails (RECIPE_EXPIRED)" and "RECIPE_ORIGIN_MISMATCH passthrough (not broken)" rows of the suite all PASS (HEAL-04 + Pitfall 3).
  </acceptance_criteria>
  <done>capability-rot-detector.js exists, eval-free, on the allowlist; the Plan-01 taxonomy suite is green; the recipe-path guard passes.</done>
</task>

<task type="auto">
  <name>Task 2: Backward-compatible schema v1->v2 (schemaVersion enum:[1,2] + optional capturedAt + expectedShape) + interpreter carry + synthesizer stamp</name>
  <files>extension/utils/capability-recipe-schema.js, extension/utils/capability-interpreter.js, extension/utils/recipe-synthesizer.js</files>
  <read_first>
    - MODIFY extension/utils/capability-recipe-schema.js (FSB_RECIPE_SCHEMA_VERSION:59; the schemaVersion property :85 const->enum; RECIPE_SCHEMA.properties:83-141; required:82; additionalProperties:false:81)
    - MODIFY extension/utils/capability-interpreter.js (the bound-spec build :472-481, the `extract:` carry at :480 is the analog)
    - MODIFY extension/utils/recipe-synthesizer.js (SCHEMA_VERSION const :71; the recipe-core build :285-293 with extract:'@' at :292)
    - Analog (additive-optional-field precedent + keeping v1 valid): the existing extract/csrf optional properties in RECIPE_SCHEMA; Phase-26 STATE note "validateRecipe error-mapping order"; Pitfall 6 (add OPTIONAL, not required; keep additionalProperties:false; re-freeze v2 hash in Plan 04)
    - The regression migrated in Task 3 of THIS plan: tests/capability-recipe-schema.test.js (v1 recipes still validate under enum:[1,2])
    - Source of truth: 32-RESEARCH.md Pattern 3 (carry-through), the additive-schema skeleton at RESEARCH :489-498, D-05/D-07/D-08, Assumption A4 (synthesized expectedShape is '@')
  </read_first>
  <action>
    In capability-recipe-schema.js: set FSB_RECIPE_SCHEMA_VERSION = 2 (the CURRENT stamp that NEW recipes — synthesized + newly bundled — are stamped with). Make the schema BACKWARD-COMPATIBLE: change the schemaVersion property (:85) from `{ const: FSB_RECIPE_SCHEMA_VERSION }` to `{ enum: [1, 2] }` so a persisted/bundled schemaVersion:1 recipe STILL validates at runtime alongside a new schemaVersion:2 recipe (this is the correct reading of D-08 "additive, v1 stays valid" — the Phase-31 persisted LEARNED recipes carry schemaVersion:1 and MUST keep validating per LEARN-04). Add two OPTIONAL properties to RECIPE_SCHEMA.properties (NOT to required): capturedAt:{ type:'string' } (an ISO timestamp for time-based rot age) and expectedShape:{ type:'string' } (a conservative JMESPath assertion — same string kind as extract). Keep additionalProperties:false unchanged and keep the required list unchanged. Note in an ASCII comment that the backward-compat mechanism is the enum (NOT a bumped const): v1 AND v2 schemaVersion both validate, and both new fields are additive/optional so a v1 recipe omitting them still validates. Confirm the existing validateRecipe error-mapping order (Phase-26 const/enum-before-additionalProperties) is not disturbed (it already handles enum; the schemaVersion now joins the enum family).

    In capability-interpreter.js: in the bound-spec build (:472-481), add two carries alongside `extract:` (mirroring it exactly): `expectedShape: (typeof recipe.expectedShape === 'string') ? recipe.expectedShape : null` and `capturedAt: (typeof recipe.capturedAt === 'string') ? recipe.capturedAt : null`. This threads the new fields into the spec so a downstream consumer (the router/rot-detector in Plan 03) can read them; the router will read recipe.expectedShape directly per RESEARCH, so this carry is the belt-and-suspenders path. Do NOT change any other spec field; do NOT change the origin-pin / query-fold logic.

    In recipe-synthesizer.js: update SCHEMA_VERSION (:71) to 2 (the current stamp — so NEW synthesized learned recipes carry schemaVersion:2; this does NOT invalidate already-persisted schemaVersion:1 learned recipes, which still validate under enum:[1,2]). In the recipe-core build (:285-293), add expectedShape:'@' (the shape-only conservative assertion — the synthesizer only has redacted shape-only capture, never a response body, so '@' = "the learned endpoint still returns a non-empty JSON response" is the strongest derivable assertion, D-07/A4) and capturedAt: a new Date().toISOString() string. Keep the core CLOSED-vocab (only schema-valid fields; flaggedForPhase32 stays on the descriptor, never the core — D-12). The validateRecipe gate at :298-303 must still pass for the synthesized recipe.
  </action>
  <verify>
    <automated>node tests/recipe-synthesizer.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/recipe-synthesizer.test.js` exits 0 (synthesized recipes carry schemaVersion:2 + expectedShape:'@' + capturedAt and still pass validateRecipe).
    - `node -e "const s=require('./extension/utils/capability-recipe-schema.js'); const sv=s.RECIPE_SCHEMA.properties.schemaVersion; const enumOk = Array.isArray(sv.enum) && sv.enum.indexOf(1)>=0 && sv.enum.indexOf(2)>=0 && sv.const===undefined; process.exit(s.FSB_RECIPE_SCHEMA_VERSION===2 && enumOk && s.RECIPE_SCHEMA.additionalProperties===false && !s.RECIPE_SCHEMA.required.includes('expectedShape') && !s.RECIPE_SCHEMA.required.includes('capturedAt') && !!s.RECIPE_SCHEMA.properties.expectedShape && !!s.RECIPE_SCHEMA.properties.capturedAt ? 0 : 1)"` exits 0 (version 2 as the stamp; schemaVersion is enum:[1,2] NOT const; both new fields are OPTIONAL properties; additionalProperties:false preserved).
    - `node -e "const fs=require('fs'),vm=require('vm');vm.runInThisContext(fs.readFileSync('extension/lib/cfworker-json-schema.min.js','utf8'));const s=require('./extension/utils/capability-recipe-schema.js');const v1=JSON.parse(fs.readFileSync('catalog/recipes/_fixtures/valid-recipe.json','utf8'));process.exit(s.validateRecipe(v1).success===true?0:1)"` exits 0 (the persisted-style v1 recipe still validates at runtime — the literal backward-compat proof for D-08).
  </acceptance_criteria>
  <done>Schema bumped to v2 with schemaVersion enum:[1,2] (v1 AND v2 both validate at runtime — true backward-compat) + optional capturedAt+expectedShape (closed vocab preserved); the interpreter carries both into the spec; the synthesizer stamps a conservative '@' expectedShape + capturedAt at schemaVersion:2; the synthesizer suite is green and a v1 recipe still validates.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate the in-chain schema regression gate for the enum bump (v1 still valid; out-of-enum rejected; v2-with-optional-fields valid)</name>
  <files>tests/capability-recipe-schema.test.js, catalog/recipes/_fixtures/valid-recipe.json</files>
  <read_first>
    - MODIFY tests/capability-recipe-schema.test.js (the version assertion :65; the v1 accept block :67-71; the schemaVersion-2-rejected block :109-114; the missing-version block :116-120)
    - DO NOT MIGRATE catalog/recipes/_fixtures/valid-recipe.json off schemaVersion:1 — it is the backward-compat fixture (it is in this plan's files_modified ONLY so an OPTIONAL sibling v2 fixture may be added; the v1 fixture itself stays v1)
    - Analog (the zero-framework check() + the cfworker vm-load idiom): this same file's existing structure (:55-65 cfworker preload, check() counters)
    - Source of truth: D-08 (enum:[1,2] backward-compat), 32-VALIDATION.md HEAL-02 additive-schema row, Pitfall 6
  </read_first>
  <action>
    Migrate tests/capability-recipe-schema.test.js so it gates the v2 enum schema (it runs INSIDE `npm test`, so it MUST be green after Plan 02, not red):
    - Line 65: change the assertion from `M.FSB_RECIPE_SCHEMA_VERSION === 1` to `M.FSB_RECIPE_SCHEMA_VERSION === 2` (the current stamp). Update the message string to "=== 2".
    - Lines 67-71: KEEP the existing `valid-recipe.json` (schemaVersion:1) -> validateRecipe success:true assertion UNCHANGED — this is the load-bearing backward-compat proof (a persisted v1 recipe still validates under enum:[1,2]). Do NOT migrate this fixture to v2. Add an ASCII comment that this asserts v1 backward-compat under the enum.
    - Lines 109-114: this block currently asserts `schemaVersion:2 -> RECIPE_SCHEMA_INVALID`, which is now INVERTED (2 is valid). REPLACE it with: (a) an OUT-OF-ENUM assertion — `Object.assign({}, valid, { schemaVersion: 3 })` (and/or 0) -> success:false AND code:'RECIPE_SCHEMA_INVALID' (an unknown version is still rejected); AND (b) a POSITIVE v2 assertion — `Object.assign({}, valid, { schemaVersion: 2, capturedAt: '2026-06-23T00:00:00.000Z', expectedShape: '@' })` -> success:true (a v2 recipe carrying the new optional fields validates). Use clear check() messages naming "out-of-enum schemaVersion 3 -> RECIPE_SCHEMA_INVALID" and "schemaVersion 2 + optional capturedAt/expectedShape -> valid".
    - Lines 116-120: KEEP the missing-schemaVersion -> RECIPE_SCHEMA_INVALID block UNCHANGED (schemaVersion is still required; only its allowed values widened to the enum).
    - OPTIONAL: add a sibling fixture catalog/recipes/_fixtures/valid-recipe-v2.json (schemaVersion:2 + capturedAt + expectedShape) and a check() that readFixture('valid-recipe-v2.json') validates success:true, to lock the v2 accept path to a fixture rather than an inline object. Keep ASCII-only, NO emojis.
  </action>
  <verify>
    <automated>node tests/capability-recipe-schema.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/capability-recipe-schema.test.js` exits 0 (migrated: version===2; the v1 fixture still validates; out-of-enum rejected; v2-with-optional-fields valid).
    - `grep -n "FSB_RECIPE_SCHEMA_VERSION === 2" tests/capability-recipe-schema.test.js` matches (the version assertion is migrated to 2).
    - `node -e "const v=require('./catalog/recipes/_fixtures/valid-recipe.json'); process.exit(v.schemaVersion===1?0:1)"` exits 0 (the backward-compat v1 fixture is NOT migrated off schemaVersion:1).
    - The test asserts BOTH an out-of-enum version (3 or 0) -> RECIPE_SCHEMA_INVALID AND a schemaVersion:2-with-optional-fields -> success:true (grep the source for both behaviors).
  </acceptance_criteria>
  <done>The in-chain schema regression gate is migrated for the enum bump: version===2 asserted, the v1 fixture still validates (backward-compat proven), an out-of-enum version is rejected, and a v2 recipe with the new optional fields validates; the gate is green inside npm test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executeBoundSpec result (page-fetched) -> classifyRecipeBroken | The result may carry an error/auth-bearing body (HTML login, 4xx JSON); the classifier reads status/redirected/data but must not leak the body |
| recipe.expectedShape (recipe data) -> jmespath engine | A JMESPath string from a recipe is evaluated read-only; it must never reach eval (Wall-1) |
| schema closed vocab -> additive fields + version widening | New optional fields must not open additionalProperties; widening schemaVersion to enum:[1,2] must not accept an out-of-enum version or break v1 validity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-MASK | Tampering/Repudiation | classifyRecipeBroken (rot-detector) | mitigate | The "present-container" predicate treats an empty-but-present container of the expected kind as a REAL empty result (not-broken, returned verbatim); only a missing path / wrong-kind body is RECIPE_EXPIRED — so a legitimate no-results is never healed away (HEAL-04, D-06). Asserted both-ways by the Plan-01 suite. |
| T-32-PASS | Elevation of Privilege | classifyRecipeBroken | mitigate | The typed-passthrough branch (success:false AND /^RECIPE_/) runs BEFORE the generic fetch-failed branch, so RECIPE_ORIGIN_MISMATCH / RECIPE_CONSENT_* classify as not-broken and never trigger a fallback (Pitfall 3). |
| T-32-LEAK | Information Disclosure | capability-rot-detector.js | mitigate | The classifier reads result.data/status/redirected but NEVER logs them raw; no console.log of a body/text (V7). Any future diagnostic must route through redactForLog (deferred to Plan 03 where logging is wired). |
| T-32-WALL1 | Tampering (Wall-1) | expectedShape eval + v2 schema | mitigate | expectedShape is type:'string' evaluated by the read-only jmespath engine (never eval'd); the module is on the recipe-path allowlist and is comment-scanned eval-free; additionalProperties:false preserved so the closed vocab does not open (Pitfall 6). |
| T-32-COMPAT | Tampering/Denial of Service | schemaVersion enum:[1,2] (backward-compat) | mitigate | schemaVersion is enum:[1,2] NOT const, so the Phase-31 persisted LEARNED recipes (schemaVersion:1) STILL validate at runtime (no mass-invalidation of LEARN-04 recipes), while an out-of-enum version is still rejected (RECIPE_SCHEMA_INVALID). The migrated in-chain regression gate (Task 3) asserts the v1 fixture validates AND an out-of-enum version is rejected, so a future const-revert would red the build (D-08). |
| T-32-FP | Denial of Service | validateExpectedShape | mitigate | The engine-absent path returns true (conservative degrade) so a missing jmespath global cannot produce a flood of false RECIPE_EXPIRED that would mask real results (D-06). |
| T-32-SC | Tampering | npm/pip/cargo installs | accept | No external packages installed (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `node tests/capability-rot-detector.test.js` exits 0 (taxonomy + expectedShape green).
- `node tests/capability-recipe-schema.test.js` exits 0 (migrated for the enum bump: version===2; v1 fixture still valid; out-of-enum rejected; v2-with-optional-fields valid).
- `node tests/recipe-synthesizer.test.js` exits 0 (synthesized expectedShape:'@' + capturedAt at schemaVersion:2).
- `node scripts/verify-recipe-path-guard.mjs` exits 0 (rot-detector eval-free + on allowlist).
</verification>

<success_criteria>
- capability-rot-detector.js exists (classifyRecipeBroken + validateExpectedShape), eval-free, taxonomy + expectedShape green.
- Schema is v2 with schemaVersion enum:[1,2] (v1 AND v2 both validate at runtime — true backward-compat) + optional capturedAt + expectedShape; additionalProperties:false preserved.
- The in-chain schema regression gate is migrated (version===2; v1 fixture still valid; out-of-enum rejected; v2-with-optional-fields valid) and green.
- Interpreter carries expectedShape + capturedAt into the spec.
- Synthesizer stamps a conservative '@' expectedShape + capturedAt; schemaVersion follows to 2.
</success_criteria>

<output>
Create `.planning/phases/32-self-healing-fallback-recipe-rot-detection-re-learn-provider/32-02-SUMMARY.md` when done
</output>
</content>
</invoke>
