---
phase: 57-agent-identity-capture
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - extension/utils/mcp-client-aliases.js
  - extension/utils/mcp-agent-providers.js
  - extension/background.js
  - package.json
  - tests/mcp-client-merged-view.test.js
  - tests/mcp-client-identity-integration.test.js
  - tests/mcp-version-parity.test.js
  - tests/visual-session-schema-lock.test.js
autonomous: true
requirements:
  - IDENT-01
  - IDENT-02
  - IDENT-03
  - IDENT-04
  - IDENT-05
must_haves:
  truths:
    - "One getMcpClients runtime action returns a stable union of clicked, installed, connected, and live evidence without deriving a provider recommendation."
    - "Known MCP client name drift joins only through a closed explicit alias table; unknown names remain visible as raw non-authoritative entries."
    - "Phase 57's new wire fields/types and runtime action are additive: existing MCPMessageType strings, tool schemas, register responses, and sunset listAgents behavior remain unchanged."
  artifacts:
    - path: "extension/utils/mcp-client-aliases.js"
      provides: "Closed canonical client-id and MCP clientInfo name mapping"
      contains: "resolveMcpClientAlias"
    - path: "extension/utils/mcp-agent-providers.js"
      provides: "Merged evidence view that rehydrates durable storage and joins live records"
      contains: "getMergedClients"
    - path: "extension/background.js"
      provides: "Own-extension getMcpClients runtime action"
      contains: "case 'getMcpClients'"
    - path: "tests/mcp-client-merged-view.test.js"
      provides: "Union, alias, raw-unknown, stale inventory, eviction, and same-context dispatch evidence"
  key_links:
    - from: "extension/background.js"
      to: "extension/utils/mcp-client-aliases.js"
      via: "alias module loads before mcp-agent-providers at service-worker boot"
      pattern: "mcp-client-aliases.*mcp-agent-providers"
    - from: "extension/background.js"
      to: "extension/utils/agent-registry.js"
      via: "getMcpClients reads cloned live records through listAgents()"
      pattern: "listAgents"
    - from: "package.json"
      to: "tests/mcp-client-merged-view.test.js"
      via: "root npm test permanently runs every Phase 57 contract"
      pattern: "mcp-client-merged-view.test.js"
---

<objective>
Publish the single canonical MCP-client evidence view and freeze Phase 57's cross-stack compatibility.

Purpose: Give Phase 58 one durable, honest read model while ensuring identity evidence never becomes implicit authority and every existing MCP/browser contract stays intact.
Output: Explicit alias vocabulary, merged query helper, `getMcpClients` runtime action, cross-stack integration tests, and permanent full-suite wiring.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-agent-identity-capture/57-CONTEXT.md
@.planning/phases/57-agent-identity-capture/57-RESEARCH.md
@.planning/phases/57-agent-identity-capture/57-VALIDATION.md
@.planning/phases/57-agent-identity-capture/57-UI-SPEC.md
@.planning/phases/57-agent-identity-capture/57-01-SUMMARY.md
@.planning/phases/57-agent-identity-capture/57-02-SUMMARY.md

<interfaces>
Alias module classic-script global:

```javascript
globalThis.FsbMcpClientAliases = {
  normalizeMcpClientName: function(name) {},
  resolveMcpClientAlias: function(name) {}
};
```

Merged entry shape (map key is canonical id or `raw:<normalized-name>`):

```javascript
{
  id: string,
  raw: boolean,
  displayName: string,
  clicked: object | null,
  installed: object | null,
  connected: object | null,
  live: object | null
}
```

Runtime response:

```javascript
{ success: true, clients: { [id]: MergedMcpClientEntry } }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the closed MCP-client alias vocabulary and deterministic merge</name>
  <files>extension/utils/mcp-client-aliases.js, extension/utils/mcp-agent-providers.js, extension/background.js, tests/mcp-client-merged-view.test.js</files>
  <read_first>
- extension/ui/onboarding.js
- mcp/src/platforms.ts
- extension/utils/mcp-agent-providers.js
- extension/utils/agent-registry.js
- extension/background.js
- .planning/phases/57-agent-identity-capture/57-CONTEXT.md
  </read_first>
  <action>
Implement D-09 and the merge half of D-08 exactly. Create `extension/utils/mcp-client-aliases.js` as a pure IIFE classic script. Export `normalizeMcpClientName(name)` using `String(name).trim().toLowerCase().replace(/[\s_-]+/g, '')` and `resolveMcpClientAlias(name)` backed only by a frozen explicit map. Seed these canonical mappings: `claude`, `claudecode`, and `anthropicclaude` → `claude-code`; `claudedesktop` → `claude-desktop`; `cursor` → `cursor`; `visualstudiocode` and `vscode` → `vscode`; `windsurf` → `windsurf`; `codex`, `openaicodex`, and `codexcli` → `codex`; `opencode` and `opencodecli` → `opencode`; `openclaw` → `openclaw`. Return null for every unlisted value. Do not infer by substring/fuzzy matching and do not add Gemini.

In `background.js`, import this module immediately before `mcp-agent-providers.js`. Extend `FsbMcpAgentProviders` with `getMergedClients(liveRecords)`. Re-read durable storage on every call. Start with every clicked/installed canonical key, then join connected entries by `resolveMcpClientAlias(record.name)`. For an unmapped non-empty name, use key `raw:` plus the normalized name (or a stable `raw:unknown-<index>` if normalization is empty), set `raw: true`, preserve `displayName` from the original name, and never merge it into clicked/installed. Join live registry clones by their optional `clientInfo` using the same rule. Each output entry must always contain all six fields from `<interfaces>` with missing evidence represented as null. Preserve the underlying evidence objects separately; do not flatten status, compute connected > installed > clicked tiers, select a recommendation, mutate storage, or render UI.

Create `tests/mcp-client-merged-view.test.js` with a fake storage and live registry. Assert every explicit alias above, separator/case normalization, null for substring lookalikes, no Gemini row, union across each evidence-only case, reconnection overwrite, unknown raw visibility/non-merge, live+connected join, null placeholders, stale installed `checkedAt` preservation, deterministic keys, fresh storage reads after simulated SW eviction, and no recommendation/provider-selection fields.
  </action>
  <acceptance_criteria>
- Alias resolution uses only the enumerated frozen mappings and returns null for unknown/fuzzy lookalikes.
- Every merged row has exact keys `id`, `raw`, `displayName`, `clicked`, `installed`, `connected`, and `live`.
- Unknown client names produce `raw:*` rows and never inherit clicked or installed evidence.
- `getMergedClients` re-reads `chrome.storage.local` and does not derive a tier, recommendation, or selected provider.
- `tests/mcp-client-merged-view.test.js` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node tests/mcp-client-merged-view.test.js</automated>
  </verify>
  <done>Every known identity source joins deterministically by closed vocabulary, while novel MCP clients remain honestly visible and non-authoritative.</done>
</task>

<task type="auto">
  <name>Task 2: Expose getMcpClients through the existing runtime-message chokepoint</name>
  <files>extension/background.js, tests/mcp-client-merged-view.test.js, tests/mcp-bridge-background-dispatch.test.js</files>
  <read_first>
- extension/background.js
- extension/utils/mcp-agent-providers.js
- extension/utils/agent-registry.js
- extension/ws/mcp-bridge-client.js
- tests/mcp-bridge-background-dispatch.test.js
- tests/runtime-contracts.test.js
- .planning/phases/57-agent-identity-capture/57-UI-SPEC.md
  </read_first>
  <action>
Complete D-08 and D-10 exactly. Add `case 'getMcpClients'` to `fsbHandleRuntimeMessage` after the existing own-extension sender guard. Obtain live records only through `globalThis.fsbAgentRegistryInstance.listAgents()` when available, otherwise use `[]`. Call `FsbMcpAgentProviders.getMergedClients(liveRecords)` asynchronously and respond with exactly `{ success: true, clients }`; on helper/read failure respond `{ success: false, error: 'mcp_client_inventory_unavailable' }` without leaking raw exception/storage data. Return `true` to keep the channel open.

Do not resurrect or modify the sunset `listAgents` runtime action. Do not call `chrome.runtime.sendMessage` from service-worker code. Prove the existing `globalThis.fsbDispatchInternalMessage({ action: 'getMcpClients' })` path reaches the same handler and envelope. Add no manifest permission, HTML/CSS, provider selection, key field, cost copy, or rendering.

Extend `tests/mcp-client-merged-view.test.js` and `tests/mcp-bridge-background-dispatch.test.js` to cover cross-context sender acceptance/rejection, same-context dispatch, empty registry, live clones, async keep-open behavior, bounded failure envelope, and exact successful response. Add source assertions that the sunset action remains deprecated and no `chrome.runtime.sendMessage({ action: 'getMcpClients' })` appears in background code.
  </action>
  <acceptance_criteria>
- `background.js` contains one `case 'getMcpClients'` returning `{ success: true, clients }` asynchronously through `sendResponse`.
- Unknown/external senders still fail through the existing sender guard; storage errors return only `mcp_client_inventory_unavailable`.
- Same-service-worker callers use `globalThis.fsbDispatchInternalMessage`, and sunset `listAgents` behavior is unchanged.
- No Phase 57 file renders a client row/status/recommendation or changes extension permissions.
- Both focused runtime/bridge tests exit 0.
  </acceptance_criteria>
  <verify>
    <automated>node tests/mcp-client-merged-view.test.js && node tests/mcp-bridge-background-dispatch.test.js && node tests/runtime-contracts.test.js</automated>
  </verify>
  <done>Every extension surface can request one consistent, durable evidence map through the established sender-guarded runtime route.</done>
</task>

<task type="auto">
  <name>Task 3: Freeze additive wire behavior and make Phase 57 contracts permanent</name>
  <files>package.json, tests/mcp-client-identity-integration.test.js, tests/mcp-version-parity.test.js, tests/visual-session-schema-lock.test.js</files>
  <read_first>
- package.json
- mcp/src/types.ts
- mcp/src/agent-scope.ts
- extension/ws/mcp-tool-dispatcher.js
- tests/mcp-version-parity.test.js
- tests/visual-session-schema-lock.test.js
- tests/tool-definitions-parity.test.js
- .planning/phases/57-agent-identity-capture/57-VALIDATION.md
  </read_first>
  <action>
Create `tests/mcp-client-identity-integration.test.js` as the cross-stack Phase 57 contract. Simulate: initialized clientInfo entering AgentScope; one register payload carrying optional `clientInfo` and `platforms`; dispatcher sanitization/live stamp/durable connected+installed writes; a reconnect updating rather than duplicating; onboarding clicked evidence; a fresh service-worker harness; and `getMcpClients` returning all evidence. Include negative controls for absent identity, malformed/oversized identity, unknown alias, offline inventory, storage rejection, and a legacy `payload: {}` register. Reuse/execute the focused tests created by Plans 01/02 rather than editing them in Wave 2.

Update `tests/mcp-version-parity.test.js` and `tests/visual-session-schema-lock.test.js` only where their exact source/token/hash pins require additive Phase 57 changes. Assert every pre-Phase-57 `MCPMessageType` string remains present and unchanged, the only new Phase 57 type is `system:client-inventory`, no MCP tool definition/schema changes, and the register response remains exact. Update source-pin counts/substrings in the same task as their source changes; do not relax a pin to a broad or truthy assertion.

Insert the six new Phase 57 test commands into root `package.json`'s existing serial `test` script immediately after the existing MCP bridge/identity/platform cluster: `mcp-client-identity.test.js`, `mcp-client-inventory.test.js`, `mcp-agent-providers-storage.test.js`, `onboarding-agent-provider-clicks.test.js`, `mcp-client-merged-view.test.js`, and `mcp-client-identity-integration.test.js`. Keep all existing test commands and order otherwise unchanged.

Run each focused test, `npm --prefix mcp run build`, and then the full `npm test`. If a failure occurs, diagnose and fix an in-scope regression or stop with exact blocker evidence; do not delete/skip an existing test or weaken an assertion to obtain green.
  </action>
  <acceptance_criteria>
- Root `npm test` permanently invokes all six Phase 57 test files without dropping any existing command.
- Existing MCP type strings/tool schemas and the exact register response are frozen; only optional payload fields and `system:client-inventory` are added.
- `tests/mcp-client-identity-integration.test.js` covers stdio/HTTP identity, both inventory paths, eviction rehydration, reconnect update, copy evidence, aliases, and merged query.
- Exact source-pin tests are updated narrowly and remain meaningful.
- `npm --prefix mcp run build` and `npm test` both exit 0.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix mcp run build && node tests/mcp-client-identity.test.js && node tests/mcp-client-inventory.test.js && node tests/mcp-agent-providers-storage.test.js && node tests/onboarding-agent-provider-clicks.test.js && node tests/mcp-client-merged-view.test.js && node tests/mcp-client-identity-integration.test.js && node tests/mcp-version-parity.test.js && node tests/visual-session-schema-lock.test.js && npm test</automated>
  </verify>
  <done>Phase 57's complete evidence flow is regression-covered, permanently in the root suite, and demonstrably additive across every existing MCP and extension boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Durable/raw identity → canonical merge | Spoofed names could be misclassified as installed/known clients. |
| Extension pages/MCP bridge → runtime query | Callers request local inventory through the service-worker chokepoint. |
| Additive Phase 57 wire changes → legacy consumers | Optional fields/new type must not mutate existing schemas or response semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T57-01 | Spoofing | Alias resolver | mitigate | Closed exact normalized map only; unknown values remain raw and gain no installed/clicked authority. |
| T57-05 | Information Disclosure | `getMcpClients` error path | mitigate | Existing own-extension sender guard and one bounded error code; no raw storage/exception leakage. |
| T57-06 | Tampering | MCP types/schemas/register response | mitigate | Narrow additive pins plus full suite; no existing string, tool schema, or response member may change. |
| T57-08 | Elevation of Privilege | Merged evidence consumers | mitigate | Return separate evidence only; do not derive authorization, selection, recommendation, or permission state. |
</threat_model>

<verification>
1. Run all five Phase 57 focused test files.
2. Run `node tests/mcp-version-parity.test.js` and `node tests/visual-session-schema-lock.test.js`.
3. Run `npm --prefix mcp run build`.
4. Run `npm test` as the final Phase 57 regression gate.
5. Confirm `git diff --check` and inspect that no onboarding HTML/CSS or manifest permission changed.
</verification>

<success_criteria>
- `getMcpClients` returns the stable clicked/installed/connected/live union after service-worker rehydration.
- Only explicit aliases merge known clients; novel identities remain raw and visible.
- Phase 58 can consume the view without amending its shape.
- INV-01 and INV-05 are covered by exact tests, not assumptions.
- The full repository suite includes and passes all Phase 57 contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/57-agent-identity-capture/57-03-SUMMARY.md`.
</output>
