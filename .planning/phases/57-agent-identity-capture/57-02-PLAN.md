---
phase: 57-agent-identity-capture
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - extension/utils/mcp-agent-providers.js
  - extension/utils/agent-registry.js
  - extension/ws/mcp-tool-dispatcher.js
  - extension/ws/mcp-bridge-client.js
  - extension/ui/onboarding.js
  - extension/background.js
  - tests/mcp-agent-providers-storage.test.js
  - tests/onboarding-agent-provider-clicks.test.js
autonomous: true
requirements:
  - IDENT-01
  - IDENT-03
  - IDENT-04
must_haves:
  truths:
    - "Every onboarding copy action records durable clicked evidence without delaying or changing the existing clipboard, checkmark, render, or toast behavior."
    - "A sanitized MCP clientInfo value is stamped on the live AgentRecord and rolled into durable connected evidence, but never participates in authorization or ownership."
    - "Both system:client-inventory frames and agent:register platforms piggybacks replace only the installed sub-map and preserve clicked/connected evidence."
  artifacts:
    - path: "extension/utils/mcp-agent-providers.js"
      provides: "Durable fsbAgentProviders envelope reads and sibling-preserving sub-map mutations"
      contains: "FSB_AGENT_PROVIDERS_STORAGE_KEY"
    - path: "extension/utils/agent-registry.js"
      provides: "Live AgentRecord clientInfo stamping"
      contains: "stampClientInfo"
    - path: "extension/ui/onboarding.js"
      provides: "Invisible, fire-and-forget copy-click persistence"
      contains: "persistCopyClick"
    - path: "tests/mcp-agent-providers-storage.test.js"
      provides: "Sanitization, durable rollup, sibling preservation, and inventory ingestion evidence"
    - path: "tests/onboarding-agent-provider-clicks.test.js"
      provides: "Exact base/fan/all attribution and unchanged feedback-timing evidence"
  key_links:
    - from: "extension/background.js"
      to: "extension/utils/mcp-agent-providers.js"
      via: "classic-service-worker importScripts before dispatcher and bridge client"
      pattern: "importScripts.*mcp-agent-providers"
    - from: "extension/ws/mcp-tool-dispatcher.js"
      to: "extension/utils/agent-registry.js"
      via: "handleAgentRegisterRoute calls stampClientInfo after sanitization"
      pattern: "stampClientInfo"
    - from: "extension/ws/mcp-bridge-client.js"
      to: "extension/utils/mcp-agent-providers.js"
      via: "system:client-inventory replaces the installed evidence snapshot"
      pattern: "system:client-inventory"
---

<objective>
Persist the three Phase 57 evidence streams on the extension side without creating any new visible UI.

Purpose: Make copy intent, connected identity, and installed inventory survive MV3 service-worker eviction and Chrome restart while preserving current register/clipboard contracts.
Output: A durable storage helper, live registry identity stamping, bridge/registration ingestion, invisible onboarding persistence, and focused extension contract tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-agent-identity-capture/57-CONTEXT.md
@.planning/phases/57-agent-identity-capture/57-RESEARCH.md
@.planning/phases/57-agent-identity-capture/57-VALIDATION.md
@.planning/phases/57-agent-identity-capture/57-UI-SPEC.md

<interfaces>
Create this classic-script global before the dispatcher and bridge client load:

```javascript
globalThis.FsbMcpAgentProviders = {
  read: async function() {},
  mutateSubmap: async function(submap, mutator) {},
  recordConnected: async function(agentId, clientInfo) {},
  replaceInstalled: async function(platforms) {}
};
```

Durable storage key and envelope:

```javascript
const FSB_AGENT_PROVIDERS_STORAGE_KEY = 'fsbAgentProviders';
{
  clicked: { [clientId]: { count, firstClickedAt, lastClickedAt, source } },
  connected: { [stableIdentityKey]: { name, version, lastSeenAt } },
  installed: { [clientId]: { detected, configPath, version?, checkedAt } }
}
```

Keep the current `agent:register` response exact:

```javascript
{ success: true, agentId, agentIdShort, ownershipTokens: {}, connectionId }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add durable provider evidence storage and live clientInfo stamping</name>
  <files>extension/utils/mcp-agent-providers.js, extension/background.js, extension/utils/agent-registry.js, tests/mcp-agent-providers-storage.test.js</files>
  <read_first>
- extension/background.js
- extension/utils/agent-registry.js
- extension/utils/mcp-task-store.js
- extension/ws/mcp-tool-dispatcher.js
- tests/agent-registry.test.js
- tests/mcp-task-store.test.js
- .planning/phases/57-agent-identity-capture/57-CONTEXT.md
  </read_first>
  <action>
Implement D-04's live-record mutator and D-06/D-07's durable envelope exactly. Create `extension/utils/mcp-agent-providers.js` as an IIFE classic script exporting only `globalThis.FsbMcpAgentProviders`. Define `FSB_AGENT_PROVIDERS_STORAGE_KEY = 'fsbAgentProviders'` and normalize every read to `{ clicked: {}, connected: {}, installed: {} }` while copying unknown sibling keys forward. Implement a module-scope Promise-chain mutex using the established `.then(fn, fn)` / swallowed-tail pattern so service-worker writers serialize. `mutateSubmap(submap, mutator)` must accept only `clicked`, `connected`, or `installed`, clone the selected map, await the mutator, and write the complete envelope with all other maps/unknown keys preserved.

Implement `recordConnected(agentId, clientInfo)` for already-sanitized evidence. Use a stable lowercase/whitespace-stripped `name` key when present; if name is absent, use `unknown:` plus the normalized version, falling back to the agent id. Overwrite that key with `{ name: clientInfo.name || '', version: clientInfo.version || '', lastSeenAt: Date.now() }` so reconnects update instead of append. Implement `replaceInstalled(platforms)` to validate the incoming map and atomically replace only `installed`; accept records containing boolean `detected`, nullable/string `configPath`, optional string `version`, and finite numeric `checkedAt`, dropping malformed fields/records.

Import the helper in `background.js` immediately after `agent-registry.js` and before `mcp-tool-dispatcher.js` / `mcp-bridge-client.js`; wrap it in the same defensive `try { importScripts(...) } catch` pattern. Do not change manifest permissions.

Add `AgentRegistry.prototype.stampClientInfo(agentId, clientInfo)` immediately beside `stampConnectionId`. Require a string agentId and a plain clientInfo object containing at least one string `name`/`version`; copy only those fields to a new `record.clientInfo`, fire-and-forget `_persist()`, and return boolean without throwing. Ensure `cloneRecord`/list/persistence naturally carries the new optional property and legacy records hydrate without it.

Create `tests/mcp-agent-providers-storage.test.js` with a fake `chrome.storage.local`, fresh-module reloads, and registry harness. Prove default-envelope creation, sibling/unknown-key preservation, serialized concurrent mutations, reconnect overwrite, version-only fallback, installed validation/replacement, eviction re-read, clientInfo stamp persistence, and legacy-record hydration. Keep the existing registry test unchanged and run it as a regression.
  </action>
  <acceptance_criteria>
- `background.js` imports `utils/mcp-agent-providers.js` after the registry and before dispatcher/bridge evaluation.
- Every storage mutation preserves the two untouched evidence maps and unknown envelope keys.
- `recordConnected` updates one stable entry's `lastSeenAt`; it never appends an array row.
- `stampClientInfo` copies only string `name` and `version`, returns false on malformed input, and never throws on persistence failure.
- The new focused test and unchanged `tests/agent-registry.test.js` both exit 0.
  </acceptance_criteria>
  <verify>
    <automated>node tests/mcp-agent-providers-storage.test.js && node tests/agent-registry.test.js</automated>
  </verify>
  <done>The extension has a durable, eviction-safe evidence envelope and live AgentRecords can carry sanitized identity without changing registry authorization behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Ingest connected identity and installed inventory through additive bridge paths</name>
  <files>extension/ws/mcp-tool-dispatcher.js, extension/ws/mcp-bridge-client.js, tests/mcp-agent-providers-storage.test.js</files>
  <read_first>
- extension/ws/mcp-tool-dispatcher.js
- extension/ws/mcp-bridge-client.js
- extension/utils/agent-registry.js
- extension/utils/mcp-agent-providers.js
- tests/agent-bridge-routes.test.js
- tests/mcp-bridge-background-dispatch.test.js
- .planning/phases/57-agent-identity-capture/57-CONTEXT.md
  </read_first>
  <action>
Complete D-04, D-05, D-14, and D-15 exactly. In `handleAgentRegisterRoute`, sanitize `payload.clientInfo` before registry/storage use: require a non-array object; accept `name` and `version` only when each is a string; truncate each to exactly 200 characters maximum; drop unknown fields; treat an empty result as absent. After successful mint/cap handling and connection-id stamping, call `reg.stampClientInfo(agentId, clientInfo)` best-effort and invoke `FsbMcpAgentProviders.recordConnected(agentId, clientInfo)` best-effort. If `payload.platforms` is a plain object, independently call `replaceInstalled(payload.platforms)`. None of these writes may change the exact response object or make registration fail.

In `MCPBridgeClient._routeMessage`, add a `case 'system:client-inventory'` before MCP tool routes. Validate `payload.platforms` is a plain object, require `globalThis.FsbMcpAgentProviders.replaceInstalled`, await the replacement, and return `{ accepted: true }`; malformed/unavailable helpers throw a bounded error which the existing `_handleMessage` converts to an error response. Do not add rendering, recommendation, or spawn behavior.

Extend the focused storage test's dispatcher/bridge harness to prove 200-character caps, unknown-field removal, name-only/version-only handling, identity never enters ownership checks, `platforms` piggyback and system frame converge on the same installed map, malformed inventory rejects, storage failure leaves registration successful, and the response remains deep-equal to the legacy/additive connectionId shape. Keep `tests/mcp-bridge-background-dispatch.test.js` unchanged and run it as a regression.
  </action>
  <acceptance_criteria>
- Dispatcher source accepts only bounded string `clientInfo.name`/`version` and never treats them as permission, cap, ownership, or routing inputs.
- `payload.platforms` and `system:client-inventory` both call the same `replaceInstalled` helper.
- Storage/helper failures are best-effort and do not change `{ success, agentId, agentIdShort, ownershipTokens, connectionId }`.
- The focused storage test and unchanged bridge-background regression both exit 0.
  </acceptance_criteria>
  <verify>
    <automated>node tests/mcp-agent-providers-storage.test.js && node tests/mcp-bridge-background-dispatch.test.js</automated>
  </verify>
  <done>Both additive daemon delivery paths persist honest installed evidence, and every successful registration persists only sanitized, non-authoritative connection identity.</done>
</task>

<task type="auto">
  <name>Task 3: Persist onboarding copy intent without visible or timing changes</name>
  <files>extension/ui/onboarding.js, tests/onboarding-agent-provider-clicks.test.js</files>
  <read_first>
- extension/ui/onboarding.js
- extension/ui/onboarding.html
- extension/ui/onboarding.css
- tests/install-identity.test.js
- tests/extension-content-script-files-completeness.test.js
- .planning/phases/57-agent-identity-capture/57-UI-SPEC.md
  </read_first>
  <action>
Implement D-16, D-17, D-18, and D-19 exactly. Inside `onboarding.js`, add `persistCopyClick(clientId, source)` using the existing `storageGet` and `storageSet` helpers; do not add a script tag, HTML/CSS change, runtime-message round trip, or new visual state. Read `fsbAgentProviders`, normalize the three sub-maps, and preserve connected/installed/unknown siblings. For each target id write `{ count: previousCount + 1, firstClickedAt: previousFiniteValue || now, lastClickedAt: now, source }`.

Call persistence exactly once from `copyCommand(text, key)` as a non-awaited, caught Promise after resolving targets but without moving/removing the existing `writeClipboard`, 1600 ms copied timer, `showToast('Install command copied')`, or immediate `render()` behavior. For `key === 'current'`, resolve `ROLL_CLIENTS[state.iconIndex].id` at invocation time and use `source: 'base'`. A normal fan id uses `source: 'fan'`. The `all` id expands in one storage mutation to exactly `['claude-code', 'claude-desktop', 'cursor', 'vscode', 'windsurf', 'codex', 'opencode']`, sets `source: 'all'` for each, and excludes `openclaw`. Storage rejection must be swallowed and must not change clipboard/check/toast/navigation behavior.

Create `tests/onboarding-agent-provider-clicks.test.js` using the repository's VM/source-contract style. Assert both base buttons still pass literal `current`, fan buttons pass canonical ids, base attribution follows the visible rolled client, the exact seven-id all expansion, count/timestamp semantics, sibling preservation, one mutation/render/toast per action, unchanged 1600/2600 ms feedback contracts, no awaited storage in the visual path, and no Phase 57 change to `onboarding.html`/`onboarding.css`.
  </action>
  <acceptance_criteria>
- `copyCommand()` still contains the exact toast `Install command copied`, the 1600 ms copied timer, and an immediate render independent of storage completion.
- Base, fan, and all writes use only source values `base`, `fan`, and `all`.
- `all` expands to exactly seven flag-backed ids and never includes `openclaw`.
- Repeated clicks preserve `firstClickedAt`, increment `count`, and replace `lastClickedAt` without rendering any value.
- `tests/onboarding-agent-provider-clicks.test.js` exits 0 and confirms HTML/CSS byte/source contracts remain unchanged.
  </acceptance_criteria>
  <verify>
    <automated>node tests/onboarding-agent-provider-clicks.test.js && node tests/runtime-contracts.test.js</automated>
  </verify>
  <done>All onboarding copy affordances invisibly persist durable intent while their existing visual, accessibility, and failure behavior stays unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP payload → extension registry/storage | Spoofable clientInfo and inventory data enter durable local state. |
| Onboarding page → shared storage envelope | A page-realm writer can race the service-worker writers. |
| Durable evidence → later provider UI | Evidence must remain descriptive and must not itself grant authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T57-01 | Spoofing | `handleAgentRegisterRoute` | mitigate | Accept only bounded name/version strings, store as evidence, and prove no ownership/cap/permission branch reads them. |
| T57-03 | Tampering | `fsbAgentProviders` RMW | mitigate | Serialize same-realm mutations and preserve every sibling/unknown key; exact tests cover both writer paths. |
| T57-04 | Denial of Service | Storage failures | mitigate | Every connected/inventory/click write is best-effort; registration and clipboard UX remain successful. |
| T57-06 | Tampering | Register response and onboarding source pins | mitigate | Deep-freeze the existing response/feedback strings and run paired source-contract regressions in the same tasks. |
</threat_model>

<verification>
1. `node tests/mcp-agent-providers-storage.test.js`
2. `node tests/agent-registry.test.js`
3. `node tests/mcp-bridge-background-dispatch.test.js`
4. `node tests/onboarding-agent-provider-clicks.test.js`
5. `node tests/runtime-contracts.test.js`
6. Wave 1 gate after Plans 57-01 and 57-02: `npm --prefix mcp run build` plus all Phase 57 focused tests and `npm test`.
</verification>

<success_criteria>
- Clicked, connected, and installed evidence survives a fresh extension/service-worker harness load.
- Spoofable clientInfo is bounded and observational only.
- Both inventory delivery paths converge without clobbering sibling evidence.
- Existing registration and onboarding UI contracts remain exact.
- All focused extension tests pass before the Wave 1 full regression.
</success_criteria>

<output>
After completion, create `.planning/phases/57-agent-identity-capture/57-02-SUMMARY.md`.
</output>
