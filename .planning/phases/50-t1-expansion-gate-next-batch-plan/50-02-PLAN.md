# 50-02 Plan: Full Regression Gate

## Objective

Run the milestone-required regression commands against the expanded T1 set.

## Tasks

1. Run `npm test`.
2. Run `npm run validate:extension`.
3. Preserve any existing unrelated dirty workspace changes.
4. Record exact pass/fail outcomes in `50-VERIFICATION.md`.

## Verification

- `npm test`
- `npm run validate:extension`
- `git diff --check` for the files touched by this closeout phase.
