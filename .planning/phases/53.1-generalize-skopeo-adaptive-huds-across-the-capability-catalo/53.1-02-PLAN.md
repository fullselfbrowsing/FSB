---
phase: 53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo
plan: "02"
type: execute
wave: 2
depends_on:
  - "53.1-01"
files_modified:
  - extension/utils/skopeo-capability-projector.js
  - tests/skopeo-capability-projection.test.js
autonomous: true
requirements:
  - ADAPT-01
  - ADAPT-02
  - ADAPT-06
  - ADAPT-10
must_haves:
  truths:
    - "An explicitly supplied current-tab URL resolves only through an exact admitted HTTPS origin to one current service/profile."
    - "D-03: The background-owned projection contains only the active tab/generation/origin/profile's bounded current-service capability groups and never the full catalog."
    - "D-07: Only `t1-ready` rows are actionable; every other canonical disposition is frozen static data with `invocable:false`."
  artifacts:
    - path: "extension/utils/skopeo-capability-projector.js"
      provides: "Pure exact-origin current-service projection"
      exports: ["STATUS", "READINESS", "createProjection", "validateProjection"]
    - path: "tests/skopeo-capability-projection.test.js"
      provides: "Origin, cache-key, readiness, grouping, and hostile-input oracle"
      contains: "t1-ready"
  key_links:
    - from: "extension/utils/skopeo-capability-projector.js"
      to: "extension/catalog/skopeo-profile-index.generated.js"
      via: "exact origin-index lookup"
      pattern: "FsbSkopeoProfileIndex"
    - from: "extension/utils/skopeo-capability-projector.js"
      to: "extension/background.js"
      via: "pure createProjection seam consumed in Plan 07"
      pattern: "createProjection"
---

<objective>
Project one exact-origin, generation-owned service/profile/capability surface for the current tab.

Purpose: Satisfy D-02, D-03, and D-07 without exposing all 2,314 descriptors to content or inventing readiness.
Output: Frozen classic-script projector and adversarial unit contract.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo/53.1-CONTEXT.md
@.planning/phases/53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo/53.1-RESEARCH.md
@.planning/phases/53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo/53.1-UI-SPEC.md
@.planning/phases/53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo/53.1-VALIDATION.md
@.planning/phases/53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo/53.1-01-SUMMARY.md

<interfaces>
From Plan 01 `extension/catalog/skopeo-profile-index.generated.js`:
```javascript
globalThis.FsbSkopeoProfileIndex = Object.freeze({
  schemaVersion, profileVersion, catalogVersion,
  originIndex, profiles, dispositions, capabilities
});
// Each capability preserves sourceReadiness/sourceTerminalState and carries one
// canonical presentationDisposition from Plan 01's closed nine-value mapping.
```

Existing lifecycle authority:
```javascript
// Positive-safe-integer tabId and generation are allocated by
// extension/utils/skopeo-session-state.js and owned by extension/background.js.
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Current tab URL → profile index | URL/host data is untrusted and must not use substring or visible-page inference. |
| Generated index → content projection | Only one bounded current-service slice may cross from background data to content. |
| Readiness data → action affordance | A malformed or stale status must never become invocable. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan | Automated negative proof |
|-----------|----------|----------|-----------|-------------|-----------------|--------------------------|
| T-53.1-01 | High | Spoofing | `createProjection` origin lookup | mitigate | Parse URL once, reject credentials/non-HTTPS/unknown origins, and use exact origin-index equality only. | `node tests/skopeo-capability-projection.test.js` rejects suffix-confusion, substring, port, scheme, credential, and unknown-origin cases. |
| T-53.1-05 | High | Elevation of privilege | readiness mapping | mitigate | Closed readiness vocabulary and `invocable === (status === 't1-ready')`; invalid status rejects the projection. | The focused test mutates every non-ready state and a forged `invocable:true` row and requires rejection/false. |
| T-53.1-08 | High | Information disclosure | projection scoping | mitigate | Bind projection to tabId + generation + exactOrigin + catalogVersion + profileVersion and include only current-service rows. | The focused test uses two tabs/origins/versions and proves no cross-key reuse or foreign-service slug leakage. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Define the RED exact-origin and readiness projection oracle</name>
  <files>tests/skopeo-capability-projection.test.js</files>
  <read_first>
    - tests/skopeo-capability-projection.test.js (confirm absent before creating)
    - tests/backing-status-annotation.test.js
    - tests/t1-terminal-states.test.js
    - tests/skopeo-session-lifecycle.test.js
    - extension/catalog/skopeo-profile-index.generated.js
    - extension/utils/skopeo-session-state.js
    - .planning/phases/53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo/53.1-VALIDATION.md
  </read_first>
  <action>
Write a standalone Node contract that imports the generated Plan 01 index and the not-yet-created projector. Cover exact HTTPS origin admission; `docs.google.com.evil.example`, `x.com`/`netflix.com`, port, scheme, credential, and unknown-origin negatives; positive safe tab/generation requirements; exact cache tuple invalidation across tab, generation, origin, catalogVersion, and profileVersion; current-service-only slugs; frozen output; group/label/params-summary bounds; and the exact closed `presentationDisposition` values `t1-ready|guarded-fail-closed|blocked|bridge-needed|uat-needed|learn-pending|discovery-pending|degraded|unsupported`. Assert that only `t1-ready` may carry `executionEnabled:true` and `invocable:true`; every other disposition remains static. Include unknown disposition, source/terminal/disposition mismatch, forged ready/actionable/enablement, and collapsed learn-vs-discovery mutations, and verify no full-catalog array appears in projection output. Run it now and retain RED only for the absent projector module.
  </action>
  <acceptance_criteria>
    - `test -f tests/skopeo-capability-projection.test.js` succeeds.
    - `rg -n "evil\\.example|credential|catalogVersion|profileVersion|presentationDisposition|guarded-fail-closed|blocked|bridge-needed|uat-needed|learn-pending|discovery-pending|degraded|unsupported|executionEnabled|invocable" tests/skopeo-capability-projection.test.js` finds every authority/status class.
    - The controlled RED names `skopeo-capability-projector.js` as missing and contains no assertion or syntax failure.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set +e; out=$(node tests/skopeo-capability-projection.test.js 2&gt;&amp;1); code=$?; set -e; test $code -ne 0; printf "%s\n" "$out" | rg "skopeo-capability-projector"'</automated>
  </verify>
  <done>The negative oracle fixes exact-origin, tuple ownership, bounded projection, and readiness semantics before implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the frozen current-service projector</name>
  <files>extension/utils/skopeo-capability-projector.js</files>
  <read_first>
    - extension/utils/skopeo-capability-projector.js (confirm absent before creating)
    - tests/skopeo-capability-projection.test.js
    - extension/catalog/skopeo-profile-index.generated.js
    - extension/utils/skopeo-session-state.js
    - extension/utils/capability-search.js
    - scripts/report-t1-terminal-states.mjs
    - .planning/phases/53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo/53.1-CONTEXT.md
  </read_first>
  <action>
Implement a dual-export `FsbSkopeoCapabilityProjector` with frozen `STATUS` (`recognized|unsupported|invalid`), frozen `READINESS` containing exactly `t1-ready|guarded-fail-closed|blocked|bridge-needed|uat-needed|learn-pending|discovery-pending|degraded|unsupported`, `createProjection(input, index)`, and `validateProjection(value)`. Input exact keys are `tabId`, `generation`, and `url`; parse with `URL`, reject username/password, non-HTTPS, explicit unexpected ports, fragments as authority, and any origin absent from the generated exact `originIndex`. Return either a bounded failure `{status,reason}` or one recursively frozen recognized projection containing the exact tuple `{tabId,generation,exactOrigin,service,appStem,profileId,profileVersion,catalogVersion}`, normalized UI profile fields, and current-service capability groups only. Cap to the schema/generator bounds (12 groups, 256 capabilities per service, 12 summarized params per capability); fail closed rather than truncate required rows. Preserve Plan 01's generated `presentationDisposition` after validating it against the raw source-readiness/terminal-evidence fields; do not reconstruct it from terminal `surfaceStatus` alone. Reject unknown or mismatched combinations. Compute `executionEnabled` and `invocable` locally as strict equality to `presentationDisposition === 't1-ready'`; never trust stored true bits. Do not cache in this module, read DOM/page copy, search by stem, or expose the complete index. Use bounded reason codes such as `origin-unsupported`, `profile-inconsistent`, and `projection-invalid`, never the raw URL.
  </action>
  <acceptance_criteria>
    - `node --check extension/utils/skopeo-capability-projector.js` exits 0.
    - `node tests/skopeo-capability-projection.test.js` exits 0 with all T-53.1-01/05/08 negatives green.
    - `rg -n "createProjection|validateProjection|FsbSkopeoCapabilityProjector" extension/utils/skopeo-capability-projector.js` finds the public seam.
    - `rg -n "includes\\(|indexOf\\(.*origin|hostname.*includes" extension/utils/skopeo-capability-projector.js` returns no origin-substring matching implementation.
    - `rg -n "presentationDisposition|guarded-fail-closed|blocked|bridge-needed|uat-needed|learn-pending|discovery-pending|degraded|unsupported" extension/utils/skopeo-capability-projector.js` finds the closed readiness contract.
    - `rg -n "presentationDisposition === 't1-ready'" extension/utils/skopeo-capability-projector.js` finds the sole execution/invocability predicate.
  </acceptance_criteria>
  <verify>
    <automated>node --check extension/utils/skopeo-capability-projector.js &amp;&amp; node tests/skopeo-capability-projection.test.js</automated>
  </verify>
  <done>One exact current-tab tuple produces one frozen current-service surface, with no catalog leakage or readiness escalation.</done>
</task>

</tasks>

<verification>
Run `node tests/skopeo-capability-projection.test.js`. Inspect one recognized projection to confirm it contains no foreign service slug and serializes without the full generated index.
</verification>

<success_criteria>
- Exact origin, tab, generation, profile version, and catalog version are independently represented and validated.
- Projection data is bounded, recursively frozen, and current-service-only.
- Every non-`t1-ready` disposition is honest static data and cannot reach an action path.
- Origin spoofing, cross-key reuse, and forged invocability negatives pass automatically.
</success_criteria>

<output>
After completion, create `.planning/phases/53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo/53.1-02-SUMMARY.md`.
</output>
