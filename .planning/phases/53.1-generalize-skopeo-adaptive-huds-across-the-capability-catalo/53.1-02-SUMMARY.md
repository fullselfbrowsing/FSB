---
phase: 53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo
plan: "02"
subsystem: current-service-capability-projection
tags: [chrome-extension, exact-origin, capability-projection, readiness, fail-closed]

requires:
  - phase: 53.1-01-catalog-profile-contract
    provides: Deterministic exact-origin profile/readiness index with separate source and terminal evidence
provides:
  - Pure exact-HTTPS-origin resolver for one generation-owned tab/profile tuple
  - Recursively frozen bounded current-service capability groups without full-catalog disclosure
  - Closed nine-state readiness validation with locally derived t1-ready-only invocability
affects: [53.1-03-context-resolver, 53.1-06-consequence-gate, 53.1-07-runtime-integration]

tech-stack:
  added: []
  patterns: [classic-script/CommonJS dual export, exact-origin equality, bounded frozen projection]

key-files:
  created:
    - extension/utils/skopeo-capability-projector.js
    - tests/skopeo-capability-projection.test.js
  modified: []

key-decisions:
  - "Bind every recognized surface to the explicit tabId/generation/exactOrigin/service/appStem/profileId/profileVersion/catalogVersion tuple and do not cache projections."
  - "Admit only one exact HTTPS origin-index row and fail quiet for the intentionally ambiguous atlassian.net service rather than guessing Jira or Confluence."
  - "Validate generated source-readiness, terminal-state, surface-status, and presentation-disposition combinations, then derive both actionable bits solely from presentationDisposition === 't1-ready'."

patterns-established:
  - "Current-service disclosure: output only normalized profile fields and at most 12 groups, 256 capabilities, and 12 summarized parameters per capability."
  - "Projection validation: reject forged evidence/actionability and unknown states while remaining safe after Chrome structured cloning."

requirements-completed: [ADAPT-01, ADAPT-02, ADAPT-06, ADAPT-10]

duration: 19 min
completed: 2026-07-16
---

# Phase 53.1 Plan 02: Current-Service Capability Projection Summary

**An exact admitted HTTPS origin now produces one generation-owned, recursively frozen profile/capability slice whose only actionable rows are canonically proven `t1-ready` capabilities**

## Performance

- **Duration:** 19 min
- **Started:** 2026-07-16T18:04:02Z
- **Completed:** 2026-07-16T18:22:49Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Added a RED-first adversarial oracle covering suffix confusion, credential/port/scheme/fragment authority, positive safe ownership keys, version boundaries, full-index leakage, projection bounds, all nine readiness values, and forged readiness/actionability evidence.
- Added `FsbSkopeoCapabilityProjector` with exact HTTPS origin-index admission, fail-quiet ambiguous-service handling, normalized profile fields, current-service grouping, recursive freezing, and classic-script/CommonJS exports.
- Proved every unambiguous catalog origin projects successfully (128 recognized), while the sole shared-stem service (`atlassian.net`) remains explicitly unsupported; a Docs projection serializes to 8,299 bytes versus the 2,131,137-byte generated index and contains zero foreign slugs.

## Task Commits

Each task was committed atomically:

1. **Task 1: Define the RED exact-origin and readiness projection oracle** - `31b1f54d` (test)
2. **Task 2: Implement the frozen current-service projector** - `e004da88` (feat)
3. **Post-task hardening: Bind profile keys to the exact stem/service identity** - `53cf4d95` (fix)

## Files Created/Modified

- `tests/skopeo-capability-projection.test.js` - Exact-origin spoofing, ownership tuple, bounds, readiness mismatch, actionability forgery, and catalog-leakage oracle.
- `extension/utils/skopeo-capability-projector.js` - Pure exact-origin resolver, profile/capability normalizer, recursive freezer, and closed projection validator.

## Decisions Made

- Keep ownership fields at the recognized projection's top level so background and content consumers can compare every authority dimension directly without reconstructing a cache key.
- Treat a malformed input/index as bounded `invalid`, an absent exact origin as bounded `unsupported`, and an ambiguous admitted origin as fail-quiet `profile-inconsistent`; failures never echo raw URL data.
- Preserve optional parameter names only up to the 12-name surface bound while retaining every required parameter; if required parameters alone exceed the bound, fail the projection rather than truncate authority-bearing input requirements.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Accepted generated profile keys containing the stem/service separator**
- **Found during:** Task 2 (Implement the frozen current-service projector)
- **Issue:** The first validator reused the generic identifier rule for `profileKey`, rejecting valid generated keys such as `gdocs@docs.google.com`.
- **Fix:** Added a bounded profile-key validator that requires the exact `appStem@service` lexical shape.
- **Files modified:** `extension/utils/skopeo-capability-projector.js`
- **Verification:** Docs recognition passed and the catalog-wide sweep recognized all 128 unambiguous origins.
- **Committed in:** `e004da88`

**2. [Rule 1 - Bug] Corrected the Calendar required-parameter fixture**
- **Found during:** Task 2 (Make the RED oracle green)
- **Issue:** The oracle named `calendar_id` as required for `gcal.create_event`, but the committed schema correctly requires `summary` and treats `calendar_id` as optional.
- **Fix:** Pointed the unchanged required-parameter preservation assertion at the committed `summary` field.
- **Files modified:** `tests/skopeo-capability-projection.test.js`
- **Verification:** The projected 16-name source summary retains `summary` and remains bounded to 12 names.
- **Committed in:** `e004da88`

**3. [Rule 1 - Bug] Classified non-string URLs as invalid input**
- **Found during:** Task 2 (Make the RED oracle green)
- **Issue:** A non-string `url` passed the ownership-key shape check and fell through to the unsupported-origin branch.
- **Fix:** Validate URL type and length with the other exact input fields before URL parsing.
- **Files modified:** `extension/utils/skopeo-capability-projector.js`
- **Verification:** All positive-safe tab/generation and malformed-input negatives pass with bounded `invalid` results.
- **Committed in:** `e004da88`

**4. [Rule 1 - Bug] Bound a valid-looking profile key to its exact stem/service pair**
- **Found during:** Plan self-check
- **Issue:** The bounded `app@service` lexical validator did not independently prove that the key's app segment matched `profile.appStem`; a coherently forged origin/profile/capability key rewrite could therefore survive slice resolution.
- **Fix:** Require `profileKey === appStem + '@' + service` and add a hostile three-layer rewrite oracle.
- **Files modified:** `extension/utils/skopeo-capability-projector.js`, `tests/skopeo-capability-projection.test.js`
- **Verification:** The forged mismatch fails bounded-invalid, focused and adjacent suites remain green, and all 128 unambiguous origins still project.
- **Committed in:** `53cf4d95`

---

**Total deviations:** 4 auto-fixed bugs
**Impact on plan:** All four fixes tightened compatibility, identity consistency, or error classification inside the planned projection contract; no scope or dependency was added.

## Issues Encountered

None beyond the four implementation and self-check corrections documented above.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 53.1-03 can validate and consume exact recognized projections carrying profile identity, genre/adapter/renderer fields, and bounded current-service groups.
- Runtime integration must compare the full ownership tuple after every structured-clone/message boundary and continue to treat only `presentationDisposition:'t1-ready'` rows as action candidates.

## Verification

- `node --check extension/utils/skopeo-capability-projector.js`
- `node tests/skopeo-capability-projection.test.js`
- `node tests/skopeo-profile-schema.test.js`
- `node tests/skopeo-profile-coverage.test.js`
- `node tests/skopeo-session-lifecycle.test.js`
- Source-level origin-substring, public-export, readiness-vocabulary, and sole-actionability-predicate gates all passed.
- Catalog-wide projection sweep: 128 recognized exact origins, 1 explicit ambiguous-service unsupported result, 0 unexpected failures.

## Self-Check: PASSED

- Both planned artifacts and this summary exist.
- Task commits `31b1f54d`, `e004da88`, and `53cf4d95` are present in history.
- Focused, adjacent profile/coverage/lifecycle, source-level, serialization, and all-origin projection gates pass.
- The only unstaged workspace change remains the pre-existing ephemeral `.planning/config.json` workflow flag.

---
*Phase: 53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo*
*Completed: 2026-07-16*
