---
phase: 53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo
plan: "04"
type: execute
wave: 4
depends_on:
  - "53.1-03"
files_modified:
  - extension/content/skopeo-adaptive-composer.js
  - extension/content/skopeo-shell.js
  - tests/skopeo-adaptive-composer.test.js
autonomous: true
requirements:
  - ADAPT-03
  - ADAPT-04
  - ADAPT-06
  - ADAPT-09
  - ADAPT-10
must_haves:
  truths:
    - "D-06: One lifecycle and Shadow shell accept only one sparse legal composition from the six primitives, four attention levels, and closed renderer atoms."
    - "Initial explicit invocation may choose Ambient or Anchored only; Focused and Interstitial require explicit Skopeo controls."
    - "The sole existing Shadow shell renders bounded text-only models with the approved copy, responsive, focus, accessibility, and collision behavior."
  artifacts:
    - path: "extension/content/skopeo-adaptive-composer.js"
      provides: "Pure closed adaptive render-model composer"
      exports: ["MODEL_VERSION", "ATOM", "compose", "validateRenderModel"]
    - path: "extension/content/skopeo-shell.js"
      provides: "One-shell adaptive render entry point preserving existing primitives and lifecycle"
      contains: "renderAdaptive"
    - path: "tests/skopeo-adaptive-composer.test.js"
      provides: "Genre/risk/readiness/sparse-composition/UI contract oracle"
  key_links:
    - from: "extension/content/skopeo-adaptive-composer.js"
      to: "extension/content/skopeo-shell.js"
      via: "validated closed render model"
      pattern: "validateRenderModel"
    - from: "extension/content/skopeo-shell.js"
      to: "53.1-UI-SPEC.md"
      via: "exact copy, atoms, responsive, focus, and accessibility contract"
      pattern: "renderAdaptive"
---

<objective>
Build the pure adaptive composer and extend the one existing shell with a closed UI-SPEC-compliant render model.

Purpose: Implement D-06 without adding a second shell, arbitrary chrome, automatic attention escalation, or misleading non-ready controls.
Output: Composer, shell entry point, and RED-first composition/security/UI oracle.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo/53.1-CONTEXT.md
@.planning/phases/53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo/53.1-RESEARCH.md
@.planning/phases/53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo/53.1-UI-SPEC.md
@.planning/phases/53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo/53.1-VALIDATION.md
@.planning/phases/53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo/53.1-03-SUMMARY.md

<interfaces>
Existing shell constants:
```javascript
PRIMITIVES = ['anchor','chip','halo','rail','ghost','gate'];
ATTENTION = { AMBIENT:'ambient', ANCHORED:'anchored', FOCUSED:'focused', INTERSTITIAL:'interstitial' };
ATTENTION_POLICY = {
  ambient:['rail'], anchored:['anchor','chip','rail','halo'],
  focused:['anchor','chip','ghost'], interstitial:['gate']
};
```

Plan 03 recognized context carries app/profile/genre/lens, optional stable entity,
current capability groups, contextEpoch, and the exact generation/origin/profile tuple.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Context/capability data → composer | Untrusted values may affect selection but never extend enums or UI structure. |
| Render model → Shadow shell | Only validated atoms, primitives, copy tokens, and geometry may create DOM. |
| Explicit controls → attention transition | Page events and asynchronous data may not select Focused/Interstitial. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan | Automated negative proof |
|-----------|----------|----------|-----------|-------------|-----------------|--------------------------|
| T-53.1-03 | High | Tampering | render-model fields | mitigate | Exact-key modelVersion 1, closed atoms/primitives/attention, count/text caps, and recursive freeze. | `node tests/skopeo-adaptive-composer.test.js` rejects unknown atoms, illegal primitive sets, excessive values, functions, HTML, and remote content. |
| T-53.1-04 | High | Spoofing | entity/anchored composition | mitigate | Anchored/chip/anchor require current stable adapter evidence; generic contexts reduce to Ambient. | The focused test supplies path/text-only entities and requires zero anchor/chip/halo output. |
| T-53.1-09 | High | Denial of service | shell geometry/focus/resources | mitigate | Reuse transactional staging, collision rollback, one live region, focus restoration, reduced motion, and the eleven-key ledger. | The focused test plus `node tests/skopeo-shell-contract.test.js` reject unsafe geometry, stale render, extra shell, and nonzero teardown. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create the RED composer, UI, and illegal-chrome oracle</name>
  <files>tests/skopeo-adaptive-composer.test.js</files>
  <read_first>
    - tests/skopeo-adaptive-composer.test.js (confirm absent before creating)
    - tests/skopeo-shell-contract.test.js
    - tests/skopeo-accessibility.test.js
    - tests/skopeo-app-context-resolver.test.js
    - extension/content/skopeo-shell.js
    - extension/content/skopeo-app-context-resolver.js
    - .planning/phases/53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo/53.1-UI-SPEC.md
    - .planning/phases/53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo/53.1-VALIDATION.md
  </read_first>
  <action>
Write a standalone Node/VM oracle before production changes. Cover every approved genre and read/write/destructive risk; smallest legal primitive set; Ambient/Anchored-only initial state; explicit `open-actions` required for Focused; explicit selected ready consequence required for Interstitial; halo only with a validated anomaly; ghost only Focused; generic/no-entity output without anchor/chip/halo; only `t1-ready` capability buttons; all other dispositions as nonfocusable status rows; exact UI-SPEC templates (`Skopeo · {App}`, `Open {App} actions`, fail-quiet, unavailable, target-withdrawn, back, and turn-off); closed atom set; narrow-table fallback; safe-first gate order; focus/live-region/reduced-motion fields; and text-only hostile payloads. Add weakened-oracle mutations for illegal combinations and automatic Focused/Interstitial so the negative proof is non-vacuous. Run now and retain RED only for the absent composer/renderAdaptive seam.
  </action>
  <acceptance_criteria>
    - `test -f tests/skopeo-adaptive-composer.test.js` succeeds.
    - `rg -n "reader-knowledge|communication|document-editor|worklist-record|dashboard-admin|transactional|media-feed|generic-app|drive-docs-deep-pack" tests/skopeo-adaptive-composer.test.js` finds all genres.
    - `rg -n "automatic|interstitial|t1-ready|status-row|textContent|reduced.motion|480|Keep reviewing" tests/skopeo-adaptive-composer.test.js` finds consequence/readiness/UI negatives.
    - Controlled RED names the missing composer or `renderAdaptive`, not malformed test setup.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set +e; out=$(node tests/skopeo-adaptive-composer.test.js 2&gt;&amp;1); code=$?; set -e; test $code -ne 0; printf "%s\n" "$out" | rg "skopeo-adaptive-composer|renderAdaptive"'</automated>
  </verify>
  <done>The complete composition and user-facing UI contract is executable and RED before production implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the pure sparse adaptive composer</name>
  <files>extension/content/skopeo-adaptive-composer.js</files>
  <read_first>
    - extension/content/skopeo-adaptive-composer.js (confirm absent before creating)
    - tests/skopeo-adaptive-composer.test.js
    - extension/content/skopeo-app-context-resolver.js
    - extension/content/skopeo-shell.js
    - extension/utils/skopeo-profile-schema.js
    - .planning/phases/53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo/53.1-CONTEXT.md
    - .planning/phases/53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo/53.1-UI-SPEC.md
  </read_first>
  <action>
Implement dual-export `FSBSkopeoAdaptiveComposer` with `MODEL_VERSION=1`, the exact closed atom enum (`section-heading`, `status-row`, `capability-row`, `fact-list`, `item-list`, `compact-table`, `timeline`, `diff`, `notice`), `compose(input)`, and `validateRenderModel`. Input exact keys separate context, explicit intent, selected group/action, anomaly evidence, result, and consequence. Return a recursively frozen model with exact authority tuple, attention, legal primitive list, lens, optional entity, bounded ready groups, unavailable status summary, renderer request, and consequence slot. Initial/default intent may emit Ambient or Anchored only; `open-actions` may emit Focused; Interstitial is legal only when a selected row is `t1-ready`, write/destructive, and has a complete consequence model. Preserve the shell ATTENTION_POLICY exactly; omit irrelevant primitives rather than placeholders. Use UI-SPEC exact copy templates and status vocabulary. Missing substitution tokens select a complete generic sentence, never raw host/slug/braces. Models never contain DOM nodes, callbacks, HTML/Markdown, styles, remote assets, or profile-defined components.
  </action>
  <acceptance_criteria>
    - `node --check extension/content/skopeo-adaptive-composer.js` exits 0.
    - Composer-only cases in `node tests/skopeo-adaptive-composer.test.js` pass before the shell leg is enabled.
    - `rg -n "MODEL_VERSION|section-heading|compact-table|validateRenderModel|open-actions|t1-ready" extension/content/skopeo-adaptive-composer.js` finds all closed seams.
    - `rg -n "innerHTML|insertAdjacentHTML|eval\\(|new Function|querySelector" extension/content/skopeo-adaptive-composer.js` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>node --check extension/content/skopeo-adaptive-composer.js &amp;&amp; node tests/skopeo-adaptive-composer.test.js --composer-only</automated>
  </verify>
  <done>The pure composer deterministically chooses only sparse, legal, readiness-honest, explicitly authorized models.</done>
</task>

<task type="auto">
  <name>Task 3: Add the closed adaptive entry point to the sole Shadow shell</name>
  <files>extension/content/skopeo-shell.js</files>
  <read_first>
    - extension/content/skopeo-shell.js
    - extension/content/skopeo-adaptive-composer.js
    - tests/skopeo-adaptive-composer.test.js
    - tests/skopeo-shell-contract.test.js
    - tests/skopeo-accessibility.test.js
    - .planning/phases/53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo/53.1-UI-SPEC.md
  </read_first>
  <action>
Extend the existing `SkopeoShell` only; do not create another host/root/runtime. Add `renderAdaptive(model, renderedAtoms)` after validating modelVersion, exact generation/authority, attention policy, atom types, and bounded text. Preserve the six primitive constants and all Phase 52 methods. Build shared lens, entity chip, focused card, status/capability rows, result region, and consequence gate using `createElement`/`textContent` only. Implement every UI-SPEC rule: exact copy/roles/names; one orange ready primary; non-ready static rows; read groups before separated write/destructive groups; 320px card/360px gate; below-480 stacking and compact-table fact-list form; safe action first and initial focus for `alertdialog`; Ambient/Anchored no focus theft; Focused title focus; withdrawal announcement; one live region; reduced-motion/forced-colors; 16px inset/8px clearance; collision rollback; no host mutation; exact prior DOM/focus restoration; and owned scope release into the existing eleven-key ledger. Recheck generation/context/profile/entity immediately before commit. Retain controlled fixture behavior and existing Drive projection methods unchanged.
  </action>
  <acceptance_criteria>
    - `node --check extension/content/skopeo-shell.js` exits 0.
    - `node tests/skopeo-adaptive-composer.test.js && node tests/skopeo-shell-contract.test.js && node tests/skopeo-accessibility.test.js` exits 0.
    - `rg -n "renderAdaptive|role.*alertdialog|aria-modal|Keep reviewing|Unavailable actions|compact-table" extension/content/skopeo-shell.js` finds UI contract implementation.
    - `rg -n "innerHTML|insertAdjacentHTML" extension/content/skopeo-shell.js` returns no newly introduced rendering sink.
    - Existing `PRIMITIVES` and `ATTENTION_POLICY` arrays remain byte-equivalent in tests and teardown returns all eleven resource keys to numeric zero.
  </acceptance_criteria>
  <verify>
    <automated>node --check extension/content/skopeo-shell.js &amp;&amp; node tests/skopeo-adaptive-composer.test.js &amp;&amp; node tests/skopeo-shell-contract.test.js &amp;&amp; node tests/skopeo-accessibility.test.js</automated>
  </verify>
  <done>The approved adaptive UI renders through one shell with exact copy, responsiveness, focus, accessibility, collision, and resource behavior.</done>
</task>

</tasks>

<verification>
Run `node tests/skopeo-adaptive-composer.test.js && node tests/skopeo-shell-contract.test.js && node tests/skopeo-accessibility.test.js` and confirm all High-threat mutations fail when the corresponding guard is sabotaged.
</verification>

<success_criteria>
- One shell renders only six primitives, four attention levels, and the approved closed atoms.
- Focused/Interstitial never arise without an explicit Skopeo action; only t1-ready rows are controls.
- UI composition, copy, narrow behavior, focus, accessibility, reduced motion, collision safety, and consequence anatomy match 53.1-UI-SPEC.md.
- Host content remains text-only data and exact-zero teardown remains intact.
</success_criteria>

<output>
After completion, create `.planning/phases/53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo/53.1-04-SUMMARY.md`.
</output>
