---
phase: 64-opencode-adapter
plan: "02"
type: execute
wave: 2
depends_on:
  - "64-01"
files_modified:
  - mcp/src/agent-providers/adapter.ts
  - mcp/src/agent-providers/policy-attestation.ts
  - mcp/src/agent-providers/spawn-supervisor.ts
  - tests/mcp-agent-provider-contract.test.js
  - tests/mcp-spawn-supervisor.test.js
autonomous: true
requirements:
  - MULTI-01
  - MULTI-03
must_haves:
  truths:
    - "[D64-03] AgentProviderAdapter still exposes exactly detect, buildSpawn, parseEvents, kill, and caps; topology, attestation, and secret bindings are immutable provider-neutral data, never a sixth method, callback, or adapter-id branch."
    - "[D64-04] The shared contract describes one daemon-owned provider server plus fresh cold/attach task children while task text remains unrepresentable outside task stdin."
    - "Serialized fixed environment is distinct from spawn-time secret environment bindings: fixedEnv can never contain OPENCODE_SERVER_PASSWORD or a secret value."
    - "The only secret binding is the exact closed pair OPENCODE_SERVER_PASSWORD plus supervisor-owned ref owned_server_basic_password, and it is representable only on the owned server and selected attach task child."
    - "Cold task children, direct Claude tasks, and process-json policy probes carry no password binding; authenticated owned-server JSON probes carry only the same opaque supervisor secret ref for header construction."
    - "[D64-11] Shared drift detail remains provider-neutral, closed, and safe after the supervisor imports Plan 01's protocol-drift module."
  artifacts:
    - path: "mcp/src/agent-providers/adapter.ts"
      provides: "Closed deeply frozen topology, attestation, fixed-env, and transient secret-binding descriptors"
    - path: "mcp/src/agent-providers/policy-attestation.ts"
      provides: "Shared provider-neutral bounded JSON-attestation verifier"
    - path: "tests/mcp-agent-provider-contract.test.js"
      provides: "Exact five-method, deep-freeze, secret non-representability, and binding-scope contract"
  key_links:
    - from: "mcp/src/agent-providers/spawn-supervisor.ts"
      to: "mcp/src/agent-providers/adapter.ts"
      via: "generic topology, process role, fixed env, secret binding, preflight source, and attestation descriptor only"
      pattern: "SpawnSecretEnvBinding|owned_server_basic_password"
    - from: "mcp/src/agent-providers/spawn-supervisor.ts"
      to: "mcp/src/agent-providers/policy-attestation.ts"
      via: "shared descriptor interpretation without provider callback/import"
      pattern: "verifyPolicyAttestation"
    - from: "mcp/src/agent-providers/spawn-supervisor.ts"
      to: "mcp/src/agent-providers/protocol-drift.ts"
      via: "shared bounded drift mapping from Plan 01"
      pattern: "AgentProtocolDriftError"
---

<objective>
Freeze the exact five-method provider-neutral topology, attestation, and transient secret-binding contract behind the already-green first-commit drift gate.

Purpose: Let later profile/supervisor work declare both server and attach authentication without serializing the Basic password or adding adapter-specific process authority.
Output: Deeply immutable topology/attestation types, a closed spawn-time secret-env binding grammar, a provider-neutral verifier, and shared safe supervisor drift mapping.

<scope_rationale>This plan is the sole declarative contract foundation. The role-aware journal remains isolated in Plan 03, and actual secret minting/resolution remains supervisor-owned in Plan 07.</scope_rationale>
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/64-opencode-adapter/64-CONTEXT.md
@.planning/phases/64-opencode-adapter/64-RESEARCH.md
@.planning/phases/64-opencode-adapter/64-PATTERNS.md
@.planning/phases/64-opencode-adapter/64-VALIDATION.md
@.planning/phases/64-opencode-adapter/64-01-SUMMARY.md
@.planning/phases/60-adapter-contract-claude-code-mvp/60-01-SUMMARY.md
@.planning/phases/60-adapter-contract-claude-code-mvp/60-02-SUMMARY.md
@mcp/src/agent-providers/adapter.ts
@mcp/src/agent-providers/spawn-supervisor.ts
@tests/mcp-agent-provider-contract.test.js

<decision_trace>
- D64-03: preserve the exact five methods and encode topology/auth as closed data.
- D64-04: model one bounded owned server and fresh per-task children without chat/session reuse.
- D64-06: task text remains stdin-only and unrepresentable in declarative state.
- D64-11: share strict bounded drift detail while native grammars remain adapter-owned.
</decision_trace>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>64-02-01: Freeze topology, attestation, and transient secret-env bindings</name>
  <files>mcp/src/agent-providers/adapter.ts, mcp/src/agent-providers/policy-attestation.ts, mcp/src/agent-providers/spawn-supervisor.ts, tests/mcp-agent-provider-contract.test.js, tests/mcp-spawn-supervisor.test.js</files>
  <read_first>Plan 01 shared provider-id/drift outputs; 64-CONTEXT Execution Topology and Hermetic Policy decisions; 64-RESEARCH Architectural Responsibility Map and Patterns 1, 2, and 4; adapter.ts complete five-method/SpawnSpec/freeze contract; claude-profile direct SpawnSpec; current supervisor drift imports</read_first>
  <behavior>
    - "The adapter interface has exactly five own method signatures: detect, buildSpawn, parseEvents, kill, and caps."
    - "A direct topology contains one task ProcessSpec; an owned-server topology contains one server, one cold task, one attach task, bounded readiness/idle policy, and closed supervisor runtime references."
    - "ProcessSpec has a serializable fixedEnv map and a separate exact frozen spawnSecretEnvBindings array; fixedEnv rejects OPENCODE_SERVER_PASSWORD and every secret-bearing value."
    - "SpawnSecretEnvBinding is a closed exact descriptor with envKey OPENCODE_SERVER_PASSWORD and secretRef owned_server_basic_password; no arbitrary env key, ref, callback, resolver, or secret value is accepted."
    - "That binding is legal only on an owned_server ProcessSpec and the attach task ProcessSpec inside owned_server topology; it is rejected on direct, cold, and policy_preflight processes."
    - "Attestations are ordered process_json or owned_server_json descriptors; server descriptors reference owned_server_basic_password for authenticated header construction but contain no password/header value."
    - "The shared verifier supports only exact_keys, non-string exact_scalar, absent, string/document sha256, nonempty_string, and all_strings_prefix-by-closed-product-ref assertions over own-data JSON."
    - "Task bytes and password values are absent from argv, fixedEnv, private artifacts, topology, bindings, runtime refs, attestations, verdicts, fingerprints, and every serialization; all nested caller data is defensively reconstructed and recursively frozen."
    - "Claude projects to direct topology with empty bindings/attestations and exact legacy argv/env behavior."
  </behavior>
  <action>Write the contract and supervisor-source assertions first. Cover every nested topology/probe/assertion/binding branch, hostile mutation, exact role placement, and source gates against a sixth method, callback/function, adapter-id discriminator, provider import, free-form template, secret resolver, or task/password value. Generalize the flat process shape into closed direct/owned-server topology and define `SpawnSecretEnvBinding` separately from `fixedEnv`: its only legal data is `{envKey:'OPENCODE_SERVER_PASSWORD', secretRef:'owned_server_basic_password'}` and freeze-time validation permits it only on the owned server and attach task specs. Cold/direct/preflight specs must have an empty binding array. Define ordered `AttestationDescriptor` entries and the exact assertion union; owned-server GET descriptors use only a bounded safe path and the same opaque secret ref. Implement `policy-attestation.ts` as the sole generic exact-own-data interpreter returning a frozen pass/closed-reason verdict with no adapter id or callback. Preserve Claude through a direct compatibility projection. Change the supervisor's drift import to Plan 01's provider-neutral module, but do not implement secret minting/resolution yet: this plan carries descriptors only, and all value materialization belongs to Plan 07's supervisor task.</action>
  <acceptance_criteria>Tests prove exactly five methods, closed roles/topologies/attestations/assertions/bindings, fixed-env versus transient-secret separation, exact server+attach placement, cold/preflight/direct absence, deep immutability, zero task/password serialization, unchanged Claude semantics, and no callback/provider-specific supervisor seam.</acceptance_criteria>
  <verify>
    <automated>node scripts/run-mcp-build-preserving-workspace.mjs --commands-json '[["node","tests/mcp-agent-provider-contract.test.js"],["node","tests/mcp-spawn-supervisor.test.js"]]'</automated>
  </verify>
  <done>The acceptance criteria above are satisfied and the automated verification passes.</done>
</task>

</tasks>

<threat_model>
  <block_on>HIGH,CRITICAL</block_on>
  <assets>
    <asset id="A64-01">The five-method adapter authority boundary and user task confidentiality.</asset>
    <asset id="A64-03">Closed transient Basic-secret binding metadata and bounded drift diagnostics.</asset>
  </assets>
  <trust_boundaries>
    <boundary id="B64-01">Untrusted adapter-returned nested data enters the serve-owned supervisor.</boundary>
    <boundary id="B64-03">A declarative secret reference later crosses into supervisor-owned spawn-time resolution.</boundary>
  </trust_boundaries>
  <threat id="T64-01" category="Elevation of Privilege" component="adapter contract" severity="HIGH" disposition="mitigate" asvs="V4">
    <mitigation>Keep exactly five methods and closed recursively frozen topology, role, attestation, and secret-binding descriptors interpreted only by the generic supervisor.</mitigation>
    <verification>Exact method/type/source gate, hostile mutation matrix, verifier negatives, and no callback/adapter-id/provider-import scan.</verification>
    <residual_risk>Owned-server lifecycle and attestation execution are implemented in Plans 07-08.</residual_risk>
  </threat>
  <threat id="T64-02" category="Information Disclosure" component="spawn secret descriptor" severity="CRITICAL" disposition="mitigate" asvs="V2,V7">
    <mitigation>Separate fixed serializable env from one closed opaque spawn-time binding; forbid the raw password in every contract, serialization, fingerprint, journal, receipt, diagnostic, log, and output.</mitigation>
    <verification>Exact-role binding positives/negatives, fixedEnv refusal, deep mutation, serialization and sentinel scans.</verification>
    <residual_risk>The password value necessarily exists transiently in the owned-server and selected attach-child process environments when Plan 07 spawns them.</residual_risk>
  </threat>
  <threat id="T64-09" category="Information Disclosure" component="drift diagnostics" severity="HIGH" disposition="mitigate" asvs="V5,V7">
    <mitigation>Use Plan 01's closed reasons and bounded indices/issue paths; discard raw exception/provider content.</mitigation>
    <verification>Accessor/prototype/sentinel/raw-message negatives through the shared supervisor mapping.</verification>
    <residual_risk>Future native schemas require a reviewed profile update.</residual_risk>
  </threat>
</threat_model>

<verification>
- The first-commit parser/fixture/drift gate remains green before and after this contract change.
- Claude direct topology and fixture normalization remain exact.
- No task/password value, sixth method, callback, free-form secret resolver, or adapter-specific supervisor import exists.
</verification>

<success_criteria>
- Later plans can declare OpenCode server and attach authentication without serializing a secret.
- The supervisor remains the sole resolver and adapters remain declarative.
- The dirty workspace and protected generated graph are restored after compiled checks.
</success_criteria>

<output>
Create `.planning/phases/64-opencode-adapter/64-02-SUMMARY.md` when complete.
</output>
