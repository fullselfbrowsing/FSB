---
phase: 64-opencode-adapter
plan: "13"
type: execute
wave: 10
depends_on:
  - "64-08"
  - "64-12"
files_modified:
  - .planning/phases/64-opencode-adapter/64-HUMAN-UAT.md
  - .planning/phases/64-opencode-adapter/64-VALIDATION.md
  - scripts/run-phase64-full-tests.mjs
  - package.json
  - .github/workflows/ci.yml
  - tests/delegation-phase-contract.test.js
  - tests/agent-provider-forbidden-flags.test.js
  - tests/phase64-full-tests-harness.test.js
autonomous: true
requirements:
  - MULTI-01
  - MULTI-02
  - MULTI-03
must_haves:
  truths:
    - "[D64-09] Genuine authenticated stream provenance is represented only by an unchecked human_needed/pending/evidence-empty milestone-end row; schema-derived fixtures never promote it."
    - "[D64-13/D64-14] The exact approved existing UI/copy/auth/billing/no-layout contract is source- and DOM-gated while live browser/accessibility evidence remains pending."
    - "[D64-16] Every one of the 28 final task ids maps exactly once to a deterministic validation command, requirement, wave, and threat; focused/root/CI/full-suite gates execute every new suite exactly once."
    - "The phase contract proves 64-01-01 is the sole first implementation task, already covers the OpenCode parser, fixture, native negatives, and existing Phase 62 CI drift job, and every later plan depends transitively on it."
    - "The phase contract proves Plan 05 has one atomic production-exposure task containing adapter registration, matrix, parser/fixture bijection, and drift smoke; no registration-only task is planned."
    - "The Phase 64 focused runner performs one workspace-preserving MCP build lifecycle, serial offline tests, bounded output, and exact before/after dirty/index/generated-graph identity."
    - "All D64-01..16, T64-01..10, MULTI-01..03, exact five-method/closed-attestation/shared-verifier/no-user-process/no-replay/private-policy/terminal/UI locks are mechanically audited before the guarded Phase 60 repository baseline."
  artifacts:
    - path: ".planning/phases/64-opencode-adapter/64-HUMAN-UAT.md"
      provides: "Single honest three-scenario milestone-end OpenCode UAT ledger"
    - path: "scripts/run-phase64-full-tests.mjs"
      provides: "Workspace-preserving serial Phase 64 focused matrix"
    - path: "tests/delegation-phase-contract.test.js"
      provides: "Requirement/decision/threat/architecture/UI/UAT/task-map source contract"
    - path: ".planning/phases/64-opencode-adapter/64-VALIDATION.md"
      provides: "Approved exact 13-plan/28-task Nyquist map"
  key_links:
    - from: "scripts/run-phase64-full-tests.mjs"
      to: "scripts/run-mcp-build-preserving-workspace.mjs"
      via: "one closed commands-json matrix over fresh compiled output"
      pattern: "run-mcp-build-preserving-workspace"
    - from: "package.json"
      to: ".github/workflows/ci.yml"
      via: "existing root test step runs each new Phase 64 suite exactly once"
      pattern: "mcp-opencode"
    - from: "tests/delegation-phase-contract.test.js"
      to: ".planning/phases/64-opencode-adapter/64-HUMAN-UAT.md"
      via: "unchecked human_needed/pending/evidence-empty enforcement"
      pattern: "64-HUMAN-UAT"
---

<objective>
Close Phase 64 with an exact Nyquist map, workspace-safe focused/root/CI gates, source/security contract, guarded repository baseline, and honest pending live evidence.

Purpose: Automated fixture/fake/DOM proof must be comprehensive without impersonating a genuine OpenCode account, process, browser, or accessibility run.
Output: One pending UAT ledger, focused runner, root/CI wiring, mechanical phase contract, approved validation map, and guarded Phase 60 full-suite pass.

<scope_rationale>UAT honesty, occurrence-count wiring, validation reconciliation, and the final baseline are one closure wave. Keeping them together prevents a green implementation from omitting a task/threat or fabricating residual live evidence.</scope_rationale>
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/64-opencode-adapter/64-CONTEXT.md
@.planning/phases/64-opencode-adapter/64-RESEARCH.md
@.planning/phases/64-opencode-adapter/64-PATTERNS.md
@.planning/phases/64-opencode-adapter/64-UI-SPEC.md
@.planning/phases/64-opencode-adapter/64-VALIDATION.md
@.planning/phases/63-native-messaging-host/63-10-SUMMARY.md
@.planning/phases/63-native-messaging-host/63-12-SUMMARY.md
@scripts/run-mcp-build-preserving-workspace.mjs
@scripts/run-phase60-full-tests.mjs
@package.json
@.github/workflows/ci.yml
@tests/delegation-phase-contract.test.js

<decision_trace>
- D64-01..D64-16: this plan installs an exact mechanical coverage audit for every locked context decision.
- D64-09: real capture provenance stays pending and cannot be synthesized.
- D64-13/D64-14: approved no-new-layout UI and honest auth/billing are blocking source/DOM contracts.
- D64-16: all focused, security, root, CI, full-suite, and human-evidence boundaries are closed.
</decision_trace>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>64-13-01: Create the single three-row pending human-UAT ledger without executing it</name>
  <files>.planning/phases/64-opencode-adapter/64-HUMAN-UAT.md, tests/delegation-phase-contract.test.js</files>
  <read_first>64-VALIDATION Manual-Only Verifications; 64-UI-SPEC Deferred Human Evidence and deterministic acceptance; Phase 63 HUMAN-UAT/phase-contract honesty pattern; 64-RESEARCH.md Human Evidence</read_first>
  <behavior>
    - "Exactly three scenarios exist: genuine authenticated OpenCode-to-browser run; live installed 1.14.25 Providers plus keyboard/screen-reader behavior; live cold versus FSB-owned attach feed/summary equivalence."
    - "Every scenario is unchecked and has exact status human_needed, result pending, and empty evidence."
    - "Prerequisites/steps/expected results require sanitization and prohibit task, credential, model metadata, raw event, path, port, endpoint, or secret retention."
    - "No fixture, fake process/HTTP, source inspection, DOM harness, screenshot, or automated result can check/promote a row."
  </behavior>
  <action>Create the phase-contract negative fixtures first, then add one `64-HUMAN-UAT.md` ledger containing exactly the three UI-SPEC/manual validation rows. Include sanitized prerequisites, user-directed milestone-end steps, expected cold/attach/kill-switch/provider/feed/summary/focus/announcement outcomes, and explicit external side-effect caution, but run none. Each row/section remains unchecked with literal `status: human_needed`, `result: pending`, and empty `evidence:`. Extend the phase contract to reject checked boxes, nonpending results, nonempty evidence, missing/duplicate/extra live scenarios, or language that treats schema fixtures/fakes/source/DOM as live provenance.</action>
  <acceptance_criteria>The single ledger contains exactly three complete but unexecuted scenarios, all human_needed/pending/evidence-empty and sanitized; the source contract fails on every attempted synthetic promotion or missing row.</acceptance_criteria>
  <verify>
    <automated>node tests/delegation-phase-contract.test.js --section phase64-uat-ledger</automated>
  </verify>
  <done>The acceptance criteria above are satisfied and the automated verification passes.</done>
</task>

<task type="auto" tdd="true">
  <name>64-13-02: Wire one workspace-safe focused matrix and exact root/CI occurrence contract</name>
  <files>scripts/run-phase64-full-tests.mjs, tests/phase64-full-tests-harness.test.js, package.json, .github/workflows/ci.yml, tests/delegation-phase-contract.test.js, tests/agent-provider-forbidden-flags.test.js</files>
  <read_first>64-VALIDATION focused command/task matrix; 64-PATTERNS.md guarded build section; run-mcp-build-preserving-workspace.mjs complete CLI/restoration contract; run-phase60-full-tests.mjs serial runner; package.json test order; CI root and drift-smoke steps</read_first>
  <behavior>
    - "Focused runner invokes the existing preserving build wrapper exactly once with a closed serial commands-json list covering every Phase 64 MCP and extension/source/UI suite, including the literal first-commit parser/fixture/drift gate."
    - "Success, test failure, spawn error, SIGINT, and SIGTERM preserve complete pre-existing mcp/build graph, bytes/modes/symlinks, raw Git index bytes/entries, dirty/staged bytes, status, and untracked listing."
    - "Root test adds the two new OpenCode suites and the Phase 64 runner harness exactly once and retains every extended existing suite exactly once; existing relative order/dependencies outside the insertion points remain unchanged."
    - "The existing CI root test step and single drift-smoke step cover Phase 64 without duplicate Linux invocations; all-green still depends on them."
    - "No bare MCP build appears in a Phase 64 plan verification or focused runner, and no generated mcp/build file is hand-edited/staged."
  </behavior>
  <action>Write the runner harness/occurrence-count tests first. Implement `run-phase64-full-tests.mjs` as a bounded shell:false coordinator that calls `run-mcp-build-preserving-workspace.mjs` once with one explicit argv matrix containing the first-commit parser/fixture/native-drift gate, adapter, topology, attestation verifier, contract, compatibility, supervisor/recovery/reverse-channel/bridge, inventory/doctor, storage/consent/routing/controller/diagnostic, Providers/sidepanel/feed, forbidden-source, and phase-contract tests. Let the existing wrapper own build/restoration. The harness injects success/nonzero/spawn-error/SIGINT/SIGTERM and asserts complete dirty/index/generated identity. Add new tests to root `npm test` exactly once and retain the single drift-smoke CI invocation. Mechanically pin that 64-01-01 is the sole Wave-1/first task, every later plan depends transitively on it, and its command covers parser/fixture/native negatives before production exposure; pin Plan 05 as one registration+matrix+parser/fixture/drift task. Update forbidden/source contracts for no sixth method, callback, adapter-id supervisor branch, OpenCode supervisor import/checker, arbitrary assertion value, user-process discovery/kill, config inheritance, replay, task leak, or UI authority. Add exact secret controls: fixedEnv rejects OPENCODE_SERVER_PASSWORD; only server/selected-attach bindings name it; injected spawn fakes prove transient propagation and post-spawn scrubbing; password/header values are absent from argv/topology/journal/fingerprint/receipt/error/log/snapshot/adapter/browser/DOM; no `--password`. Permit only the two reviewed local helper script tags.</action>
  <acceptance_criteria>The focused runner and injected failure/signal harness pass with exact workspace/index/build identity; every new/extended Phase 64 suite is represented exactly once in its intended root/CI path; drift smoke remains one CI gate; no direct build/generated edit or forbidden authority pattern survives.</acceptance_criteria>
  <verify>
    <automated>node tests/phase64-full-tests-harness.test.js && node scripts/run-phase64-full-tests.mjs</automated>
  </verify>
  <done>The acceptance criteria above are satisfied and the automated verification passes.</done>
</task>

<task type="auto" tdd="true">
  <name>64-13-03: Reconcile all 28 task rows and run the guarded security/full-repository gate</name>
  <files>.planning/phases/64-opencode-adapter/64-VALIDATION.md, tests/delegation-phase-contract.test.js, tests/agent-provider-forbidden-flags.test.js</files>
  <read_first>All 64-01..13 PLAN files and completed 64-01..12 SUMMARY files; final 64-VALIDATION per-task map/threats/sign-off; CONTEXT D64-01..16; RESEARCH T64-01..10/ASVS mapping; UI-SPEC acceptance; Phase 60 full runner and workspace wrapper</read_first>
  <behavior>
    - "Validation contains exactly one row for all 28 task ids declared across Plans 64-01 through 64-13, with matching plan/wave/requirements/threats/command and no TBD/duplicate/missing id."
    - "Wave-0 harness rows point to the test-first task that creates each adapter/topology/fixture/extension/runner asset before its dependent production action."
    - "The phase contract maps MULTI-01..03, D64-01..16, T64-01..10, approved UI/security locks, key architecture links, all plan dependencies, and exactly three pending UAT rows."
    - "Focused/security/phase-contract gates pass before the exact guarded Phase 60 full repository baseline."
    - "No HIGH/CRITICAL threat is accepted or deferred; only genuine external auth/process/browser/accessibility evidence remains human_needed."
  </behavior>
  <action>Extend the phase-contract test first to parse every Plan/Validation row and fail on mismatch, duplicate, missing id, unwrapped MCP build, absent threat/decision/requirement, invalid dependency wave, non-unique first task, or split production exposure. Reconcile `64-VALIDATION.md` with the actual 13 plans/28 tasks, precise commands, Wave-0 owners, threat coverage, sign-off, and honest implementation status; never mark human evidence green. Mechanically pin the exact first-commit parser/fixture/native-negative/CI gate, five methods, generic topology, separate fixedEnv/transient binding, server+selected-attach propagation and post-spawn non-retention, closed attestations/shared verifier, atomic registry/matrix/parser/fixture/drift exposure, exact rosters, private 1.14.25 policy, candidate barrier, owner/health/replay fences, canonical browser table, unknown billing, no UI structure, runner/root/CI membership, and UAT ledger. Run phase/source/focused gates, then the exact guarded Phase 60 baseline. Any focused, source/security, secret-retention, atomic-order, workspace, or full-suite failure blocks completion.</action>
  <acceptance_criteria>Validation has exactly 28 correctly mapped task rows across 13 plans and complete Wave-0/threat/sign-off sections; the first-commit and atomic-exposure constraints plus all requirements/decisions/threats/architecture/UI/UAT locks are mechanically owned; focused/source/full-suite gates pass; workspace identity is preserved; all live rows remain pending.</acceptance_criteria>
  <verify>
    <automated>node tests/delegation-phase-contract.test.js --section phase64-validation && node tests/agent-provider-forbidden-flags.test.js && node scripts/run-mcp-build-preserving-workspace.mjs --commands-json '[["node","scripts/run-phase60-full-tests.mjs"]]'</automated>
  </verify>
  <done>The acceptance criteria above are satisfied and the automated verification passes.</done>
</task>

</tasks>

<threat_model>
  <block_on>HIGH,CRITICAL</block_on>
  <assets>
    <asset id="A64-28">Completeness and integrity of Phase 64 automated/security evidence.</asset>
    <asset id="A64-29">User-owned dirty/staged/untracked work and protected generated MCP graph.</asset>
    <asset id="A64-30">Truthfulness of residual authenticated/process/browser/accessibility evidence.</asset>
  </assets>
  <trust_boundaries>
    <boundary id="B64-28">Plan/task metadata becomes an approved Nyquist/security completion claim.</boundary>
    <boundary id="B64-29">Build/test commands mutate generated output inside a dirty shared workspace.</boundary>
    <boundary id="B64-30">Synthetic automated evidence is distinguished from genuine external UAT.</boundary>
  </trust_boundaries>
  <threat id="T64-01" category="Elevation of Privilege" component="architecture gate" severity="HIGH" disposition="mitigate" asvs="V4"><mitigation>Source-pin exact five methods, generic topology plus closed attestation descriptors/shared verifier, no callbacks/provider imports/id branches, and a closed two-provider registry.</mitigation><verification>Phase contract and provider-contract occurrence/mutation/source-negative tests.</verification><residual_risk>None accepted; drift blocks.</residual_risk></threat>
  <threat id="T64-02" category="Information Disclosure" component="secret/task gates" severity="CRITICAL" disposition="mitigate" asvs="V2,V7"><mitigation>Require separate fixedEnv/opaque bindings, exact server+selected-attach transient spawn propagation, post-spawn env scrubbing, transient internal Basic headers, and sentinel absence across every retained/serialized/browser surface.</mitigation><verification>Injected spawn/HTTP fakes, post-call object inspection, cold/preflight negatives, source scans, and serialization/log/error/DOM sentinel suites.</verification><residual_risk>The password exists in owned server and selected attach-child process environments while they run; live OS corroboration remains UAT.</residual_risk></threat>
  <threat id="T64-03" category="Elevation of Privilege" component="process ownership gate" severity="CRITICAL" disposition="mitigate" asvs="V4"><mitigation>Block user-process discovery/attach/kill and require retained owned identity.</mitigation><verification>Topology/recovery zero-kill matrix and forbidden-source scan.</verification><residual_risk>Live OS identity remains UAT.</residual_risk></threat>
  <threat id="T64-04" category="Tampering" component="replay gate" severity="CRITICAL" disposition="mitigate" asvs="V3"><mitigation>Require pre-spawn-only fallback and exact once counts through daemon/browser/UI.</mitigation><verification>Topology, routing, sidepanel, and phase-contract tests.</verification><residual_risk>Explicit user retry remains new intent.</residual_risk></threat>
  <threat id="T64-05" category="Elevation of Privilege" component="isolation gate" severity="CRITICAL" disposition="mitigate" asvs="V4,V14"><mitigation>Source-pin exact private 1.14.25 isolation plus effective attestations.</mitigation><verification>Profile poison matrix and forbidden-flags gate.</verification><residual_risk>Future profile requires re-review.</residual_risk></threat>
  <threat id="T64-06" category="Elevation of Privilege" component="agent/tool gate" severity="CRITICAL" disposition="mitigate" asvs="V4"><mitigation>Require prompt digest, primary mode, ordered denies/final fsb_* allow, and fallback-warning rejection.</mitigation><verification>Profile/attestation/topology source and runtime tests.</verification><residual_risk>Live account/model remains UAT.</residual_risk></threat>
  <threat id="T64-07" category="Spoofing" component="owned server gate" severity="CRITICAL" disposition="mitigate" asvs="V13"><mitigation>Require Basic-auth exact health/version plus identity/generation/config binding.</mitigation><verification>Owned-server readiness/health/race/recovery matrix.</verification><residual_risk>Live loopback scheduling remains UAT.</residual_risk></threat>
  <threat id="T64-08" category="Tampering" component="terminal truth gate" severity="CRITICAL" disposition="mitigate" asvs="V5"><mitigation>Require candidate privacy through clean exit/tree/runtime settlement and completed terminal before UI success.</mitigation><verification>Parser/supervisor/store/controller/feed negative matrix.</verification><residual_risk>Live timing remains UAT.</residual_risk></threat>
  <threat id="T64-09" category="Denial of Service" component="drift gate" severity="HIGH" disposition="mitigate" asvs="V5,V7"><mitigation>Require strict bounded native schemas, adapter mutators, safe diagnostic throttling, and CI fixture bijection.</mitigation><verification>Stream fixture, drift smoke, diagnostics, root/CI occurrence tests.</verification><residual_risk>New profile remains blocked until reviewed.</residual_risk></threat>
  <threat id="T64-10" category="Information Disclosure" component="browser/UI gate" severity="CRITICAL" disposition="mitigate" asvs="V7"><mitigation>Require canonical bounded view models, unknown billing, text-only existing UI, and no native/topology/version/secret data.</mitigation><verification>Storage/routing/controller/Providers/sidepanel/feed/source sentinel tests.</verification><residual_risk>Live visual/accessibility judgment remains UAT.</residual_risk></threat>
</threat_model>

<verification>
- `node scripts/run-phase64-full-tests.mjs` passes and restores the entire pre-existing workspace/index/generated graph.
- The phase/source contract proves exact 13-plan/28-task/16-decision/10-threat/3-requirement/3-UAT coverage plus the literal first-implementation-commit and atomic production-exposure constraints.
- The exact guarded Phase 60 full repository suite passes after focused/security gates.
</verification>

<success_criteria>
- Phase 64 has complete deterministic, source-security, root, CI, and full-suite evidence.
- No automated artifact is mislabeled as genuine authenticated/browser/accessibility evidence.
- The user-owned dirty workspace remains byte- and index-identical after all compiled checks.
</success_criteria>

<output>
Create `.planning/phases/64-opencode-adapter/64-13-SUMMARY.md` when complete.
</output>
