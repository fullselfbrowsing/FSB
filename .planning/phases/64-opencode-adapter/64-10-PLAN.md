---
phase: 64-opencode-adapter
plan: "10"
type: execute
wave: 8
depends_on:
  - "64-09"
files_modified:
  - extension/utils/delegation-event-store.js
  - extension/utils/delegation-controller.js
  - extension/utils/agent-protocol-drift-diagnostics.js
  - extension/background.js
  - tests/fixtures/delegation-events.js
  - tests/delegation-event-store.test.js
  - tests/delegation-controller.test.js
  - tests/agent-protocol-drift-diagnostics.test.js
  - tests/mcp-bridge-background-dispatch.test.js
autonomous: true
requirements:
  - MULTI-01
  - MULTI-03
must_haves:
  truths:
    - "[D64-10] Canonical OpenCode normalized events use the same durable append-before-fanout lifecycle records as Claude; provider-native JSON never enters extension persistence."
    - "[D64-11] Drift diagnostics accept only exact adapter-specific closed reasons/indices/paths and are throttled independently for Claude and OpenCode."
    - "[D64-12] A persisted result remains nonterminal until the explicit authoritative terminal; failure can never hydrate as a prior success."
    - "[D64-13] Event store/controller snapshots accept exact canonical Claude/OpenCode metadata with no provider-specific state machine."
    - "[D64-14] OpenCode metrics always retain billingKind unknown and usd null; auth, server, topology, secret, model, and raw native fields are absent."
    - "[D64-16] Rehydration, route-loss/restart-loss, concurrent provider runs, and drift reporting stay deterministic across service-worker eviction."
  artifacts:
    - path: "extension/utils/delegation-event-store.js"
      provides: "Durable provider-neutral exact lifecycle entries for both shipped adapters"
    - path: "extension/utils/delegation-controller.js"
      provides: "Append-before-fanout reducer/hydrator with immutable provider identity"
    - path: "extension/utils/agent-protocol-drift-diagnostics.js"
      provides: "Closed per-adapter safe drift reporting and throttling"
  key_links:
    - from: "extension/background.js"
      to: "extension/utils/delegation-event-store.js"
      via: "accepted canonical run context on each normalized event"
      pattern: "billingKind"
    - from: "extension/utils/delegation-controller.js"
      to: "extension/utils/delegation-event-store.js"
      via: "append canonical entry before reduce/fanout"
      pattern: "appendBeforeFanout"
    - from: "extension/utils/agent-protocol-drift-diagnostics.js"
      to: "mcp/src/agent-providers/protocol-drift.ts"
      via: "matching closed adapter/reason/detail projection"
      pattern: "agent_protocol_drift"
---

<objective>
Carry canonical OpenCode identity and terminal truth through durable extension storage, hydration, and safe drift diagnostics.

Purpose: Service-worker eviction and concurrent providers must not relabel a run, replay announcements, promote a candidate result, or leak provider-native/server data.
Output: Two-provider event-store/controller schemas, immutable hydration, unknown OpenCode billing, and closed per-adapter drift reporting.

<scope_rationale>Event storage, controller reduction, and drift reporting are the durable lifecycle boundary. They share the accepted run metadata from Plan 09 and must evolve together to prevent valid OpenCode events from being rejected or mislabeled after eviction.</scope_rationale>
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/64-opencode-adapter/64-CONTEXT.md
@.planning/phases/64-opencode-adapter/64-RESEARCH.md
@.planning/phases/64-opencode-adapter/64-PATTERNS.md
@.planning/phases/64-opencode-adapter/64-UI-SPEC.md
@.planning/phases/61-delegation-ux-sw-eviction-persistence/61-02-SUMMARY.md
@.planning/phases/61-delegation-ux-sw-eviction-persistence/61-06-SUMMARY.md
@.planning/phases/62-ci-drift-smoke-gate-doctor-extensions/62-04-SUMMARY.md
@extension/utils/delegation-providers.js
@extension/utils/delegation-event-store.js
@extension/utils/delegation-controller.js
@extension/utils/agent-protocol-drift-diagnostics.js
@extension/background.js

<decision_trace>
- D64-10/D64-11: persist only normalized exact events and safe closed drift detail.
- D64-12: result remains nonterminal until explicit supervisor-authoritative terminal.
- D64-13: reuse one provider-neutral durable lifecycle.
- D64-14: OpenCode billing remains unknown with no dollar/subscription claim.
- D64-16: eviction/hydration/drift/loss behavior gains two-provider coverage.
</decision_trace>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>64-10-01: Persist exact canonical Claude/OpenCode lifecycle entries</name>
  <files>extension/utils/delegation-event-store.js, tests/fixtures/delegation-events.js, tests/delegation-event-store.test.js</files>
  <read_first>delegation-event-store.js exact payload/entry/init/metrics schemas, quotas, normalization, append and hydrate; Plan 09 canonical helper/run context; existing delegation fixture factory; UI-SPEC security and billing locks</read_first>
  <behavior>
    - "Init client is null or one exact canonical `{id,label}` pair for Claude/OpenCode; mismatches, Codex, case variants, accessors, extra keys, and mutable references fail."
    - "Entry/version/key/quota schemas remain compatible with existing valid Claude records."
    - "OpenCode result metrics force billingKind unknown and usd null even if hostile context/payload claims subscription/API/dollars."
    - "A normalized result entry retains running state; only an explicit terminal context changes completed/failed/stopped/restart_lost truth."
    - "No raw native event, task, provider model/auth, endpoint/port/secret/path/env/argv, or unknown context key can serialize."
  </behavior>
  <action>Extend fixture factories and event-store tests first for both canonical providers, hostile id/label/billing/context/raw-native values, legacy Claude envelopes, quotas, and result-before-terminal hydration. Replace the single Claude validation constant with the Plan 09 canonical helper while reconstructing and freezing the provider pair at projection. Preserve record version and exact keys wherever possible. Derive billing only from the accepted canonical context: Claude may retain subscription; OpenCode is always unknown with usd null. Keep provider-native payloads outside the API and reject unknown context/event fields. Assert serialized sentinels never include topology/version/path/secret/task/model/auth/raw JSONL.</action>
  <acceptance_criteria>Valid existing Claude and new OpenCode entries round-trip with exact immutable canonical metadata, hostile/mismatched/raw fields fail before write, OpenCode never stores subscription/usd, and a result cannot hydrate or persist as completed without terminal.</acceptance_criteria>
  <verify>
    <automated>node tests/delegation-event-store.test.js</automated>
  </verify>
  <done>The acceptance criteria above are satisfied and the automated verification passes.</done>
</task>

<task type="auto" tdd="true">
  <name>64-10-02: Generalize controller reduction, hydration, and concurrent provider identity</name>
  <files>extension/utils/delegation-controller.js, tests/fixtures/delegation-events.js, tests/delegation-controller.test.js</files>
  <read_first>delegation-controller.js closed provider constants, start, acceptEvent append-before-fanout, hydrate/providerFromEntries, timeout/hold/resume/stop/loss handling; Phase 61 controller summary; event store task above</read_first>
  <behavior>
    - "controller.start accepts only one canonical provider object and stores a defensive immutable copy; no current-setting lookup can change it later."
    - "acceptEvent always awaits canonical durable append before reduce/snapshot/fanout for either provider."
    - "Hydration derives exact provider from stored init entries, preserves existing valid Claude records, and silently restores OpenCode without replaying subscribers/announcements."
    - "Concurrent Claude and OpenCode delegations maintain separate provider/profile/sequence/terminal/hold/tab state."
    - "Restart/route loss, timeout, stop, resume, and duplicate/out-of-order events settle exactly once without provider-specific branches."
  </behavior>
  <action>Add concurrent-provider, mutable caller, append-failure, delayed append, eviction hydration, result-before-terminal, duplicate/out-of-order, route/restart loss, hold/resume/stop, and silent-subscriber tests first. Replace hard-coded provider constants and `_closedProvider`/`_providerFromEntries` checks with canonical helper validation/copy. Keep the state machine and persistence ordering provider-neutral; do not add OpenCode transitions. Preserve snapshot version/keys and legacy Claude hydration. Ensure provider metadata captured at start/entry is the only source for later snapshots and cannot be recomputed from settings.</action>
  <acceptance_criteria>Both provider runs survive hydration with exact immutable identity and sequence, persistence still precedes every visible state/fanout, result is nonterminal, all lifecycle/loss races settle once, legacy Claude data remains valid, and no OpenCode-specific state branch exists.</acceptance_criteria>
  <verify>
    <automated>node tests/delegation-controller.test.js</automated>
  </verify>
  <done>The acceptance criteria above are satisfied and the automated verification passes.</done>
</task>

<task type="auto" tdd="true">
  <name>64-10-03: Project and throttle safe adapter-specific drift details</name>
  <files>extension/utils/agent-protocol-drift-diagnostics.js, extension/background.js, tests/agent-protocol-drift-diagnostics.test.js, tests/mcp-bridge-background-dispatch.test.js</files>
  <read_first>protocol-drift.ts closed provider/reason/detail contract from Plan 01; agent-protocol-drift-diagnostics.js canonical id/reason/shape/throttle/window; background fsbReportAgentProtocolDriftOnce/final settlement and accepted run metadata</read_first>
  <behavior>
    - "Diagnostics accepts exactly canonical Claude/OpenCode ids and only the closed reason vocabulary registered for that adapter."
    - "Detail contains only bounded adapterId/profileVersion/reason/expected/eventIndex/issuePaths; raw message/event/path/argv/env/task/secret/model/config cannot enter."
    - "Rate limiting and latest timestamps are isolated per exact provider with a closed maximum of two adapters."
    - "Background reports drift once per delegation only when detail adapterId matches immutable accepted run provider; reporting failure cannot affect terminal settlement."
    - "Hydration or repeated final delivery cannot duplicate a diagnostic."
  </behavior>
  <action>Write exact OpenCode reason, cross-provider mismatch, hostile detail, timestamp isolation, eviction/repeated final, and sentinel tests first. Expand the diagnostic utility to use the canonical provider helper and closed per-adapter reason/expected tables matching MCP; do not accept a generic reason string. Increase only the exact provider timestamp capacity to two and retain bounded report windows/counts. In background, validate drift detail against the accepted run context before reporting once, then settle independently even if diagnostics throw. Strip raw exception/provider fields at validation and never use provider-supplied display labels.</action>
  <acceptance_criteria>Valid closed drift detail reports once under the correct provider, cross-provider/unknown/raw/oversize/accessor inputs are dropped, Claude/OpenCode throttle independently, repeated delivery/hydration does not duplicate, and diagnostics cannot change terminal settlement.</acceptance_criteria>
  <verify>
    <automated>node tests/agent-protocol-drift-diagnostics.test.js && node tests/mcp-bridge-background-dispatch.test.js --section agent-protocol-drift</automated>
  </verify>
  <done>The acceptance criteria above are satisfied and the automated verification passes.</done>
</task>

</tasks>

<threat_model>
  <block_on>HIGH,CRITICAL</block_on>
  <assets>
    <asset id="A64-22">Durable delegation chronology, provider identity, and terminal state across eviction.</asset>
    <asset id="A64-23">Honest OpenCode billing metadata and absence of fabricated dollars/subscription.</asset>
    <asset id="A64-24">Safe drift diagnostics and availability of the terminal path.</asset>
  </assets>
  <trust_boundaries>
    <boundary id="B64-22">Authenticated normalized MCP events enter extension durable storage.</boundary>
    <boundary id="B64-23">Persisted records re-enter controller state after service-worker eviction.</boundary>
    <boundary id="B64-24">MCP drift detail enters extension diagnostics and rate limiting.</boundary>
  </trust_boundaries>
  <threat id="T64-08" category="Tampering" component="persisted result/terminal" severity="CRITICAL" disposition="mitigate" asvs="V5">
    <mitigation>Keep result state running and require a distinct authoritative terminal append before completed hydration.</mitigation>
    <verification>Result-before-terminal, failure-after-result, eviction, duplicate, and append-order tests.</verification>
    <residual_risk>Supervisor process corroboration is proven separately in Plan 08.</residual_risk>
  </threat>
  <threat id="T64-09" category="Denial of Service" component="drift diagnostics" severity="HIGH" disposition="mitigate" asvs="V5,V7">
    <mitigation>Validate closed per-adapter reasons and bounded detail, throttle independently, and isolate diagnostic exceptions from settlement.</mitigation>
    <verification>Oversize/raw/accessor/unknown/mismatch/repeat/window negative matrix.</verification>
    <residual_risk>Diagnostic storage remains best-effort observational data.</residual_risk>
  </threat>
  <threat id="T64-10" category="Information Disclosure" component="extension persistence" severity="CRITICAL" disposition="mitigate" asvs="V7">
    <mitigation>Persist only canonical provider-neutral exact records and reject raw native/server/task/auth/model/path/env/secret fields.</mitigation>
    <verification>Serialized sentinel scans, exact-key schemas, canonical pair validation, and OpenCode unknown-billing assertions.</verification>
    <residual_risk>Canonical provider label/profile metadata is intentionally retained for run presentation.</residual_risk>
  </threat>
</threat_model>

<verification>
- Store/controller/diagnostic tests are offline, deterministic, and cover legacy Claude plus concurrent OpenCode.
- Append-before-fanout and result-nonterminal invariants are asserted under delayed/failing persistence.
- No raw provider/server/task/credential data is present in extension storage or diagnostic output.
</verification>

<success_criteria>
- OpenCode lifecycle state survives eviction without relabeling, replay, or premature success.
- Billing remains unknown and dollar-free for every OpenCode record and summary input.
- Drift is safe, provider-specific, bounded, and unable to disrupt exact-once terminal settlement.
</success_criteria>

<output>
Create `.planning/phases/64-opencode-adapter/64-10-SUMMARY.md` when complete.
</output>
