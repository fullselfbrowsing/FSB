---
phase: 64-opencode-adapter
plan: "03"
type: execute
wave: 3
depends_on:
  - "64-02"
files_modified:
  - mcp/src/agent-providers/runtime-files.ts
  - tests/mcp-agent-orphan-recovery.test.js
  - tests/mcp-agent-provider-contract.test.js
autonomous: true
requirements:
  - MULTI-01
  - MULTI-03
must_haves:
  truths:
    - "[D64-02/D64-04] Runtime state distinguishes delegation children from daemon-owned provider servers without widening discovery, cleanup, or kill authority."
    - "[D64-15] A new exact role-aware journal and private-artifact grammar is durable before owned-server supervision or replay-sensitive attach execution lands."
    - "Legacy Phase 60 version-1 Claude entries retain exact recovery behavior while unknown roles, shapes, paths, or process identities fail closed."
    - "The journal may retain only serialized fixedEnv and public process identity/fingerprint facts; OPENCODE_SERVER_PASSWORD, resolved spawn env, secret bytes, Authorization headers, and secret values are structurally impossible."
  artifacts:
    - path: "mcp/src/agent-providers/runtime-files.ts"
      provides: "Versioned role-aware runtime journal and exact private-artifact boundary"
    - path: "tests/mcp-agent-orphan-recovery.test.js"
      provides: "Legacy/new role, private-file, secret non-retention, and zero-kill recovery matrix"
  key_links:
    - from: "mcp/src/agent-providers/runtime-files.ts"
      to: "mcp/src/agent-providers/spawn-supervisor.ts"
      via: "journal-before-spawn role, generation, retained process identity, and fixed-env-only state"
      pattern: "delegation|provider_server"
---

<objective>
Version the runtime journal and private-artifact grammar for delegation and daemon-owned provider-server roles.

Purpose: Give later OpenCode profile and owned-server supervisor plans one durable, exact, role-aware filesystem/process boundary without expanding adapter authority, retaining transient secrets, or weakening Phase 60 recovery.
Output: A backward-compatible versioned journal, closed runtime-path descriptors, role-specific recovery accounting, and deterministic secret/zero-kill negative coverage.

<scope_rationale>This is a single heavy migration task over three tightly coupled files. Every later profile or supervisor plan consumes this exact runtime boundary rather than mixing migration into security-sensitive topology work.</scope_rationale>
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/64-opencode-adapter/64-CONTEXT.md
@.planning/phases/64-opencode-adapter/64-RESEARCH.md
@.planning/phases/64-opencode-adapter/64-PATTERNS.md
@.planning/phases/64-opencode-adapter/64-VALIDATION.md
@.planning/phases/64-opencode-adapter/64-02-SUMMARY.md
@.planning/phases/60-adapter-contract-claude-code-mvp/60-01-SUMMARY.md
@.planning/phases/60-adapter-contract-claude-code-mvp/60-03-SUMMARY.md
@mcp/src/agent-providers/runtime-files.ts
@mcp/src/agent-providers/spawn-supervisor.ts
@tests/mcp-agent-orphan-recovery.test.js
@tests/mcp-agent-provider-contract.test.js

<decision_trace>
- D64-02: runtime recovery may signal only identity-proven FSB-owned processes and never discovers a user OpenCode process.
- D64-04: distinguish the one daemon-owned server from fresh per-delegation task children.
- D64-15: make process roles and replay-relevant lifecycle boundaries explicit before attach support lands.
- T64-02: durable state may contain fixed public env only, never transient spawn-time secret material.
</decision_trace>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>64-03-01: Version the exact runtime journal for delegation and provider-server roles</name>
  <files>mcp/src/agent-providers/runtime-files.ts, tests/mcp-agent-orphan-recovery.test.js, tests/mcp-agent-provider-contract.test.js</files>
  <read_first>64-PATTERNS Runtime journal section; runtime-files.ts exact-key parsing, prepare/activate/remove, cleanup allowlist, recovery accounting, and version-1 journal; Phase 60 orphan-recovery tests; Plan 64-02 topology/process-role/fixed-env/secret-binding contract</read_first>
  <behavior>
    - "New journal entries use an exact closed runtime role of delegation or provider_server and retain adapter/profile/generation/executable/argv/fixedEnv/process identity only."
    - "A legacy Phase 60 version-1 Claude entry parses as a delegation and remains safely recoverable."
    - "A journal exact-key parser rejects OPENCODE_SERVER_PASSWORD, spawnSecretEnvBindings, resolved spawn env, raw secret bytes/string, Authorization header, endpoint credentials, and any secret-bearing extra field."
    - "Only an exact closed per-role private directory/file graph may be created or removed; traversal, symlink, foreign file, unknown key, and unsafe mode fail closed."
    - "Confirmed stale delegation recovery records daemon_restart_lost_run; confirmed stale provider-server recovery is cleaned without fabricating lost user work."
    - "Ambiguous or identity-mismatched processes are never killed."
  </behavior>
  <action>Write the version/legacy/role/private-artifact and secret-sentinel recovery fixtures first. Evolve the journal to a new exact schema with a closed process role while parsing legacy version-1 entries into delegation in memory. Generalize prepare/activate/remove inputs so the supervisor can journal task and owned-server trees before spawn without weakening exact keys, serialized mutation, generation binding, or process-start verification. Persist only the declarative public `fixedEnv` and process identity/fingerprint inputs; never pass the post-resolution spawn environment or Plan 02 secret binding into journal APIs. Add a closed runtime-path descriptor for the existing MCP file and reviewed OpenCode private config/test-home/managed-config directory graph; runtime-files remains the sole writer/cleaner, validates every descriptor under its minted runtime directory, writes files atomically at 0600/directories at 0700, and rejects arbitrary adapter paths. Recovery kills only a confirmed retained tree and emits restart-loss dispositions only for delegation roles. Preserve existing Claude journal bytes/behavior where no migration write is required.</action>
  <acceptance_criteria>Orphan tests prove version-1 compatibility, exact version-2 role grammar, journal-before-spawn/activate ordering, role-specific restart accounting, exact private-file containment/modes, refusal on symlink/foreign/ambiguous identity, zero signaling of user/unproven processes, and zero raw password/resolved-env/header retention.</acceptance_criteria>
  <verify>
    <automated>node scripts/run-mcp-build-preserving-workspace.mjs --commands-json '[["node","tests/mcp-agent-orphan-recovery.test.js"],["node","tests/mcp-agent-provider-contract.test.js"]]'</automated>
  </verify>
  <done>The acceptance criteria above are satisfied and the automated verification passes.</done>
</task>

</tasks>

<threat_model>
  <block_on>HIGH,CRITICAL</block_on>
  <assets>
    <asset id="A64-02">FSB-owned process identities, private runtime files, restart recovery state, and absence of transient secrets.</asset>
  </assets>
  <trust_boundaries>
    <boundary id="B64-02">Persisted runtime journals and filesystem entries re-enter a later daemon generation.</boundary>
  </trust_boundaries>
  <threat id="T64-02" category="Information Disclosure" component="runtime journal" severity="CRITICAL" disposition="mitigate" asvs="V2,V7,V12">
    <mitigation>Journal only exact fixed public env and process identity; reject resolved spawn env, secret bindings, password/header values, and unknown keys.</mitigation>
    <verification>Password/header sentinel scans across versioned records, failures, recovery output, serialized snapshots, and disk bytes.</verification>
    <residual_risk>Transient secret delivery is implemented and non-retention-tested in Plan 07.</residual_risk>
  </threat>
  <threat id="T64-03" category="Elevation of Privilege" component="orphan recovery" severity="CRITICAL" disposition="mitigate" asvs="V4,V12">
    <mitigation>Require exact role/path grammar plus retained executable/process-start/group/generation identity, fail closed on ambiguity, and never discover processes.</mitigation>
    <verification>Legacy/new journal recovery and private-artifact matrix with zero-kill negative controls.</verification>
    <residual_risk>Kernel process identity behavior on live hosts remains milestone UAT.</residual_risk>
  </threat>
</threat_model>

<verification>
- The runtime migration command passes only through the workspace-preserving build wrapper.
- Legacy Claude journal/recovery behavior remains green while version-2 roles/private artifacts are exact.
- No secret value, ambiguous identity, foreign path, or user-owned process can be retained, signaled, or removed.
</verification>

<success_criteria>
- Later OpenCode profile and owned-server plans consume one exact runtime-path/process-role contract.
- Durable state distinguishes task work from daemon infrastructure without widening kill or secret-retention authority.
- The dirty workspace and protected generated graph are restored after the compiled check.
</success_criteria>

<output>
Create `.planning/phases/64-opencode-adapter/64-03-SUMMARY.md` when complete.
</output>
