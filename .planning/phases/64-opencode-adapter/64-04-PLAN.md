---
phase: 64-opencode-adapter
plan: "04"
type: execute
wave: 4
depends_on:
  - "64-03"
files_modified:
  - mcp/src/agent-providers/opencode-detect.ts
  - mcp/src/agent-providers/opencode-profile.ts
  - tests/mcp-opencode-adapter.test.js
  - tests/agent-provider-forbidden-flags.test.js
autonomous: true
requirements:
  - MULTI-02
must_haves:
  truths:
    - "[D64-05] OpenCode 1.14.25 receives an exact private XDG/project/home/managed boundary, pure mode, and only loopback FSB MCP authority; user/project plugins, MCP, agents, commands, instructions, hooks, and skills cannot merge in."
    - "[D64-06] The task appears only on task-child stdin and never in argv, fixedEnv, private artifacts, URLs, receipts, diagnostics, or log configuration."
    - "[D64-07] The shipped primary fsb agent is prompt-digest bound, default-deny, explicitly denies external/truncation access, and finally allows only fsb_* tools."
    - "[D64-08] FSB preserves OpenCode-owned sign-in/default-model resolution without reading credentials, passing --model, or injecting an API key; unusable account/model/policy evidence fails before task acceptance."
    - "Server and attach specs declare only Plan 02's exact OPENCODE_SERVER_PASSWORD/owned_server_basic_password binding; cold/direct/preflight specs declare none, and no password value enters profile data."
  artifacts:
    - path: "mcp/src/agent-providers/opencode-detect.ts"
      provides: "Retained native executable and exact 1.14.25 profile detection"
    - path: "mcp/src/agent-providers/opencode-profile.ts"
      provides: "Static private OpenCode config, fsb policy, declarative cold/attach/server specs, exact secret bindings, and generic attestations"
    - path: "tests/mcp-opencode-adapter.test.js"
      provides: "Offline detector/profile/policy/binding contract harness"
  key_links:
    - from: "mcp/src/agent-providers/opencode-profile.ts"
      to: "mcp/ai/agents/fsb.json"
      via: "exact shipped description/prompt digest"
      pattern: "fsb.json"
    - from: "mcp/src/agent-providers/opencode-profile.ts"
      to: "mcp/src/agent-providers/adapter.ts"
      via: "owned-server topology, closed secret binding, runtime refs, and generic attestation data"
      pattern: "SpawnSecretEnvBinding|AttestationDescriptor|freezeSpawnSpec"
    - from: "tests/agent-provider-forbidden-flags.test.js"
      to: "mcp/src/agent-providers/opencode-profile.ts"
      via: "exact 1.14.25 isolation and forbidden-authority source pins"
      pattern: "OPENCODE_DISABLE_PROJECT_CONFIG"
---

<objective>
Build the exact OpenCode 1.14.25 detection and hermetic static-policy boundary against the already-green parser and generic contract/runtime foundation.

Purpose: OpenCode may use its existing sign-in/default model, but no user/project configuration, built-in tool authority, task text, or serialized Basic secret may cross into delegated browser control.
Output: A retained-binary detector, private configuration, static fsb policy, declarative cold/attach/server/preflight specs, exact transient secret bindings, and generic attestations.

<scope_rationale>Detector identity, private configuration, and effective-policy declarations are coupled by the exact 1.14.25 profile. Keeping them together prevents spawning before configuration, binding, and fail-closed attestations are defined.</scope_rationale>
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/64-opencode-adapter/64-CONTEXT.md
@.planning/phases/64-opencode-adapter/64-RESEARCH.md
@.planning/phases/64-opencode-adapter/64-PATTERNS.md
@.planning/phases/64-opencode-adapter/64-VALIDATION.md
@.planning/phases/64-opencode-adapter/64-01-SUMMARY.md
@.planning/phases/64-opencode-adapter/64-02-SUMMARY.md
@.planning/phases/64-opencode-adapter/64-03-SUMMARY.md
@.planning/phases/60-adapter-contract-claude-code-mvp/60-01-SUMMARY.md
@mcp/src/agent-providers/adapter.ts
@mcp/src/agent-providers/claude-detect.ts
@mcp/src/agent-providers/claude-profile.ts
@mcp/src/agent-providers/runtime-files.ts
@mcp/ai/agents/fsb.json
@tests/agent-provider-forbidden-flags.test.js

<decision_trace>
- D64-05: exact private configuration and pure/project/home/managed isolation.
- D64-06: stdin-only task delivery and zero task reflection.
- D64-07: static primary fsb agent with ordered default deny and only fsb_* allow.
- D64-08: preserve native sign-in/default-model ownership and fail closed when unusable.
- T64-02: declare one opaque server/attach secret binding while keeping values absent from profile/fixed data.
</decision_trace>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>64-04-01: Retain and pin the exact OpenCode 1.14.25 executable profile</name>
  <files>mcp/src/agent-providers/opencode-detect.ts, tests/mcp-opencode-adapter.test.js</files>
  <read_first>64-RESEARCH Requirements Mapping, Detection, and source evidence; claude-detect.ts complete candidate/realpath/stat/version/identity flow; compatibility.ts evidence classification; adapter.ts AdapterDetection</read_first>
  <behavior>
    - "PATH lookup checks only fixed native OpenCode executable names, uses shell:false, and retains one absolute real executable identity through the version probe."
    - "Only exact version 1.14.25 receives profileVersion 1.14.25 and may proceed to spawn; missing, malformed, changed, unsafe, older, or newer binaries return bounded diagnostics."
    - "Detection reports authState unknown and never reads OpenCode auth/config/state files, environment credentials, or a model."
    - "Probe output, argv, duration, and bytes are bounded; no raw output or path reaches the diagnostic message."
  </behavior>
  <action>Create the detector harness first with injected POSIX/Windows candidate, identity-race, output-cap, malformed version, exact version, older/newer version, and poisoned credential/config fixtures. Implement `opencode-detect.ts` by applying the retained-native identity pattern—not Claude flags—from `claude-detect.ts`: fixed PATH candidates, access/realpath/stat checks, shell-free bounded `--version`, post-probe identity recheck, and immutable retained command/argvPrefix. Bind execution to exact reviewed 1.14.25 even though compatibility UI may later classify newer same-major evidence as Degraded. Return only bounded provider-neutral codes/copy, authState unknown, and no auth/model inspection.</action>
  <acceptance_criteria>Detection accepts only a stable native OpenCode 1.14.25 identity, rejects unsafe/version/race/output cases before spawn, leaves auth unknown, and never reads or serializes provider credential/model/config data.</acceptance_criteria>
  <verify><automated>node scripts/run-mcp-build-preserving-workspace.mjs --commands-json '[["node","tests/mcp-opencode-adapter.test.js","--section","detection"]]'</automated></verify>
  <done>The acceptance criteria above are satisfied and the automated verification passes.</done>
</task>

<task type="auto" tdd="true">
  <name>64-04-02: Generate the private config, static fsb policy, and exact server/attach secret bindings</name>
  <files>mcp/src/agent-providers/opencode-profile.ts, tests/mcp-opencode-adapter.test.js</files>
  <read_first>64-RESEARCH Patterns 4-5 and Invocation Surface; 64-CONTEXT Hermetic Policy; claude-profile task/context validation and task-absence scan; fsb.json; Plan 02 topology/fixed-env/secret-binding contract; Plan 03 runtime-path contract</read_first>
  <behavior>
    - "The private opencode.json is exact with share disabled, autoupdate disabled, default_agent fsb, plugin [], command {}, instructions [], and exactly one enabled remote loopback fsb MCP with OAuth false."
    - "The fsb agent has mode primary, exact shipped description/prompt digest, steps 40, no model override, wildcard deny, external-directory/truncation deny, and final fsb_* allow."
    - "Cold argv is retained-prefix plus --pure --log-level ERROR run --format json --agent fsb; attach adds only a typed owned endpoint; serve is pure/error serve --hostname 127.0.0.1 --port 0 --mdns false."
    - "Server and attach task ProcessSpecs each declare exactly `{envKey:OPENCODE_SERVER_PASSWORD, secretRef:owned_server_basic_password}`; cold task and every process_json policy probe declare no secret binding."
    - "fixedEnv contains only reviewed non-secret isolation controls and rejects OPENCODE_SERVER_PASSWORD; no password value, task, model, continue/session/fork/share/file/command, print-logs, bypass flag, or provider credential occurs in serialized profile data."
  </behavior>
  <action>Write exact config/prompt/permission/topology/binding snapshots and hostile mutation tests first. Implement `opencode-profile.ts` as a pure defensive builder over Plans 02-03 and retained detection. Load and exact-validate fsb.json, derive the static primary agent, and return closed private artifacts for config/test-home/managed roots. Set only reviewed fixed non-secret environment controls: private XDG_CONFIG_HOME, OPENCODE_DISABLE_PROJECT_CONFIG=1, private OPENCODE_TEST_HOME, private OPENCODE_TEST_MANAGED_CONFIG_DIR, OPENCODE_DISABLE_CLAUDE_CODE_PROMPT=1, OPENCODE_DISABLE_EXTERNAL_SKILLS=1, and source-verified update/LSP disables; do not replace HOME/data/state roots. Describe cold, attach, server, and non-task preflights with Plan 02 types. The server and attach task specs declare the exact OPENCODE_SERVER_PASSWORD/owned_server_basic_password binding; cold and preflight specs declare none. Never resolve the reference or place the password key/value in fixedEnv. Endpoint authority remains a typed ref, task delivery remains task-client stdin only, and the result is recursively frozen with task/secret-value scans.</action>
  <acceptance_criteria>The generated config/specs match the reviewed 1.14.25 surface; only fsb_* resolves enabled; no model/credential/task/password value is reflected; HOME/data/state stay untouched; server and attach alone have the exact binding; cold/preflight/direct have none; nested mutation cannot change the topology.</acceptance_criteria>
  <verify><automated>node scripts/run-mcp-build-preserving-workspace.mjs --commands-json '[["node","tests/mcp-opencode-adapter.test.js","--section","profile-policy"]]'</automated></verify>
  <done>The acceptance criteria above are satisfied and the automated verification passes.</done>
</task>

<task type="auto" tdd="true">
  <name>64-04-03: Populate exact generic policy attestations and source-security pins</name>
  <files>mcp/src/agent-providers/opencode-profile.ts, tests/mcp-opencode-adapter.test.js, tests/agent-provider-forbidden-flags.test.js</files>
  <read_first>64-RESEARCH effective config/agent/tool attestation, fallback, truncation exception, source-stability notes; pinned 1.14.25 source URLs; existing forbidden-flags/CHAN gates</read_first>
  <behavior>
    - "Topology populates only generic bounded process_json descriptors for debug config/debug agent fsb and equivalent owned_server_json GET descriptors before a task can be accepted."
    - "Owned-server descriptors contain only the same opaque owned_server_basic_password ref for supervisor-side header derivation; no password/header value or attach-child env appears in attestation data."
    - "Generic assertions require only private fsb MCP authority, primary fsb prompt digest/no model override, enabled fsb_* and zero other tools, exact permission order/truncation deny, and a resolved default model."
    - "Missing/renamed/subagent fsb, fallback warning, poisoned plugin/MCP/command/instruction/skill/agent, unknown permission, unresolved model, oversize/malformed output, or raw accessor input yields bounded not-ready failure."
  </behavior>
  <action>Add offline descriptor snapshots and poison documents first. Populate the topology exclusively with Plan 02 closed data: bounded non-task ProcessSpecs for debug config/debug agent fsb, equivalent authenticated owned-server GET descriptors, byte/time limits, closed codes, and ordered assertions. String equality uses SHA-256; enabled-tool prefix uses only the closed fsb_mcp_tool_prefix ref. Owned-server requests carry only the opaque owned_server_basic_password ref, resolved later by the generic supervisor directly into a transient Authorization header. Define/export no OpenCode checker, callback, sixth method, supervisor hook, raw reducer, or adapter selector. Add source-security assertions for every isolation flag, profile pin, forbidden flags, prompt digest, permission order, no HOME/data/state/API-key/model override, no print-logs, exact server+attach secret-binding placement, fixedEnv password-key refusal, no password values, and absence of native checker exports. Preserve every Phase 60-63 invariant.</action>
  <acceptance_criteria>Clean documents satisfy only shared generic descriptors; poison/missing-model/fallback cases yield the closed verdict; profile source is declarations only; raw bodies/password values are neither retained nor logged; source gates bind profile, isolation, binding, and attestation semantics.</acceptance_criteria>
  <verify><automated>node scripts/run-mcp-build-preserving-workspace.mjs --commands-json '[["node","tests/mcp-opencode-adapter.test.js","--section","attestation"],["node","tests/agent-provider-forbidden-flags.test.js"]]'</automated></verify>
  <done>The acceptance criteria above are satisfied and the automated verification passes.</done>
</task>

</tasks>

<threat_model>
  <block_on>HIGH,CRITICAL</block_on>
  <assets>
    <asset id="A64-04">User task text and provider credentials owned by OpenCode.</asset>
    <asset id="A64-05">The shipped FSB prompt/tool permission boundary.</asset>
    <asset id="A64-06">The exact 1.14.25 executable/profile binding and private config graph.</asset>
  </assets>
  <trust_boundaries>
    <boundary id="B64-04">PATH/native version output enters retained detection.</boundary>
    <boundary id="B64-05">Private generated policy meets OpenCode's multi-source configuration loader.</boundary>
    <boundary id="B64-06">Profile-declared opaque secret bindings later enter supervisor-only resolution.</boundary>
  </trust_boundaries>
  <threat id="T64-02" category="Information Disclosure" component="task and Basic-secret declaration" severity="CRITICAL" disposition="mitigate" asvs="V2,V7">
    <mitigation>Deliver task only through task stdin; separate fixedEnv from an exact opaque binding on server/attach only; preserve OpenCode-owned auth roots without inspecting credentials.</mitigation>
    <verification>Task/password sentinels across serialized specs/config/fixedEnv/attestations/diagnostics plus exact binding-placement negatives.</verification>
    <residual_risk>The raw password is delivered transiently to the owned server and selected attach child by Plan 07.</residual_risk>
  </threat>
  <threat id="T64-05" category="Elevation of Privilege" component="configuration merge" severity="CRITICAL" disposition="mitigate" asvs="V4,V14">
    <mitigation>Use exact private config/test-home/managed roots, project disable, pure mode, external prompt/skill disables, and effective-policy attestations.</mitigation>
    <verification>Poison every external config authority and source-pin exact 1.14.25 isolation semantics.</verification>
    <residual_risk>Upstream internal isolation flags require re-review for any later profile.</residual_risk>
  </threat>
  <threat id="T64-06" category="Elevation of Privilege" component="agent and tool resolution" severity="CRITICAL" disposition="mitigate" asvs="V4">
    <mitigation>Bind prompt digest/primary/no model override and ordered wildcard/truncation denies with final fsb_* allow; reject fallback warnings.</mitigation>
    <verification>Exact effective-agent/tool fixtures for missing, subagent, override, built-in, future unknown, and truncation cases.</verification>
    <residual_risk>Genuine account/model usability remains human UAT.</residual_risk>
  </threat>
</threat_model>

<verification>
- Each detector/profile/security slice writes tests first and uses the workspace-preserving wrapper.
- Process/config snapshots contain no task, credential, model override, path leak, or raw password.
- No dependency, user-file read, imperative agent create, or live OpenCode invocation is added.
</verification>

<success_criteria>
- OpenCode 1.14.25 is safely described without inheriting user/project authority.
- Exact server/attach authentication is declarative but secret values remain supervisor-only.
- Missing account/model/policy evidence is bounded not-ready, never fallback.
</success_criteria>

<output>
Create `.planning/phases/64-opencode-adapter/64-04-SUMMARY.md` when complete.
</output>
