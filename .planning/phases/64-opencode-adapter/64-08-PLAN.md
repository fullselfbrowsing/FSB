---
phase: 64-opencode-adapter
plan: "08"
type: execute
wave: 7
depends_on:
  - "64-06"
  - "64-07"
files_modified:
  - mcp/src/agent-providers/spawn-supervisor.ts
  - mcp/src/agent-providers/serve-delegation.ts
  - tests/mcp-opencode-server-topology.test.js
  - tests/mcp-spawn-supervisor.test.js
  - tests/mcp-reverse-channel-contract.test.js
  - tests/mcp-bridge-topology.test.js
  - tests/agent-provider-forbidden-flags.test.js
autonomous: true
requirements:
  - MULTI-01
  - MULTI-02
  - MULTI-03
must_haves:
  truths:
    - "[D64-05] Cold and owned-server paths pass exact effective config/agent/tool/model attestation before task acceptance; raw attestation/provider data is discarded."
    - "[D64-08] Missing usable account/model or fallback-agent evidence becomes one bounded not-ready failure with no API-key/model substitution."
    - "[D64-10] Provider-native events remain normalized behind the existing delegation.started/delegation.event/final transport."
    - "[D64-11] Parser, stderr policy, task exit, and tree settlement failures stop the proven task tree and settle once with closed detail."
    - "[D64-12] A candidate result is published only after parser EOF, stderr drain, code 0/no signal, and verified task-tree/runtime cleanup; every other outcome discards it."
    - "[D64-15] Fallback is impossible after task-child spawn even when attach exits before emitting an event."
  artifacts:
    - path: "mcp/src/agent-providers/spawn-supervisor.ts"
      provides: "Pre-task attestation, stderr policy gate, and authoritative result commit barrier"
    - path: "mcp/src/agent-providers/serve-delegation.ts"
      provides: "Provider-neutral two-adapter reverse-channel lifecycle"
    - path: "tests/mcp-spawn-supervisor.test.js"
      provides: "Candidate/exit/tree/exact-once regression matrix for both topology modes"
  key_links:
    - from: "mcp/src/agent-providers/spawn-supervisor.ts"
      to: "mcp/src/agent-providers/policy-attestation.ts"
      via: "shared provider-neutral descriptor verifier with bounded process/server probe data"
      pattern: "verifyPolicyAttestation"
    - from: "mcp/src/agent-providers/spawn-supervisor.ts"
      to: "mcp/src/agent-providers/adapter.ts"
      via: "adapter.parseEvents provider-neutral result candidate retained until process corroboration"
      pattern: "parseEvents|resultEvent"
    - from: "mcp/src/agent-providers/serve-delegation.ts"
      to: "mcp/src/agent-providers/registry.ts"
      via: "closed adapter id lookup for delegate.start"
      pattern: "registry.require"
---

<objective>
Close the supervisor's pre-task policy and terminal-truth barriers for cold and attach execution.

Purpose: Neither private configuration nor a valid `step_finish` is enough to authorize browser work or success; effective policy, task-process exit, and cleanup must corroborate the run.
Output: Exact cold/server attestations, fail-closed fallback-warning handling, no-replay settlement, and result publication only after clean exit/tree settlement.

<scope_rationale>These tasks finalize the single supervisor authority path after Plan 07 owns topology. Attestation, replay fencing, and terminal publication are sequenced together because each constrains when `delegation.started`, task stdin, events, and final success may become externally visible.</scope_rationale>
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/64-opencode-adapter/64-CONTEXT.md
@.planning/phases/64-opencode-adapter/64-RESEARCH.md
@.planning/phases/64-opencode-adapter/64-PATTERNS.md
@.planning/phases/64-opencode-adapter/64-VALIDATION.md
@.planning/phases/60-adapter-contract-claude-code-mvp/60-03-SUMMARY.md
@.planning/phases/60-adapter-contract-claude-code-mvp/60-04-SUMMARY.md
@.planning/phases/61-delegation-ux-sw-eviction-persistence/61-05-SUMMARY.md
@mcp/src/agent-providers/adapter.ts
@mcp/src/agent-providers/policy-attestation.ts
@mcp/src/agent-providers/spawn-supervisor.ts
@mcp/src/agent-providers/serve-delegation.ts
@tests/mcp-reverse-channel-contract.test.js

<decision_trace>
- D64-05/D64-08: attest the effective isolated policy and usable native model before task acceptance.
- D64-10/D64-11: keep native grammar below the adapter and fail loud on all invalid lifecycle evidence.
- D64-12: candidate result becomes authoritative only after clean process/tree settlement.
- D64-15: replay fence remains permanent after selected task-child spawn.
- D64-16: reverse-channel/supervisor/source-security coverage is blocking.
</decision_trace>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>64-08-01: Execute exact cold and owned-server policy attestations before acceptance</name>
  <files>mcp/src/agent-providers/spawn-supervisor.ts, tests/mcp-opencode-server-topology.test.js, tests/agent-provider-forbidden-flags.test.js</files>
  <read_first>Plan 02 closed AttestationDescriptor/secret-binding contract and shared policy-attestation verifier; Plan 04 declarative OpenCode descriptor population; 64-RESEARCH.md effective debug config/agent/tool/model and authenticated server /config,/agent requirements; spawn-supervisor start acknowledgement ordering; Plan 07 verified lease/secret store</read_first>
  <behavior>
    - "Cold path interprets only closed `process_json` descriptors, running bounded shell-free debug config and debug agent fsb under the exact selected task environment before task-child spawn/acceptance."
    - "Owned lease interprets only closed `owned_server_json` descriptors, deriving a transient Basic header from owned_server_basic_password inside the supervisor for /config and /agent after health/identity validation and before becoming attachable."
    - "Only the private fsb MCP, exact primary prompt digest/no model override, at least one fsb_* tool and no other enabled tool, exact deny/allow order, and noninteractive model resolution pass."
    - "Raw config/agent/tool/model responses and provider strings are discarded after verdict and never enter journal, diagnostic, status, event, log, or extension data."
    - "Any ambiguity/malformed/timeout/poison/no-model case produces no delegation.started, task child, task stdin, or attachable lease."
  </behavior>
  <action>Add clean and poisoned cold/server attestation fixtures first, including accessor/prototype/oversize/timeout, plugin/MCP/command/instruction/skill/agent/tool/permission/model contamination, wrong prompt, subagent, raw body, password, and Authorization-header sentinels. Implement one generic supervisor probe executor that switches only on descriptor source: explicit ProcessSpec argv with shell:false/bounded output/time/fixedEnv/no secret binding/no stdin, or bounded GET whose request helper derives the transient Basic header directly from the Plan 07 supervisor-owned `owned_server_basic_password` ref and scrubs request options after invocation. Parse exact own-data JSON, call only `verifyPolicyAttestation`, immediately discard the document, and retain only its frozen pass/closed-reason verdict. Do not import/call the OpenCode profile, compare adapter id, accept callbacks, expose the secret store, or add a sixth method. Reject/clean the proven process/server on failure and return only closed detail; never retain/log raw bodies, headers, passwords, credentials, or model values.</action>
  <acceptance_criteria>Only exact clean effective policy/model evidence reaches task selection; every poisoned/malformed/missing case has zero start/task/stdin/lease side effects and bounded non-secret output; cold/server paths use the same generic verifier; source gates prove no spawn-supervisor→OpenCode import/checker/callback/id branch and no sixth method.</acceptance_criteria>
  <verify>
    <automated>node scripts/run-mcp-build-preserving-workspace.mjs --commands-json '[["node","tests/mcp-opencode-server-topology.test.js","--section","policy-attestation"],["node","tests/agent-provider-forbidden-flags.test.js"]]'</automated>
  </verify>
  <done>The acceptance criteria above are satisfied and the automated verification passes.</done>
</task>

<task type="auto" tdd="true">
  <name>64-08-02: Enforce stderr fallback sentinels, task-once delivery, and post-spawn no replay</name>
  <files>mcp/src/agent-providers/spawn-supervisor.ts, mcp/src/agent-providers/serve-delegation.ts, tests/mcp-opencode-server-topology.test.js, tests/mcp-spawn-supervisor.test.js, tests/mcp-reverse-channel-contract.test.js</files>
  <read_first>OpenCode 1.14.25 missing-agent fallback warning evidence in RESEARCH; spawn-supervisor drainStderr/writeTask/publish/settle paths; Plan 07 replay fence; reverse-channel exact event grammar</read_first>
  <behavior>
    - "Task bytes are written exactly once to only the selected task child's stdin, followed by EOF; server and preflight stdin never receive them, and the selected attach child receives its password only through Plan 07's transient spawn binding."
    - "Known bounded missing/invalid/subagent fsb fallback warning on task stderr fails the run and stops the tree without forwarding stderr."
    - "Any task-child spawn/stdin/stream/stderr/exit/cancel failure after selection settles exactly once and cannot invoke cold fallback, including zero normalized events."
    - "delegation.started contains only canonical adapterId/delegationId/profileVersion and is emitted after preflight/verified activation but before the single task write; topology/endpoint/secret never enter wire frames."
  </behavior>
  <action>Write call-order and sentinel cases first for cold, attach, direct Claude, and zero-event attach exit, including exact attach env presence at the direct spawn call, cold/preflight absence, post-spawn env scrubbing, and zero password/header retention in run state or failure output. Replace count-only stderr draining with a bounded streaming sentinel detector for only reviewed 1.14.25 fallback forms; discard all other stderr. Centralize one task-write helper guarded by the replay flag and task-child role. Audit every catch/cancel/timeout branch after task-child spawn to terminate/cleanup without another process. Extend reverse-channel tests for both ids with exact payloads and reject topology, endpoint, port, secret ref/value, raw stderr, or task reflection.</action>
  <acceptance_criteria>Each successful or failed run has at most one task child and one stdin write; fallback warnings cannot silently run the default agent; every post-spawn permutation has one terminal and no retry; wire events remain exact, bounded, and topology/secret-free.</acceptance_criteria>
  <verify>
    <automated>node scripts/run-mcp-build-preserving-workspace.mjs --commands-json '[["node","tests/mcp-opencode-server-topology.test.js","--section","task-once"],["node","tests/mcp-spawn-supervisor.test.js"],["node","tests/mcp-reverse-channel-contract.test.js"]]'</automated>
  </verify>
  <done>The acceptance criteria above are satisfied and the automated verification passes.</done>
</task>

<task type="auto" tdd="true">
  <name>64-08-03: Publish result only after clean exit, tree settlement, and runtime cleanup</name>
  <files>mcp/src/agent-providers/spawn-supervisor.ts, mcp/src/agent-providers/serve-delegation.ts, tests/mcp-spawn-supervisor.test.js, tests/mcp-opencode-server-topology.test.js, tests/mcp-reverse-channel-contract.test.js, tests/mcp-bridge-topology.test.js</files>
  <read_first>64-RESEARCH.md Pattern 7 authoritative success ordering; spawn-supervisor consumeEvents/executeRun/terminateAndCleanup/settleOnce; event-store contract that result remains nonterminal until explicit final; Phase 60 exact-once and bridge topology tests</read_first>
  <behavior>
    - "consumeEvents retains exactly one result candidate privately and never publishOrBuffer's it at parser EOF."
    - "Success order is parser EOF/candidate -> stderr clean -> child close code 0/no signal -> task tree settled -> runtime removed -> publish one result -> final succeeded."
    - "Provider is_error, parser error, missing/duplicate/post-terminal candidate, stderr sentinel, nonzero/signal, cancellation, timeout, cleanup failure, or tree_unsettled discards the candidate and publishes no success/result."
    - "Buffered pre-authority nonterminal events retain chronology and flush once; all terminal races still resolve delegate.start once."
    - "The strengthened barrier applies to direct Claude, cold OpenCode, and attached OpenCode without changing normalized/browser payload shape."
  </behavior>
  <action>Add a deterministic barrier matrix first with delayed exits/cleanup, nonzero/signal/provider-error/missing/duplicate/trailing/drift/stderr/cancel/tree-unsettled cases, and assertions over event visibility before each gate. Refactor `consumeEvents` to store but never publish the candidate. In `executeRun`, await parser, stderr, and close; validate candidate and exit; settle the verified task tree and remove its runtime state; only then publish the frozen candidate and return one succeeded final. All failure branches discard it before existing bounded terminal settlement. Keep attached server lease release/idle handling after task cleanup without treating the long-lived server as part of task success. Extend bridge/reverse-channel tests for one result then final only on the clean path and zero transient/persistable success on every failure.</action>
  <acceptance_criteria>No observer can receive result before clean code-0/no-signal/task-tree/runtime settlement; every adverse permutation receives zero result and one failed/cancelled final; cold/attach/direct success have identical normalized order; Phase 60 cancellation/hold/status/cleanup regressions remain green.</acceptance_criteria>
  <verify>
    <automated>node scripts/run-mcp-build-preserving-workspace.mjs --commands-json '[["node","tests/mcp-spawn-supervisor.test.js"],["node","tests/mcp-opencode-server-topology.test.js","--section","terminal-barrier"],["node","tests/mcp-reverse-channel-contract.test.js"],["node","tests/mcp-bridge-topology.test.js"]]'</automated>
  </verify>
  <done>The acceptance criteria above are satisfied and the automated verification passes.</done>
</task>

</tasks>

<threat_model>
  <block_on>HIGH,CRITICAL</block_on>
  <assets>
    <asset id="A64-16">Pre-task authorization and single task-delivery authority.</asset>
    <asset id="A64-17">Authoritative success ordering and persisted delegation truth.</asset>
    <asset id="A64-18">Raw effective config/agent/model/stderr/provider data that must remain local and transient.</asset>
  </assets>
  <trust_boundaries>
    <boundary id="B64-16">Untrusted effective-policy output decides whether a task child may exist.</boundary>
    <boundary id="B64-17">Parser candidate and process exit/tree evidence combine into one public result.</boundary>
    <boundary id="B64-18">MCP supervisor events cross the authenticated reverse channel toward extension persistence.</boundary>
  </trust_boundaries>
  <threat id="T64-04" category="Tampering" component="post-spawn fallback" severity="CRITICAL" disposition="mitigate" asvs="V3">
    <mitigation>Close replay before task-child spawn and route every later failure only to cleanup/terminal settlement.</mitigation>
    <verification>Call-count matrix for spawn/stdin/start/event/result/final across zero-event and race permutations.</verification>
    <residual_risk>Explicit user retry is separate new authority.</residual_risk>
  </threat>
  <threat id="T64-02" category="Information Disclosure" component="attach secret propagation" severity="CRITICAL" disposition="mitigate" asvs="V2,V7">
    <mitigation>Use only Plan 07's generic transient binding for the selected attach child's direct spawn and transient supervisor-owned header derivation; scrub ephemeral objects and retain no value.</mitigation>
    <verification>Spawn/HTTP sentinel fakes, cold/preflight absence, post-call object inspection, run-state serialization scans, and exact reverse-channel negatives.</verification>
    <residual_risk>The password exists in the selected attach-child environment while that process runs.</residual_risk>
  </threat>
  <threat id="T64-05" category="Elevation of Privilege" component="effective policy" severity="CRITICAL" disposition="mitigate" asvs="V4,V14">
    <mitigation>Execute exact cold/server attestations under the selected environment before task acceptance and discard raw results.</mitigation>
    <verification>Clean plus comprehensive poison/model/accessor/timeout fixtures with zero-side-effect assertions.</verification>
    <residual_risk>Exact 1.14.25 internal semantics require profile re-review before upgrade.</residual_risk>
  </threat>
  <threat id="T64-06" category="Elevation of Privilege" component="silent agent fallback" severity="CRITICAL" disposition="mitigate" asvs="V4">
    <mitigation>Require exact resolved agent/tool evidence and reject known bounded fallback warnings on stderr.</mitigation>
    <verification>Missing/renamed/subagent/override cases in cold and attach plus no raw stderr projection.</verification>
    <residual_risk>Unknown future warning forms are blocked by exact profile pin.</residual_risk>
  </threat>
  <threat id="T64-07" category="Spoofing" component="owned-server policy attestation" severity="CRITICAL" disposition="mitigate" asvs="V13,V14">
    <mitigation>Resolve only the supervisor-owned Basic reference and require bounded exact config/agent assertions through the shared verifier after identity/health validation.</mitigation>
    <verification>Wrong auth/responder/config/agent/document/accessor/timeout cases create no ready lease or task side effect, plus no-OpenCode-import source gates.</verification>
    <residual_risk>Real loopback and provider scheduling behavior remains milestone UAT.</residual_risk>
  </threat>
  <threat id="T64-08" category="Tampering" component="premature result" severity="CRITICAL" disposition="mitigate" asvs="V5">
    <mitigation>Keep candidate private through EOF, stderr, clean exit, tree settlement, and runtime cleanup.</mitigation>
    <verification>Visibility checkpoints and every exit/parser/cleanup/cancel negative with zero result.</verification>
    <residual_risk>Real OS exit/tree timing remains milestone UAT.</residual_risk>
  </threat>
  <threat id="T64-09" category="Information Disclosure" component="reverse-channel failure detail" severity="HIGH" disposition="mitigate" asvs="V7">
    <mitigation>Project only closed normalized events/terminal detail; discard raw attestation/stderr/provider bodies.</mitigation>
    <verification>Sentinel and exact-key reverse-channel/bridge tests.</verification>
    <residual_risk>Closed technical drift codes remain intentionally visible in expandable diagnostics.</residual_risk>
  </threat>
</threat_model>

<verification>
- All supervisor/reverse-channel checks use the preserving wrapper and one fresh compiled graph per command.
- Result visibility is asserted before and after every authoritative gate, not only from the final promise.
- No live credential/model/OpenCode server/browser evidence is counted as automated success.
</verification>

<success_criteria>
- Effective policy and usable native model are proven before browser-control task acceptance.
- Task delivery and settlement are exact-once with no attach-to-cold replay after spawn.
- A successful normalized result is impossible before clean task-process and cleanup truth.
</success_criteria>

<output>
Create `.planning/phases/64-opencode-adapter/64-08-SUMMARY.md` when complete.
</output>
