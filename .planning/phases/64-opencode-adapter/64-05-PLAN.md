---
phase: 64-opencode-adapter
plan: "05"
type: execute
wave: 5
depends_on:
  - "64-04"
files_modified:
  - mcp/src/agent-providers/opencode.ts
  - mcp/src/agent-providers/registry.ts
  - mcp/src/agent-providers/compatibility.ts
  - tests/mcp-agent-provider-contract.test.js
  - tests/mcp-opencode-adapter.test.js
  - tests/mcp-adapter-compatibility.test.js
  - tests/mcp-agent-drift-smoke.test.js
autonomous: true
requirements:
  - MULTI-01
  - MULTI-02
  - MULTI-03
must_haves:
  truths:
    - "[D64-03] The production OpenCode adapter composes detector/profile/Plan-01-parser/tree-kill behind exactly five methods and reports serverMode true, taskMode true, chat/resume false."
    - "[D64-09] Production registry, exact 1.14.25 compatibility row, already-committed fixture manifest, parser contract, and native drift smoke become one exact Claude/OpenCode bijection in one atomic task/commit."
    - "[D64-16] The Plan 01 first-commit OpenCode drift gate is green before edits and remains green after exposure; production registration cannot exist in an intermediate commit without its matrix/parser/fixture/drift contract."
    - "Codex remains absent from production adapter, matrix, fixture, and drift rosters."
  artifacts:
    - path: "mcp/src/agent-providers/opencode.ts"
      provides: "Second production five-method adapter composition"
    - path: "mcp/src/agent-providers/compatibility.ts"
      provides: "Exact two-row compatibility matrix pinned to OpenCode 1.14.25"
    - path: "tests/mcp-agent-drift-smoke.test.js"
      provides: "Closed production registry/matrix/parser/fixture bijection layered on the already-green first-commit drift gate"
  key_links:
    - from: "mcp/src/agent-providers/registry.ts"
      to: "mcp/src/agent-providers/opencode.ts"
      via: "required exact production registration in the same task as matrix/drift exposure"
      pattern: "createOpenCodeAdapter"
    - from: "mcp/src/agent-providers/compatibility.ts"
      to: "tests/fixtures/agent-streams/opencode-1.14.25/manifest.json"
      via: "exact fixtureManifest/profile/native/normalized sequence row"
      pattern: "opencode-1.14.25"
    - from: "tests/mcp-agent-drift-smoke.test.js"
      to: "mcp/src/agent-providers/registry.ts"
      via: "production registry, matrix, parser contract, and manifest ids compared as an exact bijection"
      pattern: "claude-code|opencode"
---

<objective>
Atomically promote the already-green OpenCode parser/profile into the production adapter registry and compatibility matrix with its complete drift bijection.

Purpose: Ensure there is no Phase 64 implementation commit where OpenCode is production-registered without the fixture/parser/matrix/drift gate that has existed since the first implementation commit.
Output: Production five-method adapter composition, exact two-row registry/matrix, and one blocking production-exposure bijection.

<scope_rationale>The production adapter, registry, compatibility row, and exposure-bijection assertions are one indivisible task and commit. Inventory, doctor, and browser-safe projection remain bounded in Plan 06.</scope_rationale>
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/64-opencode-adapter/64-CONTEXT.md
@.planning/phases/64-opencode-adapter/64-RESEARCH.md
@.planning/phases/64-opencode-adapter/64-PATTERNS.md
@.planning/phases/64-opencode-adapter/64-VALIDATION.md
@.planning/phases/64-opencode-adapter/64-01-SUMMARY.md
@.planning/phases/64-opencode-adapter/64-04-SUMMARY.md
@.planning/phases/62-ci-drift-smoke-gate-doctor-extensions/62-01-SUMMARY.md
@.planning/phases/62-ci-drift-smoke-gate-doctor-extensions/62-02-SUMMARY.md
@mcp/src/agent-providers/registry.ts
@mcp/src/agent-providers/compatibility.ts
@mcp/src/agent-providers/opencode-stream.ts
@tests/mcp-agent-drift-smoke.test.js
@tests/fixtures/agent-streams/opencode-1.14.25/manifest.json

<decision_trace>
- D64-03: concrete adapter proves the unchanged five-method boundary.
- D64-09: exact profile/fixture provenance joins the canonical matrix.
- D64-16: production exposure is atomic with the complete fixture/parser/matrix/drift bijection.
- Roadmap success criterion 3: Plan 01 already placed OpenCode in the Phase 62 CI drift job; this plan must preserve that green gate while exposing production registration.
</decision_trace>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>64-05-01: Atomically register OpenCode with its exact matrix and drift bijection</name>
  <files>mcp/src/agent-providers/opencode.ts, mcp/src/agent-providers/registry.ts, mcp/src/agent-providers/compatibility.ts, tests/mcp-agent-provider-contract.test.js, tests/mcp-opencode-adapter.test.js, tests/mcp-adapter-compatibility.test.js, tests/mcp-agent-drift-smoke.test.js</files>
  <read_first>Plan 01 first-commit parser/fixture/drift outputs and green command; Plan 04 detector/profile outputs; claude-code.ts composition; registry exact production constructor; compatibility exact row parser/classifier; Phase 62 matrix/CI summaries</read_first>
  <behavior>
    - "Before any production edit, the Plan 01 two-parser fixture/drift command passes with OpenCode still absent from production registry/matrix."
    - "OpenCode implements exactly detect/buildSpawn/parseEvents/kill/caps and delegates termination to the injected supervisor-owned tree killer."
    - "caps is deeply frozen and exactly taskMode true, chatMode false, resume false, serverMode true."
    - "Production registry requires exactly claude-code then opencode and rejects missing, duplicate, unknown, case-varied, or mutable registrations."
    - "Matrix row opencode has label OpenCode, profile/minimum/testedThrough 1.14.25, major 1, exact capabilities/native fields/fixture path/normalized sequence, and no widened spawn eligibility."
    - "After the production edit, registry ids, matrix ids, parser-contract ids, committed manifest ids, and production adapter ids are exactly claude-code/opencode; every production parser passes its native fixture and negative mutators."
    - "The task is committed once, only after all composition, registry, compatibility, and drift commands pass; it must not be split into registration and matrix/drift commits."
  </behavior>
  <action>First run the exact Plan 01 first-commit drift gate and record it green before changing production exposure. Then write one red production-exposure contract that requires the complete registry/matrix/parser-contract/manifest bijection and exact OpenCode adapter capabilities. Create `opencode.ts` by applying Claude's dependency-injected composition pattern to the Plan 04 detector/profile and Plan 01 parser, with no spawn/filesystem/HTTP/secret/timer authority. In the same task, expand the closed production registry to exact Claude/OpenCode order and append the exact 1.14.25 compatibility row from the committed manifest. Update the existing drift harness only to strengthen its exposure assertion from a pre-registration parser-contract roster to exact production bijection; retain all first-commit positive and native negative cases and the single CI invocation. Reject unknown/case/duplicate/orphan rows and keep Codex absent. Run all tests together, then commit all seven files as one atomic implementation commit. Do not commit or stage a registry-only or matrix-only intermediate state.</action>
  <acceptance_criteria>The Plan 01 gate is green before and after; the production registry has exactly two required adapters; the matrix/parser/fixture/drift rosters are exact and bijective; both production parsers pass native positive/negative contracts; and git history receives one atomic task commit with no registered-without-gate state.</acceptance_criteria>
  <verify>
    <automated>node scripts/run-mcp-build-preserving-workspace.mjs --commands-json '[["node","tests/mcp-agent-provider-contract.test.js"],["node","tests/mcp-opencode-adapter.test.js","--section","composition"],["node","tests/mcp-adapter-compatibility.test.js"],["node","tests/mcp-agent-drift-smoke.test.js"]]'</automated>
  </verify>
  <done>The acceptance criteria above are satisfied, the exact Plan 01 gate remains green, and this task is committed atomically.</done>
</task>

</tasks>

<threat_model>
  <block_on>HIGH,CRITICAL</block_on>
  <assets>
    <asset id="A64-10">Canonical adapter identity and exact production registry completeness.</asset>
    <asset id="A64-11">Pinned fixture/compatibility provenance and literal first-commit CI drift verdict.</asset>
  </assets>
  <trust_boundaries>
    <boundary id="B64-10">Provider implementations enter the closed production registry.</boundary>
    <boundary id="B64-11">Committed parser/fixture/matrix data authorizes production compatibility exposure.</boundary>
  </trust_boundaries>
  <threat id="T64-01" category="Elevation of Privilege" component="adapter registry" severity="HIGH" disposition="mitigate" asvs="V4">
    <mitigation>Require exact two-id registry and five-method composition; reject plugin discovery and arbitrary ids.</mitigation>
    <verification>Registry missing/duplicate/unknown/case tests and adapter authority source scan.</verification>
    <residual_risk>Generic supervisor behavior is completed by Plans 07-08.</residual_risk>
  </threat>
  <threat id="T64-05" category="Tampering" component="profile-to-compatibility binding" severity="HIGH" disposition="mitigate" asvs="V14">
    <mitigation>Bind detector, matrix, parser, fixture, manifest, and source-security profile to exact 1.14.25 in the same task.</mitigation>
    <verification>Exact five-roster bijection and version-boundary tests.</verification>
    <residual_risk>Newer releases remain observationally Degraded but spawn-blocked.</residual_risk>
  </threat>
  <threat id="T64-09" category="Tampering" component="fixture drift gate" severity="HIGH" disposition="mitigate" asvs="V5">
    <mitigation>Retain the first-commit two-parser CI gate and make production exposure atomic with exact registry/matrix/parser/manifest equality.</mitigation>
    <verification>Pre-edit and post-edit drift runs, adapter-native mutators, exact rosters, and atomic task boundary.</verification>
    <residual_risk>Live provenance remains human_needed and does not weaken CI.</residual_risk>
  </threat>
</threat_model>

<verification>
- Plan 01 drift gate passes before production exposure and again in this task's combined command.
- Registry/matrix/parser-contract/manifest equality is mechanical, not count-inferred.
- No separate registration-only task or commit exists.
</verification>

<success_criteria>
- OpenCode is a required production adapter with exact server-mode capabilities.
- There has never been a Phase 64 implementation commit with production OpenCode registration but no drift gate.
- Plan 06 receives one exact roster/classifier contract for safe projection.
</success_criteria>

<output>
Create `.planning/phases/64-opencode-adapter/64-05-SUMMARY.md` when complete.
</output>
