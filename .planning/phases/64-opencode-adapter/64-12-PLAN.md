---
phase: 64-opencode-adapter
plan: "12"
type: execute
wave: 9
depends_on:
  - "64-08"
  - "64-10"
  - "64-11"
files_modified:
  - extension/ui/sidepanel.html
  - extension/ui/options.js
  - extension/ui/sidepanel.js
  - extension/ui/delegation-feed.js
  - tests/providers-panel-ui.test.js
  - tests/delegation-sidepanel-ui.test.js
  - tests/delegation-event-store.test.js
autonomous: true
requirements:
  - MULTI-01
  - MULTI-02
  - MULTI-03
must_haves:
  truths:
    - "[D64-13] OpenCode uses the existing consent/feed/control/summary components; only nonvisual local helper script wiring may change sidepanel HTML, with no new visible structure, CSS, layout, card, renderer, or provider-specific state branch."
    - "[D64-14] The run summary shows `Billing not reported`; no subscription, dollar, free, unlimited, or inferred account/provider claim appears."
    - "[D64-01/D64-15] Cold, verified attach, and pre-task attach fallback are visually identical and produce one consent/start/feed; post-accept failure produces one failed terminal and no replay affordance."
    - "[D64-12] No success/result card renders before the authoritative terminal barrier; drift/nonzero/signal/missing/duplicate/post-terminal outcomes remain one failed state."
    - "[D64-16] Existing responsive/theme/forced-colors/reduced-motion/focus/live-region/hydration-silence contracts remain source- and DOM-tested."
  artifacts:
    - path: "extension/ui/sidepanel.js"
      provides: "Provider-label-driven existing consent and lifecycle surface"
    - path: "extension/ui/delegation-feed.js"
      provides: "Provider-neutral OpenCode feed and honest unknown-billing summary"
  key_links:
    - from: "extension/ui/sidepanel.js"
      to: "extension/background.js"
      via: "provider-free intent and canonical preflight/consent/snapshot responses"
      pattern: "FSB_DELEGATION_START"
    - from: "extension/ui/delegation-feed.js"
      to: "extension/utils/delegation-controller.js"
      via: "closed canonical snapshot only"
      pattern: "snapshot.provider"
---

<objective>
Project OpenCode through the approved existing delegated side-panel and feed UI with data-only changes.

Purpose: Users should receive the identical consent, lifecycle, feed, control, and summary UX while cold/attach mechanics and provider-native data remain invisible.
Output: Provider-label-driven consent/lifecycle copy and one provider-neutral feed/unknown-billing summary with no new visible markup or CSS.

<scope_rationale>This plan is the delegated side-panel/feed half of the UI-surface split. The Providers row is complete in Plan 11; this plan is sequenced after authoritative daemon, persistence, and Providers helper wiring so transient candidates or topology state cannot become visible.</scope_rationale>
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/64-opencode-adapter/64-CONTEXT.md
@.planning/phases/64-opencode-adapter/64-RESEARCH.md
@.planning/phases/64-opencode-adapter/64-PATTERNS.md
@.planning/phases/64-opencode-adapter/64-UI-SPEC.md
@.planning/phases/61-delegation-ux-sw-eviction-persistence/61-07-SUMMARY.md
@.planning/phases/61-delegation-ux-sw-eviction-persistence/61-08-SUMMARY.md
@extension/utils/delegation-providers.js
@extension/ui/options.js
@extension/ui/sidepanel.js
@extension/ui/delegation-feed.js

<decision_trace>
- D64-13: existing provider-neutral consent/lifecycle/feed/summary UI only.
- D64-14: exact unknown/Not reported run-summary billing copy.
- D64-01/D64-15: topology/fallback remain invisible and cannot duplicate UI state.
- D64-12: success presentation waits for supervisor-authoritative terminal truth.
- D64-16: Providers/feed/sidepanel/accessibility/source-security coverage is blocking.
</decision_trace>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>64-12-01: Data-drive the existing consent and side-panel lifecycle with canonical labels</name>
  <files>extension/ui/sidepanel.html, extension/ui/sidepanel.js, extension/ui/options.js, tests/delegation-sidepanel-ui.test.js, tests/providers-panel-ui.test.js</files>
  <read_first>64-UI-SPEC Delegated side panel, copy, lifecycle, accessibility, responsive/theme/motion locks; sidepanel response validators/consent/lifecycle/preflight/start functions and hard-coded Claude copy; options trust reset; canonical provider helper and background response schemas</read_first>
  <behavior>
    - "Canonical OpenCode responses render `Let OpenCode control this browser?`, allowed/forbidden scope, `Allow & start OpenCode`, active/background labels, control, stop, failure, and retry through existing nodes/actions."
    - "Preflight/consent/trust/snapshot validators accept exact canonical Claude/OpenCode pairs and reject mismatches; trust request uses the consent response provider, while start request stays provider-free."
    - "No `if (opencode)` renderer/state branch, second card, topology state, server spinner, model/provider picker, continuation/resume-session control, or auto-replay path exists."
    - "One pre-task fallback still has one message/consent/start/card; post-accept failure has one failure and preserves composer/retry-as-new-intent behavior."
    - "Existing focus order, 44px targets, <=350px stacking, forced-colors, reduced-motion, and hydration silence remain unchanged."
  </behavior>
  <action>Add canonical OpenCode response/lifecycle snapshots and mismatch/replay/focus/a11y/source negatives before editing. Add exactly one local `../utils/delegation-providers.js` script dependency before `delegation-feed.js`/`sidepanel.js` in `sidepanel.html`, with no visible node or CSS change. Replace hard-coded Claude response validation and visible lifecycle strings with provider labels already supplied by validated preflight/consent/snapshot state and checked by the canonical helper; use textContent/createTextNode only. Bind trust set/clear to the canonical selected/consent provider rather than a constant, but do not add providerId to FSB_DELEGATION_START or trust provider-supplied labels. Reuse every existing card/control/error/announcement path and existing CSS/HTML structure. Generalize provider-specific sentences with exact UI-SPEC patterns, preserving Claude output and no automatic retry.</action>
  <acceptance_criteria>Claude and OpenCode use the same DOM classes/semantics/actions with only canonical label substitution; the helper loads before both UI consumers; exact OpenCode copy and provider-bound trust pass; invalid identity fails closed; cold/attach/fallback never changes visible structure or duplicates state; no new CSS/provider renderer/continuation/topology path exists.</acceptance_criteria>
  <verify>
    <automated>node tests/delegation-sidepanel-ui.test.js --section opencode-lifecycle && node tests/providers-panel-ui.test.js --section delegation-trust</automated>
  </verify>
  <done>The acceptance criteria above are satisfied and the automated verification passes.</done>
</task>

<task type="auto" tdd="true">
  <name>64-12-02: Render the same normalized feed and authoritative unknown-billing summary</name>
  <files>extension/ui/delegation-feed.js, tests/delegation-sidepanel-ui.test.js, tests/delegation-event-store.test.js</files>
  <read_first>delegation-feed.js exact client/snapshot/entry/summary validators, text-only DOM creation, billing text, announce sequencing; Plan 10 controller/store snapshots; Plan 08 candidate-result barrier; UI-SPEC feed/terminal/billing/security acceptance</read_first>
  <behavior>
    - "Feed accepts an exact OpenCode init/snapshot through the same closed canonical identity validator and renders the same classes, semantic articles, definition lists, details, order, and controls as Claude."
    - "OpenCode result summary renders `Billing not reported`; subscription/dollar/free/unlimited text is impossible for billingKind unknown."
    - "Cold success and verified-attach success have identical DOM snapshots; pre-task fallback yields one feed; post-accept/drift/nonzero/signal/missing/duplicate/trailing failure yields zero success summary and one failed terminal."
    - "Strictly newer matching sequences announce once; hydration is silent and older/duplicate/mismatched provider events do not render or announce."
    - "Raw native names/JSON, task, endpoint/port/secret/path/version/model/auth/provider metadata never become text, attributes, classes, HTML, or style."
  </behavior>
  <action>Write provider-neutral feed/snapshot comparisons first using the same normalized lifecycle fixtures for Claude and OpenCode, plus cold/attach/fallback/terminal-negative, sequence/hydration, billing, hostile text/field, and source scans. Replace single-Claude client/provider validation with the canonical helper while retaining exact snapshot/entry schemas. Make billing text depend only on the closed stored billing kind; unknown maps exactly to `Billing not reported`. Keep createElement/textContent/createTextNode and existing classes/semantics; do not branch on OpenCode or expose raw/topology fields. Ensure result rows cannot render a successful summary without the controller's completed terminal state.</action>
  <acceptance_criteria>Equivalent Claude/OpenCode normalized inputs produce structurally identical safe feed DOM, OpenCode shows only Billing not reported, all candidate/exit/drift failures show zero success, hydration/sequence announcements remain correct, and sentinel/native/topology/version/secret content never reaches presentation.</acceptance_criteria>
  <verify>
    <automated>node tests/delegation-sidepanel-ui.test.js && node tests/delegation-event-store.test.js</automated>
  </verify>
  <done>The acceptance criteria above are satisfied and the automated verification passes.</done>
</task>

</tasks>

<threat_model>
  <block_on>HIGH,CRITICAL</block_on>
  <assets>
    <asset id="A64-25">Consent comprehension and unchanged delegated interaction semantics.</asset>
    <asset id="A64-26">Honest compatibility/auth/billing and authoritative success presentation.</asset>
    <asset id="A64-27">DOM/storage confidentiality against native task/server/provider data.</asset>
  </assets>
  <trust_boundaries>
    <boundary id="B64-25">Background-validated provider/snapshot data enters delegated DOM rendering.</boundary>
    <boundary id="B64-26">Persisted result/terminal state becomes a visible success/failure summary.</boundary>
    <boundary id="B64-27">User UI intent returns to background authorization without adapter authority.</boundary>
  </trust_boundaries>
  <threat id="T64-04" category="Tampering" component="UI replay" severity="HIGH" disposition="mitigate" asvs="V3">
    <mitigation>Keep start request provider-free, render only accepted state, and provide no automatic post-accept fallback/retry path.</mitigation>
    <verification>Cold/attach/fallback/post-accept DOM and call-count snapshots.</verification>
    <residual_risk>User-clicked Try message again is intentionally a new explicit action.</residual_risk>
  </threat>
  <threat id="T64-08" category="Tampering" component="premature success UI" severity="CRITICAL" disposition="mitigate" asvs="V5">
    <mitigation>Require completed controller terminal before successful summary; result entries alone remain running.</mitigation>
    <verification>Candidate/delayed-exit/drift/nonzero/signal/missing/duplicate/trailing snapshots with zero success.</verification>
    <residual_risk>Live rendering timing remains milestone human evidence.</residual_risk>
  </threat>
  <threat id="T64-10" category="Information Disclosure" component="Providers and side-panel DOM" severity="CRITICAL" disposition="mitigate" asvs="V7">
    <mitigation>Consume only closed canonical view models via text-only existing components; forbid native/version/topology/secret/model/auth/task data and dynamic HTML/style.</mitigation>
    <verification>Exact-key/sentinel/source/DOM scans plus no-new-HTML/CSS structure assertions.</verification>
    <residual_risk>Canonical provider name/status and user-approved task lifecycle are intentionally visible.</residual_risk>
  </threat>
</threat_model>

<verification>
- UI tests cover exact approved delegated copy, no-structure/CSS delta, provider parity, terminal truth, responsive/theme/a11y source locks, and safe rendering.
- No screenshot/live browser/assistive-technology result is fabricated; those remain in Plan 13's pending ledger.
- Existing seven API providers, Claude UX, and Codex Unsupported state remain regression-green.
</verification>

<success_criteria>
- Delegated OpenCode work uses the exact existing consent/feed/control/summary UX with honest unknown billing.
- Cold/attach mechanics, raw native events, credentials, and premature candidates remain invisible.
</success_criteria>

<output>
Create `.planning/phases/64-opencode-adapter/64-12-SUMMARY.md` when complete.
</output>
