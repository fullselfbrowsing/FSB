---
phase: 64-opencode-adapter
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - mcp/src/agent-providers/adapter.ts
  - mcp/src/agent-providers/protocol-drift.ts
  - mcp/src/agent-providers/claude-stream.ts
  - mcp/src/agent-providers/opencode-stream.ts
  - tests/mcp-opencode-adapter.test.js
  - tests/mcp-agent-stream-fixture.test.js
  - tests/mcp-agent-drift-smoke.test.js
  - tests/fixtures/agent-streams/opencode-1.14.25/manifest.json
  - tests/fixtures/agent-streams/opencode-1.14.25/contract-stream.jsonl
autonomous: true
requirements:
  - MULTI-03
must_haves:
  truths:
    - "This sole Wave-1 task is the literal first Phase 64 implementation task and atomic commit; no earlier Phase 64 implementation task may modify production code."
    - "[D64-09] The first implementation commit already contains a sanitized schema-derived OpenCode 1.14.25 fixture with liveCapturePending true and a source-pinned production parser; no live CLI, account, network, or browser is used."
    - "[D64-10/D64-11] The parser emits the reviewed multi-step normalized sequence, rejects every malformed, mixed-session, reordered, duplicate, overflow, provider-error, and post-candidate case with bounded agent_protocol_drift detail, and emits zero result on every negative."
    - "The existing Phase 62 CI drift-smoke command covers both Claude and OpenCode native fixture contracts in this same first commit even though OpenCode is not yet production-registered."
    - "Production registry and compatibility exposure remain Claude-only until Plan 05 atomically promotes the already-green OpenCode parser/fixture/drift contract."
  artifacts:
    - path: "mcp/src/agent-providers/opencode-stream.ts"
      provides: "Strict source-derived multi-step OpenCode 1.14.25 JSONL state machine"
    - path: "tests/fixtures/agent-streams/opencode-1.14.25/manifest.json"
      provides: "Exact sanitized schema-derived provenance with liveCapturePending true"
    - path: "tests/mcp-agent-drift-smoke.test.js"
      provides: "Generalized closed Claude/OpenCode adapter-native fixture contract executed by the existing Phase 62 CI job"
  key_links:
    - from: "tests/fixtures/agent-streams/opencode-1.14.25/contract-stream.jsonl"
      to: "mcp/src/agent-providers/opencode-stream.ts"
      via: "production parser consumes the committed native grammar"
      pattern: "step_start|tool_use|step_finish"
    - from: "mcp/src/agent-providers/opencode-stream.ts"
      to: "mcp/src/agent-providers/protocol-drift.ts"
      via: "provider-neutral bounded drift error shared with Claude"
      pattern: "AgentProtocolDriftError"
    - from: "tests/mcp-agent-drift-smoke.test.js"
      to: ".github/workflows/ci.yml"
      via: "the already-present single Phase 62 drift-smoke CI invocation executes the generalized two-parser contract"
      pattern: "mcp-agent-drift-smoke.test.js"
---

<objective>
Land the complete OpenCode parser, honest 1.14.25 fixture, and generalized adapter-native CI drift gate as the first implementation commit of Phase 64.

Purpose: Satisfy the roadmap's literal first-commit criterion before any topology, runtime, profile, registration, supervisor, browser, or UI production change can land.
Output: One provider-neutral drift module, two strict production parsers, one sanitized OpenCode fixture, native negative coverage, and one existing CI job that already exercises OpenCode.

<scope_rationale>The roadmap requires this entire protocol gate in one first atomic implementation commit. This single task intentionally groups the parser, fixture, shared drift mechanics, and generalized CI harness that must turn green together; every other Phase 64 plan depends transitively on it.</scope_rationale>
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/64-opencode-adapter/64-CONTEXT.md
@.planning/phases/64-opencode-adapter/64-RESEARCH.md
@.planning/phases/64-opencode-adapter/64-PATTERNS.md
@.planning/phases/64-opencode-adapter/64-VALIDATION.md
@.planning/phases/60-adapter-contract-claude-code-mvp/60-02-SUMMARY.md
@.planning/phases/62-ci-drift-smoke-gate-doctor-extensions/62-01-SUMMARY.md
@mcp/src/agent-providers/adapter.ts
@mcp/src/agent-providers/claude-stream.ts
@tests/mcp-agent-drift-smoke.test.js
@tests/mcp-agent-stream-fixture.test.js
@tests/fixtures/agent-streams/claude-code-2.1.177/manifest.json
@tests/fixtures/agent-streams/claude-code-2.1.177/contract-stream.jsonl
@.github/workflows/ci.yml

<decision_trace>
- D64-09: exact 1.14.25 schema-derived fixture with honest pending live provenance.
- D64-10: fixed provider-native to normalized-event mapping across multiple steps.
- D64-11: strict shape/session/order/terminal drift behavior.
- D64-12: a parsed finish is a candidate, never authoritative process success.
- Roadmap success criterion 3: OpenCode belongs to the Phase 62 drift-smoke job from the first Phase 64 implementation commit.
</decision_trace>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>64-01-01: Land the complete first-commit OpenCode parser, fixture, and CI drift gate</name>
  <files>mcp/src/agent-providers/adapter.ts, mcp/src/agent-providers/protocol-drift.ts, mcp/src/agent-providers/claude-stream.ts, mcp/src/agent-providers/opencode-stream.ts, tests/mcp-opencode-adapter.test.js, tests/mcp-agent-stream-fixture.test.js, tests/mcp-agent-drift-smoke.test.js, tests/fixtures/agent-streams/opencode-1.14.25/manifest.json, tests/fixtures/agent-streams/opencode-1.14.25/contract-stream.jsonl</files>
  <read_first>64-RESEARCH.md Patterns 6-7, exact pinned run.ts/message-v2.ts/prompt.ts sources, and first-commit requirement; Claude parser and fixture complete implementations; Phase 62 drift-smoke fixture selectors/mutators and its exact CI invocation</read_first>
  <behavior>
    - "Add opencode only to the closed provider-id type needed by shared drift detail; do not register it in the production adapter registry or compatibility matrix in this task."
    - "Extract one provider-neutral AgentProtocolDriftError, bounds, exact-own-data helpers, and immutable per-provider reason vocabulary while preserving every existing Claude normalized event and reason."
    - "Every OpenCode line is fatal-UTF-8 decoded, at most 256 KiB, strict JSON, exact own data, one of the pinned 1.14.25 native shapes, and part/session/id/call/time/cost/token/cache fields are bounded."
    - "The first step_start emits init; later starts advance state; completed/error tool records emit adjacent tool_use/tool_result; text emits assistant; optional reasoning emits assistant_delta; continuation finishes tool-calls/unknown emit no result; the first other valid finish retains one candidate and only EOF may follow."
    - "The three-step fixture emits exactly init, assistant_delta, assistant, tool_use, tool_result, tool_use, tool_result, assistant, result and is sanitized, schema-derived-contract, profileVersion 1.14.25, recorded status human_needed, and liveCapturePending true."
    - "The generalized immutable fixture-contract table contains exactly claude-code and opencode, each with native selectors, required fields, expected sequence, provenance rule, terminal rule, and negative mutators; both invoke their production parsers."
    - "Negative coverage includes invalid UTF-8/JSON, oversized line/stream, missing/extra nested fields, event before start, illegal order, mixed session, duplicate ids/calls/terminal, counter overflow, provider error, continuation at EOF, and data after candidate; every case yields bounded closed drift and zero result."
    - "The existing named CI drift-smoke command occurs exactly once and therefore executes this generalized two-parser contract in the same atomic commit; no live CLI/network/account is introduced."
  </behavior>
  <action>Write the complete first-commit red contract before production edits: require the exact Claude/OpenCode fixture-contract roster, the existing single CI invocation, honest OpenCode provenance, production-parser positive sequences, byte-boundary invariance, and all native negative cases. Add canonical `opencode` only to the shared closed provider-id type while leaving production registry/matrix exposure absent. Extract the reusable bounded drift class/helpers from `claude-stream.ts` into `protocol-drift.ts`, preserve all Claude behavior, and add only the reviewed OpenCode reasons `counter_overflow`, `duplicate_id`, `invalid_order`, `provider_error`, and `stream_too_large`. Implement `opencode-stream.ts` from the pinned 1.14.25 source schemas: one immutable session, step/message/part/call uniqueness, bounded counters, exact transition state, one held candidate, and no raw provider metadata/error body above the parser. Create the deterministic three-step sanitized fixture and exact manifest with source URLs, `schema-derived-contract`, `human_needed`, and `liveCapturePending: true`. Generalize the existing Phase 62 drift smoke into a closed adapter-native fixture table that runs both production parsers without pretending OpenCode has Claude labels; at this pre-registration boundary explicitly assert the production registry/matrix are still Claude-only so no exposure is smuggled into the first commit. Tighten parser/schema code until every negative fails for its reviewed reason with zero result. Commit this task atomically as the first Phase 64 implementation commit; do not split parser, fixture, drift harness, or CI contract into separate commits.</action>
  <acceptance_criteria>The first Phase 64 implementation commit alone contains a complete green OpenCode 1.14.25 production parser, honest schema-derived fixture, shared bounded drift contract, two-parser adapter-native drift smoke, exact existing CI invocation, and comprehensive negative drift cases; OpenCode remains absent from production registry/matrix exposure until Plan 05.</acceptance_criteria>
  <verify>
    <automated>node scripts/run-mcp-build-preserving-workspace.mjs --commands-json '[["node","tests/mcp-opencode-adapter.test.js","--section","first-commit-drift-gate"],["node","tests/mcp-agent-stream-fixture.test.js"],["node","tests/mcp-agent-drift-smoke.test.js"]]'</automated>
  </verify>
  <done>The acceptance criteria above are satisfied, the automated command passes, and this task is committed as the first Phase 64 implementation commit.</done>
</task>

</tasks>

<threat_model>
  <block_on>HIGH,CRITICAL</block_on>
  <assets>
    <asset id="A64-07">Normalized chronology, session identity, and candidate terminal truth.</asset>
    <asset id="A64-08">Sanitized offline fixture provenance and literal first-commit drift coverage.</asset>
    <asset id="A64-09">Memory/CPU bounds and confidentiality at both native JSONL boundaries.</asset>
  </assets>
  <trust_boundaries>
    <boundary id="B64-07">Arbitrary provider stdout bytes enter strict UTF-8/JSON/schema parsing.</boundary>
    <boundary id="B64-08">Checked-in schema-derived artifacts authorize an offline CI compatibility contract.</boundary>
  </trust_boundaries>
  <threat id="T64-08" category="Tampering" component="terminal candidate" severity="CRITICAL" disposition="mitigate" asvs="V5">
    <mitigation>Recognize only reviewed continuation reasons, retain exactly one other finish as a candidate, and emit no authoritative process success in the parser.</mitigation>
    <verification>Three-step fixture plus missing/duplicate/post-candidate/provider-error matrices with zero result on every negative.</verification>
    <residual_risk>Clean-exit/tree corroboration is completed by Plan 08.</residual_risk>
  </threat>
  <threat id="T64-09" category="Denial of Service" component="native parser and CI drift gate" severity="HIGH" disposition="mitigate" asvs="V5">
    <mitigation>Use fatal UTF-8, line/stream/count/string caps, exact schemas, closed reasons, one session, ordered state, and adapter-native mutators in the literal first implementation commit.</mitigation>
    <verification>Byte-split, oversize, malformed, accessor/prototype, order, session/id/counter, terminal, roster, and CI occurrence tests.</verification>
    <residual_risk>A future OpenCode profile stays spawn-blocked until reviewed.</residual_risk>
  </threat>
  <threat id="T64-10" category="Information Disclosure" component="fixture and diagnostics" severity="HIGH" disposition="mitigate" asvs="V7">
    <mitigation>Use synthetic sanitized fixture values and project only fixed normalized fields and bounded closed drift detail.</mitigation>
    <verification>Path, URL, email, credential, task, card, raw-error, accessor, and serialization sentinel scans.</verification>
    <residual_risk>Genuine provenance comparison remains human_needed.</residual_risk>
  </threat>
</threat_model>

<verification>
- This is the sole Wave-1 plan and contains exactly one atomic implementation task.
- All other Phase 64 plans depend transitively on this task, so no production change can precede the green OpenCode drift gate.
- The workspace-preserving wrapper proves compiled checks restore generated/user state.
</verification>

<success_criteria>
- The literal first implementation commit satisfies Roadmap success criterion 3 without reinterpretation.
- OpenCode parser/fixture drift is blocking in the existing Phase 62 CI job before production registration.
- Live provenance remains honestly pending.
</success_criteria>

<output>
Create `.planning/phases/64-opencode-adapter/64-01-SUMMARY.md` when complete.
</output>
