---
phase: 64-opencode-adapter
plan: "06"
type: execute
wave: 6
depends_on:
  - "64-05"
files_modified:
  - mcp/src/agent-providers/serve-delegation.ts
  - mcp/src/client-inventory.ts
  - mcp/src/diagnostics.ts
  - extension/utils/mcp-agent-providers.js
  - tests/mcp-adapter-compatibility.test.js
  - tests/mcp-agent-providers-storage.test.js
  - tests/mcp-client-inventory.test.js
  - tests/mcp-diagnostics-status.test.js
autonomous: true
requirements:
  - MULTI-01
  - MULTI-02
  - MULTI-03
must_haves:
  truths:
    - "[D64-09] Inventory, doctor, safe daemon compatibility projection, and shipped browser compatibility roster consume the exact two-provider registry/matrix contract from Plan 05 without inventing a second version policy."
    - "[D64-13] The existing dormant OpenCode Providers row becomes shippable only from validated daemon evidence; Codex remains unshipped and compatibility never selects or recommends a provider."
    - "[D64-14] OpenCode auth remains unknown and no billing claim, executable path, raw version policy, provider/model/config body, or diagnostic text crosses into extension storage."
    - "[D64-16] Installed/missing/malformed/newer evidence is deterministic across inventory, doctor, daemon compatibility, and browser-safe storage, with roster mismatch failing closed."
  artifacts:
    - path: "mcp/src/client-inventory.ts"
      provides: "Shared bounded OpenCode installation evidence without browser-local path/version leakage"
    - path: "mcp/src/diagnostics.ts"
      provides: "Local two-provider doctor evidence with OpenCode auth unknown"
    - path: "mcp/src/agent-providers/serve-delegation.ts"
      provides: "Closed browser-safe two-row compatibility projection"
    - path: "extension/utils/mcp-agent-providers.js"
      provides: "Descriptor-safe shipped compatibility storage for Claude and OpenCode while Codex stays unshipped"
  key_links:
    - from: "mcp/src/client-inventory.ts"
      to: "mcp/src/agent-providers/opencode-detect.ts"
      via: "shared retained bounded executable probe semantics"
      pattern: "opencode"
    - from: "mcp/src/diagnostics.ts"
      to: "mcp/src/agent-providers/compatibility.ts"
      via: "exact registry/matrix iteration and local-only evidence"
      pattern: "ADAPTER_COMPATIBILITY_MATRIX"
    - from: "mcp/src/agent-providers/serve-delegation.ts"
      to: "extension/utils/mcp-agent-providers.js"
      via: "sanitized schemaVersion/checkedAt/status/reason rows only"
      pattern: "adapter.compatibility"
---

<objective>
Project the exact OpenCode registry/matrix result into local inventory/doctor and the existing browser-safe compatibility plane.

Purpose: Keep executable facts useful to the local operator while exposing only closed observational status to the extension, with no selection, billing, or spawn authority.
Output: Consistent two-provider inventory/doctor evidence and a descriptor-safe Claude/OpenCode browser compatibility snapshot.

<scope_rationale>This plan is the evidence-projection half of the registry/projection split. Task 64-06-01 owns local inventory/doctor facts; Task 64-06-02 owns the daemon-to-browser sanitization boundary. Neither task composes adapters or changes parser fixtures.</scope_rationale>
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/64-opencode-adapter/64-CONTEXT.md
@.planning/phases/64-opencode-adapter/64-RESEARCH.md
@.planning/phases/64-opencode-adapter/64-PATTERNS.md
@.planning/phases/64-opencode-adapter/64-UI-SPEC.md
@.planning/phases/64-opencode-adapter/64-VALIDATION.md
@.planning/phases/62-ci-drift-smoke-gate-doctor-extensions/62-02-SUMMARY.md
@.planning/phases/62-ci-drift-smoke-gate-doctor-extensions/62-03-SUMMARY.md
@mcp/src/agent-providers/opencode-detect.ts
@mcp/src/agent-providers/compatibility.ts
@mcp/src/agent-providers/serve-delegation.ts
@mcp/src/client-inventory.ts
@mcp/src/diagnostics.ts
@extension/utils/mcp-agent-providers.js

<decision_trace>
- D64-09: exact pinned profile/fixture evidence remains the sole compatibility source.
- D64-13: promote only the existing OpenCode row; Codex remains unshipped and evidence observational.
- D64-14: auth/billing stay unknown/not reported and local/native fields stop before extension storage.
- D64-16: inventory, doctor, projection, and storage all gain blocking two-provider coverage.
</decision_trace>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>64-06-01: Reuse retained OpenCode detection across inventory and local doctor</name>
  <files>mcp/src/client-inventory.ts, mcp/src/diagnostics.ts, tests/mcp-client-inventory.test.js, tests/mcp-diagnostics-status.test.js</files>
  <read_first>Plan 05 exact registry/matrix output; opencode-detect.ts retained probe API; client-inventory.ts Claude version-probe branch and platform projection; diagnostics.ts adapter roster collection and local JSON/human output; UI-SPEC evidence-plane/security locks</read_first>
  <behavior>
    - "Inventory detects OpenCode availability from the shared bounded retained-probe semantics rather than a competing PATH/version implementation."
    - "Inventory browser-bound data contains no executable path, version, auth, model, config, native body, or diagnostic text."
    - "Doctor iterates both exact registry/matrix rows and may show bounded local path/version/status/reason while OpenCode auth remains unknown and no credential/config body is read or printed."
    - "Installed, missing, malformed, changed-identity, exact-profile, and newer evidence classify consistently and roster mismatch fails closed."
  </behavior>
  <action>Extend inventory and doctor tests first with installed/missing/malformed/changed/exact/newer OpenCode evidence, accessor/prototype inputs, roster mismatch, and path/version/auth/model/config/error sentinels. Reuse the low-level retained OpenCode detector/probe semantics instead of adding a second PATH or version parser. Strip local path/version and every native/provider field before inventory data crosses its browser-facing boundary. Verify diagnostics is already registry-driven and modify only concrete single-provider seams exposed by red tests. Local doctor may retain bounded operator-only binary/version/status/reason evidence, but must leave auth unknown, never inspect credentials/config/model bodies, and never infer billing or spawn readiness from inventory.</action>
  <acceptance_criteria>Inventory and doctor agree with exact Plan 05 detection/matrix evidence for all positive and negative cases; browser-bound inventory contains no local/native/provider fields; local doctor stays bounded/read-only with auth unknown; roster mismatch and malformed evidence fail closed.</acceptance_criteria>
  <verify>
    <automated>node scripts/run-mcp-build-preserving-workspace.mjs --commands-json '[["node","tests/mcp-client-inventory.test.js"],["node","tests/mcp-diagnostics-status.test.js"]]'</automated>
  </verify>
  <done>The acceptance criteria above are satisfied and the automated verification passes.</done>
</task>

<task type="auto" tdd="true">
  <name>64-06-02: Close the browser-safe compatibility projection and shipped roster</name>
  <files>mcp/src/agent-providers/serve-delegation.ts, extension/utils/mcp-agent-providers.js, tests/mcp-adapter-compatibility.test.js, tests/mcp-agent-providers-storage.test.js</files>
  <read_first>Plan 05 exact registry/matrix/fixture roster; serve-delegation compatibility collector and exact empty request; mcp-agent-providers descriptor-safe snapshot parser/shipped labels; Phase 62 storage and compatibility contracts; UI-SPEC no-version/no-authority locks</read_first>
  <behavior>
    - "adapter.compatibility returns exactly schemaVersion, checkedAt, and two safe status rows for claude-code/opencode; malformed/missing detection yields closed Unsupported evidence rather than omission."
    - "The extension's shipped compatibility roster is exactly Claude/OpenCode while Codex remains adapter_unshipped/Unsupported."
    - "Storage contains no executable path, raw version or semver policy, auth, billing, model, config, native event/body, diagnostic message, topology, endpoint, port, or secret."
    - "Compatibility remains observational and cannot select a provider, change recommendation, grant spawn, mutate settings, or mark state dirty."
  </behavior>
  <action>Write exact safe-projection, storage, malformed-row, duplicate/orphan/case-variant, Codex-unshipped, accessor/prototype, and sentinel assertions first. Extend only the closed shipped-compatibility membership to OpenCode and keep the safe snapshot grammar unchanged: schema version, checked timestamp, canonical id, closed status/reason, and already-approved bounded capability/provenance facts only. Keep detector/version/path detail inside the daemon classifier. Verify `serve-delegation.ts` is registry/matrix-driven; alter only concrete one-provider seams exposed by tests. Mechanically compare registry, matrix, daemon projection, and browser shipped rows as a closed bijection, while explicitly excluding Codex. Add source negatives proving compatibility cannot call selection/recommendation/save/spawn code or introduce browser semver/native fields.</action>
  <acceptance_criteria>Exactly Claude/OpenCode safe rows round-trip and every malformed/orphan/unknown/Codex row fails closed; browser storage contains no local/native/auth/billing/topology evidence; compatibility changes no selection/recommendation/settings state; the safe roster is mechanically tied to Plan 05.</acceptance_criteria>
  <verify>
    <automated>node scripts/run-mcp-build-preserving-workspace.mjs --commands-json '[["node","tests/mcp-adapter-compatibility.test.js"],["node","tests/mcp-agent-providers-storage.test.js"]]'</automated>
  </verify>
  <done>The acceptance criteria above are satisfied and the automated verification passes.</done>
</task>

</tasks>

<threat_model>
  <block_on>HIGH,CRITICAL</block_on>
  <assets>
    <asset id="A64-12">Local executable evidence versus browser-safe provider status.</asset>
    <asset id="A64-31">Closed shipped compatibility roster and observational-only authority.</asset>
  </assets>
  <trust_boundaries>
    <boundary id="B64-12">Local path/version/detection evidence crosses toward inventory and extension storage.</boundary>
    <boundary id="B64-31">Daemon compatibility rows cross the reverse channel into durable browser state.</boundary>
  </trust_boundaries>
  <threat id="T64-05" category="Tampering" component="profile-to-projection binding" severity="HIGH" disposition="mitigate" asvs="V14">
    <mitigation>Derive all classifications from the exact Plan 05 registry/matrix profile and fail closed on roster or evidence disagreement.</mitigation>
    <verification>Exact/exact-newer/missing/malformed/changed identity and roster-bijection tests.</verification>
    <residual_risk>Newer releases remain observationally Degraded and spawn-blocked until a reviewed profile exists.</residual_risk>
  </threat>
  <threat id="T64-10" category="Information Disclosure" component="doctor and browser evidence" severity="CRITICAL" disposition="mitigate" asvs="V7">
    <mitigation>Separate local doctor fields from a closed browser status projection and strip path/version/native/auth/model/config/error/topology/billing data before storage.</mitigation>
    <verification>Exact-key/accessor/prototype/sentinel projection and storage tests plus observational-authority source scans.</verification>
    <residual_risk>Local doctor intentionally shows bounded local executable facts to its local operator.</residual_risk>
  </threat>
</threat_model>

<verification>
- Inventory and doctor use the retained OpenCode detector semantics from Plan 04 and exact matrix from Plan 05.
- The browser receives a closed status projection only, with Codex still unshipped and no selection/recommendation mutation.
- Every compiled check uses the workspace-preserving wrapper and leaves generated/user state untouched.
</verification>

<success_criteria>
- OpenCode local evidence is consistent across detector, inventory, doctor, and compatibility classification.
- The browser-safe roster contains exactly Claude/OpenCode without leaking path/version/native/auth/billing/topology data.
- Evidence remains read-only and cannot select, recommend, save, or spawn a provider.
</success_criteria>

<output>
Create `.planning/phases/64-opencode-adapter/64-06-SUMMARY.md` when complete.
</output>
