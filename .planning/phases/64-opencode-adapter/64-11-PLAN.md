---
phase: 64-opencode-adapter
plan: "11"
type: execute
wave: 8
depends_on:
  - "64-06"
  - "64-09"
files_modified:
  - extension/ui/control_panel.html
  - extension/ui/providers-panel.js
  - extension/ui/options.js
  - tests/providers-panel-logic.test.js
  - tests/providers-panel-ui.test.js
  - tests/mcp-agent-providers-storage.test.js
autonomous: true
requirements:
  - MULTI-01
  - MULTI-02
  - MULTI-03
must_haves:
  truths:
    - "[D64-13] OpenCode stays the existing second Agent CLI row; safe daemon evidence may change only its existing compatibility data, never row order, recommendation, selection, visible structure, CSS, or provider-specific rendering."
    - "[D64-14] Providers shows Account/Auth `Not reported`, exact approved OpenCode billing/help/links, and `Billing not reported`; no subscription, dollar, free, unlimited, or inferred account/provider claim appears."
    - "[D64-16] Every safe OpenCode compatibility state, refresh race, selection/focus/dirty/storage invariant, copy lock, responsive/theme/accessibility source gate, and forbidden native-data boundary is deterministic."
    - "Codex remains the third agent row and fail-closed Unsupported while all seven API providers retain exact behavior."
  artifacts:
    - path: "extension/ui/providers-panel.js"
      provides: "Safe OpenCode compatibility/auth/billing data in the existing Providers row"
    - path: "extension/ui/options.js"
      provides: "Closed shipped-roster refresh/expiry/trust coordination without selection mutation"
    - path: "extension/ui/control_panel.html"
      provides: "Canonical helper load order with byte-stable visible Providers structure"
  key_links:
    - from: "extension/ui/options.js"
      to: "extension/ui/providers-panel.js"
      via: "validated OpenCode compatibility row and existing details renderer"
      pattern: "opencode"
    - from: "extension/ui/providers-panel.js"
      to: "extension/utils/mcp-agent-providers.js"
      via: "closed background-projected compatibility evidence only"
      pattern: "Supported|Degraded|Unsupported"
    - from: "extension/ui/control_panel.html"
      to: "extension/utils/delegation-providers.js"
      via: "single local script dependency before Providers consumers with no visible DOM delta"
      pattern: "delegation-providers.js"
---

<objective>
Promote only the existing OpenCode Providers row from safe evidence, with exact copy and no visible layout change.

Purpose: Make OpenCode selectable and truthfully Supported/Degraded/Unsupported while keeping availability, recommendation, selection, auth, and billing independent.
Output: Data-driven existing-row behavior, approved Not reported copy, canonical helper load order, and blocking no-layout/no-native-data tests.

<scope_rationale>This plan is the Providers-row half of the UI-surface split. It owns only the control-panel data/rendering surface; delegated consent, side-panel lifecycle, feed, and run summary move to Plan 12.</scope_rationale>
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/64-opencode-adapter/64-CONTEXT.md
@.planning/phases/64-opencode-adapter/64-RESEARCH.md
@.planning/phases/64-opencode-adapter/64-PATTERNS.md
@.planning/phases/64-opencode-adapter/64-UI-SPEC.md
@.planning/phases/64-opencode-adapter/64-VALIDATION.md
@.planning/phases/62-ci-drift-smoke-gate-doctor-extensions/62-05-SUMMARY.md
@.planning/phases/62-ci-drift-smoke-gate-doctor-extensions/62-06-SUMMARY.md
@extension/utils/delegation-providers.js
@extension/utils/mcp-agent-providers.js
@extension/ui/providers-panel.js
@extension/ui/options.js
@extension/ui/control_panel.html

<decision_trace>
- D64-13: reuse only the existing second OpenCode row and keep compatibility observational.
- D64-14: auth and billing remain unknown/Not reported with exact approved copy.
- D64-16: Providers logic, DOM, accessibility, source-security, and storage coverage is blocking.
</decision_trace>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>64-11-01: Data-drive OpenCode compatibility, refresh, and honest detail copy</name>
  <files>extension/ui/providers-panel.js, extension/ui/options.js, tests/providers-panel-logic.test.js, tests/mcp-agent-providers-storage.test.js</files>
  <read_first>64-UI-SPEC Providers state/copy/security matrices; Plan 06 browser-safe snapshot; Plan 09 canonical provider helper; providers-panel provider definitions/compatibility mapper/auth/billing models; options compatibility expiry/refresh/trust; existing Providers logic/storage tests</read_first>
  <behavior>
    - "Validated fresh OpenCode evidence renders Supported; newer/stale renders Degraded; absent/corrupt/invalid/unshipped renders Unsupported; Codex remains Unsupported."
    - "Installation, connection, compatibility, recommendation, selection, Account/Auth, Billing, API values, dirty state, and storage writes remain independent through refresh success/failure/timeout/fanout."
    - "Auth is exactly Not reported with `The CLI has not reported its account type.`; Billing uses the approved OpenCode body/links and `Billing not reported`, never subscription/usd/free/unlimited."
    - "No version/range/semver/path/topology/port/secret/model/provider-native field enters UI logic or storage."
  </behavior>
  <action>Extend logic/storage tests first with every OpenCode evidence state, refresh race, selection/recommendation/API-value/dirty/storage snapshots, exact auth/billing copy and fixed link targets, Codex negative, accessor/prototype cases, and forbidden-field/string scans. Change shipped compatibility membership from Claude-only to the closed Plan 09 canonical helper plus Plan 06 validated storage roster. Generalize options expiry, degraded summary, and trust-provider lookup over exactly the shipped rows rather than swapping a literal. Reuse the existing OpenCode definition and detail fact model; add no version parser, native field, selection/save/recommendation side effect, or OpenCode-specific renderer/state branch. Preserve all seven API providers and Claude output.</action>
  <acceptance_criteria>All safe OpenCode evidence states map to exact approved status/auth/billing data while selection, recommendation, API values, dirty state, and storage remain stable; Codex stays Unsupported; no forbidden local/native/topology field or fabricated billing claim enters the view model.</acceptance_criteria>
  <verify>
    <automated>node tests/providers-panel-logic.test.js && node tests/mcp-agent-providers-storage.test.js</automated>
  </verify>
  <done>The acceptance criteria above are satisfied and the automated verification passes.</done>
</task>

<task type="auto" tdd="true">
  <name>64-11-02: Prove the existing OpenCode row DOM, copy, and accessibility remain unchanged</name>
  <files>extension/ui/control_panel.html, tests/providers-panel-ui.test.js</files>
  <read_first>64-UI-SPEC Providers hierarchy/copy/responsive/theme/accessibility/no-layout contract; control_panel current script and visible DOM order; providers-panel-ui row/order/focus/live-region/forced-colors/reduced-motion/source assertions; Task 64-11-01 view-model output</read_first>
  <behavior>
    - "OpenCode remains row 2 with one native radio, evidence cluster, compatibility group/description, and existing selected-detail dl; Claude is row 1, Codex row 3, then seven API providers unchanged."
    - "The only HTML delta is one local `../utils/delegation-providers.js` script before providers-panel/options consumers; visible nodes, CSS references, order, classes, roles, and layout are byte-equivalent."
    - "Exact Supported/Degraded/Unsupported, Not reported, billing, help, and link copy is rendered through existing text-only nodes."
    - "Existing focus order, 44px targets, <=640px wrapping, forced-colors, reduced-motion, and live-region behavior remain source/DOM locked."
  </behavior>
  <action>Add DOM/source snapshots first for exact row order/count/semantics, script load order, copy/link attributes, focus/live-region behavior, responsive/theme/forced-colors/reduced-motion pins, Codex Unsupported, and absence of new nodes/classes/CSS/provider branches. Add exactly one local canonical-helper script dependency before `providers-panel.js` and `options.js` in `control_panel.html`; leave the visible DOM and every CSS reference unchanged. Exercise Task 64-11-01's data through the existing radio, badges, description, details `<dl>`, setup/help, and links using textContent/createTextNode only. Do not add a visible node, CSS rule/reference, provider logo/brand color, version badge, server/topology field, or OpenCode conditional renderer.</action>
  <acceptance_criteria>OpenCode is the unchanged second row and renders exact approved states/copy through existing semantics; the helper loads before consumers; visible DOM/CSS/order/focus/a11y contracts are unchanged; Codex and API rows remain exact; forbidden native/version/topology/secret data is absent.</acceptance_criteria>
  <verify>
    <automated>node tests/providers-panel-ui.test.js</automated>
  </verify>
  <done>The acceptance criteria above are satisfied and the automated verification passes.</done>
</task>

</tasks>

<threat_model>
  <block_on>HIGH,CRITICAL</block_on>
  <assets>
    <asset id="A64-25">User provider selection and unchanged Providers interaction semantics.</asset>
    <asset id="A64-26">Honest compatibility, auth, and billing presentation.</asset>
    <asset id="A64-27">Control-panel DOM/storage confidentiality against local/native provider data.</asset>
  </assets>
  <trust_boundaries>
    <boundary id="B64-25">Background-validated compatibility data enters Providers view models and DOM.</boundary>
    <boundary id="B64-27">User selection intent returns to background persistence without compatibility authority.</boundary>
  </trust_boundaries>
  <threat id="T64-05" category="Tampering" component="compatibility presentation" severity="HIGH" disposition="mitigate" asvs="V14">
    <mitigation>Consume only closed Plan 06 evidence and forbid browser version policy or compatibility-driven selection/recommendation writes.</mitigation>
    <verification>Every status/refresh race plus no-semver and no-side-effect source/logic tests.</verification>
    <residual_risk>Genuine live installed-state rendering remains milestone UAT.</residual_risk>
  </threat>
  <threat id="T64-10" category="Information Disclosure" component="Providers DOM/storage" severity="CRITICAL" disposition="mitigate" asvs="V7">
    <mitigation>Render only closed canonical status/auth/billing view models through existing text-only components and forbid native/version/topology/secret/model data.</mitigation>
    <verification>Exact-key/accessor/prototype/sentinel/source/DOM scans and no-visible-structure assertion.</verification>
    <residual_risk>Canonical provider name/status is intentionally visible.</residual_risk>
  </threat>
</threat_model>

<verification>
- Providers logic/storage tests cover exact safe evidence states, refresh races, selection independence, and forbidden fields.
- DOM/source tests prove row order, copy, script order, no visible structure/CSS delta, and existing accessibility/responsive/theme contracts.
- No live browser/accessibility result is fabricated; Plan 13 keeps it pending.
</verification>

<success_criteria>
- OpenCode is selectable and truthfully Supported/Degraded/Unsupported through its existing row.
- Account/Auth and Billing remain exact Not reported with no fabricated subscription or dollars.
- Providers gains no visible layout, CSS, provider renderer, version policy, or authority side effect.
</success_criteria>

<output>
Create `.planning/phases/64-opencode-adapter/64-11-SUMMARY.md` when complete.
</output>
