---
phase: 01-lattice-gap-survey-scaffold
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - package.json
  - tests/lattice-smoke.test.js
  - .planning/LATTICE-PIN.md
  - package-lock.json
autonomous: false
requirements:
  - "D-05 (path: dependency to lattice/packages/lattice via `file:` spec)"
  - "D-07 (developer ceremony documented; no postinstall coupling)"
  - "D-08 (.planning/LATTICE-PIN.md as the cross-repo audit trail)"
  - "D-09 AMENDED (smoke contexts = Node-side test only; manual MV3 prong downgraded per Option B)"
  - "D-10 (mint one Capability Receipt via Lattice's existing v1.1 surface)"
  - "D-11 (smoke ownership FSB-side; follows node tests/foo.test.js convention; added to npm test chain)"
  - "D-12 #2 (Node smoke check -- the substantive proof of FSB <-> Lattice round-trip)"
  - "D-12 #3 AMENDED (manual MV3 sanity reload check -- no new console errors caused by Phase 1's tree additions)"
  - "D-16 (LATTICE-PIN.md as single FSB-side cross-repo index; no per-phase XX-LATTICE-WORK.md files)"
  - "CD-06 RESOLVED (LATTICE-PIN.md uses markdown-table schema with frontmatter for current SHA)"
  - "FINT (FSB Integration Layer) -- path:dep wiring + smoke test"
  - "MCP (Wire-Contract Non-Regression) -- INV-01 holds via tool-definitions-parity.test.js continuing to pass with smoke added to chain"
  - "INV-01 (MCP wire contracts UNTOUCHED)"
  - "INV-04 (MV3-survivability iterator preserved -- agent-loop.js untouched)"
  - "INV-06 (Lattice SDK primitives live in Lattice's repo; FSB code is integration glue only)"

must_haves:
  truths:
    - "FSB's package.json declares lattice as a file: dependency pointing at ./lattice/packages/lattice"
    - "FSB's scripts.test chain includes node tests/lattice-smoke.test.js as the new final step"
    - "node tests/lattice-smoke.test.js exits 0 -- it mints AND verifies one Capability Receipt round-trip via Lattice"
    - "npm test (the full chain) exits 0 -- no regression in any of the ~144 existing tests, INV-01 byte-identity holds"
    - ".planning/LATTICE-PIN.md exists with frontmatter holding current_lattice_sha + current_branch, plus a markdown-table body row for Phase 1"
    - "Manual MV3 sanity reload check: extension reloads cleanly with no NEW console errors caused by Phase 1's tree additions; existing extension flows (popup open, sidepanel open, autopilot iteration) still work end-to-end"
    - "INV-04 holds: extension/ai/agent-loop.js setTimeout call count is unchanged from the pre-Phase-1 baseline"
  artifacts:
    - path: "package.json"
      provides: "FSB depends on Lattice via npm file: spec + smoke test in test chain"
      contains: "\"lattice\": \"file:./lattice/packages/lattice\""
    - path: "tests/lattice-smoke.test.js"
      provides: "FSB-side smoke proving the Lattice round-trip"
      contains: "createReceipt"
      min_lines: 60
    - path: ".planning/LATTICE-PIN.md"
      provides: "Single FSB-side index of FSB <-> Lattice work (D-08, D-16)"
      contains: "fsb-integration-experiments"
      min_lines: 20
    - path: "node_modules/lattice"
      provides: "Symlink target proving npm install resolved the file: dep"
      min_lines: 0
  key_links:
    - from: "tests/lattice-smoke.test.js"
      to: "lattice/packages/lattice/dist/index.js (via node_modules/lattice symlink)"
      via: "dynamic await import('lattice') from a CJS test file"
      pattern: "await\\s+import\\(['\"]lattice['\"]\\)"
    - from: "tests/lattice-smoke.test.js"
      to: "lattice.createReceipt + lattice.verifyReceipt"
      via: "round-trip: generate keypair -> sign receipt -> verify envelope"
      pattern: "createReceipt\\([\\s\\S]*verifyReceipt\\("
    - from: "package.json scripts.test"
      to: "tests/lattice-smoke.test.js"
      via: "appended to the && chain after agent-loop-empty-contents.test.js"
      pattern: "agent-loop-empty-contents\\.test\\.js && node tests/lattice-smoke\\.test\\.js"
    - from: ".planning/LATTICE-PIN.md"
      to: "lattice/fsb-integration-experiments@<SHA>"
      via: "markdown table row + frontmatter current_lattice_sha field"
      pattern: "[0-9a-f]{7,40}"
---

<objective>
Land the FSB-side scaffolding for Phase 1 (depends on Plan 01's Lattice-side commits being on `fsb-integration-experiments` HEAD with a built `dist/`). Four artifacts:

1. `package.json` -- add `"lattice": "file:./lattice/packages/lattice"` to dependencies; append `node tests/lattice-smoke.test.js` to the `scripts.test` && chain after `agent-loop-empty-contents.test.js`.
2. `tests/lattice-smoke.test.js` -- a real-runtime CJS test that dynamically imports Lattice, mints one Capability Receipt with an ephemeral Ed25519 keypair, verifies the envelope round-trips, and exits 0 on pass (1 on fail).
3. `.planning/LATTICE-PIN.md` -- the FSB-side audit-trail index recording the Lattice SHA from Plan 01 plus a Phase 1 row in the per-phase log table.
4. Manual MV3 sanity reload checkpoint (D-12 #3 AMENDED, Option B) -- developer reloads the unpacked extension, captures evidence that nothing in Phase 1's tree additions broke the existing extension. Phase 1 does NOT in-extension-import Lattice (per Option B reconciliation), so this is a sanity check, not a Lattice-in-SW proof.

Purpose: Plan 02 closes Phase 1's pass criteria D-12 #2 (Node smoke green) and D-12 #3 (manual MV3 sanity clean). D-12 #1 (audit doc on Lattice side) is closed by Plan 01.

Output: FSB-side commit on the `automation` branch wiring Lattice as a file: dependency, plus a real-runtime smoke in the test chain, plus the LATTICE-PIN.md audit trail, plus captured manual-reload evidence in the phase SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-lattice-gap-survey-scaffold/01-CONTEXT.md
@.planning/phases/01-lattice-gap-survey-scaffold/01-RESEARCH.md
@.planning/phases/01-lattice-gap-survey-scaffold/01-VALIDATION.md
@.planning/phases/01-lattice-gap-survey-scaffold/01-01-PLAN.md
@package.json
@tests/install-identity.test.js
@tests/agent-loop-empty-contents.test.js
@tests/tool-definitions-parity.test.js
@extension/manifest.json
@lattice/packages/lattice/src/index.ts

<interfaces>
<!-- Key Lattice surface the FSB smoke consumes (post-Plan 01 state: createReceipt is now re-exported). -->
<!-- Executor uses these signatures directly -- no need to re-read Lattice source. -->

From the lattice bare specifier (resolves via node_modules/lattice symlink -> lattice/packages/lattice/dist/index.js after Plan 01 rebuild):

```typescript
// Receipt mint (NEWLY re-exported by Plan 01's index.ts tweak)
function createReceipt(input: CreateReceiptInput, signer: ReceiptSigner): Promise<ReceiptEnvelope>;

interface CreateReceiptInput {
  readonly runId: string;
  readonly issuedAt?: string;
  readonly receiptId?: string;
  readonly model: { readonly requested: string; readonly observed: string | null };
  readonly route: { readonly providerId: string; readonly capabilityId: string; readonly attemptNumber: number };
  readonly usage: { readonly promptTokens: number; readonly completionTokens: number; readonly costUsd: number | string | null };
  readonly contractVerdict: "success" | "tripwire-violated" | "no-contract-match" | "execution-failed" | "validation-failed";
  readonly contractHash: string | null;
  readonly inputHashes: readonly string[];
  readonly outputHash: string | null;
  readonly redactionPolicyId?: string;
}

// Receipt verifier (typed result, never throws across the verification boundary)
function verifyReceipt(envelope: ReceiptEnvelope, keySet: KeySet): Promise<VerifyResult>;

type VerifyResult =
  | { ok: true; body: CapabilityReceiptBody; keyState: "active" | "retired" | "revoked" }
  | { ok: false; error: { kind: string; message: string } };

// Signer factory + keypair generator
function createInMemorySigner(
  privateKeyJwk: JsonWebKey,
  options: { kid: string; publicKeyJwk: JsonWebKey }
): ReceiptSigner;

function generateEd25519KeyPairJwk(): Promise<{ privateKeyJwk: JsonWebKey; publicKeyJwk: JsonWebKey }>;

// In-memory keyset factory
function createMemoryKeySet(
  entries: readonly { kid: string; publicKeyJwk: JsonWebKey; state: "active" | "retired" | "revoked" }[]
): KeySet;

// Envelope shape (returned by createReceipt; consumed by verifyReceipt)
interface ReceiptEnvelope {
  readonly payloadType: "application/vnd.lattice.receipt+json";
  readonly payload: string;  // base64-encoded canonical JSON of the receipt body
  readonly signatures: readonly { readonly keyid: string; readonly sig: string }[];
}
```

From FSB's `tests/install-identity.test.js` (the test-file convention to mirror):

```javascript
'use strict';
const assert = require('node:assert');
let passed = 0; let failed = 0;
function passAssert(cond, msg) { if (cond) { passed++; console.log('  PASS:', msg); } else { failed++; console.error('  FAIL:', msg); } }
// ... test body ...
console.log('passed:', passed);
console.log('failed:', failed);
process.exit(failed > 0 ? 1 : 0);
```

From FSB's `package.json` (current `scripts.test` chain -- the test to append AFTER):

```
... && node tests/agent-loop-empty-contents.test.js
```
(this is the current LAST entry in the chain; Plan 02 appends `&& node tests/lattice-smoke.test.js` to the end.)

From FSB's `extension/manifest.json` (verified post-reconciliation -- the background block does NOT have "type": "module"; DO NOT change this in Phase 1):

```json
"background": {
  "service_worker": "background.js"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Lattice file: dependency to package.json, append smoke to test chain, install</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (current state -- specifically the `dependencies` object on line 79-81 and `scripts.test` on line 16)
    - .planning/phases/01-lattice-gap-survey-scaffold/01-CONTEXT.md (D-05 confirms `"lattice": "file:./lattice/packages/lattice"`; the reconciliation block confirms `file:` not `link:`)
    - .planning/phases/01-lattice-gap-survey-scaffold/01-RESEARCH.md (specifically the "npm dependency spec (CD-01)" table -- `file:` is empirically verified on npm 11.12.1)
    - .planning/phases/01-lattice-gap-survey-scaffold/01-01-PLAN.md (Plan 01 must be complete; Lattice's `dist/` must exist before this task's `npm install`)
    - .planning/phases/01-lattice-gap-survey-scaffold/01-01-SHA.txt (sanity: this file should exist; confirms Plan 01 ran)
  </read_first>
  <action>
Step 1 -- precondition checks:
- Confirm `.planning/phases/01-lattice-gap-survey-scaffold/01-01-SHA.txt` exists and contains a 40-char hex SHA. If missing, STOP -- Plan 01 must complete first.
- Confirm `lattice/packages/lattice/dist/index.js` exists. If missing, STOP -- Plan 01's `pnpm build` did not run or was not committed-with state.
- Confirm `lattice/packages/lattice/src/index.ts` contains the `createReceipt` re-export line (regex `export\s*\{[^}]*createReceipt[^}]*\}\s*from`). If missing, STOP -- Plan 01's Task 1 did not land.

Step 2 -- modify `package.json` `dependencies`. The current `dependencies` object (lines 79-81) is:

```json
"dependencies": {
  "axios": "^1.6.0"
},
```

Use the Edit tool to change it to (exact target -- adds `"lattice"` AFTER `"axios"` to preserve alphabetical order; both keys are double-quoted; trailing comma after axios value is required):

```json
"dependencies": {
  "axios": "^1.6.0",
  "lattice": "file:./lattice/packages/lattice"
},
```

Step 3 -- modify `package.json` `scripts.test`. The current chain ends with `&& node tests/agent-loop-empty-contents.test.js` followed by the closing quote `",`. Use the Edit tool to append `&& node tests/lattice-smoke.test.js` so the chain ends with:

```
... && node tests/agent-loop-empty-contents.test.js && node tests/lattice-smoke.test.js",
```

The Edit tool's `old_string` should match enough left-context to be unique (e.g., the last three test files: `node tests/skill-fsb-spec.test.js && node tests/agent-loop-empty-contents.test.js`). The `new_string` is that exact left-context PLUS ` && node tests/lattice-smoke.test.js`.

Step 4 -- run `npm install` from the FSB root. This should:
- Materialize `node_modules/lattice` as a symlink into `lattice/packages/lattice/`.
- Update `package-lock.json` with the `file:` dep entry.
- Not error (Lattice's `package.json` `exports` map points at `./dist/index.js` which exists per Plan 01).

After `npm install`:
- Run: `test -L node_modules/lattice || test -d node_modules/lattice` -- assert the path is either a symlink or a directory (npm 11 creates a symlink for `file:` deps).
- Run: `ls -la node_modules/lattice/dist/ 2>&1 | head -5` -- assert `index.js` and `index.d.ts` are reachable through the symlink.
- Run: `node -e "import('lattice').then(l => { console.log(typeof l.createReceipt); process.exit(typeof l.createReceipt === 'function' ? 0 : 1); }).catch(e => { console.error('IMPORT FAIL:', e.message); process.exit(1); });"` -- the bare-specifier import from FSB's runtime resolves to the Lattice bundle AND `createReceipt` is reachable. This is the integration smoke at the npm level (Task 2 then builds the user-facing test file).

If any step fails, STOP and report which one. Do not proceed to Task 2 with a broken install.

Step 5 -- INV-01 sanity check at this point (BEFORE the smoke test file is written, so the existing `tool-definitions-parity.test.js` still runs and confirms parity):
- Run: `node tests/tool-definitions-parity.test.js` -- assert exit 0. INV-01 is unchanged by `package.json` dep additions.
- Run: `git status --porcelain` from the FSB root -- the only modified files should be `package.json` and `package-lock.json`. No `extension/` files, no `mcp/` files, no `tests/` files yet (Task 2 adds the smoke).
  </action>
  <verify>
    <automated>grep -F "\"lattice\": \"file:./lattice/packages/lattice\"" package.json && grep -F "tests/lattice-smoke.test.js" package.json && test -e node_modules/lattice && node -e "import('lattice').then(l => process.exit(typeof l.createReceipt === 'function' && typeof l.verifyReceipt === 'function' ? 0 : 1)).catch(() => process.exit(1))" && node tests/tool-definitions-parity.test.js</automated>
  </verify>
  <done>
    <acceptance_criteria>
      - `package.json` contains the exact line `"lattice": "file:./lattice/packages/lattice"` (verify with `grep -F "\"lattice\": \"file:./lattice/packages/lattice\"" package.json`)
      - `package.json` `scripts.test` chain ends with `... && node tests/agent-loop-empty-contents.test.js && node tests/lattice-smoke.test.js` (verify with `grep -F "agent-loop-empty-contents.test.js && node tests/lattice-smoke.test.js" package.json`)
      - `node_modules/lattice` exists (either as symlink or directory): `test -e node_modules/lattice` exits 0
      - `node_modules/lattice/dist/index.js` is reachable through the symlink: `test -f node_modules/lattice/dist/index.js` exits 0
      - Node dynamic import of `'lattice'` from FSB root resolves AND `typeof l.createReceipt === 'function'` AND `typeof l.verifyReceipt === 'function'` AND `typeof l.createInMemorySigner === 'function'` AND `typeof l.generateEd25519KeyPairJwk === 'function'` AND `typeof l.createMemoryKeySet === 'function'`
      - `node tests/tool-definitions-parity.test.js` exits 0 (INV-01 holds; byte-identity preserved)
      - `git status --porcelain extension/ai/agent-loop.js` is empty (INV-04 -- agent-loop.js untouched)
      - `git status --porcelain extension/ai/tool-definitions.js mcp/ai/tool-definitions.cjs` is empty (INV-01, INV-02 -- tool registries untouched)
      - `git status --porcelain` modified-file list contains EXACTLY `package.json` and `package-lock.json` (no other files modified by this task)
    </acceptance_criteria>
  </done>
</task>

<task type="auto">
  <name>Task 2: Create tests/lattice-smoke.test.js (real-runtime mint + verify round-trip)</name>
  <files>tests/lattice-smoke.test.js</files>
  <read_first>
    - tests/install-identity.test.js (FSB's manual pass/fail counter convention -- mirror exactly: 'use strict', require('node:assert'), passed/failed counters, console.log('  PASS:', msg), process.exit(failed > 0 ? 1 : 0))
    - .planning/phases/01-lattice-gap-survey-scaffold/01-RESEARCH.md (specifically the "Pattern 1: dynamic ESM import from CJS test (Node-side smoke)" code skeleton -- the executor copies the skeleton and fills in any minor adaptations)
    - .planning/phases/01-lattice-gap-survey-scaffold/01-CONTEXT.md (D-10 -- mint one Capability Receipt via Lattice's existing v1.1 surface; reconciliation block D-12 #2 CLARIFIED -- uses ephemeral Ed25519 keypair per run, no committed keys)
    - lattice/packages/lattice/src/receipts/types.ts (CapabilityReceiptBody shape -- the receipt body fields the verifier expects)
    - User feedback memory: "Real runtime tests, not static-text greps" -- the smoke MUST actually invoke createReceipt + verifyReceipt and assert result.ok === true, not grep for identifier presence
  </read_first>
  <action>
Create `tests/lattice-smoke.test.js` with the following exact content. Use the Write tool. This file follows FSB's convention (CJS, `'use strict'`, manual counters, raw `node` invocation) and uses dynamic ESM import to bridge into Lattice (which is ESM-only):

```javascript
'use strict';

/**
 * Phase 1 (v0.10.0-attempt-2) -- Lattice integration smoke test.
 *
 * Purpose: prove FSB consumes Lattice end-to-end via the npm file: dependency.
 * Mints ONE Capability Receipt with an ephemeral Ed25519 keypair, verifies
 * the envelope round-trip, asserts the verified body fields match what was
 * signed. No persistence, no shared state -- the keypair is generated per
 * test run and discarded when the process exits.
 *
 * Coverage:
 *   - CONTEXT.md D-10 (mint one receipt via Lattice's existing v1.1 surface)
 *   - CONTEXT.md D-12 #2 (Node smoke check -- the substantive proof)
 *   - INV-06 (the primitive lives in Lattice; FSB just consumes)
 *
 * Run: node tests/lattice-smoke.test.js
 */

const assert = require('node:assert/strict');

let passed = 0;
let failed = 0;

function passAssert(cond, msg) {
  if (cond) {
    passed++;
    console.log('  PASS:', msg);
  } else {
    failed++;
    console.error('  FAIL:', msg);
  }
}

function passAssertEqual(actual, expected, msg) {
  passAssert(
    actual === expected,
    msg + ' (expected: ' + JSON.stringify(expected) + ', got: ' + JSON.stringify(actual) + ')'
  );
}

(async () => {
  console.log('\n--- Lattice v1.1 smoke: mint + verify one Capability Receipt ---');

  // Dynamic ESM import -- Lattice is "type": "module"; FSB's test convention
  // is CJS. Node 16+ supports dynamic import() from CJS to ESM.
  let lattice;
  try {
    lattice = await import('lattice');
  } catch (err) {
    console.error('  FAIL: dynamic import("lattice") threw:', err && err.message ? err.message : err);
    console.error('         If you see ERR_MODULE_NOT_FOUND, did you run `cd lattice && pnpm install && pnpm build`?');
    process.exit(1);
  }

  // Surface presence checks -- prove the bare specifier resolves to Lattice's
  // public surface, and the symbols we need are reachable.
  passAssertEqual(typeof lattice.createReceipt, 'function', 'lattice.createReceipt is a function');
  passAssertEqual(typeof lattice.verifyReceipt, 'function', 'lattice.verifyReceipt is a function');
  passAssertEqual(typeof lattice.createInMemorySigner, 'function', 'lattice.createInMemorySigner is a function');
  passAssertEqual(typeof lattice.generateEd25519KeyPairJwk, 'function', 'lattice.generateEd25519KeyPairJwk is a function');
  passAssertEqual(typeof lattice.createMemoryKeySet, 'function', 'lattice.createMemoryKeySet is a function');

  if (failed > 0) {
    // Bail early -- the round-trip below will throw if a function is missing.
    console.log('\nLattice smoke: surface presence check failed; aborting before round-trip.');
    console.log('passed:', passed, 'failed:', failed);
    process.exit(1);
  }

  // Step 1: generate an ephemeral Ed25519 keypair via Lattice's WebCrypto wrapper.
  // No private material is persisted -- the keys live only in this process.
  const { privateKeyJwk, publicKeyJwk } = await lattice.generateEd25519KeyPairJwk();
  passAssert(privateKeyJwk && typeof privateKeyJwk === 'object', 'generateEd25519KeyPairJwk returned a privateKeyJwk object');
  passAssert(publicKeyJwk && typeof publicKeyJwk === 'object', 'generateEd25519KeyPairJwk returned a publicKeyJwk object');
  passAssertEqual(privateKeyJwk.kty, 'OKP', 'privateKeyJwk.kty is OKP (Ed25519)');
  passAssertEqual(privateKeyJwk.crv, 'Ed25519', 'privateKeyJwk.crv is Ed25519');

  // Step 2: build an in-memory signer keyed by the ephemeral private JWK.
  const signer = lattice.createInMemorySigner(privateKeyJwk, {
    kid: 'fsb-phase-1-smoke-key',
    publicKeyJwk: publicKeyJwk
  });
  passAssertEqual(signer.kid, 'fsb-phase-1-smoke-key', 'signer.kid round-trips through createInMemorySigner');
  passAssertEqual(typeof signer.sign, 'function', 'signer.sign is a function');

  // Step 3: mint one receipt against a stubbed capability + route + usage.
  // The stub values exercise the redact-canonicalize-sign-encode pipeline
  // without simulating any specific provider integration. costUsd is 0
  // (allowed; usageToCanonical converts to string "0" or null per I-JSON).
  let envelope;
  try {
    envelope = await lattice.createReceipt(
      {
        runId: 'fsb-phase-1-smoke-run',
        model: { requested: 'fsb-smoke-stub-model', observed: null },
        route: {
          providerId: 'fsb-smoke',
          capabilityId: 'fsb-smoke/round-trip',
          attemptNumber: 1
        },
        usage: { promptTokens: 0, completionTokens: 0, costUsd: 0 },
        contractVerdict: 'success',
        contractHash: null,
        inputHashes: [],
        outputHash: null
      },
      signer
    );
  } catch (err) {
    console.error('  FAIL: createReceipt threw:', err && err.message ? err.message : err);
    process.exit(1);
  }

  // Step 4: assert the envelope shape matches Lattice's documented DSSE form.
  passAssertEqual(
    envelope.payloadType,
    'application/vnd.lattice.receipt+json',
    'envelope.payloadType is the Lattice receipt media type'
  );
  passAssert(typeof envelope.payload === 'string' && envelope.payload.length > 0, 'envelope.payload is a non-empty string');
  passAssert(Array.isArray(envelope.signatures), 'envelope.signatures is an array');
  passAssertEqual(envelope.signatures.length, 1, 'envelope has exactly one signature');
  passAssertEqual(envelope.signatures[0].keyid, 'fsb-phase-1-smoke-key', 'envelope.signatures[0].keyid matches signer.kid');
  passAssert(
    typeof envelope.signatures[0].sig === 'string' && envelope.signatures[0].sig.length > 0,
    'envelope.signatures[0].sig is a non-empty base64 string'
  );

  // Step 5: verify the envelope round-trips through Lattice's verifier.
  const keySet = lattice.createMemoryKeySet([
    { kid: 'fsb-phase-1-smoke-key', publicKeyJwk: publicKeyJwk, state: 'active' }
  ]);
  const result = await lattice.verifyReceipt(envelope, keySet);
  passAssert(result && typeof result === 'object', 'verifyReceipt returned an object');
  passAssertEqual(result.ok, true, 'verifyReceipt result.ok is true (round-trip successful)');

  if (result.ok === true) {
    passAssertEqual(result.body.version, 'lattice-receipt/v1', 'verified body.version is lattice-receipt/v1');
    passAssertEqual(result.body.kid, 'fsb-phase-1-smoke-key', 'verified body.kid round-trips');
    passAssertEqual(result.body.runId, 'fsb-phase-1-smoke-run', 'verified body.runId round-trips');
    passAssertEqual(result.body.contractVerdict, 'success', 'verified body.contractVerdict round-trips');
    passAssertEqual(result.body.route.providerId, 'fsb-smoke', 'verified body.route.providerId round-trips');
    passAssertEqual(result.body.route.capabilityId, 'fsb-smoke/round-trip', 'verified body.route.capabilityId round-trips');
    passAssertEqual(result.body.route.attemptNumber, 1, 'verified body.route.attemptNumber round-trips');
    passAssertEqual(result.keyState, 'active', 'verified keyState is active');
  } else {
    failed++;
    console.error('  FAIL: result.ok was not true; result.error =', JSON.stringify(result.error));
  }

  // Step 6: negative round-trip -- a keyset with a different public key
  // MUST cause verifyReceipt to fail with a typed error (never throws).
  const wrongPair = await lattice.generateEd25519KeyPairJwk();
  const wrongKeySet = lattice.createMemoryKeySet([
    { kid: 'fsb-phase-1-smoke-key', publicKeyJwk: wrongPair.publicKeyJwk, state: 'active' }
  ]);
  const wrongResult = await lattice.verifyReceipt(envelope, wrongKeySet);
  passAssertEqual(wrongResult.ok, false, 'verifyReceipt with wrong public key returns ok: false (no throw)');
  if (wrongResult.ok === false) {
    passAssertEqual(
      wrongResult.error.kind,
      'signature-invalid',
      'verifyReceipt error.kind is signature-invalid when the public key does not match'
    );
  }

  console.log('\n--- Summary ---');
  console.log('passed:', passed);
  console.log('failed:', failed);
  process.exit(failed > 0 ? 1 : 0);
})().catch((err) => {
  console.error('Smoke harness uncaught error:', err && err.stack ? err.stack : err);
  process.exit(1);
});
```

After writing the file, run it directly to confirm it passes BEFORE relying on `npm test`:
- Run: `node tests/lattice-smoke.test.js` -- assert exit code 0. Expected stdout includes lines starting with `  PASS:` and the closing `passed: <N>` / `failed: 0`.

Then run the full `npm test` chain to confirm nothing regressed AND the smoke is reached:
- Run: `npm test` -- assert exit code 0. This is the substantive proof that:
  - D-12 #2 holds (smoke runs in the test chain and passes)
  - INV-01 holds (tool-definitions-parity.test.js still byte-identity-passes)
  - No other test regressed
  
If `npm test` fails on any test, STOP and report which test (the smoke or a pre-existing test). Do not proceed to Task 3 with a red test chain.
  </action>
  <verify>
    <automated>node tests/lattice-smoke.test.js && grep -F "await import('lattice')" tests/lattice-smoke.test.js && grep -F "lattice.createReceipt(" tests/lattice-smoke.test.js && grep -F "lattice.verifyReceipt(" tests/lattice-smoke.test.js && grep -F "result.ok" tests/lattice-smoke.test.js && npm test</automated>
  </verify>
  <done>
    <acceptance_criteria>
      - `tests/lattice-smoke.test.js` exists and starts with `'use strict';`
      - File line count >= 60 (sanity: full mint+verify+negative-test body, not a stub)
      - File contains literal `await import('lattice')` (the bare-specifier dynamic import)
      - File calls `lattice.createReceipt(` AT LEAST once (real runtime invocation, not a grep stub)
      - File calls `lattice.verifyReceipt(` AT LEAST once (real round-trip verification)
      - File asserts `result.ok` equals `true` (positive path)
      - File asserts `result.ok` equals `false` on the wrong-key negative path (proves typed error, no throw)
      - `node tests/lattice-smoke.test.js` exits 0 (the smoke passes standalone)
      - `npm test` exits 0 (the full chain green; D-12 #2 holds)
      - `node tests/tool-definitions-parity.test.js` still exits 0 (INV-01 byte-identity preserved)
      - `grep -c "setTimeout" extension/ai/agent-loop.js` returns 8 (INV-04 baseline preserved -- agent-loop.js untouched)
      - `git status --porcelain extension/` is empty (no extension code modified)
      - `git status --porcelain mcp/` is empty (no MCP code modified)
      - The smoke uses an EPHEMERAL keypair generated per run (verify by grep: file contains `generateEd25519KeyPairJwk()` and does NOT contain hardcoded `privateKeyJwk` literal or `process.env.LATTICE_` references)
      - No emojis in the smoke file (project CLAUDE.md global rule): `grep -P "[\x{1F300}-\x{1F9FF}]|[\x{2600}-\x{27BF}]" tests/lattice-smoke.test.js` returns no matches
    </acceptance_criteria>
  </done>
</task>

<task type="auto">
  <name>Task 3: Create .planning/LATTICE-PIN.md cross-repo audit trail</name>
  <files>.planning/LATTICE-PIN.md</files>
  <read_first>
    - .planning/phases/01-lattice-gap-survey-scaffold/01-CONTEXT.md (D-08, D-16, reconciliation CD-06 RESOLVED: markdown table with frontmatter for current pin)
    - .planning/phases/01-lattice-gap-survey-scaffold/01-RESEARCH.md (`Standard Stack -> Lattice v1.1 surface inventory` for the "Lattice work touched" content)
    - .planning/phases/01-lattice-gap-survey-scaffold/01-01-SHA.txt (the Lattice HEAD SHA captured by Plan 01 Task 3 -- this is the SHA the PIN records)
    - .planning/phases/01-lattice-gap-survey-scaffold/01-01-PLAN.md (the artifacts list -- what Lattice work this phase touched)
  </read_first>
  <action>
Step 1 -- read the Lattice SHA captured by Plan 01:
- Run: `cat .planning/phases/01-lattice-gap-survey-scaffold/01-01-SHA.txt` and capture the trimmed (single-line) output as `LATTICE_SHA`. Assert it matches the regex `^[0-9a-f]{40}$`.
- Run: `cd lattice && git rev-parse fsb-integration-experiments` and assert the output equals `LATTICE_SHA`. This confirms the file's SHA matches the actual branch HEAD (no drift since Plan 01 ran).

Step 2 -- create `.planning/LATTICE-PIN.md` with this exact content (substitute `{{LATTICE_SHA}}` with the actual 40-char SHA captured in Step 1, and `{{LATTICE_SHA_SHORT}}` with the first 7 chars):

```markdown
***
current_lattice_sha: {{LATTICE_SHA}}
current_branch: fsb-integration-experiments
last_updated: 2026-05-24
schema_version: 1
***

# Lattice Pin -- FSB <-> Lattice Cross-Repo Audit Trail

This file is the SINGLE FSB-side index of work FSB has produced on the Lattice repo at `./lattice/` during the v0.10.0-attempt-2 milestone. Per CONTEXT.md D-16 there are no per-phase `XX-LATTICE-WORK.md` files; Lattice commit messages self-document via the `Ref: FSB v0.10.0-attempt-2 Phase N` footer convention (D-14).

**Lattice repo location (local, gitignored):** `./lattice/`
**Lattice branch in use:** `fsb-integration-experiments`
**Lattice baseline:** v1.1 Capability Receipts (451 tests; cloned from https://github.com/LakshmanTurlapati/Lattice)
**Current pinned SHA:** `{{LATTICE_SHA_SHORT}}` (full SHA in frontmatter)
**Mainline PR cadence:** Deferred to v0.11.0+ per CONTEXT.md D-15. All FSB-driven Lattice work stays on the experiment branch for this milestone.

## Per-FSB-Phase Log

Each row records one FSB phase's Lattice-side commits. The `Lattice SHA` column points at Lattice's HEAD AFTER the phase's Lattice commits landed (so the row's SHA is what FSB depends on once the phase is complete). The `Lattice work touched` column summarizes what was added/modified -- the authoritative narrative is in the Lattice commit bodies themselves (each tagged `Ref: FSB v0.10.0-attempt-2 Phase N`).

| FSB Phase | Date       | Lattice SHA                                 | Branch                          | Lattice work touched                                                                                              | Notes                                                                                                                                                                              |
|-----------|------------|---------------------------------------------|---------------------------------|-------------------------------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| Phase 1   | 2026-05-24 | `{{LATTICE_SHA}}` | `fsb-integration-experiments`   | (1) `packages/lattice/src/index.ts` -- one-line re-export of `createReceipt` + `type CreateReceiptInput`. (2) `docs/fsb-integration-gaps.md` -- new 6-surface audit doc. | Phase 1 = audit + scaffolding. No primitive extensions. Per CONTEXT.md D-13 narrowed, the one-line re-export is the ONLY allowed Lattice-side code change in this phase. The audit doc is the queue driver for Phase 2+. |

## How this file gets used

- **At plan-time:** When FSB plans a new phase, the planner reads `current_lattice_sha` + `current_branch` to ground "what Lattice surface FSB depends on right now."
- **At execute-time:** When a phase touches Lattice, the executor records the resulting Lattice HEAD SHA in this file via a new row + updates the `current_lattice_sha` frontmatter field. The commit ceremony lands the PIN.md update in the same FSB commit as the rest of the phase's FSB-side changes.
- **At verify-time:** Phase verifiers cross-check `current_lattice_sha` against the actual `cd lattice && git rev-parse fsb-integration-experiments` to confirm no drift. The smoke test (or successor SDK tests) MUST pass against the pinned SHA.

## Schema notes

- `current_lattice_sha`: 40-char hex; advances per FSB phase that touches Lattice.
- `current_branch`: fixed at `fsb-integration-experiments` for the v0.10.0-attempt-2 milestone (per D-15 -- no mainline PRs).
- Per-phase rows are append-only; closed phases are not edited retroactively.
- `Lattice work touched` cells are summary-form; the source of truth is the Lattice commit body (grep for `Ref: FSB v0.10.0-attempt-2 Phase N` inside `cd lattice && git log` to find a phase's commits).
```

Step 3 -- verify the PIN.md content:
- Confirm the file exists: `test -f .planning/LATTICE-PIN.md`
- Confirm the file's frontmatter SHA matches the actual Lattice HEAD:
  ```bash
  PIN_SHA=$(grep "^current_lattice_sha:" .planning/LATTICE-PIN.md | awk '{print $2}')
  ACTUAL_SHA=$(cd lattice && git rev-parse fsb-integration-experiments)
  test "$PIN_SHA" = "$ACTUAL_SHA"
  ```
- Confirm the file has at least one body row referencing the SHA: `grep -c "{{LATTICE_SHA}}"` in the SHORT form has been substituted; the actual `LATTICE_SHA` should appear at least 2 times (frontmatter + body row).
- Confirm `current_branch: fsb-integration-experiments` appears in the frontmatter.
- Confirm the file contains the phrase `Phase 1` in a body row (markdown-table cell).

Step 4 -- since `.planning/` is gitignored at FSB's repo (verified by the planner reading `.gitignore`), the commit must use `git add -f`. The commit message for this task is built later in the wrap-up commit, NOT here -- this task only writes the file and stages it.

- Run: `git add -f .planning/LATTICE-PIN.md` (forces add despite gitignore).
- Run: `git status` -- confirm `.planning/LATTICE-PIN.md` is staged.
- Do NOT commit yet (the wrap-up commit covering all four FSB-side artifacts -- package.json, package-lock.json, smoke, PIN -- happens after Task 4 manual checkpoint clears).
  </action>
  <verify>
    <automated>test -f .planning/LATTICE-PIN.md && PIN_SHA=$(grep "^current_lattice_sha:" .planning/LATTICE-PIN.md | awk '{print $2}') && ACTUAL_SHA=$(cd lattice && git rev-parse fsb-integration-experiments) && test "$PIN_SHA" = "$ACTUAL_SHA" && grep -E "^[0-9a-f]{40}$" .planning/phases/01-lattice-gap-survey-scaffold/01-01-SHA.txt && grep "current_branch: fsb-integration-experiments" .planning/LATTICE-PIN.md && grep "^| Phase 1" .planning/LATTICE-PIN.md</automated>
  </verify>
  <done>
    <acceptance_criteria>
      - `.planning/LATTICE-PIN.md` exists
      - File line count >= 20 (sanity: contains frontmatter + intro + table + schema notes, not a stub)
      - Frontmatter contains `current_lattice_sha:` followed by a 40-char hex SHA (regex: `^current_lattice_sha: [0-9a-f]{40}$`)
      - Frontmatter `current_lattice_sha` value EQUALS the output of `cd lattice && git rev-parse fsb-integration-experiments` (no drift)
      - Frontmatter contains `current_branch: fsb-integration-experiments` (exact match)
      - Frontmatter contains `schema_version: 1`
      - First table column header (column 1) is `FSB Phase` (verify with `grep "FSB Phase" .planning/LATTICE-PIN.md`)
      - File contains at least one body row starting with `| Phase 1` (verify with `grep "^| Phase 1" .planning/LATTICE-PIN.md`)
      - The Phase 1 row references the same 40-char SHA from the frontmatter (verify by counting SHA occurrences: `grep -c "<40-char-sha>" .planning/LATTICE-PIN.md` returns >= 2)
      - `.planning/LATTICE-PIN.md` is staged for commit (verify with `git status --porcelain | grep "A  .planning/LATTICE-PIN.md"`)
      - No emojis in PIN.md (per project CLAUDE.md global rule): `grep -P "[\x{1F300}-\x{1F9FF}]|[\x{2600}-\x{27BF}]" .planning/LATTICE-PIN.md` returns no matches
    </acceptance_criteria>
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Manual MV3 sanity reload check (D-12 #3 AMENDED, Option B)</name>
  <what-built>
By this point Plan 01 + Plan 02 Tasks 1-3 have landed:

1. Lattice's `fsb-integration-experiments` branch has two new commits (the `createReceipt` re-export and the audit doc); Lattice's `dist/` is built.
2. FSB's `package.json` declares `"lattice": "file:./lattice/packages/lattice"`; `node_modules/lattice` is a working symlink.
3. FSB's `scripts.test` chain ends with `... && node tests/lattice-smoke.test.js`.
4. `tests/lattice-smoke.test.js` exists and `npm test` exits 0 (full chain green; D-12 #2 holds).
5. `.planning/LATTICE-PIN.md` is staged with the captured Lattice SHA.
6. NO `extension/` files were modified. NO `manifest.json` change. NO `extension/lattice-bridge.js` was created. The reconciliation block's Option B is honored verbatim -- Phase 1 does NOT import Lattice from any extension context.

This is the substantive Lattice <-> FSB integration check at the Node level. What remains is the MANUAL MV3 sanity reload to confirm Phase 1's tree changes do not destabilize the existing extension (D-12 #3 AMENDED per Option B). Per the reconciliation block this is a sanity check that the additions do not break the extension, NOT a proof that Lattice loads in a Chrome extension context (that is deferred to a future phase).
  </what-built>
  <how-to-verify>
Step 1 -- baseline (record what already exists BEFORE reloading):
- Optional but recommended: before doing the reload, open `chrome://extensions`, find the existing FSB extension (`FSB v0.9.67`), and click "Inspect views: service worker" to open the SW DevTools. Note any errors already present in the console (the baseline -- Phase 1 is not responsible for pre-existing errors).

Step 2 -- reload the unpacked extension:
- Open `chrome://extensions` in Chrome (or Brave / Edge / Chromium).
- Confirm "Developer mode" is enabled (top right toggle).
- Find the FSB extension card (`FSB v0.9.67` per the manifest's `name` field).
- Click the circular reload arrow ON THE FSB CARD (NOT a global Chrome restart).
- Watch the SW DevTools console as the extension reloads.

Step 3 -- assertions (mark each with PASS or FAIL in the SUMMARY):

a) **Service worker reloads cleanly.** The FSB card in `chrome://extensions` shows "service worker" with NO red "Errors" badge above the baseline. If a red Errors badge appears AND was not there pre-reload, that is a FAIL caused by Phase 1.

b) **SW console shows no NEW errors caused by Phase 1.** Pre-existing console output is acceptable. Look for any NEW lines mentioning `lattice`, `import`, `ERR_MODULE_NOT_FOUND`, `node_modules`, `package.json`. There should be NONE -- Phase 1 did NOT add any `importScripts('lattice...')` or `import 'lattice'` call inside the extension tree. If you see such errors, that is a FAIL (it means an extension file was accidentally modified to import Lattice -- Option B forbids this in Phase 1).

c) **Popup opens.** Click the FSB extension's toolbar icon -- the popup should render normally. No FAIL on this assertion unless the popup fails to load.

d) **Sidepanel opens.** Open the FSB sidepanel (via the extension's normal entry point -- right-click extension icon -> "Open side panel", or whatever FSB's standard route is). Side panel renders normally.

e) **An existing autopilot iteration still works.** Trigger one short autopilot iteration on a benign site (e.g., visit any page, ask FSB to "click the link to /about" or similar minimal task). The iteration should complete or progress at least one step. We are NOT testing Lattice integration here; we are testing that FSB's existing iterator (INV-04 `setTimeout` chain at `extension/ai/agent-loop.js:1824/2418/2487/2497`) still works.

Step 4 -- capture evidence for the SUMMARY:

- Take ONE screenshot of the `chrome://extensions` SW DevTools console showing no NEW errors. Save the screenshot path (or paste the relevant console excerpt as text) into a note for the SUMMARY.
- Note the timestamp of the reload.
- Note any pre-existing errors that were present BEFORE the reload AND remain after (these are not Phase 1's responsibility but should be acknowledged).

Step 5 -- the resume signal:

- If all five assertions (a-e) pass: type `approved` to resume execution. The executor will proceed to Task 5 (the wrap-up commit).
- If any assertion fails: type `failed: <description>` and the executor will STOP for triage. Possible causes:
  - `package.json` has malformed JSON (`npm install` left it broken)
  - An `extension/` file was accidentally modified (a Phase 1 task overstepped its scope)
  - A pre-existing extension bug surfaced (not Phase 1's responsibility, but the executor must record it in the SUMMARY)
  </how-to-verify>
  <resume-signal>Type `approved` if all five assertions (a-e) pass. Type `failed: <description>` if any assertion fails.</resume-signal>
  <files>n/a -- no files modified by this checkpoint task; it is purely an out-of-band human verification of Phase 1's tree-state stability inside the Chrome extension runtime.</files>
  <action>Human-only checkpoint -- the executor presents the `<what-built>` summary and `<how-to-verify>` instructions to the developer, then PAUSES execution until the developer types `approved` (proceed to Task 5) or `failed: <description>` (STOP for triage). The executor MUST NOT proceed to Task 5 until the resume signal is received. See `<how-to-verify>` above for the five assertions (a-e) the developer must confirm; see `<what-built>` for the pre-checkpoint state summary.</action>
  <verify>
    <automated>echo "checkpoint:human-verify -- waits for developer resume signal; no automated verification step. The developer's typed reply (approved | failed: ...) is captured in the SUMMARY along with screenshot/log evidence per the how-to-verify instructions."</automated>
  </verify>
  <done>
    <acceptance_criteria>
      - Developer typed `approved` (or equivalent positive resume signal) after performing the reload + the five assertions (a-e).
      - All five assertions a-e passed (recorded in the SUMMARY -- Task 5 captures this).
      - Screenshot OR console-log excerpt of the post-reload SW DevTools is captured for the SUMMARY artifact.
      - Reload timestamp is captured.
      - Any pre-existing-not-caused-by-Phase-1 errors are noted explicitly so the SUMMARY does not misattribute them.
      - If `failed: <description>` was typed, execution STOPPED here -- Task 5 did NOT run. This is the abort path; the executor reports the failure description.
    </acceptance_criteria>
  </done>
</task>

<task type="auto">
  <name>Task 5: Commit FSB-side changes (package.json, smoke, LATTICE-PIN.md), confirm INV-04 baseline</name>
  <files>package.json, package-lock.json, tests/lattice-smoke.test.js, .planning/LATTICE-PIN.md</files>
  <read_first>
    - .planning/phases/01-lattice-gap-survey-scaffold/01-01-PLAN.md (Plan 01's success criteria reference for the commit message body)
    - .gitignore (confirm `.planning/` is gitignored -- the LATTICE-PIN.md add requires `-f`)
    - All four files being committed (sanity check the diff before staging)
    - Existing FSB commit-message style: run `git log -5 --format="%s"` to mirror tone
  </read_first>
  <action>
Step 1 -- final pre-commit sanity:
- Run: `npm test` -- assert exit code 0 (one more confirmation; the smoke runs at the end of the chain).
- Run: `node tests/tool-definitions-parity.test.js` -- assert exit code 0 (INV-01 holds).
- Run: `grep -c "setTimeout" extension/ai/agent-loop.js` -- assert the output is 8 (INV-04 baseline -- the `setTimeout`-chained iterator is unchanged; the count was 8 in the planner's verification).
- Run: `git diff --name-only` -- assert the modified files are EXACTLY: `package.json`, `package-lock.json`. (`.planning/LATTICE-PIN.md` and `tests/lattice-smoke.test.js` should appear under "Untracked files" or "Changes to be committed" if already added; verify via `git status --short`.)

Step 2 -- stage the four files. Use `-f` for the gitignored `.planning/` path:

```bash
git add package.json package-lock.json tests/lattice-smoke.test.js && git add -f .planning/LATTICE-PIN.md
```

Verify the staging:
- Run: `git status --short` -- output should show four staged files (M for package.json, M for package-lock.json, A for tests/lattice-smoke.test.js, A for .planning/LATTICE-PIN.md). No unstaged changes related to Phase 1.

Step 3 -- create the commit. Use a HEREDOC to ensure proper multi-line formatting. The commit message references the FSB phase, the Lattice SHA, and the three D-12 pass criteria:

```bash
LATTICE_SHA=$(cd lattice && git rev-parse fsb-integration-experiments)
LATTICE_SHA_SHORT=$(cd lattice && git rev-parse --short fsb-integration-experiments)
git commit -m "$(cat <<EOF
feat(phase-1): wire Lattice as path dep + add round-trip smoke test

FSB-side scaffolding for v0.10.0-attempt-2 Phase 1 (Lattice SDK gap survey
+ integration scaffolding). Lattice-side artifacts landed in Plan 01 on
\`lattice/fsb-integration-experiments\` (Lattice HEAD pinned at ${LATTICE_SHA_SHORT}).

Changes:
- package.json: add \"lattice\": \"file:./lattice/packages/lattice\" dependency
  (D-05; npm 11 file: spec verified empirically to symlink-resolve correctly,
  per Phase 1 RESEARCH.md). Append \`node tests/lattice-smoke.test.js\` to the
  scripts.test && chain (D-11).
- tests/lattice-smoke.test.js: real-runtime smoke proving the FSB <-> Lattice
  Capability Receipt round-trip via dynamic ESM import. Generates an ephemeral
  Ed25519 keypair per run (no committed key material), mints one receipt via
  lattice.createReceipt, verifies envelope round-trip via lattice.verifyReceipt,
  asserts the typed VerifyResult contract (ok=true on positive path; ok=false
  with error.kind=signature-invalid on negative path). Follows FSB's existing
  test convention: 'use strict', CJS, manual pass/fail counters, raw node
  invocation. (D-10, D-11, D-12 #2; reconciliation D-12 #2 CLARIFIED -- this
  is the substantive proof of FSB <-> Lattice round-trip.)
- .planning/LATTICE-PIN.md: cross-repo audit-trail index. Frontmatter records
  current_lattice_sha (${LATTICE_SHA_SHORT}) + current_branch
  (fsb-integration-experiments). Body table records the Phase 1 row pointing
  at Lattice's HEAD post-Plan 01 commits. (D-08, D-16; CD-06 RESOLVED -- markdown
  table with frontmatter for current pin.)

Phase 1 reconciliation block (Option B) honored:
- No \"type\": \"module\" added to extension/manifest.json
- No extension/lattice-bridge.js created
- No in-extension import of Lattice in any context (SW / offscreen / sidepanel)
- Manual MV3 sanity reload check (D-12 #3 AMENDED) confirms Phase 1's tree
  additions do not destabilize the extension (evidence in SUMMARY).

Invariants verified:
- INV-01 MCP wire contracts UNTOUCHED -- tool-definitions-parity.test.js
  continues to pass byte-identically.
- INV-04 MV3-survivability preserved -- extension/ai/agent-loop.js setTimeout
  call count unchanged (8 occurrences at the four pinned sites).
- INV-06 -- the primitive (createReceipt + verify) lives in Lattice; FSB code
  here is integration glue only (file: dep + smoke + audit trail).

Lattice pinned SHA: ${LATTICE_SHA}
Lattice branch: fsb-integration-experiments
EOF
)"
```

Step 4 -- post-commit verification:
- Run: `git log -1 --format=%H` -- capture the FSB commit SHA as `FSB_COMMIT_SHA`.
- Run: `git log -1 --format=%s` -- assert the subject starts with `feat(phase-1):` (matches FSB's existing commit style based on recent log).
- Run: `git log -1 --format=%B | grep "Lattice pinned SHA:"` -- assert the commit body contains the Lattice SHA reference.
- Run: `git status --porcelain` -- assert empty (clean working tree post-commit).
- Run: `npm test` ONE MORE TIME -- assert exit code 0 (proves the commit's state is the same as the pre-commit-tested state).

Step 5 -- final phase verification (all three D-12 pass criteria):
- D-12 #1 (audit doc landed): `cd lattice && git show fsb-integration-experiments:docs/fsb-integration-gaps.md | head -5` -- assert output begins with `# FSB Integration Gaps`.
- D-12 #2 (Node smoke green): `node tests/lattice-smoke.test.js && npm test` -- both exit 0.
- D-12 #3 AMENDED (manual MV3 sanity): user typed `approved` at Task 4 checkpoint. Evidence captured in SUMMARY.
  </action>
  <verify>
    <automated>git log -1 --format=%s | grep -E "^feat\(phase-1\)" && git log -1 --format=%B | grep "Lattice pinned SHA:" && git status --porcelain | wc -l | tr -d ' ' | grep -E "^0$" && node tests/lattice-smoke.test.js && node tests/tool-definitions-parity.test.js && npm test && cd lattice && git show fsb-integration-experiments:docs/fsb-integration-gaps.md > /dev/null</automated>
  </verify>
  <done>
    <acceptance_criteria>
      - One new commit on FSB's `automation` branch with subject starting `feat(phase-1):`
      - Commit body contains the literal phrase `Lattice pinned SHA:` followed by a 40-char hex SHA
      - Commit body references all three D-12 pass criteria (D-12 #1 / #2 / #3 OR #1, #2, and the AMENDED #3 by name)
      - Commit body explicitly states Option B reconciliation is honored (`no extension/lattice-bridge.js`, `no manifest.json change`, `no in-extension import`)
      - `git diff HEAD~1 HEAD --name-only` outputs exactly four paths: `.planning/LATTICE-PIN.md`, `package-lock.json`, `package.json`, `tests/lattice-smoke.test.js` (in any order)
      - `git status --porcelain` outputs empty (clean working tree post-commit)
      - `npm test` exits 0 (the full chain including the smoke is green at the committed state)
      - `node tests/tool-definitions-parity.test.js` exits 0 (INV-01 byte-identity holds at the committed state)
      - `grep -c "setTimeout" extension/ai/agent-loop.js` returns 8 (INV-04 -- `setTimeout`-chained iterator preserved at the committed state)
      - `cd lattice && git rev-parse fsb-integration-experiments` returns the same SHA recorded in `.planning/LATTICE-PIN.md` frontmatter `current_lattice_sha` (no drift between Plan 01's Lattice HEAD and FSB's PIN.md)
      - `git log -1 --format=%B` body does NOT contain emojis (per CLAUDE.md global rule)
    </acceptance_criteria>
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FSB test process -> `node_modules/lattice` symlink -> Lattice working tree | After `npm install`, FSB's Node test process resolves `import 'lattice'` to `lattice/packages/lattice/dist/index.js` via the symlink. Anything in `lattice/packages/lattice/` is reachable from the test process. Bounded scope: Phase 1 does NOT import Lattice from any extension context (Option B), so the in-runtime trust surface is the test process only. |
| FSB extension runtime (SW + sidepanel + offscreen) -> nothing Lattice-related in Phase 1 | Per the reconciliation block, no extension file is modified. The extension's trust surface is exactly what it was pre-Phase-1. |
| FSB's `.planning/` directory (gitignored) -> tracked via `git add -f` | `.planning/LATTICE-PIN.md` is `git add -f`'d into the FSB repo's tracked tree. The directory is otherwise gitignored. The file content includes a Lattice SHA but no secrets. |
| Test process -> ephemeral Ed25519 keypair material | The smoke generates a private+public JWK keypair via `lattice.generateEd25519KeyPairJwk()` and discards both at process exit. No persistence. No log of private material. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | Smoke test ships a hardcoded private key in `tests/lattice-smoke.test.js`, allowing anyone with repo access to forge receipts that look like FSB's smoke-test output | mitigate | Acceptance criteria of Task 2 explicitly check the file calls `generateEd25519KeyPairJwk()` and does NOT contain a hardcoded `privateKeyJwk` literal. Each test run mints fresh material; nothing is committed. |
| T-02-02 | Tampering | `npm install` of the `file:` dep silently mutates `tests/` or `extension/` (e.g., through a malicious postinstall hook from a transitive dep) | mitigate | Task 1 acceptance criteria check `git status --porcelain extension/` is empty and `git status --porcelain mcp/` is empty AFTER `npm install`. Any unexpected modification aborts the task. Lattice has no postinstall hook (verified by reading `lattice/packages/lattice/package.json`); Lattice's transitive deps are pinned via Lattice's `pnpm-lock.yaml`. |
| T-02-03 | Tampering | The smoke test passes against a stale `dist/index.js` (e.g., from a previous Lattice build) and the `createReceipt` re-export is actually missing from the latest Lattice source | mitigate | Plan 01's Task 1 rebuilds Lattice with `clean: true` (verified in tsdown.config.ts); Task 1 here verifies the dynamic import resolves `lattice.createReceipt` as a function BEFORE Task 2 writes the smoke. The smoke itself also asserts `typeof lattice.createReceipt === 'function'` as a surface presence check before invoking the round-trip. |
| T-02-04 | Repudiation | The Phase 1 FSB commit does not reference the Lattice SHA, so post-hoc forensics cannot tie FSB's tree state to a specific Lattice tree state | mitigate | Task 5's commit body explicitly includes `Lattice pinned SHA: <40-char-sha>` AND `Lattice branch: fsb-integration-experiments`. The `.planning/LATTICE-PIN.md` frontmatter also records the SHA as a tracked artifact. Two redundant records. |
| T-02-05 | Information Disclosure | The smoke test leaks the ephemeral private key to stdout or to a test log file | mitigate | The smoke's code (per the Task 2 action body) does NOT log `privateKeyJwk`. Acceptance criteria do not require it. The only logged material is the receipt body fields (which contain no key material -- the receipt body has `kid` (a string id) but never the JWK itself). Manual review of the test file's `console.log(...)` calls confirms no private material is printed. |
| T-02-06 | Information Disclosure | The smoke test commits a real provider API key into the test fixtures (e.g., via a hardcoded route.providerId / capabilityId that looks like a real provider tag) | accept | Severity: low. The smoke uses stub values (`fsb-smoke` for providerId, `fsb-smoke/round-trip` for capabilityId). These are not real provider names; even if the file is publicly committed, no key material is exposed. |
| T-02-07 | Denial of Service | `npm test` takes too long because Plan 01's Lattice build introduces a heavy dependency | mitigate | The smoke itself is ~2 seconds local (per VALIDATION.md). The full `npm test` chain runs unchanged for ~100+ other tests; the smoke is appended as one additional ~2-second step at the end. No regression in chain runtime expected. If the chain unexpectedly grows by >5 seconds, Task 5 STOPs and reports. |
| T-02-08 | Elevation of Privilege | The `file:` dep symlink lets a malicious user with write access to `lattice/packages/lattice/dist/` inject code that runs during the next `npm test` | accept | Severity: low for Phase 1 (test-process only, no in-extension import). Same developer has write access to FSB's tree itself (the symlink does not amplify privilege). If a future phase introduces in-extension Lattice import, this threat must be revisited at elevated severity. |
| T-02-09 | Tampering | INV-04 violation -- the `setTimeout`-chained iterator at `extension/ai/agent-loop.js:1824/2418/2487/2497` is accidentally modified during this plan | mitigate | All five tasks verify `grep -c "setTimeout" extension/ai/agent-loop.js` returns 8 (the planner-verified baseline). Any change in the count aborts the task. |
| T-02-10 | Tampering | INV-01 byte-identity between `extension/ai/tool-definitions.js` and `mcp/ai/tool-definitions.cjs` accidentally broken | mitigate | All five tasks run `node tests/tool-definitions-parity.test.js` and assert exit 0. The full `npm test` chain also runs this test. |

Block on: T-02-01, T-02-02, T-02-03, T-02-04, T-02-09, T-02-10 (any of these failing aborts the relevant task; the executor STOPs and reports).
</threat_model>

<verification>
## Plan-level overall checks

After all five tasks complete:

1. `git log -1 --format=%s` on FSB's `automation` branch starts with `feat(phase-1):`.
2. `git diff HEAD~1 HEAD --name-only` lists exactly: `.planning/LATTICE-PIN.md`, `package-lock.json`, `package.json`, `tests/lattice-smoke.test.js`.
3. `npm test` exits 0 (the full chain green; D-12 #2).
4. `node tests/lattice-smoke.test.js` exits 0 (the smoke standalone).
5. `node tests/tool-definitions-parity.test.js` exits 0 (INV-01).
6. `grep -c "setTimeout" extension/ai/agent-loop.js` returns 8 (INV-04 baseline).
7. `cd lattice && git rev-parse fsb-integration-experiments` matches `.planning/LATTICE-PIN.md` frontmatter `current_lattice_sha`.
8. `git status --porcelain` is empty (no leftover modifications).
9. Manual MV3 sanity reload check evidence is captured in the SUMMARY (user typed `approved` at Task 4).
10. NO Lattice remote push occurred (Plan 01 covered this assertion; reaffirmed by the absence of any `cd lattice && git push` in Plan 02's task history).

## D-12 Pass-criteria roll-up (all three)

| # | Check | Status at end of plan |
|---|-------|----------------------|
| D-12 #1 | `lattice/docs/fsb-integration-gaps.md` exists on `fsb-integration-experiments` covering all 6 surfaces with severity tags | Closed by Plan 01 Task 2 + committed in Plan 01 Task 3 (`docs(fsb-integration):` commit on Lattice branch); cross-checked by Plan 02 Task 5 acceptance: `cd lattice && git show fsb-integration-experiments:docs/fsb-integration-gaps.md` succeeds. |
| D-12 #2 | `npm test` runs `tests/lattice-smoke.test.js` and the smoke passes (one receipt minted via Lattice) | Closed by Plan 02 Tasks 1, 2, 5. Substantive proof: smoke calls `createReceipt`, `verifyReceipt`, asserts `result.ok === true`. |
| D-12 #3 AMENDED | Manual MV3 sanity reload check: extension reloads cleanly; no NEW SW or console errors caused by Phase 1's tree additions; existing extension features still work | Closed by Plan 02 Task 4 (human verification checkpoint, captured in SUMMARY). |
</verification>

<success_criteria>
Plan 02 is complete when:

1. `package.json` declares `"lattice": "file:./lattice/packages/lattice"` and `scripts.test` chain ends with `... && node tests/lattice-smoke.test.js`.
2. `node_modules/lattice` exists as a working symlink and `node -e "import('lattice')"` resolves the public surface (createReceipt/verifyReceipt/createInMemorySigner/generateEd25519KeyPairJwk/createMemoryKeySet).
3. `tests/lattice-smoke.test.js` exists, mints + verifies one Capability Receipt round-trip with an ephemeral Ed25519 keypair, and exits 0.
4. `npm test` exits 0 (full chain green).
5. `.planning/LATTICE-PIN.md` exists, has frontmatter recording the current Lattice SHA + branch, has a Phase 1 row in the per-phase log table.
6. Manual MV3 sanity reload checkpoint passed (user typed `approved`; evidence in SUMMARY).
7. INV-01 verified (`tool-definitions-parity.test.js` still byte-identical-passes).
8. INV-04 verified (`agent-loop.js` setTimeout count == 8, unchanged).
9. INV-06 verified by construction (FSB-side code is integration glue; no primitive lives outside Lattice).
10. One FSB commit on `automation` branch with subject starting `feat(phase-1):`, body referencing the pinned Lattice SHA.
11. Phase 1's three D-12 pass criteria all green (D-12 #1 from Plan 01, D-12 #2 + #3 AMENDED from Plan 02).
</success_criteria>

<output>
After plan completion, create `.planning/phases/01-lattice-gap-survey-scaffold/01-02-SUMMARY.md` capturing:

- FSB commit SHA on `automation` and its subject.
- Lattice pinned SHA (post-Plan-01) referenced by FSB's PIN.md and the FSB commit body; assert they match `cd lattice && git rev-parse fsb-integration-experiments` at SUMMARY-write-time (no drift).
- `npm test` output -- specifically the smoke's PASS lines + the final `passed: <N>, failed: 0` line.
- INV-01 confirmation: `node tests/tool-definitions-parity.test.js` exit 0 + the byte-count parity log line.
- INV-04 confirmation: `grep -c "setTimeout" extension/ai/agent-loop.js` returns 8.
- Manual MV3 sanity reload evidence (D-12 #3 AMENDED):
  - Reload timestamp.
  - Screenshot path OR console-log excerpt showing no NEW errors caused by Phase 1.
  - PASS/FAIL for each of the five assertions (a-e from Task 4).
  - Any pre-existing errors noted as not-Phase-1's-responsibility.
- D-12 pass-criteria roll-up: all three checked + evidence path for each.
- File diff summary: four files committed (paths + delta line counts).
- One-line "what's next": Phase 2 picks up the Blocker rows from `lattice/docs/fsb-integration-gaps.md` (likely the Receipts + Observability domains -- receipt-shape extensions + step-transition tracing kinds).

SUMMARY must NOT use emojis (per project CLAUDE.md global rule).
</output>
