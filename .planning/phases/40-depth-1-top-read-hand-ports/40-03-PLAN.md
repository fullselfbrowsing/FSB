---
phase: 40-depth-1-top-read-hand-ports
plan: 03
type: execute
wave: 2
depends_on: ["40-01"]
files_modified:
  - catalog/handlers/slack.js
autonomous: true
requirements: [DEPTH-01]

must_haves:
  truths:
    - "3 slack READ slugs (list_channels, list_members, get_channel_info) ship as T1a heads on https://app.slack.com"
    - "Each slack slug UPGRADES its existing opentabs descriptor dom->T1a (slug byte-exact)"
    - "Reads reuse callSlackMethod: xoxc scraped into the BODY (never a header), xoxd rides the cookie"
    - "The scraped xoxc token never reaches a console/log line"
    - "A logged-out / token-missing path fails closed to RECIPE_DOM_FALLBACK_PENDING (no false success)"
  artifacts:
    - path: "catalog/handlers/slack.js"
      provides: "3 new slack READ T1a handlers via callSlackMethod (no new module)"
      contains: "slack.list_channels"
  key_links:
    - from: "catalog/handlers/slack.js"
      to: "FsbCapabilityCatalog.registerHandler"
      via: "self-registration of the 3 exact opentabs slugs at load"
      pattern: "registerHandler"
    - from: "catalog/handlers/slack.js slack.list_channels"
      to: "callSlackMethod"
      via: "reuse the existing xoxc-probe + body-placement helper"
      pattern: "callSlackMethod"
---

<objective>
EXTEND the existing FsbHandlerSlack head (catalog/handlers/slack.js) with the 3 top READ
opentabs slugs, reusing the proven callSlackMethod token-scrape contract (origin
https://app.slack.com, xoxc scraped from the page into the request BODY, xoxd HttpOnly
cookie rides same-origin). NO new module — these add slugs to the existing
FsbHandlerSlack global, so HEAD_HANDLER_MODULES count is UNCHANGED.

DELIVERS SC1 (3 of the 8-12 READ heads ship + upgrade), SC2 (executeBoundSpec-only via
callSlackMethod, token in body never logged), SC3 (registered into the SAME catalog, same
front doors) for the extend-module path of the DEPTH-01 contract.

READ-only; slack.send_message and other writes are Phase 41. The slugs are the EXACT
opentabs DOT-form slugs (slack.list_channels etc.) so resolve() upgrades the breadth
descriptor dom->T1a; they are DISTINCT from the existing head slug slack.conversations.list
(no collision).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-depth-1-top-read-hand-ports/40-CONTEXT.md
@catalog/handlers/slack.js
@tests/capability-head-handlers.test.js

<interfaces>
Existing slack.js helpers to REUSE unchanged: SLACK_ORIGIN='https://app.slack.com';
buildXoxcProbeSpec() (the from:'response' GET probe); readXoxcToken(probeResult) (pulls
xoxc, never logs it); buildSlackBody(token, fields) (token in the BODY as a form field);
typedRecipeError(code, extra); and the shared async callSlackMethod(slug, method, fields,
ctx) which: runs the probe, returns the probe verbatim on failure, returns
RECIPE_DOM_FALLBACK_PENDING (reason 'missing-slack-xoxc', fellBackToDom:true) if no xoxc,
else POSTs SLACK_ORIGIN + '/api/' + method with the token in a form-encoded body and the
Accept:application/json header, calling executeBoundSpec. Add slugs by giving each a handle
that calls callSlackMethod with the correct Slack web-API method name + fields.

The 3 ported slack slugs (each EXISTS as catalog/descriptors/opentabs__slack__*.json,
backing:'dom', sideEffectClass:'read'):
  slack.list_channels      props limit,types,cursor,exclude_archived (none required) -> method conversations.list
  slack.list_members       required channel (+ limit,cursor)                         -> method conversations.members
  slack.get_channel_info   required channel                                          -> method conversations.info
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 3 slack READ slugs to slack.js via callSlackMethod</name>
  <files>catalog/handlers/slack.js</files>
  <behavior>
    - slack.list_channels, slack.list_members, slack.get_channel_info each: tier 'T1a',
      origin 'https://app.slack.com', sideEffectClass 'read', async handle.
    - Each handle delegates to callSlackMethod with the matching Slack web-API method
      (conversations.list / conversations.members / conversations.info) and the args mapped
      to fields; the existing helper places the scraped xoxc in the BODY (form field), POSTs
      same-origin /api/<method>, and calls executeBoundSpec.
    - params are closed JSON Schemas (additionalProperties:false): list_members and
      get_channel_info require channel; list_channels requires nothing.
    - A missing-token path returns RECIPE_DOM_FALLBACK_PENDING (no false success) via the
      existing helper; the source never console-logs xoxc/xoxd/token (existing T-29-08 scan).
    - The 3 new slugs self-register into FsbCapabilityCatalog (the existing registration loop
      already walks every key in the handlers object — adding entries is sufficient).
  </behavior>
  <action>
    Edit catalog/handlers/slack.js. Add closed params schemas (CHANNELS_LIST_PARAMS already
    exists as CONVERSATIONS_LIST_PARAMS shape; add LIST_MEMBERS_PARAMS { channel(req), limit,
    cursor } and CHANNEL_INFO_PARAMS { channel(req) }). Add three entries to the handlers
    object mirroring the existing slack.conversations.list entry: each tier 'T1a', origin
    SLACK_ORIGIN, sideEffectClass 'read', params, and an async handle that calls
    callSlackMethod('slack.<op>', '<conversations.method>', { ...mapped fields }, ctx). For
    list_channels pass types default 'public_channel,private_channel' + limit default; for
    list_members pass channel + limit; for get_channel_info pass channel. Do NOT modify the
    helpers, the existing two slugs, the registration loop, or the dual export — the loop and
    exports already cover new keys. Keep ASCII-only, NO emojis. The token stays only in the
    bound spec body (callSlackMethod already guarantees this).
  </action>
  <verify>
    <automated>node tests/capability-head-handlers.test.js; echo EXIT=$?</automated>
  </verify>
  <done>slack.js exposes the 3 new READ slugs as T1a on app.slack.com via callSlackMethod; the slack section of capability-head-handlers.test.js passes (gitlab/notion sections may RED until 40-02/40-04).</done>
</task>

<task type="auto">
  <name>Task 2: Build the ext copy + prove the slack dom->T1a upgrade</name>
  <files>catalog/handlers/slack.js</files>
  <action>
    Run `node scripts/package-extension.mjs` so the edited catalog/handlers/slack.js is
    copied to extension/catalog/handlers/slack.js (the head-handlers test asserts they
    match). Then run the 40-01 upgrade harness to prove the 3 slack slugs now resolve T1a
    (was dom/T3) with the descriptor preserved and slug byte-exact. If a slack row REDs, the
    likely cause is a slug typo (a dead duplicate, not an upgrade) — fix the slug string in
    slack.js to byte-match the opentabs descriptor and re-run. No HEAD_HANDLER_MODULES change
    (FsbHandlerSlack already in the manifest); no background.js change (already imported).
  </action>
  <verify>
    <automated>node tests/head-handler-upgrade.test.js; echo EXIT=$?</automated>
  </verify>
  <done>The 3 slack slugs resolve T1a with byte-exact descriptor slugs in the upgrade harness; the ext copy matches the source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page response -> readXoxcToken | the scraped per-workspace xoxc token crosses into the handler |
| xoxc token -> request body | the token authenticates the same-origin /api POST (body, not header) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-05 | Information disclosure | scraped xoxc on a log line | mitigate | reuse callSlackMethod/readXoxcToken (never logs); source-scan asserts no console names xoxc/xoxd/token (head-handlers test) |
| T-40-06 | Spoofing | xoxc in a header instead of the body | mitigate | reuse buildSlackBody body-placement; test asserts token in body, not headers |
| T-40-07 | Spoofing | spec.origin vs active tab | mitigate | SLACK_ORIGIN spec.origin; executeBoundSpec origin-pin rejects a mismatch before executeScript |
| T-40-08 | Elevation | a write method slipping in | mitigate | only conversations.* READ methods; writes are Phase 41 |
| T-40-SC | Tampering | npm/pip/cargo installs | accept | no package installs in this plan (extends existing source) |
</threat_model>

<verification>
- node tests/capability-head-handlers.test.js — the slack section passes.
- node tests/head-handler-upgrade.test.js — the 3 slack slugs resolve T1a byte-exact.
- node scripts/package-extension.mjs then npm run validate:extension — gates green; ext copy matches.
- HEAD_HANDLER_MODULES count UNCHANGED (no new module). Wall 2 origin-pin + token-in-body hold.
</verification>

<success_criteria>
- 3 slack READ slugs are T1a heads on https://app.slack.com via callSlackMethod, each upgrading its opentabs descriptor.
- xoxc stays in the body, never logged; missing-token fails closed.
- No new HEAD_HANDLER_MODULES entry.
</success_criteria>

<output>
Create `.planning/phases/40-depth-1-top-read-hand-ports/40-03-SUMMARY.md` when done.
</output>
