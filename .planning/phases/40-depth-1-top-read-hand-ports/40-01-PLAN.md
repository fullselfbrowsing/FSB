---
phase: 40-depth-1-top-read-hand-ports
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/head-handler-upgrade.test.js
  - tests/head-handler-cap.test.js
  - tests/capability-head-handlers.test.js
  - package.json
autonomous: true
requirements: [DEPTH-01]

must_haves:
  truths:
    - "A new test asserts each ported opentabs slug resolves dom->T1a with the descriptor preserved and the slug byte-exact"
    - "A wrong or duplicate slug FAILS the upgrade harness (correctness keystone enforced, not hoped)"
    - "head-handler-cap.test.js expects 4 modules (adds FsbHandlerGitlab) while CAP stays 30"
    - "The upgrade harness is wired into npm test"
  artifacts:
    - path: "tests/head-handler-upgrade.test.js"
      provides: "dom->T1a upgrade-assertion harness (slug-exact correctness gate)"
      min_lines: 80
    - path: "tests/head-handler-cap.test.js"
      provides: "head-cap <=30 + expected-globals now 4 (github/slack/notion/gitlab)"
      contains: "FsbHandlerGitlab"
  key_links:
    - from: "tests/head-handler-upgrade.test.js"
      to: "capability-catalog.js resolve()"
      via: "require(catalog) then require(handler) then resolve(slug, origin)"
      pattern: "resolve\\("
---

<objective>
Wave 0 test infra FIRST. Build the dom->T1a upgrade-assertion harness (the phase
CORRECTNESS KEYSTONE), update head-cap for the 4th module (gitlab) keeping CAP=30, and
scaffold the new per-app sections in the head-handler unit suite so each Wave-1 handler
plan touches ONLY its own source (no shared-test conflict, full parallelism).

Lands BEFORE any handler so a wrong/duplicate slug FAILS LOUD: a mis-registered slug mints
a dead duplicate instead of upgrading the breadth descriptor, and only this harness catches
it.

DELIVERS SC1 + SC3 because: the upgrade harness is the only proof that "each hand-port
UPGRADES its existing opentabs slug dom->T1a" (vs minting a dead duplicate), and the cap
test freezes the module count <=30.

App-selection correction (decision_note): linear is DROPPED to Phase 41 (its GraphQL is a
SEPARATE origin client-api.linear.app reached via CORS = Phase-41 CORS-gate material),
REPLACED by gitlab (same-origin gitlab.com/api/v4). Final set = 10 READ heads: gitlab x5,
slack x3, notion x2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-depth-1-top-read-hand-ports/40-CONTEXT.md
@.planning/phases/40-depth-1-top-read-hand-ports/40-VALIDATION.md
@tests/learned-t2-outranking.test.js
@tests/head-handler-cap.test.js
@tests/capability-head-handlers.test.js

<decision_note>
CONTEXT names linear the flagship single-origin PRIMARY. Vendored source contradicts it:
vendor/opentabs-snapshot/plugins/linear/src/linear-api.ts lines 12-18 — Linear GraphQL is
https://client-api.linear.app/graphql, a SEPARATE subdomain from linear.app working only
via CORS (allow-origin: https://linear.app + allow-credentials: true). The Wall-2 origin-pin
(capability-fetch.js:299) compares tabOrigin against spec.origin; a client-api.linear.app
host is the FORBIDDEN separate-API host, and making it fetch requires leaning on cross-origin
CORS — which CONTEXT + ROADMAP assign to Phase 41's CORS-gate. Resolution: honor the locked
first-party-origin-ONLY/Wall-2 constraint over the illustrative app name (app selection is
explicit Claude's-discretion); defer linear to 41; substitute gitlab. notion + slack are
confirmed genuinely same-origin in the vendored source.
</decision_note>

<interfaces>
Loader idiom (tests/learned-t2-outranking.test.js:75-86): require(capability-catalog.js)
returns resolve + registerHandler + seedHeadHandlers AND sets global.FsbCapabilityCatalog;
requiring a handler module self-registers its slugs (the global is set at require time).

resolve(slug, origin) for a registered T1a head -> tier 'T1a' + handler + origin + descriptor
  (REGISTRY-first: capability-catalog.js:329, :396-403)
resolve(slug, origin) for a descriptor-only backing:'dom' slug with no registry entry ->
  tier 'T3' + descriptor (capability-catalog.js:347-355 = the BEFORE state)
Upgrade proof = SAME slug returns T3 with only the descriptor, T1a once the handler registers.

The 10 ported slugs (each EXISTS as catalog/descriptors/opentabs__APP__OP.json, backing:'dom',
sideEffectClass:'read', DOT-form slug):
  gitlab.list_projects, gitlab.get_project, gitlab.list_issues, gitlab.get_issue,
  gitlab.list_merge_requests  (origin https://gitlab.com)
  slack.list_channels, slack.list_members, slack.get_channel_info  (origin https://app.slack.com)
  notion.search, notion.get_database  (origin https://www.notion.so)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the dom->T1a upgrade-assertion harness (correctness keystone)</name>
  <files>tests/head-handler-upgrade.test.js</files>
  <behavior>
    - For EACH of the 10 ported slugs: after requiring capability-catalog.js + the handler,
      resolve(slug, origin) returns tier 'T1a' (NOT 'T3').
    - The resolved entry descriptor.slug equals the ported slug BYTE-EXACT; the entry exposes
      a handler with an async handle.
    - NEGATIVE CONTROL: a deliberately-wrong slug (gitlab.list_projectz) does NOT resolve
      T1a (null or T3) — a mis-registered slug is a dead duplicate the harness catches.
    - BEFORE/AFTER for one slug: with FsbRecipeIndex seeded with the backing:'dom' descriptor
      but the handler NOT required, resolve() is 'T3'; after requiring the handler, 'T1a'.
      Fresh-require the catalog so REGISTRY starts empty for the BEFORE leg.
    - Zero-framework: passed/failed + check(cond,msg) + process.exit(failed>0?1:0). ASCII only.
  </behavior>
  <action>
    Create tests/head-handler-upgrade.test.js mirroring the loader idiom in
    tests/learned-t2-outranking.test.js:75-86. require() the catalog (sets
    global.FsbCapabilityCatalog), require() each handler under test (github/slack/notion
    exist; require gitlab only if existsSync so the harness does not crash pre-Wave-1), then
    call seedHeadHandlers() once (exercises both the self-register and the manifest path).
    Assert resolve(slug, originForThatApp) for each ported slug per the behavior block; build
    the slug->origin table from the interfaces list (do NOT invent slugs). For BEFORE/AFTER,
    set global.FsbRecipeIndex to recipes:[] + descriptors:[ the real backing:'dom' descriptor
    JSON for one slug ] and delete require.cache for the catalog path between the BEFORE
    (handler absent) and AFTER (handler required) legs. A not-yet-written gitlab.js -> a
    single deterministic FAIL for the gitlab rows (correct Wave-0 RED). Refs:
    capability-catalog.js:329 / :347-355 / :396-403.
  </action>
  <verify>
    <automated>node tests/head-handler-upgrade.test.js; echo EXIT=$?</automated>
  </verify>
  <done>Harness exists, runs without crashing (EXIT 0 or 1; 1 tolerated in Wave 0 for the pre-handler gitlab rows), asserts T1a + byte-exact slug for the 10 slugs + the wrong-slug negative control + a BEFORE(T3)/AFTER(T1a) proof. github/slack/notion rows PASS. 40-05 requires EXIT 0.</done>
</task>

<task type="auto">
  <name>Task 2: Update head-handler-cap.test.js for the 4th module (KEEP CAP=30)</name>
  <files>tests/head-handler-cap.test.js</files>
  <action>
    Edit tests/head-handler-cap.test.js. Leave CAP=30 UNCHANGED (line 34). Add
    'FsbHandlerGitlab' to EXPECTED_HEAD_GLOBALS (line 38) so it has 4 entries. Change the
    exact-count check (lines 81-82) from triple-equals 3 to triple-equals 4 and update its
    message to "exactly 4 entries (Phase 40 adds gitlab; the head stays <=30)". The <=CAP
    assertion (lines 77-78) is unchanged (4<=30 passes). Do NOT touch the array-literal
    extraction regex. NOTE: this reads HEAD_HANDLER_MODULES from capability-catalog.js
    source, so it REDs until 40-02 adds the FsbHandlerGitlab array entry — that cross-plan
    RED closes GREEN in 40-02 / 40-05.
  </action>
  <verify>
    <automated>grep -c "FsbHandlerGitlab" tests/head-handler-cap.test.js</automated>
  </verify>
  <done>head-handler-cap.test.js keeps CAP=30, EXPECTED_HEAD_GLOBALS lists 4 incl FsbHandlerGitlab, count assertion is 4. (Test RED until 40-02 adds the array entry; closes in the final battery.)</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold per-app sections in capability-head-handlers.test.js + wire upgrade harness into npm test</name>
  <files>tests/capability-head-handlers.test.js, package.json</files>
  <action>
    (A) tests/capability-head-handlers.test.js — ADD three per-app assertion sections that
    Wave-1 plans turn GREEN, mirroring the existing github/slack/notion sections (lines
    122-307) and reusing the makeCtx recording stub (line 78). Scaffolded HERE so each Wave-1
    plan edits ONLY its handler source, never this shared file.
      GITLAB: require catalog/handlers/gitlab.js (guarded existsSync). For gitlab.list_projects
        and gitlab.get_issue assert tier 'T1a', origin 'https://gitlab.com', sideEffectClass
        'read', async handle. Source scan: must contain '/api/v4' and must NOT contain
        'api.gitlab.com'; must NOT match the chrome scripting/tabs pattern. Behavior:
        gitlab.list_projects.handle({}, makeCtx('https://gitlab.com',41).ctx) calls
        executeBoundSpec once with spec.method 'GET', spec.origin 'https://gitlab.com', and
        spec.url containing '/api/v4/projects'.
      SLACK: for slack.list_channels and slack.get_channel_info assert tier 'T1a', origin
        'https://app.slack.com', sideEffectClass 'read'; POST spec pinned to app.slack.com,
        token in the BODY not a header (reuse lines 224-244 + the slackProbeText ctx option).
      NOTION: for notion.search and notion.get_database assert tier 'T1a', origin
        'https://www.notion.so', sideEffectClass 'read'; POST spec hits same-origin '/api/v3'
        pinned to www.notion.so (mirror lines 295-306).
    Do NOT weaken existing assertions. Add the sections AFTER notion and BEFORE Reddit. They
    RED until the Wave-1 handlers land (expected cross-plan RED).
    (B) package.json — in scripts.test, insert the command "node tests/head-handler-upgrade.test.js"
    immediately after the existing "node tests/head-handler-cap.test.js" entry (the chain is
    a long and-joined string). Do NOT reorder other tests.
  </action>
  <verify>
    <automated>grep -c "head-handler-upgrade.test.js" package.json</automated>
  </verify>
  <done>capability-head-handlers.test.js carries gitlab/slack/notion per-app scaffolds (RED until Wave-1 lands); package.json runs head-handler-upgrade.test.js on every npm test.</done>
</task>

</tasks>

<verification>
- node tests/head-handler-upgrade.test.js runs (EXIT 0 or 1 in Wave 0; github/slack/notion PASS).
- grep confirms FsbHandlerGitlab in the cap test + the npm-test wiring.
- No production source touched here (test infra only) — Wall 1/2 + INV-01/02 untouched.
</verification>

<success_criteria>
- The dom->T1a slug-exact upgrade harness exists and is enforced (wrong/duplicate slug FAILS;
  a BEFORE(T3)/AFTER(T1a) leg proves the upgrade).
- head-handler-cap.test.js encodes the 4-module expectation with CAP=30 unchanged.
- Wave-1 handler plans can each edit ONLY their own handler source (per-app sections pre-scaffolded).
- npm test runs the upgrade harness.
</success_criteria>

<output>
Create `.planning/phases/40-depth-1-top-read-hand-ports/40-01-SUMMARY.md` when done.
</output>
