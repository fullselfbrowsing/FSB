# 51-05 Plan: Pattern-D and GAPI Bridge Terminal Holds

## Goal

Finish the Pattern-D/GAPI bridge workstream without weakening Wall 2: every bridge candidate must have an explicit disabled decision unless and until a separate approved bridge implementation exists.

## Tasks

1. Preserve the current bridge rejection gate.
   - Keep Pattern-D and GAPI candidate rows discovery-pending.
   - Keep execution disabled for `pattern-d-candidate` and `gapi-bridge-candidate` rows.
   - Preserve separate-origin negative controls for Linear, Datadog, and Jira.

2. Add descriptor-level bridge accounting.
   - Generate a terminal-state report that gives every Pattern-D/GAPI row a `bridge-needed` surface state.
   - Record the disabled bridge decision on each candidate row.
   - Keep candidate rows out of handler/recipe proof claims.

3. Add regression coverage.
   - Assert bridge-needed rows exist and all have execution disabled.
   - Assert bridge candidates remain non-invocable until an approved bridge design exists.

## Verification

- `node scripts/verify-pattern-d-gapi-gate.mjs`
- `node tests/pattern-d-gapi-gate.test.js`
- `node tests/t1-terminal-states.test.js`

## Exit Criteria

- Pattern-D/GAPI candidates are explicitly accounted for as bridge-needed, not T1-ready.
- No generic Pattern-D or GAPI bridge is enabled.
- Search/readiness surfaces cannot imply bridge rows are invocable.
