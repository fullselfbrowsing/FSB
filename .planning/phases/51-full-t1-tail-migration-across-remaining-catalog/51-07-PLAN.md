# 51-07 Plan: Write/Destructive Live-UAT Activation Ledger

## Goal

Close the write/destructive Phase 51 workstream conservatively: do not activate guessed mutations, and ledger every write/destructive descriptor as active-with-evidence, guarded fail-closed, blocked-policy, or live-UAT-required.

## Tasks

1. Reuse the Phase 49 write activation evidence gate.
   - Keep existing active write evidence and guarded fail-closed records authoritative.
   - Preserve failure on unrecorded active write activation.
   - Preserve no-secret evidence scanning.

2. Generate a write/destructive UAT ledger.
   - Include every write/destructive descriptor from the readiness matrix.
   - Mark tail mutations as `not-activated-live-uat-required`.
   - Mark existing guarded writes as `guarded-fail-closed`.
   - Mark blocked policy writes separately.

3. Add regression coverage.
   - Assert non-active write/destructive rows have `activationAllowed: false`.
   - Assert required evidence fields are present for every ledger row.
   - Assert active rows have evidence references.

## Verification

- `node scripts/verify-write-activation-evidence.mjs`
- `node tests/t1-terminal-states.test.js`
- `npm run validate:extension`

## Exit Criteria

- No new write/destructive capability activates without live UAT evidence.
- The write/destructive tail is fully accounted for in `51-WRITE-UAT-LEDGER.md/json`.
