---
phase: 29-catalog-tiered-router-bundled-head-declarative-tail-autopilo
plan: 04
type: execute
wave: 2
depends_on: ["29-02"]
files_modified:
  - extension/ws/mcp-tool-dispatcher.js
autonomous: true
requirements: [CAT-01, CAT-05]

must_haves:
  truths:
    - "handleCapabilitiesInvokeMessageRoute now calls FsbCapabilityRouter.invoke instead of the inline getRecipeBySlug->interpretRecipe->executeBoundSpec body"
    - "The wire message names (mcp:capabilities-invoke / mcp:capabilities-search) and the route table are byte-unchanged (INV-01)"
    - "The two capability tools stay OUTSIDE TOOL_REGISTRY; the frozen non-trigger registry hash is unmoved"
    - "An unknown slug still surfaces RECIPE_NOT_FOUND verbatim via the /^RECIPE_.+$/ passthrough (no errors.ts edit)"
    - "The dispatcher resolves the owned-tab origin + tabId SW-side; payload.origin is a non-authoritative override only"
  artifacts:
    - path: "extension/ws/mcp-tool-dispatcher.js"
      provides: "the reroute: invoke handler body becomes one FsbCapabilityRouter.invoke call (D-03)"
      contains: "FsbCapabilityRouter.invoke"
  key_links:
    - from: "handleCapabilitiesInvokeMessageRoute"
      to: "FsbCapabilityRouter.invoke(slug, params, {origin, tabId})"
      via: "the single internal-only reroute inside the existing handler"
      pattern: "FsbCapabilityRouter\\.invoke"
    - from: "the route table MCP_PHASE199_MESSAGE_ROUTES"
      to: "handleCapabilitiesInvokeMessageRoute"
      via: "UNCHANGED binding (mcp:capabilities-invoke)"
      pattern: "mcp:capabilities-invoke"
---

<objective>
Insert the router INSIDE the existing Phase-28 invoke handler (D-03, internal-only): `handleCapabilitiesInvokeMessageRoute`'s current body (`getRecipeBySlug` -> `interpretRecipe` -> `executeBoundSpec`) is now the router's T1b tier (added in Plan 02), and the handler calls `FsbCapabilityRouter.invoke(...)` instead. The wire names, the route table, and TOOL_REGISTRY are untouched — so the frozen INV-01 registry hash never moves and the two-tool surface proof stays green.

Purpose: This is the MCP front door of the "one engine, two front doors" model (INV-02). It must be a reroute, NOT a parallel path (Anti-Pattern: a second invoke route alongside the dispatcher path is the parallel stack INV-02 forbids). The model never supplies the authoritative origin; the dispatcher resolves the owned-tab origin SW-side (the search-handler pattern), and the two-point origin-pin still holds downstream in executeBoundSpec.
Output: the rewired `handleCapabilitiesInvokeMessageRoute` in `extension/ws/mcp-tool-dispatcher.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-catalog-tiered-router-bundled-head-declarative-tail-autopilo/29-CONTEXT.md
@.planning/phases/29-catalog-tiered-router-bundled-head-declarative-tail-autopilo/29-RESEARCH.md
@.planning/phases/29-catalog-tiered-router-bundled-head-declarative-tail-autopilo/29-PATTERNS.md

<interfaces>
<!-- The reroute. The handler body collapses to ONE router call. -->

Router entry (Plan 02 shipped it):
FsbCapabilityRouter.invoke(slug, args, { origin, tabId }) -> { success:true, ...result, tier }
| { success:false, code, errorCode, error, ... }  (typed RECIPE_* fall-through)

Current handler body to REPLACE (mcp-tool-dispatcher.js:2198-2221) — the getRecipeBySlug ->
interpretRecipe -> tabId-resolve -> executeBoundSpec body that moved INTO the router's T1b tier.

SW-side origin/tabId resolution to KEEP (copy from the search handler, mcp-tool-dispatcher.js:2178-2186):
let ownedOrigin = payload.origin || null;
if (!ownedOrigin) { try { const tabs = await chrome.tabs.query({active:true,currentWindow:true});
  ownedOrigin = (tabs[0] && tabs[0].url) ? new URL(tabs[0].url).origin : null; } catch (e) { ownedOrigin = null; } }
(payload.origin is a non-authoritative override; the model never supplies the authoritative origin — D-11.)

Route table — DO NOT TOUCH (mcp-tool-dispatcher.js:112-113):
'mcp:capabilities-search': { routeFamily:'capabilities', handler: handleCapabilitiesSearchMessageRoute },
'mcp:capabilities-invoke': { routeFamily:'capabilities', handler: handleCapabilitiesInvokeMessageRoute },

The router-unavailable guard (mirror createMcpRouteError, the existing handler's unavailable branch):
if (typeof FsbCapabilityRouter === 'undefined' || typeof FsbCapabilityRouter.invoke !== 'function')
  return createMcpRouteError('invoke_capability','capabilities', MCP_ROUTE_RECOVERY_HINT, { error:'Capability router unavailable' });
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire handleCapabilitiesInvokeMessageRoute to call the router (internal-only reroute)</name>
  <read_first>
    - extension/ws/mcp-tool-dispatcher.js:2198-2221 (the FULL current invoke handler body to replace — the getRecipeBySlug/interpretRecipe/executeBoundSpec body now lives in the router's T1b tier)
    - extension/ws/mcp-tool-dispatcher.js:2172-2190 (the search handler — copy its SW-side ownedOrigin resolution; payload.origin is an override only)
    - extension/ws/mcp-tool-dispatcher.js:108-113 (the route table region — the two capability bindings MUST stay byte-identical)
    - .planning/phases/29-catalog-tiered-router-bundled-head-declarative-tail-autopilo/29-RESEARCH.md lines 372-394 (the exact rewired handler shape, D-03, INV-01-safe)
    - .planning/phases/29-catalog-tiered-router-bundled-head-declarative-tail-autopilo/29-PATTERNS.md lines 186-219 (the reroute analog + the route-table DO-NOT-TOUCH excerpt)
    - tests/capability-mcp-surface.test.js:55-75, 151-172 (the INV-01 hash + out-of-registry assertions that must stay green)
  </read_first>
  <action>
    Replace the body of `handleCapabilitiesInvokeMessageRoute` in `extension/ws/mcp-tool-dispatcher.js` so it: (1) guards `if (typeof FsbCapabilityRouter === 'undefined' || typeof FsbCapabilityRouter.invoke !== 'function') return createMcpRouteError('invoke_capability', 'capabilities', MCP_ROUTE_RECOVERY_HINT, { error: 'Capability router unavailable' });` (replacing the old `Capability engine unavailable` typeof-FsbCapabilitySearch guard); (2) resolves `tabId` (Number.isFinite(payload.tab_id) ? payload.tab_id : the active tab via chrome.tabs.query) and `origin` (payload.origin || the active-tab origin via new URL(tabs[0].url).origin, copying the search handler's SW-side resolution — the model never supplies the authoritative origin); (3) `return await FsbCapabilityRouter.invoke(payload.slug, payload.params || {}, { origin, tabId });`. Remove the old inline `getRecipeBySlug`/`interpretRecipe`/`executeBoundSpec` body (it now lives in the router's T1b tier). DO NOT change the route table (`MCP_PHASE199_MESSAGE_ROUTES`), the wire names (`mcp:capabilities-invoke`/`-search`), or add anything to TOOL_REGISTRY. The typed fall-through reasons (RECIPE_NOT_FOUND/RECIPE_LEARN_PENDING/RECIPE_DOM_FALLBACK_PENDING) the router returns surface verbatim via the existing /^RECIPE_.+$/ passthrough — no errors.ts edit. Update the two header comments above the handler/route-table that currently say "NO Phase-29 capability-router" to reflect the router is now wired (the reroute is internal-only).
  </action>
  <acceptance_criteria>
    - `handleCapabilitiesInvokeMessageRoute` body contains `FsbCapabilityRouter.invoke(payload.slug, payload.params || {}, { origin, tabId })` and no longer contains the inline `FsbCapabilityInterpreter.interpretRecipe`/`FsbCapabilityFetch.executeBoundSpec` calls
    - The route table still binds `'mcp:capabilities-invoke'` and `'mcp:capabilities-search'` to their handlers, byte-unchanged
    - Neither `invoke_capability` nor `search_capabilities` is added to TOOL_REGISTRY (no edit to tool-definitions)
    - `npm --prefix mcp run build` succeeds and `node tests/capability-mcp-surface.test.js` exits 0 (INV-01 hash unmoved, 2 tools on the wire, RECIPE_NOT_FOUND verbatim)
  </acceptance_criteria>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('extension/ws/mcp-tool-dispatcher.js','utf8'); if(s.indexOf('FsbCapabilityRouter.invoke')===-1)throw new Error('router not wired'); const body=s.slice(s.indexOf('async function handleCapabilitiesInvokeMessageRoute'), s.indexOf('async function handleCapabilitiesInvokeMessageRoute')+1400); if(/FsbCapabilityFetch\.executeBoundSpec/.test(body))throw new Error('inline executeBoundSpec still present in invoke handler'); if(s.indexOf(\"'mcp:capabilities-invoke'\")===-1||s.indexOf(\"'mcp:capabilities-search'\")===-1)throw new Error('route table changed'); console.log('reroute shape OK')" && npm --prefix mcp run build && node tests/capability-mcp-surface.test.js</automated>
  </verify>
  <done>The invoke handler is a single router call, the route table and wire names are unchanged, the tools stay out-of-registry, and the INV-01 surface proof stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client ↔ dispatcher | The model supplies slug+params but NEVER the authoritative origin; the dispatcher resolves the owned-tab origin SW-side (single un-spoofable point). |
| dispatcher ↔ router | The reroute is internal-only; there is no second invoke path (a parallel route would be the INV-02-forbidden parallel stack). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-11 | Spoofing | client-supplied origin used as authoritative | mitigate | The handler resolves ownedOrigin SW-side from the active tab; payload.origin is a non-authoritative override only (D-11). The downstream executeBoundSpec re-pins regardless. |
| T-29-12 | Tampering | a capability tool leaking into TOOL_REGISTRY and moving the frozen hash | mitigate | The reroute touches only the handler body; no tool-definitions edit; capability-mcp-surface.test.js re-asserts EXPECTED_NON_TRIGGER_REGISTRY_HASH is unmoved and both names are absent from TOOL_REGISTRY (INV-01). |
| T-29-13 | Elevation of Privilege | a parallel invoke route bypassing the pin | mitigate | The router is inserted INSIDE the existing handler (one front door); no new route is added. Verified by the unchanged route-table assertion. |
| T-29-SC | Tampering | npm/pip/cargo installs | mitigate | Zero external packages this phase. No install task. |
</threat_model>

<verification>
- `npm --prefix mcp run build && node tests/capability-mcp-surface.test.js` exits 0 (INV-01 hash unmoved; 2 tools on the wire; RECIPE_NOT_FOUND verbatim).
- The invoke handler body is a single `FsbCapabilityRouter.invoke(...)` call; no inline interpretRecipe/executeBoundSpec remains in it.
- The route table and wire names are byte-unchanged; nothing added to TOOL_REGISTRY.
</verification>

<success_criteria>
- The MCP invoke front door calls the shared router (the T1b body now lives in the router); the reroute is internal-only.
- The wire names, route table, and TOOL_REGISTRY are untouched — the frozen INV-01 registry hash never moves and the two-tool surface proof stays green.
- Typed fall-through reasons surface verbatim via the existing /^RECIPE_.+$/ passthrough (no errors.ts edit).
- The dispatcher resolves the owned origin SW-side; the model never supplies the authoritative origin.
</success_criteria>

<output>
Create `.planning/phases/29-catalog-tiered-router-bundled-head-declarative-tail-autopilo/29-04-SUMMARY.md` when done
</output>
