---
phase: 29-catalog-tiered-router-bundled-head-declarative-tail-autopilo
plan: 02
type: execute
wave: 1
depends_on: ["29-01"]
files_modified:
  - extension/utils/capability-catalog.js
  - extension/utils/capability-router.js
  - extension/background.js
autonomous: true
requirements: [CAT-01, CAT-03, CAT-05]

must_haves:
  truths:
    - "The router exposes a single SW-global entry FsbCapabilityRouter.invoke(slug, args, {origin, tabId}) returning {success:true, ...result, tier} on a hit OR the dual-field typed-error shape {success:false, code, errorCode, error} (D-02), and selects a tier (T0 to T1a to T1b to T2 to T3) for a slug, biased by tab origin"
    - "A T1b recipe routes through the lifted interpretRecipe to executeBoundSpec path and returns the normalized shape stamped tier:'T1b'"
    - "An unknown slug returns RECIPE_NOT_FOUND; a T2 entry returns RECIPE_LEARN_PENDING; a T3 entry returns RECIPE_DOM_FALLBACK_PENDING and does NOT call executeTool"
    - "Both new dual-export IIFE SW modules — capability-router.js and capability-catalog.js (D-01) — are eval-free and remain on RECIPE_PATH_ALLOWLIST (guard green)"
    - "The SW loads catalog then router via additive importScripts after capability-search.js"
  artifacts:
    - path: "extension/utils/capability-catalog.js"
      provides: "slug to {tier, handler|recipe, descriptor} registry; resolve(slug, origin)"
      exports: ["FsbCapabilityCatalog"]
      min_lines: 40
    - path: "extension/utils/capability-router.js"
      provides: "FsbCapabilityRouter.invoke(slug, args, {origin, tabId}) tier dispatch + typed fall-through"
      exports: ["FsbCapabilityRouter"]
      min_lines: 70
    - path: "extension/background.js"
      provides: "importScripts load slots for catalog + router after capability-search.js"
      contains: "capability-router.js"
  key_links:
    - from: "extension/utils/capability-router.js"
      to: "FsbCapabilityFetch.executeBoundSpec"
      via: "typeof-guarded global, called in the T1b/T0 tier"
      pattern: "executeBoundSpec"
    - from: "extension/utils/capability-router.js"
      to: "FsbCapabilityInterpreter.interpretRecipe + FsbCapabilitySearch.getRecipeBySlug"
      via: "the lifted routerless body in case T1b"
      pattern: "interpretRecipe"
    - from: "extension/utils/capability-catalog.js"
      to: "FsbCapabilitySearch.getRecipeBySlug"
      via: "T1b recipe source (D-04)"
      pattern: "getRecipeBySlug"
---

<objective>
Build the two new pure SW modules — `capability-catalog.js` (the authoritative slug to tier registry) and `capability-router.js` (tier selection + origin bias + typed fall-through) — and load them at SW startup. The router's T1b tier is the VERBATIM-LIFTED routerless body from the Phase-28 dispatcher handler; T0 is the no-auth declarative special-case; T2/T3 are typed-fall-through seams that return a reason and do NOT execute. This turns the router test green for every tier path that does not depend on the head handlers (T1a) or the front-door wiring.

Purpose: This is the engine both front doors will share (INV-02). It must be eval-free (Wall-1), Node-unit-testable via typeof-guarded globals, and must NOT bypass the two-point origin-pin (every credentialed call goes through `executeBoundSpec`, which re-pins — D-12).
Output: `extension/utils/capability-catalog.js`, `extension/utils/capability-router.js`, additive importScripts in `extension/background.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-catalog-tiered-router-bundled-head-declarative-tail-autopilo/29-CONTEXT.md
@.planning/phases/29-catalog-tiered-router-bundled-head-declarative-tail-autopilo/29-RESEARCH.md
@.planning/phases/29-catalog-tiered-router-bundled-head-declarative-tail-autopilo/29-PATTERNS.md

<interfaces>
<!-- Lifted/cloned contracts. Implement EXACTLY to these so Plan 01's tests pass. -->

createRecipeError to CLONE (capability-interpreter.js:85-93):
returns { success:false, code, errorCode:code, error:code, ...extra }. Only this dual-field shape
survives the /^RECIPE_.+$/ MCP passthrough (mcp/src/errors.ts:137) verbatim — NO errors.ts edit.

T1b recipe source + origin-bias lever (capability-search.js):
- getRecipeBySlug(slug) -> recipe | null   (:222)
- search(query, ownedOrigin, topN)         (origin-biased; ORIGIN_BOOST=4 at :55)
- _stableSortByOwnedService(hits, ownedService)  (owned-service-first re-rank, :209-219 — the bias semantics)

The routerless body that BECOMES case 'T1b' (mcp-tool-dispatcher.js:2202-2220 — lift verbatim,
swap direct refs for typeof-guarded globals): getRecipeBySlug -> if !recipe RECIPE_NOT_FOUND ->
interpretRecipe(recipe,args) -> if !interpreted.success return interpreted -> resolve tabId
(payload.tab_id else active tab) -> executeBoundSpec(interpreted.spec, tabId).

Execution primitive (capability-fetch.js:272 — re-pins active tab BEFORE side effect):
executeBoundSpec(spec, tabId) -> { success, status, finalUrl, redirected, data, text }
| { success:false, code:'RECIPE_ORIGIN_MISMATCH', ... }

typeof-guarded accessor convention (capability-search.js:57-69 — mirror as _catalog/_search/_interp/_fetch):
function _getMiniSearch() { return (typeof MiniSearch !== 'undefined' && MiniSearch) ? MiniSearch : null; }

Dual-export IIFE tail (capability-interpreter.js:372-385 — copy the shape):
var exportsObj = { invoke: invoke }; global.FsbCapabilityRouter = exportsObj;
if (typeof module !== 'undefined' && module.exports) module.exports = exportsObj;
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author capability-catalog.js (slug to {tier, handler|recipe, descriptor} registry)</name>
  <behavior>
    - resolve('github.notifications', 'https://github.com') returns an entry with tier:'T1b' and the github recipe (sourced via getRecipeBySlug)
    - resolve('does.not.exist', anyOrigin) returns null
    - a slug declared T1a in the registry resolves to tier:'T1a' regardless of origin (origin biases search ranking only, never tier choice — RESEARCH Open Q3)
    - the module is a dual-export IIFE exposing global.FsbCapabilityCatalog and module.exports
  </behavior>
  <read_first>
    - extension/utils/capability-search.js:48-72 (typeof-guarded accessors, INDEX_OPTIONS, ORIGIN_BOOST), :118-119 (_slugToRecipe build), :209-224 (_stableSortByOwnedService + getRecipeBySlug), :226-241 (dual-export tail)
    - extension/utils/capability-interpreter.js:372-385 (the dual-export tail shape)
    - catalog/recipes/github-notifications.json + catalog/descriptors/github-notifications.json (the data the T1b seed entry references)
    - .planning/phases/29-catalog-tiered-router-bundled-head-declarative-tail-autopilo/29-PATTERNS.md lines 100-128 (the catalog registry pattern + origin-bias reuse)
  </read_first>
  <action>
    Create `extension/utils/capability-catalog.js` as a dual-export IIFE shell mirroring the capability-interpreter.js tail, eval-free (no `eval`/`new Function`/`import(` even in comments — it is on RECIPE_PATH_ALLOWLIST). Expose `resolve(slug, origin)` returning `{ tier, handler|recipe, descriptor } | null`. The registry is an explicit per-slug map declaring each slug's authoritative `tier` (a slug is EITHER T1a OR T1b — no runtime tie-break, RESEARCH Open Q3). For T1b/T0 slugs the entry's recipe is sourced from `FsbCapabilitySearch.getRecipeBySlug(slug)` via a typeof-guarded `_search()` accessor (D-04 — the Phase-28 slug to recipe map stays the T1b source); seed `github.notifications` as a T1b entry. For T1a slugs the entry declares `tier:'T1a'` plus a reference (handler global name + slug key) the router resolves at call time — leave the actual T1a entries to Plan 03, but expose a documented mechanism Plan 03 uses to register them (a mutable registry object the handler modules push into at load, or a typeof-guarded lookup of `globalThis.FsbHandler*` globals; planner's choice, keep it declarative). Add a reusable origin-bias helper mirroring `_stableSortByOwnedService` (CAT-01 "biased by tab origin") that re-ranks candidate recipes owned-origin-first; it must NOT change the tier of a known slug. Load via background.js importScripts in Task 3.
  </action>
  <acceptance_criteria>
    - `extension/utils/capability-catalog.js` exists, is a dual-export IIFE exporting `FsbCapabilityCatalog` with a `resolve` function
    - Contains NONE of the literal substrings `eval`, `new Function`, `import(` (allowlist-clean)
    - `resolve('does.not.exist', null)` returns null; `resolve('github.notifications', 'https://github.com').tier === 'T1b'`
    - `node scripts/verify-recipe-path-guard.mjs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node tests/capability-router.test.js; node -e "const c=require('./extension/utils/capability-catalog.js'); if(typeof c.resolve!=='function')throw new Error('no resolve'); if(c.resolve('does.not.exist',null)!==null)throw new Error('unknown-not-null'); const s=require('fs').readFileSync('extension/utils/capability-catalog.js','utf8'); ['eval','new Function','import('].forEach(t=>{if(s.indexOf(t)!==-1){console.error('FORBIDDEN '+t);process.exit(1)}}); console.log('catalog OK')" && node scripts/verify-recipe-path-guard.mjs</automated>
  </verify>
  <done>The catalog module exists, resolves a known T1b slug and a null for unknown, is eval-free, and the guard passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author capability-router.js (tier dispatch + lifted T1b body + typed fall-through)</name>
  <behavior>
    - invoke('github.notifications', {}, {origin:'https://github.com', tabId:11}) routes T1b through interpretRecipe to executeBoundSpec and stamps tier:'T1b' on a success result
    - invoke('unknown.slug', {}, ctx) returns { success:false, code:'RECIPE_NOT_FOUND', errorCode:'RECIPE_NOT_FOUND', error:'RECIPE_NOT_FOUND', slug:'unknown.slug' }
    - a catalog entry with tier:'T2' makes invoke return RECIPE_LEARN_PENDING (no execution)
    - a catalog entry with tier:'T3' makes invoke return RECIPE_DOM_FALLBACK_PENDING AND neither executeTool nor chrome.scripting is called
    - a T1a entry invokes handler.handle(args, ctx) where ctx.executeBoundSpec is FsbCapabilityFetch.executeBoundSpec; on success stamps tier:'T1a'
    - all returned error codes match /^RECIPE_.+$/
  </behavior>
  <read_first>
    - extension/ws/mcp-tool-dispatcher.js:2198-2221 (the FULL routerless body to lift into case 'T1b' — getRecipeBySlug, interpretRecipe, tabId resolve, executeBoundSpec)
    - extension/utils/capability-interpreter.js:85-93 (createRecipeError — clone as the router's _err helper) and :372-385 (dual-export tail)
    - extension/utils/capability-search.js:57-69 (typeof-guarded accessor convention)
    - extension/utils/capability-fetch.js:272-298 (executeBoundSpec signature + the active-tab origin-pin the router must NOT bypass)
    - .planning/phases/29-catalog-tiered-router-bundled-head-declarative-tail-autopilo/29-RESEARCH.md lines 220-288 (Pattern 1: the full router shape) and lines 296-318 (Pattern 3 + Don't Hand-Roll: never re-implement the fetch/auth/error-shape)
    - .planning/phases/29-catalog-tiered-router-bundled-head-declarative-tail-autopilo/29-PATTERNS.md lines 29-96 (router analogs + the lifted-body excerpt + the typeof-guarded accessor mirror)
  </read_first>
  <action>
    Create `extension/utils/capability-router.js` as a dual-export IIFE exposing `FsbCapabilityRouter.invoke(slug, args, ctx)` where `ctx = { origin, tabId, source? }`. It is a PURE module: no direct `chrome.*`, no `fetch`, no `eval`/`new Function`/`import(` (it is on RECIPE_PATH_ALLOWLIST; Anti-Pattern: do NOT fold it into capability-interpreter.js). Read collaborators via typeof-guarded accessors `_catalog()`/`_search()`/`_interp()`/`_fetch()` so Node tests inject stubs. Clone `createRecipeError` as a local `_err(code, extra)`. Implementation: resolve `entry = _catalog().resolve(slug, ctx.origin)`; if no entry return `_err('RECIPE_NOT_FOUND', {slug})`. Switch on `entry.tier`: case 'T1a' — resolve the handler (per the catalog's registration mechanism), call `await handler.handle(args||{}, { origin:ctx.origin, tabId:ctx.tabId, executeBoundSpec:_fetch().executeBoundSpec, interpretRecipe:_interp().interpretRecipe })`, on `success===true` stamp `tier:'T1a'`, else return the handler's own typed error; case 'T0' and case 'T1b' — the lifted body: `recipe = entry.recipe || _search().getRecipeBySlug(slug)`; if !recipe `_err('RECIPE_NOT_FOUND',{slug})`; `interpreted = _interp().interpretRecipe(recipe, args||{})`; if `!interpreted.success` return interpreted verbatim (typed RECIPE_*); `out = await _fetch().executeBoundSpec(interpreted.spec, ctx.tabId)`; on success stamp `tier` = 'T0' or 'T1b'; case 'T2' — `return _err('RECIPE_LEARN_PENDING', {slug})` (Phase-31 stub, no-op); case 'T3' — `return _err('RECIPE_DOM_FALLBACK_PENDING', {slug})` (Phase-32 seam — return the reason, NEVER call executeTool/chrome.scripting); default — `_err('RECIPE_NOT_FOUND', {slug})`. The router routes; it never re-targets — the pin lives inside executeBoundSpec on every real-tier path. Reason-code names are planner-chosen but MUST match /^RECIPE_.+$/.
  </action>
  <acceptance_criteria>
    - `extension/utils/capability-router.js` exists, dual-export, exposes `FsbCapabilityRouter.invoke`
    - Contains NONE of `eval`, `new Function`, `import(`, `chrome.`, `fetch(` (pure module)
    - `node tests/capability-router.test.js` passes the tier-order, T1b lifted-path tier:'T1b', RECIPE_NOT_FOUND, RECIPE_LEARN_PENDING, RECIPE_DOM_FALLBACK_PENDING, and T3-no-exec assertions (the T1a-handler and origin-pin assertions go green once Plan 03 lands its handlers; if the test gates them behind an injected stub handler they pass now too)
    - `node scripts/verify-recipe-path-guard.mjs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('extension/utils/capability-router.js','utf8'); ['eval','new Function','import(','chrome.','fetch('].forEach(t=>{if(s.indexOf(t)!==-1){console.error('FORBIDDEN '+t);process.exit(1)}}); console.log('router pure OK')" && node tests/capability-router.test.js && node scripts/verify-recipe-path-guard.mjs</automated>
  </verify>
  <done>The router exists as a pure eval-free module, the router test passes for all tier/typed-reason paths not gated on Plan-03 handlers, and the guard passes.</done>
</task>

<task type="auto">
  <name>Task 3: Wire catalog + router into the SW importScripts load order</name>
  <read_first>
    - extension/background.js:119-163 (the capability-family importScripts block; the additive try/catch-per-line pattern and the load order: lib -> schema -> auth-strategies -> interpreter -> fetch -> recipe-index.generated -> capability-search -> buildOrRestore)
    - .planning/phases/29-catalog-tiered-router-bundled-head-declarative-tail-autopilo/29-PATTERNS.md lines 291-301 (the importScripts slot order: ... capability-search.js -> capability-catalog.js -> capability-router.js)
  </read_first>
  <action>
    In `extension/background.js`, add two additive `try { importScripts('utils/capability-catalog.js'); } catch (e) { console.error('[FSB] Failed to load capability-catalog.js:', e.message); }` and the analogous line for `utils/capability-router.js`, placed AFTER the existing `importScripts('utils/capability-search.js')` line and after the `buildOrRestore` block — the catalog reads `FsbCapabilitySearch.getRecipeBySlug` so it must load after capability-search.js, and the router reads the catalog so it loads after the catalog. Additive lines ONLY (background.js is a byte-frozen esbuild input; no manifest/permission change). Add a one-line Phase-29 comment above the two lines noting they are the tiered-router engine (CAT-01) loaded last of the capability family.
  </action>
  <acceptance_criteria>
    - `extension/background.js` contains `importScripts('utils/capability-catalog.js')` and `importScripts('utils/capability-router.js')`, both AFTER the `importScripts('utils/capability-search.js')` line
    - The two lines use the same `try { ... } catch (e) { console.error(...) }` shape as the surrounding block
    - No non-additive edit to the file (the existing importScripts lines and SW bootstrap are unchanged)
  </acceptance_criteria>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('extension/background.js','utf8'); const cat=s.indexOf(\"importScripts('utils/capability-catalog.js')\"); const rtr=s.indexOf(\"importScripts('utils/capability-router.js')\"); const srch=s.indexOf(\"importScripts('utils/capability-search.js')\"); if(cat===-1||rtr===-1)throw new Error('missing importScripts'); if(!(cat>srch&&rtr>srch))throw new Error('load order: catalog/router must follow capability-search'); console.log('load order OK')"</automated>
  </verify>
  <done>The SW loads the catalog then the router after capability-search.js via additive try/catch importScripts lines.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| router/catalog module ↔ MV3 Wall-1 | Both are on RECIPE_PATH_ALLOWLIST; reintroducing eval/new Function/import here would be remote-code-as-data. |
| router ↔ executeBoundSpec (the pin) | The router selects a tier but never re-targets; every credentialed call goes through executeBoundSpec, which re-asserts the active-tab origin-pin. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-04 | Tampering | capability-router.js / capability-catalog.js reachable from the recipe path | mitigate | Both kept eval-free even in comments; on RECIPE_PATH_ALLOWLIST (Plan 01); Check 4 reds CI on any dynamic-code substring. Verified by the per-task forbidden-substring assertion. |
| T-29-05 | Elevation of Privilege / Spoofing | router bypassing the origin-pin (credential-replay) | mitigate | The router never calls chrome.scripting/fetch itself; the T1b/T0/T1a paths all route through executeBoundSpec (capability-fetch.js:291-298) which re-pins; a mismatch returns RECIPE_ORIGIN_MISMATCH with no side effect. The router is not a pin bypass (D-12, Pitfall 3). |
| T-29-06 | Information Disclosure | typed fall-through reason leaking internals | accept | Reasons are fixed RECIPE_* codes carrying only `slug`; no token/cookie/CSRF material is placed in the error (handlers that scrape tokens are Plan 03 and must not log them). Low risk — the codes are a closed set. |
| T-29-SC | Tampering | npm/pip/cargo installs | mitigate | Zero external packages this phase (RESEARCH §Package Legitimacy Audit = N/A); the recipe-path guard is the supply-chain control. No install task. |
</threat_model>

<verification>
- `node tests/capability-router.test.js` passes the tier-order, T1b, RECIPE_NOT_FOUND, RECIPE_LEARN_PENDING, RECIPE_DOM_FALLBACK_PENDING, and T3-no-exec assertions.
- `node scripts/verify-recipe-path-guard.mjs` exits 0 (both modules eval-free and on the allowlist).
- Both modules contain no `eval`/`new Function`/`import(`; the router additionally contains no `chrome.`/`fetch(`.
- `extension/background.js` loads catalog then router after capability-search.js (additive only).
</verification>

<success_criteria>
- The catalog resolves slug -> {tier, handler|recipe, descriptor} with an explicit per-slug tier and a null for unknown slugs; origin biases ranking only.
- The router dispatches T0/T1a/T1b to real execution (via executeBoundSpec) and T2/T3 to typed-fall-through seams that return a reason and do NOT execute.
- Every fall-through reason matches /^RECIPE_.+$/ and uses the dual-field createRecipeError shape (surfaces verbatim through mapFSBError — no errors.ts edit).
- Both modules are eval-free, on the allowlist (guard green), and loaded at SW startup.
- The two-point origin-pin holds on every real-tier path; the router never re-targets.
</success_criteria>

<output>
Create `.planning/phases/29-catalog-tiered-router-bundled-head-declarative-tail-autopilo/29-02-SUMMARY.md` when done
</output>
