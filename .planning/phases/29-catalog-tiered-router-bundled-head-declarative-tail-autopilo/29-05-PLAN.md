---
phase: 29-catalog-tiered-router-bundled-head-declarative-tail-autopilo
plan: 05
type: execute
wave: 3
depends_on: ["29-02", "29-04"]
files_modified:
  - extension/ai/tool-executor.js
  - extension/ai/agent-loop.js
autonomous: true
requirements: [CAT-04]

must_haves:
  truths:
    - "Autopilot reaches the same FsbCapabilityRouter SW-global the MCP dispatcher calls — one engine, two front doors (INV-02)"
    - "The capability branch is a pre-executeTool guard (BEFORE _te_getToolByName), NOT an executeBackgroundTool switch case (the out-of-registry trap)"
    - "The two capability tools stay OUTSIDE TOOL_REGISTRY; getPublicTools() does not list them; the frozen registry hash is unmoved"
    - "The agent-loop.js setTimeout iterator stays byte-untouched (INV-04); only an additive system-prompt hint is added"
    - "Both front doors return the same result shape for the same slug+args (parity test green)"
  artifacts:
    - path: "extension/ai/tool-executor.js"
      provides: "CAPABILITY_TOOL_NAMES guard + executeCapabilityToolForAutopilot calling globalThis.FsbCapabilityRouter"
      contains: "FsbCapabilityRouter"
    - path: "extension/ai/agent-loop.js"
      provides: "additive buildSystemPrompt capability hint (iterator untouched)"
      contains: "capabilit"
  key_links:
    - from: "executeTool (top, before _te_getToolByName)"
      to: "executeCapabilityToolForAutopilot -> globalThis.FsbCapabilityRouter.invoke"
      via: "the pre-switch capability guard (Pitfall 1 correction)"
      pattern: "CAPABILITY_TOOL_NAMES"
    - from: "extension/ai/tool-executor.js capability branch"
      to: "buildAutopilotTriggerParams (ownership strip)"
      via: "the trigger-branch shape mirrored for capabilities"
      pattern: "buildAutopilotTriggerParams"
---

<objective>
Add the autopilot front door (CAT-04 / INV-02): a NEW capability branch in `tool-executor.js` that, for the capability tools, calls the SAME `globalThis.FsbCapabilityRouter` SW-global the MCP dispatcher calls — runtime-layer parity, no parallel autopilot stack. Critically, the branch is a pre-`executeTool` guard (BEFORE `_te_getToolByName`), NOT an `executeBackgroundTool` switch case: the capability tools are out-of-registry (INV-01), so `_te_getToolByName('invoke_capability')` returns null and `executeTool` returns "Unknown tool" before any switch is consulted. Add a minimal additive system-prompt hint so the autopilot model can originate capability calls, keeping the agent-loop.js setTimeout iterator byte-untouched (INV-04).

Purpose: This completes "one engine, two front doors." The mechanism mirrors the proven `trigger` dual-path (one SW-global `fsbTriggerDispatchToolRequest`, two callers) but at a different hook point and with a different global (`FsbCapabilityRouter`). The tools are NOT added to TOOL_REGISTRY (Anti-Pattern 1: that bloats getPublicTools() and moves the frozen hash).
Output: the capability guard + `executeCapabilityToolForAutopilot` in `extension/ai/tool-executor.js`, an additive system-prompt hint in `extension/ai/agent-loop.js`, and the parity test green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-catalog-tiered-router-bundled-head-declarative-tail-autopilo/29-CONTEXT.md
@.planning/phases/29-catalog-tiered-router-bundled-head-declarative-tail-autopilo/29-RESEARCH.md
@.planning/phases/29-catalog-tiered-router-bundled-head-declarative-tail-autopilo/29-PATTERNS.md

<interfaces>
<!-- The autopilot branch. Mirror the trigger SHAPE; DIFFERENT hook point + DIFFERENT global. -->

The pre-executeTool dispatch gate (tool-executor.js:665-673) — the capability guard goes ABOVE _te_getToolByName:
async function executeTool(name, params, tabId, options = {}) {
  const tool = _te_getToolByName(name);
  if (!tool) { return makeResult({ success:false, error:`Unknown tool: ${name}` }); }   // <-- out-of-registry dies HERE
  ...
  switch (tool._route) { ... }   // <-- only reached after a registry hit
}

The trigger branch SHAPE to mirror (tool-executor.js:402-423): strip ownership via
buildAutopilotTriggerParams, call the SW-global, wrap in makeResult. Capability equivalent calls
globalThis.FsbCapabilityRouter.invoke(...) instead of globalThis.fsbTriggerDispatchToolRequest(...).

buildAutopilotTriggerParams (tool-executor.js:55-74) — REUSE verbatim: strips agent_id/ownership_token,
normalizes the tab alias, injects tab_id.

makeResult (tool-executor.js:45-52) -> { success, hadEffect, error, navigationTriggered, result }.

Router entry (Plan 02): globalThis.FsbCapabilityRouter.invoke(slug, args, { origin, tabId, source }).

getPublicTools (agent-loop.js:673-678) maps ONLY _al_TOOL_REGISTRY -> the capability tools are absent
from the LLM tool list; an additive buildSystemPrompt hint is needed for autopilot to ORIGINATE a call.

INV-04 iterator (agent-loop.js:2725 + the 5000ms/2000ms retry variants) — byte-untouched; only the
buildSystemPrompt string is edited.

RESEARCH §Code Examples lines 397-431: the full executeCapabilityToolForAutopilot shape (the
recommended pre-executeTool guard).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the pre-executeTool capability guard + executeCapabilityToolForAutopilot (the shared-router front door)</name>
  <read_first>
    - extension/ai/tool-executor.js:663-695 (executeTool — the _te_getToolByName gate and the _route switch; the guard goes at the TOP, before _te_getToolByName)
    - extension/ai/tool-executor.js:400-423 (the trigger branch SHAPE to mirror) and :55-82 (buildAutopilotTriggerParams + autopilotTriggerHadEffect) and :45-52 (makeResult)
    - .planning/phases/29-catalog-tiered-router-bundled-head-declarative-tail-autopilo/29-RESEARCH.md lines 335-340 (Pitfall 1: WHY it cannot be a switch case) and lines 397-431 (the executeCapabilityToolForAutopilot shape)
    - .planning/phases/29-catalog-tiered-router-bundled-head-declarative-tail-autopilo/29-PATTERNS.md lines 223-287 (the trigger-branch + buildAutopilotTriggerParams + makeResult excerpts) and lines 464-490 (the load-bearing "NOT a switch case" pattern)
    - tests/capability-autopilot-parity.test.js (Plan 01 — the parity assertions this task must turn green)
  </read_first>
  <action>
    In `extension/ai/tool-executor.js`, define `const CAPABILITY_TOOL_NAMES = new Set(['invoke_capability', 'search_capabilities']);` and an async `executeCapabilityToolForAutopilot(name, params, tabId)` that: resolves `const router = (typeof globalThis !== 'undefined') ? globalThis.FsbCapabilityRouter : null;` and returns `makeResult({ success:false, error:'FsbCapabilityRouter unavailable' })` if it/`router.invoke` is missing (mirror the trigger unavailable branch); strips ownership via `const finalParams = buildAutopilotTriggerParams(params, tabId);`; resolves `origin` from `chrome.tabs.get(tabId)` (try/catch; executeBoundSpec re-pins regardless); for `invoke_capability` calls `await router.invoke(finalParams.slug, finalParams.params || {}, { origin, tabId, source:'autopilot' })`, for `search_capabilities` calls `FsbCapabilitySearch.search(finalParams.query || '', origin, 5)` (search never mutates); wraps the response in `makeResult({ success: response && response.success !== false, hadEffect: success && name === 'invoke_capability', error: success ? null : (response?.error || response?.errorCode || null), result: response })`. Then add the guard at the TOP of `executeTool(name, ...)` — BEFORE `const tool = _te_getToolByName(name)` — `if (CAPABILITY_TOOL_NAMES.has(name)) return executeCapabilityToolForAutopilot(name, params, tabId);`. This is the Pitfall-1 correction: it CANNOT be an executeBackgroundTool switch case because out-of-registry tools never reach the switch. Do NOT add the tools to TOOL_REGISTRY. Do NOT add a parallel route — both front doors call the same globalThis.FsbCapabilityRouter.
  </action>
  <acceptance_criteria>
    - `extension/ai/tool-executor.js` defines `CAPABILITY_TOOL_NAMES` containing both tool names and `executeCapabilityToolForAutopilot`
    - The guard `if (CAPABILITY_TOOL_NAMES.has(name)) return executeCapabilityToolForAutopilot(...)` appears at the TOP of `executeTool`, lexically BEFORE the `_te_getToolByName(name)` call (so out-of-registry tools route to the branch, not to "Unknown tool")
    - The branch calls `globalThis.FsbCapabilityRouter.invoke` (NOT a new dispatch mechanism) and reuses `buildAutopilotTriggerParams` + `makeResult`
    - No `case 'invoke_capability'`/`case 'search_capabilities'` is added to any switch; nothing added to TOOL_REGISTRY
    - `node tests/capability-autopilot-parity.test.js` passes the both-front-doors-same-global + result-shape-identity + out-of-registry assertions
  </acceptance_criteria>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('extension/ai/tool-executor.js','utf8'); if(s.indexOf('CAPABILITY_TOOL_NAMES')===-1||s.indexOf('executeCapabilityToolForAutopilot')===-1)throw new Error('branch missing'); if(s.indexOf('globalThis.FsbCapabilityRouter')===-1)throw new Error('not calling shared router'); const fn=s.indexOf('async function executeTool('); const guard=s.indexOf('CAPABILITY_TOOL_NAMES.has(name)'); const gate=s.indexOf('_te_getToolByName(name)', fn); if(!(guard>fn && guard<gate))throw new Error('guard must be inside executeTool BEFORE _te_getToolByName'); if(/case 'invoke_capability'|case 'search_capabilities'/.test(s))throw new Error('must not be a switch case'); console.log('autopilot guard OK')" && node tests/capability-autopilot-parity.test.js</automated>
  </verify>
  <done>The capability branch is a pre-executeTool guard calling the shared FsbCapabilityRouter global, reusing the trigger ownership-strip/makeResult shape, out-of-registry, and the parity test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add the additive system-prompt capability hint (iterator byte-untouched, INV-04)</name>
  <read_first>
    - extension/ai/agent-loop.js (buildSystemPrompt — grep for the function; the additive hint string goes here) and :673-678 (getPublicTools is registry-only — WHY the hint is needed)
    - extension/ai/agent-loop.js:2725 and the 5000ms/2000ms retry setTimeout variants (the INV-04 iterator region that MUST stay byte-identical)
    - tests/agent-loop-iterator-guard.test.js (Plan 01 — the byte guard that must stay green)
    - .planning/phases/29-catalog-tiered-router-bundled-head-declarative-tail-autopilo/29-RESEARCH.md lines 342-347 (Pitfall 2 + Open Question 1: a tiny prompt hint, NOT 2 tool schemas; gate the requirement on the parity test)
    - .planning/phases/29-catalog-tiered-router-bundled-head-declarative-tail-autopilo/29-PATTERNS.md lines 492-505 (the LLM-visibility note: additive string only)
  </read_first>
  <action>
    In `extension/ai/agent-loop.js`'s `buildSystemPrompt`, add a SMALL additive string (a sentence or two, NOT two tool schemas) telling the autopilot model that two capability tools exist — `search_capabilities` (find an authenticated API capability for the active tab's site) and `invoke_capability` (execute one by slug) — and that it should prefer them for first-party authenticated actions on the current origin, falling back to DOM tools otherwise. This is the Pitfall-2 fix: the tools are out-of-registry so getPublicTools() cannot list them; a prompt hint lets autopilot ORIGINATE a capability call. Touch ONLY the buildSystemPrompt string. Do NOT edit the setTimeout iterator (the three `session._nextIterationTimer = setTimeout(function() { runAgentIteration(sessionId, options); }, <ms>)` lines) or any iteration scheduling — INV-04 the iterator stays byte-identical. Per RESEARCH Open Q1, the CAT-04 requirement is gated on the parity test (the branch routes correctly to the same engine); full autopilot-originated capability use is a later refinement, so a minimal hint suffices.
  </action>
  <acceptance_criteria>
    - `extension/ai/agent-loop.js` buildSystemPrompt mentions `search_capabilities` and `invoke_capability` (a short hint, not full tool schemas)
    - The three `setTimeout(function() { runAgentIteration(sessionId, options); }, <ms>)` iterator lines are byte-unchanged
    - `node tests/agent-loop-iterator-guard.test.js` exits 0 (INV-04 preserved)
    - The capability tools are still NOT in _al_TOOL_REGISTRY / getPublicTools()
  </acceptance_criteria>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('extension/ai/agent-loop.js','utf8'); if(s.indexOf('search_capabilities')===-1||s.indexOf('invoke_capability')===-1)throw new Error('hint missing'); console.log('hint OK')" && node tests/agent-loop-iterator-guard.test.js</automated>
  </verify>
  <done>An additive system-prompt hint surfaces the two capability tools to autopilot; the setTimeout iterator is byte-untouched and the INV-04 guard passes.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite phase-close gate (INV-01 + INV-04 + the whole capability surface)</name>
  <read_first>
    - package.json:17 (the scripts.test && chain — now including the three Plan-01 tests after capability-mcp-surface.test.js)
    - tests/capability-mcp-surface.test.js (INV-01 surface proof) + tests/capability-router.test.js + tests/capability-autopilot-parity.test.js + tests/agent-loop-iterator-guard.test.js
    - .planning/phases/29-catalog-tiered-router-bundled-head-declarative-tail-autopilo/29-VALIDATION.md (the phase-gate: full npm test green before verify-work; plus the human_needed live smoke recorded, not blocking)
  </read_first>
  <action>
    Run the full `npm test` chain (which includes the mid-chain `npm --prefix mcp run build` the INV-01 surface test requires) and confirm it exits 0 — proving: the reroute kept the frozen registry hash unmoved (capability-mcp-surface), the router tier/typed-reason/origin-pin behavior (capability-router), the one-engine-two-front-doors parity + out-of-registry (capability-autopilot-parity), and the INV-04 iterator byte guard (agent-loop-iterator-guard). Also run `node scripts/verify-recipe-path-guard.mjs` to confirm the new router/catalog modules are eval-free and on the allowlist. If any test reds, fix the offending code (NOT the test) within this plan's files; if a red is in another plan's file (e.g. a stale baseline like the Phase-28 lattice importScripts-count fix), apply the minimal Rule-1 correction and note it in the SUMMARY. The single live-only property (a real authenticated head handler returning logged-in data) remains the Plan-03 human_needed UAT and does NOT block this headless gate (Phase 27/28 posture).
  </action>
  <acceptance_criteria>
    - `npm test` exits 0 (the full chain, including the mid-chain mcp build)
    - `node scripts/verify-recipe-path-guard.mjs` exits 0
    - `node tests/capability-mcp-surface.test.js`, `node tests/capability-router.test.js`, `node tests/capability-autopilot-parity.test.js`, `node tests/agent-loop-iterator-guard.test.js` each exit 0
  </acceptance_criteria>
  <verify>
    <automated>npm test && node scripts/verify-recipe-path-guard.mjs</automated>
  </verify>
  <done>The full test suite is green: INV-01 hash unmoved, router behavior proven, autopilot parity proven, INV-04 iterator unchanged, recipe-path guard green. The live head smoke remains recorded human_needed and does not block the headless gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| autopilot ↔ capability engine | Autopilot reaches the engine via the SAME router global as MCP; there is no autopilot-only stack that could bypass the origin-pin. |
| LLM tool list ↔ TOOL_REGISTRY | The capability tools stay out-of-registry; the model is told they exist via a prompt hint, never via a registry-derived tool schema (protects the frozen INV-01 hash). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-14 | Elevation of Privilege | autopilot capability call bypassing the origin-pin via a parallel path | mitigate | The autopilot branch calls the SAME globalThis.FsbCapabilityRouter -> SAME executeBoundSpec -> SAME two-point pin. No parallel stack (INV-02). Verified by the parity test (both doors hit the same spied global). |
| T-29-15 | Tampering | a capability tool leaking into TOOL_REGISTRY/getPublicTools (frozen hash moves) | mitigate | The branch is a pre-executeTool guard, not a registry tool; the parity test re-asserts out-of-registry + the unmoved EXPECTED_NON_TRIGGER_REGISTRY_HASH. |
| T-29-16 | Denial of Service | the INV-04 setTimeout iterator accidentally edited | mitigate | Only the buildSystemPrompt string is touched; the agent-loop-iterator-guard.test.js byte guard reds CI on any iterator edit. |
| T-29-17 | Information Disclosure | the prompt hint leaking capability internals to the model | accept | The hint names only the two tool names + their purpose; it carries no origins, tokens, or recipe data. Low risk. |
| T-29-SC | Tampering | npm/pip/cargo installs | mitigate | Zero external packages this phase. No install task. |
</threat_model>

<verification>
- `node tests/capability-autopilot-parity.test.js` exits 0 (both front doors call the same FsbCapabilityRouter global; identical result shape; tools out-of-registry; hash unmoved).
- `node tests/agent-loop-iterator-guard.test.js` exits 0 (INV-04 iterator byte-untouched).
- `npm test` exits 0 (full chain incl. mcp build → INV-01 surface proof green).
- `node scripts/verify-recipe-path-guard.mjs` exits 0 (router/catalog eval-free, on the allowlist).
</verification>

<success_criteria>
- Autopilot reaches the SAME capability engine as MCP via a tool-executor branch — runtime-layer parity, no parallel stack (INV-02).
- The branch is a pre-executeTool guard (the out-of-registry Pitfall-1 correction), not a switch case.
- The capability tools stay out-of-registry; getPublicTools() does not list them; the frozen registry hash is unmoved.
- The agent-loop.js setTimeout iterator is byte-untouched (INV-04); only an additive system-prompt hint is added so autopilot can originate capability calls.
- The full headless suite is green; the live head smoke is the recorded Plan-03 human_needed UAT and does not block the gate.
</success_criteria>

<output>
Create `.planning/phases/29-catalog-tiered-router-bundled-head-declarative-tail-autopilo/29-05-SUMMARY.md` when done
</output>
