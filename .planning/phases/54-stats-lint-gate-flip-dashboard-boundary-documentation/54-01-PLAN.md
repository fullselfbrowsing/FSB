---
phase: 54-stats-lint-gate-flip-dashboard-boundary-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - showcase/angular/package.json
  - showcase/angular/eslint.config.js
  - showcase/angular/src/locale/I18N-BOUNDARIES.md
autonomous: true
requirements: [CI-01, CI-05]

must_haves:
  truths:
    - "package.json lint:i18n no longer ignores src/app/pages/stats/**"
    - "npm run lint:i18n exits 0 with stats included and dashboard still ignored"
    - "In-repo doc explains dashboard exclusion as permanent authenticated-app boundary"
  artifacts:
    - path: "showcase/angular/src/locale/I18N-BOUNDARIES.md"
      provides: "CI-05 permanent dashboard boundary rationale"
      min_lines: 15
---

<objective>
Flip the stats `lint:i18n` gate on, fix structural ARIA false positives, and document the permanent dashboard exclusion.

Purpose: CI-01 + CI-05.

Output: Updated package.json + eslint.config.js; I18N-BOUNDARIES.md; green `npm run lint:i18n`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-stats-lint-gate-flip-dashboard-boundary-documentation/54-CONTEXT.md
@.planning/phases/53-trans-unit-resync-stats-translation-transcreation-review/53-STATS-RECONCILIATION.md

<threat_model>
| ID | Severity | Threat | Mitigation |
|----|----------|--------|------------|
| T-54-01 | low | Ignoring user-facing attributes by mistake | Only add aria-live/aria-busy (machine tokens), not aria-label |
</threat_model>
</context>

<tasks>
<task type="auto">
  <name>Flip stats lint gate + document dashboard boundary</name>
  <files>showcase/angular/package.json, showcase/angular/eslint.config.js, showcase/angular/src/locale/I18N-BOUNDARIES.md</files>
  <action>
1. In `package.json` `lint:i18n`, remove `--ignore-pattern "src/app/pages/stats/**"`; keep dashboard ignore. Add a short comment in I18N-BOUNDARIES.md referencing the script.
2. In `eslint.config.js` `ignoreAttributes`, add `aria-live` and `aria-busy` under the ARIA structural group (with a one-line comment).
3. Write `showcase/angular/src/locale/I18N-BOUNDARIES.md` documenting: marketing pages must pass lint:i18n; stats now included; dashboard permanently excluded (authenticated app, not marketing); pointer to REQUIREMENTS CI-05 / Phase 54.
4. Run `npm run lint:i18n` from showcase/angular — must exit 0.
  </action>
  <verify>
    <automated>cd showcase/angular && npm run lint:i18n && ! grep -q 'pages/stats' package.json && grep -q 'pages/dashboard' package.json && test -f src/locale/I18N-BOUNDARIES.md && grep -q 'dashboard' src/locale/I18N-BOUNDARIES.md && grep -q 'aria-live' eslint.config.js</automated>
  </verify>
  <done>Stats lint gate on and green; dashboard boundary documented.</done>
</task>
</tasks>
