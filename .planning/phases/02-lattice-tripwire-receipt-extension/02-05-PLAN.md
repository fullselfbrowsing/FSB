---
phase: 02
plan: 05
type: execute
wave: 5
depends_on: ["02-01", "02-02", "02-03", "02-04"]
files_modified:
  - .planning/LATTICE-PIN.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [LSDK-02, LSDK-03, LSDK-04, LSDK-05, LSDK-06, LSDK-07, LSDK-08]
requirements_addressed: [LSDK-02, LSDK-03, LSDK-04, LSDK-05, LSDK-06, LSDK-07, LSDK-08]
decisions_addressed: [D-16, D-17, CD-05]

must_haves:
  truths:
    - ".planning/LATTICE-PIN.md frontmatter current_lattice_sha advances from 22bf98627ae86b1576db5d34cf447ab2b321b3e1 (Phase 1 HEAD) to the new Lattice HEAD after all Phase 2 Lattice commits land"
    - ".planning/LATTICE-PIN.md frontmatter last_updated advances to Phase 2 completion date"
    - ".planning/LATTICE-PIN.md body has ONE new row appended for Phase 2 referencing all 4 Phase 2 Lattice commit SHAs (02-01 receipts, 02-02 bands, 02-03a re-export, 02-03 audit-doc flip)"
    - ".planning/REQUIREMENTS.md LSDK section gets concrete REQ-IDs LSDK-02 through LSDK-08 populated with their Covered status + descriptions, replacing TBD placeholders"
    - "PIN.md frontmatter SHA exactly matches `cd lattice && git rev-parse fsb-integration-experiments` at write time (no drift)"
    - "Phase 1 entries in LATTICE-PIN.md body are UNCHANGED (append-only audit trail per CD-06 schema)"
    - "Phase 1 LSDK-01 entry in REQUIREMENTS.md is UNCHANGED"
    - "One FSB commit lands on automation branch: docs(02): bump LATTICE-PIN.md to Phase 2 Lattice HEAD + populate LSDK-02..08 REQ-IDs"
    - "FSB git status clean post-commit (no stray modified files)"
  artifacts:
    - path: ".planning/LATTICE-PIN.md"
      provides: "Updated cross-repo audit trail with Phase 2 row + bumped SHA"
      contains: "Phase 2"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Concrete LSDK-02..LSDK-08 REQ-IDs populated under the LSDK section"
      contains: "LSDK-02"
  key_links:
    - from: ".planning/LATTICE-PIN.md"
      to: "lattice/fsb-integration-experiments HEAD"
      via: "frontmatter current_lattice_sha + per-phase markdown table row"
      pattern: "current_lattice_sha:"
    - from: ".planning/REQUIREMENTS.md"
      to: "Phase 2 deliverables on Lattice's fsb-integration-experiments branch"
      via: "LSDK-02..LSDK-08 entries describing the shipped surface"
      pattern: "LSDK-02"
---

<objective>
Close Phase 2 with the FSB-side audit-trail bumps: update `.planning/LATTICE-PIN.md` to advance the pinned Lattice SHA from Phase 1's `22bf98627ae86b1576db5d34cf447ab2b321b3e1` to the new Lattice HEAD after all four Phase 2 Lattice commits land, and append one new Phase 2 row to the per-phase markdown table. Populate `.planning/REQUIREMENTS.md` LSDK section with concrete REQ-IDs LSDK-02 through LSDK-08 (7 REQs total per CD-05 recommendation), replacing the TBD placeholder text from Phase 1. Each REQ-ID describes a Phase 2 audit-doc row closure with its underlying Lattice surface.

Purpose: Close the D-16 (LATTICE-PIN.md bumped once at phase end) + D-17 (LSDK REQ-IDs populated at audit-doc row granularity) deliverables. With this commit, Phase 2 closes -- all 7 LSDK REQs become traceable from REQUIREMENTS.md to specific Lattice commits via LATTICE-PIN.md + the audit doc's row backlinks.

Output: One FSB commit on `automation` branch with subject `docs(02): bump LATTICE-PIN.md to Phase 2 Lattice HEAD + populate LSDK-02..08 REQ-IDs`. No further Lattice commits in this plan -- all Lattice work landed in Plans 02-01 through 02-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/02-lattice-tripwire-receipt-extension/02-CONTEXT.md
@.planning/phases/02-lattice-tripwire-receipt-extension/02-RESEARCH.md
@.planning/phases/02-lattice-tripwire-receipt-extension/02-VALIDATION.md
@.planning/phases/02-lattice-tripwire-receipt-extension/02-01-SUMMARY.md
@.planning/phases/02-lattice-tripwire-receipt-extension/02-02-SUMMARY.md
@.planning/phases/02-lattice-tripwire-receipt-extension/02-03-SUMMARY.md
@.planning/phases/02-lattice-tripwire-receipt-extension/02-04-SUMMARY.md
@.planning/phases/02-lattice-tripwire-receipt-extension/02-01-LATTICE-SHA.txt
@.planning/phases/02-lattice-tripwire-receipt-extension/02-02-LATTICE-SHA.txt
@.planning/phases/02-lattice-tripwire-receipt-extension/02-03a-LATTICE-SHA.txt
@.planning/phases/02-lattice-tripwire-receipt-extension/02-03-LATTICE-SHA.txt

@.planning/LATTICE-PIN.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-lattice-gap-survey-scaffold/01-02-SUMMARY.md

<interfaces>
Current LATTICE-PIN.md frontmatter (verified at research time):
```yaml
 ---
current_lattice_sha: 22bf98627ae86b1576db5d34cf447ab2b321b3e1
current_branch: fsb-integration-experiments
last_updated: 2026-05-24
schema_version: 1
 ---
```

Current LATTICE-PIN.md body (verified):
- "# Lattice Pin -- FSB <-> Lattice Cross-Repo Audit Trail" h1
- Description paragraph + 5 metadata bullets (location, branch, baseline, current SHA, mainline PR cadence)
- "## Per-FSB-Phase Log" section
- Table header: `| FSB Phase | Date | Lattice SHA | Branch | Lattice work touched | Notes |`
- ONE row for Phase 1 referencing commits ab6c1f6, 195e5ae, 22bf986
- Tail: "## How this file gets used" + "## Schema notes" (DO NOT touch)

Current REQUIREMENTS.md LSDK section (lines 38-48, verified):
```markdown
### LSDK -- Lattice SDK Extensions ...

Goal: extend Lattice's v1.1 Capability Receipts foundation with the primitives FSB's autopilot needs.

- [x] **LSDK-01..N (audit) -- DONE 2026-05-24 (Phase 01 Plan 01-01):** ... (long description)
- [ ] **LSDK-NN..M (TBD):** Receipt-shaped state envelopes additions / extensions ...
- [ ] **LSDK-MM..K (TBD):** Tripwire / hook primitive parity ...
- [ ] **LSDK-KK..L (TBD):** Provider adapter extensions ...
- [ ] **LSDK-LL..P (TBD):** Task-delegation primitive shape ...
- [ ] **LSDK-PP..Q (TBD):** MV3-survivability adapter contract ...
```

Phase 2 closes the receipt + tripwire/hook subset; the 3 placeholders that map to those (LSDK-NN..M Receipt-shaped + LSDK-MM..K Tripwire/hook parity) are EXPANDED to concrete REQ-IDs. Other placeholders (LSDK-KK..L providers, LSDK-LL..P delegation, LSDK-PP..Q MV3-survivability) stay as TBD -- they map to Phases 4, 5, 6.

Captured Lattice SHAs (from prior Phase 2 plans):
- 02-01-LATTICE-SHA.txt -- the receipts commit (LSDK-02 + LSDK-03)
- 02-02-LATTICE-SHA.txt -- the bands commit (LSDK-04 + LSDK-05 + LSDK-06 + LSDK-07 + LSDK-08)
- 02-03a-LATTICE-SHA.txt -- the index.ts re-export bump
- 02-03-LATTICE-SHA.txt -- the audit-doc flip (and final Lattice HEAD post-Phase-2)

The final Lattice HEAD for `current_lattice_sha` is `02-03-LATTICE-SHA.txt` (the audit-doc flip is the chronologically-last Phase 2 commit).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update .planning/LATTICE-PIN.md frontmatter SHA + append Phase 2 row</name>
  <files>.planning/LATTICE-PIN.md</files>
  <read_first>
    - .planning/LATTICE-PIN.md (full -- 37 lines; Phase 1 row schema reference)
    - .planning/phases/02-lattice-tripwire-receipt-extension/02-01-LATTICE-SHA.txt
    - .planning/phases/02-lattice-tripwire-receipt-extension/02-02-LATTICE-SHA.txt
    - .planning/phases/02-lattice-tripwire-receipt-extension/02-03a-LATTICE-SHA.txt
    - .planning/phases/02-lattice-tripwire-receipt-extension/02-03-LATTICE-SHA.txt
    - .planning/phases/01-lattice-gap-survey-scaffold/01-02-SUMMARY.md (CD-06 schema reference)
  </read_first>
  <behavior>
    - Test 1: Frontmatter `current_lattice_sha` field updates from `22bf98627ae86b1576db5d34cf447ab2b321b3e1` to the content of `02-03-LATTICE-SHA.txt` (which is Lattice HEAD after ALL Phase 2 Lattice commits land).
    - Test 2: Frontmatter `last_updated` advances to today's date (`2026-05-24` or later).
    - Test 3: Frontmatter `current_branch` and `schema_version` are byte-unchanged.
    - Test 4: Per-phase markdown table gains ONE new row for Phase 2 with all 4 Phase 2 Lattice commit short-SHAs in the "Lattice work touched" column.
    - Test 5: Phase 1 row in the body is byte-unchanged (append-only audit trail).
    - Test 6: Document tail sections ("## How this file gets used" + "## Schema notes") are byte-unchanged.
    - Test 7: Frontmatter SHA matches `cd lattice && git rev-parse fsb-integration-experiments` exactly (cross-repo SHA sync).
  </behavior>
  <action>
**Step 1.1 -- Load the four Phase 2 Lattice SHAs.**

```bash
RECEIPTS_SHA=$(cat /Users/lakshmanturlapati/Desktop/FSB/automation/.planning/phases/02-lattice-tripwire-receipt-extension/02-01-LATTICE-SHA.txt)
BANDS_SHA=$(cat /Users/lakshmanturlapati/Desktop/FSB/automation/.planning/phases/02-lattice-tripwire-receipt-extension/02-02-LATTICE-SHA.txt)
REEXPORT_SHA=$(cat /Users/lakshmanturlapati/Desktop/FSB/automation/.planning/phases/02-lattice-tripwire-receipt-extension/02-03a-LATTICE-SHA.txt)
AUDITDOC_SHA=$(cat /Users/lakshmanturlapati/Desktop/FSB/automation/.planning/phases/02-lattice-tripwire-receipt-extension/02-03-LATTICE-SHA.txt)
echo "Receipts:    $RECEIPTS_SHA (short: ${RECEIPTS_SHA:0:7})"
echo "Bands:       $BANDS_SHA (short: ${BANDS_SHA:0:7})"
echo "Re-export:   $REEXPORT_SHA (short: ${REEXPORT_SHA:0:7})"
echo "Audit-doc:   $AUDITDOC_SHA (short: ${AUDITDOC_SHA:0:7})"
```

Confirm `AUDITDOC_SHA` matches Lattice HEAD (cross-repo sync gate):

```bash
EXPECT_SHA=$(cd /Users/lakshmanturlapati/Desktop/FSB/automation/lattice && git rev-parse fsb-integration-experiments)
echo "$AUDITDOC_SHA"
echo "$EXPECT_SHA"
test "$AUDITDOC_SHA" = "$EXPECT_SHA" && echo "SHA SYNC OK" || echo "SHA DRIFT -- STOP"
```

If "SHA DRIFT", STOP. The Lattice tree has changed since Plan 02-03 committed; investigate.

**Step 1.2 -- Modify `.planning/LATTICE-PIN.md` frontmatter.**

Replace the current frontmatter block:

```yaml
 ---
current_lattice_sha: 22bf98627ae86b1576db5d34cf447ab2b321b3e1
current_branch: fsb-integration-experiments
last_updated: 2026-05-24
schema_version: 1
 ---
```

with the new frontmatter (substituting `<AUDITDOC_SHA>` with the value from Step 1.1, and using today's date):

```yaml
 ---
current_lattice_sha: <AUDITDOC_SHA>
current_branch: fsb-integration-experiments
last_updated: 2026-05-24
schema_version: 1
 ---
```

If today's wall-clock date is different from `2026-05-24` (the date in CLAUDE.md context), use the actual current date `YYYY-MM-DD`. The orchestrator may run later than the planner; use the executor-time wall-clock when running this task.

Update the in-body bullet `**Current pinned SHA:**` to reference the new short-SHA: replace `22bf986` with `${AUDITDOC_SHA:0:7}` (first 7 chars).

**Step 1.3 -- Append the Phase 2 row to the per-FSB-Phase Log table.**

The current table has a header + Phase 1 row. Append ONE new row AFTER the Phase 1 row, BEFORE the "## How this file gets used" section. The row text (using short SHAs):

```markdown
| Phase 2   | 2026-05-24 | `<AUDITDOC_SHA>` | `fsb-integration-experiments`   | (1) `packages/lattice/src/receipts/types.ts` + `receipt.ts` + `verify.ts` + receipt tests (`<RECEIPTS_SHORT>`) -- CapabilityReceiptBody.version literal-union "lattice-receipt/v1" \| "lattice-receipt/v1.1"; six new optional step-marker fields (stepName, stepIndex, parentStepName, previousStepName, sessionId, timestamp). (2) `packages/lattice/src/contract/bands.ts` + `bands.test.ts` (`<BANDS_SHORT>`) -- createHookPipeline factory; priority bands (SAFETY > OBSERVABILITY > EXTENSION); per-handler matcher regex; race-with-log per-handler budget (default 100ms; HOOK_TIMEOUT via TracerLike); structuredClone + Object.freeze context; irreversible freeze(); HookLifecycleEvent union (BEFORE_PROVIDER \| AFTER_PROVIDER \| BEFORE_TOOL \| AFTER_TOOL). (3) `packages/lattice/src/index.ts` (`<REEXPORT_SHORT>`) -- re-export createHookPipeline + HookPipeline type + HookLifecycleEvent type. dist/ rebuilt via `pnpm build`. (4) `docs/fsb-integration-gaps.md` (`<AUDITDOC_SHORT>`) -- six audit-doc rows flipped to Covered (Receipts 2-3 + Tripwires/hooks 2-5) + one new lifecycle-event-union row. | Phase 2 = receipt v1.1 schema extension + tripwire band pipeline primitive. Lattice's full vitest suite green (existing 451 + new receipts + new bands tests). FSB consumes via the existing file: dep; new `tests/lattice-tripwire-smoke.test.js` validates the surface end-to-end. |
```

Replace `<AUDITDOC_SHA>` with the 40-char SHA. Replace `<RECEIPTS_SHORT>`, `<BANDS_SHORT>`, `<REEXPORT_SHORT>`, `<AUDITDOC_SHORT>` with their 7-character prefixes.

**Step 1.4 -- Verify Phase 1 row + tail sections unchanged.**

Run:
```bash
grep -c "Phase 1" /Users/lakshmanturlapati/Desktop/FSB/automation/.planning/LATTICE-PIN.md
grep -c "Phase 2" /Users/lakshmanturlapati/Desktop/FSB/automation/.planning/LATTICE-PIN.md
grep -c "ab6c1f6\|195e5ae\|22bf986" /Users/lakshmanturlapati/Desktop/FSB/automation/.planning/LATTICE-PIN.md
grep -c "^## How this file gets used" /Users/lakshmanturlapati/Desktop/FSB/automation/.planning/LATTICE-PIN.md
grep -c "^## Schema notes" /Users/lakshmanturlapati/Desktop/FSB/automation/.planning/LATTICE-PIN.md
```

Expected: Phase 1 appears at least 2 times (Phase 1 row + the "**Current pinned SHA:**" reference may still mention it -- DO NOT remove). Phase 2 appears at least 1 time (the new row). The three Phase 1 short-SHAs (ab6c1f6, 195e5ae, 22bf986) all still appear. Tail headers appear once each.

NO commit in this task. Task 2 lands LATTICE-PIN.md + REQUIREMENTS.md as ONE FSB commit.
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Desktop/FSB/automation && grep -c "Phase 2" .planning/LATTICE-PIN.md && grep -c "current_lattice_sha:" .planning/LATTICE-PIN.md && PIN_SHA=$(grep "^current_lattice_sha:" .planning/LATTICE-PIN.md | awk '{print $2}') && LATTICE_SHA=$(cd lattice && git rev-parse fsb-integration-experiments) && test "$PIN_SHA" = "$LATTICE_SHA"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "^current_lattice_sha:" .planning/LATTICE-PIN.md` returns 1 line.
    - `grep "^current_lattice_sha:" .planning/LATTICE-PIN.md | awk '{print $2}'` equals `cd lattice && git rev-parse fsb-integration-experiments` output (40-char hex).
    - The frontmatter SHA is NOT `22bf98627ae86b1576db5d34cf447ab2b321b3e1` (Phase 1 SHA -- if still present, the bump didn't happen).
    - `grep -c "Phase 2" .planning/LATTICE-PIN.md` returns >= 1 (new row).
    - `grep -c "createHookPipeline" .planning/LATTICE-PIN.md` returns >= 1.
    - Phase 1 short-SHAs `ab6c1f6`, `195e5ae`, `22bf986` still appear (the Phase 1 row remains intact -- append-only).
    - `grep -c "^## How this file gets used" .planning/LATTICE-PIN.md` returns 1.
    - `grep -c "^## Schema notes" .planning/LATTICE-PIN.md` returns 1.
    - No emojis in the file.
  </acceptance_criteria>
  <done>LATTICE-PIN.md frontmatter SHA bumped to Phase 2 HEAD; new Phase 2 row appended; Phase 1 row + tail sections unchanged; cross-repo SHA sync verified.</done>
</task>

<task type="auto">
  <name>Task 2: Populate LSDK-02..LSDK-08 in .planning/REQUIREMENTS.md and commit the FSB-side Phase 2 closure</name>
  <files>.planning/REQUIREMENTS.md, .planning/LATTICE-PIN.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full; locate the LSDK section at the bottom -- lines ~38-48)
    - .planning/phases/02-lattice-tripwire-receipt-extension/02-RESEARCH.md (section: "REQUIREMENTS.md LSDK-02..LSDK-08 population (planner draft)" + "Audit-Doc Row Closure Map")
    - .planning/phases/02-lattice-tripwire-receipt-extension/02-CONTEXT.md (D-17 + CD-05 -- REQ-ID granularity)
    - .planning/LATTICE-PIN.md (just-updated; the new Phase 2 row's SHA references)
  </read_first>
  <behavior>
    - Test 1: REQUIREMENTS.md LSDK section gains 7 new bullet entries: LSDK-02 through LSDK-08, each describing one closed audit-doc row.
    - Test 2: Each LSDK-NN entry is marked `[x]` (Done) since Phase 2 ships them.
    - Test 3: LSDK-01 entry (Phase 1 audit) is byte-unchanged.
    - Test 4: The other category placeholders (LSDK-KK..L providers, LSDK-LL..P delegation, LSDK-PP..Q MV3-survivability) remain as TBD -- they belong to Phases 4, 5, 6.
    - Test 5: FINT, MCP, PRV sections are byte-unchanged (Phase 2's FINT-02 deliverable is the smoke; D-18 leaves FINT REQ-ID population deferred unless surfaced).
    - Test 6: Hard Invariants section is byte-unchanged.
    - Test 7: One FSB commit lands on `automation` branch with subject `docs(02): bump LATTICE-PIN.md to Phase 2 Lattice HEAD + populate LSDK-02..08 REQ-IDs`.
    - Test 8: FSB git status clean post-commit.
  </behavior>
  <action>
**Step 2.1 -- Read the current REQUIREMENTS.md LSDK section.**

The LSDK section currently contains:

```markdown
### LSDK -- Lattice SDK Extensions (work in `./lattice` on `fsb-integration-experiments` branch)

Goal: extend Lattice's v1.1 Capability Receipts foundation with the primitives FSB's autopilot needs.

- [x] **LSDK-01..N (audit) -- DONE 2026-05-24 (Phase 01 Plan 01-01):** Lattice SDK gap survey landed at `lattice/docs/fsb-integration-gaps.md` (91 lines, 6 domain headers, 21 severity-tagged rows: Receipts=2, Tripwires/hooks=4, Providers=5, Delegation=1, MV3-survivability=2, Observability/step-markers=4). Severity tags drive Phase 2+ ordering (Blocker > Important > Nice-to-have). Lattice commit `195e5ae`. Concrete LSDK-NN REQ-IDs for each row are populated in Phase 2 setup (deferred per CONTEXT.md `<deferred>` block). Phase 01 Plan 01-01 also re-exported `createReceipt` + `CreateReceiptInput` from Lattice's bare specifier (commit `ab6c1f6`), and Plan 01-02 added a packaging fix (catalog: -> literals) so npm 11 can install via `file:` (Lattice commit `22bf986`, user-authorized D-13 expansion).
- [ ] **LSDK-NN..M (TBD):** Receipt-shaped state envelopes additions / extensions -- ensure Lattice can represent the step transitions FSB's autopilot emits (`stepName`, `stepIndex`, `timestamp`, `sessionId`, `parentStepName`, `previousStepName`, and any new fields surfaced by phase discussion).
- [ ] **LSDK-MM..K (TBD):** Tripwire / hook primitive parity -- priority bands (SAFETY > OBSERVABILITY > EXTENSION), matcher regex, race-with-log per-handler budget, frozen contexts, mid-session registration freeze.
- [ ] **LSDK-KK..L (TBD):** Provider adapter extensions -- bring Lattice's provider abstraction into parity with FSB's 7-provider matrix (Anthropic, OpenAI, xAI, Gemini, LM Studio, OpenRouter, custom OpenAI-compatible).
- [ ] **LSDK-LL..P (TBD):** Task-delegation primitive shape (if Lattice opens multi-agent policy; otherwise design as a Lattice-adjacent FSB-side primitive that consumes Lattice's receipt + tripwire surface).
- [ ] **LSDK-PP..Q (TBD):** MV3-survivability adapter contract -- documented interface for runtimes whose execution context can be evicted mid-flow.
```

**Step 2.2 -- Replace the TBD bullets for receipts (LSDK-NN..M) and tripwires (LSDK-MM..K) with concrete LSDK-02 through LSDK-08 entries.**

Load the short SHAs for the embed:

```bash
RECEIPTS_SHORT=$(cat /Users/lakshmanturlapati/Desktop/FSB/automation/.planning/phases/02-lattice-tripwire-receipt-extension/02-01-LATTICE-SHA.txt | head -c 7)
BANDS_SHORT=$(cat /Users/lakshmanturlapati/Desktop/FSB/automation/.planning/phases/02-lattice-tripwire-receipt-extension/02-02-LATTICE-SHA.txt | head -c 7)
REEXPORT_SHORT=$(cat /Users/lakshmanturlapati/Desktop/FSB/automation/.planning/phases/02-lattice-tripwire-receipt-extension/02-03a-LATTICE-SHA.txt | head -c 7)
AUDITDOC_SHORT=$(cat /Users/lakshmanturlapati/Desktop/FSB/automation/.planning/phases/02-lattice-tripwire-receipt-extension/02-03-LATTICE-SHA.txt | head -c 7)
```

Replace the TWO bullets:
- `- [ ] **LSDK-NN..M (TBD):** Receipt-shaped state envelopes ...`
- `- [ ] **LSDK-MM..K (TBD):** Tripwire / hook primitive parity ...`

With the SEVEN new bullets below (substituting the short SHAs):

```markdown
- [x] **LSDK-02 -- DONE 2026-05-24 (Phase 02 Plan 02-01):** Receipt v1.1 step-marker fields. `CapabilityReceiptBody` extended with 5 optional top-level fields: `stepName?: string`, `stepIndex?: number`, `parentStepName?: string`, `previousStepName?: string`, `timestamp?: string` (ISO-8601 RFC 3339). Schema `version` literal becomes a discriminated union `"lattice-receipt/v1" | "lattice-receipt/v1.1"`; verifier accepts both. v1 receipts (Phase 1 callers) continue to verify unchanged. JCS canonicalization byte-stable (RFC 8785 alphabetical key sort handles new fields automatically; no canonical.ts source change). Redaction manifest unchanged (D-04: step-marker fields are stable identifiers, not user content). Lattice commit `<RECEIPTS_SHORT>`.
- [x] **LSDK-03 -- DONE 2026-05-24 (Phase 02 Plan 02-01):** Receipt v1.1 sessionId field. `CapabilityReceiptBody.sessionId?: string` added (FSB ties step markers to a persistent session across SW eviction). Same commit as LSDK-02. Lattice commit `<RECEIPTS_SHORT>`.
- [x] **LSDK-04 -- DONE 2026-05-24 (Phase 02 Plan 02-02):** Tripwire band pipeline -- priority bands. `lattice/packages/lattice/src/contract/bands.ts` (NEW) exports `createHookPipeline()` factory + `HookPipeline` interface + `BAND` const (SAFETY=0, OBSERVABILITY=1, EXTENSION=2). Handlers in lower-numbered bands run first; within-band registration order preserved. Pure `evaluateTripwires` at `tripwire.ts` is NOT modified -- bands.ts is a sibling primitive (D-06). Lattice commit `<BANDS_SHORT>`.
- [x] **LSDK-05 -- DONE 2026-05-24 (Phase 02 Plan 02-02):** Tripwire band pipeline -- per-handler matcher regex + race-with-log budget. `RegisterOptions.matcher?: RegExp` gates per-handler invocation; `RegisterOptions.budgetMs?: number` defaults to 100ms; budget timer firing emits `HOOK_TIMEOUT` event via the existing `TracerLike.event(name, attributes)` surface with payload `{event, band, budgetMs, sessionId?, handlerIndex, elapsedMs}` (stable identifiers only -- no user content). No-abort `Promise.race` pattern (CD-01): handler keeps running in background after timeout; CPU-leak risk explicitly accepted per CONTEXT.md D-09. Lattice commit `<BANDS_SHORT>`.
- [x] **LSDK-06 -- DONE 2026-05-24 (Phase 02 Plan 02-02):** Tripwire band pipeline -- frozen-context evaluation. `pipeline.run(event, context)` wraps each handler's context in `structuredClone(ctx)` + `Object.freeze(ctx)`. Handler mutations do NOT leak back to the calling site (verified by vitest case). Lattice commit `<BANDS_SHORT>`.
- [x] **LSDK-07 -- DONE 2026-05-24 (Phase 02 Plan 02-02):** Tripwire band pipeline -- mid-session registration freeze. `pipeline.freeze()` is irreversible; subsequent `pipeline.register()` throws `Error` whose `.name === "PIPELINE_FROZEN"`. `freeze()` is idempotent; `run()` still works after freeze. Lattice commit `<BANDS_SHORT>`.
- [x] **LSDK-08 -- DONE 2026-05-24 (Phase 02 Plan 02-02 + 02-03):** Tripwire band pipeline -- typed `HookLifecycleEvent` union: `"BEFORE_PROVIDER" | "AFTER_PROVIDER" | "BEFORE_TOOL" | "AFTER_TOOL"`. Separate vocabulary from `tracing.ts:RunEventKind` by design (D-12) -- run events describe Lattice runtime stages, lifecycle events describe pluggable hook attach-points. Re-exported from package surface at `src/index.ts` (Lattice commit `<REEXPORT_SHORT>`); FSB consumes via bare specifier `import { HookLifecycleEvent } from "lattice"`. Lattice commit `<BANDS_SHORT>` (definition) + `<REEXPORT_SHORT>` (re-export).
```

Substitute the four short-SHA placeholders with the values from Step 2.1.

Keep the existing bullets for `LSDK-KK..L providers`, `LSDK-LL..P delegation`, `LSDK-PP..Q MV3-survivability` (all still TBD; Phases 4, 5, 6).

Keep `LSDK-01..N (audit)` bullet byte-identical.

**Step 2.3 -- Verify other REQUIREMENTS.md sections are unchanged.**

```bash
grep -c "FINT-01" /Users/lakshmanturlapati/Desktop/FSB/automation/.planning/REQUIREMENTS.md   # MUST be >= 1 (Phase 1 deliverable)
grep -c "MCP-01\|MCP-02" /Users/lakshmanturlapati/Desktop/FSB/automation/.planning/REQUIREMENTS.md   # MUST be >= 1 each
grep -c "INV-01\|INV-02\|INV-03\|INV-04\|INV-05\|INV-06" /Users/lakshmanturlapati/Desktop/FSB/automation/.planning/REQUIREMENTS.md   # MUST be >= 6
grep -c "LSDK-02\|LSDK-03\|LSDK-04\|LSDK-05\|LSDK-06\|LSDK-07\|LSDK-08" /Users/lakshmanturlapati/Desktop/FSB/automation/.planning/REQUIREMENTS.md   # MUST be 7 (one per new REQ)
```

**Step 2.4 -- Stage and commit on FSB's automation branch.**

Confirm FSB branch:
```bash
cd /Users/lakshmanturlapati/Desktop/FSB/automation && git rev-parse --abbrev-ref HEAD
```

Expected: `automation`.

Stage both .planning/ files. NOTE: `.planning/` is gitignored in FSB; use `git add -f` per the Phase 1 precedent (01-02-SUMMARY.md documents this `-f` pattern):

```bash
cd /Users/lakshmanturlapati/Desktop/FSB/automation && git add -f .planning/LATTICE-PIN.md .planning/REQUIREMENTS.md
```

Verify staged diff is exactly two files:

```bash
cd /Users/lakshmanturlapati/Desktop/FSB/automation && git diff --cached --stat
```

Expected output: 2 files changed.

Commit using HEREDOC:

```bash
cd /Users/lakshmanturlapati/Desktop/FSB/automation && git commit -m "$(cat <<'EOF'
docs(02): bump LATTICE-PIN.md to Phase 2 Lattice HEAD + populate LSDK-02..08 REQ-IDs

Phase 2 closure on the FSB audit-trail side. Two artifact updates:

(1) .planning/LATTICE-PIN.md
  - Frontmatter current_lattice_sha advances from Phase 1's
    22bf98627ae86b1576db5d34cf447ab2b321b3e1 to the new Lattice HEAD
    after all four Phase 2 Lattice commits land.
  - Per-FSB-Phase Log table gains one new row for Phase 2 referencing
    all four Phase 2 Lattice commit SHAs:
      * feat(receipts) at lattice/packages/lattice/src/receipts/ (LSDK-02 + LSDK-03)
      * feat(contract) at lattice/packages/lattice/src/contract/bands.ts (LSDK-04..08)
      * feat(api) at lattice/packages/lattice/src/index.ts (re-export bump)
      * docs(fsb-integration) at lattice/docs/fsb-integration-gaps.md (audit row flips)
  - Phase 1 row in the body is UNCHANGED (append-only audit trail per CD-06).

(2) .planning/REQUIREMENTS.md
  - LSDK section gets concrete REQ-IDs LSDK-02 through LSDK-08 populated
    with their Covered status + descriptions. Replaces the TBD bullets
    that Phase 1's LSDK-01 audit identified.
  - LSDK-02 (receipt step-marker fields) + LSDK-03 (sessionId) -- Lattice
    commit at receipts/types.ts + receipt.ts + verify.ts.
  - LSDK-04..08 (band pipeline + lifecycle event union) -- Lattice commit
    at contract/bands.ts.
  - LSDK-01 (Phase 1 audit) entry UNCHANGED.
  - Provider / delegation / MV3-survivability bullets remain TBD --
    they map to Phases 4, 5, 6 per the v0.10.0-attempt-2 ROADMAP.
  - FINT, MCP, PRV sections + Hard Invariants are byte-unchanged.

Per CONTEXT.md D-16 (LATTICE-PIN.md bumped ONCE at phase end) and D-17
(LSDK REQ-IDs populated at audit-doc row granularity). Phase 2's seven
REQs trace cleanly: REQUIREMENTS.md -> LATTICE-PIN.md Phase 2 row ->
Lattice commit SHA -> Lattice repo HEAD on fsb-integration-experiments.

Lattice working tree confirmed clean; D-15 (no push to Lattice remote)
holds: `cd lattice && git reflog | grep -c push` returns 0.
EOF
)"
```

**Step 2.5 -- Verify the commit landed and FSB tree is clean.**

```bash
cd /Users/lakshmanturlapati/Desktop/FSB/automation && git log -1 --format="%s"   # MUST match the subject
cd /Users/lakshmanturlapati/Desktop/FSB/automation && git status --porcelain     # MUST be empty
cd /Users/lakshmanturlapati/Desktop/FSB/automation/lattice && git status --porcelain   # MUST be empty
cd /Users/lakshmanturlapati/Desktop/FSB/automation/lattice && git reflog -20 | grep -c push   # MUST be 0 (D-15)
```

NO push to FSB git either. Orchestrator handles push decisions.
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Desktop/FSB/automation && grep -c "LSDK-02\|LSDK-03\|LSDK-04\|LSDK-05\|LSDK-06\|LSDK-07\|LSDK-08" .planning/REQUIREMENTS.md | grep -q "^7$" && git log -1 --format="%s" | grep -F "docs(02): bump LATTICE-PIN.md to Phase 2 Lattice HEAD + populate LSDK-02..08 REQ-IDs" && git status --porcelain | wc -l | grep -q "^0$" && cd /Users/lakshmanturlapati/Desktop/FSB/automation/lattice && git reflog -20 | grep -c push | grep -q "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "LSDK-02" .planning/REQUIREMENTS.md` returns 1.
    - `grep -c "LSDK-03" .planning/REQUIREMENTS.md` returns 1.
    - `grep -c "LSDK-04" .planning/REQUIREMENTS.md` returns 1.
    - `grep -c "LSDK-05" .planning/REQUIREMENTS.md` returns 1.
    - `grep -c "LSDK-06" .planning/REQUIREMENTS.md` returns 1.
    - `grep -c "LSDK-07" .planning/REQUIREMENTS.md` returns 1.
    - `grep -c "LSDK-08" .planning/REQUIREMENTS.md` returns 1.
    - `grep -c "LSDK-01" .planning/REQUIREMENTS.md` returns >= 1 (Phase 1 entry preserved).
    - `grep -c "DONE 2026-05-24 (Phase 02" .planning/REQUIREMENTS.md` returns 7 (one per new LSDK).
    - `grep -c "FINT-01" .planning/REQUIREMENTS.md` returns >= 1 (Phase 1 deliverable preserved).
    - `grep -c "MCP-01\|MCP-02" .planning/REQUIREMENTS.md` returns >= 2 (Phase 1 entries preserved).
    - `grep -c "createHookPipeline" .planning/REQUIREMENTS.md` returns >= 1.
    - `grep -c "lattice-receipt/v1.1" .planning/REQUIREMENTS.md` returns >= 1.
    - `cd /Users/lakshmanturlapati/Desktop/FSB/automation && git log -1 --format="%s"` returns exactly `docs(02): bump LATTICE-PIN.md to Phase 2 Lattice HEAD + populate LSDK-02..08 REQ-IDs`.
    - `cd /Users/lakshmanturlapati/Desktop/FSB/automation && git diff HEAD~1 HEAD --name-only` returns exactly: `.planning/LATTICE-PIN.md`, `.planning/REQUIREMENTS.md`.
    - `cd /Users/lakshmanturlapati/Desktop/FSB/automation && git status --porcelain` returns empty.
    - `cd /Users/lakshmanturlapati/Desktop/FSB/automation/lattice && git status --porcelain` returns empty.
    - `cd /Users/lakshmanturlapati/Desktop/FSB/automation/lattice && git reflog -20 | grep -c push` returns 0 (D-15 still holds across the whole phase).
    - No emojis in either file or the commit message.
  </acceptance_criteria>
  <done>REQUIREMENTS.md LSDK-02..08 populated; one FSB commit lands closing Phase 2; both FSB + Lattice trees clean; D-15 (no push) preserved end-to-end.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FSB plan-time SHA capture -> LATTICE-PIN.md frontmatter | Cross-repo SHA sync gate; planner reads from Lattice's git, writes to FSB's .planning/. |
| REQUIREMENTS.md REQ-IDs -> downstream traceability | REQ-IDs become the index for milestone verification + audit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | T (Tampering) | Cross-repo SHA drift | mitigate | Step 1.1 explicitly compares `02-03-LATTICE-SHA.txt` against `cd lattice && git rev-parse fsb-integration-experiments`. STOP if drift. The acceptance criteria run the SHA-match gate again at verification time. |
| T-02-05-02 | T (Tampering) | Wrong SHA written to PIN.md (typo) | mitigate | Step 1.1 reads the SHA from a file (no manual typing); Step 1.4 grep + the acceptance gate compares the in-file SHA against the live Lattice rev-parse. |
| T-02-05-03 | I (Info disclosure) | LATTICE-PIN.md exposed via FSB git? | accept | `.planning/` is .gitignored; the `-f` add flag (from CD-06 + Phase 1 precedent) means the file IS committed to FSB git -- intentional, mirrors Phase 1. The file contains no PII / secrets, only Lattice SHAs + Phase narratives. |
| T-02-05-04 | T (Tampering) | Phase 1 LATTICE-PIN.md row tampered with | mitigate | Step 1.4 confirms Phase 1 short-SHAs (ab6c1f6, 195e5ae, 22bf986) still appear in the file. Append-only audit trail per CD-06. |
| T-02-05-05 | T (Tampering) | LSDK-01 description overwritten | mitigate | Step 2.2 says replace ONLY the two specific TBD bullets (LSDK-NN..M Receipt-shaped + LSDK-MM..K Tripwire/hook). LSDK-01 entry stays byte-identical. Acceptance grep confirms LSDK-01 still present. |
| T-02-05-06 | E (Elevation of privilege) | Accidental push to Lattice remote | mitigate | This plan touches NO Lattice files. `cd lattice && git status --porcelain` returns empty -- no Lattice work happens here. Step 2.5 reflog grep returns 0. |
| T-02-05-07 | T (Tampering) | Wrong commit message subject | mitigate | The HEREDOC commit message has the exact subject string spec'd. Acceptance gate grep -F matches the literal string. |

Block on: high.

</threat_model>

<verification>

After both tasks complete, verify at the plan boundary AND at the phase boundary (this is the final Phase 2 plan):

```bash
# LATTICE-PIN.md frontmatter sync
PIN_SHA=$(grep "^current_lattice_sha:" /Users/lakshmanturlapati/Desktop/FSB/automation/.planning/LATTICE-PIN.md | awk '{print $2}')
LATTICE_SHA=$(cd /Users/lakshmanturlapati/Desktop/FSB/automation/lattice && git rev-parse fsb-integration-experiments)
test "$PIN_SHA" = "$LATTICE_SHA" && echo "PIN-SHA SYNC OK" || echo "DRIFT"

# Phase 2 row appended
grep -c "Phase 2" /Users/lakshmanturlapati/Desktop/FSB/automation/.planning/LATTICE-PIN.md

# Phase 1 row preserved
grep -c "ab6c1f6\|195e5ae\|22bf986" /Users/lakshmanturlapati/Desktop/FSB/automation/.planning/LATTICE-PIN.md

# LSDK REQ-IDs populated
grep -c "LSDK-0[2-8]" /Users/lakshmanturlapati/Desktop/FSB/automation/.planning/REQUIREMENTS.md

# Phase 1 LSDK-01 preserved
grep -c "LSDK-01" /Users/lakshmanturlapati/Desktop/FSB/automation/.planning/REQUIREMENTS.md

# FSB commit landed
cd /Users/lakshmanturlapati/Desktop/FSB/automation && git log -1 --format="%s"

# FSB + Lattice trees both clean
cd /Users/lakshmanturlapati/Desktop/FSB/automation && git status --porcelain
cd /Users/lakshmanturlapati/Desktop/FSB/automation/lattice && git status --porcelain

# D-15 final check: zero pushes on Lattice across all of Phase 2
cd /Users/lakshmanturlapati/Desktop/FSB/automation/lattice && git reflog -50 | grep -c push

# Phase 2 Lattice commits count: should be exactly 4 since Phase 1 HEAD (22bf986)
cd /Users/lakshmanturlapati/Desktop/FSB/automation/lattice && git log 22bf986..HEAD --format="%s" | wc -l   # MUST be 4

# Every Phase 2 Lattice commit carries the Ref footer
cd /Users/lakshmanturlapati/Desktop/FSB/automation/lattice && git log 22bf986..HEAD --format="%B" | grep -c "Ref: FSB v0.10.0-attempt-2 Phase 2"   # MUST be 4

# INV gates
grep -c "setTimeout" /Users/lakshmanturlapati/Desktop/FSB/automation/extension/ai/agent-loop.js   # MUST be 8
cd /Users/lakshmanturlapati/Desktop/FSB/automation && node tests/tool-definitions-parity.test.js  # MUST exit 0

# Phase 1 smoke regression check
cd /Users/lakshmanturlapati/Desktop/FSB/automation && node tests/lattice-smoke.test.js  # MUST exit 0 (29 PASS)

# Phase 2 smoke
cd /Users/lakshmanturlapati/Desktop/FSB/automation && node tests/lattice-tripwire-smoke.test.js  # MUST exit 0

# Option B reconciliation
cd /Users/lakshmanturlapati/Desktop/FSB/automation && git diff $(git merge-base origin/main HEAD)..HEAD --name-only | grep -E "^(extension|mcp)/" | wc -l   # MUST be 0

# Full test chain
cd /Users/lakshmanturlapati/Desktop/FSB/automation && npm test
```

</verification>

<success_criteria>

- [ ] `.planning/LATTICE-PIN.md` frontmatter `current_lattice_sha` matches `cd lattice && git rev-parse fsb-integration-experiments` (40-char hex).
- [ ] `.planning/LATTICE-PIN.md` body has 1 new Phase 2 row appended (Phase 1 row + Phase 2 row total in the per-phase table); all 4 Phase 2 Lattice commit SHAs referenced.
- [ ] Phase 1 row in PIN.md is byte-unchanged (append-only).
- [ ] `.planning/REQUIREMENTS.md` LSDK section gains 7 concrete REQ-IDs: LSDK-02 through LSDK-08, all marked `[x]` Done.
- [ ] LSDK-01 entry in REQUIREMENTS.md is byte-unchanged.
- [ ] Provider / delegation / MV3-survivability TBD bullets remain (Phases 4/5/6).
- [ ] FINT, MCP, PRV sections + Hard Invariants block byte-unchanged.
- [ ] One FSB commit lands on `automation` branch with subject `docs(02): bump LATTICE-PIN.md to Phase 2 Lattice HEAD + populate LSDK-02..08 REQ-IDs`.
- [ ] FSB `git status` empty post-commit.
- [ ] Lattice `git status` empty post-commit (no Lattice work in this plan).
- [ ] D-15 holds end-to-end: `cd lattice && git reflog | grep -c push` returns 0.
- [ ] Exactly 4 Lattice commits since Phase 1 HEAD (`22bf986`), all carrying the `Ref: FSB v0.10.0-attempt-2 Phase 2` footer.
- [ ] Phase 1 smoke (`tests/lattice-smoke.test.js`) and Phase 2 smoke (`tests/lattice-tripwire-smoke.test.js`) both exit 0.
- [ ] INV-01 holds (`node tests/tool-definitions-parity.test.js` exits 0 with 142 PASS).
- [ ] INV-04 holds (`grep -c "setTimeout" extension/ai/agent-loop.js` returns 8).
- [ ] Option B reconciliation holds: zero `extension/*` or `mcp/*` modifications since branch reset.
- [ ] `npm test` exits 0 (full chain green).
- [ ] No emojis anywhere (PIN.md, REQUIREMENTS.md, commit message).

</success_criteria>

<output>
After completion, create `.planning/phases/02-lattice-tripwire-receipt-extension/02-05-SUMMARY.md` capturing:
- The PIN.md frontmatter old -> new SHA diff
- The 7 LSDK REQ-IDs populated with their underlying Lattice commit SHAs
- The FSB commit SHA + subject
- Phase 2 closure summary: 4 Lattice commits + 2 FSB commits total, all D-15 / D-14 ceremony observed
- Cross-repo audit trail proof: REQUIREMENTS.md -> LATTICE-PIN.md Phase 2 row -> Lattice SHA -> Lattice commit message Ref footer

Phase 2 is closed when this commit lands. The verifier (`/gsd-verify-phase 2`) reads this SUMMARY chain.
</output>
