---
phase: 28-lean-mcp-surface-capability-search-eval-harness
plan: 04
type: execute
wave: 3
depends_on: ["28-01", "28-02", "28-03"]
files_modified:
  - tests/capability-mcp-surface.test.js
  - package.json
autonomous: true
requirements: [SURF-03, SURF-05, SURF-02]
user_setup: []

must_haves:
  truths:
    - "A new tests/capability-mcp-surface.test.js asserts the built MCP module exposes search_capabilities AND invoke_capability on the wire"
    - "The test asserts the tool-definitions-parity registry hash is UNCHANGED (INV-01) — the two tools are out-of-registry"
    - "The test asserts the queue split: search_capabilities is in readOnlyTools (bypasses) and invoke_capability is not (serialized)"
    - "The test asserts an unknown slug yields RECIPE_NOT_FOUND, and both new tests are appended to the npm test chain"
  artifacts:
    - path: "tests/capability-mcp-surface.test.js"
      provides: "INV-01 proof: two tools on wire + registry hash unchanged + queue split + RECIPE_NOT_FOUND"
      contains: "EXPECTED_NON_TRIGGER_REGISTRY_HASH"
      min_lines: 40
    - path: "package.json"
      provides: "Both new phase tests appended to the test &&-chain"
      contains: "capability-mcp-surface.test.js"
  key_links:
    - from: "tests/capability-mcp-surface.test.js"
      to: "mcp/build (the built MCP module)"
      via: "dynamic import / require of the built runtime to assert both tool names registered"
      pattern: "search_capabilities"
    - from: "tests/capability-mcp-surface.test.js"
      to: "tests/tool-definitions-parity.test.js"
      via: "re-asserts EXPECTED_NON_TRIGGER_REGISTRY_HASH unchanged (INV-01)"
      pattern: "EXPECTED_NON_TRIGGER_REGISTRY_HASH"
    - from: "package.json"
      to: "tests/capability-mcp-surface.test.js"
      via: "appended to the test &&-chain after capability-search-eval.test.js"
      pattern: "capability-mcp-surface.test.js"
---

<objective>
Deliver the INV-01 surface proof: a new tests/capability-mcp-surface.test.js asserting both capability tools are on the wire while the tool-definitions-parity registry hash stays unchanged (the explicit INV-01 proof for this phase), plus the queue split (search bypasses / invoke serializes) and the RECIPE_NOT_FOUND path. Append both new phase tests to the npm test chain so the full milestone gate runs in CI.

Purpose: SURF-03 (both tools out-of-registry, registry hash unchanged — the INV-01 assertion), SURF-05 (queue split assertion), and the RECIPE_NOT_FOUND half of SURF-02. Honors D-15.

Output: tests/capability-mcp-surface.test.js and the package.json test-chain entry.

Depends on 28-02 (the built MCP module exposes the two tools), 28-03 (the dispatcher invoke path for the RECIPE_NOT_FOUND assertion), and 28-01 (shared package.json — the eval-test entry was added there; this plan appends the surface-test entry).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-lean-mcp-surface-capability-search-eval-harness/28-CONTEXT.md
@.planning/phases/28-lean-mcp-surface-capability-search-eval-harness/28-RESEARCH.md
@.planning/phases/28-lean-mcp-surface-capability-search-eval-harness/28-PATTERNS.md
@.planning/phases/28-lean-mcp-surface-capability-search-eval-harness/28-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the live tree 2026-06-20. Use directly. -->

tool-definitions-parity.test.js (the INV-01 lock):
  - EXPECTED_NON_TRIGGER_REGISTRY_HASH = 'ad6efb8cc3275d964488b67222129b1c0278c5c3b69c64888d926beb89a3926b' (:52).
  - stable(value) recursive key-sort stringify (:54-63); registryHash(nonTriggerTools) === EXPECTED_NON_TRIGGER_REGISTRY_HASH (:134); TOOL_REGISTRY.length === EXPECTED_NON_TRIGGER_TOOL_NAMES.length + TRIGGER_TOOL_NAMES.length (:136).

Built MCP module: npm --prefix mcp run build runs EARLIER in the test chain; the built runtime can be dynamic-imported / required to enumerate registered tool names (the mcp-recovery-messaging.test.js precedent dynamic-imports the built mcp module). search_capabilities + invoke_capability are registered via server.tool() in capabilities.ts (out-of-registry) — they appear on the McpServer tool list but NOT in TOOL_REGISTRY.

queue.ts: search_capabilities is in the readOnlyTools Set (bypasses); invoke_capability is NOT (serialized). The enqueue bypass: if (readOnlyTools.has(name)) return fn().

Dispatcher invoke handler (Plan 03): an unknown slug returns { success:false, code:'RECIPE_NOT_FOUND', errorCode:'RECIPE_NOT_FOUND', error:'RECIPE_NOT_FOUND', slug }. mapFSBError surfaces RECIPE_.+ verbatim (errors.ts:137).

Zero-framework convention (tests/capability-interpreter.test.js): const assert = require('assert'); let passed/failed; function check(cond,msg); process.exit(failed>0?1:0). UMD require('../extension/lib/minisearch.min.js') works in Node.

package.json test chain (:17): after Plan 01 it ENDS with ... && node tests/capability-fetch.test.js && node tests/capability-search-eval.test.js. ci (:32) runs npm test.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write capability-mcp-surface.test.js (two tools on wire + registry hash unchanged + queue split + RECIPE_NOT_FOUND)</name>
  <read_first>
    - tests/capability-interpreter.test.js (the zero-framework convention: requires, check counters, process.exit)
    - tests/tool-definitions-parity.test.js (lines 38-63 the EXPECTED_NON_TRIGGER_TOOL_NAMES list + EXPECTED_NON_TRIGGER_REGISTRY_HASH + stable() stringify; 125-136 the registry-size + hash assertions)
    - tests/mcp-recovery-messaging.test.js (the precedent for dynamic-importing / requiring the BUILT mcp module to inspect registered tools)
    - mcp/src/queue.ts (the readOnlyTools Set — assert search_capabilities present, invoke_capability absent)
    - .planning/phases/28-lean-mcp-surface-capability-search-eval-harness/28-PATTERNS.md (section "tests/capability-mcp-surface.test.js" — the hash assert + wire-presence + queue split + RECIPE_NOT_FOUND)
    - .planning/phases/28-lean-mcp-surface-capability-search-eval-harness/28-VALIDATION.md (the SURF-02/03/05 -> test map rows)
  </read_first>
  <action>
    Create tests/capability-mcp-surface.test.js following the zero-framework FSB convention (const assert = require('assert'); passed/failed counters; function check(cond,msg); process.exit(failed>0?1:0)). Assert four things (D-15):

    (a) BOTH tools on the wire (SURF-03): enumerate the tools the built MCP runtime registers (dynamic-import / require the built mcp module the same way mcp-recovery-messaging.test.js does; build it first if the test runs standalone) and assert the registered tool-name set includes both 'search_capabilities' and 'invoke_capability'. If directly enumerating McpServer's internal tool list is impractical in this build, fall back to a source-level assertion that mcp/src/tools/capabilities.ts registers both via server.tool() AND that registerCapabilityTools is called in mcp/src/runtime.ts — but prefer the built-module enumeration (the true wire proof).

    (b) Registry hash UNCHANGED (SURF-03 / INV-01): require the built tool-definitions and assert registryHash(nonTriggerTools) === EXPECTED_NON_TRIGGER_REGISTRY_HASH (reuse the value 'ad6efb8cc3275d964488b67222129b1c0278c5c3b69c64888d926beb89a3926b' and the stable()/registryHash helper from tool-definitions-parity.test.js). The two new tools must NOT have moved the hash because they are out-of-registry. (The existing tool-definitions-parity.test.js already proves this; this test re-asserts it adjacent to the wire-presence check so a single file is the INV-01 proof for the phase.)

    (c) Queue split (SURF-05): require mcp/src/queue.ts (or the built queue) and assert the readOnlyTools Set CONTAINS 'search_capabilities' (bypasses) and does NOT contain 'invoke_capability' (serialized). A source-level string assertion on the queue source is acceptable if instantiating the TaskQueue is impractical; prefer instantiating and probing readOnlyTools.has(...) if the export allows.

    (d) RECIPE_NOT_FOUND (SURF-02): assert that an unknown slug yields the RECIPE_NOT_FOUND typed error and that mapFSBError surfaces RECIPE_.+ verbatim. Either: exercise the dispatcher invoke handler with all three engine globals present but getRecipeBySlug mocked to return null (and executeBoundSpec mocked so it is NOT reached), asserting code/errorCode/error === 'RECIPE_NOT_FOUND'; OR assert mapFSBError({ errorCode:'RECIPE_NOT_FOUND', error:'RECIPE_NOT_FOUND', success:false }) returns 'RECIPE_NOT_FOUND' verbatim against the built errors module. Choose the lighter-weight reliable form; the goal is to prove the unknown-slug code surfaces verbatim, not to stand up the full SW.

    Keep the file free of dynamic-code substrings (it is a test under tests/, not on the recipe-path guard, but stay clean defensively). Do NOT add the two tools to TOOL_REGISTRY anywhere — that would move the hash and red BOTH this test and tool-definitions-parity.test.js.
  </action>
  <verify>
    <automated>node tests/capability-mcp-surface.test.js</automated>
  </verify>
  <acceptance_criteria>
    - tests/capability-mcp-surface.test.js exists and `node tests/capability-mcp-surface.test.js` exits 0 (run npm --prefix mcp run build first if standalone).
    - The test references both tool names: `grep -c "search_capabilities" tests/capability-mcp-surface.test.js` >= 1 AND `grep -c "invoke_capability" tests/capability-mcp-surface.test.js` >= 1.
    - The test references EXPECTED_NON_TRIGGER_REGISTRY_HASH (the INV-01 hash assertion): `grep -c "EXPECTED_NON_TRIGGER_REGISTRY_HASH" tests/capability-mcp-surface.test.js` >= 1.
    - The test asserts search_capabilities in readOnlyTools and invoke_capability NOT in it (queue split).
    - The test asserts RECIPE_NOT_FOUND surfaces verbatim: `grep -c "RECIPE_NOT_FOUND" tests/capability-mcp-surface.test.js` >= 1.
  </acceptance_criteria>
  <done>The surface test proves both tools on the wire, the INV-01 registry hash unchanged, the queue split, and RECIPE_NOT_FOUND verbatim surfacing — and passes.</done>
</task>

<task type="auto">
  <name>Task 2: Append both new phase tests to the npm test chain and confirm the full gate</name>
  <read_first>
    - package.json (line 17 — after Plan 01 the test chain ENDS with node tests/capability-search-eval.test.js; line 32 ci runs npm test)
    - .planning/phases/28-lean-mcp-surface-capability-search-eval-harness/28-PATTERNS.md (section "tests/capability-mcp-surface.test.js" — the test-chain append note)
    - .planning/phases/28-lean-mcp-surface-capability-search-eval-harness/28-VALIDATION.md (Sampling Rate — per-wave npm test, phase-gate npm run ci)
    - tests/capability-mcp-surface.test.js (created in Task 1)
  </read_first>
  <action>
    Append " && node tests/capability-mcp-surface.test.js" to the package.json test script (:17) AFTER the existing trailing node tests/capability-search-eval.test.js (added in Plan 01). The chain order after this plan ends: ... && node tests/capability-fetch.test.js && node tests/capability-search-eval.test.js && node tests/capability-mcp-surface.test.js. ci (:32) runs npm test, so both new tests gate automatically. Do not reorder or remove any existing chain entry.

    Run npm test once to confirm the full chain is green end-to-end with both new phase tests included (this exercises the mcp build + tool-definitions-parity + capability-* + the two new phase tests together). If the run is too heavy to complete in the task budget, at minimum confirm the tail of the chain green: node tests/capability-search-eval.test.js && node tests/capability-mcp-surface.test.js && node tests/tool-definitions-parity.test.js.
  </action>
  <verify>
    <automated>node tests/capability-search-eval.test.js && node tests/capability-mcp-surface.test.js && node tests/tool-definitions-parity.test.js</automated>
  </verify>
  <acceptance_criteria>
    - package.json test script references both new phase tests: `grep -c "capability-search-eval.test.js" package.json` >= 1 AND `grep -c "capability-mcp-surface.test.js" package.json` >= 1.
    - The surface-test entry comes AFTER the eval-test entry in the chain; no existing entry was reordered or removed.
    - The verify command (eval + surface + parity) exits 0 — the milestone gate (recall@5>=0.9, wrong-invoke=0, two tools on wire, INV-01 hash unchanged) is green.
  </acceptance_criteria>
  <done>Both new phase tests are in the npm test chain after capability-fetch.test.js, and the tail-of-chain gate (eval + surface + parity) is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| new tools -> TOOL_REGISTRY (NOT crossed) | The INV-01 boundary the surface test enforces: the two tools must remain out-of-registry |
| CI test chain -> milestone gate | The eval + surface tests are the deterministic gate that blocks regressions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-03 | Tampering (schema-lock regression) | tool-definitions-parity registry hash | mitigate | capability-mcp-surface.test.js re-asserts EXPECTED_NON_TRIGGER_REGISTRY_HASH unchanged AND both tools on wire; appended to npm test so any future TOOL_REGISTRY drift reds CI (Task 1, Task 2) |
| T-28-05 | DoS (queue) regression | readOnlyTools split | mitigate | The surface test asserts search_capabilities bypasses and invoke_capability serializes; a future accidental move of invoke_capability into readOnlyTools reds the gate (Task 1) |
| T-28-08 | Tampering (context bloat) | progressive-disclosure cap | mitigate | The eval test (Plan 01) caps hits at <=5; this plan keeps that test in the gating chain so the anti-bloat invariant cannot silently regress (Task 2) |
| T-28-SC | Tampering | npm/pip/cargo installs | accept | Phase 28 introduces ZERO new external packages (RESEARCH Package Legitimacy Audit). No install task in this plan; no per-install legitimacy checkpoint required |
</threat_model>

<verification>
Per-task commit: `node tests/capability-mcp-surface.test.js`
Plan close: `node tests/capability-search-eval.test.js && node tests/capability-mcp-surface.test.js && node tests/tool-definitions-parity.test.js`
Phase gate (before /gsd:verify-work): full `npm run ci` green (validate:extension + npm test + mcp-smoke + showcase).
</verification>

<success_criteria>
- tests/capability-mcp-surface.test.js proves both tools on the wire, the INV-01 registry hash unchanged, the queue split, and RECIPE_NOT_FOUND verbatim — and passes.
- Both new phase tests are appended to the npm test chain after capability-fetch.test.js; ci runs them.
- The tail-of-chain gate (eval + surface + parity) is green: recall@5>=0.9, wrong-invoke=0, two tools on wire, INV-01 hash unchanged.
</success_criteria>

<output>
Create `.planning/phases/28-lean-mcp-surface-capability-search-eval-harness/28-04-SUMMARY.md` when done
</output>
