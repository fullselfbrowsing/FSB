---
phase: 28-lean-mcp-surface-capability-search-eval-harness
plan: 03
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - extension/ws/mcp-tool-dispatcher.js
  - extension/ws/mcp-bridge-client.js
autonomous: true
requirements: [SURF-01, SURF-02]
user_setup: []

must_haves:
  truths:
    - "mcp:capabilities-search resolves the owned-tab origin SW-side (un-spoofable; model-supplied origin is a non-authoritative override), calls FsbCapabilitySearch.search, and returns { success:true, results } capped at <=5 ranked schema-on-hit hits"
    - "mcp:capabilities-invoke runs the routerless direct path slug -> getRecipeBySlug -> interpretRecipe -> executeBoundSpec and returns the normalized { success,status,finalUrl,redirected,data,text } shape or a typed RECIPE_* error verbatim"
    - "An unknown slug returns RECIPE_NOT_FOUND in the dual-field shape, surfacing verbatim via the existing errors.ts passthrough (no errors.ts edit)"
    - "The bridge-client switch delegates both new message types into dispatchMcpMessageRoute via thin _handleCapabilities* methods"
    - "Realizes CONTEXT decisions D-06 (routerless slug->interpretRecipe->executeBoundSpec invoke), D-07 (normalized result shape + free RECIPE_NOT_FOUND passthrough), D-10 (mcp:capabilities-search read-only + mcp:capabilities-invoke queued routes), D-11 (owned-tab origin resolved authoritatively SW-side for the bias; model-supplied origin is a non-authoritative override)"
  artifacts:
    - path: "extension/ws/mcp-tool-dispatcher.js"
      provides: "Two MCP_PHASE199_MESSAGE_ROUTES entries + handleCapabilitiesSearchMessageRoute + handleCapabilitiesInvokeMessageRoute + SW-side origin/tab resolution"
      contains: "handleCapabilitiesInvokeMessageRoute"
    - path: "extension/ws/mcp-bridge-client.js"
      provides: "Two switch cases + _handleCapabilitiesSearch + _handleCapabilitiesInvoke delegates"
      contains: "_handleCapabilitiesInvoke"
  key_links:
    - from: "extension/ws/mcp-tool-dispatcher.js"
      to: "extension/utils/capability-search.js"
      via: "FsbCapabilitySearch.search (bias) + FsbCapabilitySearch.getRecipeBySlug (invoke lookup)"
      pattern: "FsbCapabilitySearch"
    - from: "extension/ws/mcp-tool-dispatcher.js"
      to: "extension/utils/capability-interpreter.js + capability-fetch.js"
      via: "FsbCapabilityInterpreter.interpretRecipe then FsbCapabilityFetch.executeBoundSpec (routerless invoke)"
      pattern: "executeBoundSpec"
    - from: "extension/ws/mcp-bridge-client.js"
      to: "extension/ws/mcp-tool-dispatcher.js"
      via: "dispatchMcpMessageRoute for both mcp:capabilities-search and mcp:capabilities-invoke"
      pattern: "dispatchMcpMessageRoute"
---

<objective>
Wire the extension SW transport for the two capability tools: two MCP_PHASE199_MESSAGE_ROUTES entries (read-only search + queued invoke) with their dispatcher handlers, and the two bridge-client switch cases that delegate into dispatchMcpMessageRoute. The search handler resolves the owned-tab origin authoritatively SW-side for the bias; the invoke handler runs the routerless direct Phase-27 path (slug -> interpretRecipe -> executeBoundSpec) and surfaces RECIPE_NOT_FOUND for unknown slugs.

Purpose: SURF-01 (search returns ranked, schema-on-hit, origin-biased results — the SW resolves the un-spoofable origin) and SURF-02 (invoke executes the selected capability with validated params and returns a structured result). Honors D-06, D-07, D-10, D-11.

Output: the two dispatcher routes + handlers in mcp-tool-dispatcher.js and the two bridge delegates in mcp-bridge-client.js.

Depends on Plan 01 (28-01): the handlers call FsbCapabilitySearch.search and FsbCapabilitySearch.getRecipeBySlug, so extension/utils/capability-search.js must exist and be loaded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-lean-mcp-surface-capability-search-eval-harness/28-CONTEXT.md
@.planning/phases/28-lean-mcp-surface-capability-search-eval-harness/28-RESEARCH.md
@.planning/phases/28-lean-mcp-surface-capability-search-eval-harness/28-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the live tree 2026-06-20. Use directly. -->

mcp-tool-dispatcher.js:
  - MCP_PHASE199_MESSAGE_ROUTES (:84-116) — object map; the search-memory entry (:103) uses the standalone handler form: 'mcp:search-memory': { routeFamily: 'observability', handler: handleSearchMemoryMessageRoute }.
  - handleSearchMemoryMessageRoute (:2141-2159) — the exact template: guard (typeof X undefined -> createMcpRouteError) -> resolve -> clamp topN via boundedPositiveInt -> call -> return { success: true, results }.
  - boundedPositiveInt(value, defaultValue, maxValue) (:1413) — reuse for the topN clamp.
  - createMcpRouteError(tool, routeFamily, recoveryHint = MCP_ROUTE_RECOVERY_HINT, extra = {}) (:125); MCP_ROUTE_RECOVERY_HINT (:46).
  - createMcpOwnershipError(code, extra) (:190) returns the dual-field { success:false, code, errorCode:code, error:code, ...extra } shape used by every SW-side route error.
  - dispatchMcpMessageRoute (:462) — looks up MCP_PHASE199_MESSAGE_ROUTES[type] and calls route.handler.

Owned-tab origin resolution (un-spoofable) — the executeBoundSpec pattern (capability-fetch.js:285-291): tabOrigin = (tab && tab.url) ? new URL(tab.url).origin : null; wrapped in try/catch -> null.

Engine entry points (from Phase 26/27, loaded as SW globals):
  - FsbCapabilitySearch.search(query, ownedOrigin, topN) -> array of <=topN { slug, service, sideEffectClass, description, score, params } (Plan 01).
  - FsbCapabilitySearch.getRecipeBySlug(slug) -> recipe | null (Plan 01).
  - FsbCapabilityInterpreter.interpretRecipe(recipe, args) -> { success: true, spec } (validates args vs recipe.params SW-side) OR a typed { success:false, code/errorCode/error: 'RECIPE_*' } (capability-interpreter.js:236-369).
  - FsbCapabilityFetch.executeBoundSpec(spec, tabId) -> { success:true, status, finalUrl, redirected, data, text } OR { success:false, ... } (capability-fetch.js:272-385).

errors.ts passthrough (:137): explicitCode matching /^(TRIGGER_.+|RECIPE_.+|INVALID_TRIGGER_ID|INVALID_TAB_ID|LIFECYCLE_UNAVAILABLE|REFRESH_POLL_INTERVAL_TOO_LOW)$/ returns verbatim. RECIPE_NOT_FOUND is currently unused -> free to introduce; RECOVERY_AMBIGUOUS already whitelisted (:71). NO errors.ts edit needed.

mcp-bridge-client.js:
  - _handleMessage switch (:455-509); the search-memory case (:468) returns this._handleSearchMemory(payload); default (:507-508) throws 'Unknown MCP message type: ' + type.
  - _handleSearchMemory delegate (around :591) — const response = await dispatchMcpMessageRoute({ type:'mcp:search-memory', payload, client: this }); return response || {};
  - _getActiveTab (:517-520) — const [tab] = await chrome.tabs.query({ active:true, currentWindow:true }); return tab; (available if needed; origin/tab resolution lives in the dispatcher handlers).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the two dispatcher routes + handlers (read-only search with SW-side origin, routerless queued invoke)</name>
  <read_first>
    - extension/ws/mcp-tool-dispatcher.js (lines 84-116 MCP_PHASE199_MESSAGE_ROUTES, 2141-2159 handleSearchMemoryMessageRoute template, 1413 boundedPositiveInt, 125 createMcpRouteError, 190 createMcpOwnershipError, 46 MCP_ROUTE_RECOVERY_HINT)
    - extension/utils/capability-fetch.js (lines 272-385 executeBoundSpec result shape, 285-291 the new URL(tab.url).origin resolution pattern)
    - extension/utils/capability-interpreter.js (lines 236-369 interpretRecipe — validates recipe.params SW-side, returns { success, spec } or typed RECIPE_*)
    - .planning/phases/28-lean-mcp-surface-capability-search-eval-harness/28-PATTERNS.md (section "extension/ws/mcp-tool-dispatcher.js" — the route entries, both handler bodies, the un-spoofable origin resolution, the ownership-gate awareness note)
    - .planning/phases/28-lean-mcp-surface-capability-search-eval-harness/28-RESEARCH.md (lines 494-533 — the verified dispatcher route + handler reference draft)
  </read_first>
  <action>
    Add two entries to MCP_PHASE199_MESSAGE_ROUTES (:84-116), both using the standalone handler: form (like mcp:search-memory, NOT helperName:): 'mcp:capabilities-search': { routeFamily: 'capabilities', handler: handleCapabilitiesSearchMessageRoute } and 'mcp:capabilities-invoke': { routeFamily: 'capabilities', handler: handleCapabilitiesInvokeMessageRoute }.

    Define handleCapabilitiesSearchMessageRoute({ payload }) (SURF-01; model on handleSearchMemoryMessageRoute):
    - Availability guard: if (typeof FsbCapabilitySearch === 'undefined' || typeof FsbCapabilitySearch.search !== 'function') return createMcpRouteError('search_capabilities', 'capabilities', MCP_ROUTE_RECOVERY_HINT, { error: 'Capability search unavailable' }).
    - Resolve the owned-tab origin SW-side (un-spoofable, D-11): var ownedOrigin = payload.origin || null; (optional override) then if (!ownedOrigin) { try { var tabs = await chrome.tabs.query({ active: true, currentWindow: true }); ownedOrigin = (tabs[0] && tabs[0].url) ? new URL(tabs[0].url).origin : null; } catch (e) { ownedOrigin = null; } }. The model NEVER supplies the authoritative origin.
    - Clamp topN: var topN = boundedPositiveInt(payload.topN, 5, 5).
    - var results = FsbCapabilitySearch.search(payload.query || '', ownedOrigin, topN); return { success: true, results } (the search-memory return shape).

    Define handleCapabilitiesInvokeMessageRoute({ payload }) (SURF-02, routerless direct path D-06/D-07):
    - Availability guard: if (typeof FsbCapabilitySearch === 'undefined' || typeof FsbCapabilityInterpreter === 'undefined' || typeof FsbCapabilityFetch === 'undefined') return createMcpRouteError('invoke_capability', 'capabilities', MCP_ROUTE_RECOVERY_HINT, { error: 'Capability engine unavailable' }).
    - var recipe = FsbCapabilitySearch.getRecipeBySlug(payload.slug); if (!recipe) return { success: false, code: 'RECIPE_NOT_FOUND', errorCode: 'RECIPE_NOT_FOUND', error: 'RECIPE_NOT_FOUND', slug: payload.slug } (the dual-field shape — surfaces verbatim via errors.ts:137; NO errors.ts edit).
    - var interpreted = FsbCapabilityInterpreter.interpretRecipe(recipe, payload.params || {}); if (!interpreted || interpreted.success !== true) return interpreted (the typed RECIPE_* error verbatim — interpretRecipe already validated params vs recipe.params SW-side, D-06).
    - Resolve tabId: var tabId = Number.isFinite(payload.tab_id) ? payload.tab_id : null; if (tabId === null) { try { var t = await chrome.tabs.query({ active: true, currentWindow: true }); tabId = t[0] ? t[0].id : null; } catch (e) { tabId = null; } }.
    - return await FsbCapabilityFetch.executeBoundSpec(interpreted.spec, tabId) (the normalized { success,status,finalUrl,redirected,data,text } shape).

    Do NOT add a new fetch path that bypasses the engine origin-pin — executeBoundSpec re-asserts tabOrigin === spec.origin (capability-fetch.js:291) before any side effect (Wall 2 / V1 Architecture). Phase 28 runs UNGATED (the consent gate is Phase 30); these handlers resolve the active/owned tab directly via chrome.tabs.query, mirroring the search_memory precedent that does not gate either.
  </action>
  <verify>
    <automated>node --check extension/ws/mcp-tool-dispatcher.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check extension/ws/mcp-tool-dispatcher.js` exits 0 (no syntax error).
    - Both route entries exist: `grep -c "mcp:capabilities-search" extension/ws/mcp-tool-dispatcher.js` >= 1 and `grep -c "mcp:capabilities-invoke" extension/ws/mcp-tool-dispatcher.js` >= 1.
    - Both handler functions are defined: `grep -c "handleCapabilitiesSearchMessageRoute" extension/ws/mcp-tool-dispatcher.js` >= 2 (route entry + definition) and `grep -c "handleCapabilitiesInvokeMessageRoute" extension/ws/mcp-tool-dispatcher.js` >= 2.
    - The search handler resolves origin via new URL(...).origin from chrome.tabs.query and clamps topN via boundedPositiveInt(payload.topN, 5, 5).
    - The invoke handler calls FsbCapabilitySearch.getRecipeBySlug, returns the RECIPE_NOT_FOUND dual-field shape on absent recipe (`grep -c "RECIPE_NOT_FOUND" extension/ws/mcp-tool-dispatcher.js` >= 1), calls FsbCapabilityInterpreter.interpretRecipe, and returns FsbCapabilityFetch.executeBoundSpec(interpreted.spec, tabId).
    - No errors.ts edit was made (RECIPE_NOT_FOUND relies on the existing passthrough).
  </acceptance_criteria>
  <done>Both routes + handlers exist; search resolves the un-spoofable origin and returns <=5 schema-on-hit results; invoke runs the routerless path and surfaces RECIPE_NOT_FOUND for unknown slugs; the file parses.</done>
</task>

<task type="auto">
  <name>Task 2: Add the two bridge-client switch cases + thin delegate methods</name>
  <read_first>
    - extension/ws/mcp-bridge-client.js (lines 455-509 the _handleMessage switch + default throw at 507, the _handleSearchMemory delegate around :591, _getActiveTab :517-520)
    - .planning/phases/28-lean-mcp-surface-capability-search-eval-harness/28-PATTERNS.md (section "extension/ws/mcp-bridge-client.js" — the switch cases + the two delegate clones)
    - .planning/phases/28-lean-mcp-surface-capability-search-eval-harness/28-RESEARCH.md (lines 535-549 — the verified bridge delegate reference draft)
    - extension/ws/mcp-tool-dispatcher.js (the dispatchMcpMessageRoute target + the two new routes from Task 1)
  </read_first>
  <action>
    In extension/ws/mcp-bridge-client.js, add two cases to the _handleMessage switch (:455-509) BEFORE the default: throw — case 'mcp:capabilities-search': return this._handleCapabilitiesSearch(payload); and case 'mcp:capabilities-invoke': return this._handleCapabilitiesInvoke(payload);. Match the surrounding case style (the search-memory case at :468 returns this._handleSearchMemory(payload)).

    Add two thin delegate methods modeled on _handleSearchMemory (:591): async _handleCapabilitiesSearch(payload) { const response = await dispatchMcpMessageRoute({ type: 'mcp:capabilities-search', payload, client: this }); return response || {}; } and async _handleCapabilitiesInvoke(payload) { const response = await dispatchMcpMessageRoute({ type: 'mcp:capabilities-invoke', payload, client: this }); return response || {}; }. The origin/tab resolution lives in the DISPATCHER handlers (Task 1), so these delegates are pure pass-throughs — do NOT resolve origin/tab here.
  </action>
  <verify>
    <automated>node --check extension/ws/mcp-bridge-client.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check extension/ws/mcp-bridge-client.js` exits 0 (no syntax error).
    - Both switch cases exist before the default throw: `grep -c "mcp:capabilities-search" extension/ws/mcp-bridge-client.js` >= 1 and `grep -c "mcp:capabilities-invoke" extension/ws/mcp-bridge-client.js` >= 1.
    - Both delegate methods are defined: `grep -c "_handleCapabilitiesSearch" extension/ws/mcp-bridge-client.js` >= 2 (case + method) and `grep -c "_handleCapabilitiesInvoke" extension/ws/mcp-bridge-client.js` >= 2.
    - Each delegate calls dispatchMcpMessageRoute with the matching type and returns response || {}.
  </acceptance_criteria>
  <done>The bridge-client switch routes both new message types into dispatchMcpMessageRoute via thin delegates; the file parses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bridge payload (model-influenced) -> SW dispatcher handler | query/slug/params/origin/tab_id arrive from the model via the bridge |
| SW handler -> capability engine (interpret + fetch) | The invoke handler crosses into the credentialed MAIN-world fetch engine |
| owned-tab resolution -> search bias | The authoritative origin is established SW-side, not trusted from the model |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Spoofing / Elevation | model-supplied origin / tab_id for bias and invoke | mitigate | Owned-tab origin resolved SW-side via new URL(tab.url).origin (un-spoofable); payload.origin/tab_id are non-authoritative overrides; the engine active-tab origin-pin (executeBoundSpec :291) rejects a wrong-tab invoke before any side effect (Task 1) |
| T-28-02 | Tampering (wrong-but-plausible destructive auto-fire) | routerless invoke path | mitigate | invoke runs slug -> interpretRecipe (validates params + origin-pin) -> executeBoundSpec only; sideEffectClass is visible in the search results (Plan 01) so the model can disambiguate; the eval wrong-invoke=0 gate (Plan 01) backs the ranking. (Enforced disambiguation-before-mutate is Phase 30.) |
| T-28-07 | Information Disclosure | invoke result payload | mitigate | executeBoundSpec returns only non-secret response data { success,status,finalUrl,redirected,data,text } (capability-fetch.js:207-215); the handler forwards that shape unchanged — no cookies/auth in the payload |
| T-28-07b | Error Handling | unknown slug / interpret failure | mitigate | Unknown slug -> RECIPE_NOT_FOUND dual-field shape; interpret failures returned verbatim as typed RECIPE_* — surfaced via the existing errors.ts:137 passthrough, no leakage of internals (Task 1) |
| T-28-SC | Tampering | npm/pip/cargo installs | accept | Phase 28 introduces ZERO new external packages (RESEARCH Package Legitimacy Audit). No install task in this plan; no per-install legitimacy checkpoint required |
</threat_model>

<verification>
Per-task commit: `node --check extension/ws/mcp-tool-dispatcher.js && node --check extension/ws/mcp-bridge-client.js`
Plan close: `node --check extension/ws/mcp-tool-dispatcher.js && node --check extension/ws/mcp-bridge-client.js && node scripts/validate-extension.mjs`
The end-to-end wire behavior (search returns hits, invoke runs the path, RECIPE_NOT_FOUND surfaces) is asserted by the surface test in Plan 04 (which mocks executeBoundSpec).
</verification>

<success_criteria>
- Two MCP_PHASE199_MESSAGE_ROUTES entries (search read-only, invoke queued) with their standalone handlers exist and the dispatcher parses.
- The search handler resolves the un-spoofable owned-tab origin SW-side, clamps topN to <=5, and returns { success:true, results }.
- The invoke handler runs slug -> getRecipeBySlug -> interpretRecipe -> executeBoundSpec, returns the normalized result shape, and surfaces RECIPE_NOT_FOUND for unknown slugs (no errors.ts edit).
- The bridge-client switch delegates both new message types into dispatchMcpMessageRoute and parses.
</success_criteria>

<output>
Create `.planning/phases/28-lean-mcp-surface-capability-search-eval-harness/28-03-SUMMARY.md` when done
</output>
