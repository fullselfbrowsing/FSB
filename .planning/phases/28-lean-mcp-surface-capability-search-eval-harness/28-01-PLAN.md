---
phase: 28-lean-mcp-surface-capability-search-eval-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extension/utils/capability-search.js
  - catalog/descriptors/github-notifications.json
  - catalog/descriptors/_fixtures/seed-descriptors.json
  - catalog/descriptors/_fixtures/seed-recipes.json
  - catalog/descriptors/_fixtures/intent-cases.json
  - scripts/package-extension.mjs
  - scripts/verify-recipe-path-guard.mjs
  - extension/background.js
  - tests/capability-search-eval.test.js
  - package.json
autonomous: true
requirements: [SURF-04, SURF-06, SURF-01]
user_setup: []

must_haves:
  truths:
    - "A persisted minisearch index is built from descriptor docs over intent synonyms + service + actionVerb + sideEffectClass and snapshots to chrome.storage.local under key fsbCapabilityIndex with a catalogVersion stamp"
    - "search(query, ownedOrigin, topN) returns <=5 ranked hits, each carrying slug, service, sideEffectClass, description, score, and the matched recipe's params schema (schema-on-hit)"
    - "The eval harness measures recall@5 and wrong-invoke over a seeded near-neighbor fixture set and the build fails when recall@5 < 0.9 OR wrong-invoke != 0"
    - "The catalog (recipes + descriptors) ships in the packaged extension via a build-time generated dual-export IIFE loaded at SW startup"
  artifacts:
    - path: "extension/utils/capability-search.js"
      provides: "MiniSearch index + slug->recipe map + INDEX_OPTIONS + buildOrRestore/buildIndex/search/getRecipeBySlug/deriveSideEffect"
      contains: "global.FsbCapabilitySearch"
      min_lines: 90
    - path: "catalog/descriptors/github-notifications.json"
      provides: "Descriptor for the one real recipe (slug == recipe id github.notifications)"
      contains: "intentSynonyms"
    - path: "catalog/descriptors/_fixtures/seed-descriptors.json"
      provides: "8-12 synthetic head-capability descriptors with near-neighbor send/post/message services"
      contains: "intentSynonyms"
    - path: "catalog/descriptors/_fixtures/intent-cases.json"
      provides: "intent -> expectedSlug pairs (3-5 paraphrases each) for the eval gate"
      contains: "expectedSlug"
    - path: "tests/capability-search-eval.test.js"
      provides: "recall@5 + wrong-invoke gate + toJSON/loadJSON round-trip assertion"
      contains: "wrong-invoke"
  key_links:
    - from: "tests/capability-search-eval.test.js"
      to: "extension/utils/capability-search.js"
      via: "require() of INDEX_OPTIONS + buildIndex helper (single source of truth, no options drift)"
      pattern: "require.*capability-search"
    - from: "extension/utils/capability-search.js"
      to: "extension/lib/minisearch.min.js"
      via: "MiniSearch UMD global; loadJSON(json, INDEX_OPTIONS) reuses the construction options constant"
      pattern: "INDEX_OPTIONS"
    - from: "scripts/package-extension.mjs"
      to: "extension/catalog/recipe-index.generated.js"
      via: "build-time generator emitting global.FsbRecipeIndex = { recipes, descriptors } before zip"
      pattern: "FsbRecipeIndex"
---

<objective>
Stand up the capability-search index layer, ship the catalog into the extension package, and deliver the eval-harness gate (recall@5 >= 0.9 AND wrong-invoke = 0). This is the Wave-0 test deliverable plus the one genuinely-new logic file in the phase. The index is the catalog's quality ceiling (Pitfall 6), so its eval gate is the milestone gate.

Purpose: SURF-04 (persisted minisearch index over synonyms+service+verb+side-effect, snapshotted to chrome.storage.local), SURF-06 (recall@k + wrong-invoke eval gate), and the search-ranking foundation for SURF-01. Honors D-01, D-02, D-04, D-05, D-08, D-12, D-13, D-14, D-16.

Output: extension/utils/capability-search.js (MiniSearch instance + slug->recipe map + persistence), catalog/descriptors/* (real descriptor + seeded synthetic fixtures), a catalog-ship step in package-extension.mjs, the allowlist entry, the SW startup wiring, and tests/capability-search-eval.test.js wired into the npm test chain.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-lean-mcp-surface-capability-search-eval-harness/28-CONTEXT.md
@.planning/phases/28-lean-mcp-surface-capability-search-eval-harness/28-RESEARCH.md
@.planning/phases/28-lean-mcp-surface-capability-search-eval-harness/28-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the live tree 2026-06-20. Use directly. -->

Dual-export IIFE shell to clone (extension/utils/capability-interpreter.js:1-2 open, :374-385 close):
  (function(global) { 'use strict'; ...logic... var exportsObj = {...}; global.FsbCapabilitySearch = exportsObj; if (typeof module !== 'undefined' && module.exports) { module.exports = exportsObj; } })(typeof globalThis !== 'undefined' ? globalThis : this);

typeof-guarded vendored-global accessor (capability-interpreter.js:55-72): return (typeof X !== 'undefined' && X) ? X : null;

Frozen side-effect source (capability-fetch.js:228): var MUTATING_METHODS = { POST: true, PUT: true, PATCH: true, DELETE: true };

MiniSearch UMD API (extension/lib/minisearch.min.js; require() works in Node; global MiniSearch):
  - new MiniSearch(options) — options carry idField, fields (searchable), storeFields (returned on hit)
  - ms.addAll(documents); ms.search(query, { combineWith, prefix, fuzzy, boost, boostDocument }); ms.toJSON()
  - MiniSearch.loadJSON(json, options) — options arg MANDATORY; throws 'loadJSON should be given the same options used when serializing the index' if absent/mismatched
  - boost = per-field weight map; boostDocument(id, term, storedFields) = per-doc multiplier (confirm arg order vs vendored source; Open Question 1)

Recipe shape (catalog/recipes/github-notifications.json — closed vocab, NO synonyms): { schemaVersion, id, origin, endpoint, method, authStrategy, extract }
Recipe with params (catalog/recipes/_fixtures/valid-recipe.json): adds params: { type:'object', properties, required } — the schema-on-hit source.

package-extension.mjs helpers (:4-11): execFileSync, existsSync, mkdirSync, readFileSync, rmSync from node:fs; dirname/join/resolve from node:path; ROOT, EXT_ROOT (= join(ROOT,'extension')), DIST_DIR; fail(message). Zip at :56 zips EXT_ROOT only. writeFileSync NOT yet imported.

RECIPE_PATH_ALLOWLIST (scripts/verify-recipe-path-guard.mjs:85-98) — array of strings; Check 4 (:288-296) enumerates extension/utils/capability-*.js from disk and FAILS on any not on the allowlist (FAIL-CLOSED).

background.js importScripts order block ends at capability-fetch.js (:143). minisearch.min.js already loaded at :120 ("not wired until Phase 28").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the catalog descriptors + seeded eval fixtures</name>
  <read_first>
    - catalog/recipes/github-notifications.json (the real recipe; slug must equal its id github.notifications)
    - catalog/recipes/_fixtures/valid-recipe.json (the params sub-document shape for schema-on-hit)
    - .planning/phases/28-lean-mcp-surface-capability-search-eval-harness/28-PATTERNS.md (section "catalog/descriptors/*.json + _fixtures/" — the descriptor doc shape D-01 and the seed guidance)
    - .planning/phases/28-lean-mcp-surface-capability-search-eval-harness/28-RESEARCH.md (lines 551-585 — descriptor shape + eval fixture sketch; Open Question 2 — near-neighbor seeding)
  </read_first>
  <action>
    Create the descriptor for the one real recipe at catalog/descriptors/github-notifications.json with EXACTLY these fields (D-01): slug ("github.notifications" — MUST equal the recipe id), service ("github.com"), intentSynonyms (4-5 intent paraphrases like "show my github notifications", "check github alerts", "list unread github notifications"), description ("List your unread GitHub notifications"), actionVerb ("list"), sideEffectClass ("read"). intentSynonyms are MANDATORY — terse endpoint names are the #1 recall-failure mode (Pitfall 1, D-02).

    Create the eval seed under catalog/descriptors/_fixtures/ (test-only data, NOT shipped, NOT on the recipe-path guard — mirrors catalog/recipes/_fixtures/):
    - seed-descriptors.json: an array of 8-12 synthetic head-capability descriptors (same field shape as above). CRITICAL for D-14 / Open Question 2: include NEAR-NEIGHBOR capabilities so the index can actually FAIL — e.g. several distinct send/post/message services (slack.post-message, discord.send-message, email.send, twitter.post-tweet), a read-vs-mutate-vs-destructive contrast (github.notifications read, github.create-issue mutate, github.delete-repo destructive), and at least two same-service capabilities differing only by verb. Author sideEffectClass per the method the paired recipe will use (GET=read, POST/PUT/PATCH=mutate, DELETE=destructive).
    - seed-recipes.json: an array of minimal recipes (one per seed descriptor, matching slug==recipe id) each carrying a method (so sideEffectClass cross-checks, D-02) and a params JSON-Schema sub-document (so schema-on-hit returns a non-null schema, D-08). Use the valid-recipe.json shape.
    - intent-cases.json: an array of { intent, expectedSlug } pairs, 3-5 paraphrases per seed capability. Include deliberately tricky near-neighbor intents ("message my team" -> the slack slug, NOT discord/email) so wrong-invoke=0 is a real bar, not trivially satisfied. Aim for >=30 fixtures total so recall@5 is a meaningful fraction.
  </action>
  <verify>
    <automated>node -e "const d=require('./catalog/descriptors/github-notifications.json'),sd=require('./catalog/descriptors/_fixtures/seed-descriptors.json'),sr=require('./catalog/descriptors/_fixtures/seed-recipes.json'),ic=require('./catalog/descriptors/_fixtures/intent-cases.json');const a=require('assert');a(d.slug==='github.notifications');a(Array.isArray(d.intentSynonyms)&&d.intentSynonyms.length>=3);a(sd.length>=8);a(sr.length===sd.length);const s=new Set(sd.map(x=>x.slug));a(sr.every(r=>s.has(r.id)));a(sr.every(r=>r.params&&r.params.type==='object'));a(ic.length>=30);a(ic.every(c=>s.has(c.expectedSlug)||c.expectedSlug==='github.notifications'));const c=new Set(sd.map(x=>x.sideEffectClass));a(c.has('read')&&c.has('mutate')&&c.has('destructive'));console.log('descriptors+fixtures OK',sd.length,'seeds',ic.length,'cases')"</automated>
  </verify>
  <acceptance_criteria>
    - catalog/descriptors/github-notifications.json exists; slug === github.notifications; intentSynonyms has >=3 entries; sideEffectClass === "read".
    - seed-descriptors.json has >=8 descriptors covering all three of read/mutate/destructive; includes >=3 near-neighbor send/post/message services.
    - seed-recipes.json has one recipe per seed descriptor (id matches a descriptor slug); every recipe carries a params object.
    - intent-cases.json has >=30 { intent, expectedSlug } pairs; every expectedSlug resolves to a known slug.
    - The verify command above exits 0.
  </acceptance_criteria>
  <done>Real descriptor + seeded near-neighbor synthetic descriptors + paired recipes + >=30 intent->slug fixtures exist and pass the structural check.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create capability-search.js (MiniSearch index + slug->recipe map + persistence) and register it on the recipe-path allowlist</name>
  <read_first>
    - extension/utils/capability-interpreter.js (lines 1-2 open shell, 55-72 typeof-guarded accessor, 374-385 close + dual export — clone this shell)
    - extension/utils/capability-fetch.js (line 228 MUTATING_METHODS — the deriveSideEffect source)
    - extension/lib/minisearch.min.js (the vendored UMD source — confirm the boostDocument(id, term, storedFields) arg order at implementation per Open Question 1; the loadJSON throw is verified)
    - .planning/phases/28-lean-mcp-surface-capability-search-eval-harness/28-RESEARCH.md (lines 386-491 — the verified reference draft of this exact module; treat as the starting implementation)
    - .planning/phases/28-lean-mcp-surface-capability-search-eval-harness/28-PATTERNS.md (section "extension/utils/capability-search.js" — the LOAD-BEARING loadJSON constraint + CI-guard coupling)
    - scripts/verify-recipe-path-guard.mjs (lines 85-98 RECIPE_PATH_ALLOWLIST, 288-296 Check 4 fail-closed disk-drift)
  </read_first>
  <behavior>
    - Test (round-trip, SURF-04): build index via the exported helper over the seed descriptors, toJSON(), then MiniSearch.loadJSON(json, INDEX_OPTIONS) -> searching the restored index yields IDENTICAL top hits to the original. Calling loadJSON WITHOUT the options arg throws.
    - Test (schema-on-hit, SURF-01/D-08): a search hit carries slug, service, sideEffectClass, description, score, AND params (the matched recipe's schema, or null if the recipe has none).
    - Test (cap, SURF-01): search(query, origin, topN) never returns more than min(topN,5) hits, default 5.
    - Test (origin bias, SURF-01/D-11): with two otherwise-equal candidates, the one whose service matches the passed ownedOrigin host outranks the generic one.
    - Test (side-effect derivation, D-02): deriveSideEffect('DELETE')==='destructive', deriveSideEffect('POST')==='mutate', deriveSideEffect('GET')==='read'.
  </behavior>
  <action>
    Create extension/utils/capability-search.js as a dual-export IIFE (clone the capability-interpreter.js shell). Use RESEARCH lines 386-491 as the verified reference draft.

    Define a MODULE-LEVEL INDEX_OPTIONS constant (the load-bearing fix for Pitfall 3): { idField: 'slug', fields: ['intentSynonyms','description','service','actionVerb'], storeFields: ['slug','service','sideEffectClass','description'] }. Pass this SAME constant to BOTH new MiniSearch(INDEX_OPTIONS) and MiniSearch.loadJSON(json, INDEX_OPTIONS) — the vendored source throws 'loadJSON should be given the same options used when serializing the index' otherwise. Export INDEX_OPTIONS so the eval test reuses it (no options drift).

    Define module-level constants: STORAGE_KEY = 'fsbCapabilityIndex', ORIGIN_BOOST = 4 (Claude's Discretion — tune via the eval test). Clone typeof-guarded accessors _getMiniSearch() (guards MiniSearch), _getChrome() (guards globalThis.chrome), _getCatalog() (guards FsbRecipeIndex, the build-time global from Task 4 — degrade to { recipes: [], descriptors: [] } when absent).

    Implement and export this surface (D-04, D-05, D-06, D-08):
    - deriveSideEffect(method): DELETE -> 'destructive'; POST/PUT/PATCH -> 'mutate'; else (GET/HEAD) -> 'read'. Mirrors MUTATING_METHODS.
    - buildIndex(descriptors, slugToRecipe): a PURE helper that constructs new MiniSearch(INDEX_OPTIONS), addAlls the descriptor docs (cross-checking each sideEffectClass against deriveSideEffect(slugToRecipe[slug].method) per D-02), returns the instance. This is the SINGLE source of truth the eval test reuses.
    - buildOrRestore() (async, D-05): reads _getCatalog(), builds the slug->recipe map from cat.recipes, computes a catalogVersion stamp (count + version/hash; a content hash is fine per Assumption A5); tries chrome.storage.local.get(STORAGE_KEY) and MiniSearch.loadJSON(snap.index, INDEX_OPTIONS) when snap.catalogVersion matches, ELSE buildIndex(...) + chrome.storage.local.set({ [STORAGE_KEY]: { catalogVersion, index: ms.toJSON() } }). Never rebuild on every wake when the snapshot matches.
    - search(query, ownedOrigin, topN) (D-08, D-11): ms.search(query, { combineWith:'OR', prefix:true, fuzzy:0.2, boost:{ intentSynonyms:3, description:1 }, boostDocument:(id,term,stored)=> (stored && stored.service && ownedService && stored.service.indexOf(ownedService)!==-1) ? ORIGIN_BOOST : 1 }), clamp to Math.max(1, Math.min(Number(topN)||5, 5)), map each hit to { slug, service, sideEffectClass, description, score, params: (slugToRecipe[slug]||{}).params || null }. Resolve ownedService from new URL(ownedOrigin).host in try/catch. Per Open Question 1, confirm the boostDocument arg order; if it differs, fall back to a post-search re-rank by service.
    - getRecipeBySlug(slug): return slugToRecipe[slug] || null (invoke lookup, used by Plan 03).

    Keep the file FREE of the literal strings eval-open-paren, new-Function, import-open-paren — even in comments (the guard scans comments). In the SAME edit, add 'extension/utils/capability-search.js', to RECIPE_PATH_ALLOWLIST in scripts/verify-recipe-path-guard.mjs (Pitfall 5 — Check 4 fails closed on any on-disk capability-*.js absent from the allowlist).
  </action>
  <verify>
    <automated>node scripts/verify-recipe-path-guard.mjs && node -e "const S=require('./extension/utils/capability-search.js'),a=require('assert');a(typeof S.buildIndex==='function'&&typeof S.search==='function'&&typeof S.getRecipeBySlug==='function'&&typeof S.deriveSideEffect==='function');a(S.deriveSideEffect('DELETE')==='destructive'&&S.deriveSideEffect('POST')==='mutate'&&S.deriveSideEffect('GET')==='read');a(S.INDEX_OPTIONS&&S.INDEX_OPTIONS.idField==='slug');console.log('capability-search.js exports + guard OK')"</automated>
  </verify>
  <acceptance_criteria>
    - extension/utils/capability-search.js exists, contains global.FsbCapabilitySearch, the module.exports guard, and a module-level INDEX_OPTIONS constant reused at both new MiniSearch(INDEX_OPTIONS) and MiniSearch.loadJSON(..., INDEX_OPTIONS).
    - The allowlist entry is present: running `grep -c "capability-search.js" scripts/verify-recipe-path-guard.mjs` returns >= 1.
    - `node scripts/verify-recipe-path-guard.mjs` exits 0 (new file on the allowlist, no forbidden substrings).
    - `grep -E "eval\(|new Function|import\(" extension/utils/capability-search.js` returns nothing.
    - deriveSideEffect returns destructive/mutate/read for DELETE/POST/GET; INDEX_OPTIONS is exported with idField 'slug'.
    - The export surface includes { buildOrRestore, buildIndex, search, getRecipeBySlug, deriveSideEffect, INDEX_OPTIONS }.
  </acceptance_criteria>
  <done>capability-search.js exists with the load-bearing INDEX_OPTIONS reuse, the full export surface, is eval-free, and is registered on the recipe-path allowlist (guard green).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write the eval-harness gate (capability-search-eval.test.js) and wire it into the test chain</name>
  <read_first>
    - tests/capability-interpreter.test.js (lines 40-60 — the zero-framework convention: require()s, let passed/failed, function check, process.exit(failed>0?1:0))
    - .planning/phases/28-lean-mcp-surface-capability-search-eval-harness/28-RESEARCH.md (lines 564-585 — the eval fixture + gate sketch; lines 240-246 — recall@k / wrong-invoke definitions)
    - .planning/phases/28-lean-mcp-surface-capability-search-eval-harness/28-VALIDATION.md (Per-Task Verification Map rows SURF-01/04/06; the gate thresholds)
    - extension/utils/capability-search.js (the INDEX_OPTIONS + buildIndex helper this test reuses — created in Task 2)
    - package.json (line 17 — the test chain ENDS with node tests/capability-fetch.test.js)
  </read_first>
  <behavior>
    - recall@5 (SURF-06): fraction of intent-cases.json fixtures whose expectedSlug appears in the top-5 hit slugs. Gate: recall@5 >= 0.9.
    - wrong-invoke (SURF-06, D-13): fraction of fixtures whose top-1 hit slug != expectedSlug. Gate: wrong-invoke === 0 (non-negotiable).
    - round-trip (SURF-04): toJSON() -> MiniSearch.loadJSON(json, INDEX_OPTIONS) reproduces identical top hits for a sample query; loadJSON without the options arg throws.
    - schema-on-hit + cap (SURF-01): every hit carries params (or null) + sideEffectClass; no query returns more than 5 hits.
  </behavior>
  <action>
    Create tests/capability-search-eval.test.js following the zero-framework FSB convention (clone tests/capability-interpreter.test.js:40-60: const assert = require('assert'), counters, process.exit(failed>0?1:0)). Import MiniSearch via require('../extension/lib/minisearch.min.js') (UMD require works in Node). Import { INDEX_OPTIONS, buildIndex, search } from require('../extension/utils/capability-search.js') so the test uses the SAME options as the runtime module (prevents the silent options-drift failure mode). Import the seed: require('../catalog/descriptors/_fixtures/seed-descriptors.json'), seed-recipes.json, intent-cases.json.

    Build the slug->recipe map from seed-recipes.json (keyed by id), build the index via the module's buildIndex(seedDescriptors, slugToRecipe). Loop the fixtures: run ms.search(intent, { combineWith:'OR', prefix:true, fuzzy:0.2, boost:{ intentSynonyms:3 } }); top5 = hits.slice(0,5).map(h=>h.id); increment hit when top5.includes(f.expectedSlug); increment wrongInvoke when hits[0] && hits[0].id !== f.expectedSlug. Compute recall = hit/FIXTURES.length, wrongRate = wrongInvoke/FIXTURES.length. console.log both to 3 decimals in a line containing the literal substrings "recall@5=" and "wrong-invoke=". Then assert(recall >= 0.9, ...) AND assert(wrongRate === 0, ...) (D-13). Add a round-trip assertion (toJSON -> loadJSON(json, INDEX_OPTIONS) -> identical sample-query hit slugs; and assert loadJSON(json) without options throws). Add a schema-on-hit + cap assertion via the module's search(...) (every hit has a params field present and sideEffectClass; length <= 5). process.exit non-zero if any assertion fails.

    Append " && node tests/capability-search-eval.test.js" to the package.json test script IMMEDIATELY after the existing trailing "node tests/capability-fetch.test.js". Do NOT add the surface test entry here (that file does not exist yet — Plan 04 adds it; adding it now would break this plan's own npm test).

    If the seeded index trivially hits recall@5 == 1.0 with wrong-invoke == 0 on the FIRST run (defeating the gate's purpose per Open Question 2), tighten the seed in Task 1's fixtures (add more near-neighbor intents a naive index would mis-rank) until the gate is a genuine bar that a naive single-field index would fail — then confirm the tuned INDEX_OPTIONS + boosts pass it.
  </action>
  <verify>
    <automated>node tests/capability-search-eval.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/capability-search-eval.test.js` exits 0 and prints a line containing "recall@5=" and "wrong-invoke=".
    - The test requires ../extension/utils/capability-search.js (single source of truth for INDEX_OPTIONS): `grep -c "capability-search" tests/capability-search-eval.test.js` returns >= 1.
    - The test asserts BOTH recall >= 0.9 AND wrongRate === 0: `grep -c "0.9" tests/capability-search-eval.test.js` >= 1 and the file references a === 0 comparison on the wrong-invoke metric.
    - The test asserts the loadJSON round-trip and that a missing options arg throws.
    - `grep -c "capability-search-eval.test.js" package.json` returns >= 1 AND `grep -c "capability-mcp-surface.test.js" package.json` returns 0 (surface test not yet referenced).
  </acceptance_criteria>
  <done>The eval gate runs deterministically, reuses the module's INDEX_OPTIONS, asserts recall@5>=0.9 AND wrong-invoke=0 plus the round-trip, and is wired into npm test after capability-fetch.test.js.</done>
</task>

<task type="auto">
  <name>Task 4: Ship the catalog into the extension package (build-time generated IIFE) and load it at SW startup</name>
  <read_first>
    - scripts/package-extension.mjs (lines 4-11 imports + ROOT/EXT_ROOT/DIST_DIR/fail helpers; 41-67 the excludes + zip-of-extension-only flow — the D-16 gap)
    - extension/background.js (lines 119-143 the importScripts order block; minisearch.min.js at :120, capability-fetch.js at :143 — the additive insertion point)
    - .planning/phases/28-lean-mcp-surface-capability-search-eval-harness/28-PATTERNS.md (section "scripts/package-extension.mjs" — the generated-IIFE recommendation + naming caution; section "extension/background.js" — the load-order + buildOrRestore call)
    - .planning/phases/28-lean-mcp-surface-capability-search-eval-harness/28-RESEARCH.md (Pitfall 4 — catalog absent from package; lines 174, 188, 308 — the generated recipe-index.generated.js IIFE)
    - extension/utils/capability-search.js (consumes the FsbRecipeIndex global via _getCatalog — created in Task 2)
  </read_first>
  <action>
    Add the catalog-ship step to scripts/package-extension.mjs (D-16; chosen mechanism = the build-time generated dual-export IIFE per RESEARCH Primary recommendation — MV3-cold-start-safe, no SW fetch race). Add writeFileSync to the existing node:fs import. BEFORE the zip step (:56): read every catalog/recipes/*.json and catalog/descriptors/*.json (EXCLUDING the _fixtures/ subdirectories — those are test-only), assemble { recipes: [...], descriptors: [...] }, and write extension/catalog/recipe-index.generated.js as a dual-export IIFE that sets global.FsbRecipeIndex = DATA and the module.exports guard, with the data inlined as a JSON literal (mkdirSync the extension/catalog/ dir first if absent). The generated file lives under extension/, so the existing zip-of-EXT_ROOT step ships it automatically — no new zip arg needed. Keep the generated source free of the forbidden dynamic-code substrings (defensive; it is pure data and is NOT under extension/utils/, so it does not trip the Check-4 glob).

    In extension/background.js, ADDITIVELY (do not reorder existing lines — background.js is a byte-frozen esbuild input) insert AFTER capability-fetch.js at :143, each wrapped in try/catch matching the surrounding style: importScripts('catalog/recipe-index.generated.js') (the FsbRecipeIndex global) THEN importScripts('utils/capability-search.js') (which reads that global). Load order is load-bearing: both minisearch.min.js (:120) and recipe-index.generated.js must precede capability-search.js. After the import block, fire FsbCapabilitySearch.buildOrRestore() at SW startup (it is async — call it without blocking; tolerate absence with a typeof guard so a missing module never throws at boot).
  </action>
  <verify>
    <automated>node scripts/package-extension.mjs && node -e "const fs=require('fs'),a=require('assert');a(fs.existsSync('extension/catalog/recipe-index.generated.js'));const R=require('./extension/catalog/recipe-index.generated.js');a(Array.isArray(R.descriptors)&&R.descriptors.length>=1);a(Array.isArray(R.recipes)&&R.recipes.length>=1);console.log('catalog ships:',R.recipes.length,'recipes',R.descriptors.length,'descriptors')"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/package-extension.mjs imports writeFileSync and generates extension/catalog/recipe-index.generated.js before the zip step.
    - `node scripts/package-extension.mjs` exits 0 and the generated file exports non-empty recipes AND descriptors arrays (the verify node -e exits 0). The _fixtures/ descriptors are NOT included (only the real shipped descriptor).
    - extension/catalog/recipe-index.generated.js contains global.FsbRecipeIndex and is free of eval/new Function/import dynamic-code substrings.
    - extension/background.js references both: `grep -c "recipe-index.generated.js" extension/background.js` >= 1 AND `grep -c "utils/capability-search.js" extension/background.js` >= 1; both inserted AFTER the capability-fetch.js line; buildOrRestore() is invoked at startup.
    - No existing background.js line was reordered (only additive try/catch importScripts lines + the buildOrRestore call were inserted).
  </acceptance_criteria>
  <done>The catalog ships into the package via a generated FsbRecipeIndex IIFE, background.js loads it then capability-search.js then fires buildOrRestore(), and a packaged build contains non-empty recipe+descriptor data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalog descriptor data -> minisearch index | Untrusted-shaped descriptor/recipe JSON crosses into the in-memory index and the chrome.storage.local snapshot |
| chrome.storage.local snapshot -> live index | A persisted snapshot from a prior catalog version crosses back into the serving index on SW wake |
| disk capability-*.js -> recipe-path CI guard | A new on-disk capability module crosses the Wall-1 no-remotely-hosted-code boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04 | Tampering (integrity) | chrome.storage.local['fsbCapabilityIndex'] snapshot | mitigate | catalogVersion stamp on the snapshot (D-05); buildOrRestore() rebuilds + re-snapshots on mismatch so a stale snapshot never serves a deleted/renamed capability (Task 2, Task 4) |
| T-28-04b | Tampering / EoP | descriptor JSON -> minisearch | mitigate | Descriptors are pure data into minisearch (no eval); capability-search.js is added to RECIPE_PATH_ALLOWLIST and kept free of eval/new Function/import (Task 2); the recipe schema stays the Phase-26 closed vocab (untouched) |
| T-28-06 | Tampering (high-consequence auto-fire) | search ranking feeding invoke | mitigate | sideEffectClass surfaced in every hit (D-03, Task 2 search()); the eval gate enforces wrong-invoke = 0 over near-neighbor fixtures (Task 3) so a wrong-but-plausible destructive top-1 fails the build |
| T-28-SC | Tampering | npm/pip/cargo installs | accept | Phase 28 introduces ZERO new external packages (RESEARCH Package Legitimacy Audit: minisearch/jmespath/@cfworker vendored + slop-audited in Phase 26). No install task exists in this plan, so no per-install legitimacy checkpoint is required |
</threat_model>

<verification>
Per-task commit: `node tests/capability-search-eval.test.js`
Plan close: `node scripts/verify-recipe-path-guard.mjs && node tests/capability-search-eval.test.js && node scripts/package-extension.mjs`
The eval gate (recall@5 >= 0.9 AND wrong-invoke = 0) is the milestone gate; it must be green before the plan is considered done.
</verification>

<success_criteria>
- capability-search.js exists with INDEX_OPTIONS reused at construction + loadJSON, full export surface, eval-free, on the recipe-path allowlist (guard green).
- The seeded descriptor + fixture set stresses ranking with near-neighbors (read/mutate/destructive contrast; >=30 intent cases).
- `node tests/capability-search-eval.test.js` exits 0: recall@5 >= 0.9 AND wrong-invoke = 0, plus the toJSON/loadJSON round-trip.
- The catalog ships: a packaged build produces extension/catalog/recipe-index.generated.js with non-empty recipes + descriptors; background.js loads it then capability-search.js then fires buildOrRestore().
- `package.json` test chain runs the eval test (and does NOT yet reference the surface test).
</success_criteria>

<output>
Create `.planning/phases/28-lean-mcp-surface-capability-search-eval-harness/28-01-SUMMARY.md` when done
</output>
