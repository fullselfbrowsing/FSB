---
phase: 28-lean-mcp-surface-capability-search-eval-harness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mcp/src/tools/capabilities.ts
  - mcp/src/runtime.ts
  - mcp/src/queue.ts
autonomous: true
requirements: [SURF-03, SURF-05, SURF-01, SURF-02]
user_setup: []

must_haves:
  truths:
    - "search_capabilities and invoke_capability are registered via server.tool() in a new capabilities.ts, called from runtime.ts, and NEVER added to TOOL_REGISTRY (INV-01)"
    - "search_capabilities bridges to mcp:capabilities-search and bypasses the mutation queue because its name is in the readOnlyTools Set; invoke_capability bridges to mcp:capabilities-invoke and is serialized through queue.enqueue"
    - "search_capabilities exposes { query, origin?, topN? }; invoke_capability exposes a generic { slug, params?, tab_id? } (per-recipe param validation is SW-side)"
    - "The built MCP module compiles cleanly and the existing tool-definitions-parity registry hash is unchanged"
    - "Realizes CONTEXT decisions D-09 (capabilities.ts registerCapabilityTools called from runtime.ts), D-10 (search zod {query,origin?,topN?} joins readOnlyTools while generic invoke {slug,params?,tab_id?} is queue-serialized), D-15 (additive out-of-registry tools — no fsb-mcp-server version bump, parity hash unchanged)"
  artifacts:
    - path: "mcp/src/tools/capabilities.ts"
      provides: "registerCapabilityTools — two out-of-registry server.tool() calls + the read-only/queued split"
      contains: "registerCapabilityTools"
      min_lines: 50
    - path: "mcp/src/queue.ts"
      provides: "search_capabilities added to the readOnlyTools Set"
      contains: "search_capabilities"
    - path: "mcp/src/runtime.ts"
      provides: "registerCapabilityTools call alongside the other register*Tools"
      contains: "registerCapabilityTools"
  key_links:
    - from: "mcp/src/runtime.ts"
      to: "mcp/src/tools/capabilities.ts"
      via: "registerCapabilityTools(server, bridge, queue, agentScope) after registerVaultTools"
      pattern: "registerCapabilityTools"
    - from: "mcp/src/tools/capabilities.ts"
      to: "extension/ws/mcp-tool-dispatcher.js"
      via: "bridge messages mcp:capabilities-search (read-only) and mcp:capabilities-invoke (queued)"
      pattern: "mcp:capabilities"
    - from: "mcp/src/tools/capabilities.ts"
      to: "mcp/src/queue.ts"
      via: "queue.enqueue('search_capabilities') bypasses; queue.enqueue('invoke_capability') serializes"
      pattern: "queue.enqueue"
---

<objective>
Add the lean two-tool MCP wire surface — search_capabilities (read-only, queue-bypass) and invoke_capability (queued) — registered OUTSIDE TOOL_REGISTRY via server.tool() in a new capabilities.ts, wired into runtime.ts, with search_capabilities joining the readOnlyTools Set in queue.ts. This is the INV-01-preserving seam: the frozen registry hash never moves because TOOL_REGISTRY is untouched.

Purpose: SURF-03 (both tools out-of-registry, ~63 schemas byte-identical, INV-01), SURF-05 (search read-only/queue-bypass vs invoke serialized), and the tool-definition halves of SURF-01 (search shape) and SURF-02 (invoke shape). Honors D-09, D-10.

Output: mcp/src/tools/capabilities.ts (registerCapabilityTools), the runtime.ts registration call, and the queue.ts readOnlyTools entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-lean-mcp-surface-capability-search-eval-harness/28-CONTEXT.md
@.planning/phases/28-lean-mcp-surface-capability-search-eval-harness/28-RESEARCH.md
@.planning/phases/28-lean-mcp-surface-capability-search-eval-harness/28-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the live tree 2026-06-20. Use directly. -->

vault.ts (the out-of-registry precedent):
  - Import block (:1-7): McpServer from '@modelcontextprotocol/sdk/server/mcp.js' (VALUE import, not import type); z from 'zod'; type WebSocketBridge from '../bridge.js'; type TaskQueue from '../queue.js'; AgentScope from '../agent-scope.js'; sendAgentScopedBridgeMessage from '../agent-bridge.js'; mapFSBError from '../errors.js'.
  - SECURITY doc-comment (:16): "registered directly (not via TOOL_REGISTRY) to maintain an explicit security boundary."
  - Signature (:20): export function registerVaultTools(server, bridge, queue, agentScope): void
  - Queued/serialized body (fill_credential :55-80): if (!bridge.isConnected) return mapFSBError({success:false,error:'extension_not_connected'}); return queue.enqueue('fill_credential', async () => { const targetTabId = typeof tab_id === 'number' ? tab_id : null; const payload = { domain }; if (tab_id !== undefined) payload.tab_id = tab_id; const result = await sendAgentScopedBridgeMessage(bridge, agentScope, 'mcp:fill-credential', payload, { timeout: 15_000, targetTabId }); return mapFSBError(result); });
  - use_payment_method (:105-125) uses timeout 125_000 — the longer-timeout precedent for a real round-trip.

observability.ts search_memory (:84-111) — the read-only schema-on-hit precedent: server.tool('search_memory', '...ranked by relevance...', { query: z.string(), domain: z.string().optional(), type: z.enum([...]).optional(), topN: z.coerce.number().int().positive().finite().optional() }, async ({...}) => { if (!bridge.isConnected) return mapFSBError({...}); return queue.enqueue('search_memory', async () => { const result = await bridge.sendAndWait({ type:'mcp:search-memory', payload:{ query, filters, options } }, { timeout: 5_000 }); return mapFSBError(result); }); }). search_memory STILL calls queue.enqueue even though it bypasses — Set membership decides bypass-vs-serialize.

queue.ts readOnlyTools Set (:30-45) + bypass (:51-54): the Set literal is [...registryReadOnly, then a "Non-registry read-only tools" block ending 'search_memory','get_memory_stats','list_agents','get_agent_stats','get_agent_history']. enqueue(toolName, fn): if (this.readOnlyTools.has(toolName)) return fn(); else serialize via the queue.

runtime.ts (:6-13 imports, :36-43 calls): registerVaultTools(server, bridge, queue, agentScope) at :42; registerAutopilotTools at :43. All register*Tools share the (server, bridge, queue, agentScope) signature.

errors.ts mapFSBError surfaces RECIPE_.+ verbatim (:137). No errors.ts edit needed in this plan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create capabilities.ts with the two out-of-registry tools and add search_capabilities to the readOnlyTools Set</name>
  <read_first>
    - mcp/src/tools/vault.ts (lines 1-7 imports, 12-25 the SECURITY doc-comment + signature, 55-80 the fill_credential queued/serialized body, 105-125 use_payment_method longer-timeout precedent)
    - mcp/src/tools/observability.ts (lines 84-111 the search_memory read-only schema-on-hit body — the search_capabilities model)
    - mcp/src/queue.ts (lines 30-54 — the readOnlyTools Set + the enqueue bypass; add 'search_capabilities' here)
    - .planning/phases/28-lean-mcp-surface-capability-search-eval-harness/28-PATTERNS.md (section "mcp/src/tools/capabilities.ts" — imports/signature/two bodies; section "mcp/src/queue.ts" — the single-line Set add)
    - .planning/phases/28-lean-mcp-surface-capability-search-eval-harness/28-RESEARCH.md (lines 322-383 — the verified capabilities.ts reference draft)
  </read_first>
  <action>
    Create mcp/src/tools/capabilities.ts modeled byte-for-byte on vault.ts (imports) + observability.ts (the read-only body). Copy the vault.ts import block verbatim (:1-7) — vault.ts imports McpServer as a VALUE (import { McpServer }), not import type; match that. Add a SECURITY doc-comment stating the tools are "registered directly (not via TOOL_REGISTRY) to maintain an explicit security boundary" (the INV-01 intent marker). Export function registerCapabilityTools(server: McpServer, bridge: WebSocketBridge, queue: TaskQueue, agentScope: AgentScope): void.

    search_capabilities (SURF-01, SURF-05; model on search_memory):
    - zod shape (D-10): { query: z.string().describe('Natural-language intent...'), origin: z.string().optional().describe('Optional origin override; omit to use the active tab origin'), topN: z.coerce.number().int().positive().finite().optional().describe('Max results (default 5, max 5)') }.
    - Description states it returns up to topN ranked capabilities each with slug, service, side-effect class (read/mutate/destructive), one-line description, AND the params JSON-Schema (schema-on-hit), biased toward the active tab origin.
    - Body: if (!bridge.isConnected) return mapFSBError({ success:false, error:'extension_not_connected' }); then return queue.enqueue('search_capabilities', async () => { const result = await bridge.sendAndWait({ type:'mcp:capabilities-search', payload:{ query, origin, topN } }, { timeout: 5_000 }); return mapFSBError(result); }). The literal tool-name string MUST be 'search_capabilities' (matched against the readOnlyTools Set).

    invoke_capability (SURF-02, SURF-05; model on fill_credential):
    - GENERIC zod shape (D-10 — a static schema cannot express per-recipe params; validation is SW-side): { slug: z.string().describe('Capability slug from a search_capabilities hit'), params: z.record(z.any()).optional().describe('Parameters matching the hit params JSON-Schema'), tab_id: z.coerce.number().int().positive().finite().optional().describe('Optional tab id; omit to use the active/owned tab') }.
    - Description states it executes the service real web API in the user authenticated session and that mutating capabilities perform real side effects.
    - Body: if (!bridge.isConnected) return mapFSBError({...}); then return queue.enqueue('invoke_capability', async () => { const targetTabId = typeof tab_id === 'number' ? tab_id : null; const payload: Record<string, unknown> = { slug, params: params || {} }; if (tab_id !== undefined) payload.tab_id = tab_id; const result = await sendAgentScopedBridgeMessage(bridge, agentScope, 'mcp:capabilities-invoke', payload, { timeout: 30_000, targetTabId }); return mapFSBError(result); }). 'invoke_capability' MUST NOT be added to the readOnlyTools Set (it serializes). Use a 30_000 ms timeout.

    In mcp/src/queue.ts, add the single line 'search_capabilities', to the "Non-registry read-only tools" block of the readOnlyTools Set (e.g. right after 'search_memory',). Do NOT add 'invoke_capability'. This Set is the ENTIRE read-only/queued split (SURF-05).

    Do NOT touch TOOL_REGISTRY, tool-definitions.js, or tool-definitions.cjs — adding the tools there would move EXPECTED_NON_TRIGGER_REGISTRY_HASH and red tool-definitions-parity.test.js AND force autopilot exposure (Pitfall 2 / Anti-Pattern 1).
  </action>
  <verify>
    <automated>grep -c "registerCapabilityTools" mcp/src/tools/capabilities.ts</automated>
  </verify>
  <acceptance_criteria>
    - mcp/src/tools/capabilities.ts exists and exports registerCapabilityTools(server, bridge, queue, agentScope).
    - The file contains a SECURITY doc-comment referencing "not via TOOL_REGISTRY".
    - Both tool names are present (`grep -c "'search_capabilities'" mcp/src/tools/capabilities.ts` >= 1 and `grep -c "'invoke_capability'" mcp/src/tools/capabilities.ts` >= 1).
    - Both bridge message types are present (`grep -c "mcp:capabilities-search" mcp/src/tools/capabilities.ts` >= 1 and `grep -c "mcp:capabilities-invoke" mcp/src/tools/capabilities.ts` >= 1).
    - search_capabilities body wraps in queue.enqueue('search_capabilities', ...); invoke_capability body wraps in queue.enqueue('invoke_capability', ...) and uses sendAgentScopedBridgeMessage.
    - mcp/src/queue.ts contains 'search_capabilities' in the readOnlyTools Set (`grep -c "search_capabilities" mcp/src/queue.ts` >= 1) and does NOT contain 'invoke_capability' (`grep -c "invoke_capability" mcp/src/queue.ts` === 0).
    - TOOL_REGISTRY, tool-definitions.js, tool-definitions.cjs are untouched.
  </acceptance_criteria>
  <done>capabilities.ts registers both tools out-of-registry with the correct shapes, bridge messages, and queue split; search_capabilities is in the readOnlyTools Set and invoke_capability is not.</done>
</task>

<task type="auto">
  <name>Task 2: Wire registerCapabilityTools into runtime.ts and verify the build + INV-01 hash</name>
  <read_first>
    - mcp/src/runtime.ts (lines 6-13 the import block, 36-43 the register*Tools call block — add the import + call here)
    - .planning/phases/28-lean-mcp-surface-capability-search-eval-harness/28-PATTERNS.md (section "mcp/src/runtime.ts" — the import + call-site one-liner)
    - tests/tool-definitions-parity.test.js (lines 52, 132-136 — the frozen EXPECTED_NON_TRIGGER_REGISTRY_HASH the build must NOT move)
    - mcp/src/tools/capabilities.ts (created in Task 1 — the registerCapabilityTools export)
  </read_first>
  <action>
    In mcp/src/runtime.ts add import { registerCapabilityTools } from './tools/capabilities.js'; to the import block (alongside the other ./tools/*.js imports), and add registerCapabilityTools(server, bridge, queue, agentScope); to the register*Tools call block immediately after registerVaultTools(server, bridge, queue, agentScope); (order is irrelevant for out-of-registry tools, but keep it adjacent to the vault call for readability). Use the .js extension in the import specifier (the existing imports use .js — TS NodeNext module resolution).

    Build the MCP module (npm --prefix mcp run build) to confirm capabilities.ts compiles into the build output and the registration is reachable. The build must produce no TypeScript errors. Confirm the existing tool-definitions-parity test still passes — the registry hash MUST be unchanged because TOOL_REGISTRY was never touched (this is the INV-01 proof; the dedicated surface test that re-asserts both tools-on-wire + hash-unchanged is Plan 04).
  </action>
  <verify>
    <automated>npm --prefix mcp run build && node tests/tool-definitions-parity.test.js</automated>
  </verify>
  <acceptance_criteria>
    - mcp/src/runtime.ts imports registerCapabilityTools and calls it with (server, bridge, queue, agentScope) — `grep -c "registerCapabilityTools" mcp/src/runtime.ts` >= 2 (one import, one call).
    - `npm --prefix mcp run build` exits 0 (capabilities.ts compiles; no TS errors).
    - `node tests/tool-definitions-parity.test.js` exits 0 — EXPECTED_NON_TRIGGER_REGISTRY_HASH unchanged (INV-01 held).
    - The built MCP output (mcp/build/...) includes the compiled capabilities module.
  </acceptance_criteria>
  <done>runtime.ts registers the capability tools, the MCP module builds cleanly, and the INV-01 registry hash is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client (model) -> MCP server tool | The model supplies query/slug/params/origin/tab_id across the tool input boundary |
| MCP server tool -> WebSocket bridge | Tool payloads cross the bridge into the extension SW (search read-only, invoke queued) |
| new tools -> TOOL_REGISTRY (NOT crossed) | The INV-01 boundary: the two tools deliberately do NOT enter the registry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-03 | Tampering (schema-lock) | TOOL_REGISTRY / tool-definitions parity hash | mitigate | Both tools register ONLY via server.tool() out-of-registry (vault precedent); TOOL_REGISTRY untouched so EXPECTED_NON_TRIGGER_REGISTRY_HASH never moves; Task 2 re-runs tool-definitions-parity.test.js as the gate (INV-01) |
| T-28-05 | Tampering / DoS (queue) | mutation queue serialization | mitigate | invoke_capability is serialized via queue.enqueue and is NOT in readOnlyTools; only search_capabilities (read-only) bypasses — a mutating invoke can never race ahead of an in-flight mutation (D-10) |
| T-28-01a | Spoofing | model-supplied origin param | mitigate | The origin param is documented as a non-authoritative OVERRIDE only; the authoritative owned-tab origin is resolved SW-side in the dispatcher (Plan 03) — the tool layer just forwards it |
| T-28-SC | Tampering | npm/pip/cargo installs | accept | Phase 28 introduces ZERO new external packages (RESEARCH Package Legitimacy Audit). No install task exists in this plan; no per-install legitimacy checkpoint required |
</threat_model>

<verification>
Per-task commit: `npm --prefix mcp run build`
Plan close: `npm --prefix mcp run build && node tests/tool-definitions-parity.test.js`
INV-01 gate: the registry hash must be unchanged after registering the two out-of-registry tools.
</verification>

<success_criteria>
- capabilities.ts registers search_capabilities + invoke_capability via server.tool() with the D-10 shapes, the correct bridge messages, and the read-only/queued split.
- search_capabilities is in the readOnlyTools Set; invoke_capability is not.
- runtime.ts calls registerCapabilityTools.
- The MCP module builds cleanly and tool-definitions-parity.test.js is green (INV-01 hash unchanged).
- TOOL_REGISTRY / tool-definitions.{js,cjs} are byte-untouched.
</success_criteria>

<output>
Create `.planning/phases/28-lean-mcp-surface-capability-search-eval-harness/28-02-SUMMARY.md` when done
</output>
