---
phase: 21-package-intake-contract-mapping
plan: "02"
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - tests/phantom-stream-exports.test.js
  - .planning/phases/21-package-intake-contract-mapping/21-PACKAGE-SURFACE.md
autonomous: true
requirements: ["PKG-02", "PKG-03"]
user_setup: []
must_haves:
  truths:
    - "All package exports used by later phases are verified from installed code, not README prose."
    - "ESM imports for protocol, capture, renderer, relay, WebSocket transport, and adapters are covered by a Node smoke test."
    - "Any missing surface blocks later migration instead of being papered over."
  artifacts:
    - path: "tests/phantom-stream-exports.test.js"
      provides: "Installed package export and symbol smoke"
      contains: "PACKAGE_EXPORTS"
    - path: ".planning/phases/21-package-intake-contract-mapping/21-PACKAGE-SURFACE.md"
      provides: "Human-readable package surface map"
      contains: "@full-self-browsing/phantom-stream/renderer"
---

<objective>
Verify the installed PhantomStream API surface and record the import paths later migration phases may use.

Purpose: Phase 22-24 must not replace FSB internals based on README assumptions. This plan proves the package export map and runtime symbols in an executable smoke test.
Output: `tests/phantom-stream-exports.test.js` and `21-PACKAGE-SURFACE.md`.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/PHANTOMSTREAM-PIN.md
@.planning/phases/21-package-intake-contract-mapping/21-CONTEXT.md
@.planning/phases/21-package-intake-contract-mapping/21-01-SUMMARY.md
@package.json
@package-lock.json
@tests/phantom-stream-public-package.test.js

<interfaces>
Expected package import paths:
```text
@full-self-browsing/phantom-stream
@full-self-browsing/phantom-stream/protocol
@full-self-browsing/phantom-stream/capture
@full-self-browsing/phantom-stream/renderer
@full-self-browsing/phantom-stream/relay
@full-self-browsing/phantom-stream/transport/websocket
@full-self-browsing/phantom-stream/adapters/extension
@full-self-browsing/phantom-stream/adapters/playwright
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build executable export smoke</name>
  <files>tests/phantom-stream-exports.test.js</files>
  <read_first>
    - tests/phantom-stream-public-package.test.js
    - node_modules/@full-self-browsing/phantom-stream/package.json
  </read_first>
  <action>Create a Node ESM-capable smoke test that dynamically imports every expected package subpath. For each import path, assert the module object is non-empty and that key runtime symbols exist. Include symbols for package root, protocol envelope/messages/remote-control helpers, capture creation/config surface, renderer creation/diff/sanitize surfaces, relay creation/backends/limits, WebSocket transport creation, and extension/playwright adapters. Use flexible symbol groups where package naming is precise from installed code, but do not pass a subpath with zero useful exports.</action>
  <acceptance_criteria>
    - The test imports every expected subpath from the installed package.
    - The test has explicit expected symbol names or symbol groups per subpath.
    - The test fails on missing or empty package surfaces.
  </acceptance_criteria>
  <verify>
    <automated>node tests/phantom-stream-exports.test.js</automated>
  </verify>
  <done>Installed package exports are executable and non-empty.</done>
</task>

<task type="auto">
  <name>Task 2: Check ESM and MV3-consumable constraints</name>
  <files>tests/phantom-stream-exports.test.js</files>
  <read_first>
    - package.json
    - node_modules/@full-self-browsing/phantom-stream/package.json
  </read_first>
  <action>Extend the smoke test to assert package `type: module`, root `exports` includes the expected subpaths, and browser/extension-facing modules do not require CommonJS globals at import time. Do not attempt a full Chrome extension bundle in this phase; the gate is import-time feasibility and export shape.</action>
  <acceptance_criteria>
    - Package metadata says `type: module`.
    - Expected subpaths are present in `exports`.
    - Importing extension, capture, renderer, and transport surfaces in Node does not throw at module evaluation time.
  </acceptance_criteria>
  <verify>
    <automated>node tests/phantom-stream-exports.test.js</automated>
  </verify>
  <done>ESM import feasibility is recorded in the smoke test.</done>
</task>

<task type="auto">
  <name>Task 3: Record package surface map</name>
  <files>.planning/phases/21-package-intake-contract-mapping/21-PACKAGE-SURFACE.md</files>
  <read_first>
    - tests/phantom-stream-exports.test.js
    - node_modules/@full-self-browsing/phantom-stream/package.json
  </read_first>
  <action>Create `21-PACKAGE-SURFACE.md` listing each verified import path, the symbols proven by the smoke test, the likely owning migration phase, and any caveat. Explicitly state that later phases may only use these verified import paths unless they add a new smoke test first.</action>
  <acceptance_criteria>
    - Every expected import path appears in the artifact.
    - The artifact maps package surfaces to Phases 22, 23, and 24.
    - The artifact states whether migration is blocked or unblocked.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "@full-self-browsing/phantom-stream/capture\\|@full-self-browsing/phantom-stream/renderer\\|@full-self-browsing/phantom-stream/relay" .planning/phases/21-package-intake-contract-mapping/21-PACKAGE-SURFACE.md</automated>
  </verify>
  <done>The package surface map exists and matches the smoke test.</done>
</task>

</tasks>

<verification>
Run:

```bash
node tests/phantom-stream-public-package.test.js
node tests/phantom-stream-exports.test.js
git diff --check
```
</verification>

<success_criteria>
- PKG-02 has executable export evidence.
- Missing or incompatible exports would block later migration; if the smoke passes, Phases 22-24 may plan against the verified paths.
- No production stream code imports PhantomStream in this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/21-package-intake-contract-mapping/21-02-SUMMARY.md`.
</output>
