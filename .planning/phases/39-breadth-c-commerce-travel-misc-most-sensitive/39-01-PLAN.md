---
phase: 39-breadth-c-commerce-travel-misc-most-sensitive
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extension/config/service-denylist.json
  - extension/utils/service-denylist.js
  - docs/LEGAL.md
  - scripts/verify-classification-gate.mjs
  - scripts/verify-catalog-crosscheck.mjs
  - catalog/descriptors/_fixtures/batch-unclassified-commerce-origin.fixture.json
  - catalog/descriptors/_fixtures/payment-op-safe-invocable.fixture.json
  - tests/breadth-batch-gate.test.js
  - tests/catalog-crosscheck.test.js
  - tests/payment-op-guard.test.js
  - package.json
autonomous: true
requirements: [BRDTH-01, BRDTH-02, BRDTH-03]

must_haves:
  truths:
    - "Every commerce/travel/misc batch origin Phase 39 will import is classified denied/sensitive/safe in extension/config/service-denylist.json per the payment/money-movement axis BEFORE the import plans (02-06) run -- payment-bearing commerce/travel origins are SENSITIVE (writes posture-B gated, reads under Auto), pure money-movement / catastrophic-checkout apps are DENIED, genuinely read-only commerce browsing is safe; the merge-time classifyGate has zero unclassified commerce/payment origins to abort on"
    - "An UNCLASSIFIED commerce/payment batch origin (the new commerce fixture, a checkout/cart brand host absent from the denylist) makes classifyGate return failures.length>0 naming the origin AND citing the finance/payment axis -- the build aborts fail-closed (the payment screening enforced as a build failure, not a hope)"
    - "The classification-gate heuristic (scripts/verify-classification-gate.mjs sensitivityHeuristic finance/payment axis) is EXTENDED with payment/checkout/charge/cart/place-order tokens so it INDEPENDENTLY backstops the commerce/payment category on host/slug/description alone -- a commerce checkout/cart/place-order op trips the axis even if the explicit classification line were dropped (the 39 backstop, carried from 38-REVIEW MED-01); the tokens are deliberately SPECIFIC (no bare order/orders that would trip a list_orders read)"
    - "The verify-catalog-crosscheck.mjs PAYMENT-OP GUARD FAILS THE BUILD when any payment-bearing op is classified safe-and-API-invocable, and it detects EVERY payment op-name this phase emits via BOTH a payment-VERB set (matched by verbPrefix: buy/book/place/reserve/checkout/pay/purchase/charge/order/request/register) AND a literal op-name set -- so place_order/checkout/complete_booking AND request_ride/buy_now/place_bid/book_stay/book_flight/book_hotel/buy_tickets/register_for_event/create_order are all recognized; proven by a payment-op-safe-invocable fixture the guard rejects AND a per-verb-family verify probe; a DOM-only payment op on a sensitive origin PASSES"
    - "docs/LEGAL.md Categorization Axes records the payment / money-movement classification (commerce/travel payment-bearing -> sensitive; pure money-movement -> denied; read-only browsing -> safe; the triple money-no-movement-under-Auto mitigation: DOM-only + the payment-op CI guard + sensitive/denied origins) defensibly against the three existing axes"
    - "node scripts/verify-classification-gate.mjs + node scripts/verify-catalog-crosscheck.mjs + npm run validate:extension all exit 0 after the denylist + gate extension (the committed corpus + roster still classify or are benign; no existing descriptor regresses)"
  artifacts:
    - path: "extension/config/service-denylist.json"
      provides: "the sensitivity-screening roster EXTENDED for the commerce/travel/misc batch per the payment/money-movement axis: payment-bearing commerce/travel origins classified sensitive (writes posture-B gated); the catastrophic money-movement set classified denied; the DENY-01 set + the Phase-35/38 roster unchanged; opentable explicitly SENSITIVE (held-card reservation, payment-adjacent)"
      contains: "sensitiveOrigins"
    - path: "scripts/verify-classification-gate.mjs"
      provides: "the classifyGate sensitivityHeuristic finance/payment axis WIDENED with payment/checkout/charge/cart/place-order tokens (the 39 host backstop) so the commerce/payment category is independently flagged on host/slug/description -- SPECIFIC tokens only (no bare order/orders)"
      contains: "checkout"
    - path: "scripts/verify-catalog-crosscheck.mjs"
      provides: "a NEW payment-op guard (checkPaymentOpsNotSafeInvocable) that FAILS THE BUILD when a payment-bearing op is safe-and-API-invocable, keying on BOTH a payment-VERB set (via verbPrefix) AND a literal op-name set so all 12 phase-39 payment op-names are detected -- the headline money-no-movement-under-Auto CI assertion"
      contains: "checkPaymentOpsNotSafeInvocable"
    - path: "docs/LEGAL.md"
      provides: "the Categorization-Axes roster EXTENDED to name the payment / money-movement classification + the triple mitigation (DOM-only + the payment-op CI guard + sensitive/denied origins)"
      contains: "Categorization Axes"
    - path: "catalog/descriptors/_fixtures/batch-unclassified-commerce-origin.fixture.json"
      provides: "a commerce/payment batch origin (checkout/cart brand host) that trips the finance/payment sensitivity axis but is NOT classified -> the gate fixture proving the merge aborts fail-closed on an unclassified commerce/payment origin (distinct from the Phase-37 wallet finance fixture + the Phase-38 social fixture)"
      contains: "commerce"
    - path: "catalog/descriptors/_fixtures/payment-op-safe-invocable.fixture.json"
      provides: "a payment-bearing op (e.g. place_order/checkout) declared on a safe origin AND/OR promoted to backing handler -> the fixture the NEW payment-op guard rejects (the build fails on a payment-safe-invocable op)"
      contains: "payment"
    - path: "tests/payment-op-guard.test.js"
      provides: "the payment-op guard proof (via the REAL crossCheck/checkPaymentOpsNotSafeInvocable exports): a payment op on a safe origin OR a payment op promoted to handler/recipe FAILS; a DOM-only payment op on a sensitive origin PASSES; AND each verb family (buy_tickets/book_flight/request_ride/place_bid/create_order) is detected so the 12 op-names are covered"
      contains: "checkPaymentOpsNotSafeInvocable"
    - path: "tests/breadth-batch-gate.test.js"
      provides: "the merge-time batch-gate proof EXTENDED: the new unclassified-commerce fixture aborts the gate citing the finance/payment axis; a screened commerce batch (a sensitive payment origin + a safe read origin) passes classifyGate"
      contains: "batch-unclassified-commerce-origin"
  key_links:
    - from: "extension/config/service-denylist.json sensitiveOrigins/deniedOrigins"
      to: "scripts/verify-classification-gate.mjs classifyGate (via service-denylist.js classify)"
      via: "a screened payment-bearing commerce origin classifies sensitive -> classifyGate continues past it AND the posture-B re-gate fires on its writes; a money-movement origin classifies denied"
      pattern: "sensitiveOrigins|deniedOrigins|classify"
    - from: "scripts/verify-catalog-crosscheck.mjs checkPaymentOpsNotSafeInvocable"
      to: "catalog/descriptors/_fixtures/payment-op-safe-invocable.fixture.json + the committed corpus"
      via: "the guard fails the build when a payment op (by verb-prefix OR op-name) is safe-and-invocable; over the real corpus every payment op is DOM-only on a sensitive/denied origin -> the guard passes"
      pattern: "checkPaymentOpsNotSafeInvocable|place_order|buy_tickets|book_flight"
    - from: "scripts/verify-classification-gate.mjs sensitivityHeuristic"
      to: "tests/breadth-batch-gate.test.js batch-unclassified-commerce-origin.fixture.json"
      via: "the widened finance/payment axis (checkout/cart/place-order tokens) trips the unclassified commerce fixture -> classifyGate aborts naming it + the axis"
      pattern: "finance/payment|checkout|cart"
---

<objective>
Land the PAYMENT-SCREENING MACHINERY that gates the Phase-39 commerce/travel/misc import — this
plan MUST complete BEFORE the import plans (02-06) because the merge-time classifyGate fail-closes
on any unclassified commerce/payment origin, and because the payment-op CI guard must exist before
a payment descriptor lands. This is a DENYLIST + GATE + DOC + PROOF change against the FROZEN
Phase-37/38 importer (NO importer machinery edit). It implements the locked payment-screening
framework from 39-CONTEXT (the most-security-relevant batch):

1. **Classify every batch origin per the payment / money-movement axis (BRDTH-02):** extend
   extension/config/service-denylist.json so each commerce/travel/misc origin Phase 39 imports is
   classified denied/sensitive/safe per the payment axis — payment-bearing commerce/travel origins
   (cart/checkout/order/booking) -> SENSITIVE (writes posture-B re-gated, reads under Auto); pure
   money-movement / catastrophic-checkout apps -> DENIED; genuinely read-only commerce browsing ->
   safe. Conservative default: an unknown commerce/payment app -> sensitive (never safe-writable).

2. **Extend the classification-gate heuristic with payment tokens (the 39 backstop):** widen the
   finance/payment axis in scripts/verify-classification-gate.mjs with payment/checkout/charge/cart/
   place-order tokens so the heuristic INDEPENDENTLY flags the commerce/payment category on host/slug/
   description — the explicit classification line is no longer a single point of failure (carried
   from 38-REVIEW MED-01; the 38 social-axis widening is the precedent). The tokens are SPECIFIC
   (no bare order/orders that would trip a benign list_orders read).

3. **Add the payment-op CI guard (THE headline — no money movement under Auto):** add a guard to
   scripts/verify-catalog-crosscheck.mjs that FAILS THE BUILD if any payment-bearing op is ever
   classified safe-and-API-invocable — a payment op on a non-sensitive/non-denied origin, OR any
   payment op promoted to backing recipe/handler, fails the build. The guard detects a payment op via
   BOTH a payment-VERB set (matched by verbPrefix) AND a literal op-name set, so EVERY one of the 12
   payment op-names this phase emits (place_order, checkout, complete_booking, request_ride, buy_now,
   place_bid, book_stay, book_flight, book_hotel, buy_tickets, register_for_event, create_order) is
   recognized — not just the three that happen to be literal keys. This is the third leg of the triple
   mitigation (the breadth backing:'dom' default + payment origins sensitive/denied are the other
   two). Proven by a payment-op-safe-invocable fixture the guard rejects AND a per-verb-family probe.

4. **DOC the payment axis:** extend docs/LEGAL.md Categorization Axes to name the payment /
   money-movement classification + the triple money-no-movement-under-Auto mitigation.

5. **Prove the fail-closed gate (BRDTH-02):** a NEW unclassified-commerce fixture + an extended
   breadth-batch-gate proof: an unclassified commerce/payment batch origin aborts classifyGate
   citing the finance/payment axis; a screened commerce batch passes.

Purpose: the screening is the SECURITY contract of this most-sensitive batch — a payment op writable
under Auto = MONEY MOVED without consent (place an order, charge a card, book a paid reservation).
The triple mitigation (breadth DOM-only + the payment-op CI guard + payment origins sensitive/denied)
is established HERE before any descriptor lands; the import plans 02-06 rely on it. (BRDTH-03 conservative
DOM-only default is the FROZEN backing:'dom' importer default — no machinery edit needed; tests/dom-only-default.test.js
already proves it and the import plans re-assert it per sub-batch.)
Output: extended denylist roster (commerce origins classified per the payment axis) + the widened
classification heuristic + the NEW payment-op CI guard (verb-set + op-name-set keyed) + the LEGAL payment axis; the
unclassified-commerce gate fixture + extended gate proof; the payment-op-guard proof (incl. per-verb-family coverage); all registered
in CI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-breadth-c-commerce-travel-misc-most-sensitive/39-CONTEXT.md
@.planning/phases/39-breadth-c-commerce-travel-misc-most-sensitive/39-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the live codebase. No exploration required. -->

THE FROZEN MACHINERY (do NOT edit; this plan is denylist + gate + doc + tests only):
- scripts/import-opentabs-catalog.mjs backingFor(_app, _service) RETURNS 'dom' for EVERY data-batch
  app (line ~175). So every commerce/travel/misc app the import plans 02-06 emit is automatically
  backing:'dom' = DOM-only (T3) = the conservative default the CONTEXT requires. NO importer edit.
- scripts/import-opentabs-catalog.mjs runImport() calls classifyGate(gateItemsList) over EVERY
  enumerated batch origin BEFORE writing and THROWS (exit 1) on any unclassified denied/sensitive
  origin. The import plans run this; THIS plan must classify the commerce origins so it does not abort.

extension/config/service-denylist.json (the screening roster -- the file THIS plan edits):
  { "v":1, "deniedOrigins":[...14...], "sensitiveOrigins":[...27...], "deniedReason":"..." }.
  Host-pattern form (LOCKED, matches service-denylist.js loader): 'https://*.<domain>' = apex + any
  subdomain (host===domain OR host endsWith '.'+domain); a pattern with NO '*' = that EXACT origin
  (scheme+host). EXACT-host forms (music.youtube.com NOT *.youtube.com) keep a pattern no broader than
  OpenTabs' own urlPattern host scope (Phase 35 decision). CURRENT roster: deniedOrigins has the DENY-01
  set (robinhood/fidelity/carta + netflix/spotify/twitch/steam/youtube-music/tinder/onlyfans) + banking
  (chase/bofa/wells/irs); sensitiveOrigins has the payments seed (dashboard.stripe.com, *.coinbase.com,
  console.twilio.com, app.ynab.com) + IG/FB/TikTok/X + messaging (slack/discord/teams/whatsapp/telegram)
  + AI-chat/microblog (chatgpt/claude/bsky/mastodon/threads). The commerce/travel/misc origins are NOT
  yet classified (classify() returns {denied:false,sensitive:false} for them today).

service-denylist.js classify(origin) -> { sensitive, denied, reason? }: denied implies sensitive; a
  sensitiveOrigins match -> {sensitive:true,denied:false}; a deniedOrigins match -> {sensitive:true,
  denied:true}; no match -> {sensitive:false,denied:false}. load() reads ../config/service-denylist.json.
  The gate + the consent re-gate both consume classify(); editing the JSON re-classifies an origin.
  The matcher supports ONLY 'https://*.<domain>' (suffix) and exact-host forms -- no other '*' form.

scripts/verify-classification-gate.mjs (the file THIS plan edits, item 2):
  AXES is an array of { axis, re } objects; sensitivityHeuristic(host, slug, description) tests
  host.toLowerCase()+' '+slug+' '+description against each axis RegExp and returns the FIRST match
  { suspect:true, axis, keyword } or { suspect:false }. The finance/payment axis RegExp today is:
    /\b(bank|banking|pay|payment|payments|wallet|invoice|billing|card|stripe|coinbase|crypto|broker|
    brokerage|trade|trading|fund|funds|portfolio|tax|budget|ynab|venmo|paypal|wise|fidelity|robinhood|
    schwab|carta|treasury)\b/i
  The 38-REVIEW MED-01 precedent WIDENED the social/messaging axis with brand+category tokens. Mirror
  it: ADD commerce/payment tokens (checkout, cart, place-order, purchase, charge, charges, basket,
  deposit, withdraw, money-transfer, remittance, cashapp, escrow, ...) to the finance/payment axis RegExp
  -- KEEP the existing \b...\b anchoring (so e.g. 'card' does not match inside a benign substring) and the
  fail-closed policy (widen, never weaken; a benign false-positive is fixed via the safeAllowlist, NEVER
  by removing a token). DO NOT add a bare 'order'/'orders' token (it would trip a benign list_orders read).
  classifyGate(items, opts) is dual-exported + chained into validate:extension.

scripts/verify-catalog-crosscheck.mjs (the file THIS plan edits, item 3):
  crossCheck(descriptors) re-derives sideEffectClass from provenance.signals and FAILS when declared <
  derived (under-stated destructive). checkReadOnlySafeOrigins(descriptors) (the 38 MED-02 guard)
  asserts every descriptor whose service is in READ_ONLY_SAFE_SERVICES is sideEffectClass 'read'. The
  CLI runCli() reads the committed corpus (readCorpusDescriptors, TOP-LEVEL only, excludes _fixtures/)
  and MERGES crossCheck + checkReadOnlySafeOrigins failures -> process.exit(1) on any. ADD a THIRD guard
  checkPaymentOpsNotSafeInvocable(descriptors, classifyFn) (dual-export it) and merge its failures into
  the same set. Each emitted descriptor carries { slug, service, sideEffectClass, backing, params, provenance }.

scripts/lib/side-effect-class.mjs (the SHARED module the guard IMPORTS -- do NOT edit; key facts):
  - verbPrefix(opName) -> the leading verb TOKEN, lowercased; snake_case AND camelCase aware. So
    'place_order' -> 'place', 'buyNow' -> 'buy', 'book_flight' -> 'book', 'request_ride' -> 'request',
    'register_for_event' -> 'register', 'create_order' -> 'create'. THIS is the verb-keying helper the
    payment-op guard MUST use (it splits at the first '_' OR the first inner capital).
  - opNameFromSlug(slug) -> the trailing op-name token: 'shopco.place_order' -> 'place_order'.
  - CRITICAL: WRITE_VERBS does NOT contain buy/book/place/reserve/request/order -- those are NOT write
    verbs in the side-effect lattice (a 'place_order' classes write via {method:'POST'}, NOT via its verb).
    So the payment-op guard CANNOT reuse WRITE_VERBS to find payment ops; it MUST enumerate its OWN
    payment-VERB set. Reuse verbPrefix/opNameFromSlug (the parsing) but define the payment-verb set in
    the guard itself.

extension/utils/capability-router.js _evaluateConsent (FsbConsentGate.evaluate, posture B / DENY-04):
  reads the LIVE global FsbServiceDenylist at call time. For classify().sensitive===true + mode 'auto',
  a WRITE (method in {POST,PUT,PATCH,DELETE} OR a descriptor sideEffectClass 'write'/'mutating'/
  'destructive') WITHOUT the per-origin mutating flag -> { decision:(not 'allow'), error:{code/errorCode/
  error:'RECIPE_CONSENT_MUTATING_REQUIRED'} }; a READ -> 'allow'; setOriginMutating(origin,true) elevates.
  (This plan does NOT edit the router -- it classifies the origins so a payment WRITE flows through this
  existing gate; the end-to-end proof on a REAL emitted payment descriptor is Plan 39-07.)

THE FIXTURE SHAPE (catalog/descriptors/_fixtures/, excluded from the shipped catalog by readJsonDir
  non-recursion): mirror batch-unclassified-origin.fixture.json (the Phase-37 finance fixture) for the
  commerce one, and shape the payment-op-safe-invocable fixture as a descriptor object carrying
  provenance.signals so crossCheck reads it (slug a payment op like 'shopcorp.place_order', a safe service
  host, sideEffectClass write, backing 'handler' OR a safe-origin service -- whichever the guard keys on).

THE TEST CONVENTION (zero-framework, ASCII-only, NO emojis): a passed/failed counter + check(cond,msg)
  + process.exit(failed>0?1:0). breadth-batch-gate.test.js + catalog-crosscheck.test.js are the templates
  to mirror/extend; the ESM gate is loaded from CJS via a dynamic await import().
</interfaces>

# Reuse (do NOT rebuild) -- the frozen Phase-35/37/38 screening machinery:
@scripts/verify-classification-gate.mjs
@scripts/verify-catalog-crosscheck.mjs
@scripts/lib/side-effect-class.mjs
@extension/utils/service-denylist.js
@extension/config/service-denylist.json
@tests/breadth-batch-gate.test.js
@tests/catalog-crosscheck.test.js
@catalog/descriptors/_fixtures/batch-unclassified-origin.fixture.json
@catalog/descriptors/_fixtures/batch-unclassified-social-origin.fixture.json
@docs/LEGAL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Classify the commerce/travel/misc batch origins per the payment axis in service-denylist.json + extend the docs/LEGAL.md Categorization Axes</name>
  <files>extension/config/service-denylist.json, docs/LEGAL.md</files>
  <read_first>
    - extension/config/service-denylist.json (the current 14 denied + 27 sensitive roster + the host-pattern form + the leading _comment recording the Phase-35/38 expansions)
    - extension/utils/service-denylist.js (classify() + the host-pattern matcher: '*.<domain>' suffix vs exact-host -- confirm the supported forms before adding origins)
    - docs/LEGAL.md (the "### Categorization Axes" section: the three axes + the ToS-hostile roster + the Phase-38 comms/social additions to axis 3)
    - .planning/phases/39-breadth-c-commerce-travel-misc-most-sensitive/39-CONTEXT.md (the payment-screening framework: payment-bearing -> sensitive; pure money-movement -> denied; read-only -> safe; conservative default)
    - .planning/research/FEATURES.md (the anti-feature/denylist table: stripe writes / money movement = sensitive Ask-only never Auto; the ToS/payment reasoning)
  </read_first>
  <action>
    Extend extension/config/service-denylist.json to classify the commerce/travel/misc batch origins
    Phase 39 (plans 02-06) will import, per the 39-CONTEXT payment / money-movement axis. Apply the
    payment-conservative-by-default rule: an unknown commerce/payment app defaults to SENSITIVE (never
    safe-writable). Use EXACT-host or apex-suffix patterns mirroring the Phase-35/38 discipline (a
    pattern no broader than the app's own origin).

    Classify per the payment axis (the per-app list is explicit + defensible per the quality gate):

    SENSITIVE tier (add to sensitiveOrigins -- payment/checkout/order-bearing commerce + travel +
    paid-booking; reads run under Auto, WRITES posture-B re-gated by the existing _evaluateConsent):
      - food delivery / rideshare (payment on file, places paid orders / rides):
        "https://www.doordash.com", "https://www.ubereats.com", "https://*.uber.com",
        "https://*.lyft.com", "https://www.grubhub.com", "https://www.instacart.com",
        "https://www.dominos.com", "https://www.chipotle.com"
      - retail / marketplace (checkout, places paid orders):
        "https://www.amazon.com", "https://www.ebay.com", "https://www.etsy.com",
        "https://www.bestbuy.com", "https://www.costco.com", "https://www.walmart.com",
        "https://www.target.com", "https://www.craigslist.org", "https://*.shopify.com"
      - travel / transport (paid bookings, money movement on confirm):
        "https://www.booking.com", "https://www.airbnb.com", "https://www.expedia.com",
        "https://www.kayak.com", "https://www.opentable.com"
      - opentable is EXPLICITLY SENSITIVE: an OpenTable reservation holds a card (a held-card / paid
        reservation is payment-adjacent), so classify "https://www.opentable.com" SENSITIVE
        UNCONDITIONALLY here -- do NOT make it conditional on "only if it has a paid op". Its
        reserve_table op (a held-card reservation) is therefore DOM-only-on-sensitive in 39-04, and the
        import will not abort on it. (Leaving opentable unclassified would abort 39-04's import.)
      - scheduling / local services with paid flows: classify here ONLY the commerce origins whose
        emitted ops include a payment/checkout/order op (besides opentable, which is unconditional above).
        A purely read/availability scheduling app like calendly stays safe -- see the SAFE tier below.

    NOTE: the EXACT host set above is the AUTHORITATIVE classification for the apps NAMED in 39-CONTEXT
    + ARCHITECTURE.md. The import plans 02-06 vendor each app at its OpenTabs urlPatterns host; if a
    vendored host differs from the host classified here (e.g. an apex vs www form), the import plan's
    classifyGate WILL abort -- that is the screen-then-import contract working (the 38-02 threads
    precedent: the vendored origin MUST equal the screened origin). Each import plan re-confirms its
    origins are classified here before importing; if a new commerce app is discovered during vendoring
    that is NOT in this list, the import plan adds it to sensitiveOrigins (payment-bearing) or
    deniedOrigins (money-movement) BEFORE importing -- never imports it unclassified.

    DENIED tier (add to deniedOrigins -- the most catastrophic money-movement / pure-payment apps, like
    DENY-01; never imported even DOM-only): classify as DENIED any vendored commerce app whose PRIMARY
    function is direct money movement with no benign read surface -- e.g. a peer-to-peer money-transfer
    or standalone-checkout/wallet app (paypal "https://www.paypal.com", venmo "https://*.venmo.com",
    cash-app "https://cash.app", wise "https://wise.com", western-union "https://www.westernunion.com")
    IF such an app is in the OpenTabs surface this batch vendors. Do NOT vendor a denied app's
    descriptors (the import plans exclude deniedOrigins, like DENY-01). If none of these standalone
    money-movement apps are in the vendored commerce surface, leave deniedOrigins's existing set
    unchanged + record the reasoning in the SUMMARY.

    SAFE tier (do NOT classify -- genuinely read-only commerce browsing / availability, reads run under
    Auto, NO write op emitted): a pure-availability or read-only app whose vendored ops are all reads --
    e.g. calendly "https://calendly.com" (availability/booking-link reads only), yelp, tripadvisor,
    zillow, grafana (added to READ_ONLY_SAFE_SERVICES by their import plans). Leave them UNCLASSIFIED
    (the heuristic, after Task 2's widening, must NOT trip a pure-read host -- the widened tokens are
    deliberately SPECIFIC, checkout/cart/place-order/charge, NOT a bare 'order'/'book'/'reserve', so a
    calendly availability read or a yelp business search does NOT trip the axis). A safe app is added to
    verify-catalog-crosscheck.mjs READ_ONLY_SAFE_SERVICES ONLY by its import plan, and ONLY when its
    emitted ops are genuinely read-only (the 38 MED-02 guard).

    DO NOT remove or alter any existing deniedOrigins/sensitiveOrigins entry (the DENY-01 set, the
    Phase-35 banking/payments seed, the Phase-38 comms/social additions all stay). Update the leading
    "_comment" to record the Phase-39 commerce/travel/misc extension (payment-bearing commerce/travel
    classified sensitive per the payment axis; opentable sensitive [held-card]; catastrophic
    money-movement denied; read-only browsing safe), mirroring how the comment records the Phase-35/38
    expansions.

    Extend docs/LEGAL.md's "### Categorization Axes" section: under axis 3 (Sensitivity), name the
    Phase-39 commerce/travel/misc additions -- payment-bearing commerce/travel/paid-booking origins
    classified SENSITIVE (writes re-gated, reads under Auto) as the conservative default for this
    category; pure money-movement apps DENIED (axis 1/2 money-harm); read-only commerce browsing safe.
    State the rationale: a payment-bearing op writable under Auto = MONEY MOVED without consent (an order
    placed, a card charged, a paid reservation booked), so the category defaults toward LESS reach. Name
    the TRIPLE mitigation that makes money-movement-under-Auto impossible: (a) every breadth descriptor
    is DOM-only (not API-invocable); (b) a CI guard forbids any payment op being safe-and-API-invocable;
    (c) payment origins are sensitive (posture-B gated) or denied. Keep it consistent with the existing
    axis prose (ASCII, no emojis).
  </action>
  <verify>
    <automated>node -e "const D=require('./extension/utils/service-denylist.js');D.load().then(()=>{const sens=['https://www.doordash.com','https://www.amazon.com','https://www.booking.com','https://www.airbnb.com','https://www.bestbuy.com','https://www.costco.com','https://www.opentable.com'];let ok=true;for(const o of sens){const c=D.classify(o);if(!(c.sensitive===true&&c.denied===false)){console.error('NOT sensitive:',o,JSON.stringify(c));ok=false}}const den=['https://www.netflix.com','https://digital.fidelity.com'];for(const o of den){const c=D.classify(o);if(c.denied!==true){console.error('DENY-01 regressed:',o,JSON.stringify(c));ok=false}}const stillSens=['https://discord.com','https://dashboard.stripe.com','https://chatgpt.com'];for(const o of stillSens){const c=D.classify(o);if(c.sensitive!==true){console.error('prior roster regressed:',o,JSON.stringify(c));ok=false}}if(!ok)process.exit(1);console.log('PASS payment-axis classification: commerce/travel + opentable sensitive; DENY-01 + prior roster intact')})"</automated>
  </verify>
  <acceptance_criteria>
    - classify() returns {sensitive:true, denied:false} for the payment-bearing commerce/travel origins added to sensitiveOrigins (doordash/amazon/booking/airbnb/bestbuy/costco/... per the list above) AND for opentable (held-card reservation -> unconditionally sensitive, NOT conditional).
    - classify() returns denied===true for the DENY-01 set (netflix/fidelity/...) and any newly-denied money-movement app -- nothing regressed.
    - classify() still returns sensitive===true for the Phase-35/38 roster (discord/dashboard.stripe.com/chatgpt) -- no prior classification removed.
    - docs/LEGAL.md Categorization Axes names the Phase-39 payment/money-movement classification + the triple money-no-movement-under-Auto mitigation, defensibly against the three axes.
    - No DENY-01/Phase-35/Phase-38 entry removed; no over-broad '*.' pattern added for an app that ships an exact host; the per-app classification is explicit + defensible.
  </acceptance_criteria>
  <done>Every commerce/travel/misc batch origin is classified per the payment / money-movement axis (payment-bearing -> sensitive incl. opentable held-card, money-movement -> denied, read-only -> safe) and the LEGAL doc records the rationale + the triple mitigation -- the merge-time classifyGate that 02-06 run now has zero unclassified commerce/payment origins to abort on (opentable included, so 39-04 cannot abort on it).</done>
</task>

<task type="auto">
  <name>Task 2: Widen the classification-gate finance/payment heuristic with SPECIFIC payment tokens + add the payment-op CI guard (verb-set + op-name-set keyed) to the crosscheck</name>
  <files>scripts/verify-classification-gate.mjs, scripts/verify-catalog-crosscheck.mjs</files>
  <read_first>
    - scripts/verify-classification-gate.mjs (the AXES array + the finance/payment axis RegExp + the 38-REVIEW MED-01 comment on the social/messaging widening -- the precedent to mirror; the \b anchoring + the fail-closed policy)
    - scripts/verify-catalog-crosscheck.mjs (crossCheck + checkReadOnlySafeOrigins (the 38 MED-02 guard) + runCli's readCorpusDescriptors + the merged failure set + the dual-export idiom)
    - scripts/lib/side-effect-class.mjs (verbPrefix + opNameFromSlug -- reuse to recover a payment op's verb/op-name from the slug; CONFIRM WRITE_VERBS does NOT contain buy/book/place/reserve/request/order, so the guard MUST define its OWN payment-verb set; do NOT edit this module)
    - extension/utils/service-denylist.js (classify() + load() -- the guard consults classify('https://'+service); runCli must await Denylist.load() before calling the guard)
  </read_first>
  <action>
    (a) WIDEN the finance/payment axis in scripts/verify-classification-gate.mjs (the 39 backstop,
    mirroring the 38-REVIEW MED-01 social-axis widening). ADD commerce/payment tokens to the
    finance/payment axis RegExp so a commerce checkout/cart/place-order/charge op trips the axis on host/
    slug/description alone. ADD these SPECIFIC tokens: checkout, cart, basket, place-order, purchase,
    charge, charges, deposit, withdraw, remittance, money-transfer, cashapp, escrow. DO NOT add a bare
    'order' or 'orders' token -- a bare 'order' would trip a benign 'list_orders'/'get_order' READ op
    (a latent false-positive); the compound/specific 'place-order' is added instead so a paid order trips
    while a read does not. KEEP the existing \b...\b word-boundary anchoring (each new token anchored) and
    EVERY existing token. Add a comment block (mirroring the MED-01 comment) stating: this widening makes
    the explicit commerce/payment classification no longer a single point of failure; the tokens are
    deliberately SPECIFIC (checkout/cart/place-order/charge over a bare generic 'buy'/'book'/'order') so a
    benign read app (calendly availability reads, a yelp business search, a list_orders read) is NOT
    false-tripped. The fail-closed policy stands: widen never weaken; a benign false-positive is fixed via
    the safeAllowlist, never by removing a token.

    (b) ADD the payment-op CI guard to scripts/verify-catalog-crosscheck.mjs (item 3, THE headline). Add
    and dual-export checkPaymentOpsNotSafeInvocable(descriptors, classifyFn) -> { failures }. The guard
    MUST detect a payment op via TWO explicit, complementary paths (this is the blocker fix -- the guard
    must cover ALL 12 payment op-names this phase emits, not just the three that are literal keys):

      PATH 1 -- a payment-VERB set matched via verbPrefix(opName) (import verbPrefix from
      scripts/lib/side-effect-class.mjs; recover opName via opNameFromSlug(slug)). ENUMERATE the set
      EXPLICITLY -- these verbs are NOT in WRITE_VERBS, so this set MUST be defined in the guard itself:
        PAYMENT_VERBS = { buy, book, place, reserve, checkout, pay, purchase, charge, order, request, register }
      So verbPrefix('place_order')='place' (HIT), 'buyNow'/'buy_now'='buy' (HIT), 'book_flight'='book' (HIT),
      'book_stay'='book' (HIT), 'book_hotel'='book' (HIT), 'buy_tickets'='buy' (HIT), 'request_ride'='request'
      (HIT), 'place_bid'='place' (HIT), 'register_for_event'='register' (HIT), 'create_order'='create' --
      NOTE 'create' is NOT a payment verb, so create_order is caught by PATH 2 (op-name set) below.

      PATH 2 -- a literal PAYMENT_OP op-name set (matched against opNameFromSlug(slug)) for the op-names
      whose VERB is benign but whose FULL op-name moves money:
        PAYMENT_OP_NAMES = { place_order, checkout, checkout_cart, complete_booking, submit_payment,
          add_payment_method, confirm_order, create_order, buy_now, buy_tickets, place_bid, book_stay,
          book_flight, book_hotel, register_for_event, request_ride, purchase_tickets }
      (create_order is the key case PATH 1 misses -- its verb 'create' is benign -- so it MUST be in this
      op-name set.)

      An op is a PAYMENT op if EITHER PATH 1 (verbPrefix in PAYMENT_VERBS) OR PATH 2 (opNameFromSlug in
      PAYMENT_OP_NAMES) matches.

      For each descriptor whose op is a payment op: it is SAFE-AND-INVOCABLE (a FAILURE) when EITHER
      (i) its backing is 'recipe' or 'handler' (promoted to API-invocable -- a payment op must NEVER be
      invocable from breadth), OR (ii) classifyFn('https://'+service) returns NOT sensitive AND NOT denied
      (the origin is safe -> a payment op on a safe origin would run under Auto). Push a failure naming the
      slug + service + backing + the matched path (verb vs op-name) + the reason ("payment op X on a safe
      origin" / "payment op X promoted to backing handler") + the fix ("classify <service> sensitive/denied
      OR do not emit this payment op OR keep it backing:'dom' on a sensitive origin"). A payment op that is
      backing:'dom' on a sensitive/denied origin PASSES (DOM-only on a gated origin = no money movement
      under Auto).

      KEEP THE GUARD SCOPED: it fails ONLY a payment op on a safe-AND-API-invocable origin. A DOM-only
      payment op on a SENSITIVE origin passes (e.g. reserve_table on opentable -- opentable is sensitive
      per Task 1, so reserve_table DOM-only-on-sensitive PASSES). The READ-only-safe apps
      (calendly/zillow/yelp/tripadvisor/grafana) emit ONLY read ops (search/list/get) -- none of those
      verbs/op-names is in PAYMENT_VERBS/PAYMENT_OP_NAMES -- so NONE of their ops is a payment op and the
      guard never keys on them (confirm: list/get/search/query are not payment verbs).

      In runCli(): require service-denylist.js + `await Denylist.load()` (the gate is build-time, so an
      await is fine), then call checkPaymentOpsNotSafeInvocable(corpus, Denylist.classify) and MERGE its
      failures into the existing allFailures set (alongside crossCheck + checkReadOnlySafeOrigins) before
      the process.exit(1) decision. Update the PASS summary line to mention the payment-op guard.
    Keep crossCheck + checkReadOnlySafeOrigins UNCHANGED. Do NOT edit side-effect-class.mjs or the importer.
  </action>
  <verify>
    <automated>node -e "(async()=>{const g=await import('./scripts/verify-classification-gate.mjs');const probes=[['https://shop.example','x.checkout_cart','complete checkout and charge the card'],['https://store.example','x.place_order','place a paid order']];let ok=true;for(const[host,slug,desc]of probes){const v=g.sensitivityHeuristic(new URL(host).hostname,slug,desc);if(!(v.suspect===true&&v.axis==='finance/payment')){console.error('payment token MISS:',slug,JSON.stringify(v));ok=false}}const ord=g.sensitivityHeuristic('shop.example','x.list_orders','list my past orders');if(ord.suspect===true&&ord.axis==='finance/payment'){console.error('bare order/orders FALSE-tripped a list_orders read:',JSON.stringify(ord));ok=false}const x=await import('./scripts/verify-catalog-crosscheck.mjs');if(typeof x.checkPaymentOpsNotSafeInvocable!=='function'){console.error('checkPaymentOpsNotSafeInvocable not exported');ok=false}else{const safeClassify=()=>({sensitive:false,denied:false});const sensClassify=()=>({sensitive:true,denied:false});const fams=['buy_tickets','book_flight','request_ride','place_bid','create_order','place_order'];for(const op of fams){const f=x.checkPaymentOpsNotSafeInvocable([{slug:'shopco.'+op,service:'shopco.example',sideEffectClass:'write',backing:'dom'}],safeClassify);if(!(f.failures&&f.failures.length>0)){console.error('guard MISSED a payment verb family on a safe origin:',op);ok=false}}const promo=x.checkPaymentOpsNotSafeInvocable([{slug:'shopco.checkout',service:'shopco.example',sideEffectClass:'write',backing:'handler'}],sensClassify);if(!(promo.failures&&promo.failures.length>0)){console.error('guard did NOT fail a payment op promoted to handler');ok=false}const pass=x.checkPaymentOpsNotSafeInvocable([{slug:'opentable.reserve_table',service:'www.opentable.com',sideEffectClass:'write',backing:'dom'}],sensClassify);if(pass.failures&&pass.failures.length>0){console.error('guard FALSE-failed a DOM-only payment op on a sensitive origin (reserve_table/opentable):',JSON.stringify(pass.failures));ok=false}const ro=x.checkPaymentOpsNotSafeInvocable([{slug:'yelp.search_businesses',service:'www.yelp.com',sideEffectClass:'read',backing:'dom'},{slug:'calendly.get_availability',service:'calendly.com',sideEffectClass:'read',backing:'dom'}],safeClassify);if(ro.failures&&ro.failures.length>0){console.error('guard FALSE-failed a read-only-safe op (yelp/calendly carry no payment verb):',JSON.stringify(ro.failures));ok=false}}if(!ok)process.exit(1);console.log('PASS heuristic widened (no bare-order false-trip) + payment-op guard covers all verb families, fails safe-invocable, passes DOM-only-on-sensitive + read-only-safe')})()"</automated>
  </verify>
  <acceptance_criteria>
    - sensitivityHeuristic flags a commerce checkout/cart/place-order/charge op (suspect:true, axis 'finance/payment') on host/slug/description alone; every existing finance token still present; \b anchoring kept; a bare 'order'/'orders' token is NOT added (a list_orders read does NOT trip the axis).
    - checkPaymentOpsNotSafeInvocable is dual-exported and detects a payment op via BOTH a payment-VERB set (verbPrefix: buy/book/place/reserve/checkout/pay/purchase/charge/order/request/register) AND a literal op-name set (incl. create_order, complete_booking) -- so EACH of buy_tickets/book_flight/request_ride/place_bid/create_order/place_order is recognized.
    - The guard FAILS a payment op on a safe origin (each verb family) AND a payment op promoted to backing recipe/handler; it PASSES a backing:'dom' payment op on a sensitive origin (reserve_table on opentable); it does NOT key on read-only-safe ops (yelp/calendly search/get -- no payment verb).
    - runCli() awaits Denylist.load() and merges the payment-op guard failures into the exit-decision set; crossCheck + checkReadOnlySafeOrigins unchanged.
    - node scripts/verify-catalog-crosscheck.mjs exits 0 over the committed corpus (no existing descriptor is a payment-safe-invocable op -- the current corpus has no commerce payment ops yet).
  </acceptance_criteria>
  <done>The classification heuristic independently backstops the commerce/payment category (SPECIFIC payment tokens added, no bare-order false-trip, fail-closed) AND the crosscheck has a genuine payment-op CI guard that detects ALL 12 phase-39 payment op-names (payment-VERB set + op-name set) and fails the build on any payment-safe-invocable op while passing a DOM-only payment op on a gated origin -- the third leg of the triple money-no-movement-under-Auto mitigation, now actually able to detect the gap.</done>
</task>

<task type="auto">
  <name>Task 3: Fail-closed gate fixtures + proofs (unclassified-commerce gate proof + payment-op-guard proof incl. per-verb-family coverage), registered in CI</name>
  <files>catalog/descriptors/_fixtures/batch-unclassified-commerce-origin.fixture.json, catalog/descriptors/_fixtures/payment-op-safe-invocable.fixture.json, tests/breadth-batch-gate.test.js, tests/catalog-crosscheck.test.js, tests/payment-op-guard.test.js, package.json</files>
  <read_first>
    - catalog/descriptors/_fixtures/batch-unclassified-origin.fixture.json + batch-unclassified-social-origin.fixture.json (the finance + social fixture shapes to mirror for a COMMERCE one)
    - tests/breadth-batch-gate.test.js (the (a) finance fail-closed / (b) safe-batch / (c) social fail-closed / (d) screened-social-pass blocks to extend; the dynamic-import of the ESM gate + Denylist.load() live-roster pattern)
    - tests/catalog-crosscheck.test.js (the REAL crossCheck-export proof shape -- the template for the payment-op-guard test driving the REAL checkPaymentOpsNotSafeInvocable export)
    - scripts/verify-catalog-crosscheck.mjs (confirm the checkPaymentOpsNotSafeInvocable export signature + the PAYMENT_VERBS/PAYMENT_OP_NAMES sets from Task 2)
    - scripts/verify-classification-gate.mjs (the widened finance/payment axis the commerce fixture must trip)
    - package.json (the "test" chain -- register the new payment-op-guard test after sensitive-write-import-gate.test.js)
  </read_first>
  <action>
    (a) Create catalog/descriptors/_fixtures/batch-unclassified-commerce-origin.fixture.json mirroring
    batch-unclassified-origin.fixture.json but for a COMMERCE/PAYMENT batch origin that trips the widened
    finance/payment axis and is NOT classified: service a checkout/cart brand host absent from
    service-denylist.json (e.g. "checkout.shopcorp.example"), slug a payment op (e.g. "shopcorp.place_order"
    or "shopcorp.checkout_cart"), sideEffectClass "write", an _fixture note explaining it is the BRDTH-02
    commerce-origin fail-closed proof (distinct from the Phase-37 finance fixture + the Phase-38 social
    fixture) and that _fixtures/ is excluded by readJsonDir non-recursion so it never ships.

    (b) Create catalog/descriptors/_fixtures/payment-op-safe-invocable.fixture.json: a descriptor object
    shaped for the payment-op guard -- slug a payment op ("shopcorp.place_order"), service a SAFE host
    ("shopcorp.example", NOT in the denylist), sideEffectClass "write", backing "handler" (BOTH failure
    conditions in one fixture: a payment op that is safe-origin AND promoted to invocable), params a
    closed object, an _fixture note explaining it is the payment-op-guard fixture the build MUST reject
    (a payment op must never be safe-and-API-invocable), excluded from the shipped catalog.

    (c) Extend tests/breadth-batch-gate.test.js: add a block (e) that loads the new
    batch-unclassified-commerce-origin fixture, runs classifyGate([commerceItem]) and asserts
    failures.length>0 AND the failure names the commerce origin AND cites the finance/payment axis (the
    payment-axis fail-closed proof, distinct from the existing finance (a) + social (c) blocks). Add a
    block (f) that asserts a SCREENED commerce batch passes: classifyGate over a sensitive payment origin
    (e.g. {origin:'https://www.doordash.com',...}) + a safe read origin returns failures.length===0
    (Task-1's classification makes the payment origin governed -> the import will not abort). Keep the
    existing (a)-(d) blocks UNCHANGED. (breadth-batch-gate is already registered + chained via
    verify-classification-gate.mjs -- no new registration for it.)

    (d) Create tests/payment-op-guard.test.js (THE headline CI assertion proof) driving the REAL
    checkPaymentOpsNotSafeInvocable export (mirror tests/catalog-crosscheck.test.js's real-export pattern;
    zero-framework: check(cond,msg) + process.exit(failed>0?1:0); ASCII, no emojis):
      - Load the payment-op-safe-invocable.fixture.json from disk; assert checkPaymentOpsNotSafeInvocable
        ([fixtureDescriptor], safeClassify) returns failures.length>0 naming the slug (a payment op on a
        safe origin AND promoted to handler FAILS the build).
      - PER-VERB-FAMILY COVERAGE (the blocker proof): for EACH of buy_tickets, book_flight, request_ride,
        place_bid, create_order, place_order (a hand-built {slug:'shopco.'+op, service:'shopco.example',
        sideEffectClass:'write', backing:'dom'} with a SAFE classify stub returning
        {sensitive:false,denied:false}), assert checkPaymentOpsNotSafeInvocable returns failures.length>0 --
        proving each verb family (buy/book/request/place/create-order) is detected, not just the literal
        place_order/checkout keys. (This is the CI exercise of the coverage the blocker required.)
      - Assert a payment op on a sensitive origin but backing:'dom' (a hand-built {slug:
        'opentable.reserve_table', service:'www.opentable.com', sideEffectClass:'write', backing:'dom'},
        classify stub returning {sensitive:true,denied:false}) returns failures.length===0 (DOM-only on a
        gated origin PASSES -- no money movement under Auto; opentable is sensitive per 39-01).
      - Assert a READ-ONLY-SAFE op carries NO payment verb: a non-payment read on a safe origin (e.g.
        {slug:'yelp.search_businesses', service:'www.yelp.com', backing:'dom', sideEffectClass:'read'} and
        {slug:'calendly.get_availability', service:'calendly.com', backing:'dom', sideEffectClass:'read'})
        returns failures.length===0 (the guard is payment-op-scoped, not a blanket safe-write ban -- it
        must not false-fail a benign commerce read; confirms the read-only-safe apps carry no payment verb).
      - Use the REAL committed Denylist where the proof is about real classification (mirror the
        live-roster pattern), and a small classify stub where the proof is about the guard's branch logic.
      Register tests/payment-op-guard.test.js in package.json "test" after node
      tests/sensitive-write-import-gate.test.js (do NOT remove any existing test).

    Optionally extend tests/catalog-crosscheck.test.js with a payment-op-guard assertion block (the same
    REAL export) if it reads more naturally there -- but the dedicated payment-op-guard.test.js is the
    registered proof. Do NOT edit the importer, the gates' core logic, or service-denylist.json here.
  </action>
  <verify>
    <automated>node tests/breadth-batch-gate.test.js && node tests/payment-op-guard.test.js && grep -q payment-op-guard package.json && grep -q "batch-unclassified-commerce-origin" tests/breadth-batch-gate.test.js && grep -Eq "buy_tickets|book_flight|request_ride|place_bid|create_order" tests/payment-op-guard.test.js && test -f catalog/descriptors/_fixtures/payment-op-safe-invocable.fixture.json && node scripts/verify-classification-gate.mjs && node scripts/verify-catalog-crosscheck.mjs && npm run validate:extension && echo PAYMENT-SCREENING-PROOFS-OK</automated>
  </verify>
  <acceptance_criteria>
    - breadth-batch-gate.test.js passes: the new commerce-origin fixture makes classifyGate abort (failures>0, names the origin, cites finance/payment axis); the screened commerce batch passes (failures===0); the existing finance/social/screened-social blocks still pass.
    - payment-op-guard.test.js passes: the payment-op-safe-invocable fixture FAILS the guard; EACH verb family (buy_tickets/book_flight/request_ride/place_bid/create_order/place_order) on a safe origin FAILS the guard; a DOM-only payment op on a sensitive origin (reserve_table/opentable) PASSES; a read-only-safe op (yelp/calendly) PASSES (no payment verb).
    - The two fixtures live under catalog/descriptors/_fixtures/ (excluded from the shipped catalog).
    - tests/payment-op-guard.test.js is registered in package.json "test"; no existing test removed.
    - node scripts/verify-classification-gate.mjs + node scripts/verify-catalog-crosscheck.mjs + npm run validate:extension all exit 0 (the committed corpus + roster still classify; no existing descriptor regresses).
  </acceptance_criteria>
  <done>The payment screening's properties are proven in CI: the merge aborts fail-closed on an unclassified commerce/payment origin (BRDTH-02), and the payment-op CI guard fails the build on any payment-safe-invocable op -- with per-verb-family coverage proving all 12 phase-39 payment op-names are detected -- while passing a DOM-only payment op on a gated origin (reserve_table/opentable) and a read-only-safe op; the triple money-no-movement-under-Auto mitigation is established and the import plans 02-06 are unblocked (zero unclassified commerce origins for the merge-time classifyGate to abort on, and the guard armed + coverage-proven before any payment descriptor lands).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| commerce/payment origin classification -> merge-time classifyGate (build-time) | an unscreened payment/checkout origin's descriptor must be refused before it can land in the shipped corpus |
| a payment-bearing op -> the side-effect/backing crosscheck (build-time) | a payment op (checkout/charge/place_order/buy_tickets/book_flight/request_ride/...) must never be classified safe-and-API-invocable -- if it were, it would run under Auto = money moved without consent |
| screened sensitive payment origin -> invoke consent gate (runtime) | a payment WRITE on a sensitive origin must re-enforce the per-origin mutating opt-in (posture B) before it executes (proven end-to-end on a REAL emitted descriptor in Plan 39-07) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | Elevation of Privilege | a payment-bearing op (place_order/checkout/charge/complete_booking/buy_tickets/book_flight/request_ride/place_bid/create_order/...) writable/invocable under Auto = MONEY MOVED without consent -- THE headline threat | mitigate | TRIPLE mitigation established here: (a) the FROZEN backingFor() stamps backing:'dom' for every breadth descriptor -> DOM-only, not API-invocable (re-asserted per sub-batch in 02-06); (b) the NEW payment-op CI guard (Task 2) detects EVERY payment op-name via a payment-VERB set (verbPrefix) + a literal op-name set and FAILS THE BUILD if any payment op is safe-and-API-invocable (proven by payment-op-safe-invocable.fixture.json + per-verb-family probes, Task 3); (c) every payment origin is classified sensitive/denied (Task 1) so its DOM-path write is posture-B gated |
| T-39-02 | Elevation of Privilege | a payment origin mis-classified safe -> its DOM-path write runs under Auto with no re-gate | mitigate | the conservative default classifies every commerce/payment origin sensitive (Task 1, payment-conservative-by-default; opentable held-card unconditionally sensitive); the widened finance/payment heuristic (Task 2a, SPECIFIC tokens, no bare-order false-trip) independently backstops on host/slug/description so an accidentally-dropped classification still trips classifyGate; the merge-time gate aborts fail-closed on any unclassified commerce origin (Task 3, proven by batch-unclassified-commerce-origin.fixture.json) |
| T-39-12 | Elevation of Privilege | the payment-op guard NAME-MATCHING too narrow -- a payment op-name the guard does not recognize (buy_now/place_bid/request_ride/create_order) slips through as safe-invocable, defeating the headline control | mitigate | the guard keys on BOTH a payment-VERB set (verbPrefix: buy/book/place/reserve/checkout/pay/purchase/charge/order/request/register) AND a literal op-name set (create_order/complete_booking/...) so all 12 phase-39 payment op-names are covered (Task 2b); the per-verb-family CI probe (Task 3d) FAILS the build if any family stops being detected -- the coverage is exercised, not assumed |
| T-39-03 | Tampering | the DENY-01 fully-denied set (brokerage robinhood/fidelity/carta + ToS-hostile media) OR the catastrophic money-movement set leaking back in as imported descriptors | mitigate | the DENY-01 set stays in deniedOrigins (Task 1, unchanged + asserted denied===true); newly-denied money-movement apps added to deniedOrigins; the import plans never vendor a denied app, and the merge gate refuses a denied origin's descriptor |
| T-39-04 | Spoofing | a payment op surfaced as a confident API-invocable hit (a fabricated checkout/charge call from guessed auth -- the research anti-pattern) | mitigate | the payment-op guard forbids backing recipe/handler for any payment op (Task 2b); the frozen backing:'dom' default + tests/dom-only-default.test.js make every breadth payment op DOM-only / discovery-pending, never learn-seeded; never auto-mint a recipe from guessed auth |
| T-39-05 | Tampering | a denylist edit silently broadening a commerce app's scope (a '*.' suffix where an exact host was meant) -> over-denial or an unintended apex match | mitigate | exact-host / apex-suffix discipline for the commerce origins (Task 1, mirroring the Phase-35 exact-host rule); the classification verify asserts the precise classify() verdict per origin |
| T-39-SC | Tampering | npm/pip/cargo installs | accept | Phase 39 Plan 01 adds NO new packages (denylist JSON + gate script + doc + test edits only; zod/tsx already devDeps); no install task -> the package-legitimacy gate is N/A this plan. The import plans 02-06 vendor metadata only (no installs); the package-legitimacy gate stays N/A for the phase |
</threat_model>

<verification>
- `node -e` classify probe: the payment-bearing commerce/travel origins + opentable classify sensitive; the DENY-01 set + the Phase-35/38 roster classify unchanged.
- `node -e` heuristic + guard probe: the widened finance/payment axis flags a checkout/cart/place-order op and does NOT false-trip a list_orders read; checkPaymentOpsNotSafeInvocable fails a payment-safe-invocable op for EACH verb family (buy_tickets/book_flight/request_ride/place_bid/create_order/place_order), passes a DOM-only payment op on a sensitive origin (reserve_table/opentable), and passes a read-only-safe op (yelp/calendly).
- `node tests/breadth-batch-gate.test.js` passes: the unclassified-commerce fixture aborts the gate citing the finance/payment axis; the screened commerce batch passes.
- `node tests/payment-op-guard.test.js` passes: the payment-op-safe-invocable fixture fails the guard; each verb family on a safe origin fails; a DOM-only payment op on a sensitive origin passes; a read-only-safe op passes.
- `node scripts/verify-classification-gate.mjs` + `node scripts/verify-catalog-crosscheck.mjs` + `npm run validate:extension` all exit 0 (no existing descriptor regresses).
</verification>

<success_criteria>
- BRDTH-02 (payment screening): every commerce/travel/misc batch origin is classified denied/sensitive/safe per the payment axis BEFORE 02-06 import (opentable sensitive held-card); the merge-time classifyGate aborts fail-closed on an unclassified commerce/payment origin -- the screening enforced as a build failure.
- Payment-op CI guard (the headline): no payment-bearing op can be classified safe-and-API-invocable -- the guard detects ALL 12 phase-39 payment op-names (payment-VERB set + op-name set), fails the build on a payment-safe-invocable op (fixture-proven + per-verb-family-proven) and passes a DOM-only payment op on a gated origin; the third leg of the triple money-no-movement-under-Auto mitigation.
- BRDTH-01/03 prerequisites: the payment-conservative-by-default classification + the frozen backing:'dom' default mean every payment op the import plans emit is DOM-only on a sensitive/denied origin (re-asserted per sub-batch); no payment op armed via recipe/handler.
- docs/LEGAL.md records the payment/money-movement axis + the triple mitigation defensibly.
- Regression: verify-classification-gate.mjs + verify-catalog-crosscheck.mjs + validate:extension stay exit 0.
</success_criteria>

<output>
Create `.planning/phases/39-breadth-c-commerce-travel-misc-most-sensitive/39-01-SUMMARY.md` when done.
</output>
</output>
