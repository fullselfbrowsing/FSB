---
phase: 39-breadth-c-commerce-travel-misc-most-sensitive
plan: 05
type: execute
wave: 5
depends_on: ["39-01", "39-04"]
files_modified:
  - vendor/opentabs-snapshot/plugins/ticketmaster/**
  - vendor/opentabs-snapshot/plugins/stubhub/**
  - vendor/opentabs-snapshot/plugins/eventbrite/**
  - vendor/opentabs-snapshot/plugins/yelp/**
  - vendor/opentabs-snapshot/plugins/tripadvisor/**
  - vendor/opentabs-snapshot/plugins/calendly/**
  - extension/config/service-denylist.json
  - scripts/verify-catalog-crosscheck.mjs
  - vendor/opentabs-snapshot/PIN.md
  - catalog/descriptors/_fixtures/_provenance.json
  - catalog/descriptors/_fixtures/intent-cases.json
  - extension/catalog/recipe-index.generated.js
  - tests/breadth-search-return.test.js
  - tests/capability-search-eval.test.js
autonomous: true
requirements: [BRDTH-01, BRDTH-02, BRDTH-03]

must_haves:
  truths:
    - "The events / local-services / scheduling sub-batch (ticketmaster, stubhub, eventbrite, yelp, tripadvisor, calendly) is vendored as hermetic metadata-only slices and imported via the FROZEN importer (ZERO machinery edit beyond STEM_OVERRIDES data-map entries where a host derives wrong) -- each returns from search with >=3 app-disambiguated synonyms + side-effect class + backing:'dom' per op"
    - "The PAYMENT-bearing event apps (ticketmaster/stubhub/eventbrite -- buy tickets) are classified SENSITIVE (added to 39-01's roster in service-denylist.json BEFORE this import); the READ-ONLY apps (yelp, tripadvisor reviews/listings; calendly availability) are SAFE -- and calendly/yelp/tripadvisor are added to verify-catalog-crosscheck.mjs READ_ONLY_SAFE_SERVICES ONLY because their emitted ops are genuinely read-only (the 38 MED-02 guard); the merge-time classifyGate PASSES for all 6"
    - "Each ticket-buying op (buy_tickets/purchase_tickets) classes write AND is backing:'dom' on a sensitive origin -> the payment-op CI guard PASSES; the read-only apps emit ONLY read ops (search/list/get) -> the READ_ONLY_SAFE guard PASSES (a re-vendored write on a safe origin would FAIL the build)"
    - "The snapshot is regenerated via package-extension.mjs (INV-01 byte-stable); validate:extension exits 0; no prior descriptor clobbered"
    - "node tests/no-dead-entry.test.js stays exit 0; the eval keeps recall@5>=0.9 wrong-invoke=0.000 (incl. cross-app ticket collision probes -- ticketmaster-vs-stubhub-vs-eventbrite buy/list); the SW cold-start budget holds (flagged if approaching the gate)"
  artifacts:
    - path: "vendor/opentabs-snapshot/plugins/ticketmaster/src/index.ts"
      provides: "vendored ticketmaster metadata slice (ticketmaster.com SENSITIVE; buy_tickets is the DOM-only-on-sensitive payment-op-guard subject for events)"
      contains: "TicketmasterPlugin"
    - path: "extension/config/service-denylist.json"
      provides: "the roster EXTENDED for the payment-bearing event apps (ticketmaster/stubhub/eventbrite -> sensitive); the read-only apps left safe"
      contains: "sensitiveOrigins"
    - path: "scripts/verify-catalog-crosscheck.mjs"
      provides: "READ_ONLY_SAFE_SERVICES EXTENDED with the genuinely-read-only safe commerce origins (yelp/tripadvisor/calendly) so a re-vendored write on one FAILS the build (the 38 MED-02 invariant)"
      contains: "READ_ONLY_SAFE_SERVICES"
    - path: "tests/breadth-search-return.test.js"
      provides: "the per-op breadth assertion EXTENDED incl. cross-app ticket collision probes (wrong-invoke=0)"
      contains: "ticketmaster"
  key_links:
    - from: "extension/config/service-denylist.json (39-05 extension) + scripts/verify-catalog-crosscheck.mjs READ_ONLY_SAFE_SERVICES"
      to: "scripts/import-opentabs-catalog.mjs runImport classifyGate + the crosscheck"
      via: "ticket apps sensitive -> classifyGate passes + payment-op guard passes (DOM-only on sensitive); read-only apps safe + in READ_ONLY_SAFE -> their reads pass, a future write FAILS"
      pattern: "sensitiveOrigins|READ_ONLY_SAFE_SERVICES|classifyGate"
    - from: "scripts/import-opentabs-catalog.mjs enumerateBatchApps"
      to: "vendor/opentabs-snapshot/plugins/{ticketmaster,stubhub,eventbrite,yelp,tripadvisor,calendly}"
      via: "the frozen enumeration auto-discovers the new dirs; each emits opentabs__<stem>__* descriptors backing:'dom'"
      pattern: "opentabs__(ticketmaster|stubhub|eventbrite|yelp|tripadvisor|calendly)__"
---

<objective>
Import the events / local-services / scheduling sub-batch (ticketmaster, stubhub, eventbrite, yelp,
tripadvisor, calendly) as DATA-ONLY via the FROZEN machinery, gated on the 39-01 payment screening.
This sub-batch SPANS BOTH tiers: payment-bearing ticket apps (buy tickets = money movement -> SENSITIVE)
AND genuinely read-only apps (reviews/listings/availability -> SAFE). It therefore EXTENDS the 39-01
roster (classify the new ticket origins sensitive) and EXTENDS the 38 MED-02 READ_ONLY_SAFE invariant
(add the read-only safe origins so a future write on one fails the build). Reuses the FROZEN screen-then-
import contract + the per-wave gate re-assertion (the 39-02/03/04 structure is the proven template).

1. EXTEND the screening (a small 39-01-style edit, BEFORE vendoring): classify ticketmaster/stubhub/
   eventbrite SENSITIVE in service-denylist.json (payment-bearing); add yelp/tripadvisor/calendly to
   verify-catalog-crosscheck.mjs READ_ONLY_SAFE_SERVICES (the 38 MED-02 guard) because their emitted ops
   are genuinely read-only.
2. Vendor the 6 apps as hermetic metadata slices (Wall 1: metadata-only, mirror todoist EXACTLY). The
   ticket apps carry a buy_tickets payment write; the read-only apps emit ONLY reads. Real z.object
   inputs. Explicit transport signals.
3. Run the FROZEN importer: classifyGate PASSES (ticket origins sensitive, read-only origins safe-benign);
   every emitted descriptor is backing:'dom'; the payment-op CI guard PASSES (ticket payment ops DOM-only
   on sensitive origins); the READ_ONLY_SAFE guard PASSES (read-only apps emit only reads).
4. Regenerate the snapshot; validate:extension reconciles. Re-assert the per-wave gates.

Purpose: complete the events + local-services + scheduling category, exercising BOTH the payment-op
guard (ticket apps) AND the read-only-safe invariant (review/availability apps) in one sub-batch.
Output: 6 apps' descriptors (ticket apps backing:'dom' on sensitive origins; read-only apps safe +
read-only-locked); extended roster + READ_ONLY_SAFE set; regenerated snapshot; all per-wave gates green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-breadth-c-commerce-travel-misc-most-sensitive/39-CONTEXT.md
@.planning/phases/39-breadth-c-commerce-travel-misc-most-sensitive/39-01-SUMMARY.md
@.planning/phases/39-breadth-c-commerce-travel-misc-most-sensitive/39-04-SUMMARY.md

<interfaces>
<!-- The FROZEN machinery + the 39-01 screening + the 39-02/03/04 import contract (proven template). -->
- extension/config/service-denylist.json (THIS plan extends sensitiveOrigins for the ticket apps): add
  EXACT-host / apex-suffix patterns for ticketmaster.com, stubhub.com, eventbrite.com (payment-bearing ->
  sensitive). Use the same form discipline as 39-01. Leave yelp/tripadvisor/calendly UNCLASSIFIED (safe);
  verify the 39-01-widened heuristic does NOT false-trip their read hosts (yelp/tripadvisor/calendly are
  not finance/payment-token hosts; their read ops should not trip the axis -- confirm via the gate).
- scripts/verify-catalog-crosscheck.mjs READ_ONLY_SAFE_SERVICES (THIS plan extends it; the 38 MED-02
  guard): currently { 'reddit.com', 'www.reddit.com' }. ADD the genuinely-read-only safe commerce origins
  this sub-batch imports -- the vendored hosts for yelp/tripadvisor/calendly (e.g. 'www.yelp.com',
  'www.tripadvisor.com', 'calendly.com'). checkReadOnlySafeOrigins asserts every descriptor whose service
  is in this set is sideEffectClass 'read' -> a future re-vendor adding a write op for one of these FAILS
  the build (forcing an explicit re-classification). Add an origin here ONLY because it is left safe
  SPECIFICALLY because it is read-only (the MED-02 rule).
- scripts/import-opentabs-catalog.mjs: enumerateBatchApps() auto-discovers each new dir; backingFor()
  returns 'dom'; runImport() classifyGate-aborts on any unclassified origin AND feedSeedDescriptors
  upserts each slug. NO importer edit beyond a STEM_OVERRIDES data-map entry where the frozen split('.')[0]
  is not the brand.
- side-effect verbs (scripts/lib/side-effect-class.mjs, FROZEN): buy/purchase/create -> WRITE; cancel/
  delete -> DESTRUCTIVE; list/get/search -> READ. The ticket buy op carries `api(..., { method:'POST' })`;
  reads `api(..., { method:'GET' })`. Do NOT edit the shared module.
- The payment-op CI guard (39-01): every ticket payment op (buy_tickets/purchase_tickets) must be
  backing:'dom' on a sensitive origin. Confirm 'buy'/'purchase' is in the guard's PAYMENT_OP set (39-01
  Task 2b) so a ticket purchase op is treated as a payment op; if not, the buy op is still backing:'dom'
  on a sensitive origin so it is safe either way -- but the guard SHOULD catch ticket purchases (they
  move money), so verify the PAYMENT_OP set covers buy/purchase.
- The vendored-slice contract (replicate todoist EXACTLY). The 39-02/03/04 slices are worked examples.
- breadth-search-return.test.js: widen the corpusFiles regex + raise the floor + add cross-app ticket
  collision probes (ticketmaster-vs-stubhub-vs-eventbrite buy/list; yelp-vs-tripadvisor reviews) all
  wrong-invoke=0.
- The eval + cold-start budget: the importer seed-feeds; keep generous gates; widen the cold-start byte
  budget ONLY if legitimately exceeded while per-descriptor footprint stays flat, with a comment -- do NOT
  change the data layout; Phase-43 gate stays separate. FLAG in the SUMMARY if approached.
</interfaces>

# Reuse references (the proven slice + import + screening shapes -- do NOT rebuild):
@vendor/opentabs-snapshot/plugins/todoist/src/index.ts
@vendor/opentabs-snapshot/plugins/todoist/src/sdk-stub.ts
@vendor/opentabs-snapshot/plugins/todoist/src/tools/create-task.ts
@vendor/opentabs-snapshot/plugins/todoist/src/tools/list-tasks.ts
@scripts/verify-catalog-crosscheck.mjs
@extension/config/service-denylist.json
@vendor/opentabs-snapshot/PIN.md
@tests/breadth-search-return.test.js
@tests/capability-search-eval.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the screening (ticket origins sensitive + read-only origins in READ_ONLY_SAFE) and vendor the 6 metadata slices</name>
  <files>extension/config/service-denylist.json, scripts/verify-catalog-crosscheck.mjs, vendor/opentabs-snapshot/plugins/{ticketmaster,stubhub,eventbrite,yelp,tripadvisor,calendly}/**, vendor/opentabs-snapshot/PIN.md</files>
  <read_first>
    - extension/config/service-denylist.json (the 39-01-extended roster + the host-pattern form; add the ticket origins to sensitiveOrigins)
    - scripts/verify-catalog-crosscheck.mjs (the READ_ONLY_SAFE_SERVICES set + checkReadOnlySafeOrigins -- the 38 MED-02 guard; extend the set with the read-only safe origins)
    - extension/utils/service-denylist.js (classify() + the host-pattern matcher -- confirm the form before adding origins)
    - vendor/opentabs-snapshot/plugins/todoist/ + a 39-02/03/04 slice (the full slice structure + a same-category worked example)
    - scripts/import-opentabs-catalog.mjs (confirm each host's frozen-derived stem; add a STEM_OVERRIDES data-map entry where 'www.<brand>.com'->'www')
    - vendor/opentabs-snapshot/PIN.md (the SHA + MIT pin + the per-phase append-only log)
  </read_first>
  <action>
    (a) EXTEND extension/config/service-denylist.json sensitiveOrigins with the payment-bearing event apps
    (mirroring the 39-01 exact-host discipline): "https://www.ticketmaster.com", "https://www.stubhub.com",
    "https://www.eventbrite.com" (ticket purchase = money movement -> sensitive, writes posture-B gated).
    Leave yelp/tripadvisor/calendly UNCLASSIFIED (the safe read-only tier). Update the leading "_comment"
    to note the 39-05 event-apps extension. Do NOT remove any existing entry.

    (b) EXTEND scripts/verify-catalog-crosscheck.mjs READ_ONLY_SAFE_SERVICES (the 38 MED-02 guard) with the
    vendored hosts for the genuinely-read-only apps this sub-batch imports: 'www.yelp.com',
    'www.tripadvisor.com', 'calendly.com' (use the EXACT service host each app vendors, lowercased). Add a
    comment noting these are safe SPECIFICALLY because their vendored ops are read-only (reviews/listings/
    availability), so a future re-vendor adding a write op for one FAILS the build (forcing a re-classify).
    Do NOT change crossCheck or the payment-op guard.

    (c) Vendor the 6 apps mirroring the todoist slice structure EXACTLY (Wall 1: metadata-only, NO dist/,
    NO executed handle(), NO runtime .js). For EACH: package.json (opentabs.urlPatterns host below + _role
    + _note + MIT), src/sdk-stub.ts (VERBATIM copy), src/index.ts, src/<app>-api.ts (inert stub),
    src/tools/<op>.ts (defineTool, real z.object input, never-run handle naming helper+method). Add a
    STEM_OVERRIDES data-map entry keyed by the dir name where the frozen split('.')[0] is not the brand.

    Hosts + ops (real z.object inputs):
      - ticketmaster -> "*://www.ticketmaster.com/*" (SENSITIVE): search_events (read), get_event (read),
        list_orders (read), buy_tickets (WRITE -- the payment op; api POST).
      - stubhub      -> "*://www.stubhub.com/*"     (SENSITIVE): search_events (read), get_listing (read),
        list_orders (read), buy_tickets (WRITE -- the payment op; api POST).
      - eventbrite   -> "*://www.eventbrite.com/*"  (SENSITIVE): search_events (read), get_event (read),
        list_orders (read), register_for_event (WRITE -- the payment op for paid events; api POST).
      - yelp         -> "*://www.yelp.com/*"        (SAFE read-only): search_businesses (read),
        get_business (read), list_reviews (read). NO write op.
      - tripadvisor  -> "*://www.tripadvisor.com/*" (SAFE read-only): search_locations (read),
        get_location (read), list_reviews (read). NO write op.
      - calendly     -> "*://calendly.com/*"        (SAFE read-only): list_event_types (read),
        get_availability (read), list_scheduled_events (read). NO write op (booking a Calendly slot is the
        invitee flow, not in scope; keep calendly the safe read tier per 39-CONTEXT).
    The ticket buy/register write op carries an explicit `{ method:'POST' }`; reads `api(..., {method:'GET'})`.
    The read-only apps emit ONLY read ops (no apiVoid, no POST) -- so checkReadOnlySafeOrigins passes. Do
    NOT vendor any e2e/prescript fixture or any DENY-01 / money-movement denied app. Do NOT edit the
    importer (except a STEM_OVERRIDES data-map entry) or side-effect-class.mjs.

    Append the 6 apps to vendor/opentabs-snapshot/PIN.md's per-phase log as a Phase-39 row (batch C
    sub-batch 4: events [sensitive, payment] + local-services/scheduling [safe, read-only]).
  </action>
  <verify>
    <automated>node -e "const D=require('./extension/utils/service-denylist.js');D.load().then(()=>{const sens=['https://www.ticketmaster.com','https://www.stubhub.com','https://www.eventbrite.com'];const safe=['https://www.yelp.com','https://www.tripadvisor.com','https://calendly.com'];let ok=true;for(const o of sens){if(D.classify(o).sensitive!==true){console.error('ticket origin NOT sensitive:',o);ok=false}}for(const o of safe){const c=D.classify(o);if(c.sensitive!==false||c.denied!==false){console.error('read-only app should be safe:',o,JSON.stringify(c));ok=false}}if(!ok)process.exit(1);console.log('PASS ticket apps sensitive; read-only apps safe')})" && node -e "(async()=>{const x=await import('./scripts/verify-catalog-crosscheck.mjs');const fail=x.checkReadOnlySafeOrigins([{slug:'yelp.write_review',service:'www.yelp.com',sideEffectClass:'write'}]);if(!(fail.failures&&fail.failures.length>0)){console.error('READ_ONLY_SAFE did not catch a yelp write');process.exit(1)}const pass=x.checkReadOnlySafeOrigins([{slug:'yelp.search_businesses',service:'www.yelp.com',sideEffectClass:'read'}]);if(pass.failures&&pass.failures.length>0){console.error('READ_ONLY_SAFE false-failed a yelp read');process.exit(1)}console.log('PASS READ_ONLY_SAFE: yelp write fails, yelp read passes')})()" && node --import tsx -e "const apps=['ticketmaster','stubhub','eventbrite','yelp','tripadvisor','calendly'];(async()=>{for(const a of apps){const m=await import('./vendor/opentabs-snapshot/plugins/'+a+'/src/index.ts');const p=m.default;if(!p||!Array.isArray(p.tools)||p.tools.length<3){console.error('bad slice',a);process.exit(1)}console.log(a,'->',(p.urlPatterns&&p.urlPatterns[0]),p.tools.map(t=>t.name).join(','))}process.exit(0)})().catch(e=>{console.error(e.message);process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - classify() returns sensitive for ticketmaster/stubhub/eventbrite; safe (sensitive:false, denied:false) for yelp/tripadvisor/calendly.
    - READ_ONLY_SAFE_SERVICES includes the yelp/tripadvisor/calendly vendored hosts; checkReadOnlySafeOrigins FAILS a write op on one and PASSES a read op (the 38 MED-02 invariant extended).
    - All 6 apps import under tsx with >=3 ops; the ticket apps carry a buy/register payment write; the read-only apps emit ONLY read ops; real z.object inputs; no handle() executes; provenance-scaffold green.
    - The emitted stems are the brands (opentabs__ticketmaster__* etc.) -- STEM_OVERRIDES data-map entries added per host that derives wrong; no DENY-01 / denied app vendored; side-effect-class.mjs + crossCheck + the payment-op guard untouched.
  </acceptance_criteria>
  <done>The screening is extended (ticket apps sensitive; yelp/tripadvisor/calendly in READ_ONLY_SAFE) and the 6 apps are vendored -- ticket apps carry a DOM-only-on-sensitive payment write, read-only apps emit only reads (read-only-locked); PIN.md per-phase log updated (Phase-39 batch C sub-batch 4).</done>
</task>

<task type="auto">
  <name>Task 2: Import the sub-batch, regenerate the snapshot, re-assert the per-wave gates (incl. the payment-op + READ_ONLY_SAFE guards)</name>
  <files>extension/catalog/recipe-index.generated.js, catalog/descriptors/_fixtures/intent-cases.json, catalog/descriptors/_fixtures/_provenance.json, tests/breadth-search-return.test.js, tests/capability-search-eval.test.js</files>
  <read_first>
    - scripts/import-opentabs-catalog.mjs (the FROZEN enumerateBatchApps + runImport + classifyGate + feedSeedDescriptors -- RUN, do NOT edit beyond a STEM_OVERRIDES data-map entry from Task 1)
    - scripts/package-extension.mjs + scripts/validate-extension.mjs (regen + byte-compare; INV-01)
    - scripts/verify-catalog-crosscheck.mjs (the crosscheck + the payment-op guard + the extended READ_ONLY_SAFE guard -- RUN, do NOT edit further)
    - tests/breadth-search-return.test.js (the corpusFiles regex ~line 77-79 + COLLISION_SET)
    - tests/capability-search-eval.test.js + tests/no-dead-entry.test.js (run, do NOT edit)
    - catalog/descriptors/_fixtures/intent-cases.json (the eval fixtures to grow)
    - .planning/phases/39-breadth-c-commerce-travel-misc-most-sensitive/39-04-SUMMARY.md (the proven import+regen+gate re-assert worked example)
  </read_first>
  <action>
    (a) Run `node --import tsx ./scripts/import-opentabs-catalog.mjs`. Confirm: opentabs__ticketmaster__* /
    __stubhub__* / __eventbrite__* / __yelp__* / __tripadvisor__* / __calendly__* appear with the brand
    stems; EVERY emitted descriptor carries backing:'dom'; the merge-time classifyGate PASSES (ticket
    origins sensitive, read-only origins safe-benign); the payment-op CI guard PASSES (ticket buy ops
    DOM-only on sensitive origins); the READ_ONLY_SAFE guard PASSES (yelp/tripadvisor/calendly emit only
    reads); all prior descriptors (incl. 39-02/03/04) untouched; seed-descriptors.json carries the new
    slugs.

    (b) Run `node scripts/package-extension.mjs` (INV-01 byte-stable IIFE/djb2); confirm `node
    scripts/validate-extension.mjs` exits 0.

    (c) Extend catalog/descriptors/_fixtures/intent-cases.json: a ticket read ("find events on
    ticketmaster" -> ticketmaster.search_events), a ticket payment write ("buy tickets on ticketmaster" ->
    ticketmaster.buy_tickets; "buy tickets on stubhub" -> stubhub.buy_tickets), a read-only-app read ("find
    restaurants on yelp" -> yelp.search_businesses; "check my calendly availability" ->
    calendly.get_availability), expectedSlug aligned to the emitted real slugs. Extend
    tests/breadth-search-return.test.js: widen the corpusFiles regex, raise the floor, add cross-app
    collision probes to COLLISION_SET (ticketmaster-vs-stubhub-vs-eventbrite buy/list; yelp-vs-tripadvisor
    reviews) all wrong-invoke=0. Do NOT hand-edit seed-descriptors.json.

    (d) Re-assert the per-wave gates: `node scripts/verify-catalog-crosscheck.mjs` (exit 0 -- ticket buy
    ops class write; read-only apps' ops class read; NO under-statement; the payment-op guard passes
    (ticket ops DOM-only on sensitive); the READ_ONLY_SAFE guard passes (read-only apps emit only reads)),
    `node tests/no-dead-entry.test.js` (exit 0), and `node tests/capability-search-eval.test.js`
    (recall@5>=0.9, wrong-invoke=0.000 incl. the ticket collisions; cold-start asserts pass). Widen the
    cold-start budget ONLY if the grown index legitimately exceeds the gate while per-descriptor footprint
    stays flat, with a comment -- do NOT change the data layout, do NOT defer the Phase-43 gate. FLAG in the
    SUMMARY if approached.

    Do NOT edit the importer (beyond Task-1's STEM_OVERRIDES entry), capability-search.js, the gates' core
    logic, or service-denylist.json (Task 1 already extended it).
  </action>
  <verify>
    <automated>node --import tsx ./scripts/import-opentabs-catalog.mjs && test -f catalog/descriptors/opentabs__ticketmaster__buy_tickets.json && test -f catalog/descriptors/opentabs__yelp__search_businesses.json && node -e "const fs=require('fs');const buy=JSON.parse(fs.readFileSync('catalog/descriptors/opentabs__ticketmaster__buy_tickets.json','utf8'));if(buy.backing!=='dom'||buy.sideEffectClass!=='write'){console.error('ticket buy op not DOM-only write',buy.backing,buy.sideEffectClass);process.exit(1)}const yelp=JSON.parse(fs.readFileSync('catalog/descriptors/opentabs__yelp__search_businesses.json','utf8'));if(yelp.sideEffectClass!=='read'){console.error('yelp op not read',yelp.sideEffectClass);process.exit(1)}console.log('PASS ticket buy DOM-only write; yelp read')" && node scripts/package-extension.mjs && npm run validate:extension && node scripts/verify-catalog-crosscheck.mjs && node tests/no-dead-entry.test.js && node tests/breadth-search-return.test.js && node tests/capability-search-eval.test.js</automated>
  </verify>
  <acceptance_criteria>
    - The 6 apps' descriptors are emitted (opentabs__<brand>__*) and inlined; EVERY one carries backing:'dom'; the merge-time classifyGate did NOT abort; no prior descriptor clobbered.
    - validate:extension exits 0 (snapshot reconciled; classification gate + crosscheck + recipe-path guard green); INV-01 byte-stable.
    - verify-catalog-crosscheck.mjs exits 0: ticket buy ops class write + the payment-op guard passes (DOM-only on sensitive); the read-only apps emit only reads + the READ_ONLY_SAFE guard passes.
    - no-dead-entry.test.js exits 0; breadth-search-return.test.js passes incl. the cross-app ticket collisions (wrong-invoke=0).
    - capability-search-eval.test.js prints recall@5>=0.9, wrong-invoke=0.000, cold-start asserts green (flagged in the SUMMARY if approaching the budget).
  </acceptance_criteria>
  <done>The 6 events/local-services/scheduling apps are imported as DOM-only data via the frozen machinery (gate passed; ticket payment ops DOM-only on sensitive origins; read-only apps read-only-locked), the snapshot regenerated (INV-01 byte-stable), and the per-wave gates re-asserted: crosscheck (incl. payment-op + READ_ONLY_SAFE guards) + no-dead-entry + eval green and the cold-start budget held.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OpenTabs vendored metadata -> FSB descriptor corpus (build-time) | the events/local-services metadata; a forbidden field, a denied app, or an unscreened ticket origin must never enter the corpus |
| a ticket-purchase op -> the side-effect/backing crosscheck (build-time) | a buy_tickets op must class write AND stay backing:'dom' on a sensitive origin -- never safe-and-API-invocable |
| a read-only safe origin -> the READ_ONLY_SAFE crosscheck (build-time) | a future write op on yelp/tripadvisor/calendly (left safe because read-only) must FAIL the build, forcing an explicit re-classification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | Elevation of Privilege | a ticket-purchase op (buy_tickets) writable/invocable under Auto = money moved without consent | mitigate | every ticket payment op ships backing:'dom' (frozen default -> DOM-only); the payment-op CI guard passes only because each is DOM-only on a sensitive origin; the ticket origins classified sensitive in Task 1 so their DOM-path write is posture-B gated |
| T-39-08 | Elevation of Privilege | a read-only safe origin (yelp/tripadvisor/calendly) later gaining a write op that ships writable-under-Auto (the 38 MED-02 threat) | mitigate | the origins are added to READ_ONLY_SAFE_SERVICES (Task 1b) -> checkReadOnlySafeOrigins FAILS the build on any non-read op for them, forcing an explicit re-classification rather than a silent writable-under-Auto ship (proven by the yelp-write verify) |
| T-39-02 | Elevation of Privilege | a ticket origin landing unscreened -> its purchase write runs under Auto with no re-gate | mitigate | Task 1a classifies the ticket origins sensitive BEFORE vendoring; the merge-time classifyGate aborts on any unclassified origin |
| T-39-04 | Spoofing | a ticket-purchase op surfaced as a confident API-invocable hit | mitigate | the frozen backing:'dom' default makes every payment op DOM-only / discovery-pending; the payment-op guard forbids any payment op being backing recipe/handler |
| T-39-06 | Tampering | a vendored slice emitting a forbidden field name (Wall 1) OR a ticket buy op under-stated as read | mitigate | the importer's recursive forbidden-field pre-scan throws before emit; verify-catalog-crosscheck.mjs fails the build on under-statement (buy_tickets class write via verb + {method:'POST'}) |
| T-39-SC | Tampering | npm/pip/cargo installs | accept | no new packages this plan (denylist JSON + gate edit + data-only vendoring + test edits; zod/tsx already devDeps); package-legitimacy gate N/A |
</threat_model>

<verification>
- `node -e` classify probe: ticketmaster/stubhub/eventbrite sensitive; yelp/tripadvisor/calendly safe; checkReadOnlySafeOrigins fails a yelp write + passes a yelp read.
- `node --import tsx ./scripts/import-opentabs-catalog.mjs` emits the 6 apps (all backing:'dom'); the gate did NOT abort; the payment-op + READ_ONLY_SAFE guards pass; seed-descriptors.json seed-fed.
- `npm run validate:extension` exits 0 (snapshot reconciled; classification gate + crosscheck + recipe-path guard green).
- `node scripts/verify-catalog-crosscheck.mjs && node tests/no-dead-entry.test.js && node tests/breadth-search-return.test.js && node tests/capability-search-eval.test.js` pass (ticket buy ops write + DOM-only-on-sensitive; read-only apps read-only-locked; ticket collisions wrong-invoke=0; recall@5>=0.9; cold-start within budget).
</verification>

<success_criteria>
- BRDTH-01: the 6 events/local-services/scheduling apps' descriptors are imported via the frozen machinery + returned by search with synonyms + side-effect class + backing:'dom'.
- BRDTH-02: the merge-time classifyGate passed only because the ticket origins were screened sensitive (Task 1) and the read-only origins are safe-benign; the payment-op CI guard passed (ticket ops DOM-only on sensitive); the READ_ONLY_SAFE guard locks the read-only apps to reads.
- BRDTH-03: every emitted descriptor is DOM-only (backing:'dom', invocable=false, discovery-pending); no ticket payment op armed via recipe/handler.
- No regression: no prior descriptor clobbered; crosscheck (incl. payment-op + READ_ONLY_SAFE guards) + no-dead-entry + the eval (recall@5>=0.9, wrong-invoke=0, cold-start within budget) green; validate:extension exits 0.
</success_criteria>

<output>
Create `.planning/phases/39-breadth-c-commerce-travel-misc-most-sensitive/39-05-SUMMARY.md` when done.
</output>
