---
phase: 39-breadth-c-commerce-travel-misc-most-sensitive
plan: 04
type: execute
wave: 4
depends_on: ["39-01", "39-03"]
files_modified:
  - vendor/opentabs-snapshot/plugins/booking/**
  - vendor/opentabs-snapshot/plugins/airbnb/**
  - vendor/opentabs-snapshot/plugins/expedia/**
  - vendor/opentabs-snapshot/plugins/kayak/**
  - vendor/opentabs-snapshot/plugins/opentable/**
  - vendor/opentabs-snapshot/PIN.md
  - catalog/descriptors/_fixtures/_provenance.json
  - catalog/descriptors/_fixtures/intent-cases.json
  - extension/catalog/recipe-index.generated.js
  - tests/breadth-search-return.test.js
  - tests/capability-search-eval.test.js
autonomous: true
requirements: [BRDTH-01, BRDTH-02, BRDTH-03]

must_haves:
  truths:
    - "The travel / transport sub-batch (booking, airbnb, expedia, kayak, opentable) is vendored as hermetic metadata-only slices and imported via the FROZEN importer (ZERO machinery edit beyond a STEM_OVERRIDES data-map entry where a host derives wrong) -- each returns from search with >=3 app-disambiguated synonyms + side-effect class + backing:'dom' per op"
    - "The merge-time classifyGate PASSES because 39-01 classified every paid-booking travel origin SENSITIVE -- each emitted descriptor's origin classifies sensitive; the payment-op CI guard PASSES (every complete_booking/book_stay/reserve op that moves money is backing:'dom' on a sensitive origin)"
    - "Each app's PAYMENT-bearing booking op (complete_booking/book_stay/book_flight/reserve_table-if-paid) classes write; cancel ops class destructive; search/availability/list_trips browsing classes read; every payment booking op is DOM-only on a sensitive origin (the payment-op guard confirms it). A pure-availability reservation op with NO money movement (opentable reserve_table, free) may class write but its origin is still sensitive (paid-booking adjacent) OR safe-read-only per 39-01 -- follow the 39-01 classification"
    - "The snapshot is regenerated via package-extension.mjs (INV-01 byte-stable); validate:extension exits 0; no prior descriptor clobbered"
    - "node tests/no-dead-entry.test.js stays exit 0; the eval keeps recall@5>=0.9 wrong-invoke=0.000 (incl. cross-app booking collision probes -- booking-vs-expedia-vs-airbnb book/reserve); the SW cold-start budget holds (flagged if approaching the gate)"
  artifacts:
    - path: "vendor/opentabs-snapshot/plugins/booking/src/index.ts"
      provides: "vendored booking.com metadata slice (booking.com SENSITIVE per 39-01; complete_booking is the DOM-only-on-sensitive payment-op-guard subject for travel)"
      contains: "BookingPlugin"
    - path: "extension/catalog/recipe-index.generated.js"
      provides: "the snapshot REGENERATED after the travel/transport sub-batch (INV-01 byte-stable)"
    - path: "tests/breadth-search-return.test.js"
      provides: "the per-op breadth assertion EXTENDED to the travel apps incl. cross-app book/reserve collision probes (wrong-invoke=0)"
      contains: "booking"
  key_links:
    - from: "scripts/import-opentabs-catalog.mjs enumerateBatchApps"
      to: "vendor/opentabs-snapshot/plugins/{booking,airbnb,expedia,kayak,opentable}"
      via: "the frozen enumeration auto-discovers the new dirs (no importer edit); each emits opentabs__<stem>__* descriptors backing:'dom'"
      pattern: "opentabs__(booking|airbnb|expedia|kayak|opentable)__"
    - from: "scripts/import-opentabs-catalog.mjs runImport classifyGate"
      to: "extension/config/service-denylist.json (39-01)"
      via: "every paid-booking travel origin classifies sensitive (39-01) -> the merge gate does NOT abort"
      pattern: "classifyGate"
    - from: "scripts/verify-catalog-crosscheck.mjs checkPaymentOpsNotSafeInvocable"
      to: "the emitted opentabs__<app>__complete_booking / __book_stay / __book_flight descriptors"
      via: "each paid-booking op is backing:'dom' on a sensitive origin -> the guard PASSES"
      pattern: "complete_booking|book_stay|book_flight|reserve|backing"
---

<objective>
Import the travel / transport sub-batch (booking, airbnb, expedia, kayak, opentable) as DATA-ONLY via
the FROZEN machinery (NO importer edit beyond a STEM_OVERRIDES data-map entry where a host derives
wrong), gated on the 39-01 payment screening + the 39-03 grown corpus. Paid-booking travel apps move
money on confirm (a reservation charges a card), so each was classified SENSITIVE in 39-01 and each
payment booking op must ship DOM-only on that sensitive origin. Reuses the FROZEN screen-then-import
contract + the per-wave gate re-assertion verbatim (the 39-02/03 structure is the proven template).

1. Vendor the 5 apps as hermetic metadata slices (Wall 1: metadata-only, mirror todoist EXACTLY). Each
   vendored urlPatterns host MUST equal the 39-01 screened origin. Real z.object inputs (destination,
   dates, guests, property/flight id, reservation id) -- NOT empty schemas. Explicit payment-booking
   write/destructive signals so the crosscheck classes them.
2. Run the FROZEN importer: enumerateBatchApps auto-discovers the dirs; classifyGate PASSES (all
   paid-booking origins sensitive via 39-01); every emitted descriptor is backing:'dom'; the payment-op
   CI guard PASSES (every paid-booking op DOM-only on a sensitive origin).
3. Regenerate the snapshot; validate:extension reconciles. Re-assert the per-wave gates (crosscheck incl.
   the payment-op guard + no-dead-entry + the eval incl. cross-app book/reserve collisions + cold-start).

Purpose: land the travel/transport category DOM-only on sensitive origins, proving the screen-then-
import contract + the triple mitigation hold for paid-booking money movement.
Output: 5 travel/transport apps' descriptors (all backing:'dom' on sensitive origins, paid-booking ops
DOM-only-not-invocable); regenerated snapshot; all per-wave gates green incl. the payment-op guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-breadth-c-commerce-travel-misc-most-sensitive/39-CONTEXT.md
@.planning/phases/39-breadth-c-commerce-travel-misc-most-sensitive/39-01-SUMMARY.md
@.planning/phases/39-breadth-c-commerce-travel-misc-most-sensitive/39-03-SUMMARY.md

<interfaces>
<!-- The FROZEN machinery + the 39-01 screening + the 39-02/03 import contract (the proven template). -->
- scripts/import-opentabs-catalog.mjs: enumerateBatchApps() auto-discovers each new vendored dir;
  backingFor() returns 'dom'; runImport() classifyGate-aborts on any unclassified origin AND
  feedSeedDescriptors upserts each slug. NO importer edit beyond a STEM_OVERRIDES data-map entry where the
  frozen host->stem derivation is wrong (e.g. 'www.booking.com'->'www' -> add { booking:'booking' }).
  CONFIRM the emitted stem is the brand before relying on it.
- side-effect verbs (scripts/lib/side-effect-class.mjs, FROZEN): book/complete/reserve/create/add ->
  WRITE; cancel/delete -> DESTRUCTIVE; list/get/search -> READ. Each payment booking write carries an
  explicit `api(..., { method:'POST' })`; each cancel `apiVoid(..., { method:'DELETE' })`; reads
  `api(..., { method:'GET' })`. Do NOT edit the shared module. (NOTE: 'reserve'/'book' is a WRITE verb;
  if the widened classification heuristic (39-01 Task 2a) used a bare 'book'/'reserve' token it could
  trip a pure-availability READ -- 39-01 used the SPECIFIC checkout/charge/place-order tokens instead, so
  a search/availability read does NOT trip the axis; confirm the read ops emit class read.)
- 39-01 classified these origins SENSITIVE: booking.com, airbnb.com, expedia.com, kayak.com (paid
  bookings); opentable.com classification follows 39-01 (sensitive if its reserve op is treated as a
  paid/held-card booking; safe-read-only if 39-01 left it safe -- read the 39-01 SUMMARY + classify()
  before vendoring). The import ABORTS if a vendored origin is not classified. The payment-op CI guard
  requires every paid-booking op to be backing:'dom' on a sensitive origin.
- The vendored-slice contract (replicate todoist EXACTLY): package.json + sdk-stub.ts (verbatim) +
  <app>-api.ts (inert stub) + index.ts + tools/<op>.ts. The 39-02/03 slices are same-category worked
  examples.
- breadth-search-return.test.js: widen the corpusFiles regex + raise the floor + add cross-app collision
  probes (booking-vs-expedia-vs-airbnb book_stay/complete_booking; kayak-vs-expedia flight search) all
  wrong-invoke=0.
- The eval + cold-start budget: the importer seed-feeds; keep generous gates; widen the cold-start byte
  budget ONLY if the grown index legitimately exceeds it while per-descriptor footprint stays flat, with
  a comment noting the grown count -- do NOT change the data layout; the Phase-43 gate stays separate.
  FLAG in the SUMMARY if approached.
</interfaces>

# Reuse references (the proven slice + import shapes -- do NOT rebuild):
@vendor/opentabs-snapshot/plugins/todoist/src/index.ts
@vendor/opentabs-snapshot/plugins/todoist/src/sdk-stub.ts
@vendor/opentabs-snapshot/plugins/todoist/src/todoist-api.ts
@vendor/opentabs-snapshot/plugins/todoist/src/tools/create-task.ts
@vendor/opentabs-snapshot/plugins/todoist/src/tools/list-tasks.ts
@vendor/opentabs-snapshot/plugins/todoist/src/tools/delete-task.ts
@vendor/opentabs-snapshot/PIN.md
@tests/breadth-search-return.test.js
@tests/capability-search-eval.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor the travel / transport metadata slices (booking, airbnb, expedia, kayak, opentable)</name>
  <files>vendor/opentabs-snapshot/plugins/{booking,airbnb,expedia,kayak,opentable}/** (each: package.json, src/sdk-stub.ts, src/index.ts, src/<app>-api.ts, src/tools/*.ts), vendor/opentabs-snapshot/PIN.md</files>
  <read_first>
    - vendor/opentabs-snapshot/plugins/todoist/ (the full slice structure to replicate)
    - vendor/opentabs-snapshot/plugins/booking OR a 39-02/03 slice (the same-category worked example: a payment write + reads + a cancel destructive)
    - vendor/opentabs-snapshot/plugins/todoist/src/tools/create-task.ts (write: api POST) + delete-task.ts (destructive: apiVoid DELETE) + list-tasks.ts (read: api GET)
    - scripts/import-opentabs-catalog.mjs (confirm each host's frozen-derived stem; add a STEM_OVERRIDES data-map entry where 'www.<brand>.com'->'www')
    - extension/config/service-denylist.json + .planning/phases/39-.../39-01-SUMMARY.md (CONFIRM each vendored origin's classification: sensitive for paid bookings; opentable per 39-01)
    - vendor/opentabs-snapshot/PIN.md (the SHA + MIT pin + the per-phase append-only log)
  </read_first>
  <action>
    Create the 5 vendored metadata slices mirroring the todoist slice structure EXACTLY (and the 39-02/03
    slices as same-category worked examples) -- Wall 1: metadata-only, NO dist/, NO executed handle(), NO
    runtime .js. For EACH app: package.json (opentabs.urlPatterns host below + _role + _note + MIT),
    src/sdk-stub.ts (VERBATIM copy of todoist's), src/index.ts (<App>Plugin), src/<app>-api.ts (inert
    transport stub), src/tools/<op>.ts per op (real z.object input + never-run handle naming the upstream
    helper+method).

    Hosts (authoritative urlPatterns[0]; MUST equal the 39-01 screened origin -- CONFIRM via classify()
    + the 39-01 SUMMARY before vendoring; add a STEM_OVERRIDES data-map entry where the frozen split('.')[0]
    is not the brand):
      - booking   -> "*://www.booking.com/*"   (service www.booking.com; STEM_OVERRIDE booking)
      - airbnb    -> "*://www.airbnb.com/*"     (service www.airbnb.com; STEM_OVERRIDE airbnb)
      - expedia   -> "*://www.expedia.com/*"    (service www.expedia.com; STEM_OVERRIDE expedia)
      - kayak     -> "*://www.kayak.com/*"      (service www.kayak.com; STEM_OVERRIDE kayak)
      - opentable -> "*://www.opentable.com/*"  (service www.opentable.com; STEM_OVERRIDE opentable)
    CONFIRM the emitted slug is opentabs__<brand>__* before relying on it; add the STEM_OVERRIDES entry if
    a stem comes out 'www'. The `service` field keeps the real host; only the stem/slug/filename is
    canonicalized.

    Ops per app (real z.object inputs; at least one PAYMENT-bearing booking write + reads):
      - booking:   search_stays (read), get_property (read), list_bookings (read), complete_booking
        (WRITE -- the payment op; api POST), cancel_booking (DESTRUCTIVE -- apiVoid DELETE).
      - airbnb:    search_listings (read), get_listing (read), list_trips (read), book_stay (WRITE -- the
        payment op; api POST), cancel_reservation (DESTRUCTIVE).
      - expedia:   search_flights (read), search_hotels (read), list_trips (read), book_flight (WRITE --
        the payment op; api POST), book_hotel (WRITE -- payment op; api POST).
      - kayak:     search_flights (read), search_hotels (read), get_price_alert (read), create_price_alert
        (WRITE; api POST). (kayak is primarily a META-SEARCH read app; its create_price_alert is a benign
        write -- it is NOT a payment op. If kayak has NO money-movement op, it has no payment op for the
        guard, but its origin is still classified per 39-01; confirm the read ops emit class read.)
      - opentable: search_restaurants (read), get_restaurant (read), list_reservations (read),
        reserve_table (WRITE -- a reservation; api POST), cancel_reservation (DESTRUCTIVE).
    Each payment booking write op carries an explicit `{ method:'POST' }` literal; each cancel
    `apiVoid(..., { method:'DELETE' })`; reads `api(..., { method:'GET' })`. Do NOT vendor any e2e/prescript
    fixture or any DENY-01 / money-movement denied app. Do NOT edit the importer (except a STEM_OVERRIDES
    data-map entry) or side-effect-class.mjs.

    NOTE on the payment-op guard interaction: a paid-booking write op (complete_booking/book_stay/
    book_flight) is a PAYMENT op for the guard -> it MUST be backing:'dom' on a sensitive origin (which the
    frozen default + 39-01 guarantee). A non-money write (kayak.create_price_alert) is NOT a payment op (it
    is not in the PAYMENT_OP token set) so the guard does not key on it -- but verify the guard's PAYMENT_OP
    set (39-01 Task 2b) does not accidentally include 'reserve'/'book' so broadly that a non-paid op trips;
    if reserve_table is a free reservation, 39-01 may have left opentable safe -- in that case
    reserve_table on a SAFE origin would be flagged by the guard ONLY IF reserve is a PAYMENT_OP token.
    Resolve consistently: either opentable is SENSITIVE (39-01) so reserve_table DOM-only-on-sensitive
    passes, OR reserve is NOT a PAYMENT_OP token (it is a free reservation) so the guard ignores it. Follow
    the 39-01 classification + PAYMENT_OP set; if a conflict surfaces (a free reserve op on a safe origin
    that the guard flags), STOP and reconcile in the SUMMARY rather than weakening the guard.

    Append the 5 apps to vendor/opentabs-snapshot/PIN.md's per-phase log as a Phase-39 row (batch C
    sub-batch 3: travel + transport; paid-booking -> sensitive origins, DOM-only by the frozen default).
  </action>
  <verify>
    <automated>node --import tsx -e "const apps=['booking','airbnb','expedia','kayak','opentable'];(async()=>{for(const a of apps){const m=await import('./vendor/opentabs-snapshot/plugins/'+a+'/src/index.ts');const p=m.default;if(!p||!Array.isArray(p.tools)||p.tools.length<4){console.error('bad slice',a);process.exit(1)}console.log(a,'->',(p.urlPatterns&&p.urlPatterns[0]),p.tools.map(t=>t.name).join(','))}process.exit(0)})().catch(e=>{console.error(e.message);process.exit(1)})" && node -e "const D=require('./extension/utils/service-denylist.js');D.load().then(()=>{const o=['https://www.booking.com','https://www.airbnb.com','https://www.expedia.com','https://www.kayak.com'];let ok=true;for(const x of o){const c=D.classify(x);if(!(c.sensitive===true||c.denied===false&&c.sensitive===false)){}if(c.denied===true){console.error('travel origin should not be denied:',x);ok=false}}const paid=['https://www.booking.com','https://www.airbnb.com','https://www.expedia.com'];for(const x of paid){if(D.classify(x).sensitive!==true){console.error('paid-booking origin NOT sensitive (39-01 must classify it):',x);ok=false}}if(!ok)process.exit(1);console.log('PASS paid-booking origins classify sensitive; travel origins not denied')})"</automated>
  </verify>
  <acceptance_criteria>
    - All 5 apps import under tsx with >=4 ops each; real z.object inputs; no handle() executes; provenance-scaffold green. The paid-booking apps (booking/airbnb/expedia) each carry a payment-bearing booking write.
    - Each vendored urlPatterns host equals the 39-01 screened origin (classify() sensitive for the paid-booking origins); the emitted stem is the brand (opentabs__booking__* etc.) -- a STEM_OVERRIDES data-map entry added per host that derives wrong.
    - Each payment booking write op carries {method:'POST'}; each cancel carries apiVoid {method:'DELETE'}; reads are {method:'GET'}.
    - No DENY-01 / money-movement denied app or fixture vendored; side-effect-class.mjs untouched; the only importer touch (if any) is a STEM_OVERRIDES data-map entry; any reserve/book vs PAYMENT_OP-set conflict is reconciled (not by weakening the guard).
  </acceptance_criteria>
  <done>The 5 travel/transport apps are vendored as hermetic metadata slices with correct hosts/stems (paid-booking origins classified sensitive in 39-01), each carrying its booking write op + reads with explicit transport signals; PIN.md per-phase log updated (Phase-39 batch C sub-batch 3).</done>
</task>

<task type="auto">
  <name>Task 2: Import the sub-batch, regenerate the snapshot, re-assert the per-wave gates (incl. the payment-op guard)</name>
  <files>extension/catalog/recipe-index.generated.js, catalog/descriptors/_fixtures/intent-cases.json, catalog/descriptors/_fixtures/_provenance.json, tests/breadth-search-return.test.js, tests/capability-search-eval.test.js</files>
  <read_first>
    - scripts/import-opentabs-catalog.mjs (the FROZEN enumerateBatchApps + runImport + classifyGate + feedSeedDescriptors -- RUN, do NOT edit beyond a STEM_OVERRIDES data-map entry from Task 1)
    - scripts/package-extension.mjs + scripts/validate-extension.mjs (regen + byte-compare; INV-01)
    - scripts/verify-catalog-crosscheck.mjs (the crosscheck + the payment-op guard from 39-01 -- RUN, do NOT edit)
    - tests/breadth-search-return.test.js (the corpusFiles regex ~line 77-79 + COLLISION_SET)
    - tests/capability-search-eval.test.js + tests/no-dead-entry.test.js (run, do NOT edit)
    - catalog/descriptors/_fixtures/intent-cases.json (the eval fixtures to grow)
    - .planning/phases/39-breadth-c-commerce-travel-misc-most-sensitive/39-03-SUMMARY.md (the proven import+regen+gate re-assert worked example)
  </read_first>
  <action>
    (a) Run `node --import tsx ./scripts/import-opentabs-catalog.mjs`. Confirm: opentabs__booking__* /
    __airbnb__* / __expedia__* / __kayak__* / __opentable__* appear with the brand stems; EVERY emitted
    descriptor carries backing:'dom'; the merge-time classifyGate PASSES (origins classified via 39-01);
    the payment-op CI guard PASSES (every paid-booking op DOM-only on a sensitive origin); all prior
    descriptors (incl. 39-02/03) untouched; seed-descriptors.json carries the new slugs.

    (b) Run `node scripts/package-extension.mjs` (INV-01 byte-stable IIFE/djb2); confirm `node
    scripts/validate-extension.mjs` exits 0.

    (c) Extend catalog/descriptors/_fixtures/intent-cases.json: a read ("search stays on booking" ->
    booking.search_stays; "find flights on kayak" -> kayak.search_flights), a payment booking write ("book
    a stay on airbnb" -> airbnb.book_stay; "complete my booking.com reservation" -> booking.complete_booking;
    "book a flight on expedia" -> expedia.book_flight), a destructive ("cancel my booking" ->
    booking.cancel_booking), expectedSlug aligned to the emitted real slugs. Extend
    tests/breadth-search-return.test.js: widen the corpusFiles regex, raise the floor, add cross-app
    collision probes to COLLISION_SET (booking-vs-expedia-vs-airbnb book/complete; kayak-vs-expedia flight
    search) all wrong-invoke=0. Do NOT hand-edit seed-descriptors.json.

    (d) Re-assert the per-wave gates: `node scripts/verify-catalog-crosscheck.mjs` (exit 0 -- paid-booking
    ops class write, cancels destructive, reads read; NO under-statement AND the payment-op guard passes
    because every paid-booking op is DOM-only on a sensitive origin), `node tests/no-dead-entry.test.js`
    (exit 0), and `node tests/capability-search-eval.test.js` (recall@5>=0.9, wrong-invoke=0.000 incl. the
    travel collisions; cold-start asserts pass). Widen the cold-start budget ONLY if the grown index
    legitimately exceeds the gate while per-descriptor footprint stays flat, with a comment noting the
    grown count -- do NOT change the data layout, do NOT defer the Phase-43 gate. FLAG in the SUMMARY if
    approached.

    Do NOT edit the importer (beyond Task-1's STEM_OVERRIDES entry), capability-search.js, the gates'
    core logic, or service-denylist.json.
  </action>
  <verify>
    <automated>node --import tsx ./scripts/import-opentabs-catalog.mjs && test -f catalog/descriptors/opentabs__booking__complete_booking.json && test -f catalog/descriptors/opentabs__airbnb__book_stay.json && test -f catalog/descriptors/opentabs__expedia__book_flight.json && node -e "const fs=require('fs');for(const s of ['opentabs__booking__complete_booking','opentabs__airbnb__book_stay','opentabs__expedia__book_flight']){const d=JSON.parse(fs.readFileSync('catalog/descriptors/'+s+'.json','utf8'));if(d.backing!=='dom'){console.error('paid-booking op NOT dom:',s,d.backing);process.exit(1)}if(d.sideEffectClass!=='write'&&d.sideEffectClass!=='destructive'){console.error('paid-booking op NOT write/destructive:',s,d.sideEffectClass);process.exit(1)}}console.log('PASS paid-booking ops are DOM-only writes')" && node scripts/package-extension.mjs && npm run validate:extension && node scripts/verify-catalog-crosscheck.mjs && node tests/no-dead-entry.test.js && node tests/breadth-search-return.test.js && node tests/capability-search-eval.test.js</automated>
  </verify>
  <acceptance_criteria>
    - The 5 apps' descriptors are emitted (opentabs__<brand>__*) and inlined; EVERY one carries backing:'dom'; the merge-time classifyGate did NOT abort; no prior descriptor clobbered.
    - validate:extension exits 0 (snapshot reconciled; classification gate + crosscheck + recipe-path guard green); INV-01 byte-stable.
    - verify-catalog-crosscheck.mjs exits 0: paid-booking ops class write/destructive AND the payment-op guard PASSES (every paid-booking op DOM-only on a sensitive origin).
    - no-dead-entry.test.js exits 0 over the grown corpus; breadth-search-return.test.js passes incl. the cross-app book/reserve collisions (wrong-invoke=0).
    - capability-search-eval.test.js prints recall@5>=0.9, wrong-invoke=0.000, cold-start asserts green (flagged in the SUMMARY if approaching the budget).
  </acceptance_criteria>
  <done>The 5 travel/transport apps are imported as DOM-only data via the frozen machinery (gate passed; every paid-booking op DOM-only on a sensitive origin per the payment-op guard), the snapshot regenerated (INV-01 byte-stable), and the per-wave gates re-asserted: crosscheck (incl. payment-op guard) + no-dead-entry + eval green and the cold-start budget held.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OpenTabs vendored metadata -> FSB descriptor corpus (build-time) | the travel/transport metadata; a forbidden field, a denied app, or an unscreened paid-booking origin must never enter the corpus |
| a paid-booking op -> the side-effect/backing crosscheck (build-time) | a complete_booking/book_stay/book_flight op must class write AND stay backing:'dom' on a sensitive origin -- never safe-and-API-invocable |
| screened sensitive paid-booking origin -> invoke consent gate (runtime) | each app's booking WRITE flows through the posture-B re-gate (the end-to-end proof on a REAL emitted descriptor is Plan 39-07) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | Elevation of Privilege | a paid-booking op (complete_booking/book_stay/book_flight) writable/invocable under Auto = a paid reservation booked / a card charged without consent | mitigate | every paid-booking op ships backing:'dom' (frozen default -> DOM-only, asserted in Task 2); the payment-op CI guard (39-01) passes only because each is DOM-only on a sensitive origin; each origin classified sensitive in 39-01 so its DOM-path write is posture-B gated |
| T-39-02 | Elevation of Privilege | a paid-booking origin landing unscreened -> its booking write runs under Auto with no re-gate | mitigate | the merge-time classifyGate aborts on any unclassified origin; Task 1 confirms each vendored origin == the 39-01 screened origin before vendoring; a mismatch aborts the import |
| T-39-04 | Spoofing | a paid-booking op surfaced as a confident API-invocable hit (a fabricated book_flight call) | mitigate | the frozen backing:'dom' default makes every paid-booking op DOM-only / discovery-pending; the payment-op guard forbids any payment op being backing recipe/handler; never auto-mint a recipe |
| T-39-06 | Tampering | a vendored slice emitting a forbidden field name (Wall 1) OR a paid-booking op under-stated as read | mitigate | the importer's recursive forbidden-field pre-scan (UNCHANGED) throws before emit; verify-catalog-crosscheck.mjs fails the build on under-statement (book_stay/complete_booking class write via verb + {method:'POST'}) |
| T-39-07 | Tampering | the payment-op guard weakened to accommodate a free reserve op (opentable) on a safe origin | mitigate | Task 1 reconciles a reserve/book vs PAYMENT_OP-set conflict by classification (opentable sensitive OR reserve not a PAYMENT_OP token), never by weakening the guard; the conflict is surfaced in the SUMMARY |
| T-39-SC | Tampering | npm/pip/cargo installs | accept | no new packages this plan (data-only vendoring + test edits; zod/tsx already devDeps); package-legitimacy gate N/A |
</threat_model>

<verification>
- `node --import tsx ./scripts/import-opentabs-catalog.mjs` emits the 5 apps (all backing:'dom'); the gate did NOT abort; the payment-op guard passes; seed-descriptors.json seed-fed.
- `npm run validate:extension` exits 0 (snapshot reconciled; classification gate + crosscheck + recipe-path guard green).
- `node scripts/verify-catalog-crosscheck.mjs && node tests/no-dead-entry.test.js && node tests/breadth-search-return.test.js && node tests/capability-search-eval.test.js` pass (paid-booking ops write + DOM-only-on-sensitive; every new slug resolves T3; book/reserve collisions wrong-invoke=0; recall@5>=0.9; cold-start within budget).
</verification>

<success_criteria>
- BRDTH-01: the 5 travel/transport apps' descriptors are imported via the frozen machinery + returned by search with synonyms + side-effect class + backing:'dom'.
- BRDTH-02: the merge-time classifyGate passed only because 39-01 screened the origins; the payment-op CI guard passed because every paid-booking op is DOM-only on a sensitive origin.
- BRDTH-03: every emitted descriptor is DOM-only (backing:'dom', invocable=false, discovery-pending), never confident-invocable, never learn-seeded; no paid-booking op armed via recipe/handler.
- No regression: no prior descriptor clobbered; crosscheck (incl. payment-op guard) + no-dead-entry + the eval (recall@5>=0.9, wrong-invoke=0, cold-start within budget) green; validate:extension exits 0.
</success_criteria>

<output>
Create `.planning/phases/39-breadth-c-commerce-travel-misc-most-sensitive/39-04-SUMMARY.md` when done.
</output>
