---
phase: 39-breadth-c-commerce-travel-misc-most-sensitive
plan: 07
type: execute
wave: 7
depends_on: ["39-01", "39-02", "39-03", "39-04", "39-05", "39-06"]
files_modified:
  - scripts/coverage-report.mjs
  - tests/coverage-report.test.js
  - tests/sensitive-write-import-gate.test.js
  - package.json
autonomous: true
requirements: [BRDTH-01, BRDTH-02, BRDTH-03]

must_haves:
  truths:
    - "scripts/coverage-report.mjs emits the discoverable-AND-invocable coverage breakdown across the full ~117-app real set -- head (hand-ported T1a/T1b handler/recipe) / learn-on-visit (T2-seeded) / DOM-only (T3) / dead (resolve() returns null) -- reading the committed catalog + resolve() tiers; it reports the per-app + per-descriptor counts and asserts ZERO dead entries (every searchable slug resolves to a non-null tier)"
    - "The coverage report confirms COMPLETE coverage of all real OpenTabs apps VERIFIED AGAINST the 39-06 remaining-real-app manifest (39-06-REMAINING-APPS.md): every VENDORED+IMPORTED manifest row is represented in the catalog; the e2e/prescript fixtures + the DENY-01/money-movement denied set are correctly EXCLUDED (matching the manifest's excluded-with-reason rows); and the commerce/travel/misc batch is DOM-only (every Phase-39 descriptor backing:'dom', invocable=false). A manifest row with no catalog descriptor FAILS the coverage test naming the gap"
    - "tests/sensitive-write-import-gate.test.js is EXTENDED to prove the payment-write screening END-TO-END on a REAL emitted PAYMENT descriptor (e.g. opentabs__doordash__place_order or opentabs__amazon__place_order, loaded from disk): classify(origin).sensitive===true; a payment WRITE without the per-origin mutating flag -> RECIPE_CONSENT_MUTATING_REQUIRED (posture B, dual-field byte-exact); the read op passes under Auto; the per-origin mutating flag elevates the write -- the headline mitigation proven on the descriptor that ACTUALLY SHIPS"
    - "The payment-op CI guard (39-01) PASSES over the COMPLETE corpus -- no payment op anywhere is safe-and-API-invocable (every payment op is backing:'dom' on a sensitive origin); the coverage report cross-confirms no payment descriptor has backing recipe/handler"
    - "The FULL-corpus gate re-assert is green: npm run validate:extension + node scripts/verify-catalog-crosscheck.mjs + node tests/no-dead-entry.test.js + node tests/capability-search-eval.test.js all exit 0 at the now-complete real-app corpus; the SW cold-start headroom is reported (flagged if near the budget -- the Phase-43 full-corpus gate is where it is closed)"
  artifacts:
    - path: "scripts/coverage-report.mjs"
      provides: "the full-corpus coverage report (success criterion 3): head/learn-on-visit/DOM-only/dead breakdown across the ~117-app real set, reading the committed catalog + resolve() tiers; dual-export (a reportCoverage() function + a CLI) so the test drives the real function"
      contains: "reportCoverage"
    - path: "tests/coverage-report.test.js"
      provides: "the coverage-report proof: zero dead entries; complete real-app coverage VERIFIED AGAINST the 39-06 remaining-app manifest; the commerce/travel/misc batch is DOM-only; the head/learn/dom/dead counts are reported"
      contains: "reportCoverage"
    - path: "tests/sensitive-write-import-gate.test.js"
      provides: "the END-TO-END payment-write proof EXTENDED to a REAL emitted PAYMENT descriptor (the headline money-no-movement-under-Auto mitigation on a shipped payment descriptor)"
      contains: "place_order"
  key_links:
    - from: "scripts/coverage-report.mjs reportCoverage"
      to: "extension/catalog/recipe-index.generated.js (FsbRecipeIndex.descriptors) + extension/utils/capability-catalog.js resolve()"
      via: "reads each descriptor's backing + drives resolve() to bucket head(T1a/T1b)/learn(T2)/dom(T3)/dead(null) across the full corpus"
      pattern: "reportCoverage|resolve|backing"
    - from: "tests/coverage-report.test.js"
      to: ".planning/phases/39-.../39-06-REMAINING-APPS.md (the manifest)"
      via: "the coverage test verifies realAppCount covers every VENDORED+IMPORTED manifest row -- a missing one fails naming the gap"
      pattern: "REMAINING-APPS|realAppCount"
    - from: "tests/sensitive-write-import-gate.test.js"
      to: "extension/utils/capability-router.js _evaluateConsent + catalog/descriptors/opentabs__<app>__place_order.json"
      via: "a REAL emitted payment WRITE descriptor (service a sensitive commerce origin, sideEffectClass write) routed through the live posture-B re-gate -> RECIPE_CONSENT_MUTATING_REQUIRED without the flag"
      pattern: "RECIPE_CONSENT_MUTATING_REQUIRED|place_order"
    - from: "scripts/verify-catalog-crosscheck.mjs checkPaymentOpsNotSafeInvocable"
      to: "the complete commerce/travel/misc payment descriptor set"
      via: "the payment-op guard passes over the complete corpus -- no payment op is safe-and-API-invocable"
      pattern: "checkPaymentOpsNotSafeInvocable"
---

<objective>
Close Phase 39: emit the FULL-CORPUS COVERAGE REPORT (success criterion 3), prove the payment-write
screening END-TO-END on a REAL emitted payment descriptor (the headline mitigation, on a shipped
descriptor), and re-assert the full-corpus gate over the now-complete real-app catalog. This is the
phase's verification + reporting close — no new app import (39-02..06 completed coverage):

1. **The coverage report (success criterion 3):** create scripts/coverage-report.mjs (dual-export:
   reportCoverage() + a CLI) that reads the committed catalog (FsbRecipeIndex.descriptors) and drives
   capability-catalog.js resolve() to bucket every real-app descriptor into head (T1a/T1b handler/recipe)
   / learn-on-visit (T2-seeded) / DOM-only (T3) / dead (resolve() returns null) across the ~117-app real
   set. It reports the per-app + per-descriptor counts, asserts ZERO dead entries, confirms COMPLETE
   real-app coverage VERIFIED AGAINST the 39-06 remaining-app manifest (the e2e/prescript fixtures + the
   DENY-01/money-movement denied set correctly excluded), and confirms the commerce/travel/misc batch is
   DOM-only.

2. **The END-TO-END payment-write proof on a SHIPPED descriptor (the headline):** extend
   tests/sensitive-write-import-gate.test.js (the proven discord pattern) to a REAL emitted PAYMENT
   descriptor (opentabs__doordash__place_order / opentabs__amazon__place_order, loaded from disk):
   classify(origin).sensitive===true; the payment WRITE without the per-origin mutating flag ->
   RECIPE_CONSENT_MUTATING_REQUIRED (posture B, dual-field byte-exact, INV-03); the read op passes under
   Auto; the per-origin mutating flag elevates the write — money-no-movement-under-Auto proven on the
   descriptor that actually ships.

3. **The full-corpus gate re-assert:** validate:extension + crosscheck (incl. the payment-op guard) +
   no-dead-entry + the eval all green over the complete real-app corpus; the SW cold-start headroom
   reported (flagged if near the budget — Phase-43 closes the full-corpus scale gate).

Purpose: deliver the reportable head/learn/dom/dead breakdown (the phase's third success criterion) AND
prove the headline payment-write mitigation on a real shipped payment descriptor AND confirm the complete
real-app corpus stays green — the phase is done when these hold.
Output: the coverage-report script + test; the end-to-end payment-write proof; the full-corpus gate
re-assert; all registered in CI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-breadth-c-commerce-travel-misc-most-sensitive/39-CONTEXT.md
@.planning/phases/39-breadth-c-commerce-travel-misc-most-sensitive/39-01-SUMMARY.md
@.planning/phases/39-breadth-c-commerce-travel-misc-most-sensitive/39-06-SUMMARY.md

<interfaces>
<!-- The catalog/resolve()/consent-gate surfaces the coverage report + the end-to-end proof read. -->
- extension/catalog/recipe-index.generated.js: the IIFE assigns global.FsbRecipeIndex = { descriptors:
  [...], recipes: [...], version } and dual-exports it via module.exports. Each descriptor carries
  { slug, service, intentSynonyms, description, actionVerb, sideEffectClass, params, backing, provenance }.
  require() it (or read it as the catalog source) to enumerate the full committed descriptor set.
- extension/utils/capability-catalog.js resolve(slug, origin) -> { tier, handler|recipe|descriptor } or
  null. The tiers: T1a (head handler -- HEAD_HANDLER_MODULES: github/slack/notion), T1b/T0 (bundled
  recipe), T2 (learn-pending, backing:'learn'), T3 (DOM-fallback, backing:'dom' or absent), null (dead --
  genuinely unknown slug). The CGEN-03 fallback (line ~347) maps a descriptor-only slug to T2 (backing
  'learn') or T3 (backing 'dom'/absent). require capability-catalog.js to call resolve(); plant
  global.FsbRecipeIndex first (the catalog reads _getDescriptor from it) -- the no-dead-entry.test.js
  pattern is the template. seedHeadHandlers() registers the head slugs as T1a (call it after planting the
  head handler globals, OR just bucket by backing: 'handler' -> head, 'recipe' -> head, 'learn' -> learn,
  'dom'/absent -> dom, and cross-check a sample via resolve()).
- THE 39-06 REMAINING-APP MANIFEST (.planning/phases/39-.../39-06-REMAINING-APPS.md): the derived
  checklist 39-06 produced -- every real-app dir under vendor/opentabs-snapshot/plugins/* MINUS the
  e2e/prescript fixtures MINUS the DENY-01/money-movement denied set MINUS the already-imported set, with a
  disposition per row (VENDORED+IMPORTED | EXCLUDED-with-reason). The coverage test reads this manifest (or
  its enumerated VENDORED+IMPORTED app list) and asserts realAppCount covers every VENDORED+IMPORTED row.
  A manifest row that has NO matching catalog descriptor is a coverage hole -> the test FAILS naming it
  (this is the completeness contract -- the manifest is the source of truth for "what must be covered",
  not a hand-maintained number).
- The coverage BUCKETS (the head/learn/dom/dead breakdown, success criterion 3):
    head        = descriptors whose backing is 'handler' OR 'recipe' (day-one invocable: the 3 heads +
                  any hand-authored recipe like reddit-inbox.json) -- resolve() returns T1a/T1b/T0.
    learn       = descriptors whose backing is 'learn' (T2-seeded; none in breadth today -- discovery is
                  Phase 42 -- so this bucket is ~0 now, reported for completeness).
    dom         = descriptors whose backing is 'dom' or absent (T3 DOM-only; the entire breadth corpus
                  incl. the commerce/travel/misc batch) -- resolve() returns T3.
    dead        = any searchable slug whose resolve() returns null (MUST be 0 -- the no-dead-entry
                  invariant; the report asserts this).
  Report per-app (group by service) AND per-descriptor totals; print the ~117-app real-app count
  (distinct services across the opentabs__ corpus + the heads + hand-authored recipes).
- tests/no-dead-entry.test.js: the generalized opentabs__* loader + resolve() invariant -- the proven
  pattern for planting FsbRecipeIndex + driving resolve(); mirror its setup in coverage-report.mjs/test.
- tests/sensitive-write-import-gate.test.js (the file THIS plan extends): the proven END-TO-END pattern --
  installChromeStorageStub() + require consent-policy-store + capability-router (plants FsbConsentGate) +
  await Denylist.load() against the committed roster + assign globalThis.FsbServiceDenylist + load a REAL
  emitted descriptor from disk + drive it through Gate.evaluate. Today it proves discord.send_message;
  EXTEND it (a new block) to a REAL emitted PAYMENT descriptor. The descriptor's sideEffectClass is READ
  FROM THE LOADED JSON (not a hardcoded literal). FsbConsentGate.evaluate({origin, slug, method?,
  entry:{tier, descriptor:{sideEffectClass}}}) -> for classify().sensitive + mode 'auto', a WRITE without
  the per-origin mutating flag -> { decision:(not 'allow'), error:{code/errorCode/error:
  'RECIPE_CONSENT_MUTATING_REQUIRED'} }; a READ -> 'allow'; setOriginMutating(origin,true) elevates.
- extension/utils/consent-policy-store.js: setOriginMode(origin,'auto') + setOriginMutating(origin,bool)
  + _reset(). The store the consent test drives (plant the in-memory chrome.storage stub it reads).
- THE PAYMENT DESCRIPTOR to prove on: pick a REAL emitted payment WRITE descriptor whose origin is
  sensitive -- opentabs__doordash__place_order.json (service www.doordash.com, sideEffectClass write,
  backing dom) is a clean choice (39-02); opentabs__amazon__place_order.json (39-03) is an alternative.
  Also load its app's read op (e.g. opentabs__doordash__list_orders.json) for the read-allows assertion.
- THE TEST CONVENTION (zero-framework, ASCII-only, NO emojis): a passed/failed counter + check(cond,msg)
  + process.exit(failed>0?1:0). no-dead-entry.test.js + sensitive-write-import-gate.test.js are templates.
- package.json "test" chain: register the new coverage-report test. The coverage-report CLI may also be
  chained into validate:extension if it reads naturally there, but the registered TEST is the gate.
</interfaces>

# Reuse references (the proven resolve()/coverage + consent-gate-proof shapes -- do NOT rebuild):
@extension/utils/capability-catalog.js
@tests/no-dead-entry.test.js
@tests/sensitive-write-import-gate.test.js
@tests/consent-mutation-gate.test.js
@extension/utils/service-denylist.js
@extension/utils/consent-policy-store.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the full-corpus coverage report (head/learn-on-visit/DOM-only/dead breakdown across ~117 apps) + its test (completeness verified against the 39-06 manifest)</name>
  <files>scripts/coverage-report.mjs, tests/coverage-report.test.js, package.json</files>
  <read_first>
    - extension/utils/capability-catalog.js (resolve() + the tiers + the CGEN-03 descriptor-only fallback line ~347 + HEAD_HANDLER_MODULES + seedHeadHandlers + _getDescriptor reading FsbRecipeIndex)
    - tests/no-dead-entry.test.js (the proven pattern: plant global.FsbRecipeIndex from the committed catalog, require capability-catalog.js, drive resolve() over every opentabs__ slug, assert non-null)
    - extension/catalog/recipe-index.generated.js (the FsbRecipeIndex shape: descriptors[] + recipes[] + the dual module.exports tail -- the catalog source to read)
    - .planning/phases/39-breadth-c-commerce-travel-misc-most-sensitive/39-06-REMAINING-APPS.md (the remaining-app manifest 39-06 produced -- the VENDORED+IMPORTED rows are the completeness source of truth the coverage test verifies realAppCount against)
    - catalog/descriptors/reddit-inbox.json (a hand-authored backing:'recipe' descriptor -- a 'head' bucket member; confirm the bucketing includes hand-authored recipes)
    - package.json (the "test" chain -- register tests/coverage-report.test.js after node tests/sensitive-write-import-gate.test.js)
  </read_first>
  <action>
    Create scripts/coverage-report.mjs (dual-export idiom, mirroring verify-classification-gate.mjs: a
    reportCoverage() function + a CLI guarded by the import.meta.url === pathToFileURL(process.argv[1])
    check; ASCII, no emojis; Wall-1-clean -- build tooling, no dynamic-code constructs in code or comments).

    reportCoverage(catalog) -> { totals, byApp, deadSlugs, realAppCount } where catalog is the committed
    FsbRecipeIndex ({ descriptors, recipes }). For each descriptor:
      - bucket by backing: 'handler'|'recipe' -> head; 'learn' -> learn; 'dom'|absent -> dom.
      - CROSS-CHECK a sample (or every) slug via capability-catalog.js resolve(slug, 'https://'+service):
        a 'head' descriptor should resolve T1a/T1b/T0; a 'learn' -> T2; a 'dom' -> T3; a resolve()===null
        is a DEAD entry (push to deadSlugs). Plant global.FsbRecipeIndex = catalog before requiring
        capability-catalog.js (the _getDescriptor read path), mirroring no-dead-entry.test.js; call
        seedHeadHandlers() after planting the head handler globals if you bucket the heads via resolve()
        rather than by backing (either approach -- document which).
      - group counts by service (per-app) AND sum the per-descriptor totals { head, learn, dom, dead }.
      - realAppCount = the count of DISTINCT real apps represented (distinct services across opentabs__*
        + the heads github/slack/notion + hand-authored-recipe services), reported alongside the ~117
        milestone target so completeness is visible.
    The CLI: read the committed catalog (require extension/catalog/recipe-index.generated.js), call
    reportCoverage, PRINT a human-readable breakdown (the per-app table + the totals + the realAppCount vs
    ~117 + the commerce/travel/misc DOM-only confirmation), and process.exit(1) if deadSlugs.length > 0
    (zero-dead-entry is a hard assertion) else exit(0).

    Create tests/coverage-report.test.js (zero-framework: check(cond,msg) + process.exit(failed>0?1:0))
    driving the REAL reportCoverage export over the committed catalog:
      - assert totals.dead === 0 (every searchable slug resolves to a non-null tier -- the no-dead-entry
        invariant, cross-confirmed here over the complete corpus).
      - assert totals.dom > 0 and that EVERY commerce/travel/misc descriptor (slug opentabs__<app>__* for
        the Phase-39 apps -- doordash/amazon/booking/ticketmaster/dominos/... ) is in the dom bucket
        (backing:'dom', invocable=false) -- the batch is DOM-only.
      - assert totals.head includes the 3 heads (github/slack/notion) + the hand-authored recipes
        (reddit-inbox) -- the head bucket is correct.
      - COMPLETENESS VERIFIED AGAINST THE 39-06 MANIFEST: read the VENDORED+IMPORTED app list from
        .planning/phases/39-.../39-06-REMAINING-APPS.md (parse the manifest's VENDORED+IMPORTED rows -- each
        names an app/service) AND assert that for EVERY such row the catalog has at least one descriptor for
        that service (i.e. the manifest's covered set is a subset of byApp). If ANY VENDORED+IMPORTED
        manifest row has no catalog descriptor, FAIL naming the missing app (a real app on the manifest that
        is not in the catalog is a coverage hole the import plans must close -- do NOT pass a silent gap).
        Also assert realAppCount covers all real apps the snapshot vendors (every vendored dir except the
        e2e/prescript fixtures + the DENY-01/money-movement denied set, matching the manifest); print the
        count vs the ~117 target.
      - assert NO descriptor in the dom bucket whose op is a payment op has backing recipe/handler (cross-
        confirm the payment-op guard: no payment op is invocable).
    Register tests/coverage-report.test.js in package.json "test" after node
    tests/sensitive-write-import-gate.test.js (do NOT remove any existing test).
  </action>
  <verify>
    <automated>node scripts/coverage-report.mjs && node tests/coverage-report.test.js && grep -q coverage-report package.json && grep -Eq "REMAINING-APPS|39-06" tests/coverage-report.test.js && echo COVERAGE-REPORT-OK</automated>
  </verify>
  <acceptance_criteria>
    - scripts/coverage-report.mjs dual-exports reportCoverage() + has a CLI; it reads the committed catalog + drives resolve() and prints the head/learn-on-visit/DOM-only/dead breakdown per-app + totals + the realAppCount vs ~117.
    - reportCoverage asserts ZERO dead entries (every searchable slug resolves to a non-null tier); the CLI exits 1 if any dead entry exists.
    - tests/coverage-report.test.js passes: totals.dead===0; every Phase-39 commerce/travel/misc descriptor is DOM-only (backing:'dom', in the dom bucket); the head bucket includes the 3 heads + the hand-authored recipes; the coverage is VERIFIED AGAINST the 39-06 manifest (every VENDORED+IMPORTED manifest row has a catalog descriptor, else FAIL naming the gap); realAppCount covers all real apps the snapshot vendors (complete coverage, no silent gap); no payment op is backing recipe/handler.
    - tests/coverage-report.test.js is registered in package.json "test"; no existing test removed.
  </acceptance_criteria>
  <done>The full-corpus coverage report (success criterion 3) is reportable: the head/learn-on-visit/DOM-only/dead breakdown across the ~117-app real set, with zero dead entries asserted, complete real-app coverage VERIFIED AGAINST the 39-06 remaining-app manifest (every VENDORED+IMPORTED row covered, gaps named), and the commerce/travel/misc batch confirmed DOM-only -- emitted by scripts/coverage-report.mjs and proven by tests/coverage-report.test.js.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the payment-write screening END-TO-END on a REAL emitted payment descriptor + re-assert the full-corpus gate</name>
  <files>tests/sensitive-write-import-gate.test.js</files>
  <read_first>
    - tests/sensitive-write-import-gate.test.js (the proven discord END-TO-END block: installChromeStorageStub + require consent-policy-store + capability-router + await Denylist.load() + assign globalThis.FsbServiceDenylist + load the REAL emitted descriptor from disk + drive Gate.evaluate; the dual-field RECIPE_CONSENT_MUTATING_REQUIRED assertion + the flag-elevates assertion -- the EXACT template to mirror for a payment descriptor)
    - extension/utils/capability-router.js (_evaluateConsent step-(3.5) posture-B re-gate -- the live code the payment write flows through; reads classify() at call time)
    - extension/utils/consent-policy-store.js (setOriginMode / setOriginMutating / _reset)
    - extension/utils/service-denylist.js (load() + classify() -- await load() against the committed roster so the payment origin classifies sensitive)
    - catalog/descriptors/opentabs__doordash__place_order.json + opentabs__doordash__list_orders.json (the REAL emitted payment WRITE + read descriptors this test loads from disk -- confirm service www.doordash.com, the write op sideEffectClass write, backing dom)
    - scripts/verify-catalog-crosscheck.mjs + tests/no-dead-entry.test.js + tests/capability-search-eval.test.js (the full-corpus gates to re-run -- do NOT edit)
  </read_first>
  <action>
    (a) EXTEND tests/sensitive-write-import-gate.test.js: add a NEW block (mirroring the existing discord
    END-TO-END block EXACTLY) that proves the payment-write screening on a REAL emitted PAYMENT descriptor.
    With the LIVE committed Denylist loaded (await Denylist.load() against the committed roster -- NOT a
    fabricated classify; assign globalThis.FsbServiceDenylist) and a payment origin (www.doordash.com):
      - assert classify('https://www.doordash.com').sensitive===true (the 39-01 screening governs it -- the
        write-gating precondition).
      - load the REAL emitted opentabs__doordash__place_order.json + opentabs__doordash__list_orders.json
        from disk; assert place_order's service==='www.doordash.com', sideEffectClass==='write',
        backing==='dom' (the actual shipped payment descriptor, DOM-only -- AND its write is posture-B
        gated), and list_orders sideEffectClass==='read'.
      - set doordash mode 'auto' + mutating false; route the descriptors through the live gate driving each
        descriptor's REAL sideEffectClass (read from the loaded JSON) into entry.descriptor.sideEffectClass:
          - the READ (list_orders) -> decision==='allow' (reads run under Auto).
          - the payment WRITE (place_order) WITHOUT the flag -> decision!=='allow' AND
            error.code===error.errorCode===error.error==='RECIPE_CONSENT_MUTATING_REQUIRED' (INV-03
            dual-field; the descriptor's sideEffectClass 'write' triggers the re-gate even with no method).
          - then setOriginMutating('https://www.doordash.com', true) -> the same payment write returns
            decision==='allow' (the per-origin mutating flag elevates the sensitive payment write).
    Keep the existing discord block UNCHANGED (it stays as the comms/social canary). This proves the
    headline money-no-movement-under-Auto mitigation on the descriptor that ACTUALLY SHIPS for the
    payment-bearing category -- closing the loop on the most catastrophic threat of this batch. (The test
    is ALREADY registered in package.json -- no new registration.)

    (b) Re-assert the FULL-corpus gate (the phase close): run `npm run validate:extension` (exit 0 --
    snapshot reconciled; classification gate + crosscheck [incl. the payment-op guard over the complete
    corpus] + recipe-path guard green), `node scripts/verify-catalog-crosscheck.mjs` (exit 0 -- no payment
    op anywhere is safe-and-API-invocable; no under-statement; READ_ONLY_SAFE holds), `node
    tests/no-dead-entry.test.js` (exit 0 -- zero dead entries over the complete corpus), and `node
    tests/capability-search-eval.test.js` (recall@5>=0.9, wrong-invoke=0.000; the SW cold-start asserts).
    Report the cold-start headroom at the complete-corpus size in the SUMMARY (flag if near the budget --
    the Phase-43 full-corpus SCALE gate is where it is closed; do NOT change the data layout here). Do NOT
    edit the gates, the importer, capability-search.js, or service-denylist.json.
  </action>
  <verify>
    <automated>node tests/sensitive-write-import-gate.test.js && grep -q "doordash__place_order\|place_order" tests/sensitive-write-import-gate.test.js && npm run validate:extension && node scripts/verify-catalog-crosscheck.mjs && node tests/no-dead-entry.test.js && node tests/capability-search-eval.test.js && node scripts/coverage-report.mjs && echo PHASE-39-CLOSE-OK</automated>
  </verify>
  <acceptance_criteria>
    - sensitive-write-import-gate.test.js passes with the NEW payment block: it loads the REAL emitted opentabs__doordash__place_order.json (asserts service www.doordash.com, sideEffectClass write, backing dom) + the read op; with the LIVE committed roster (classify sensitive), the read -> 'allow'; the payment write without the per-origin mutating flag -> RECIPE_CONSENT_MUTATING_REQUIRED (dual-field byte-exact); setOriginMutating(true) elevates the write to 'allow'.
    - The descriptor's sideEffectClass is read from the loaded JSON (not a hardcoded literal) so the proof exercises the real emitted payment class; the existing discord block stays unchanged.
    - npm run validate:extension + node scripts/verify-catalog-crosscheck.mjs + node tests/no-dead-entry.test.js + node tests/capability-search-eval.test.js + node scripts/coverage-report.mjs all exit 0 over the complete real-app corpus.
    - The SW cold-start headroom at the complete-corpus size is reported in the SUMMARY (flagged if near the budget; the data layout is unchanged; Phase-43 closes the full-corpus scale gate).
  </acceptance_criteria>
  <done>The payment-write screening is proven END-TO-END on a REAL emitted payment descriptor (opentabs__doordash__place_order) through the live posture-B consent gate (read allows, payment write re-gated to RECIPE_CONSENT_MUTATING_REQUIRED, flag elevates) -- the headline money-no-movement-under-Auto mitigation holds on the descriptor that actually ships; and the full-corpus gate is green over the complete real-app catalog (validate:extension + crosscheck incl. the payment-op guard + no-dead-entry + the eval + the coverage report all exit 0, cold-start headroom reported).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the committed catalog -> resolve() tier bucketing (build-time report) | the coverage report must surface any dead entry (a searchable slug resolving to null) and any payment op that is invocable -- a reporting blind spot would hide a money-movement risk |
| screened sensitive payment origin -> invoke consent gate (runtime) | a REAL emitted payment WRITE descriptor must re-enforce the per-origin mutating opt-in (posture B) before executing -- the headline mitigation, proven end-to-end on a shipped descriptor |
| the complete real-app corpus -> the full-corpus gate (build-time) | validate:extension + crosscheck + no-dead-entry + eval must stay green at the largest corpus before Phase-43 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | Elevation of Privilege | a REAL shipped payment WRITE descriptor (place_order) executing under Auto without the mutating opt-in -> money moved (an order placed) without consent -- THE headline threat, end-to-end | mitigate | the payment origin classified sensitive (39-01); the live posture-B re-gate returns RECIPE_CONSENT_MUTATING_REQUIRED on the EMITTED payment descriptor without the flag -- proven on the shipped descriptor (Task 2), not a hand-built call; the coverage report cross-confirms no payment op is invocable |
| T-39-10 | Information Disclosure | a dead descriptor (searchable slug resolving to null) hiding in the complete corpus -> a RECIPE_NOT_FOUND surprise / an unreported coverage hole | mitigate | the coverage report asserts totals.dead===0 over the complete corpus (Task 1) AND no-dead-entry.test.js re-asserts the invariant (Task 2); a dead entry fails both |
| T-39-04 | Spoofing | a payment op slipping into the head/learn bucket (backing recipe/handler) -> a confident API-invocable payment hit | mitigate | the coverage report asserts no payment op is backing recipe/handler (Task 1); the payment-op CI guard (39-01) re-asserted over the complete corpus (Task 2) fails the build on any payment-safe-invocable op |
| T-39-11 | Tampering | a coverage report that silently undercounts (a missing real app passing as "complete") | mitigate | the coverage test reads the 39-06 manifest's VENDORED+IMPORTED rows and FAILS naming the gap if any row has no catalog descriptor (Task 1) -- a missing real app is a coverage hole verified against the manifest, not a silent pass |
| T-39-SC | Tampering | npm/pip/cargo installs | accept | no new packages this plan (a report script + test edits; zod/tsx already devDeps); package-legitimacy gate N/A. Phase 39 added NO new packages across all plans (denylist + gates + metadata vendoring + tests only) |
</threat_model>

<verification>
- `node scripts/coverage-report.mjs` exits 0 and prints the head/learn-on-visit/DOM-only/dead breakdown across the ~117-app real set (zero dead entries; commerce/travel/misc DOM-only).
- `node tests/coverage-report.test.js` passes: totals.dead===0; the Phase-39 batch is DOM-only; the head bucket is correct; complete real-app coverage verified against the 39-06 manifest (no silent gap); no payment op invocable.
- `node tests/sensitive-write-import-gate.test.js` passes: the REAL emitted opentabs__doordash__place_order descriptor is posture-B write-gated (read allows, payment write -> RECIPE_CONSENT_MUTATING_REQUIRED, flag elevates); the discord block unchanged.
- `npm run validate:extension && node scripts/verify-catalog-crosscheck.mjs && node tests/no-dead-entry.test.js && node tests/capability-search-eval.test.js` all exit 0 over the complete real-app corpus (the payment-op guard holds; recall@5>=0.9; wrong-invoke=0; cold-start headroom reported).
</verification>

<success_criteria>
- Success criterion 3 (coverage report): the discoverable-AND-invocable head/learn-on-visit/DOM-only/dead breakdown is reportable across the full ~117-app real set, with zero dead entries and complete real-app coverage verified against the 39-06 manifest.
- The headline mitigation (end-to-end): the REAL emitted payment WRITE descriptor (opentabs__doordash__place_order) is posture-B write-gated through the live consent gate (RECIPE_CONSENT_MUTATING_REQUIRED without the flag; read allows; flag elevates) -- money-no-movement-under-Auto proven on a shipped payment descriptor.
- The payment-op CI guard holds over the COMPLETE corpus: no payment op anywhere is safe-and-API-invocable (every payment op is DOM-only on a sensitive origin); the coverage report cross-confirms no payment op is invocable.
- Full-corpus gate green: validate:extension + crosscheck + no-dead-entry + the eval + the coverage report all exit 0 at the complete real-app corpus; the SW cold-start headroom reported (Phase-43 closes the full-corpus scale gate).
</success_criteria>

<output>
Create `.planning/phases/39-breadth-c-commerce-travel-misc-most-sensitive/39-07-SUMMARY.md` when done.
</output>
