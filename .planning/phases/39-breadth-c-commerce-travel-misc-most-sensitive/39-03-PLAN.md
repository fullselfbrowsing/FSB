---
phase: 39-breadth-c-commerce-travel-misc-most-sensitive
plan: 03
type: execute
wave: 3
depends_on: ["39-01", "39-02"]
files_modified:
  - vendor/opentabs-snapshot/plugins/amazon/**
  - vendor/opentabs-snapshot/plugins/ebay/**
  - vendor/opentabs-snapshot/plugins/etsy/**
  - vendor/opentabs-snapshot/plugins/bestbuy/**
  - vendor/opentabs-snapshot/plugins/costco/**
  - vendor/opentabs-snapshot/plugins/walmart/**
  - vendor/opentabs-snapshot/plugins/target/**
  - vendor/opentabs-snapshot/PIN.md
  - catalog/descriptors/_fixtures/_provenance.json
  - catalog/descriptors/_fixtures/intent-cases.json
  - extension/catalog/recipe-index.generated.js
  - tests/breadth-search-return.test.js
  - tests/capability-search-eval.test.js
autonomous: true
requirements: [BRDTH-01, BRDTH-02, BRDTH-03]

must_haves:
  truths:
    - "The retail / marketplace sub-batch (amazon, ebay, etsy, bestbuy, costco, walmart, target) is vendored as hermetic metadata-only slices and imported via the FROZEN importer (ZERO machinery edit beyond a STEM_OVERRIDES data-map entry where a host derives wrong) -- each returns from search with >=3 app-disambiguated synonyms + side-effect class + backing:'dom' per op"
    - "The merge-time classifyGate PASSES because 39-01 classified every retail origin SENSITIVE (checkout/place-order bearing) -- each emitted descriptor's origin classifies sensitive; the payment-op CI guard PASSES (every place_order/checkout op is backing:'dom' on a sensitive origin -- DOM-only, not API-invocable)"
    - "Each app's PAYMENT-bearing op (place_order/add_to_cart->checkout/buy_now) classes write; cancel/remove ops class destructive; product/order browsing (search_products/list_orders/track_order) classes read; every payment op is DOM-only on a sensitive origin (the payment-op guard confirms it)"
    - "The snapshot is regenerated via package-extension.mjs (INV-01 byte-stable); validate:extension exits 0; no prior descriptor clobbered"
    - "node tests/no-dead-entry.test.js stays exit 0; the eval keeps recall@5>=0.9 wrong-invoke=0.000 (incl. cross-app retail place_order collision probes -- amazon-vs-walmart-vs-target buy/checkout); the SW cold-start budget holds (flagged if approaching the gate)"
  artifacts:
    - path: "vendor/opentabs-snapshot/plugins/amazon/src/index.ts"
      provides: "vendored amazon metadata slice (amazon.com SENSITIVE per 39-01; the place_order payment op is DOM-only-on-sensitive -- the payment-op-guard subject for retail)"
      contains: "AmazonPlugin"
    - path: "extension/catalog/recipe-index.generated.js"
      provides: "the snapshot REGENERATED after the retail/marketplace sub-batch (INV-01 byte-stable)"
    - path: "tests/breadth-search-return.test.js"
      provides: "the per-op breadth assertion EXTENDED to the retail apps incl. cross-app buy/checkout collision probes (wrong-invoke=0)"
      contains: "amazon"
  key_links:
    - from: "scripts/import-opentabs-catalog.mjs enumerateBatchApps"
      to: "vendor/opentabs-snapshot/plugins/{amazon,ebay,etsy,bestbuy,costco,walmart,target}"
      via: "the frozen enumeration auto-discovers the new dirs (no importer edit); each emits opentabs__<stem>__* descriptors backing:'dom'"
      pattern: "opentabs__(amazon|ebay|etsy|bestbuy|costco|walmart|target)__"
    - from: "scripts/import-opentabs-catalog.mjs runImport classifyGate"
      to: "extension/config/service-denylist.json (39-01)"
      via: "every retail origin classifies sensitive (39-01) -> the merge gate does NOT abort"
      pattern: "classifyGate"
    - from: "scripts/verify-catalog-crosscheck.mjs checkPaymentOpsNotSafeInvocable"
      to: "the emitted opentabs__<app>__place_order / __checkout / __buy_now descriptors"
      via: "each payment op is backing:'dom' on a sensitive origin -> the guard PASSES"
      pattern: "place_order|checkout|buy_now|backing"
---

<objective>
Import the retail / marketplace sub-batch (amazon, ebay, etsy, bestbuy, costco, walmart, target) as
DATA-ONLY via the FROZEN machinery (NO importer edit beyond a STEM_OVERRIDES data-map entry where a
host derives wrong), gated on the 39-01 payment screening + the 39-02 grown corpus. These are
checkout-bearing retail apps: each places paid orders, so each origin was classified SENSITIVE in 39-01
and each payment op must ship DOM-only on that sensitive origin. Reuses the FROZEN screen-then-import
contract + the per-wave gate re-assertion verbatim (the 39-02 structure is the proven template).

1. Vendor the 7 apps as hermetic metadata slices (Wall 1: metadata-only, mirror the todoist slice
   EXACTLY). Each vendored urlPatterns host MUST equal the 39-01 screened sensitive origin. Real
   z.object inputs (product/item ids, cart, address, order id) -- NOT empty schemas. Explicit
   payment-op write/destructive signals so the crosscheck classes them.
2. Run the FROZEN importer: enumerateBatchApps auto-discovers the dirs; classifyGate PASSES (all 7
   origins sensitive via 39-01); every emitted descriptor is backing:'dom'; the payment-op CI guard
   PASSES (every payment op DOM-only on a sensitive origin).
3. Regenerate the snapshot; validate:extension reconciles. Re-assert the per-wave gates (crosscheck incl.
   the payment-op guard + no-dead-entry + the eval incl. cross-app buy/checkout collisions + cold-start).

Purpose: land the retail/marketplace category DOM-only on sensitive origins, proving the screen-then-
import contract + the triple mitigation hold for retail checkout. (The catastrophic money-movement set
is DENIED in 39-01, not vendored here.)
Output: 7 retail/marketplace apps' descriptors (all backing:'dom' on sensitive origins, payment ops
DOM-only-not-invocable); regenerated snapshot; all per-wave gates green incl. the payment-op guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-breadth-c-commerce-travel-misc-most-sensitive/39-CONTEXT.md
@.planning/phases/39-breadth-c-commerce-travel-misc-most-sensitive/39-01-SUMMARY.md
@.planning/phases/39-breadth-c-commerce-travel-misc-most-sensitive/39-02-SUMMARY.md

<interfaces>
<!-- The FROZEN machinery + the 39-01 screening + the 39-02 import contract (the proven template). -->
- scripts/import-opentabs-catalog.mjs: enumerateBatchApps() auto-discovers each new vendored dir and
  emits opentabs__<stem>__* descriptors; backingFor() returns 'dom' for every data-batch app; runImport()
  classifyGate-aborts on any unclassified origin AND feedSeedDescriptors upserts each slug into the eval
  seed. NO importer edit beyond a STEM_OVERRIDES data-map entry where the frozen host->stem derivation
  (service.replace(/^app\./,'').split('.')[0]) is wrong (e.g. 'www.amazon.com'->'www' -> add { amazon:
  'amazon' }; CONFIRM the emitted stem is the brand before relying on it -- the 38-02 threads / 38-03
  reddit precedent).
- side-effect verbs (scripts/lib/side-effect-class.mjs, FROZEN): place/checkout/buy/add/create/order ->
  WRITE; cancel/delete/remove -> DESTRUCTIVE; list/get/search/track -> READ. Each payment write op carries
  an explicit `api(..., { method:'POST' })`; each cancel `apiVoid(..., { method:'DELETE' })`; reads
  `api(..., { method:'GET' })`. Do NOT edit the shared module.
- 39-01 classified these origins SENSITIVE: amazon.com, ebay.com, etsy.com, bestbuy.com, costco.com,
  walmart.com, target.com (verify each via classify() before vendoring -- the import ABORTS if a vendored
  origin is not the screened one). The payment-op CI guard (39-01) requires every payment op to be
  backing:'dom' on a sensitive origin.
- The vendored-slice contract (replicate todoist EXACTLY): package.json + sdk-stub.ts (verbatim) +
  <app>-api.ts (inert stub) + index.ts + tools/<op>.ts (defineTool, real z.object input, never-run handle
  naming the upstream helper+method). The 39-02 food-delivery slices are a same-category worked example.
- breadth-search-return.test.js: widen the corpusFiles regex (~line 77-79) + raise the corpus.length
  floor + add cross-app collision probes (amazon-vs-walmart-vs-target place_order/buy_now; ebay-vs-etsy
  marketplace listings) all wrong-invoke=0.
- The eval + cold-start budget: the importer seed-feeds; keep generous gates; widen the cold-start byte
  budget ONLY if the grown index legitimately exceeds it while per-descriptor footprint stays flat, with
  a comment noting the grown count (the 38-02 precedent) -- do NOT change the data layout; the Phase-43
  full-corpus gate stays separate. FLAG in the SUMMARY if the budget is approached.
</interfaces>

# Reuse references (the proven slice + import shapes -- do NOT rebuild):
@vendor/opentabs-snapshot/plugins/todoist/src/index.ts
@vendor/opentabs-snapshot/plugins/todoist/src/sdk-stub.ts
@vendor/opentabs-snapshot/plugins/todoist/src/todoist-api.ts
@vendor/opentabs-snapshot/plugins/todoist/src/tools/create-task.ts
@vendor/opentabs-snapshot/plugins/todoist/src/tools/list-tasks.ts
@vendor/opentabs-snapshot/plugins/todoist/src/tools/delete-task.ts
@vendor/opentabs-snapshot/PIN.md
@tests/breadth-search-return.test.js
@tests/capability-search-eval.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor the retail / marketplace metadata slices (amazon, ebay, etsy, bestbuy, costco, walmart, target)</name>
  <files>vendor/opentabs-snapshot/plugins/{amazon,ebay,etsy,bestbuy,costco,walmart,target}/** (each: package.json, src/sdk-stub.ts, src/index.ts, src/<app>-api.ts, src/tools/*.ts), vendor/opentabs-snapshot/PIN.md</files>
  <read_first>
    - vendor/opentabs-snapshot/plugins/todoist/ (the full slice structure to replicate)
    - vendor/opentabs-snapshot/plugins/doordash/ (the 39-02 same-category worked example: a payment write op + reads + a cancel destructive, the exact shape to mirror)
    - vendor/opentabs-snapshot/plugins/todoist/src/tools/create-task.ts (write: api POST) + delete-task.ts (destructive: apiVoid DELETE) + list-tasks.ts (read: api GET)
    - scripts/import-opentabs-catalog.mjs (confirm each host's frozen-derived stem; add a STEM_OVERRIDES data-map entry where 'www.<brand>.com'->'www')
    - extension/config/service-denylist.json (CONFIRM each vendored origin is classified sensitive by 39-01)
    - vendor/opentabs-snapshot/PIN.md (the SHA + MIT pin + the per-phase append-only log)
  </read_first>
  <action>
    Create the 7 vendored metadata slices mirroring the todoist slice structure EXACTLY (and the 39-02
    food-delivery slices as the same-category worked example) -- Wall 1: metadata-only, NO dist/, NO
    executed handle(), NO runtime .js. For EACH app: package.json (opentabs.urlPatterns host below +
    _role "opentabs-metadata-snapshot" + _note + MIT), src/sdk-stub.ts (VERBATIM copy of todoist's),
    src/index.ts (<App>Plugin), src/<app>-api.ts (inert transport stub), src/tools/<op>.ts per op (real
    z.object input + never-run handle naming the upstream helper+method).

    Hosts (authoritative urlPatterns[0]; MUST equal the 39-01 screened sensitive origin -- CONFIRM via
    classify() before vendoring; add a STEM_OVERRIDES data-map entry keyed by the dir name where the
    frozen split('.')[0] is not the brand):
      - amazon  -> "*://www.amazon.com/*"   (service www.amazon.com; STEM_OVERRIDE amazon)
      - ebay    -> "*://www.ebay.com/*"     (service www.ebay.com; STEM_OVERRIDE ebay)
      - etsy    -> "*://www.etsy.com/*"     (service www.etsy.com; STEM_OVERRIDE etsy)
      - bestbuy -> "*://www.bestbuy.com/*"  (service www.bestbuy.com; STEM_OVERRIDE bestbuy)
      - costco  -> "*://www.costco.com/*"   (service www.costco.com; STEM_OVERRIDE costco)
      - walmart -> "*://www.walmart.com/*"  (service www.walmart.com; STEM_OVERRIDE walmart)
      - target  -> "*://www.target.com/*"   (service www.target.com; STEM_OVERRIDE target)
    CONFIRM the emitted slug is opentabs__<brand>__* (NOT opentabs__www__*) before relying on it; if a
    stem comes out 'www', the STEM_OVERRIDES entry is required (the documented data-map extension, INV-01
    holds). The `service` field keeps the real host; only the stem/slug/filename is canonicalized.

    Ops per app (real z.object inputs; at least one PAYMENT-bearing write + reads; key the payment write):
      - amazon:  search_products (read), get_product (read), list_orders (read), track_order (read),
        place_order (WRITE -- the payment op; api POST), cancel_order (DESTRUCTIVE -- apiVoid DELETE).
      - ebay:    search_listings (read), get_listing (read), list_orders (read), place_bid (WRITE --
        payment-bearing; api POST), buy_now (WRITE -- the payment op; api POST).
      - etsy:    search_listings (read), get_listing (read), list_orders (read), add_to_cart (WRITE; api
        POST), checkout (WRITE -- the payment op; api POST).
      - bestbuy: search_products (read), get_product (read), list_orders (read), place_order (WRITE --
        the payment op; api POST), cancel_order (DESTRUCTIVE).
      - costco:  search_products (read), get_product (read), list_orders (read), place_order (WRITE --
        the payment op; api POST).
      - walmart: search_products (read), get_product (read), list_orders (read), place_order (WRITE --
        the payment op; api POST), cancel_order (DESTRUCTIVE).
      - target:  search_products (read), get_product (read), list_orders (read), place_order (WRITE --
        the payment op; api POST).
    Each payment write op carries an explicit `{ method:'POST' }` literal; each cancel
    `apiVoid(..., { method:'DELETE' })`; reads `api(..., { method:'GET' })`. Do NOT vendor any
    e2e/prescript fixture or any DENY-01 / money-movement denied app. Do NOT edit the importer (except a
    STEM_OVERRIDES data-map entry) or side-effect-class.mjs.

    Append the 7 apps to vendor/opentabs-snapshot/PIN.md's per-phase log as a Phase-39 row (batch C
    sub-batch 2: retail + marketplace; all checkout-bearing -> sensitive origins, DOM-only by the frozen
    default).
  </action>
  <verify>
    <automated>node --import tsx -e "const apps=['amazon','ebay','etsy','bestbuy','costco','walmart','target'];(async()=>{for(const a of apps){const m=await import('./vendor/opentabs-snapshot/plugins/'+a+'/src/index.ts');const p=m.default;if(!p||!Array.isArray(p.tools)||p.tools.length<4){console.error('bad slice',a);process.exit(1)}const hasPayment=p.tools.some(t=>/place_order|checkout|buy_now/.test(t.name));if(!hasPayment){console.error('no payment op in',a);process.exit(1)}console.log(a,'->',(p.urlPatterns&&p.urlPatterns[0]),p.tools.map(t=>t.name).join(','))}process.exit(0)})().catch(e=>{console.error(e.message);process.exit(1)})" && node -e "const D=require('./extension/utils/service-denylist.js');D.load().then(()=>{const o=['https://www.amazon.com','https://www.ebay.com','https://www.etsy.com','https://www.bestbuy.com','https://www.costco.com','https://www.walmart.com','https://www.target.com'];let ok=true;for(const x of o){if(D.classify(x).sensitive!==true){console.error('vendored origin NOT sensitive (39-01 must classify it):',x);ok=false}}if(!ok)process.exit(1);console.log('PASS all 7 retail origins classify sensitive')})"</automated>
  </verify>
  <acceptance_criteria>
    - All 7 apps import under tsx with >=4 ops each incl. at least one payment-bearing write (place_order/checkout/buy_now); real z.object inputs; no handle() executes; provenance-scaffold green.
    - Each vendored urlPatterns host equals the 39-01 screened sensitive origin (classify() sensitive for each); the emitted stem is the brand (opentabs__amazon__* etc.) -- a STEM_OVERRIDES data-map entry added per host that derives wrong.
    - Each payment write op carries {method:'POST'}; each cancel carries apiVoid {method:'DELETE'}; reads are {method:'GET'}.
    - No DENY-01 / money-movement denied app or fixture vendored; side-effect-class.mjs untouched; the only importer touch (if any) is a STEM_OVERRIDES data-map entry.
  </acceptance_criteria>
  <done>The 7 retail/marketplace apps are vendored as hermetic metadata slices with correct hosts/stems (each origin classified sensitive in 39-01), each carrying a payment-bearing write op + reads with explicit transport signals; PIN.md per-phase log updated (Phase-39 batch C sub-batch 2).</done>
</task>

<task type="auto">
  <name>Task 2: Import the sub-batch, regenerate the snapshot, re-assert the per-wave gates (incl. the payment-op guard)</name>
  <files>extension/catalog/recipe-index.generated.js, catalog/descriptors/_fixtures/intent-cases.json, catalog/descriptors/_fixtures/_provenance.json, tests/breadth-search-return.test.js, tests/capability-search-eval.test.js</files>
  <read_first>
    - scripts/import-opentabs-catalog.mjs (the FROZEN enumerateBatchApps + runImport + classifyGate + feedSeedDescriptors -- RUN, do NOT edit beyond a STEM_OVERRIDES data-map entry from Task 1)
    - scripts/package-extension.mjs + scripts/validate-extension.mjs (regen + byte-compare; INV-01)
    - scripts/verify-catalog-crosscheck.mjs (the crosscheck + the payment-op guard from 39-01 -- RUN, do NOT edit)
    - tests/breadth-search-return.test.js (the corpusFiles regex ~line 77-79 + COLLISION_SET)
    - tests/capability-search-eval.test.js + tests/no-dead-entry.test.js (run, do NOT edit)
    - catalog/descriptors/_fixtures/intent-cases.json (the eval fixtures to grow)
    - .planning/phases/39-breadth-c-commerce-travel-misc-most-sensitive/39-02-SUMMARY.md (the proven import+regen+gate re-assert worked example)
  </read_first>
  <action>
    (a) Run `node --import tsx ./scripts/import-opentabs-catalog.mjs` to emit the 7 apps via the FROZEN
    machinery. Confirm: opentabs__amazon__* / __ebay__* / __etsy__* / __bestbuy__* / __costco__* /
    __walmart__* / __target__* appear with the brand stems; EVERY emitted descriptor carries backing:'dom';
    the merge-time classifyGate PASSES (all 7 origins sensitive via 39-01); the payment-op CI guard PASSES
    (every payment op DOM-only on a sensitive origin); all prior descriptors (incl. the 39-02 food-delivery
    apps) untouched; seed-descriptors.json carries the new slugs.

    (b) Run `node scripts/package-extension.mjs` (INV-01 byte-stable IIFE/djb2); confirm `node
    scripts/validate-extension.mjs` exits 0.

    (c) Extend catalog/descriptors/_fixtures/intent-cases.json: a read ("track my amazon order" ->
    amazon.track_order; "search etsy listings" -> etsy.search_listings), a payment write ("order this on
    amazon" -> amazon.place_order; "buy it now on ebay" -> ebay.buy_now; "checkout my etsy cart" ->
    etsy.checkout), a destructive ("cancel my walmart order" -> walmart.cancel_order), expectedSlug aligned
    to the emitted real slugs. Extend tests/breadth-search-return.test.js: widen the corpusFiles regex,
    raise the floor, add cross-app collision probes to COLLISION_SET (amazon-vs-walmart-vs-target
    place_order/buy_now; ebay-vs-etsy marketplace listings) all wrong-invoke=0. Do NOT hand-edit
    seed-descriptors.json.

    (d) Re-assert the per-wave gates: `node scripts/verify-catalog-crosscheck.mjs` (exit 0 -- payment ops
    class write, cancels destructive, reads read; NO under-statement AND the payment-op guard passes
    because every payment op is DOM-only on a sensitive origin), `node tests/no-dead-entry.test.js` (exit
    0), and `node tests/capability-search-eval.test.js` (recall@5>=0.9, wrong-invoke=0.000 incl. the retail
    collisions; cold-start asserts pass). Widen the cold-start budget ONLY if the grown index legitimately
    exceeds the gate while per-descriptor footprint stays flat (params not indexed), with a comment noting
    the grown count -- do NOT change the data layout, do NOT defer the Phase-43 gate. FLAG in the SUMMARY
    if the budget is approached.

    Do NOT edit the importer (beyond Task-1's STEM_OVERRIDES entry), capability-search.js, the gates'
    core logic, or service-denylist.json.
  </action>
  <verify>
    <automated>node --import tsx ./scripts/import-opentabs-catalog.mjs && test -f catalog/descriptors/opentabs__amazon__place_order.json && test -f catalog/descriptors/opentabs__ebay__buy_now.json && test -f catalog/descriptors/opentabs__etsy__checkout.json && node -e "const fs=require('fs');for(const s of ['opentabs__amazon__place_order','opentabs__ebay__buy_now','opentabs__etsy__checkout','opentabs__walmart__place_order']){const d=JSON.parse(fs.readFileSync('catalog/descriptors/'+s+'.json','utf8'));if(d.backing!=='dom'){console.error('payment op NOT dom:',s,d.backing);process.exit(1)}if(d.sideEffectClass!=='write'&&d.sideEffectClass!=='destructive'){console.error('payment op NOT write/destructive:',s,d.sideEffectClass);process.exit(1)}}console.log('PASS retail payment ops are DOM-only writes')" && node scripts/package-extension.mjs && npm run validate:extension && node scripts/verify-catalog-crosscheck.mjs && node tests/no-dead-entry.test.js && node tests/breadth-search-return.test.js && node tests/capability-search-eval.test.js</automated>
  </verify>
  <acceptance_criteria>
    - The 7 apps' descriptors are emitted (opentabs__<brand>__*) and inlined; EVERY one carries backing:'dom'; the merge-time classifyGate did NOT abort; no prior descriptor clobbered.
    - validate:extension exits 0 (snapshot reconciled; classification gate + crosscheck + recipe-path guard green); INV-01 byte-stable.
    - verify-catalog-crosscheck.mjs exits 0: payment ops class write/destructive AND the payment-op guard PASSES (every payment op DOM-only on a sensitive origin).
    - no-dead-entry.test.js exits 0 over the grown corpus; breadth-search-return.test.js passes incl. the cross-app buy/checkout collisions (wrong-invoke=0).
    - capability-search-eval.test.js prints recall@5>=0.9, wrong-invoke=0.000, cold-start asserts green (flagged in the SUMMARY if approaching the budget).
  </acceptance_criteria>
  <done>The 7 retail/marketplace apps are imported as DOM-only data via the frozen machinery (gate passed; every payment op DOM-only on a sensitive origin per the payment-op guard), the snapshot regenerated (INV-01 byte-stable), and the per-wave gates re-asserted: crosscheck (incl. payment-op guard) + no-dead-entry + eval green and the cold-start budget held.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OpenTabs vendored metadata -> FSB descriptor corpus (build-time) | the retail/marketplace metadata; a forbidden field, a denied app, or an unscreened payment origin must never enter the corpus |
| a payment-bearing op -> the side-effect/backing crosscheck (build-time) | a place_order/checkout/buy_now op must class write AND stay backing:'dom' on a sensitive origin -- never safe-and-API-invocable |
| screened sensitive payment origin -> invoke consent gate (runtime) | each app's payment WRITE flows through the posture-B re-gate (the end-to-end proof on a REAL emitted descriptor is Plan 39-07) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | Elevation of Privilege | a retail payment op (place_order/checkout/buy_now) writable/invocable under Auto = money moved without consent | mitigate | every payment op ships backing:'dom' (frozen default -> DOM-only, asserted in Task 2); the payment-op CI guard (39-01) passes only because each is DOM-only on a sensitive origin; each origin classified sensitive in 39-01 so its DOM-path write is posture-B gated |
| T-39-02 | Elevation of Privilege | a retail origin landing unscreened -> its write runs under Auto with no re-gate | mitigate | the merge-time classifyGate aborts on any unclassified origin; Task 1 confirms each vendored origin == the 39-01 screened sensitive origin before vendoring; a mismatch aborts the import |
| T-39-04 | Spoofing | a retail payment op surfaced as a confident API-invocable hit (a fabricated checkout call) | mitigate | the frozen backing:'dom' default makes every payment op DOM-only / discovery-pending; the payment-op guard forbids any payment op being backing recipe/handler; never auto-mint a recipe |
| T-39-06 | Tampering | a vendored slice emitting a forbidden field name (Wall 1) OR a payment op under-stated as read | mitigate | the importer's recursive forbidden-field pre-scan (UNCHANGED) throws before emit; verify-catalog-crosscheck.mjs fails the build on under-statement (place_order/buy_now class write via verb + {method:'POST'}) |
| T-39-03 | Tampering | a DENY-01 / money-movement denied app vendored into this sub-batch | mitigate | only the 7 screened-sensitive apps vendored; no deniedOrigins app vendored; the merge gate refuses a denied origin's descriptor |
| T-39-SC | Tampering | npm/pip/cargo installs | accept | no new packages this plan (data-only vendoring + test edits; zod/tsx already devDeps); package-legitimacy gate N/A |
</threat_model>

<verification>
- `node --import tsx ./scripts/import-opentabs-catalog.mjs` emits the 7 apps (all backing:'dom'); the gate did NOT abort; the payment-op guard passes; seed-descriptors.json seed-fed.
- `npm run validate:extension` exits 0 (snapshot reconciled; classification gate + crosscheck + recipe-path guard green).
- `node scripts/verify-catalog-crosscheck.mjs && node tests/no-dead-entry.test.js && node tests/breadth-search-return.test.js && node tests/capability-search-eval.test.js` pass (payment ops write + DOM-only-on-sensitive; every new slug resolves T3; buy/checkout collisions wrong-invoke=0; recall@5>=0.9; cold-start within budget).
</verification>

<success_criteria>
- BRDTH-01: the 7 retail/marketplace apps' descriptors are imported via the frozen machinery + returned by search with synonyms + side-effect class + backing:'dom'.
- BRDTH-02: the merge-time classifyGate passed only because 39-01 screened the origins sensitive; the payment-op CI guard passed because every payment op is DOM-only on a sensitive origin.
- BRDTH-03: every emitted descriptor is DOM-only (backing:'dom', invocable=false, discovery-pending), never confident-invocable, never learn-seeded; no payment op armed via recipe/handler.
- No regression: no prior descriptor clobbered; crosscheck (incl. payment-op guard) + no-dead-entry + the eval (recall@5>=0.9, wrong-invoke=0, cold-start within budget) green; validate:extension exits 0.
</success_criteria>

<output>
Create `.planning/phases/39-breadth-c-commerce-travel-misc-most-sensitive/39-03-SUMMARY.md` when done.
</output>
