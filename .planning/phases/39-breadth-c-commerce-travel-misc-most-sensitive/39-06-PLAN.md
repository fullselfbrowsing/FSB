---
phase: 39-breadth-c-commerce-travel-misc-most-sensitive
plan: 06
type: execute
wave: 6
depends_on: ["39-01", "39-05"]
files_modified:
  - vendor/opentabs-snapshot/plugins/shopify/**
  - vendor/opentabs-snapshot/plugins/craigslist/**
  - vendor/opentabs-snapshot/plugins/dominos/**
  - vendor/opentabs-snapshot/plugins/chipotle/**
  - vendor/opentabs-snapshot/plugins/zillow/**
  - vendor/opentabs-snapshot/plugins/grafana/**
  - extension/config/service-denylist.json
  - scripts/verify-catalog-crosscheck.mjs
  - vendor/opentabs-snapshot/PIN.md
  - catalog/descriptors/_fixtures/_provenance.json
  - catalog/descriptors/_fixtures/intent-cases.json
  - extension/catalog/recipe-index.generated.js
  - tests/breadth-search-return.test.js
  - tests/capability-search-eval.test.js
autonomous: true
requirements: [BRDTH-01, BRDTH-02, BRDTH-03]

must_haves:
  truths:
    - "The remaining commerce/misc apps that COMPLETE coverage of all real OpenTabs apps (the named 39-CONTEXT examples not yet imported -- shopify, craigslist, dominos, chipotle -- plus any remaining real-app stragglers in the vendored surface, e.g. zillow listings, grafana-read observability-misc) are vendored as hermetic metadata-only slices and imported via the FROZEN importer (ZERO machinery edit beyond STEM_OVERRIDES data-map entries) -- each returns from search with >=3 app-disambiguated synonyms + side-effect class + backing:'dom' per op"
    - "EVERY remaining app's origin is classified BEFORE import (payment-bearing commerce -> sensitive in service-denylist.json; genuinely read-only -> safe + in READ_ONLY_SAFE_SERVICES; money-movement -> denied + NOT vendored) so the merge-time classifyGate has zero unclassified origins to abort on; the payment-op CI guard PASSES (every payment op DOM-only on a sensitive origin)"
    - "After this sub-batch, enumerateBatchApps() over the vendored snapshot covers ALL real OpenTabs apps (every vendored dir except the e2e/prescript fixtures + the DENY-01/money-movement denied set) -- the coverage-report plan (39-07) confirms completeness across the ~117-app real set"
    - "The snapshot is regenerated via package-extension.mjs (INV-01 byte-stable); validate:extension exits 0; no prior descriptor clobbered; the existing hand-authored recipes (reddit-inbox.json etc.) are NOT clobbered"
    - "node tests/no-dead-entry.test.js stays exit 0; the eval keeps recall@5>=0.9 wrong-invoke=0.000; the SW cold-start budget holds (flagged if approaching the gate -- this is the largest corpus before the Phase-43 full-corpus gate)"
  artifacts:
    - path: "vendor/opentabs-snapshot/plugins/shopify/src/index.ts"
      provides: "vendored shopify metadata slice (*.shopify.com SENSITIVE per 39-01; a checkout/order op is the DOM-only-on-sensitive payment-op-guard subject)"
      contains: "ShopifyPlugin"
    - path: "extension/config/service-denylist.json"
      provides: "the roster EXTENDED for any remaining payment-bearing commerce origin (shopify already in 39-01; dominos/chipotle/craigslist classified) + any newly-discovered money-movement app denied"
      contains: "sensitiveOrigins"
    - path: "scripts/verify-catalog-crosscheck.mjs"
      provides: "READ_ONLY_SAFE_SERVICES EXTENDED with any genuinely-read-only remaining safe origin (e.g. zillow/craigslist-if-read-only) so a re-vendored write FAILS the build (the 38 MED-02 invariant)"
      contains: "READ_ONLY_SAFE_SERVICES"
    - path: "extension/catalog/recipe-index.generated.js"
      provides: "the snapshot REGENERATED after the final import sub-batch (INV-01 byte-stable) -- the complete real-app corpus"
  key_links:
    - from: "scripts/import-opentabs-catalog.mjs enumerateBatchApps"
      to: "vendor/opentabs-snapshot/plugins/* (ALL real-app dirs after this sub-batch)"
      via: "the frozen enumeration auto-discovers every vendored dir (no importer edit); the complete real-app set emits opentabs__<stem>__* descriptors backing:'dom'"
      pattern: "opentabs__(shopify|craigslist|dominos|chipotle)__"
    - from: "extension/config/service-denylist.json + scripts/verify-catalog-crosscheck.mjs"
      to: "scripts/import-opentabs-catalog.mjs runImport classifyGate + the crosscheck"
      via: "every remaining origin classified (sensitive/safe/denied) BEFORE import -> classifyGate passes + the payment-op + READ_ONLY_SAFE guards pass"
      pattern: "classifyGate|READ_ONLY_SAFE_SERVICES|checkPaymentOpsNotSafeInvocable"
---

<objective>
COMPLETE descriptor coverage of ALL real OpenTabs apps by importing the remaining commerce/misc apps as
DATA-ONLY via the FROZEN machinery, gated on the 39-01 payment screening. This sub-batch closes the
breadth: after it, enumerateBatchApps() over the vendored snapshot covers every real app (every vendored
dir except the e2e/prescript fixtures + the DENY-01/money-movement denied set). It imports the named
39-CONTEXT examples not yet covered (shopify, craigslist, dominos, chipotle) PLUS any remaining real-app
stragglers in the vendored surface (e.g. zillow listings, a grafana-read observability-misc app), each
screened first. Reuses the FROZEN screen-then-import contract + the per-wave gate re-assertion verbatim.

1. ENUMERATE the remaining real apps + SCREEN each (a 39-01-style edit BEFORE vendoring): classify every
   remaining origin in service-denylist.json (payment-bearing commerce -> sensitive; money-movement ->
   denied + NOT vendored) and/or verify-catalog-crosscheck.mjs READ_ONLY_SAFE_SERVICES (genuinely
   read-only -> safe + read-only-locked). Apply the payment-conservative-by-default rule (unknown
   commerce/payment -> sensitive, never safe-writable).
2. Vendor the remaining apps as hermetic metadata slices (Wall 1: metadata-only, mirror todoist EXACTLY).
   Payment-bearing apps carry their payment write op DOM-only on a sensitive origin; read-only apps emit
   only reads. Real z.object inputs; explicit transport signals.
3. Run the FROZEN importer: classifyGate PASSES (every remaining origin classified); every emitted
   descriptor is backing:'dom'; the payment-op + READ_ONLY_SAFE guards PASS.
4. Regenerate the snapshot; validate:extension reconciles. Re-assert the per-wave gates over the
   now-COMPLETE real-app corpus (this is the largest corpus before the Phase-43 full-corpus gate -- FLAG
   the cold-start budget if approached).

Purpose: complete BRDTH-01 coverage of all real OpenTabs apps as DOM-only data, with every commerce/
payment origin screened. The coverage report (39-07) then confirms completeness + emits the head/learn/
dom/dead breakdown across the ~117-app real set.
Output: the remaining real apps' descriptors (payment apps backing:'dom' on sensitive origins; read-only
apps read-only-locked); extended roster + READ_ONLY_SAFE set; regenerated complete-corpus snapshot; all
per-wave gates green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-breadth-c-commerce-travel-misc-most-sensitive/39-CONTEXT.md
@.planning/phases/39-breadth-c-commerce-travel-misc-most-sensitive/39-01-SUMMARY.md
@.planning/phases/39-breadth-c-commerce-travel-misc-most-sensitive/39-05-SUMMARY.md

<interfaces>
<!-- The FROZEN machinery + the 39-01 screening + the 39-02..05 import contract (proven template). -->
- THE REMAINING REAL-APP SET: the named 39-CONTEXT commerce examples NOT yet imported by 39-02..05 are
  shopify (*.shopify.com -- already classified sensitive in 39-01), craigslist (craigslist.org -- 39-01),
  dominos (www.dominos.com -- 39-01), chipotle (www.chipotle.com -- 39-01). PLUS any remaining real apps
  in the broader OpenTabs surface that are neither dev/PM (37) nor comms/social/content (38) nor already
  imported by 39-02..05 -- representative "misc" apps to round out coverage: zillow (real-estate listings,
  read-only -> safe), grafana (observability-read, read-only -> safe; the FEATURES.md stretch list).
  Enumerate the remaining real apps the milestone targets that are not yet vendored; for EACH, classify it
  before vendoring. If the real-app surface this batch must complete includes MORE apps than the
  representative set here, vendor them too (the goal is COMPLETE coverage) -- group them by tier
  (payment->sensitive, read-only->safe, money-movement->denied) and screen each. Do NOT omit a real app
  because it is "misc"; the planner has no authority to drop a real app (the coverage report 39-07 is the
  completeness check).
- extension/config/service-denylist.json (THIS plan extends it for any remaining unclassified origin):
  dominos/chipotle/shopify/craigslist are ALREADY classified sensitive in 39-01 (confirm via classify());
  classify any NEW commerce/payment origin sensitive, any money-movement origin denied. Use the 39-01
  exact-host / apex-suffix discipline.
- scripts/verify-catalog-crosscheck.mjs READ_ONLY_SAFE_SERVICES (THIS plan extends it; the 38 MED-02
  guard): ADD any genuinely-read-only remaining safe origin's vendored host (e.g. 'www.zillow.com',
  'grafana.com' or its vendored host) so a future write FAILS the build. Add an origin here ONLY because
  it is left safe SPECIFICALLY because it is read-only.
- scripts/import-opentabs-catalog.mjs: enumerateBatchApps() auto-discovers each new vendored dir;
  backingFor() returns 'dom'; runImport() classifyGate-aborts on any unclassified origin AND
  feedSeedDescriptors upserts each slug. NO importer edit beyond STEM_OVERRIDES data-map entries where the
  frozen split('.')[0] is not the brand (e.g. 'www.dominos.com'->'www'). For craigslist (craigslist.org
  apex) the frozen split('.')[0] is 'craigslist' -- NO override needed; CONFIRM per host.
- side-effect verbs (scripts/lib/side-effect-class.mjs, FROZEN): place/checkout/order/post/create -> WRITE;
  cancel/delete -> DESTRUCTIVE; list/get/search -> READ. Payment writes carry `api(..., {method:'POST'})`;
  reads `api(..., {method:'GET'})`. Do NOT edit the shared module.
- The payment-op CI guard (39-01) + the READ_ONLY_SAFE guard (38 MED-02): every payment op DOM-only on a
  sensitive origin; every read-only-safe origin emits only reads.
- The vendored-slice contract (replicate todoist EXACTLY). The 39-02..05 slices are worked examples.
- breadth-search-return.test.js: widen the corpusFiles regex + raise the floor + add collision probes
  (dominos-vs-chipotle place_order; craigslist-vs-ebay marketplace listings) all wrong-invoke=0.
- The eval + cold-start budget: this is the LARGEST corpus before Phase-43. Widen the cold-start byte
  budget ONLY if the grown index legitimately exceeds it while per-descriptor footprint stays flat, with a
  comment noting the grown count -- do NOT change the data layout, do NOT defer the Phase-43 full-corpus
  gate. FLAG the cold-start headroom in the SUMMARY (39-CONTEXT requires flagging if this batch approaches
  the budget -- it is the most likely sub-batch to).
</interfaces>

# Reuse references (the proven slice + import + screening shapes -- do NOT rebuild):
@vendor/opentabs-snapshot/plugins/todoist/src/index.ts
@vendor/opentabs-snapshot/plugins/todoist/src/sdk-stub.ts
@vendor/opentabs-snapshot/plugins/todoist/src/tools/create-task.ts
@vendor/opentabs-snapshot/plugins/todoist/src/tools/list-tasks.ts
@scripts/verify-catalog-crosscheck.mjs
@extension/config/service-denylist.json
@catalog/descriptors/reddit-inbox.json
@vendor/opentabs-snapshot/PIN.md
@tests/breadth-search-return.test.js
@tests/capability-search-eval.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Screen + vendor the remaining real apps (completing all real-app coverage)</name>
  <files>extension/config/service-denylist.json, scripts/verify-catalog-crosscheck.mjs, vendor/opentabs-snapshot/plugins/{shopify,craigslist,dominos,chipotle,zillow,grafana}/** (and any further remaining real apps), vendor/opentabs-snapshot/PIN.md</files>
  <read_first>
    - extension/config/service-denylist.json (CONFIRM shopify/craigslist/dominos/chipotle are already classified sensitive in 39-01; classify any NEW remaining origin)
    - scripts/verify-catalog-crosscheck.mjs (READ_ONLY_SAFE_SERVICES + checkReadOnlySafeOrigins -- extend with any read-only safe remaining origin)
    - vendor/opentabs-snapshot/plugins/todoist/ + a 39-02..05 slice (the full slice structure + worked examples for both a payment app and a read-only app)
    - catalog/descriptors/reddit-inbox.json (a hand-authored recipe descriptor the import must NOT clobber -- confirm any new opentabs__* slug is a distinct filename)
    - scripts/import-opentabs-catalog.mjs (confirm each host's frozen-derived stem; add STEM_OVERRIDES data-map entries where 'www.<brand>.com'->'www'; craigslist.org apex needs none)
    - .planning/phases/39-.../39-05-SUMMARY.md + .planning/research/FEATURES.md (the stretch/misc app list to round out coverage)
    - vendor/opentabs-snapshot/PIN.md (the SHA + MIT pin + the per-phase append-only log)
  </read_first>
  <action>
    (a) ENUMERATE + SCREEN the remaining real apps. Confirm shopify/craigslist/dominos/chipotle are
    classified sensitive in 39-01 (via classify()). For any NEW remaining real app this batch must
    complete: classify it in service-denylist.json BEFORE vendoring -- payment-bearing commerce ->
    sensitiveOrigins (exact-host discipline); money-movement -> deniedOrigins (and do NOT vendor it);
    genuinely read-only -> leave safe AND add its vendored host to verify-catalog-crosscheck.mjs
    READ_ONLY_SAFE_SERVICES. Apply the payment-conservative-by-default rule (unknown commerce/payment ->
    sensitive). Update the service-denylist.json "_comment" + add READ_ONLY_SAFE comments noting the 39-06
    completion extension.

    (b) Vendor the remaining apps mirroring the todoist slice structure EXACTLY (Wall 1: metadata-only, NO
    dist/, NO executed handle(), NO runtime .js). For EACH: package.json + sdk-stub.ts (verbatim) +
    <app>-api.ts (inert stub) + index.ts + tools/<op>.ts (defineTool, real z.object input, never-run
    handle naming helper+method). Add STEM_OVERRIDES data-map entries where the frozen split('.')[0] is
    not the brand.

    Representative remaining apps + hosts + ops (vendor these AND any further real apps needed for
    complete coverage -- the coverage report 39-07 is the completeness check; do NOT stop short of a real
    app):
      - shopify    -> "*://*.shopify.com/*"      (SENSITIVE): list_products (read), get_product (read),
        list_orders (read), create_order (WRITE -- the payment op; api POST), cancel_order (DESTRUCTIVE).
      - craigslist -> "*://craigslist.org/*"     (SENSITIVE per 39-01; marketplace, contact/transact):
        search_listings (read), get_listing (read), post_listing (WRITE -- api POST), delete_listing
        (DESTRUCTIVE -- apiVoid DELETE). (craigslist.org apex -> stem 'craigslist', NO override needed;
        CONFIRM.)
      - dominos    -> "*://www.dominos.com/*"     (SENSITIVE): list_stores (read), get_menu (read),
        list_orders (read), place_order (WRITE -- the payment op; api POST), track_order (read).
      - chipotle   -> "*://www.chipotle.com/*"    (SENSITIVE): list_locations (read), get_menu (read),
        list_orders (read), place_order (WRITE -- the payment op; api POST).
      - zillow     -> "*://www.zillow.com/*"      (SAFE read-only): search_listings (read), get_listing
        (read), get_home_value (read). NO write op -> add 'www.zillow.com' to READ_ONLY_SAFE_SERVICES.
      - grafana    -> "*://grafana.com/*"         (SAFE read-only observability-misc): list_dashboards
        (read), get_dashboard (read), query_metrics (read). NO write op -> add the vendored grafana host to
        READ_ONLY_SAFE_SERVICES. (CONFIRM the grafana host classifies safe-benign; the 39-01 payment tokens
        must not false-trip it.)
    Each payment write carries `{ method:'POST' }`; each destructive `apiVoid(..., { method:'DELETE' })`;
    reads `api(..., { method:'GET' })`. The read-only apps (zillow/grafana) emit ONLY reads. Do NOT vendor
    any e2e/prescript fixture or any DENY-01 / money-movement denied app. Do NOT edit the importer (except
    STEM_OVERRIDES data-map entries) or side-effect-class.mjs.

    Append the remaining apps to vendor/opentabs-snapshot/PIN.md's per-phase log as a Phase-39 row (batch C
    sub-batch 5: completion -- remaining commerce [sensitive] + read-only misc [safe]; coverage of all real
    apps complete).
  </action>
  <verify>
    <automated>node -e "const D=require('./extension/utils/service-denylist.js');D.load().then(()=>{const sens=['https://shop.myshopify.com','https://craigslist.org','https://www.dominos.com','https://www.chipotle.com'];const safe=['https://www.zillow.com','https://grafana.com'];let ok=true;for(const o of sens){if(D.classify(o).sensitive!==true){console.error('commerce origin NOT sensitive:',o);ok=false}}for(const o of safe){const c=D.classify(o);if(c.denied===true){console.error('read-only app should not be denied:',o);ok=false}}if(!ok)process.exit(1);console.log('PASS remaining commerce sensitive; read-only misc not denied')})" && node -e "(async()=>{const x=await import('./scripts/verify-catalog-crosscheck.mjs');const fail=x.checkReadOnlySafeOrigins([{slug:'zillow.post_listing',service:'www.zillow.com',sideEffectClass:'write'}]);if(!(fail.failures&&fail.failures.length>0)){console.error('READ_ONLY_SAFE did not catch a zillow write');process.exit(1)}console.log('PASS READ_ONLY_SAFE catches a zillow write')})()" && node --import tsx -e "const apps=['shopify','craigslist','dominos','chipotle','zillow','grafana'];(async()=>{for(const a of apps){const m=await import('./vendor/opentabs-snapshot/plugins/'+a+'/src/index.ts');const p=m.default;if(!p||!Array.isArray(p.tools)||p.tools.length<3){console.error('bad slice',a);process.exit(1)}console.log(a,'->',(p.urlPatterns&&p.urlPatterns[0]),p.tools.map(t=>t.name).join(','))}process.exit(0)})().catch(e=>{console.error(e.message);process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - classify() returns sensitive for shopify/craigslist/dominos/chipotle (+ any new payment commerce); safe for zillow/grafana (+ any new read-only app); denied for any new money-movement app (and it is NOT vendored).
    - READ_ONLY_SAFE_SERVICES includes the zillow/grafana vendored hosts (+ any new read-only host); checkReadOnlySafeOrigins FAILS a write op on one.
    - All remaining apps import under tsx with >=3 ops; payment apps carry a payment write; read-only apps emit ONLY read ops; real z.object inputs; no handle() executes; provenance-scaffold green; reddit-inbox.json + the other hand-authored recipes not clobbered (distinct filenames).
    - The emitted stems are the brands -- STEM_OVERRIDES data-map entries added per host that derives wrong (craigslist.org needs none); no DENY-01 / money-movement denied app vendored; side-effect-class.mjs + crossCheck + the payment-op guard untouched.
    - No real app omitted as "misc" -- the remaining real-app surface is covered (the coverage report 39-07 confirms completeness).
  </acceptance_criteria>
  <done>Every remaining real app is screened (payment commerce -> sensitive, read-only -> safe + read-only-locked, money-movement -> denied + not vendored) and vendored as hermetic metadata slices; coverage of all real OpenTabs apps is complete (enumerateBatchApps over the snapshot now covers every real app); PIN.md per-phase log updated (Phase-39 batch C sub-batch 5: completion).</done>
</task>

<task type="auto">
  <name>Task 2: Import the final sub-batch, regenerate the complete-corpus snapshot, re-assert the per-wave gates</name>
  <files>extension/catalog/recipe-index.generated.js, catalog/descriptors/_fixtures/intent-cases.json, catalog/descriptors/_fixtures/_provenance.json, tests/breadth-search-return.test.js, tests/capability-search-eval.test.js</files>
  <read_first>
    - scripts/import-opentabs-catalog.mjs (the FROZEN enumerateBatchApps + runImport + classifyGate + feedSeedDescriptors -- RUN, do NOT edit beyond Task-1's STEM_OVERRIDES data-map entries)
    - scripts/package-extension.mjs + scripts/validate-extension.mjs (regen + byte-compare; INV-01)
    - scripts/verify-catalog-crosscheck.mjs (the crosscheck + the payment-op guard + the extended READ_ONLY_SAFE guard -- RUN, do NOT edit further)
    - tests/breadth-search-return.test.js (the corpusFiles regex ~line 77-79 + COLLISION_SET)
    - tests/capability-search-eval.test.js + tests/no-dead-entry.test.js (run, do NOT edit)
    - catalog/descriptors/_fixtures/intent-cases.json (the eval fixtures to grow)
    - .planning/phases/39-breadth-c-commerce-travel-misc-most-sensitive/39-05-SUMMARY.md (the proven import+regen+gate re-assert worked example)
  </read_first>
  <action>
    (a) Run `node --import tsx ./scripts/import-opentabs-catalog.mjs` to emit the remaining apps via the
    FROZEN machinery. Confirm: opentabs__shopify__* / __craigslist__* / __dominos__* / __chipotle__* /
    __zillow__* / __grafana__* (+ any further remaining apps) appear with the brand stems; EVERY emitted
    descriptor carries backing:'dom'; the merge-time classifyGate PASSES (every remaining origin classified
    via 39-01 + Task 1); the payment-op CI guard PASSES (every payment op DOM-only on a sensitive origin);
    the READ_ONLY_SAFE guard PASSES (read-only apps emit only reads); all prior descriptors (incl.
    39-02..05) + the hand-authored recipes (reddit-inbox.json etc.) untouched; seed-descriptors.json
    carries the new slugs.

    (b) Run `node scripts/package-extension.mjs` (INV-01 byte-stable IIFE first-line + djb2/dual-export
    tail; only the DATA literal grows -- this is now the COMPLETE real-app corpus); confirm `node
    scripts/validate-extension.mjs` exits 0.

    (c) Extend catalog/descriptors/_fixtures/intent-cases.json: a payment write ("order pizza on dominos" ->
    dominos.place_order; "place a chipotle order" -> chipotle.place_order; "create a shopify order" ->
    shopify.create_order), a read-only-app read ("search zillow listings" -> zillow.search_listings; "look
    up a home value on zillow" -> zillow.get_home_value), expectedSlug aligned to the emitted real slugs.
    Extend tests/breadth-search-return.test.js: widen the corpusFiles regex, raise the floor, add collision
    probes to COLLISION_SET (dominos-vs-chipotle place_order; craigslist-vs-ebay marketplace listings;
    zillow-vs-craigslist real-estate-vs-classifieds) all wrong-invoke=0. Do NOT hand-edit seed-descriptors.json.

    (d) Re-assert the per-wave gates over the now-COMPLETE real-app corpus: `node
    scripts/verify-catalog-crosscheck.mjs` (exit 0 -- payment ops class write, destructives destructive,
    reads read; NO under-statement; the payment-op guard passes; the READ_ONLY_SAFE guard passes), `node
    tests/no-dead-entry.test.js` (exit 0 -- every opentabs__ slug resolves to a non-null seam), and `node
    tests/capability-search-eval.test.js` (recall@5>=0.9, wrong-invoke=0.000; cold-start asserts pass).
    This is the LARGEST corpus before Phase-43: widen the cold-start byte budget ONLY if the grown index
    legitimately exceeds the gate while per-descriptor footprint stays flat (params not indexed), with a
    comment noting the grown count -- do NOT change the data layout, do NOT defer the Phase-43 full-corpus
    gate. FLAG the cold-start headroom in the SUMMARY (this sub-batch is the most likely to approach the
    budget; 39-CONTEXT requires flagging it).

    Do NOT edit the importer (beyond Task-1's STEM_OVERRIDES entries), capability-search.js, the gates'
    core logic, or service-denylist.json (Task 1 already extended it).
  </action>
  <verify>
    <automated>node --import tsx ./scripts/import-opentabs-catalog.mjs && test -f catalog/descriptors/opentabs__dominos__place_order.json && test -f catalog/descriptors/opentabs__shopify__create_order.json && test -f catalog/descriptors/opentabs__zillow__search_listings.json && test -f catalog/descriptors/reddit-inbox.json && node -e "const fs=require('fs');const o=JSON.parse(fs.readFileSync('catalog/descriptors/opentabs__dominos__place_order.json','utf8'));if(o.backing!=='dom'||o.sideEffectClass!=='write'){console.error('dominos place_order not DOM-only write',o.backing,o.sideEffectClass);process.exit(1)}const z=JSON.parse(fs.readFileSync('catalog/descriptors/opentabs__zillow__search_listings.json','utf8'));if(z.sideEffectClass!=='read'){console.error('zillow op not read',z.sideEffectClass);process.exit(1)}const inbox=JSON.parse(fs.readFileSync('catalog/descriptors/reddit-inbox.json','utf8'));if(inbox.backing!=='recipe'){console.error('reddit-inbox clobbered',inbox.backing);process.exit(1)}console.log('PASS final sub-batch DOM-only; reddit-inbox intact')" && node scripts/package-extension.mjs && npm run validate:extension && node scripts/verify-catalog-crosscheck.mjs && node tests/no-dead-entry.test.js && node tests/breadth-search-return.test.js && node tests/capability-search-eval.test.js</automated>
  </verify>
  <acceptance_criteria>
    - The remaining apps' descriptors are emitted (opentabs__<brand>__*) and inlined; EVERY one carries backing:'dom'; the merge-time classifyGate did NOT abort; no prior descriptor or hand-authored recipe (reddit-inbox.json) clobbered.
    - validate:extension exits 0 (snapshot reconciled; classification gate + crosscheck + recipe-path guard green); INV-01 byte-stable.
    - verify-catalog-crosscheck.mjs exits 0: payment ops class write/destructive + the payment-op guard passes (DOM-only on sensitive); read-only apps emit only reads + the READ_ONLY_SAFE guard passes.
    - no-dead-entry.test.js exits 0 over the complete corpus; breadth-search-return.test.js passes incl. the new collisions (wrong-invoke=0).
    - capability-search-eval.test.js prints recall@5>=0.9, wrong-invoke=0.000, cold-start asserts green at the LARGEST size (the cold-start headroom flagged in the SUMMARY).
  </acceptance_criteria>
  <done>The remaining real apps are imported as DOM-only data via the frozen machinery (gate passed; payment ops DOM-only on sensitive origins; read-only apps read-only-locked), the complete-corpus snapshot regenerated (INV-01 byte-stable), and the per-wave gates re-asserted over the COMPLETE real-app corpus: crosscheck (incl. payment-op + READ_ONLY_SAFE guards) + no-dead-entry + eval green and the cold-start headroom flagged -- coverage of all real OpenTabs apps is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OpenTabs vendored metadata -> FSB descriptor corpus (build-time) | the remaining commerce/misc metadata; a forbidden field, a denied app, or an unscreened payment origin must never enter the corpus |
| a payment-bearing op -> the side-effect/backing crosscheck (build-time) | a place_order/create_order op must class write AND stay backing:'dom' on a sensitive origin -- never safe-and-API-invocable |
| a read-only safe origin -> the READ_ONLY_SAFE crosscheck (build-time) | a future write op on zillow/grafana (left safe because read-only) must FAIL the build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | Elevation of Privilege | a remaining payment op (place_order/create_order) writable/invocable under Auto = money moved without consent | mitigate | every payment op ships backing:'dom' (frozen default -> DOM-only); the payment-op CI guard passes only because each is DOM-only on a sensitive origin; each origin classified sensitive (39-01 / Task 1) so its DOM-path write is posture-B gated |
| T-39-02 | Elevation of Privilege | a remaining commerce origin landing unscreened -> its write runs under Auto with no re-gate | mitigate | Task 1 classifies every remaining origin BEFORE vendoring (payment-conservative-by-default); the merge-time classifyGate aborts on any unclassified origin |
| T-39-08 | Elevation of Privilege | a read-only safe origin (zillow/grafana) later gaining a write op that ships writable-under-Auto | mitigate | the origins added to READ_ONLY_SAFE_SERVICES (Task 1) -> checkReadOnlySafeOrigins FAILS the build on any non-read op for them (proven by the zillow-write verify) |
| T-39-03 | Tampering | a DENY-01 / money-movement denied app vendored into the completion sub-batch | mitigate | money-movement apps classified denied + NOT vendored (Task 1); only screened-sensitive/safe apps vendored; the merge gate refuses a denied origin's descriptor |
| T-39-09 | Tampering | the completion import clobbering a hand-authored recipe (reddit-inbox.json) -- a regression to an existing T1b read | mitigate | opentabs__<app>__* are distinct filenames/slugs vs the hand-authored recipes; Task 2 asserts reddit-inbox.json is untouched post-import (the 38-03 precedent) |
| T-39-06 | Tampering | a vendored slice emitting a forbidden field name (Wall 1) OR a payment op under-stated as read | mitigate | the importer's recursive forbidden-field pre-scan throws before emit; verify-catalog-crosscheck.mjs fails the build on under-statement |
| T-39-SC | Tampering | npm/pip/cargo installs | accept | no new packages this plan (denylist JSON + gate edit + data-only vendoring + test edits; zod/tsx already devDeps); package-legitimacy gate N/A |
</threat_model>

<verification>
- `node -e` classify probe: shopify/craigslist/dominos/chipotle sensitive; zillow/grafana safe; checkReadOnlySafeOrigins catches a zillow write.
- `node --import tsx ./scripts/import-opentabs-catalog.mjs` emits the remaining apps (all backing:'dom'); the gate did NOT abort; the payment-op + READ_ONLY_SAFE guards pass; reddit-inbox.json untouched; seed-descriptors.json seed-fed.
- `npm run validate:extension` exits 0 (snapshot reconciled; classification gate + crosscheck + recipe-path guard green over the complete real-app corpus).
- `node scripts/verify-catalog-crosscheck.mjs && node tests/no-dead-entry.test.js && node tests/breadth-search-return.test.js && node tests/capability-search-eval.test.js` pass (payment ops write + DOM-only-on-sensitive; read-only apps read-only-locked; collisions wrong-invoke=0; recall@5>=0.9; cold-start within budget, headroom flagged).
</verification>

<success_criteria>
- BRDTH-01 (coverage COMPLETE): all remaining real OpenTabs apps' descriptors are imported via the frozen machinery + returned by search -- enumerateBatchApps over the snapshot now covers every real app (the coverage report 39-07 confirms it).
- BRDTH-02: every remaining origin was screened BEFORE import (payment -> sensitive, read-only -> safe, money-movement -> denied); the merge-time classifyGate passed; the payment-op + READ_ONLY_SAFE guards passed.
- BRDTH-03: every emitted descriptor is DOM-only (backing:'dom', invocable=false, discovery-pending); no payment op armed via recipe/handler.
- No regression: no prior descriptor or hand-authored recipe clobbered; crosscheck (incl. payment-op + READ_ONLY_SAFE guards) + no-dead-entry + the eval (recall@5>=0.9, wrong-invoke=0, cold-start within budget, headroom flagged) green; validate:extension exits 0.
</success_criteria>

<output>
Create `.planning/phases/39-breadth-c-commerce-travel-misc-most-sensitive/39-06-SUMMARY.md` when done.
</output>
