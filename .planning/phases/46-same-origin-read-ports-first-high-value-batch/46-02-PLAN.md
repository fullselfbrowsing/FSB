# 46-02 Plan: Catalog Wiring + Origin Gates

## Objective

Make the new T1a heads visible to the shipped extension, search readiness surface, and build-time safety gates.

## Tasks

1. Import the three new handler modules from `extension/background.js`.
2. Add the three head globals to `HEAD_HANDLER_MODULES`.
3. Mark the 10 selected slugs `t1-ready` in `capability-search.js`.
4. Teach `verify-origin-classification.mjs` to join relative vendored API bases to the handler origin.
5. Add the new handler files to recipe-path and T1 port contract allowlists.

## Acceptance

- `seedHeadHandlers()` can register all 10 new slugs.
- Origin classification reports Netlify, Bitbucket, and CircleCI as same-origin.
- The T1 port contract gate recognizes handler proof for all new rows.
