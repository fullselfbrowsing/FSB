# 46-03 Plan: Verification + Closeout

## Objective

Prove the 10 descriptors now resolve as executable T1a reads and that the existing safety gates still hold.

## Tasks

1. Extend `tests/capability-head-handlers.test.js` for exact URL/query/spec/fallback behavior.
2. Extend `tests/head-handler-upgrade.test.js` for dom-to-T1a slug-exact resolution.
3. Extend `tests/head-handler-cap.test.js`, `tests/verify-origin-classification.test.js`, and `tests/t1-readiness-report.test.js`.
4. Run focused head, origin, readiness, guarded-write, and consent regression tests.
5. Run `npm run validate:extension`.
6. Update roadmap and phase evidence.

## Acceptance

- At least 10 additional read descriptors are T1-ready.
- Guarded writes remain fail-closed.
- No separate-origin or Pattern-D runtime semantics are introduced.
