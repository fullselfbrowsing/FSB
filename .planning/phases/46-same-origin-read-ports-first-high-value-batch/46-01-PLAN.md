# 46-01 Plan: Candidate Selection + Handler Ports

## Objective

Port the first 10 same-origin read descriptors to T1a handler modules without changing descriptor metadata or MCP surface area.

## Tasks

1. Add `catalog/handlers/netlify.js` with four GET handlers pinned to `https://app.netlify.com`.
2. Add `catalog/handlers/bitbucket.js` with three GET handlers pinned to `https://bitbucket.org`.
3. Add `catalog/handlers/circleci.js` with three GET handlers pinned to `https://app.circleci.com`.
4. Mirror the files into `extension/catalog/handlers/`.
5. Add logged-out/wrong-shape guards that return dual-field `RECIPE_DOM_FALLBACK_PENDING`.

## Acceptance

- Every selected slug exports a T1a handler with `sideEffectClass:'read'`.
- Every handler calls `ctx.executeBoundSpec` only.
- Every spec is `GET`, `same-origin-cookie`, and pinned to the app's first-party origin.
