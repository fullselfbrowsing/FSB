# 49-01 Plan: Write Activation Evidence Ledger

## Goal

Create a committed, redacted evidence ledger that records which write/destructive rows are active and which remain guarded fail-closed.

## Scope

- Add `catalog/write-activation-evidence.json`.
- Record active Notion writes with Phase 41 UAT references.
- Record `slack.chat.postMessage` as a legacy active exception.
- Record the five remaining guarded writes with fail-closed reasons and required evidence fields.

## Verification

- Evidence contains no literal token/cookie/secret values.
- Every current active write row has an evidence record.
- Every current guarded fail-closed row has a guarded record.
