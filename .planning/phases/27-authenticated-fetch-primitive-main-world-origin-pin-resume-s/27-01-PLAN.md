---
phase: 27-authenticated-fetch-primitive-main-world-origin-pin-resume-s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extension/utils/capability-interpreter.js
  - tests/capability-interpreter.test.js
  - mcp/src/errors.ts
  - scripts/verify-recipe-path-guard.mjs
autonomous: true
requirements: [FETCH-03, FETCH-04]
must_haves:
  truths:
    - "interpretRecipe folds spec.query into spec.url BEFORE the origin-pin re-assertion"
    - "A recipe whose effective (post-query-fold) URL resolves to a foreign origin is rejected with RECIPE_ORIGIN_MISMATCH and emits no executeScript/fetch"
    - "A protocol-relative effective target is rejected with RECIPE_ORIGIN_MISMATCH"
    - "RECOVERY_AMBIGUOUS surfaces verbatim through the built mcp/build/errors.js (not collapsed to action_rejected)"
    - "capability-fetch.js is registered on RECIPE_PATH_ALLOWLIST and node scripts/verify-recipe-path-guard.mjs still PASSes"
  artifacts:
    - path: "extension/utils/capability-interpreter.js"
      provides: "query-fold + origin-pin re-assertion at the pre-flagged slot; new RECIPE_ORIGIN_MISMATCH code"
      contains: "RECIPE_ORIGIN_MISMATCH"
    - path: "mcp/src/errors.ts"
      provides: "RECOVERY_AMBIGUOUS registered in CODE_ONLY_ERROR_KEYS"
      contains: "RECOVERY_AMBIGUOUS"
    - path: "scripts/verify-recipe-path-guard.mjs"
      provides: "capability-fetch.js on the recipe-path allowlist"
      contains: "extension/utils/capability-fetch.js"
  key_links:
    - from: "extension/utils/capability-interpreter.js"
      to: "createRecipeError RECIPE_ORIGIN_MISMATCH"
      via: "typed RETURN (never throw), BOTH code+errorCode set"
      pattern: "RECIPE_ORIGIN_MISMATCH"
    - from: "mcp/src/errors.ts"
      to: "CODE_ONLY_ERROR_KEYS RECOVERY_AMBIGUOUS"
      via: "resolveErrorKey verbatim passthrough"
      pattern: "RECOVERY_AMBIGUOUS"
---

<objective>
Lay the three foundational, no-new-file edits the Phase 27 fetch primitive depends on, all inside the service worker / MCP layer with the interpreter's no-network charter (26-D-11) preserved:

1. Inside interpretRecipe, FOLD spec.query into spec.url (D-09) and then RE-ASSERT origin-pin against the EFFECTIVE URL (D-08 part 1), filling the slot the schema author pre-flagged. A cross-origin or protocol-relative effective target returns the new typed RECIPE_ORIGIN_MISMATCH before any side effect (FETCH-03 part 1).
2. Register RECOVERY_AMBIGUOUS in mcp/src/errors.ts via the SAME verbatim-passthrough extension point the RECIPE_*/TRIGGER_* families use, so the MCP host can later distinguish "ambiguous - ask the user" from a generic rejection (D-12, FETCH-04 surfacing).
3. Add extension/utils/capability-fetch.js to RECIPE_PATH_ALLOWLIST in scripts/verify-recipe-path-guard.mjs AHEAD of the file's creation (Wave 2) - safe because Check 1 skips a not-yet-existent path and Check 4 only fails on a disk file ABSENT from the allowlist (Wall 1 / D-02, FETCH-01 prerequisite).

Purpose: these are the load-bearing seams Plan 02's capability-fetch.js consumes (a folded+pinned spec, the RECOVERY_AMBIGUOUS code, and a pre-prepared allowlist). Done first so the primitive lands into ready ground.
Output: a query-folded, origin-pinned interpreter with RECIPE_ORIGIN_MISMATCH; a registered RECOVERY_AMBIGUOUS MCP code; an allowlist already naming capability-fetch.js; and extended interpreter tests proving the pin.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-authenticated-fetch-primitive-main-world-origin-pin-resume-s/27-CONTEXT.md
@.planning/phases/27-authenticated-fetch-primitive-main-world-origin-pin-resume-s/27-RESEARCH.md
@.planning/phases/27-authenticated-fetch-primitive-main-world-origin-pin-resume-s/27-PATTERNS.md

<interfaces>
<!-- Exact contracts the executor needs. Extracted from the codebase on automation-worktree. Use directly - no exploration required. -->

interpretRecipe spec-assembly slot to edit (extension/utils/capability-interpreter.js around lines 305-318): spec is assembled with url:templated.url at line 307, query:built.query at line 311 (values ALREADY encodeURIComponent-escaped by buildRequest - do NOT double-encode the value); bindAuthStrategy is called at line 318. The fold + pin insert goes between the assembled spec (line 315) and the bindAuthStrategy call (line 318).

Typed-error helper already in the file (extension/utils/capability-interpreter.js:85-93) - createRecipeError(code, extra): RETURN never throw, sets BOTH code and errorCode and error to the same string, then copies extra fields. Reuse it for RECIPE_ORIGIN_MISMATCH.

Deferred-pin comment that pre-flags this work (extension/utils/capability-interpreter.js:128-129): states origin-pin is ENFORCED IN PHASE 27 (FETCH-03).

errors.ts registration point (mcp/src/errors.ts:54-68) - CODE_ONLY_ERROR_KEYS Set, last member 'TOOL_REMOVED' at line 67. resolveErrorKey (lines 104-109) reads errorCode then code and returns it verbatim when the Set has it. The RECIPE_* family (incl. the new RECIPE_ORIGIN_MISMATCH) ALREADY surfaces via the existing /RECIPE_.+/ regex arm - NO errors.ts edit needed for RECIPE_ORIGIN_MISMATCH; only RECOVERY_AMBIGUOUS needs registering.

Allowlist to extend (scripts/verify-recipe-path-guard.mjs:84-91) - three existing capability entries then lib entries. ADD 'extension/utils/capability-fetch.js' after the auth-strategies entry, before the lib/ entries. Check 1 skips a path whose file does not exist yet (safeRead null -> continue, line 124); Check 4 (lines 272-281) fails only when a DISK file is ABSENT from the allowlist. So adding capability-fetch.js now keeps the guard green.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fold query into URL then re-assert origin-pin in interpretRecipe (RECIPE_ORIGIN_MISMATCH)</name>
  <files>extension/utils/capability-interpreter.js</files>
  <read_first>
    - extension/utils/capability-interpreter.js (READ lines 50-93 the accessors + createRecipeError; 122-145 templateEndpoint + the 128-129 deferred-pin comment; 290-324 the spec-assembly slot)
    - .planning/phases/27-authenticated-fetch-primitive-main-world-origin-pin-resume-s/27-RESEARCH.md (the interpretRecipe edit fold-query-plus-origin-pin sketch, lines 474-494)
    - .planning/phases/27-authenticated-fetch-primitive-main-world-origin-pin-resume-s/27-PATTERNS.md (the capability-interpreter.js MODIFIED section, lines 101-130)
    - extension/utils/capability-recipe-schema.js (READ lines 95-108 origin/endpoint patterns; 131-147 the csrf if/then - context for why the schema alone is insufficient and the interpreter must re-resolve the EFFECTIVE url)
  </read_first>
  <behavior>
    - Empty query map: effectiveUrl equals templated.url; spec.url unchanged; success.
    - Same-origin query: effectiveUrl gains a key=value suffix (with ? or & as appropriate); value NOT double-encoded; spec.url equals effectiveUrl; success.
    - Cross-origin effective target (query value injects an absolute foreign URL, or endpoint resolves cross-origin): returns success:false with code and errorCode both RECIPE_ORIGIN_MISMATCH; no side effect.
    - Protocol-relative effective target (begins //evil.com): returns RECIPE_ORIGIN_MISMATCH; no side effect.
  </behavior>
  <action>
    Between the assembled spec object (capability-interpreter.js line 315) and the bindAuthStrategy call (line 318), insert two steps, preserving the no-network charter (NO fetch, NO chrome.scripting, NO executeScript anywhere in this file - 26-D-11):

    Step A (D-09, query-fold): build effectiveUrl from templated.url by appending the already-escaped built.query map. Keys come from Object.keys(built.query or empty). For each key emit encodeURIComponent(key) + '=' + built.query[key] - the VALUES are already encodeURIComponent-escaped by buildRequest fillPlacementMap(..., true), so encode ONLY the key, never the value. Join pairs with '&'. Append with '?' when templated.url has no '?' else '&'. When the query map is empty, effectiveUrl equals templated.url.

    Step B (D-08 part 1, origin-pin re-assertion): resolve new URL(effectiveUrl, recipe.origin) inside a try/catch (catch means mismatch). If resolution failed OR resolved.origin does not equal recipe.origin, RETURN createRecipeError('RECIPE_ORIGIN_MISMATCH', { url: effectiveUrl, origin: recipe.origin }). This rejects protocol-relative //evil.com (which new URL('//evil.com', origin) re-targets to a foreign origin) and any query-injected cross-origin re-target BEFORE any caller can act on the spec.

    Then set spec.url = effectiveUrl (replacing the bare templated.url) so the bound spec carries the TRUE effective request target, and proceed to bindAuthStrategy unchanged.

    Use the new code string RECIPE_ORIGIN_MISMATCH exactly. Do NOT touch errors.ts for this code - the existing /RECIPE_.+/ passthrough already surfaces it. Keep the file ASCII-only and free of eval, new Function, and the dynamic-import construct even in comments (it is on the recipe-path allowlist; CI guard Check 1 is a word-boundary regex).
  </action>
  <verify>
    <automated>node tests/capability-interpreter.test.js</automated>
  </verify>
  <acceptance_criteria>
    - grep -n RECIPE_ORIGIN_MISMATCH extension/utils/capability-interpreter.js returns at least one line inside interpretRecipe's assembly path.
    - Source order is fold-then-pin: the query append occurs BEFORE the new URL(effectiveUrl, recipe.origin) resolution.
    - spec.url is assigned effectiveUrl (not the bare templated.url) on the success path.
    - No-network/no-dynamic-code invariant intact: running grep -nE for fetch, executeScript, eval-open-paren, new Function, import-open-paren, or chrome.scripting against the file (excluding comment lines) returns nothing.
    - node tests/capability-interpreter.test.js exits 0 (existing no-network boundary assertion still holds: executeScript and fetch recorders called 0 times).
  </acceptance_criteria>
  <done>interpretRecipe folds query then pins origin; a foreign or protocol-relative effective URL returns RECIPE_ORIGIN_MISMATCH before any side effect; the no-network charter and existing tests are intact.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the interpreter test for query-fold + RECIPE_ORIGIN_MISMATCH (incl. protocol-relative)</name>
  <files>tests/capability-interpreter.test.js</files>
  <read_first>
    - tests/capability-interpreter.test.js (READ lines 1-130 the loader + executeScript/fetch recorders + check() helper; the existing recipe-drive cases to mirror)
    - extension/utils/capability-interpreter.js (the edit from Task 1 - the exact effectiveUrl/pin behavior to assert)
    - .planning/phases/27-authenticated-fetch-primitive-main-world-origin-pin-resume-s/27-RESEARCH.md (Validation Architecture FETCH-03 row line 645; the Wave 0 Gaps item about extending capability-interpreter.test.js with the //evil.com rejection, line 664)
    - catalog/recipes/_fixtures/valid-recipe.json (the recipe field-set the cases construct from)
  </read_first>
  <action>
    Add cases to tests/capability-interpreter.test.js reusing the existing check(cond, msg) helper, the installChromeMock substrate, and the executeScriptCalls/fetchCalls recorders already set up at the top of the file. Assert:

    (a) A recipe with a request.query placement whose templated value keeps the same origin folds into spec.url: interpretRecipe returns success:true and spec.url contains the expected key=value suffix (with ? or &), value NOT double-encoded.

    (b) A recipe/args combination whose folded query would re-target to a DIFFERENT origin returns success:false with code and errorCode both RECIPE_ORIGIN_MISMATCH. Construct with origin https://github.com and a query value injecting an absolute https://evil.com/... so new URL(effectiveUrl, origin).origin does not equal origin. If schema/template encoding prevents an absolute-URL value from surviving, assert the equivalent effective-cross-origin rejection via the most reachable construction; the load-bearing assertion is the typed rejection of an effective cross-origin target.

    (c) A protocol-relative effective target is rejected: arrange an effectiveUrl beginning //evil.com (e.g. via a query value) and assert RECIPE_ORIGIN_MISMATCH.

    (d) Across ALL new rejection cases, the executeScriptCalls and fetchCalls recorders remain EMPTY (no side effect) - reuse the suite's recorder assertion or assert length 0 inline.

    Keep the file ASCII-only. It is test-only and NOT on the recipe-path allowlist.
  </action>
  <verify>
    <automated>node tests/capability-interpreter.test.js</automated>
  </verify>
  <acceptance_criteria>
    - node tests/capability-interpreter.test.js exits 0 with new PASS lines for: query folds into spec.url (no double-encode); cross-origin effective target maps to RECIPE_ORIGIN_MISMATCH; protocol-relative maps to RECIPE_ORIGIN_MISMATCH.
    - Every RECIPE_ORIGIN_MISMATCH case asserts the executeScript recorder and the fetch recorder are both empty before/at rejection.
    - grep -c RECIPE_ORIGIN_MISMATCH tests/capability-interpreter.test.js returns at least 2 (cross-origin + protocol-relative).
  </acceptance_criteria>
  <done>The interpreter test proves the query-fold and both origin-pin rejection paths (cross-origin and protocol-relative) with no side effect, and exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: Register RECOVERY_AMBIGUOUS in errors.ts + add capability-fetch.js to the recipe-path allowlist</name>
  <files>mcp/src/errors.ts, scripts/verify-recipe-path-guard.mjs</files>
  <read_first>
    - mcp/src/errors.ts (READ lines 54-68 the CODE_ONLY_ERROR_KEYS Set; 100-135 resolveErrorKey and the RECIPE_*/TRIGGER_* verbatim-passthrough comment explaining the surfacing mechanism)
    - scripts/verify-recipe-path-guard.mjs (READ lines 80-135 the allowlist + Check 1 grep + FORBIDDEN word-boundary patterns; 248-298 Check 4 disk-glob drift + the PASS/exit convention)
    - .planning/phases/27-authenticated-fetch-primitive-main-world-origin-pin-resume-s/27-PATTERNS.md (the mcp/src/errors.ts MODIFIED section lines 152-177 and the verify-recipe-path-guard.mjs MODIFIED section lines 181-207)
    - tests/capability-interpreter.test.js (READ lines 250-266 region: the dynamic-import-of-built-errors passthrough check pattern - the same shape proves RECOVERY_AMBIGUOUS, though that assertion lives in Plan 02's suite)
  </read_first>
  <action>
    Two one-line additive edits, both INV-01-safe (no MCP tool schema touched, no .cjs mirror touched):

    Edit A (D-12, mcp/src/errors.ts): add the string member 'RECOVERY_AMBIGUOUS' to the CODE_ONLY_ERROR_KEYS Set, after the last member 'TOOL_REMOVED' (line 67), with a one-line ASCII comment tagging it Phase 27 FETCH-04 mid-mutation eviction ambiguity. Do NOT extend the line-133 regex - the Set add is the cleaner single-token registration. This makes resolveErrorKey return RECOVERY_AMBIGUOUS verbatim (not action_rejected) when a result carries code/errorCode of RECOVERY_AMBIGUOUS.

    Edit B (D-02, scripts/verify-recipe-path-guard.mjs): add the repo-relative string literal 'extension/utils/capability-fetch.js' to RECIPE_PATH_ALLOWLIST (lines 84-91), in the capability cluster immediately after the capability-auth-strategies entry and BEFORE the extension/lib entries. Do NOT touch SANCTIONED_SITES (lines 96-100) or the FORBIDDEN regexes (lines 105-109). The file does not exist yet (Plan 02 creates it); the guard stays green (Check 1 skips an absent path; Check 4 only fails on a DISK file absent from the allowlist).

    After both edits, rebuild the MCP module so the built errors.js reflects Edit A: npm --prefix mcp run build.
  </action>
  <verify>
    <automated>npm --prefix mcp run build && grep -q RECOVERY_AMBIGUOUS mcp/build/errors.js && node scripts/verify-recipe-path-guard.mjs</automated>
  </verify>
  <acceptance_criteria>
    - mcp/src/errors.ts CODE_ONLY_ERROR_KEYS Set contains the member RECOVERY_AMBIGUOUS (grep -c returns >= 1).
    - npm --prefix mcp run build exits 0 and grep -q RECOVERY_AMBIGUOUS mcp/build/errors.js succeeds (the built artifact carries the new key; the full verbatim-passthrough assertion via mapFSBError runs in Plan 02's tests/capability-fetch.test.js).
    - scripts/verify-recipe-path-guard.mjs RECIPE_PATH_ALLOWLIST array contains 'extension/utils/capability-fetch.js'.
    - node scripts/verify-recipe-path-guard.mjs exits 0 (PASS) even though capability-fetch.js does not exist yet.
  </acceptance_criteria>
  <done>RECOVERY_AMBIGUOUS is registered and present in the built errors module; capability-fetch.js is on the recipe-path allowlist; the guard still PASSes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recipe data -> interpreter | The recipe (and invoke args) are untrusted-shaped data; the interpreter binds them into a request spec. The effective request URL is the security-sensitive output of this boundary. |
| SW result -> MCP host | A typed error code (code/errorCode) crosses to the MCP host, which maps it to a user-facing message. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Tampering | interpretRecipe query-fold | mitigate | Fold spec.query into the URL BEFORE the origin-pin (D-09) so the pin guards the TRUE effective target; a query-injected re-target cannot evade validation. |
| T-27-02 | Elevation of Privilege / Spoofing | interpretRecipe origin-pin | mitigate | Re-assert new URL(effectiveUrl, recipe.origin).origin === recipe.origin; reject cross-origin with RECIPE_ORIGIN_MISMATCH before returning the spec (D-08 part 1). The active-tab session pin is Plan 02. |
| T-27-03 | Tampering | protocol-relative endpoint | mitigate | new URL('//evil.com', origin) resolves to a foreign origin and is rejected by the same pin; the schema also rejects // at line 104-108 (defense in depth). |
| T-27-04 | Tampering | query double-encoding | accept | built.query values are already encodeURIComponent-escaped; the fold encodes only the key. Re-encoding the value would corrupt data, not improve safety; accepted with a code comment. |
| T-27-05 | Information Disclosure | RECOVERY_AMBIGUOUS / RECIPE_ORIGIN_MISMATCH surfacing | mitigate | Only the typed code string is surfaced (no secrets, no body) via the existing CODE_ONLY/regex passthrough; INV-01-safe (no tool schema changed). |
| T-27-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs NO packages (npm --prefix mcp run build compiles existing source only). Package Legitimacy Gate is N/A for Phase 27 (RESEARCH.md confirms zero new dependencies). |

Highest-severity unmitigated item: none. T-27-04 is an accepted non-issue (data-correctness, not a vulnerability). All credential-redirection vectors (T-27-01..03) are mitigated at the interpreter; the complementary active-tab session pin lands in Plan 02.
</threat_model>

<verification>
- node tests/capability-interpreter.test.js exits 0 (query-fold + origin-pin cases green; no-network boundary intact).
- npm --prefix mcp run build exits 0 and mcp/build/errors.js contains RECOVERY_AMBIGUOUS.
- node scripts/verify-recipe-path-guard.mjs exits 0 (PASS) with capability-fetch.js on the allowlist (file not yet on disk - guard still green).
- No new dynamic-code constructs (eval / new Function / dynamic import) introduced into capability-interpreter.js.
</verification>

<success_criteria>
- interpretRecipe folds query then pins origin; cross-origin and protocol-relative effective targets are rejected with RECIPE_ORIGIN_MISMATCH before any side effect (FETCH-03 part 1).
- RECOVERY_AMBIGUOUS is registered in CODE_ONLY_ERROR_KEYS and present in the built errors module (FETCH-04 surfacing prerequisite).
- capability-fetch.js is on RECIPE_PATH_ALLOWLIST; the recipe-path CI guard PASSes (Wall 1 / D-02 prerequisite for Plan 02's new file).
- The interpreter's no-network charter (26-D-11) is preserved; all existing capability tests still pass.
</success_criteria>

<output>
Create `.planning/phases/27-authenticated-fetch-primitive-main-world-origin-pin-resume-s/27-01-SUMMARY.md` when done.
</output>
