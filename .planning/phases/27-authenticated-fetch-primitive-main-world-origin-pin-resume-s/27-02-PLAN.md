---
phase: 27-authenticated-fetch-primitive-main-world-origin-pin-resume-s
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - extension/utils/capability-fetch.js
  - extension/background.js
  - catalog/recipes/github-notifications.json
  - catalog/recipes/_fixtures/valid-github-notifications.json
  - tests/capability-fetch.test.js
  - package.json
autonomous: true
requirements: [FETCH-01, FETCH-02, FETCH-03, FETCH-04, FETCH-05]
user_setup: []
must_haves:
  truths:
    - "capabilityFetchInPage is a self-contained func referencing only Web APIs and args[0] - no jmespath, no getFSB, no require, no importScripts, no sibling helper"
    - "The in-page func sets credentials:'include' and threads a scraped CSRF token into headers[csrfSource.header] before fetch"
    - "executeBoundSpec rejects when the active-tab origin != spec.origin and records NO executeScript call (active-tab session pin, FETCH-03 part 2)"
    - "executeBoundSpec writes a BEFORE_API_REQUEST snapshot before executeScript and deletes it on completion"
    - "A mutating-method in-flight snapshot classifies to RECOVERY_AMBIGUOUS and is never re-issued; a GET is re-issuable"
    - "The read-only extract runs SW-side via FsbCapabilityInterpreter.getFSBJmespath().search after the body returns"
    - "capability-fetch.js contains zero eval / new Function / dynamic-import constructs and node scripts/verify-recipe-path-guard.mjs PASSes"
    - "The hardcoded github.com GET /notifications recipe validates against the closed schema"
    - "node tests/capability-fetch.test.js exits 0 and is wired into the package.json test chain"
    - "Decisions covered: D-01: capability-fetch.js module; D-03: serialization-safe in-page func; D-04: direct-drive tabId (no MCP tool/router); D-05: in-page CSRF scrape; D-06: from-response deferred to Phase 29; D-07: SW-side extract; D-10: resume-sidecar write; D-11: classifyOnWake mid-mutation classifier; D-13: github GET /notifications proof recipe; D-15: CI mock smoke half; D-16: reserved /_graphql csrf exemplar"
  artifacts:
    - path: "extension/utils/capability-fetch.js"
      provides: "capabilityFetchInPage (page func) + executeBoundSpec (SW wrapper) + classifyOnWake (mid-mutation classifier)"
      exports: ["capabilityFetchInPage", "executeBoundSpec", "classifyOnWake"]
    - path: "catalog/recipes/github-notifications.json"
      provides: "the hardcoded FETCH-05 proof recipe (github.com GET /notifications, same-origin-cookie)"
      contains: "same-origin-cookie"
    - path: "tests/capability-fetch.test.js"
      provides: "CI-side smoke suite for FETCH-01..05 (stubbed executeScript recorder, in-memory storage, built-errors passthrough)"
      min_lines: 120
  key_links:
    - from: "extension/utils/capability-fetch.js executeBoundSpec"
      to: "chrome.scripting.executeScript world MAIN func capabilityFetchInPage args spec"
      via: "fixed func injection (NOT eval/new Function)"
      pattern: "world.*MAIN"
    - from: "extension/utils/capability-fetch.js executeBoundSpec"
      to: "FsbMcpTaskStore.writeSnapshot BEFORE_API_REQUEST"
      via: "resume-sidecar write before fetch"
      pattern: "BEFORE_API_REQUEST"
    - from: "extension/utils/capability-fetch.js executeBoundSpec"
      to: "FsbCapabilityInterpreter.getFSBJmespath().search"
      via: "SW-side extract after body returns"
      pattern: "getFSBJmespath"
    - from: "extension/background.js"
      to: "importScripts utils/capability-fetch.js"
      via: "additive SW bootstrap, last of the capability family"
      pattern: "capability-fetch.js"
---

<objective>
Build the Wall-2 spine: a fixed, bundled page-MAIN-world authenticated fetch that carries first-party HttpOnly cookies, scrapes CSRF in-page, pins origin a second time at the active tab, survives SW eviction via the resume-sidecar, classifies mid-mutation ambiguity, and runs the read-only extract SW-side. Drive ONE hardcoded github.com recipe directly (no MCP tool, no router - those are Phase 28/29) and prove the whole wiring in CI with a stubbed executeScript recorder.

Purpose: this is the riskiest unknown of the milestone (does a same-origin credentialed fetch in the page MAIN world actually attach the user's cookies?) reduced to its smallest provable form. The CI suite proves every mechanic FSB owns; the one property only a live browser can show (real cookies attach -> logged-in shape) is deferred to Plan 03's human-gated UAT.
Output: extension/utils/capability-fetch.js (new, on the already-prepared allowlist), the additive background.js importScripts wiring, the hardcoded recipe (+ a _fixtures copy for the build-time guard), and tests/capability-fetch.test.js wired into npm test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-authenticated-fetch-primitive-main-world-origin-pin-resume-s/27-CONTEXT.md
@.planning/phases/27-authenticated-fetch-primitive-main-world-origin-pin-resume-s/27-RESEARCH.md
@.planning/phases/27-authenticated-fetch-primitive-main-world-origin-pin-resume-s/27-PATTERNS.md
@.planning/phases/27-authenticated-fetch-primitive-main-world-origin-pin-resume-s/27-VALIDATION.md

<interfaces>
<!-- Exact contracts the executor needs, extracted on automation-worktree. Use directly - no exploration required. -->

Module shell to clone (extension/utils/mcp-task-store.js:1-2, 59-61, 177-194): a dual-export IIFE assigning a global Fsb* name AND module.exports, with a lazy _getChrome() accessor that reads globalThis.chrome at call time (so the module loads under the Node harness where chrome is mocked AFTER load). Mirror this exact shell. New global name: FsbCapabilityFetch; exports { capabilityFetchInPage, executeBoundSpec, classifyOnWake }.

MAIN-world injection seam to clone the SHAPE of (extension/ws/mcp-bridge-client.js:908-962, _handleExecuteJS): await chrome.scripting.executeScript({ target:{tabId}, world:'MAIN', func, args:[...] }); read const injectionResult = results && results[0]; if absent return failure; read injectionResult.result; wrap the whole call in try/catch that on a thrown executeScript returns { success:false, error:'... failed: ...' }. CRITICAL: that seam uses new Function(userCode) - capability-fetch.js MUST NOT; the func is the FIXED named function capabilityFetchInPage. (The sibling seam tool-executor.js:374-400 uses eval(jsCode) - same prohibition.)

Resume-sidecar public API (extension/utils/mcp-task-store.js:128-167): writeSnapshot(taskId, snapshotObj) (no-ops on bad input), readSnapshot(taskId) (null when unknown), deleteSnapshot(taskId) (idempotent; empties the key when the last record is gone), listInFlightSnapshots() (filters status === 'in_progress'). Reached as globalThis.FsbMcpTaskStore. Snake_case envelope keys used by existing writers (mcp-bridge-client.js:1321-1331): task_id, status, started_at, last_heartbeat_at, target_tab_id, current_step (existing writers put a NUMBER; Phase 27 writes the STRING 'BEFORE_API_REQUEST'), plus Phase-27 additions method and origin. Mixed current_step types are SAFE: reconciliation filters only on status==='in_progress' (mcp-task-store.js:166), never on current_step type.

SW-side JMESPath accessor (extension/utils/capability-interpreter.js:76-78): getFSBJmespath() returns the lowercase jmespath global or null. Reached from the wrapper as FsbCapabilityInterpreter.getFSBJmespath(); call jp.search(json, spec.extract) inside try/catch (leave data raw on throw). The engine is NOT in page scope - this is why extract runs SW-side (D-07).

Typed-error RETURN discipline (extension/utils/capability-interpreter.js:85-93, createRecipeError): set BOTH code and errorCode (and error) to the same string. Use for the wrapper's RECIPE_ORIGIN_MISMATCH (active-tab mismatch) and RECOVERY_AMBIGUOUS returns. RECIPE_ORIGIN_MISMATCH already surfaces via the existing /RECIPE_.+/ errors.ts arm; RECOVERY_AMBIGUOUS was registered in Plan 01.

Lattice marker taxonomy to REUSE-BY-STRING ONLY (extension/ai/lattice-runtime-adapter.js:281-307): marker strings BEFORE_API_REQUEST -> ON_ERROR_SW_EVICTION_MID_REQUEST; non-safe -> RECOVERY_AMBIGUOUS. CAVEAT-1: Lattice resume() reads snapshot.payload (JSON string) + state._currentStepName (camelCase); the task-store envelope is a FLAT object with current_step (snake_case). DO NOT feed a task-store snapshot into Lattice resume(). Define a THIN LOCAL classifyOnWake(snapshot) that reads the flat current_step + method.

Auth-strategy spec shapes (extension/utils/capability-auth-strategies.js:65-82): same-origin-cookie adds credentials:'include' to the spec; csrf-header-scrape adds csrfSource (default { from:'meta', selector:'meta[name=csrf-token]', header:'X-CSRF-Token' }). The bound spec field set the wrapper/func consume: { url, method, headers, body, query, authStrategy, csrfSource?, _authNeed?, credentials?, origin, extract }.

background.js capability importScripts block (extension/background.js:106-134): existing order jmespath -> minisearch -> cfworker-json-schema -> capability-recipe-schema -> capability-auth-strategies -> capability-interpreter, each wrapped in try { importScripts(...) } catch (e) { console.error(...) }. mcp-task-store.js loads far earlier (~line 34) so FsbMcpTaskStore is already present.

Hardcoded recipe shape that PASSES the closed schema (validated field-by-field in RESEARCH.md:534-556): { schemaVersion:1, id:'github.notifications', origin:'https://github.com', endpoint:'/notifications', method:'GET', authStrategy:'same-origin-cookie', extract:'@' }. extract '@' is the JMESPath identity (returns the whole body) and is optional. csrf is omitted (only required when authStrategy==='csrf-header-scrape').

Test scaffold analogs: tests/capability-interpreter.test.js (loader via vm.runInThisContext for the cfworker IIFE, installChromeMock, executeScript recorder at lines 74-97, built-errors dynamic import passthrough at lines 250-266); tests/mcp-task-store.test.js (freshRequireStore + installChromeMock backing real in-memory chrome.storage.session, storage round-trip); tests/recipe-path-guard.test.js (passed/failed counters + check()). Harness: tests/fixtures/run-task-harness.js exports installChromeMock, installVirtualClock, createStorageArea.

package.json test chain (package.json:17): the &&-chain ends with ... && node tests/recipe-path-guard.test.js. Append && node tests/capability-fetch.test.js after it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: capability-fetch.js module shell + capabilityFetchInPage (serialization-safe in-page func)</name>
  <files>extension/utils/capability-fetch.js</files>
  <read_first>
    - extension/utils/mcp-task-store.js (READ lines 1-2, 59-61, 177-194 the dual-export IIFE shell + lazy _getChrome)
    - extension/ws/mcp-bridge-client.js (READ lines 908-962 the _handleExecuteJS injection func shape - clone the SHAPE, NOT the new Function body)
    - .planning/phases/27-authenticated-fetch-primitive-main-world-origin-pin-resume-s/27-RESEARCH.md (the capabilityFetchInPage sketch lines 356-405, and Pitfall 1 lines 321-325 + Pitfall 5 lines 345-349)
    - .planning/phases/27-authenticated-fetch-primitive-main-world-origin-pin-resume-s/27-PATTERNS.md (the capability-fetch.js NEW section lines 29-97, especially the in-page func properties lines 73-77 and CAVEAT-2 line 74)
    - extension/utils/capability-interpreter.js (READ lines 1-49 the header/ASCII-only convention and the NO EMOJIS ASCII-only source footer to mirror)
  </read_first>
  <behavior>
    - capabilityFetchInPage(spec) references ONLY document, fetch, JSON, URL, Object and args[0] (spec). It contains NO identifier jmespath / getFSB / require / importScripts / FsbMcpTaskStore / FsbCapabilityInterpreter.
    - When spec.csrfSource.from === 'meta' and selector set: token read via document.querySelector(selector); for an input selector the value is read from .value, for other elements from .content/getAttribute('content'); token threaded into headers[spec.csrfSource.header] before fetch.
    - fetch is called with credentials:'include' and redirect:'manual'; a GET/HEAD carries no body.
    - The func reads status and url FIRST, then response.text() inside try/catch, size-caps the text at a fixed CAP, then attempts JSON.parse inside try/catch; returns { ok, status, finalUrl, redirected, json, text } and NEVER any cookie/auth material; on a thrown error returns { error: message }.
  </behavior>
  <action>
    Create extension/utils/capability-fetch.js as a dual-export IIFE mirroring mcp-task-store.js's shell: assign global.FsbCapabilityFetch and module.exports = { capabilityFetchInPage, executeBoundSpec, classifyOnWake }; include the lazy function _getChrome() that returns globalThis.chrome or null. Begin the file with the same NO EMOJIS, ASCII-only source header-block convention used by capability-interpreter.js, and a doc comment naming Phase 27 / FETCH-01..05 / D-01..D-16. (executeBoundSpec and classifyOnWake are added in Task 2 - define stubs now or implement Task 1's func first; the export object must list all three names by end of Task 2.)

    Implement capabilityFetchInPage(spec) as a FIXED, self-contained named function (D-03), modeling RESEARCH.md:356-405. It MUST reference only Web APIs (document, fetch, JSON, URL, Object) and args[0]; NO closure vars, NO sibling helpers, NO SW globals (no jmespath, no getFSB*, no FsbMcpTaskStore). Behavior:
    - FETCH-02 CSRF live-scrape BEFORE the request (D-05): when spec.csrfSource && spec.csrfSource.header, read the token. For from==='meta' with a selector, querySelector(selector); read .value when the matched element is an input (CAVEAT-2: the reserved /_graphql recipe's input[name=authenticity_token] holds the token in .value, not .content), otherwise el.getAttribute('content') || el.content. For from==='cookie' (planner discretion to ship now or defer), a minimal document.cookie parse keyed by spec.csrfSource.selector. Thread a found token into headers[spec.csrfSource.header].
    - FETCH-01: fetch(spec.url, { method: spec.method || 'GET', headers, body, credentials:'include', redirect:'manual' }). credentials:'include' is what attaches first-party HttpOnly cookies (Wall 2). redirect:'manual' keeps a 302 -> /login observable (D-14). Only attach init.body for non-GET/HEAD methods.
    - Defensive body read (Pitfall 5): read status and url FIRST; then response.text() in try/catch; size-cap the text at a fixed CAP (your discretion, e.g. 256*1024) before parsing; JSON.parse in try/catch (null on failure). Return { ok, status, finalUrl, redirected, json, text } where text is null when json parsed. NEVER return cookies or auth material.

    Keep the file ASCII-only and free of eval / new Function / the dynamic-import construct EVEN IN COMMENTS OR STRINGS (it is on the recipe-path allowlist that Plan 01 prepared; CI guard Check 1 is a word-boundary regex - a comment like no-eval-paren would trip it). Add the closing NO EMOJIS, ASCII-only source footer line.
  </action>
  <verify>
    <automated>node scripts/verify-recipe-path-guard.mjs</automated>
  </verify>
  <acceptance_criteria>
    - extension/utils/capability-fetch.js exists, exports FsbCapabilityFetch globally and via module.exports with capabilityFetchInPage in the export object.
    - Serialization-safety static guard: capabilityFetchInPage.toString() contains the substrings credentials and include and contains NONE of jmespath, getFSB, require, importScripts, FsbMcpTaskStore, FsbCapabilityInterpreter (assert in Task 3's test; runnable now via a one-off node require + toString check).
    - The fetch init uses credentials:'include' and redirect:'manual' (grep -n confirms both literals).
    - node scripts/verify-recipe-path-guard.mjs exits 0 (PASS) - capability-fetch.js is on the allowlist (Plan 01) AND contains no forbidden dynamic-code construct; Check 4 now counts 4 on-disk capability modules all allowlisted.
    - The file is ASCII-only (no non-ASCII bytes) and ends with the NO EMOJIS, ASCII-only source footer.
  </acceptance_criteria>
  <done>capability-fetch.js exists with a serialization-safe capabilityFetchInPage that scrapes CSRF, fetches with credentials:'include', and returns only non-secret response data; the recipe-path guard passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: executeBoundSpec (active-tab pin + sidecar + SW-side extract) + classifyOnWake + background wiring</name>
  <files>extension/utils/capability-fetch.js, extension/background.js</files>
  <read_first>
    - extension/utils/capability-fetch.js (the Task 1 shell + capabilityFetchInPage - this task adds executeBoundSpec + classifyOnWake into the same IIFE)
    - extension/ws/mcp-bridge-client.js (READ lines 908-962 the executeScript read shape; 1318-1369 the in_progress + terminal write cadence + best-effort catch posture)
    - extension/utils/mcp-task-store.js (READ lines 128-167 writeSnapshot/readSnapshot/deleteSnapshot/listInFlightSnapshots)
    - extension/ai/lattice-runtime-adapter.js (READ lines 278-308 the resume() marker taxonomy - REUSE THE STRINGS ONLY; CAVEAT-1)
    - extension/utils/capability-interpreter.js (READ lines 76-78 getFSBJmespath; 85-93 createRecipeError dual-field)
    - extension/background.js (READ lines 104-138 the capability importScripts block + load order)
    - .planning/phases/27-authenticated-fetch-primitive-main-world-origin-pin-resume-s/27-RESEARCH.md (executeBoundSpec sketch lines 408-471; resume-sidecar cadence lines 260-281; ResumePolicy open question 2 lines 603-606; background.js wiring lines 523-531)
  </read_first>
  <behavior>
    - executeBoundSpec(spec, tabId): resolves the tab origin via chrome.tabs.get(tabId); if tabOrigin is null or != spec.origin, returns { success:false, code:'RECIPE_ORIGIN_MISMATCH', errorCode:'RECIPE_ORIGIN_MISMATCH', error } and performs NO executeScript (FETCH-03 part 2).
    - On a matching origin: writes a BEFORE_API_REQUEST in_progress snapshot (with method + origin) BEFORE executeScript; calls executeScript({world:'MAIN', func:capabilityFetchInPage, args:[spec]}); on completion writes a terminal snapshot then deleteSnapshot; returns a normalized { success, status, finalUrl, redirected, data, text } or { success:false, error }.
    - extract: when spec.extract is set and the returned json is non-null, data = FsbCapabilityInterpreter.getFSBJmespath().search(json, spec.extract) inside try/catch (raw json on throw or when the engine is absent).
    - classifyOnWake(snapshot): for an in-flight snapshot whose method is POST/PUT/PATCH/DELETE returns 'RECOVERY_AMBIGUOUS'; for GET (or HEAD) returns a re-issuable verdict; a SAFE/absent marker is treated per the Lattice taxonomy strings. Never returns a verdict that blind-retries a mutating method.
    - All writeSnapshot/deleteSnapshot calls are best-effort (guarded + catch) and never block or crash the fetch.
  </behavior>
  <action>
    Extend extension/utils/capability-fetch.js (same IIFE) with executeBoundSpec(spec, tabId) and classifyOnWake(snapshot), modeling RESEARCH.md:408-471. Reach SW globals via typeof-guards (NOT closure/import): var store = globalThis.FsbMcpTaskStore; the jmespath engine via (FsbCapabilityInterpreter && FsbCapabilityInterpreter.getFSBJmespath) ? FsbCapabilityInterpreter.getFSBJmespath() : null.

    executeBoundSpec(spec, tabId):
    1. FETCH-03 part 2 (D-08 part 2): resolve the active/owned tab via _getChrome().tabs.get(tabId); compute tabOrigin = new URL(tab.url).origin in try/catch. If !tabOrigin or tabOrigin !== spec.origin, RETURN a dual-field RECIPE_ORIGIN_MISMATCH (set code AND errorCode, mirror createRecipeError) BEFORE any executeScript. This is what makes FETCH-01 actually authenticated (cookies attach only on spec.origin).
    2. FETCH-04 (D-10): synthesize a task id (your discretion, unique in the in-flight window + discoverable by listInFlightSnapshots, e.g. cap_fetch_ + spec.origin + _ + Date.now()). If store && store.writeSnapshot, await writeSnapshot(taskId, { task_id, status:'in_progress', started_at:Date.now(), last_heartbeat_at:Date.now(), target_tab_id:tabId, current_step:'BEFORE_API_REQUEST', method:spec.method, origin:spec.origin }) - best-effort (.catch). The single bounded fetch needs NO 30s heartbeat; the cadence collapses to write -> fetch -> terminal -> delete.
    3. Clone the executeScript read shape (mcp-bridge-client.js:915-948) with func:capabilityFetchInPage, args:[spec], world:'MAIN'. Wrap in try/catch: on a thrown executeScript (restricted page / tab gone), write a terminal error snapshot, deleteSnapshot, and return { success:false, error:'executeScript failed: ...' }.
    4. Read r = results && results[0] ? results[0].result : null. Write a terminal snapshot (status complete when r && !r.error else error) then deleteSnapshot(taskId) (best-effort). If !r return failure; if r.error return { success:false, error:r.error }.
    5. D-07 SW-side extract: data = r.json; if spec.extract && data != null, jp = the guarded getFSBJmespath(); if jp && jp.search, try data = jp.search(r.json, spec.extract) catch leave raw. Return { success:true, status:r.status, finalUrl:r.finalUrl, redirected:r.redirected, data, text:r.text }.

    classifyOnWake(snapshot) (CAVEAT-1, D-11): a THIN LOCAL classifier - do NOT call Lattice resume(). Read the flat snapshot.current_step and snapshot.method. Reuse the Lattice marker STRINGS: when current_step === 'BEFORE_API_REQUEST' (or any non-safe in-flight marker) AND method is one of POST/PUT/PATCH/DELETE, return 'RECOVERY_AMBIGUOUS' (surface, never blind-retry). For GET/HEAD return a re-issuable verdict (e.g. 'ON_ERROR_SW_EVICTION_MID_REQUEST' / a documented re-issue token). Ensure the export object now lists capabilityFetchInPage, executeBoundSpec, classifyOnWake.

    Then edit extension/background.js: add ONE additive line try { importScripts('utils/capability-fetch.js'); } catch (e) { console.error('[FSB] Failed to load capability-fetch.js:', e.message); } placed LAST of the capability family (AFTER capability-interpreter.js so FsbCapabilityInterpreter.getFSBJmespath exists). Additive only - do not reorder existing lines.

    Keep capability-fetch.js ASCII-only and free of eval / new Function / dynamic-import even in comments/strings (recipe-path allowlist; Check 1 word-boundary regex).
  </action>
  <verify>
    <automated>node scripts/verify-recipe-path-guard.mjs && node tests/capability-fetch.test.js</automated>
  </verify>
  <acceptance_criteria>
    - executeBoundSpec returns a dual-field RECIPE_ORIGIN_MISMATCH (code AND errorCode set) when the mock tab origin != spec.origin, and the executeScript recorder stays EMPTY (no side effect) - asserted by Task 3's test.
    - A BEFORE_API_REQUEST in_progress snapshot is observable via readSnapshot DURING the call and is gone after (deleted) - asserted by Task 3's test using the in-memory chrome.storage.session.
    - classifyOnWake returns 'RECOVERY_AMBIGUOUS' for a synthetic POST in-flight snapshot and a re-issuable verdict for a GET snapshot - asserted by Task 3's test.
    - extract runs SW-side: grep -n getFSBJmespath extension/utils/capability-fetch.js shows the call inside executeBoundSpec (not in capabilityFetchInPage).
    - background.js contains exactly one importScripts('utils/capability-fetch.js') line, after the capability-interpreter.js importScripts line.
    - node scripts/verify-recipe-path-guard.mjs exits 0 (capability-fetch.js still dynamic-code-free) and node tests/capability-fetch.test.js exits 0.
  </acceptance_criteria>
  <done>executeBoundSpec pins the active-tab origin, wraps the fetch in the BEFORE_API_REQUEST sidecar, runs extract SW-side, and exposes a thin classifyOnWake that never blind-retries a mutation; background.js loads the module last of the capability family; the guard and the new suite pass.</done>
</task>

<task type="auto">
  <name>Task 3: Hardcoded recipe (+ fixture copy) + tests/capability-fetch.test.js (FETCH-01..05 CI) + npm test chain</name>
  <files>catalog/recipes/github-notifications.json, catalog/recipes/_fixtures/valid-github-notifications.json, tests/capability-fetch.test.js, package.json</files>
  <read_first>
    - catalog/recipes/_fixtures/valid-recipe.json (the recipe field-set template)
    - extension/utils/capability-recipe-schema.js (READ lines 79-148 the closed schema; 231-299 validateRecipe - the gate the recipe must pass)
    - tests/capability-interpreter.test.js (READ the full loader + recorder + built-errors passthrough; this is the primary clone target)
    - tests/mcp-task-store.test.js (READ lines 35-53 freshRequireStore + makeSnapshot + installChromeMock backing in-memory chrome.storage.session)
    - extension/utils/capability-fetch.js (the Task 1+2 module under test - the exact return shapes and export names)
    - .planning/phases/27-authenticated-fetch-primitive-main-world-origin-pin-resume-s/27-RESEARCH.md (Validation Architecture Phase Requirements -> Test Map lines 641-647; the mock-vs-live split lines 654-657; Wave 0 Gaps lines 659-664)
    - .planning/phases/27-authenticated-fetch-primitive-main-world-origin-pin-resume-s/27-VALIDATION.md (the Per-Task Verification Map + Wave 0 Requirements)
  </read_first>
  <action>
    Create catalog/recipes/github-notifications.json (D-13) with the schema-passing shape from the interfaces block: schemaVersion 1, id github.notifications, origin https://github.com, endpoint /notifications, method GET, authStrategy same-origin-cookie, extract '@'. ASCII-only JSON. Also create catalog/recipes/_fixtures/valid-github-notifications.json (an identical copy) so the recipe-path guard's Check 2 fixture run validates the shape at build time (a free closed-vocabulary proof; Check 2 scans only _fixtures/).

    Create tests/capability-fetch.test.js as a zero-framework Node script cloning tests/capability-interpreter.test.js's loader (vm.runInThisContext for the cfworker IIFE, then require schema + auth-strategies + interpreter + the new capability-fetch module) + tests/mcp-task-store.test.js's in-memory chrome.storage.session via installChromeMock. Use the check(cond, msg) + passed/failed + process.exit(failed>0?1:0) convention. Cover (RESEARCH.md:641-647):
    - FETCH-01: stub chrome.scripting.executeScript as a recorder that returns a fixture InjectionResult { result: { ok:true, status:200, finalUrl:'https://github.com/notifications', json:{...}, text:null } }. Drive github-notifications.json through interpretRecipe -> executeBoundSpec with the mock tab origin == https://github.com. Assert the recorder captured a { world:'MAIN', func, args:[spec] } call; assert the captured func.toString() contains credentials and include and contains NONE of jmespath/getFSB/require/importScripts (serialization-safety static guard, Pitfall 1).
    - FETCH-02: run capabilityFetchInPage directly in Node with a stubbed document.querySelector and a stubbed global fetch recorder; pass a spec with csrfSource { from:'meta', selector:'meta[name=csrf-token]', header:'X-CSRF-Token' }; assert the recorded request headers include the X-CSRF-Token with the scraped value. (Optional: an input-selector case asserting the .value read path per CAVEAT-2.)
    - FETCH-03: set the mock tab origin != spec.origin; assert executeBoundSpec returns code RECIPE_ORIGIN_MISMATCH AND the executeScript recorder stays EMPTY. (The interpreter-side cross-origin + protocol-relative rejection is already covered in Plan 01's capability-interpreter.test.js.)
    - FETCH-04: using the in-memory chrome.storage.session, assert a BEFORE_API_REQUEST snapshot exists via readSnapshot DURING the executeBoundSpec call (e.g. inspect from inside the stubbed executeScript) and is gone after; call classifyOnWake with a synthetic POST in-flight snapshot -> RECOVERY_AMBIGUOUS, with a GET snapshot -> re-issuable.
    - FETCH-05 (mock half): the end-to-end drive above returns the success shape { success:true, status:200, data, ... } from the fixture; ALSO load catalog/recipes/github-notifications.json from disk and assert validateRecipe accepts it. Dynamic-import mcp/build/errors.js and assert mapFSBError on { success:false, code:'RECOVERY_AMBIGUOUS' } surfaces RECOVERY_AMBIGUOUS verbatim and NOT action_rejected (the Plan-01 registration proof; clone tests/capability-interpreter.test.js:250-266).

    Restore any patched globals (global.fetch, document) at the end. Then append to package.json scripts.test (line 17): after the current tail node tests/recipe-path-guard.test.js, add &&  node tests/capability-fetch.test.js. ASCII-only throughout.
  </action>
  <verify>
    <automated>node tests/capability-fetch.test.js && node scripts/verify-recipe-path-guard.mjs</automated>
  </verify>
  <acceptance_criteria>
    - catalog/recipes/github-notifications.json exists and validateRecipe accepts it (asserted in the test).
    - catalog/recipes/_fixtures/valid-github-notifications.json exists; node scripts/verify-recipe-path-guard.mjs PASSes (Check 2 validates the new fixture).
    - node tests/capability-fetch.test.js exits 0 with PASS lines covering FETCH-01 (MAIN-world recorder + serialization-safety guard), FETCH-02 (CSRF header threaded), FETCH-03 (active-tab mismatch -> RECIPE_ORIGIN_MISMATCH, recorder empty), FETCH-04 (BEFORE_API_REQUEST snapshot lifecycle + classifyOnWake POST->RECOVERY_AMBIGUOUS, GET->re-issuable), FETCH-05 mock (success shape + recipe validates + RECOVERY_AMBIGUOUS surfaces verbatim).
    - package.json scripts.test ends with the new && node tests/capability-fetch.test.js entry (grep -c confirms presence).
    - The serialization-safety assertion explicitly checks func.toString() contains NO jmespath/getFSB/require/importScripts substring.
  </acceptance_criteria>
  <done>The hardcoded recipe validates, the CI suite proves all five FETCH wiring properties through mocks/stubs/fixtures, and npm test runs the new suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SW -> page MAIN world | The fixed func is stringified and re-parsed in the page; the spec (data) crosses via args; the page realm holds the user's first-party cookies. |
| page MAIN world -> SW | Only the non-secret response body (json/text/status) crosses back; auth material (cookies) never leaves the browser jar. |
| active tab -> credentialed fetch | Cookies attach based on the tab's origin; an origin mismatch would attach the wrong session's cookies. |
| chrome.storage.session | The in-flight snapshot persists hot state across SW eviction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-06 | Elevation of Privilege | capability-fetch.js dynamic code | mitigate | The func is a FIXED named function; the recipe supplies only data via args; capability-fetch.js is on the CI-guard allowlist and contains zero eval/new Function/dynamic-import (Check 1). Wall 1 (D-01/D-02). |
| T-27-07 | Spoofing / Elevation of Privilege | active-tab origin mismatch | mitigate | executeBoundSpec asserts active-tab origin === spec.origin before executeScript; a mismatch returns RECIPE_ORIGIN_MISMATCH and fires no fetch (D-08 part 2). Prevents "right URL, wrong/attacker tab session". |
| T-27-08 | Information Disclosure | auth material crossing the boundary | mitigate | capabilityFetchInPage returns ONLY { ok, status, finalUrl, redirected, json, text }; it never reads or returns cookies; the body is size-capped. FSB never reads HttpOnly cookies (GOV-06 posture enforced early). |
| T-27-09 | Tampering / Repudiation | double-mutation on SW eviction | mitigate | classifyOnWake returns RECOVERY_AMBIGUOUS for a mutating-method in-flight snapshot; it is never blind-retried; only GET/HEAD may re-issue (D-11). The proof recipe is a GET to keep the happy path idempotent. |
| T-27-10 | Tampering | CSRF token misread (.value vs .content) | mitigate | The in-page scrape reads .value for input selectors and .content for meta (CAVEAT-2); a wrong read yields a missing/empty token, not a wrong-origin request. Live verification of the reserved /_graphql shape is deferred to Phase 29 / the FETCH-02 sub-task. |
| T-27-11 | Denial of Service | oversized response body across structured-clone | mitigate | The func size-caps response.text() at a fixed CAP before parsing (Pitfall 5); a multi-MB HTML page cannot stall the boundary or the SW. |
| T-27-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs NO packages (RESEARCH.md Package Legitimacy Audit: zero new dependencies; the three relevant libs shipped in Phase 26). npm test only builds + runs existing source. |

Highest-severity unmitigated item: none. The single irreducibly-unprovable-in-CI property (real GitHub HttpOnly cookies attach and yield a logged-in body shape) is NOT a code vulnerability - it is a Chrome+GitHub behavior, deferred to Plan 03's human-gated UAT by design (D-15). T-27-10's live CSRF-DOM-shape confirmation is a Phase 29 / FETCH-02 follow-up, not a Phase 27 blocker (the FETCH-01 GET proof needs no CSRF).
</threat_model>

<verification>
- node tests/capability-fetch.test.js exits 0 (FETCH-01..05 CI mock assertions green).
- node scripts/verify-recipe-path-guard.mjs exits 0 (capability-fetch.js dynamic-code-free + on the allowlist; the new _fixtures recipe validates via Check 2; Check 4 counts 4 on-disk capability modules all allowlisted).
- grep confirms capabilityFetchInPage references no jmespath/getFSB/require/importScripts; executeBoundSpec calls getFSBJmespath SW-side; background.js loads capability-fetch.js last of the capability family.
- npm test (full chain) runs the new suite at its tail after npm --prefix mcp run build.
</verification>

<success_criteria>
- An authenticated fetch executes in the page MAIN world via the fixed func with credentials:'include' (FETCH-01); CSRF scrapes in-page into the declared header (FETCH-02).
- The active-tab origin is pinned a second time in the wrapper; a mismatch is rejected before any side effect (FETCH-03 part 2, complementing Plan 01's interpreter pin).
- The fetch is wrapped in the BEFORE_API_REQUEST resume-sidecar; mid-mutation ambiguity classifies to RECOVERY_AMBIGUOUS and never blind-retries (FETCH-04).
- The hardcoded github.com recipe validates and drives end-to-end through the stubbed seam to a logged-in-shape success result in CI (FETCH-05 mock half; the live half is Plan 03).
- capability-fetch.js is dynamic-code-free and on the recipe-path allowlist (Wall 1); background.js wiring is additive; no manifest/permission change (CAP-05 posture).
</success_criteria>

<output>
Create `.planning/phases/27-authenticated-fetch-primitive-main-world-origin-pin-resume-s/27-02-SUMMARY.md` when done.
</output>
