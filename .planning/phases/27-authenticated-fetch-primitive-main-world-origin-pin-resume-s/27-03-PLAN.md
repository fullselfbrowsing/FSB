---
phase: 27-authenticated-fetch-primitive-main-world-origin-pin-resume-s
plan: 03
type: execute
wave: 3
depends_on: ["27-01", "27-02"]
files_modified:
  - .planning/phases/27-authenticated-fetch-primitive-main-world-origin-pin-resume-s/27-HUMAN-UAT.md
autonomous: false
requirements: [FETCH-05]
must_haves:
  truths:
    - "A 27-HUMAN-UAT.md doc exists recording the live logged-in-shape assertion as human_needed, in the established FSB UAT format"
    - "The live FETCH-05 assertion is surfaced to the user as a human-gated checkpoint and never auto-run (no application is launched automatically)"
  artifacts:
    - path: ".planning/phases/27-authenticated-fetch-primitive-main-world-origin-pin-resume-s/27-HUMAN-UAT.md"
      provides: "the human_needed live-browser UAT scenario(s) for the github.com GET /notifications logged-in-shape assertion"
      contains: "human_needed"
  key_links:
    - from: "27-HUMAN-UAT.md UAT-27-01"
      to: "catalog/recipes/github-notifications.json"
      via: "the recipe the human runs in a real logged-in github.com tab"
      pattern: "github-notifications"
---

<objective>
Close out FETCH-05's live half. The CI suite (Plan 02) proves every mechanic FSB owns through mocks/stubs/fixtures, but the ONE property the entire phase exists to de-risk - real GitHub HttpOnly cookies actually attach in the page MAIN world and yield a LOGGED-IN body shape (not logged-out) - is irreducibly a live-browser observation that cannot exist in CI without shipping a real GitHub credential (forbidden by GOV-06 / auth-stays-local). Record it as a human_needed UAT scenario in FSB's established format and surface it to the user as a human-gated checkpoint.

Purpose: give the milestone an honest, recorded evidence gap (not a fabricated pass) that joins the project's existing live-browser UAT debt ledger in STATE.md - mirroring the Phase 25 PhantomStream posture.
Output: .planning/phases/27-authenticated-fetch-primitive-main-world-origin-pin-resume-s/27-HUMAN-UAT.md (status human_needed) and a checkpoint that hands the user exact run-and-observe steps without auto-launching anything.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-authenticated-fetch-primitive-main-world-origin-pin-resume-s/27-CONTEXT.md
@.planning/phases/27-authenticated-fetch-primitive-main-world-origin-pin-resume-s/27-RESEARCH.md
@.planning/phases/27-authenticated-fetch-primitive-main-world-origin-pin-resume-s/27-VALIDATION.md

@.planning/milestones/v0.12.0-phases/25-parity-removal-docs-browser-uat/25-HUMAN-UAT.md
@catalog/recipes/github-notifications.json

<interfaces>
<!-- The exact doc convention + the assertion to record. -->

HUMAN-UAT format to mirror (.planning/milestones/v0.12.0-phases/25-parity-removal-docs-browser-uat/25-HUMAN-UAT.md): YAML frontmatter with phase, status human_needed, created; a heading "# Phase NN Human UAT: <title>"; an intro paragraph stating these scenarios were not executed autonomously and must not be treated as passed until a human records results; a "## Setup" numbered list; a "## Required Scenarios" table with columns ID, Scenario, Procedure, Expected Outcome, Status; and a "## Recording Results" closing paragraph. Scenario IDs use the UAT-<phase>-NN pattern (here UAT-27-01 and up).

The FETCH-05 live assertion (D-14, D-15; RESEARCH.md:646-657; VALIDATION.md Manual-Only Verifications): a human, signed in to github.com, runs the hardcoded github.com GET /notifications recipe (catalog/recipes/github-notifications.json, authStrategy same-origin-cookie) through the Phase-27 entry path in a real tab, and confirms the response is HTTP 200 (NOT a 302 to /login?return_to=...) AND/OR that the user-login meta tag is NON-EMPTY (empty means logged out; the username means logged in). The session rides HttpOnly _gh_sess / logged_in cookies that JS cannot read, so a logged-in shape is obtainable ONLY via the page-context credentials:'include' fetch - exactly the property being proven.

Project rule (global CLAUDE.md): never run applications automatically; ASCII-only, NO EMOJIS. The checkpoint hands the human steps; it does not launch Chrome or start any server.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 27-HUMAN-UAT.md (live logged-in-shape assertion, human_needed)</name>
  <files>.planning/phases/27-authenticated-fetch-primitive-main-world-origin-pin-resume-s/27-HUMAN-UAT.md</files>
  <read_first>
    - .planning/milestones/v0.12.0-phases/25-parity-removal-docs-browser-uat/25-HUMAN-UAT.md (the exact format to mirror: frontmatter, intro, Setup, Required Scenarios table, Recording Results)
    - .planning/phases/27-authenticated-fetch-primitive-main-world-origin-pin-resume-s/27-VALIDATION.md (the Manual-Only Verifications row - the precise procedure + expected outcome to transcribe)
    - .planning/phases/27-authenticated-fetch-primitive-main-world-origin-pin-resume-s/27-RESEARCH.md (the mock-vs-live split lines 654-657, the D-14 assertion lines 646-648, the A1/A2 caveats lines 588-589 to note re-verification)
    - catalog/recipes/github-notifications.json (the recipe the human runs - name it in the procedure)
  </read_first>
  <action>
    Create .planning/phases/27-authenticated-fetch-primitive-main-world-origin-pin-resume-s/27-HUMAN-UAT.md mirroring the Phase 25 doc structure exactly:

    - Frontmatter: phase 27-authenticated-fetch-primitive-main-world-origin-pin-resume-s, status human_needed, created 2026-06-20.
    - Heading: a "# Phase 27 Human UAT" title naming the authenticated MAIN-world fetch logged-in-shape closeout.
    - Intro paragraph: state this scenario requires a real Chrome session signed in to github.com, was NOT executed in the autonomous run, and must not be treated as passed until a human records the observed result; note the CI suite tests/capability-fetch.test.js already proves all wiring through mocks.
    - "## Setup": numbered steps - load extension/ as an unpacked Chrome extension; sign in to github.com in a normal https tab; keep the active tab on https://github.com (the origin-pin requires active-tab origin to equal https://github.com); have the hardcoded recipe catalog/recipes/github-notifications.json available to the Phase-27 entry path.
    - "## Required Scenarios": a table with columns ID, Scenario, Procedure, Expected Outcome, Status. Include at least:
      - UAT-27-01 (logged-in shape): run the github.com GET /notifications recipe via the Phase-27 entry path in the signed-in tab. Expected: HTTP 200 (NOT 302 to /login?return_to=...) AND/OR the user-login meta tag NON-EMPTY (your username). Status human_needed.
      - UAT-27-02 (logged-out contrast, recommended): sign out of github.com (or use a fresh profile) and run the same recipe. Expected: a 302 to /login (redirect:'manual' surfaces it) and/or an empty/absent user-login meta - confirming the UAT-27-01 result was genuinely cookie-attached, not a static page. Status human_needed.
      - UAT-27-03 (origin-pin live, optional): with the active tab on a NON-github origin, attempt the recipe. Expected: RECIPE_ORIGIN_MISMATCH, no request fired. Status human_needed.
    - "## Recording Results": instruct that when executed, each human_needed status is replaced with pass/fail/partial plus date, Chrome version, extension commit, and a short observed-outcome note; re-verify the A1/A2 GitHub-behavior assumptions (the user-login meta name, the 302 shape) at run time since GitHub's web UI can change.

    ASCII-only, NO EMOJIS. Do not include any command that launches Chrome or a server - the steps are for the human to perform.
  </action>
  <verify>
    <automated>node -e "process.exit(require('fs').readFileSync('.planning/phases/27-authenticated-fetch-primitive-main-world-origin-pin-resume-s/27-HUMAN-UAT.md','utf8').includes('human_needed')?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - The file .planning/phases/27-authenticated-fetch-primitive-main-world-origin-pin-resume-s/27-HUMAN-UAT.md exists.
    - It has frontmatter status human_needed and a Required Scenarios table containing UAT-27-01 with the 200-not-302 and user-login meta expectation.
    - It names catalog/recipes/github-notifications.json as the recipe to run and states the active tab must be on https://github.com.
    - It is ASCII-only with no emojis and contains no command that auto-launches an application.
  </acceptance_criteria>
  <done>27-HUMAN-UAT.md records the live logged-in-shape FETCH-05 assertion (and the logged-out contrast + live origin-pin checks) as human_needed in FSB's established UAT format.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-gated live FETCH-05 logged-in-shape verification</name>
  <action>This is a blocking human-verify checkpoint - it presents the live-run-and-observe steps and PAUSES; it does not execute or auto-launch anything (project rule: never run applications automatically). On resume, record the operator-provided outcome into 27-HUMAN-UAT.md or accept the human_needed UAT as recorded debt.</action>
  <what-built>
    Plan 01 + Plan 02 delivered the full Phase-27 authenticated-fetch primitive and proved every FSB-owned mechanic in CI: tests/capability-fetch.test.js asserts the MAIN-world recorder + serialization-safety, the in-page CSRF threading, the active-tab origin-pin (no side effect on mismatch), the BEFORE_API_REQUEST resume-sidecar lifecycle, the classifyOnWake mutating-method RECOVERY_AMBIGUOUS, and the end-to-end success shape through the stubbed seam; node scripts/verify-recipe-path-guard.mjs confirms capability-fetch.js is dynamic-code-free and on the allowlist. The hardcoded recipe catalog/recipes/github-notifications.json validates against the closed schema. Task 1 of this plan wrote 27-HUMAN-UAT.md. The only remaining property is the irreducibly-live one: that real GitHub HttpOnly cookies attach in the page MAIN world and return a logged-in shape.
  </what-built>
  <how-to-verify>
    This step is human-gated and must NOT be auto-run (the project rule forbids launching applications automatically). To execute it yourself now (optional):
    1. Load extension/ as an unpacked Chrome extension and sign in to github.com in a normal https tab; keep that tab active (origin must be https://github.com).
    2. Run the hardcoded github.com GET /notifications recipe (catalog/recipes/github-notifications.json) through the Phase-27 entry path against that tab.
    3. Confirm the response is HTTP 200 (NOT a 302 to /login?return_to=...) AND/OR that the user-login meta tag is non-empty (your username). Optionally repeat signed-out to confirm the logged-out contrast (302 / empty meta), and from a non-github tab to confirm RECIPE_ORIGIN_MISMATCH.
    4. Record the outcome in 27-HUMAN-UAT.md (replace human_needed with pass/fail/partial + date, Chrome version, extension commit, observed note).

    If you do not want to run the live browser session now, approve to ACCEPT the human_needed UAT as recorded debt - it joins the existing v0.10/v0.11/v0.12 live-browser UAT ledger in STATE.md (the documented FSB posture; CI green does not depend on it).
  </how-to-verify>
  <resume-signal>Type "approved" to accept the human_needed UAT as recorded debt, or paste the observed live results (pass/fail/partial + environment) to record them in 27-HUMAN-UAT.md.</resume-signal>
  <done>The operator has either recorded live FETCH-05 results in 27-HUMAN-UAT.md or accepted the human_needed UAT as documented debt; the phase proceeds with the evidence gap honestly recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human operator -> live github.com session | The UAT runs against the operator's own authenticated session; no FSB-held credential is involved. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-12 | Repudiation | unverified live claim | mitigate | The doc is status human_needed and explicitly forbids treating the scenario as passed until a human records dated, environment-stamped results; no fabricated pass enters the ledger (D-15). |
| T-27-13 | Information Disclosure | self-access on the operator's own account | accept | The UAT is a personal, supervised, read-only GET on the operator's own github.com session (GitHub Acceptable Use Policy defensible per CONTEXT.md); no third-party data, no credential handling by FSB. |
| T-27-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs NO packages and runs no build; it authors one markdown doc and presents a checkpoint. |

Highest-severity unmitigated item: none. The plan only documents and gates; it introduces no code path.
</threat_model>

<verification>
- .planning/phases/27-authenticated-fetch-primitive-main-world-origin-pin-resume-s/27-HUMAN-UAT.md exists with status human_needed and the UAT-27-01 logged-in-shape scenario.
- The checkpoint surfaces the live FETCH-05 assertion to the user without auto-launching any application.
- No code, build, or package change is introduced by this plan.
</verification>

<success_criteria>
- The live FETCH-05 logged-in-shape assertion is recorded as a human_needed UAT scenario in the established FSB format (FETCH-05 live half).
- The user is given exact run-and-observe steps and an explicit accept-as-debt path, consistent with the project's never-auto-launch rule and the existing UAT debt ledger.
</success_criteria>

<output>
Create `.planning/phases/27-authenticated-fetch-primitive-main-world-origin-pin-resume-s/27-03-SUMMARY.md` when done.
</output>
