---
phase: 13-public-lattice-package-integration
plan: 01
type: execute
wave: 0
status: complete
completed: 2026-06-15
source: retrospective-gsd-backfill
depends_on:
  - phase: 12-side-panel-follows-automation
requirements:
  - FINT-25
  - FINT-26
  - FINT-27
  - FINT-28
files_expected:
  - package.json
  - package-lock.json
  - .planning/LATTICE-PIN.md
  - tests/helpers/lattice-public-pin.js
  - tests/lattice-public-package.test.js
  - extension/offscreen/lattice-host.js
  - extension/dist/offscreen/lattice-host.js
---

# Plan 13-01: Public Package Replug

## Objective

Replace the missing local Lattice checkout dependency with the current public package while preserving FSB's `lattice` import surface and existing runtime behavior.

## Must Haves

- `package.json` depends on `"lattice": "npm:@full-self-browsing/lattice@1.3.0"`.
- `package.json` includes devDependency `"@full-self-browsing/lattice-cli": "1.3.0"`.
- `package-lock.json` resolves `node_modules/lattice` to package name `@full-self-browsing/lattice`, version `1.3.0`, with the expected registry integrity.
- Root `engines.node` is `>=24.0.0`.
- `.planning/LATTICE-PIN.md` records the npm package, alias, package integrity, source tag `v1.3.0`, and source commit `069c9aea4b5875393c96ad7e6ffeec4afbe70f34`.
- A focused public package smoke validates package metadata, runtime exports, receipt schema `lattice-receipt/v1.2`, and CLI command surface.
- Existing Lattice smokes continue to verify behavior against the public package.
- Extension offscreen bundle is regenerated from the public package dependency graph.

## Tasks

1. Replug runtime and CLI dependencies in `package.json` and `package-lock.json`.
2. Add a reusable package-pin helper that cross-checks `package.json`, `package-lock.json`, and `.planning/LATTICE-PIN.md`.
3. Add public runtime and CLI smoke coverage.
4. Update existing Lattice smokes from local-clone receipt expectations to public package receipt expectations.
5. Rebuild extension dist artifacts.
6. Update planning records for Phase 13.
7. Run focused and full verification.

## Acceptance Criteria

- `npm run test:lattice` passes.
- `npm run build` passes.
- `npm test` passes.
- `npm audit --omit=dev` passes.
- `node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" init verify-work 13` reports a non-null `phase_dir` and `has_verification: true`.
