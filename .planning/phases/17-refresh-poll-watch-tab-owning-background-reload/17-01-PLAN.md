---
phase: 17-refresh-poll-watch-tab-owning-background-reload
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/trigger-refresh-poll.test.js
  - extension/utils/trigger-manager.js
  - extension/utils/trigger-lifecycle.js
  - package.json
autonomous: true
requirements: [WATCH-03]
user_setup: []

must_haves:
  truths:
    - "The Wave 0 validation gap is closed first: tests/trigger-refresh-poll.test.js exists and is wired into package.json before refresh-poll reload implementation proceeds"
    - "Refresh-poll defaults to poll_interval_ms 60000 when no interval is supplied"
    - "Refresh-poll rejects any supplied interval below FsbTriggerLifecycle.TRIGGER_ALARM_MIN_PERIOD_MS (30000) with code REFRESH_POLL_INTERVAL_TOO_LOW and guidance to use live-observe"
    - "Accepted refresh-poll snapshots persist poll_interval_ms and schedule their fsbTrigger:<id> alarm for the next poll tick, not only for deadline_at"
    - "Refresh-poll jitter is light, floor-safe, and never schedules later than deadline_at"
  artifacts:
    - path: "tests/trigger-refresh-poll.test.js"
      provides: "Phase 17 Wave 0 Node harness and cadence tests for WATCH-03"
      contains: "REFRESH_POLL_INTERVAL_TOO_LOW"
    - path: "extension/utils/trigger-manager.js"
      provides: "arm-time refresh-poll interval normalization and poll_interval_ms persistence"
      contains: "poll_interval_ms"
    - path: "extension/utils/trigger-lifecycle.js"
      provides: "refresh-poll next-alarm scheduling while preserving deadline_at as TTL"
      contains: "scheduleNextRefreshPollAlarm"
  key_links:
    - from: "extension/utils/trigger-manager.js armTrigger"
      to: "extension/utils/trigger-lifecycle.js"
      via: "snapshot.poll_interval_ms + lifecycle.armTrigger(snapshot)"
      pattern: "poll_interval_ms"
    - from: "extension/utils/trigger-lifecycle.js"
      to: "chrome.alarms.create"
      via: "fsbTrigger:<id> alarm created at next poll tick for refresh-poll snapshots"
      pattern: "scheduleNextRefreshPollAlarm"
---

<objective>
Close the Phase 17 Wave 0 validation gap and implement the refresh-poll cadence contract before any tab reload code lands.

Purpose: WATCH-03 is the safety gate for refresh-poll. A poll below Chrome's 30s alarm floor must reject loudly instead of silently hammering pages, and accepted refresh-poll triggers must carry a persisted normalized interval so service-worker eviction cannot reset cadence.
Output: `tests/trigger-refresh-poll.test.js`, the package test-chain entry, arm-time interval normalization in `trigger-manager.js`, and refresh-poll alarm scheduling helpers in `trigger-lifecycle.js`.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-refresh-poll-watch-tab-owning-background-reload/17-CONTEXT.md
@.planning/phases/17-refresh-poll-watch-tab-owning-background-reload/17-RESEARCH.md
@.planning/phases/17-refresh-poll-watch-tab-owning-background-reload/17-PATTERNS.md
@.planning/phases/17-refresh-poll-watch-tab-owning-background-reload/17-VALIDATION.md
@extension/utils/trigger-manager.js
@extension/utils/trigger-lifecycle.js
@tests/trigger-lifecycle.test.js

<interfaces>
Existing constants and arm contracts:
```javascript
// extension/utils/trigger-lifecycle.js
var TRIGGER_ALARM_PREFIX = 'fsbTrigger:';
var FSB_TRIGGER_DEFAULT_TTL_MS = 21600000;
var TRIGGER_ALARM_MIN_PERIOD_MS = 30000;
var TRIGGER_ALARM_MIN_PERIOD_MINUTES = 0.5;
async function armTrigger(snapshot) { /* writes snapshot, creates alarm */ }
async function restoreTriggersFromStorage() { /* re-arms armed snapshots */ }
```

```javascript
// extension/utils/trigger-manager.js
function armTrigger(spec) {
  var snapshot = {
    trigger_id: safeSpec.trigger_id,
    status: 'armed',
    condition: safeSpec.condition,
    baseline: ...,
    last_value: ...,
    selector: safeSpec.selector,
    target_tab_id: safeSpec.target_tab_id,
    agent_id: safeSpec.agent_id,
    armed_at: now,
    deadline_at: now + ttl
  };
  return lifecycle.armTrigger(snapshot);
}
```

Required new internal contract:
```javascript
// Refresh-poll accepted snapshot fields
{
  watch: 'refresh-poll',
  poll_interval_ms: 60000,       // default, or caller-supplied >= 30000
  next_poll_at: 1710000000000,   // persisted next fsbTrigger:<id> wake
  deadline_at: 1710021600000     // absolute TTL/reap boundary, never cadence
}
```

Typed reject shape:
```javascript
{
  error: 'REFRESH_POLL_INTERVAL_TOO_LOW',
  code: 'REFRESH_POLL_INTERVAL_TOO_LOW',
  min_interval_ms: 30000,
  requested_interval_ms: <number>,
  guidance: 'Use live-observe for sub-30s changes.'
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the refresh-poll Wave 0 harness and arm-time interval normalization</name>
  <files>tests/trigger-refresh-poll.test.js, extension/utils/trigger-manager.js, package.json</files>
  <read_first>
    - tests/trigger-refresh-poll.test.js (file being created)
    - extension/utils/trigger-manager.js (file being modified)
    - package.json (file being modified)
    - .planning/phases/17-refresh-poll-watch-tab-owning-background-reload/17-VALIDATION.md (Wave 0 Requirements and Per-Task Verification Map)
    - .planning/phases/17-refresh-poll-watch-tab-owning-background-reload/17-CONTEXT.md D-03, D-04, D-05, D-14
    - .planning/phases/17-refresh-poll-watch-tab-owning-background-reload/17-RESEARCH.md Pattern 1 and Pitfall 1
    - tests/trigger-lifecycle.test.js createStorageArea/createChromeMock/check harness patterns
    - tests/trigger-cap.test.js armTrigger cap test pattern
  </read_first>
  <behavior>
    - `FsbTriggerManager.armTrigger({ watch:'refresh-poll' })` persists `watch:'refresh-poll'` and `poll_interval_ms:60000`.
    - `FsbTriggerManager.armTrigger({ watch:'refresh-poll', poll_interval_ms:45000 })` persists `poll_interval_ms:45000`.
    - Any alias accepted by the project surface (`poll_interval_ms`, `pollIntervalMs`, `interval_ms`, `intervalMs`) below 30000 returns `REFRESH_POLL_INTERVAL_TOO_LOW` and never delegates to `FsbTriggerLifecycle.armTrigger`.
    - Non-refresh watches preserve the existing armTrigger behavior and do not gain `poll_interval_ms`.
  </behavior>
  <action>
    Create `tests/trigger-refresh-poll.test.js` as a plain Node script using the existing `check()` counter pattern from `tests/trigger-lifecycle.test.js`. Build a minimal harness that fresh-requires `extension/utils/value-extractor.js`, `extension/utils/trigger-store.js`, `extension/utils/trigger-lifecycle.js`, and `extension/utils/trigger-manager.js` under a mocked `chrome.storage.session` and `chrome.alarms`. Include reusable helpers named `createStorageArea`, `createChromeMock`, `setupRefreshPollHarness`, `makeRefreshPollSpec`, and `readSnapshot`.

    In `extension/utils/trigger-manager.js`, add arm-time refresh-poll normalization inside `armTrigger(spec)` before building the snapshot. Treat `safeSpec.watch === 'refresh-poll' || safeSpec.watch === 'refresh_poll' || safeSpec.mode === 'refresh-poll'` as refresh-poll. For refresh-poll only, compute `requested_interval_ms` from `safeSpec.poll_interval_ms`, `safeSpec.pollIntervalMs`, `safeSpec.interval_ms`, or `safeSpec.intervalMs`; default to `60000` when none is supplied. Reject any non-finite interval or any interval below `FsbTriggerLifecycle.TRIGGER_ALARM_MIN_PERIOD_MS` with the typed shape in `<interfaces>` and guidance exactly `Use live-observe for sub-30s changes.`. Do not silently clamp. On success, persist `watch:'refresh-poll'` and `poll_interval_ms:<integer milliseconds>` on the snapshot before delegating to `lifecycle.armTrigger(snapshot)`. Do not modify `evaluate()` and do not add any comparison logic.

    Append `&& node tests/trigger-refresh-poll.test.js` to the root `package.json` test chain near the existing trigger tests, after `node tests/trigger-observe-pulse.test.js` if present or after `node tests/trigger-observe.test.js` otherwise. Preserve valid JSON and do not reorder unrelated test entries.
  </action>
  <verify>
    <automated>node tests/trigger-refresh-poll.test.js && node tests/trigger-manager.test.js && node tests/trigger-cap.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/trigger-refresh-poll.test.js` exits 0 and includes passing cases for default 60000, accepted 45000, sub-floor rejection, and non-refresh no-poll-field behavior.
    - `grep -c "REFRESH_POLL_INTERVAL_TOO_LOW" tests/trigger-refresh-poll.test.js extension/utils/trigger-manager.js` is at least 2.
    - `grep -c "poll_interval_ms" extension/utils/trigger-manager.js` is at least 1.
    - `grep -c "node tests/trigger-refresh-poll.test.js" package.json` returns 1.
    - `node tests/trigger-manager.test.js && node tests/trigger-cap.test.js` exits 0.
  </acceptance_criteria>
  <done>Refresh-poll arming persists a normalized interval, rejects sub-floor requests with explicit live-observe guidance, and the new Wave 0 test harness is green and wired into the package test chain.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Schedule refresh-poll fsbTrigger alarms at next poll ticks while preserving deadline_at as TTL</name>
  <files>extension/utils/trigger-lifecycle.js, tests/trigger-refresh-poll.test.js</files>
  <read_first>
    - extension/utils/trigger-lifecycle.js (file being modified)
    - tests/trigger-refresh-poll.test.js (file being modified)
    - .planning/phases/17-refresh-poll-watch-tab-owning-background-reload/17-CONTEXT.md D-03, D-05
    - .planning/phases/17-refresh-poll-watch-tab-owning-background-reload/17-RESEARCH.md Pitfall 5 and Architecture Patterns
    - .planning/phases/17-refresh-poll-watch-tab-owning-background-reload/17-PATTERNS.md extension/utils/trigger-lifecycle.js section
    - tests/trigger-lifecycle.test.js cases G, H, I for restore/re-arm/orphan sweep patterns
  </read_first>
  <behavior>
    - `FsbTriggerLifecycle.armTrigger(refreshPollSnapshot)` writes `next_poll_at` and creates `fsbTrigger:<id>` with `{ when: next_poll_at }`, not `{ when: deadline_at }`.
    - `deadline_at` remains an absolute TTL/reap boundary and is never overwritten by cadence logic.
    - `restoreTriggersFromStorage()` re-arms an armed refresh-poll snapshot at the persisted `next_poll_at` if it is finite, future, floor-safe, and before `deadline_at`; otherwise it computes a fresh floor-safe next poll that is not later than `deadline_at`.
    - Jitter is deterministic in tests and light in production. It must never reduce the first poll below 30000ms from now unless the only earlier valid wake is `deadline_at` for TTL reap.
  </behavior>
  <action>
    In `extension/utils/trigger-lifecycle.js`, add small internal helpers near the alarm helpers:
    `isRefreshPollSnapshot(snapshot)`, `refreshPollIntervalMs(snapshot)`, `computeRefreshPollJitterMs(intervalMs)`, `computeRefreshPollNextAt(snapshot, now)`, and exported `scheduleNextRefreshPollAlarm(snapshot, now)`.

    Concrete values: default interval is `60000`; floor is `TRIGGER_ALARM_MIN_PERIOD_MS`; jitter is `Math.floor(Math.random() * 3000)` in production, capped so `next_poll_at` is never later than `deadline_at`. For tests, allow deterministic jitter by reading an optional numeric `snapshot.poll_jitter_ms` when present; clamp that value to `0..3000`. `computeRefreshPollNextAt` returns `Math.min(now + intervalMs + jitterMs, deadline_at)` when `deadline_at` is finite, and `now + intervalMs + jitterMs` otherwise. If the remaining TTL is less than the floor, return `deadline_at` so the existing TTL reap path owns the next tick.

    Update `armTrigger(snapshot)` so it writes `snapshot.next_poll_at = computeRefreshPollNextAt(snapshot, Date.now())` for refresh-poll snapshots and creates the alarm at `next_poll_at`; non-refresh snapshots keep the current `{ when: deadline_at }` behavior byte-shaped as much as possible. Update `restoreTriggersFromStorage()` so refresh-poll survivors are re-armed with `{ when: snap.next_poll_at }` when valid, else compute and persist a new `next_poll_at` before creating the alarm. Preserve malformed/deadline/terminal/orphan behavior for all snapshots.

    Extend `tests/trigger-refresh-poll.test.js` to cover arm-time `next_poll_at`, restore-time scheduling, deadline preservation, deterministic jitter using `poll_jitter_ms`, and floor/deadline behavior. Do not remove any existing `trigger-lifecycle.test.js` cases.
  </action>
  <verify>
    <automated>node tests/trigger-refresh-poll.test.js && node tests/trigger-lifecycle.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/trigger-refresh-poll.test.js && node tests/trigger-lifecycle.test.js` exits 0.
    - `grep -c "scheduleNextRefreshPollAlarm" extension/utils/trigger-lifecycle.js` is at least 2, including the export.
    - `grep -c "next_poll_at" extension/utils/trigger-lifecycle.js tests/trigger-refresh-poll.test.js` is at least 4.
    - Existing lifecycle restore/orphan tests still pass: `node tests/trigger-lifecycle.test.js` exits 0.
    - No standing timer is introduced: `grep -c "setInterval" extension/utils/trigger-lifecycle.js` returns 0.
  </acceptance_criteria>
  <done>Refresh-poll snapshots use the existing `fsbTrigger:<id>` namespace for recurring poll wakes, with persisted `next_poll_at`, light deterministic-testable jitter, and `deadline_at` preserved as the TTL/reap boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trigger spec -> persisted snapshot | Caller-supplied interval values become stored cadence configuration. Invalid or sub-floor values must be rejected before persistence. |
| persisted snapshot -> chrome.alarms | Snapshot cadence drives survivable alarm creation. A wrong `when` can cause reload hammering or missed TTL reaps. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03 | Denial of Service | refresh-poll interval normalization and alarm scheduling | mitigate | Reject intervals below `FsbTriggerLifecycle.TRIGGER_ALARM_MIN_PERIOD_MS` with `REFRESH_POLL_INTERVAL_TOO_LOW`; default to 60000; persist `poll_interval_ms`; add floor-safe jitter capped to 3000ms; preserve `deadline_at` as TTL. |
| T-17-01 | Tampering / Denial of Service | cross-agent tab reload before ownership validation | transfer | This plan creates cadence only and performs no reload. Plan 17-03 owns pre-reload ownership validation and blocks `chrome.tabs.reload` on `TAB_NOT_OWNED`. |
| T-17-02 | Spoofing / Tampering | challenge/login/CAPTCHA page false fire | transfer | This plan performs no page read/evaluation. Plan 17-04 owns blocker classification before staging values. |
| T-17-04 | Tampering | stale selector / missing element treated as empty text | transfer | This plan performs no content read. Plan 17-02 makes `triggerRead` return explicit `ELEMENT_NOT_FOUND`; Plan 17-03 consumes it without evaluating empty text. |
</threat_model>

<verification>
Run:
`node tests/trigger-refresh-poll.test.js && node tests/trigger-manager.test.js && node tests/trigger-cap.test.js && node tests/trigger-lifecycle.test.js`
</verification>

<success_criteria>
- Wave 0 validation file exists and is package-wired.
- WATCH-03 is implemented: 60000ms default, 30000ms hard floor, typed sub-floor rejection, persisted `poll_interval_ms`, floor-safe jitter, and deadline-safe poll scheduling.
- No reload/read/pulse behavior is introduced in this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/17-refresh-poll-watch-tab-owning-background-reload/17-01-SUMMARY.md`.
</output>
