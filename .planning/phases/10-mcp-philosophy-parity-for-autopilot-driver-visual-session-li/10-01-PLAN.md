---
phase: 10-mcp-philosophy-parity-for-autopilot-driver-visual-session-li
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extension/utils/mcp-visual-session.js
  - extension/utils/mcp-visual-session-lifecycle.js
  - extension/ai/agent-loop.js
  - tests/mcp-philosophy-parity-smoke.test.js
  - package.json
autonomous: true
requirements:
  - FINT-16
user_setup: []

must_haves:
  truths:
    - "D-02: `MCP_VISUAL_CLIENT_LABELS` accepts the new internal client label `FSB Autopilot` without colliding with existing normalized labels."
    - "D-01: `recordVisualSessionTick` lifecycle entries carry an optional `driver` discriminator and default absent legacy entries to `mcp`."
    - "D-03: Autopilot emits `recordVisualSessionTick` from the agent-loop TOOL_DISPATCH boundary adjacent to the Phase 8 step.transition emission."
    - "The new visual-session tick is guarded, fire-and-forget, and never placed inside a deferred-iterator schedule callback."
    - "Wave 0 smoke harness exists, is appended to the npm test chain, and covers allowlist, driver round-trip, restore backward compatibility, and allowlist rejection behavior."
    - "D-07: INV-04 remains byte-frozen: `grep -c setTimeout extension/ai/agent-loop.js` returns 8 and iterator pattern count remains 4."
    - "D-07: INV-06 remains byte-frozen: no Lattice-side files are modified."
  artifacts:
    - path: "extension/utils/mcp-visual-session.js"
      provides: "`FSB Autopilot` allowlist entry"
      contains: "FSB Autopilot"
    - path: "extension/utils/mcp-visual-session-lifecycle.js"
      provides: "optional `driver` field on lifecycle entry update/create paths"
      contains: "driver:"
    - path: "extension/ai/agent-loop.js"
      provides: "autopilot `recordVisualSessionTick` call at TOOL_DISPATCH boundary"
      contains: "recordVisualSessionTick"
    - path: "tests/mcp-philosophy-parity-smoke.test.js"
      provides: "Phase 10 Wave 0 smoke harness"
      contains: "Part 1"
  key_links:
    - from: "agent-loop TOOL_DISPATCH boundary"
      to: "MCPVisualSessionLifecycleUtils.recordVisualSessionTick"
      via: "guarded call carrying client `FSB Autopilot`, driver `autopilot`, and visualReason `autopilot-tool-dispatch:<tool>`"
      pattern: "recordVisualSessionTick"
    - from: "mcp-visual-session-lifecycle nextEntry"
      to: "dashboard/restore readers"
      via: "new optional discriminator field with legacy default"
      pattern: "driver:\\s*(existingEntry\\.driver|fields\\.driver)"
---

<objective>
Ship the Phase 10 visual-session side of MCP-philosophy parity for the autopilot driver. The executor extends the MCP visual-session client allowlist, carries a driver discriminator in the per-tab lifecycle entry, inserts the autopilot visual-session tick at the existing TOOL_DISPATCH boundary, and lands the initial smoke harness that later Phase 10 plans fill.

Purpose: close FINT-16 by making autopilot tool dispatches visible through the same visual-session lifecycle used by MCP clients, without changing MCP wire shape, Lattice code, or the deferred-iterator schedule.

Backfill note: this plan was reconstructed on 2026-06-15 from existing Phase 10 execution artifacts because 10-01-SUMMARY.md, 10-REVIEW.md, and 10-VERIFICATION.md existed but the PLAN.md artifact was absent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/LATTICE-PIN.md
@.planning/phases/10-mcp-philosophy-parity-for-autopilot-driver-visual-session-li/10-CONTEXT.md
@extension/utils/mcp-visual-session.js
@extension/utils/mcp-visual-session-lifecycle.js
@extension/ws/mcp-bridge-client.js
@extension/ai/agent-loop.js
@tests/lattice-step-emitter-smoke.test.js
@package.json

<interfaces>
- `extension/utils/mcp-visual-session.js` owns `MCP_VISUAL_CLIENT_LABELS` and `toClientLabelKey`.
- `extension/utils/mcp-visual-session-lifecycle.js` owns `recordVisualSessionTick`, validates clients through the allowlist, and writes per-tab entries under the `mcpVisualSession:<tabId>` prefix.
- `extension/ws/mcp-bridge-client.js` provides the precedent for visual-session ticks around MCP dispatcher activity.
- `extension/ai/agent-loop.js` already contains the Phase 8 TOOL_DISPATCH `sendLatticeStepTransition` emission. Insert the autopilot visual-session tick immediately after that block by text anchor, not by stale line number.
- Phase 10 preserves INV-01, INV-04, and INV-06. Do not modify MCP tool definitions, deferred-iterator callback bodies, or any file under `lattice/`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend visual-session allowlist and lifecycle entry shape</name>
  <files>extension/utils/mcp-visual-session.js, extension/utils/mcp-visual-session-lifecycle.js, tests/mcp-philosophy-parity-smoke.test.js</files>
  <read_first>
    - extension/utils/mcp-visual-session.js allowlist and `toClientLabelKey`
    - extension/utils/mcp-visual-session-lifecycle.js `recordVisualSessionTick` update/create `nextEntry` paths
    - 10-CONTEXT.md D-01 and D-02
  </read_first>
  <behavior>
    - Append `FSB Autopilot` to `MCP_VISUAL_CLIENT_LABELS`; do not reorder existing labels.
    - Preserve label normalization behavior and verify `toClientLabelKey('FSB Autopilot')` is unique.
    - Add an optional `driver` field to both lifecycle entry update and create paths.
    - Default absent or unrecognized driver values to `mcp`; preserve `existingEntry.driver` on updates.
    - Add initial smoke assertions for allowlist accept, key uniqueness, driver round-trip, and restore backward compatibility.
  </behavior>
  <verify>
    <automated>node tests/mcp-philosophy-parity-smoke.test.js</automated>
  </verify>
  <done>
    - Allowlist accepts `FSB Autopilot`.
    - Lifecycle entries can record `driver: 'autopilot'` and default legacy entries to `mcp`.
    - Smoke Parts 1-3 cover the new surface.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit autopilot visual-session tick at TOOL_DISPATCH</name>
  <files>extension/ai/agent-loop.js, tests/mcp-philosophy-parity-smoke.test.js</files>
  <read_first>
    - extension/ai/agent-loop.js Phase 8 TOOL_DISPATCH emission block
    - extension/ws/mcp-bridge-client.js visual-session tick call sites
    - 10-CONTEXT.md D-03 and D-07
  </read_first>
  <behavior>
    - Insert a guarded, fire-and-forget `MCPVisualSessionLifecycleUtils.recordVisualSessionTick(...)` call immediately after the Phase 8 TOOL_DISPATCH emission block.
    - Payload must include `client: 'FSB Autopilot'`, `driver: 'autopilot'`, `visualReason: 'autopilot-tool-dispatch:' + call.name`, and `isFinal: false`.
    - Use the established `typeof X === 'function'` + try/catch pattern.
    - Do not insert into any deferred-iterator schedule callback and do not add the literal `setTimeout` token in new production comments.
  </behavior>
  <verify>
    <automated>grep -c "setTimeout" extension/ai/agent-loop.js && node tests/mcp-philosophy-parity-smoke.test.js && node tests/lattice-step-emitter-smoke.test.js</automated>
  </verify>
  <done>
    - `recordVisualSessionTick` is anchored after TOOL_DISPATCH and before local tool interception.
    - INV-04 remains at 8 `setTimeout` matches and 4 iterator patterns.
    - Phase 8 smoke baseline stays green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Land Wave 0 smoke harness and npm test-chain entry</name>
  <files>tests/mcp-philosophy-parity-smoke.test.js, package.json</files>
  <read_first>
    - package.json `scripts.test` chain
    - existing raw Node smoke conventions in tests/lattice-step-emitter-smoke.test.js
  </read_first>
  <behavior>
    - Create `tests/mcp-philosophy-parity-smoke.test.js` using the existing PASS/FAIL counter style.
    - Fill Parts 1-4 with real assertions for Plan 10-01.
    - Leave Parts 5-10 as explicit placeholders for Plans 10-02 and 10-03 so the chain stays green.
    - Append the new smoke as the final command in `package.json` `scripts.test`.
  </behavior>
  <verify>
    <automated>node tests/mcp-philosophy-parity-smoke.test.js && npm test && npm run build</automated>
  </verify>
  <done>
    - Smoke exits 0 with at least 20 PASS / 0 FAIL.
    - Full npm test chain exits 0.
    - Build exits 0.
  </done>
</task>

</tasks>

<verification>
- `node tests/mcp-philosophy-parity-smoke.test.js`
- `node tests/lattice-step-emitter-smoke.test.js`
- `grep -c "setTimeout" extension/ai/agent-loop.js` must return `8`.
- `cd lattice && git rev-parse HEAD` remains the pinned SHA recorded in `.planning/LATTICE-PIN.md`.
- `npm test`
- `npm run build`
</verification>

<success_criteria>
- FINT-16 behavior is implemented and covered by smoke tests.
- Autopilot visual-session ticks are visible to the existing lifecycle layer with `driver: 'autopilot'`.
- MCP and legacy visual-session behavior remains backward compatible.
- INV-01, INV-04, and INV-06 remain unchanged.
- 10-01-SUMMARY.md can truthfully report Plan 10-01 complete.
</success_criteria>
