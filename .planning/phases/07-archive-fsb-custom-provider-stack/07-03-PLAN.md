---
phase: 07-archive-fsb-custom-provider-stack
plan: 03
type: execute
wave: 3
depends_on:
  - 07-01-PLAN.md
  - 07-02-PLAN.md
files_modified:
  - .planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md
autonomous: true
requirements:
  - FINT-09

must_haves:
  truths:
    - "`.planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md` is CREATED (new file; does not currently exist). Frontmatter records the standard verification schema (verdict: human_needed; phase: 07; created_date: 2026-05-27; verifier_type: human_uat). Body contains: (1) Phase 7 deliverable summary referencing Plan 07-01 + 07-02 SUMMARYs; (2) automated verification table (smoke + grep audit results captured by Plan 07-01); (3) `human_verification` section containing the consolidated UAT-1 procedure with all 6 sub-assertions step-by-step."
    - "The `human_verification` section MUST include: (a) preparation step (run `npm run build` so `extension/dist/` reflects latest source if applicable; if Phase 7 introduced no bundler-relevant changes, document that no rebuild is needed); (b) Chrome MV3 reload step (load unpacked at `/Users/lakshmanturlapati/Desktop/FSB/automation/extension/`); (c) SW console open step (chrome://extensions -> FSB card -> click `service worker` link -> DevTools console opens); (d) offscreen page console open step (chrome://extensions -> FSB card -> click the offscreen page link if visible; alternatively trigger via SW console paste); (e) the 6 numbered sub-assertions with EXPECTED outcome per assertion + WHAT-TO-LOOK-FOR per assertion + WHAT-TO-AVOID (specific error patterns)."
    - "The 6 sub-assertions match v0.10.0-MILESTONE-AUDIT.md UAT-1 verbatim: (a) SW reloads cleanly; no NEW `lattice/import/ERR_MODULE_NOT_FOUND` errors caused by Phases 1-7 (pre-existing baseline errors OK); (b) Popup opens normally (no JS errors in popup console); (c) Sidepanel opens normally (no JS errors in sidepanel console); (d) One autopilot iteration completes at least one step (Phase 7-aware addendum: routed through Lattice provider bridge UNCONDITIONALLY since flag is gone); (e) `chrome.offscreen.createDocument` call resolves; offscreen page loads; (f) Offscreen page console shows `[FSB lattice-host]` boot log AND `[FSB lattice-provider-bridge] boot: Phase 7 bridge shim registered (unconditional; legacy fallback removed)` (Phase 7 Plan 07-01 Task 2's new boot log text) — no errors from `import('lattice')` resolution."
    - "Step-by-step xAI test-connection procedure included: (1) Open extension Options page; (2) Provider = xAI; Model = grok-4-1-fast; (3) Paste fresh xAI API key from user's xAI dashboard (key NOT recorded in this doc); (4) Click `Test Connection`; (5) EXPECTED: success message shown in UI within 5s; SW console shows `[FSB lattice-provider-bridge]` boot log + `lattice-provider-execute` envelope flow; (6) UNEXPECTED: 400 `xa***cy` style masked-echo error (xai-key-rejected-400 was closed in Phase 6 by FINT-08; if it recurs in Phase 7 that is a regression)."
    - "Step-by-step autopilot iteration procedure included: (1) Navigate to a simple test page (e.g., user picks a single static page in their preferred Chrome tab); (2) Open sidepanel; (3) Click `Start Autopilot Session` with prompt `say 'hello' once and stop`; (4) EXPECTED: sidepanel shows >= 1 iteration step completing; SW console shows `lattice-provider-execute` envelope flow; offscreen console shows the xAI adapter invocation; (5) UNEXPECTED: hung session, network errors mentioning legacy `universal-provider.js sendRequest`, or any reference to `FSB_LATTICE_PROVIDER_BRIDGE_ENABLED`."
    - "The doc EXPLICITLY tells the user: the test is RUN BY THE USER in Chrome on their workstation; the user reports results back as one of three verdicts (`UAT-1 PASS` / `UAT-1 PARTIAL <details>` / `UAT-1 FAIL <details>`); the verifier-agent (`/gsd-verify-phase 7`) or follow-on Plan 07-04 will record the user's verdict + capture evidence (screenshots optional)."
    - "The doc EXPLICITLY references Plan 07-04 as the post-UAT ceremony plan that flips v0.10.0-MILESTONE-AUDIT.md status from in_progress to passed (gated on UAT-1 user-confirmed green)."
    - "ZERO production code touched in this plan."
    - "ZERO Lattice-side commits in this plan (INV-06 holds; LATTICE-PIN.md current_lattice_sha UNCHANGED)."
  artifacts:
    - path: ".planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md"
      provides: "Phase 7 verification report scaffold + consolidated UAT-1 6-sub-assertion procedure ready for user execution"
      contains: "UAT-1"
      min_lines: 80
  key_links:
    - from: ".planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md human_verification"
      to: ".planning/v0.10.0-MILESTONE-AUDIT.md UAT-1 section"
      via: "6 sub-assertions copied verbatim + extended with Phase 7 boot-log expectation"
      pattern: "UAT-1"
---

<objective>
Wave 3 of Phase 7: generate the consolidated UAT-1 6-sub-assertion procedure into `.planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md`. This file is the user-facing milestone-end UAT instructions document. The user runs the procedure in Chrome on their workstation; results are reported back; Plan 07-04 ceremony records the verdict + flips v0.10.0-MILESTONE-AUDIT.md status if UAT-1 passes.

The doc is the FULL test plan + expected outcomes + what-to-look-for + what-to-avoid for each assertion. It is written for the user to execute literally (not as background context). Per CONTEXT.md `<decisions>` "UAT-1 execution mode: User runs + reports (Q2 — locked)".

Output: 1 new file (`.planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md`). ONE FSB commit. ZERO production code + ZERO Lattice-side commits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v0.10.0-MILESTONE-AUDIT.md
@.planning/phases/07-archive-fsb-custom-provider-stack/07-CONTEXT.md
@.planning/phases/07-archive-fsb-custom-provider-stack/07-01-SUMMARY.md
@.planning/phases/07-archive-fsb-custom-provider-stack/07-02-SUMMARY.md

<interfaces>
<!-- v0.10.0-MILESTONE-AUDIT.md UAT-1 section (lines 224-249 in the current file). The 6 sub-assertions copied verbatim from this source. -->

```
### UAT-1: Manual MV3 reload + offscreen host smoke (single session)

**Procedure:**
1. Run `pnpm build` from repo root to ensure `extension/dist/` reflects latest source
2. Open `chrome://extensions` (Chrome / Brave / Edge), enable Developer mode if not already on
3. If extension not loaded: "Load unpacked" -> select `extension/` directory. If already loaded: click the circular reload arrow on the FSB extension card (NOT a global Chrome restart)
4. Click "service worker" link on the card to open SW DevTools console
5. Trigger offscreen host: paste into SW console -> chrome.offscreen.createDocument(...)

**Assertions (all must pass):**
| # | Check | Source |
|---|-------|--------|
| a | SW reloads cleanly; no NEW `lattice/import/ERR_MODULE_NOT_FOUND` errors caused by Phases 1-5 (pre-existing baseline errors OK) | Phase 1 D-12 #3 |
| b | Popup opens normally | Phase 1 D-12 #3 |
| c | Sidepanel opens normally | Phase 1 D-12 #3 |
| d | One autopilot iteration completes at least one step | Phase 1 D-12 #3 |
| e | `chrome.offscreen.createDocument` call resolves; offscreen page loads | Phase 5 |
| f | Offscreen page console shows `lattice-host.js` boot log; no errors from `import('lattice')` resolution | Phase 5 |
```

<!-- Phase 7 EXTENSIONS to the 6 sub-assertions: -->

- Sub-assertion (a) Phase 7 extension: also no NEW errors caused by Phases 6-7 (flag-removal + bridge-unconditional).
- Sub-assertion (d) Phase 7 extension: the autopilot iteration MUST route through the Lattice provider bridge unconditionally (no `FSB_LATTICE_PROVIDER_BRIDGE_ENABLED` fallback exists post-Phase 7; observe SW console for `[FSB lattice-provider-bridge]` envelope flow).
- Sub-assertion (f) Phase 7 extension: also assert `[FSB lattice-provider-bridge] boot: Phase 7 bridge shim registered (unconditional; legacy fallback removed)` (NEW Plan 07-01 Task 2 boot log line); the OLD `(default-on; flag = FSB_LATTICE_PROVIDER_BRIDGE_ENABLED)` boot log must NOT appear.

<!-- Additional Phase 7-specific xAI test-connection sub-test (per CONTEXT.md <specifics> step 5) -->

- xAI Test-Connection sub-test: Provider = xAI; Model = grok-4-1-fast; paste fresh xAI API key; click Test Connection; EXPECTED success within 5s; SW console shows the bridge envelope flow.

<!-- Standard VERIFICATION.md frontmatter -->

```yaml
# (yaml document delimiter removed for parser safety)
phase: 07
phase_name: archive-fsb-custom-provider-stack
verdict: human_needed
verifier_type: human_uat
created_date: 2026-05-27
gated_on: "UAT-1 user execution + verdict report"
post_uat_plan: 07-04-PLAN.md
gaps_found: []
# (yaml document delimiter removed for parser safety)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md with frontmatter + Phase 7 deliverable summary + automated verification results + human_verification section containing the consolidated UAT-1 6-sub-assertion procedure + xAI test-connection sub-test + autopilot iteration sub-test + user-verdict reporting protocol</name>
  <read_first>
    - .planning/v0.10.0-MILESTONE-AUDIT.md lines 220-260 (UAT-1 procedure + 6 sub-assertions verbatim)
    - .planning/phases/07-archive-fsb-custom-provider-stack/07-CONTEXT.md `<specifics>` block (6-step UAT-1 procedure as locked by user discuss-phase)
    - .planning/phases/07-archive-fsb-custom-provider-stack/07-01-SUMMARY.md (extract: Plan 07-01 commit SHA + the new bridge boot log text + smoke chain final PASS count)
    - .planning/phases/07-archive-fsb-custom-provider-stack/07-02-SUMMARY.md (extract: Plan 07-02 commit SHA + REQUIREMENTS FINT-09 flip confirmation + LATTICE-PIN row reference)
    - .planning/phases/01-lattice-gap-survey-scaffold/01-02-SUMMARY.md (Phase 1 carryforward UAT context: the original 5-assertion MV3 reload procedure that consolidated into UAT-1)
  </read_first>
  <files>.planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md</files>
  <behavior>
    - File is CREATED (does not exist before this task; does not modify any existing file)
    - Frontmatter contains: `phase: 07`, `phase_name: archive-fsb-custom-provider-stack`, `verdict: human_needed`, `verifier_type: human_uat`, `created_date: 2026-05-27`, `gated_on: "UAT-1 user execution + verdict report"`, `post_uat_plan: 07-04-PLAN.md`, `gaps_found: []`
    - Body sections (in this order):
      - `# Phase 7 Verification Report`
      - `## Phase Summary` — 1 paragraph: Phase 7 = archive FSB custom provider stack (Strategy B: flag removal only; physical archive deferred to v0.11.0+); references Plan 07-01 (flag strip across 4 files) + Plan 07-02 (REQUIREMENTS FINT-09 flip + LATTICE-PIN row) + THIS plan (Plan 07-03 generates UAT-1 procedure) + Plan 07-04 (post-UAT ceremony)
      - `## Automated Verification (Phase 7 production code)` — table of automated checks already executed: smoke chain PASS count (from Plan 07-01); grep audit for `FSB_LATTICE_PROVIDER_BRIDGE_ENABLED` returning 0; INV-04 setTimeout count = 8; npm test exit code 0; node --check on both modified production files
      - `## Cross-Phase Invariants` — table confirming INV-01..06 all HOLDING after Phase 7
      - `## Human Verification (UAT-1 — milestone-end gate)` — the user-facing procedure (see structured action below)
      - `## User Verdict Reporting` — explicit instructions for the user to report back with one of three verdicts + format
      - `## Post-UAT Ceremony` — pointer to Plan 07-04
    - The `Human Verification` section MUST include:
      - **Preparation** step (npm run build NOT needed if Phase 7 introduced no bundler-relevant changes; document accordingly)
      - **Step-by-step Chrome procedure** (load unpacked at the absolute path `/Users/lakshmanturlapati/Desktop/FSB/automation/extension/` if not already loaded; otherwise click reload arrow on FSB card)
      - **6 sub-assertions table** (a-f) with: # | Check | Phase 7 expected outcome | What to LOOK FOR | What to AVOID
      - **xAI Test-Connection sub-test** (Provider=xAI; Model=grok-4-1-fast; click Test Connection; expected success in <5s + SW console shows bridge envelope flow)
      - **Autopilot iteration sub-test** (load a simple page; start autopilot session with `say 'hello' once and stop`; observe >= 1 iteration step; SW console shows lattice-provider-execute envelope flow)
      - **Boot log expectations** (NEW Phase 7 bridge boot log: `[FSB lattice-provider-bridge] boot: Phase 7 bridge shim registered (unconditional; legacy fallback removed)`; OLD `(default-on; flag = FSB_LATTICE_PROVIDER_BRIDGE_ENABLED)` boot log must NOT appear)
      - **Error patterns to NOT see**: any `FSB_LATTICE_PROVIDER_BRIDGE_ENABLED` reference; any `universal-provider.js sendRequest` reference; the xai-key-rejected-400 `xa***cy` masked-echo pattern (closed in Phase 6 — regression check); `lattice/import/ERR_MODULE_NOT_FOUND` from NEW Phase 6-7 code (pre-existing baseline errors OK)
    - The `User Verdict Reporting` section MUST explicitly list 3 verdicts: `UAT-1 PASS` (all 6 sub-assertions pass + both sub-tests green); `UAT-1 PARTIAL <details>` (some sub-assertions pass, others fail — user lists which); `UAT-1 FAIL <details>` (one or more assertions fail blocking the milestone — user describes what broke)
    - The doc MUST NOT contain emojis (per /Users/lakshmanturlapati/.claude/CLAUDE.md global rules)
    - File MUST be >= 80 lines (the must_haves min_lines floor; ensures all 6 sub-assertions + both sub-tests + reporting protocol + post-UAT pointer are spelled out)
  </behavior>
  <action>
    Step 1 — Read all `<read_first>` files. Extract:
    - Plan 07-01 head FSB commit SHA from 07-01-SUMMARY.md
    - Plan 07-02 head FSB commit SHA from 07-02-SUMMARY.md
    - Smoke final PASS count from 07-01-SUMMARY.md (expected 83 — was 85 at Phase 6 end; -2 from the flag-assertion semantic flip)
    - The new bridge boot log text (verbatim) from Plan 07-01 Task 2: `[FSB lattice-provider-bridge] boot: Phase 7 bridge shim registered (unconditional; legacy fallback removed)`

    Step 2 — Write `.planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md` with the following structure:

    ```markdown
# (yaml document delimiter removed for parser safety)
    phase: 07
    phase_name: archive-fsb-custom-provider-stack
    verdict: human_needed
    verifier_type: human_uat
    created_date: 2026-05-27
    gated_on: "UAT-1 user execution + verdict report"
    post_uat_plan: 07-04-PLAN.md
    gaps_found: []
# (yaml document delimiter removed for parser safety)

    # Phase 7 Verification Report

    ## Phase Summary

    Phase 7 (archive FSB custom provider stack) ships under Strategy B (CONTEXT.md decisions): the `FSB_LATTICE_PROVIDER_BRIDGE_ENABLED` feature flag is fully stripped from production + test code, the bridge becomes unconditional, INV-03 wording is strengthened, FINT-09 is closed in REQUIREMENTS.md, LATTICE-PIN.md gains a Phase 7 row, and the consolidated UAT-1 milestone-end gate is run as a single Chrome MV3 reload session. Physical archive of `extension/ai/universal-provider.js` to `extension/_archive/` is DEFERRED to v0.11.0+ pending Lattice-native providerInstance metadata migration (5 in-extension consumers still need the file — see FINT-09 narrative in REQUIREMENTS.md for full rationale).

    Plans completed:
    - Plan 07-01 (FSB commit `<07-01-sha>`): strip flag from agent-loop.js (callProviderWithTools tail) + lattice-provider-bridge.js (JSDoc + boot log) + lattice-provider-bridge-smoke.test.js (Part 3 + 5 flag assertions to expect zero) + agent-loop-empty-contents.test.js (rewritten to intercept bridge envelope instead of legacy provider stub).
    - Plan 07-02 (FSB commit `<07-02-sha>`): REQUIREMENTS.md INV-03 third era clause revised to Strategy B form + FINT-09 narrative + traceability flipped Pending -> Complete + Total v1 + Last updated footers bumped; LATTICE-PIN.md Phase 7 row appended (current_lattice_sha frontmatter UNCHANGED at `e95067bf...` per INV-06).
    - Plan 07-03 (THIS file): consolidated UAT-1 6-sub-assertion procedure generated into the Human Verification section below.
    - Plan 07-04 (gated on user UAT-1 verdict): post-UAT ceremony — flip v0.10.0-MILESTONE-AUDIT.md UAT-1 status + milestone status from in_progress to passed.

    ## Automated Verification (Phase 7 production code)

    All automated checks executed as part of Plan 07-01 + 07-02 and re-runnable via `npm test`:

    | Check | Command | Expected | Actual (Plan 07-01) |
    |-------|---------|----------|---------------------|
    | Smoke chain green | `npm test` | exit 0 | exit 0 |
    | Smoke PASS count | `node tests/lattice-provider-bridge-smoke.test.js | tail -1` | `passed: 83` (was 85 at Phase 6 end; -2 from flag-assertion semantic flip) | <fill from 07-01-SUMMARY.md> |
    | Flag stripped from prod + tests | `grep -rn "FSB_LATTICE_PROVIDER_BRIDGE_ENABLED" extension/ tests/ | wc -l` | `0` | `0` |
    | INV-04 setTimeout count | `grep -c "setTimeout" extension/ai/agent-loop.js` | `8` | `8` |
    | Legacy fallback deleted | `grep -c "providerInstance\.sendRequest" extension/ai/agent-loop.js` | `0` | `0` |
    | Unconditional bridge call | `grep -c "executeViaBridge(" extension/ai/agent-loop.js` | `1` | `1` |
    | universal-provider.js still on disk (Strategy B) | `test -f extension/ai/universal-provider.js && echo OK` | `OK` | `OK` |
    | _archive/ does NOT exist (physical archive deferred) | `test ! -d extension/_archive && echo OK` | `OK` | `OK` |
    | agent-loop.js syntactically valid | `node --check extension/ai/agent-loop.js` | exit 0 | exit 0 |
    | bridge syntactically valid | `node --check extension/ai/lattice-provider-bridge.js` | exit 0 | exit 0 |

    ## Cross-Phase Invariants

    All Hard Invariants HOLDING after Phase 7:

    | INV | Status | Evidence |
    |-----|--------|----------|
    | INV-01 MCP wire contracts UNTOUCHED | HOLDING | `tests/tool-definitions-parity.test.js` still 142 PASS |
    | INV-02 Tool surface parity | HOLDING | `extension/ai/tool-definitions.js` + `mcp/ai/tool-definitions.cjs` BYTE-FROZEN across Phase 7 |
    | INV-03 Provider parity (Strategy B-aware) | HOLDING | All 7 providers route via offscreen bridge UNCONDITIONALLY (Plan 07-01); INV-03 wording in REQUIREMENTS.md updated to Strategy B 3-era narrative |
    | INV-04 MV3-survivability iterator BYTE-FROZEN | HOLDING | `grep -c "setTimeout" extension/ai/agent-loop.js` returns 8; iterator pattern preserved (line numbers shifted ~5 upward from Phase 6 end positions due to flag wrapper deletion; content-based discovery in smoke Part 6 absorbs the shift) |
    | INV-05 No resurrection of deprecated modules | HOLDING | `extension/agents/{agent-executor,agent-manager,agent-scheduler}.js` BYTE-FROZEN + DEPRECATED banners intact |
    | INV-06 Lattice primitives live in Lattice's repo | HOLDING | Phase 7 ships ZERO Lattice-side commits; `current_lattice_sha` UNCHANGED at `e95067bf...` |

    ## Human Verification (UAT-1 — milestone-end gate)

    This is the consolidated UAT-1 procedure covering Phase 1 (MV3 SW boot) + Phase 5 (offscreen host loads) + Phase 6 (provider bridge resolves xAI test request) + Phase 7 (no regressions after legacy path is removed) in a SINGLE Chrome MV3 reload session.

    Per Phase 7 CONTEXT.md `<decisions>` (Q2 — locked): UAT-1 execution mode = "User runs + reports". You run the procedure below in Chrome on your workstation; you report results back; Plan 07-04 ceremony records the verdict + flips v0.10.0-MILESTONE-AUDIT.md status if UAT-1 passes.

    ### Preparation

    1. No `npm run build` rebuild is needed for Phase 7 (Phase 7 introduces no bundler-relevant changes; only flag-strip in agent-loop.js + bridge JSDoc/boot log + smoke + regression test; the existing `extension/dist/offscreen/lattice-host.js` from Phase 6 is still current).
    2. If you HAVE made any local edits to extension source after the last `npm run build`, run `npm run build` from the repo root first to refresh `extension/dist/`.

    ### Chrome MV3 reload procedure

    1. Open `chrome://extensions` in Chrome (or Brave / Edge).
    2. Enable Developer mode (top-right toggle) if not already on.
    3. **If extension not loaded:** click `Load unpacked` -> navigate to and select `/Users/lakshmanturlapati/Desktop/FSB/automation/extension/`. **If already loaded:** click the circular reload arrow on the FSB extension card (NOT a global Chrome restart).
    4. On the FSB extension card, click the `service worker` link to open the SW DevTools console.
    5. On the FSB extension card, if an offscreen page link appears, click it to open the offscreen page DevTools console. If no link appears yet, proceed — the offscreen page opens automatically on first autopilot session (Plan 06-02 `ensureLatticeOffscreen()` from `onStartup`) or when the SW receives a `lattice-provider-execute` envelope.

    ### 6 sub-assertions (all must pass)

    | # | Check | Phase 7 expected outcome | What to LOOK FOR | What to AVOID |
    |---|-------|--------------------------|-------------------|----------------|
    | a | SW reloads cleanly | No NEW errors caused by Phases 1-7 (pre-existing baseline errors are OK) | SW console first-page-load shows initialisation logs + `[FSB lattice-provider-bridge]` boot log line | `lattice/import/ERR_MODULE_NOT_FOUND` originating from Phase 6 or 7 code; any uncaught exception trace mentioning `agent-loop.js` or `lattice-provider-bridge.js` |
    | b | Popup opens normally | Popup renders without JS errors | Popup UI shows the usual FSB controls; popup-console (right-click popup -> Inspect) is error-free | Reference errors mentioning `universal-provider` or `UniversalProvider` (Strategy B keeps the global; the popup's `<script src>` load must still resolve) |
    | c | Sidepanel opens normally | Sidepanel renders without JS errors | Sidepanel shows the autopilot prompt UI; sidepanel-console is error-free | Reference errors mentioning the removed flag |
    | d | One autopilot iteration completes >= 1 step | Autopilot runs through the Lattice provider bridge UNCONDITIONALLY (no flag, no fallback) | Sidepanel shows >= 1 step in the iteration history; SW console shows the `lattice-provider-execute` envelope being sent to the offscreen host AND the matching ok:true response | Hung session; any `providerInstance.sendRequest` reference in stack traces; the `FSB_LATTICE_PROVIDER_BRIDGE_ENABLED` flag mentioned anywhere |
    | e | `chrome.offscreen.createDocument` resolves; offscreen page loads | Offscreen page is created and visible in chrome://extensions | Offscreen page link visible on FSB extension card; offscreen page DevTools console accessible | `chrome.offscreen` API errors; "no offscreen reason" errors |
    | f | Offscreen page console shows boot logs cleanly | `[FSB lattice-host]` boot log line visible AND the bridge boot log `[FSB lattice-provider-bridge] boot: Phase 7 bridge shim registered (unconditional; legacy fallback removed)` visible (only the SW side runs the bridge; the offscreen side runs lattice-host) | The two expected boot log lines (one in SW console for the bridge, one in offscreen console for the host) | OLD boot log text `(default-on; flag = FSB_LATTICE_PROVIDER_BRIDGE_ENABLED)` — its presence means the Plan 07-01 Task 2 boot log update did not deploy |

    ### xAI Test-Connection sub-test (verifies Phase 6 FINT-08 P1+P2 closure non-regression + Phase 7 flag-strip non-regression)

    1. Open the extension Options page (right-click FSB icon -> Options OR chrome://extensions -> FSB card -> Details -> Extension options).
    2. In the provider settings: Provider = `xAI`; Model = `grok-4-1-fast`.
    3. Paste a fresh xAI API key from your xAI dashboard. (Do NOT record the key in this doc or in any commit.)
    4. Click `Test Connection`.
    5. **Expected:** Success message shown in UI within ~5s. SW console shows `[FSB lattice-provider-bridge]` boot log + `lattice-provider-execute` envelope sent and ok:true response received.
    6. **What to AVOID:** Any 400 error with the masked-echo pattern `xa***cy` (xai-key-rejected-400 was closed in Phase 6 via FINT-08 P1 trim + P2 storage-read rewrite; if it recurs in Phase 7 that signals a regression and UAT-1 FAILS).

    ### Autopilot iteration sub-test (verifies sub-assertion (d) end-to-end + Phase 7 flag-strip non-regression)

    1. Navigate to a simple static test page in a Chrome tab (any single static page — example: `https://example.com`).
    2. Open the FSB sidepanel.
    3. Click `Start Autopilot Session` with the prompt: `say 'hello' once and stop`.
    4. **Expected:** Sidepanel shows >= 1 iteration step completing. SW console shows the `lattice-provider-execute` envelope flow. Offscreen page console shows the xAI adapter invocation.
    5. **What to AVOID:** Hung session; network errors mentioning the legacy `universal-provider.js sendRequest`; any reference to `FSB_LATTICE_PROVIDER_BRIDGE_ENABLED`; uncaught exceptions in `callProviderWithTools` or the bridge.

    ## User Verdict Reporting

    After running the procedure, report back with ONE of these three verdicts:

    - **`UAT-1 PASS`** — all 6 sub-assertions pass AND both sub-tests (xAI Test-Connection + autopilot iteration) are green. Plan 07-04 ceremony will flip v0.10.0-MILESTONE-AUDIT.md `UAT-1` to executed + milestone status `in_progress` -> `passed`. Optional: capture screenshots for evidence (sidepanel iteration history + SW console bridge envelope flow).
    - **`UAT-1 PARTIAL <details>`** — some sub-assertions pass, others fail. List which sub-assertions failed + what you observed. Plan 07-04 ceremony will record the partial verdict + may schedule a follow-on gap-closure plan instead of flipping the milestone status.
    - **`UAT-1 FAIL <details>`** — one or more critical sub-assertions fail, blocking the milestone. Describe what broke (which assertion, what error, which page/UI). Plan 07-04 ceremony will record the verdict + recommend rolling back Plan 07-01 (restore the flag wrapper + legacy fallback) OR escalating to a debug session.

    Reply in the conversation that invoked `/gsd-verify-phase 7` (or to the assistant that ran Plan 07-03) with the verdict + details. The verifier-agent / Plan 07-04 will pick up from there.

    ## Post-UAT Ceremony

    See `.planning/phases/07-archive-fsb-custom-provider-stack/07-04-PLAN.md` for the post-UAT ceremony. Plan 07-04 is gated on the UAT-1 user-confirmed verdict:

    - If `UAT-1 PASS`: flip `.planning/v0.10.0-MILESTONE-AUDIT.md` `UAT-1` status to `executed` + milestone status `in_progress` -> `passed`; backfill Plan 07-03 + Plan 07-04 SHAs into the `.planning/LATTICE-PIN.md` Phase 7 row Notes cell.
    - If `UAT-1 PARTIAL` or `UAT-1 FAIL`: record the verdict in `.planning/v0.10.0-MILESTONE-AUDIT.md` UAT-1 status_history; mark UAT-1 status as `pending_execution` or `failed` per the user's verdict; DO NOT flip milestone status.
    ```

    Replace `<07-01-sha>` and `<07-02-sha>` with actual SHAs captured in Step 1. Backfill the "Smoke PASS count" actual value from Plan 07-01 SUMMARY.

    Step 3 — Verify with the verification commands.
  </action>
  <verify>
    <automated>test -f .planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md && echo "exists"; wc -l .planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md; grep -c "UAT-1" .planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md; grep -c "Strategy B" .planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md; grep -c "Phase 7 bridge shim registered" .planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md` exists
    - File is `>= 80 lines` (`wc -l` returns >= 80)
    - Frontmatter contains `verdict: human_needed`, `verifier_type: human_uat`, `gated_on:`, `post_uat_plan: 07-04-PLAN.md`
    - `grep -c "UAT-1" .planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md` returns `>= 5` (UAT-1 referenced in section header + procedure intro + 3 verdict labels minimum)
    - `grep -c "Strategy B" .planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md` returns `>= 2` (Phase Summary + INV table)
    - `grep -c "Phase 7 bridge shim registered" .planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md` returns `>= 1` (NEW boot log expectation referenced in sub-assertion (f))
    - `grep -c "FSB_LATTICE_PROVIDER_BRIDGE_ENABLED" .planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md` returns `>= 1` (referenced in "What to AVOID" as a NEGATIVE signal — flag must NOT appear in runtime)
    - `grep -c "xai-key-rejected-400" .planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md` returns `>= 1` (Phase 6 regression check referenced in xAI Test-Connection sub-test)
    - `grep -c "grok-4-1-fast" .planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md` returns `>= 1` (xAI Test-Connection model specified per CONTEXT.md <specifics>)
    - `grep -c "/Users/lakshmanturlapati/Desktop/FSB/automation/extension/" .planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md` returns `>= 1` (Chrome MV3 load-unpacked absolute path specified)
    - `grep -c "UAT-1 PASS\\|UAT-1 PARTIAL\\|UAT-1 FAIL" .planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md` returns `>= 3` (3 verdict options listed)
    - `grep -c "07-04-PLAN.md" .planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md` returns `>= 1` (post-UAT ceremony pointer)
    - Zero emojis present (per global CLAUDE.md rules): a grep for common emoji patterns returns 0
    - File contains 6 sub-assertion rows (a-f) in a markdown table
  </acceptance_criteria>
  <done>
    .planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md created with frontmatter + Phase Summary + Automated Verification table + Cross-Phase Invariants table + Human Verification (UAT-1 procedure with Preparation + Chrome reload steps + 6 sub-assertions + xAI Test-Connection sub-test + autopilot iteration sub-test + boot log expectations + error patterns to avoid) + User Verdict Reporting (3 verdicts) + Post-UAT Ceremony pointer. File is >= 80 lines. Zero emojis.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan ↔ user-facing docs | The doc is the test procedure the user executes literally in Chrome on their workstation. If steps are ambiguous, the user runs an unintended test or misreports the verdict. Mitigation: every step lists EXPECTED outcomes + WHAT TO LOOK FOR + WHAT TO AVOID. |
| Plan ↔ API key exposure | The xAI Test-Connection sub-test asks the user to paste a fresh xAI API key. The doc EXPLICITLY tells the user NOT to record the key in the doc or in any commit. The captured evidence (screenshots) is the user's responsibility to redact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Tampering | doc accuracy (sub-assertion drift from v0.10.0-MILESTONE-AUDIT.md) | mitigate | Sub-assertions a-f copied VERBATIM from v0.10.0-MILESTONE-AUDIT.md UAT-1 table (lines 240-249); Phase 7 extensions appended as explicit addendums per sub-assertion. Grep acceptance criteria check for both UAT-1 references + NEW Phase 7 boot log text. |
| T-07-03-02 | Information disclosure | user pastes API key into chat | accept | Doc explicitly tells user NOT to record the key in the doc OR in any commit. User Verdict Reporting section says "Optional: capture screenshots for evidence" — user controls screenshot capture + redaction. |
| T-07-03-03 | Repudiation | user reports `UAT-1 PASS` but verdict was actually `PARTIAL` | accept | The 3-verdict reporting protocol is explicit. Plan 07-04 records the verdict in v0.10.0-MILESTONE-AUDIT.md `status_history` (date + verdict + note + verifier signature). Future audit can re-run UAT-1 to validate. |
</threat_model>

<verification>
Plan 07-03 deliverable verification:

```bash
# File exists + size + structure
test -f .planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md && echo "exists"
wc -l .planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md   # >= 80

# Sections present
grep -c "## Phase Summary" .planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md         # 1
grep -c "## Automated Verification" .planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md # 1
grep -c "## Cross-Phase Invariants" .planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md # 1
grep -c "## Human Verification" .planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md    # 1
grep -c "## User Verdict Reporting" .planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md # 1
grep -c "## Post-UAT Ceremony" .planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md     # 1

# 6 sub-assertions + sub-tests
grep -c "UAT-1" .planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md                    # >= 5
grep -c "Strategy B" .planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md               # >= 2
grep -c "Phase 7 bridge shim registered" .planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md  # >= 1
grep -c "grok-4-1-fast" .planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md            # >= 1
grep -c "xai-key-rejected-400" .planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md     # >= 1

# Zero production code touched
git status --porcelain extension/ tests/                                                                    # empty
```
</verification>

<success_criteria>
- `.planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md` created with full UAT-1 procedure
- File >= 80 lines
- Frontmatter records `verdict: human_needed` + `verifier_type: human_uat` + `gated_on:` + `post_uat_plan: 07-04-PLAN.md`
- All 6 sub-assertions (a-f) spelled out in a markdown table with Check + Expected + Look For + Avoid columns
- xAI Test-Connection sub-test + autopilot iteration sub-test + 3 verdict reporting options included
- ZERO production code touched
- ZERO Lattice-side commits (INV-06 holds)
- ONE FSB commit: `docs(07-03): generate UAT-1 6-sub-assertion procedure into 07-VERIFICATION.md` with `Ref: FSB v0.10.0-attempt-2 Phase 7 Plan 07-03`
- Wave 3 -> Wave 4 handoff: User runs UAT-1 + reports verdict; Plan 07-04 gated on user verdict
</success_criteria>

<output>
After completion, create `.planning/phases/07-archive-fsb-custom-provider-stack/07-03-SUMMARY.md` per the template at $HOME/.claude/get-shit-done/templates/summary.md.

Include:
- 07-VERIFICATION.md file size + structural summary (sections + sub-assertions count)
- Captured Plan 07-01 + 07-02 SHAs that were backfilled into 07-VERIFICATION.md
- Plan 07-03 FSB commit SHA
- Confirmation that UAT-1 procedure is now ready for user execution
- Wave 3 -> Wave 4 handoff: user runs UAT-1 in Chrome; reports verdict; Plan 07-04 records verdict + flips milestone status (gated on PASS verdict)
- Reminder to user: the next action is to run UAT-1 manually in Chrome per the 07-VERIFICATION.md procedure, then report back
</output>
