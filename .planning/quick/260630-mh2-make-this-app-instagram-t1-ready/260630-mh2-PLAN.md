---
quick_id: 260630-mh2
description: Make this app instagram T1-ready
status: completed
date: 2026-06-30
---

# Quick Task 260630-mh2: Make Instagram T1-ready

## Execution Note

Implementation intentionally narrowed this planner output. The completed work promotes only public Instagram profile, post, and topsearch reads, and keeps the social/feed/mutation rows guarded fail-closed. Private or session-bound reads listed in the original plan were not activated because the current evidence did not justify shipping authenticated internal request shapes.

## Scope

Promote Instagram's same-origin GET read surface to T1a and register its POST/social mutation rows as guarded fail-closed. Keep execution pinned to `https://www.instagram.com`; do not introduce a separate-origin API bridge, direct `fetch`, `chrome.tabs`, `chrome.scripting`, `document.cookie`, local storage reads, bearer auth, or secret-bearing logs.

Read rows to make `t1-ready`:

- `instagram.get_conversation_messages`
- `instagram.get_current_user`
- `instagram.get_followers`
- `instagram.get_following`
- `instagram.get_friendship_status`
- `instagram.get_post`
- `instagram.get_post_comments`
- `instagram.get_post_likers`
- `instagram.get_stories_tray`
- `instagram.get_user_posts`
- `instagram.get_user_profile`
- `instagram.get_user_stories`
- `instagram.list_collections`
- `instagram.list_conversations`
- `instagram.list_saved_posts`
- `instagram.search`
- `instagram.search_hashtags`
- `instagram.search_users`

Guarded fail-closed rows:

- `instagram.create_comment`
- `instagram.follow_user`
- `instagram.get_home_feed`
- `instagram.get_suggested_users`
- `instagram.like_post`
- `instagram.save_post`
- `instagram.send_message`
- `instagram.unfollow_user`
- `instagram.unlike_post`
- `instagram.unsave_post`

## Source Audit

- GOAL: "Make this app instagram T1-ready" is covered by Task 1 read/guarded handler creation, Task 2 readiness/evidence wiring, and Task 3 regression/generation.
- STATE invariants: two-tool capability surface, MV3 Wall 1, same-origin Wall 2, sensitive-origin flagging, and fail-closed writes are preserved by using bundled handler code, `executeBoundSpec`, and guarded mutation records only.
- Project skills: no `.claude/skills/` or `.agents/skills/` project skills were present; no `AGENTS.md` or `CLAUDE.md` project instruction file was present.

## Tasks

### 1. Add the Instagram same-origin head

Files:

- `catalog/handlers/instagram.js`
- `extension/catalog/handlers/instagram.js`

Action:

- Create an IIFE/CommonJS-compatible handler module exporting `FsbHandlerInstagram`.
- Use `INSTAGRAM_ORIGIN = "https://www.instagram.com"` and service `instagram.com`.
- Register the 18 read slugs above as `tier: "T1a"`, `sideEffectClass: "read"`, `origin: INSTAGRAM_ORIGIN`, with closed params matching the existing descriptors.
- Build specs only through `ctx.executeBoundSpec`, with `authStrategy: "same-origin-cookie"`, `origin: INSTAGRAM_ORIGIN`, and URLs under `https://www.instagram.com/api/v1/...`.
- Use the vendored endpoint shapes from `vendor/opentabs-snapshot/plugins/instagram/src/tools/*.ts`:
  - `/accounts/edit/web_form_data/` plus `/users/web_profile_info/?username=...` for `get_current_user`
  - `/users/web_profile_info/?username=...` for `get_user_profile`
  - `/media/{media_id}/info/`, `/media/{media_id}/comments/`, `/media/{media_id}/likers/`
  - `/friendships/{user_id}/followers/`, `/friendships/{user_id}/following/`, `/friendships/show/{user_id}/`
  - `/feed/user/{user_id}/`, `/feed/user/{user_id}/story/`, `/feed/reels_tray/`
  - `/direct_v2/inbox/`, `/direct_v2/threads/{thread_id}/`
  - `/feed/saved/posts/`, `/collections/list/`
  - `/web/search/topsearch/`, `/tags/search/`
- Include `Accept: application/json`, `X-IG-App-ID: 936619743392459`, and `X-Requested-With: XMLHttpRequest`; use only same-origin bootstrap data for any optional CSRF carrier and return `RECIPE_DOM_FALLBACK_PENDING` if a required carrier or response shape is missing.
- Sanitize outputs: do not return raw bootstrap HTML, cookies, CSRF tokens, `ds_user_id`, email, phone number, or auth headers. Shape guards should reject redirected, 401/403, non-object, or Instagram error envelopes.
- Register the 10 guarded rows above as `tier: "T1a"` fail-closed entries whose `handle()` returns INV-03-compatible `RECIPE_DOM_FALLBACK_PENDING` without calling `ctx.executeBoundSpec`.
- Copy the handler byte-for-byte to `extension/catalog/handlers/instagram.js`.

Verify (automated):

- `node -e "const h=require('./catalog/handlers/instagram.js'); const read=['instagram.get_conversation_messages','instagram.get_current_user','instagram.get_followers','instagram.get_following','instagram.get_friendship_status','instagram.get_post','instagram.get_post_comments','instagram.get_post_likers','instagram.get_stories_tray','instagram.get_user_posts','instagram.get_user_profile','instagram.get_user_stories','instagram.list_collections','instagram.list_conversations','instagram.list_saved_posts','instagram.search','instagram.search_hashtags','instagram.search_users']; const guarded=['instagram.create_comment','instagram.follow_user','instagram.get_home_feed','instagram.get_suggested_users','instagram.like_post','instagram.save_post','instagram.send_message','instagram.unfollow_user','instagram.unlike_post','instagram.unsave_post']; for (const slug of read) if (!h[slug] || h[slug].sideEffectClass !== 'read' || h[slug].origin !== 'https://www.instagram.com') throw new Error('bad read '+slug); for (const slug of guarded) if (!h[slug] || h[slug].sideEffectClass !== 'write' || h[slug].origin !== 'https://www.instagram.com') throw new Error('bad guarded '+slug); console.log('PASS instagram handler surface');"`
- `cmp catalog/handlers/instagram.js extension/catalog/handlers/instagram.js`

Done:

- Instagram has a bundled same-origin handler pair with 18 executable read rows and 10 inert guarded rows, all pinned to `https://www.instagram.com`.

### 2. Wire Instagram into readiness, origin, and write-evidence gates

Files:

- `extension/background.js`
- `extension/utils/capability-catalog.js`
- `extension/utils/capability-search.js`
- `scripts/report-t1-readiness.mjs`
- `scripts/coverage-report.mjs`
- `scripts/verify-t1-port-contract.mjs`
- `scripts/verify-origin-classification.mjs`
- `scripts/verify-recipe-path-guard.mjs`
- `catalog/write-activation-evidence.json`

Action:

- Add `extension/catalog/handlers/instagram.js` to the service-worker import list near the other catalog head handlers.
- Add `{ global: "FsbHandlerInstagram", service: "instagram.com", origin: "https://www.instagram.com" }` to `HEAD_HANDLER_MODULES`.
- Add `instagram.js` to every handler-loader or handler-module list used by readiness reporting, coverage reporting, T1 port-contract verification, origin-classification verification, and recipe-path allowlisting.
- Keep `https://*.instagram.com` classified sensitive; add exact `FsbHandlerInstagram` origin assertions to the origin-classification unit test in Task 3.
- Add all 10 guarded Instagram rows to `GUARDED_SET` in `scripts/report-t1-readiness.mjs`.
- Add matching `guardedWrites` records to `catalog/write-activation-evidence.json` with `status: "guarded-fail-closed"`, a specific `failClosedReason` per slug, template `.planning/phases/49-guarded-writes-activation-pipeline/49-LIVE-UAT-TEMPLATE.md`, and required evidence fields `["method", "path", "bodyShape", "csrfLocation", "auditRedactionProof", "loadedExtensionSmoke"]`.
- Do not edit vendored OpenTabs files and do not regenerate descriptors from the importer.

Verify (automated):

- `node scripts/verify-recipe-path-guard.mjs`
- `node scripts/verify-origin-classification.mjs`
- `node scripts/verify-t1-port-contract.mjs`
- `node scripts/verify-write-activation-evidence.mjs`
- `node scripts/coverage-report.mjs`

Done:

- Resolver, search/readiness surfaces, origin gates, path guard, and write-evidence gate all recognize Instagram's new T1a/guarded split.

### 3. Add regression coverage and refresh generated artifacts

Files:

- `tests/capability-head-handlers.test.js`
- `tests/head-handler-upgrade.test.js`
- `tests/head-handler-cap.test.js`
- `tests/verify-origin-classification.test.js`
- `tests/guarded-write-failclosed.test.js`
- `tests/t1-terminal-states.test.js`
- `.planning/phases/44-t1-readiness-inventory-status-surface/44-T1-READINESS.json`
- `.planning/phases/44-t1-readiness-inventory-status-surface/44-T1-READINESS.md`
- `extension/catalog/recipe-index.generated.js`
- `dist/fsb-extension-v0.9.90.zip`
- `.planning/STATE.md`
- `.planning/quick/260630-mh2-make-this-app-instagram-t1-ready/260630-mh2-SUMMARY.md`

Action:

- Extend `tests/capability-head-handlers.test.js` with an Instagram section that proves:
  - all 18 read slugs are T1a read entries pinned to `https://www.instagram.com`
  - all 10 guarded rows return `RECIPE_DOM_FALLBACK_PENDING` and never call `executeBoundSpec`
  - no direct `fetch`, `XMLHttpRequest`, `chrome.tabs`, `chrome.scripting`, `document.cookie`, local storage reads, bearer auth, or secret-bearing logging appears in the handler source
  - representative specs are correct for `get_user_profile`, `get_post`, `search_users`, `list_conversations`, and `list_saved_posts`
  - bad auth/shape cases fail closed
- Add all 28 Instagram rows to `tests/head-handler-upgrade.test.js`: 18 ready reads plus 10 guarded rows should resolve through `instagram.js` at `https://www.instagram.com`.
- Update `tests/head-handler-cap.test.js` exact head count from 25 to 26 and include `FsbHandlerInstagram`.
- Extend `tests/verify-origin-classification.test.js` with `FsbHandlerInstagram === https://www.instagram.com` and same-origin pass assertions.
- Extend `tests/guarded-write-failclosed.test.js` with the 10 guarded Instagram rows.
- Extend `tests/t1-terminal-states.test.js` so Instagram's read slugs are `t1-ready`, guarded rows are `t1-guarded-fail-closed`, and no Instagram row remains incorrectly labeled direct-ready without handler proof.
- Run `node scripts/report-t1-readiness.mjs` to refresh the Phase 44 readiness report.
- Run `npm run package:extension` so the generated extension bundle includes `extension/catalog/handlers/instagram.js`.
- Record the execution result in `260630-mh2-SUMMARY.md` and add a Quick Tasks Completed row in `.planning/STATE.md`.

Verify (automated):

- `node tests/capability-head-handlers.test.js`
- `node tests/head-handler-upgrade.test.js`
- `node tests/head-handler-cap.test.js`
- `node tests/verify-origin-classification.test.js`
- `node tests/guarded-write-failclosed.test.js`
- `node tests/t1-terminal-states.test.js`
- `node scripts/report-t1-readiness.mjs`
- `node scripts/verify-t1-readiness-gate.mjs`
- `npm run package:extension`
- `npm run validate:extension`

Done:

- Focused tests and extension validation pass; readiness artifacts show Instagram with 18 `t1-ready` reads and 10 guarded fail-closed rows; quick-task bookkeeping is recorded.

## Success Criteria

- `search_capabilities` can surface Instagram read rows as handler-backed `t1-ready`, while guarded rows are visibly fail-closed.
- `invoke_capability` routes Instagram reads through the bundled handler and same-origin `executeBoundSpec`.
- Instagram POST/social mutation rows never execute a network spec without live mutation-body UAT evidence.
- Recipe-path, origin-classification, readiness, write-evidence, and package validation gates are green.
