---
phase: quick-260531-63l
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extension/offscreen/lattice-host.html
autonomous: true
requirements:
  - UAT-1-sub-f
must_haves:
  truths:
    - "The offscreen document loads lattice-host.js from extension/dist/offscreen/ (the bundled output with the 'lattice' bare specifier rewritten)."
    - "Chrome no longer throws 'Failed to resolve module specifier \"lattice\"' on offscreen document load."
    - "The SW createDocument URL (chrome.offscreen.createDocument({url: 'offscreen/lattice-host.html', ...})) is unchanged."
  artifacts:
    - path: "extension/offscreen/lattice-host.html"
      provides: "Offscreen host HTML pointing at bundled ESM"
      contains: '<script type="module" src="../dist/offscreen/lattice-host.js">'
  key_links:
    - from: "extension/offscreen/lattice-host.html"
      to: "extension/dist/offscreen/lattice-host.js"
      via: "<script type=\"module\" src> relative path"
      pattern: 'src="\.\./dist/offscreen/lattice-host\.js"'
---

<objective>
Wire the offscreen lattice-host.html to the bundled dist lattice-host.js so the offscreen document actually loads at MV3 runtime.

Purpose: Unblocks UAT-1 sub-assertion (f). Phase 5 Plan 05-04 landed the source HTML pointing at sibling source JS that uses a bare `lattice` specifier; Chrome ESM resolver rejects that. The bundled output at extension/dist/offscreen/lattice-host.js (84937 bytes, 0 bare-`lattice` specifiers) has already rewritten the bare specifier to inline Lattice modules. Only the HTML's `<script src>` needs to point at it.

Output: One-line edit to extension/offscreen/lattice-host.html changing the script src.

Rationale (root cause): Phase 5 Plan 05-04's offscreen HTML header explicitly states "Production: a future phase flips manifest to load from dist/; in Plan 05-04 the source HTML + source JS land in extension/ and the bundled JS lands in extension/dist/." That flip was never executed in Phase 5, 6, or 7. UAT-1 was deferred at each gate, so the latent bug surfaced now at the first real Chrome reload session: `lattice-host.html:1 Uncaught TypeError: Failed to resolve module specifier "lattice". Relative references must start with either "/", "./", or "../"`. The dist bundle (built 2026-05-27) is current and correct; only the HTML's `<script src>` points to the wrong file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@extension/offscreen/lattice-host.html

<interfaces>
<!-- Existing on-disk facts the executor needs. No exploration required. -->

Current line 35 of extension/offscreen/lattice-host.html:
```html
  <script type="module" src="lattice-host.js"></script>
```

Bundled output already exists on disk:
- Path: extension/dist/offscreen/lattice-host.js
- Size: 84937 bytes
- Bare-`lattice` specifier count: 0 (esbuild rewrote them)
- Built: 2026-05-27

SW createDocument call (DO NOT MODIFY): chrome.offscreen.createDocument({url: 'offscreen/lattice-host.html', ...})
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint offscreen HTML script src to bundled dist output</name>
  <files>extension/offscreen/lattice-host.html</files>
  <action>
Edit extension/offscreen/lattice-host.html line 35.

Change exactly this line:
  `  <script type="module" src="lattice-host.js"></script>`
to:
  `  <script type="module" src="../dist/offscreen/lattice-host.js"></script>`

Constraints:
- Do NOT modify the HTML header comment block (lines 8-34). It is documentation of Phase 5 Plan 05-04 intent; leave it as historical context.
- Do NOT rebuild the bundle. extension/dist/offscreen/lattice-host.js already exists at 84937 bytes with 0 bare-`lattice` specifiers (verified pre-plan).
- Do NOT copy the HTML into extension/dist/. The dist-HTML approach was explicitly rejected in favor of this smaller, faster relative-path approach.
- Do NOT modify the SW createDocument URL (`offscreen/lattice-host.html`). Only the `<script src>` inside the HTML changes.
- Path resolution check: HTML lives at `extension/offscreen/lattice-host.html`; bundle lives at `extension/dist/offscreen/lattice-host.js`. From the HTML's directory, `../` reaches `extension/`, then `dist/offscreen/lattice-host.js` reaches the bundle. Correct relative path: `../dist/offscreen/lattice-host.js`.
  </action>
  <verify>
    <automated>test -f extension/dist/offscreen/lattice-host.js &amp;&amp; grep -q 'src="\.\./dist/offscreen/lattice-host\.js"' extension/offscreen/lattice-host.html &amp;&amp; ! grep -q 'src="lattice-host\.js"' extension/offscreen/lattice-host.html</automated>
  </verify>
  <done>
- extension/offscreen/lattice-host.html line 35 reads `  <script type="module" src="../dist/offscreen/lattice-host.js"></script>`.
- The old `src="lattice-host.js"` string no longer appears anywhere in the HTML.
- extension/dist/offscreen/lattice-host.js still exists on disk (untouched).
- No other files modified.
  </done>
</task>

</tasks>

<verification>
1. `test -f extension/dist/offscreen/lattice-host.js` exits 0 (bundle still present).
2. `grep -c 'src="\.\./dist/offscreen/lattice-host\.js"' extension/offscreen/lattice-host.html` returns 1.
3. `grep -c 'src="lattice-host\.js"' extension/offscreen/lattice-host.html` returns 0 (old path gone).
4. `git diff --stat extension/offscreen/lattice-host.html` shows 1 file changed, 1 insertion(+), 1 deletion(-).
5. `git status --porcelain extension/` shows only `M extension/offscreen/lattice-host.html` (no other extension changes).
</verification>

<success_criteria>
- HTML script src points at `../dist/offscreen/lattice-host.js` (the bundled output).
- Bundle file resolves on disk from the HTML's relative path.
- SW createDocument URL unchanged.
- No bundler rebuild executed; no new files created; no dist HTML copies introduced.
- UAT-1 sub-assertion (f) "lattice-host bare-specifier resolves at runtime" is now satisfiable on next Chrome MV3 reload (actual reload verification is the human UAT step, not part of this quick task).
</success_criteria>

<output>
After completion, no SUMMARY required (quick task). Commit message: `fix(offscreen): wire lattice-host.html to bundled dist lattice-host.js (UAT-1 sub-f)`.
</output>
