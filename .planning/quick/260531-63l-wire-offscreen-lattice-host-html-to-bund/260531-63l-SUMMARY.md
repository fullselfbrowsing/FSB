---
quick_task: 260531-63l
title: Wire offscreen lattice-host.html to bundled dist lattice-host.js
status: complete
completed: 2026-05-31
requirements:
  - UAT-1-sub-f
key_files:
  modified:
    - extension/offscreen/lattice-host.html
  untouched:
    - extension/dist/offscreen/lattice-host.js  # 84937 bytes, 0 bare-`lattice` specifiers
commits:
  - 65b00d75: fix(offscreen) wire lattice-host.html to bundled dist lattice-host.js
metrics:
  tasks: 1
  files_changed: 1
  insertions: 1
  deletions: 1
---

# Quick Task 260531-63l: Wire offscreen lattice-host.html to bundled dist Summary

One-line `<script src>` repoint in `extension/offscreen/lattice-host.html` from sibling source `lattice-host.js` (bare `lattice` specifier that Chrome ESM resolver rejects) to bundled output `../dist/offscreen/lattice-host.js` (esbuild rewrote bare specifiers inline). Unblocks UAT-1 sub-assertion (f) on next MV3 reload.

## What Changed

| File | Before (line 35) | After (line 35) |
|------|------------------|-----------------|
| `extension/offscreen/lattice-host.html` | `  <script type="module" src="lattice-host.js"></script>` | `  <script type="module" src="../dist/offscreen/lattice-host.js"></script>` |

Header comment block (lines 8-34) preserved as historical Phase 5 Plan 05-04 documentation per plan constraint.

## Verification (all gates green)

| Gate | Result |
|------|--------|
| `test -f extension/dist/offscreen/lattice-host.js` | exit 0 (bundle still present, 84937 bytes) |
| `grep -c 'src="\.\./dist/offscreen/lattice-host\.js"' extension/offscreen/lattice-host.html` | 1 |
| `grep -c 'src="lattice-host\.js"' extension/offscreen/lattice-host.html` | 0 (old path gone) |
| `git diff --stat extension/offscreen/lattice-host.html` | 1 file changed, 1 insertion(+), 1 deletion(-) |
| `git status --porcelain extension/` | only `M extension/offscreen/lattice-host.html` |
| Combined plan verify expression | exit 0 (PASS) |

## Deviations from Plan

None. Plan executed exactly as written.

## Constraints Honored

- Did NOT rebuild the bundle (dist/offscreen/lattice-host.js untouched on disk).
- Did NOT copy HTML into extension/dist/ (relative-path approach kept).
- Did NOT modify SW `chrome.offscreen.createDocument({url: 'offscreen/lattice-host.html'})` URL.
- Did NOT modify the HTML header comment block (lines 8-34 historical doc preserved).
- No other files modified inside extension/.

## Root Cause Recap

Phase 5 Plan 05-04 landed source HTML pointing at sibling source JS that uses a bare `lattice` specifier. The bundled output at `extension/dist/offscreen/lattice-host.js` had the bare specifier inlined by esbuild, but the manifest-equivalent path (the HTML `<script src>`) was never flipped to point at it. UAT-1 deferral at each phase gate (1-7) kept this latent until the first real Chrome MV3 reload session surfaced `lattice-host.html:1 Uncaught TypeError: Failed to resolve module specifier "lattice"`. This task flips the HTML script src to the bundle. The flip is one line.

## Follow-up

- UAT-1 sub-assertion (f) "lattice-host bare-specifier resolves at runtime" is now satisfiable on next Chrome MV3 reload; actual reload verification remains a milestone HUMAN-UAT step (not in this quick task's scope).

## Self-Check: PASSED

- File `extension/offscreen/lattice-host.html` exists and contains the new `src="../dist/offscreen/lattice-host.js"` (verified via grep gate above).
- Bundle `extension/dist/offscreen/lattice-host.js` exists on disk at 84937 bytes (verified pre and post edit).
- Commit `65b00d75` present in `git log --oneline` (verified via `git rev-parse --short HEAD` immediately post-commit).
