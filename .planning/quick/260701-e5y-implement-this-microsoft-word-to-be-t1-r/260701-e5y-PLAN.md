---
quick_id: 260701-e5y
status: complete
created: 2026-07-01
---

# Quick 260701-e5y Plan: Microsoft Word T1 Readiness

Implement Microsoft Word as an active T1 head for safe read rows while keeping mutation rows guarded fail-closed. Scope is limited to Word-specific handler behavior, the Word page-read bridge needed to supply Graph auth context, and tests/readiness gates that explicitly reference Word.

## Task 1. Implement the Word T1 read head

Files:
- `catalog/handlers/msword.js`
- `extension/catalog/handlers/msword.js`
- `extension/utils/capability-fetch.js`

Action:
- Add active `msword.*` read handlers that obtain Graph auth only through `ctx.executeBoundPageRead`, then issue GET-only `ctx.executeBoundSpec` calls pinned to `https://word.cloud.microsoft`.
- Add the bounded page-read `microsoft-word` auth context to the shared page-read primitive.
- Preserve all Word mutations/destructive rows as inert guarded fail-closed handlers.
- Keep the extension handler copy byte-for-byte identical to the catalog handler.

Verify:
- `node --check catalog/handlers/msword.js`
- `node --check extension/catalog/handlers/msword.js`
- `cmp catalog/handlers/msword.js extension/catalog/handlers/msword.js`

Done:
- Word read descriptors resolve through T1a handlers and mutation descriptors still never execute writes without live UAT evidence.

## Task 2. Update Word readiness and origin proof tests

Files:
- `tests/capability-head-handlers.test.js`
- `tests/head-handler-upgrade.test.js`
- `tests/t1-readiness-report.test.js`
- `tests/capability-fetch.test.js`
- `scripts/verify-origin-classification.mjs`
- `tests/verify-origin-classification.test.js`

Action:
- Change Word assertions from guarded-only to active Microsoft Graph read accommodation.
- Add Word read slugs to the resolver upgrade and readiness invariants.
- Add page-read and origin-classification checks for the Word Graph bridge.

Verify:
- `node tests/capability-head-handlers.test.js`
- `node tests/head-handler-upgrade.test.js`
- `node tests/t1-readiness-report.test.js`
- `node tests/capability-fetch.test.js`
- `node tests/verify-origin-classification.test.js`
- `node scripts/verify-origin-classification.mjs`

Done:
- Focused tests prove Word reads are active T1a, guarded writes remain inert, and the Graph read accommodation is explicit and bounded.
