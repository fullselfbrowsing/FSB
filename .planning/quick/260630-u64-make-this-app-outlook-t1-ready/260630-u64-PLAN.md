---
quick_id: 260630-u64
status: planned
created: 2026-07-01
description: Make this app Outlook T1-ready
---

# Quick Task 260630-u64: Make this app Outlook T1-ready

## Task 1: Add Outlook Graph read handler proof

Files:
- `catalog/handlers/outlook.js`
- `extension/catalog/handlers/outlook.js`
- `extension/background.js`
- `extension/utils/capability-catalog.js`
- `extension/utils/capability-fetch.js`
- `scripts/verify-origin-classification.mjs`

Action:
- Implement Outlook T1a read handlers backed by Microsoft Graph GET requests through `executeBoundSpec`.
- Add a bounded Outlook page-read `auth_context` bridge that extracts scoped Graph tokens from the active Outlook page.
- Register Outlook in service-worker startup, head seeding, and origin-classification as an explicit page-bearer Graph read accommodation.

Verify:
- Outlook read rows resolve as `t1-ready`.
- Outlook Graph calls are origin-pinned to `https://outlook.cloud.microsoft`, use `credentials:'omit'`, and do not return token material.

Done:
- Outlook read rows no longer appear as discovery-only tail rows.

## Task 2: Guard Outlook writes and update T1 gates

Files:
- `extension/utils/capability-search.js`
- `scripts/report-t1-readiness.mjs`
- `scripts/verify-t1-port-contract.mjs`
- `scripts/coverage-report.mjs`
- `extension/config/service-denylist.json`
- `tests/*`

Action:
- Register Outlook write/destructive and POST-shaped schedule rows as guarded fail-closed handlers.
- Mark Outlook read and guarded rows in search/readiness surfaces.
- Add Outlook to T1 port, coverage, denylist sensitivity, page-read, origin-classification, head-cap, upgrade, and guarded-write tests.

Verify:
- Run focused Outlook tests and the extension validation gate.

Done:
- Outlook has 12 `t1-ready` read rows and 12 `t1-guarded-fail-closed` rows.
