---
status: completed
created: 2026-07-01
task: Implement Glama to be T1 ready
---

# Quick Task: Implement Glama to be T1 ready

## Scope

Make the existing Glama catalog descriptors executable as T1a reads without broad catalog churn:

- Add a `glama.js` head handler mirrored under `catalog/handlers/` and `extension/catalog/handlers/`.
- Route all `glama.*` reads through a bounded `executeBoundPageRead` request pinned to `https://glama.ai`.
- Add the Glama page-read branch that uses Glama's first-party React Router loader data.
- Register Glama in the T1 handler manifests, readiness/search status lists, origin verifier, recipe-path guard, and port-contract mapping.
- Add focused Glama readiness tests.

## Validation

- `node --check catalog/handlers/glama.js`
- `node --check extension/catalog/handlers/glama.js`
- `node --check tests/glama-t1-ready.test.js`
- `node --check scripts/verify-origin-classification.mjs`
- `cmp -s catalog/handlers/glama.js extension/catalog/handlers/glama.js`
- `node tests/glama-t1-ready.test.js`
- Shared readiness/guard probes, with unrelated concurrent failures recorded in the summary.
