---
quick_id: 260701-2ku
status: completed
must_haves:
  truths:
    - Claude read descriptors resolve through a same-origin T1a handler on https://claude.ai.
    - Claude write/destructive descriptors remain guarded fail-closed until live mutation-body evidence exists.
    - Existing parallel app changes are not reverted or staged.
  artifacts:
    - catalog/handlers/claude.js
    - extension/catalog/handlers/claude.js
    - app-scoped readiness/evidence/test updates
  key_links:
    - scripts/report-t1-readiness.mjs
    - extension/utils/capability-catalog.js
    - extension/background.js
---

# Quick Task 260701-2ku: Implement Claude to be T1 ready using GSD quick skill

## Task 1: Add Claude T1a handler

files:
- catalog/handlers/claude.js
- extension/catalog/handlers/claude.js

action:
- Implement same-origin `/api` read handlers for Claude account, organization, conversation, model, and project reads.
- Implement guarded fail-closed entries for Claude conversation/project mutations and destructive deletes.

verify:
- Handler exports all 14 Claude slugs.
- Reads call `ctx.executeBoundSpec` with `authStrategy: same-origin-cookie`, `origin: https://claude.ai`, and no direct credential APIs.
- Guarded writes return `RECIPE_DOM_FALLBACK_PENDING` and never call `executeBoundSpec`.

done:
- Claude read rows can resolve T1a, writes remain inert.

## Task 2: Wire handler into T1 readiness surfaces

files:
- extension/background.js
- extension/utils/capability-catalog.js
- scripts/report-t1-readiness.mjs
- scripts/verify-recipe-path-guard.mjs
- catalog/write-activation-evidence.json

action:
- Add Claude to the service-worker handler load list, catalog handler manifest, readiness report handler list, guarded fail-closed slug list, and write evidence ledger.

verify:
- Readiness report shows Claude reads as `t1-ready` and Claude writes/destructive rows as `t1-guarded-fail-closed`.
- Write activation evidence has matching guarded records.

done:
- Readiness/report tooling accepts Claude T1 state.

## Task 3: Add targeted regression coverage

files:
- tests/capability-head-handlers.test.js
- tests/head-handler-upgrade.test.js
- tests/guarded-write-failclosed.test.js
- tests/t1-readiness-report.test.js
- tests/write-activation-evidence.test.js

action:
- Add Claude read/write handler assertions and update exact readiness/evidence expectations.

verify:
- Run the Claude-relevant handler tests plus T1 readiness and write evidence gates.

done:
- Tests cover the new Claude T1 readiness behavior.
