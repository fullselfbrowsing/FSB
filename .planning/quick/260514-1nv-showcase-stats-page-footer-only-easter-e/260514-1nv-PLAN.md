---
phase: quick-260514-1nv
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - showcase/angular/package.json
  - showcase/angular/src/app/app.routes.ts
  - showcase/angular/src/app/app.routes.server.ts
  - showcase/angular/src/app/core/stats/github-stats.service.ts
  - showcase/angular/src/app/core/stats/github-stats.types.ts
  - showcase/angular/src/app/pages/stats/stats-page.component.ts
  - showcase/angular/src/app/pages/stats/stats-page.component.html
  - showcase/angular/src/app/pages/stats/stats-page.component.scss
  - showcase/angular/src/app/layout/showcase-shell/showcase-shell.component.html
autonomous: true
requirements:
  - STATS-01  # /stats Easter-egg route (footer-only, hidden from crawlers/prerender)
  - STATS-02  # GitHub data service: REST fetch + ETag/If-None-Match + in-memory cache + visibility-aware 5min polling
  - STATS-03  # Stats page: cumulative stars default + 6 alternate views, SSR/hydration-safe, FSB design tokens
  - STATS-04  # Crawler exclusion: sitemap.xml, llms.txt, llms-full.txt, prerender-routes.txt, hreflang, robots meta

must_haves:
  truths:
    - "Visiting /stats in a browser renders the FSB shell + a stats page with a cumulative-stars chart by default."
    - "A 'Stats' link exists in the footer (Project column) and is the ONLY in-app discoverable path to /stats."
    - "Data refreshes every 5 minutes while the page is open and visible; refresh pauses when tab is hidden and stops on route leave / component destroy."
    - "/stats does NOT appear in sitemap.xml, llms.txt, llms-full.txt, prerender-routes.txt, hreflang alternates, nav, or mobile nav."
    - "/stats renders a non-blocking skeleton on SSR / cold load (no fetch on server); chart + GitHub fetches initialize only after hydration in the browser."
    - "User can switch between views: cumulative stars (default), weekly stars w/ %delta, issues open vs closed, forks growth, PRs opened vs merged, commits over time, maintenance signal (releases / tag cadence)."
    - "GitHub 403/rate-limit shows a friendly 'GitHub rate-limited — retrying at HH:MM' card; page does not crash."
    - "<meta name=\"robots\" content=\"noindex, nofollow\"> is set on /stats and <title> is 'FSB · Stats'."
  artifacts:
    - path: "showcase/angular/src/app/core/stats/github-stats.types.ts"
      provides: "TypeScript types for repo summary, star event, issue, PR, fork, commit, release; StatsViewId enum."
    - path: "showcase/angular/src/app/core/stats/github-stats.service.ts"
      provides: "Injectable service: per-endpoint ETag store, in-memory response cache, paginated fetch with hard caps, visibility-aware 5-min polling primitive, BehaviorSubjects per dataset, rate-limit handling."
    - path: "showcase/angular/src/app/pages/stats/stats-page.component.ts"
      provides: "Standalone Angular component for /stats: SSR-safe (browser-only fetch + Chart.js init via afterNextRender / isPlatformBrowser), 7 view toggle, meta robots=noindex, document.visibilityState integration."
      contains: "afterNextRender|isPlatformBrowser"
    - path: "showcase/angular/src/app/pages/stats/stats-page.component.html"
      provides: "View-switcher tabs, <canvas> mount point, skeleton/loading state, rate-limit error card."
    - path: "showcase/angular/src/app/pages/stats/stats-page.component.scss"
      provides: "Styles using existing CSS tokens (--bg-card, --primary, --border-color, --text-secondary, etc.). No new tokens."
    - path: "showcase/angular/src/app/app.routes.ts"
      provides: "Route registration for path 'stats' -> StatsPageComponent (lazy loadComponent)."
      contains: "path: 'stats'"
    - path: "showcase/angular/src/app/app.routes.server.ts"
      provides: "ServerRoute entry for 'stats' with RenderMode.Client (NOT prerendered)."
      contains: "path: 'stats'"
    - path: "showcase/angular/src/app/layout/showcase-shell/showcase-shell.component.html"
      provides: "Single new <a routerLink=\"/stats\"> entry inside footer-column 'Project' (NOT inside .nav-links and NOT inside .nav-mobile)."
    - path: "showcase/angular/package.json"
      provides: "chart.js dependency added (no ng2-charts wrapper) at a pinned version compatible with Angular 20 / TS 5.9."
      contains: "\"chart.js\""
  key_links:
    - from: "showcase/angular/src/app/pages/stats/stats-page.component.ts"
      to: "showcase/angular/src/app/core/stats/github-stats.service.ts"
      via: "constructor inject(GitHubStatsService) + subscribe to per-view BehaviorSubject"
      pattern: "inject\\(GitHubStatsService\\)"
    - from: "showcase/angular/src/app/core/stats/github-stats.service.ts"
      to: "api.github.com/repos/LakshmanTurlapati/FSB/*"
      via: "browser-only fetch() with If-None-Match header + ETag cache (Map<endpoint, {etag, body, fetchedAt}>)"
      pattern: "api\\.github\\.com/repos/LakshmanTurlapati/FSB"
    - from: "showcase/angular/src/app/app.routes.ts"
      to: "showcase/angular/src/app/pages/stats/stats-page.component.ts"
      via: "loadComponent dynamic import"
      pattern: "import\\('\\./pages/stats/stats-page\\.component'\\)"
    - from: "showcase/angular/src/app/layout/showcase-shell/showcase-shell.component.html"
      to: "/stats"
      via: "routerLink in footer-column.project"
      pattern: "routerLink=\"/stats\""
---

<objective>
Add a hidden /stats Easter-egg page to the FSB showcase Angular site that visualizes live GitHub stats for `LakshmanTurlapati/FSB`. The page is reachable only via a single footer link in the existing showcase shell. It must inherit the existing FSB design tokens (no new design language), refresh data every 5 minutes while open + visible, be SSR/hydration safe, and be invisible to crawlers (no sitemap / no llms.txt entry / no prerender / no hreflang fan-out / robots=noindex,nofollow).

Purpose: Give "for nerds" visitors a live signals dashboard for the repo without polluting public navigation, SEO, or the LLM ingest surface. This is an aesthetic + observability win, not a marketed feature.

Output: 1 new core service + types, 1 new lazy-loaded page component (TS+HTML+SCSS), 1 footer link, 1 router registration, 1 server-route mode pin to Client, and a `chart.js` dependency pinned in `package.json`. Zero changes to crawler-file generators, hreflang generator, or `prerender-routes.txt` (which already excludes /stats by omission — that is the invariant we preserve).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

# Existing showcase patterns the executor mirrors -- DO NOT explore the codebase beyond these references.
@showcase/angular/src/app/app.routes.ts
@showcase/angular/src/app/app.routes.server.ts
@showcase/angular/src/app/layout/showcase-shell/showcase-shell.component.html
@showcase/angular/src/app/pages/dashboard/dashboard-page.component.ts
@showcase/angular/src/app/pages/about/about-page.component.ts
@showcase/angular/src/styles.scss
@showcase/angular/prerender-routes.txt
@showcase/angular/scripts/build-crawler-files.mjs
@showcase/angular/scripts/verify-hreflang.mjs
@showcase/angular/public/sitemap.xml
@showcase/angular/public/llms.txt

<interfaces>
<!-- Extracted from existing showcase code. The executor uses these directly. -->

From showcase/angular/src/app/app.routes.ts (current shape — add 'stats' entry alongside existing routes):
- `export const routes: Routes = [ { path: '', loadComponent: ... }, ..., { path: '**', redirectTo: '' } ]`
- New entry to insert BEFORE the `path: '**'` wildcard:
  `{ path: 'stats', loadComponent: () => import('./pages/stats/stats-page.component').then(m => m.StatsPageComponent) }`

From showcase/angular/src/app/app.routes.server.ts (current shape — `/dashboard` is Client-rendered; mirror that for `/stats`):
- `export const serverRoutes: ServerRoute[] = [ { path: '', renderMode: RenderMode.Prerender }, ..., { path: 'dashboard', renderMode: RenderMode.Client }, { path: '**', renderMode: RenderMode.Client } ]`
- New entry to insert BEFORE `path: '**'`:
  `{ path: 'stats', renderMode: RenderMode.Client }`
- Rationale: prevents the prerender pass from baking a stale HTML and from running browser-only Chart.js / fetch code on the Node SSR pass. Same posture as `/dashboard`.

From showcase/angular/src/app/pages/dashboard/dashboard-page.component.ts (noindex pattern to mirror in StatsPageComponent.ngOnInit):
- `this.meta.updateTag({ name: 'robots', content: 'noindex, nofollow' });`
- Standalone component, templateUrl + styleUrl pattern, `inject(...)` for DI.

SSR-guard pattern (already established at showcase/angular/src/app/core/i18n/locale.service.ts):
- `private readonly platformId = inject(PLATFORM_ID);`
- `if (!isPlatformBrowser(this.platformId)) return;`
- Imports: `import { PLATFORM_ID, inject } from '@angular/core'; import { isPlatformBrowser } from '@angular/common';`

From showcase/angular/src/styles.scss (design tokens to reuse — do NOT invent new ones):
- Colors: `--primary` (#ff6b35), `--primary-hover`, `--primary-gradient`, `--primary-glow`
- Surfaces: `--bg-body`, `--bg-card`, `--bg-elevated`, `--bg-nav`
- Text: `--text-primary`, `--text-secondary`, `--text-muted`
- Borders: `--border-color`, `--border-hover`, `--fsb-border-subtle`, `--fsb-border-accent`
- Radii: `--radius-sm`, `--radius-md`, `--radius-lg`, `--radius-xl`
- Shadows: `--shadow-sm`, `--shadow-md`, `--shadow-lg`, `--shadow-glow`
- Motion: `--transition-fast`, `--transition-base`, `--transition-slow`
- Layout: `--nav-height`, `--max-width`, `--section-padding`
- Light mode auto-flips via `[data-theme="light"]` (already wired by ThemeService — no work needed)

From showcase/angular/src/app/layout/showcase-shell/showcase-shell.component.html (footer Project column to extend):
```
<div class="footer-column">
  <h4 i18n="@@shell.footer.col.project.title">Project</h4>
  <a href="https://github.com/lakshmanturlapati/FSB" target="_blank" rel="noopener" ...>GitHub</a>
  <a href="https://github.com/lakshmanturlapati/FSB/issues" ...>Issues</a>
  <a href="https://github.com/lakshmanturlapati/FSB/blob/main/LICENSE" ...>MIT License</a>
</div>
```
The new "Stats" link is added inside this same `<div class="footer-column">` (Project column), AFTER "MIT License". It uses `routerLink="/stats"` (internal route), NOT an external `href`. It is NOT added to `.nav-links`, NOT added to `.nav-mobile`, and NOT added to the footer Pages column.

From showcase/angular/prerender-routes.txt (invariant — file is NOT touched by this plan):
```
/
/about
/agents
/privacy
/support
```
There is no `/dashboard` entry here either; `/stats` follows the same pattern by omission. The plan MUST NOT add `/stats` to this file.

From showcase/angular/scripts/build-crawler-files.mjs (invariant — file is NOT touched):
- `const ROUTES = [ '/', '/about', '/agents', '/support', '/privacy' ]` drives `sitemap.xml` generation.
- `llms-full.txt` is copied verbatim from `scripts/llms-full.source.md`.
- `/stats` is excluded from sitemap by omission. The plan MUST NOT add `/stats` to the ROUTES array.

From showcase/angular/public/llms.txt (invariant — file is NOT touched):
- Static file; lists `/about`, `/agents`, `/support`, `/privacy`. No `/stats` reference may be added.

From showcase/angular/scripts/verify-hreflang.mjs (invariant — file is NOT touched):
- `const ROUTE_PATHS = ['', '/about', '/agents', '/privacy', '/support']` drives hreflang assertion.
- Since `/stats` is `RenderMode.Client`, no prerendered `index.html` is emitted for it under any locale subpath, so the verifier never sees it. No code change needed.

Frozen contract anchors (DO NOT modify any file that anchors these tags):
- v0.9.36, v0.9.60, v0.9.61, v0.9.62 — none of the files modified by this plan are anchor files; this is recorded for the executor's awareness.

GitHub REST API endpoints used (unauthenticated; 60 req/hr/IP):
- `GET https://api.github.com/repos/LakshmanTurlapati/FSB` -- totals (stargazers_count, forks_count, open_issues_count, subscribers_count, default_branch, pushed_at)
- `GET https://api.github.com/repos/LakshmanTurlapati/FSB/stargazers` with `Accept: application/vnd.github.v3.star+json` -- paginated, returns `[{ starred_at, user }]`; used for cumulative-stars-over-time and weekly-stars-delta
- `GET https://api.github.com/repos/LakshmanTurlapati/FSB/issues?state=all&per_page=100` -- filter out items with `pull_request` field (those are PRs, not issues); use `created_at` and `closed_at`
- `GET https://api.github.com/repos/LakshmanTurlapati/FSB/pulls?state=all&per_page=100` -- use `created_at` and `merged_at`
- `GET https://api.github.com/repos/LakshmanTurlapati/FSB/commits?per_page=100` -- use `commit.author.date`; optional `?since=ISO` window
- `GET https://api.github.com/repos/LakshmanTurlapati/FSB/forks?sort=oldest&per_page=100` -- use `created_at`
- `GET https://api.github.com/repos/LakshmanTurlapati/FSB/releases?per_page=100` -- use `published_at` for "maintenance" signal (release cadence)

Pagination cap (to stay under unauthenticated rate limits): hard-cap each paginated endpoint at 2 pages × 100 items = 200 items, OR last-6-months window via `?since=` when the endpoint supports it. Document the cap inline in `github-stats.service.ts`. Rationale: unauth rate limit is 60 req/hr/IP; 7 endpoints × ~2 pages = 14 req on initial load, leaving headroom for periodic refresh.

Rate-limit headers to read on every response (browser fetch exposes them on public CORS):
- `X-RateLimit-Remaining` -- numeric
- `X-RateLimit-Reset` -- epoch seconds; convert to local HH:MM for the error card
- HTTP 403 with `X-RateLimit-Remaining: 0` -> emit a `RateLimitedState` to subscribers; UI renders the rate-limit card; do NOT throw.

"Maintenance" signal definition (pick one and document in service):
- Time-series of releases per month over last 12 months, computed from `released[].published_at`.
- Rationale: releases / tags are the cleanest public maintenance signal and only need 1 endpoint; avoids the cost of computing closed-issue ratios across multiple paginated endpoints. If `releases` is empty, fall back to "commits per week over last 12 weeks" using the commits dataset already fetched.

7 view IDs (StatsViewId union type — used by the page component and the service):
- `'stars-cumulative'` (default)
- `'stars-weekly'`
- `'issues-open-vs-closed'`
- `'forks-growth'`
- `'prs-opened-vs-merged'`
- `'commits-over-time'`
- `'maintenance'`

Chart library choice: `chart.js` (plain, no Angular wrapper).
- Rationale: ng2-charts adds an Angular wrapper layer we do not need; chart.js alone is smaller, simpler to SSR-guard (we never import it on the server), and renders to a `<canvas>` element we can mount conditionally after `afterNextRender` / `isPlatformBrowser`. Tree-shakeable when imported via `chart.js/auto` only on the browser path (use a dynamic `await import('chart.js/auto')` inside the browser-only branch to keep it out of the SSR bundle).
- Version: pin `chart.js: "^4.4.0"` (current stable, ESM, Angular 20 + TS 5.9 compatible).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GitHub stats service + types + chart.js dep (browser-only, ETag cache, 5-min visibility-aware polling)</name>
  <files>
    showcase/angular/package.json,
    showcase/angular/src/app/core/stats/github-stats.types.ts,
    showcase/angular/src/app/core/stats/github-stats.service.ts
  </files>
  <action>
Add the `chart.js: "^4.4.0"` dependency to `showcase/angular/package.json` under `"dependencies"` (keep the existing key order; insert alphabetically after `@angular/ssr`). Do NOT add `ng2-charts`. Do NOT bump any other version. Do NOT touch `"version"` (this is a feature add on Refinements, not a release — STATE.md and PROJECT.md both pin the showcase version to 0.9.65).

Create `showcase/angular/src/app/core/stats/github-stats.types.ts`:
- Export a `StatsViewId` string-literal union with the 7 values listed in `<interfaces>`.
- Export interfaces: `RepoSummary` (id, name, stargazers_count, forks_count, open_issues_count, subscribers_count, default_branch, pushed_at).
- Export `StarEvent { starred_at: string }`.
- Export `IssueEvent { number: number; created_at: string; closed_at: string | null; pull_request?: unknown }` (the `pull_request` discriminator lets the service filter PRs out of the issues stream).
- Export `PullEvent { number: number; created_at: string; merged_at: string | null; closed_at: string | null }`.
- Export `CommitEvent { sha: string; commit: { author: { date: string } } }`.
- Export `ForkEvent { id: number; created_at: string }`.
- Export `ReleaseEvent { id: number; published_at: string; tag_name: string }`.
- Export `TimeSeriesPoint { t: string; y: number }` and `WeeklyDelta { weekStart: string; count: number; deltaPct: number | null }`.
- Export a `DatasetState<T> = { kind: 'loading' } | { kind: 'ready'; data: T; fetchedAt: number } | { kind: 'rate-limited'; resetAt: number } | { kind: 'error'; message: string }` discriminated union.

Create `showcase/angular/src/app/core/stats/github-stats.service.ts` as `@Injectable({ providedIn: 'root' })`:
- Inject `PLATFORM_ID` and gate ALL `fetch` calls behind `isPlatformBrowser(this.platformId)`. On the server, every public method short-circuits to a `loading` state and emits nothing — no network, no `setInterval`, no `document` access. This is the SSR safety contract.
- Constants: `OWNER = 'LakshmanTurlapati'`, `REPO = 'FSB'`, `API_BASE = 'https://api.github.com'`, `MAX_PAGES = 2`, `PER_PAGE = 100`, `POLL_INTERVAL_MS = 5 * 60 * 1000` (5 minutes).
- Internal ETag store: `private readonly etagCache = new Map<string, { etag: string; body: unknown; fetchedAt: number }>()`. Key by endpoint URL. On every request, set `If-None-Match: <etag>` when a cached etag exists; on `304 Not Modified`, return the cached body (do NOT call `response.json()` on a 304). On `200`, update the etag from `response.headers.get('etag')` and store the parsed body.
- Rate-limit handling: read `X-RateLimit-Remaining` and `X-RateLimit-Reset` on every response. If status is `403` AND `X-RateLimit-Remaining === '0'`, emit `{ kind: 'rate-limited', resetAt: Number(reset) * 1000 }` to every active dataset subject and STOP further requests in the current refresh cycle. Do NOT throw.
- Pagination helper: `private async fetchPaginated<T>(path, accept?)` that calls `?page=1..MAX_PAGES&per_page=100`, stops early when the response array length is `< PER_PAGE`, respects ETag per page, and concatenates results.
- Per-dataset `BehaviorSubject<DatasetState<...>>` (8 of them: `repoSummary$`, `stars$`, `weeklyStars$`, `issues$`, `forks$`, `prs$`, `commits$`, `releases$`). Each starts at `{ kind: 'loading' }` on the server AND on the browser cold start.
- `start()`: idempotent; called by the page component in `afterNextRender`. Guards: do nothing on server; do nothing if already started. Fires an immediate `refreshAll()`, then sets up a `setInterval` of `POLL_INTERVAL_MS` (store handle in `this.pollHandle`). Also installs a `document.addEventListener('visibilitychange', ...)` handler: when `document.hidden === true`, clear the interval; when it flips back to false, fire an immediate `refreshAll()` and restart the interval.
- `stop()`: clears the interval, removes the visibilitychange listener, resets `pollHandle = null`. Called by the page component's `ngOnDestroy`.
- `refreshAll()`: launches the 7 endpoint fetches in `Promise.allSettled` (NOT `Promise.all` — one failure must not cascade), each updating its own subject. Hard-cap pagination per `MAX_PAGES`.
- Time-series aggregators (pure, no I/O — easy to unit-test later): `cumulativeStarsSeries(stars: StarEvent[]): TimeSeriesPoint[]` (sort by `starred_at` ascending, emit running count, bucket by day), `weeklyStarsDelta(stars: StarEvent[]): WeeklyDelta[]` (last 12 ISO weeks, percentage vs prior week, `null` for the first week), `issuesOpenVsClosed(issues: IssueEvent[]): { opened: TimeSeriesPoint[]; closed: TimeSeriesPoint[] }` (filter out items where `pull_request !== undefined`), `forksGrowth(forks: ForkEvent[]): TimeSeriesPoint[]`, `prsOpenedVsMerged(prs: PullEvent[]): { opened: TimeSeriesPoint[]; merged: TimeSeriesPoint[] }`, `commitsOverTime(commits: CommitEvent[]): TimeSeriesPoint[]` (bucket by week, last 12 weeks), `maintenanceSignal(releases: ReleaseEvent[], commits: CommitEvent[]): TimeSeriesPoint[]` — releases per month over last 12 months; if `releases.length === 0`, fall back to commits per week over last 12 weeks.
- All aggregators must be exported (`public` methods or exported pure functions in the same file) so the page component can call them directly off the latest dataset value.
- No use of `console.log` in steady state. Use `console.warn` only for rate-limit and aggregator-input-shape mismatches.
- Per-decision documentation block at the top of the file: a comment naming the rate-limit cap (60/hr unauth), the `MAX_PAGES = 2` choice, the visibility-aware polling design, and the maintenance-signal definition (releases-per-month, commits-per-week fallback).

DO NOT create any chart code in this file. DO NOT touch `showcase/angular/scripts/build-crawler-files.mjs`, `verify-hreflang.mjs`, `prerender-routes.txt`, `public/llms.txt`, or `core/seo/locale-seo.ts` — these enforce the crawler-exclusion invariant by omission, and the plan preserves that posture (D-04).
  </action>
  <verify>
    <automated>cd showcase/angular && npm install --no-audit --no-fund && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -E "error TS|Found 0 errors" | head -20</automated>
  </verify>
  <done>
    `package.json` lists `"chart.js": "^4.4.0"` under `dependencies` and no other field has changed (especially not `"version"`). `npm install` completes cleanly and produces `node_modules/chart.js/`. `github-stats.types.ts` exports `StatsViewId`, all 7 event interfaces, `TimeSeriesPoint`, `WeeklyDelta`, and `DatasetState<T>`. `github-stats.service.ts` compiles under strict TS, gates every fetch behind `isPlatformBrowser(PLATFORM_ID)`, implements ETag caching keyed by URL with 304 short-circuit, treats `403 + X-RateLimit-Remaining: 0` as a graceful `rate-limited` state instead of throwing, caps pagination at `MAX_PAGES = 2`, exposes 8 `BehaviorSubject`s and 7 pure aggregator functions, and starts/stops a 5-minute interval that pauses on `document.hidden === true`. `tsc --noEmit` reports zero errors in the new files. No edits to `build-crawler-files.mjs`, `verify-hreflang.mjs`, `prerender-routes.txt`, `public/llms.txt`, or `locale-seo.ts`.
  </done>
</task>

<task type="auto">
  <name>Task 2: /stats page component + route registration + server-route Client mode + footer link</name>
  <files>
    showcase/angular/src/app/pages/stats/stats-page.component.ts,
    showcase/angular/src/app/pages/stats/stats-page.component.html,
    showcase/angular/src/app/pages/stats/stats-page.component.scss,
    showcase/angular/src/app/app.routes.ts,
    showcase/angular/src/app/app.routes.server.ts,
    showcase/angular/src/app/layout/showcase-shell/showcase-shell.component.html
  </files>
  <action>
Create the standalone `StatsPageComponent` at `showcase/angular/src/app/pages/stats/stats-page.component.{ts,html,scss}`:

`stats-page.component.ts`:
- `@Component({ selector: 'app-stats-page', standalone: true, templateUrl: ..., styleUrl: ..., imports: [] })`.
- Inject: `Title`, `Meta`, `PLATFORM_ID`, `GitHubStatsService`, `DOCUMENT`, and use Angular's `afterNextRender` for browser-only chart bootstrap.
- `ngOnInit` (runs on both server and client): set `<title>` to `'FSB · Stats'` and `<meta name="robots" content="noindex, nofollow">`. Do NOT call `emitLocaleHead` (we do not want hreflang fan-out for this page). Do NOT set canonical.
- `afterNextRender(() => { ... })` block (browser-only by construction): `await import('chart.js/auto')`, capture the default Chart class, call `this.statsService.start()`, subscribe to all 8 dataset BehaviorSubjects, and render the currently-selected view into a `<canvas #chartCanvas>` via `ViewChild`. Destroy and recreate the Chart instance on view switch and on dataset update.
- Component state signals (or plain fields): `selectedView: StatsViewId = 'stars-cumulative'` (the default), `viewState: 'loading' | 'ready' | 'rate-limited' | 'error'`, and a derived `rateLimitedUntil: Date | null`.
- `setView(id: StatsViewId)`: updates `selectedView`, redraws the chart from the latest dataset value, no new fetch (data is shared across views).
- `ngOnDestroy`: call `this.statsService.stop()`, destroy the chart instance, unsubscribe.
- DO NOT import `chart.js` at the top of the file. Use only a dynamic `await import('chart.js/auto')` inside `afterNextRender` so the SSR bundle never includes Chart.js.

`stats-page.component.html`:
- Wrap content in `<section class="stats-page container">`.
- Heading: `<h1>FSB · Stats <span class="muted">for nerds</span></h1>`.
- View-switcher: a row of 7 buttons (one per `StatsViewId`), each toggling `selectedView`, with the active state visually distinct. Buttons have stable `data-view` attributes for the verify step (e.g., `data-view="stars-cumulative"`).
- Chart mount: `<div class="chart-card"><canvas #chartCanvas></canvas></div>`. Skeleton shimmer block shown when `viewState === 'loading'`.
- Rate-limit card: shown when `viewState === 'rate-limited'`, copy "GitHub rate-limited — retrying at {{ rateLimitedUntil | date:'HH:mm' }}".
- Error card: shown when `viewState === 'error'` with a brief retry hint.
- Footer micro-note: "Live from the GitHub REST API. Auto-refreshes every 5 minutes."

`stats-page.component.scss`:
- Use ONLY existing tokens from `src/styles.scss` (the list is in `<interfaces>`). No new tokens, no new color literals.
- Layout: `.stats-page` uses `max-width: var(--max-width)`, padded with `var(--section-padding)`.
- `.chart-card` uses `background: var(--bg-card); border: 1px solid var(--border-color); border-radius: var(--radius-lg); padding: 1.5rem; box-shadow: var(--shadow-md);`.
- View-switcher buttons: pill-shaped, `--bg-elevated` background, `--text-secondary` text; active button uses `--primary` background and `--bg-body` text. Transitions via `--transition-base`.
- Skeleton block: 16:9 aspect, animated `linear-gradient` shimmer using `--bg-elevated` and `--border-color` stops. Respects `prefers-reduced-motion`.
- Light theme: works automatically — all tokens flip under `[data-theme="light"]` already.

Wire the route in `showcase/angular/src/app/app.routes.ts`:
- Insert this entry BEFORE the existing `{ path: '**', redirectTo: '' }` line:
  `{ path: 'stats', loadComponent: () => import('./pages/stats/stats-page.component').then(m => m.StatsPageComponent) },`

Wire the server-route mode in `showcase/angular/src/app/app.routes.server.ts`:
- Insert this entry BEFORE the existing `{ path: '**', renderMode: RenderMode.Client }` line:
  `{ path: 'stats', renderMode: RenderMode.Client },`
- This guarantees `/stats` is NEVER prerendered. No `index.html` is emitted for it under any locale subpath, which means `verify-hreflang.mjs` never sees it and the hreflang verifier remains green without any code change.

Wire the footer link in `showcase/angular/src/app/layout/showcase-shell/showcase-shell.component.html`:
- Inside the existing `<div class="footer-column">` whose `<h4>` is "Project" (currently containing GitHub / Issues / MIT License), append one new line AFTER the License link:
  `<a routerLink="/stats" i18n="@@shell.footer.project.stats">Stats</a>`
- Add the `i18n` marker so this string flows through the existing extraction pipeline; the `lint:i18n` CI gate requires every footer link to carry one.
- DO NOT add a Stats entry to `.nav-links` (desktop nav). DO NOT add it to `.nav-mobile` (mobile nav). DO NOT add it to the footer "Pages" column. The Project column is the only location.
- DO NOT add `/stats` to `prerender-routes.txt`, `scripts/build-crawler-files.mjs` (the `ROUTES` array), `public/llms.txt`, `public/llms-full.txt`, or `scripts/verify-hreflang.mjs` (the `ROUTE_PATHS` array). The crawler-exclusion invariant is preserved by omission — adding /stats to any of these files breaks the Easter-egg contract.

Note for executor: this introduces ONE new untranslated trans-unit (`@@shell.footer.project.stats`). The existing `i18nMissingTranslation: error` build gate means the 5 non-en XLIFFs must also receive a translation OR the trans-unit must be marked source-only for now. Since this is a one-word footer link and the project's pattern is to translate everything, add the English source to `src/locale/messages.xlf` via the existing `extract-i18n-clean` pipeline if the build fails. If the executor encounters an i18n build error here, the minimal fix is: run `npm run extract-i18n-clean` (or whatever the project's extract script is — visible in package.json) and let translators / the existing AI-fill pipeline handle the 5 target XLIFFs. If that pipeline is not invocable in this session, the executor should add a single `<trans-unit>` to each of `messages.es.xlf`, `messages.de.xlf`, `messages.ja.xlf`, `messages.zh-CN.xlf`, `messages.zh-TW.xlf` with the obvious local translation of "Stats" (Estadísticas / Statistiken / 統計 / 统计 / 統計). Keep this minimal — it is the only translation work in this plan.
  </action>
  <verify>
    <automated>cd showcase/angular && npm run build 2>&1 | tail -40 && echo "---" && grep -c "routerLink=\"/stats\"" src/app/layout/showcase-shell/showcase-shell.component.html && echo "---" && grep -v '^#' prerender-routes.txt | grep -c "stats" && grep -v '^//' src/app/app.routes.server.ts | grep -c "path: 'stats'.*Client" && grep -v '^//' src/app/app.routes.ts | grep -c "path: 'stats'"</automated>
  </verify>
  <done>
    `ng build` completes successfully with the existing budgets (initial < 1MB, anyComponentStyle < 64kB) and emits NO `dist/showcase-angular/browser/stats/index.html` under any locale subpath (confirmed by inspecting the prerender output directory listing). `routerLink="/stats"` appears exactly once in `showcase-shell.component.html`, inside the Project footer column, and zero times in `.nav-links` / `.nav-mobile` / Pages column. `prerender-routes.txt` (excluding comments) contains zero `stats` matches. `app.routes.server.ts` registers `{ path: 'stats', renderMode: RenderMode.Client }` exactly once. `app.routes.ts` registers `path: 'stats'` exactly once with a `loadComponent` import. Navigating to `/stats` in a browser dev server renders the FSB shell, the skeleton state, then the cumulative-stars chart once data arrives; switching views redraws the chart without re-fetching; closing the tab or hiding it via Cmd-Tab pauses the 5-min interval (verifiable in DevTools Performance / Network panel). `<meta name="robots" content="noindex, nofollow">` is present in the served HTML and `<title>` is `FSB · Stats`. `sitemap.xml`, `llms.txt`, `llms-full.txt`, and the hreflang verifier output are byte-identical to their pre-change state.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> api.github.com | Outbound public API call from the user's browser to GitHub's REST endpoint. No FSB credentials cross this boundary (unauthenticated requests). |
| GitHub response body -> Chart.js | Untrusted JSON from a third party is parsed and fed into Chart.js for rendering. |
| /stats URL -> crawler / LLM ingestion | The page must remain undiscoverable to web crawlers, sitemaps, llms.txt consumers, and search engines. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick1nv-01 | Information Disclosure | github-stats.service.ts | mitigate | Unauthenticated fetch only. No `Authorization` header is ever sent. No FSB user tokens, no session cookies, no telemetry. Only the public repo identifier `LakshmanTurlapati/FSB` is transmitted. |
| T-quick1nv-02 | Tampering | stats-page.component.ts (GitHub JSON -> Chart.js) | mitigate | Aggregator functions validate field shapes (`typeof event.starred_at === 'string'`) and skip malformed records via `console.warn`. Chart.js consumes only numeric `y` and ISO-string `t` values produced by our own aggregators — never raw GitHub fields. Defense against a hypothetically poisoned GitHub response. |
| T-quick1nv-03 | Denial of Service | github-stats.service.ts pagination + polling | mitigate | Hard cap `MAX_PAGES = 2` on every paginated endpoint; 5-minute polling interval (not aggressive); polling pauses on `document.hidden`; 403 + rate-limit-remaining=0 emits a graceful rate-limited state instead of retrying in a loop. Worst case is ~14 requests on initial load and ~14 every 5 min while the tab is foregrounded — well under the 60/hr unauth limit. |
| T-quick1nv-04 | Information Disclosure | /stats route discoverability | mitigate | Route is `RenderMode.Client` (no prerendered HTML); excluded from `sitemap.xml`, `llms.txt`, `llms-full.txt`, `prerender-routes.txt`, hreflang fan-out, and both `.nav-links` and `.nav-mobile`. The component sets `<meta name="robots" content="noindex, nofollow">` for the rare case a crawler stumbles in. Only entry point is the footer Project column. |
| T-quick1nv-05 | Tampering / Spoofing | chart.js dynamic import on browser | accept | `chart.js` is a well-known package (~10M weekly downloads). Pinned to `^4.4.0`. Subresource integrity is not applicable to a bundled-from-npm import. Risk is the same as every other npm dep already in this project. |
| T-quick1nv-06 | Repudiation | (n/a — no user actions, no writes) | accept | The page is read-only. There is no user state to repudiate. |
| T-quick1nv-07 | Elevation of Privilege | (n/a — no auth, no roles) | accept | Unauthenticated public-data view; no privilege boundary to cross. |
</threat_model>

<verification>
End-to-end phase verification (run after both tasks complete):

1. **Build green:** `cd showcase/angular && npm run build` exits 0 with no new TS errors and respects the existing budgets.
2. **Crawler-exclusion invariants byte-identical:**
   - `git diff -- showcase/angular/prerender-routes.txt` is empty.
   - `git diff -- showcase/angular/public/sitemap.xml` is empty (regenerated by prebuild but content for the route list is unchanged).
   - `git diff -- showcase/angular/public/llms.txt` is empty.
   - `git diff -- showcase/angular/public/llms-full.txt` is empty (or only the date-header line if the prebuild reran).
   - `git diff -- showcase/angular/scripts/build-crawler-files.mjs` is empty.
   - `git diff -- showcase/angular/scripts/verify-hreflang.mjs` is empty.
   - `git diff -- showcase/angular/src/app/core/seo/locale-seo.ts` is empty.
3. **Hreflang verifier green:** `cd showcase/angular && npm run verify:hreflang` exits 0, file count is still exactly 30 (6 locales × 5 routes), `/stats` is NOT present in the output.
4. **No prerendered /stats HTML:** `find dist/showcase-angular/browser -path '*stats*' -name 'index.html'` returns zero results.
5. **Footer-only entry:** `grep -c 'routerLink="/stats"' showcase/angular/src/app/layout/showcase-shell/showcase-shell.component.html` equals `1`; that one occurrence is inside the Project footer column (verify by reading surrounding lines).
6. **Manual smoke:** start `npm start`, navigate to `http://localhost:4200/stats`, confirm: cumulative-stars chart renders within ~3s, view-switcher works across all 7 views, hard-refresh shows skeleton then chart (no console errors), DevTools Network panel shows GitHub fetches stop when the tab is backgrounded and resume when foregrounded, the `<meta name="robots" content="noindex, nofollow">` tag is present in the served document, and `<title>` is `FSB · Stats`.
7. **Frozen anchors untouched:** `git diff` does not touch any file referencing the v0.9.36 / v0.9.60 / v0.9.61 / v0.9.62 contract anchors.
</verification>

<success_criteria>
- A single Easter-egg `/stats` route is live in the showcase Angular app.
- The page renders live GitHub stats for `LakshmanTurlapati/FSB` across 7 views (cumulative stars default + weekly stars / issues / forks / PRs / commits / maintenance).
- Data refreshes every 5 minutes while the tab is open AND visible; pauses on hide; stops on route leave.
- The route is invisible to crawlers and LLM ingest surfaces: not in nav, not in mobile nav, not in sitemap, not in llms.txt / llms-full.txt, not prerendered, not in hreflang fan-out, and tagged `robots: noindex, nofollow`.
- The only in-app entry point is one link in the footer Project column.
- The page uses ONLY existing FSB design tokens and inherits the existing dark/light theme switch without any new theme work.
- SSR pass renders a non-blocking skeleton; Chart.js and GitHub fetches never execute on the Node server.
- GitHub rate-limit (403 + remaining=0) renders a graceful retry-at-HH:MM card instead of crashing.
- `npm run build` and `npm run verify:hreflang` both exit 0 with no new files in the prerender output.
- No version number is bumped. No frozen contract anchor (v0.9.36 / v0.9.60 / v0.9.61 / v0.9.62) is touched.
</success_criteria>

<output>
After completion, create `.planning/quick/260514-1nv-showcase-stats-page-footer-only-easter-e/260514-1nv-SUMMARY.md` recording:
- Final file list and chart.js version installed.
- The exact line numbers / context where the footer "Stats" link was inserted.
- Confirmation that no crawler-file generators were modified (the executor must list each invariant file and state "unchanged").
- Any i18n trans-units added for `@@shell.footer.project.stats` and which target XLIFFs were updated.
- Manual smoke results (chart renders, view switching, visibility-pause behavior).
- Bundle-size delta from `chart.js` (dynamic import means it should land in a lazy chunk, NOT in the initial bundle — verify and record the actual chunk size).
</output>
