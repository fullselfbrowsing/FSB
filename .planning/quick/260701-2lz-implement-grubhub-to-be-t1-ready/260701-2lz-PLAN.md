# Quick Task 260701-2lz: Implement Grubhub to be T1 ready

**Date:** 2026-07-01
**Status:** Complete

## Scope

Own the Grubhub implementation only. Promote the existing `grubhub.*` OpenTabs descriptors from DOM/discovery backing to reviewed T1 handler rows without touching unrelated app implementations.

## Tasks

1. Add a Grubhub handler module.
   - Files: `catalog/handlers/grubhub.js`, `extension/catalog/handlers/grubhub.js`
   - Action: Implement same-origin bound-spec reads for `grubhub.list_restaurants`, `grubhub.get_restaurant`, and `grubhub.list_orders`; register `grubhub.place_order` and `grubhub.cancel_order` as guarded fail-closed handlers.
   - Verify: Handler exports exact existing descriptor slugs, pins `https://www.grubhub.com`, calls only `ctx.executeBoundSpec` for reads, and returns byte-stable fallback for guarded write/destructive rows.

2. Register Grubhub in the T1 loader and validation surfaces.
   - Files: `extension/utils/capability-catalog.js`, `scripts/report-t1-readiness.mjs`, `scripts/verify-t1-port-contract.mjs`, `scripts/verify-origin-classification.mjs`, `scripts/verify-recipe-path-guard.mjs`
   - Action: Add only the Grubhub entries needed for handler loading, readiness reporting, guarded rows, and allowlist/origin gates.
   - Verify: T1 readiness and port-contract gates resolve Grubhub rows correctly.

3. Add focused regression coverage.
   - Files: `tests/capability-head-handlers.test.js`, `tests/head-handler-upgrade.test.js`, `tests/head-handler-cap.test.js`, `tests/t1-readiness-report.test.js`
   - Action: Cover Grubhub handler behavior, exact slug upgrades, readiness rows, extension copy parity, and adjusted head count.
   - Verify: Run the narrow T1/handler tests.
