---
quick_id: 260701-2ki
slug: implement-aws-to-be-t1-ready
status: in-progress
created: 2026-07-01
---

# Quick Task 260701-2ki: Implement AWS to be T1 ready

Promote AWS Console descriptors into the reviewed T1 head without adding a
cross-origin SigV4 bridge. Console metadata reads execute through same-origin
bound specs; AWS service API reads and mutations fail closed until a future
credential bridge is explicitly designed.

## Tasks

### 1. Add AWS handler

- Files: `catalog/handlers/aws.js`, `extension/catalog/handlers/aws.js`
- Action: add a T1a handler pinned to `https://console.aws.amazon.com`.
  `aws.get_current_user` and `aws.list_regions` read console metadata through
  `ctx.executeBoundSpec`; SigV4-backed service reads return byte-stable fallback;
  Lambda invoke and EC2 start/stop remain guarded fail-closed.
- Verify: handler has no direct network, credential, storage, or extension API
  access and never references `amazonaws.com`.
- Done: all AWS descriptor slugs have reviewed T1a entries.

### 2. Wire AWS into T1 manifests

- Files: `extension/background.js`, `extension/utils/capability-catalog.js`,
  `extension/utils/capability-search.js`, `scripts/report-t1-readiness.mjs`,
  `scripts/verify-t1-port-contract.mjs`, `scripts/verify-origin-classification.mjs`,
  `scripts/verify-recipe-path-guard.mjs`, `scripts/coverage-report.mjs`
- Action: add AWS to the handler import/manifest/report/port/origin/allowlist
  surfaces, add AWS guarded write slugs, and correct EC2 start/stop side-effect
  class to write in descriptor/catalog surfaces.
- Verify: readiness rows resolve to T1-ready for reads and guarded fail-closed for
  writes, with no Pattern-D gate failures.
- Done: readiness tooling recognizes AWS as a reviewed head.

### 3. Add focused coverage and run gates

- Files: `tests/capability-head-handlers.test.js`,
  `tests/head-handler-upgrade.test.js`, `tests/head-handler-cap.test.js`,
  `tests/guarded-write-failclosed.test.js`, `tests/t1-readiness-report.test.js`
- Action: add AWS handler behavior, dom-to-T1a upgrade, guarded-write, head-cap,
  and readiness assertions.
- Verify: run targeted handler/readiness/port/origin/package gates.
- Done: tests prove AWS is origin-pinned and fail-closed where execution is not
  approved.
