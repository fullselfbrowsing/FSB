---
quick_id: 260617-g1w
description: Upgrade Lattice runtime and CLI packages to 1.4.0
status: complete
created: 2026-06-17
---

# Quick Task 260617-g1w Plan

Upgrade FSB's audited public Lattice pin from `@full-self-browsing/lattice@1.3.0` to `1.4.0`, including the developer CLI, receipt schema expectations, and the `.planning/LATTICE-PIN.md` audit trail.

## Must Haves

- `package.json` and `package-lock.json` pin `lattice` to `npm:@full-self-browsing/lattice@1.4.0`.
- `package.json` and `package-lock.json` pin `@full-self-browsing/lattice-cli` to `1.4.0`.
- `tests/helpers/lattice-public-pin.js` expects the 1.4.0 package metadata, tarball integrity, source tag commit, and receipt schema.
- Lattice smoke tests accept `lattice-receipt/v1.3` produced by 1.4.0.
- `.planning/LATTICE-PIN.md` records the active 1.4.0 pin and appends a quick-task row.
- Build and Lattice smoke verification pass under Node 24.

## Tasks

1. Update package metadata and install the 1.4.0 runtime and CLI pins.

2. Update FSB's pin guard and audit trail to match the published 1.4.0 package.

3. Run focused verification: build, public package smoke, full `test:lattice`, and any related smoke that fails due to the receipt version change.
