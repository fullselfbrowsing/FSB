# Quick Task 260701-2mc: Implement StubHub to be T1 ready

## Scope

Promote StubHub's reviewed read operations to bundled T1a handler execution while preserving the existing payment-op safety rule. `stubhub.buy_tickets` stays DOM-backed and out of the executable head because the payment-op guard rejects handler/recipe promotion for money-moving operations.

## Tasks

1. Add a StubHub same-origin read handler.
   - Files: `catalog/handlers/stubhub.js`, `extension/catalog/handlers/stubhub.js`
   - Action: Implement T1a handlers pinned to `https://www.stubhub.com` for `stubhub.search_events`, `stubhub.get_listing`, and `stubhub.list_orders`, using `executeBoundSpec` only and shape-checked JSON responses.
   - Verify: Handler exports only the read slugs, never calls direct network/browser/credential APIs, and returns typed DOM fallback on missing execution primitive or shape mismatch.

2. Register StubHub in T1 manifests and readiness surfaces.
   - Files: `extension/background.js`, `extension/utils/capability-catalog.js`, `scripts/report-t1-readiness.mjs`, `scripts/verify-t1-port-contract.mjs`, `scripts/verify-origin-classification.mjs`, `tests/head-handler-cap.test.js`
   - Action: Add `FsbHandlerStubhub` to the loader/catalog manifests, readiness handler list, port contract map, and first-party origin proof map.
   - Verify: StubHub read rows resolve as T1a handler-backed and the head-handler cap updates from 67 to 68.

3. Promote StubHub read descriptors and add tests.
   - Files: `catalog/descriptors/opentabs__stubhub__get_listing.json`, `catalog/descriptors/opentabs__stubhub__list_orders.json`, `catalog/descriptors/opentabs__stubhub__search_events.json`, `extension/catalog/recipe-index.generated.js`, `tests/capability-head-handlers.test.js`, `tests/t1-readiness-report.test.js`
   - Action: Change only the three read descriptors from `backing:"dom"` to `backing:"handler"` and cover the read handler behavior plus the buy-ticket non-promotion boundary.
   - Verify: Run focused handler/readiness/origin/port/payment gates.
