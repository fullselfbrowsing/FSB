# Quick Plan: Implement Tinder to be T1 ready

Quick ID: 260701-2o4
Date: 2026-07-01
Workspace: /Users/lakshman/conductor/workspaces/fsb/louisville

## Scope

Own only the Tinder T1-readiness slice in a shared dirty workspace. Do not revert or normalize other agents' concurrent app work.

## Plan

1. Add a bundled Tinder handler for safe GET reads only:
   - `tinder.get_current_user`
   - `tinder.get_fast_match_count`
   - `tinder.get_fast_match_preview`
   - `tinder.get_recommendations`
   - `tinder.get_user`
   - `tinder.list_matches`
2. Keep all Tinder mutation-shaped operations guarded fail-closed:
   - metadata/update POSTs
   - swipe actions
   - message actions
   - profile/location writes
   - unmatch destructive action
3. Move Tinder from hard-denied to sensitive-origin policy so reviewed reads can run while mutation posture stays conservative.
4. Wire Tinder into service-worker loading, catalog seeding, readiness/search status, port-contract mappings, recipe-path/coverage gates, origin-classification proof, tests, and write-evidence records.
5. Regenerate the extension catalog index and run the narrow checks that directly prove the Tinder slice.

## Verification Target

Tinder should show:
- 6 `t1-ready` read rows with handler proof on `https://www.tinder.com`.
- 10 `t1-guarded-fail-closed` mutation rows with handler proof on `https://www.tinder.com`.
- no direct network/storage source violations in `tinder.js`.
- no mutation execution from guarded rows.
