---
quick_id: 260701-2mh
status: planned
description: Make Uber Eats implementation T1 ready
created: 2026-07-01
---

# Quick Task 260701-2mh: Make Uber Eats Implementation T1 Ready

## Scope

Promote the existing Uber Eats descriptor surface from discovery-only DOM backing to a reviewed T1 head:

- `ubereats.list_restaurants`, `ubereats.get_menu`, and `ubereats.list_orders` resolve as T1a same-origin reads.
- `ubereats.place_order` and `ubereats.cancel_order` resolve as guarded fail-closed write/destructive handlers until live mutation-body UAT exists.
- Extension registration, readiness/search status, verifier mapping, and focused tests recognize the new handler.

## Tasks

1. Add the Uber Eats handler module.
   - Files: `catalog/handlers/ubereats.js`, `extension/catalog/handlers/ubereats.js`
   - Action: Implement explicit same-origin read specs plus guarded fail-closed mutation handlers.
   - Verify: Handler exports all five descriptor slugs, reads use `executeBoundSpec`, guarded mutations do not execute.
   - Done: Both source and extension copies match.

2. Wire the handler into T1 registration and readiness.
   - Files: `extension/background.js`, `extension/utils/capability-catalog.js`, `extension/utils/capability-search.js`, `scripts/report-t1-readiness.mjs`, `scripts/verify-t1-port-contract.mjs`, `tests/head-handler-cap.test.js`
   - Action: Add Uber Eats to the handler load list, head manifest, ready/guarded slug lists, and verifier app mapping.
   - Verify: Readiness reports Uber Eats reads as `t1-ready` and writes as `t1-guarded-fail-closed`.
   - Done: No per-app MCP tool or unrelated product surface is introduced.

3. Add focused coverage.
   - Files: `tests/capability-head-handlers.test.js`
   - Action: Add fixture data and assertions for Uber Eats reads, guarded mutations, fallback behavior, and extension copy parity.
   - Verify: Run the focused head-handler tests and readiness/port-contract gates.
   - Done: Tests pass or any unrelated pre-existing failures are documented.
