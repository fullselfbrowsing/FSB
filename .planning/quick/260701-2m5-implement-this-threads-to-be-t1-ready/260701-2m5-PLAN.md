---
status: planned
created: 2026-07-01
quick_id: 260701-2m5
slug: implement-this-threads-to-be-t1-ready
---

# Quick Task 260701-2m5: Implement Threads to be T1 ready

## Scope

Make the existing Threads descriptors resolve through reviewed T1 handler wiring where safe. Public single-thread reads can use a same-origin bound-spec head. Posting must remain guarded fail-closed until live mutation-body UAT exists. The authenticated timeline remains discovery-pending.

## Tasks

1. Add `catalog/handlers/threads.js` and the extension mirror with:
   - T1a same-origin read handler for `threads.get_thread`.
   - Guarded fail-closed inert handler for `threads.create_thread`.
   - No direct network calls, no extension API access, no cookies/storage reads, and no secret logging.

2. Wire Threads into the catalog/runtime verifier surfaces:
   - `extension/background.js`
   - `extension/utils/capability-catalog.js`
   - `extension/utils/capability-search.js`
   - `scripts/report-t1-readiness.mjs`
   - `scripts/coverage-report.mjs`
   - `scripts/verify-t1-port-contract.mjs`
   - relevant T1/head-handler tests and guarded evidence.

3. Run narrow verification:
   - Handler syntax, mirror, and behavior smoke.
   - Head-handler cap/upgrade and guarded-write checks.
   - Readiness report checks proving `threads.get_thread` is ready, `threads.create_thread` is guarded, and `threads.list_timeline` remains discovery-pending.
