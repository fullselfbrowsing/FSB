# Quick Task 260701-e5j: Implement ClickUp T1 readiness

Status: in_progress
Date: 2026-07-01

## Goal

Promote the existing ClickUp catalog rows from DOM fallback to verified T1-ready read handlers, matching the local T1 app patterns.

## Scope

- Add a ClickUp head handler for all existing `clickup.get_*` read descriptors.
- Mirror the handler into the extension bundle and register it in the head handler catalog/background loader.
- Add an origin-pinned page-bearer read bridge that uses ClickUp's captured page JWT and `cuHandshake` API base.
- Extend T1 readiness, port-contract, recipe-path, and origin-classification gates with ClickUp source proofs.
- Add focused regression coverage for the ClickUp handler, page-read bridge, and readiness report.

## Verification

- `node -c extension/utils/capability-fetch.js`
- `node tests/clickup-t1-ready.test.js`
- `node tests/head-handler-cap.test.js`
- `node scripts/verify-origin-classification.mjs`
- `node scripts/verify-t1-port-contract.mjs`
- `node scripts/verify-recipe-path-guard.mjs`
