# Quick Task 260701-e5i: Implement this Confluence to be T1 ready

Promote Confluence descriptors to the existing T1 standard without broadening mutation execution.

1. Implement the Confluence T1a handler.
   - Files: `catalog/handlers/confluence.js`, `extension/catalog/handlers/confluence.js`.
   - Action: Add same-origin tenant-aware Confluence Cloud REST reads for safe read descriptors, deriving the active `*.atlassian.net` tenant from router context and using same-origin-cookie bound specs. Add guarded fail-closed entries for write/destructive descriptors.
   - Verify: Handler unit coverage proves read specs are tenant-pinned and guarded mutations never execute.

2. Register Confluence in T1 manifests and gates.
   - Files: `extension/utils/capability-catalog.js`, `scripts/report-t1-readiness.mjs`, `scripts/verify-origin-classification.mjs`, `scripts/verify-t1-port-contract.mjs`, `catalog/write-activation-evidence.json`, `tests/head-handler-cap.test.js`, `tests/head-handler-upgrade.test.js`.
   - Action: Add the handler global/module and guarded write rows to the same surfaces used by Jira/Airtable and add an origin-classification proof for `/api/v2` + `/rest/api` relative runtime use.
   - Verify: Head count, upgrade, readiness, origin-classification, and port-contract gates include Confluence.

3. Add focused Confluence regression coverage and run narrow verification.
   - Files: `tests/confluence-t1-handler.test.js`, `package.json`.
   - Action: Register a zero-framework regression that checks handler export parity, tenant origin rejection, read path construction, fallback behavior, guarded writes, and readiness rows.
   - Verify: Run the new test plus the existing narrow T1 gates.
