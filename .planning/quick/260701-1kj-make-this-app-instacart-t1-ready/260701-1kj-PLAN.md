---
quick_id: 260701-1kj
slug: make-this-app-instacart-t1-ready
status: complete
---

# Quick Task 260701-1kj: Make this app Instacart T1-ready

## Goal

Promote the safe, technically provable Instacart same-origin read subset to T1a while leaving location/page-state, checkout navigation, write, and destructive rows in the discovery tail.

## Tasks

1. Add an Instacart T1a handler for conservative GraphQL reads.
   - Files: `catalog/handlers/instacart.js`, `extension/catalog/handlers/instacart.js`
   - Action: Implement `get_current_user`, `list_addresses`, `list_active_carts`, `get_cart`, `list_orders`, and `get_order` through `ctx.executeBoundSpec` against `https://www.instacart.com/graphql`, with origin pins, same-origin-cookie auth, persisted-query hashes, strict shape guards, no direct fetch, and byte-stable DOM fallback errors.
   - Verify: Handler unit tests prove request specs, mapped outputs, and bad-shape fallback.

2. Wire Instacart into the T1 registry and verifier.
   - Files: `extension/background.js`, `extension/utils/capability-catalog.js`, `scripts/verify-t1-port-contract.mjs`, Instacart descriptor JSON files, generated catalog artifacts.
   - Action: Load/register `FsbHandlerInstacart`, add verifier app mapping, and mark only the six promoted read descriptors as `backing:"handler"`.
   - Verify: Readiness report marks promoted rows `t1-ready`; excluded rows stay non-ready.

3. Extend focused regression coverage and GSD state.
   - Files: `tests/capability-head-handlers.test.js`, `tests/head-handler-upgrade.test.js`, `tests/t1-readiness-report.test.js`, `.planning/STATE.md`, quick-task summary.
   - Action: Add Instacart upgrade/contract/readiness assertions, update quick-task tracking, and record verification commands.
   - Verify: Run targeted T1/head-handler tests, readiness gate, port contract gate, and extension validation.
