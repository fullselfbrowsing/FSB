---
quick_id: 260701-e6c
status: complete
created: 2026-07-01
description: Implement this Twitch to be T1 ready using GSD quick skill
---

# Quick Task 260701-e6c: Implement Twitch T1 Readiness

## Goal

Make Twitch T1-ready in the repo's current catalog contract: Twitch read descriptors resolve to reviewed T1a handler-backed rows, the origin is classified as sensitive rather than policy-blocked for read execution, and the T1 readiness/search/terminal-state gates recognize those rows as executable.

## Tasks

1. Add the Twitch T1a read handler and page-read bridge.
   - Files: `catalog/handlers/twitch.js`, `extension/catalog/handlers/twitch.js`, `extension/utils/capability-fetch.js`, `extension/utils/capability-catalog.js`, `extension/background.js`
   - Action: register all 14 Twitch read descriptors as origin-pinned T1a handlers that call `executeBoundPageRead`; implement a bounded `twitch` page-read branch that reads auth only inside `https://www.twitch.tv` and calls `https://gql.twitch.tv/gql`.
   - Verify: handler unit tests cover dispatch, fail-closed behavior, source safety, and byte-for-byte extension copy.

2. Wire Twitch into readiness and policy surfaces.
   - Files: `extension/config/service-denylist.json`, `scripts/report-t1-readiness.mjs`, `scripts/verify-origin-classification.mjs`, `scripts/verify-t1-port-contract.mjs`, `scripts/coverage-report.mjs`, `scripts/verify-recipe-path-guard.mjs`, `extension/utils/capability-search.js`
   - Action: move Twitch from denied to sensitive, add head-module manifests, add origin-classification proof for the app-specific GraphQL page-bearer read base, and mark Twitch read slugs search-ready.
   - Verify: readiness report shows 14 Twitch `t1-ready` rows and no Twitch blocked rows.

3. Extend focused gates.
   - Files: `tests/capability-head-handlers.test.js`, `tests/capability-fetch.test.js`, `tests/head-handler-upgrade.test.js`, `tests/head-handler-cap.test.js`, `tests/t1-readiness-report.test.js`, `tests/t1-terminal-states.test.js`, `tests/service-denylist.test.js`, `tests/verify-origin-classification.test.js`
   - Action: add Twitch assertions for handler behavior, page-read auth confinement, dom->T1a upgrades, head cap, readiness rows, search override, sensitive classification, and origin bridge proof.
   - Verify: run focused tests plus T1 validation scripts.

## Outcome

Completed. Twitch now has reviewed T1a read handlers for all 14 Twitch read slugs, routes execution through the existing bounded Twitch page-read bridge, and is treated as sensitive rather than hard-denied so the read handlers can be T1-ready under the repo readiness contract.
