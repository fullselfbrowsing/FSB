---
quick_id: 260701-2m8
slug: make-kayak-t1-ready
status: complete
---

# Quick Task 260701-2m8: Make Kayak T1 ready

## Goal

Promote the existing Kayak OpenTabs descriptors to T1 readiness without touching unrelated app integrations.

## Tasks

1. Add a Kayak head handler.
   - Files: `catalog/handlers/kayak.js`, `extension/catalog/handlers/kayak.js`
   - Action: implement same-origin `T1a` handlers for `kayak.search_flights`, `kayak.search_hotels`, and `kayak.get_price_alert`; add an inert guarded handler for `kayak.create_price_alert`.
   - Verify: handler unit coverage records first-party `executeBoundSpec` calls for reads and no request for the guarded write.
   - Done: handler exports exact descriptor slugs and uses only `executeBoundSpec`.

2. Wire Kayak into T1 manifests and readiness status.
   - Files: `extension/utils/capability-catalog.js`, `scripts/report-t1-readiness.mjs`, `scripts/verify-t1-port-contract.mjs`, `extension/utils/capability-search.js`
   - Action: add Kayak to the head-handler module lists and mark `kayak.create_price_alert` guarded fail-closed.
   - Verify: readiness and T1 port gates include Kayak rows with correct states.
   - Done: Kayak reads report `t1-ready`; the write reports `t1-guarded-fail-closed`.

3. Update narrow tests for the new head.
   - Files: focused T1/head-handler tests only.
   - Action: add Kayak expected slugs, exact head count, origin-classification metadata, and handler parity checks.
   - Verify: run focused handler, readiness, origin, and port-contract tests.
   - Done: tests pass without changing unrelated app behavior.
