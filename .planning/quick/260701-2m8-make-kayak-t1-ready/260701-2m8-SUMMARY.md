---
quick_id: 260701-2m8
slug: make-kayak-t1-ready
status: complete
date: 2026-07-01
---

# Quick Task 260701-2m8: Make Kayak T1 ready

## Outcome

Kayak is wired as a T1 head-handler app with three safe same-origin reads and one guarded fail-closed write.

- `kayak.search_flights`: `t1-ready`, `T1a`, handler-backed read.
- `kayak.search_hotels`: `t1-ready`, `T1a`, handler-backed read.
- `kayak.get_price_alert`: `t1-ready`, `T1a`, handler-backed read.
- `kayak.create_price_alert`: `t1-guarded-fail-closed`, `T1a`, no mutation is attempted.

## Implementation Notes

- Added identical Kayak handlers under `catalog/handlers/` and `extension/catalog/handlers/`.
- Registered the handler in the extension catalog/bootstrap surfaces.
- Added Kayak readiness, origin-classification, T1 port-contract, guarded-write, and handler-parity coverage.
- Left writes inert because mutation-body UAT is not available for Kayak.

## Verification

- `node -c catalog/handlers/kayak.js`
- `node -c extension/catalog/handlers/kayak.js`
- `cmp -s catalog/handlers/kayak.js extension/catalog/handlers/kayak.js`
- Focused Kayak handler exercise for read URL binding and guarded write no-op behavior.
- Kayak readiness rows show three `t1-ready` reads and one `t1-guarded-fail-closed` write.
- Kayak-scoped T1 port-contract validation returned no failures.
- Kayak-scoped origin-classification validation returned no failures.
- `node tests/t1-readiness-report.test.js`
- `node tests/guarded-write-failclosed.test.js`
- `node tests/head-handler-upgrade.test.js`

## Known Non-Kayak Failures

- Full `node scripts/verify-t1-port-contract.mjs` currently fails on unrelated app rows including PostHog, Tinder, AWS, Linear, TikTok, and Tinder guarded-write classification.
- Full `node scripts/verify-recipe-path-guard.mjs` currently fails on unrelated handler allowlist drift; Kayak is not among the reported failures.
- Full `node tests/capability-head-handlers.test.js` currently fails on unrelated Home Depot and Sentry assertions; the Kayak assertions pass.

## Commit

No commit was created from this shared workspace because many touched shared files already contain concurrent worker edits. Committing from here would risk bundling unrelated app work.
