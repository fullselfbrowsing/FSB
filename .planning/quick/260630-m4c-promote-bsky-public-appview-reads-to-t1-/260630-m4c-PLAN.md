---
quick_id: 260630-m4c
description: Promote bsky public AppView reads to T1-ready
status: complete
created: 2026-06-30
---

# Quick Task 260630-m4c: Promote bsky public AppView reads to T1-ready

## Scope

Promote only Bluesky public AppView read endpoints that are callable without a user token through `api.bsky.app`. Keep authenticated/private reads, chat reads, writes, and destructive actions in the catalog tail until separate live mutation or auth evidence exists.

## Tasks

1. Add a `bsky.js` T1a handler pair under `catalog/handlers/` and `extension/catalog/handlers/` for the public AppView read slugs:
   - `bsky.get_author_feed`
   - `bsky.get_feed`
   - `bsky.get_followers`
   - `bsky.get_follows`
   - `bsky.get_list_feed`
   - `bsky.get_post_thread`
   - `bsky.get_posts`
   - `bsky.get_user_profile`
   - `bsky.get_user_profiles`
   - `bsky.search_posts`
   - `bsky.search_users`
   - `bsky.search_users_typeahead`

2. Wire bsky into the T1 head manifest, background imports, readiness/port tooling, origin classification, recipe-path guard, and coverage report. Add `public.api.bsky.app` to the sensitive-origin roster so bsky reads remain flagged consistently with `bsky.app`.

3. Extend focused tests:
   - handler source/behavior test coverage for bsky public AppView reads and excluded auth/private/write rows
   - dom-to-T1a upgrade assertions for bsky read slugs
   - origin-classification and denylist tests for the documented public AppView base
   - importScripts count smoke updates

## Verification

Run the focused T1 gate stack:

- `node tests/capability-head-handlers.test.js`
- `node tests/capability-fetch.test.js`
- `node tests/head-handler-upgrade.test.js`
- `node tests/verify-origin-classification.test.js`
- `node tests/service-denylist.test.js`
- `node tests/lattice-provider-bridge-smoke.test.js`
- `node scripts/verify-origin-classification.mjs`
- `node scripts/verify-recipe-path-guard.mjs`
- `node scripts/verify-t1-readiness-gate.mjs`
- `node scripts/verify-t1-port-contract.mjs`
