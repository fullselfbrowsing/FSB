---
quick_id: 260701-2md
slug: make-mastodon-t1-ready
status: implemented
created: 2026-07-01
---

# Quick Task 260701-2md: Make Mastodon T1 Ready

## Scope

Promote the Mastodon catalog rows to a T1-ready terminal state without changing descriptor data:

- `mastodon.get_status` and `mastodon.list_timeline` become same-origin T1a read handlers.
- `mastodon.create_status` and `mastodon.delete_status` become T1a guarded fail-closed handlers.

## Plan

1. Add byte-identical Mastodon handler modules under `catalog/handlers/` and `extension/catalog/handlers/`.
2. Wire the handler into service-worker loading, head seeding, readiness reporting, search status, port-contract mapping, origin classification, coverage reporting, guarded-write tests, and write evidence.
3. Verify Mastodon readiness, fail-closed writes, same-origin classification, and port-contract behavior with focused checks.
