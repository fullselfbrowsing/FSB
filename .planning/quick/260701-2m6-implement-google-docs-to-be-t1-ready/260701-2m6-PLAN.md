---
quick_id: 260701-2m6
slug: implement-google-docs-to-be-t1-ready
description: Implement Google Docs to be T1 ready
status: in_progress
created: 2026-07-01
---

# Quick Task 260701-2m6: Implement Google Docs to be T1 ready

## Task 1: Add Google Docs T1a handler

Files:
- `catalog/handlers/gdocs.js`
- `extension/catalog/handlers/gdocs.js`

Action:
- Add a `gdocs` bundled head that registers existing `gdocs.*` descriptor slugs.
- Execute safe Drive/document reads through `executeBoundSpec` pinned to `https://docs.google.com`.
- Keep write/destructive document/comment operations guarded fail-closed until live mutation-body UAT exists.

Verify:
- Focused handler tests cover read spec construction, shape mapping, fail-closed guards, and copied extension handler parity.

Done:
- Handler exists in both catalog and extension copies.

## Task 2: Wire T1 readiness surfaces

Files:
- `extension/background.js`
- `extension/utils/capability-catalog.js`
- `scripts/report-t1-readiness.mjs`
- `scripts/verify-t1-port-contract.mjs`

Action:
- Add the `gdocs` handler to the service-worker loader, catalog head manifest, readiness handler module list, guarded fail-closed list, and port-contract app mapping.

Verify:
- T1 readiness, port-contract, and guarded-write gates include Google Docs rows correctly.

Done:
- `gdocs` read rows resolve T1a/t1-ready; write/destructive rows resolve T1a but report guarded fail-closed.

## Task 3: Add narrow tests and run gates

Files:
- `tests/capability-head-handlers.test.js`
- `tests/head-handler-upgrade.test.js`
- `tests/guarded-write-failclosed.test.js`

Action:
- Add Google Docs focused assertions to the existing head-handler, upgrade, and guarded-write suites.

Verify:
- Run the narrowest relevant tests and validators for Google Docs T1 readiness.

Done:
- Tests pass without touching unrelated catalog-tail changes.
