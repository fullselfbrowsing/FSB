---
quick_id: 260701-2kj
status: complete
created: 2026-07-01
---

# Quick Task 260701-2kj: Implement Azure to be T1 ready

## Goal

Promote Azure descriptors from Pattern-D discovery candidates to T1 readiness using the existing head-handler and guarded-write patterns.

## Tasks

1. Add Azure T1 handler coverage.
   - Files: `catalog/handlers/azure.js`, `extension/catalog/handlers/azure.js`, `extension/utils/capability-fetch.js`
   - Action: Add read handlers through the bounded Azure page-read primitive and guarded fail-closed handlers for write/destructive slugs.
   - Verify: Azure read slugs resolve as T1a handlers; guarded writes return `RECIPE_DOM_FALLBACK_PENDING` without execution.

2. Wire Azure into T1 gate manifests.
   - Files: readiness/port/origin/handler manifests and write activation evidence.
   - Action: Add Azure to handler loader maps, service-worker import, origin-classification accommodation, guarded slug/evidence lists, and locked-count tests.
   - Verify: T1 readiness passes; Azure-specific origin, port contract, and write evidence checks pass. Full branch gates remain red on non-Azure parallel work.

3. Add focused Azure validation.
   - Files: `tests/azure-head-handler.test.js`
   - Action: Cover Azure handler registration, read page-read dispatch, missing primitive fail-closed, and guarded mutation inertness.
   - Verify: Run targeted Azure test plus repo T1 gates.
