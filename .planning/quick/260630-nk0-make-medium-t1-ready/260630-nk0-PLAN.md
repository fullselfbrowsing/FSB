---
quick_id: 260630-nk0
slug: make-medium-t1-ready
status: completed
created: 2026-06-30
completed: 2026-06-30
---

# Quick Task 260630-nk0: Make Medium T1-ready

Promote Medium from discovery-only catalog coverage to the current T1 contract:
safe same-origin GraphQL reads execute through a bundled handler, while mutations
remain guarded fail-closed pending live mutation-body UAT.

## Tasks

### 1. Add Medium handler

- Files: `catalog/handlers/medium.js`, `extension/catalog/handlers/medium.js`
- Action: implement Medium same-origin GraphQL read handlers for the 15 read descriptors and guarded fail-closed entries for the 5 write descriptors.
- Verify: handlers are pinned to `https://medium.com`, call only `ctx.executeBoundSpec`, use `same-origin-cookie`, and return byte-stable fallback on shape mismatch or guarded writes.
- Done: Medium read and write descriptor slugs export T1a entries.

### 2. Wire Medium into T1 registries

- Files: `extension/background.js`, `extension/utils/capability-catalog.js`, `scripts/report-t1-readiness.mjs`, `scripts/verify-t1-port-contract.mjs`, `scripts/coverage-report.mjs`
- Action: add `FsbHandlerMedium` to the service-worker import list, catalog head manifest, readiness resolver module list, guarded-write list, and port-contract handler map.
- Verify: Medium rows resolve as `t1-ready` for reads and `t1-guarded-fail-closed` for writes.
- Done: readiness and port-contract tooling recognizes Medium as a reviewed head.

### 3. Add focused regression coverage

- Files: `tests/capability-head-handlers.test.js`, `tests/head-handler-upgrade.test.js`, `tests/head-handler-cap.test.js`, `tests/guarded-write-failclosed.test.js`, `tests/t1-readiness-report.test.js`, `tests/lattice-provider-bridge-smoke.test.js`
- Action: add Medium fixture responses, handler shape/spec tests, upgrade rows, guarded-write rows, readiness assertions, and head/import count updates.
- Verify: run targeted handler/readiness/port/origin/package gates.
- Done: tests prove Medium is executable for reads and inert for writes.
