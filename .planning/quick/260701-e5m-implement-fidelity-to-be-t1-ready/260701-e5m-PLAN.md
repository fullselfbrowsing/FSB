---
quick_id: 260701-e5m
slug: implement-fidelity-to-be-t1-ready
status: complete
created: 2026-07-01
---

# Quick Task 260701-e5m: Implement Fidelity to be T1 ready

## Goal

Promote Fidelity from descriptor-only T3 discovery rows to an explicit T1a handler surface while preserving the hard denylist for `https://digital.fidelity.com`.

## Tasks

1. Add an inert Fidelity handler
   - Files: `catalog/handlers/fidelity.js`, `extension/catalog/handlers/fidelity.js`
   - Action: Export all 13 Fidelity descriptor slugs as `T1a` read handlers with descriptor-matching param schemas. Each handler returns the standard `RECIPE_DOM_FALLBACK_PENDING` fallback and never calls execution primitives.
   - Verify: Handler exports all Fidelity descriptor slugs, source safety scan passes, and invoking handlers records zero bound execution calls.

2. Wire Fidelity into T1 resolver manifests
   - Files: `extension/background.js`, `extension/utils/capability-catalog.js`, `scripts/report-t1-readiness.mjs`, `scripts/verify-t1-port-contract.mjs`
   - Action: Add Fidelity to handler imports, head-handler manifest, readiness report module list, and port-contract handler mapping.
   - Verify: Readiness rows for Fidelity resolve as `T1a` with handler proof while remaining `blocked` due the denylist.

3. Add focused regression coverage
   - Files: `tests/fidelity-t1-ready.test.js`
   - Action: Cover mirror equality, descriptor coverage, fallback shape, no execution calls, resolver upgrade, and readiness report state.
   - Verify: Run the new test and the narrow T1 readiness/report checks that can validate this path.
