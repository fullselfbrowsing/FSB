---
quick_id: 260701-2lm
slug: make-doordash-t1-ready
status: complete
---

# Quick Task 260701-2lm: Make DoorDash T1-ready

## Goal

Promote the safe, technically provable DoorDash same-origin GraphQL read subset to T1a while leaving favorite/profile/default-address/notification mutations in the discovery tail.

## Tasks

1. Add a DoorDash T1a handler for conservative GraphQL reads.
   - Files: `catalog/handlers/doordash.js`, `extension/catalog/handlers/doordash.js`
   - Action: Implement `get_current_user`, `list_addresses`, `list_orders`, `get_order`, `list_payment_methods`, and `get_notifications` through `ctx.executeBoundSpec` against `https://www.doordash.com/graphql/<operationName>`, with same-origin-cookie auth, CSRF source metadata, origin pins, strict response-shape guards, and typed DOM fallback errors.
   - Verify: Handler unit tests prove request specs, mapped outputs, and bad-shape fallback.

2. Wire DoorDash into the T1 registry and readiness pipeline.
   - Files: `extension/background.js`, `extension/utils/capability-catalog.js`, `scripts/verify-t1-port-contract.mjs`, `scripts/report-t1-readiness.mjs`, `scripts/verify-recipe-path-guard.mjs`, DoorDash descriptor JSON files, generated catalog artifacts.
   - Action: Load/register `FsbHandlerDoordash`, add verifier/report mappings, allowlist the handler, and mark only the six promoted read descriptors as `backing: "handler"`.
   - Verify: Readiness report marks promoted rows `t1-ready`; mutation rows stay non-ready.

3. Extend focused regression coverage and GSD state.
   - Files: `tests/capability-head-handlers.test.js`, `tests/head-handler-upgrade.test.js`, `tests/head-handler-cap.test.js`, `tests/t1-readiness-report.test.js`, `tests/t1-terminal-states.test.js`, `.planning/STATE.md`, quick-task summary.
   - Action: Add DoorDash upgrade/contract/readiness/search override assertions and record verification commands.
   - Verify: Run targeted handler, upgrade, readiness, terminal-state, port-contract, recipe-path, and generated catalog checks.
