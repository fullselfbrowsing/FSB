---
phase: quick-260531-5tw
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - extension/README.md
  - extension/manifest.json
  - package.json
  - package-lock.json
  - mcp/README.md
  - showcase/angular/package.json
  - showcase/angular/package-lock.json
  - showcase/angular/src/app/core/seo/version.ts
  - store-assets/chrome-web-store/listing-copy.md
autonomous: true
requirements:
  - QUICK-260531-5tw
must_haves:
  truths:
    - "All 16 occurrences of 0.9.67 are updated to 0.9.90 across the 10 listed files"
    - "No occurrence of 0.9.67 remains in the repo outside excluded files (mcp/package.json, showcase/server/package.json)"
    - "mcp/package.json (0.9.2) is unchanged — MCP has independent versioning"
    - "showcase/server/package.json (0.9.50) is unchanged — server has independent versioning"
    - "showcase/angular/public/llms-full.txt and sitemap.xml are not modified by this plan (crawler artifacts managed separately by user)"
    - "extension/manifest.json reports version 0.9.90 and is valid JSON"
    - "package.json / package-lock.json / showcase/angular/package.json / showcase/angular/package-lock.json are valid JSON with version 0.9.90"
    - "showcase/angular/src/app/core/seo/version.ts exports APP_VERSION = '0.9.90' (updated by hand per prior PR #56 lesson)"
  artifacts:
    - path: "extension/manifest.json"
      provides: "Chrome extension manifest at version 0.9.90"
      contains: "\"version\": \"0.9.90\""
    - path: "package.json"
      provides: "Root npm package at version 0.9.90"
      contains: "\"version\": \"0.9.90\""
    - path: "package-lock.json"
      provides: "Lockfile root self-reference at 0.9.90"
      contains: "0.9.90"
    - path: "showcase/angular/package.json"
      provides: "Angular showcase package at 0.9.90"
      contains: "\"version\": \"0.9.90\""
    - path: "showcase/angular/package-lock.json"
      provides: "Angular showcase lockfile self-reference at 0.9.90"
      contains: "0.9.90"
    - path: "showcase/angular/src/app/core/seo/version.ts"
      provides: "APP_VERSION constant at 0.9.90 for SEO/footer/live-site display"
      contains: "0.9.90"
    - path: "README.md"
      provides: "Top-level README reflecting 0.9.90 (title, badge URL, prose)"
      contains: "0.9.90"
    - path: "extension/README.md"
      provides: "Extension README reflecting 0.9.90"
      contains: "0.9.90"
    - path: "mcp/README.md"
      provides: "MCP README's contextual mention of the extension version updated to 0.9.90"
      contains: "0.9.90"
    - path: "store-assets/chrome-web-store/listing-copy.md"
      provides: "Chrome Web Store listing copy at 0.9.90"
      contains: "0.9.90"
  key_links:
    - from: "extension/manifest.json"
      to: "showcase/angular/src/app/core/seo/version.ts"
      via: "manual sync (generator script not run — see Phase 7 lesson, PR #56)"
      pattern: "0\\.9\\.90"
    - from: "package.json"
      to: "package-lock.json"
      via: "root self-reference fields (lines 3 and 9)"
      pattern: "0\\.9\\.90"
    - from: "showcase/angular/package.json"
      to: "showcase/angular/package-lock.json"
      via: "root self-reference fields (lines 3 and 9)"
      pattern: "0\\.9\\.90"
---

<objective>
Update the extension version string from 0.9.67 to 0.9.90 across all 16 known occurrences in 10 files.

Purpose: Bring all version references in lockstep with the upcoming 0.9.90 release. Per user memory ("Version bump 0.9.66 missed APP_VERSION"), the Angular SEO version.ts file MUST be updated by hand so the live homepage does not display a stale version post-deploy.

Output: Repo state where `grep -rn "0\.9\.67" .` returns zero hits outside the explicitly excluded files (mcp/package.json at 0.9.2, showcase/server/package.json at 0.9.50) and crawler artifacts the user manages separately.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md

# Exclusions (DO NOT modify)
# - mcp/package.json (currently 0.9.2 — independent MCP versioning per mcp/README.md line 534)
# - showcase/server/package.json (currently 0.9.50 — independent server versioning)
# - showcase/angular/public/llms-full.txt and showcase/angular/public/sitemap.xml (crawler output, user manages separately; already in modified state in git)

# Prior-art lesson (from MEMORY.md):
# PR #56 (0.9.65 -> 0.9.66) forgot to update showcase/angular/src/app/core/seo/version.ts,
# so the live homepage continued displaying the old version after deploy even though the
# file header says "Generated by scripts/build-crawler-files.mjs from manifest.json -- do not edit by hand".
# Per user directive, this plan updates version.ts BY HAND alongside manifest.json.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update extension + root package version strings (extension manifest, root package.json, package-lock.json, extension/README.md)</name>
  <files>
    extension/manifest.json,
    extension/README.md,
    package.json,
    package-lock.json
  </files>
  <action>
Replace every literal occurrence of `0.9.67` with `0.9.90` in the following files using the Edit tool (one Edit call per occurrence, preserving surrounding text exactly):

1. `extension/manifest.json`
   - Line 3 ("name" field includes the version) — update the version segment inside the name string from `0.9.67` to `0.9.90`.
   - Line 4 (`"version": "0.9.67"`) — change to `"version": "0.9.90"`.
   - After both edits, file must remain valid JSON (no trailing commas introduced, quotes balanced).

2. `extension/README.md`
   - Line 3 — single occurrence of `0.9.67` -> `0.9.90`.

3. `package.json`
   - Line 3 (`"version": "0.9.67"`) — change to `"version": "0.9.90"`.
   - Line 120 (badge URL containing `0.9.67`) — change to `0.9.90`.
   - File must remain valid JSON.

4. `package-lock.json`
   - Line 3 (`"version": "0.9.67"`) — change to `"version": "0.9.90"`.
   - Line 9 (root package self-reference `"version": "0.9.67"`) — change to `"version": "0.9.90"`.
   - File must remain valid JSON.
   - Do NOT run `npm install` to regenerate the lockfile — the only fields changing are the root package self-references, which is a safe in-place edit. Regenerating the lockfile could pull in unrelated dependency drift.

After these edits there should be ZERO occurrences of `0.9.67` in these four files. Do NOT touch any other file in this task.
  </action>
  <verify>
    <automated>grep -n "0\.9\.67" extension/manifest.json extension/README.md package.json package-lock.json; test $? -eq 1 &amp;&amp; node -e "JSON.parse(require('fs').readFileSync('extension/manifest.json','utf8')); JSON.parse(require('fs').readFileSync('package.json','utf8')); JSON.parse(require('fs').readFileSync('package-lock.json','utf8')); console.log('JSON OK')" &amp;&amp; grep -c "\"version\": \"0.9.90\"" extension/manifest.json package.json package-lock.json</automated>
  </verify>
  <done>
- `grep -n "0\.9\.67"` across the four files returns nothing (exit 1).
- All three JSON files parse cleanly.
- `extension/manifest.json`, `package.json`, and `package-lock.json` each contain at least one `"version": "0.9.90"` line.
- `extension/README.md` contains `0.9.90` on line 3.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Angular showcase version strings + APP_VERSION constant (showcase/angular package files, version.ts)</name>
  <files>
    showcase/angular/package.json,
    showcase/angular/package-lock.json,
    showcase/angular/src/app/core/seo/version.ts
  </files>
  <action>
Replace `0.9.67` with `0.9.90` in the Angular showcase tree.

1. `showcase/angular/package.json`
   - Line 3 (`"version": "0.9.67"`) — change to `"version": "0.9.90"`.
   - File must remain valid JSON.

2. `showcase/angular/package-lock.json`
   - Line 3 (`"version": "0.9.67"`) — change to `"version": "0.9.90"`.
   - Line 9 (root package self-reference) — change to `"version": "0.9.90"`.
   - Do NOT run `npm install`. In-place edit of root self-references only.

3. `showcase/angular/src/app/core/seo/version.ts`
   - Update the `APP_VERSION` constant from `'0.9.67'` to `'0.9.90'`.
   - IMPORTANT: the file header comment states "Generated by scripts/build-crawler-files.mjs from manifest.json — do not edit by hand". Per the user's MEMORY.md ("Version bump 0.9.66 missed APP_VERSION"), we are intentionally editing this by hand so the live homepage does not display a stale version post-deploy. Do NOT run the generator script (it would also touch crawler artifacts the user manages separately).
   - Leave the header comment text unchanged — only the version literal changes.

After these edits there should be ZERO occurrences of `0.9.67` in these three files.
  </action>
  <verify>
    <automated>grep -n "0\.9\.67" showcase/angular/package.json showcase/angular/package-lock.json showcase/angular/src/app/core/seo/version.ts; test $? -eq 1 &amp;&amp; node -e "JSON.parse(require('fs').readFileSync('showcase/angular/package.json','utf8')); JSON.parse(require('fs').readFileSync('showcase/angular/package-lock.json','utf8')); console.log('JSON OK')" &amp;&amp; grep -c "0.9.90" showcase/angular/src/app/core/seo/version.ts</automated>
  </verify>
  <done>
- `grep -n "0\.9\.67"` across the three files returns nothing (exit 1).
- Both Angular JSON files parse cleanly.
- `showcase/angular/src/app/core/seo/version.ts` contains `0.9.90` (at least once, in the APP_VERSION constant).
  </done>
</task>

<task type="auto">
  <name>Task 3: Update remaining docs/copy references and run final repo-wide guard (README.md, mcp/README.md, store-assets listing-copy, repo-wide grep guard)</name>
  <files>
    README.md,
    mcp/README.md,
    store-assets/chrome-web-store/listing-copy.md
  </files>
  <action>
Update the remaining documentation and store-asset references, then run a repo-wide guard to confirm no `0.9.67` strings linger outside the explicitly excluded files.

1. `README.md` (3 occurrences)
   - Line 1: H1 title contains `0.9.67` — update to `0.9.90`.
   - Line 12: badge URL contains `0.9.67` — update to `0.9.90`.
   - Line 35: prose mention of `0.9.67` — update to `0.9.90`.

2. `extension/README.md` — already handled in Task 1; do NOT re-edit here.

3. `mcp/README.md`
   - Line 534: contextual mention of the extension version `0.9.67` — update to `0.9.90`.
   - This line refers to the FSB extension version, NOT the MCP package version. The MCP package itself (mcp/package.json at 0.9.2) is OUT OF SCOPE and MUST NOT be touched.

4. `store-assets/chrome-web-store/listing-copy.md`
   - Line 5: update `0.9.67` -> `0.9.90`.

5. Repo-wide guard (final sweep):
   - Run a grep across the repo and confirm the only remaining `0.9.67` hits are zero, AND the excluded files still hold their independent versions:
     - `mcp/package.json` still has `"version": "0.9.2"`
     - `showcase/server/package.json` still has `"version": "0.9.50"`
   - Do NOT modify `showcase/angular/public/llms-full.txt` or `showcase/angular/public/sitemap.xml` even if they contain stale version strings — those are crawler artifacts the user regenerates separately.
  </action>
  <verify>
    <automated>grep -rn "0\.9\.67" --include='*.md' --include='*.json' --include='*.ts' --include='*.js' --exclude-dir=node_modules --exclude-dir=lattice --exclude-dir=.planning .; test $? -eq 1 &amp;&amp; grep -c "\"version\": \"0.9.2\"" mcp/package.json &amp;&amp; grep -c "\"version\": \"0.9.50\"" showcase/server/package.json &amp;&amp; grep -c "0.9.90" README.md mcp/README.md store-assets/chrome-web-store/listing-copy.md</automated>
  </verify>
  <done>
- Repo-wide grep for `0\.9\.67` (excluding node_modules, lattice, .planning) returns ZERO hits (exit 1).
- `mcp/package.json` still pins `"version": "0.9.2"` (independent versioning preserved).
- `showcase/server/package.json` still pins `"version": "0.9.50"` (independent versioning preserved).
- `README.md` contains `0.9.90` at least three times (title, badge URL, prose).
- `mcp/README.md` contains `0.9.90` (on line 534, replacing the prior contextual mention).
- `store-assets/chrome-web-store/listing-copy.md` contains `0.9.90`.
- No modifications to `showcase/angular/public/llms-full.txt` or `showcase/angular/public/sitemap.xml` introduced by this plan (their existing git-status-modified state is unrelated crawler output).
  </done>
</task>

</tasks>

<verification>
Post-plan repo-wide checks:

1. Zero `0.9.67` strings remain in tracked code/docs:
   ```bash
   grep -rn "0\.9\.67" --include='*.md' --include='*.json' --include='*.ts' --include='*.js' \
     --exclude-dir=node_modules --exclude-dir=lattice --exclude-dir=.planning .
   ```
   Expected: no output, exit code 1.

2. Independent-versioning files are untouched:
   ```bash
   grep "\"version\":" mcp/package.json showcase/server/package.json
   ```
   Expected: `mcp/package.json` shows `0.9.2`; `showcase/server/package.json` shows `0.9.50`.

3. New version is present in all expected places:
   ```bash
   grep -rn "0\.9\.90" --include='*.md' --include='*.json' --include='*.ts' \
     --exclude-dir=node_modules --exclude-dir=lattice --exclude-dir=.planning .
   ```
   Expected: hits in all 10 files listed in `files_modified`.

4. JSON validity check:
   ```bash
   for f in extension/manifest.json package.json package-lock.json showcase/angular/package.json showcase/angular/package-lock.json; do
     node -e "JSON.parse(require('fs').readFileSync('$f','utf8'))" || echo "INVALID: $f"
   done
   ```
   Expected: no `INVALID:` lines.

5. Crawler artifacts confirmation (sanity, not a gate):
   ```bash
   git status --porcelain showcase/angular/public/llms-full.txt showcase/angular/public/sitemap.xml
   ```
   Expected: same modified-state as before the plan ran (this plan does not touch them).
</verification>

<success_criteria>
- All 16 occurrences of `0.9.67` updated to `0.9.90` across the 10 listed files.
- Repo-wide grep for `0.9.67` (excluding node_modules, lattice, .planning) returns zero hits.
- `mcp/package.json` remains at `0.9.2`; `showcase/server/package.json` remains at `0.9.50`.
- All edited JSON files parse cleanly.
- `showcase/angular/src/app/core/seo/version.ts` reflects `0.9.90` so the live homepage will not show a stale version after deploy.
- Crawler artifacts (`showcase/angular/public/llms-full.txt`, `sitemap.xml`) are not modified by this plan.
</success_criteria>

<output>
After completion, create `.planning/quick/260531-5tw-update-extension-version-to-0-9-90-acros/260531-5tw-SUMMARY.md` summarizing:
- The 16 replacements performed (file:line:before -> after table)
- Confirmation that excluded files were not touched
- Output of the final repo-wide `grep -rn "0\.9\.67"` guard
</output>
