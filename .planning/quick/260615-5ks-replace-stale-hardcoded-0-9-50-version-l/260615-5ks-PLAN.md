---
phase: quick-260615-5ks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extension/background.js
  - extension/content/lifecycle.js
  - extension/ui/sidepanel.js
  - extension/ui/popup.js
  - extension/ai/agent-loop.js
  - extension/ai/ai-providers.js
  - extension/ai/universal-provider.js
  - extension/config/config.js
  - extension/ui/options.js
  - extension/utils/action-verification.js
  - extension/utils/automation-logger.js
autonomous: true
requirements: [QT-260615-5ks]
must_haves:
  truths:
    - "Extension runtime logs report the version that matches manifest.json (currently 0.9.90), not the stale 0.9.50 literal"
    - "Runtime version emissions auto-track future manifest bumps without further code edits"
    - "Cosmetic file-header comments show the current 0.9.90 version"
    - "The only remaining 0.9.50 reference under extension/ is the intentional historical note in overlay-state.js"
  artifacts:
    - path: "extension/background.js"
      provides: "onInstalled logInit emits dynamic manifest version"
      contains: "chrome.runtime.getManifest().version"
    - path: "extension/content/lifecycle.js"
      provides: "content_script loaded logInit emits dynamic manifest version with guard"
      contains: "getManifest"
    - path: "extension/ui/sidepanel.js"
      provides: "side panel load logs built from dynamic manifest version"
      contains: "getManifest"
    - path: "extension/ui/popup.js"
      provides: "chat interface load log built from dynamic manifest version"
      contains: "getManifest"
  key_links:
    - from: "extension runtime log emissions"
      to: "extension/manifest.json version field"
      via: "chrome.runtime.getManifest().version"
      pattern: "getManifest\\(\\)\\.version"
---

<objective>
Replace stale hardcoded `0.9.50` / `v0.9.50` version literals in the FSB extension. The 0.9.90 version bump (quick task 260531-5tw) updated canonical sources (manifest.json, package.json, README, showcase APP_VERSION) but missed these hardcoded log/comment literals, so runtime logs incorrectly report `version: 0.9.50`.

Two classes of fix:
- **Group A (runtime emissions):** Make version dynamic by reading `chrome.runtime.getManifest().version` (following the established pattern at `extension/ws/ws-client.js:898`). Do NOT hardcode `0.9.90` here — the manifest is the single source of truth and future bumps must auto-track.
- **Group B (header comments):** Static doc comments — just bump the literal to `0.9.90`.

Purpose: FSB runtime telemetry/logs report the correct, current version, and runtime version reporting self-maintains across future version bumps.
Output: 11 modified extension files; `git grep "0\.9\.50" -- extension/` returns only the one intentional historical note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# Canonical version source (single source of truth — DO NOT edit, only read):
# extension/manifest.json -> "version": "0.9.90"

<interfaces>
<!-- Established codebase pattern the executor must follow for Group A. -->
<!-- Use these directly — no further codebase exploration needed. -->

CANONICAL DYNAMIC-VERSION PATTERN (extension/ws/ws-client.js:898, service-worker context):
```javascript
version: chrome.runtime.getManifest().version,
```

logInit signature (extension/utils/automation-logger.js:602):
```javascript
logInit(component, status, details = {}) { /* ... */ }
```
The version is just a field inside the third `details` object argument.

CONTENT-SCRIPT chrome.runtime GUARD IDIOM (extension/content/lifecycle.js:535):
```javascript
if (!chrome.runtime?.id) { /* context invalidated */ }
```
lifecycle.js already accesses chrome.runtime via optional chaining when guarding.
Mirror that idiom for the getManifest call in the content script (Task 1, site 2).

CONTEXT NOTE on chrome.runtime.getManifest() availability:
- background.js site is inside `chrome.runtime.onInstalled.addListener` (full SW context) — no guard needed.
- sidepanel.js / popup.js sites run in extension UI pages (extension origin) — chrome.runtime is
  synchronously available there; no guard needed.
- lifecycle.js site is a CONTENT SCRIPT injected into arbitrary pages — chrome.runtime.getManifest()
  IS available, but the surrounding extension context can be invalidated, so use the optional-chaining
  guard + fallback idiom shown above.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make Group A runtime version emissions dynamic (read from manifest)</name>
  <files>extension/background.js, extension/content/lifecycle.js, extension/ui/sidepanel.js, extension/ui/popup.js</files>
  <action>
Replace the five hardcoded runtime version emissions so each reads the live manifest version instead of a string literal. Do NOT hardcode `0.9.90` in any of these — the value must come from `chrome.runtime.getManifest().version` so future version bumps auto-track. Grep for each exact literal string rather than trusting line numbers (line numbers shift).

Edit each site:

1. extension/background.js — find `automationLogger.logInit('extension', 'installed', { version: 'v0.9.50' });` (inside the `chrome.runtime.onInstalled.addListener` callback). Change the `version` value to `chrome.runtime.getManifest().version`. Keep it a bare version (NO `v` prefix) to match the manifest format (`0.9.90`, not `v0.9.90`). Result: `{ version: chrome.runtime.getManifest().version }`. No guard needed (full SW context inside onInstalled).

2. extension/content/lifecycle.js — find `logger.logInit('content_script', 'loaded', { version: '0.9.50', url: window.location.href });`. This is a CONTENT SCRIPT. Replace `'0.9.50'` with a guarded dynamic read using the optional-chaining idiom established in this same file (see `chrome.runtime?.id` at the guard site): use `chrome?.runtime?.getManifest?.().version` with a string fallback of `'unknown'` (i.e. `chrome?.runtime?.getManifest?.().version || 'unknown'`). Keep the `url: window.location.href` field unchanged. Bare version, no `v` prefix.

3. extension/ui/sidepanel.js — find `console.log('FSB v0.9.50 side panel loaded');` (inside the async DOMContentLoaded handler). Rebuild as a template literal: `console.log(\`FSB v${chrome.runtime.getManifest().version} side panel loaded\`);`. Here the `v` prefix is part of the human-readable log string and stays; the version number itself comes from the manifest. No guard needed (extension UI page context).

4. extension/ui/sidepanel.js — find `console.log('FSB v0.9.50 side panel script loaded');` (module top-level). Rebuild as: `console.log(\`FSB v${chrome.runtime.getManifest().version} side panel script loaded\`);`. Same rationale as site 3.

5. extension/ui/popup.js — find `console.log('FSB v0.9.50 chat interface loaded');` (module top-level). Rebuild as: `console.log(\`FSB v${chrome.runtime.getManifest().version} chat interface loaded\`);`.

Do NOT touch any other lines, any package.json, manifest.json, or the historical note in extension/utils/overlay-state.js.
  </action>
  <verify>
    <automated>test -z "$(git grep -n "0\.9\.50\|v0\.9\.50" -- extension/background.js extension/content/lifecycle.js extension/ui/sidepanel.js extension/ui/popup.js)" && echo PASS_NO_STALE_LITERALS</automated>
    <automated>test "$(git grep -c "getManifest()\.version" -- extension/background.js extension/content/lifecycle.js extension/ui/sidepanel.js extension/ui/popup.js | wc -l | tr -d ' ')" = "4" && echo PASS_FOUR_FILES_DYNAMIC</automated>
  </verify>
  <done>
All five Group A runtime emissions read the version from `chrome.runtime.getManifest().version` (lifecycle.js content-script site uses the guarded `chrome?.runtime?.getManifest?.().version || 'unknown'` form). No `0.9.50`/`v0.9.50` literal remains in background.js, lifecycle.js, sidepanel.js, or popup.js. No version is hardcoded to `0.9.90` in these files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Bump Group B cosmetic file-header comments to 0.9.90</name>
  <files>extension/ai/agent-loop.js, extension/ai/ai-providers.js, extension/ai/universal-provider.js, extension/background.js, extension/config/config.js, extension/ui/options.js, extension/ui/popup.js, extension/ui/sidepanel.js, extension/utils/action-verification.js, extension/utils/automation-logger.js</files>
  <action>
Update the static version number in the doc/header comments from `0.9.50` to `0.9.90`. These are cosmetic comments (e.g. `// ... for FSB v0.9.50` or `* ... for FSB v0.9.50`); static text is correct here — do NOT convert these to dynamic getManifest calls. Grep for the exact comment literals rather than trusting line numbers.

In each file, change the comment's `v0.9.50` to `v0.9.90` (preserve `v` prefix and surrounding comment text exactly):

- extension/ai/agent-loop.js — header comment: `* Agent Loop Engine for FSB v0.9.50`
- extension/ai/ai-providers.js — header comment: `* AI Provider implementations for FSB v0.9.50`
- extension/ai/universal-provider.js — header comment: `* Universal AI Provider for FSB v0.9.50`
- extension/background.js — top comment: `// Background service worker for FSB v0.9.50` (this is line 1, distinct from the Task 1 logInit edit at ~13458)
- extension/config/config.js — header comment: `* Configuration management for FSB v0.9.50`
- extension/ui/options.js — top comment: `// FSB v0.9.50 - Modern Dashboard Control Panel Script`
- extension/ui/popup.js — top comment: `// Modern Chat Interface Script for FSB v0.9.50` (line 1, distinct from the Task 1 console.log edit at ~960)
- extension/ui/sidepanel.js — top comment: `// Side Panel Script for FSB v0.9.50 - Persistent UI` (line 1, distinct from the Task 1 console.log edits)
- extension/utils/action-verification.js — top comment: `// Action Verification Module for FSB v0.9.50`
- extension/utils/automation-logger.js — comment: `// Automation Logger for FSB v0.9.50`

Do NOT touch extension/utils/overlay-state.js (historical note — leave as-is). Do NOT touch any package.json, manifest.json, mcp/, or showcase/ files.
  </action>
  <verify>
    <automated>test -z "$(git grep -n "0\.9\.50\|v0\.9\.50" -- extension/ai/agent-loop.js extension/ai/ai-providers.js extension/ai/universal-provider.js extension/config/config.js extension/ui/options.js extension/utils/action-verification.js extension/utils/automation-logger.js)" && echo PASS_GROUPB_HEADERS_CLEAN</automated>
    <automated>test "$(git grep -c "FSB v0\.9\.90" -- extension/ | wc -l | tr -d ' ')" -ge "8" && echo PASS_HEADERS_BUMPED</automated>
  </verify>
  <done>
All ten Group B header comments read `v0.9.90`. No `0.9.50`/`v0.9.50` literal remains in any of the ten listed files (note: background.js, popup.js, and sidepanel.js are fully clean only after Task 1 also completes their runtime-emission edits). overlay-state.js untouched.
  </done>
</task>

</tasks>

<verification>
Phase-level acceptance (run after both tasks complete):

1. The ONLY remaining `0.9.50` reference under `extension/` is the intentional historical note in overlay-state.js:
   ```
   git grep -n "0\.9\.50\|v0\.9\.50" -- extension/
   ```
   Expected: exactly one line — `extension/utils/overlay-state.js:419` (the "...omitted...in v0.9.50" historical note). Any other line is a failure.

2. Runtime emissions are dynamic, not hardcoded to 0.9.90:
   ```
   git grep -n "0\.9\.90\|v0\.9\.90" -- extension/background.js extension/content/lifecycle.js extension/ui/sidepanel.js extension/ui/popup.js
   ```
   Expected: matches ONLY in cosmetic header comments (background.js line 1, popup.js line 1, sidepanel.js line 1). The logInit calls and the three runtime console.log strings must NOT contain a `0.9.90` literal — they must use `getManifest().version`.

3. Out-of-scope guard — these must NOT have been modified:
   ```
   git status --porcelain -- extension/manifest.json package.json mcp/ showcase/ extension/utils/overlay-state.js
   ```
   Expected: empty output (no changes to any of these).

4. (Optional sanity) The repo has a large `npm test` suite. A targeted grep is the primary gate; `npm test` may be run as a non-blocking sanity check but is NOT required for this cosmetic/log-string change. Do NOT run the application.
</verification>

<success_criteria>
- All 5 Group A runtime emissions read `chrome.runtime.getManifest().version` (lifecycle.js content-script site is guarded with optional chaining + `'unknown'` fallback).
- All 10 Group B header comments show `v0.9.90`.
- `git grep "0\.9\.50" -- extension/` returns ONLY `extension/utils/overlay-state.js:419`.
- No runtime emission hardcodes `0.9.90`; manifest remains the single source of truth.
- extension/manifest.json, all package.json files, mcp/, showcase/, and overlay-state.js are untouched.
</success_criteria>

<output>
Create `.planning/quick/260615-5ks-replace-stale-hardcoded-0-9-50-version-l/260615-5ks-SUMMARY.md` when done.
</output>
