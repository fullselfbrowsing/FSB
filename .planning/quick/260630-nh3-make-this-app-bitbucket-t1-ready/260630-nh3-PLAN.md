---
quick: 260630-nh3
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
files_modified:
  - catalog/handlers/bitbucket.js
  - extension/catalog/handlers/bitbucket.js
  - extension/catalog/recipe-index.generated.js
  - extension/utils/capability-search.js
  - scripts/report-t1-readiness.mjs
  - catalog/write-activation-evidence.json
  - tests/capability-head-handlers.test.js
  - tests/head-handler-upgrade.test.js
  - tests/guarded-write-failclosed.test.js
  - tests/t1-readiness-report.test.js
  - tests/t1-terminal-states.test.js
requirements:
  - QT-260630-nh3
user_setup: []
must_haves:
  truths:
    - "Bitbucket read descriptors resolve as T1a handler-backed capabilities on https://bitbucket.org."
    - "Bitbucket write/destructive descriptors are registered as guarded fail-closed T1a rows and never call ctx.executeBoundSpec."
    - "Search/readiness surfaces mark Bitbucket read slugs as t1-ready and mutation slugs as t1-guarded-fail-closed."
    - "The expanded handler uses only same-origin /!api/2.0 GET specs for reads and does not call browser scripting, direct network APIs, or token/storage APIs."
  artifacts:
    - path: "catalog/handlers/bitbucket.js"
      provides: "Canonical Bitbucket T1a read and guarded-write handler module"
      contains: "FsbHandlerBitbucket"
    - path: "extension/catalog/handlers/bitbucket.js"
      provides: "Extension-root bundled copy"
      contains: "FsbHandlerBitbucket"
    - path: "catalog/write-activation-evidence.json"
      provides: "Guarded evidence records for Bitbucket mutation rows"
      contains: "bitbucket.create_pull_request"
  key_links:
    - from: "catalog/handlers/bitbucket.js"
      to: "extension/utils/capability-catalog.js"
      via: "FsbCapabilityCatalog.registerHandler for each bitbucket.* slug"
      pattern: "registerHandler\\('bitbucket\\."
    - from: "scripts/report-t1-readiness.mjs"
      to: "extension/catalog/handlers/bitbucket.js"
      via: "HANDLER_MODULES includes bitbucket.js so readiness resolves handler proof"
      pattern: "'bitbucket\\.js'"
---

<objective>
Make Bitbucket T1-ready under the current T1 safety posture: promote safe same-origin
read descriptors to direct T1a execution and keep mutation-capable rows guarded
fail-closed until live mutation-body UAT exists.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand the Bitbucket handler</name>
  <files>catalog/handlers/bitbucket.js, extension/catalog/handlers/bitbucket.js</files>
  <action>
Replace the previous three-read Bitbucket head with a closed, data-driven
`FsbHandlerBitbucket` that covers all 18 existing Bitbucket reads using first-party
`https://bitbucket.org/!api/2.0` GET specs through `ctx.executeBoundSpec`.

Register the nine write/destructive descriptors as inert guarded handlers returning
the dual-field `RECIPE_DOM_FALLBACK_PENDING` shape without calling
`ctx.executeBoundSpec`.
  </action>
  <verify>
    <automated>node tests/capability-head-handlers.test.js</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire readiness and safety evidence</name>
  <files>extension/utils/capability-search.js, scripts/report-t1-readiness.mjs, catalog/write-activation-evidence.json</files>
  <action>
Add all Bitbucket read slugs to `T1_READY_SLUGS`, add all Bitbucket mutation slugs to
the guarded fail-closed readiness sets, and record guarded write evidence entries
requiring method/path/body/CSRF/redaction/load-smoke proof before activation.
  </action>
  <verify>
    <automated>node tests/t1-readiness-report.test.js</automated>
    <automated>node scripts/report-t1-readiness.mjs</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lock Bitbucket regression coverage</name>
  <files>tests/capability-head-handlers.test.js, tests/head-handler-upgrade.test.js, tests/guarded-write-failclosed.test.js, tests/t1-terminal-states.test.js</files>
  <action>
Extend handler behavior, dom-to-T1 upgrade, guarded-write inertness, and
terminal-state/search override tests so Bitbucket cannot silently regress to
discovery-pending or active mutation execution.
  </action>
  <verify>
    <automated>node tests/head-handler-upgrade.test.js</automated>
    <automated>node tests/guarded-write-failclosed.test.js</automated>
  </verify>
</task>

</tasks>
