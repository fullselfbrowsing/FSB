---
quick_id: 260630-vo5
slug: make-this-app-circleci-t1-ready
status: planned
created: 2026-07-01
type: execute
requirements:
  - QT-260630-vo5
---

# Quick Task 260630-vo5: Make CircleCI T1-ready

## Goal

Promote the remaining CircleCI first-party `/api/v2` read descriptors to T1a handler-backed execution, and register CircleCI write/destructive descriptors as guarded fail-closed T1a heads pending live mutation-body UAT.

## Scope

- Extend the existing `catalog/handlers/circleci.js` same-origin head.
- Keep all reads pinned to `https://app.circleci.com/api/v2` with `same-origin-cookie`.
- Keep mutation rows inert: they must return `RECIPE_DOM_FALLBACK_PENDING` without calling `executeBoundSpec`.
- Refresh readiness/search/test surfaces so CircleCI rows are honestly reported as either `t1-ready` or `t1-guarded-fail-closed`.

## Tasks

1. Extend the CircleCI handler.
   - Add T1a read handlers for the remaining read descriptors:
     `get_context`, `get_flaky_tests`, `get_pipeline_config`,
     `get_project_workflow_metrics`, `get_workflow_job_metrics`,
     `get_workflow_runs`, `list_context_env_vars`, `list_contexts`,
     `list_env_vars`, and `list_schedules`.
   - Add guarded fail-closed entries for CircleCI write/destructive descriptors:
     `approve_job`, `cancel_job`, `cancel_workflow`, `create_context`,
     `create_env_var`, `create_schedule`, `delete_context`, `delete_env_var`,
     `delete_schedule`, `rerun_workflow`, `trigger_pipeline`, and
     `update_schedule`.
   - Copy the canonical handler to `extension/catalog/handlers/circleci.js`.

2. Update focused gates.
   - Add CircleCI read/write rows to upgrade, guarded-write, readiness, search, and handler behavior tests.
   - Keep origin-classification unchanged, because the existing CircleCI `/api/v2` relative-base proof covers the extended handler.

3. Regenerate reporting artifacts and record completion.
   - Run the T1 readiness, tail-worklist, and terminal-state report scripts.
   - Update `.planning/STATE.md` and write `260630-vo5-SUMMARY.md`.

## Verification

- `node -c catalog/handlers/circleci.js`
- `cmp -s catalog/handlers/circleci.js extension/catalog/handlers/circleci.js`
- `node tests/capability-head-handlers.test.js`
- `node tests/head-handler-upgrade.test.js`
- `node tests/guarded-write-failclosed.test.js`
- `node tests/t1-readiness-report.test.js`
- `node tests/t1-terminal-states.test.js`
- `node scripts/verify-origin-classification.mjs`
- `node scripts/verify-t1-port-contract.mjs`
- `node scripts/verify-recipe-path-guard.mjs`
- `node scripts/report-t1-readiness.mjs`
- `node scripts/report-t1-tail-worklist.mjs`
- `node scripts/report-t1-terminal-states.mjs`
