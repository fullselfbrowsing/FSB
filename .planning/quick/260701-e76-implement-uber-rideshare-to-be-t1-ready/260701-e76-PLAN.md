---
quick_id: 260701-e76
status: planned
description: Implement Uber rideshare to be T1 ready
created: 2026-07-01
---

# Quick Task 260701-e76: Implement Uber Rideshare to be T1 Ready

## Scope

Promote the existing `uber.*` rideshare descriptor surface to a reviewed T1a head without touching `ubereats.*`.

- `uber.get_current_user`, `uber.search_locations`, `uber.get_travel_status`, `uber.get_membership`, `uber.get_past_activities`, `uber.get_enabled_products`, `uber.get_upcoming_activities`, and `uber.get_product_suggestions` resolve as T1a same-origin reads.
- No Uber ride/payment mutation is introduced. If a future Uber write descriptor appears, it must stay guarded fail-closed until live mutation-body UAT exists.
- Existing sensitive-origin policy for `https://*.uber.com` remains unchanged.

## Tasks

1. Add the Uber rideshare handler module.
   - Files: `catalog/handlers/uber.js`, `extension/catalog/handlers/uber.js`
   - Action: Implement explicit same-origin `/api/...` read specs through `ctx.executeBoundSpec`, with origin pins, same-origin-cookie auth, strict response-shape guards, and typed DOM fallback results.
   - Verify: Handler exports all eight `uber.*` read slugs, makes no direct browser credential/storage/network calls, and the extension copy matches.
   - Done: No `ubereats.*` handler or descriptor is changed.

2. Wire the handler into T1 registration and readiness.
   - Files: `extension/background.js`, `extension/utils/capability-catalog.js`, `extension/utils/capability-search.js`, `scripts/report-t1-readiness.mjs`, `scripts/verify-t1-port-contract.mjs`
   - Action: Add Uber to the handler load list, head manifest, search readiness overrides, readiness report preloads, and port-contract app mapping.
   - Verify: Targeted readiness smoke reports the eight Uber rows as `t1-ready` with handler proof and sensitive origin classification, not blocked.
   - Done: Service-denylist/legal policy gates are preserved.

3. Add focused coverage and record completion.
   - Files: `tests/uber-t1-ready.test.js`, `.planning/quick/260701-e76-implement-uber-rideshare-to-be-t1-ready/260701-e76-SUMMARY.md`, `.planning/STATE.md`
   - Action: Add handler request/shape/fallback/search-readiness coverage, then record quick-task outcome.
   - Verify: Run `node tests/uber-t1-ready.test.js`, syntax checks, and targeted readiness/port smoke.
   - Done: Commit scoped files if the shared dirty workspace permits it; otherwise document working-tree-only status.
