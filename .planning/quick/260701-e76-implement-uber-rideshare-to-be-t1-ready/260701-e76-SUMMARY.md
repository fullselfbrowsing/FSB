---
quick_id: 260701-e76
slug: implement-uber-rideshare-to-be-t1-ready
status: complete
completed_at: "2026-07-01T15:20:00Z"
commit: working-tree
---

# Quick Task 260701-e76 Summary: Implement Uber Rideshare to be T1 Ready

## Outcome

Complete in the working tree.

Promoted the existing Uber rideshare read surface to T1a handler coverage:

- `uber.get_current_user`
- `uber.search_locations`
- `uber.get_travel_status`
- `uber.get_membership`
- `uber.get_past_activities`
- `uber.get_enabled_products`
- `uber.get_upcoming_activities`
- `uber.get_product_suggestions`

No Uber Eats handler or descriptor was changed. No Uber ride/payment write was introduced.

## Changes

- Added `catalog/handlers/uber.js` and mirrored it to `extension/catalog/handlers/uber.js`.
- Implemented same-origin-cookie `/api/...` read specs through `ctx.executeBoundSpec` only, with `https://www.uber.com` origin pins, typed fail-closed fallback results, and no direct browser credential/storage/network primitive.
- Kept `uber.get_past_activities` trip-only at the handler layer so Eats order history is not surfaced through the Uber rideshare head.
- Wired Uber into extension handler loading, head-handler seeding, capability-search readiness status, readiness-report preloading, T1 port-contract app mapping, and the recipe-path handler allowlist.
- Added `tests/uber-t1-ready.test.js` for request specs, mapping, fail-closed behavior, search readiness labels, readiness rows, extension mirror parity, and targeted port-contract validation.

## Verification

Passed:

- `node --check catalog/handlers/uber.js`
- `node --check extension/catalog/handlers/uber.js`
- `node --check tests/uber-t1-ready.test.js`
- `node --check extension/utils/capability-search.js && node --check extension/utils/capability-catalog.js && node --check scripts/report-t1-readiness.mjs && node --check scripts/verify-t1-port-contract.mjs`
- `cmp catalog/handlers/uber.js extension/catalog/handlers/uber.js`
- `git diff --check -- catalog/handlers/uber.js extension/catalog/handlers/uber.js tests/uber-t1-ready.test.js extension/background.js extension/utils/capability-catalog.js extension/utils/capability-search.js scripts/report-t1-readiness.mjs scripts/verify-t1-port-contract.mjs scripts/verify-recipe-path-guard.mjs .planning/quick/260701-e76-implement-uber-rideshare-to-be-t1-ready/260701-e76-PLAN.md`
- `node tests/uber-t1-ready.test.js` (19 passed, 0 failed)

## Commit Status

Working-tree only.

The shared Conductor workspace was already very dirty, and several files required for Uber wiring already contained unrelated edits from other agents (`extension/background.js`, `extension/utils/capability-catalog.js`, `extension/utils/capability-search.js`, `scripts/report-t1-readiness.mjs`, `scripts/verify-t1-port-contract.mjs`, `scripts/verify-recipe-path-guard.mjs`, and `.planning/STATE.md`). Staging those whole files would have included unrelated work, so no commit was created.

## Blockers

No Uber-specific blocker remains. The only blocker to an atomic commit is the mixed shared working tree.
