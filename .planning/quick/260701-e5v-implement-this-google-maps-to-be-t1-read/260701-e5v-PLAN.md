---
status: planned
quick_id: 260701-e5v
slug: implement-this-google-maps-to-be-t1-read
created: 2026-07-01
---

# Quick Task 260701-e5v: Implement This Google Maps To Be T1 Ready

## Scope

Promote the existing `gmaps.*` OpenTabs descriptors to T1 readiness without broad catalog churn.

## Plan

1. Add a bundled Google Maps head handler for safe read behavior:
   - deterministic Maps URL builders,
   - bounded same-origin page reads for active Maps state,
   - first-party Google HTML reads via `executeBoundSpec` for place search/details.
2. Keep `gmaps.set_travel_mode` guarded fail-closed until live mutation evidence exists.
3. Register the handler in the extension catalog/background load path and update Maps-only readiness/evidence assertions.
4. Verify with focused Maps tests and the narrow readiness/evidence gates that can run in the dirty workspace.

## Gates

- No direct handler `fetch`, `chrome.tabs`, `chrome.scripting`, cookies, or storage APIs.
- Reads resolve as `T1a` for `https://www.google.com`.
- Write row returns byte-stable `RECIPE_DOM_FALLBACK_PENDING` and calls no execution primitive.
- Shared files receive only Google Maps-specific entries.
