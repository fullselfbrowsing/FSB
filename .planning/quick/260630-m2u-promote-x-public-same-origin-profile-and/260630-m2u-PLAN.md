---
quick_id: 260630-m2u
description: Promote X public same-origin profile and tweet reads to T1-ready
status: complete
date: 2026-06-30
---

# Quick Task 260630-m2u: Promote X public same-origin profile and tweet reads to T1-ready

## Scope

Promote the safe public read-only X descriptors that can be proven through first-party `https://x.com` HTML pages:

- `x.get_tweet`
- `x.get_user_profile`

Leave X timelines, bookmarks, followers/likers/retweeters, search, lists, and mutation-like rows in the catalog tail until authenticated GraphQL operation-hash, CSRF, write-UAT, and policy evidence exists.

## Tasks

1. Add an X T1a handler module.
   - Files: `catalog/handlers/x.js`, `extension/catalog/handlers/x.js`
   - Action: Build same-origin `GET` specs to public X tweet/profile pages, parse public metadata/JSON-LD, and return dual-field DOM fallback on missing or unexpected page shape.
   - Verify: Handler source uses only `ctx.executeBoundSpec`, pins `origin: https://x.com`, performs no direct fetch/browser API calls, and registers exact existing `x.*` slugs.

2. Wire X into T1 readiness surfaces.
   - Files: `extension/background.js`, `extension/utils/capability-catalog.js`, `extension/utils/capability-search.js`, `scripts/report-t1-readiness.mjs`, `scripts/verify-t1-port-contract.mjs`
   - Action: Add `FsbHandlerX` to the handler manifest/import list and readiness/search verifier mappings.
   - Verify: Readiness reports `x.get_tweet` and `x.get_user_profile` as handler-backed `t1-ready`; search no longer labels those two rows discovery-pending.

3. Add targeted contract tests.
   - Files: `tests/capability-head-handlers.test.js`, `tests/head-handler-upgrade.test.js`, `tests/t1-terminal-states.test.js`
   - Action: Assert X handler behavior, source safety, extension copy parity, exact slug upgrade, and search readiness override coverage.
   - Verify: Run focused T1/readiness tests plus `npm run validate:extension` if feasible in the current dirty workspace.
