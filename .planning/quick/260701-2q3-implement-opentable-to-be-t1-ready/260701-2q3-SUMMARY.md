# Quick Task 260701-2q3 Summary: Implement OpenTable T1 Readiness

## Outcome

Implemented OpenTable T1 readiness with same-origin read handlers for restaurant search, restaurant detail, and reservation listing. Reservation creation and cancellation are registered as T1 guarded fail-closed handlers because they are held-card and booking-affecting mutations.

## Changes

- Added `catalog/handlers/opentable.js` and mirrored it into `extension/catalog/handlers/opentable.js`.
- Registered the OpenTable handler in the extension catalog loader and MV3 background import path.
- Added verifier/readiness wiring for OpenTable handler proofs and guarded write classification.
- Added focused OpenTable handler coverage plus catalog/readiness test expectations.

## Verification

- `node tests/opentable-t1-handler.test.js` - passed, 16/16.
- `node tests/head-handler-cap.test.js` - passed, 5/5.
- Focused readiness report check for the five OpenTable slugs - passed.
- Focused capability catalog resolution check for the five OpenTable slugs - passed.
- `node scripts/verify-origin-classification.mjs` - OpenTable classifies as same-origin; broader script still fails on unrelated in-flight app heads.
- `node scripts/verify-t1-port-contract.mjs` - fails on unrelated PostHog/Tinder/AWS/TikTok rows; no OpenTable failures reported.
- `node tests/t1-readiness-report.test.js` - fails on unrelated AWS readiness rows; OpenTable guarded and ready expectations pass.

## Residual Risk

- OpenTable read endpoints are derived from the vendored OpenTabs metadata and are guarded by shape checks, but were not live-smoked against a real authenticated OpenTable session.
- Reservation create/cancel remain intentionally inert until live mutation-body UAT and consent/audit checks are available.
- No commit was created because the worktree contains broad unrelated parallel edits; the OpenTable scope is left in the working tree.
