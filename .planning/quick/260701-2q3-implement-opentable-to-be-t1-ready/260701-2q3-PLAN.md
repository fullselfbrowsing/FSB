---
status: planned
created: 2026-07-01
quick_id: 260701-2q3
slug: implement-opentable-to-be-t1-ready
---

# Quick Task 260701-2q3: Implement OpenTable to be T1 ready

## Scope

Make the existing OpenTable descriptors resolve through reviewed T1 handler wiring. Public/credentialed GET rows should execute through the same-origin bound-spec path. Reservation writes must remain fail-closed until live mutation-body UAT exists.

## Tasks

1. Add `catalog/handlers/opentable.js` and extension copy with:
   - T1a same-origin read handlers for `opentable.search_restaurants`, `opentable.get_restaurant`, and `opentable.list_reservations`.
   - Guarded fail-closed inert handlers for `opentable.reserve_table` and `opentable.cancel_reservation`.
   - No direct network calls, no Chrome API access, no secret logging.

2. Wire OpenTable into the catalog/runtime verifier surfaces:
   - `extension/utils/capability-catalog.js`
   - `extension/background.js`
   - `scripts/report-t1-readiness.mjs`
   - `scripts/verify-t1-port-contract.mjs`
   - relevant T1/head-handler tests.

3. Run narrow verification:
   - OpenTable handler behavior checks.
   - T1 readiness / port-contract checks that prove three ready reads and two guarded writes.
