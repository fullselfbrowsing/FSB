---
quick_id: 260701-2mc
slug: implement-zendesk-to-be-t1-ready
status: completed
date: 2026-07-01
---

# Quick Task 260701-2mc: Implement Zendesk to be T1 ready

## Scope

Promote Zendesk descriptors from DOM/discovery to the T1 surface by adding a reviewed same-origin handler, wiring it into the catalog/service-worker/verification manifests, and keeping Zendesk mutations guarded fail-closed until live mutation-body UAT exists.

## Tasks

1. Add Zendesk T1 handler
   - Files: `catalog/handlers/zendesk.js`, `extension/catalog/handlers/zendesk.js`
   - Action: Implement same-origin `GET /api/v2` read handlers for Zendesk read descriptors, plus inert guarded handlers for write/destructive descriptors.
   - Verify: Handler exports all 17 Zendesk descriptor slugs, pins `https://zendesk.com`, uses only `ctx.executeBoundSpec`, and guarded writes call no execution primitive.
   - Done: Handler loads in Node, extension copy matches canonical source, and all 17 Zendesk slugs are exported.

2. Wire Zendesk into T1 manifests
   - Files: `extension/background.js`, `extension/utils/capability-catalog.js`, `scripts/report-t1-readiness.mjs`, `scripts/verify-t1-port-contract.mjs`, `scripts/verify-origin-classification.mjs`, `scripts/verify-recipe-path-guard.mjs`
   - Action: Register/load `FsbHandlerZendesk`, include Zendesk in handler verification, and add Zendesk writes to the guarded fail-closed set.
   - Verify: `zendesk.*` resolves as T1a or guarded fail-closed in readiness and port-contract checks.
   - Done: T1 gates recognize Zendesk as a handler-backed app; readiness reports 13 read rows as `t1-ready` and 4 mutation rows as `t1-guarded-fail-closed`.

3. Extend focused tests
   - Files: `tests/capability-head-handlers.test.js`, `tests/head-handler-upgrade.test.js`, `tests/guarded-write-failclosed.test.js`, `tests/t1-readiness-report.test.js`
   - Action: Add Zendesk coverage to handler shape, dom-to-T1a upgrade, guarded write, and readiness expectations.
   - Verify: Run targeted T1 tests and relevant scripts.
   - Done: Tests pass for the Zendesk surface; remaining broad-gate failures are unrelated concurrent Home Depot/Sentry/PostHog/Tinder/TikTok/origin-map/allowlist work.
