# Quick Task 260630-m1q: Make this app Stripe T1-ready

## Scope

Promote Stripe Dashboard read-only OpenTabs rows to T1a using a same-origin dashboard head. Keep Stripe write/destructive rows unregistered until live mutation-body UAT exists.

## Tasks

### 1. Add Stripe same-origin read head

Files:
- `catalog/handlers/stripe.js`
- `extension/catalog/handlers/stripe.js`
- `extension/background.js`
- `extension/utils/capability-catalog.js`

Action:
- Add a Stripe Dashboard handler that bootstraps from `https://dashboard.stripe.com`, extracts dashboard session carriers from the active dashboard response, then executes read-only `/v1` GET specs through `executeBoundSpec`.
- Register 21 read slugs as T1a.
- Do not register create/update/delete/finalize/void Stripe rows.

Done:
- Stripe read slugs are handler-backed and origin-pinned to `https://dashboard.stripe.com`.

### 2. Add readiness and contract coverage

Files:
- `extension/utils/capability-search.js`
- `scripts/report-t1-readiness.mjs`
- `scripts/verify-t1-port-contract.mjs`
- `scripts/verify-origin-classification.mjs`
- `scripts/verify-recipe-path-guard.mjs`
- `scripts/coverage-report.mjs`
- `tests/head-handler-upgrade.test.js`
- `tests/capability-head-handlers.test.js`
- `tests/t1-terminal-states.test.js`

Action:
- Mark the 21 Stripe read slugs as T1-ready in search/readiness surfaces.
- Add Stripe to the handler loaders, port-contract map, origin-classification map, recipe-path allowlist, and upgrade proof.
- Assert handler hygiene: no direct fetch, no `chrome.tabs`/`chrome.scripting`, no `api.stripe.com`, and no secret-bearing console logging.

Done:
- Stripe appears in T1 readiness, terminal-state, port-contract, and same-origin classification checks.

### 3. Refresh generated bundle and shared gates

Files:
- `extension/catalog/handlers/stripe.js`
- `extension/catalog/recipe-index.generated.js`
- `dist/fsb-extension-v0.9.90.zip`

Action:
- Run `npm run package:extension`.
- Run focused T1 gates and `npm run validate:extension`.
- Where existing parallel head work blocked shared gates, apply narrow compatibility fixes without activating writes.

Done:
- Extension bundle includes Stripe handler copy and validation is green.
