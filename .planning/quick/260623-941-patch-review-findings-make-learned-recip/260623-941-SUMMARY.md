---
status: complete
quick_id: 260623-941
date: 2026-06-23
code_commit: b1d74e87
---

# Quick Task 260623-941 Summary

## Result

Patched both review findings.

- Synthesized learned recipes now declare generated endpoint placeholders in `recipe.params` and carry transient `replayArgs` so the immediate replay gate can bind `/api/items/{id}` before promotion.
- Discovery-session replay and `promoteAfterReplay` pass those transient args to `interpretRecipe`, while the learned store still persists only the recipe and descriptor.
- `extension/catalog/recipe-index.generated.js` is now committed for unpacked dev loads, regenerated from the current catalog, and `validate-extension` fails if the snapshot is missing or stale.

## Verification

- `node tests/recipe-synthesizer.test.js`
- `node tests/learned-promote-after-replay.test.js`
- `npm run validate:extension`
- `node scripts/validate-extension.mjs`
- `git diff --check`
- `git diff --cached --check`

## Commits

- `b1d74e87` - `fix(capabilities): patch learned replay and catalog snapshot`
