# Quick 260630-u7d Plan: Make Airbnb T1-Ready

## Scope
- Promote reviewed Airbnb read-only OpenTabs descriptors to bundled T1a head handlers.
- Keep `airbnb.remove_from_wishlist` out of the handler/readiness path because it is destructive.
- Preserve same-origin execution through `executeBoundSpec` / `executeBoundPageRead`.

## Work
1. Add `catalog/handlers/airbnb.js` with same-origin GraphQL GET reads and current-page extraction reads.
2. Extend page-read support for Airbnb SSR data extraction.
3. Wire the handler into extension imports, capability catalog/search, T1 readiness, port-contract, origin, and path-guard surfaces.
4. Correct `airbnb.is_host` from generated `write` to source-audited `read`.
5. Add focused unit coverage and update readiness/search assertions.
6. Regenerate the extension catalog snapshot and copied handler.

## Verification
- Syntax-check the new handler.
- Run focused head-handler, readiness, origin, port-contract, and search tests.
- Note any unrelated pre-existing workspace failures separately.
