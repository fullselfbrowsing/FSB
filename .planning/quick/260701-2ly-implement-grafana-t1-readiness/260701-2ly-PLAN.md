# 260701-2ly Plan: Implement Grafana T1 Readiness

## Scope

Promote Grafana dashboard and metric read capabilities from DOM-backed descriptors to same-origin T1 handlers. Keep the implementation limited to Grafana plus the shared handler registry/test wiring required for T1 readiness.

## Tasks

1. Add a Grafana same-origin handler for dashboard list, dashboard detail, and metric query reads.
2. Mark Grafana descriptors and generated extension index entries as handler-backed.
3. Wire the handler into the extension service worker, capability catalog, T1 readiness reporting, and T1 port verifier.
4. Add focused head-handler tests covering origin pinning, no credential replay, bounded spec construction, read-only metric query guarding, and response-shape fallback behavior.
5. Run the narrow verification set and record unrelated shared-workspace blockers.

