---
quick_id: 260617-e2w
slug: fix-review-findings-provider-bridge-time
status: planned
created: 2026-06-17
---

# Quick Task 260617-e2w: Fix review findings

Fix three review findings from the workspace review:

1. Restore bounded provider request timeouts for the offscreen Lattice provider bridge.
2. Recreate or retry the offscreen provider host before failing with `host_unreachable`.
3. Normalize attribute trigger extraction so supported top-level `attribute`/`attrName` inputs are evaluated correctly.

## Tasks

### 1. Provider bridge runtime safety

- Files: `extension/ai/agent-loop.js`, `extension/ai/lattice-provider-bridge.js`, `extension/background.js`
- Action: add a bounded abort timeout around bridge-backed provider calls and ensure the offscreen host is available before dispatching provider messages.
- Verify: focused provider bridge tests or targeted source-level tests cover timeout propagation and host ensure/retry behavior.

### 2. Attribute trigger alias normalization

- Files: `extension/background.js`, `extension/utils/value-extractor.js`, trigger tests
- Action: make top-level `attribute`/`attrName` inputs reach the condition descriptor used by `FsbValueExtractor.extractValue()`.
- Verify: tests cover a trigger armed with top-level `extract: "attribute"` and `attrName`/`attribute` rather than only `condition.attribute`.

### 3. Regression checks

- Files: focused test files only
- Action: run targeted trigger/provider tests and inspect the resulting diff.
- Verify: no unrelated source churn; GSD summary and state updated.
