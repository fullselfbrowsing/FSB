---
quick_id: 260701-2lr
status: completed
---

# Quick Task 260701-2lr: implement this MiniMax to be T1 ready

## Task 1: Add MiniMax T1a handler

files:
- catalog/handlers/minimax.js
- extension/catalog/handlers/minimax.js
- extension/utils/capability-fetch.js

action:
- Implement a MiniMax handler pinned to `https://agent.minimax.io`.
- Route `minimax.get_current_user` through the bounded page-read primitive so the page's signed Axios instance performs the read.
- Register all POST/write/destructive MiniMax rows as guarded fail-closed handlers that return `RECIPE_DOM_FALLBACK_PENDING` without calling any execution primitive.

verify:
- Handler exports all MiniMax descriptors.
- `get_current_user` dispatches `namespace: minimax`, `action: get_current_user`.
- Guarded rows are byte-stable fallback responses and inert.

## Task 2: Wire readiness surfaces

files:
- catalog/descriptors/opentabs__minimax__*.json
- extension/background.js
- extension/utils/capability-catalog.js
- extension/utils/capability-search.js
- scripts/report-t1-readiness.mjs
- scripts/coverage-report.mjs
- scripts/verify-recipe-path-guard.mjs
- scripts/verify-t1-port-contract.mjs
- catalog/write-activation-evidence.json

action:
- Flip MiniMax descriptor backing to `handler`.
- Add MiniMax to handler load manifests, readiness tooling, guarded-fail-closed sets, and the write activation ledger.

verify:
- Readiness reports MiniMax profile read as `t1-ready` and all remaining MiniMax rows as guarded fail-closed.

## Task 3: Add focused regression coverage

files:
- tests/minimax-t1-ready.test.js
- tests/head-handler-cap.test.js
- tests/write-activation-evidence.test.js

action:
- Add a MiniMax-specific test covering handler behavior, resolver upgrade, readiness state, and guarded evidence.
- Update exact-count tests touched by the new handler and guarded rows.

verify:
- Run the focused MiniMax test and the relevant T1 gates.
