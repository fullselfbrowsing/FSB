---
status: complete
quick_id: 260701-2lo
slug: make-onenote-t1-ready
created: 2026-07-01
---

# Quick Task 260701-2lo: Make OneNote T1 ready

## Goal

Promote the existing OneNote catalog descriptors to T1 status by adding reviewed same-origin Microsoft Graph reads and guarded fail-closed write handlers, then wire the OneNote head through the existing T1 readiness/reporting gates.

## Tasks

1. Add the OneNote handler and page-read auth bridge.
   - Files: `catalog/handlers/onenote.js`, `extension/catalog/handlers/onenote.js`, `extension/utils/capability-fetch.js`
   - Action: Implement Graph-backed read handlers that obtain the bearer token only through bounded `executeBoundPageRead`; register write descriptors as inert guarded handlers.
   - Verify: Handler source uses no direct network, storage, cookie, or extension credential APIs; writes return `RECIPE_DOM_FALLBACK_PENDING` without calling execution primitives.

2. Wire OneNote into T1 manifests and reporting.
   - Files: `extension/background.js`, `extension/utils/capability-catalog.js`, `extension/utils/capability-search.js`, `scripts/report-t1-readiness.mjs`, `scripts/verify-origin-classification.mjs`, `scripts/verify-t1-port-contract.mjs`, `scripts/verify-recipe-path-guard.mjs`
   - Action: Add OneNote handler module, read readiness overrides, guarded write entries, and Microsoft Graph origin accommodation.
   - Verify: T1 readiness and origin-classification gates recognize OneNote reads as executable and writes as guarded fail-closed.

3. Add focused regression coverage and run narrow checks.
   - Files: `tests/capability-head-handlers.test.js`, `tests/head-handler-upgrade.test.js`, `tests/guarded-write-failclosed.test.js`, `tests/head-handler-cap.test.js`, `tests/t1-readiness-report.test.js`, `tests/t1-terminal-states.test.js`, `tests/verify-origin-classification.test.js`
   - Action: Cover OneNote registration, Graph GET spec construction, token non-leakage, read mapping, guarded writes, resolver upgrade, search override, and origin classification.
   - Verify: Run the OneNote/T1 focused Node test set and any needed static gates.
