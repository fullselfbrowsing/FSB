---
status: complete
quick_id: 260701-2sl
slug: implement-posthog-to-be-t1-ready
created: 2026-07-01
---

# Quick Task 260701-2sl: Implement PostHog to be T1 ready

## Goal
Promote PostHog catalog descriptors from DOM/discovery-only to T1-ready coverage using the existing same-origin handler pattern.

## Tasks

1. [x] Add a PostHog handler module.
   - Files: `catalog/handlers/posthog.js`, `extension/catalog/handlers/posthog.js`
   - Action: Register exact `posthog.*` descriptor slugs. Read descriptors execute same-origin API GET specs against `https://us.posthog.com`; write/destructive descriptors are guarded fail-closed until live mutation-body UAT exists.
   - Verify: handler exports T1a entries, maps representative params, rejects unsafe query execution, and uses no direct network/credential APIs.

2. [x] Wire PostHog into the T1 catalog/runtime.
   - Files: `extension/utils/capability-catalog.js`, `extension/background.js`, `scripts/report-t1-readiness.mjs`
   - Action: Add `FsbHandlerPosthog` to the head manifest and service worker import chain, load it in readiness reporting, and classify PostHog write/destructive rows as guarded fail-closed.
   - Verify: readiness report resolves PostHog read rows as `t1-ready` and guarded rows as `t1-guarded-fail-closed`.

3. [x] Extend focused tests.
   - Files: `tests/capability-head-handlers.test.js`, `tests/head-handler-upgrade.test.js`, `tests/t1-readiness-report.test.js`
   - Action: Add PostHog handler behavior assertions, dom-to-T1 upgrade assertions, extension copy checks, and readiness expectations.
   - Verify: run the narrow tests plus syntax/load checks.
