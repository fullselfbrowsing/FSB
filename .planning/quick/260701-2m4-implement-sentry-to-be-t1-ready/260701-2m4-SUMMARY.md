---
quick_id: 260701-2m4
slug: implement-sentry-to-be-t1-ready
status: implemented-uncommitted
completed: 2026-07-01
commit: blocked
---

# Quick Task 260701-2m4 Summary

## Outcome

Implemented Sentry T1 readiness in the working tree.

- Added a Sentry same-origin `/api/0` T1a handler with 19 read handlers.
- Kept `sentry.create_comment` and `sentry.update_issue` guarded fail-closed.
- Wired Sentry into the background handler load path, head-handler manifest, readiness report, port-contract map, recipe-path guard allowlist, coverage loader, and origin-classification proof.
- Added Sentry assertions to the head-handler, upgrade, readiness, and guarded-write tests.

## Verification

Passed:

- `node -c catalog/handlers/sentry.js`
- `cmp -s catalog/handlers/sentry.js extension/catalog/handlers/sentry.js`
- `node -e "const h=require('./catalog/handlers/sentry.js'); console.log(Object.keys(h).length, h['sentry.list_projects'].origin, h['sentry.create_comment'].sideEffectClass)"`
- `node tests/head-handler-cap.test.js`
- `node tests/head-handler-upgrade.test.js`
- `node tests/guarded-write-failclosed.test.js`
- `node tests/lattice-provider-bridge-smoke.test.js`
- `node tests/t1-readiness-report.test.js`
- `node scripts/report-t1-readiness.mjs --validate`
- `node scripts/coverage-report.mjs`

Sentry spot-check:

- `sentry.list_projects` and `sentry.search_issues` report `t1-ready`.
- `sentry.create_comment` and `sentry.update_issue` report `t1-guarded-fail-closed`.
- `scripts/verify-origin-classification.mjs` classifies `FsbHandlerSentry` as same-origin at `https://sentry.io/api/0`.

Existing unrelated failures observed:

- `node tests/capability-head-handlers.test.js` fails only `homedepot.get_store_context reads the first-party bootstrap page`.
- `node scripts/verify-t1-port-contract.mjs` fails on pre-existing PostHog verifier mapping/guarded-write rows.
- `node scripts/verify-recipe-path-guard.mjs` fails on pre-existing allowlist drift for multiple existing handlers.
- `node scripts/verify-origin-classification.mjs` fails on pre-existing unmapped/unverifiable heads; Sentry itself is same-origin.

## Commit Status

No commit created. The repository already had staged unrelated changes before the Sentry commit step, and several Sentry hunks sit next to unrelated shared-file hunks that Git could not split automatically. I stopped rather than stage or commit other agents' work.
