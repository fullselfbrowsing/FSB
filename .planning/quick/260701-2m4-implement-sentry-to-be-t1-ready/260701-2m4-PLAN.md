---
quick_id: 260701-2m4
slug: implement-sentry-to-be-t1-ready
status: implemented-uncommitted
created: 2026-07-01
mode: quick
---

# Quick Task 260701-2m4: Implement Sentry to be T1 ready

## Goal

Promote the existing Sentry opentabs descriptors from DOM fallback to the reviewed T1 head path by adding a same-origin Sentry handler, guarded write rows, catalog wiring, and focused tests.

## Tasks

1. Add Sentry head handler
   - Files: `catalog/handlers/sentry.js`, `extension/catalog/handlers/sentry.js`
   - Action: Implement T1a read handlers for Sentry `/api/0` GET endpoints on `https://sentry.io`; register `create_comment` and `update_issue` as guarded fail-closed writes.
   - Verify: Handler exports all Sentry descriptor slugs, read specs call `ctx.executeBoundSpec`, guarded writes do not.
   - Done: Handler and extension copy are byte-identical.

2. Wire readiness and catalog registration
   - Files: `extension/utils/capability-catalog.js`, `scripts/report-t1-readiness.mjs`, `scripts/verify-t1-port-contract.mjs`, `tests/head-handler-cap.test.js`, `tests/head-handler-upgrade.test.js`, `tests/t1-readiness-report.test.js`
   - Action: Add Sentry to the head manifest, readiness handler list, guarded-write set, verifier app map, and expected T1 upgrade/readiness assertions.
   - Verify: T1 readiness and port contract scripts classify Sentry reads as `t1-ready` and Sentry writes as `t1-guarded-fail-closed`.
   - Done: Sentry resolves as T1a through the existing catalog path.

3. Add focused behavioral tests and run targeted gates
   - Files: `tests/capability-head-handlers.test.js`
   - Action: Add Sentry tests for endpoint building, org extraction, fallback behavior, no direct browser/network primitives, and guarded writes.
   - Verify: Run focused handler/readiness/contract tests and readiness scripts.
   - Done: Sentry-specific tests pass; unrelated shared-tree blockers are documented in the summary.
