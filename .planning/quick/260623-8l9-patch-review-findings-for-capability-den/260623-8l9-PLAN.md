---
status: complete
quick_id: 260623-8l9
description: Patch review findings for capability denylist startup, network capture debugger ownership, handler schemas, and dev handler loading
---

# Quick Task 260623-8l9 Plan

Patch the four code-review findings without touching unrelated workspace changes.

## Task 1: Close capability security races

Files:
- extension/utils/service-denylist.js
- extension/utils/capability-router.js
- extension/background.js
- tests/service-denylist.test.js

Action:
- Make denylist loading idempotent and awaitable.
- Have the consent gate wait for denylist readiness before checking denied/sensitive origins.
- Keep the startup load as a cache warmup, not the load-bearing guarantee.

Verify:
- `node tests/service-denylist.test.js`

Done:
- Complete. Denylisted origins cannot pass the gate during service-worker startup.

## Task 2: Fix network capture debugger ownership

Files:
- extension/utils/network-capture.js
- tests/network-capture-consent.test.js

Action:
- Stop force-detaching another debugger owner on attach collision.
- Return the existing attach failure so discovery fails closed instead of leaking a debugger attachment.

Verify:
- `node tests/network-capture-consent.test.js`

Done:
- Complete. Attach collision does not call detach and does not leave a session active.

## Task 3: Add T1a parameter schemas and dev-load handlers

Files:
- catalog/descriptors/*.json
- extension/utils/capability-search.js
- extension/utils/capability-catalog.js
- extension/utils/capability-router.js
- catalog/handlers/*.js
- extension/catalog/handlers/*.js
- .gitignore
- tests/capability-search-eval.test.js
- tests/capability-head-handlers.test.js

Action:
- Add descriptor-level params schemas for handler-backed capabilities.
- Surface descriptor params in search hits when no declarative recipe exists.
- Validate handler args before dispatching T1a handlers.
- Track extension-root handler copies for unpacked dev loads.

Verify:
- `node tests/capability-search-eval.test.js`
- `node tests/capability-head-handlers.test.js`
- `node tests/capability-router.test.js`

Done:
- Complete. Handler-backed hits surface schemas, invalid handler args fail before dispatch, and tracked extension handler copies support unpacked dev loads.
