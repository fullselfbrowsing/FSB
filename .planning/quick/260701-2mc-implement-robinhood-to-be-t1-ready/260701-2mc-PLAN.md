---
quick_id: 260701-2mc
slug: implement-robinhood-to-be-t1-ready
status: complete
created: 2026-07-01
mode: quick
---

# Quick Task 260701-2mc: Implement Robinhood to be T1 ready

## Context

Robinhood descriptors are present in the catalog, but the service remains explicitly denylisted as brokerage/trading (`https://*.robinhood.com`). Preserve that policy: do not remove or weaken the denylist to inflate readiness metrics. Add the direct handler surface so Robinhood has reviewed T1 handler coverage, while runtime/readiness remains blocked by policy until that product/legal decision changes.

## Tasks

### 1. Add Robinhood handler

- files: `catalog/handlers/robinhood.js`, `extension/catalog/handlers/robinhood.js`
- action: Implement same-origin first-party API read handlers for Robinhood read descriptors, and guarded fail-closed handlers for watchlist mutations.
- verify: Handler module loads in Node, exports every Robinhood catalog slug, and write/destructive handlers return `RECIPE_DOM_FALLBACK_PENDING` without executing.
- done: complete

### 2. Wire handler registration and gates

- files: `extension/background.js`, `extension/utils/capability-catalog.js`, `scripts/report-t1-readiness.mjs`, `scripts/verify-t1-port-contract.mjs`, `tests/guarded-write-failclosed.test.js`
- action: Register/import the handler, include Robinhood in verifier mappings, and add Robinhood guarded-write slugs to fail-closed gates while keeping denied-origin status authoritative.
- verify: Readiness report shows Robinhood as blocked-policy despite handler proof; port contract and guarded-write tests pass.
- done: complete

### 3. Verify and summarize

- files: `.planning/STATE.md`, `.planning/quick/260701-2mc-implement-robinhood-to-be-t1-ready/260701-2mc-SUMMARY.md`
- action: Run targeted tests, record the policy blocker and outputs, update quick task state, and commit atomically.
- verify: Tests listed in SUMMARY.md match command output; final git commit contains only Robinhood-scoped implementation and GSD quick artifacts.
- done: complete
