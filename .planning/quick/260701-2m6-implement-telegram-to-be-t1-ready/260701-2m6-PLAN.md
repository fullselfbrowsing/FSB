---
quick_id: 260701-2m6
slug: implement-telegram-to-be-t1-ready
status: complete
created: 2026-07-01
---

# Quick Task 260701-2m6: Implement Telegram to be T1 ready

## Scope

Promote the existing Telegram catalog descriptors to the same T1 pattern used by
WhatsApp and Discord: read-only operations route through a same-origin Telegram
page-read handler, and mutation-capable operations are present but guarded
fail-closed until live mutation-body UAT exists.

## Tasks

1. Add Telegram T1 handler support
   - Files: `catalog/handlers/telegram.js`, `extension/catalog/handlers/telegram.js`,
     `extension/background.js`, `extension/utils/capability-catalog.js`,
     `extension/utils/capability-fetch.js`
   - Action: implement Telegram read handlers, guarded mutation handlers, bundled
     handler registration, and Telegram namespace support in the bounded page-read
     primitive.
   - Verify: handler module loads in Node and Telegram reads call only
     `executeBoundPageRead`; guarded mutations call no execution primitive.
   - Done: Telegram slugs resolve as T1a handler-backed entries.

2. Correct Telegram descriptor and readiness surfaces
   - Files: `catalog/descriptors/opentabs__telegram__*.json`,
     `extension/catalog/recipe-index.generated.js`,
     `scripts/report-t1-readiness.mjs`, `scripts/verify-t1-port-contract.mjs`
   - Action: mark Telegram mutation-capable descriptors as write/destructive,
     register their fail-closed readiness, and teach the port verifier about the
     Telegram handler.
   - Verify: T1 readiness and port-contract tests show Telegram reads as
     `t1-ready` and Telegram mutations as `t1-guarded-fail-closed`.
   - Done: no Telegram mutation is executable without UAT.

3. Add focused regression coverage
   - Files: `tests/capability-head-handlers.test.js`,
     `tests/head-handler-cap.test.js`, `tests/t1-readiness-report.test.js`
   - Action: add Telegram handler invariants and update exact head-handler
     manifest expectations.
   - Verify: run narrow tests covering handler behavior, readiness reporting,
     handler cap, and port contract.
   - Done: checks pass without touching unrelated app work.
