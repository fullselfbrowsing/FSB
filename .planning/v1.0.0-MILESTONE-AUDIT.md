---
milestone: v1.0.0
milestone_name: Full App Catalog (OpenTabs Parity)
audited: 2026-06-26
status: passed
scores:
  requirements: 17/17
  phases: 10/10
  integration: 5/5
  flows: 5/5
gaps: {}   # no critical blockers
tech_debt:
  - phase: 41-depth-2-remaining-hand-ports-guarded-writes
    items:
      - "Live guarded-write mutation-body UAT (41-HUMAN-UAT.md, 7 rows) — human_needed; the writes ship FAIL-CLOSED (inert) until live-captured. NON-blocking."
      - "Pattern-D cross-origin execution deferred (41-DEFERRAL.md): linear (client-api.linear.app) + datadog/jira (per-org subdomains) + supabase/cloud-consoles stay T3-DOM (discoverable + DOM-invocable); the Wall-2 cross-origin extension is a future user-blessed story."
  - phase: 42-discovery-seeding-tail-learn
    items:
      - "Live discovery first-visit capture UAT (42-HUMAN-UAT.md, 3 rows: linear/todoist/stripe) — human_needed; a hint never executes (the seed→T2 + promote + redactor are fixture-proven). NON-blocking."
  - phase: 43-catalog-scale-milestone-gate
    items:
      - "W-1: getOriginHealth (the degraded/needs-re-port accessor) is live-data-backed but has no production CONSUMER yet (no UI/search-hit reads it). The CONTEXT-permitted status-accessor form is satisfied; a user-visible reader is post-milestone."
      - "Search-precision open frontier: the breadth corpus-tier adversarial cross-app probes (~0.526 wrong-invoke) remain a RECORDED baseline (the 190-fixture SURF-06 eval is a HARD regression pin at 0.000; the broader adversarial paraphrase frontier is future work)."
  - phase: v2-deferred-families
    items:
      - "GAPI-01: Google Workspace gapi-bridge handler family (window.gapi.client.request trampoline) — deferred to v2 (REQUIREMENTS.md)."
      - "CLOUD-01: cloud-console Pattern-D ports (aws-console/azure/google-cloud/terraform-cloud) pending per-app CORS verification — v2."
      - "UATX-01: per-app live guarded-write UAT closeout across the depth tier — v2 (mirrors the v0.9.99 live-UAT posture)."
  - phase: process
    items:
      - "9/10 v1.0.0 VALIDATION.md files carry a stale `nyquist_compliant: false` frontmatter flag (only Phase 37 was flipped to true). Coverage IS present — every phase has a VALIDATION.md with the per-SC automated-verify map and passed verification green — only the draft flag was never flipped. A doc-flag gap, not a coverage gap."
nyquist:
  compliant_phases: ["37"]
  partial_phases: ["35","36","38","39","39.5","40","41","42","43"]
  missing_phases: []
  overall: partial   # all phases HAVE a VALIDATION.md + passed verification; the nyquist_compliant flag is stale on 9 (coverage present)
---

# v1.0.0 — Full App Catalog (OpenTabs Parity) — Milestone Audit

**Status: PASSED.** All 17 requirements satisfied (3-source cross-referenced), all 10 phases verified, cross-phase integration 5/5 wired end-to-end, and the milestone gate (`full npm test EXIT 0`) is met in the real working tree. No critical gaps. Accumulated tech debt is all NON-blocking (human-needed live UAT on fail-closed/inert surfaces, explicitly-deferred v2 families, a post-milestone UI follow-up, and a stale doc-flag).

## Definition of Done — assessment

**Goal:** scale the v0.9.99 capability path from a 4-service head to the full ~119-app OpenTabs surface by feeding the existing tiers (breadth = closed-vocab descriptors as data; depth = hand-ported handlers; tail = seeded discovery + DOM fallback) — no new MCP tool, no new router branch, INV-01..04 + Walls 1/2 preserved.

**Achieved:**
- **Breadth:** the full real pinned-SHA OpenTabs source imported — **~129 apps / 2,306 opentabs descriptors** (+ heads/recipes = 2,314 total), all returned by `search_capabilities` with synonyms + side-effect class + backing-status. (The original "~2,523-op" was an upper estimate of the raw plugin surface; the shipped catalog is the real source MINUS the security denylist (netflix/spotify/brokerage/etc. never imported) and the self-hosted skips (grafana/sqlpad) and the 2 CI fixtures — i.e. full parity of the allowed+importable surface at the SHA, by design, not a shortfall.)
- **Depth:** 21 T1a heads hand-ported (github/slack/notion/gitlab) — top READ ops (Phase 40) + guarded WRITE ops (Phase 41, fail-closed) — via the `github.js` contract, first-party origin, executeBoundSpec-only, each upgrading its breadth descriptor dom→T1a.
- **Tail:** discovery seeded (129 origins / 255 endpoint hints) into the consent-gated Phase-31 capture→learn→promote path; the structural redactor verified no-leak across the 119-app field universe.
- **Scale + gate:** full-corpus index 1.372MB / cold-start ~11ms / 621.7B-flat; the SURF-06 eval wrong-invoke driven to 0.000 (HARD regression pin); recipe-rot self-heal hardened + wired; INV-03 7-provider byte-equal; **full npm test EXIT 0** — the milestone gate.
- **Invariants:** INV-01 (frozen MCP registry / catalog IIFE-djb2), INV-02 (single router.invoke, both front doors), INV-03 (typed reasons byte-equal), INV-04 (agent-loop untouched), Wall-1 (no remotely-hosted code; recipe-path-guard green), Wall-2 (active-tab origin-pin) — ALL preserved + guard-tested.

The milestone delivers its core value — **reliable single-attempt execution at OpenTabs scale** — with the security walls intact.

## Requirements coverage (17/17 satisfied — 3-source cross-referenced)

Each REQ-ID verified across REQUIREMENTS.md traceability (`[x]` + Complete), the assigned phase's VERIFICATION.md (status: passed), and the SUMMARY frontmatter (requirements-completed).

| REQ | Phase | VERIFICATION | Traceability | Status |
|-----|-------|--------------|--------------|--------|
| DENY-01..04 | 35 | passed | [x] Complete | **satisfied** |
| CGEN-01..04 | 36 | passed | [x] Complete | **satisfied** |
| BRDTH-01..03 | 37 (cont. 38/39/39.5) | passed | [x] Complete | **satisfied** |
| DEPTH-01 | 40 | passed | [x] Complete | **satisfied** |
| DEPTH-02 | 41 | passed | [x] Complete | **satisfied** |
| DSEED-01..02 | 42 | passed | [x] Complete | **satisfied** |
| SCALE-01..02 | 43 | passed | [x] Complete | **satisfied** |

No orphaned requirements (every REQ-ID appears in its phase VERIFICATION). No unsatisfied requirements → no FAIL-gate trip.

## Phases (10/10 verified passed)

| Phase | Name | Verify | Note |
|-------|------|--------|------|
| 35 | Denylist + Import-Time Classification Gate | passed | the security floor, lands first |
| 36 | Codegen Pipeline + No-Dead-Entry Resolution | passed | the load-bearing pipeline |
| 37 | Breadth A — Dev/Productivity | passed | the BRDTH contract |
| 38 | Breadth B — Comms/Social/Content | passed | sensitivity-screened |
| 39 | Breadth C — Commerce/Travel/Misc | passed | payment-screening machinery |
| 39.5 | Full OpenTabs Source Import [INSERTED] | passed | true parity — 2,306 real descriptors |
| 40 | Depth 1 — Top READ Hand-Ports | passed | 10 T1a reads, dom→T1a |
| 41 | Depth 2 — Guarded Writes + CORS-gate | passed | fail-closed writes, the CORS gate |
| 42 | Discovery Seeding + Tail Learn | passed | seeds + the 119-app no-leak redactor |
| 43 | Catalog-Scale + Milestone Gate | passed | scale + precision + self-heal + the gate |

## Cross-phase integration (5/5 WIRED — gsd-integration-checker)

| Flow | Status |
|------|--------|
| search → resolve → invoke (one catalog, 2,314 descriptors, 0 resolve null) | WIRED |
| consent gate BEFORE tier dispatch (DENY-04 posture B) | WIRED |
| tier dispatch — both front doors → SAME router.invoke (INV-02) | WIRED |
| discovery → learn → upgrade + rot self-heal (scheduler wired) | WIRED |
| the guards chain (classifyGate → validate:extension → runtime gate) | WIRED |

INV-02 (single router, no parallel stack) + INV-03 (typed reasons byte-equal across 7 providers) both HOLD. No broken flows, no dead ends, no cross-phase wiring gap.

## Tech debt (NON-blocking — see frontmatter for the full list)

All deferred items ship behind a fail-closed/inert surface or are explicitly v2/post-milestone:
- **Live UAT** (guarded-write bodies 41-HUMAN-UAT.md + discovery first-visit 42-HUMAN-UAT.md) — irreducibly needs a logged-in browser; the mechanisms are fixture-proven + the surfaces ship fail-closed.
- **Pattern-D cross-origin execution** (41-DEFERRAL.md) — separate-origin apps demoted to T3-DOM (still usable); the Wall-2 extension is a future user-blessed story.
- **W-1** — getOriginHealth has no UI consumer yet (status-accessor satisfied).
- **Search-precision open frontier** — the corpus-tier adversarial probes (~0.526) are a recorded baseline; the 190-fixture eval is a HARD pin at 0.
- **v2 families** — GAPI-01, CLOUD-01, UATX-01.
- **Process** — 9/10 VALIDATION.md `nyquist_compliant` flags are stale (coverage present, flag not flipped).

## Nyquist coverage

| Status | Phases |
|--------|--------|
| COMPLIANT | 37 |
| PARTIAL (VALIDATION.md present + verified green; `nyquist_compliant` flag stale) | 35, 36, 38, 39, 39.5, 40, 41, 42, 43 |
| MISSING | (none) |

Every phase has a VALIDATION.md with a per-SC automated-verify map and passed its goal verification with all gates green; the `nyquist_compliant: false` flag on 9 phases is a stale draft-default that was never flipped (a doc-flag gap, not a coverage gap). Optional: run `/gsd-validate-phase <N>` to flip the flags retroactively — does not change any shipped behavior.

## Verdict

**v1.0.0 PASSED.** Ready to complete the milestone (archive + tag). The tech debt is tracked + non-blocking; recommend carrying the live-UAT + v2 families into the backlog.
