# Requirements: FSB v0.10.0 Autopilot via Lattice SDK (attempt 2)

**Milestone:** v0.10.0 Autopilot via Lattice SDK (attempt 2)
**Branch:** `automation` (reset to merge-base with `main` on 2026-05-24)
**Customer:** FSB Chrome extension's autopilot loop (driven by Sidepanel UI and autopilot mode). The improvements are for the IN-EXTENSION agent loop; the same tool surface that MCP exposes is reused -- no parallel tool stacks.
**SDK direction:** [Lattice](https://github.com/LakshmanTurlapati/Lattice) -- in this attempt, FSB consumes Lattice as a path: dependency. Lattice is cloned at `./lattice/` (gitignored) on branch `fsb-integration-experiments`. SDK extensions land in Lattice's repo first; FSB integration glue lands in FSB's repo.
**Source-of-truth research:** TBD (to be regenerated for the Lattice-first approach via `/gsd-discuss-phase` + research synthesis)
**Phase numbering:** Reset to **Phase 1** for v0.10.0-attempt-2.
**Pivot context:** `.planning/milestones/v0.10.0-attempt-1-pre-pivot/PIVOT-v0.10.0-PLAN.md`

**Last updated:** 2026-05-27 — Phase 6 FINT-07 + FINT-08 completed (Plans 06-01 through 06-04; offscreen handler + SW startup wiring + bridge shim + agent-loop swap + options.js test-connection rewrite + saveSettings trim defense; closes xai-key-rejected-400 P1+P2 + audit gap G3 by side effect). Original scope-extension entry from earlier today preserved in commit history (commit `c17e262a`).

---

## Goal

Improve FSB's task completion reliability by adopting [Lattice](https://github.com/LakshmanTurlapati/Lattice) as the autopilot engine's underlying SDK. Build the runtime primitives (state envelopes, tripwire safety contracts, observability primitives, delegation primitive, MV3-survivability adapters) inside Lattice on the `fsb-integration-experiments` branch, then have FSB's autopilot engine consume Lattice as a thin runtime adapter. The Lattice round-trip validates continuously during development rather than being deferred to an end-of-milestone smoke test.

The framing is still outcome-first: "whatever makes FSB do tasks better." Lattice is a means to that end. The pivot vs attempt-1 is that the primitives live in the SDK (Lattice) rather than in the consumer (FSB).

---

## Hard Invariants (NON-NEGOTIABLE -- gate every requirement)

- **INV-01 MCP wire contracts UNTOUCHED.** Every existing MCP-exposed tool's schema, semantics, and request/response shape stays byte-identical. Verified by `tests/tool-definitions-parity.test.js` between `extension/ai/tool-definitions.js` and `mcp/ai/tool-definitions.cjs`. Net-new tools (if any) must land byte-identically to BOTH registries in the SAME phase commit.
- **INV-02 Tool surface parity.** FSB's autopilot loop uses the SAME tool registry that MCP exposes (`TOOL_REGISTRY` in `extension/ai/tool-definitions.js`). No parallel "autopilot-only" tool stack. Any tool the autopilot uses, MCP clients can also use.
- **INV-03 Provider parity (strengthened 2026-05-27 — Phase 6/7 scope).** Through Phase 5: every improvement worked equally across all 7 `extension/ai/universal-provider.js` targets (Anthropic, OpenAI, xAI, Gemini, LM Studio, OpenRouter, custom OpenAI-compatible) and no improvement regressed any single provider. From Phase 6 onward: FSB consumes Lattice's 7 provider adapters exclusively for the runtime path via the offscreen Lattice host bus (Phase 5 FINT-04). From Phase 7 end onward: `extension/ai/universal-provider.js` archived to `extension/_archive/`; bridge becomes unconditional. Verified continuously by `tests/lattice-provider-bridge-smoke.test.js` across all 7 logical providers.
- **INV-04 MV3-survivability preserved.** The existing `setTimeout`-chained iterator pattern (`agent-loop.js:1824/2418/2487/2497`) is load-bearing for Chrome's 30s-silence + 5-min idle kill. No refactor that replaces this iterator pattern -- Lattice integration is additive runtime adaptation, not iterator replacement.
- **INV-05 No resurrection of deprecated modules.** `extension/agents/agent-executor.js` / `agent-manager.js` / `agent-scheduler.js` (DEPRECATED v0.9.45rc1, superseded by OpenClaw / Claude Routines) stay frozen. New code lands in fresh paths.
- **INV-06 (NEW) Lattice SDK primitives live in Lattice's repo, not in FSB's.** Any runtime primitive used by both FSB's autopilot and any potential future Lattice consumer goes into Lattice on `fsb-integration-experiments`. FSB-side code is integration glue (path: dependency wiring, MV3 runtime adapters, sidepanel UI) and never re-implements primitives that belong in Lattice.

---

## v1 Requirements (this milestone -- HIGH-LEVEL SCAFFOLD)

Detailed REQ-IDs are populated during phase planning (`/gsd-discuss-phase` + `/gsd-plan-phase`). The categories below sketch the scope.

### LSDK -- Lattice SDK Extensions (work in `./lattice` on `fsb-integration-experiments` branch)

Goal: extend Lattice's v1.1 Capability Receipts foundation with the primitives FSB's autopilot needs.

- [x] **LSDK-01..N (audit) -- DONE 2026-05-24 (Phase 01 Plan 01-01):** Lattice SDK gap survey landed at `lattice/docs/fsb-integration-gaps.md` (91 lines, 6 domain headers, 21 severity-tagged rows: Receipts=2, Tripwires/hooks=4, Providers=5, Delegation=1, MV3-survivability=2, Observability/step-markers=4). Severity tags drive Phase 2+ ordering (Blocker > Important > Nice-to-have). Lattice commit `195e5ae`. Concrete LSDK-NN REQ-IDs for each row are populated in Phase 2 setup (deferred per CONTEXT.md `<deferred>` block). Phase 01 Plan 01-01 also re-exported `createReceipt` + `CreateReceiptInput` from Lattice's bare specifier (commit `ab6c1f6`), and Plan 01-02 added a packaging fix (catalog: -> literals) so npm 11 can install via `file:` (Lattice commit `22bf986`, user-authorized D-13 expansion).
- [x] **LSDK-02 -- DONE 2026-05-24 (Phase 02 Plan 02-01):** CapabilityReceiptBody extended with 5 step-transition fields (`stepName`, `stepIndex`, `parentStepName`, `previousStepName`, `timestamp`) -- audit-doc Receipts row 2 covered. Fields are flat optional readonly top-level fields; v1 callers leave them undefined (backward compatible). Lattice commit `5c48134` on `fsb-integration-experiments` (NOT pushed -- D-15).
- [x] **LSDK-03 -- DONE 2026-05-24 (Phase 02 Plan 02-01):** CapabilityReceiptBody extended with `sessionId` field + schema version literal bumped to discriminated union `"lattice-receipt/v1" | "lattice-receipt/v1.1"` -- audit-doc Receipts row 3 covered. `createReceipt` applies a version-bump heuristic (any of the 6 step-marker fields set -> v1.1; otherwise v1). `verify.ts:asReceiptBody` accepts both literals; unknown literals still return `version-mismatch`. JCS canonical (canonical.ts) and redaction manifest (redact.ts) both BYTE-FROZEN. Lattice commit `5c48134`.
- [x] **LSDK-04 -- DONE 2026-05-24 (Phase 02 Plan 02-02):** Priority bands enum (`SAFETY=0` > `OBSERVABILITY=1` > `EXTENSION=2`) with lower-numbered-band-runs-first invariant + within-band registration-order preservation -- audit-doc Tripwires/hooks row 2 covered. New module `lattice/packages/lattice/src/contract/bands.ts` ships `createHookPipeline()` factory + `BAND` const + `BAND_ORDER`. The pure `evaluateTripwires` at `tripwire.ts:53-86` BYTE-FROZEN (D-06; bands.ts is a SIBLING primitive). Lattice commit `ba6172c` on `fsb-integration-experiments` (NOT pushed -- D-15).
- [x] **LSDK-05 -- DONE 2026-05-24 (Phase 02 Plan 02-02):** Per-handler matcher regex (opt-in via `RegisterOptions.matcher`) + race-with-log per-handler budget (default 100ms; configurable via `RegisterOptions.budgetMs`; HOOK_TIMEOUT emitted via `TracerLike.event?.()` on timeout with stable-identifier payload `{event, band, budgetMs, sessionId?, handlerIndex, elapsedMs}`) -- audit-doc Tripwires/hooks row 3 covered. No-abort `Promise.race` pattern (handler keeps running after timeout; pipeline continues to next handler). CD-01 RESOLVED. Lattice commit `ba6172c`.
- [x] **LSDK-06 -- DONE 2026-05-24 (Phase 02 Plan 02-02):** Frozen handler context -- each handler receives a `structuredClone` + `Object.freeze` view of the caller's context; mutations on the handler side do NOT leak back to the calling site -- audit-doc Tripwires/hooks row 4 covered. `freezeContext()` helper with try/catch fallback for unclonable inputs. Lattice commit `ba6172c`.
- [x] **LSDK-07 -- DONE 2026-05-24 (Phase 02 Plan 02-02):** Irreversible mid-session registration freeze -- `pipeline.freeze()` flips a boolean; subsequent `register()` throws `Error` with `name === "PIPELINE_FROZEN"` (constant `PIPELINE_FROZEN_ERROR_NAME`). `freeze()` is idempotent; `run()` still works after freeze -- audit-doc Tripwires/hooks row 5 covered. Lattice commit `ba6172c`.
- [x] **LSDK-08 -- DONE 2026-05-24 (Phase 02 Plan 02-02):** `HookLifecycleEvent` typed literal-union (`"BEFORE_PROVIDER" | "AFTER_PROVIDER" | "BEFORE_TOOL" | "AFTER_TOOL"`) shipping in `bands.ts` -- intentionally SEPARATE from `tracing.ts`'s `RunEventKind` (D-12). Run events describe Lattice runtime stages; lifecycle events describe pluggable hook attach-points. `tracing.ts` BYTE-FROZEN -- audit-doc additive-lifecycle row covered. Lattice commit `ba6172c`.
- [x] **LSDK-09 -- DONE 2026-05-24 (Phase 03 Plan 03-01 + Plan 03-02):** `"step.transition"` literal added as the 17th and final entry in Lattice's `RunEventKind` union at `lattice/packages/lattice/src/tracing/tracing.ts`. Dotted-namespace sibling of run.* / stage.* / provider.* / tool.* / replay.*. `RunEvent` interface BYTE-UNCHANGED (step fields ride in `metadata?: Record<string, unknown>` per D-03). Audit-doc Observability row 2 ("step.transition event kind + step.* sub-events") flipped Blocker -> Covered. Lattice commits `fd254c4` (tracing literal) + `acdbb8a` (public-surface re-export of `STEP_TRANSITION_EVENT_NAME` constant from `./contract/checkpoint.js`) + `7afd62f` (audit-doc closure).
- [x] **LSDK-10 -- DONE 2026-05-24 (Phase 03 Plan 03-01 + Plan 03-02):** `createCheckpointHook(options)` factory shipped at new sibling module `lattice/packages/lattice/src/contract/checkpoint.ts` (260 lines + 240-line test file with 15 vitest cases). Factory returns `HookHandler<CheckpointHookContext>`; caller registers on Phase 2's `HookPipeline` (typically `band: BAND.OBSERVABILITY`). Per invocation: emits exactly one `step.transition` tracer event AND (when signer provided) mints exactly one v1.1 Capability Receipt with step-marker fields populated -- the envelope IS the inspector record. Best-effort mint (D-07): signer failures absorbed via try/catch, surfaced as `metadata.mintError`, no upstream throw. Audit-doc Observability row 3 ("Inspector envelope shape that Lattice can sign as a Capability Receipt directly") flipped Blocker -> Covered. Lattice commits `a67f476` (factory + tests) + `acdbb8a` (public-surface re-export of `createCheckpointHook` + `DEFAULT_CHECKPOINT_BAND` + `CheckpointHookContext` + `CheckpointHookOptions`) + `7afd62f` (audit-doc closure).
- [x] **LSDK-11 -- DONE 2026-05-24 (Phase 03 Plan 03-01 + Plan 03-02):** `STEP_TRANSITION_EVENT_NAME = "step.transition"` constant + `DEFAULT_CHECKPOINT_BAND = BAND.OBSERVABILITY` constant exported from `./contract/checkpoint.ts` for caller clarity at the registration site (D-06 documented convention; factory does NOT auto-register). Re-exported via Lattice's flat public surface at `src/index.ts` so FSB consumes via `lattice.STEP_TRANSITION_EVENT_NAME` + `lattice.DEFAULT_CHECKPOINT_BAND`. Lattice commits `a67f476` + `acdbb8a`.
- [x] **LSDK-12 -- DONE 2026-05-24 (Phase 03 Plan 03-01 + Plan 03-02):** `CheckpointHookContext` interface (`stepName`, `stepIndex`, optional `parentStepName`, optional `previousStepName`, `timestamp` -- all readonly stable identifiers per D-04 carryforward) + `CheckpointHookOptions` interface (`runId` required; optional `tracer`, `signer`, `sessionId`, `model`, `route`, `contractVerdict`) exported from `./contract/checkpoint.ts` and re-exported from the public surface as type-only exports. Lattice commits `a67f476` + `acdbb8a`.
- [x] **LSDK-13 -- DONE 2026-05-24 (Phase 03 Plan 03-01):** TDD coverage for the checkpoint factory -- `checkpoint.test.ts` with 15 vitest cases across 7 describe blocks (factory identity, tracer-only mode, signer mode mint+verify round-trip, best-effort mint signer-throws, 3-call linked-list threading, HookPipeline integration, tracer-absent mint-still-works). Real signer (`createInMemorySigner` over `generateEd25519KeyPairJwk` ephemeral keypair) and real verifier (`verifyReceipt` + `createMemoryKeySet`) -- no static-text grep, no mock-only signatures. Lattice suite advanced 332 -> 347 PASS; baseline preserved through Plan 03-02 (re-export adds zero tests). Lattice commit `a67f476`.
- [x] **LSDK-14 -- DONE 2026-05-24 (Phase 04 Plan 04-01 + Plan 04-04 + Plan 04-05):** Anthropic native provider adapter shipped at `lattice/packages/lattice/src/providers/anthropic.ts` -- full custom adapter for `/v1/messages` API (top-level `system` field + `content[0].text` response shape) mirroring FSB `extension/ai/universal-provider.js:280-297` (request) + `:566-573` (response/usage). 9 vitest cases per D-09 contract. `x-api-key` + `anthropic-version: 2023-06-01` headers; `input_tokens`/`output_tokens` usage extraction (NOT prompt/completion); AbortSignal wired via spread pattern per D-05; non-OK throws `Error("Anthropic provider failed with ${status}.")`. Streaming + prompt caching + tool use deferred. Re-exported from `src/index.ts` (`createAnthropicProvider` + type `AnthropicProviderOptions`). Audit-doc Providers row (Blocker) flipped to Covered. Lattice commits `cf31d82` (adapter + tests) + `e5659a8` (public surface re-export) + `f9c7ef4` (INV-03 parity smoke) + `f1c943b` (audit-doc closure). FSB-side smoke `tests/lattice-providers-smoke.test.js` exercises end-to-end.
- [x] **LSDK-15 -- DONE 2026-05-24 (Phase 04 Plan 04-02 + Plan 04-04 + Plan 04-05):** Gemini native provider adapter shipped at `lattice/packages/lattice/src/providers/gemini.ts` -- full custom adapter for `/v1beta/models/{model}:generateContent` API (`contents[].parts[].text` request shape, `candidates[0].content.parts[0].text` response, `usageMetadata.promptTokenCount`/`candidatesTokenCount` usage, 4 HARM_CATEGORY safetySettings at BLOCK_NONE per FSB convention, `?key=` query-string auth, role `user`/`model` preserved per D-07) mirroring FSB `extension/ai/universal-provider.js:210-274` + `:551-563`. 10 vitest cases per D-09 contract + 3 Gemini-specific cases (safetySettings, query-string auth, role mapping). Missing-candidates throws `Error("Gemini provider returned no candidates.")`. Streaming + multimodal (vision) deferred. Re-exported from `src/index.ts` (`createGeminiProvider` + type `GeminiProviderOptions`). Audit-doc Providers row (Blocker) flipped to Covered. Lattice commits `7a32b00` (adapter + tests) + `e5659a8` (public surface re-export) + `f9c7ef4` (INV-03 parity smoke) + `f1c943b` (audit-doc closure).
- [x] **LSDK-16 -- DONE 2026-05-24 (Phase 04 Plan 04-03 + Plan 04-04 + Plan 04-05):** xAI native provider adapter shipped at `lattice/packages/lattice/src/providers/xai.ts` -- thin wrapper around `createOpenAICompatibleProvider` pinned to `https://api.x.ai/v1`. PRESERVES xAI's `completion_tokens_details.reasoning_tokens` quirk per D-07: legacy `UsageRecord.totalTokens` recomputed to INCLUDE `reasoning_tokens` when present (matches FSB `extension/ai/universal-provider.js:585-594` production behavior). Phase 7 normalized `Usage` (promptTokens/completionTokens/costUsd) unchanged -- represents billable tokens only; reasoning_tokens accessible via `response.rawResponse.usage.completion_tokens_details.reasoning_tokens`. 9 vitest cases including the explicit `reasoning_tokens` quirk verification (Test 4b). Tool-streaming deferred. Re-exported from `src/index.ts` (`createXaiProvider` + type `XaiProviderOptions`). Audit-doc Providers row (Important) flipped to Covered. Lattice commits `09a495e` (adapter + tests) + `e5659a8` (public surface re-export) + `f9c7ef4` (INV-03 parity smoke) + `f1c943b` (audit-doc closure).
- [x] **LSDK-17 -- DONE 2026-05-24 (Phase 04 Plan 04-03 + Plan 04-04 + Plan 04-05):** OpenRouter native provider adapter shipped at `lattice/packages/lattice/src/providers/openrouter.ts` -- thin wrapper around `createOpenAICompatibleProvider` pinned to `https://openrouter.ai/api/v1`. First-class named adapter for ceremony parity. Model-routing array / fallback-array DEFERRED per D-17 carryforward (caller supplies single model id; multi-model fallback is a follow-on phase; JSDoc on factory documents the carryforward). 7 vitest cases per D-09 contract. Streaming deferred. Re-exported from `src/index.ts` (`createOpenRouterProvider` + type `OpenRouterProviderOptions`). Audit-doc Providers row (Nice-to-have) flipped to Covered. Lattice commits `1cfc13c` (adapter + tests) + `e5659a8` (public surface re-export) + `f9c7ef4` (INV-03 parity smoke) + `f1c943b` (audit-doc closure).
- [x] **LSDK-18 -- DONE 2026-05-24 (Phase 04 Plan 04-03 + Plan 04-04 + Plan 04-05):** LM Studio native provider adapter shipped at `lattice/packages/lattice/src/providers/lm-studio.ts` -- thin wrapper around `createOpenAICompatibleProvider` pinned to `http://localhost:1234/v1`. `apiKey` is OPTIONAL (CD-03: LM Studio is no-auth by convention; no opt-out flag); when omitted, the underlying factory's conditional at `adapters.ts:53` sends no `Authorization` header. Latency-tail diagnostics DEFERRED per D-16 carryforward (LM Studio IS the named INV-03 latency canary, but the diagnostics module belongs in a follow-on observability phase; JSDoc on factory + audit-doc row note both document the carryforward). 8 vitest cases per D-09 contract + the explicit no-Authorization-by-default verification. Streaming deferred. Re-exported from `src/index.ts` (`createLmStudioProvider` + type `LmStudioProviderOptions`). Audit-doc Providers row (Important) flipped to Covered. Lattice commits `40457ff` (adapter + tests) + `e5659a8` (public surface re-export) + `f9c7ef4` (INV-03 parity smoke) + `f1c943b` (audit-doc closure).
- [x] **LSDK-19 -- DONE 2026-05-24 (Phase 05 Plan 05-02 + Plan 05-03):** `SurvivabilityAdapter<TState>` interface + ResumePolicy + SerializedSnapshot + EvictionHook + UnsubscribeFn shipped at `lattice/packages/lattice/src/runtime/survivability.ts`. 4 contract methods (serialize, deserialize, onEviction, resume). Documented interface for runtimes whose execution context can be evicted mid-flow (Chrome MV3 SW, Cloudflare Workers, AWS Lambda). ResumePolicy literal-union: `SAFE | RECOVERY_AMBIGUOUS | ON_ERROR_SW_EVICTION_MID_REQUEST | ON_ERROR_SW_EVICTION_MID_TOOL_DISPATCH` (CD-E carries forward FSB attempt-1 02-04-PLAN.md CONSERVATIVE recovery taxonomy). Composition conventions documented in JSDoc (not enforced): D-09 onEviction hooks SHOULD register in BAND.SAFETY; D-10 SerializedSnapshot.payload MAY embed v1.1 ReceiptEnvelope. CONSERVATIVE recovery dispatcher EXPLICITLY OUT OF SCOPE per CONTEXT.md D-22. Lattice commit `a4609bc` (interface + ref impl) + `109d6ae` (public surface re-export) + `e95067b` (audit-doc closure).
- [x] **LSDK-20 -- DONE 2026-05-24 (Phase 05 Plan 05-02):** `createNoopSurvivabilityAdapter()` reference implementation shipped at `lattice/packages/lattice/src/runtime/survivability.ts` (244 lines including the interface). Analog to `createFakeProvider` -- records eviction events via Set<EvictionHook> + round-trips state via JSON.stringify/parse; default resume policy = `SAFE`. Companion `survivability.test.ts` (265 lines) ships 17 vitest cases covering factory identity, serialize/deserialize round-trip, onEviction lifecycle, resume() ResumePolicy contract, type-level discriminated union, integration with real v1.1 Capability Receipt envelope (Test 12 exercises DSSE+JCS round-trip with ephemeral Ed25519 keypair from `generateEd25519KeyPairJwk()` + `createInMemorySigner` + `verifyReceipt` + `createMemoryKeySet`). Lattice vitest 397 -> 414 PASS / 0 FAIL. Lattice commit `a4609bc`.
- [x] **LSDK-21 -- DONE 2026-05-24 (Phase 05 Plan 05-03):** Public-surface re-export at `lattice/packages/lattice/src/index.ts` -- 1 value export (`createNoopSurvivabilityAdapter`) + 5 type-only re-exports (`SurvivabilityAdapter`, `SerializedSnapshot`, `EvictionHook`, `ResumePolicy`, `UnsubscribeFn`) from `./runtime/survivability.js`. Bare-specifier `await import('lattice')` from FSB root resolves `createNoopSurvivabilityAdapter` as `function`; 11 Phase 1-4 carryforward exports preserved. dist/ rebuilt locally via tsdown clean:true. Lattice commit `109d6ae`.
- [x] **LSDK-22 -- DONE 2026-05-24 (Phase 05 Plan 05-03):** Audit-doc closure at `lattice/docs/fsb-integration-gaps.md` -- 2 MV3-survivability Blocker rows flipped to `Covered` with backlink SHAs ("Adapter contract for runtimes whose execution context can be evicted mid-flow" + "Cross-process resumption from persisted state envelope"). Phase 1-4 Covered rows preserved untouched. After this commit: 7 of 8 audit-doc Blocker rows closed across Phases 1-5; 1 Out-of-scope (Delegation primitive contingent on Lattice multi-agent policy; Phase 6 if Lattice opens). Lattice commit `e95067b`.
- [ ] **LSDK-LL..P (TBD):** Task-delegation primitive shape (if Lattice opens multi-agent policy; otherwise design as a Lattice-adjacent FSB-side primitive that consumes Lattice's receipt + tripwire surface).

### FINT -- FSB Integration Layer (work in `automation` branch)

Goal: wire FSB's autopilot engine to Lattice as a path: dependency.

- [x] **FINT-01 (path:dep wiring + Node smoke) -- DONE 2026-05-24 (Phase 01 Plan 01-02):** `"lattice": "file:./lattice/packages/lattice"` landed in `package.json` (line 81); `node_modules/lattice` resolves as a working symlink; `tests/lattice-smoke.test.js` appended to `scripts.test` chain and exercises the FSB <-> Lattice round-trip via dynamic `await import('lattice')` from a CJS test file (29 PASS / 0 FAIL). Real-runtime invocation of `lattice.createReceipt` + `lattice.verifyReceipt` with an ephemeral Ed25519 keypair per run (no committed key material). MV3 SW + sidepanel in-extension load DEFERRED to a future bundler-aware phase per Option B reconciliation (no `extension/*` modifications in Phase 1). Manual MV3 sanity reload (D-12 #3 AMENDED) DEFERRED-PENDING-UAT per user directive. FSB commits: `658ed87e`, `1545c14c`, `be95d158`. Cross-repo audit trail at `.planning/LATTICE-PIN.md`.
- [x] **FINT-03 (esbuild bundler infrastructure) -- DONE 2026-05-24 (Phase 05 Plan 05-01):** Per-entrypoint esbuild bundler config at `esbuild.config.js` (~140 lines) -- offscreen + content-script + sidepanel entries -- emits to `extension/dist/` (CD-A: single tree). `npm run build` invokes (D-05). esbuild ^0.24.0 in devDependencies (resolved to 0.24.2; no plugins per Phase 5 threat-model row 1). Sourcemap strategy per D-04: external .js.map for offscreen + sidepanel; sourcemaps disabled for content-script bundles (lean injection payload). Bundler-installed-not-wired per D-06: ZERO `manifest.json` change in Plan 05-01; ZERO behavior diff (production still loads classic `extension/background.js` + 153 importScripts chain unchanged). `extension/dist/` added to `.gitignore`. FSB commit `fbbe02d5`.
- [x] **FINT-04 (hybrid offscreen Lattice host) -- DONE 2026-05-24 (Phase 05 Plan 05-04):** FIRST in-extension Lattice consumption surface in this milestone (Option B carryforward from Phase 1 ends here). New offscreen document `extension/offscreen/lattice-host.html` declaring `<script type="module" src="lattice-host.js">` + ESM bundle-input `extension/offscreen/lattice-host.js` (~230 lines). Imports `createCheckpointHook` + `createHookPipeline` + `createInMemorySigner` + `generateEd25519KeyPairJwk` + `createNoopSurvivabilityAdapter` + 2 constants from the `'lattice'` bare specifier. Listens on `chrome.runtime.onMessage` for `{type: 'lattice-step-transition'}` envelopes per D-16; composes Phase 2 `createHookPipeline` + Phase 3 `createCheckpointHook` to mint v1.1 receipts; posts back `{type: 'lattice-receipt-minted'}` (or `{type: 'lattice-receipt-mint-failed'}` per D-07 best-effort mint). Cross-extension messages rejected via `sender.id === chrome.runtime.id` check. `extension/manifest.json` appends `'offscreen/lattice-host.html'` to `web_accessible_resources.resources` (CD-C); existing `"offscreen"` permission reused. `background.js` BYTE-FROZEN per D-17 (153 importScripts chain untouched). Bundler emits `extension/dist/offscreen/lattice-host.js` (ESM, 64870 bytes) with the `'lattice'` bare specifier rewritten (zero unresolved references in dist bundle). esbuild external list added for node:* builtins (Lattice dist top-level imports artifact-storage submodule that our surface does not reach; tree-shaken at bundle time). FSB commit `8ab0c6df`.
- [x] **FINT-05 (FSB standalone MV3-survivability adapter) -- DONE 2026-05-24 (Phase 05 Plan 05-05):** Standalone module `extension/ai/lattice-runtime-adapter.js` (272 lines; CJS-compatible for classic SW importScripts + Node tests) implementing Lattice's `SurvivabilityAdapter<TState>` contract over `chrome.storage.session`. 4 contract methods (serialize, deserialize, onEviction, resume) + FSB-specific `fireEvictionHooks(state)` helper. Per D-19 STANDALONE: does NOT modify `agent-loop.js`, `runAgentLoop`, or any existing iterator (INV-04 setTimeout iterator at lines 1824/2418/2487/2497 stays byte-frozen). Per D-20 + CD-D feature-flag gated: `globalThis.FSB_LATTICE_RUNTIME_ADAPTER_ENABLED` defaults `false`; uniform check pattern `if (typeof FSB_LATTICE_RUNTIME_ADAPTER_ENABLED !== "undefined" && FSB_LATTICE_RUNTIME_ADAPTER_ENABLED) { ... }` gates every production write to `chrome.storage.session`. ResumePolicy dispatch over FSB attempt-1 marker vocabulary (CD-E): `BEFORE_API_REQUEST` -> `ON_ERROR_SW_EVICTION_MID_REQUEST`; `BEFORE_TOOL_EXECUTION` -> `ON_ERROR_SW_EVICTION_MID_TOOL_DISPATCH`; `BEFORE_ITERATION` / `BEFORE_NEXT_ITERATION_SCHEDULE` / undefined -> `SAFE`; unknown -> `RECOVERY_AMBIGUOUS`. LRU bound documented in JSDoc (default cap = 50 per sessionId); enforcement deferred per threat-model row 3 (Phase 5 is contract-shipping; LRU is follow-on). CONSERVATIVE recovery dispatcher (the actual rewiring into runAgentLoop's restored-mode branches) EXPLICITLY OUT OF SCOPE per D-22; deferred to follow-on milestone. FSB commit `e1d9f491`.
- [x] **FINT-06 (Node-side survivability smoke + ceremony) -- DONE 2026-05-24 (Phase 05 Plan 05-05):** Real-runtime Node smoke `tests/lattice-survivability-smoke.test.js` (292 lines; 40 PASS / 0 FAIL real-runtime end-to-end). Mocks only `chrome.storage.session` (in-memory) + `chrome.runtime` (no-op) because those Chrome APIs are unavailable in Node; uses real `createReceipt` + `verifyReceipt` + ephemeral Ed25519 keypair (no Lattice mocks; mirrors Phase 1-4 smoke convention "Real runtime tests, not static-text"). 5-part coverage: Part 1 surface presence (Plan 05-03 re-export + Phase 1-4 carryforward); Part 2 adapter 4 contract methods + ResumePolicy dispatch over FSB attempt-1 marker vocabulary; Part 3 real v1.1 receipt embedded in adapter state + serialize/deserialize + verifyReceipt round-trip (proves DSSE+JCS preserved through JSON round-trip); Part 4 feature flag default-off (zero writes) + flag-on (writes recorded in mock storage) + onEviction lifecycle (UnsubscribeFn idempotency); Part 5 Phase 1-4 Lattice surface carryforward. Appended to `package.json` `scripts.test` chain as the FINAL entry (after `lattice-providers-smoke.test.js`). Phase 1+2+3+4 chain entries BYTE-FROZEN. FSB commit `e1d9f491` (combined with FINT-05).
- [x] **FINT-07 -- DONE 2026-05-27 (Phase 6 Plans 06-01 + 06-02):** Extension/offscreen/lattice-host.js extended with a second `chrome.runtime.onMessage` listener for `lattice-provider-execute` + `lattice-provider-abort`. Strategy A per CONTEXT.md post-research amendment + 06-RESEARCH Section 16: autopilot path does its own `fetch()` inside the offscreen page using FSB's pre-built provider-formatted `requestBody` (preserves multi-turn messages + tools + provider-specific cache_control / systemInstruction / generationConfig that Lattice's `adapter.execute({task, artifacts, outputs})` would silently drop); test-connection path uses `adapter.execute()` natively (single-shot single-user-message fits Lattice's ProviderRunRequest contract). `PROVIDER_FACTORIES` dispatch map for 7 providers with explicit FSB->Lattice key normalisations (`xai -> createXaiProvider`, `openai -> createOpenAIProvider`, `anthropic -> createAnthropicProvider`, `gemini -> createGeminiProvider`, `openrouter -> createOpenRouterProvider`, `lmstudio -> createLmStudioProvider` (no hyphen; camelCase Lm), `custom -> createOpenAICompatibleProvider`). Per-call AbortController registry keyed by `crypto.randomUUID()` requestId; cleanup in finally{} on settle. Cross-extension origin check via `sender.id !== chrome.runtime.id`. Error envelope shape: `{ok: true, response: {rawResponse: <json>}}` or `{ok: false, error: {kind: 'aborted' | 'adapter_error' | 'host_unreachable' | 'invalid_provider' | 'fetch_error', message, providerError?}}`. `extension/background.js` gains ONE new bare `importScripts('ai/lattice-provider-bridge.js')` at line 12 between `ai/cli-parser.js` (line 11) and `ai/ai-integration.js` (line 13; alphabetical placement; 153 importScripts -> 154 byte-frozen relative order) + idempotent `ensureLatticeOffscreen()` helper with `hasDocument` guard + `WORKERS` reason value, called fire-and-forget from BOTH `onInstalled` + `onStartup` after `initializeAnalytics()` -- closes audit gap G3 from `.planning/audit/v0.10.0-MILESTONE-AUDIT.md` (SW now opens offscreen host at startup, mirroring Phase 269 telemetry-alarm idempotent-on-both-events pattern). Phase 5 `lattice-step-transition` handler BYTE-FROZEN; background.js otherwise byte-frozen aside from the 4 small inserts. Verified via `tests/lattice-provider-bridge-smoke.test.js` Parts 1+2+3+4+5 (61 PASS at Plan 06-03 end; 71 PASS at Plan 06-04 end; 85 PASS at Plan 06-05 end). FSB commits: Plan 06-01 `a48d13ea` (offscreen handler) + `1573b825` (Parts 1-4 smoke fill) + Plan 06-02 `0a9555d2` (SW wiring) + `4aa4d4ea` (Part 5 smoke fill).
- [x] **FINT-08 -- DONE 2026-05-27 (Phase 6 Plans 06-03 + 06-04):** New thin shim `extension/ai/lattice-provider-bridge.js` (146 lines) exports `executeViaBridge(providerKey, config, requestBody, opts)` via dual export (`globalScope.executeViaBridge` for classic SW + `module.exports` for Node CJS; mirrors Phase 5 Plan 05-05 `lattice-runtime-adapter.js` idiom). `crypto.randomUUID()` per-call requestId. AbortSignal -> companion `{type: 'lattice-provider-abort', requestId}` `sendMessage` with `addEventListener('abort', ..., {once: true})` + finally{} `removeEventListener` (Pitfall 3 listener-leak prevention belt-and-suspenders). Envelope unwrap: `ok:true` returns `envelope.response.rawResponse` (autopilot raw HTTP body) OR `envelope.response` (test-connection ProviderRunResponse) via simple truthy fallback -- preserves the byte-identical raw HTTP body shape that `universalProvider.sendRequest` returns today so downstream `tool-use-adapter.js` stays BYTE-FROZEN; `ok:false` throws typed `Error` with `err.code` in {`aborted`, `adapter_error`, `host_unreachable`, `invalid_provider`, `fetch_error`} + optional `err.providerError` pass-through. `extension/ai/agent-loop.js` line 1044 swapped to feature-flag-gated branch: `if (typeof FSB_LATTICE_PROVIDER_BRIDGE_ENABLED === 'undefined' || FSB_LATTICE_PROVIDER_BRIDGE_ENABLED) return executeViaBridge(...); return providerInstance.sendRequest(requestBody);` -- default-on per ROADMAP; legacy fallback preserved for runtime rollback safety (Phase 7 strips flag + archives `universal-provider.js`). Switch + `requestBody` construction at lines 957-1042 BYTE-FROZEN; INV-04 setTimeout iterator PATTERN BYTE-FROZEN (absolute positions shifted from 1841/2439/2508/2518 to 1857/2455/2524/2534 = +16 lines expected; `grep -c setTimeout` still returns 8). `extension/ui/options.js` `checkApiConnection()` rewritten (lines 1077-1131): reads API key from per-provider input fields via `PROVIDER_KEY_GETTERS` map returning `(elements.apiKey?.value || '').trim()` / `(document.getElementById('xxxApiKey')?.value || '').trim()` (NOT from chrome.storage via `getStoredSettings`) and delegates to `executeViaBridge(provider, config, {__testConnection: true}, {mode: 'test-connection'})`. `saveSettings()` (lines 977-1029) defense-in-depth `.trim()` on all 9 input-derived string fields: `apiKey` + `geminiApiKey` + `openaiApiKey` + `anthropicApiKey` + `customApiKey` + `customEndpoint` + `openrouterApiKey` + `lmstudioBaseUrl` + `captchaApiKey`. **9-field rationale (ROADMAP wording vs implementation reconciliation):** FINT-08 implementation extends ROADMAP's "all 7 API key fields" wording to "9 input-derived string fields" (8 LLM-side incl. 2 URL endpoints -- `customEndpoint + lmstudioBaseUrl` -- for defense-in-depth + 1 CAPTCHA key). Rationale: URLs can carry credentials in some configurations (e.g., basic-auth `https://user:pass@host` or query-string tokens); trimming on save prevents trailing whitespace from breaking provider routing AND prevents credential leakage through malformed URL parsing. The extra 2 fields beyond ROADMAP wording are pure defense-in-depth -- no functional regression risk, fully additive to the spec. Closes the checker's audit warning about the "7 vs 9" wording mismatch by documenting the URL-endpoint defense-in-depth justification in this narrative. **Closes xai-key-rejected-400 P1 (missing trim on save -- root cause: clipboard managers / paste handlers add trailing whitespace; xAI's masked echo `xa***cy` does not reveal trailing bytes) + P2 (stale storage read -- old `checkApiConnection` read from `chrome.storage` via `getStoredSettings` BEFORE the user clicked Save) as side effects of the Phase 6 migration. Documented in `.planning/debug/xai-key-rejected-400.md`.** Verified via `tests/lattice-provider-bridge-smoke.test.js` (85 PASS / 0 FAIL across all 6 Parts at Plan 06-05 end; FINT-08 sub-items a/b/c/d/e all green). FSB commits: Plan 06-03 `fd3408a7` (bridge shim) + `f1aa4734` (agent-loop swap) + `18de341a` (Parts 1+3+5 smoke fill) + Plan 06-04 `0cc21c6a` (saveSettings trim) + `f9007731` (checkApiConnection rewrite) + `f2f0a27d` (Part 5 smoke fill).
- [ ] **FINT-09 (Phase 7 — archive custom provider stack + flag removal):** Grep audit confirming zero `extension/*` references to `universal-provider.js` outside `_archive/`. Move `extension/ai/universal-provider.js` → `extension/_archive/universal-provider.js` (preserved for git-blame archaeology; recoverable via git history). Strip `FSB_LATTICE_PROVIDER_BRIDGE_ENABLED` feature flag from `extension/ai/agent-loop.js`, `extension/ui/options.js`, `extension/ai/lattice-provider-bridge.js`; bridge call sites become unconditional. Update test fixtures referencing old path. Code-search audit confirms zero `FSB_LATTICE_PROVIDER_BRIDGE_ENABLED` references repo-wide.
- [ ] **FINT-NN..M (TBD, follow-on milestone):** Adapt FSB's `runAgentLoop` to emit step transitions through Lattice's receipt API (replacing the FSB-only `setStepMarker` helper from v0.10.0-attempt-1).
- [ ] **FINT-MM..K (TBD, follow-on milestone):** Adapt FSB's safety / observability / extension hooks to Lattice's tripwire band system (replacing the FSB-only HookPipeline from v0.10.0-attempt-1).
- [x] **FINT-KK..L -- PROMOTED 2026-05-27 (Phase 6 + Phase 7):** Originally a TBD placeholder ("Adapt FSB's universal-provider.js to Lattice's provider abstraction OR keep FSB-side and align signatures"). After the `xai-key-rejected-400` debug session surfaced defects in FSB's custom stack that Lattice's adapters do not have (missing trim + stale storage read in `options.js`), this row was promoted to concrete in-scope work: FINT-07 (offscreen provider handler) + FINT-08 (agent-loop + options rewire) + FINT-09 (archive). Closes via Phases 6 + 7.
- [ ] **FINT-LL..P (TBD, follow-on milestone):** Sidepanel Agent State Inspector view consuming Lattice's receipt timeline (single source of truth for both display + future signing).
- [ ] **FINT-PP..Q (TBD, follow-on milestone):** SW eviction resumption via Lattice's MV3-survivability adapter contract (replacing the FSB-only `_al_handleRestoredMode` from v0.10.0-attempt-1).

### MCP -- Wire-Contract Non-Regression (Phase N + cross-cutting)

Goal: prove INV-01 (MCP wire contracts UNTOUCHED) and INV-02 (tool surface parity) hold across every Lattice-integration change.

- [x] **MCP-01 -- HOLDING through Phase 01 (2026-05-24):** `tests/tool-definitions-parity.test.js` continues to pass byte-identically pre/post Phase 01 (142 PASS / 0 FAIL at SUMMARY-write time). Verified at every Plan 01-02 task gate. Re-verified at end of Phase 01 plans. Will be re-verified at every future v0.10.0-attempt-2 phase gate.
- [x] **MCP-02 -- HOLDING through Phase 01 (2026-05-24):** Lattice `file:` dependency introduction did NOT mutate `TOOL_REGISTRY`. `git status --porcelain extension/ai/tool-definitions.js mcp/ai/tool-definitions.cjs` empty across the entire plan. Tool registry surface is byte-identical to the pre-Phase-1 baseline.
- [ ] **MCP-03..N (TBD):** Additional MCP non-regression checks as Lattice integration phases land.

### PRV -- Provider Parity (Phase N + cross-cutting)

Goal: prove INV-03 holds across all 7 providers after Lattice-driven provider adapter alignment.

- [ ] **PRV-01..N (TBD):** Per-provider regression suite after each Lattice integration phase.

---

## Out of Scope (explicit exclusions)

- **OOS-01 FSB-side primitive re-implementation.** v0.10.0-attempt-1 built its own hook pipeline + checkpoint-hook + step-marker layer + session-resume dispatcher inside FSB. Attempt-2 does NOT re-build these; if FSB needs them, Lattice gets them first per INV-06.
- **OOS-02 Multi-agent crews / agent-team orchestration** (CrewAI / AutoGen style). Wrong domain for FSB. Lattice also currently excludes this; if Lattice opens its multi-agent policy mid-milestone, delegation primitive becomes in-scope.
- **OOS-03 Coding-agent patterns** (Aider / Cline / OpenCode diff-then-apply). Wrong domain.
- **OOS-04 Skills primitive (full domain-specific tool+prompt loading)** -- deferred to v0.11.0+.
- **OOS-05 Replacing the `setTimeout`-chained iterator pattern.** INV-04. Lattice integration is additive runtime adaptation.
- **OOS-06 Streaming-aware hook events / chunk-level events.** Provider-parity violation (INV-03) for Gemini single-shot path.
- **OOS-07 Auto-fetching skills from URLs.** Highest-risk skills anti-pattern.
- **OOS-08 Resurrection of `extension/agents/*`.** INV-05.
- **OOS-09 v0.9.70 Showcase Dashboard work** (streaming fix, Sync-tab restore, 16:10 viewport). Continues in parallel on `main`.
- **OOS-10 Telemetry surface follow-ups** (first-run banner, "View what we send", region-gated opt-in, etc.). Carry-forward from v0.9.69.
- **OOS-11 Showcase i18n carry-forwards.** Carry-forward from v0.9.63 / v0.9.65.

---

## Reference: v0.10.0-attempt-1 (archived)

The FSB-first attempt shipped Phase 1 hooks-foundation (priority bands, matcher, race-with-log, freeze, 5 new lifecycle events, lockBand, telemetry-hook, MCP-04 byte-identity passthrough, MCP-05 TOOL_REGISTRY freeze, PRV-02 hook-event-count parity) and Phase 2 state-inspectability-carve-out (LIFECYCLE_EVENTS.STEP_TRANSITION, checkpoint-hook.js with Lattice receipt-shape envelope, 12 step markers in runAgentIteration, additive persistSession schema, sidepanel Agent State Inspector view, full MV3 SW eviction resumption with CONSERVATIVE recovery for mid-API-request and mid-tool-dispatch). 617/617 test assertions green. The patterns themselves are intellectually correct; attempt-2 moves them into Lattice instead of duplicating them in FSB.

Archived under:
- Branch: `pre-pivot-archive/v0.10.0-fsb-first`
- On-disk: `.planning/milestones/v0.10.0-attempt-1-pre-pivot/` (full Phase 1 + Phase 2 artifact sets including 981-line RESEARCH.md, 694-line UI-SPEC.md, threat models, verification reports)

---

## Traceability

To be populated during phase planning. Each REQ-ID will map to exactly one phase. 100% coverage verified by `gsd-plan-checker` before execution.

| REQ-ID | Phase | Status |
|--------|-------|--------|
| LSDK-01 | 01 | Complete (Phase 01 Plan 01-01: gap survey + createReceipt re-export) |
| FINT-01 | 01 | Complete (Phase 01 Plan 01-02: file: dep wiring + 29 PASS smoke) |
| MCP-01 | cross-cutting (Phase 01..N) | Holding (Phase 01 verified) |
| MCP-02 | cross-cutting (Phase 01..N) | Holding (Phase 01 verified) |
| LSDK-02 | 02 | Complete (Phase 02 Plan 02-01: step-transition fields on CapabilityReceiptBody; Lattice commit 5c48134) |
| LSDK-03 | 02 | Complete (Phase 02 Plan 02-01: sessionId field + schema version bumped to v1.1 literal-union; Lattice commit 5c48134) |
| LSDK-04 | 02 | Complete (Phase 02 Plan 02-02: priority bands enum + lower-band-first + within-band registration-order; Lattice commit ba6172c) |
| LSDK-05 | 02 | Complete (Phase 02 Plan 02-02: per-handler matcher regex + no-abort race-with-log + HOOK_TIMEOUT via TracerLike; Lattice commit ba6172c) |
| LSDK-06 | 02 | Complete (Phase 02 Plan 02-02: structuredClone + Object.freeze handler context; Lattice commit ba6172c) |
| LSDK-07 | 02 | Complete (Phase 02 Plan 02-02: irreversible pipeline.freeze() + PIPELINE_FROZEN error name; Lattice commit ba6172c) |
| LSDK-08 | 02 | Complete (Phase 02 Plan 02-02: HookLifecycleEvent literal-union separate from RunEventKind; Lattice commit ba6172c) |
| LSDK-09 | 03 | Complete (Phase 03 Plan 03-01 + Plan 03-02: step.transition literal in RunEventKind union; Lattice commits fd254c4 + acdbb8a + 7afd62f) |
| LSDK-10 | 03 | Complete (Phase 03 Plan 03-01 + Plan 03-02: createCheckpointHook factory; Lattice commits a67f476 + acdbb8a + 7afd62f) |
| LSDK-11 | 03 | Complete (Phase 03 Plan 03-01 + Plan 03-02: STEP_TRANSITION_EVENT_NAME + DEFAULT_CHECKPOINT_BAND constants; Lattice commits a67f476 + acdbb8a) |
| LSDK-12 | 03 | Complete (Phase 03 Plan 03-01 + Plan 03-02: CheckpointHookContext + CheckpointHookOptions interfaces; Lattice commits a67f476 + acdbb8a) |
| LSDK-13 | 03 | Complete (Phase 03 Plan 03-01: checkpoint.test.ts 15 vitest cases; Lattice commit a67f476) |
| LSDK-14 | 04 | Complete (Phase 04 Plan 04-01 + Plan 04-04 + Plan 04-05: Anthropic adapter; Lattice commits `cf31d82` + `e5659a8` + `f9c7ef4` + `f1c943b`) |
| LSDK-15 | 04 | Complete (Phase 04 Plan 04-02 + Plan 04-04 + Plan 04-05: Gemini adapter; Lattice commits `7a32b00` + `e5659a8` + `f9c7ef4` + `f1c943b`) |
| LSDK-16 | 04 | Complete (Phase 04 Plan 04-03 + Plan 04-04 + Plan 04-05: xAI adapter with reasoning_tokens quirk; Lattice commits `09a495e` + `e5659a8` + `f9c7ef4` + `f1c943b`) |
| LSDK-17 | 04 | Complete (Phase 04 Plan 04-03 + Plan 04-04 + Plan 04-05: OpenRouter adapter; Lattice commits `1cfc13c` + `e5659a8` + `f9c7ef4` + `f1c943b`) |
| LSDK-18 | 04 | Complete (Phase 04 Plan 04-03 + Plan 04-04 + Plan 04-05: LM Studio adapter; Lattice commits `40457ff` + `e5659a8` + `f9c7ef4` + `f1c943b`) |
| LSDK-19 | 05 | Complete (Phase 05 Plan 05-02 + Plan 05-03: SurvivabilityAdapter interface + ResumePolicy + 4 supporting types; Lattice commits `a4609bc` + `109d6ae` + `e95067b`) |
| LSDK-20 | 05 | Complete (Phase 05 Plan 05-02: createNoopSurvivabilityAdapter ref impl + 17 vitest cases; Lattice commit `a4609bc`) |
| LSDK-21 | 05 | Complete (Phase 05 Plan 05-03: src/index.ts re-export of factory + 5 types; Lattice commit `109d6ae`) |
| LSDK-22 | 05 | Complete (Phase 05 Plan 05-03: 2 MV3-survivability audit-doc rows flipped Blocker -> Covered with backlink SHAs; Lattice commit `e95067b`) |
| FINT-03 | 05 | Complete (Phase 05 Plan 05-01: esbuild bundler infra + scripts.build + extension/dist/; FSB commit `fbbe02d5`) |
| FINT-04 | 05 | Complete (Phase 05 Plan 05-04: hybrid offscreen Lattice host + manifest WAR entry + bundler emits offscreen ESM bundle; FSB commit `8ab0c6df`) |
| FINT-05 | 05 | Complete (Phase 05 Plan 05-05: standalone lattice-runtime-adapter.js over chrome.storage.session; feature-flag default-off; FSB commit `e1d9f491`) |
| FINT-06 | 05 | Complete (Phase 05 Plan 05-05: Node smoke lattice-survivability-smoke.test.js 40 PASS; package.json scripts.test chain extended; FSB commit `e1d9f491`) |
| FINT-07 | 06 | Complete (Phase 06 Plan 06-01 offscreen `lattice-provider-execute` + `lattice-provider-abort` handlers + Plan 06-02 `background.js` `ensureLatticeOffscreen()` SW startup wiring; FSB commits `a48d13ea` + `1573b825` + `0a9555d2` + `4aa4d4ea`; closes audit gap G3 from `.planning/audit/v0.10.0-MILESTONE-AUDIT.md`) |
| FINT-08 | 06 | Complete (Phase 06 Plan 06-03 `lattice-provider-bridge.js` shim + `agent-loop.js` feature-flag-gated swap + Plan 06-04 `options.js` `checkApiConnection` rewrite + `saveSettings` 9-field trim defense; FSB commits `fd3408a7` + `f1aa4734` + `18de341a` + `0cc21c6a` + `f9007731` + `f2f0a27d`; closes xai-key-rejected-400 P1+P2 by side effect; 9-field trim narrative reconciles "7 API key fields" ROADMAP wording with the additional URL-endpoint defense-in-depth -- see FINT-08 narrative entry for full rationale) |
| FINT-09 | 07 | Pending (Phase 07 — archive universal-provider.js + feature-flag removal) |
| FINT-KK..L | 06 + 07 | Promoted (was TBD placeholder; closed via FINT-07/08/09 over Phases 6 + 7) |
| (more) | (TBD follow-on milestone) | Pending |

**Total v1 requirements:** TBD (31 Complete + 1 newly-promoted = 32 concrete after FINT-07/08 transition Pending -> Complete on 2026-05-27; remaining FINT/MCP/PRV TBD IDs populated during downstream milestones; FINT-09 still Pending awaiting Phase 07 archive + flag removal)
