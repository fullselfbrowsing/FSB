---
phase: 269-install-identity-opt-out-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extension/utils/install-identity.js
  - extension/background.js
  - extension/ui/control_panel.html
  - tests/install-identity.test.js
  - package.json
autonomous: true
requirements:
  - IDENT-01
  - IDENT-02
  - IDENT-03
  - IDENT-04
  - IDENT-05
  - CONS-01
  - CONS-02
user_setup: []

must_haves:
  truths:
    - "After a fresh install, calling globalThis.fsbInstallIdentity.getOrCreateInstallUuid() mints a v4 UUID and persists it to chrome.storage.local under fsbInstallUuid (IDENT-01)."
    - "Calling getOrCreateInstallUuid() a second time returns the SAME UUID -- no re-mint -- across both onInstalled and onStartup lifecycles (IDENT-02)."
    - "When chrome.storage.local is unavailable / throws, getOrCreateInstallUuid() resolves to null with NO throw and NO session-only UUID fallback (IDENT-03)."
    - "Module never reads or writes chrome.storage.sync; storage namespace is exclusively chrome.storage.local (IDENT-04)."
    - "Module export surface is identity-only -- getOrCreateInstallUuid, isTelemetryOptedOut, setTelemetryOptOut, key constants -- and contains no URL / prompt / DOM / clipboard fields (IDENT-05)."
    - "The Control Panel Advanced Settings tab shows a 'Privacy & Telemetry' card with a 'Send anonymous usage data' toggle that renders ON by default on a fresh profile (CONS-01)."
    - "Clicking the toggle writes fsbTelemetryOptOut = !current to chrome.storage.local synchronously and the visible toggle state reflects the new value within 100ms (CONS-02)."
    - "Stored UUID that fails v4 regex validation on next read is defensively re-minted with one warning log; the new UUID is returned and persisted."
  artifacts:
    - path: "extension/utils/install-identity.js"
      provides: "globalThis.fsbInstallIdentity with getOrCreateInstallUuid, isTelemetryOptedOut, setTelemetryOptOut + key constants"
      exports:
        - "globalThis.fsbInstallIdentity.getOrCreateInstallUuid"
        - "globalThis.fsbInstallIdentity.isTelemetryOptedOut"
        - "globalThis.fsbInstallIdentity.setTelemetryOptOut"
        - "globalThis.fsbInstallIdentity.FSB_INSTALL_UUID_KEY"
        - "globalThis.fsbInstallIdentity.FSB_TELEMETRY_OPT_OUT_KEY"
        - "module.exports (for Node test harness)"
      contains: "fsbInstallUuid, fsbTelemetryOptOut, crypto.randomUUID, /^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i"
      min_lines: 60
    - path: "extension/background.js"
      provides: "importScripts hook + onInstalled + onStartup boot-time UUID seed"
      contains:
        - "importScripts('utils/install-identity.js')"
        - "fsbInstallIdentity.getOrCreateInstallUuid"
    - path: "extension/ui/control_panel.html"
      provides: "Privacy & Telemetry settings-card with toggle + storage round-trip JS"
      contains:
        - "id=\"card-privacy-telemetry\""
        - "id=\"fsbTelemetryOptOut\""
        - "Send anonymous usage data"
        - "Tokens used, MCP client name, active agent count. No URLs, prompts, or DOM."
        - "Read full policy"
    - path: "tests/install-identity.test.js"
      provides: "Unit tests: mint-once, reuse-on-restart, null-on-unavailable, opt-out toggle round-trip, defensive UUID re-mint on corruption"
      min_lines: 120
    - path: "package.json"
      provides: "Wires tests/install-identity.test.js into npm test chain"
      contains: "tests/install-identity.test.js"
  key_links:
    - from: "extension/background.js (top of importScripts chain, BEFORE analytics.js at line 31)"
      to: "extension/utils/install-identity.js"
      via: "importScripts('utils/install-identity.js')"
      pattern: "importScripts\\(['\"]utils/install-identity\\.js['\"]\\)"
    - from: "extension/background.js chrome.runtime.onInstalled at line 13015"
      to: "globalThis.fsbInstallIdentity.getOrCreateInstallUuid()"
      via: "lifecycle handler invocation between initializeAnalytics() (line 13019) and loadDebugMode() (line 13022)"
      pattern: "fsbInstallIdentity\\.getOrCreateInstallUuid\\(\\)"
    - from: "extension/background.js chrome.runtime.onStartup at line 13051"
      to: "globalThis.fsbInstallIdentity.getOrCreateInstallUuid()"
      via: "idempotent re-call on every service-worker startup"
      pattern: "fsbInstallIdentity\\.getOrCreateInstallUuid\\(\\)"
    - from: "extension/ui/control_panel.html #fsbTelemetryOptOut checkbox"
      to: "chrome.storage.local.fsbTelemetryOptOut"
      via: "inline <script> at end of control_panel.html: storage read on DOMContentLoaded sets initial state (checked = !optOut); change-event handler writes opt-out value with sync await chrome.storage.local.set"
      pattern: "chrome\\.storage\\.local\\.(get|set).*fsbTelemetryOptOut"
    - from: "tests/install-identity.test.js"
      to: "extension/utils/install-identity.js (Node CommonJS require)"
      via: "module.exports + mocked chrome.storage.local via createStorageArea helper (mirrors tests/agent-cap-storage.test.js:26-63)"
      pattern: "require\\(['\"]\\.\\./extension/utils/install-identity\\.js['\"]\\)"
---

<objective>
Land Phase 269: a self-contained anonymous install-identity module + a Control Panel kill-switch. This is the foundation block of v0.9.69's telemetry pipeline. After this phase ships:

- Every FSB install has a stable v4 UUID in `chrome.storage.local.fsbInstallUuid`, lazily minted on first `onInstalled` or `onStartup`, idempotent across re-fires.
- The user sees a "Privacy & Telemetry" card in Advanced Settings whose toggle (ON by default) writes `chrome.storage.local.fsbTelemetryOptOut`.
- The module exposes `globalThis.fsbInstallIdentity` for downstream consumers (TelemetryCollector in Phase 272, MCPMetricsRecorder in Phase 271).
- Tests cover mint-once, reuse-on-restart, null-on-unavailable, opt-out round-trip, defensive UUID re-mint on corruption.

**Purpose:** Unblock the entire v0.9.69 telemetry chain (Phases 270-275) without leaking any PII surface. NO outbound HTTP, NO event collection, NO server contract -- those land in Phases 272 / 273. This phase is identity + UI only.

**Output:**
- New module: `extension/utils/install-identity.js`.
- Background-worker boot integration: importScripts + onInstalled + onStartup hooks in `extension/background.js`.
- UI card: new "Privacy & Telemetry" `.settings-card` in `extension/ui/control_panel.html` `#advanced > .advanced-settings-grid` (mounted as the LAST child of the grid at line 341).
- Test coverage: `tests/install-identity.test.js`, wired into `npm test` via `package.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/269-install-identity-opt-out-scaffold/269-CONTEXT.md
@.planning/phases/269-install-identity-opt-out-scaffold/269-UI-SPEC.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md

@extension/utils/analytics.js
@extension/ai/cost-tracker.js
@extension/manifest.json
@tests/agent-cap-storage.test.js
@tests/change-report-toggle.test.js

<interfaces>
<!-- Key types and patterns the executor needs. Extracted from codebase. -->
<!-- Use these directly -- no codebase exploration needed. -->

From extension/manifest.json (already grants required permissions; NO manifest change required):
```
"permissions": ["storage", "unlimitedStorage", ..., "alarms", ...]
```

From extension/background.js (importScripts chain, lines 4-100). New import goes at line 4 -- TOP of the chain, BEFORE analytics.js (currently line 31):
```javascript
// LINE 4 currently:
importScripts('config/config.js');
importScripts('config/init-config.js');
// ...
importScripts('utils/analytics.js');     // line 31 -- DO NOT precede with the new import HERE; precede at TOP
```

From extension/background.js chrome.runtime.onInstalled (line 13015) and onStartup (line 13051) -- exact hook points:
```javascript
chrome.runtime.onInstalled.addListener(async () => {
  automationLogger.logInit('extension', 'installed', { version: 'v0.9.50' });
  initializeAnalytics();                        // line 13019
  // <-- INSERT: await globalThis.fsbInstallIdentity.getOrCreateInstallUuid(); HERE
  await loadDebugMode();                        // line 13022
  // ...
});

chrome.runtime.onStartup.addListener(async () => {
  automationLogger.logServiceWorker('startup', {});
  initializeAnalytics();                        // line 13053
  // <-- INSERT: await globalThis.fsbInstallIdentity.getOrCreateInstallUuid(); HERE
  await loadDebugMode();                        // line 13055
  // ...
});
```

From extension/ai/cost-tracker.js (function/prototype pattern to MIRROR -- NOT ES module class):
```javascript
'use strict';

function CostTracker(costLimit) {        // function constructor, not `class`
  this.totalCost = 0;
  // ...
}
CostTracker.prototype.record = function(model, inputTokens, outputTokens, provider) {
  // ...
};

// CommonJS export for Node test harness:
if (typeof module !== 'undefined' && module.exports) {
  module.exports = { MODEL_PRICING, estimateCost, CostTracker };
}
```

NOTE: For install-identity.js, the public surface is attached to `globalThis.fsbInstallIdentity`
(camelCase identifier as specified by CONTEXT D-decision), NOT `globalThis.FSBInstallIdentity`.
The research ARCHITECTURE.md §2.1 uses `FSBInstallIdentity` (PascalCase) as a sketch; CONTEXT.md
locked decision overrides it -- use `fsbInstallIdentity` (camelCase) per CONTEXT constraint #2.

From extension/ui/control_panel.html (existing modern-toggle pattern at line 462-471 -- MIRROR THIS):
```html
<div class="settings-card">
  <div class="settings-card-header">
    <div class="settings-card-icon">
      <i class="fas fa-shield-alt"></i>            <!-- pick an existing FontAwesome icon -->
    </div>
    <div class="settings-card-title">
      <h3>Privacy & Telemetry</h3>
      <p>Anonymous usage data</p>
    </div>
  </div>
  <div class="settings-card-content">
    <div class="setting-item">
      <label class="modern-toggle">
        <input type="checkbox" id="fsbTelemetryOptOut" checked>
        <span class="toggle-track"><span class="toggle-thumb"></span></span>
        <div class="toggle-content">
          <span class="toggle-label">Send anonymous usage data</span>
          <span class="toggle-desc">Tokens used, MCP client name, active agent count. No URLs, prompts, or DOM.</span>
        </div>
      </label>
      <a class="setting-hint" href="/privacy#telemetry-disclosure" target="_blank" rel="noopener noreferrer"
         style="display:block; margin-top:8px;">Read full policy &rarr;</a>
    </div>
  </div>
</div>
```

NOTE on toggle semantics: `<input type="checkbox" id="fsbTelemetryOptOut" checked>` -- the `checked` attribute
represents "telemetry ON" (the user's mental model). The storage key `fsbTelemetryOptOut` stores `true` when
the user has OPTED OUT. The wiring inverts: `optedOut = !checkbox.checked` on write,
`checkbox.checked = !storedOptOut` on read. The `id` matches the storage key for grep traceability.

From tests/agent-cap-storage.test.js (lines 26-80 -- chrome.storage mock helpers to MIRROR):
```javascript
function createStorageArea(initial) {
  const store = Object.assign({}, initial || {});
  return {
    async get(keys) { /* handles string|array|object|null */ },
    async set(values) { Object.assign(store, values); },
    async remove(keys) { /* ... */ },
    _dump() { return Object.assign({}, store); }
  };
}

function setupChromeMock(opts) {
  const local = createStorageArea(opts.local || {});
  globalThis.chrome = {
    runtime: { id: 'phase-269-test', lastError: null },
    storage: { local, onChanged: { addListener: () => {} } }
  };
}
```

From package.json (current `test` script structure to extend):
```
"test": "node tests/test-overlay-state.js && node tests/cost-tracker-ordering.test.js && ... && node tests/agent-loop-empty-contents.test.js"
```
The chain is plain `&&`-joined; append `&& node tests/install-identity.test.js` near related telemetry / cost tests (e.g. after `tests/cost-tracker.test.js`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create install-identity.js module + boot-time integration in background.js</name>
  <files>extension/utils/install-identity.js, extension/background.js</files>
  <behavior>
    Module surface (globalThis.fsbInstallIdentity + module.exports for Node tests):

    - getOrCreateInstallUuid() -> Promise&lt;string|null&gt;
      - Reads chrome.storage.local.fsbInstallUuid.
      - If stored value matches v4 regex /^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i, returns it unchanged.
      - If stored value is missing OR fails regex (corruption), mints a fresh crypto.randomUUID(), stores it, returns it. On corruption path, emits ONE console.warn line "[FSB Telemetry] Stored install UUID failed validation; minting fresh".
      - If chrome.storage.local.get or .set throws / rejects (e.g., the storage area is undefined), catches the error and returns null. NO throw, NO session-only fallback UUID.
    - isTelemetryOptedOut() -> Promise&lt;boolean&gt;
      - Returns chrome.storage.local.fsbTelemetryOptOut === true. Missing key returns false (ON by default per D-02).
      - On storage error: returns false (treat as ON; downstream collectors will gate on UUID null separately).
    - setTelemetryOptOut(value) -> Promise&lt;void&gt;
      - Writes Boolean(value) to chrome.storage.local.fsbTelemetryOptOut.
      - Used by control_panel.html toggle wiring.
    - Constants:
      - FSB_INSTALL_UUID_KEY = 'fsbInstallUuid'
      - FSB_TELEMETRY_OPT_OUT_KEY = 'fsbTelemetryOptOut'

    Behavior tests (Task 3 will codify):

    - Test 1 (mint-once): Fresh store -> getOrCreateInstallUuid() returns a v4 UUID; storage now contains fsbInstallUuid; second call returns the SAME string; storage was written exactly ONCE.
    - Test 2 (reuse-on-restart): Pre-seeded store with a valid v4 UUID -> getOrCreateInstallUuid() returns the same UUID; storage was NOT re-written.
    - Test 3 (null-on-unavailable): Stub chrome.storage.local to throw on .get -> getOrCreateInstallUuid() resolves to null; NO uncaught throw observable.
    - Test 4 (opt-out round-trip): setTelemetryOptOut(true) then isTelemetryOptedOut() -> true; setTelemetryOptOut(false) -> false. Storage key is exactly fsbTelemetryOptOut.
    - Test 5 (defensive re-mint on corruption): Pre-seed fsbInstallUuid with the literal string 'not-a-uuid' -> getOrCreateInstallUuid() returns a FRESH valid v4 UUID; storage was overwritten; console.warn was emitted once.
    - Test 6 (no chrome.storage.sync): grep the module source -- assert ZERO occurrences of 'chrome.storage.sync' (IDENT-04 grep gate).
  </behavior>
  <action>
    Create extension/utils/install-identity.js:

    1. File preamble with JSDoc explaining purpose, phase reference (Phase 269 / v0.9.69), and a one-line warning "NEVER use chrome.storage.sync for telemetry-related keys (IDENT-04)".

    2. Use 'use strict'.

    3. Declare the v4 regex as a module constant: const UUID_V4_REGEX = /^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;

    4. Declare the two storage-key constants exactly as specified (camelCase, per CONTEXT D-decision -- NOTE: this is the user-locked deviation from REQUIREMENTS.md IDENT-01's snake_case `fsb_install_uuid`; use camelCase here): FSB_INSTALL_UUID_KEY = 'fsbInstallUuid', FSB_TELEMETRY_OPT_OUT_KEY = 'fsbTelemetryOptOut'.

    5. Implement getOrCreateInstallUuid as an async function wrapped in try/catch around the entire body. Inside:
       a. const data = await chrome.storage.local.get([FSB_INSTALL_UUID_KEY]);
       b. const existing = data && data[FSB_INSTALL_UUID_KEY];
       c. If typeof existing === 'string' && UUID_V4_REGEX.test(existing): return existing.
       d. If typeof existing === 'string' && !UUID_V4_REGEX.test(existing): console.warn one-line "[FSB Telemetry] Stored install UUID failed validation; minting fresh".
       e. Mint via crypto.randomUUID() (do NOT add a polyfill; STACK §3c confirms crypto.randomUUID is available in MV3 SW since Chrome 92).
       f. await chrome.storage.local.set({ [FSB_INSTALL_UUID_KEY]: uuid });
       g. return uuid.
       h. Outer catch: return null. Do NOT mint a session-only UUID; do NOT throw.

    6. Implement isTelemetryOptedOut as an async function: try { const data = await chrome.storage.local.get([FSB_TELEMETRY_OPT_OUT_KEY]); return data && data[FSB_TELEMETRY_OPT_OUT_KEY] === true; } catch { return false; }.

    7. Implement setTelemetryOptOut(value): await chrome.storage.local.set({ [FSB_TELEMETRY_OPT_OUT_KEY]: Boolean(value) });. Let exceptions propagate (caller decides; the UI wiring in Task 2 will not await -- but tests will).

    8. Export:
       - globalThis.fsbInstallIdentity = { getOrCreateInstallUuid, isTelemetryOptedOut, setTelemetryOptOut, FSB_INSTALL_UUID_KEY, FSB_TELEMETRY_OPT_OUT_KEY }; (camelCase per CONTEXT D-decision -- NOT PascalCase as ARCHITECTURE.md sketch shows)
       - if (typeof module !== 'undefined' && module.exports) { module.exports = { getOrCreateInstallUuid, isTelemetryOptedOut, setTelemetryOptOut, FSB_INSTALL_UUID_KEY, FSB_TELEMETRY_OPT_OUT_KEY, UUID_V4_REGEX }; }

    9. Do NOT add chrome.alarms / chrome.runtime.sendMessage / fetch / setTimeout / any outbound side effect. Identity + storage only.

    Modify extension/background.js:

    10. At the TOP of the importScripts chain (the very first non-comment line of importScripts -- currently line 4 `importScripts('config/config.js');`), INSERT immediately ABOVE it: `importScripts('utils/install-identity.js');`. The new file MUST load BEFORE every other module, especially analytics.js (line 31), so downstream code can call `globalThis.fsbInstallIdentity.*` synchronously after boot.

    11. Inside chrome.runtime.onInstalled (line 13015 handler), INSERT between `initializeAnalytics();` (line 13019) and `await loadDebugMode();` (line 13022):
        ```
        try {
          const seededUuid = await globalThis.fsbInstallIdentity.getOrCreateInstallUuid();
          if (seededUuid) {
            console.log('[FSB Telemetry] Install UUID seeded');
          }
        } catch (e) {
          // Defensive -- module guarantees no-throw, but log if a bug regresses it.
          console.error('[FSB Telemetry] Install UUID seed failed:', e && e.message);
        }
        ```

    12. Inside chrome.runtime.onStartup (line 13051 handler), INSERT the SAME try/catch block between `initializeAnalytics();` (line 13053) and `await loadDebugMode();` (line 13055). The call is idempotent -- get-or-create returns the existing UUID on every wake.

    13. Do NOT modify any other listener, ALARM, message handler, or storage call in background.js.

    Do NOT touch extension/manifest.json -- the required `storage` permission is already at line 9 (verified via grep).
  </action>
  <verify>
    <automated>node -e "
      // Smoke that the module loads + has correct shape under Node (no chrome stubs needed for shape check).
      const m = require('./extension/utils/install-identity.js');
      const keys = Object.keys(m);
      const required = ['getOrCreateInstallUuid', 'isTelemetryOptedOut', 'setTelemetryOptOut', 'FSB_INSTALL_UUID_KEY', 'FSB_TELEMETRY_OPT_OUT_KEY', 'UUID_V4_REGEX'];
      const missing = required.filter(k => !keys.includes(k));
      if (missing.length) { console.error('MISSING EXPORTS:', missing); process.exit(1); }
      if (m.FSB_INSTALL_UUID_KEY !== 'fsbInstallUuid') { console.error('WRONG UUID KEY:', m.FSB_INSTALL_UUID_KEY); process.exit(1); }
      if (m.FSB_TELEMETRY_OPT_OUT_KEY !== 'fsbTelemetryOptOut') { console.error('WRONG OPT-OUT KEY:', m.FSB_TELEMETRY_OPT_OUT_KEY); process.exit(1); }
      if (typeof m.getOrCreateInstallUuid !== 'function') { console.error('NOT A FUNCTION'); process.exit(1); }
      console.log('OK: module shape valid');
    " &amp;&amp; grep -v '^//\|^[[:space:]]*\*' extension/utils/install-identity.js | grep -c 'chrome\.storage\.sync' | (read count; if [ "$count" -ne 0 ]; then echo "FAIL: chrome.storage.sync referenced in install-identity.js (IDENT-04 grep gate)"; exit 1; else echo "OK: no chrome.storage.sync in module"; fi) &amp;&amp; grep -n "importScripts('utils/install-identity.js')" extension/background.js &amp;&amp; grep -c "fsbInstallIdentity\.getOrCreateInstallUuid" extension/background.js | (read count; if [ "$count" -lt 2 ]; then echo "FAIL: getOrCreateInstallUuid called &lt;2 times (need onInstalled + onStartup)"; exit 1; else echo "OK: $count getOrCreateInstallUuid call sites"; fi)</automated>
  </verify>
  <done>
    - extension/utils/install-identity.js exists, loads under Node without chrome stubs, and exports the 6 required identifiers with exact key strings.
    - getOrCreateInstallUuid is async, validates v4 shape with the regex, re-mints defensively with one warn log on corruption, returns null on storage error with no throw.
    - The module source contains ZERO occurrences of chrome.storage.sync (IDENT-04 grep gate passes).
    - extension/background.js line 4 (or thereabouts -- the new TOP of the importScripts chain) reads importScripts('utils/install-identity.js'); the existing config.js / analytics.js / ws-client.js imports are unchanged in order BELOW it.
    - Both chrome.runtime.onInstalled and chrome.runtime.onStartup contain `await globalThis.fsbInstallIdentity.getOrCreateInstallUuid()` between initializeAnalytics() and loadDebugMode().
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Privacy & Telemetry settings-card with toggle wiring in control_panel.html</name>
  <files>extension/ui/control_panel.html</files>
  <action>
    Goal: Append a new "Privacy & Telemetry" `.settings-card` as the LAST child of `<div class="advanced-settings-grid">` inside `<section class="content-section" id="advanced">` (the grid container is at line 341; the last existing card ends well before the section close at line ~current; place the new card immediately BEFORE the `</div>` that closes `.advanced-settings-grid`).

    1. Locate the current closing `</div>` of `.advanced-settings-grid` (`grep -n "advanced-settings-grid" extension/ui/control_panel.html` then find the matching close). The new card must be the LAST .settings-card inside the grid (per UI-SPEC.md "as the LAST child").

    2. Insert this markup IMMEDIATELY BEFORE that closing `</div>`:

        ```html
        &lt;!-- Phase 269 / CONS-01, CONS-02: Anonymous Telemetry kill switch.
             Toggle checked = telemetry ON (default). Storage key fsbTelemetryOptOut stores
             true ONLY when the user has opted OUT. Inverse wiring at script block below. --&gt;
        &lt;div class="settings-card" id="card-privacy-telemetry"&gt;
          &lt;div class="settings-card-header"&gt;
            &lt;div class="settings-card-icon"&gt;
              &lt;i class="fas fa-shield-alt"&gt;&lt;/i&gt;
            &lt;/div&gt;
            &lt;div class="settings-card-title"&gt;
              &lt;h3&gt;Privacy &amp; Telemetry&lt;/h3&gt;
              &lt;p&gt;Anonymous usage data&lt;/p&gt;
            &lt;/div&gt;
          &lt;/div&gt;
          &lt;div class="settings-card-content"&gt;
            &lt;div class="setting-item"&gt;
              &lt;label class="modern-toggle"&gt;
                &lt;input type="checkbox" id="fsbTelemetryOptOut" checked
                       aria-label="Anonymous usage data is being sent. Click to stop."&gt;
                &lt;span class="toggle-track"&gt;&lt;span class="toggle-thumb"&gt;&lt;/span&gt;&lt;/span&gt;
                &lt;div class="toggle-content"&gt;
                  &lt;span class="toggle-label"&gt;Send anonymous usage data&lt;/span&gt;
                  &lt;span class="toggle-desc"&gt;Tokens used, MCP client name, active agent count. No URLs, prompts, or DOM.&lt;/span&gt;
                &lt;/div&gt;
              &lt;/label&gt;
              &lt;a class="setting-hint" href="/privacy#telemetry-disclosure"
                 target="_blank" rel="noopener noreferrer"
                 style="display:block; margin-top:8px;"&gt;Read full policy &rarr;&lt;/a&gt;
            &lt;/div&gt;
          &lt;/div&gt;
        &lt;/div&gt;
        ```

       The element classes (`settings-card`, `settings-card-header`, `settings-card-icon`, `settings-card-title`, `settings-card-content`, `setting-item`, `modern-toggle`, `toggle-track`, `toggle-thumb`, `toggle-content`, `toggle-label`, `toggle-desc`, `setting-hint`) are all existing FSB classes used by sibling Advanced Settings cards (see lines 344-474 for the existing patterns). Reuse them verbatim -- DO NOT introduce new classes, new stylesheets, or inline color overrides. The `fa-shield-alt` icon is in the existing FontAwesome bundle (verify via grep `fa-shield` in extension/ui/control_panel.html; if absent, fallback to `fa-lock`).

    3. At the bottom of the file, immediately before the final `</body>` close tag (find with grep for `</body>` -- there is exactly one), add a small inline `<script>` block:

        ```html
        &lt;script&gt;
          // Phase 269 / CONS-01, CONS-02: kill-switch wiring.
          // Storage key fsbTelemetryOptOut === true means user has opted OUT.
          // Toggle checkbox `checked` state is the INVERSE: checked = telemetry ON.
          (function () {
            const TOGGLE_ID = 'fsbTelemetryOptOut';
            const STORAGE_KEY = 'fsbTelemetryOptOut';

            function applyAriaLabel(checked) {
              const el = document.getElementById(TOGGLE_ID);
              if (!el) return;
              el.setAttribute('aria-label', checked
                ? 'Anonymous usage data is being sent. Click to stop.'
                : 'Anonymous usage data is NOT being sent. Click to re-enable.');
            }

            async function initPrivacyToggle() {
              const el = document.getElementById(TOGGLE_ID);
              if (!el) return;
              try {
                const data = await chrome.storage.local.get([STORAGE_KEY]);
                const optedOut = data &amp;&amp; data[STORAGE_KEY] === true;
                el.checked = !optedOut; // checked = telemetry ON
                applyAriaLabel(el.checked);
              } catch (_e) {
                // Storage unavailable -- default UI to ON (matches D-02 default-on policy).
                el.checked = true;
                applyAriaLabel(true);
              }
              el.addEventListener('change', async () => {
                const optedOut = !el.checked; // checked off -> opted out
                applyAriaLabel(el.checked);
                try {
                  await chrome.storage.local.set({ [STORAGE_KEY]: optedOut });
                } catch (_e) {
                  // No user-facing error per UI-SPEC ("no nag screen").
                  console.warn('[FSB Telemetry] opt-out write failed');
                }
              });
            }

            if (document.readyState === 'loading') {
              document.addEventListener('DOMContentLoaded', initPrivacyToggle);
            } else {
              initPrivacyToggle();
            }
          })();
        &lt;/script&gt;
        ```

       Rationale for an inline `<script>` (not a new `extension/ui/install-identity-ui.js` file): control_panel.html already uses inline DOMContentLoaded blocks for small UI wires; adding a fourth top-level UI module would balloon the importScripts-equivalent surface for ~30 lines of code. CONTEXT.md "Claude's Discretion" item allows this -- it says "extend existing UI wiring if a single file already wires Advanced Settings; otherwise add a small file". Since the document already mixes inline `<script>` blocks with external `<script src=...>` (verified at lines 1451-1478), the inline block is the lowest-friction choice and consistent with existing precedent. (per CONTEXT Claude's Discretion #1)

    4. Do NOT add a "Reset ID" button. Do NOT add a "Wipe my data" button. Do NOT add a first-run banner / modal / nag (CONTEXT hard constraint #8). Do NOT show the UUID value to the user (deferred to Phase 275 per CONTEXT line 98).

    5. Do NOT touch the navigation rail at line 74-77 (the `data-section="advanced"` nav item already exists and points to the existing `#advanced` section).

    6. Sanity check the toggle-position CONS-02 ("within 100ms"): the addEventListener path is a fire-and-forget `chrome.storage.local.set` after applying the aria-label and after the browser's native checkbox visual flip. The visual flip is synchronous (browser-native), so the visible state reflects the new value in well under 100ms; the storage write is async but the *visible* state is what CONS-02 requires.
  </action>
  <verify>
    <automated>grep -c "id=\"card-privacy-telemetry\"" extension/ui/control_panel.html | (read c; [ "$c" -eq 1 ] || { echo "FAIL: card-privacy-telemetry not found exactly once (got $c)"; exit 1; }; echo OK) &amp;&amp; grep -c "id=\"fsbTelemetryOptOut\"" extension/ui/control_panel.html | (read c; [ "$c" -eq 1 ] || { echo "FAIL: toggle id not found exactly once (got $c)"; exit 1; }; echo OK) &amp;&amp; grep -q "Send anonymous usage data" extension/ui/control_panel.html &amp;&amp; grep -q "Tokens used, MCP client name, active agent count. No URLs, prompts, or DOM." extension/ui/control_panel.html &amp;&amp; grep -q "/privacy#telemetry-disclosure" extension/ui/control_panel.html &amp;&amp; grep -q "Read full policy" extension/ui/control_panel.html &amp;&amp; grep -c "chrome\.storage\.local\.\(get\|set\)" extension/ui/control_panel.html | (read c; [ "$c" -ge 2 ] || { echo "FAIL: expected &ge;2 chrome.storage.local read/write sites for new toggle wiring, got $c"; exit 1; }; echo OK) &amp;&amp; grep -v '^[[:space:]]*//' extension/ui/control_panel.html | grep -c 'chrome\.storage\.sync' | (read c; [ "$c" -eq 0 ] || { echo "FAIL: chrome.storage.sync referenced in control_panel.html (IDENT-04 grep gate)"; exit 1; }; echo OK)</automated>
  </verify>
  <done>
    - extension/ui/control_panel.html contains exactly one `<div class="settings-card" id="card-privacy-telemetry">` block inside `.advanced-settings-grid` as the LAST child of that grid.
    - The card includes the toggle with id="fsbTelemetryOptOut" (checked by default), label "Send anonymous usage data", subtitle "Tokens used, MCP client name, active agent count. No URLs, prompts, or DOM.", and "Read full policy →" link pointing to /privacy#telemetry-disclosure.
    - An inline `<script>` block before `</body>` reads the storage key on DOMContentLoaded (sets `checked = !optedOut`) and writes the inverse on change-event. Aria-label flips between the two CONS-02 states.
    - No new CSS classes, no new external `.js` file, no chrome.storage.sync references.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add tests/install-identity.test.js + wire into npm test chain</name>
  <files>tests/install-identity.test.js, package.json</files>
  <behavior>
    The test file must cover, in order:

    1. Mint-once on first call: fresh chrome mock with empty local -> getOrCreateInstallUuid() returns a v4 UUID; local store now contains fsbInstallUuid = that UUID; local.set was invoked exactly once.
    2. Reuse-on-restart: pre-seed local with a valid v4 UUID -> getOrCreateInstallUuid() returns the same UUID; local.set was NOT invoked.
    3. Null on storage unavailable: chrome.storage.local.get throws -> getOrCreateInstallUuid() resolves to null; no uncaught exception observable.
    4. Opt-out round-trip: setTelemetryOptOut(true) then isTelemetryOptedOut() returns true; setTelemetryOptOut(false) -> false; setTelemetryOptOut() with missing key in store -> false.
    5. Defensive re-mint on corruption: pre-seed local with fsbInstallUuid = 'not-a-uuid' -> getOrCreateInstallUuid() returns a FRESH valid v4 UUID; store overwritten; console.warn was emitted exactly once with the expected prefix.
    6. IDENT-04 grep gate: the module source file is read from disk; assert ZERO occurrences of `chrome.storage.sync` (excluding comment lines).
    7. Storage-key string check: assert FSB_INSTALL_UUID_KEY === 'fsbInstallUuid' and FSB_TELEMETRY_OPT_OUT_KEY === 'fsbTelemetryOptOut' (catches accidental snake_case regressions to the REQUIREMENTS.md verbatim text -- per CONTEXT D-decision the camelCase is the locked truth).

    All tests run synchronously via top-level async IIFE, mirror the assertion style of tests/cost-tracker.test.js (assert / assertEqual / assert.deepStrictEqual). Use the createStorageArea + setupChromeMock helpers transplanted from tests/agent-cap-storage.test.js lines 26-80.

    Exit code: process.exit(failed > 0 ? 1 : 0) at the end. Print "PASS / FAIL" summary identical to neighboring tests.
  </behavior>
  <action>
    Create tests/install-identity.test.js:

    1. Use the Node CommonJS pattern from tests/cost-tracker.test.js (top-level `const` requires, `passed`/`failed` counters, assert / assertEqual / assertClose helpers, final `process.exit`).

    2. Transplant createStorageArea + setupChromeMock helpers from tests/agent-cap-storage.test.js lines 26-100 (omit the `session` storage piece -- this phase uses only `local`). Adapt setupChromeMock to install global.chrome with: runtime { id, lastError: null }, storage { local: createStorageArea(initial), onChanged: { addListener: noop } }. The mock must include a `_callCounts` accumulator on the local area so tests can assert "set was called once" / "set was NOT called".

    3. Add a helper `setupBrokenStorage()` that installs a chrome.storage.local whose `.get` and `.set` methods throw synchronously, so the null-on-unavailable test can verify the module's outer try/catch.

    4. Add a helper `captureWarn()` that monkey-patches console.warn into a counter+messages array, returning a `restore()` callback.

    5. Add a helper `freshRequire()` to delete the install-identity require cache and re-load it per-test so module-level side effects (globalThis.fsbInstallIdentity assignment) reset cleanly.

    6. Write the 7 test sections in order:
       - "--- Test: mint-once on first call (IDENT-01) ---"
       - "--- Test: reuse-on-restart (IDENT-02) ---"
       - "--- Test: null on storage unavailable (IDENT-03) ---"
       - "--- Test: opt-out round-trip (CONS-01, CONS-02) ---"
       - "--- Test: defensive re-mint on corruption ---"
       - "--- Test: IDENT-04 grep gate -- no chrome.storage.sync in module source ---"
       - "--- Test: storage-key string locks (camelCase per CONTEXT D-decision) ---"

       For the grep-gate test: `const src = require('fs').readFileSync(require.resolve('../extension/utils/install-identity.js'), 'utf8');` then strip `//` line comments and `/* ... */` block comments and assert `src.indexOf('chrome.storage.sync') === -1`.

    7. End with the summary block: `console.log('\n--- Summary ---'); console.log('passed:', passed); console.log('failed:', failed); process.exit(failed > 0 ? 1 : 0);`.

    Modify package.json:

    8. In the `test` script (the long `&&`-chained string), append ` && node tests/install-identity.test.js` immediately AFTER ` && node tests/cost-tracker.test.js` (it logically groups with cost / analytics tests). Preserve every other test name and order in the chain exactly. Do NOT modify any other npm script.

    Do NOT modify Jest / Mocha configuration files; FSB uses plain `node tests/*.test.js` invocations only (verified in package.json head -- no jest / mocha / vitest deps).
  </action>
  <verify>
    <automated>node tests/install-identity.test.js &amp;&amp; grep -c "node tests/install-identity\.test\.js" package.json | (read c; [ "$c" -eq 1 ] || { echo "FAIL: install-identity.test.js not wired (or wired twice) in package.json (got $c)"; exit 1; }; echo OK) &amp;&amp; node -e "const pkg = require('./package.json'); const test = pkg.scripts.test; const idxId = test.indexOf('install-identity.test.js'); const idxCost = test.indexOf('cost-tracker.test.js'); if (idxId === -1) { console.error('FAIL: install-identity.test.js missing from test script'); process.exit(1); } if (idxCost !== -1 &amp;&amp; idxId &lt; idxCost) { console.error('FAIL: install-identity.test.js should appear AFTER cost-tracker.test.js'); process.exit(1); } console.log('OK: test wiring order'); "</automated>
  </verify>
  <done>
    - tests/install-identity.test.js exists, runs under `node tests/install-identity.test.js`, prints a PASS line for every numbered test case (7 sections), and exits with code 0 when all pass.
    - package.json `scripts.test` chain contains exactly one occurrence of `node tests/install-identity.test.js`, positioned after `node tests/cost-tracker.test.js`.
    - The grep-gate inside the test passes (module source contains no chrome.storage.sync references outside comments).
    - All 7 test sections (mint-once, reuse-on-restart, null-on-unavailable, opt-out round-trip, defensive re-mint, IDENT-04 grep gate, storage-key string locks) report PASS.
  </done>
</task>

</tasks>

<verification>
Phase 269 ships when ALL of the following hold:

1. `node tests/install-identity.test.js` exits 0 with every section reporting PASS.
2. `grep -n "importScripts('utils/install-identity.js')" extension/background.js` shows the import at the TOP of the importScripts chain (before line containing `importScripts('utils/analytics.js')`).
3. `grep -c "fsbInstallIdentity\.getOrCreateInstallUuid" extension/background.js` >= 2 (onInstalled + onStartup hooks).
4. `grep -c "id=\"fsbTelemetryOptOut\"" extension/ui/control_panel.html` == 1.
5. `grep -c "Send anonymous usage data" extension/ui/control_panel.html` == 1.
6. `grep -v '^//\|^[[:space:]]*\*\|^[[:space:]]*//' extension/utils/install-identity.js | grep -c 'chrome\.storage\.sync'` == 0 (IDENT-04 hard gate).
7. `npm test` succeeds end-to-end (full chain passes, no regressions to existing tests).
8. Manual smoke (developer): load unpacked extension on a fresh profile, open Control Panel > Advanced Settings; "Privacy & Telemetry" card is present, toggle is ON, click flips it OFF with visible response well under 100ms, refreshing the panel shows the toggle still OFF; reloading the extension preserves the toggle state and preserves the UUID stored under `chrome.storage.local.fsbInstallUuid` (verify via chrome://extensions devtools -> storage inspector).

No outbound HTTP, no event collection, no server contract -- those land in Phases 272 / 273.
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User device <-> chrome.storage.local | Storage is local-only; never crosses the device. NO chrome.storage.sync (would create cross-device linkability -- D-11, IDENT-04). |
| Service worker module load <-> downstream consumers | install-identity.js loads at TOP of importScripts; downstream modules (Phase 271 MCPMetricsRecorder, Phase 272 TelemetryCollector) call via globalThis.fsbInstallIdentity. If module fails to load, downstream callers see undefined -- handled by Phase 272's null-check (IDENT-03). |
| Control Panel page <-> service worker | Document context (control_panel.html) reads chrome.storage.local directly; does NOT call into service-worker module surface. Storage is the IPC channel. |
| Future Phase 272 outbound POST <-> server | NOT in scope for Phase 269. No outbound HTTP, no fetch, no XHR added by this phase. Threat surface remains exactly what it was before Phase 269. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-269-01 | I (Information disclosure) | extension/utils/install-identity.js storage namespace | mitigate | Hard-code `chrome.storage.local` only (D-11, IDENT-04). Grep-gate in Task 3 test asserts ZERO `chrome.storage.sync` references in module source (excluding comments). Future cross-device linkability via `chrome.storage.sync` is impossible without first failing CI. |
| T-269-02 | T (Tampering) | Stored UUID in chrome.storage.local | mitigate | Defensive v4-regex validation on every read. A corrupted / hand-edited fsbInstallUuid value (e.g. via Chrome DevTools storage editor) triggers a re-mint, one warn log, and continues. The user can choose to wipe the value via DevTools but cannot inject an arbitrary string that masquerades as a UUID downstream. |
| T-269-03 | D (Denial of service) | Storage unavailability (corrupted profile, enterprise policy) | mitigate | getOrCreateInstallUuid() returns null on storage error (IDENT-03). Phase 272's TelemetryCollector treats null as a hard no-op. The extension's core automation function is unaffected -- this phase adds zero blocking dependencies on storage success. |
| T-269-04 | I (Information disclosure) | Identity module export surface | mitigate | Module exports ONLY identity primitives: getOrCreateInstallUuid, isTelemetryOptedOut, setTelemetryOptOut, two key constants, one regex. No URL / DOM / prompt / clipboard / form-value fields. Test 6 in Task 3 asserts grep-gate over the module source. Downstream payload allowlist (Phase 273 INGEST-09) is the gate for outbound data; this phase contributes only the UUID. |
| T-269-05 | E (Elevation of privilege via consent bypass) | Control Panel toggle wiring | accept | The toggle directly writes chrome.storage.local. A malicious extension or sibling installed alongside FSB could theoretically read fsbTelemetryOptOut. This is the same trust posture as every other FSB local-storage key (fsbUsageData, fsbCurrentModel, etc.) and is constrained by Chrome's per-extension storage isolation. ACCEPT: documented in privacy policy (Phase 275 / CONS-03). |
| T-269-06 | R (Repudiation) | onInstalled / onStartup boot log | mitigate | The single `console.log('[FSB Telemetry] Install UUID seeded')` line at boot is a developer-visible audit trail. The seed call is wrapped in try/catch with a distinct `console.error('[FSB Telemetry] Install UUID seed failed:', ...)` path that pins seed failure to a recognizable string -- searchable in chrome://extensions logs. NO PII (the UUID is not logged). |
| T-269-07 | S (Spoofing) | UUID generation source | mitigate | `crypto.randomUUID()` is the Node 20+ / Chrome 92+ MV3 native, RFC 4122 v4 compliant generator. Decision D-01 / STACK §3c. No third-party npm `uuid` / `nanoid` introduces a supply-chain seam. |
</threat_model>

<success_criteria>
**Observable user behavior (must all be true):**

1. After a fresh install, the user can open the extension Control Panel > Advanced Settings and see a "Privacy & Telemetry" card with a "Send anonymous usage data" toggle that is ON by default.
2. The user can flip the toggle OFF -- the visible track / thumb position changes within 100ms with NO "Apply" button and NO nag screen / modal.
3. The user can reload the extension and the toggle state persists.
4. The user can trigger an extension service-worker eviction + revival cycle (any 30s+ idle period, or `chrome://extensions` -> Inspect -> close DevTools) and confirm via storage inspector that `fsbInstallUuid` and `fsbTelemetryOptOut` survive unchanged.
5. On an incognito profile (where chrome.storage.local is unavailable for the SW context), the boot-time UUID seed does not throw or crash; the extension boots normally.

**Observable code state:**

1. `extension/utils/install-identity.js` is the only place where `crypto.randomUUID()` is called in this phase's diff.
2. The module source contains ZERO occurrences of `chrome.storage.sync` (excluding comments).
3. Both `chrome.runtime.onInstalled` and `chrome.runtime.onStartup` in `extension/background.js` call `await globalThis.fsbInstallIdentity.getOrCreateInstallUuid()` between `initializeAnalytics()` and `loadDebugMode()`.
4. `npm test` passes end-to-end (full chain), including the new `tests/install-identity.test.js`.

**Observable next-phase readiness (Phase 271 / 272 unblock test):**

5. From Node, `require('./extension/utils/install-identity.js')` returns an object with the 6 required identifiers. Phase 271 / 272 can build against this surface.
</success_criteria>

<output>
After all tasks complete, create `.planning/phases/269-install-identity-opt-out-scaffold/269-01-SUMMARY.md` describing:
- Tasks completed (3/3).
- Files created: `extension/utils/install-identity.js`, `tests/install-identity.test.js`.
- Files modified: `extension/background.js` (importScripts + 2 lifecycle hooks), `extension/ui/control_panel.html` (1 new settings-card + 1 inline script block), `package.json` (1 new line in `test` chain).
- Requirements satisfied: IDENT-01, IDENT-02, IDENT-03, IDENT-04, IDENT-05, CONS-01, CONS-02 (7/7).
- Decisions implemented (with file:line refs): D-01 (UUID v4 via crypto.randomUUID), D-02 (kill-switch only, ON by default, no banner), D-11 / IDENT-04 (chrome.storage.local only, grep-gated).
- Patterns to follow downstream: future telemetry modules (Phase 271 / 272) should call `globalThis.fsbInstallIdentity.getOrCreateInstallUuid()` lazily on first use and short-circuit on null; check `isTelemetryOptedOut()` on every flush per BEAT-07.
- Test commands: `node tests/install-identity.test.js` and `npm test`.
</output>
