---
phase: 38-breadth-b-comms-social-content-sensitivity-screened
plan: 02
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - vendor/opentabs-snapshot/plugins/chatgpt/package.json
  - vendor/opentabs-snapshot/plugins/chatgpt/src/sdk-stub.ts
  - vendor/opentabs-snapshot/plugins/chatgpt/src/index.ts
  - vendor/opentabs-snapshot/plugins/chatgpt/src/chatgpt-api.ts
  - vendor/opentabs-snapshot/plugins/chatgpt/src/tools/list-conversations.ts
  - vendor/opentabs-snapshot/plugins/chatgpt/src/tools/get-conversation.ts
  - vendor/opentabs-snapshot/plugins/chatgpt/src/tools/send-message.ts
  - vendor/opentabs-snapshot/plugins/claude/package.json
  - vendor/opentabs-snapshot/plugins/claude/src/sdk-stub.ts
  - vendor/opentabs-snapshot/plugins/claude/src/index.ts
  - vendor/opentabs-snapshot/plugins/claude/src/claude-api.ts
  - vendor/opentabs-snapshot/plugins/claude/src/tools/list-conversations.ts
  - vendor/opentabs-snapshot/plugins/claude/src/tools/get-conversation.ts
  - vendor/opentabs-snapshot/plugins/claude/src/tools/send-message.ts
  - vendor/opentabs-snapshot/plugins/bluesky/package.json
  - vendor/opentabs-snapshot/plugins/bluesky/src/sdk-stub.ts
  - vendor/opentabs-snapshot/plugins/bluesky/src/index.ts
  - vendor/opentabs-snapshot/plugins/bluesky/src/bluesky-api.ts
  - vendor/opentabs-snapshot/plugins/bluesky/src/tools/list-timeline.ts
  - vendor/opentabs-snapshot/plugins/bluesky/src/tools/get-profile.ts
  - vendor/opentabs-snapshot/plugins/bluesky/src/tools/create-post.ts
  - vendor/opentabs-snapshot/plugins/bluesky/src/tools/delete-post.ts
  - vendor/opentabs-snapshot/plugins/mastodon/package.json
  - vendor/opentabs-snapshot/plugins/mastodon/src/sdk-stub.ts
  - vendor/opentabs-snapshot/plugins/mastodon/src/index.ts
  - vendor/opentabs-snapshot/plugins/mastodon/src/mastodon-api.ts
  - vendor/opentabs-snapshot/plugins/mastodon/src/tools/list-timeline.ts
  - vendor/opentabs-snapshot/plugins/mastodon/src/tools/get-status.ts
  - vendor/opentabs-snapshot/plugins/mastodon/src/tools/create-status.ts
  - vendor/opentabs-snapshot/plugins/mastodon/src/tools/delete-status.ts
  - vendor/opentabs-snapshot/plugins/threads/package.json
  - vendor/opentabs-snapshot/plugins/threads/src/sdk-stub.ts
  - vendor/opentabs-snapshot/plugins/threads/src/index.ts
  - vendor/opentabs-snapshot/plugins/threads/src/threads-api.ts
  - vendor/opentabs-snapshot/plugins/threads/src/tools/list-timeline.ts
  - vendor/opentabs-snapshot/plugins/threads/src/tools/get-thread.ts
  - vendor/opentabs-snapshot/plugins/threads/src/tools/create-thread.ts
  - vendor/opentabs-snapshot/PIN.md
  - catalog/descriptors/_fixtures/_provenance.json
  - catalog/descriptors/_fixtures/intent-cases.json
  - extension/catalog/recipe-index.generated.js
  - tests/breadth-search-return.test.js
  - tests/capability-search-eval.test.js
autonomous: true
requirements: [BRDTH-01, BRDTH-03]

must_haves:
  truths:
    - "chatgpt, claude (AI-chat) + bluesky, mastodon, threads (microblog/fediverse) descriptors are imported via the FROZEN Plan-37 machinery (NO importer edit) and returned by search with >=3 app-disambiguated synonyms + side-effect class + backing field 'dom' per op"
    - "Every emitted descriptor for this sub-batch carries backing:'dom' (the frozen conservative default) -> search returns it invocable=false / backingStatus='discovery-pending' -- DOM-only, never a confident invocable hit, never learn-seeded (the AI-chat + social ToS-hostile routing)"
    - "The merge-time classifyGate PASSES on this sub-batch because Plan 38-01 classified chatgpt.com/claude.ai/bsky.app/mastodon.social/threads.net (all sensitive) -- the import does NOT abort; running the importer WITHOUT the 38-01 classification would have aborted fail-closed (the gate ordering is real)"
    - "The write ops (bluesky.create_post / mastodon.create_status / threads.create_thread class write; *.delete_* class destructive; chatgpt/claude send_message class write) are correctly side-effect-classed; crosscheck stays green over the grown corpus"
    - "After this sub-batch lands, node tests/no-dead-entry.test.js (the generalized opentabs__* loader) stays exit 0 (every new comms/social descriptor-only slug resolves to T3) and the eval keeps recall@5>=0.9 wrong-invoke=0.000 (the Plan-37 importer seed-feeds every emitted slug); the SW cold-start budget holds at the grown size"
    - "npm run validate:extension exits 0 and the breadth/eval suites are green on the grown corpus"
  artifacts:
    - path: "vendor/opentabs-snapshot/plugins/chatgpt/src/index.ts"
      provides: "vendored chatgpt AI-chat metadata slice (host chatgpt.com; DOM-only by the frozen backing default; send_message is the sensitive write that posture-B gates)"
      contains: "ChatgptPlugin"
    - path: "vendor/opentabs-snapshot/plugins/bluesky/src/tools/create-post.ts"
      provides: "a social WRITE op (create_post) to exercise the side-effect write class + the sensitive-write routing on a screened social origin"
      contains: "create_post"
    - path: "vendor/opentabs-snapshot/plugins/mastodon/src/tools/delete-status.ts"
      provides: "a social DESTRUCTIVE op (delete_status) to exercise the crosscheck destructive class on the grown corpus"
      contains: "delete_status"
    - path: "extension/catalog/recipe-index.generated.js"
      provides: "the snapshot REGENERATED after this sub-batch emit (INV-01 IIFE/djb2 unchanged; only the DATA literal grows) so validate:extension reconciles"
    - path: "tests/breadth-search-return.test.js"
      provides: "the per-op breadth assertion EXTENDED to cover the AI-chat + microblog sub-batch (every op returns with synonyms + class + backing:'dom')"
      contains: "chatgpt"
  key_links:
    - from: "scripts/import-opentabs-catalog.mjs enumerateBatchApps"
      to: "vendor/opentabs-snapshot/plugins/{chatgpt,claude,bluesky,mastodon,threads}"
      via: "the FROZEN Plan-37 enumeration auto-discovers the new vendored dirs (no importer edit) and emits their descriptors"
      pattern: "opentabs__(chatgpt|claude|bluesky|mastodon|threads)__"
    - from: "scripts/import-opentabs-catalog.mjs runImport classifyGate"
      to: "extension/config/service-denylist.json (classified by Plan 38-01)"
      via: "the merge-time batch-coverage gate consults classify() over the new origins; Plan 38-01's classification makes them governed -> no abort"
      pattern: "classifyGate|sensitiveOrigins"
    - from: "scripts/import-opentabs-catalog.mjs backingFor"
      to: "every emitted descriptor's backing field"
      via: "the frozen backingFor() returns 'dom' -> DOM-only routing for the whole sub-batch (the conservative default; no edit)"
      pattern: "backing.*dom|backingFor"
    - from: "scripts/import-opentabs-catalog.mjs feedSeedDescriptors"
      to: "catalog/descriptors/_fixtures/seed-descriptors.json"
      via: "the Plan-37 seed-feeding upserts each newly emitted slug into the eval-indexed seed so this plan's new intent-cases resolve (recall + wrong-invoke=0)"
      pattern: "seed-descriptors|opentabs__(chatgpt|claude|bluesky|mastodon|threads)__"
---

<objective>
Import the AI-chat + microblog/fediverse sub-batch of the comms/social/content category —
chatgpt, claude, bluesky, mastodon, threads — as DATA-ONLY metadata slices via the FROZEN
Phase-37 machinery (NO importer edit), gated on the Plan 38-01 screening, then re-assert the
per-wave gates over the grown corpus:

1. Vendor the five apps as hermetic metadata slices (Wall 1: metadata-only, NO dist/, NO
   executed handle()), mirroring the proven Phase-37 slice contract. None is in STEM_OVERRIDES
   (chatgpt.com->chatgpt, claude.ai->claude, bsky.app->bsky, mastodon.social->mastodon,
   threads.net->threads all derive correctly) -> genuinely data-only.
2. Run the FROZEN importer: enumerateBatchApps auto-discovers the new dirs; the merge-time
   classifyGate PASSES because Plan 38-01 classified all five origins sensitive (running it
   WITHOUT 38-01 would abort fail-closed -- the gate ordering is real, not decorative). Every
   emitted descriptor carries backing:'dom' (the frozen conservative default) -> DOM-only (T3,
   never confident-invocable, never learn-seeded) -- the AI-chat + social ToS-hostile routing
   with ZERO machinery edit.
3. Regenerate the snapshot; validate:extension reconciles. Re-assert the per-wave gates over
   the grown corpus: crosscheck (the social write/destructive ops classed correctly) +
   no-dead-entry (the generalized loader checks every new descriptor-only slug -> T3) + the eval
   (recall@5>=0.9, wrong-invoke=0; the importer seed-feeds every new slug) + the SW cold-start
   budget at the grown size.

Purpose: deliver the first sub-batch of the sensitive comms/social category as DOM-only data,
proving the screened import works -- the conservative default makes AI-chat + social apps
discoverable-but-DOM-only, and the gate ordering (screen-then-import) is enforced.
Output: chatgpt/claude/bluesky/mastodon/threads descriptors (all backing:'dom'); regenerated
snapshot; per-wave gates green over the grown corpus.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-breadth-b-comms-social-content-sensitivity-screened/38-CONTEXT.md
@.planning/phases/38-breadth-b-comms-social-content-sensitivity-screened/38-01-SUMMARY.md

<interfaces>
<!-- The FROZEN Plan-37 machinery this plan reuses (do NOT edit it). -->
- scripts/import-opentabs-catalog.mjs: enumerateBatchApps() imports EVERY vendored plugin dir
  except already-head services (github.com/app.slack.com/www.notion.so), todoist, and
  /e2e|prescript|-test$/ fixtures. Vendoring a new app dir is sufficient -- NO importer edit.
  synthSynonyms emits >=3 app-disambiguated phrases (every phrase carries the serviceStem token);
  extractDescriptors stamps backing:'dom' via backingFor() (which ignores its args -> ALWAYS 'dom'
  for this data batch -> DOM-only routing); runImport() aborts on any unclassified batch origin via
  classifyGate AND feeds each emitted descriptor's searchable shape into _fixtures/seed-descriptors
  .json (the eval seed). The slug/filename is opentabs__<serviceStem>__<op>.json.
- STEM_OVERRIDES (Plan-37): { jira, confluence, cloudflare, datadog } only. NONE of this plan's
  apps is in it -- chatgpt.com->'chatgpt', claude.ai->'claude', bsky.app->'bsky',
  mastodon.social->'mastodon', threads.net->'threads' all derive correctly through the frozen
  host-derivation (service.replace(/^app\./,'').split('.')[0]). Do NOT add a STEM_OVERRIDES entry.
- The host-derivation: service = package.json.opentabs.urlPatterns[0] host (leading '*.' stripped);
  serviceStem = host.replace(/^app\./,'').split('.')[0]. So urlPatterns MUST be the authoritative
  host: chatgpt -> "*://chatgpt.com/*" (stem chatgpt); claude -> "*://claude.ai/*" (stem claude);
  bluesky -> "*://bsky.app/*" (stem bsky -- NOTE the dir is `bluesky` but the host/stem/slug is
  `bsky`; the dir name and the stem differ, which is fine -- the slug follows the HOST stem);
  mastodon -> "*://mastodon.social/*" (stem mastodon); threads -> "*://threads.net/*" (stem threads,
  via the package.json host threads.net -> NOT www.threads.net so the stem is threads).
- The vendored-slice contract (replicate todoist/linear EXACTLY): package.json with
  opentabs.urlPatterns[0]=*://<host>/* + _role "opentabs-metadata-snapshot" + license MIT;
  src/sdk-stub.ts (VERBATIM copy of todoist's sdk-stub.ts -- exports defineTool identity factory +
  abstract OpenTabsPlugin); src/<app>-api.ts (inert transport stub: api/apiVoid throwers, a comment
  recording the upstream method per op); src/index.ts (class <App>Plugin extends OpenTabsPlugin with
  name/description/displayName/urlPatterns/homepage/tools[]; then const plugin=new <App>Plugin();
  export default plugin; export { plugin }); src/tools/<op>.ts (export const <opCamel> = defineTool({
  name:'<op_snake>', displayName, description, summary, icon, group, input: z.object({...}), output:
  z.object({...}), handle: async(...) => { /* NEVER executed; comment names the upstream helper+method */ } }).
- side-effect verbs (scripts/lib/side-effect-class.mjs, FROZEN): WRITE_VERBS includes create/send/
  post/update; DESTRUCTIVE_VERBS includes delete/remove. READ_VERBS includes list/get/search/query.
  So create_post/create_status/create_thread/send_message class WRITE on the op-name verb;
  delete_post/delete_status class DESTRUCTIVE; list_*/get_* class read. Reinforce send_message and
  the create_* ops with an explicit {method:'POST'} literal in the stub (the methodClass floor) and
  delete_* with apiVoid {method:'DELETE'} (apiDelete -> destructive) so the class is unambiguous on
  BOTH the verb and method axes. Do NOT add any verb to the shared module.
- The CGEN-04 cold-start asserts in tests/capability-search-eval.test.js: serialize the built index
  + assert the serialized length + loadJSON+first-search stay within budget. They re-run at the
  grown size; params are schema-on-hit (NOT indexed/stored) so the per-descriptor footprint stays
  flat. Keep generous gates that still PROVE the layout (raise only if the grown index legitimately
  exceeds the gate while the per-descriptor footprint stays flat -- full-corpus scale is Phase 43).
- The eval (capability-search-eval.test.js) indexes seed-descriptors.json + iterates intent-cases
  .json. The importer seed-feeds every emitted slug, so this plan's new intent-cases have an indexed
  descriptor. Do NOT hand-edit seed-descriptors.json -- the importer populates it.
- NOTE: www.reddit.com is NOT in this plan (reddit content-reads are Plan 03's safe tier); do NOT
  vendor reddit here. discord is Plan 03 (the sensitive-write end-to-end proof origin).
</interfaces>

# Reuse references (the proven Phase-37 slice + import flow):
@vendor/opentabs-snapshot/plugins/todoist/src/index.ts
@vendor/opentabs-snapshot/plugins/todoist/src/sdk-stub.ts
@vendor/opentabs-snapshot/plugins/todoist/src/todoist-api.ts
@vendor/opentabs-snapshot/plugins/todoist/src/tools/create-task.ts
@vendor/opentabs-snapshot/plugins/todoist/src/tools/delete-task.ts
@vendor/opentabs-snapshot/plugins/linear/src/index.ts
@vendor/opentabs-snapshot/PIN.md
@tests/capability-search-eval.test.js
@tests/breadth-search-return.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor the chatgpt + claude + bluesky + mastodon + threads metadata slices (DOM-only by the frozen default)</name>
  <files>vendor/opentabs-snapshot/plugins/{chatgpt,claude,bluesky,mastodon,threads}/** (each: package.json, src/sdk-stub.ts, src/index.ts, src/<app>-api.ts, src/tools/*.ts), vendor/opentabs-snapshot/PIN.md</files>
  <read_first>
    - vendor/opentabs-snapshot/plugins/todoist/ (the FULL slice structure to replicate: package.json + sdk-stub.ts + todoist-api.ts + index.ts + tools/)
    - vendor/opentabs-snapshot/plugins/todoist/src/tools/create-task.ts (a write op via `api` POST -- the create/send shape)
    - vendor/opentabs-snapshot/plugins/todoist/src/tools/delete-task.ts (the destructive shape for delete_post/delete_status)
    - vendor/opentabs-snapshot/plugins/linear/src/index.ts (a second <App>Plugin example)
    - scripts/import-opentabs-catalog.mjs (confirm NONE of these five apps is in STEM_OVERRIDES and the host-derivation gives the right stem; do NOT edit)
    - vendor/opentabs-snapshot/PIN.md (the SHA 4b170216 + MIT pin + the per-phase append-only log)
    - .planning/phases/38-breadth-b-comms-social-content-sensitivity-screened/38-CONTEXT.md (the batch membership + the DOM-only-default rule)
  </read_first>
  <action>
    Create the five vendored metadata slices mirroring the todoist slice structure EXACTLY (Wall 1:
    metadata-only, NO dist/, NO executed handle()). For EACH app: package.json (opentabs.urlPatterns
    host per the interfaces block + _role "opentabs-metadata-snapshot" + license MIT), src/sdk-stub.ts
    (a VERBATIM copy of todoist's sdk-stub.ts), src/index.ts (the <App>Plugin with the ops below),
    src/<app>-api.ts (an inert transport stub -- api/apiVoid throwers, a comment recording the upstream
    method per op), and src/tools/<op>.ts per op with a real z.object input + a never-run handle whose
    comment names the upstream helper+method.

    Hosts (the AUTHORITATIVE urlPatterns[0], driving the stem -- NO STEM_OVERRIDES):
      - chatgpt  -> "*://chatgpt.com/*"        (stem chatgpt)
      - claude   -> "*://claude.ai/*"          (stem claude)
      - bluesky  -> "*://bsky.app/*"           (stem bsky -- dir `bluesky`, host/stem/slug `bsky`)
      - mastodon -> "*://mastodon.social/*"    (stem mastodon)
      - threads  -> "*://threads.net/*"        (stem threads -- use threads.net NOT www.threads.net)

    Ops per app (real z.object inputs reflecting each op's actual params -- title/text/handle/ids,
    NOT empty schemas):
      - chatgpt: list_conversations (read), get_conversation (read), send_message (WRITE -- the
        sensitive AI-chat write). send_message handle references api('/conversation',{method:'POST',...})
        (a comment names the upstream POST -> write).
      - claude:  list_conversations (read), get_conversation (read), send_message (WRITE). Same shape.
      - bluesky: list_timeline (read), get_profile (read), create_post (WRITE -- create verb +
        {method:'POST'}), delete_post (DESTRUCTIVE -- apiVoid {method:'DELETE'} -> apiDelete/destructive).
      - mastodon: list_timeline (read), get_status (read), create_status (WRITE), delete_status (DESTRUCTIVE).
      - threads: list_timeline (read), get_thread (read), create_thread (WRITE).

    Make the side-effect signals explicit so crosscheck classes correctly: the create_*/send_message
    ops carry an `api(..., { method:'POST' })` literal in the never-run handle (the methodClass write
    floor reinforcing the create/send verb); the delete_* ops use `apiVoid(..., { method:'DELETE' })`
    (the apiDelete/DELETE destructive signal). Do NOT add or edit any STEM_OVERRIDES entry, and do
    NOT edit the importer / side-effect-class.mjs (the create/send/delete verbs are already in the
    shared sets; the method literals are defense-in-depth).

    Append chatgpt/claude/bluesky/mastodon/threads to vendor/opentabs-snapshot/PIN.md's per-phase log
    as a new Phase-38 row (batch B sub-batch 1: AI-chat + microblog/fediverse; all DOM-only by the
    frozen backing default; screened sensitive in 38-01). Do NOT vendor any e2e/prescript fixture or
    any DENY-01 denied app (netflix/spotify/twitch/steam/youtube-music/tinder/onlyfans).
  </action>
  <verify>
    <automated>node --import tsx -e "const apps=['chatgpt','claude','bluesky','mastodon','threads'];(async()=>{for(const a of apps){const m=await import('./vendor/opentabs-snapshot/plugins/'+a+'/src/index.ts');const p=m.default;if(!p||!Array.isArray(p.tools)||p.tools.length<3){console.error('bad slice',a);process.exit(1)}const host=(p.urlPatterns&&p.urlPatterns[0])||'';console.log(a,'->',host,p.tools.map(t=>t.name).join(','))}process.exit(0)})().catch(e=>{console.error(e.message);process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - All five slices import under tsx with >=3 ops each and real z.object inputs (z.toJSONSchema yields properties, not {}); no handle() executes during import (inert stubs throw only if called).
    - urlPatterns hosts are exactly chatgpt.com / claude.ai / bsky.app / mastodon.social / threads.net (so the derived stems are chatgpt/claude/bsky/mastodon/threads -- no STEM_OVERRIDES needed).
    - bluesky exposes create_post + delete_post; mastodon exposes create_status + delete_status; chatgpt/claude expose send_message (the sensitive writes); the create_*/send ops carry a {method:'POST'} literal and the delete_* ops an apiVoid {method:'DELETE'}.
    - No DENY-01 denied app and no e2e/prescript fixture was vendored; no STEM_OVERRIDES entry added; the importer/side-effect-class.mjs untouched.
  </acceptance_criteria>
  <done>chatgpt/claude/bluesky/mastodon/threads vendored as hermetic metadata slices importable under tsx with correct hosts/stems and explicit write/destructive method signals; PIN.md per-phase log updated (Phase-38 batch B sub-batch 1).</done>
</task>

<task type="auto">
  <name>Task 2: Import the AI-chat + microblog sub-batch via the frozen importer, regenerate the snapshot, re-assert the per-wave gates</name>
  <files>extension/catalog/recipe-index.generated.js, catalog/descriptors/_fixtures/intent-cases.json, catalog/descriptors/_fixtures/_provenance.json, tests/breadth-search-return.test.js, tests/capability-search-eval.test.js</files>
  <read_first>
    - scripts/import-opentabs-catalog.mjs (the FROZEN enumerateBatchApps + runImport + classifyGate + feedSeedDescriptors -- RUN, do NOT edit)
    - scripts/package-extension.mjs + scripts/validate-extension.mjs (the snapshot regen + the byte-compare)
    - tests/breadth-search-return.test.js (the per-op breadth assertion + the corpusFiles regex ~line 77-79 to extend to the new apps)
    - tests/capability-search-eval.test.js (the eval harness + the CGEN-04 cold-start asserts)
    - tests/no-dead-entry.test.js (the generalized opentabs__* seam-resolution invariant -- run, do NOT edit)
    - catalog/descriptors/_fixtures/intent-cases.json (the eval fixtures to grow with the new apps)
    - .planning/phases/38-breadth-b-comms-social-content-sensitivity-screened/38-01-SUMMARY.md (confirm the five origins are classified sensitive so the gate passes)
  </read_first>
  <action>
    (a) Run `node --import tsx ./scripts/import-opentabs-catalog.mjs` to emit the five new apps via
    the FROZEN machinery. Confirm: opentabs__chatgpt__*, opentabs__claude__*, opentabs__bsky__*,
    opentabs__mastodon__*, opentabs__threads__* appear (note the bluesky dir emits the bsky stem);
    EVERY emitted descriptor carries backing:'dom' (the conservative default); the merge-time
    classifyGate PASSES (it does NOT abort) because Plan 38-01 classified all five origins sensitive;
    existing descriptors (the 70 dev/productivity + the github/slack/notion/reddit heads) are
    untouched; seed-descriptors.json carries the new slugs (importer-fed).

    (b) Run `node scripts/package-extension.mjs` to regenerate extension/catalog/recipe-index.generated
    .js (INV-01: the IIFE first line + the djb2/dual-export tail byte-stable; only the DATA literal
    grows); confirm `node scripts/validate-extension.mjs` exits 0 (snapshot reconciled; classification
    gate + crosscheck + recipe-path guard green over the grown corpus).

    (c) Extend catalog/descriptors/_fixtures/intent-cases.json with read + write + destructive fixtures
    for the new apps (at least: a read e.g. "show my bluesky timeline" -> bsky.list_timeline; a write
    e.g. "post to bluesky" -> bsky.create_post and "send a message in chatgpt" -> chatgpt.send_message;
    a destructive e.g. "delete a mastodon post" -> mastodon.delete_status) with expectedSlug aligned to
    the emitted REAL slugs (the bsky/chatgpt/claude/mastodon/threads stems). Extend
    tests/breadth-search-return.test.js: widen the corpusFiles regex (~line 77-79) to include
    (chatgpt|claude|bsky|mastodon|threads) so the per-op assertion (>=3 synonyms + side-effect class +
    backing) covers the new sub-batch, and raise the corpus.length floor to match the grown count. Do
    NOT hand-edit seed-descriptors.json -- the importer's seed-feeding already populated it.

    (d) Re-assert the per-wave gates over the grown corpus: `node scripts/verify-catalog-crosscheck.mjs`
    (exit 0 -- create_*/send_message write, delete_* destructive, list_*/get_* read; no under-statement),
    `node tests/no-dead-entry.test.js` (exit 0 -- the generalized loader checks every new opentabs__
    descriptor-only slug, all resolving to a non-null T3 seam since backing:'dom'), and
    `node tests/capability-search-eval.test.js` (recall@5>=0.9, wrong-invoke=0.000 -- the seed-fed new
    slugs resolve; the AI-chat/social ops disambiguate by app token; AND the CGEN-04 cold-start asserts
    pass at the grown size). If the grown index legitimately exceeds the smoke cold-start gate while the
    per-descriptor footprint stays flat (params not indexed), widen the assert budget with a comment
    noting the grown descriptor count -- do NOT change the data layout and do NOT defer the full-corpus
    scale gate (that is Phase 43).

    Do NOT edit the importer, capability-search.js, the gates, or service-denylist.json (the screening
    is frozen from Plan 38-01; the machinery from Phase 37).
  </action>
  <verify>
    <automated>node --import tsx ./scripts/import-opentabs-catalog.mjs && test -f catalog/descriptors/opentabs__chatgpt__send_message.json && test -f catalog/descriptors/opentabs__bsky__create_post.json && test -f catalog/descriptors/opentabs__mastodon__delete_status.json && node -e "const fs=require('fs');for(const s of ['opentabs__chatgpt__send_message','opentabs__bsky__create_post','opentabs__mastodon__delete_status','opentabs__claude__send_message','opentabs__threads__create_thread']){const d=JSON.parse(fs.readFileSync('catalog/descriptors/'+s+'.json','utf8'));if(d.backing!=='dom'){console.error('NOT dom-backed:',s,d.backing);process.exit(1)}}console.log('PASS all sub-batch descriptors backing=dom')" && node scripts/package-extension.mjs && npm run validate:extension && node tests/no-dead-entry.test.js && node tests/breadth-search-return.test.js && node tests/capability-search-eval.test.js</automated>
  </verify>
  <acceptance_criteria>
    - The five apps' descriptors are emitted (opentabs__chatgpt__*, opentabs__claude__*, opentabs__bsky__*, opentabs__mastodon__*, opentabs__threads__*) and inlined; EVERY one carries backing:'dom'; the merge-time classifyGate did NOT abort (Plan 38-01 classified the origins); existing descriptors untouched.
    - validate:extension exits 0 (snapshot reconciled; classification gate + crosscheck + recipe-path guard green); INV-01 IIFE/djb2 shape byte-stable.
    - verify-catalog-crosscheck.mjs exits 0 (create_*/send_message write; bluesky.delete_post / mastodon.delete_status destructive; list_*/get_* read -- no under-statement).
    - no-dead-entry.test.js exits 0 over the grown corpus (every new opentabs__ slug resolves to a non-null T3 seam).
    - capability-search-eval.test.js prints recall@5>=0.9, wrong-invoke=0.000 (seed-fed new slugs resolve; no cross-app mis-invoke), and its cold-start asserts pass at the grown size.
    - breadth-search-return.test.js passes covering the new AI-chat + microblog ops (synonyms + class + backing:'dom').
  </acceptance_criteria>
  <done>The AI-chat + microblog/fediverse sub-batch is imported as DOM-only data via the frozen machinery (gate passed because 38-01 screened the origins), the snapshot regenerated, and the per-wave gates re-asserted over the grown corpus: crosscheck + no-dead-entry + eval green and the cold-start budget held.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OpenTabs vendored metadata -> FSB descriptor corpus (build-time) | the AI-chat + social metadata read by the importer; a forbidden field or a denied app must never enter the corpus |
| screened origin classification -> merge-time classifyGate (build-time) | the import must abort if any of these five origins were left unscreened (38-01 must precede 38-02) |
| imported comms/social descriptor -> search hit -> invoke (runtime) | the AI-chat + social ops must surface DOM-only (discovery-pending), never confident-invocable; their writes flow to the posture-B re-gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | Elevation of Privilege | an AI-chat / social app surfaced as confident-invocable -> writable under Auto (send a message / post on someone's account) | mitigate | the frozen backingFor() stamps backing:'dom' for every emitted descriptor -> DOM-only (T3), invocable=false / discovery-pending, never learn-seeded (asserted on every sub-batch descriptor in Task 2); writes additionally flow through the posture-B re-gate (the origins are sensitive per 38-01) |
| T-38-06 | Elevation of Privilege | this sub-batch imported BEFORE its origins were screened (a 38-02-before-38-01 ordering slip) | mitigate | depends_on 38-01 (wave 2 after wave 1); the merge-time classifyGate would ABORT fail-closed on any unclassified origin -- running the importer without 38-01 aborts (the gate ordering is real, asserted by the gate passing only after 38-01) |
| T-38-07 | Tampering | a social write/destructive op (create_post/delete_status) under-stated as read -> ungated mutation | mitigate | the create/send verbs class write and delete classes destructive on the op-name verb, reinforced by the {method:'POST'}/apiVoid{method:'DELETE'} signals; verify-catalog-crosscheck.mjs fails the build on under-statement (asserted exit 0 in Task 2d) |
| T-38-04 | Tampering | a vendored slice emitting a forbidden field name into shipped params (Wall 1) OR a DENY-01 app vendored | mitigate | the importer's recursive forbidden-field pre-scan (UNCHANGED) throws before emit; recipe-path-guard green via validate:extension; no DENY-01 app vendored (Task 1) |
| T-38-05 | Tampering | the vendored handle() runtime (DOM/fetch) dragged into the build import graph | accept/mitigate | the per-app sdk-stub + inert transport stub resolve defineTool/api without the real SDK; the importer reads metadata only and never executes handle() -- the same Wall-1 boundary Phase 36/37 verified |
| T-38-SC | Tampering | npm/pip/cargo installs | accept | no new packages this plan (data-only vendoring; zod/tsx already devDeps); package-legitimacy gate N/A |
</threat_model>

<verification>
- `node --import tsx ./scripts/import-opentabs-catalog.mjs` emits the five new apps (all backing:'dom'); the gate did NOT abort (38-01 screened the origins); existing descriptors untouched; seed-descriptors.json seed-fed.
- `npm run validate:extension` exits 0 (snapshot reconciled; classification gate + crosscheck + recipe-path guard green over the grown corpus).
- `node tests/no-dead-entry.test.js` exits 0 (every new opentabs__ slug resolves to T3).
- `node tests/breadth-search-return.test.js && node tests/capability-search-eval.test.js` pass (synonyms+class+backing:'dom' per op; recall@5>=0.9, wrong-invoke=0.000, cold-start asserts green at the grown size).
</verification>

<success_criteria>
- BRDTH-01: chatgpt/claude/bluesky/mastodon/threads descriptors are imported via the frozen machinery (no importer edit) and returned by search with >=3 app-disambiguated synonyms + side-effect class per op.
- BRDTH-03: every emitted descriptor carries backing:'dom' -> DOM-only (invocable=false, discovery-pending), never a confident invocable hit, never learn-seeded -- the AI-chat + social ToS-hostile routing by the conservative default.
- The screen-then-import ordering is real: the import passed the merge-time classifyGate only because Plan 38-01 classified the origins (an unscreened origin would abort fail-closed).
- Regression: crosscheck (social write/destructive classed) + no-dead-entry (generalized loader) + the eval (recall@5>=0.9, wrong-invoke=0, cold-start within budget) stay green on the grown corpus; validate:extension exits 0.
</success_criteria>

<output>
Create `.planning/phases/38-breadth-b-comms-social-content-sensitivity-screened/38-02-SUMMARY.md` when done.
</output>
</content>
