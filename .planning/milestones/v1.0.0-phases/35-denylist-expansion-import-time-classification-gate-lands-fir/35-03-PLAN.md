---
phase: 35-denylist-expansion-import-time-classification-gate-lands-fir
plan: 03
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - extension/utils/capability-router.js
  - tests/consent-mutation-gate.test.js
autonomous: true
requirements: [DENY-04]
must_haves:
  truths:
    - "A WRITE (mutating side-effect) to a sensitive origin without the per-origin mutating flag returns RECIPE_CONSENT_MUTATING_REQUIRED"
    - "A READ to a sensitive origin passes under Auto (the re-gate is write-only)"
    - "A WRITE to a sensitive origin WITH the per-origin mutating flag passes"
    - "A WRITE to a NON-sensitive origin passes under Auto (posture B narrows, does not revert, the opt-out base from 68ceea90)"
    - "The typed reason string is byte-equal to the pre-68ceea90 RECIPE_CONSENT_MUTATING_REQUIRED and uses the dual-field _err shape (INV-03)"
  artifacts:
    - path: "extension/utils/capability-router.js"
      provides: "Posture-B step (3.5) mutating-elevation branch scoped to classify(origin).sensitive in _evaluateConsent"
      contains: "RECIPE_CONSENT_MUTATING_REQUIRED"
    - path: "tests/consent-mutation-gate.test.js"
      provides: "Sensitive-origin posture-B cases (GET allow / POST block / POST+flag allow) + non-sensitive regression cases"
      contains: "RECIPE_CONSENT_MUTATING_REQUIRED"
  key_links:
    - from: "extension/utils/capability-router.js _evaluateConsent"
      to: "extension/utils/service-denylist.js classify()"
      via: "denylist.classify(origin).sensitive consulted at step (3.5) before the auto-allow"
      pattern: "classify\\(origin\\)"
    - from: "extension/utils/capability-router.js"
      to: "the _err dual-field typed-reason helper"
      via: "_err('RECIPE_CONSENT_MUTATING_REQUIRED', {origin, slug})"
      pattern: "_err\\('RECIPE_CONSENT_MUTATING_REQUIRED'"
    - from: "tests/consent-mutation-gate.test.js"
      to: "consent-policy-store.js setOriginMutating"
      via: "Store.setOriginMutating(origin, true) flips the sensitive-write decision to allow"
      pattern: "setOriginMutating"
---

<objective>
Re-introduce ONE mutating-elevation branch into `capability-router._evaluateConsent`, scoped to `classify(origin).sensitive === true`. A WRITE (mutating side-effect) to a sensitive origin without the per-origin mutating flag returns the dual-field `RECIPE_CONSENT_MUTATING_REQUIRED`; reads pass under Auto everywhere; non-sensitive writes pass. This NARROWS the "fully open under Auto" base committed in `68ceea90` to sensitive origins only — the deliberate posture-B refinement (DENY-04). Then extend `tests/consent-mutation-gate.test.js` with sensitive-origin cases (and keep the existing non-sensitive cases green as the regression canary).

Purpose: Posture B is the headline security control of the milestone. It must be impossible for a sensitive-origin WRITE to run under Auto without the per-origin mutating flag, while the denylist still hard-blocks the categorically-prohibited worst and non-sensitive origins stay fully open.

Output: The scoped step-(3.5) branch in `_evaluateConsent` and the posture-B test cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-denylist-expansion-import-time-classification-gate-lands-fir/35-CONTEXT.md
@.planning/phases/35-denylist-expansion-import-time-classification-gate-lands-fir/35-RESEARCH.md

<interfaces>
<!-- The exact insertion context in _evaluateConsent. Variables already exist; do NOT recompute them. -->
<!-- Source read live: extension/utils/capability-router.js:359-463; consent-policy-store.js getConsentForOrigin/setOriginMutating -->

_evaluateConsent(params) step order (verified):
  (1) denylist.isDenied(origin) -> RECIPE_CONSENT_BLOCKED        (hard floor, lines 383-394)
  (2) store absent -> Phase-29 harness allow OR fail-closed off  (405-415)
      null origin -> off                                         (418-425)
  (3) mode === 'off' -> RECIPE_CONSENT_REQUIRED                  (434-441)
  (4) mode === 'ask' -> RECIPE_CONSENT_REQUIRED                  (444-451)
  --- INSERT (3.5) HERE: after the mode==='ask' block, BEFORE the final return ---
  (final) return { decision:'allow', method, sideEffectClass }   (462) <- mode === 'auto'

Already computed in scope (DO NOT recompute):
  var mutating = _isMutatingSideEffect(sideEffectClass) || !!MUTATING_METHODS[method];   (line 366)
  var mutatingAllowed = !!(consent && consent.mutating);                                  (line 431)
  var denylist = _denylist();    var origin = p.origin;    var slug = p.slug;

The typed-reason helper (capability-router.js:61-69):
  _err(code, extra) -> { success:false, code, errorCode, error, ...extra }   // ONLY shape that survives /^RECIPE_.+$/

RECOVERED byte-exact (git show 68ceea90^:extension/utils/capability-router.js, removed commit 68ceea903c6792b68024945bec826a784d3e44bb):
  error: _err('RECIPE_CONSENT_MUTATING_REQUIRED', { origin: origin, slug: slug })
  // The exact string is RECIPE_CONSENT_MUTATING_REQUIRED. Re-use BYTE-FOR-BYTE (INV-03).

consent-policy-store.js (verified): DEFAULT_MODE='auto'; getConsentForOrigin(env,origin) -> {mode,mutating};
  setOriginMutating(origin, allowed) -> Promise<void>; setOriginMode(origin, mode); _reset().

The invoke caller (lines 674-681) treats ANY verdict.decision !== 'allow' as blocked and returns verdict.error verbatim,
  so a distinct decision label 'mutating_required' flows through correctly (it is !== 'allow').
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add sensitive-origin posture-B cases to consent-mutation-gate.test.js (RED first)</name>
  <files>tests/consent-mutation-gate.test.js</files>
  <read_first>
    - tests/consent-mutation-gate.test.js (the file being modified — the existing NON-sensitive github.com cases are the regression canary and must stay; extend, do not rewrite)
    - extension/utils/capability-router.js (the _evaluateConsent step order + the already-computed mutating/mutatingAllowed variables + the _err helper)
    - extension/utils/consent-policy-store.js (setOriginMode / setOriginMutating / _reset — the per-origin mutating flag this test flips)
    - .planning/phases/35-denylist-expansion-import-time-classification-gate-lands-fir/35-RESEARCH.md (Q4 — the regression surface + the exact dual-field expectation + Pitfall 2/3)
  </read_first>
  <behavior>
    - SENSITIVE origin (classify stub returns {sensitive:true, denied:false}), mode 'auto', mutating flag false:
        GET (read) -> decision 'allow'.
        POST (mutating) -> decision !== 'allow', error.code === 'RECIPE_CONSENT_MUTATING_REQUIRED', error.errorCode === 'RECIPE_CONSENT_MUTATING_REQUIRED', error.error === 'RECIPE_CONSENT_MUTATING_REQUIRED' (dual-field, INV-03).
        T1a 'write' descriptor (sideEffectClass write, no method) -> RECIPE_CONSENT_MUTATING_REQUIRED.
    - SAME sensitive origin after setOriginMutating(origin, true): POST -> decision 'allow' (the per-origin flag elevates the sensitive write).
    - NON-sensitive origin (the existing github.com cases, classify stub {sensitive:false}): GET allow, POST allow, every MUTATING verb allow, explicitly-Off still blocks — UNCHANGED (posture B must not touch non-sensitive writes).
    - The typed code surfaces through error.code AND error.errorCode (the byte-equality + dual-field guard).
  </behavior>
  <action>
    Extend tests/consent-mutation-gate.test.js. KEEP the existing block (the non-sensitive github.com origin with the `classify() -> {sensitive:false, denied:false}` stub and its allow assertions) exactly as-is — it is the regression canary proving posture B does not revert the opt-out base. Then add a SECOND block that installs a SENSITIVE classify stub: set `globalThis.FsbServiceDenylist = { classify(){ return {sensitive:true, denied:false}; }, isDenied(){ return {denied:false}; }, load(){ return Promise.resolve(); } }` BEFORE requiring the router (or re-require with a fresh module cache if needed; follow the existing test's require ordering — the gate reads the live global at call time). Use a sensitive origin literal such as `https://dashboard.stripe.com`. Reset the store (Store._reset()), setOriginMode(origin,'auto'). Assert: GET -> decision 'allow'; POST (entry sideEffectClass 'mutating') -> decision !== 'allow' AND error.code / error.errorCode / error.error all === 'RECIPE_CONSENT_MUTATING_REQUIRED'; a T1a write-descriptor (entry { tier:'T1a', descriptor:{ sideEffectClass:'write' } }) -> RECIPE_CONSENT_MUTATING_REQUIRED. Then `await Store.setOriginMutating(origin, true)` and assert POST -> decision 'allow'. Author RED-first: these assertions fail until Task 2 adds the branch. Reuse the existing `check(cond,msg)` counter and the `PASS=/FAIL=` + `process.exit(1)` tail. Keep ASCII-only. Do NOT assert anything that would require the non-sensitive github.com cases to change.
  </action>
  <verify>
    <automated>node tests/consent-mutation-gate.test.js; test $? -ne 0 && echo "RED as expected (branch not yet added)" || echo "already green"</automated>
  </verify>
  <acceptance_criteria>
    - tests/consent-mutation-gate.test.js contains the literal `RECIPE_CONSENT_MUTATING_REQUIRED` and a sensitive-origin stub returning `{sensitive:true, denied:false}`.
    - Before Task 2, `node tests/consent-mutation-gate.test.js` exits non-zero (RED) — the sensitive POST currently returns allow (no branch yet).
    - The existing non-sensitive github.com assertions are still present verbatim.
  </acceptance_criteria>
  <done>The posture-B sensitive-origin cases exist and fail RED; the non-sensitive regression cases are intact.</done>
</task>

<task type="auto">
  <name>Task 2: Insert the scoped posture-B step (3.5) branch in _evaluateConsent</name>
  <files>extension/utils/capability-router.js</files>
  <read_first>
    - extension/utils/capability-router.js (the file being modified — _evaluateConsent at lines 359-463; insert AFTER the mode==='ask' block at ~451 and BEFORE the final `return { decision:'allow' }` at ~462)
    - .planning/phases/35-denylist-expansion-import-time-classification-gate-lands-fir/35-RESEARCH.md (Q4 — the exact branch, its insertion point relative to steps 1-4, the INV-03 byte-equality constraint, and Pitfall 2 "scope strictly to sensitive")
  </read_first>
  <action>
    Recover the byte-exact removed branch first: run `git show 68ceea903c6792b68024945bec826a784d3e44bb^:extension/utils/capability-router.js` and locate the line emitting `_err('RECIPE_CONSENT_MUTATING_REQUIRED', ...)`. The exact typed string is `RECIPE_CONSENT_MUTATING_REQUIRED` — reuse it BYTE-FOR-BYTE (INV-03; a drifted string like RECIPE_MUTATING_CONSENT_REQUIRED breaks the 7-provider passthrough). Insert a new step (3.5) in `_evaluateConsent` immediately AFTER the `mode === 'ask'` block (ending ~line 451) and BEFORE the final `return { decision: 'allow', ... }` (~line 462). The branch fires ONLY when `mutating && !mutatingAllowed` (both variables already exist in scope — do NOT recompute them) AND `classify(origin).sensitive === true`. Read the classification through the already-resolved `denylist` accessor with a typeof guard and a try/catch returning null on throw (mirror the step-1 `isDenied` guard pattern already in the function): obtain `cls = denylist.classify(origin)` only when `denylist && typeof denylist.classify === 'function'`. When `cls && cls.sensitive === true`, return `{ decision: 'mutating_required', method: method, sideEffectClass: sideEffectClass, error: _err('RECIPE_CONSENT_MUTATING_REQUIRED', { origin: origin, slug: slug }) }`. Use the distinct `'mutating_required'` decision label (the invoke caller only checks `!== 'allow'`, so it flows through and audits correctly; it is clearer than reusing 'ask'). When the origin is NOT sensitive, fall through to the existing final auto-allow unchanged (non-sensitive reads AND writes still pass — posture B narrows, never reverts). Add a concise comment naming DENY-04 / posture B / the 68ceea90 narrowing and that classify().sensitive (D-14) is the single source of truth. Keep the module free of eval/new Function/import() in code and comments (it is on RECIPE_PATH_ALLOWLIST; verify-recipe-path-guard.mjs Check 1 scans it). Do NOT touch the step (1) denylist block, the store-absent degradation, or the off/ask paths.
  </action>
  <verify>
    <automated>node tests/consent-mutation-gate.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/consent-mutation-gate.test.js` exits 0 with FAIL=0: the sensitive POST returns RECIPE_CONSENT_MUTATING_REQUIRED, the sensitive GET allows, the sensitive POST-with-flag allows, and ALL existing non-sensitive github.com cases stay allow.
    - `grep -n "RECIPE_CONSENT_MUTATING_REQUIRED" extension/utils/capability-router.js` shows the string emitted via `_err(` (dual-field), inside a branch guarded by `cls.sensitive === true`.
    - `grep -nE "eval\\(|new Function|import\\(" extension/utils/capability-router.js | grep -v '^[0-9]*:[[:space:]]*//' || true` finds no NON-comment dynamic-code construct (the branch added none).
    - The branch sits AFTER the `mode === 'ask'` return and BEFORE the final `decision: 'allow'` return (step 3.5 ordering).
  </acceptance_criteria>
  <done>The scoped sensitive-write re-gate is live; sensitive writes without the flag return the byte-exact dual-field RECIPE_CONSENT_MUTATING_REQUIRED; non-sensitive writes and all reads pass.</done>
</task>

<task type="auto">
  <name>Task 3: Run the consent + denylist regression surface to confirm posture B did not break neighbors</name>
  <files>(no new files; runs existing suites)</files>
  <read_first>
    - tests/consent-gate.test.js, tests/consent-chokepoint.test.js, tests/service-denylist.test.js, tests/consent-policy-store.test.js (the neighboring consent suites that predate posture B — they use non-sensitive or denied origins and must stay green)
    - extension/utils/capability-router.js (the branch added in Task 2 — confirm scope did not catch a non-sensitive path)
  </read_first>
  <action>
    Run the consent/denylist regression suites and confirm GREEN. The expectation (per 35-RESEARCH Q4 regression surface): consent-mutation-gate (now extended) passes; consent-gate / consent-chokepoint / consent-policy-store / service-denylist stay green because they exercise non-sensitive or denied origins, which posture B does not touch. If any neighbor goes red, the most likely cause is the branch firing on a non-sensitive origin (Pitfall 2 — the scope guard `cls.sensitive === true` was dropped or mis-placed) or a classify() call throwing without the try/catch guard; fix the branch scope, do NOT loosen the neighbor's assertion. This task adds no files; it is the cross-suite confirmation that DENY-04 narrowed rather than reverted the posture.
  </action>
  <verify>
    <automated>node tests/consent-mutation-gate.test.js && node tests/consent-gate.test.js && node tests/consent-chokepoint.test.js && node tests/consent-policy-store.test.js && node tests/service-denylist.test.js</automated>
  </verify>
  <acceptance_criteria>
    - All five suites in the verify command exit 0 (FAIL=0 each).
    - No neighbor assertion was modified to achieve green (`git diff` shows changes only to capability-router.js + consent-mutation-gate.test.js for this plan).
    - `node scripts/verify-recipe-path-guard.mjs` exits 0 (the router edit reintroduced no dynamic-code construct on the recipe path).
  </acceptance_criteria>
  <done>Posture B passes its own suite and leaves the consent/denylist neighbors green; the Wall-1 recipe-path guard stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP/autopilot front door -> _evaluateConsent | Both front doors share this single consent chokepoint (INV-02); a sensitive-origin WRITE crossing it under Auto without the mutating flag is the access-control decision posture B enforces |
| typed reason -> MCP /^RECIPE_.+$/ passthrough | The reason must surface byte-equal across 7 providers; a drifted string or a non-dual-field shape is silently dropped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-09 | Elevation of Privilege | sensitive-origin WRITE under Auto without the mutating flag | mitigate | The step-(3.5) branch scoped to classify(origin).sensitive returns RECIPE_CONSENT_MUTATING_REQUIRED for a mutating call without the per-origin flag; reads pass, the flag elevates (DENY-04, posture B) |
| T-35-10 | Tampering | re-gate reverts the whole opt-out posture (non-sensitive writes blocked) | mitigate | The branch fires ONLY inside `cls.sensitive === true`; the existing non-sensitive github.com cases are the regression canary (Pitfall 2). Task 3 cross-checks the consent neighbors stay green |
| T-35-11 | Repudiation / Tampering | typed-reason drift breaks the MCP passthrough -> a security reason silently dropped | mitigate | The string RECIPE_CONSENT_MUTATING_REQUIRED is recovered byte-exact from git (68ceea90^) and emitted via the dual-field _err helper; the test asserts error.code AND error.errorCode AND error.error all carry it (INV-03) |
| T-35-12 | Tampering | dynamic-code construct reintroduced on the recipe-path router | mitigate | capability-router.js is on RECIPE_PATH_ALLOWLIST; verify-recipe-path-guard.mjs Check 1 scans it; Task 3 runs the guard to confirm the branch added none |
| T-35-SC | Tampering | npm/pip/cargo installs | accept | Phase 35 installs ZERO external packages (verified RESEARCH "Package Legitimacy Audit"); no install step exists, so the slopcheck gate is not applicable here |
</threat_model>

<verification>
- `node tests/consent-mutation-gate.test.js` exits 0 (sensitive POST blocked, sensitive GET + sensitive POST-with-flag + all non-sensitive cases allow).
- `node tests/consent-gate.test.js && node tests/consent-chokepoint.test.js && node tests/consent-policy-store.test.js && node tests/service-denylist.test.js` all exit 0.
- `node scripts/verify-recipe-path-guard.mjs` exits 0.
- The typed reason `RECIPE_CONSENT_MUTATING_REQUIRED` is byte-equal to the pre-68ceea90 string and uses the dual-field `_err` shape.
</verification>

<success_criteria>
- DENY-04: a sensitive-origin WRITE requires the per-origin mutating flag (posture B); reads run under Auto everywhere; non-sensitive origins stay fully open.
- The typed reason is byte-equal across providers (INV-03) via the dual-field `_err` shape.
- The opt-out base from 68ceea90 is narrowed, not reverted (non-sensitive regression canary green).
</success_criteria>

<output>
Create `.planning/phases/35-denylist-expansion-import-time-classification-gate-lands-fir/35-03-SUMMARY.md` when done.
</output>
