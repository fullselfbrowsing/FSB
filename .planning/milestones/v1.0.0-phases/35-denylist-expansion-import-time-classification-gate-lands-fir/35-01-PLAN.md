---
phase: 35-denylist-expansion-import-time-classification-gate-lands-fir
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extension/config/service-denylist.json
  - tests/service-denylist.test.js
autonomous: true
requirements: [DENY-01, DENY-02]
must_haves:
  truths:
    - "Every categorically-prohibited roster origin classifies denied:true (robinhood, fidelity, carta, netflix, spotify, twitch, steam, youtube-music, tinder, onlyfans)"
    - "Every allowed-but-sensitive roster origin classifies {sensitive:true, denied:false} (stripe, coinbase, twilio, ynab, instagram, facebook, tiktok, x, + messaging set)"
    - "instagram/facebook/tiktok/x are sensitive, NOT denied (reads run under Auto; writes mutating-gated by posture B in Plan 03)"
    - "Exact-host origins (digital.fidelity.com, app.carta.com, open.spotify.com, store.steampowered.com, music.youtube.com, dashboard.stripe.com, console.twilio.com, app.ynab.com) match only their exact host, never a broad subdomain"
    - "The existing FSB seed (chase, bankofamerica, wellsfargo, irs.gov, mail.google.com, outlook.live.com, outlook.office.com, *.gov) is preserved, not removed"
  artifacts:
    - path: "extension/config/service-denylist.json"
      provides: "Expanded deniedOrigins + sensitiveOrigins roster (DENY-01/02)"
      contains: "https://digital.fidelity.com"
    - path: "tests/service-denylist.test.js"
      provides: "Per-origin classify() assertions over the full denied + sensitive roster"
      contains: "music.youtube.com"
  key_links:
    - from: "tests/service-denylist.test.js"
      to: "extension/utils/service-denylist.js classify()"
      via: "require + await load() + classify(origin)"
      pattern: "classify\\("
    - from: "extension/utils/service-denylist.js"
      to: "extension/config/service-denylist.json"
      via: "load() reads the bundled JSON (loader UNCHANGED, data-only change)"
      pattern: "deniedOrigins|sensitiveOrigins"
---

<objective>
Expand `extension/config/service-denylist.json` so the named DENY-01/02 roster is fully classified — every categorically-prohibited origin in `deniedOrigins`, every allowed-but-sensitive origin in `sensitiveOrigins` — using the EXACT origin patterns OpenTabs itself targets (several are exact-host, not subdomain-wildcard). This is a DATA-ONLY change: the `service-denylist.js` loader/classifier is untouched. Then extend `tests/service-denylist.test.js` to assert each roster origin classifies correctly.

Purpose: Under the shipped opt-out Auto default, the denylist is the ONE hard floor. A gap in this JSON array is indistinguishable from an allow decision, so the roster MUST be complete before any descriptor (Phase 36+) can reach a finance/ToS-hostile origin. This plan lands the data the Plan 02 gate and the Plan 03 posture-B re-gate both consume via `classify()`.

Output: An expanded `service-denylist.json` (DENY-01 + DENY-02) and per-origin roster assertions in `tests/service-denylist.test.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-denylist-expansion-import-time-classification-gate-lands-fir/35-CONTEXT.md
@.planning/phases/35-denylist-expansion-import-time-classification-gate-lands-fir/35-RESEARCH.md

<interfaces>
<!-- The classifier contract this plan's data feeds. Loader is UNCHANGED; do NOT edit service-denylist.js. -->
<!-- Source: extension/utils/service-denylist.js (read live) -->

classify(origin) -> { sensitive, denied, reason? }
  // denied:true IMPLIES sensitive:true; a sensitiveOrigins match returns { sensitive:true, denied:false }.
isDenied(origin) -> { denied, reason? }
load() -> Promise<void>   // reads extension/config/service-denylist.json; await before classify() in tests.

Matcher contract (LOCKED, verified at service-denylist.js:84-113):
  'https://*.robinhood.com'  -> kind:'suffix'  -> matches apex robinhood.com AND any subdomain.
  'https://digital.fidelity.com' (NO '*') -> kind:'exact' -> matches ONLY that exact host (scheme must match).
  A bare '*' anywhere other than a leading '*.' -> null (unsupported, no match).

Current JSON envelope (extension/config/service-denylist.json, v:1):
  { v, deniedReason, deniedOrigins:[...], sensitiveOrigins:[...] }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend service-denylist.test.js with per-origin roster assertions (RED first)</name>
  <files>tests/service-denylist.test.js</files>
  <read_first>
    - tests/service-denylist.test.js (the file being modified — extend, do NOT rewrite the existing GOV-08/D-14 assertions)
    - extension/utils/service-denylist.js (the classify()/load() contract + the _parsePattern exact-vs-suffix matcher at lines 84-113)
    - .planning/phases/35-denylist-expansion-import-time-classification-gate-lands-fir/35-RESEARCH.md (the "Roster -> Origins" tables in Q2 — every concrete origin + form)
  </read_first>
  <behavior>
    - After load(), classify(origin).denied === true for a CONCRETE origin under EACH denied roster pattern: https://robinhood.com, https://digital.fidelity.com, https://app.carta.com, https://www.netflix.com, https://open.spotify.com, https://www.twitch.tv, https://store.steampowered.com, https://music.youtube.com, https://www.tinder.com, https://www.onlyfans.com.
    - classify(origin) === {sensitive:true, denied:false} for a CONCRETE origin under EACH sensitive roster pattern: https://dashboard.stripe.com, https://www.coinbase.com, https://console.twilio.com, https://app.ynab.com, https://www.instagram.com, https://www.facebook.com, https://www.tiktok.com, https://x.com, https://web.whatsapp.com, https://web.telegram.org, https://www.slack.com, https://discord.com, https://teams.microsoft.com.
    - Exact-host discrimination: classify('https://api.stripe.com').sensitive === false and classify('https://www.youtube.com').denied === false (proves dashboard.stripe.com / music.youtube.com did NOT over-broaden to the whole domain).
    - Negative control: classify('https://www.fidelity.com').denied is allowed to be false (fidelity is exact digital.fidelity.com only) — assert classify('https://digital.fidelity.com').denied === true AND that a benign non-roster origin like https://github.com stays {sensitive:false, denied:false}.
    - The existing seed still classifies: classify a concrete chase origin -> denied:true (regression guard that the seed was not dropped).
  </behavior>
  <action>
    Add a new assertion block to tests/service-denylist.test.js AFTER the existing checks, inside the same async IIFE (reuse the already-required Denylist + the awaited load()). Author it RED-first: write the assertions before Task 2 changes the JSON, so the run fails until the data lands. Define two arrays of {origin, form} literals taken VERBATIM from 35-RESEARCH Q2: deniedRoster = [robinhood.com via https://robinhood.com, https://digital.fidelity.com, https://app.carta.com, https://www.netflix.com, https://open.spotify.com, https://www.twitch.tv, https://store.steampowered.com, https://music.youtube.com, https://www.tinder.com, https://www.onlyfans.com]; sensitiveRoster = [https://dashboard.stripe.com, https://www.coinbase.com, https://console.twilio.com, https://app.ynab.com, https://www.instagram.com, https://www.facebook.com, https://www.tiktok.com, https://x.com, https://web.whatsapp.com, https://web.telegram.org, https://www.slack.com, https://discord.com, https://teams.microsoft.com]. For each denied origin assert classify(origin).denied === true with the origin in the message; for each sensitive origin assert classify(origin).sensitive === true && classify(origin).denied === false with the origin in the message. Add the three exact-host discrimination assertions (api.stripe.com sensitive false; www.youtube.com denied false; digital.fidelity.com denied true). Keep the existing `check(cond,msg)` counter and the trailing `PASS=/FAIL=` + `process.exit(1)` convention unchanged. Do NOT add any `== 0` gate on empty/unfiltered data — every assertion checks a concrete present value (the existing RED-test discipline at the top of the file).
  </action>
  <verify>
    <automated>node tests/service-denylist.test.js; test $? -ne 0 && echo "RED as expected (data not yet expanded)" || echo "already green"</automated>
  </verify>
  <acceptance_criteria>
    - tests/service-denylist.test.js contains the literal strings `https://digital.fidelity.com`, `https://music.youtube.com`, `https://dashboard.stripe.com`, and `https://x.com`.
    - Before Task 2 lands the data, `node tests/service-denylist.test.js` exits non-zero (RED) with FAIL lines naming the un-classified roster origins.
    - The file still contains the pre-existing GOV-08 BLOCKED-checked-first assertions (they were extended, not replaced).
  </acceptance_criteria>
  <done>The roster assertions exist and fail RED against the un-expanded JSON; existing assertions are intact.</done>
</task>

<task type="auto">
  <name>Task 2: Expand deniedOrigins + sensitiveOrigins in service-denylist.json (DENY-01/02)</name>
  <files>extension/config/service-denylist.json</files>
  <read_first>
    - extension/config/service-denylist.json (the file being modified — append to the existing arrays; preserve v:1, deniedReason, and the existing seed entries)
    - .planning/phases/35-denylist-expansion-import-time-classification-gate-lands-fir/35-RESEARCH.md (Q2 "Roster -> Origins" tables — the authoritative origin + form per app, including the exact-host nuances)
    - extension/utils/service-denylist.js (confirm the exact-vs-suffix matcher so each pattern form is correct)
  </read_first>
  <action>
    Append to `deniedOrigins` (DENY-01, categorically prohibited) these EXACT patterns, keeping the existing chase/bankofamerica/wellsfargo/irs.gov entries: `https://*.robinhood.com` (suffix), `https://digital.fidelity.com` (exact — NOT *.fidelity.com), `https://app.carta.com` (exact), `https://*.netflix.com` (suffix), `https://open.spotify.com` (exact), `https://*.twitch.tv` (suffix), `https://store.steampowered.com` (exact), `https://music.youtube.com` (exact — NOT *.youtube.com, which would catch youtube proper), `https://*.tinder.com` (suffix), `https://*.onlyfans.com` (suffix). Append to `sensitiveOrigins` (DENY-02, allowed-but-sensitive) these EXACT patterns, keeping the existing bank/mail/*.gov entries: `https://dashboard.stripe.com` (exact — the dashboard origin, NOT api.stripe.com), `https://*.coinbase.com` (suffix), `https://console.twilio.com` (exact), `https://app.ynab.com` (exact), `https://*.instagram.com` (suffix), `https://*.facebook.com` (suffix), `https://*.tiktok.com` (suffix), `https://*.x.com` (suffix), and the messaging set (locked per 35-RESEARCH Q1/A2 — writes mutating-gated under posture B, reads under Auto): `https://web.whatsapp.com` (exact), `https://web.telegram.org` (exact), `https://*.slack.com` (suffix), `https://discord.com` (exact), `https://teams.live.com`, `https://teams.microsoft.com`, `https://teams.cloud.microsoft` (three exact). instagram/facebook/tiktok/x go in sensitiveOrigins ONLY (NOT deniedOrigins) per CONTEXT — fully-denied is reserved for the brokerage + ToS-hostile-media set. Do NOT change the loader, the `v` version, or `deniedReason`. Optionally extend the leading `_comment` to note the v1.0.0 roster expansion (do not remove the existing comment text). Keep the file ASCII-only, no trailing commas (valid JSON).
  </action>
  <verify>
    <automated>node -e "const j=require('./extension/config/service-denylist.json'); const need=['https://digital.fidelity.com','https://music.youtube.com','https://store.steampowered.com','https://open.spotify.com','https://app.carta.com','https://*.robinhood.com','https://*.netflix.com','https://*.twitch.tv','https://*.tinder.com','https://*.onlyfans.com']; const sens=['https://dashboard.stripe.com','https://*.coinbase.com','https://console.twilio.com','https://app.ynab.com','https://*.instagram.com','https://*.facebook.com','https://*.tiktok.com','https://*.x.com']; const md=need.filter(o=>!j.deniedOrigins.includes(o)); const ms=sens.filter(o=>!j.sensitiveOrigins.includes(o)); if(md.length||ms.length){console.error('MISSING denied:',md,'sensitive:',ms);process.exit(1)} if(j.deniedOrigins.includes('https://*.instagram.com')){console.error('instagram must NOT be denied');process.exit(1)} console.log('denylist roster OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "JSON.parse(require('fs').readFileSync('extension/config/service-denylist.json','utf8'))"` succeeds (valid JSON).
    - All 10 denied roster patterns and all 8 core sensitive roster patterns are present (the verify command exits 0).
    - `https://*.instagram.com` / `https://*.facebook.com` / `https://*.tiktok.com` / `https://*.x.com` appear in sensitiveOrigins and NOT in deniedOrigins.
    - The original seed entries (`https://*.chase.com`, `https://*.bankofamerica.com`, `https://*.wellsfargo.com`, `https://*.irs.gov`, `https://mail.google.com`, `https://outlook.live.com`, `https://outlook.office.com`, `https://*.gov`) are still present.
  </acceptance_criteria>
  <done>The JSON is valid and carries the full denied + sensitive roster with exact-host forms; the seed is preserved; instagram/facebook/tiktok/x are sensitive-not-denied.</done>
</task>

<task type="auto">
  <name>Task 3: Turn the roster test GREEN and confirm no over-broadening</name>
  <files>tests/service-denylist.test.js, extension/config/service-denylist.json</files>
  <read_first>
    - tests/service-denylist.test.js (the assertions added in Task 1 — confirm they now pass)
    - extension/config/service-denylist.json (the data landed in Task 2)
  </read_first>
  <action>
    Run the extended test and confirm GREEN. If any roster assertion still fails, reconcile the failing origin against the Task 2 pattern (the most likely cause is an exact-vs-suffix form mismatch — e.g. accidentally writing `https://*.stripe.com` instead of `https://dashboard.stripe.com`, which would break the `api.stripe.com sensitive false` discrimination assertion). Do NOT weaken an assertion to make it pass; fix the data. Confirm the two exact-host discrimination assertions hold (api.stripe.com not sensitive; www.youtube.com not denied) — these prove the exact-host forms did not collapse to whole-domain wildcards. This task adds NO new files; it is the GREEN half of the Task 1 RED.
  </action>
  <verify>
    <automated>node tests/service-denylist.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/service-denylist.test.js` exits 0 with a `FAIL=0` summary line.
    - The run includes PASS lines naming `https://digital.fidelity.com`, `https://music.youtube.com`, and `https://dashboard.stripe.com`.
    - No assertion was deleted or loosened relative to Task 1 (the GREEN is achieved by the Task 2 data, not by editing the test down).
  </acceptance_criteria>
  <done>The full denied + sensitive roster classifies correctly; exact-host discrimination holds; the suite is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| descriptor/import time -> denylist data | A finance/ToS origin reachable under opt-out Auto is gated ONLY by its presence in this JSON; an absent entry === an allow decision |
| denylist pattern -> origin match | An over-broad pattern (e.g. `*.youtube.com`) blocks a legitimate sibling origin (youtube proper), a self-inflicted DoS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | Elevation of Privilege | service-denylist.json deniedOrigins | mitigate | The brokerage + ToS-hostile roster (robinhood/fidelity/carta/netflix/spotify/twitch/steam/youtube-music/tinder/onlyfans) is added to deniedOrigins so a credential-replay write under Auto is hard-blocked at the gate's step (1) before any per-origin policy (DENY-01) |
| T-35-02 | Tampering / EoP | a gap in the JSON array | mitigate | This plan lands the named roster fully; Plan 02's fail-closed gate makes any FUTURE unclassified sensitivity-suspect origin a CI failure, not a silent allow (DENY-03) |
| T-35-03 | Denial of Service (self-inflicted) | over-broad denylist pattern | mitigate | Exact-host patterns matching OpenTabs' own urlPattern scope (digital.fidelity.com, dashboard.stripe.com, music.youtube.com NOT *.youtube.com); the `api.stripe.com sensitive false` + `www.youtube.com denied false` discrimination assertions prove no over-broadening |
| T-35-04 | Spoofing | instagram/facebook/tiktok/x reads | accept | Classified sensitive (not denied) per CONTEXT: reads run under Auto, writes are mutating-gated by Plan 03 (posture B). Accepting read-under-Auto is the documented product posture; the write surface is covered by DENY-04 |
</threat_model>

<verification>
- `node tests/service-denylist.test.js` exits 0 (the full roster + exact-host discrimination + the existing GOV-08 BLOCKED-first path all green).
- `node -e "JSON.parse(require('fs').readFileSync('extension/config/service-denylist.json','utf8'))"` succeeds.
- The loader (`extension/utils/service-denylist.js`) is unchanged (`git diff --stat` shows only the JSON + the test touched by this plan).
</verification>

<success_criteria>
- DENY-01: every categorically-prohibited roster origin classifies `denied:true`.
- DENY-02: every allowed-but-sensitive roster origin classifies `{sensitive:true, denied:false}`.
- instagram/facebook/tiktok/x are sensitive, not denied.
- No origin pattern is broader than the app's actual OpenTabs urlPattern host scope.
- The existing FSB seed is preserved.
</success_criteria>

<output>
Create `.planning/phases/35-denylist-expansion-import-time-classification-gate-lands-fir/35-01-SUMMARY.md` when done.
</output>
