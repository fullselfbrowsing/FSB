---
phase: 37-breadth-a-dev-productivity-least-sensitive
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vendor/opentabs-snapshot/plugins/linear/package.json
  - vendor/opentabs-snapshot/plugins/linear/src/sdk-stub.ts
  - vendor/opentabs-snapshot/plugins/linear/src/index.ts
  - vendor/opentabs-snapshot/plugins/linear/src/linear-api.ts
  - vendor/opentabs-snapshot/plugins/linear/src/tools/create-issue.ts
  - vendor/opentabs-snapshot/plugins/linear/src/tools/list-issues.ts
  - vendor/opentabs-snapshot/plugins/linear/src/tools/get-issue.ts
  - vendor/opentabs-snapshot/plugins/linear/src/tools/update-issue.ts
  - vendor/opentabs-snapshot/plugins/linear/src/tools/create-comment.ts
  - vendor/opentabs-snapshot/plugins/asana/package.json
  - vendor/opentabs-snapshot/plugins/asana/src/sdk-stub.ts
  - vendor/opentabs-snapshot/plugins/asana/src/index.ts
  - vendor/opentabs-snapshot/plugins/asana/src/asana-api.ts
  - vendor/opentabs-snapshot/plugins/asana/src/tools/create-task.ts
  - vendor/opentabs-snapshot/plugins/asana/src/tools/list-tasks.ts
  - vendor/opentabs-snapshot/plugins/asana/src/tools/get-task.ts
  - vendor/opentabs-snapshot/plugins/asana/src/tools/update-task.ts
  - vendor/opentabs-snapshot/PIN.md
  - scripts/import-opentabs-catalog.mjs
  - extension/utils/capability-search.js
  - catalog/descriptors/github-issues.json
  - catalog/descriptors/github-issues-create.json
  - catalog/descriptors/github-notifications.json
  - catalog/descriptors/notion-load-page.json
  - catalog/descriptors/notion-spaces.json
  - catalog/descriptors/slack-message.json
  - catalog/descriptors/slack-conversations-list.json
  - catalog/descriptors/reddit-inbox.json
  - catalog/descriptors/_fixtures/_provenance.json
  - catalog/descriptors/_fixtures/intent-cases.json
  - catalog/descriptors/_fixtures/seed-descriptors.json
  - catalog/descriptors/_fixtures/batch-unclassified-origin.fixture.json
  - extension/catalog/recipe-index.generated.js
  - tests/no-dead-entry.test.js
  - tests/breadth-search-return.test.js
  - tests/breadth-batch-gate.test.js
  - tests/backing-status-annotation.test.js
  - package.json
autonomous: true
requirements: [BRDTH-01, BRDTH-02, BRDTH-03]

must_haves:
  truths:
    - "search_capabilities('create an issue in linear') returns linear.create_issue as the top (confident) hit; asana/todoist create_* are NOT the top hit (wrong-invoke=0 on the cross-app create_* collision set)"
    - "Every imported descriptor returned by search carries >=3-4 intent synonyms, a side-effect class, AND a backing-status enum whose FIELD value is one of recipe/handler/learn/dom (the canonical resolve()-seam value; 'discovery-pending'/'learn-pending' is the DISPLAY annotation only)"
    - "A pending-only descriptor (backing dom/learn, no recipe/handler) returns from search annotated discovery-pending, NOT as a confident invocable hit; a backing:handler descriptor (github/slack/notion head) IS a confident hit"
    - "Importing a batch whose origin is not classified denied/sensitive/safe aborts the build (exit != 0) naming the offending origin -- the merge-time per-batch gate 38/39 inherit"
    - "node scripts/verify-catalog-crosscheck.mjs and node tests/no-dead-entry.test.js stay exit 0 on the grown corpus (todoist + linear + asana); the no-dead-entry loader now matches ALL opentabs__*.json (not just todoist) so descriptor-only linear/asana slugs ARE checked and resolve to T3/T2"
    - "node tests/capability-search-eval.test.js stays exit 0 on the grown corpus: the importer's seed-feeding step writes each emitted real descriptor's searchable shape into _fixtures/seed-descriptors.json, so the eval (which indexes seed-descriptors.json) HAS an indexed descriptor for every intent-cases.json expectedSlug -> recall@5>=0.9 + wrong-invoke=0"
    - "npm run validate:extension exits 0 (snapshot reconciled, classification gate + crosscheck green)"
  artifacts:
    - path: "scripts/import-opentabs-catalog.mjs"
      provides: "directory-enumerated batch import (replaces hardcoded SMOKE_APPS), per-app serviceStem/displayService OVERRIDE (jira/confluence/cloudflare/datadog -> canonical distinct stems), MED-03 app-disambiguated synonym generator, backing-status stamping, merge-time batch-gate abort, AND a seed-feeding step that mirrors each emitted descriptor's searchable shape into _fixtures/seed-descriptors.json"
      contains: "enumerateBatchApps"
    - path: "extension/utils/capability-search.js"
      provides: "backing-status carried through buildIndex + surfaced/annotated in every search() hit"
      contains: "backing"
    - path: "vendor/opentabs-snapshot/plugins/linear/src/index.ts"
      provides: "vendored linear metadata slice (GraphQL/camelCase transport -- the MED-03 collision app)"
      contains: "LinearPlugin"
    - path: "vendor/opentabs-snapshot/plugins/asana/src/index.ts"
      provides: "vendored asana metadata slice (create_task -- the cross-app collision near-neighbor)"
      contains: "AsanaPlugin"
    - path: "tests/no-dead-entry.test.js"
      provides: "the no-dead-entry corpus loader GENERALIZED to all opentabs__*.json so each new batch's descriptor-only slugs are genuinely checked (no longer a todoist-only false-green); line-74 backing assertion widened to tolerate handler/learn/dom"
      contains: "opentabs__"
    - path: "tests/breadth-batch-gate.test.js"
      provides: "proof the merge aborts on an unclassified batch origin"
      contains: "batch-unclassified-origin"
    - path: "tests/backing-status-annotation.test.js"
      provides: "proof a pending-only descriptor is not a confident invocable hit; a handler-backed one is"
    - path: "tests/breadth-search-return.test.js"
      provides: "proof every imported op returns from search with synonyms + side-effect class + backing; MED-03 wrong-invoke=0 on create_* near-neighbors over the REAL emitted corpus (the GENUINE collision proof; the eval-side fixtures are redundant)"
    - path: "extension/catalog/recipe-index.generated.js"
      provides: "the catalog snapshot REGENERATED via package-extension.mjs after the emit (INV-01 IIFE/djb2 unchanged; only the DATA literal grows) so validate:extension reconciles"
  key_links:
    - from: "scripts/import-opentabs-catalog.mjs"
      to: "vendor/opentabs-snapshot/plugins/*"
      via: "enumerateBatchApps reads the vendored plugin directory (excludes already-head/todoist + denied)"
      pattern: "readdirSync.*plugins|enumerateBatchApps"
    - from: "scripts/import-opentabs-catalog.mjs"
      to: "the emitted descriptor slug/filename"
      via: "displayServiceStem(app, derivedStem) overrides the host-derived stem for collision apps so jira!=confluence and cloudflare/datadog get canonical distinct stems"
      pattern: "displayServiceStem|STEM_OVERRIDES"
    - from: "scripts/import-opentabs-catalog.mjs"
      to: "catalog/descriptors/_fixtures/seed-descriptors.json"
      via: "seed-feeding step mirrors each emitted descriptor's searchable shape into the eval-indexed seed set so every intent-case expectedSlug has an indexed descriptor"
      pattern: "seed-descriptors|feedSeed"
    - from: "scripts/import-opentabs-catalog.mjs"
      to: "scripts/verify-classification-gate.mjs classifyGate"
      via: "gate-before-emit AND a merge-time batch-coverage assertion that aborts on any unclassified batch origin"
      pattern: "classifyGate"
    - from: "extension/utils/capability-search.js search()"
      to: "the hit's backing field"
      via: "buildIndex stores backing; search() annotates invocable-vs-pending by it"
      pattern: "backing"
    - from: "tests/no-dead-entry.test.js"
      to: "catalog/descriptors/opentabs__*.json"
      via: "the corpus loader matches all opentabs__ descriptors (not just todoist) so each batch's seam resolution is verified"
      pattern: "opentabs__"
    - from: "scripts/package-extension.mjs"
      to: "extension/catalog/recipe-index.generated.js"
      via: "regen after emit -> readJsonDir inlines the new linear/asana descriptors so validate-extension byte-compare reconciles"
      pattern: "recipe-index.generated|package-extension"
---

<objective>
Establish the breadth contract that Phases 38-39 reuse verbatim, and prove it on the
MED-03 collision-critical apps. The MACHINERY changes that the whole phase depends on land
HERE so 02/03/04 stay genuinely data-only against corrected, frozen machinery:

1. **Importer enumeration (BRDTH-01):** replace the hardcoded `SMOKE_APPS = ['todoist']`
   with a directory enumeration of the vendored dev/productivity plugins, EXCLUDING apps
   that already have a REGISTRY/head descriptor (github/slack/notion) and the
   already-imported todoist smoke slice, so the importer never clobbers an existing entry.
2. **serviceStem/displayService OVERRIDE (BRDTH-01):** the frozen host-derived stem
   (`service.replace(/^app\./,'').split('.')[0]`) yields WRONG/colliding stems for four
   later-batch apps -- `*.atlassian.net` -> the subdomain (jira AND confluence COLLIDE),
   `dash.cloudflare.com` -> `dash`, `app.datadoghq.com` -> `datadoghq`. Add a one-time
   per-app stem OVERRIDE keyed by the vendored dir name so jira/confluence get DISTINCT
   canonical stems and cloudflare/datadog get their brand stems. This is the machinery
   change 02/04 rely on -- without it those plans cannot emit the slugs they assert and
   jira would be indistinguishable from confluence.
3. **MED-03 synonym fix (BRDTH-01, carried from 36-REVIEW):** rewrite the heuristic
   `synthSynonyms` so cross-app `create_*` near-neighbors (asana/linear/todoist) carry an
   app/origin-disambiguating token in every synthesized phrase and never emit a
   grammatically-broken phrase (no "list a tasks"), proven by wrong-invoke=0 on the
   real emitted collision set (in breadth-search-return.test.js).
4. **Backing-status (BRDTH-03):** stamp every imported descriptor with a `backing` enum
   and carry it through `capability-search.js` so search annotates a pending-only
   descriptor as discovery-pending, never a confident invocable hit; backfill
   `backing: 'handler'` onto the existing github/slack/notion head descriptors and
   `backing: 'dom'` onto the declarative-tail descriptors so the annotation is correct
   for the whole shipped corpus. CANONICAL ENUM: the descriptor FIELD value is one of
   recipe/handler/learn/dom -- 'learn' (NOT 'learn-pending') is the value resolve()
   (capability-catalog.js:351) routes to T2 and no-dead-entry.test.js already keys on;
   'discovery-pending'/'learn-pending' is the search DISPLAY annotation label only, never
   the field value (no resolve() change -> lower risk).
5. **Merge-time batch gate (BRDTH-02):** reuse Phase 35's `classifyGate` as a merge-time
   denylist-coverage assertion that ABORTS the import (build fails) when any batch origin
   is unclassified -- establishing the per-batch gate 38/39 inherit.
6. **Eval seed-feeding (BRDTH-01, the eval-gate fix):** the eval harness
   (capability-search-eval.test.js) indexes ONLY _fixtures/seed-descriptors.json but
   iterates _fixtures/intent-cases.json. Add a seed-feeding step to the importer that
   writes each emitted real descriptor's searchable shape into seed-descriptors.json, so
   every intent-case expectedSlug the later plans add HAS an indexed descriptor and the
   eval's recall/wrong-invoke gate stays satisfiable across 02/03/04.
7. **No-dead-entry generalization (CGEN-03 at breadth):** generalize the no-dead-entry
   corpus loader (currently hardcoded to opentabs__todoist__) to match ALL opentabs__*.json
   so each batch's NEW descriptor-only slugs are genuinely checked for non-null T3/T2
   resolution (the loader is the thing 02/03/04's per-wave no-dead-entry assertion relies on).

Vendor the linear + asana metadata slices (the 2 apps needed alongside the already-vendored
todoist to exercise the create_* collision fix in this plan), regenerate the catalog
snapshot, and land the Wave-0 tests.

Purpose: the breadth contract is the deliverable, not just the apps -- 38/39 must reuse
search-returns-with-synonyms + side-effect-class + backing-status, batch-gated on denylist,
with a different category, against CORRECTED machinery that emits distinct stems, feeds the
eval seed, and is genuinely no-dead-entry-checked.
Output: extended importer (enumeration + stem override + MED-03 synonyms + backing + batch
gate + seed-feeding) + search annotation; linear/asana descriptors; regenerated snapshot;
generalized no-dead-entry loader + 3 Wave-0 tests; the existing corpus backfilled with backing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-breadth-a-dev-productivity-least-sensitive/37-CONTEXT.md
@.planning/phases/37-breadth-a-dev-productivity-least-sensitive/37-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the live codebase. No exploration required. -->

The vendored-slice contract (replicate the todoist shape for linear + asana). Each app dir
under vendor/opentabs-snapshot/plugins/<app>/ contains:
  - package.json: { "_role":"opentabs-metadata-snapshot", "name":"@opentabs-dev/opentabs-plugin-<app>",
    "type":"module", "opentabs": { "displayName", "description", "urlPatterns":["*://<host>/*"], "homepage" },
    "license":"MIT" } -- urlPatterns[0] host is the AUTHORITATIVE service/origin.
  - src/sdk-stub.ts: VERBATIM copy of vendor/opentabs-snapshot/plugins/todoist/src/sdk-stub.ts
    (exports `defineTool = (config) => config` identity factory + abstract `OpenTabsPlugin`;
    the importer reads .name/.description/.input/.group/.summary only, NEVER runs handle()).
  - src/<app>-api.ts: inert transport stub (api/apiVoid/graphql throwers that throw if executed;
    a comment records the upstream default-method verb facts for auditability). For a GraphQL
    app (linear), export an inert `graphql` helper; for a REST app (asana), export inert
    `api`/`apiVoid` like todoist-api.ts.
  - src/index.ts: `class <App>Plugin extends OpenTabsPlugin { name; description; displayName;
    urlPatterns; homepage; tools: ToolDefinition[] = [ ...imported tool consts ] }` then
    `const plugin = new <App>Plugin(); export default plugin; export { plugin };`
  - src/tools/<op>.ts: `export const <opCamel> = defineTool({ name:'<op_snake>', displayName,
    description, summary, icon, group, input: z.object({...}), output: z.object({...}),
    handle: async (...) => { /* NEVER executed; comment names the upstream helper+method */ } })`.

The importer (scripts/import-opentabs-catalog.mjs) public surface the executor edits:
  - `const SMOKE_APPS = ['todoist'];` (line ~77) -> REPLACE with `enumerateBatchApps()`.
  - `serviceStem = service.replace(/^app\./,'').split('.')[0]` inside extractDescriptors
    (line ~294) -> route through a `displayServiceStem(app, derivedStem)` that consults a
    `STEM_OVERRIDES` map keyed by the vendored dir name. The derived stem is correct for
    linear (linear.app->linear), asana (app.asana.com->asana), clickup (app.clickup.com->
    clickup), airtable (airtable.com->airtable), gitlab/bitbucket/vercel/netlify/sentry/
    posthog/circleci. It is WRONG only for: jira (*.atlassian.net->subdomain), confluence
    (*.atlassian.net->subdomain, AND collides with jira), cloudflare (dash.cloudflare.com->
    'dash'), datadog (app.datadoghq.com->'datadoghq'). STEM_OVERRIDES therefore maps exactly
    { jira:'jira', confluence:'confluence', cloudflare:'cloudflare', datadog:'datadog' }
    (keyed by dir name). Everything else falls through to the derived stem unchanged.
  - `function synthSynonyms(tool, serviceStem)` (lines ~236-260) -> the MED-03 rewrite target.
  - `extractDescriptors(app)` stamps `backing: 'dom'` (line ~328) -> make backing a per-app
    backing policy (handler/recipe/learn/dom).
  - `runImport()` (lines ~346-386) already does classifyGate-before-emit; ADD the merge-time
    batch-coverage assertion + the seed-feeding step there.
  - imports `{ verbPrefix, deriveClass } from './lib/side-effect-class.mjs'` and
    `{ classifyGate } from './verify-classification-gate.mjs'` (UNCHANGED -- reuse).

capability-search.js INDEX_OPTIONS (lines 48-52) -- the load-bearing options reused at loadJSON:
  idField:'slug'; fields (searchable):['intentSynonyms','description','service','actionVerb'];
  storeFields (returned on hit):['slug','service','sideEffectClass','description'].
  buildIndex(descriptors, slugToRecipe) (line 110) maps each descriptor into the indexed doc.
  search(query, ownedOrigin, topN) (line 223) returns hits with {slug,service,sideEffectClass,
  description,score,params}. ORIGIN_BOOST=4 already biases owned-origin hits (D-11).

The eval (tests/capability-search-eval.test.js): line 47 requires _fixtures/seed-descriptors.json;
  line 49 requires _fixtures/intent-cases.json; line 77 buildIndex(SEED_DESCRIPTORS,...); the
  recall@5/wrong-invoke loop (lines 86-103) iterates intent-cases AGAINST the seed-indexed ms.
  An intent-case whose expectedSlug is NOT in seed-descriptors.json is BOTH a recall miss AND a
  wrong-invoke -> the gate exits 1. seed-descriptors.json ALREADY carries asana.create_task /
  asana.list_tasks / linear.create_issue (the 36-04 near-neighbors, slug form `<stem>.<op>` ==
  the emitted form). The seed-feeding step extends this to EVERY emitted app.

classifyGate (scripts/verify-classification-gate.mjs):
  classifyGate(items, opts) -> { failures: string[] }; items: [{ origin, service?, slug?, description? }];
  an origin classified denied/sensitive -> OK; an unclassified sensitivity-suspect -> a failure.
  Denylist.load() MUST be awaited before classifyGate (the importer already does this).
  The dev/productivity origins (linear.app, app.asana.com, ...) are NOT in the denylist ->
  classify safe-by-default and pass (correct for the least-sensitive batch).

resolve() descriptor-only fallback (extension/utils/capability-catalog.js:347-355):
  a searchable slug with no REGISTRY handler + no recipe returns
  { tier: desc.backing==='learn' ? 'T2' : 'T3', descriptor } -- backing drives the seam tier.
  THE CANONICAL FIELD VALUE for the T2 seam is the literal 'learn' (NOT 'learn-pending') --
  this is why the descriptor `backing` field value is 'learn'/'dom'/'handler'/'recipe'.

no-dead-entry.test.js (tests/no-dead-entry.test.js): line 62-64 currently filters the corpus to
  `name.indexOf('opentabs__todoist__') === 0` -> it NEVER checks linear/asana/later-batch slugs
  (a false-green for every new app). Line 74 asserts every loaded descriptor is backing
  'dom'/absent. Generalizing the loader to all opentabs__*.json is the machinery change 02/03/04's
  per-wave assertion depends on.
</interfaces>

# Reuse (do NOT rebuild) -- the Phase-36 pipeline:
@scripts/import-opentabs-catalog.mjs
@scripts/lib/side-effect-class.mjs
@scripts/verify-classification-gate.mjs
@extension/utils/capability-search.js
@extension/utils/capability-catalog.js
@tests/capability-search-eval.test.js
@tests/no-dead-entry.test.js
@catalog/descriptors/_fixtures/seed-descriptors.json
@catalog/descriptors/_fixtures/intent-cases.json
@vendor/opentabs-snapshot/plugins/todoist/src/index.ts
@vendor/opentabs-snapshot/plugins/todoist/src/tools/create-task.ts
@vendor/opentabs-snapshot/plugins/todoist/src/tools/close-task.ts
@vendor/opentabs-snapshot/plugins/todoist/src/sdk-stub.ts
@vendor/opentabs-snapshot/plugins/todoist/src/todoist-api.ts
@catalog/descriptors/opentabs__todoist__create_task.json
@catalog/descriptors/github-issues.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Vendor the linear + asana metadata slices (the MED-03 collision apps)</name>
  <files>vendor/opentabs-snapshot/plugins/linear/** (package.json, src/sdk-stub.ts, src/index.ts, src/linear-api.ts, src/tools/{create-issue,list-issues,get-issue,update-issue,create-comment}.ts), vendor/opentabs-snapshot/plugins/asana/** (package.json, src/sdk-stub.ts, src/index.ts, src/asana-api.ts, src/tools/{create-task,list-tasks,get-task,update-task}.ts), vendor/opentabs-snapshot/PIN.md</files>
  <read_first>
    - vendor/opentabs-snapshot/plugins/todoist/package.json (the slice package.json shape to replicate)
    - vendor/opentabs-snapshot/plugins/todoist/src/index.ts (the plugin/tools export shape)
    - vendor/opentabs-snapshot/plugins/todoist/src/sdk-stub.ts (copy VERBATIM into each new app)
    - vendor/opentabs-snapshot/plugins/todoist/src/todoist-api.ts (the inert transport-stub shape)
    - vendor/opentabs-snapshot/plugins/todoist/src/tools/create-task.ts (a write op via `api` POST)
    - vendor/opentabs-snapshot/plugins/todoist/src/tools/close-task.ts (a write op via `apiVoid`)
    - vendor/opentabs-snapshot/PIN.md (the SHA 4b170216 + MIT pin these slices inherit)
  </read_first>
  <behavior>
    - `await import()` of vendor/opentabs-snapshot/plugins/linear/src/index.ts resolves under tsx and exposes a plugin with tools[] (name='linear', urlPatterns=['*://linear.app/*']); each tool exposes .name/.description/.input/.summary/.group; no handle() runs.
    - `await import()` of asana/src/index.ts resolves (name='asana', urlPatterns=['*://app.asana.com/*']).
    - linear's transport stub exports an inert `graphql` helper (GraphQL/camelCase app); asana's exports inert `api`/`apiVoid` (REST). All throw if executed.
    - Every tool's `input` is a real z.object reflecting the op's actual params (not a stub {}); z.toJSONSchema(input) produces closed additionalProperties:false params.
  </behavior>
  <action>
    Create the linear and asana vendored metadata slices mirroring the todoist slice structure EXACTLY (Wall 1: metadata-only -- NO dist/, NO executed handle()). For EACH app create package.json, src/sdk-stub.ts (a VERBATIM copy of todoist's sdk-stub.ts), src/index.ts, src/<app>-api.ts (the inert transport stub), and src/tools/<op>.ts per op.

    linear (service host linear.app, GraphQL transport -- the camelCase/GraphQL collision app): ops create_issue, list_issues, get_issue, update_issue, create_comment. Its linear-api.ts exports an inert `graphql` helper (a thrower) whose name matches GRAPHQL_TRANSPORT_RE in side-effect-class.mjs so the importer's carve-out fires; each tool's handle references `graphql(...)` in its never-run closure and a comment records the upstream operation (e.g. create_issue -> graphql issueCreate mutation -> write; list_issues -> graphql issues query -> read; update_issue -> issueUpdate -> write; create_comment -> commentCreate -> write; get_issue -> issue query -> read). Keep the FSB slug verb in snake form the importer expects (create_issue, list_issues, ...); verbPrefix is camelCase-aware either way.

    asana (service host app.asana.com, REST transport): ops create_task, list_tasks, get_task, update_task. Its asana-api.ts exports inert `api`/`apiVoid` like todoist-api.ts; create_task handle references `api('/tasks', { method:'POST', ... })` (a comment names the upstream POST -> write); list_tasks/get_task reference `api(..., { method:'GET' })` (read); update_task references `api(..., { method:'PUT' })` (write).

    Each tool `input` MUST be a real z.object reflecting the op's actual parameters (title/name/content, ids, labels, assignee, etc.), faithful to the OpenTabs op shapes -- NOT an empty or placeholder schema. The slice provenance is the PIN.md SHA (4b17021637d2cac12b8d84d21c40e765aa7b85e9) + MIT; the importer reads service from package.json.opentabs.urlPatterns and stamps the SHA from its OPENTABS_SHA constant, so no per-slice SHA literal is needed beyond matching the pin. Append `linear` and `asana` to vendor/opentabs-snapshot/PIN.md's per-phase log (Phase 37 batch A).

    Do NOT vendor an e2e-test/prescript fixture plugin and do NOT vendor any denied/sensitive app.
  </action>
  <verify>
    <automated>node --import tsx -e "import('./vendor/opentabs-snapshot/plugins/linear/src/index.ts').then(m=>{const p=m.default;if(!p||!Array.isArray(p.tools)||p.tools.length<5)process.exit(1);console.log('linear tools',p.tools.map(t=>t.name).join(','));return import('./vendor/opentabs-snapshot/plugins/asana/src/index.ts')}).then(m=>{const p=m.default;if(!p||!Array.isArray(p.tools)||p.tools.length<4)process.exit(1);console.log('asana tools',p.tools.map(t=>t.name).join(','));process.exit(0)}).catch(e=>{console.error(e.message);process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - The linear slice imports under tsx and exposes >=5 tools (create_issue, list_issues, get_issue, update_issue, create_comment); the asana slice exposes >=4 (create_task, list_tasks, get_task, update_task).
    - Each tool's input is a real z.object (z.toJSONSchema yields properties, not {}).
    - No handle() body executes during import (no fetch/DOM error thrown); the inert stubs throw ONLY if called.
    - linear-api.ts exports a `graphql` helper matching /graphql|gql/i; asana-api.ts exports `api`/`apiVoid`.
  </acceptance_criteria>
  <done>linear (5 ops) + asana (4 ops) vendored as hermetic metadata slices that import cleanly under tsx with real zod input schemas; PIN.md per-phase log notes the Phase-37 batch-A apps.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend the importer -- batch enumeration, serviceStem override, MED-03 synonyms, backing enum, merge-time batch gate, eval seed-feeding</name>
  <files>scripts/import-opentabs-catalog.mjs</files>
  <read_first>
    - scripts/import-opentabs-catalog.mjs (SMOKE_APPS line ~77; serviceStem derive ~294; synthSynonyms ~236-260; extractDescriptors backing stamp ~328; runImport gate ~346-386)
    - scripts/lib/side-effect-class.mjs (verbPrefix + deriveClass -- UNCHANGED, reuse; note delete_record/archive_project already in SIDE_EFFECT_OVERRIDES, purge/void/cancel/archive already DESTRUCTIVE_VERBS)
    - scripts/verify-classification-gate.mjs (classifyGate contract -- the merge-time gate)
    - extension/utils/capability-catalog.js (resolve() line ~351: backing==='learn' -> T2 -- the canonical 'learn' field value)
    - catalog/descriptors/_fixtures/seed-descriptors.json (the eval-indexed seed set the seed-feeding step extends; already carries asana/linear create near-neighbors in `<stem>.<op>` slug form)
    - .planning/phases/36-codegen-pipeline-no-dead-entry-resolution/36-REVIEW.md (MED-03 finding: the synthSynonyms collision/grammar weakness, lines 231-245)
    - catalog/descriptors/opentabs__todoist__create_task.json (the current emitted descriptor shape to preserve)
  </read_first>
  <behavior>
    - enumerateBatchApps() reads vendor/opentabs-snapshot/plugins/, returns the dev/productivity app dirs EXCLUDING: any app whose derived service already has a REGISTRY/head descriptor (github/slack/notion), the already-imported todoist, and any e2e-test/prescript fixture dir. Importing is idempotent: re-running emits the same flat opentabs__<svc>__<op>.json set without clobbering github/notion/slack/todoist descriptors.
    - displayServiceStem(app, derivedStem) returns STEM_OVERRIDES[app] when present, else derivedStem. STEM_OVERRIDES = { jira:'jira', confluence:'confluence', cloudflare:'cloudflare', datadog:'datadog' }. So jira (*.atlassian.net) and confluence (*.atlassian.net) emit DISTINCT opentabs__jira__/opentabs__confluence__ slugs (no collision); cloudflare emits opentabs__cloudflare__ (not opentabs__dash__); datadog emits opentabs__datadog__ (not opentabs__datadoghq__). linear/asana/clickup/airtable/gitlab/bitbucket/vercel/netlify/sentry/posthog/circleci fall through unchanged.
    - synthSynonyms(tool, serviceStem) emits >=3 (target 4) phrases, EVERY phrase containing the serviceStem token (app-disambiguating), and NEVER a grammatically-broken phrase: drop the "<verb> a <noun>" form when noun is plural; prefer "<verb> a <singular-noun> in <service>" / "<verb> <noun> in <service>" / "<summary> in <service>".
    - extractDescriptors stamps a backing enum per a per-app backing policy; for the Phase-37 data batch (no hand-port, no seed) backing is 'dom'. The value is one of recipe/handler/learn/dom (the canonical resolve()-seam values; 'learn' NOT 'learn-pending').
    - runImport() aborts (throws -> exit 1) when ANY enumerated batch origin is unclassified denied/sensitive/safe (the merge-time batch-coverage assertion via classifyGate), BEFORE writing any descriptor.
    - runImport() (after a successful emit) feeds the eval seed: for each emitted descriptor it upserts the descriptor's searchable shape (slug, service, intentSynonyms, description, actionVerb, sideEffectClass, backing) into catalog/descriptors/_fixtures/seed-descriptors.json keyed by slug (idempotent; existing seed entries for that slug are replaced, hand-authored non-opentabs seeds untouched).
  </behavior>
  <action>
    Edit scripts/import-opentabs-catalog.mjs:

    (a) Replace the hardcoded `const SMOKE_APPS = ['todoist'];` with `enumerateBatchApps()` -- a function that `readdirSync(VENDOR_ROOT)`s the vendored plugin dirs and returns those to import, EXCLUDING (i) apps whose derived service already owns a shipped REGISTRY/head descriptor (github.com/app.slack.com/www.notion.so -- read the existing catalog/descriptors/*.json service set, or a small explicit EXISTING_HEAD_SERVICES set documented in a comment), (ii) `todoist` (already imported in Phase 36), and (iii) any dir name matching /e2e|prescript|-test$/ (OpenTabs CI fixtures). Keep the existing per-app extract/gate/emit loop; just drive it off the enumerated list. Re-running MUST be idempotent and MUST NOT overwrite the github/notion/slack/todoist descriptors.

    (b) Add the serviceStem OVERRIDE: define `const STEM_OVERRIDES = { jira:'jira', confluence:'confluence', cloudflare:'cloudflare', datadog:'datadog' };` (a one-time machinery map, keyed by vendored dir name, with a comment naming the host each corrects: *.atlassian.net jira/confluence collision; dash.cloudflare.com; app.datadoghq.com) and a `function displayServiceStem(app, derivedStem) { return STEM_OVERRIDES[app] || derivedStem; }`. In extractDescriptors, after deriving `serviceStem = service.replace(/^app\./,'').split('.')[0]`, set `serviceStem = displayServiceStem(app, serviceStem)` BEFORE it is used to build the slug. The slug stays `<serviceStem>.<op>` and the filename `opentabs__<serviceStem>__<op>.json` -- now with the corrected stem. (The `service` field keeps the real host; only the stem/slug/filename is canonicalized.)

    (c) Rewrite `synthSynonyms` to fix MED-03 (per 36-REVIEW MED-03 + CONTEXT "Intent synonyms + MED-03"): every synthesized phrase MUST carry the serviceStem token so the index can disambiguate cross-app near-neighbors; DROP the `${parts[0]} a ${noun}` form when the noun is plural (so list_tasks never yields "list a tasks"); keep the verb+noun+service forms and the summary+service form. Guarantee >=3 entries, cap at 5. The curated disambiguation is the serviceStem-in-every-phrase rule plus the owned-origin search bias already in capability-search.js (ORIGIN_BOOST).

    (d) Make `backing` a per-app policy value rather than a hardcoded literal: extractDescriptors stamps `backing` from a `backingFor(app, service)` helper returning 'dom' for the Phase-37 data batch (no hand-port, no seed). Keep the enum to recipe/handler/learn/dom (the canonical 'learn' field value resolve() routes to T2 -- NOT 'learn-pending', which is a display label only). (Head handlers and seeds land in later phases; this batch is dom-backed.)

    (e) Add the merge-time batch-coverage assertion in runImport(): after building gateItemsList for the enumerated batch and BEFORE writing, call classifyGate(gateItemsList) (Denylist already loaded) and throw a descriptive Error (-> process.exit(1)) if failures.length -- naming the offending origin(s). This is the per-batch gate 38/39 inherit; preserve the existing per-emit classifyGate call (reuse the already-awaited Denylist.load(); do not duplicate the load).

    (f) Add the eval seed-feeding step in runImport() AFTER the descriptors are written: a `feedSeedDescriptors(emittedDescriptors)` that reads catalog/descriptors/_fixtures/seed-descriptors.json, upserts each emitted descriptor's searchable shape ({ slug, service, intentSynonyms, description, actionVerb, sideEffectClass, backing }) keyed by slug (replace an existing same-slug entry, append a new one, leave every non-matching seed entry -- including the hand-authored non-opentabs near-neighbors -- untouched), and writes it back sorted by slug. This makes the eval (which buildIndexes seed-descriptors.json) have an indexed descriptor for every emitted slug, so the intent-cases the later plans add resolve (recall) and rank correctly (wrong-invoke=0). Do NOT feed intent-cases.json (test fixtures author those); only feed the descriptor index.

    Do NOT change the flat emit filename convention (opentabs__<serviceStem>__<op>.json, now with the corrected stem), the provenance shape, the forbidden-field pre-scan, or the side-effect derivation (deriveClass stays the shared module).
  </action>
  <verify>
    <automated>node --import tsx ./scripts/import-opentabs-catalog.mjs && node -e "const d=require('./catalog/descriptors/opentabs__linear__create_issue.json');const a=require('./catalog/descriptors/opentabs__asana__create_task.json');const seed=require('./catalog/descriptors/_fixtures/seed-descriptors.json');const fed=seed.some(s=>s.slug==='linear.create_issue')&&seed.some(s=>s.slug==='asana.create_task');const ok=d.intentSynonyms.every(s=>/linear/i.test(s))&&a.intentSynonyms.every(s=>/asana/i.test(s))&&d.intentSynonyms.length>=3&&['recipe','handler','learn','dom'].includes(d.backing)&&!a.intentSynonyms.some(s=>/\ba (tasks|issues)\b/i.test(s))&&fed;if(!ok){console.error('synonym/backing/seed-feed check failed',d.intentSynonyms,a.intentSynonyms,d.backing,'fed='+fed);process.exit(1)}console.log('PASS importer enumeration + stem-override + MED-03 synonyms + backing + seed-feed');process.exit(0)"</automated>
  </verify>
  <acceptance_criteria>
    - `node --import tsx ./scripts/import-opentabs-catalog.mjs` emits opentabs__linear__*.json (5) + opentabs__asana__*.json (4) and re-emits the 7 todoist descriptors WITHOUT clobbering github/notion/slack descriptors.
    - STEM_OVERRIDES is defined with the four collision-app keys; displayServiceStem routes the stem so jira/confluence/cloudflare/datadog would emit their canonical distinct slugs (proven on the real apps in 02/04).
    - Every linear/asana descriptor's intentSynonyms (>=3) each contain the service stem; no phrase matches "<verb> a <plural-noun>".
    - Every emitted descriptor carries `backing` in {recipe,handler,learn,dom}.
    - seed-descriptors.json now contains a same-slug entry for every emitted descriptor (linear.create_issue, asana.create_task, ... and the todoist ops), so the eval has an indexed descriptor for each.
    - A batch with an origin not classified denied/sensitive/safe makes the importer exit != 0 (proven in Task 4's gate test against the fixture).
  </acceptance_criteria>
  <done>The importer enumerates the vendored batch, canonicalizes the stem for collision apps, emits app-disambiguated grammatically-clean synonyms, stamps a backing enum (canonical 'learn'/'dom'), aborts the build on an unclassified batch origin, and feeds the eval seed; linear+asana descriptors emitted; todoist/head descriptors untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Backing-status through search + backfill the shipped corpus + generalize the no-dead-entry loader, then regenerate the snapshot</name>
  <files>extension/utils/capability-search.js, tests/no-dead-entry.test.js, catalog/descriptors/{github-issues,github-issues-create,github-notifications,notion-load-page,notion-spaces,slack-message,slack-conversations-list,reddit-inbox}.json, extension/catalog/recipe-index.generated.js</files>
  <read_first>
    - extension/utils/capability-search.js (INDEX_OPTIONS 48-52; buildIndex 110-129; search 223-266; addLearnedRecipe doc mapper 317-325)
    - tests/no-dead-entry.test.js (the corpus loader lines 62-68; the backing assertion line 74; the resolve() invariant loop)
    - extension/utils/capability-catalog.js (resolve() line ~351: backing==='learn' -> T2, else T3)
    - catalog/descriptors/github-issues.json + github-issues-create.json + github-notifications.json (head descriptors lacking backing)
    - catalog/descriptors/notion-load-page.json + notion-spaces.json + slack-message.json + slack-conversations-list.json + reddit-inbox.json (declarative-tail descriptors lacking backing)
    - scripts/package-extension.mjs (readJsonDir 51-89 -- the snapshot regen path; INV-01 IIFE/djb2)
    - scripts/validate-extension.mjs (88-105 -- the stale-snapshot byte-compare)
  </read_first>
  <behavior>
    - buildIndex carries `backing` into the indexed doc and INDEX_OPTIONS.storeFields includes `backing` so every search() hit returns it.
    - search() annotates each hit with an invocability signal derived from backing: a hit whose backing is 'handler' or 'recipe' is a confident invocable hit (invocable=true); a hit whose backing is 'dom' or 'learn' is returned but marked discovery-pending (invocable=false, backingStatus carries the enum, displayed as 'discovery-pending'/'learn-pending') -- NOT presented as a confident invocable hit.
    - The github/slack/notion head descriptors carry backing:'handler'; the declarative-tail descriptors carry backing:'dom'. The opentabs__* descriptors already carry backing from Task 2.
    - INDEX_OPTIONS stays a single constant passed to BOTH new MiniSearch(...) and loadJSON(...) (the load-bearing contract); adding a storeField does not break the round-trip.
    - no-dead-entry.test.js loads ALL opentabs__*.json (not just opentabs__todoist__) so linear/asana (and every later batch) descriptor-only slugs are genuinely checked for non-null T3/T2 resolution; its line-74 backing assertion tolerates backing in {'dom','handler','learn',undefined} (the grown corpus may include non-dom backings).
    - After the edits, `node scripts/package-extension.mjs` regenerates extension/catalog/recipe-index.generated.js so validate-extension's byte-compare reconciles (IIFE wrapper + djb2 tail UNCHANGED; only the DATA literal grows).
  </behavior>
  <action>
    (a) Edit extension/utils/capability-search.js: add `backing` to INDEX_OPTIONS.storeFields; in buildIndex's addAll mapper carry `backing: d.backing || 'dom'` into the indexed doc; in search()'s hit mapper surface the backing and derive an annotation -- add a `backingStatus` (the raw enum) and an `invocable` boolean (true iff backing is 'handler' or 'recipe') to each returned hit so a caller distinguishes a confident invocable hit from a discovery-pending one. Do NOT change idField, the searchable `fields`, ORIGIN_BOOST, or the loadJSON option-reuse discipline. Mirror the same backing fields in addLearnedRecipe's doc mapper for parity (a learned recipe is backing:'recipe').

    (b) Backfill `backing` on every shipped descriptor under catalog/descriptors/ that lacks it: github-issues.json, github-issues-create.json, github-notifications.json -> "handler" (T1a head); notion-load-page.json, notion-spaces.json, slack-message.json, slack-conversations-list.json, reddit-inbox.json -> "dom" (declarative tail / DOM-backed reads). Add the field next to sideEffectClass; change nothing else in those files.

    (c) Generalize tests/no-dead-entry.test.js so the corpus loader (currently `name.indexOf('opentabs__todoist__') === 0`) matches ALL `opentabs__*.json` flat descriptors (e.g. `name.indexOf('opentabs__') === 0 && name.endsWith('.json')`), so the no-dead-entry invariant genuinely covers linear/asana (and every later batch's) descriptor-only slugs -- not just todoist. Widen the line-74 backing assertion from `d.backing === 'dom' || d.backing === undefined` to also tolerate `'handler'` and `'learn'` (the grown corpus may carry non-dom backings; the invariant under test is non-null seam resolution, not a single backing value). Keep the concrete todoist.create_task T3 assertion and the synthetic backing:'learn' T2 assertion and the negative control UNCHANGED. The loader change is what makes 02/03/04's per-wave no-dead-entry assertion real.

    (d) Regenerate the catalog snapshot: run `node scripts/package-extension.mjs` so extension/catalog/recipe-index.generated.js inlines the linear/asana descriptors + the backing backfill. Confirm the IIFE/djb2 shape is byte-stable (only the DATA literal grows) and `node scripts/validate-extension.mjs` reconciles (exit 0, no stale-snapshot failure).
  </action>
  <verify>
    <automated>node scripts/package-extension.mjs && node scripts/validate-extension.mjs && node tests/capability-search-eval.test.js && node tests/no-dead-entry.test.js && node -e "const fs=require('fs');const t=fs.readFileSync('tests/no-dead-entry.test.js','utf8');if(t.includes(\"'opentabs__todoist__'\")&&!t.includes(\"indexOf('opentabs__')\")){console.error('no-dead-entry loader not generalized');process.exit(1)}console.log('PASS no-dead-entry loader generalized')"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validate-extension.mjs` exits 0 (snapshot reconciled; no stale-snapshot failure).
    - capability-search-eval.test.js still passes (round-trip + schema-on-hit intact after adding the backing storeField).
    - no-dead-entry.test.js loads ALL opentabs__*.json (todoist + linear + asana) and stays exit 0 -- the linear/asana descriptor-only slugs resolve to a non-null T3/T2 seam (proving the generalized loader checks them, not a todoist-only false-green).
    - A search hit for a github head slug exposes invocable=true/backingStatus='handler'; a hit for an opentabs__ dom descriptor exposes invocable=false/backingStatus='dom'.
  </acceptance_criteria>
  <done>backing flows descriptor -> buildIndex -> search annotation; the whole shipped corpus carries backing; the no-dead-entry loader is generalized to all opentabs__* so each batch is genuinely checked; snapshot regenerated and validate-extension green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Wave-0 proof tests -- breadth search-return (the GENUINE MED-03 collision proof), merge-time batch gate, backing annotation; register in CI</name>
  <files>tests/{breadth-search-return,breadth-batch-gate,backing-status-annotation}.test.js, catalog/descriptors/_fixtures/{batch-unclassified-origin.fixture,intent-cases}.json, package.json</files>
  <read_first>
    - tests/capability-search-eval.test.js (the eval harness pattern: plant MiniSearch + FsbRecipeIndex globals, buildIndex, recall@5/wrong-invoke loop, the "recall@5=...wrong-invoke=..." METRICS line; lines 47/49/77/86 -- the seed-indexed-vs-intent-cases-iterated contract the seed-feeding fixes)
    - tests/no-dead-entry.test.js (the zero-framework check(cond,msg)/process.exit pattern + loading the real emitted corpus)
    - catalog/descriptors/_fixtures/intent-cases.json (the eval fixture format {intent, expectedSlug})
    - catalog/descriptors/_fixtures/seed-descriptors.json (now seed-fed by the Task-2 importer; already carries asana/linear/todoist create near-neighbors)
    - catalog/descriptors/_fixtures/unclassified-sensitive.fixture.json (the Phase-35 unclassified-origin fixture shape to mirror for the batch-gate test)
    - scripts/verify-classification-gate.mjs (classifyGate -- the merge-time gate the batch-gate test drives)
    - package.json (the `test` chain line ~17 + `validate:extension` line ~31 -- where the new tests register)
  </read_first>
  <behavior>
    - tests/breadth-search-return.test.js: builds the index over the REAL emitted opentabs__linear__*/opentabs__asana__*/opentabs__todoist__* descriptors and asserts EVERY op returns from search with >=3 intentSynonyms, a sideEffectClass, and a backing field; prints a "recall@5=... wrong-invoke=..." METRICS line and asserts recall@5>=0.9. This is the GENUINE MED-03 collision proof: it asserts "create an issue in linear" tops linear.create_issue, "create a task in asana" tops asana.create_task, "create a task in todoist" tops todoist.create_task over the REAL emitted create_* set with wrong-invoke=0 (the eval-side intent-cases are a redundant secondary signal -- this real-corpus test is the load-bearing one).
    - tests/breadth-batch-gate.test.js: loads catalog/descriptors/_fixtures/batch-unclassified-origin.fixture.json (a descriptor whose origin trips a sensitivity axis but is unclassified), runs it through classifyGate (Denylist loaded) and asserts failures.length>0; asserts the real dev/productivity batch origins (linear.app, app.asana.com) pass classifyGate (failures.length===0). Proves the merge aborts on an unclassified origin and passes the safe batch.
    - tests/backing-status-annotation.test.js: builds the index, asserts a backing:'handler' descriptor (a github head slug) returns invocable=true/backingStatus='handler' and a backing:'dom' descriptor (an opentabs__ slug) returns invocable=false/backingStatus='dom' -- a pending-only descriptor returns but is NOT a confident invocable hit.
    - MED-03 collision (eval secondary signal): intent-cases.json gains cross-app create_* fixtures for linear/asana/todoist; because Task-2 seed-fed those emitted slugs into seed-descriptors.json, capability-search-eval.test.js (which reads intent-cases.json AND indexes seed-descriptors.json) keeps recall@5>=0.9 + wrong-invoke=0 -- "create an issue in linear" tops linear.create_issue, none cross-invokes.
    - All three new tests register in package.json `test` so `npm test` runs them.
  </behavior>
  <action>
    Create the batch-unclassified-origin fixture and the three new tests, add the MED-03 collision intent fixtures, and register everything.

    (a) Create catalog/descriptors/_fixtures/batch-unclassified-origin.fixture.json -- a single descriptor mirroring unclassified-sensitive.fixture.json but representing a BATCH origin that trips a sensitivity axis (e.g. a payment/finance brand host) and is NOT classified. _fixtures/ is excluded by readJsonDir non-recursion so it never ships.

    (b) Create tests/breadth-batch-gate.test.js (BRDTH-02): require classifyGate from scripts/verify-classification-gate.mjs, await Denylist.load(), assert classifyGate([fixtureItem]).failures.length>0 (build would abort), AND assert classifyGate([{origin:'https://linear.app',...},{origin:'https://app.asana.com',...}]).failures.length===0 (the safe batch passes). Zero-framework check()/process.exit pattern.

    (c) Create tests/backing-status-annotation.test.js (BRDTH-03): plant MiniSearch + a FsbRecipeIndex with a handler-backed descriptor and a dom-backed descriptor, buildOrRestore, assert the search annotation (invocable/backingStatus) distinguishes them.

    (d) Create tests/breadth-search-return.test.js (BRDTH-01) -- the GENUINE MED-03 proof: build the index over the real emitted opentabs__linear__*/opentabs__asana__*/opentabs__todoist__* corpus; assert per-op synonyms>=3 + side-effect class + backing present + recall@5>=0.9; assert wrong-invoke=0 over the cross-app create_* near-neighbor set (linear/asana/todoist create_* each tops its own slug, none cross-invokes); print the METRICS line. This real-corpus collision proof is load-bearing; do NOT rely on the eval-side fixtures for the wrong-invoke=0 claim.

    (e) Add cross-app create_* fixtures to catalog/descriptors/_fixtures/intent-cases.json with expectedSlug aligned to the emitted real slugs (linear.create_issue / asana.create_task / todoist.create_task) as a SECONDARY eval signal. The eval indexes _fixtures/seed-descriptors.json, which Task 2's seed-feeding now extends with every emitted slug (linear.create_issue / asana.create_task / todoist.create_task already in the `<stem>.<op>` form) -- so the expectedSlug resolves. Re-run capability-search-eval.test.js and confirm recall@5>=0.9 + wrong-invoke=0.000. (If a seed near-neighbor slug form differs from the emitted form, the seed-feeding upsert reconciles it; do NOT hand-edit seed-descriptors.json here.)

    (f) Register the three new test files in package.json `test` (append to the chain after head-handler-cap.test.js). Do NOT remove any existing test. (breadth-batch-gate exercises classifyGate, already chained in validate:extension via verify-classification-gate.mjs.)
  </action>
  <verify>
    <automated>node tests/breadth-batch-gate.test.js && node tests/backing-status-annotation.test.js && node tests/breadth-search-return.test.js && node tests/capability-search-eval.test.js && grep -q breadth-search-return package.json && grep -q breadth-batch-gate package.json && grep -q backing-status-annotation package.json && echo WAVE-0-OK</automated>
  </verify>
  <acceptance_criteria>
    - All three new tests pass: breadth-batch-gate (failures>0 on the unclassified fixture, 0 on the safe batch), backing-status-annotation (handler invocable=true, dom invocable=false), breadth-search-return (every op has synonyms+class+backing, recall@5>=0.9, AND wrong-invoke=0 on the real-corpus create_* collision set).
    - capability-search-eval.test.js prints recall@5>=0.9 + wrong-invoke=0.000 over the collision-augmented intent-cases (its seed is fed by Task 2, so the new expectedSlugs resolve; no cross-app create_* mis-invoke).
    - The three test files are registered in package.json `test`.
  </acceptance_criteria>
  <done>The three Wave-0 proof tests pass and are registered in CI; the breadth contract (search-return + batch-gate + backing-annotation) is proven on linear/asana/todoist, with the GENUINE MED-03 wrong-invoke=0 over the real emitted corpus and the eval as a seed-fed secondary signal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OpenTabs vendored metadata -> FSB descriptor corpus (build-time) | untrusted upstream metadata is read by the importer; a wrong origin or a forbidden field could enter the shipped catalog |
| descriptor corpus -> search_capabilities result (runtime) | an agent/user reads a search hit and may invoke it; a pending-only descriptor must not look invocable |
| search hit -> invoke gate (runtime) | a synonym collision could route an intent to the wrong app's authenticated op |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | Elevation of Privilege | the merge-time batch import (a sensitive/ToS origin's descriptor reaching the corpus because its origin was not denylist-classified) | mitigate | runImport() calls classifyGate over EVERY enumerated batch origin BEFORE writing and ABORTS (exit 1) on any unclassified denied/sensitive/safe origin (Task 2e); proven by tests/breadth-batch-gate.test.js against batch-unclassified-origin.fixture.json (fail-closed) |
| T-37-02 | Spoofing | a pending-only descriptor surfaced as a confident invocable hit -> a user/agent invokes something with no backing | mitigate | every descriptor carries a backing enum; search() annotates invocable=false / backingStatus for backing dom/learn so a discovery-pending descriptor is never presented as a confident invocable hit (Task 3a); proven by tests/backing-status-annotation.test.js |
| T-37-03 | Spoofing | synonym collision causing wrong-app invoke (cross-app create_* near-neighbors: asana/linear/todoist) | mitigate | MED-03 synonym rewrite puts the app/origin token in every synthesized phrase + owned-origin search bias; proven by wrong-invoke=0 over the REAL emitted create_* corpus in tests/breadth-search-return.test.js (Task 2c + Task 4d), with the eval intent-cases a seed-fed secondary signal (Task 4e) |
| T-37-04 | Tampering | a vendored slice emitting a forbidden field name (script/expr/transform/code/fn/js) into shipped params (Wall 1) | mitigate | the importer's recursive forbidden-field pre-scan (assertCleanParams, UNCHANGED from Phase 36) THROWS before emit; verify-recipe-path-guard.mjs stays green via validate:extension |
| T-37-05 | Tampering | the vendored handle() runtime (DOM/fetch) dragged into the build importer's import graph | accept/mitigate | the per-app sdk-stub + inert transport stub resolve defineTool/api/graphql without the real SDK; the importer reads metadata only and never executes handle() -- the same Wall-1 boundary Phase 36 verified for todoist |
| T-37-08 | Spoofing | the serviceStem OVERRIDE mapping a stem to the WRONG brand (e.g. jira<->confluence swapped) -> a slug that routes an intent to the wrong authenticated app | mitigate | STEM_OVERRIDES is keyed by the vendored dir name (jira/confluence/cloudflare/datadog) with each value its own canonical stem; the breadth-search-return + eval tests assert each app's create_*/op tops its OWN slug (wrong-invoke=0), catching a swapped override |
| T-37-SC | Tampering | npm/pip/cargo installs | accept | Phase 37 adds NO new packages (zod/tsx/@opentabs-dev/plugin-sdk already devDeps from Phase 36); no install task -> the package-legitimacy gate is N/A this phase |
</threat_model>

<verification>
- `node --import tsx ./scripts/import-opentabs-catalog.mjs` emits the linear/asana descriptors + re-emits todoist, without clobbering github/notion/slack, and feeds seed-descriptors.json.
- `node scripts/validate-extension.mjs` exits 0 (snapshot reconciled; classification gate + crosscheck + recipe-path guard green).
- `node scripts/verify-catalog-crosscheck.mjs` exits 0 over the grown corpus (no under-stated destructive op; linear GraphQL writes never classed read).
- `node tests/no-dead-entry.test.js` exits 0 with the GENERALIZED loader (every opentabs__ searchable slug -- todoist + linear + asana -- resolves to a non-null seam tier).
- `node tests/breadth-search-return.test.js && node tests/breadth-batch-gate.test.js && node tests/backing-status-annotation.test.js` all pass (breadth-search-return carries the genuine wrong-invoke=0 MED-03 proof).
- `node tests/capability-search-eval.test.js` prints recall@5>=0.9 + wrong-invoke=0.000 over the collision-augmented intent-cases (seed-fed).
</verification>

<success_criteria>
- BRDTH-01: linear + asana (+ the existing todoist) descriptors are imported and returned by search with >=3-4 intent synonyms + a side-effect class per op; the importer enumerates the vendored batch (no hardcoded app list), canonicalizes the stem for collision apps, feeds the eval seed, and clobbers nothing.
- BRDTH-02: the merge carries a denylist-coverage assertion (classifyGate) that aborts the build on an unclassified batch origin -- the per-batch gate 38/39 inherit.
- BRDTH-03: every descriptor carries a backing enum (canonical field value 'learn'/'dom'/'handler'/'recipe'); search annotates a pending-only descriptor as discovery-pending, not a confident invocable hit.
- MED-03 (carried from 36-REVIEW): cross-app create_* near-neighbors disambiguate by app/origin with wrong-invoke=0, proven over the REAL emitted corpus in breadth-search-return.test.js.
- Infrastructure for 02/03/04: the serviceStem override (distinct jira/confluence/cloudflare/datadog stems), the generalized no-dead-entry loader, and the eval seed-feeding all land here so the later sub-batches are genuinely data-only against corrected machinery.
- Regression: crosscheck + no-dead-entry + the eval harness stay green on the grown corpus; validate:extension exits 0.
</success_criteria>

<output>
Create `.planning/phases/37-breadth-a-dev-productivity-least-sensitive/37-01-SUMMARY.md` when done.
</output>
