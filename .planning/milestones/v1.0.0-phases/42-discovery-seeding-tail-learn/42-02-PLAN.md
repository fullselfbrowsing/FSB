---
phase: 42-discovery-seeding-tail-learn
plan: 02
type: execute
wave: 1
depends_on: [42-01]
files_modified:
  - extension/utils/network-capture-redactor.js
  - extension/utils/network-capture.js
  - extension/utils/recipe-synthesizer.js
autonomous: true
requirements: [DSEED-01, DSEED-02]

must_haves:
  truths:
    - "AUTH_CARRIER_DENYLIST (+ a token-shape pattern set) covers the full 119-app auth-carrier field universe"
    - "A token-shaped URL PATH segment (JWT / stripe / github / slack / aws / google / Microsoft Graph `u!` share-id) is scrubbed to a `:tok` placeholder before the path is returned, while a benign hyphenated slug survives LITERAL"
    - "The 119-app redaction no-leak test (Plan 01 Task 3) is GREEN across all 3 sinks — including the path-token-in-segment hostile case AND the benign-slug negative case"
    - "The structure-only invariant holds: redactRequest still returns exactly { method, path, origin, headerNames }, reads header NAMES only (never values), and the path is the pathname structure with only token-SHAPED segments masked (no value-read); redactResponse still returns {status,mimeType} only"
    - "network-capture.js loads discovery-seeds.json lazily + no-throw (degrades to empty seeds), mirroring service-denylist"
    - "A seeded origin+path match raises the synthesizer's recognition (a metadata seedMatch flag) but NEVER triggers a fetch and NEVER changes whether a recipe executes"
  artifacts:
    - path: "extension/utils/network-capture-redactor.js"
      provides: "Extended AUTH_CARRIER_DENYLIST + path-segment token-shape scrubbing + header-name token-shape defense-in-depth, structure-only preserved"
      contains: "session_api_key"
    - path: "extension/utils/network-capture.js"
      provides: "discovery-seeds loader (lazy chrome.runtime.getURL fetch + Node require fallback, no-throw)"
      contains: "discovery-seeds.json"
  key_links:
    - from: "extension/utils/network-capture-redactor.js"
      to: "the learned-recipe envelope (synthesizer endpoint / descriptor.description)"
      via: "path-segment token-shape scrub in _shapeUrl before the path is returned"
      pattern: ":tok"
    - from: "extension/utils/network-capture.js"
      to: "extension/config/discovery-seeds.json"
      via: "chrome.runtime.getURL fetch (no-throw, degrade to empty)"
      pattern: "discovery-seeds\\.json"
    - from: "extension/utils/recipe-synthesizer.js"
      to: "the loaded seeds"
      via: "seed lookup raises recognition metadata only"
      pattern: "seed"
---

<objective>
Turn the DSEED-02 security keystone GREEN and wire the seed recognition path —
WITHOUT changing the structure-only redactor contract or granting any fetch right.
Four moves: (1) EXTEND `AUTH_CARRIER_DENYLIST` (+ a conservative header-NAME
token-shape pattern set) to the full 119-app auth-carrier field universe; (2) ADD
path-segment token-shape scrubbing to `_shapeUrl` so a token embedded in a URL
PATH segment (the OAuth callback / reset / share-link vector, incl the vendored
Microsoft Graph `u!` share-id) is masked to `:tok` before the path is persisted —
closing the leak into the learned-recipe `endpoint`/`descriptor.description`
(SC3 sink #1) — while benign hyphenated slugs survive LITERAL; (3) add a lazy,
no-throw `discovery-seeds.json` loader to `network-capture.js` mirroring
`service-denylist.js`; (4) make the synthesizer raise its recognition (a metadata
`seedMatch` flag) when a CAPTURED call matches a seeded origin+path — biasing
recognition ONLY, never executing. Moves (1)+(2) together turn the Plan-01 119-app
no-leak test (now incl the path-token-in-segment hostile case) GREEN.

Purpose: This is the DSEED-02 headline (the redactor proves zero auth substring at
scale — across header NAMES, header VALUES, the query string, AND now token-shaped
path SEGMENTS) plus the DSEED-01 seeding contract (a hint biases recognition,
never runs).
Output: the extended denylist, the path-segment scrubber, the seed loader, and the
recognition-bias hook.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-discovery-seeding-tail-learn/42-CONTEXT.md
@.planning/phases/42-discovery-seeding-tail-learn/42-01-SUMMARY.md

# The redactor to EXTEND (AUTH_CARRIER_DENYLIST :62; _shapeUrl :80-96 keeps u.pathname VERBATIM; structure-only redactRequest :99-121 / redactResponse :127-133):
@extension/utils/network-capture-redactor.js

# The loader MODEL to mirror (lazy chrome.runtime.getURL fetch + no-throw + Node require fallback):
@extension/utils/service-denylist.js

# The capture module the loader lands in (FsbNetworkCapture export :436-449; _redactor accessor :107):
@extension/utils/network-capture.js

# The synthesizer the redacted path flows INTO -- _toPathTemplate :118 / _looksVolatileSegment :110-116 keep a
# separator-bearing segment LITERAL (so a JWT/sk_live_/u! share-id stays literal -> persists into endpoint +
# descriptor.description); it ALREADY parameterizes prefixless high-entropy segments (all-digit / UUID / hex>=16 /
# alnum>=20-WITHOUT-separator), so path scrubbing only needs to add the distinctive-prefix token shapes:
@extension/utils/recipe-synthesizer.js

<interfaces>
<!-- The seeds object (Plan 01) the loader reads and the synthesizer consults: -->
<!--   { "<origin>": { hints:[{op,method,path}], provenance:{...} }, "_meta": {...} } -->
<!-- The redactor exports { redactRequest, redactResponse, AUTH_CARRIER_DENYLIST }. -->
<!-- redactRequest -> { method, path, origin, headerNames }   (NAMES only; auth carriers dropped; -->
<!--                                                            token-SHAPED path segments masked to :tok) -->
<!-- redactResponse -> { status, mimeType }                   (no headers, no body) -->
<!-- The redacted `path` is what the synthesizer reads as observedCall.path -> it becomes the recipe -->
<!-- `endpoint` and the descriptor `description` (the learned-recipe envelope), so a token left in a -->
<!-- path segment leaks unless masked HERE, upstream, before the path is returned. -->
<!-- The synthesizer's synthesize(observedCall) returns { recipe, descriptor, flaggedForPhase32 } | null. -->
<!-- A seedMatch flag is METADATA on the candidate/recipe-bookkeeping ONLY; it does not gate execution, -->
<!-- does not add a fetch, and must not change the recipe's closed-vocab executable fields. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EXTEND AUTH_CARRIER_DENYLIST + ADD path-segment token-shape scrubbing to the 119-app universe — structure-only preserved</name>
  <files>extension/utils/network-capture-redactor.js</files>
  <behavior>
    - The Plan-01 119-app no-leak test (`tests/network-capture-redaction.test.js`) goes GREEN: NONE of the 119-app auth-carrier NAMES (session_api_key, csrf_token, csrftoken, linear-client-id, organization, x-xsrf-token, x-amz-security-token, *-client-id / *-session-* families, ...) survive in headerNames.
    - The Plan-01 path-token-in-segment hostile case goes GREEN: a request whose URL PATH carries each distinctive token shape as a SEGMENT (`/auth/callback/eyJ...<sig>`, `/reset/eyJ...`, `/s/sk_live_...`, `/shares/u!aHR0cHM_...AB/driveItem`, `/x/gho_...`) has each token-shaped segment masked to `:tok` in the returned `path` — so the serialized artifact AND the resulting `path` contain ZERO of the distinctive sentinel token substrings.
    - The Plan-01 benign-slug NEGATIVE case stays GREEN: a benign hyphenated slug segment (`/orgs/my-long-organization-name/repos`) is NOT masked — it survives LITERAL in the returned `path` (proving the distinctive-prefix set does NOT false-positive on hyphenated slugs / org/repo names, so legitimate recipe templates still match the real path on replay).
    - redactRequest STILL returns exactly { method, path, origin, headerNames } and the loop STILL reads header NAMES only — never a header value (the structure-only invariant is INVIOLATE). Path-segment masking matches a segment's SHAPE only (the path is an already-kept structural field; shape-matching a segment is NOT a credential value-read).
    - redactResponse STILL returns exactly { status, mimeType }.
    - The existing 7-substring no-leak + the existing carrier removals stay GREEN (no regression).
    - The Phase-31 redaction suite (existing cases) stays GREEN.
  </behavior>
  <action>
TWO additive moves in `network-capture-redactor.js`, both structure-only (no value-read), both dynamic-code-FREE.

(A) EXTEND the `AUTH_CARRIER_DENYLIST` regex (network-capture-redactor.js:62) to cover the full 119-app
auth-carrier NAME universe audited from the vendored `*-api.ts` getAuth()/header-construction structs. Add the
SC-named carriers and their families: `session_api_key` / `.*session[_-]?api[_-]?key.*`, `csrf_token` / `csrftoken`
/ `.*csrf.*` / `.*xsrf.*` (already partially covered — ensure the underscore + bare forms match), `linear-client-id`
/ `.*client-id` (only the auth-bearing client-id families — keep it anchored so a benign `content-id`-style name is
not over-matched), `organization` ONLY where it is the auth-carrier form (prefer the explicit `linear-client-id` /
`x-organization`-style names over a bare `organization` to avoid over-redaction; document the choice in a comment),
plus the cross-app families `x-amz-security-token`, `x-amz-.*`, `.*-session-.*`, `.*-api-key`, `.*apikey.*`,
`.*access[_-]?token.*`, `.*id[_-]?token.*`, `.*refresh[_-]?token.*`. ADD a SEPARATE conservative header-NAME
token-SHAPE defense-in-depth set (per CONTEXT: name-based is the floor, token-shape is belt-and-suspenders for a
value that slips into a NAME position) — a small array of regexes for `sk_live_`/`sk_test_`-style keys,
`xoxc-`/`xoxb-`, `ghp_`/`gho_`, and a JWT `eyJ[A-Za-z0-9_-]+\.` prefix; apply it ONLY to the header NAME being
considered (never to a value — the redactor must not start reading values). Export the extended denylist unchanged
in shape (`AUTH_CARRIER_DENYLIST` plus, if you add the header-name token-shape set, an additional named export — do
NOT remove the existing export). Do NOT touch `redactRequest` / `redactResponse` return shapes or add any
value-read.

(B) ADD path-segment token-shape scrubbing in `_shapeUrl` (network-capture-redactor.js:80-96) — or a small new
`_shapePath(pathname)` helper applied to `u.pathname` BEFORE `_shapeUrl` returns. This closes the SC3 sink #1 leak:
today `_shapeUrl` keeps `u.pathname` VERBATIM, so a token embedded in a path SEGMENT survives into the redacted
`path` -> the synthesizer's `_looksVolatileSegment` keeps any separator-bearing segment LITERAL -> the token
persists into the learned recipe `endpoint` and the descriptor `description` (the learned-recipe envelope). Split the
pathname on `/`, and replace any SEGMENT that matches a DISTINCTIVE token shape with the placeholder `:tok`; re-join
preserving the leading slash. This is STRUCTURE-ONLY — the path is an already-kept structural field, and matching a
segment's SHAPE is not a credential value-read; the return shape stays exactly `{ path, origin }` (so `redactRequest`
still returns `{ method, path, origin, headerNames }` unchanged). Use a PRECISE distinctive-prefix set (NOT a broad
"long mixed segment with a separator" pattern — that would FALSE-POSITIVE on benign hyphenated slugs
(`/orgs/my-long-organization-name`, `/pages/my-document-title`, long repo names) -> mask them to `:tok` -> break the
legitimate recipe template so the endpoint no longer matches the real path on replay). Scrub ONLY these unambiguous
token shapes (each anchored at the SEGMENT start):
  - JWT:               `^eyJ[A-Za-z0-9_-]{10,}\.[A-Za-z0-9_-]+`     (the `eyJ...\.` prefix is distinctive)
  - Stripe:            `^(sk|pk|rk)_(live|test)_[A-Za-z0-9]{8,}`
  - GitHub:            `^gh[opsur]_[A-Za-z0-9]{20,}`
  - Slack:             `^xox[bcpars]-[A-Za-z0-9-]{8,}`
  - AWS access key:    `^(AKIA|ASIA)[A-Z0-9]{16}$`
  - Google OAuth:      `^ya29\.[A-Za-z0-9_-]+`
  - MS Graph share-id: `^u!`     (the documented vendored vector — excel-api.ts:145 builds `u!${base64url}`, and
                                  both excel-api.ts:151 + microsoft-word-api.ts:176 request `/shares/${shareId}/driveItem`)
Do NOT add a broad "clearly token-shaped long base64url/hex segment" rule unless it can be tightly bounded to avoid
benign slugs; if unsure, OMIT it — the synthesizer's EXISTING prefixless-high-entropy parameterization already covers
prefixless random path tokens (it parameterizes all-digit / UUID / hex>=16 / alnum>=20-WITHOUT-separator segments).
Record this RATIONALE in a code comment: the distinctive-prefix set covers the documented real vectors (OAuth
callback / reset / share-link tokens + the vendored `u!` share-id) with near-zero false-positive risk; the synthesizer
already handles prefixless high-entropy segments; favor LITERAL on ambiguity so benign slugs (`/v1/charges`, org/repo
names) are untouched. Keep the module dynamic-code-FREE (no run-string-as-code / function-from-string, even in
comments). Do NOT touch `redactResponse`.
  </action>
  <verify>
    <automated>node tests/network-capture-redaction.test.js</automated>
  </verify>
  <done>tests/network-capture-redaction.test.js is fully GREEN (incl the 119-app universe block, the path-token-in-segment hostile case masked to :tok, AND the benign-slug negative case surviving literal). redactRequest/redactResponse shapes are unchanged; no value is ever read; only token-SHAPED path segments are masked (benign slugs untouched). The module stays dynamic-code-free.</done>
</task>

<task type="auto">
  <name>Task 2: Add the discovery-seeds loader to network-capture.js (lazy, no-throw)</name>
  <files>extension/utils/network-capture.js</files>
  <action>
Add a seed loader to `network-capture.js` mirroring the `service-denylist.js` loader EXACTLY in posture: a
module-level `_seeds` cache + a `load()`-style lazy initializer that (a) in the SW/browser path fetches
`chrome.runtime.getURL('config/discovery-seeds.json')` and parses it; (b) in the Node path `require('../config/
discovery-seeds.json')`; (c) on ANY failure degrades to EMPTY seeds (`{}`) and emits a single best-effort
`rateLimitedWarn`/console.warn — NEVER throws on the boot path. Expose a synchronous read accessor
`getSeedsSync()` -> the loaded seeds object (or `{}` if not yet loaded) and a `getSeedForOrigin(origin)` ->
`{ hints, provenance } | null`. Add these to the `FsbNetworkCapture` export object (network-capture.js:436-449)
WITHOUT changing the existing exported surface (`startSession`/`endSession`/`_onCdpEvent`/`_filterResourceType`/
`_getObservedCalls`/`_getLastEndedCalls` stay byte-stable). CRITICAL per SC1: this loader reads METADATA — it adds
NO host permission, NO manifest change, and is NOT a fetch right to the seeded origins. It does NOT call
startSession differently, does NOT change the consent gate (`_runGate`), and does NOT change the capture core. The
seeds are only consulted later (Task 3 + Plan 03's resolve), never to initiate a network call.
  </action>
  <verify>
    <automated>node -e "globalThis.FsbNetworkCaptureRedactor={redactRequest:r=>r,redactResponse:r=>r,AUTH_CARRIER_DENYLIST:/x/};const c=require('./extension/utils/network-capture.js');if(typeof c.getSeedsSync!=='function')throw new Error('no getSeedsSync');if(typeof c.getSeedForOrigin!=='function')throw new Error('no getSeedForOrigin');['startSession','endSession','_onCdpEvent','_filterResourceType','_getObservedCalls'].forEach(k=>{if(typeof c[k]!=='function')throw new Error('regressed export '+k)});const s=c.getSeedForOrigin('https://dashboard.stripe.com');console.log('seed accessor ok; stripe seed', s?('hints='+(s.hints||[]).length):'null')"</automated>
  </verify>
  <done>network-capture.js exposes getSeedsSync()/getSeedForOrigin() that read discovery-seeds.json no-throw and degrade to {} on error; the existing exported surface is unchanged; no manifest/permission change; the consent gate + capture core are untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Synthesizer recognition bias on a seed match (metadata only, never executes)</name>
  <files>extension/utils/recipe-synthesizer.js</files>
  <behavior>
    - When synthesize(observedCall) runs on a CAPTURED call whose origin+path matches a seeded hint, the synthesized candidate carries a metadata recognition flag (e.g. recipe bookkeeping `seedMatch:true` or a candidate-level confidence note) — proving the hint RAISED recognition.
    - The flag is METADATA ONLY: the recipe's closed-vocab executable fields (origin, endpoint, method, authStrategy, extract, expectedShape) are byte-IDENTICAL to the no-seed synthesis of the same call. A seed never changes WHAT executes.
    - A hint NEVER triggers a fetch: synthesize is pure (no chrome.*, no network) and stays pure. With NO captured call, no recipe is produced for a seeded origin (the seed alone synthesizes nothing).
    - An unseeded captured call synthesizes exactly as before (no flag, no behavior change) — regression-free.
  </behavior>
  <action>
In `recipe-synthesizer.js synthesize()` (around the recipe-core build, :309-327), consult the loaded seeds
(via the `FsbNetworkCapture.getSeedForOrigin(origin)` accessor from Task 2, typeof-guarded so the Node synthesizer
suite degrades to no-seed when capture is absent). If the captured call's origin is seeded AND its path matches a
seeded hint path (exact or template-equivalent), stamp a METADATA recognition flag on the returned candidate's
bookkeeping (NOT on the executable recipe vocab) — e.g. set `flaggedForPhase32`-sibling bookkeeping `seedMatch:true`
on the returned object, or attach it to the candidate wrapper the discovery session reads. Do NOT add any
closed-vocab field to `recipe` (schema-lock forbids it) and do NOT change origin/endpoint/method/authStrategy/
extract/expectedShape. The seed is recognition BIAS: it can only raise confidence/recognition that a captured call
corresponds to a known seeded endpoint; it can NEVER initiate a fetch and NEVER change whether the candidate is
promote-after-replay-gated (the existing discovery-session.js clean-replay-or-DISCARD path is UNCHANGED). Keep
synthesize pure (no chrome.*, no network).
  </action>
  <verify>
    <automated>node tests/recipe-synthesizer.test.js</automated>
  </verify>
  <done>The synthesizer raises a metadata recognition flag on a seeded origin+path match while keeping the executable recipe vocab byte-identical to the no-seed case; it stays pure (no fetch); the existing synthesizer suite is GREEN.</done>
</task>

</tasks>

<verification>
- `node tests/network-capture-redaction.test.js` GREEN (the 119-app universe no-leak passes; the path-token-in-segment hostile case is masked to :tok; the benign-slug negative case survives literal; structure-only preserved).
- `node tests/recipe-synthesizer.test.js` GREEN (recognition bias is metadata-only; recipe vocab unchanged).
- `node tests/network-capture.test.js` and `node tests/network-capture-consent.test.js` GREEN (capture core + consent gate untouched).
- The network-capture loader reads discovery-seeds.json no-throw and exposes getSeedForOrigin without changing the existing export surface.
</verification>

<success_criteria>
- The redactor denylist covers the 119-app auth-carrier universe (incl session_api_key / csrftoken / linear-client-id) AND a token-shaped URL PATH segment (JWT / stripe / github / slack / aws / google / `u!` share-id) is scrubbed to :tok before the path is persisted — so no path-embedded token reaches the learned-recipe envelope — while benign hyphenated slugs survive literal. The structure-only invariant is preserved (names never values; path is shape with only token-shaped segments masked; redactResponse stays {status,mimeType}).
- The seed loader is lazy + no-throw, adds NO host permission, and does not alter the capture core or consent gate.
- A seed biases recognition (metadata flag) but never executes — proven by byte-identical recipe vocab and a pure synthesize.
</success_criteria>

<output>
Create `.planning/phases/42-discovery-seeding-tail-learn/42-02-SUMMARY.md` when done — list the exact carrier names/patterns + header-name token-shape regexes added to the denylist, the EXACT distinctive-prefix set used for path-segment scrubbing (JWT / stripe / github / slack / aws / google / `u!`), where the path scrub lives (`_shapeUrl` / `_shapePath`), the loader accessor names, and the seedMatch flag location (so Plan 05's no-leak/no-execute battery can reference them).
</output>
