---
phase: 42-discovery-seeding-tail-learn
plan: 05
type: execute
wave: 2
depends_on: [42-01, 42-02, 42-03, 42-04]
files_modified:
  - .planning/phases/42-discovery-seeding-tail-learn/42-HUMAN-UAT.md
autonomous: true
requirements: [DSEED-01, DSEED-02]

must_haves:
  truths:
    - "Full `npm test` exits 0 (all suites incl the 4 new/extended Phase-42 tests)"
    - "`npm run validate:extension` passes (manifest sanity + all verify gates)"
    - "manifest.json host_permissions is byte-unchanged from ['<all_urls>'] (the no-new-permission keystone, asserted)"
    - "The capture->learn->promote core + the consent gate (_runGate) are byte-unchanged (git diff confirms only the additive seam touches)"
    - "INV-01 (catalog/djb2 shape) + INV-03 (RECIPE_LEARN_PENDING byte-stable code) are GREEN"
    - "The live first-authenticated-visit capture is recorded as human_needed (42-HUMAN-UAT.md), not fabricated"
  artifacts:
    - path: ".planning/phases/42-discovery-seeding-tail-learn/42-HUMAN-UAT.md"
      provides: "The carried-forward live-first-visit UAT slice (the sole human_needed item)"
      contains: "human_needed"
  key_links:
    - from: "the final battery"
      to: "npm test + npm run validate:extension"
      via: "EXIT 0 gate"
      pattern: "npm test"
---

<objective>
The Phase-42 closing battery: prove the whole milestone surface is green and the
locked invariants hold, then record the one irreducibly-live item as documented
human_needed debt. Run full `npm test` (EXIT 0), `npm run validate:extension`,
assert the no-manifest-change keystone, confirm via `git diff` that the
capture->learn->promote core + the consent gate are untouched (only the additive
seams — denylist, seed loader, resolve branch, affordance — changed), and confirm
INV-01 + INV-03 green. Finally author `42-HUMAN-UAT.md` for the live
first-authenticated-visit capture (per seeded origin) — the seed->T2 + promote +
redactor are ALL proven headless; only the live capture on a real logged-in tab is
carried forward.

Purpose: This is the phase gate. It does not add behavior; it proves the four
deliverables landed without regressing the invariants and records the live debt.
Output: a green battery + the UAT doc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-discovery-seeding-tail-learn/42-CONTEXT.md
@.planning/phases/42-discovery-seeding-tail-learn/42-VALIDATION.md
@.planning/phases/42-discovery-seeding-tail-learn/42-01-SUMMARY.md
@.planning/phases/42-discovery-seeding-tail-learn/42-02-SUMMARY.md
@.planning/phases/42-discovery-seeding-tail-learn/42-03-SUMMARY.md
@.planning/phases/42-discovery-seeding-tail-learn/42-04-SUMMARY.md

# The UAT format to mirror (human_needed live-capture slice, redacted, non-blocking for CI):
@.planning/phases/41-depth-2-remaining-hand-ports-guarded-writes/41-HUMAN-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full battery — npm test EXIT 0 + validate:extension + no-manifest-change + capture/consent-core-unchanged + INV-01/INV-03</name>
  <files></files>
  <action>
Run the closing gates and confirm each:
1. `npm test` exits 0 (all suites, incl the new/extended Phase-42 tests: discovery-seeds-load, seed-resolve-t2,
   recipe-learn-pending-affordance, and the 119-app network-capture-redaction extension).
2. `npm run validate:extension` passes (manifest sanity + recipe-path-guard + classification-gate + crosscheck +
   no-duplicate-stem + origin-classification + no-orphan-descriptor).
3. The no-manifest-change keystone: assert `extension/manifest.json` host_permissions === ['<all_urls>'] byte-for-byte
   (re-run `node tests/discovery-seeds-load.test.js` which owns this assertion; additionally `git diff --quiet --
   extension/manifest.json` to confirm the manifest is byte-unchanged on this branch for the phase).
4. Capture/consent core UNCHANGED: `git diff` the Phase-31 core files (`extension/utils/network-capture.js`
   `_runGate`/`startSession`/`_onCdpEvent`/`endSession`; `extension/utils/discovery-session.js`) and confirm the
   ONLY network-capture.js change is the ADDITIVE seed loader (getSeedsSync/getSeedForOrigin) — the consent gate
   `_runGate`, `startSession`, `_onCdpEvent`, `endSession` bodies are byte-unchanged; discovery-session.js is
   untouched. Confirm via `node tests/network-capture-consent.test.js` + `node tests/consent-gate.test.js` +
   `node tests/learned-promote-after-replay.test.js` all GREEN (promote-after-replay CASE A/B/C: clean replay
   promotes, failed replay DISCARDS — the hint never executes).
5. INV-01: `node tests/catalog-inline-shape.test.js` + `node tests/no-dead-entry.test.js` GREEN (the resolve seed
   branch added a branch, not a data-shape change).
6. INV-03: `node tests/recipe-learn-pending-affordance.test.js` + `node tests/learned-t2-outranking.test.js` GREEN
   (RECIPE_LEARN_PENDING code byte-stable; the dual/triple-field shape intact).
If any gate is red, STOP and report the failure (do not fabricate a pass).
  </action>
  <verify>
    <automated>npm test && npm run validate:extension && node tests/discovery-seeds-load.test.js && node tests/network-capture-consent.test.js && node tests/learned-promote-after-replay.test.js && node tests/catalog-inline-shape.test.js && node tests/recipe-learn-pending-affordance.test.js && git diff --quiet -- extension/manifest.json && echo "PHASE-42 BATTERY GREEN; manifest byte-unchanged"</automated>
  </verify>
  <done>npm test + validate:extension pass; manifest host_permissions byte-unchanged; the capture core + consent gate + discovery-session are confirmed unchanged (only the additive seam touches); promote-after-replay + INV-01 + INV-03 are GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Author 42-HUMAN-UAT.md — the live first-authenticated-visit capture (sole human_needed)</name>
  <files>.planning/phases/42-discovery-seeding-tail-learn/42-HUMAN-UAT.md</files>
  <action>
Create `.planning/phases/42-discovery-seeding-tail-learn/42-HUMAN-UAT.md` mirroring the Phase-41 UAT format
(frontmatter `status: human_needed`; a "Why this is human_needed (not CI-provable)" section; a "Setup" section; a
"Required Scenarios" table). Content per CONTEXT/VALIDATION: the seed->T2 resolve, promote-after-replay (clean +
fail), the affordance, the 119-app redactor no-leak, and the no-manifest-change keystone are ALL proven HEADLESS;
the ONE irreducibly-live property is that on a REAL authenticated tab for a seeded origin, the discovery session
captures + replays + promotes a T2 recipe that then outranks the descriptor-T3, and the three sinks
(learned-recipe envelope, audit ring, diagnostic ring) carry ZERO auth substring for that real capture. State
clearly: a hint NEVER executes, so nothing learns or leaks without the consent-gated real capture — this is
carried-forward, user-gated debt, NON-blocking for CI (the headless gates pass). Add a Required Scenarios table
with 2-3 representative seeded origins (e.g. a non-sensitive seeded origin like `https://linear.app`, and note that
SENSITIVE seeded origins require the confirmedSensitive consent step). Each row: open an authenticated tab, run the
consent-gated discovery session, confirm a captured+replayed call promotes a T2 that outranks the descriptor, and
confirm the sinks carry zero auth substring; record redacted observation + date + Chrome version + commit. Reiterate
the GOV-06 rule: NEVER record a real token/CSRF/cookie VALUE — only the location/shape, redacted. Mark every row
`human_needed`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/42-discovery-seeding-tail-learn/42-HUMAN-UAT.md';const t=fs.readFileSync(p,'utf8');if(t.indexOf('human_needed')===-1)throw new Error('missing human_needed');if(!/Required Scenarios/.test(t))throw new Error('missing scenarios table');if(!/zero auth substring|ZERO auth substring/i.test(t))throw new Error('missing the no-leak live assertion');console.log('UAT doc ok')"</automated>
  </verify>
  <done>42-HUMAN-UAT.md exists with status: human_needed, a why-not-CI section, a setup section, and a Required Scenarios table (2-3 seeded origins) asserting the live promote-after-replay outranking + zero-auth-substring sinks; it is explicitly non-blocking for CI and forbids recording real token values.</done>
</task>

</tasks>

<verification>
- `npm test` EXIT 0 and `npm run validate:extension` pass.
- `git diff --quiet -- extension/manifest.json` (manifest byte-unchanged).
- `node tests/network-capture-consent.test.js`, `node tests/consent-gate.test.js`, `node tests/learned-promote-after-replay.test.js` GREEN (consent gate + capture core + promote-after-replay unchanged).
- `node tests/catalog-inline-shape.test.js`, `node tests/no-dead-entry.test.js` GREEN (INV-01).
- `node tests/recipe-learn-pending-affordance.test.js`, `node tests/learned-t2-outranking.test.js` GREEN (INV-03).
- 42-HUMAN-UAT.md exists, human_needed, non-blocking.
</verification>

<success_criteria>
- The full Phase-42 surface is green (npm test + validate:extension) with the locked invariants intact (no manifest change; capture/consent/promote core unchanged; INV-01 + INV-03 byte-stable).
- The live first-authenticated-visit capture is recorded as documented human_needed debt, not fabricated.
</success_criteria>

<output>
Create `.planning/phases/42-discovery-seeding-tail-learn/42-05-SUMMARY.md` when done — record the npm test + validate:extension results, the manifest byte-unchanged confirmation, the git-diff confirmation that only the additive seams changed, and the UAT scenario count.
</output>
