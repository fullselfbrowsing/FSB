---
phase: 39-breadth-c-commerce-travel-misc-most-sensitive
plan: 02
type: execute
wave: 2
depends_on: ["39-01"]
files_modified:
  - vendor/opentabs-snapshot/plugins/doordash/**
  - vendor/opentabs-snapshot/plugins/ubereats/**
  - vendor/opentabs-snapshot/plugins/uber/**
  - vendor/opentabs-snapshot/plugins/lyft/**
  - vendor/opentabs-snapshot/plugins/grubhub/**
  - vendor/opentabs-snapshot/plugins/instacart/**
  - vendor/opentabs-snapshot/PIN.md
  - catalog/descriptors/_fixtures/_provenance.json
  - catalog/descriptors/_fixtures/intent-cases.json
  - extension/catalog/recipe-index.generated.js
  - tests/breadth-search-return.test.js
  - tests/capability-search-eval.test.js
autonomous: true
requirements: [BRDTH-01, BRDTH-02, BRDTH-03]

must_haves:
  truths:
    - "The food-delivery + rideshare sub-batch (doordash, ubereats, uber, lyft, grubhub, instacart) is vendored as hermetic metadata-only slices (Wall 1: NO dist/, NO executed handle(), NO runtime .js) and imported via the FROZEN importer (ZERO machinery edit) -- each app returns from search with >=3 app-disambiguated synonyms + side-effect class + backing:'dom' per op"
    - "The merge-time classifyGate PASSES because 39-01 classified every one of these origins SENSITIVE (payment-bearing) -- each emitted descriptor's origin classifies sensitive; no unscreened payment origin ships; the payment-op CI guard PASSES (every payment op is backing:'dom' on a sensitive origin -- DOM-only, not API-invocable)"
    - "Each app's PAYMENT-bearing op (place_order/checkout/request_ride/...) classes write (or destructive where it cancels) AND is backing:'dom' on a sensitive origin -> the payment-op guard confirms it is NOT safe-and-API-invocable; reads (list_orders/track_order/list_restaurants) class read"
    - "The snapshot is regenerated via package-extension.mjs (INV-01: IIFE first-line + djb2/dual-export tail byte-stable, only the DATA literal grows); validate:extension exits 0; no prior descriptor (the 70 dev + 24 comms/social + the heads) is clobbered"
    - "node tests/no-dead-entry.test.js stays exit 0 (every new opentabs__ payment descriptor-only slug resolves to T3); the eval keeps recall@5>=0.9 wrong-invoke=0.000 (incl. cross-app order/ride collision probes); the SW cold-start budget holds at the now-grown size (flagged if it approaches the gate, per 39-CONTEXT)"
  artifacts:
    - path: "vendor/opentabs-snapshot/plugins/doordash/src/index.ts"
      provides: "vendored doordash metadata slice (doordash.com SENSITIVE per 39-01; payment-bearing place_order is the DOM-only-on-sensitive payment-op-guard subject)"
      contains: "DoordashPlugin"
    - path: "extension/catalog/recipe-index.generated.js"
      provides: "the snapshot REGENERATED after the food-delivery+rideshare sub-batch (INV-01 byte-stable) so validate:extension reconciles"
    - path: "tests/breadth-search-return.test.js"
      provides: "the per-op breadth assertion EXTENDED to the new apps incl. cross-app order/ride collision probes (wrong-invoke=0)"
      contains: "doordash"
  key_links:
    - from: "scripts/import-opentabs-catalog.mjs enumerateBatchApps"
      to: "vendor/opentabs-snapshot/plugins/{doordash,ubereats,uber,lyft,grubhub,instacart}"
      via: "the frozen enumeration auto-discovers the new dirs (no importer edit); each emits opentabs__<stem>__* descriptors backing:'dom'"
      pattern: "opentabs__(doordash|ubereats|uber|lyft|grubhub|instacart)__"
    - from: "scripts/import-opentabs-catalog.mjs runImport classifyGate"
      to: "extension/config/service-denylist.json (39-01)"
      via: "every food-delivery/rideshare origin classifies sensitive (39-01) -> the merge gate does NOT abort; an unscreened origin WOULD abort (the screen-then-import contract)"
      pattern: "classifyGate"
    - from: "scripts/verify-catalog-crosscheck.mjs checkPaymentOpsNotSafeInvocable"
      to: "the emitted opentabs__<app>__place_order / __checkout / __request_ride descriptors"
      via: "each payment op is backing:'dom' on a sensitive origin -> the guard PASSES (DOM-only, not safe-invocable)"
      pattern: "place_order|checkout|request_ride|backing"
---

<objective>
Import the FIRST commerce sub-batch — food delivery + rideshare (doordash, ubereats, uber, lyft,
grubhub, instacart) — as DATA-ONLY via the FROZEN machinery (NO importer edit), gated on the 39-01
payment screening. These are the canonical payment-bearing apps: each places paid orders / rides, so
each origin was classified SENSITIVE in 39-01 and each payment op must ship DOM-only on that sensitive
origin (the triple mitigation in action). Reuses the FROZEN Phase-37/38 screen-then-import contract +
the per-wave gate re-assertion verbatim:

1. Vendor the 6 apps as hermetic metadata slices (Wall 1: metadata-only, mirror the todoist slice
   structure EXACTLY). Each app's vendored urlPatterns host MUST equal the origin 39-01 classified
   sensitive (the screen-then-import contract: vendored origin == screened origin, the 38-02 threads
   precedent). Emit real z.object inputs reflecting actual params (restaurant/store ids, item ids,
   address, order id) -- NOT empty schemas. Make the payment-op write/destructive signals explicit so
   the crosscheck classes them correctly.
2. Run the FROZEN importer: enumerateBatchApps auto-discovers the dirs; the merge-time classifyGate
   PASSES (all 6 origins classify sensitive via 39-01); every emitted descriptor is backing:'dom'
   (DOM-only); the payment-op CI guard PASSES (every payment op is DOM-only on a sensitive origin).
3. Regenerate the snapshot; validate:extension reconciles. Re-assert the per-wave gates over the grown
   corpus: crosscheck (incl. the payment-op guard) + no-dead-entry + the eval (incl. cross-app order/ride
   collision probes, wrong-invoke=0) + the SW cold-start budget.

Purpose: land the most-obviously-payment-bearing commerce apps DOM-only on sensitive origins, proving
the screen-then-import contract + the triple mitigation hold for the food-delivery/rideshare category.
Output: 6 food-delivery/rideshare apps' descriptors (all backing:'dom' on sensitive origins, payment
ops DOM-only-not-invocable); regenerated snapshot; all per-wave gates green incl. the payment-op guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-breadth-c-commerce-travel-misc-most-sensitive/39-CONTEXT.md
@.planning/phases/39-breadth-c-commerce-travel-misc-most-sensitive/39-01-SUMMARY.md

<interfaces>
<!-- The FROZEN machinery (Phase 37) + the 39-01 screening this plan reuses (do NOT edit either). -->
- scripts/import-opentabs-catalog.mjs: enumerateBatchApps() imports every vendored plugin dir except
  head services (github.com/app.slack.com/www.notion.so), todoist, and /e2e|prescript|-test$/ fixtures.
  A vendored food-delivery/rideshare dir IS enumerated and emits opentabs__<stem>__* descriptors.
  backingFor() returns 'dom' for every data-batch app. runImport() classifyGate-aborts on any
  unclassified batch origin AND feedSeedDescriptors upserts each emitted slug into the eval seed. NO
  importer edit. STEM_OVERRIDES is { jira, confluence, cloudflare, datadog, threads } only -- add an
  entry ONLY if a vendored host's frozen-derived stem (service.replace(/^app\./,'').split('.')[0]) is
  wrong/colliding (see the per-app host notes below); otherwise data-only.
- The frozen host->stem derivation: 'www.doordash.com' -> 'www' is WRONG. Two options (the 38-03 reddit
  precedent): EITHER set urlPatterns host to the form whose split('.')[0] is the brand
  (NOT possible for a www-only host) OR add a STEM_OVERRIDES entry { doordash:'doordash', ... }. For
  these apps the screened origins (39-01) are the www/apex forms, so a STEM_OVERRIDES data-map entry per
  app whose host derives wrong is the canonicalization (a DATA-MAP extension, the 37-01 per-app extension
  point -- INV-01 holds; NOT a logic change). CONFIRM the emitted stem before relying on it; if a host's
  split('.')[0] already yields the brand (e.g. a host like 'uber.com' apex), NO override is needed.
- side-effect verbs (scripts/lib/side-effect-class.mjs, FROZEN): create/place/order/checkout/request/
  add/submit -> WRITE family; cancel/delete -> DESTRUCTIVE; list/get/track/search -> READ. Make each
  payment write op carry an explicit transport signal: `api(..., { method:'POST' })` for a
  place_order/checkout/request_ride; `apiVoid(..., { method:'DELETE' })` for a cancel_order. reads use
  `api(..., { method:'GET' })`. Do NOT edit the shared module.
- 39-01 classified these origins SENSITIVE: doordash.com, ubereats.com, *.uber.com, *.lyft.com,
  grubhub.com, instacart.com (verify each via classify() before vendoring -- the import will ABORT if a
  vendored origin is not the screened one). The payment-op CI guard (39-01) requires every payment op
  (place_order/checkout/request_ride/...) to be backing:'dom' on a sensitive origin -- which the frozen
  default + the 39-01 classification guarantee.
- The vendored-slice contract (replicate todoist EXACTLY): package.json (opentabs.urlPatterns host +
  _role "opentabs-metadata-snapshot" + _note + MIT); src/sdk-stub.ts (VERBATIM copy of todoist's);
  src/<app>-api.ts (inert api/apiVoid throwers + upstream-method comment); src/index.ts (<App>Plugin +
  default/plugin export); src/tools/<op>.ts (defineTool with a real z.object input + a never-run handle
  naming the upstream helper+method).
- breadth-search-return.test.js: widen the corpusFiles regex (~line 77-79) to include the new stems and
  raise the corpus.length floor; add cross-app collision probes (doordash-vs-grubhub-vs-ubereats
  place_order; uber-vs-lyft request_ride) all wrong-invoke=0.
- The eval + cold-start budget: the importer seed-feeds; keep generous gates that still prove the
  schema-on-hit layout; widen the cold-start byte budget ONLY if the grown index legitimately exceeds it
  while per-descriptor footprint stays flat (params NOT indexed), with a comment noting the grown count
  (the 38-02 precedent) -- do NOT change the data layout; the Phase-43 full-corpus gate stays separate.
</interfaces>

# Reuse references (the proven slice + import shapes -- do NOT rebuild):
@vendor/opentabs-snapshot/plugins/todoist/src/index.ts
@vendor/opentabs-snapshot/plugins/todoist/src/sdk-stub.ts
@vendor/opentabs-snapshot/plugins/todoist/src/todoist-api.ts
@vendor/opentabs-snapshot/plugins/todoist/src/tools/create-task.ts
@vendor/opentabs-snapshot/plugins/todoist/src/tools/list-tasks.ts
@vendor/opentabs-snapshot/plugins/todoist/src/tools/delete-task.ts
@vendor/opentabs-snapshot/PIN.md
@tests/breadth-search-return.test.js
@tests/capability-search-eval.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor the food-delivery + rideshare metadata slices (doordash, ubereats, uber, lyft, grubhub, instacart)</name>
  <files>vendor/opentabs-snapshot/plugins/{doordash,ubereats,uber,lyft,grubhub,instacart}/** (each: package.json, src/sdk-stub.ts, src/index.ts, src/<app>-api.ts, src/tools/*.ts), vendor/opentabs-snapshot/PIN.md</files>
  <read_first>
    - vendor/opentabs-snapshot/plugins/todoist/ (the full slice structure to replicate: package.json + sdk-stub.ts + <app>-api.ts + index.ts + tools/*.ts)
    - vendor/opentabs-snapshot/plugins/todoist/src/tools/create-task.ts (the write shape: api {method:'POST'}) + delete-task.ts (the destructive shape: apiVoid {method:'DELETE'}) + list-tasks.ts (the read shape: api default GET)
    - vendor/opentabs-snapshot/plugins/discord/ (a recent multi-op slice with a write + destructive op, the 38-03 precedent)
    - scripts/import-opentabs-catalog.mjs (confirm whether each host needs a STEM_OVERRIDES entry: the frozen split('.')[0] of the vendored host -- 'www.doordash.com'->'www' needs an override; an apex 'uber.com'->'uber' does not)
    - extension/config/service-denylist.json (CONFIRM each vendored origin is classified sensitive by 39-01 -- the vendored urlPatterns host MUST equal the screened origin)
    - vendor/opentabs-snapshot/PIN.md (the SHA + MIT pin + the per-phase append-only log)
  </read_first>
  <action>
    Create the 6 vendored metadata slices mirroring the todoist slice structure EXACTLY (Wall 1:
    metadata-only, NO dist/, NO executed handle(), NO runtime .js). For EACH app: package.json
    (opentabs.urlPatterns host below + _role "opentabs-metadata-snapshot" + _note + MIT), src/sdk-stub.ts
    (VERBATIM copy of todoist's), src/index.ts (<App>Plugin with the ops below), src/<app>-api.ts (inert
    transport stub mirroring todoist-api.ts), src/tools/<op>.ts per op (real z.object input + never-run
    handle naming the upstream helper+method).

    Hosts (authoritative urlPatterns[0]; MUST equal the 39-01 screened sensitive origin -- CONFIRM via
    classify() before vendoring):
      - doordash  -> "*://www.doordash.com/*"   (service www.doordash.com; STEM_OVERRIDE doordash needed)
      - ubereats  -> "*://www.ubereats.com/*"   (service www.ubereats.com; STEM_OVERRIDE ubereats needed)
      - uber      -> "*://*.uber.com/*"         (service uber.com; CONFIRM stem 'uber' -- if the derived
                                                 stem is wrong, add a STEM_OVERRIDE)
      - lyft      -> "*://*.lyft.com/*"         (service lyft.com; CONFIRM stem 'lyft')
      - grubhub   -> "*://www.grubhub.com/*"    (service www.grubhub.com; STEM_OVERRIDE grubhub needed)
      - instacart -> "*://www.instacart.com/*"  (service www.instacart.com; STEM_OVERRIDE instacart needed)
    For any host whose frozen split('.')[0] does NOT yield the brand stem, add a STEM_OVERRIDES entry
    keyed by the vendored DIR name (the 37-01 per-app extension point, a DATA-MAP entry -- the ONLY
    candidate importer touch; the 38-02 threads precedent). CONFIRM the emitted slug is opentabs__<brand>__*
    (NOT opentabs__www__*) before relying on it; if a stem comes out 'www', the override is required --
    add it (this is the documented data-map extension, INV-01 holds). The `service` field keeps the real
    host; only the stem/slug/filename is canonicalized.

    Ops per app (real z.object inputs; at least one PAYMENT-bearing write + reads; key the payment write
    so the crosscheck classes it correctly):
      - doordash: list_restaurants (read), get_restaurant (read), list_orders (read), track_order (read),
        place_order (WRITE -- the payment op; `api(..., { method:'POST' })`), cancel_order (DESTRUCTIVE --
        `apiVoid(..., { method:'DELETE' })`).
      - ubereats: list_restaurants (read), get_menu (read), list_orders (read), place_order (WRITE
        payment op), cancel_order (DESTRUCTIVE).
      - uber: list_ride_options (read), get_ride_estimate (read), list_trips (read), request_ride (WRITE
        -- the payment op; api POST), cancel_ride (DESTRUCTIVE -- apiVoid DELETE).
      - lyft: list_ride_types (read), get_ride_estimate (read), list_rides (read), request_ride (WRITE
        payment op), cancel_ride (DESTRUCTIVE).
      - grubhub: list_restaurants (read), get_restaurant (read), list_orders (read), place_order (WRITE
        payment op), cancel_order (DESTRUCTIVE).
      - instacart: list_stores (read), search_products (read), list_orders (read), checkout (WRITE -- the
        payment op; api POST), cancel_order (DESTRUCTIVE).
    Each payment write op carries an explicit `{ method:'POST' }` literal (so crosscheck classes write on
    BOTH the verb AND method axes); each cancel carries `apiVoid(..., { method:'DELETE' })`; reads use
    `api(..., { method:'GET' })`. Do NOT vendor any e2e/prescript fixture or any DENY-01 / money-movement
    denied app. Do NOT edit the importer (except a STEM_OVERRIDES data-map entry where a host derives
    wrong) or side-effect-class.mjs.

    Append the 6 apps to vendor/opentabs-snapshot/PIN.md's per-phase log as a new Phase-39 row (batch C
    sub-batch 1: food delivery + rideshare; all payment-bearing -> sensitive origins, DOM-only by the
    frozen default).
  </action>
  <verify>
    <automated>node --import tsx -e "const apps=['doordash','ubereats','uber','lyft','grubhub','instacart'];(async()=>{for(const a of apps){const m=await import('./vendor/opentabs-snapshot/plugins/'+a+'/src/index.ts');const p=m.default;if(!p||!Array.isArray(p.tools)||p.tools.length<4){console.error('bad slice',a);process.exit(1)}const host=(p.urlPatterns&&p.urlPatterns[0])||'';const hasPayment=p.tools.some(t=>/place_order|checkout|request_ride/.test(t.name));if(!hasPayment){console.error('no payment op in',a);process.exit(1)}console.log(a,'->',host,p.tools.map(t=>t.name).join(','))}process.exit(0)})().catch(e=>{console.error(e.message);process.exit(1)})" && node -e "const D=require('./extension/utils/service-denylist.js');D.load().then(()=>{const o=['https://www.doordash.com','https://www.ubereats.com','https://www.uber.com','https://www.lyft.com','https://www.grubhub.com','https://www.instacart.com'];let ok=true;for(const x of o){if(D.classify(x).sensitive!==true){console.error('vendored origin NOT sensitive (39-01 must classify it):',x);ok=false}}if(!ok)process.exit(1);console.log('PASS all 6 vendored origins classify sensitive')})"</automated>
  </verify>
  <acceptance_criteria>
    - All 6 apps import under tsx with >=4 ops each incl. at least one payment-bearing write (place_order/checkout/request_ride); real z.object inputs; no handle() executes; provenance-scaffold stays green.
    - Each vendored urlPatterns host equals the 39-01 screened sensitive origin (classify() returns sensitive for each); the emitted stem is the brand (opentabs__doordash__* etc., NOT opentabs__www__*) -- a STEM_OVERRIDES data-map entry added per host that derives wrong.
    - Each payment write op carries {method:'POST'}; each cancel carries apiVoid {method:'DELETE'}; reads are {method:'GET'}.
    - No DENY-01 / money-movement denied app or fixture vendored; side-effect-class.mjs untouched; the only importer touch (if any) is a STEM_OVERRIDES data-map entry.
  </acceptance_criteria>
  <done>The 6 food-delivery/rideshare apps are vendored as hermetic metadata slices with correct hosts/stems (each origin classified sensitive in 39-01), each carrying a payment-bearing write op + reads with explicit transport signals; PIN.md per-phase log updated (Phase-39 batch C sub-batch 1).</done>
</task>

<task type="auto">
  <name>Task 2: Import the sub-batch, regenerate the snapshot, re-assert the per-wave gates (incl. the payment-op guard)</name>
  <files>extension/catalog/recipe-index.generated.js, catalog/descriptors/_fixtures/intent-cases.json, catalog/descriptors/_fixtures/_provenance.json, tests/breadth-search-return.test.js, tests/capability-search-eval.test.js</files>
  <read_first>
    - scripts/import-opentabs-catalog.mjs (the FROZEN enumerateBatchApps + runImport + classifyGate + feedSeedDescriptors -- RUN, do NOT edit beyond a STEM_OVERRIDES data-map entry from Task 1)
    - scripts/package-extension.mjs + scripts/validate-extension.mjs (regen + byte-compare; INV-01 IIFE/djb2)
    - scripts/verify-catalog-crosscheck.mjs (the crosscheck + the NEW payment-op guard from 39-01 -- RUN, do NOT edit)
    - tests/breadth-search-return.test.js (the corpusFiles regex ~line 77-79 + the COLLISION_SET to extend with cross-app order/ride probes)
    - tests/capability-search-eval.test.js (the eval harness + the CGEN-04 cold-start asserts)
    - tests/no-dead-entry.test.js (the generalized opentabs__* invariant -- run, do NOT edit)
    - catalog/descriptors/_fixtures/intent-cases.json (the eval fixtures to grow)
  </read_first>
  <action>
    (a) Run `node --import tsx ./scripts/import-opentabs-catalog.mjs` to emit the 6 apps via the FROZEN
    machinery. Confirm: opentabs__doordash__* / opentabs__ubereats__* / opentabs__uber__* /
    opentabs__lyft__* / opentabs__grubhub__* / opentabs__instacart__* appear with the brand stems; EVERY
    emitted descriptor carries backing:'dom'; the merge-time classifyGate PASSES (all 6 origins sensitive
    via 39-01 -> no abort); the payment-op CI guard PASSES (every payment op is backing:'dom' on a
    sensitive origin); all prior descriptors (the 70 dev + 24 comms/social + the heads) untouched;
    seed-descriptors.json carries the new slugs.

    (b) Run `node scripts/package-extension.mjs` to regenerate extension/catalog/recipe-index.generated.js
    (INV-01 byte-stable IIFE first-line + djb2/dual-export tail; only the DATA literal grows); confirm
    `node scripts/validate-extension.mjs` exits 0.

    (c) Extend catalog/descriptors/_fixtures/intent-cases.json with fixtures for the new apps: a read
    ("track my doordash order" -> doordash.track_order; "list my uber trips" -> uber.list_trips), a
    payment write ("order food on doordash" -> doordash.place_order; "request an uber ride" ->
    uber.request_ride; "checkout my instacart cart" -> instacart.checkout), a destructive ("cancel my
    doordash order" -> doordash.cancel_order), expectedSlug aligned to the emitted real slugs. Extend
    tests/breadth-search-return.test.js: widen the corpusFiles regex to include the new stems, raise the
    corpus.length floor, and add cross-app collision probes to COLLISION_SET (doordash-vs-grubhub-vs-
    ubereats place_order; uber-vs-lyft request_ride) all wrong-invoke=0. Do NOT hand-edit
    seed-descriptors.json (the importer seed-feeds it).

    (d) Re-assert the per-wave gates over the grown corpus: `node scripts/verify-catalog-crosscheck.mjs`
    (exit 0 -- each place_order/checkout/request_ride classes write, each cancel destructive, reads read;
    NO under-statement AND the payment-op guard passes because every payment op is DOM-only on a sensitive
    origin), `node tests/no-dead-entry.test.js` (exit 0 -- every new opentabs__ slug resolves to T3), and
    `node tests/capability-search-eval.test.js` (recall@5>=0.9, wrong-invoke=0.000 incl. the order/ride
    collisions; the cold-start asserts pass at the grown size). Widen the cold-start budget ONLY if the
    grown index legitimately exceeds the gate while the per-descriptor footprint stays flat (params not
    indexed), with a comment noting the grown count (the 38-02 precedent) -- do NOT change the data layout
    and do NOT defer the Phase-43 full-corpus gate. If the cold-start budget is approached, FLAG it in the
    SUMMARY (39-CONTEXT requires flagging if this batch approaches the budget).

    Do NOT edit the importer (beyond Task-1's STEM_OVERRIDES data-map entry), capability-search.js, the
    gates' core logic, or service-denylist.json.
  </action>
  <verify>
    <automated>node --import tsx ./scripts/import-opentabs-catalog.mjs && test -f catalog/descriptors/opentabs__doordash__place_order.json && test -f catalog/descriptors/opentabs__uber__request_ride.json && test -f catalog/descriptors/opentabs__instacart__checkout.json && node -e "const fs=require('fs');for(const s of ['opentabs__doordash__place_order','opentabs__uber__request_ride','opentabs__instacart__checkout','opentabs__grubhub__place_order']){const d=JSON.parse(fs.readFileSync('catalog/descriptors/'+s+'.json','utf8'));if(d.backing!=='dom'){console.error('payment op NOT dom (must be DOM-only on a sensitive origin):',s,d.backing);process.exit(1)}if(d.sideEffectClass!=='write'&&d.sideEffectClass!=='destructive'){console.error('payment op NOT a write/destructive class:',s,d.sideEffectClass);process.exit(1)}}console.log('PASS payment ops are DOM-only writes')" && node scripts/package-extension.mjs && npm run validate:extension && node scripts/verify-catalog-crosscheck.mjs && node tests/no-dead-entry.test.js && node tests/breadth-search-return.test.js && node tests/capability-search-eval.test.js</automated>
  </verify>
  <acceptance_criteria>
    - The 6 apps' descriptors are emitted (opentabs__<brand>__*) and inlined; EVERY one carries backing:'dom'; the merge-time classifyGate did NOT abort; no prior descriptor clobbered.
    - validate:extension exits 0 (snapshot reconciled; classification gate + crosscheck + recipe-path guard green); INV-01 byte-stable.
    - verify-catalog-crosscheck.mjs exits 0: each payment op classes write/destructive AND the payment-op guard PASSES (every payment op is backing:'dom' on a sensitive origin -- not safe-and-API-invocable).
    - no-dead-entry.test.js exits 0 over the grown corpus; breadth-search-return.test.js passes incl. the cross-app order/ride collisions (wrong-invoke=0).
    - capability-search-eval.test.js prints recall@5>=0.9, wrong-invoke=0.000, cold-start asserts green at the grown size (flagged in the SUMMARY if approaching the budget).
  </acceptance_criteria>
  <done>The 6 food-delivery/rideshare apps are imported as DOM-only data via the frozen machinery (gate passed; every payment op DOM-only on a sensitive origin per the payment-op guard), the snapshot regenerated (INV-01 byte-stable), and the per-wave gates re-asserted over the grown corpus: crosscheck (incl. payment-op guard) + no-dead-entry + eval green and the cold-start budget held.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OpenTabs vendored metadata -> FSB descriptor corpus (build-time) | the food-delivery/rideshare metadata read by the importer; a forbidden field, a denied app, or an unscreened payment origin must never enter the corpus |
| a payment-bearing op -> the side-effect/backing crosscheck (build-time) | a place_order/checkout/request_ride op must class write/destructive AND stay backing:'dom' on a sensitive origin -- never safe-and-API-invocable |
| screened sensitive payment origin -> invoke consent gate (runtime) | each app's payment WRITE flows through the posture-B re-gate (the end-to-end proof on a REAL emitted descriptor is Plan 39-07) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | Elevation of Privilege | a payment op (place_order/checkout/request_ride) writable/invocable under Auto = money moved without consent | mitigate | every payment op ships backing:'dom' (the frozen default -> DOM-only, not API-invocable, asserted in Task 2); the payment-op CI guard (39-01) passes only because each is DOM-only on a sensitive origin; each origin classified sensitive in 39-01 so its DOM-path write is posture-B gated |
| T-39-02 | Elevation of Privilege | a payment origin landing unscreened -> its write runs under Auto with no re-gate | mitigate | the merge-time classifyGate aborts on any unclassified origin; Task 1 confirms each vendored origin == the 39-01 screened sensitive origin (classify() asserted sensitive before vendoring); a mismatch aborts the import (the screen-then-import contract) |
| T-39-04 | Spoofing | a payment op surfaced as a confident API-invocable hit (a fabricated checkout call from guessed auth) | mitigate | the frozen backing:'dom' default makes every payment op DOM-only / discovery-pending (tests/dom-only-default.test.js + backing-status-annotation.test.js, re-asserted); the payment-op guard forbids any payment op being backing recipe/handler; never auto-mint a recipe |
| T-39-06 | Tampering | a vendored slice emitting a forbidden field name (Wall 1) OR a payment op under-stated as read | mitigate | the importer's recursive forbidden-field pre-scan (UNCHANGED) throws before emit; recipe-path-guard green via validate:extension; verify-catalog-crosscheck.mjs fails the build on under-statement (place_order/request_ride class write/destructive via verb + {method:'POST'/'DELETE'}) |
| T-39-03 | Tampering | a DENY-01 / money-movement denied app vendored into this sub-batch | mitigate | only the 6 screened-sensitive apps vendored (Task 1); no deniedOrigins app vendored; the merge gate would refuse a denied origin's descriptor |
| T-39-SC | Tampering | npm/pip/cargo installs | accept | no new packages this plan (data-only vendoring + test edits; zod/tsx already devDeps); package-legitimacy gate N/A |
</threat_model>

<verification>
- `node --import tsx ./scripts/import-opentabs-catalog.mjs` emits the 6 apps (all backing:'dom'); the gate did NOT abort; the payment-op guard passes; seed-descriptors.json seed-fed.
- `npm run validate:extension` exits 0 (snapshot reconciled; classification gate + crosscheck + recipe-path guard green over the grown corpus).
- `node scripts/verify-catalog-crosscheck.mjs && node tests/no-dead-entry.test.js && node tests/breadth-search-return.test.js && node tests/capability-search-eval.test.js` pass (payment ops class write/destructive + DOM-only-on-sensitive; every new slug resolves T3; order/ride collisions wrong-invoke=0; recall@5>=0.9; cold-start within budget).
</verification>

<success_criteria>
- BRDTH-01: the 6 food-delivery/rideshare apps' descriptors are imported via the frozen machinery + returned by search with synonyms + side-effect class + backing:'dom'.
- BRDTH-02: the merge-time classifyGate passed only because 39-01 screened the origins sensitive; the payment-op CI guard passed because every payment op is DOM-only on a sensitive origin.
- BRDTH-03: every emitted descriptor is DOM-only (backing:'dom', invocable=false, discovery-pending), never confident-invocable, never learn-seeded; no payment op armed via recipe/handler.
- No regression: no prior descriptor clobbered; crosscheck (incl. payment-op guard) + no-dead-entry + the eval (recall@5>=0.9, wrong-invoke=0, cold-start within budget) green; validate:extension exits 0.
</success_criteria>

<output>
Create `.planning/phases/39-breadth-c-commerce-travel-misc-most-sensitive/39-02-SUMMARY.md` when done.
</output>
