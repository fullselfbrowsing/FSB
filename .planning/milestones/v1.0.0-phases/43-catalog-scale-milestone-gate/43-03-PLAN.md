---
phase: 43-catalog-scale-milestone-gate
plan: 03
type: execute
wave: 3
depends_on: [43-01]
files_modified:
  - extension/utils/relearn-scheduler.js
  - extension/utils/learned-recipe-store.js
  - extension/background.js
autonomous: true
requirements: [SCALE-02]

must_haves:
  truths:
    - "extension/utils/relearn-scheduler.js exists and exports FsbRelearnScheduler / scheduleRelearn(origin, fn, opts): N rot events for ONE origin within the coalescing window invoke the supplied re-learn fn EXACTLY ONCE (no thundering-herd of CDP attaches); two DISTINCT origins each get their own single re-learn (keyed by origin) -- turning tests/relearn-coalescing.test.js GREEN"
    - "The scheduler applies exponential back-off keyed by origin: after a re-learn fn that resolves ok:false (a failed/denied attempt), the NEXT scheduled re-learn for that origin is deferred by a growing delay (base, 2x, 4x ... capped); the schedule is computed against an INJECTABLE now()/clock so the test asserts it synchronously (no wall-clock sleep, runs < 5s)"
    - "The scheduler state is BOUNDED (a cap on tracked origins + a ceiling on the back-off delay) so it can never grow unbounded -- mirroring the PER_ORIGIN_CAP discipline"
    - "The scheduler is CONSENT-PRESERVING: it INVOKES the supplied re-learn fn (the consent-gated runDiscovery) and never re-implements capture/consent; an ok:false (e.g. RECIPE_CONSENT_*) result is treated as a failed attempt for back-off and the scheduler captures NOTHING itself"
    - "A per-(origin,slug) recurrence counter (added to learned-recipe-store.js as ADDITIVE exports recordRot(origin, slug)/dispositionFor(origin, slug), or a bounded sibling ring) classifies a first/below-threshold broken verdict TRANSIENT and a >= threshold repeat SYSTEMIC; a non-broken (ok) outcome RESETS the counter; the store is bounded; a typed RECIPE_* security passthrough is NEVER counted -- turning tests/rot-recurrence-classify.test.js GREEN"
    - "An app-level degraded/needs-re-port status accessor (added to learned-recipe-store.js as an ADDITIVE getOriginHealth(origin) method) reports healthy for a live origin and degraded:true / 'needs-re-port' for an origin whose learned recipes are all quarantined (or whose recurrence crossed systemic), without breaking getLearnedSync/quarantine/promote -- turning tests/app-degraded-surfacing.test.js GREEN"
    - "NO change to the consent gate (_evaluateConsent/_runGate), executeBoundSpec, tier dispatch, the classifyRecipeBroken taxonomy, or the per-origin cap/LRU/quarantine: the scheduler + recurrence + surfacing are ADDITIVE, bounded, consent-gated, and fail-safe"
    - "extension/background.js loads relearn-scheduler.js via importScripts AFTER discovery-session.js (additive line, independently try/catch'd); the recipe-path guard stays green (relearn-scheduler.js is NOT a capability-*.js name and does not bind/execute recipes, so it is off the recipe-path allowlist by construction)"
  artifacts:
    - path: "extension/utils/relearn-scheduler.js"
      provides: "The per-origin re-learn coalescing/back-off scheduler (FsbRelearnScheduler/scheduleRelearn) -- additive, bounded, consent-preserving, dynamic-code-free, dual-export IIFE"
      min_lines: 90
      contains: "scheduleRelearn"
    - path: "extension/utils/learned-recipe-store.js"
      provides: "Additive recordRot/dispositionFor recurrence counter + getOriginHealth degraded accessor -- the existing envelope/cap/LRU/quarantine exports UNCHANGED"
      contains: "getOriginHealth"
    - path: "extension/background.js"
      provides: "An additive importScripts('utils/relearn-scheduler.js') after discovery-session.js"
      contains: "relearn-scheduler.js"
  key_links:
    - from: "tests/relearn-coalescing.test.js"
      to: "extension/utils/relearn-scheduler.js"
      via: "require()/global FsbRelearnScheduler.scheduleRelearn the test pins"
      pattern: "scheduleRelearn"
    - from: "tests/rot-recurrence-classify.test.js"
      to: "extension/utils/learned-recipe-store.js"
      via: "the additive recordRot/dispositionFor exports atop the unchanged classifyRecipeBroken verdict"
      pattern: "dispositionFor"
    - from: "tests/app-degraded-surfacing.test.js"
      to: "extension/utils/learned-recipe-store.js"
      via: "the additive getOriginHealth(origin) accessor"
      pattern: "getOriginHealth"
---

<objective>
THE NET-NEW SELF-HEAL IMPLEMENTATION (SCALE-02). Turn the three RED contracts authored
in 43-01 GREEN by building the per-origin re-learn coalescing/back-off scheduler, the
per-(origin,slug) recurrence-based systemic-vs-transient counter, and the app-level
degraded/needs-re-port status accessor -- ALL additive, bounded, consent-gated, and
fail-safe atop the SHIPPED Phase-32 self-heal substrate.

The thundering-herd this fixes: capability-router.js `_quarantineAndRelearn` (line 194)
fires `discovery.runDiscovery(origin, { tabId })` FIRE-AND-FORGET on EVERY broken
verdict. At 119-app scale, one vendor changing site-wide rots N recipes on one origin and
triggers N concurrent CDP attaches. The scheduler COALESCES those N into ONE consent-gated
re-learn per origin, with exponential back-off on repeated failure.

The recurrence layer sits ON TOP of the UNCHANGED `classifyRecipeBroken` verdict (it
consumes the verdict, never replaces it): a one-off 4xx/5xx is TRANSIENT (retry), repeated
rot on the same op is SYSTEMIC (the site changed -> escalate to quarantine + surface). The
degraded accessor makes a stale/quarantined origin VISIBLE ("this app needs re-learning")
instead of silently failing.

Purpose: harden recipe-rot self-heal for the now-119-app surface -- the SCALE-02 deliverable
that prevents a 119-app thundering-herd, distinguishes a blip from a site-wide change, and
surfaces a degraded app.
Output: the new scheduler module + additive recurrence/degraded exports on the learned store
+ the SW importScripts wiring; the three 43-01 RED tests GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-catalog-scale-milestone-gate/43-CONTEXT.md
@.planning/phases/43-catalog-scale-milestone-gate/43-VALIDATION.md

# The three RED contracts this plan turns GREEN (author's intent is the acceptance shape).
@tests/relearn-coalescing.test.js
@tests/rot-recurrence-classify.test.js
@tests/app-degraded-surfacing.test.js

# The substrate this builds ON -- read these for the EXACT shapes the additions must respect.
@extension/utils/learned-recipe-store.js
@extension/utils/capability-rot-detector.js
@extension/utils/capability-router.js

<interfaces>
<!-- The real collaborator shapes. The scheduler + recurrence + surfacing implement
     AGAINST these and must NOT change any of them. Extracted from the codebase. -->

From extension/utils/learned-recipe-store.js (the envelope + cap/LRU/quarantine -- UNCHANGED;
the recurrence counter + getOriginHealth are ADDITIVE alongside these):
  module.exports / global.FsbLearnedRecipeStore = {
    STORAGE_KEY: 'fsbLearnedRecipes', PAYLOAD_VERSION: 1, PER_ORIGIN_CAP: 24,
    readAll() -> Promise<{ v, recipes: { [origin]: { [slug]: { recipe, descriptor,
                  capturedAt, lastSuccessAt, successCount, quarantined } } } }>,
    getLearned(slug, origin) -> Promise<{recipe,descriptor}|null>,
    getLearnedSync(slug, origin) -> {recipe,descriptor}|null,   // null when quarantined
    hydrateSyncCache() -> Promise<void>,
    promote(origin, recipe, descriptor, opts) -> Promise<void>,
    quarantine(slug, origin) -> Promise<void>,   // FLAGS quarantined:true, never deletes
    _reset() -> Promise<void>   // test hook -- drops the sync mirror + persisted envelope
  }
  // The in-memory sync mirror _syncMirror[origin][slug] = { recipe, descriptor, quarantined }
  // is the synchronous source getLearnedSync + getOriginHealth read. Null-proto at both
  // levels (a __proto__ origin/slug survives as own data). Keep that discipline for any
  // new per-origin map the recurrence counter adds.

From extension/utils/capability-rot-detector.js (UNCHANGED this phase -- recurrence layers
ON TOP of its verdict, never edits the taxonomy):
  classifyRecipeBroken(result, recipe) -> { broken: boolean, code: string|null, reason: string }
    // broken:true codes: RECIPE_EXPIRED, RECIPE_HTTP_4XX, RECIPE_HTTP_5XX
    // broken:false: RECIPE_LOGGED_OUT (surfaced) | null (ok / typed RECIPE_* passthrough)

From extension/utils/capability-router.js (the rot hook the scheduler debounces -- this plan
does NOT edit the router; it only PUBLISHES the scheduler the router/future work calls):
  _quarantineAndRelearn(slug, tierLabel, origin, tabId)  // module-internal; calls
    discovery.runDiscovery(origin, { tabId }) FIRE-AND-FORGET on EVERY broken verdict.
    // The coalescing scheduler is the debounce LAYER this re-learn flows through. The router
    // edit to ROUTE through the scheduler is intentionally NOT in this plan's scope (it would
    // touch the router's classify hook). This plan ships the scheduler + its tests GREEN; the
    // router-side adoption (if 43-01's contract requires it for runtime wiring) is a follow-up
    // confirmed by the milestone gate. CONFIRM during execution whether relearn-coalescing.test.js
    // requires the router to call the scheduler (it should require only the scheduler module's
    // own coalescing behavior, per 43-01's interface note) -- do NOT widen scope into the router
    // unless the RED test pins it.

From extension/utils/discovery-session.js (the consent-gated re-learn fn -- UNCHANGED; the
scheduler INVOKES it, never replaces it):
  runDiscovery(origin, opts) -> Promise<{ ok:true, ... } | { ok:false, reason:'RECIPE_CONSENT_*' }>
    // the consent gate (_runGate) runs INSIDE network-capture.startSession before any debugger
    // attach -- a denied/default-OFF/sensitive origin re-learns NOTHING. The scheduler treats an
    // ok:false as a failed attempt for back-off and captures NOTHING itself.

From extension/background.js (the SW importScripts ordering -- the new module loads AFTER
discovery-session.js at ~line 243; recurrence/degraded are on the already-loaded learned store):
  line 242: importScripts('utils/learned-recipe-store.js')
  line 243: importScripts('utils/discovery-session.js')   // <- relearn-scheduler.js goes AFTER this
  lines 250-255: the hydrateSyncCache() startup block (the additive-startup precedent to mirror)

From scripts/verify-recipe-path-guard.mjs (Check 4 -- the constraint on the module NAME):
  RECIPE_PATH_ALLOWLIST is the EXACTLY-six-plus-explicit list; Check 4 globs
  extension/utils/capability-*.js from disk and FAILS CLOSED on any capability-* module NOT on
  the allowlist. relearn-scheduler.js is deliberately NOT named capability-* (it does not bind or
  execute a recipe -- it only schedules a consent-gated fn call), so it is OFF the recipe path by
  construction and needs NO allowlist entry. Keep it dynamic-code-FREE anyway (ASCII-only, no
  eval/new Function/dynamic import even in comments) to match the family discipline.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create extension/utils/relearn-scheduler.js (per-origin coalescing + exponential back-off + bounded + consent-preserving) -> relearn-coalescing.test.js GREEN</name>
  <files>extension/utils/relearn-scheduler.js</files>
  <behavior>
    - COALESCING: scheduleRelearn(origin, fn, opts) called N times for ONE origin within the
      coalescing window invokes fn EXACTLY ONCE; two distinct origins each invoke their own fn once.
    - BACK-OFF: after an fn that resolves ok:false, the next scheduled re-learn for that origin is
      deferred by an exponentially growing delay (base, 2x, 4x ...) capped at a ceiling; asserted
      synchronously via the injectable now()/clock + flush hook in opts (no real setTimeout race).
    - BOUNDED: the tracked-origin map is capped and the back-off delay has a ceiling -- state can
      never grow unbounded.
    - CONSENT-PRESERVED: the scheduler calls the supplied fn (the consent-gated runDiscovery) and
      re-implements neither capture nor consent; an ok:false result counts as a failed attempt for
      back-off and the scheduler captures nothing itself.
  </behavior>
  <action>
Create extension/utils/relearn-scheduler.js as a dual-export IIFE mirroring the module shell
of extension/utils/learned-recipe-store.js (the `(function(global){ 'use strict'; ... })
(typeof globalThis !== 'undefined' ? globalThis : this)` wrapper publishing
global.FsbRelearnScheduler AND module.exports). ASCII-only, NO emojis, ZERO dynamic-code
constructs even in comments (so the recipe-path guard's comment scan stays clean even though
this module is off the allowlist).

Read tests/relearn-coalescing.test.js FIRST and implement EXACTLY the seam it pins: the export
name (FsbRelearnScheduler), the entry point signature scheduleRelearn(origin, fn, opts), and the
injectable test seam (a now()/clock + a deterministic flush hook passed via opts). Do NOT invent a
different shape -- the RED test is the contract.

Implement scheduleRelearn(origin, fn, opts):
  - COALESCING: keep a per-origin record { pending, timer, attempt, nextEligibleAt }. When
    scheduleRelearn fires for an origin that already has a pending/in-window entry, COALESCE (do
    NOT schedule a second fn invocation) -- N calls within the coalescing window collapse to ONE fn
    invocation. Keying is by origin string. A distinct origin gets its own independent record.
  - WINDOW + CLOCK: use opts.now (a function returning ms) when provided, else Date.now; use
    opts.setTimer/opts.clearTimer (injectable) when provided, else setTimeout/clearTimeout. Default
    coalescing window: a small constant (e.g. COALESCE_WINDOW_MS = 2000) -- documented + exported as
    a named constant so the test can reference it. Provide a flush(origin?) (or opts.flush) seam the
    test calls to deterministically run the due re-learn WITHOUT a wall-clock wait.
  - BACK-OFF: track per-origin `attempt`. On an fn that resolves ok:false (or rejects), increment
    attempt and compute the next eligible time as now + min(BASE_BACKOFF_MS * 2^(attempt-1),
    MAX_BACKOFF_MS). A scheduleRelearn that arrives before nextEligibleAt is deferred (re-armed for
    nextEligibleAt), NOT run immediately. On an fn that resolves ok:true, RESET attempt to 0 (the
    origin recovered). Export BASE_BACKOFF_MS + MAX_BACKOFF_MS as named constants.
  - BOUNDED: cap the tracked-origin map (e.g. MAX_TRACKED_ORIGINS, mirroring PER_ORIGIN_CAP=24
    discipline -- pick a sane ceiling and evict the least-recently-touched origin when exceeded) and
    cap the back-off at MAX_BACKOFF_MS. Document both as the bound the test asserts.
  - CONSENT-PRESERVING + FAIL-SAFE: the scheduler ONLY calls fn(origin) (the caller passes the
    consent-gated runDiscovery bound to the origin); it never touches network-capture/consent
    itself. Wrap the fn call so a throw/rejection is swallowed into a failed-attempt (best-effort,
    never poisons the scheduler) -- mirror the fire-and-forget posture of capability-router.js
    `_quarantineAndRelearn`. Treat ok:false EXACTLY like a failed attempt for back-off.
  - Provide a _reset() test hook (clear the tracked-origin map) mirroring the store's _reset.

Do NOT edit capability-router.js in this task. Do NOT touch network-capture/discovery-session/the
consent gate. The scheduler is a standalone additive module + its test.
  </action>
  <verify>
    <automated>node tests/relearn-coalescing.test.js && grep -q "scheduleRelearn" extension/utils/relearn-scheduler.js && grep -q "module.exports" extension/utils/relearn-scheduler.js && ! grep -nE "eval\(|new Function|importScripts" extension/utils/relearn-scheduler.js && echo "SCHEDULER-GREEN+CLEAN"</automated>
  </verify>
  <done>relearn-scheduler.js exists with the FsbRelearnScheduler/scheduleRelearn seam the RED test pins; tests/relearn-coalescing.test.js exits 0 (N->1 coalescing, exponential back-off, bounded state, consent-preserving all asserted); the module is dynamic-code-free; the router/consent gate are untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the additive recurrence counter + getOriginHealth degraded accessor to learned-recipe-store.js -> rot-recurrence-classify.test.js + app-degraded-surfacing.test.js GREEN</name>
  <files>extension/utils/learned-recipe-store.js</files>
  <behavior>
    - recordRot(origin, slug) increments a per-(origin,slug) recurrence count; dispositionFor(origin,
      slug) returns TRANSIENT below the threshold and SYSTEMIC at/above it (a documented constant,
      e.g. RECURRENCE_SYSTEMIC_THRESHOLD = 3).
    - A non-broken (ok) outcome RESETS the counter for that (origin,slug) (recordOk/resetRot or the
      shape the RED test pins).
    - The recurrence store is BOUNDED (a cap / sibling ring), never unbounded.
    - A typed RECIPE_* security passthrough is NEVER counted as a rot (the caller passes only
      broken:true verdicts; the store must also defend if asked to count a non-rot -- mirror the
      capability-rot-detector security-passthrough guard).
    - getOriginHealth(origin) reports { degraded:false } / healthy for a live origin and
      { degraded:true, status:'needs-re-port', origin } for an origin whose learned recipes are all
      quarantined (or whose recurrence crossed systemic).
    - getLearnedSync / quarantine / promote / readAll shapes are UNCHANGED.
  </behavior>
  <action>
Read tests/rot-recurrence-classify.test.js and tests/app-degraded-surfacing.test.js FIRST and
implement EXACTLY the export names + semantics they pin (the RED tests are the contract): the
recurrence pair (e.g. recordRot(origin, slug) + dispositionFor(origin, slug), plus the
reset-on-success entry the test calls) and the degraded accessor (getOriginHealth(origin), or the
exact name app-degraded-surfacing.test.js requires). If a test pins a DIFFERENT accessor name (e.g.
a standalone FsbAppDegradedStatus), follow the test -- but PREFER the additive learned-store method
since that is where the quarantine state already lives.

Add to learned-recipe-store.js as PURELY ADDITIVE code (the existing envelope/cap/LRU/quarantine/
getLearnedSync/promote/_reset stay byte-identical except for the new exports appended to exportsObj
and the _reset extension noted below):

(1) A bounded per-(origin,slug) recurrence counter. Use a module-scope null-proto map
    _recurrence[origin][slug] = count (Object.create(null) at both levels, mirroring the
    _syncMirror discipline so a __proto__ origin/slug survives as own data). Bound it: cap the
    tracked (origin) entries (reuse PER_ORIGIN_CAP or a sibling RECURRENCE_CAP) and evict the
    least-recently-touched origin when exceeded -- never unbounded.
      - recordRot(origin, slug): increment _recurrence[origin][slug] (string-typed guards; no-op on
        non-string args). Define RECURRENCE_SYSTEMIC_THRESHOLD (e.g. 3) as a named exported const.
      - dispositionFor(origin, slug): return 'systemic' when the count >= the threshold, else
        'transient' (and 'transient' for an unseen pair / count 0).
      - the reset-on-success entry the test pins (e.g. recordOk(origin, slug) or resetRot): delete /
        zero _recurrence[origin][slug] so a recovered op stops trending systemic.
      - NEVER count a non-rot: recordRot is the documented "broken-only" entry; if the test feeds a
        verdict, only a broken:true verdict increments (a RECIPE_* typed passthrough / ok must not).
        Mirror the capability-rot-detector.test.js T-32-PASS guard the RED test references.

(2) getOriginHealth(origin) -- the ADDITIVE degraded/needs-re-port accessor. Read the SYNCHRONOUS
    _syncMirror for the origin (the same source getLearnedSync uses): an origin with at least one
    live, non-quarantined learned recipe is healthy ({ degraded:false, status:'ok', origin }); an
    origin whose learned recipes ALL carry quarantined:true (or whose recurrence crossed systemic
    for its slugs) is degraded ({ degraded:true, status:'needs-re-port', origin }). An unknown /
    un-hydrated origin returns a defined healthy-ish default (degraded:false) -- never throws. Match
    the exact field names the RED test asserts.

(3) Extend _reset() to ALSO clear _recurrence (so a fresh test starts empty), mirroring how it drops
    _syncMirror. Append recordRot / dispositionFor / the reset entry / getOriginHealth /
    RECURRENCE_SYSTEMIC_THRESHOLD to exportsObj. Keep the module dynamic-code-free (it is ON the
    recipe-path allowlist -- the guard scans its comments).

Do NOT modify the persisted envelope SHAPE (recurrence is in-memory + bounded, like _syncMirror; it
does not need to survive a SW restart -- a fresh start re-accumulates, the conservative direction).
Do NOT change getLearnedSync / quarantine / promote / readAll behavior. Do NOT touch
capability-rot-detector.js -- recurrence consumes its verdict, never edits the taxonomy.
  </action>
  <verify>
    <automated>node tests/rot-recurrence-classify.test.js && node tests/app-degraded-surfacing.test.js && grep -q "getOriginHealth" extension/utils/learned-recipe-store.js && grep -q "dispositionFor" extension/utils/learned-recipe-store.js && node tests/learned-recipe-store.test.js && node tests/learned-t2-outranking.test.js && node tests/seed-resolve-t2.test.js && echo "RECURRENCE+DEGRADED-GREEN+STORE-UNREGRESSED"</automated>
  </verify>
  <done>rot-recurrence-classify.test.js + app-degraded-surfacing.test.js exit 0; recordRot/dispositionFor/the reset entry + getOriginHealth are additive exports; the recurrence store is bounded + null-proto; the existing learned-store/t2-outranking/seed-resolve tests still pass (no regression to the cap/LRU/quarantine/getLearnedSync contract).</done>
</task>

<task type="auto">
  <name>Task 3: Wire relearn-scheduler.js into the SW via importScripts after discovery-session.js (additive) + confirm validate:extension</name>
  <files>extension/background.js</files>
  <action>
Add ONE additive importScripts line to extension/background.js immediately AFTER line 243
(`importScripts('utils/discovery-session.js')`) and BEFORE the hydrateSyncCache startup block
(line ~250). Mirror the existing pattern EXACTLY:
  try { importScripts('utils/relearn-scheduler.js'); } catch (e) { console.error('[FSB] Failed to load relearn-scheduler.js:', e.message); }
Add a one-line comment above it noting it is the SCALE-02 per-origin re-learn coalescing/back-off
scheduler, loaded after the discovery stack it debounces, additive (no manifest/permission change).

Use the Edit tool -- insert the single line + comment surgically; do NOT rewrite the importScripts
block or touch any other line. The recurrence counter + getOriginHealth need NO new importScripts
(they are additive exports on the already-loaded learned-recipe-store.js at line 242).

Then run the structural gate battery to confirm the additive module + the learned-store additions
did not break a guard:
  npm run validate:extension
This runs recipe-path-guard (Check 4 globs capability-*.js -- relearn-scheduler.js is NOT
capability-* so it is not required on the allowlist; the learned store is already on it and stays
dynamic-code-free), classification-gate, catalog-crosscheck, no-duplicate-stem, origin-
classification, and no-orphan-descriptor. All must exit 0.

NOTE: this plan does NOT package the extension (no scripts/package-extension.mjs run needed -- no
catalog/descriptor change). The new module ships into the packaged SW the next time
package-extension.mjs runs (43-04's milestone gate confirms the whole suite + validate). If the
dev-tree SW path resolves importScripts directly, the wiring is live immediately.
  </action>
  <verify>
    <automated>grep -q "importScripts('utils/relearn-scheduler.js')" extension/background.js && npm run validate:extension && echo "WIRED+VALIDATE-GREEN"</automated>
  </verify>
  <done>extension/background.js loads relearn-scheduler.js via an additive try/catch'd importScripts after discovery-session.js; npm run validate:extension exits 0 (all structural guards green over the additive module + learned-store additions); no manifest/permission change.</done>
</task>

</tasks>

<verification>
- node tests/relearn-coalescing.test.js exits 0 (coalescing N->1 + exponential back-off + bounded +
  consent-preserving).
- node tests/rot-recurrence-classify.test.js exits 0 (transient/systemic threshold + reset-on-success
  + bounded + security-passthrough never-counted).
- node tests/app-degraded-surfacing.test.js exits 0 (healthy vs degraded/needs-re-port, visible-not-silent,
  additive-non-breaking).
- node tests/learned-recipe-store.test.js + node tests/learned-t2-outranking.test.js +
  node tests/seed-resolve-t2.test.js exit 0 (the store's cap/LRU/quarantine/getLearnedSync contract is
  unregressed by the additive recurrence + getOriginHealth).
- node tests/capability-rot-detector.test.js exits 0 (the classifyRecipeBroken taxonomy is UNCHANGED).
- node tests/capability-router.test.js + node tests/consent-chokepoint.test.js +
  node tests/consent-mutation-gate.test.js exit 0 (the consent gate / executeBoundSpec / tier dispatch
  are untouched).
- npm run validate:extension exits 0 (recipe-path-guard / classification-gate / crosscheck / no-dup-stem /
  origin-classification / no-orphan over the additive module + the learned-store additions).
- git diff confirms relearn-scheduler.js is NET-NEW and learned-recipe-store.js changes are ADDITIVE
  (new exports + _recurrence + getOriginHealth + the _reset extension), not edits to the envelope/cap/
  LRU/quarantine logic.
</verification>

<success_criteria>
- The per-origin re-learn coalescing/back-off scheduler exists, is bounded + consent-preserving + fail-
  safe, and turns relearn-coalescing.test.js GREEN.
- The recurrence-based systemic-vs-transient counter + the app-level degraded/needs-re-port accessor are
  ADDITIVE on the learned store and turn rot-recurrence-classify.test.js + app-degraded-surfacing.test.js
  GREEN.
- The consent gate, executeBoundSpec, tier dispatch, the classifyRecipeBroken taxonomy, and the per-origin
  cap/LRU/quarantine are UNCHANGED; the scheduler is wired additively into the SW; validate:extension green.
- This wave delivers SC2 BECAUSE the three self-heal hardening behaviors (per-origin coalescing/back-off,
  recurrence systemic-vs-transient, degraded surfacing) are now IMPLEMENTED + CI-asserted GREEN atop the
  unchanged shipped substrate -- the 119-app thundering-herd is debounced, a blip is distinguished from a
  site-wide change, and a stale app is visible instead of silently failing.
</success_criteria>

<output>
Create `.planning/phases/43-catalog-scale-milestone-gate/43-03-SUMMARY.md` when done.
Record: the scheduler's coalescing window + back-off curve (base/factor/ceiling) + the tracked-origin
bound; the recurrence systemic threshold + where the counter lives (in-memory bounded null-proto map) +
its reset-on-success + the never-count-security guard; the getOriginHealth seam (the exact field shape) +
the origin it keys; confirmation the consent gate / executeBoundSpec / tier dispatch / classifyRecipeBroken
/ cap-LRU-quarantine were UNTOUCHED; the importScripts position; whether relearn-coalescing.test.js
required any router-side adoption (it should not) and what was wired if so.
</output>
