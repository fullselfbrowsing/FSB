---
phase: 43-catalog-scale-milestone-gate
plan: 04
type: execute
wave: 4
depends_on: [43-02, 43-03]
files_modified:
  - .planning/phases/43-catalog-scale-milestone-gate/43-MILESTONE-GATE.md
autonomous: true
requirements: [SCALE-01, SCALE-02]

must_haves:
  truths:
    - "INV-03 7-provider byte-equality holds: tests/provider-parity.test.js exits 0 with the typed fallback reason distinct.length===1 across xai/openai/anthropic/gemini/openrouter/lmstudio/custom"
    - "MIT provenance is complete: tests/provenance-scaffold.test.js exits 0 -- the catalog _provenance.json covers all apps MIT + SHA-pinned, vendor PIN.md embeds the verbatim MIT grant + disclaimer, no gap, Wall-1 no-runtime-js holds"
    - "The SCALE-01 scale gate holds: tests/full-corpus-scale.test.js exits 0 (serialized index < 2MB, loadJSON+first-search < 100ms, < 700B/descriptor, > 2000 descriptors)"
    - "The SCALE-01 precision HARD bar holds: tests/capability-search-eval.test.js exits 0 with the full-corpus wrong-invoke as a HARD assertion at the achieved bar (target 0) + recall@5 >= 0.9 HARD; tests/breadth-search-return.test.js exits 0 with curated collision wrong-invoke=0 HARD"
    - "The SCALE-02 self-heal tests are GREEN: tests/relearn-coalescing.test.js + tests/rot-recurrence-classify.test.js + tests/app-degraded-surfacing.test.js all exit 0"
    - "INV-01..04 + Walls 1/2 guards are all green: npm run validate:extension exits 0 (recipe-path-guard / classification-gate / catalog-crosscheck / no-duplicate-stem / origin-classification / no-orphan-descriptor); and the in-suite guards (capability-mcp-surface, capability-autopilot-parity, no-dead-entry, head-handler-cap, catalog-inline-shape, payment-op-guard, guarded-write-failclosed) pass"
    - "THE MILESTONE GATE: full `npm test` EXITS 0 over the whole catalog + all guards (breadth + depth + discovery + scale + self-heal + provider parity + provenance)"
    - "The carried-forward NON-blocking human_needed UAT is RECORDED for the milestone audit: 41-HUMAN-UAT.md (guarded-write [ASSUMED-ENDPOINT] live mutation-body capture) + 42-HUMAN-UAT.md (live first-authenticated-visit discovery capture per seeded origin) are surfaced as milestone debt that does NOT gate Phase 43 (the surfaces ship fail-closed/inert)"
  artifacts:
    - path: ".planning/phases/43-catalog-scale-milestone-gate/43-MILESTONE-GATE.md"
      provides: "The milestone-gate sign-off record: every gate's command + measured result (scale numbers, wrong-invoke bar, provider distinct, provenance app count, npm test EXIT 0) + the carried-forward NON-blocking UAT debt list (with optional-live-confirmation steps) for the milestone audit"
      min_lines: 40
  key_links:
    - from: ".planning/phases/43-catalog-scale-milestone-gate/43-MILESTONE-GATE.md"
      to: "the full npm test result"
      via: "the recorded EXIT 0 + each sub-gate's measured number"
      pattern: "npm test"
    - from: "43-MILESTONE-GATE.md"
      to: "the carried-forward UAT debt"
      via: "the recorded 41-HUMAN-UAT.md + 42-HUMAN-UAT.md NON-blocking entries"
      pattern: "HUMAN-UAT"
---

<objective>
THE MILESTONE GATE (the final close of v1.0.0). This plan is mostly a verification/sign-off
battery: it asserts that the WHOLE milestone is green -- the SCALE-01 scale + precision HARD
bars, the SCALE-02 self-heal tests, INV-03 7-provider byte-equality, MIT provenance, INV-01..04 +
Walls 1/2 guards -- and that full `npm test` EXITS 0. That npm-test-EXIT-0 over the complete
catalog IS the milestone gate (the v0.9.99 Phase-32 milestone-gate posture).

It also RECORDS the carried-forward NON-blocking human_needed UAT (41-HUMAN-UAT.md guarded-write
mutation bodies + 42-HUMAN-UAT.md discovery first-visit) as milestone debt for the milestone
audit -- these do NOT gate Phase 43 (the surfaces ship fail-closed/inert). The plan is FULLY
AUTONOMOUS: following the Phase-42 milestone-gate precedent (42-05), the carried-forward live UAT
is recorded as a DOCUMENT (debt with optional live-confirmation steps), not a blocking checkpoint --
consistent with 43-VALIDATION.md ("no NEW human_needed item; carried-forward UAT is non-blocking
debt") and the autonomous-run posture.

Purpose: prove the v1.0.0 milestone is met -- everything from breadth + depth + discovery + scale +
self-heal is green and the full suite passes, with the live-UAT debt honestly surfaced.
Output: the 43-MILESTONE-GATE.md sign-off record (every gate command + measured result + the
carried-forward NON-blocking UAT debt + its optional live-confirmation steps).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-catalog-scale-milestone-gate/43-CONTEXT.md
@.planning/phases/43-catalog-scale-milestone-gate/43-VALIDATION.md

# The two upstream deliverables this gate confirms landed.
@.planning/phases/43-catalog-scale-milestone-gate/43-02-PLAN.md
@.planning/phases/43-catalog-scale-milestone-gate/43-03-PLAN.md

# The carried-forward NON-blocking UAT debt to record (NOT to execute -- the surfaces are inert).
@.planning/phases/41-depth-2-remaining-hand-ports-guarded-writes/41-HUMAN-UAT.md
@.planning/phases/42-discovery-seeding-tail-learn/42-HUMAN-UAT.md

<gate_map>
<!-- Every milestone gate, its exact command, and the bar it must clear. The full
     `npm test` chain (package.json) already contains EVERY one of these test files;
     running each directly first gives a fast, attributable signal before the whole-suite run. -->

INV-03 (7-provider byte-equal):   node tests/provider-parity.test.js
  -> distinct.length === 1 across xai/openai/anthropic/gemini/openrouter/lmstudio/custom.

MIT provenance:                   node tests/provenance-scaffold.test.js
  -> catalog _provenance.json { sha:pinned, license:'MIT', apps:[...] } (apps.length > 0);
     vendor PIN.md verbatim MIT grant + 'AS IS' disclaimer; Wall-1 no-runtime-js.

SCALE-01 scale gate:              node tests/full-corpus-scale.test.js
  -> serialized index < 2MB, loadJSON + first-search < 100ms, < 700B/descriptor, > 2000 descriptors.

SCALE-01 precision HARD bar:      node tests/capability-search-eval.test.js   (43-02 flipped it HARD)
  + node tests/breadth-search-return.test.js
  -> full-corpus wrong-invoke HARD at the achieved bar (target 0) + recall@5 >= 0.9 HARD;
     curated collision wrong-invoke = 0 HARD.

SCALE-02 self-heal (43-03):       node tests/relearn-coalescing.test.js
  + node tests/rot-recurrence-classify.test.js + node tests/app-degraded-surfacing.test.js
  -> all exit 0 (coalescing/back-off, recurrence systemic-vs-transient, degraded surfacing).

INV-01..04 + Walls 1/2 guards:    npm run validate:extension
  (validate-extension + recipe-path-guard + classification-gate + catalog-crosscheck +
   no-duplicate-stem + origin-classification + no-orphan-descriptor)
  + the in-suite guards: node tests/capability-mcp-surface.test.js (MCP surface / INV-01),
    node tests/capability-autopilot-parity.test.js (autopilot parity / INV-02),
    node tests/no-dead-entry.test.js, node tests/head-handler-cap.test.js,
    node tests/catalog-inline-shape.test.js, node tests/payment-op-guard.test.js,
    node tests/guarded-write-failclosed.test.js.

THE MILESTONE GATE:               npm test
  -> EXIT 0 over the WHOLE suite (every gate above is a member of the chain; the whole-suite
     pass is the authoritative milestone signal).
</gate_map>

<carried_forward_uat>
<!-- NON-blocking milestone debt. RECORD, do NOT execute. The surfaces ship fail-closed/inert. -->
41-HUMAN-UAT.md (phase 41, plans 02/04, status: human_needed): Guarded-Write [ASSUMED-ENDPOINT]
  live mutation-body capture -- the guarded-write bodies need a live authenticated mutation to
  confirm the captured request shape. The write path ships FAIL-CLOSED until confirmed, so it does
  not gate the milestone.
42-HUMAN-UAT.md (phase 42, plan 05, status: human_needed): Live First-Authenticated-Visit Capture
  per Seeded Origin -- the discovery seed's first-visit capture needs a live signed-in visit per
  seeded origin. The seeded T2 path surfaces an actionable RECIPE_LEARN_PENDING affordance (inert)
  until learned, so it does not gate the milestone.
Both are recorded in the milestone audit as carried-forward v2/post-milestone debt (the deferred-
ideas posture in 43-CONTEXT.md), consistent with the v0.9.99 Phase-26..34 live-UAT carry-forward.
</carried_forward_uat>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run every milestone sub-gate directly + confirm the SCALE/self-heal/INV-03/provenance bars, then run the full `npm test` (THE milestone gate)</name>
  <files>(verification only -- no source file modified in this task)</files>
  <action>
Run each sub-gate DIRECTLY first (fast, attributable), capturing each measured result, THEN run the
whole suite. Do NOT modify any source -- this task PROVES the milestone is green; if any gate is
RED, STOP and report which one (it means an upstream plan, 43-02 or 43-03, did not fully land --
that is a gap to close before the milestone, not something to patch here).

Sub-gates (record each number/result):
  node tests/provider-parity.test.js            # INV-03: distinct.length===1 across 7 providers
  node tests/provenance-scaffold.test.js        # MIT provenance: app count, SHA, PIN.md, no-runtime-js
  node tests/full-corpus-scale.test.js          # SCALE-01 scale: record index MB / cold-start ms / B-per-descriptor / count
  node tests/capability-search-eval.test.js     # SCALE-01 precision: record wrong-invoke (HARD bar) + recall@5
  node tests/breadth-search-return.test.js      # curated collision wrong-invoke=0 HARD
  node tests/relearn-coalescing.test.js         # SCALE-02 coalescing/back-off
  node tests/rot-recurrence-classify.test.js    # SCALE-02 recurrence systemic-vs-transient
  node tests/app-degraded-surfacing.test.js     # SCALE-02 degraded surfacing
  node tests/capability-mcp-surface.test.js     # INV-01 MCP surface
  node tests/capability-autopilot-parity.test.js# INV-02 autopilot parity
  npm run validate:extension                    # INV-01..04 + Walls 1/2 structural guards

THE MILESTONE GATE -- the whole suite:
  npm test
Record the EXIT code. It MUST be 0. The whole-suite pass over breadth + depth + discovery + scale +
self-heal + provider parity + provenance + every guard IS the v1.0.0 milestone gate.

If `npm test` exits non-zero: capture the FIRST failing test + its output, STOP, and report it as a
milestone-blocking gap (the fix belongs in the responsible plan, not in this sign-off plan). Do NOT
edit tests to make them pass.
  </action>
  <verify>
    <automated>node tests/provider-parity.test.js && node tests/provenance-scaffold.test.js && node tests/full-corpus-scale.test.js && node tests/capability-search-eval.test.js && node tests/breadth-search-return.test.js && node tests/relearn-coalescing.test.js && node tests/rot-recurrence-classify.test.js && node tests/app-degraded-surfacing.test.js && npm run validate:extension && npm test && echo "MILESTONE-GATE-GREEN-NPM-TEST-EXIT-0"</automated>
  </verify>
  <done>Every sub-gate exits 0 with its measured result captured (scale numbers, wrong-invoke HARD bar, recall@5, provider distinct===1, provenance app count); npm run validate:extension exits 0; full npm test EXITS 0 -- the milestone gate is green.</done>
</task>

<task type="auto">
  <name>Task 2: Write 43-MILESTONE-GATE.md -- the sign-off record (every gate + measured result + carried-forward NON-blocking UAT debt + its optional live-confirmation steps)</name>
  <files>.planning/phases/43-catalog-scale-milestone-gate/43-MILESTONE-GATE.md</files>
  <action>
Create .planning/phases/43-catalog-scale-milestone-gate/43-MILESTONE-GATE.md recording the
milestone sign-off from Task 1's measured results. Use the Write tool. Include:

  - A header: "Phase 43 / v1.0.0 Milestone Gate -- Sign-Off" + the date.
  - A GATE RESULTS table: one row per gate (INV-03 provider parity, MIT provenance, SCALE-01 scale,
    SCALE-01 precision, SCALE-02 coalescing, SCALE-02 recurrence, SCALE-02 degraded, INV-01 MCP
    surface, INV-02 autopilot parity, validate:extension structural guards, full npm test) with
    columns: Gate | Command | Bar | Measured Result | PASS/FAIL. Fill Measured Result from Task 1
    (e.g. scale: "1.40MB / 11ms / 621B / 2318 descriptors"; precision: "wrong-invoke=<X> (HARD bar),
    recall@5=<Y>"; provider: "distinct=[<CODE>], length=1"; provenance: "<N> apps MIT + SHA-pinned";
    npm test: "EXIT 0").
  - THE MILESTONE GATE line: full `npm test` EXIT 0 -- the authoritative v1.0.0 milestone signal.
  - A CARRIED-FORWARD NON-BLOCKING UAT DEBT section listing the two human_needed items verbatim from
    <carried_forward_uat> (41-HUMAN-UAT.md guarded-write mutation bodies; 42-HUMAN-UAT.md discovery
    first-visit), each marked NON-blocking with its fail-closed/inert rationale + a pointer to the
    source file. State explicitly these do NOT gate Phase 43 and are recorded for the milestone audit
    (the post-milestone audit -> complete -> cleanup lifecycle). Include an OPTIONAL LIVE-CONFIRMATION
    sub-section (so the milestone audit / a later human can retire the debt early WITHOUT blocking
    this phase):
      1. Guarded-write mutation bodies (41-HUMAN-UAT.md): with a signed-in session on a guarded-write
         origin, trigger one guarded mutation and confirm the captured request shape matches the
         [ASSUMED-ENDPOINT] recipe (the write path is FAIL-CLOSED until confirmed -- confirming it does
         not change the milestone result).
      2. Discovery first-visit (42-HUMAN-UAT.md): visit a seeded origin while signed in and confirm the
         first-authenticated-visit capture learns the capability (until then the T2 path surfaces the
         inert RECIPE_LEARN_PENDING affordance).
    State that skipping these is the DEFAULT for the autonomous run: the milestone stands green and the
    two items remain recorded debt.
  - A short ATTESTATION: INV-01..04 + Walls 1/2 green; no consent-gate / executeBoundSpec / tier-
    dispatch / classifyRecipeBroken / cap-LRU-quarantine change (the self-heal was additive); no
    manifest/permission change; no index-shape/params-leak regression.

ASCII-only, no emojis. This file is the milestone audit's input.
  </action>
  <verify>
    <automated>test -f .planning/phases/43-catalog-scale-milestone-gate/43-MILESTONE-GATE.md && grep -q "npm test" .planning/phases/43-catalog-scale-milestone-gate/43-MILESTONE-GATE.md && grep -q "EXIT 0" .planning/phases/43-catalog-scale-milestone-gate/43-MILESTONE-GATE.md && grep -q "HUMAN-UAT" .planning/phases/43-catalog-scale-milestone-gate/43-MILESTONE-GATE.md && grep -qE "41-HUMAN-UAT|42-HUMAN-UAT" .planning/phases/43-catalog-scale-milestone-gate/43-MILESTONE-GATE.md && echo "MILESTONE-RECORD-WRITTEN"</automated>
  </verify>
  <done>43-MILESTONE-GATE.md exists with the gate-results table (every gate + measured result), the npm-test-EXIT-0 milestone line, the two carried-forward NON-blocking UAT debt entries (with source-file pointers + optional live-confirmation steps), and the additive-only/no-regression attestation.</done>
</task>

</tasks>

<verification>
- node tests/provider-parity.test.js exits 0 (INV-03 distinct.length===1, 7 providers).
- node tests/provenance-scaffold.test.js exits 0 (MIT + SHA + PIN.md + no-runtime-js).
- node tests/full-corpus-scale.test.js exits 0 (scale budget held).
- node tests/capability-search-eval.test.js exits 0 (wrong-invoke HARD bar + recall@5 >= 0.9 HARD).
- node tests/breadth-search-return.test.js exits 0 (curated collision wrong-invoke=0 HARD).
- node tests/relearn-coalescing.test.js + rot-recurrence-classify.test.js + app-degraded-surfacing.test.js
  exit 0 (SCALE-02 self-heal).
- npm run validate:extension exits 0 (INV-01..04 + Walls 1/2 structural guards).
- npm test EXITS 0 (THE MILESTONE GATE -- the whole suite).
- 43-MILESTONE-GATE.md records every gate + measured result + the carried-forward NON-blocking UAT debt.
</verification>

<success_criteria>
- Every milestone sub-gate is green with its measured result captured.
- Full `npm test` EXITS 0 -- the v1.0.0 milestone gate.
- INV-03 7-provider byte-equality + MIT provenance + INV-01..04 + Walls 1/2 all green.
- 43-MILESTONE-GATE.md is the sign-off record with the gate table + the carried-forward NON-blocking UAT
  debt for the milestone audit.
- This wave delivers SC3 (and confirms SC1 + SC2) BECAUSE it ASSERTS the whole milestone is green -- the
  typed fallback reason is byte-equal across all 7 providers, MIT provenance is complete, full npm test
  EXITS 0, and INV-01..04 + Walls 1/2 guards all pass -- with the live-UAT debt honestly recorded as
  NON-blocking carry-forward. The v1.0.0 milestone is met.
</success_criteria>

<output>
Create `.planning/phases/43-catalog-scale-milestone-gate/43-04-SUMMARY.md` when done.
Record: each gate's measured result (scale numbers, wrong-invoke HARD bar + recall@5, provider distinct,
provenance app count, npm test EXIT code); confirmation the full npm test exited 0; the path to
43-MILESTONE-GATE.md; and the two carried-forward NON-blocking UAT items + their fail-closed rationale +
that they were recorded (not executed) for the milestone audit.
</output>
