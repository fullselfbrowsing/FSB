---
phase: 39.5-full-opentabs-source-import
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vendor/opentabs-snapshot/plugins/
  - vendor/opentabs-snapshot/PIN.md
  - vendor/opentabs-snapshot/_provenance.json
  - tests/vendor-augment-preserved.test.js
autonomous: true
requirements: [BRDTH-01]
must_haves:
  truths:
    - "All 117 real OpenTabs plugin dirs (119 tarball plugins minus the 2 CI fixtures) are vendored under vendor/opentabs-snapshot/plugins/ with REAL src/** (no sdk-stub.ts)"
    - "The 13 hand-authored-only apps (amazon/etsy/eventbrite/grubhub/kayak/lyft/mastodon/opentable/shopify/stubhub/threads/ticketmaster/ubereats) remain vendored with their hand-authored src untouched (augment, not replace)"
    - "The hand-authored grafana slice (grafana.com origin) is preserved; the real empty-origin grafana is NOT vendored over it"
    - "PIN.md and _provenance.json record the pinned SHA and the augment policy (13 hand-only marked no-upstream@SHA)"
  artifacts:
    - path: "vendor/opentabs-snapshot/plugins/airtable/src/index.ts"
      provides: "A re-vendored real plugin src (no sdk-stub) -- the overlap-regenerated case"
      contains: "@opentabs-dev/plugin-sdk"
    - path: "tests/vendor-augment-preserved.test.js"
      provides: "Wave-0 augment guard: asserts the 13 hand-only dirs + grafana survive and 117 real dirs are present"
      min_lines: 40
  key_links:
    - from: "vendor/opentabs-snapshot/plugins/<real-app>/src/index.ts"
      to: "@opentabs-dev/plugin-sdk"
      via: "direct import (no stub) -- resolves from node_modules under tsx"
      pattern: "from ['\"]@opentabs-dev/plugin-sdk['\"]"
---

<objective>
Fetch the pinned-SHA OpenTabs repo tarball and vendor all 117 real plugin metadata slices (real `src/**`, NO sdk-stub) into `vendor/opentabs-snapshot/plugins/`, AUGMENTING -- not replacing -- the existing corpus: the 40 overlapping apps are re-vendored from real source, 77 net-new real apps are added, and the 13 hand-authored-only apps (no upstream at the SHA) plus the hand-authored grafana slice are PRESERVED untouched. Update the vendor PIN.md + _provenance.json. Add a Wave-0 augment guard test that asserts the preserved set survives.

Purpose: This is the foundation of true OpenTabs parity. The frozen importer (Plan 04) enumerates the vendored dir; everything downstream depends on the correct 117-real + 13-hand-only vendored set existing first. The spike proved all 117 import cleanly from exactly this tarball with the real SDK resolving under tsx (no stub).
Output: ~130 vendored plugin dirs (117 real + 13 hand-only, grafana hand-authored preserved), updated PIN.md + _provenance.json, and tests/vendor-augment-preserved.test.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39.5-full-opentabs-source-import/39.5-CONTEXT.md
@.planning/phases/39-breadth-c-commerce-travel-misc-most-sensitive/FULL-PARITY-SPIKE.md

<read_first>
- .planning/phases/39-breadth-c-commerce-travel-misc-most-sensitive/FULL-PARITY-SPIKE.md (Section 5.1 fetch+vendor mechanism; Section 5.2 SDK/schemas dependency; Section 5.5 replace-vs-augment -- the 13 hand-only preserve list)
- .planning/phases/39.5-full-opentabs-source-import/39.5-CONTEXT.md (<decisions> "Fetch + vendor" and "AUGMENT, not replace")
- vendor/opentabs-snapshot/PIN.md (the pin file format: frontmatter SHA + license + per-phase log; mirrors LATTICE-PIN.md)
- vendor/opentabs-snapshot/_provenance.json (the apps[] provenance the importer fills; the scaffold to extend)
- vendor/opentabs-snapshot/plugins/airtable/ (an EXISTING hand-authored slice: package.json + src/index.ts + src/airtable-api.ts + src/sdk-stub.ts + src/tools/*.ts -- the layout the real vendor replaces, dropping sdk-stub.ts)
- tests/provenance-scaffold.test.js (the EXISTING Wall-1 vendor-dir test -- asserts the snapshot dir contains no runtime .js; the analog for the new augment guard)
</read_first>

<interfaces>
<!-- The vendored slice layout the frozen importer (scripts/import-opentabs-catalog.mjs) expects. -->
<!-- readPluginMeta(app) reads: vendor/opentabs-snapshot/plugins/<app>/package.json -> pkg.opentabs.urlPatterns[0] -->
<!-- extractDescriptors(app) imports: vendor/opentabs-snapshot/plugins/<app>/src/index.ts (default export plugin with tools[]) -->
<!-- extractTransportSignals reads: vendor/opentabs-snapshot/plugins/<app>/src/tools/<op-kebab>.ts as TEXT -->
<!-- The real plugin src/tools/*.ts import siblings: '../<app>-api.js' (the real api file, NOT a stub) and './schemas.js' (sibling shared zod) -->

The pinned tarball:
  gh api repos/opentabs-dev/opentabs/tarball/4b17021637d2cac12b8d84d21c40e765aa7b85e9 > repo.tar.gz
  tar -xzf repo.tar.gz   ->   opentabs-dev-opentabs-<short>/plugins/<app>/{package.json, src/**}

The 2 CI fixtures to EXCLUDE (the importer's FIXTURE_DIR_RE already skips these at enumerate time, but do NOT vendor them either): the e2e-test and prescript-test plugin dirs.

The 13 hand-authored-only apps (NO upstream at the SHA) to PRESERVE untouched:
  amazon, etsy, eventbrite, grubhub, kayak, lyft, mastodon, opentable, shopify, stubhub, threads, ticketmaster, ubereats

The 2 real apps to NOT vendor (Plan 02 carves the skip; here just do not overwrite the hand-authored grafana):
  grafana (real has empty urlPatterns -> the hand-authored grafana.com slice is PRESERVED instead), sqlpad (no hand-authored slice -> simply not vendored)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fetch the pinned-SHA tarball and vendor the 117 real plugin slices (augment-preserving)</name>
  <files>vendor/opentabs-snapshot/plugins/</files>
  <read_first>
    - FULL-PARITY-SPIKE.md Section 5.1 (the exact tarball fetch + per-plugin copy of package.json + src/** only)
    - FULL-PARITY-SPIKE.md Section 5.5 (AUGMENT: the 13 hand-only preserve list; the 40 overlap regenerate; 77 net-new)
    - vendor/opentabs-snapshot/plugins/airtable/ (the existing slice layout to replace for the 40 overlaps -- real src drops sdk-stub.ts)
  </read_first>
  <action>
    Fetch the pinned tarball with `gh api repos/opentabs-dev/opentabs/tarball/4b17021637d2cac12b8d84d21c40e765aa7b85e9 > /tmp/opentabs-39_5.tar.gz` and extract to a scratch dir under /tmp (NOT inside the repo). For EACH non-fixture plugin dir in the extracted `plugins/`, vendor ONLY `package.json` + the full `src/**` tree (index.ts, the `<app>-api.ts` real transport file, and `tools/*.ts` incl `schemas.ts`) into `vendor/opentabs-snapshot/plugins/<app>/`. Vendor the REAL src -- do NOT synthesize an sdk-stub.ts or an `<app>-api` stub (per the spike, the real `@opentabs-dev/plugin-sdk@0.0.113` + `@opentabs-dev/shared` resolve cleanly under tsx from node_modules; both are already installed devDeps). Drop everything else from each upstream dir (dist/, tests, build artifacts, icons beyond src). EXCLUDE the 2 CI fixture dirs (the e2e-test and prescript-test plugins -- matched by the importer's FIXTURE_DIR_RE `(^|[-_])(e2e|prescript)([-_]|$)|-test$`). Net 117 real plugin dirs vendored. AUGMENT discipline (do NOT replace wholesale): (a) for the 40 apps that overlap by name with the existing hand-authored slices, the real src REPLACES the hand-authored stub-slice (real op sets are the source of truth -- richer/more accurate); (b) the 13 hand-authored-only apps (amazon, etsy, eventbrite, grubhub, kayak, lyft, mastodon, opentable, shopify, stubhub, threads, ticketmaster, ubereats) have NO upstream at the SHA -- their existing hand-authored dirs MUST remain byte-untouched; (c) the real `grafana` (empty urlPatterns) MUST NOT be vendored over the existing hand-authored grafana.com slice -- skip vendoring real grafana so the hand-authored grafana stays; (d) do NOT vendor sqlpad (no hand-authored slice -- it is simply absent). Operationalize the preserve-set as an explicit skip-list so the copy loop never writes into the 13 hand-only dirs or the grafana dir. Remove the /tmp scratch tarball + extraction when done (keep the repo `git status` clean of scratch). NO change to any importer/gate source in this task.
  </action>
  <verify>
    <automated>test $(ls -d vendor/opentabs-snapshot/plugins/*/ | wc -l | tr -d ' ') -ge 130 && for a in amazon etsy eventbrite grubhub kayak lyft mastodon opentable shopify stubhub threads ticketmaster ubereats grafana; do test -d "vendor/opentabs-snapshot/plugins/$a" || { echo "MISSING preserved $a"; exit 1; }; done && grep -rl "@opentabs-dev/plugin-sdk" vendor/opentabs-snapshot/plugins/airtable/src/index.ts >/dev/null && ! test -f vendor/opentabs-snapshot/plugins/airtable/src/sdk-stub.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - 117 real plugin dirs vendored with real src and no sdk-stub (the airtable overlap shows `@opentabs-dev/plugin-sdk` imported directly in src/index.ts and NO src/sdk-stub.ts)
    - the 13 hand-only dirs + the hand-authored grafana dir are all present and byte-untouched (preserved)
    - the 2 CI fixtures (e2e-test, prescript-test) are NOT vendored; sqlpad is NOT vendored; real grafana did NOT overwrite the hand-authored grafana
    - total vendored dirs >= 130 (117 real overlapping+net-new + 13 hand-only; grafana counted in both the real-skip and the preserve)
    - no /tmp scratch artifacts remain inside the repo (`git status` clean of scratch files)
  </acceptance_criteria>
  <done>vendor/opentabs-snapshot/plugins/ holds the 117 real slices (no stub) + the 13 hand-only + grafana preserved, fixtures+sqlpad excluded.</done>
</task>

<task type="auto">
  <name>Task 2: Update PIN.md + _provenance.json for the full-corpus augment</name>
  <files>vendor/opentabs-snapshot/PIN.md, vendor/opentabs-snapshot/_provenance.json</files>
  <read_first>
    - vendor/opentabs-snapshot/PIN.md (the frontmatter SHA/license + the per-phase log section to append a Phase-39.5 entry to)
    - vendor/opentabs-snapshot/_provenance.json (the apps[] array shape -- the importer's fillProvenance() upserts real apps; the hand-only apps need a no-upstream marker)
    - scripts/import-opentabs-catalog.mjs fillProvenance() (lines ~773-797 -- the per-app provenance entry shape: {app, service, source, sha, license, sourcePath, descriptors})
  </read_first>
  <action>
    Append a Phase-39.5 entry to the PIN.md per-phase log documenting the full real-source import at SHA 4b17021637d2cac12b8d84d21c40e765aa7b85e9: 117 real plugins vendored (real src, no sdk-stub), the 13 hand-authored-only apps preserved as "hand-authored, no upstream@SHA", grafana/sqlpad real apps skipped (hand-authored grafana preserved). Keep the existing frontmatter SHA/license/ref unchanged (the SHA is already the pinned one). In _provenance.json, ensure the 13 hand-only apps carry an explicit `source: "hand-authored"` (or equivalent no-upstream marker) entry distinct from the `source: "opentabs"` real apps, so provenance is auditable -- the importer's fillProvenance() will upsert the real opentabs apps at Plan 04 run time, but the 13 hand-only markers must be recorded here so they are not mistaken for real-source. Do NOT pre-populate the real-app descriptor lists (Plan 04's import fills those); record only the augment policy + the hand-only markers. ASCII-only, no emojis.
  </action>
  <verify>
    <automated>grep -q "39.5" vendor/opentabs-snapshot/PIN.md && grep -q "4b17021637d2cac12b8d84d21c40e765aa7b85e9" vendor/opentabs-snapshot/PIN.md && node -e "const p=require('./vendor/opentabs-snapshot/_provenance.json'); const apps=p.apps||[]; const hand=apps.filter(a=>a&&/hand/i.test(String(a.source||''))); if(hand.length<13){console.error('hand-only markers:',hand.length);process.exit(1);} console.log('OK hand-only markers',hand.length)"</automated>
  </verify>
  <acceptance_criteria>
    - PIN.md has a Phase-39.5 log entry naming the SHA, the 117-real vendor, the 13-hand-only preserve, and the grafana/sqlpad skip
    - PIN.md frontmatter SHA/license stays the pinned 4b17021637d2cac12b8d84d21c40e765aa7b85e9 / MIT
    - _provenance.json records all 13 hand-only apps with a no-upstream (hand-authored) source marker distinct from `opentabs`
    - real-app descriptor lists are NOT pre-populated (Plan 04 fills them at import time)
  </acceptance_criteria>
  <done>PIN.md + _provenance.json document the augment: pinned SHA, 117 real vendored, 13 hand-only preserved as no-upstream.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wave-0 augment guard test (the 13 hand-only + grafana survive; 117 real present)</name>
  <files>tests/vendor-augment-preserved.test.js</files>
  <behavior>
    - Test 1: vendor/opentabs-snapshot/plugins/ contains >= 130 plugin dirs (117 real + 13 hand-only)
    - Test 2: each of the 13 hand-only apps (amazon..ubereats) is a present dir with a package.json (preserved)
    - Test 3: the hand-authored grafana dir is present (real empty-origin grafana did not replace it)
    - Test 4: a sampled overlap app (airtable) src/index.ts imports @opentabs-dev/plugin-sdk directly AND has NO src/sdk-stub.ts (real src, augment regenerated)
    - Test 5: the e2e-test and prescript-test fixture dirs are NOT vendored; sqlpad is NOT vendored
  </behavior>
  <read_first>
    - tests/provenance-scaffold.test.js (the EXISTING vendor-dir test convention: REPO_ROOT, fs.readdirSync, check(cond,msg) counter, process.exit(failed>0?1:0), ASCII-only)
    - FULL-PARITY-SPIKE.md Section 5.5 (the augment guarantee this test enforces)
  </read_first>
  <action>
    Write tests/vendor-augment-preserved.test.js as a zero-framework FSB node test (module-level passed/failed counters, synchronous check(cond,msg), process.exit(failed>0?1:0), ASCII-only, no emojis) enforcing the augment guarantee: (1) >= 130 vendored plugin dirs; (2) each of the 13 hand-only apps is a present dir with a package.json; (3) the grafana dir is present; (4) the airtable overlap src/index.ts imports `@opentabs-dev/plugin-sdk` and has no src/sdk-stub.ts; (5) neither e2e-test nor prescript-test nor sqlpad is a vendored dir. This is the Wave-0 augment guard the phase validation requires (a regression that drops a hand-only app -- a wholesale-replace mistake -- FAILS here). Register the test in package.json's `test` script chain by appending ` && node tests/vendor-augment-preserved.test.js` after the existing catalog tests (after tests/payment-op-guard.test.js).
  </action>
  <verify>
    <automated>node tests/vendor-augment-preserved.test.js && grep -q "vendor-augment-preserved.test.js" package.json</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/vendor-augment-preserved.test.js` exits 0 over the vendored set
    - the test FAILS if any of the 13 hand-only dirs or grafana is removed (the augment regression guard is real)
    - the test is registered in the package.json `test` chain
  </acceptance_criteria>
  <done>tests/vendor-augment-preserved.test.js asserts the 13 hand-only + grafana survive and 117 real slices are present; registered in npm test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream tarball -> vendored src | untrusted third-party source enters the repo; only metadata (package.json + src) is vendored, never shipped to the browser (Wall 1) |
| vendored real plugin -> the build importer | the importer reads metadata only (never executes handle()); the real SDK resolves under tsx but its DOM/fetch code is never run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39.5-01 | Tampering | wholesale vendor swap drops the 13 hand-only payment/commerce apps | mitigate | AUGMENT-preserve skip-list in Task 1 + the Wave-0 augment guard test (Task 3) FAILS if any hand-only app or grafana is dropped |
| T-39.5-02 | Tampering | a real plugin's runtime (dist/ / handle()) gets vendored and could ship | mitigate | Task 1 vendors ONLY package.json + src/** (no dist/); Wall 1 holds (importer is build-time, executes no handle()); the existing recipe-path-guard scans shipped files |
| T-39.5-SC | Tampering | the upstream tarball SHA could drift from the pin | mitigate | the tarball is fetched at the EXACT pinned SHA 4b17021637d2cac12b8d84d21c40e765aa7b85e9; PIN.md frontmatter SHA is unchanged + the per-phase log records it |
</threat_model>

<verification>
- `node tests/vendor-augment-preserved.test.js` passes (the augment guard)
- `ls -d vendor/opentabs-snapshot/plugins/*/ | wc -l` >= 130
- PIN.md + _provenance.json carry the Phase-39.5 augment record
- No importer/gate source changed in this plan (Plan 02 owns the machinery)
- `git status` clean of /tmp scratch
</verification>

<success_criteria>
117 real plugin slices vendored (real src, no sdk-stub) + the 13 hand-only + grafana preserved; PIN.md/_provenance.json updated; the Wave-0 augment guard test passes and is registered. SC1's vendor half is satisfied (the import itself is Plan 04).
</success_criteria>

<output>
Create `.planning/phases/39.5-full-opentabs-source-import/39.5-01-SUMMARY.md` when done.
</output>
