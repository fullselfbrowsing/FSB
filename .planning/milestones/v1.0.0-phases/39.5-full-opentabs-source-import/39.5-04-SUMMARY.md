---
phase: 39.5-full-opentabs-source-import
plan: 04
subsystem: catalog
tags: [opentabs, descriptor-import, side-effect-classification, service-denylist, read-only-safe-invariant, payment-op-guard, posture-b]

# Dependency graph
requires:
  - phase: 39.5-01
    provides: "re-vendored full 117-real-plugin OpenTabs source + the augment-preserved 13 hand-only slices (vendor-augment-preserved gate)"
  - phase: 39.5-02
    provides: "STEM_OVERRIDES (collision-free filenames) + the no-duplicate-stem CI gate + the target.apply_promo_code carve + the sqlpad/empty-origin skip"
  - phase: 39.5-03
    provides: "full-corpus origin re-screening + the COMMERCE_SENSITIVE_SERVICES build backstop + linkedin/youtube classification"
provides:
  - "The FULL real-source descriptor corpus: 2,383 opentabs__*.json (the 40 overlaps regenerated from real src, the 13 hand-only preserved -- augment) + the regenerated recipe-index.generated.js (2,391 descriptors, INV-01 IIFE/djb2 intact)"
  - "reddit + calendly reclassified SENSITIVE (the real plugins ship write ops) -- the gate-prescribed conservative fix (discord/linkedin posture); reads under Auto, writes posture-B mutating-gated"
  - "DEF-39.5-03-A resolved: classifyGate CI sweep screens each origin on host + canonical slug only (drops op-prose) -- the full-corpus importer exits 0"
  - "Every full-scale gate green: classifyGate, crosscheck (READ_ONLY_SAFE + payment-op guard + COMMERCE backstop), no-dead-entry, no-duplicate-stem, recipe-path-guard"
affects: [40-depth-1, 41-depth-2, 42-discovery-seeding, 43-catalog-scale]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "READ_ONLY_SAFE invariant as a self-healing build gate: when the real reddit/calendly src added writes, the gate FAILED THE BUILD (its job) -> the prescribed fix is to reclassify SENSITIVE + remove from READ_ONLY_SAFE, never to weaken the gate"
    - "Origin screening keys on host + canonical slug ONLY (op free-text prose is not an origin signal) -- DEF-39.5-03-A, prevents benign dev/infra prose tokens (billing/budget/signal/health/tax) from false-tripping a correctly-safe origin at full-corpus scale"

key-files:
  created:
    - ".planning/phases/39.5-full-opentabs-source-import/39.5-04-SUMMARY.md"
  modified:
    - "catalog/descriptors/opentabs__*.json (2,383 -- the full real-source emit; 40 overlaps regenerated, 13 hand-only preserved)"
    - "extension/catalog/recipe-index.generated.js (regenerated; 2,391 descriptors; INV-01 intact)"
    - "extension/config/service-denylist.json (reddit *.reddit.com + calendly calendly.com -> sensitiveOrigins)"
    - "scripts/verify-catalog-crosscheck.mjs (reddit/calendly removed from READ_ONLY_SAFE_SERVICES; +zillow.com apex; comments updated)"
    - "scripts/verify-classification-gate.mjs (DEF-39.5-03-A: per-origin screen on host+slug, drops op-prose)"
    - "scripts/import-opentabs-catalog.mjs (DEF-39.5-03-A staged fix)"
    - "catalog/descriptors/_fixtures/safe-origin-write.fixture.json (re-pointed reddit -> grafana write)"
    - "tests/catalog-crosscheck.test.js (blocks h/i re-pointed reddit -> grafana)"
    - "tests/coverage-report.test.js (realAppCount floor > 50 -> >= 110 for the ~130-app corpus)"
    - "tests/sensitive-write-import-gate.test.js (doordash read-op host: www -> real apex doordash.com, host-agnostic)"
    - "tests/breadth-search-return.test.js (reddit collision probe: list_subreddit_posts orphan -> real list_posts)"
    - "catalog/descriptors/opentabs__reddit__list_subreddit_posts.json (DELETED -- stale orphan; real plugin emits list_posts)"

key-decisions:
  - "Option A (orchestrator-approved): reddit + calendly are SENSITIVE, not READ_ONLY_SAFE -- the real plugins have writes, so the conservative gate-prescribed fix applies (NOT a downgrade)"
  - "Re-pointed the MED-02 read-only-safe fixture/test from reddit -> grafana (the still-genuinely-read-only stand-in) to keep the 'a write on a safe origin fails the build' proof valid"
  - "reddit/calendly classified via service-denylist.json (social/scheduling), NOT the COMMERCE_SENSITIVE roster (no payment verb -> the payment-op guard does not key on them)"
  - "coverage-report realAppCount floor raised to >= 110 (actual 145) with margin so the exact count is not brittle to the apex/www host split"
  - "Search-PRECISION-at-scale failures (capability-search-eval + breadth-search-return wrong-invoke/index-size/cold-start) deferred to Phase 43 (SCALE-01/02) -- the documented Top Risk, not in this plan's verify/criteria/validate:extension"

patterns-established:
  - "Self-healing security gate: a CHECKED read-only-safe invariant that fails the build when a re-vendor adds writes, forcing an explicit reclassification rather than a silent writable-under-Auto ship"

requirements-completed: [BRDTH-01, BRDTH-02, BRDTH-03]

# Metrics
duration: ~40min
completed: 2026-06-26
---

# Phase 39.5 Plan 04: Full OpenTabs Source Import Summary

**The real ~117-plugin source import landed (2,383 opentabs descriptors, augment-preserving) with every full-scale security/correctness gate green; reddit + calendly reclassified SENSITIVE because the real plugins ship write ops (the gate-prescribed conservative fix), and DEF-39.5-03-A resolved so the full-corpus classifyGate exits 0.**

## Performance

- **Duration:** ~40 min (checkpoint-resume: prior session ran the full import + hit the READ_ONLY_SAFE checkpoint; this session applied the approved Option A + completed Task 2)
- **Completed:** 2026-06-26
- **Tasks:** 2 (Task 1 import staged by the prior session; Option A resolution + Task 2 completed here)
- **Files modified:** ~2,243 descriptor-path files + 9 script/test/config files (1 orphan deleted)

## Accomplishments
- The FULL real-source corpus emitted: **2,383 opentabs__*.json** descriptors (the 40 overlapping apps regenerated from the real Plan-01 src replacing the hand-authored ones; the 13 hand-only apps preserved -- augment held, `vendor-augment-preserved` green at 115 real-src slices) + the regenerated **recipe-index.generated.js** (2,391 descriptors, INV-01 IIFE/djb2 shape intact -- only the data grew).
- **Option A applied (orchestrator-approved):** reddit (`*.reddit.com` apex-suffix) + calendly (`calendly.com` exact-host) reclassified **SENSITIVE** in service-denylist.json (the real plugins ship writes: reddit edit_text/send_message/delete/...; calendly create/update/activate/deactivate/clone/delete_event_type) and **removed** from `READ_ONLY_SAFE_SERVICES`. Reads run under Auto; writes are posture-B mutating-gated (the discord/linkedin posture). The stale orphan `opentabs__reddit__list_subreddit_posts.json` was deleted (the real plugin emits `list_posts`) and the snapshot regenerated without it. The MED-02 read-only-safe fixture/test was re-pointed reddit -> grafana (the still-read-only-safe stand-in).
- **DEF-39.5-03-A resolved** (the deferred full-corpus classifyGate false-trips): the staged `verify-classification-gate.mjs` fix screens each emitted ORIGIN on **host + canonical slug only**, dropping the op free-text prose (which legitimately mentions axis tokens -- billing/budget/signal/health/tax -- on correctly-safe dev/infra origins at full-corpus scale). The host + slug checks both survive; `import-classify-gate-call.test.js` exits 0 over the full corpus.
- **Every full-scale gate GREEN:** `validate:extension` exit 0 -- validate-extension (285 JS), recipe-path-guard (Wall 1, no opentabs runtime shipped), classifyGate (145 corpus + 23 roster origins all classified), crosscheck (2,383 signal-bearing descriptors; READ_ONLY_SAFE now passes on yelp/tripadvisor/zillow/grafana; payment-op guard -- no payment op safe-and-invocable; 32 COMMERCE_SENSITIVE brands sensitive + disjoint), no-duplicate-stem (127 apps -> 127 stems, 0 collisions). Full `npm test` green EXCEPT the 2 deferred Phase-43 search-precision tests.
- **Task 2:** coverage-report.test.js updated for the ~130-app corpus (realAppCount floor `> 50` -> `>= 110`, actual **145**); totals.dead === 0; every Phase-39 commerce stem backing:dom (333 descriptors over 30 apps); the head bucket unchanged (8: the 3 heads + reddit.inbox/github.notifications); 26 payment ops, NONE API-invocable. The **doordash payment-write proof** + **discord write proof** still pass on the regenerated shipped descriptors (sensitive-write-import-gate 30/30).

## Task Commits

This plan was completed from a STAGED checkpoint state (the prior session ran the full import and stopped at the READ_ONLY_SAFE crosscheck checkpoint). The import + the orchestrator-approved Option-A resolution + Task 2 are committed as one atomic feat commit (the import output and its inseparable gate/classification fixes ship together):

1. **Task 1 (import) + Option A (reddit/calendly SENSITIVE; orphan deleted; fixture re-pointed) + Task 2 (coverage-report floor)** - `b9a4b22a` (feat)

**Plan metadata:** `__DOCS_COMMIT__` (docs: complete plan)

## Files Created/Modified
- `catalog/descriptors/opentabs__*.json` - 2,383 real-source descriptors (40 overlaps regenerated, 13 hand-only preserved)
- `catalog/descriptors/opentabs__reddit__list_subreddit_posts.json` - DELETED (stale orphan; real plugin emits list_posts)
- `extension/catalog/recipe-index.generated.js` - regenerated catalog (2,391 descriptors, INV-01 intact)
- `extension/config/service-denylist.json` - reddit `*.reddit.com` + calendly `calendly.com` -> sensitiveOrigins (+ audit comment)
- `scripts/verify-catalog-crosscheck.mjs` - reddit/calendly removed from READ_ONLY_SAFE_SERVICES (+ zillow.com apex); comments updated
- `scripts/verify-classification-gate.mjs` + `scripts/import-opentabs-catalog.mjs` - DEF-39.5-03-A (per-origin host+slug screen)
- `catalog/descriptors/_fixtures/safe-origin-write.fixture.json` - re-pointed reddit -> grafana write
- `catalog/descriptors/_fixtures/_provenance.json` + `seed-descriptors.json` - filled by the import
- `tests/catalog-crosscheck.test.js` - blocks (h)/(i) re-pointed reddit -> grafana
- `tests/coverage-report.test.js` - realAppCount floor raised to >= 110
- `tests/sensitive-write-import-gate.test.js` - doordash read-op host made host-agnostic (real apex doordash.com)
- `tests/breadth-search-return.test.js` - reddit collision probe -> real list_posts; reddit-now-sensitive framing
- `.planning/phases/39.5-.../deferred-items.md` - DEF-39.5-03-A marked resolved; DEF-39.5-04-A logged

## Decisions Made
- **Option A (approved):** reddit + calendly are SENSITIVE, not READ_ONLY_SAFE. The real vendored plugins expose write ops, so the READ_ONLY_SAFE invariant correctly failed the build; the conservative gate-prescribed fix (reclassify sensitive, posture-B gate the writes -- the discord/linkedin posture) was applied, NOT a downgrade of the gate.
- **grafana as the re-point target:** all 3 grafana descriptors are on `grafana.com` (in READ_ONLY_SAFE), read-only, committed-stable -- the cleanest still-genuinely-read-only stand-in to keep the MED-02 "a write on a safe origin fails the build" proof valid after reddit left the set.
- **reddit/calendly NOT on the COMMERCE roster:** they are social/scheduling brands carrying no payment verb, so the payment-op guard never keys on them; the denylist is their gate (the disjointness assertion enforces they are not also READ_ONLY_SAFE).
- **realAppCount floor >= 110 (actual 145):** deliberate margin so the exact count is not brittle to the per-op apex/www host split the real plugins vendor.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Re-pointed the doordash read-op host assertion (stale www assumption after the real import)**
- **Found during:** Task 2 verification (sensitive-write-import-gate.test.js)
- **Issue:** The test asserted the `list_orders` READ descriptor is on `www.doordash.com`, but the real doordash plugin emits `list_orders` on the bare apex `doordash.com` (place_order stays on www.doordash.com). The test failed (29 passed, 1 failed). NOT a security regression -- both hosts classify SENSITIVE (the `*.doordash.com` apex-suffix covers both), and the PAYMENT-WRITE proof (place_order on www.doordash.com) is fully intact.
- **Fix:** Made the read-op assertion host-agnostic -- assert the read descriptor is a `*.doordash.com` SENSITIVE origin + read, and route the read-gate through the descriptor's OWN origin. The read-runs-under-Auto proof holds on the real apex.
- **Files modified:** tests/sensitive-write-import-gate.test.js
- **Verification:** sensitive-write-import-gate 30/30 pass
- **Committed in:** `b9a4b22a`

**2. [Rule 3 - Blocking] Re-pointed the breadth-search-return reddit collision probe (deleted orphan slug)**
- **Found during:** Task 2 verification (full npm test)
- **Issue:** breadth-search-return.test.js hardcoded `expected: 'reddit.list_subreddit_posts'` -- the stale orphan slug Option A step 3 deleted -- so it got the real `reddit.list_posts` and failed. The expectation referenced a descriptor that no longer exists.
- **Fix:** Re-pointed the expectation to `reddit.list_posts` (the real plugin op) and updated the test's reddit framing (reddit is now SENSITIVE, not the SAFE content tier). That probe now passes.
- **Files modified:** tests/breadth-search-return.test.js
- **Verification:** the "browse the posts in a subreddit on reddit" probe tops reddit.list_posts (PASS)
- **Committed in:** `b9a4b22a`

---

**Total deviations:** 2 auto-fixed (1 bug, 1 blocking) -- both are stale test expectations the real import / Option-A deletion invalidated; neither weakens any guard.
**Impact on plan:** No scope creep. Both fixes realign tests to the real emitted descriptors. The payment-write and read-only-safe security proofs remain valid.

## Deferred Issues

**Search-PRECISION-at-scale (DEF-39.5-04-A) -- OUT OF SCOPE, belongs to Phase 43 (SCALE-01/02).**
Two tests fail purely from the full ~2,400-descriptor corpus the import mandates -- `capability-search-eval.test.js` (13 passed, 3 failed) and `breadth-search-return.test.js` (76 passed, 16 failed): cross-app near-neighbor ranking collisions (`wrong-invoke` 0.060 / 0.188; e.g. "log a bug in linear" -> linear.archive_issue; "look for airfare on expedia" -> expedia.book_flight) plus the index-size (1450KB) + cold-start (13.83ms) budgets. `recall@5` PASSES on both (0.992 / 1.000 -- the correct op is in the top-5). NONE of the residual collisions involves the Option-A surface (reddit/calendly/grafana/doordash -- verified 0). This is the documented "Search precision + SW cold-start at ~2,523 docs" Top Risk that STATE.md and both tests' own messages assign to Phase 43; it is NOT in this plan's `<verify>` / success criteria / `validate:extension` (exit-0 green). The fix is the Phase-43 architectural mandate (intentSynonym enrichment + owned-origin ranking bias + index searchable-text-only + cold-start benchmark), not a per-task auto-fix. Logged in deferred-items.md.

## Issues Encountered
- The doordash read-op + reddit collision test expectations encoded stale hosts/slugs that the real import / Option-A deletion changed -- resolved via the two in-scope deviations above (the only test changes the real corpus required beyond the planned re-points).

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- The full real-source breadth corpus is landed and every full-scale security/correctness gate is green -- the breadth half of the milestone is complete; Phases 40-41 (depth hand-ports) and 42 (discovery seeding) build on this catalog.
- **Phase 43 (SCALE-01/02) carries the deferred search-precision-at-scale work** (DEF-39.5-04-A): wrong-invoke=0 over the full corpus + the index-size + cold-start budgets. The catalog is at the ~2,400-descriptor scale those gates must be re-tuned for.

## Self-Check: PASSED

- All claimed created/modified files exist on disk (SUMMARY.md, recipe-index.generated.js, service-denylist.json, both gate scripts, the re-pointed fixture, the updated tests).
- The deleted orphan `opentabs__reddit__list_subreddit_posts.json` is confirmed ABSENT; the real `opentabs__reddit__list_posts.json` is present.
- The import commit `b9a4b22a` exists in git history.

---
*Phase: 39.5-full-opentabs-source-import*
*Completed: 2026-06-26*
