---
phase: 39.5-full-opentabs-source-import
plan: 05
type: execute
wave: 5
depends_on: [39.5-04]
files_modified:
  - tests/capability-search-eval.test.js
  - tests/full-corpus-scale.test.js
  - package.json
autonomous: true
requirements: [BRDTH-01, BRDTH-02, BRDTH-03]
must_haves:
  truths:
    - "The serialized search index at the full ~2,374-op corpus is measured and asserted < ~2MB"
    - "loadJSON + first-search (cold-start) at the full corpus is asserted < ~100ms"
    - "The eval recall@5 >= 0.9 and wrong-invoke == 0 still hold over the full-corpus seed"
    - "Full `npm test` exits 0 (the phase gate) -- every catalog + new test green at full scale"
  artifacts:
    - path: "tests/full-corpus-scale.test.js"
      provides: "The full-corpus SCALE assert: builds the real index over the complete catalog, measures serialized size + cold-start, asserts < 2MB / < 100ms"
      min_lines: 50
  key_links:
    - from: "tests/full-corpus-scale.test.js"
      to: "extension/utils/capability-search.js buildIndex + INDEX_OPTIONS"
      via: "build the real index over the regenerated full catalog -> measure toJSON() + loadJSON"
      pattern: "buildIndex|INDEX_OPTIONS"
    - from: "tests/full-corpus-scale.test.js"
      to: "extension/catalog/recipe-index.generated.js"
      via: "index the FULL committed descriptor set (not the seed)"
      pattern: "recipe-index.generated"
---

<objective>
Prove the full-corpus SCALE budget and close coverage. MEASURE the real serialized minisearch index built over the complete ~2,400+ descriptor catalog and ASSERT it is < ~2MB AND loadJSON+first-search (cold-start) is < ~100ms (the SCALE-01 breadth-corpus budget). Raise the eval harness's outer ceilings to the SCALE-01 band, keep the recall@5/wrong-invoke gate green, and run the full `npm test` (the phase gate). Phase 43 owns the authoritative SCALE-01 milestone gate; this phase proves the breadth corpus is within budget.

Purpose: The headline perf risk -- the spike projected the index to ~1.31MB at 2,374 ops (inside the 1-2MB band but its upper half). This plan measures the REAL serialized index at the full corpus and asserts it lands inside ~2MB / ~100ms, with the synthSynonyms cap (6/op) holding it down and storeFields-trimming available as a lever only if it overshoots. This is the last gate before the phase is parity-complete.
Output: the full-corpus scale test + the raised eval ceilings + a green full `npm test`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39.5-full-opentabs-source-import/39.5-CONTEXT.md
@.planning/phases/39-breadth-c-commerce-travel-misc-most-sensitive/FULL-PARITY-SPIKE.md

<read_first>
- FULL-PARITY-SPIKE.md Section 4 (scale implications: 579.9 B/entry serialized -> ~1.31MB @ 2,374; the binding constraint is the serialized index; the mitigations -- synthSynonyms cap 6/op already, trim description from storeFields, shard) + TOP RISK 3
- .planning/phases/39.5-full-opentabs-source-import/39.5-CONTEXT.md (<decisions> "Scale" -- MEASURE the real serialized index, ASSERT < ~2MB + < ~100ms; keep the synthSynonyms cap; trim storeFields ONLY if it overshoots; Phase 43 owns the authoritative SCALE-01)
- tests/capability-search-eval.test.js (the EXISTING smoke index asserts: the 512KB byte ceiling [line ~158], the < 700 bytes/descriptor flatness [~167], the < 10ms smoke load [~173] -- these are SMOKE asserts over the SEED set; the new full-corpus scale test is the real measurement; the recall@5>=0.9 + wrong-invoke==0 gate [~118-119] must stay green)
- extension/utils/capability-search.js buildIndex + INDEX_OPTIONS + catalogVersion (the SAME builder + options the runtime uses -- the scale test must use these, not a hand-rolled index, so it measures what ships)
- extension/catalog/recipe-index.generated.js (the regenerated full catalog from Plan 04 -- the descriptor array to index)
- extension/lib/minisearch.min.js (the UMD MiniSearch the eval requires)
- scripts/coverage-report.mjs (re-run for realAppCount ~130 -- the coverage close)
</read_first>

<interfaces>
<!-- The SCALE-01 breadth-corpus budget (CONTEXT + spike Section 4): -->
<!--   serialized minisearch index  < ~2 MB   (spike projects ~1.31MB at 2,374 ops -- inside, upper half) -->
<!--   loadJSON(serialized, INDEX_OPTIONS) + first search  < ~100 ms  (cold-start) -->
<!-- Build the index the SAME way capability-search.js does: buildIndex(descriptors, slugToRecipe) with INDEX_OPTIONS; serialize via JSON.stringify(ms.toJSON()); restore via MiniSearch.loadJSON(serialized, INDEX_OPTIONS) (the options arg is MANDATORY -- loadJSON without it throws). -->

<!-- The eval test's existing outer ceilings (capability-search-eval.test.js) are SMOKE asserts over the SEED set: -->
<!--   - the 512KB byte ceiling (line ~158): a generous OUTER backstop for the SEED index -- the full-corpus measurement is the NEW test below -->
<!--   - the < 700 bytes/descriptor flatness (line ~167): the real params-leak regression signal -- KEEP this (it stays flat ~558-580 at full scale; a params leak would blow past it) -->
<!--   - the < 10ms smoke load (line ~173): over the SEED set -- KEEP (the full-corpus cold-start is the new test's < 100ms assert) -->
<!-- The recall@5 >= 0.9 + wrong-invoke == 0 gate (lines ~118-119) MUST stay green -- the feedSeedDescriptors at Plan 04 kept the seed in lockstep with the emitted corpus, so every intent-case expectedSlug has an indexed descriptor. -->

<!-- The lever if the index OVERSHOOTS ~2MB (Claude's Discretion per CONTEXT): trim `description` from INDEX_OPTIONS.storeFields in capability-search.js (description is schema-on-hit via the descriptor map, not needed in the stored index) -- a flat reduction that does not touch the searchable fields. The synthSynonyms cap (6/op) is already in place. Do NOT shard in this phase (Phase 43's concern). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Full-corpus SCALE test (real serialized index < 2MB + cold-start < 100ms)</name>
  <files>tests/full-corpus-scale.test.js, package.json</files>
  <behavior>
    - Test 1: the real index built over the COMPLETE committed catalog (recipe-index.generated.js descriptors) serializes to < ~2MB (the SCALE-01 breadth budget; report the measured bytes + bytes/descriptor)
    - Test 2: MiniSearch.loadJSON(serialized, INDEX_OPTIONS) + a first search completes in < ~100ms (cold-start)
    - Test 3: bytes/descriptor stays flat (< ~700) at full scale (no params-leak regression -- the schema-on-hit layout held)
    - Test 4: the index actually covers the full corpus (descriptor count matches the committed catalog, > 2000)
  </behavior>
  <read_first>
    - tests/capability-search-eval.test.js (the index-build + serialize + loadJSON measurement pattern to mirror: plant global.MiniSearch from extension/lib/minisearch.min.js, buildIndex with INDEX_OPTIONS, JSON.stringify(ms.toJSON()), MiniSearch.loadJSON(serialized, INDEX_OPTIONS), performance.now() timing)
    - extension/utils/capability-search.js buildIndex + INDEX_OPTIONS (the runtime builder/options -- use these, not a hand-rolled index)
    - extension/catalog/recipe-index.generated.js (the full descriptor array to index)
    - FULL-PARITY-SPIKE.md Section 4 (the projected ~1.31MB + the < 2MB / < 100ms budget)
  </read_first>
  <action>
    Write tests/full-corpus-scale.test.js as a zero-framework FSB node test (check(cond,msg) counter, PASS=/FAIL=, process.exit(failed>0?1:0), ASCII-only) that measures the REAL full-corpus index: require extension/lib/minisearch.min.js and plant global.MiniSearch; require the regenerated extension/catalog/recipe-index.generated.js for the full descriptor array; build the index via capability-search.js's buildIndex(descriptors, slugToRecipe) with the runtime INDEX_OPTIONS (the SAME builder + options that ship -- so the measurement reflects production, not a hand-rolled index); serialize via JSON.stringify(ms.toJSON()) and assert the serialized length < ~2MB (2 * 1024 * 1024), reporting the measured KB + bytes/descriptor; restore via MiniSearch.loadJSON(serialized, INDEX_OPTIONS) + run a first search and assert the elapsed cold-start time < ~100ms via performance.now(); assert bytes/descriptor < ~700 (the flatness/no-params-leak signal at full scale); assert the indexed descriptor count matches the committed catalog (> 2000). If the index OVERSHOOTS ~2MB, apply the documented lever -- trim `description` from INDEX_OPTIONS.storeFields in extension/utils/capability-search.js (description is schema-on-hit, not needed stored) -- and re-measure; do NOT shard (Phase 43's concern). Register the test in package.json's `test` chain (append ` && node tests/full-corpus-scale.test.js`). FLAG in the SUMMARY that this is the breadth-corpus scale proof and Phase 43 owns the authoritative SCALE-01 milestone gate.
  </action>
  <verify>
    <automated>node tests/full-corpus-scale.test.js && grep -q "full-corpus-scale.test.js" package.json</automated>
  </verify>
  <acceptance_criteria>
    - the real serialized index over the COMPLETE catalog is < ~2MB (measured + reported)
    - loadJSON + first-search cold-start is < ~100ms (measured + reported)
    - bytes/descriptor stays flat < ~700 at full scale (no params-leak regression)
    - the indexed descriptor count matches the committed full catalog (> 2000)
    - the test is registered in npm test; if storeFields was trimmed, the SUMMARY flags it
  </acceptance_criteria>
  <done>tests/full-corpus-scale.test.js measures the real full-corpus index < 2MB + cold-start < 100ms (SCALE-01 breadth budget), flatness held; registered in npm test.</done>
</task>

<task type="auto">
  <name>Task 2: Keep the eval gate green + coverage close + full npm test (the phase gate)</name>
  <files>tests/capability-search-eval.test.js</files>
  <read_first>
    - tests/capability-search-eval.test.js (the recall@5>=0.9 + wrong-invoke==0 gate [~118-119] -- must stay green; the smoke flatness/load asserts [~167/~173] over the SEED set -- keep; the 512KB SEED byte ceiling [~158] -- a generous outer backstop, widen its comment to note the full-corpus measurement is in tests/full-corpus-scale.test.js)
    - scripts/coverage-report.mjs (re-run for realAppCount ~130 -- the coverage close)
    - package.json `test` script (the full chain -- confirm every catalog + new test is registered and the whole chain is green)
  </read_first>
  <action>
    Confirm the eval gate stays green at full scale: run tests/capability-search-eval.test.js and verify recall@5 >= 0.9 + wrong-invoke == 0 still hold (the feedSeedDescriptors at Plan 04 kept the seed in lockstep, so every intent-case has an indexed descriptor). The eval's SEED-set smoke asserts (the 512KB byte ceiling, the < 700 flatness, the < 10ms smoke load) measure the SEED index and stay as-is -- update ONLY the 512KB-ceiling comment to point at tests/full-corpus-scale.test.js as the authoritative full-corpus measurement (do NOT raise the seed smoke ceilings; the new test owns the full-corpus budget). Re-run `node scripts/coverage-report.mjs` and confirm realAppCount reflects the full corpus (~130 / >= ~110 distinct services) -- the coverage close (BRDTH-01/02/03 -- the complete real-app set returns from search). Finally run the FULL `npm test` (the phase gate) and confirm it exits 0 -- every catalog gate (classification, crosscheck incl payment-op, no-dead-entry, no-duplicate-stem, coverage, the doordash/discord proofs) AND the new tests (vendor-augment-preserved, no-duplicate-stem, full-corpus-screen, full-corpus-scale) are green at full scale. If any test fails, fix it (or surface a genuine gap) before declaring the phase parity-complete.
  </action>
  <verify>
    <automated>node tests/capability-search-eval.test.js && node scripts/coverage-report.mjs && npm test</automated>
  </verify>
  <acceptance_criteria>
    - the eval recall@5 >= 0.9 and wrong-invoke == 0 still hold over the full-corpus seed
    - coverage-report shows realAppCount reflecting the full corpus (~130 / >= ~110), reported vs ~117
    - full `npm test` exits 0 (every catalog + new test green at full scale -- the phase gate)
    - the seed smoke ceilings are unchanged; only the 512KB-ceiling comment points at the full-corpus scale test
  </acceptance_criteria>
  <done>The eval gate stays green (recall@5 >= 0.9, wrong-invoke == 0), coverage closes at ~130 apps, and full `npm test` exits 0 -- the phase is parity-complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the serialized index -> the cold-started service worker | an oversized index blows the cold-start budget -> the first search is slow/unavailable (availability, not a security breach) |
| the eval seed -> the search ranking | a recall miss / wrong-invoke means the AI surfaces or invokes the wrong op (a correctness, and at scale a safety, concern) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39.5-13 | Denial of Service | the serialized index overshoots the cold-start budget at ~2,374 ops | mitigate | the full-corpus scale test (Task 1) measures + asserts < 2MB / < 100ms; the synthSynonyms cap (6/op) holds it down; trim `description` from storeFields is the documented lever; Phase 43 owns the authoritative SCALE-01 |
| T-39.5-14 | Tampering | a params-leak (additionalProperties bleeding into the stored index) silently bloats the index | mitigate | the bytes/descriptor flatness assert (< 700, Task 1 + the eval smoke) catches a params-leak as a sudden jump, distinct from linear growth |
| T-39.5-15 | Elevation of Privilege | a wrong-invoke at scale surfaces/invokes the wrong op (a payment near-neighbor) | mitigate | the eval wrong-invoke == 0 gate (Task 2) stays green over the full-corpus seed; the payment-op guard (Plan 04) independently bars any payment op from being API-invocable |
| T-39.5-SC | Tampering | npm/pip/cargo installs | accept | NO new packages this plan (a test + an optional storeFields trim); no install task |
</threat_model>

<verification>
- `node tests/full-corpus-scale.test.js` passes (< 2MB index + < 100ms cold-start at full corpus)
- `node tests/capability-search-eval.test.js` passes (recall@5 >= 0.9, wrong-invoke == 0)
- `node scripts/coverage-report.mjs` reports realAppCount ~130 (>= ~110)
- `npm test` exits 0 (the phase gate -- every catalog + new test green)
</verification>

<success_criteria>
SC4 is satisfied: the serialized index < ~2MB + cold-start < ~100ms at the full ~2,374-op corpus; crosscheck + no-dead-entry + the eval (recall@5 >= 0.9, wrong-invoke == 0) + full `npm test` green; Wall 1 holds (recipe-path-guard); INV-01 IIFE/djb2 intact. OpenTabs parity is complete.
</success_criteria>

<output>
Create `.planning/phases/39.5-full-opentabs-source-import/39.5-05-SUMMARY.md` when done.
</output>
