---
phase: 39.5-full-opentabs-source-import
plan: 02
type: execute
wave: 2
depends_on: [39.5-01]
files_modified:
  - scripts/import-opentabs-catalog.mjs
  - scripts/verify-no-duplicate-stem.mjs
  - tests/no-duplicate-stem.test.js
  - tests/import-forbidden-prescan.test.js
  - package.json
autonomous: true
requirements: [BRDTH-01]
must_haves:
  truths:
    - "STEM_OVERRIDES carries a canonical, collision-free stem for all ~40 wrong/colliding-stem real apps (6 collision groups incl the 4-way `console`)"
    - "A no-duplicate-stem CI gate FAILS the build if any two enumerated apps would emit the same opentabs__<stem>__ filename prefix"
    - "target.apply_promo_code's business `code` input field is renamed to `promo_code` so its op survives the Wall-1 pre-scan, while script/expr/transform/fn/js stay ALWAYS-fatal"
    - "The real grafana + sqlpad (empty urlPatterns) are skipped by the importer with NO core-path change"
  artifacts:
    - path: "scripts/verify-no-duplicate-stem.mjs"
      provides: "The no-duplicate-stem CI gate (dual-export + CLI), wired into validate:extension"
      min_lines: 60
      exports: ["checkNoDuplicateStem"]
    - path: "tests/no-duplicate-stem.test.js"
      provides: "Proof: the gate FAILS on a synthetic stem collision and PASSES on the real override map"
      min_lines: 40
  key_links:
    - from: "scripts/import-opentabs-catalog.mjs"
      to: "STEM_OVERRIDES"
      via: "displayServiceStem() per-app override (line ~227-239)"
      pattern: "STEM_OVERRIDES\\s*="
    - from: "scripts/verify-no-duplicate-stem.mjs"
      to: "scripts/import-opentabs-catalog.mjs"
      via: "enumerate the vendored apps + derive each stem -> assert no two collide"
      pattern: "opentabs__"
---

<objective>
Extend the frozen importer's DATA-MAPS to handle the full 117-real corpus -- WITHOUT touching the load/transpile/extract/z.toJSONSchema/pre-scan/side-effect core path: (1) add ~40 STEM_OVERRIDES entries (disambiguating the 6 collision groups incl the 4-way `console`) so every app emits a canonical, collision-free `opentabs__<stem>__*` filename; (2) add a no-duplicate-stem CI gate (the correctness backstop -- a missing override silently clobbers a descriptor); (3) carve `target.apply_promo_code`'s business `code` field to `promo_code` so its op survives the Wall-1 forbidden-field pre-scan while keeping script/expr/transform/fn/js always-fatal; (4) skip the real grafana + sqlpad (empty urlPatterns).

Purpose: BLOCKER A (stem collisions) is THE correctness risk -- the complete override map + the no-dup-stem gate are non-negotiable. BLOCKER B (target.apply_promo_code) is the ONLY forbidden-in-input op corpus-wide. BLOCKER C (grafana/sqlpad) is 2 self-hosted apps that have no static origin. All three are small, enumerated, mechanical -- the spike proved the core path imports the real source unchanged.
Output: the extended importer + scripts/verify-no-duplicate-stem.mjs + tests/no-duplicate-stem.test.js + the extended forbidden-prescan test, all wired into validate:extension / npm test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39.5-full-opentabs-source-import/39.5-CONTEXT.md
@.planning/phases/39-breadth-c-commerce-travel-misc-most-sensitive/FULL-PARITY-SPIKE.md

<read_first>
- FULL-PARITY-SPIKE.md Section 2 BLOCKER A (the 40 wrong/colliding stems + the 6 collision-group table), BLOCKER B (target.apply_promo_code carve options i/ii), BLOCKER C (grafana/sqlpad skip), Section 5.6 (the importer changes list)
- .planning/phases/39.5-full-opentabs-source-import/39.5-CONTEXT.md (<decisions> "Stem collisions", "Carves / skips" -- default rename for target.apply_promo_code; SKIP grafana+sqlpad)
- scripts/import-opentabs-catalog.mjs (STEM_OVERRIDES line ~227-239; displayServiceStem line ~237; enumerateBatchApps line ~104-130 + the EXISTING_HEAD_SERVICES/FIXTURE_DIR_RE skips; extractDescriptors line ~603-659 incl the assertCleanParams call at ~627; readPluginMeta line ~577-589)
- scripts/verify-classification-gate.mjs (the dual-export + CLI skeleton to MIRROR for the new gate: ROOT via fileURLToPath, failures[] accumulator, process.exit(1) on fail / exit(0)+PASS on pass, the import.meta.url pathToFileURL(process.argv[1]) guard)
- tests/import-forbidden-prescan.test.js (the carve test extends this -- the assertCleanParams THROWS proof; add a target.apply_promo_code rename proof)
- scripts/lib/side-effect-class.mjs FORBIDDEN equivalent: the importer's FORBIDDEN set (line ~280) = {script,expr,transform,code,fn,js}
</read_first>

<interfaces>
<!-- The exact STEM_OVERRIDES extension point (scripts/import-opentabs-catalog.mjs line ~227). -->
<!-- Current map (Phase 37-39) keys by vendored DIR name; displayServiceStem(app, derivedStem) returns the override or the derived stem. -->
<!-- ADD ~40 entries. The 6 collision groups MUST get DISTINCT stems (else two apps emit the same opentabs__<stem>__* filename and silently clobber): -->

  console (4-way): aws-console->'aws', clickhouse->'clickhouse', google-cloud->'gcloud', twilio->'twilio'
  www:             google-maps->'googlemaps' (or 'gmaps'), npm->'npm', reddit->'reddit', yelp->'yelp'   [reddit/yelp already overridden in 39]
  atlassian:       jira->'jira', confluence->'confluence'   [ALREADY present]
  cloud:           mongodb-atlas->'mongodbatlas' (or 'atlas'), temporal-cloud->'temporal'
  slack:           slack->(skipped: EXISTING_HEAD), slack-enterprise->'slackent'
  web:             telegram->'telegram', whatsapp->'whatsapp'
  ~28 singles (wrong stem -> brand stem): azure->'azure', bluesky->'bsky' [present], cloudflare->'cloudflare' [present],
    cockroachdb->'cockroachdb', datadog->'datadog' [present], docker-hub->'dockerhub', excel-online->'excel',
    fidelity->(denied -- not imported), google-analytics->'googleanalytics', google-calendar->'gcal',
    google-docs->'gdocs', google-drive->'gdrive', hackernews->'hackernews', microsoft-word->'word',
    minimax-agent->'minimax', newrelic->'newrelic', panda-express->'pandaexpress', posthog->'posthog' [present],
    spotify->(denied), steam->(denied), stripe->'stripe', terraform-cloud->'terraform',
    youtube-music->(denied), etc.
<!-- NOTE: denied apps (fidelity/spotify/steam/youtube-music) are never emitted (classifyGate denied -> the importer still derives a stem but they bucket denied); give them a canonical stem anyway for slug stability, OR rely on the denylist -- the no-dup-stem gate operates over the ENUMERATED set, so any app whose stem would collide MUST get a distinct override regardless of denied status. The EXACT canonical names are Claude's Discretion (avoid NEW collisions; the no-dup-stem gate enforces). -->

<!-- The target.apply_promo_code carve (BLOCKER B). The op file: plugins/target/src/tools/apply-promo-code.ts has input z.object({ code: z.string()... }). -->
<!-- 'code' is in the importer's FORBIDDEN set -> assertCleanParams THROWS, aborting the whole target import. The DEFAULT decision (CONTEXT): a NARROW per-op input-field rename code->promo_code for THIS op only. -->
<!-- Implement as a scoped rename in extractDescriptors (BEFORE assertCleanParams) keyed on (app==='target' && tool.name==='apply_promo_code'): rename the 'code' property in the flattened params (and in any required[] entry) to 'promo_code'. Keep script/expr/transform/fn/js ALWAYS-fatal (do NOT add them to any allowlist). Fallback (Claude's Discretion if the rename adds risk): skip the single op. -->

<!-- The grafana/sqlpad skip (BLOCKER C). enumerateBatchApps() already skips fixtures + EXISTING_HEAD_SERVICES. -->
<!-- Add a SKIP_APPS set = {'sqlpad'} plus skip the REAL grafana whose service derives '' (empty urlPatterns). Since the hand-authored grafana (grafana.com) is PRESERVED (Plan 01), and the real grafana is NOT vendored (Plan 01 skipped vendoring it), the importer enumerates the hand-authored grafana dir (grafana.com origin) -- which imports fine. So the ONLY new skip the importer needs is sqlpad (and any app readPluginMeta derives '' for -> already throws in extractDescriptors; convert that to a clean skip for empty-urlPatterns apps so a future self-hosted app does not abort the batch). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend STEM_OVERRIDES (~40) + carve target.apply_promo_code + skip empty-origin apps</name>
  <files>scripts/import-opentabs-catalog.mjs</files>
  <read_first>
    - scripts/import-opentabs-catalog.mjs STEM_OVERRIDES (line ~227-239), displayServiceStem (~237), extractDescriptors (~603-659) incl assertCleanParams call (~627), enumerateBatchApps (~104-130), readPluginMeta (~577-589)
    - FULL-PARITY-SPIKE.md Section 2 BLOCKER A table (the 6 collision groups), BLOCKER B (the code->promo_code rename), BLOCKER C (grafana/sqlpad)
    - the <interfaces> block above (the concrete ~40 override entries + collision-group disambiguation + the carve location)
  </read_first>
  <action>
    Extend the STEM_OVERRIDES map (scripts/import-opentabs-catalog.mjs line ~227) with a canonical stem for ALL ~40 wrong/colliding-stem real apps, disambiguating the 6 collision groups so no two enumerated apps share a stem: the 4-way `console` group (aws-console->aws, clickhouse->clickhouse, google-cloud->gcloud, twilio->twilio), the `www` group (google-maps, npm, reddit, yelp -- reddit/yelp already present), the `cloud` group (mongodb-atlas, temporal-cloud), the `slack` group (slack-enterprise->slackent; slack itself is an EXISTING_HEAD so it is enumerate-skipped), the `web` group (telegram, whatsapp), plus the ~28 single wrong-stem apps (azure, cockroachdb, docker-hub, excel-online, google-analytics/calendar/docs/drive, hackernews, microsoft-word, minimax-agent, newrelic, panda-express, terraform-cloud, stripe, etc.). The EXACT canonical names are yours to choose -- the only hard constraint is NO new collisions (the no-dup-stem gate in Task 2 enforces this). Keep the existing jira/confluence/cloudflare/datadog/threads/bluesky/posthog + all 39-* commerce entries unchanged; extend the explanatory comment with a Phase-39.5 note (the canonicalization is a DATA-MAP extension of the 37-01 extension point, NOT a logic change -- INV-01 holds; the `service` field keeps the real host, only the stem/slug/filename is canonicalized). Add the target.apply_promo_code CARVE: in extractDescriptors, BEFORE the assertCleanParams(params, tool.name) call, when `app === 'target' && tool.name === 'apply_promo_code'`, rename the `code` property in the flattened params object (under properties, and in the required[] array if present) to `promo_code` -- a narrow, audited business-`code` rename that lets the op survive the Wall-1 pre-scan; do NOT weaken the FORBIDDEN set and do NOT add script/expr/transform/fn/js to any allowlist (they stay always-fatal). Add the empty-origin SKIP: introduce a SKIP_APPS set containing 'sqlpad', and in enumerateBatchApps (or extractDescriptors) skip any app in SKIP_APPS AND any app whose readPluginMeta derives an empty `service` (empty urlPatterns) with a clean continue instead of the current throw -- so a self-hosted empty-origin app is skipped cleanly rather than aborting the batch (the hand-authored grafana with grafana.com still imports normally; only the real-empty-origin apps are skipped). NO change to the load/transpile/extract/z.toJSONSchema/pre-scan/side-effect classification path otherwise.
  </action>
  <verify>
    <automated>node -e "import('./scripts/import-opentabs-catalog.mjs').then(m=>{ if(typeof m.extractDescriptors!=='function'){process.exit(1)} console.log('importer imports OK'); })" && grep -q "apply_promo_code" scripts/import-opentabs-catalog.mjs && grep -q "promo_code" scripts/import-opentabs-catalog.mjs && grep -q "aws-console" scripts/import-opentabs-catalog.mjs && grep -q "slackent" scripts/import-opentabs-catalog.mjs</automated>
  </verify>
  <acceptance_criteria>
    - STEM_OVERRIDES carries distinct canonical stems for the 6 collision groups (aws-console/clickhouse/google-cloud/twilio all distinct; mongodb-atlas vs temporal-cloud distinct; slack-enterprise->slackent; telegram vs whatsapp distinct) + the ~28 singles
    - extractDescriptors renames target.apply_promo_code's `code` input field to `promo_code` BEFORE the pre-scan, so that op no longer trips assertCleanParams; script/expr/transform/fn/js remain in FORBIDDEN (un-allowlisted)
    - sqlpad + any empty-urlPatterns app are skipped cleanly (no throw); the hand-authored grafana (grafana.com) still imports
    - the importer module imports without error; NO change to the core load/extract/classify path
  </acceptance_criteria>
  <done>The importer's data-maps cover the full corpus: ~40 stem overrides (collision-free), the promo_code carve, the empty-origin skip -- core path untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the no-duplicate-stem CI gate (the BLOCKER-A correctness backstop)</name>
  <files>scripts/verify-no-duplicate-stem.mjs, tests/no-duplicate-stem.test.js, package.json</files>
  <behavior>
    - Test 1: checkNoDuplicateStem over the REAL enumerated vendored set yields 0 collisions (the complete override map ships)
    - Test 2: checkNoDuplicateStem over a SYNTHETIC two-app set that derive the same stem yields >= 1 failure NAMING both apps + the colliding stem (the gate is real)
    - Test 3: the gate's CLI exits 0 over the committed corpus (PASS path) -- proven by importing the dual-export and running checkNoDuplicateStem on the real set
  </behavior>
  <read_first>
    - scripts/verify-classification-gate.mjs (the dual-export + CLI skeleton to mirror EXACTLY: failures[] accumulator, process.exit on fail/pass, the import.meta.url guard, ASCII-only Wall-1 build-tooling discipline)
    - scripts/import-opentabs-catalog.mjs enumerateBatchApps + readPluginMeta + displayServiceStem (the gate re-derives each app's stem the SAME way the importer does -- import these exports rather than re-deriving, so the gate cannot drift from the importer)
    - tests/payment-op-guard.test.js (the real-export-driven test convention: dynamic import() of the .mjs dual-export from a CJS test, check(cond,msg), process.exit)
  </read_first>
  <action>
    Create scripts/verify-no-duplicate-stem.mjs mirroring verify-classification-gate.mjs's dual-export + CLI skeleton (ASCII-only, Wall-1 build-tooling discipline, no run-string-as-code constructs). Export `checkNoDuplicateStem(apps)` -> { failures: string[] }: for each app it derives the SAME stem the importer emits (import enumerateBatchApps/readPluginMeta/displayServiceStem from import-opentabs-catalog.mjs and apply `service.replace(/^app\./,'').split('.')[0]` then displayServiceStem(app, derivedStem) -- so the gate computes the EXACT filename prefix `opentabs__<stem>__` the importer writes), groups apps by stem, and pushes a failure naming BOTH (all) apps + the shared stem for any stem owned by >1 app. A missing STEM_OVERRIDE that lets two apps collide is then a BUILD FAILURE, not a silent clobber. The CLI (on direct invocation via the import.meta.url guard) enumerates the real vendored set, runs checkNoDuplicateStem, prints a PASS summary or the failures, and process.exit(1) on any collision. Wire it into validate:extension by appending ` && node scripts/verify-no-duplicate-stem.mjs` to the validate:extension script in package.json (after verify-catalog-crosscheck.mjs). Write tests/no-duplicate-stem.test.js (zero-framework FSB node test; dynamic import() of the .mjs dual-export as in payment-op-guard.test.js) proving: (1) the real enumerated set yields 0 collisions; (2) a synthetic two-app collision yields a failure naming both apps + the stem; register the test in package.json's `test` chain (append ` && node tests/no-duplicate-stem.test.js`).
  </action>
  <verify>
    <automated>node scripts/verify-no-duplicate-stem.mjs && node tests/no-duplicate-stem.test.js && grep -q "verify-no-duplicate-stem.mjs" package.json && grep -q "no-duplicate-stem.test.js" package.json</automated>
  </verify>
  <acceptance_criteria>
    - the importer emits 0 colliding opentabs__<stem>__* filenames over the real corpus (checkNoDuplicateStem returns 0 failures on the enumerated set)
    - the no-duplicate-stem gate FAILS on a synthetic collision, naming both apps + the colliding stem
    - the gate re-derives stems via the importer's OWN exports (cannot drift from what the importer writes)
    - the gate is wired into validate:extension and the test into npm test
  </acceptance_criteria>
  <done>scripts/verify-no-duplicate-stem.mjs (dual-export + CLI, wired into validate:extension) + tests/no-duplicate-stem.test.js prove the override map is collision-free and a collision fails the build.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend the forbidden-prescan test with the target.apply_promo_code carve proof</name>
  <files>tests/import-forbidden-prescan.test.js</files>
  <behavior>
    - Test 1: a flattened params object with a top-level `code` property still trips preScanForbidden (the FORBIDDEN set is unchanged -- `code` is still fatal in general)
    - Test 2: after the importer's scoped rename, the target.apply_promo_code params expose `promo_code` (not `code`) and pass assertCleanParams (the carve is narrow + works)
    - Test 3: script/expr/transform/fn/js still trip the pre-scan (the eval-able names stay always-fatal -- the carve did NOT weaken them)
  </behavior>
  <read_first>
    - tests/import-forbidden-prescan.test.js (the existing 6-fixture proof + the assertCleanParams THROWS test -- extend it; do not rewrite)
    - scripts/import-opentabs-catalog.mjs extractDescriptors carve (the code->promo_code rename added in Task 1) + assertCleanParams + preScanForbidden
  </read_first>
  <action>
    Extend tests/import-forbidden-prescan.test.js (do NOT rewrite the existing 6-fixture rows) with a carve block proving: (1) a generic top-level `code` field STILL trips preScanForbidden (the FORBIDDEN set is intact -- the carve is per-op, not a global allowlist); (2) the importer's scoped rename for target.apply_promo_code produces params exposing `promo_code` and NOT `code`, which then pass assertCleanParams without throwing -- exercise this by extracting the real target.apply_promo_code descriptor via the importer's extractDescriptors('target') (the vendored real target slice exists after Plan 01) and asserting the emitted params have a `promo_code` property and no `code` property; (3) the eval-able names script/expr/transform/fn/js each STILL trip the pre-scan (re-assert via preScanForbidden on a planted fixture, confirming the carve left them always-fatal). Keep the zero-framework convention (PASS=/FAIL=, process.exit(1) on any failure). This test already runs in the npm test chain (no re-registration needed).
  </action>
  <verify>
    <automated>node tests/import-forbidden-prescan.test.js</automated>
  </verify>
  <acceptance_criteria>
    - the carve proof asserts target.apply_promo_code emits a `promo_code` field (renamed) and no `code` field, passing assertCleanParams
    - a generic `code` field still trips preScanForbidden (the carve is narrow)
    - script/expr/transform/fn/js still trip the pre-scan (always-fatal preserved)
    - `node tests/import-forbidden-prescan.test.js` exits 0
  </acceptance_criteria>
  <done>The forbidden-prescan test proves the target.apply_promo_code carve renames code->promo_code, keeps generic `code` + the eval-able names fatal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vendored real plugin params -> shipped descriptor | the importer's pre-scan is the Wall-1 gate; a forbidden eval-able field name must never reach a shipped descriptor |
| importer stem derivation -> emitted filename | a wrong/missing override silently overwrites another app's descriptor (data corruption, not a crash) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39.5-03 | Tampering | a STEM collision silently clobbers a descriptor (BLOCKER A) | mitigate | the complete ~40-entry STEM_OVERRIDES map (Task 1) + the no-duplicate-stem CI gate (Task 2) FAILS the build on any collision, re-deriving stems via the importer's own exports |
| T-39.5-04 | Elevation of Privilege | an eval-able input field name ships in a descriptor (Wall 1) | mitigate | the target.apply_promo_code carve renames ONLY the business `code` token (Task 1); script/expr/transform/fn/js stay in FORBIDDEN (un-allowlisted); the forbidden-prescan test (Task 3) re-asserts they are always-fatal |
| T-39.5-05 | Denial of Service | an empty-origin self-hosted app aborts the whole batch import (BLOCKER C) | mitigate | the empty-urlPatterns/sqlpad clean-skip (Task 1) replaces the throw, so a self-hosted app is skipped not crashed |
| T-39.5-SC | Tampering | npm/pip/cargo installs | accept | NO new packages this plan; @opentabs-dev/plugin-sdk + zod + tsx are already-installed pinned devDeps (RESEARCH/spike verified); no install task |
</threat_model>

<verification>
- `node scripts/verify-no-duplicate-stem.mjs` PASS over the real enumerated set (0 collisions)
- `node tests/no-duplicate-stem.test.js` passes (synthetic collision FAILS the gate)
- `node tests/import-forbidden-prescan.test.js` passes (the carve proof)
- the importer module imports cleanly; core path unchanged (only STEM_OVERRIDES + the scoped carve + the skip-set added)
- validate:extension + npm test register the new gate + tests
</verification>

<success_criteria>
SC2 is satisfied: STEM_OVERRIDES complete (~40, collision-free), the no-duplicate-stem CI gate fails on a synthetic collision and passes on the real map. BLOCKER B (promo_code carve) + BLOCKER C (grafana/sqlpad skip) handled. Core path untouched (INV-01 / Wall 1 hold).
</success_criteria>

<output>
Create `.planning/phases/39.5-full-opentabs-source-import/39.5-02-SUMMARY.md` when done.
</output>
