---
phase: 40-depth-1-top-read-hand-ports
plan: 05
type: execute
wave: 3
depends_on: ["40-02", "40-03", "40-04"]
files_modified:
  - .planning/phases/40-depth-1-top-read-hand-ports/40-05-SUMMARY.md
autonomous: true
requirements: [DEPTH-01]

must_haves:
  truths:
    - "All 10 ported READ slugs resolve T1a byte-exact (the upgrade harness is fully GREEN)"
    - "Both front doors hit the SAME FsbCapabilityRouter.invoke for the T1a heads (INV-02 parity)"
    - "The origin-pin rejects a mismatched origin BEFORE executeScript (Wall 2)"
    - "Head cap holds: HEAD_HANDLER_MODULES has 4 modules, <=30 (INV-01 frozen registry hash unmoved)"
    - "Full npm test EXITs 0 and validate:extension passes"
  artifacts:
    - path: ".planning/phases/40-depth-1-top-read-hand-ports/40-05-SUMMARY.md"
      provides: "the green-battery sign-off + the live-UAT debt handoff"
      min_lines: 20
  key_links:
    - from: "npm test"
      to: "the head-handler + router + cap + upgrade battery"
      via: "the full CI chain EXIT 0"
      pattern: "head-handler-upgrade"
---

<objective>
Final wave: run the FULL green battery proving the DEPTH-01 contract holds end-to-end across
BOTH the new-module (gitlab) and extend-module (slack/notion) paths, then record the
carried-forward live-UAT debt. No production code unless a gate fails — this plan is the
phase acceptance gate.

DELIVERS SC1 + SC2 + SC3 jointly: the upgrade harness GREEN proves all 10 slugs upgraded
dom->T1a byte-exact (SC1); the head-handlers source-scan + fetch origin-pin prove
executeBoundSpec-only/same-origin/no-token-logged/no-api-subdomain (SC2); the router-parity
+ head-cap + INV-01 hash prove one-engine-two-front-doors at <=30 modules (SC3).

This is the MECHANISM + security deliverable. Live endpoint correctness on authenticated
tabs is the sole documented human_needed item (40-VALIDATION.md Manual-Only) — the handlers
ship fail-closed so security holds regardless.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-depth-1-top-read-hand-ports/40-CONTEXT.md
@.planning/phases/40-depth-1-top-read-hand-ports/40-VALIDATION.md
@tests/capability-router.test.js
@tests/capability-autopilot-parity.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the head-handler + upgrade + cap + router battery green</name>
  <files>.planning/phases/40-depth-1-top-read-hand-ports/40-05-SUMMARY.md</files>
  <action>
    First ensure the ext copies are current: run `node scripts/package-extension.mjs` (copies
    all catalog/handlers/*.js -> extension/catalog/handlers/). Then run, and require EXIT 0
    on each: node tests/head-handler-upgrade.test.js (all 10 slugs resolve T1a byte-exact +
    the negative control + BEFORE/AFTER); node tests/capability-head-handlers.test.js (all
    per-app sections incl gitlab/slack/notion); node tests/head-handler-cap.test.js (4
    modules, CAP 30); node tests/capability-router.test.js (CAT-01/02 tier dispatch + the
    origin-pin RECIPE_ORIGIN_MISMATCH-before-executeScript + INV-02 parity); node
    tests/capability-autopilot-parity.test.js (INV-01: capability tools out of TOOL_REGISTRY,
    frozen non-trigger registry hash unmoved + both-front-doors parity). If any REDs, fix the
    offending handler source (NOT the router/catalog/fetch core — those are reused unchanged)
    and re-run. Record each command + its EXIT in 40-05-SUMMARY.md.
  </action>
  <verify>
    <automated>node tests/capability-router.test.js; echo EXIT=$?</automated>
  </verify>
  <done>head-handler-upgrade, capability-head-handlers, head-handler-cap, capability-router, capability-autopilot-parity all EXIT 0; INV-02 parity + origin-pin + INV-01 hash confirmed.</done>
</task>

<task type="auto">
  <name>Task 2: Full npm test + validate:extension green; write the sign-off SUMMARY</name>
  <files>.planning/phases/40-depth-1-top-read-hand-ports/40-05-SUMMARY.md</files>
  <action>
    Run `npm run validate:extension` (recipe-path-guard Wall-1 + classification-gate +
    crosscheck + no-duplicate-stem + no-orphan-descriptor) and require EXIT 0 — confirms the
    opentabs descriptors are UNCHANGED (still backing:'dom'; the heads upgrade via
    REGISTRY-first, no descriptor edit, no orphan, no dup-stem). Then run the full `npm test`
    and require EXIT 0. Write .planning/phases/40-depth-1-top-read-hand-ports/40-05-SUMMARY.md
    using the summary template: list the 10 upgraded slugs + their origins; confirm
    HEAD_HANDLER_MODULES=4 (<=30); confirm Wall 1 (no forbidden descriptor field, path-guard
    green), Wall 2 (origin-pin), INV-01 (no new MCP tool, frozen hash unmoved), INV-02
    (router parity) intact; record the app-selection correction (linear deferred to Phase 41
    as separate-API-origin/CORS; gitlab substituted) so the milestone log carries it; and
    record the carried-forward human_needed live-UAT debt (per-app authenticated-tab endpoint
    correctness + the logged-out-guard live confirmation) verbatim from 40-VALIDATION.md
    Manual-Only.
  </action>
  <verify>
    <automated>npm run validate:extension; echo EXIT=$?</automated>
  </verify>
  <done>validate:extension EXIT 0 and full npm test EXIT 0; 40-05-SUMMARY.md records the green battery, the 10 upgraded slugs, the head count, the invariants, the linear->gitlab correction, and the live-UAT debt handoff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| both front doors -> FsbCapabilityRouter.invoke | MCP dispatcher + autopilot must hit the SAME engine (no autopilot-only path) |
| active tab origin -> executeBoundSpec | the origin-pin is the single chokepoint for every credentialed head call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-13 | Elevation | an autopilot-only invoke path bypassing consent/pin | mitigate | capability-autopilot-parity.test.js asserts both doors hit the SAME router.invoke (INV-02) |
| T-40-14 | Tampering | a new MCP tool / moved registry hash (INV-01) | mitigate | the frozen EXPECTED_NON_TRIGGER_REGISTRY_HASH assertion stays green; capability tools stay out of TOOL_REGISTRY |
| T-40-15 | Spoofing | origin-pin regression | mitigate | capability-router.test.js + capability-fetch.test.js assert RECIPE_ORIGIN_MISMATCH fires before executeScript |
| T-40-16 | Information disclosure | a forbidden descriptor field (Wall 1) sneaking via a head | accept/verify | verify-recipe-path-guard.mjs green; heads are CODE not descriptors, descriptors unchanged |
| T-40-SC | Tampering | npm/pip/cargo installs | accept | no package installs in this phase (source + existing harnesses only); legitimacy gate N/A |
</threat_model>

<verification>
- node tests/head-handler-upgrade.test.js — EXIT 0, all 10 slugs T1a byte-exact.
- node tests/capability-head-handlers.test.js — EXIT 0, all per-app sections.
- node tests/head-handler-cap.test.js — EXIT 0, 4 modules / CAP 30.
- node tests/capability-router.test.js + node tests/capability-autopilot-parity.test.js — EXIT 0 (INV-02 + origin-pin + INV-01).
- npm run validate:extension — EXIT 0 (Wall 1 + crosscheck + no-orphan + no-dup-stem).
- npm test — EXIT 0.
</verification>

<success_criteria>
- The full battery (upgrade + head-handlers + cap + router + autopilot-parity + validate:extension + npm test) is EXIT 0.
- 10 READ slugs upgraded dom->T1a across the new-module (gitlab) + extend-module (slack/notion) paths.
- HEAD_HANDLER_MODULES=4 (<=30); INV-01/INV-02 + Wall 1/2 intact.
- 40-05-SUMMARY.md records the sign-off, the linear->gitlab correction, and the live-UAT debt.
</success_criteria>

<output>
Create `.planning/phases/40-depth-1-top-read-hand-ports/40-05-SUMMARY.md` when done.
</output>
