---
phase: 40-depth-1-top-read-hand-ports
plan: 04
type: execute
wave: 2
depends_on: ["40-01"]
files_modified:
  - catalog/handlers/notion.js
autonomous: true
requirements: [DEPTH-01]

must_haves:
  truths:
    - "2 notion READ slugs (notion.search, notion.get_database) ship as T1a heads on https://www.notion.so"
    - "Each notion slug UPGRADES its existing opentabs descriptor dom->T1a (slug byte-exact)"
    - "Reads reuse buildRpcSpec: POST same-origin /api/v3 RPC, token_v2 cookie rides automatically"
    - "A logged-out body is rejected (no 200-with-logged-out-body false success)"
  artifacts:
    - path: "catalog/handlers/notion.js"
      provides: "2 new notion READ T1a handlers via buildRpcSpec (no new module)"
      contains: "notion.search"
  key_links:
    - from: "catalog/handlers/notion.js"
      to: "FsbCapabilityCatalog.registerHandler"
      via: "self-registration of the 2 exact opentabs slugs at load"
      pattern: "registerHandler"
    - from: "catalog/handlers/notion.js notion.search"
      to: "buildRpcSpec"
      via: "reuse the existing /api/v3 RPC spec builder"
      pattern: "buildRpcSpec"
---

<objective>
EXTEND the existing FsbHandlerNotion head (catalog/handlers/notion.js) with the 2 top READ
opentabs slugs, reusing the proven buildRpcSpec contract (origin https://www.notion.so,
POST same-origin /api/v3 RPC, the token_v2 HttpOnly cookie rides the request automatically).
NO new module — these add slugs to the existing FsbHandlerNotion global, so
HEAD_HANDLER_MODULES count is UNCHANGED.

DELIVERS SC1 (2 of the 8-12 READ heads ship + upgrade), SC2 (executeBoundSpec-only via
buildRpcSpec, same-origin /api/v3, no token logged), SC3 (registered into the SAME catalog,
same front doors) for the extend-module path of the DEPTH-01 contract.

Notion is genuinely same-origin (www.notion.so/api/v3, verified in
vendor/opentabs-snapshot/plugins/notion/src/notion-api.ts:102). READ-only; notion writes
(create/update/delete) are Phase 41. The slugs are the EXACT opentabs DOT-form slugs
(notion.search, notion.get_database) so resolve() upgrades the breadth descriptor dom->T1a;
they are DISTINCT from the existing head slugs notion.getSpaces / notion.loadPage (no collision).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-depth-1-top-read-hand-ports/40-CONTEXT.md
@catalog/handlers/notion.js
@vendor/opentabs-snapshot/plugins/notion/src/notion-api.ts
@tests/capability-head-handlers.test.js

<interfaces>
Existing notion.js helper to REUSE unchanged: NOTION_ORIGIN='https://www.notion.so';
buildRpcSpec(op, requestBody) -> { url: NOTION_ORIGIN+'/api/v3/'+op, method:'POST',
headers:{Content-Type:application/json, Accept:application/json}, body:JSON.stringify(body),
query:{}, authStrategy:'same-origin-cookie', origin:NOTION_ORIGIN, extract:'@' }. Add slugs
by giving each a handle that calls ctx.executeBoundSpec(buildRpcSpec('<op>', <body>),
ctx.tabId) ONCE. The token_v2 cookie authenticates same-origin; no scraped token is needed.

The 2 ported notion slugs (each EXISTS as catalog/descriptors/opentabs__notion__*.json,
backing:'dom', sideEffectClass:'read'):
  notion.search        required query (+ limit)            -> /api/v3 search RPC (op e.g. 'search', body { query, limit })
  notion.get_database  required database_id                -> /api/v3 record-fetch RPC (op e.g. 'getRecordValues'/'syncRecordValues', body keyed by database_id)
(The exact /api/v3 op name + body shape are [ASSUMED] like the existing notion.getSpaces — carried-forward live UAT debt; the handler ships fail-closed via the logged-out guard.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 2 notion READ slugs to notion.js via buildRpcSpec</name>
  <files>catalog/handlers/notion.js</files>
  <behavior>
    - notion.search and notion.get_database each: tier 'T1a', origin 'https://www.notion.so',
      sideEffectClass 'read', async handle that calls executeBoundSpec exactly once with a
      buildRpcSpec POST to same-origin /api/v3.
    - params are closed JSON Schemas (additionalProperties:false): search requires query (+
      optional limit); get_database requires database_id.
    - LOGGED-OUT GUARD: after executeBoundSpec, a shape check rejects a logged-out/redirect
      body (e.g. require the extracted result to be a non-null object/array shaped like the
      RPC payload), returning the dual-field RECIPE_DOM_FALLBACK_PENDING (slug, reason
      'notion-logged-out-or-rot', fellBackToDom:true) on a wrong shape; success otherwise.
    - Source contains '/api/v3', does NOT contain 'api.notion.com', does NOT reference
      chrome.scripting/chrome.tabs; the 2 new slugs self-register (the existing registration
      loop already walks every key in the handlers object).
  </behavior>
  <action>
    Edit catalog/handlers/notion.js. Add closed params schemas SEARCH_PARAMS { query(req),
    limit } and GET_DATABASE_PARAMS { database_id(req) }. Add two entries to the handlers
    object mirroring the existing notion.getSpaces entry: each tier 'T1a', origin
    NOTION_ORIGIN, sideEffectClass 'read', params, and an async handle that builds the body
    and calls ctx.executeBoundSpec(buildRpcSpec('<op>', <body>), ctx.tabId), then applies the
    logged-out guard before returning. Use the search RPC op for notion.search (body { query:
    a.query, limit: a.limit||20 }) and a record-fetch RPC op for notion.get_database (body
    keyed by a.database_id), annotated [ASSUMED] exactly like notion.getSpaces/loadPage (live
    op-path correctness is carried-forward UAT debt). Do NOT modify buildRpcSpec, the existing
    two slugs, the registration loop, or the dual export — the loop and exports already cover
    new keys. ASCII-only, NO emojis.
  </action>
  <verify>
    <automated>node tests/capability-head-handlers.test.js; echo EXIT=$?</automated>
  </verify>
  <done>notion.js exposes the 2 new READ slugs as T1a on www.notion.so via buildRpcSpec with a logged-out guard; the notion section of capability-head-handlers.test.js passes (gitlab/slack sections may RED until 40-02/40-03).</done>
</task>

<task type="auto">
  <name>Task 2: Build the ext copy + prove the notion dom->T1a upgrade</name>
  <files>catalog/handlers/notion.js</files>
  <action>
    Run `node scripts/package-extension.mjs` so the edited catalog/handlers/notion.js is
    copied to extension/catalog/handlers/notion.js (the head-handlers test asserts they
    match). Then run the 40-01 upgrade harness to prove the 2 notion slugs now resolve T1a
    (was dom/T3) with the descriptor preserved and slug byte-exact. If a notion row REDs, the
    likely cause is a slug typo (a dead duplicate, not an upgrade) — fix the slug string in
    notion.js to byte-match the opentabs descriptor and re-run. No HEAD_HANDLER_MODULES change
    (FsbHandlerNotion already in the manifest); no background.js change (already imported).
  </action>
  <verify>
    <automated>node tests/head-handler-upgrade.test.js; echo EXIT=$?</automated>
  </verify>
  <done>The 2 notion slugs resolve T1a with byte-exact descriptor slugs in the upgrade harness; the ext copy matches the source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI-chosen args -> notion.js handle | untrusted slug args cross into a credentialed same-origin RPC |
| page MAIN world -> token_v2 cookie | the same-origin HttpOnly cookie authenticates the /api/v3 RPC |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-09 | Spoofing | spec.origin vs active tab | mitigate | NOTION_ORIGIN spec.origin via buildRpcSpec; executeBoundSpec origin-pin rejects a mismatch before executeScript |
| T-40-10 | Information disclosure | separate-API host slip | mitigate | reuse buildRpcSpec (/api/v3 same-origin only); source asserts NO 'api.notion.com' |
| T-40-11 | Spoofing | 200-with-logged-out-body as success | mitigate | per-handler logged-out shape guard returns RECIPE_DOM_FALLBACK_PENDING (CONTEXT Top Risk) |
| T-40-12 | Elevation | a write RPC slipping in as read | mitigate | only the search + record-fetch READ RPCs ported; writes are Phase 41 |
| T-40-SC | Tampering | npm/pip/cargo installs | accept | no package installs in this plan (extends existing source) |
</threat_model>

<verification>
- node tests/capability-head-handlers.test.js — the notion section passes.
- node tests/head-handler-upgrade.test.js — the 2 notion slugs resolve T1a byte-exact.
- node scripts/package-extension.mjs then npm run validate:extension — gates green; ext copy matches.
- HEAD_HANDLER_MODULES count UNCHANGED (no new module). Wall 2 origin-pin holds; token_v2 stays cookie-borne.
</verification>

<success_criteria>
- 2 notion READ slugs are T1a heads on https://www.notion.so via buildRpcSpec, each upgrading its opentabs descriptor.
- Same-origin /api/v3 only, no token logged, logged-out body rejected.
- No new HEAD_HANDLER_MODULES entry.
</success_criteria>

<output>
Create `.planning/phases/40-depth-1-top-read-hand-ports/40-04-SUMMARY.md` when done.
</output>
