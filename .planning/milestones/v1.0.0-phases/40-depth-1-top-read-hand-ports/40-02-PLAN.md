---
phase: 40-depth-1-top-read-hand-ports
plan: 02
type: execute
wave: 2
depends_on: ["40-01"]
files_modified:
  - catalog/handlers/gitlab.js
  - extension/utils/capability-catalog.js
  - extension/background.js
autonomous: true
requirements: [DEPTH-01]

must_haves:
  truths:
    - "5 gitlab READ slugs ship as T1a heads on the first-party origin https://gitlab.com"
    - "Each gitlab slug UPGRADES its existing opentabs descriptor dom->T1a (slug byte-exact)"
    - "FsbHandlerGitlab is registered in HEAD_HANDLER_MODULES and loaded via background.js importScripts"
    - "Every gitlab read calls ctx.executeBoundSpec exactly once; no api-subdomain, no chrome.scripting/tabs"
    - "A logged-out body is rejected by an expectedShape/extract guard (no 200-with-logged-out-body success)"
  artifacts:
    - path: "catalog/handlers/gitlab.js"
      provides: "5 gitlab READ T1a handlers (GitHub-GET template), same-origin gitlab.com/api/v4"
      min_lines: 90
      contains: "gitlab.list_projects"
    - path: "extension/utils/capability-catalog.js"
      provides: "HEAD_HANDLER_MODULES gains FsbHandlerGitlab (4th module, <=30)"
      contains: "FsbHandlerGitlab"
  key_links:
    - from: "catalog/handlers/gitlab.js"
      to: "FsbCapabilityCatalog.registerHandler"
      via: "self-registration of the 5 exact opentabs slugs at load"
      pattern: "registerHandler"
    - from: "extension/background.js"
      to: "catalog/handlers/gitlab.js"
      via: "importScripts after notion.js (line ~193)"
      pattern: "handlers/gitlab.js"
---

<objective>
Port GitLab's top 5 READ ops as first-class T1a heads via the GitHub-GET contract, on
GitLab's OWN first-party origin https://gitlab.com (same-origin REST at /api/v4 — verified
in vendor/opentabs-snapshot/plugins/gitlab/src/gitlab-api.ts:10-14), each UPGRADING its
existing opentabs__gitlab__* descriptor dom->T1a by registering the EXACT slug.

DELIVERS SC1 (5 of the 8-12 READ heads ship + upgrade), SC2 (executeBoundSpec-only,
same-origin, no token logged), SC3 (4th HEAD_HANDLER_MODULES entry, <=30, loaded via
background.js) for the new-module path of the DEPTH-01 contract.

GitLab is the same-origin replacement for the deferred linear (see 40-01 decision_note):
its API is gitlab.com/api/v4 (same host + /api path), so the Wall-2 origin-pin passes with
spec.origin=https://gitlab.com — NO CORS, NO separate subdomain. READ-only; mutating ops
(create/update/merge) are Phase 41.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-depth-1-top-read-hand-ports/40-CONTEXT.md
@catalog/handlers/github.js
@vendor/opentabs-snapshot/plugins/gitlab/src/gitlab-api.ts
@tests/capability-head-handlers.test.js

<interfaces>
GitHub-GET template (catalog/handlers/github.js): IIFE on (global); per-slug entry
{ tier:'T1a', origin, sideEffectClass:'read', params:<closed JSON Schema>, async
handle(args,ctx) }; handle builds ONE spec { url, method:'GET', headers:{Accept:
'application/json'}, body:null, query:{}, authStrategy:'same-origin-cookie', origin,
extract } and calls ctx.executeBoundSpec(spec, ctx.tabId) ONCE; self-registers each slug via
FsbCapabilityCatalog.registerHandler (typeof-guarded); exports global.FsbHandlerGitlab +
module.exports. NO chrome.*, eval-free, ASCII-only, NO emojis.

Vendored GitLab reality (gitlab-api.ts): base https://gitlab.com/api/v4 (same-origin with a
gitlab.com tab); reads are GET with the same-origin session cookie; window.gon.current_username
indicates login; <meta name="csrf-token"> is only needed for MUTATING requests (out of scope
this phase). buildUrl appends ?query params via URLSearchParams.

The 5 ported gitlab slugs (each EXISTS as catalog/descriptors/opentabs__gitlab__*.json,
backing:'dom', sideEffectClass:'read'; props shown):
  gitlab.list_projects       props membership,owned,search,visibility,order_by,sort,per_page,page  (none required)  -> GET /api/v4/projects
  gitlab.get_project         required project                                                       -> GET /api/v4/projects/:id
  gitlab.list_issues         required project (+ state,labels,assignee_username,search,...)          -> GET /api/v4/projects/:id/issues
  gitlab.get_issue           required project,issue_iid                                              -> GET /api/v4/projects/:id/issues/:iid
  gitlab.list_merge_requests required project (+ state,labels,author_username,...)                   -> GET /api/v4/projects/:id/merge_requests
(:id = URL-encoded project path or numeric id; mirror buildUrl's encodeURIComponent + URLSearchParams.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create catalog/handlers/gitlab.js (5 READ T1a handlers, same-origin /api/v4)</name>
  <files>catalog/handlers/gitlab.js</files>
  <behavior>
    - Module exports the 5 slugs (gitlab.list_projects, gitlab.get_project, gitlab.list_issues,
      gitlab.get_issue, gitlab.list_merge_requests); each is tier 'T1a', origin
      'https://gitlab.com', sideEffectClass 'read', with an async handle.
    - Each handle builds exactly ONE spec with method 'GET', origin 'https://gitlab.com', url
      under '/api/v4', authStrategy 'same-origin-cookie', and calls executeBoundSpec once.
    - params are closed JSON Schemas (additionalProperties:false) requiring the documented
      required props (project for get/list-scoped; project+issue_iid for get_issue).
    - LOGGED-OUT GUARD: an expectedShape/extract rejects a logged-out body so a 200 with a
      sign-in HTML page is NOT reported as success (per CONTEXT Top Risk). Realize this as a
      post-result shape check in handle (e.g. require the extracted data to be an array for
      list_* / an object carrying an id for get_*) returning the dual-field
      RECIPE_DOM_FALLBACK_PENDING failure (slug, reason 'gitlab-logged-out-or-rot',
      fellBackToDom:true) when the shape is wrong; success otherwise.
    - Source contains '/api/v4', does NOT contain 'api.gitlab.com', does NOT reference
      chrome.scripting/chrome.tabs, no console.* names a token/cookie variable.
  </behavior>
  <action>
    Copy the github.js shape verbatim in STRUCTURE (the IIFE shell, typedRecipeError helper,
    the handlers object, the self-registration loop, the dual export). Set GITLAB_ORIGIN =
    'https://gitlab.com'. For each of the 5 slugs author a closed params schema from the
    descriptor props in <interfaces> and a handle that builds the GET url under
    GITLAB_ORIGIN + '/api/v4/...': encodeURIComponent the project path/id; for get_issue
    append '/issues/' + issue_iid; for the list_* ops fold the optional filter props into a
    querystring with URLSearchParams-equivalent encoding (mirror gitlab-api.ts buildUrl,
    lines 50-61). Set spec.extract to a JMESPath that yields the payload to shape-check (e.g.
    '@'); after executeBoundSpec, apply the logged-out guard from <behavior> before returning
    success. Headers: { 'Accept': 'application/json' }, body:null, authStrategy
    'same-origin-cookie'. Self-register all 5 via FsbCapabilityCatalog.registerHandler with
    descriptor { slug, service:'gitlab.com', sideEffectClass:'read', params } (typeof-guarded).
    Export global.FsbHandlerGitlab + module.exports. ASCII-only, NO emojis. Annotate the
    [ASSUMED] internal-path note exactly like github.js (live correctness is carried-forward
    UAT debt; the handler ships fail-closed).
  </action>
  <verify>
    <automated>node tests/capability-head-handlers.test.js; echo EXIT=$?</automated>
  </verify>
  <done>catalog/handlers/gitlab.js exists with 5 T1a READ slugs on https://gitlab.com/api/v4, executeBoundSpec-only, logged-out guard, no api-subdomain/chrome.*; the gitlab section of capability-head-handlers.test.js passes (the slack/notion extend sections may still RED until 40-03/40-04).</done>
</task>

<task type="auto">
  <name>Task 2: Register FsbHandlerGitlab in HEAD_HANDLER_MODULES + load via background.js</name>
  <files>extension/utils/capability-catalog.js, extension/background.js</files>
  <action>
    (A) extension/utils/capability-catalog.js — append to the HEAD_HANDLER_MODULES array
    (lines 241-245) a 4th entry: { global: 'FsbHandlerGitlab', service: 'gitlab.com', origin:
    'https://gitlab.com' }. This keeps the count at 4 (<=30) and lets seedHeadHandlers() seed
    the gitlab slugs from the authoritative manifest (defense-in-depth alongside the module
    self-register). Do NOT change registerHandler/seedHeadHandlers/resolve logic.
    (B) extension/background.js — add, immediately AFTER the notion.js importScripts line
    (~line 193), a try/catch importScripts('catalog/handlers/gitlab.js') line mirroring the
    existing three (independent try/catch so an absent handler degrades to RECIPE_NOT_FOUND,
    never a load crash). The existing seedHeadHandlers() call below it then re-asserts gitlab.
    Build step: run `node scripts/package-extension.mjs` so catalog/handlers/gitlab.js is
    copied to extension/catalog/handlers/gitlab.js (the head-handlers test asserts the ext
    copy matches the source).
  </action>
  <verify>
    <automated>node tests/head-handler-cap.test.js; echo EXIT=$?</automated>
  </verify>
  <done>HEAD_HANDLER_MODULES has 4 entries incl FsbHandlerGitlab; background.js importScripts loads gitlab.js; the ext copy exists; head-handler-cap.test.js passes (count 4, CAP 30).</done>
</task>

<task type="auto">
  <name>Task 3: Prove the gitlab dom->T1a upgrade + run the wave gate</name>
  <files>catalog/handlers/gitlab.js</files>
  <action>
    No new code unless a gate fails. Run the 40-01 upgrade harness and the catalog gates to
    prove the 5 gitlab slugs now resolve T1a (was dom/T3) with the descriptor preserved and
    the slug byte-exact, and that no descriptor/crosscheck gate regressed (the opentabs
    gitlab descriptors are UNCHANGED — still backing:'dom' — and resolve() upgrades them via
    REGISTRY-first; the head wins). If the upgrade harness REDs for any gitlab slug, the most
    likely cause is a slug typo in gitlab.js (a dead duplicate, not an upgrade) — fix the
    slug string in catalog/handlers/gitlab.js to byte-match the descriptor and re-run.
    Re-run the package build first if the ext copy is stale.
  </action>
  <verify>
    <automated>node tests/head-handler-upgrade.test.js; echo EXIT=$?</automated>
  </verify>
  <done>All 5 gitlab slugs resolve T1a with byte-exact descriptor slugs in the upgrade harness (the gitlab rows now PASS); validate:extension catalog gates unregressed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI-chosen args -> gitlab.js handle | untrusted slug args cross into a credentialed same-origin fetch |
| page MAIN world -> gitlab.com session cookie | the same-origin cookie authenticates the /api/v4 read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | Spoofing | gitlab.js spec.origin vs active tab | mitigate | spec.origin='https://gitlab.com'; executeBoundSpec origin-pin (capability-fetch.js:299) rejects a mismatch BEFORE executeScript |
| T-40-02 | Information disclosure | separate-API host slip | mitigate | source asserts NO 'api.gitlab.com'; only same-origin /api/v4 (head-handlers test source-scan) |
| T-40-03 | Elevation | a mutating op slipping in as read | mitigate | only the 5 READ slugs ported; GET-only specs; writes are Phase 41 |
| T-40-04 | Spoofing | 200-with-logged-out-body masquerading as success | mitigate | per-handler logged-out shape guard returns RECIPE_DOM_FALLBACK_PENDING (CONTEXT Top Risk) |
| T-40-SC | Tampering | npm/pip/cargo installs | accept | no package installs in this plan (pure source + existing test harness); legitimacy gate N/A |
</threat_model>

<verification>
- node tests/capability-head-handlers.test.js — the gitlab section passes.
- node tests/head-handler-cap.test.js — 4 modules, CAP 30.
- node tests/head-handler-upgrade.test.js — the 5 gitlab slugs resolve T1a byte-exact.
- node scripts/package-extension.mjs then npm run validate:extension — catalog gates green.
- Wall 2 holds (origin-pin); Wall 1 unaffected (no descriptor field added); INV-01/02 untouched.
</verification>

<success_criteria>
- 5 gitlab READ slugs are T1a heads on https://gitlab.com, each upgrading its opentabs descriptor.
- FsbHandlerGitlab is the 4th HEAD_HANDLER_MODULES entry (<=30) and loads via background.js.
- executeBoundSpec-only, same-origin /api/v4, no token logged, logged-out body rejected.
</success_criteria>

<output>
Create `.planning/phases/40-depth-1-top-read-hand-ports/40-02-SUMMARY.md` when done.
</output>
