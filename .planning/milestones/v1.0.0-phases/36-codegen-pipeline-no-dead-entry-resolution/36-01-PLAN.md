---
phase: 36-codegen-pipeline-no-dead-entry-resolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/import-opentabs-catalog.mjs
  - vendor/opentabs-snapshot/plugins/todoist/package.json
  - vendor/opentabs-snapshot/plugins/todoist/src/index.ts
  - vendor/opentabs-snapshot/plugins/todoist/src/tools/*.ts
  - catalog/descriptors/_fixtures/_provenance.json
  - extension/catalog/recipe-index.generated.js
  - package.json
  - tests/import-extraction.test.js
  - tests/import-forbidden-prescan.test.js
  - tests/import-classify-gate-call.test.js
  - scripts/verify-catalog-crosscheck.mjs
  - tests/catalog-crosscheck.test.js
  - tests/no-dead-entry.test.js
  - tests/catalog-inline-shape.test.js
  - tests/head-handler-cap.test.js
autonomous: true
requirements: [CGEN-01]
user_setup: []

must_haves:
  truths:
    - "Running `node --import tsx scripts/import-opentabs-catalog.mjs` emits flat provenance-stamped descriptor JSON for the smoke app (todoist) under catalog/descriptors/ (NOT a subdir)"
    - "Each emitted descriptor's params is z.toJSONSchema(tool.input) with $schema stripped and additionalProperties:false (the closed-vocab contract), carrying provenance {source:opentabs, sha, sourcePath, license, signals}"
    - "The forbidden-field recursive pre-scan rejects any emitted descriptor containing a field named script/expr/transform/code/fn/js at ANY depth (top/nested/array/union/$defs) and passes a clean descriptor"
    - "The importer calls classifyGate() (after await Denylist.load()) BEFORE writing any JSON and aborts the emit if an unclassified sensitive origin is present"
    - "zod and tsx are devDependencies ONLY (not dependencies); no runtime opentabs/sdk/zod ships; verify-recipe-path-guard.mjs stays green"
    - "All Phase-36 test files + verify-catalog-crosscheck.mjs are registered in package.json (test + validate:extension) as passing stubs so CI stays green across every wave"
    - "After the emit, `node scripts/package-extension.mjs` regenerates extension/catalog/recipe-index.generated.js so the committed snapshot includes the emitted todoist descriptors and `node scripts/validate-extension.mjs` reconciles (exit 0, NO stale-snapshot failure) at Plan 01's end"
  artifacts:
    - path: "scripts/import-opentabs-catalog.mjs"
      provides: "build-time tsx importer: extraction + recursive pre-scan + classifyGate-before-emit + provenance stamp + flat emit"
      min_lines: 80
    - path: "catalog/descriptors/opentabs__todoist__create_task.json"
      provides: "one emitted flat smoke descriptor (representative of the emitted set)"
      contains: "additionalProperties"
    - path: "catalog/descriptors/_fixtures/_provenance.json"
      provides: "apps[] filled with per-descriptor provenance for the smoke app"
      contains: "todoist"
    - path: "extension/catalog/recipe-index.generated.js"
      provides: "the catalog snapshot REGENERATED via package-extension.mjs after the emit so validate:extension's byte-compare reconciles (committed with the emit, no stale-snapshot fail)"
      contains: "todoist"
    - path: "tests/import-forbidden-prescan.test.js"
      provides: "the 6-position recursive pre-scan fixture proof (top/nested/array/union/$defs + clean)"
      exports: []
    - path: "scripts/verify-catalog-crosscheck.mjs"
      provides: "passing STUB (exit 0) registered in validate:extension; Plan 03 fills the real cross-check"
      contains: "process.exit"
  key_links:
    - from: "scripts/import-opentabs-catalog.mjs"
      to: "scripts/verify-classification-gate.mjs"
      via: "import { classifyGate } + await Denylist.load() before emit"
      pattern: "classifyGate"
    - from: "scripts/import-opentabs-catalog.mjs"
      to: "catalog/descriptors/"
      via: "flat write opentabs__<service>__<op>.json (readJsonDir is non-recursive)"
      pattern: "catalog/descriptors"
    - from: "scripts/package-extension.mjs"
      to: "extension/catalog/recipe-index.generated.js"
      via: "regen after emit -> readJsonDir(catalog/descriptors) inlines the new todoist descriptors so validate-extension.mjs byte-compare reconciles (Task 4)"
      pattern: "recipe-index.generated|package-extension"
    - from: "package.json"
      to: "all Phase-36 test files + verify-catalog-crosscheck.mjs"
      via: "test chain + validate:extension chain (stubs keep CI green)"
      pattern: "no-dead-entry|catalog-crosscheck|head-handler-cap"
---

<objective>
Build the build-time OpenTabs descriptor importer (CGEN-01): load the pinned smoke-app
(todoist) plugin metadata under tsx, extract each op into a closed-vocab `params` descriptor
via `z.toJSONSchema`, run the Wall-1 recursive forbidden-field pre-scan, call the Phase-35
`classifyGate()` denylist floor BEFORE emitting, and write FLAT provenance-stamped descriptor
JSON. THEN regenerate the committed catalog snapshot (`extension/catalog/recipe-index.generated.js`)
via `package-extension.mjs` so the emit and the snapshot reconcile and `validate:extension` is
green at this plan's end (no stale-snapshot failure). Also harden the dependency surface
(zod/tsx -> devDependencies only) and scaffold the package.json test/validate registration for the
whole phase as passing stubs so CI stays green at every wave.

Purpose: this is the load-bearing emit path. Phases 37-39 reuse it batch by batch; everything
downstream (the cross-check gate, the no-dead-entry resolve, the inline+eval) consumes what
this emits. Getting the closed-params contract + the Wall-1 pre-scan + the denylist-first gate
right here is the whole point of Phase 36 (prove the machinery, not the content). Co-locating the
snapshot regen here (not deferring it to Wave 2) keeps THIS plan's own validate:extension
assertion satisfiable the moment the descriptors land.

Output: `scripts/import-opentabs-catalog.mjs`; the vendored todoist metadata; flat
`catalog/descriptors/opentabs__todoist__*.json`; the filled `_fixtures/_provenance.json apps[]`;
the REGENERATED `extension/catalog/recipe-index.generated.js` snapshot (reconciled with the emit);
the 3 importer tests; package.json deps hardened + every Phase-36 test/script registered as a
passing stub.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-codegen-pipeline-no-dead-entry-resolution/36-CONTEXT.md
@.planning/phases/36-codegen-pipeline-no-dead-entry-resolution/36-RESEARCH.md

<interfaces>
<!-- Contracts the importer consumes. Extracted from the codebase + RESEARCH. Use directly. -->

classifyGate (scripts/verify-classification-gate.mjs, ESM):
  export function classifyGate(items, opts) -> { failures: string[] }
  - items: Array<{ origin: string, service?: string, slug?: string, description?: string }>
  - opts.safeAllowlist: optional Set|array of curated-benign origins
  - MUST `await Denylist.load()` (extension/utils/service-denylist.js) BEFORE calling, else
    classify() reads an empty roster and everything fails-closed (the safe direction).
  export function sensitivityHeuristic(host, slug, description)

Denylist (extension/utils/service-denylist.js, dual-export IIFE -> module.exports for Node):
  load() -> Promise   (populate classify() from the committed roster)
  classify(origin) -> { sensitive, denied, reason? }

z.toJSONSchema(tool.input) EMPIRICALLY-VERIFIED output shapes (zod@4.4.3, RESEARCH Mechanic 1):
  z.object({a, b:opt})        -> {type:object, properties:{...}, required:[a], additionalProperties:false}
  z.string().default('hi')    -> {default:'hi', type:string}  AND key STAYS in required
  z.union([str,num])          -> {anyOf:[{type:string},{type:number}]}
  z.enum([...])               -> {type:string, enum:[...]}
  z.record(z.string(),z.string()) -> {type:object, propertyNames:{type:string}, additionalProperties:{type:string}}
  z.string().nullable()       -> {anyOf:[{type:string},{type:null}]}
  z.array(z.string())         -> {type:array, items:{type:string}}
  z.lazy(...) default         -> {$ref:#/$defs/__schemaN} + $defs   (use default cycles:'ref', NEVER 'throw')
  .transform()/.pipe()        -> THROWS  (let it throw -> loud import failure, the safe direction)
  plain z.object() already emits additionalProperties:false  => the closed `params` contract for free; just delete params.$schema

OpenTabs plugin metadata read at build (NEVER run handle bodies):
  package.json.opentabs.urlPatterns -> service/origin
  import() src/index.ts -> plugin.tools[]; per tool read .name/.description/.input/.group/.summary ONLY

Existing flat descriptor shape (reference, VERIFIED): catalog/descriptors/github-issues-create.json
  has "additionalProperties": false ; flat sibling of github-issues.json etc.

readJsonDir (scripts/package-extension.mjs:51-54) is NON-RECURSIVE -> a catalog/descriptors/opentabs/
SUBDIR would be silently dropped. EMIT FLAT: catalog/descriptors/opentabs__<service>__<op>.json
(filename namespace), provenance carried INSIDE each descriptor. (RESOLVED A1; refinement of
CONTEXT Area-1-Q3.)

Snapshot reconcile seam (scripts/package-extension.mjs:51-89 + scripts/validate-extension.mjs:80-105):
  package-extension.mjs regenerates extension/catalog/recipe-index.generated.js by
    readJsonDir(catalog/descriptors) -> JSON.stringify({recipes,descriptors}) into the IIFE DATA literal.
  validate-extension.mjs BYTE-COMPARES the git-tracked recipe-index.generated.js (via require()) against a
    FRESH {recipes: readJsonDir(recipes), descriptors: readJsonDir(descriptors)} and FAILS "stale" if they differ.
  => emitting opentabs__todoist__*.json grows readJsonDir(descriptors) from 8 -> 9+, so the committed snapshot
    MUST be regenerated in THIS plan (Task 4) or validate:extension fails stale. The IIFE wrapper + djb2 tail are
    UNCHANGED (INV-01) -- only the DATA literal grows. Plan 04 (Wave 2) VERIFIES this reconciled snapshot's shape;
    it does NOT re-own the regen.

Provenance SHA (pinned, hermetic): 4b17021637d2cac12b8d84d21c40e765aa7b85e9 ; license MIT.
The vendor pin lives at vendor/opentabs-snapshot/_provenance.json; the catalog scaffold is
catalog/descriptors/_fixtures/_provenance.json with "apps": [] (Phase 36 fills it).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden dependency surface + scaffold the phase-wide package.json registration (Wall-1 + CI green)</name>
  <files>package.json, scripts/verify-catalog-crosscheck.mjs, tests/import-extraction.test.js, tests/import-forbidden-prescan.test.js, tests/import-classify-gate-call.test.js, tests/catalog-crosscheck.test.js, tests/no-dead-entry.test.js, tests/catalog-inline-shape.test.js, tests/head-handler-cap.test.js</files>
  <read_first>
    - package.json (lines 17 `test` chain; line 31 `validate:extension`; lines 88-89 zod/tsx CURRENTLY under `dependencies` — a Wall-1 risk surface)
    - 36-RESEARCH.md Standard Stack (zod/tsx/@opentabs-dev/plugin-sdk are BUILD-TIME devDependencies ONLY) + Package Legitimacy Audit (jiti is [SUS], EXCLUDED; tsx is the loader; sdk pinned exact, devDep only) + Pitfall 5 (shipping zod/sdk is a Wall-1 breach)
    - 36-VALIDATION.md "Wave 0 Gaps" (the exact test file list) + the convention: standalone node scripts, PASS=/FAIL=, process.exit(1)
  </read_first>
  <action>
    Move `zod` (^4.4.3) and `tsx` (^4.22.4) OUT of `dependencies` and INTO `devDependencies` (they are build-time only; shipping them breaches Wall 1). Add `@opentabs-dev/plugin-sdk` at exact `0.0.113` to `devDependencies` (resolves `defineTool`/`OpenTabsPlugin` when importing a plugin module; devDep only, never shipped). Do NOT add jiti (slopcheck flagged it [SUS] typosquat-near "vite"). Run `npm install` to refresh the lockfile, then confirm zod/tsx/sdk appear ONLY under devDependencies.

    Create passing STUB files (each exits 0 with a single `console.log('PASS (stub) — filled by Plan NN')`) so they can be registered NOW and CI stays green at every wave; later plans replace the bodies in-place WITHOUT re-touching package.json:
    - `tests/import-extraction.test.js`, `tests/import-forbidden-prescan.test.js`, `tests/import-classify-gate-call.test.js` (this plan fills these in Tasks 2-3),
    - `tests/catalog-crosscheck.test.js` (Plan 03 fills),
    - `tests/no-dead-entry.test.js` (Plan 02 fills),
    - `tests/catalog-inline-shape.test.js`, `tests/head-handler-cap.test.js` (Plan 04 fills),
    - `scripts/verify-catalog-crosscheck.mjs` — a STUB that `process.exit(0)` AND dual-exports a no-op `crossCheck` (Plan 03 replaces the body with the real derived-vs-declared fail-safe-high gate).

    Register every one of those 7 test files into the package.json `test` chain (append `&& node tests/<name>.test.js` after the existing `capability-rot-detector.test.js` tail), and register `scripts/verify-catalog-crosscheck.mjs` into `validate:extension` AFTER `verify-classification-gate.mjs` (the chain becomes `... && node scripts/verify-classification-gate.mjs && node scripts/verify-catalog-crosscheck.mjs`). This plan OWNS package.json for the entire phase; no other Phase-36 plan edits it. Because the stubs all exit 0, `npm test` and `npm run validate:extension` stay green from Wave 1 onward.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const bad=(p.dependencies||{}).zod||(p.dependencies||{}).tsx; const ok=(p.devDependencies||{}).zod&&(p.devDependencies||{}).tsx&&(p.devDependencies||{})['@opentabs-dev/plugin-sdk']; if(bad||!ok){console.error('FAIL deps placement');process.exit(1)} console.log('PASS deps placement')"</automated>
    <automated>npm run validate:extension</automated>
    <automated>node scripts/verify-recipe-path-guard.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node -e` deps-placement check prints `PASS deps placement` (zod/tsx/@opentabs-dev/plugin-sdk under devDependencies; zod/tsx absent from dependencies).
    - `npm run validate:extension` exits 0 (the registered `verify-catalog-crosscheck.mjs` stub exits 0; recipe-path guard + classification gate still green; the snapshot is still in sync at this point because no descriptors have been emitted yet).
    - `node scripts/verify-recipe-path-guard.mjs` exits 0 (no shipped file imports zod/sdk).
    - `package.json` `test` string contains all 7 new test filenames; `validate:extension` contains `verify-catalog-crosscheck.mjs` after `verify-classification-gate.mjs`.
  </acceptance_criteria>
  <done>zod/tsx are devDeps + plugin-sdk added; the 7 stub test files + the crosscheck stub exist and exit 0; package.json registers them all; validate:extension + recipe-path guard green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Vendor the todoist smoke metadata + write the importer (extraction + provenance + flat emit + classifyGate-before-emit)</name>
  <files>vendor/opentabs-snapshot/plugins/todoist/package.json, vendor/opentabs-snapshot/plugins/todoist/src/index.ts, vendor/opentabs-snapshot/plugins/todoist/src/tools/*.ts, scripts/import-opentabs-catalog.mjs, catalog/descriptors/_fixtures/_provenance.json, tests/import-extraction.test.js, tests/import-classify-gate-call.test.js</files>
  <behavior>
    - Importer extracts a todoist op into a descriptor whose `params` equals z.toJSONSchema(tool.input) with $schema deleted and additionalProperties:false present.
    - Descriptor carries provenance {source:'opentabs', sha:'4b17021637d2cac12b8d84d21c40e765aa7b85e9', sourcePath:'plugins/todoist/src/tools/<op>.ts', license:'MIT', signals:{transportHelper, httpMethod, opNameVerb}}.
    - slug is `todoist.<op_name>` (OpenTabs `app__op` -> FSB `app.op` deterministic map); the written filename is `opentabs__todoist__<op>.json` FLAT under catalog/descriptors/.
    - The importer calls classifyGate([{origin,service,slug,description}]) after `await Denylist.load()` and THROWS/aborts (writes nothing) if failures.length > 0; todoist (benign per the Phase-35 119-app sweep) produces zero failures and emits.
    - `_fixtures/_provenance.json` apps[] is filled with a per-app entry recording which descriptors came from todoist + the SHA.
  </behavior>
  <read_first>
    - 36-RESEARCH.md "Code Examples — The importer skeleton" (the EXACT path: import z from 'zod'; import classifyGate; await Denylist.load(); z.toJSONSchema; delete params.$schema; provenance block with signals; classifyGate-before-emit; flat write) + Mechanic 1 (the verified shapes, the additionalProperties:false-by-default fact, slug+provenance rule) + Pitfall 1 (FLAT emit; readJsonDir non-recursion) + Pitfall 5 (devDeps only)
    - 36-CONTEXT.md Area 1 (zod->params flattening permissive; provenance SHA; Wall-1 runtime)
    - vendor/opentabs-snapshot/_provenance.json (the authoritative pin — match its SHA) + vendor/opentabs-snapshot/PIN.md
    - catalog/descriptors/_fixtures/_provenance.json (the `"apps": []` scaffold to fill; read its `_note` — Phase 36 extends apps[])
    - catalog/descriptors/github-issues-create.json (the closed-params descriptor shape to match)
    - the OpenTabs todoist plugin source upstream (pinned SHA 4b170216): package.json.opentabs.urlPatterns + src/tools/*.ts op definitions (`.name`/`.description`/`.input` zod schemas/transport helper) — vendor a metadata-only slice (NO dist/, NO handle runtime; Wall 1)
  </read_first>
  <action>
    Vendor a metadata-only slice of the OpenTabs todoist plugin under
    `vendor/opentabs-snapshot/plugins/todoist/` — `package.json` (with `opentabs.urlPatterns` ->
    the todoist origin) + `src/index.ts` (the plugin definition exporting `tools`) + the op files
    under `src/tools/` that define `.name`/`.description`/`.input` (zod). Ship NO `dist/`, NO
    `handle()` runtime that touches `fetch`/`document` at build (the importer reads metadata only;
    handle bodies are never executed). If `@opentabs-dev/plugin-sdk`'s real `defineTool` is
    insufficient or drags transitive DOM/fetch surface, vendor a ~30-line `sdk-stub.ts`
    (`defineTool = c => c`; abstract `OpenTabsPlugin`) and import that instead (hardening per RESEARCH Alternatives).

    Write `scripts/import-opentabs-catalog.mjs` (run via `node --import tsx ./scripts/import-opentabs-catalog.mjs`):
    `import { z } from 'zod'`; `import { classifyGate } from './verify-classification-gate.mjs'`;
    `import Denylist from '../extension/utils/service-denylist.js'`. `await Denylist.load()`. For
    each plugin: read `package.json.opentabs.urlPatterns` -> service/origin; `await import()` the
    plugin index (tsx transpiles TS on import); for each `tool` in `plugin.tools` read ONLY
    `.name/.description/.input/.group/.summary`. Compute `params = z.toJSONSchema(tool.input)` (default
    target draft-2020-12, default `cycles:'ref'`; set NO `unrepresentable` override so a
    `.transform()` throws loudly), then `delete params.$schema`. Build the descriptor exactly per the
    RESEARCH skeleton: `slug` = `todoist.<tool.name>`, `service`, `intentSynonyms` (>=3-4 phrases
    from displayName/summary/description), `description`, `actionVerb` = verb-prefix(tool.name),
    `sideEffectClass` (call the side-effect inferer — Plan 03 owns the cross-check, but the importer
    must STAMP a class; implement the inline inference per Mechanic 2's priority order: GraphQL/RPC
    carve-out, named-verb helper, generic api({method}), op-name verb, override floor, fail-safe-high
    MAX; persist the raw `signals:{transportHelper, httpMethod, opNameVerb}` into provenance so the
    Plan-03 gate re-derives without re-parsing TS), `params`, `backing` (`'dom'` for the non-seeded
    smoke app — drives the Plan-02 resolve() T3 leg), and the `provenance` block with the pinned SHA +
    sourcePath + license + signals.

    GATE BEFORE EMIT: call `classifyGate([{ origin:`https://${service}`, service, slug, description }])`;
    if `failures.length` THROW naming the failures (write nothing). Otherwise write each descriptor to
    `catalog/descriptors/opentabs__todoist__<op>.json` (FLAT — Pitfall 1; the `opentabs/` namespace is
    the filename prefix + `provenance.source`, NOT a physical subdir). Fill
    `catalog/descriptors/_fixtures/_provenance.json` `apps[]` with a todoist entry listing the emitted
    descriptor slugs + the SHA. (Task 3 adds the forbidden-field pre-scan call between extraction and
    emit; write the importer so that hook slots in cleanly. Task 4 then regenerates the catalog snapshot
    from these emitted files — do NOT regenerate the snapshot inside the importer; the importer only emits
    the descriptor JSON.)

    Write `tests/import-extraction.test.js`: drive `z.toJSONSchema` over a small fixture zod schema
    and assert the Mechanic-1 shapes (object->additionalProperties:false + optional drops from
    required; union->anyOf; enum->{type:string,enum}; default emits "default" + stays required;
    $schema stripped). Write `tests/import-classify-gate-call.test.js`: assert the importer aborts
    (throws / non-zero, writes nothing) when fed an item whose origin is sensitivity-suspect-but-
    unclassified, and emits when fed the benign todoist origin (use classifyGate directly with a
    crafted item to prove the call is wired BEFORE emit).
  </action>
  <verify>
    <automated>node --import tsx ./scripts/import-opentabs-catalog.mjs</automated>
    <automated>node tests/import-extraction.test.js</automated>
    <automated>node tests/import-classify-gate-call.test.js</automated>
    <automated>node -e "const fs=require('fs'),p=require('path');const d='catalog/descriptors';const flat=fs.readdirSync(d).filter(f=>f.startsWith('opentabs__todoist__')&&f.endsWith('.json'));if(!flat.length){console.error('FAIL no flat todoist descriptors');process.exit(1)};const one=JSON.parse(fs.readFileSync(p.join(d,flat[0])));const ok=one.params&&one.params.additionalProperties===false&&!one.params.$schema&&one.provenance&&one.provenance.sha==='4b17021637d2cac12b8d84d21c40e765aa7b85e9'&&one.provenance.signals&&one.slug.startsWith('todoist.');if(!ok){console.error('FAIL descriptor shape',JSON.stringify(one).slice(0,200));process.exit(1)};console.log('PASS '+flat.length+' flat descriptors, closed params + provenance')"</automated>
  </verify>
  <acceptance_criteria>
    - `node --import tsx ./scripts/import-opentabs-catalog.mjs` exits 0 and writes >=1 `catalog/descriptors/opentabs__todoist__*.json` FLAT (no `catalog/descriptors/opentabs/` subdir is created).
    - The shape check prints PASS: an emitted descriptor has `params.additionalProperties === false`, NO `params.$schema`, `provenance.sha === '4b17021637d2cac12b8d84d21c40e765aa7b85e9'`, a `provenance.signals` object, and a `todoist.`-prefixed slug.
    - `catalog/descriptors/_fixtures/_provenance.json` `apps[]` is non-empty and names `todoist` + the emitted slugs.
    - `node tests/import-extraction.test.js` and `node tests/import-classify-gate-call.test.js` print PASS / exit 0; the classify-gate test proves the gate is called BEFORE emit (aborts on an unclassified-sensitive item).
    - NOTE: `validate:extension` is intentionally NOT asserted here — the snapshot is now out of sync until Task 4 regenerates it. Task 3 re-runs the importer (still no snapshot assertion); Task 4 reconciles + re-asserts validate:extension green.
  </acceptance_criteria>
  <done>The importer extracts todoist ops to flat closed-params provenance-stamped descriptors, gates on classifyGate before emit, fills the provenance apps[], and the extraction + classify-gate-call tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The Wall-1 recursive forbidden-field pre-scan (reject script/expr/transform/code/fn/js at every depth)</name>
  <files>scripts/import-opentabs-catalog.mjs, tests/import-forbidden-prescan.test.js</files>
  <behavior>
    - collectPropertyNames(node, acc) recurses every object/array value; whenever a key `properties` holds an object, it collects that object's child keys into acc.
    - preScanForbidden(params) returns the case-insensitive intersection of collected property names with {script, expr, transform, code, fn, js}.
    - The importer THROWS (emits nothing for that op) when preScanForbidden returns a non-empty array, naming the offending field name(s) + the op.
    - The 6 EMPIRICALLY-VERIFIED fixture rows: clean-create-issue (names owner/repo/title/body -> []), forbidden-top (script -> [script]), forbidden-nested (config/code/name -> [code]), forbidden-in-array (steps/fn -> [fn]), forbidden-in-union (payload/expr/safe -> [expr]), forbidden-in-defs (root/js/kids -> [js]).
  </behavior>
  <read_first>
    - 36-RESEARCH.md Mechanic 1 "The forbidden-field-name pre-scan" (the EMPIRICALLY-PROVEN 6-position fixture table + the rule: walk the FLATTENED JSON Schema, match case-insensitively against {script, expr, transform, code, fn, js}) + "Code Examples — The importer skeleton" (the exact collectPropertyNames + preScanForbidden functions) + Pitfall 4 (a forbidden name in a nested/$defs/union branch must be caught; recipe-path-guard does NOT catch descriptor field names — the pre-scan is a SEPARATE guard)
    - scripts/import-opentabs-catalog.mjs (the importer from Task 2 — slot the pre-scan call between z.toJSONSchema and the classifyGate/emit)
    - scripts/verify-recipe-path-guard.mjs (CONFIRM it scans only the 6-file allowlist + extension/utils/capability-*.js disk-glob + catalog/handlers/*.js — it does NOT scan descriptor JSON field names, which is why this pre-scan is required)
  </read_first>
  <action>
    Add `collectPropertyNames(node, acc)` and `preScanForbidden(params)` to
    `scripts/import-opentabs-catalog.mjs` exactly per the RESEARCH skeleton: recurse into every
    object/array value; for any key literally `properties` whose value is a non-array object, add every
    child key of that object to the accumulator; then return the case-insensitive intersection with
    `FORBIDDEN = new Set(['script','expr','transform','code','fn','js'])`. Wire the call in the per-op
    loop AFTER `delete params.$schema` and BEFORE the classifyGate/emit: `const hits =
    preScanForbidden(params); if (hits.length) throw new Error(...op name + hits...)`. The walk runs
    over the FLATTENED JSON Schema (post z.toJSONSchema) so a forbidden name in a `$defs` branch, a
    `z.union`->`anyOf` branch, a `z.record` value-schema, or an array `items` is caught.

    Write `tests/import-forbidden-prescan.test.js` importing the real `collectPropertyNames`/
    `preScanForbidden` from the importer module (export them for the test, or re-declare the verified
    functions in the test and assert the importer's behavior via a fixture round-trip). Assert the EXACT
    6 fixture rows from Mechanic 1: clean schema -> FORBIDDEN_HITS=[] (PASS), and one forbidden name per
    position (top=script, nested=code, array=fn, union=expr, $defs=js) each detected. Zero false
    positives on the clean fixture (owner/repo/title/body).
  </action>
  <verify>
    <automated>node tests/import-forbidden-prescan.test.js</automated>
    <automated>node --import tsx ./scripts/import-opentabs-catalog.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/import-forbidden-prescan.test.js` prints PASS / exits 0; it asserts all 6 verified rows: clean -> [], forbidden-top -> [script], forbidden-nested -> [code], forbidden-in-array -> [fn], forbidden-in-union -> [expr], forbidden-in-defs -> [js].
    - The importer's emitted todoist descriptors contain NO field named script/expr/transform/code/fn/js at any depth (the importer re-run still exits 0 because todoist ops are clean).
    - A hand-planted forbidden-field fixture fed through the pre-scan causes the importer to throw (emits nothing for that op).
  </acceptance_criteria>
  <done>The recursive pre-scan is wired into the importer between extraction and emit; the 6-position fixture test passes; the smoke emit stays clean and exits 0.</done>
</task>

<task type="auto">
  <name>Task 4: Regenerate the committed catalog snapshot from the emit so validate:extension reconciles (INV-01 byte-shape preserved)</name>
  <files>extension/catalog/recipe-index.generated.js</files>
  <read_first>
    - scripts/package-extension.mjs (lines 51-89: readJsonDir(catalog/descriptors) is non-recursive -> picks up the FLAT opentabs__todoist__*.json the importer just wrote; the IIFE generator lines 74-85 wraps {recipes,descriptors} as the DATA literal + dual-export tail — only the DATA literal grows, the wrapper + tail are UNCHANGED, INV-01)
    - scripts/validate-extension.mjs (lines 80-105: require()s the git-tracked extension/catalog/recipe-index.generated.js and BYTE-COMPARES it against a FRESH {recipes,descriptors} via JSON.stringify; differs -> "stale snapshot" FAIL. This is the assertion Tasks 2-3 leave unsatisfiable until this task regenerates.)
    - 36-RESEARCH.md Mechanic 4 "IIFE + djb2 byte-identity (INV-01)" (adding descriptors only grows the DATA literal; the wrapper + djb2 generation code are untouched) + Pitfall 1 (the FLAT-emit / non-recursion contract — a subdir drop would leave the smoke slugs OUT of the regenerated snapshot)
    - extension/catalog/recipe-index.generated.js (the current 8-descriptor snapshot this task regenerates to 9+; confirm the regen only grows DATA.descriptors and leaves the `(function(global){ ... global.FsbRecipeIndex = DATA; ... })` wrapper byte-identical in shape)
  </read_first>
  <action>
    After Tasks 2-3 have emitted the flat `catalog/descriptors/opentabs__todoist__*.json` descriptors, run
    `node scripts/package-extension.mjs` to REGENERATE `extension/catalog/recipe-index.generated.js`. This
    is the ONLY action: the importer (Tasks 2-3) emits descriptor JSON; this task reconciles the committed
    snapshot so `validate-extension.mjs`'s byte-compare passes. `package-extension.mjs` also rebuilds the
    dist zip + copies handlers — those are harmless idempotent side effects; the load-bearing output is the
    regenerated `recipe-index.generated.js` with the new todoist descriptors inlined into the DATA literal.

    Then run `node scripts/validate-extension.mjs` and confirm it exits 0 (the snapshot now matches a fresh
    `readJsonDir(catalog/descriptors)` — 9+ descriptors including the todoist slugs). The regenerated
    `extension/catalog/recipe-index.generated.js` is part of THIS plan's `files_modified` and is committed
    together with the emitted descriptors (a single coherent commit: descriptors + their snapshot).

    Confirm INV-01 holds: the regen MUST only grow `DATA.descriptors`; the IIFE wrapper
    (`(function(global){ 'use strict'; var DATA = ...; global.FsbRecipeIndex = DATA; ... })`) + the dual-export
    tail are byte-identical in SHAPE to the pre-phase file (only the inlined descriptor data changed). Do NOT
    hand-edit the generated file — it is machine-generated by package-extension.mjs. (Plan 04 Task 1 then locks
    this IIFE/djb2 shape with a structural assertion; this task just makes the snapshot consistent so the
    Wave-1 gate + this plan's own validate:extension assertion are TRUE.)
  </action>
  <verify>
    <automated>node scripts/package-extension.mjs</automated>
    <automated>node scripts/validate-extension.mjs</automated>
    <automated>npm run validate:extension</automated>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('extension/catalog/recipe-index.generated.js','utf8');const shapeOk=/\(function\s*\(\s*global\s*\)/.test(s)&&/global\.FsbRecipeIndex\s*=\s*DATA/.test(s)&&/module\.exports\s*=\s*DATA/.test(s);const todoistInlined=s.includes('todoist.');if(!shapeOk){console.error('FAIL INV-01 IIFE wrapper/tail shape drifted');process.exit(1)};if(!todoistInlined){console.error('FAIL todoist descriptors not inlined into snapshot (Pitfall-1 subdir drop?)');process.exit(1)};console.log('PASS snapshot reconciled: IIFE shape intact + todoist inlined')"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/package-extension.mjs` exits 0 and regenerates `extension/catalog/recipe-index.generated.js` with the emitted todoist descriptors inlined into the DATA literal (descriptor count grows 8 -> 9+).
    - `node scripts/validate-extension.mjs` exits 0 — NO "stale snapshot" failure (the committed snapshot now byte-matches a fresh `readJsonDir(catalog/descriptors)`).
    - `npm run validate:extension` exits 0 end-to-end (validate-extension reconciled + recipe-path-guard + classification-gate + crosscheck stub).
    - The INV-01 shape check prints `PASS snapshot reconciled: IIFE shape intact + todoist inlined`: the `(function(global){ ... })` wrapper + `global.FsbRecipeIndex = DATA` + `module.exports = DATA` tail are intact and a `todoist.` slug is present in the inlined DATA.
  </acceptance_criteria>
  <done>The committed catalog snapshot is regenerated from the emit so validate:extension reconciles (exit 0, no stale-snapshot fail) at this plan's end; the IIFE/djb2 wrapper shape is byte-stable (INV-01); the todoist descriptors are inlined.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OpenTabs vendored metadata -> importer | Untrusted upstream plugin source read at build; only `.name/.description/.input` metadata is read, handle() bodies are NEVER executed (no fetch/document at build) |
| importer output -> shipped catalog (recipe-index.generated.js) | A descriptor is DATA inlined into the SW; a forbidden field name or a shipped zod/sdk import would breach Wall 1 |
| build deps (zod/tsx/sdk) -> node_modules | A typosquatted/slopsquatted build dep could execute at install/build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | Tampering / Elevation | emitted descriptor field name (code-as-data on the recipe path) | mitigate | Recursive forbidden-field pre-scan (Task 3) over the FLATTENED JSON Schema rejects script/expr/transform/code/fn/js at top/nested/array/union/$defs; verify-recipe-path-guard.mjs stays green (it guards the 6 recipe-path files, a separate layer) |
| T-36-02 | Elevation / Info Disclosure | an unclassified sensitive origin emitting a descriptor (writable the moment it lands under opt-out Auto) | mitigate | classifyGate() called after Denylist.load() BEFORE any write (Task 2); an unclassified sensitivity-suspect origin aborts the emit (denylist-first floor, Phase 35) |
| T-36-03 | Tampering | slopsquatted/typosquatted build dep executes at build (e.g. jiti~vite) | mitigate | tsx (slopcheck [OK]) is the loader; jiti ([SUS]) excluded; @opentabs-dev/plugin-sdk pinned exact + devDep only + optional vendored stub; zod/tsx moved to devDependencies (never shipped) |
| T-36-04 | Tampering | shipping zod/sdk/opentabs runtime into the MV3 bundle (Wall-1 breach) | mitigate | zod/tsx/sdk are devDependencies ONLY (Task 1); the importer is scripts/*.mjs (never importScripts'd); the shipped catalog is pure-data JSON; recipe-path guard's import( grep stays green |
| T-36-05 | Tampering (build integrity) | a stale catalog snapshot shipping (the emit grows catalog/descriptors but the committed recipe-index.generated.js still reflects the OLD corpus -> the SW loads an index inconsistent with the on-disk descriptors) | mitigate | Task 4 regenerates the snapshot via package-extension.mjs immediately after the emit and re-asserts validate-extension.mjs reconciles (exit 0); the regenerated snapshot is committed WITH the descriptors (one coherent commit) so the byte-compare gate cannot drift |
| T-36-SC | Tampering | npm installs (zod/tsx/@opentabs-dev/plugin-sdk) | mitigate | RESEARCH Package Legitimacy Audit ran slopcheck 0.6.1: [OK] zod, [OK] tsx, [SUS] jiti (NOT used); sdk scoped pinned-exact devDep only. No [ASSUMED]/[SUS] package is installed -> no blocking-human checkpoint required this plan |
</threat_model>

<verification>
- `npm run validate:extension` exits 0 (validate-extension RECONCILED against the flat-emit catalog + recipe-path-guard + classification-gate + the crosscheck STUB).
- `node scripts/validate-extension.mjs` exits 0 with NO stale-snapshot failure (the regenerated recipe-index.generated.js byte-matches a fresh readJsonDir(catalog/descriptors)).
- `node scripts/verify-recipe-path-guard.mjs` exits 0 (no shipped file imports zod/sdk).
- `node --import tsx ./scripts/import-opentabs-catalog.mjs` exits 0 and emits flat closed-params provenance-stamped todoist descriptors.
- `node scripts/package-extension.mjs` exits 0 and regenerates extension/catalog/recipe-index.generated.js with the todoist descriptors inlined (IIFE/djb2 wrapper shape byte-stable, INV-01).
- `node tests/import-extraction.test.js && node tests/import-forbidden-prescan.test.js && node tests/import-classify-gate-call.test.js` all exit 0.
- zod/tsx are devDependencies; plugin-sdk added as devDependency; none under dependencies.
</verification>

<success_criteria>
CGEN-01 satisfied: a build-time `scripts/import-opentabs-catalog.mjs` (tsx) extracts each smoke-app op's metadata (slug, params via z.toJSONSchema, service/origin, action verb, description) into flat provenance-stamped descriptor JSON, SHA-pinned, gated by classifyGate before emit and by the recursive Wall-1 pre-scan, with NO runtime opentabs/sdk/zod shipped (Wall 1; recipe-path guard green). The committed catalog snapshot (extension/catalog/recipe-index.generated.js) is REGENERATED from the emit so `validate:extension` reconciles (exit 0, no stale-snapshot fail) at this plan's end, with the IIFE/djb2 wrapper shape byte-stable (INV-01). package.json registers the whole phase's tests/gate as passing stubs so CI stays green across every wave.
</success_criteria>

<output>
Create `.planning/phases/36-codegen-pipeline-no-dead-entry-resolution/36-01-SUMMARY.md` when done.
</output>
