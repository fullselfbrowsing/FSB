---
phase: 36-codegen-pipeline-no-dead-entry-resolution
plan: 04
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - tests/catalog-inline-shape.test.js
  - tests/head-handler-cap.test.js
  - tests/capability-search-eval.test.js
  - catalog/descriptors/_fixtures/seed-descriptors.json
  - catalog/descriptors/_fixtures/intent-cases.json
autonomous: true
requirements: [CGEN-04]
user_setup: []

must_haves:
  truths:
    - "The recipe-index.generated.js snapshot REGENERATED + committed by Plan 01 already inlines the emitted flat catalog/descriptors/opentabs__*.json via the EXISTING readJsonDir path; this plan VERIFIES (does NOT re-own) that the IIFE wrapper + dual-export tail are byte-stable in shape (INV-01) and the todoist slugs are present"
    - "Re-running package-extension.mjs over the SAME corpus (Plan 04 adds zero shipped descriptors — only _fixtures/ which do not ship) is IDEMPOTENT: the snapshot stays byte-identical, proving the generator path is unchanged and restore-not-rebuild holds"
    - "The catalogVersion djb2 hash shifts ONLY when the slug set changes (a same-corpus rebuild restores, not rebuilds) — INV-01: the IIFE/djb2 generation code is unchanged"
    - "On the smoke category (todoist), the existing eval harness re-passes: recall@5 >= 0.9 AND wrong-invoke == 0 over the extended near-neighbor fixtures (no harness code change, only fixture data + 2 asserts)"
    - "The serialized smoke index is < ~50KB and loadJSON(serialized, INDEX_OPTIONS) + first search is < ~10ms (the cold-start measurement machinery exists before breadth lands)"
    - "HEAD_HANDLER_MODULES.length <= the cap (~30); Phase 36 adds ZERO head handlers so it passes at 3 (breadth stays descriptors-only)"
  artifacts:
    - path: "tests/catalog-inline-shape.test.js"
      provides: "VERIFIES (against the Plan-01-reconciled snapshot) the recipe-index.generated.js IIFE shape + djb2 catalogVersion are byte-stable + idempotent and the smoke slugs are inlined (INV-01)"
      exports: []
    - path: "tests/head-handler-cap.test.js"
      provides: "asserts HEAD_HANDLER_MODULES.length <= CAP (head stays descriptors-only)"
      exports: []
    - path: "tests/capability-search-eval.test.js"
      provides: "extended: smoke-category recall@5>=0.9 + wrong-invoke==0 + serialized-size + loadJSON+first-search time asserts"
      exports: []
    - path: "catalog/descriptors/_fixtures/seed-descriptors.json"
      provides: "+~8-12 todoist near-neighbor descriptors (cross-app wrong-invoke pressure)"
      contains: "todoist"
    - path: "catalog/descriptors/_fixtures/intent-cases.json"
      provides: "+~3-4 intent phrases per smoke op (recall@5 cases)"
      contains: "todoist"
  key_links:
    - from: "scripts/package-extension.mjs readJsonDir"
      to: "catalog/descriptors/opentabs__*.json"
      via: "Plan 01 already regenerated the snapshot via this non-recursive flat read; Plan 04 VERIFIES the inlined result + idempotent re-run (INV-01 IIFE/djb2 unchanged)"
      pattern: "readJsonDir|recipe-index.generated"
    - from: "tests/capability-search-eval.test.js"
      to: "catalog/descriptors/_fixtures/{seed-descriptors,intent-cases}.json"
      via: "the existing recall@5>=0.9 + wrong-invoke==0 harness gates the smoke category from fixture data"
      pattern: "recall|wrong-invoke"
---

<objective>
Prove the machinery ships and scales (CGEN-04): VERIFY that the snapshot Plan 01 already
regenerated + committed inlines the emitted flat descriptors via the EXISTING `readJsonDir` path
with the `recipe-index.generated.js` IIFE shape + djb2 `catalogVersion` byte-unchanged (INV-01) and
the todoist slugs present, confirm re-running `package-extension.mjs` over the same corpus is
idempotent (Plan 04 adds zero shipped descriptors), re-pass the existing eval harness on the todoist
smoke category (recall@5 >= 0.9, wrong-invoke == 0) by EXTENDING the fixture data only, add the
serialized-index-size + cold-start-time assertions so the measurement machinery exists before breadth
lands, and add the HEAD_HANDLER_MODULES cap assertion so "breadth = descriptors-only, the head never
sprawls" is a CI failure, not a hope.

Purpose: this is the "the moment 2,523 descriptors land they inline + search cleanly" proof, scoped
to ONE non-sensitive smoke category. Phase 43 re-runs the same size/cold-start gate at full scale;
this phase proves the data-layout (params NOT indexed, schema-on-hit) holds and the generator path
is untouched. Plan 01 owns the snapshot regen (it co-locates regen with the emit so validate:extension
is green in Wave 1); Plan 04 is the VERIFIER, not a second regen owner — so this plan does NOT list
extension/catalog/recipe-index.generated.js in files_modified and never asserts against a pre-emit
(stale) baseline.

Output: `tests/catalog-inline-shape.test.js`; `tests/head-handler-cap.test.js`; an extended
`tests/capability-search-eval.test.js`; the extended `_fixtures/{seed-descriptors,intent-cases}.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-codegen-pipeline-no-dead-entry-resolution/36-CONTEXT.md
@.planning/phases/36-codegen-pipeline-no-dead-entry-resolution/36-RESEARCH.md

<interfaces>
<!-- The inlining + search + cold-start seams (read live). Use directly. -->

scripts/package-extension.mjs (VERIFIED live):
  readJsonDir(absDir) = readdirSync(absDir).filter(name => name.endsWith('.json'))   // NON-RECURSIVE (line 51-54)
  const descriptors = readJsonDir(join(CATALOG_ROOT, 'descriptors'))                 // line 68 — picks up FLAT opentabs__*.json
  const catalogData = JSON.stringify({ recipes, descriptors }, null, 2)              // line 73
  recipe-index.generated.js = an IIFE: `(function(global){ var DATA = {recipes,descriptors}; global.FsbRecipeIndex = DATA; ... })`
  => adding descriptors ONLY grows the DATA JSON literal; the IIFE wrapper + dual-export tail are untouched (INV-01).
  NOTE: Plan 01 (Wave 1, Task 4) ALREADY ran this after the emit, so the committed snapshot is consistent
    BEFORE this plan runs. Plan 04 re-running it over the SAME corpus must leave the file byte-identical (idempotent).

scripts/validate-extension.mjs (VERIFIED live, lines 80-105):
  require()s the committed extension/catalog/recipe-index.generated.js and BYTE-COMPARES it against a fresh
    {recipes,descriptors} via JSON.stringify; differs -> "stale snapshot" FAIL.
  => Because Plan 01 already reconciled the snapshot with the emit, validate:extension is green entering Wave 2.
    This plan adds NO shipped descriptors (only _fixtures/, excluded by non-recursion), so it does NOT touch the
    snapshot or its baseline — it asserts the EXISTING consistent state, never a pre-emit one.

capability-search.js (VERIFIED live):
  INDEX_OPTIONS.fields = ['intentSynonyms','description','service','actionVerb']   // searchable text ONLY
  INDEX_OPTIONS.storeFields = ['slug','service','sideEffectClass','description']   // params NOT indexed/stored (schema-on-hit)
  _computeCatalogVersion(descriptors, recipes, declaredVersion) = djb2 over sorted slugs (lines 190-200)
  => catalogVersion shifts ONLY when the slug set changes; a same-corpus rebuild RESTORES the SW index (no rebuild).

tests/capability-search-eval.test.js (VERIFIED live — the harness to EXTEND, not rewrite):
  requires MiniSearch (extension/lib/minisearch.min.js), INDEX_OPTIONS + buildIndex + search (capability-search.js),
    and the fixtures catalog/descriptors/_fixtures/{seed-descriptors.json, seed-recipes.json, intent-cases.json}.
  Already asserts: recall@5 >= 0.9 AND wrong-invoke == 0 over the seeded near-neighbor set; the
    toJSON()->loadJSON(json, INDEX_OPTIONS) round-trip reproduces identical top hits.
  intent-cases.json entries are {intent, expectedSlug}; the existing fixtures use ~3 phrases/op.
  Extending = add todoist near-neighbors to seed-descriptors.json + ~3-4 intent phrases/op to
    intent-cases.json + add the serialized-size + loadJSON-time asserts. NO harness logic rewrite.

HEAD_HANDLER_MODULES (capability-catalog.js, VERIFIED line 215-219): 3 entries (github/slack/notion).
  Phase 36 adds ZERO head handlers -> the cap assertion passes at 3. CAP ~30 (milestone "head stays 15-30").

Smoke fixtures must NOT ship: keep near-neighbor eval corpus under catalog/descriptors/_fixtures/
(readJsonDir is non-recursive -> _fixtures/ is excluded from the shipped catalog). The REAL emitted
smoke descriptors live top-level (opentabs__todoist__*.json) and DO ship + inline (Plan 01 emitted them
and regenerated the snapshot; this plan verifies that result).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inline-shape lock (VERIFY the Plan-01-reconciled snapshot: IIFE + djb2 catalogVersion byte-stable + idempotent) + head-cap assertion</name>
  <files>tests/catalog-inline-shape.test.js, tests/head-handler-cap.test.js</files>
  <read_first>
    - 36-RESEARCH.md Mechanic 4 "IIFE + djb2 byte-identity (INV-01)" (the generated file shape; adding descriptors only grows the DATA literal; catalogVersion shifts only on a slug-set change; restore-not-rebuild) + Pitfall 1 (the FLAT-emit / non-recursion warning signs: catalogVersion unchanged after adding descriptors = the subdir-drop bug — assert the OPPOSITE: the smoke slugs ARE present) + "HEAD_HANDLER_MODULES cap assertion" subsection (assert length <= CAP ~30; Phase 36 adds zero head handlers -> passes at 3)
    - 36-VALIDATION.md CGEN-04 rows (IIFE/djb2 unchanged after inlining; head-cap)
    - scripts/package-extension.mjs (readJsonDir line 51-54 non-recursive; the recipe-index.generated.js IIFE generator lines 74-85 — the wrapper + dual-export tail this test locks) + scripts/validate-extension.mjs (lines 80-105, the byte-compare staleness gate Plan 01 already reconciled) + the existing tests/recipe-schema-lock.test.js (a shape-lock precedent to mirror the assertion style)
    - 36-01-PLAN.md Task 4 (Plan 01 ALREADY regenerated + committed extension/catalog/recipe-index.generated.js after the emit; this plan VERIFIES that artifact's shape, it does NOT re-own the regen — do not assert against a pre-emit baseline)
    - extension/utils/capability-catalog.js HEAD_HANDLER_MODULES (line 215-219; the 3 entries; require the module / parse the array)
    - catalog/descriptors/opentabs__todoist__*.json (the emitted smoke descriptors that must appear in the already-inlined DATA) + extension/catalog/recipe-index.generated.js (the Plan-01-reconciled snapshot under test — it ALREADY contains the todoist slugs)
  </read_first>
  <action>
    Write `tests/catalog-inline-shape.test.js`. The snapshot under test is the one Plan 01 ALREADY
    regenerated + committed (Plan 01 Task 4) — this test VERIFIES it; it does NOT own the regen and MUST NOT
    assert against a pre-emit baseline. Read the committed `extension/catalog/recipe-index.generated.js`
    (via require()) and assert: (a) the IIFE wrapper shape is intact — it is a single `(function(global){ ... })`
    that sets `global.FsbRecipeIndex = DATA` with the dual-export tail (regex/structural assertion on the
    wrapper + tail, NOT a full-file byte diff, so the grown descriptor set does not falsely fail); (b) the
    inlined `DATA.descriptors` INCLUDES the emitted todoist slugs (guards against the Pitfall-1 silent
    subdir-drop — if readJsonDir had missed them, the smoke slugs would be absent from the committed snapshot);
    (c) IDEMPOTENCY / restore-not-rebuild — re-running the generation path over the SAME on-disk corpus (Plan 04
    adds zero shipped descriptors) reproduces a byte-identical snapshot, and `_computeCatalogVersion` over the
    inlined descriptors is DETERMINISTIC (computing it twice over the same corpus yields the identical djb2
    string) and CHANGES when a slug is added/removed (compute over the corpus +/- one synthetic slug and assert
    the hash differs). The point: the generator code path is unchanged (INV-01), the emitted descriptors actually
    inlined (Plan 01), and a same-corpus rebuild restores rather than rebuilds.

    Idempotency mechanics (no files_modified conflict with Plan 01): if the test invokes
    `scripts/package-extension.mjs` to prove idempotency, it must treat the regen as a no-op over the unchanged
    corpus — capture the snapshot bytes before, run the generator, and assert the bytes are identical afterward
    (Plan 04 owns NO shipped descriptors, so the corpus is unchanged and the file must not move). Do NOT add
    `extension/catalog/recipe-index.generated.js` to this plan's files_modified — Plan 01 owns it; this plan only
    reads/asserts it. Equivalently, the test may compute the expected DATA literal in-memory from
    `readJsonDir(catalog/descriptors)` and compare to the committed file WITHOUT rewriting it.

    Write `tests/head-handler-cap.test.js`: read `HEAD_HANDLER_MODULES` from capability-catalog.js
    (require the module or parse the array) and assert `HEAD_HANDLER_MODULES.length <= CAP` with `CAP =
    30`. Assert it equals 3 today (github/slack/notion) so a future breadth phase that sneaks an
    imperative head handler in fails the gate. Project convention (PASS=/FAIL=, process.exit(1)).
  </action>
  <verify>
    <automated>node tests/catalog-inline-shape.test.js</automated>
    <automated>node tests/head-handler-cap.test.js</automated>
    <automated>node -e "const fs=require('fs');const f='extension/catalog/recipe-index.generated.js';if(!fs.existsSync(f)){console.error('FAIL generated snapshot missing (Plan 01 Task 4 must have regenerated it)');process.exit(1)};const before=fs.readFileSync(f,'utf8');const ok=/global\.FsbRecipeIndex\s*=\s*DATA/.test(before)&&/function\s*\(\s*global\s*\)/.test(before)&&before.includes('todoist');if(!ok){console.error('FAIL IIFE shape or todoist slug missing in the Plan-01-reconciled snapshot');process.exit(1)};console.log('PASS Plan-01 snapshot verified: IIFE shape intact + todoist inlined')"</automated>
    <automated>node scripts/package-extension.mjs >/dev/null 2>&1; node -e "const cp=require('child_process');const fs=require('fs');const f='extension/catalog/recipe-index.generated.js';const a=fs.readFileSync(f,'utf8');cp.execFileSync(process.execPath,['scripts/package-extension.mjs'],{stdio:'ignore'});const b=fs.readFileSync(f,'utf8');if(a!==b){console.error('FAIL package-extension.mjs is NOT idempotent over the unchanged corpus (snapshot moved)');process.exit(1)};console.log('PASS regen idempotent over unchanged corpus (restore-not-rebuild)')"</automated>
    <automated>node scripts/validate-extension.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/catalog-inline-shape.test.js` exits 0: the IIFE wrapper + `global.FsbRecipeIndex = DATA` tail are intact; the inlined DATA.descriptors include the emitted todoist slugs; `_computeCatalogVersion` is deterministic over the same corpus and changes when the slug set changes; the test reads/asserts the Plan-01-committed snapshot and does NOT assert against a pre-emit baseline.
    - `node tests/head-handler-cap.test.js` exits 0: `HEAD_HANDLER_MODULES.length <= 30` (and equals 3 today).
    - The Plan-01-snapshot verify prints `PASS Plan-01 snapshot verified: IIFE shape intact + todoist inlined` (proves Plan 01's regen picked up the FLAT descriptors — no Pitfall-1 subdir drop).
    - The idempotency check prints `PASS regen idempotent over unchanged corpus (restore-not-rebuild)` and `node scripts/validate-extension.mjs` exits 0 (re-running package-extension.mjs leaves the snapshot byte-identical and still reconciled — no stale-snapshot fail; this plan adds zero shipped descriptors).
  </acceptance_criteria>
  <done>The generated catalog (regenerated + committed by Plan 01) is VERIFIED to inline the emitted smoke descriptors via the unchanged readJsonDir/IIFE/djb2 path (INV-01 byte-stable shape, deterministic catalogVersion, idempotent re-run); the head-cap assertion passes at 3. This plan does not re-own the snapshot file.</done>
</task>

<task type="auto">
  <name>Task 2: Smoke-category eval re-pass (recall@5>=0.9 + wrong-invoke==0) + serialized-size + cold-start-time asserts (fixtures + 2 asserts only)</name>
  <files>catalog/descriptors/_fixtures/seed-descriptors.json, catalog/descriptors/_fixtures/intent-cases.json, tests/capability-search-eval.test.js</files>
  <read_first>
    - 36-RESEARCH.md Mechanic 4 "The eval-harness fixture (extend the EXISTING capability-search-eval.test.js)" (add ~8-12 todoist near-neighbor descriptors to seed-descriptors.json with cross-app near-neighbors — todoist create_task near asana/linear/github create_*; add ~3-4 intent phrases per smoke op to intent-cases.json; the existing recall@5>=0.9 + wrong-invoke==0 asserts then gate the smoke category automatically — NO harness code change, only fixture data) + "Concrete SW cold-start budget" subsection (the EXACT two asserts: serialized.length < 50*1024; loadJSON(serialized, INDEX_OPTIONS) + restored.search(...) elapsed < 10ms; the index-layout discipline — params NOT indexed/stored, schema-on-hit)
    - 36-VALIDATION.md CGEN-04 eval row (recall@5>=0.9 AND wrong-invoke==0; index < ~50KB; loadJSON+first-search < ~10ms)
    - tests/capability-search-eval.test.js (the existing harness: how it requires the fixtures, builds the index via buildIndex(INDEX_OPTIONS), computes recall/wrong-invoke at lines ~83-105, and does the toJSON/loadJSON round-trip ~124-138 — EXTEND, do not rewrite; add the size + time asserts using the SAME INDEX_OPTIONS object so loadJSON does not throw "loadJSON requires same options")
    - catalog/descriptors/_fixtures/seed-descriptors.json + intent-cases.json (the existing near-neighbor corpus + {intent,expectedSlug} cases — match their shape exactly when adding todoist entries) + catalog/descriptors/opentabs__todoist__*.json (the emitted descriptors — mirror their slug/intentSynonyms/service so the fixture near-neighbors are realistic)
  </read_first>
  <action>
    Extend `catalog/descriptors/_fixtures/seed-descriptors.json` with ~8-12 todoist smoke descriptors that
    create cross-app near-neighbor pressure (todoist `create_task` alongside asana `create_task`, linear
    `create_issue`, github `create_issue`; todoist `list_tasks`/`complete_task`/`update_task` etc.) so the
    wrong-invoke gate is genuinely exercised. Match the existing fixture entry shape (slug, service,
    intentSynonyms, description, sideEffectClass) exactly. Extend
    `catalog/descriptors/_fixtures/intent-cases.json` with ~3-4 natural-language intent phrases per todoist
    op as `{intent, expectedSlug}` cases (match the existing ~3-phrases/op style). These fixtures live under
    _fixtures/ so they do NOT ship (readJsonDir non-recursion excludes them) AND do NOT alter the shipped
    catalog snapshot Plan 01 reconciled (so validate:extension stays green — no new stale baseline).

    In `tests/capability-search-eval.test.js` (EXTEND, no logic rewrite): after the existing buildIndex +
    the existing recall@5/wrong-invoke gate (which now automatically covers the added todoist cases), add
    the two cold-start/size asserts from Mechanic 4 using the SAME INDEX_OPTIONS object the harness already
    imports: `const serialized = JSON.stringify(ms.toJSON());` assert `serialized.length < 50*1024`; then
    `const t0 = performance.now(); const restored = MiniSearch.loadJSON(serialized, INDEX_OPTIONS);
    restored.search('create a task'); const elapsed = performance.now() - t0;` assert `elapsed < 10`. Keep
    the existing PASS=/FAIL= reporting + the existing acceptance line that greps for "recall@5=" and
    "wrong-invoke=".
  </action>
  <verify>
    <automated>node tests/capability-search-eval.test.js</automated>
    <automated>node scripts/validate-extension.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/capability-search-eval.test.js` exits 0 with the existing harness logic unchanged: recall@5 >= 0.9 AND wrong-invoke == 0 over the extended fixture set (now including the todoist near-neighbors).
    - The test prints the serialized smoke index size and asserts it is < ~50KB, and asserts loadJSON(serialized, INDEX_OPTIONS) + first search completes < ~10ms (the cold-start measurement machinery now exists).
    - The added fixtures live under catalog/descriptors/_fixtures/ (do NOT ship; readJsonDir non-recursion excludes them) so `node scripts/validate-extension.mjs` stays at exit 0 (no new stale-snapshot baseline introduced); the todoist near-neighbors create real cross-app wrong-invoke pressure (todoist vs asana/linear/github create_*).
  </acceptance_criteria>
  <done>The smoke category re-passes the existing recall@5>=0.9 + wrong-invoke==0 gate via fixture data only, and the serialized-size + cold-start-time assertions are added (proving the SCALE-01 data-layout holds before breadth lands); the shipped snapshot is untouched so validate:extension stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| emitted descriptors -> shipped recipe-index.generated.js | The non-recursive readJsonDir must pick up the FLAT descriptors; a silent subdir drop would ship an empty catalog that "passes" over nothing (Plan 01 regenerated the snapshot; this plan verifies the smoke slugs are present) |
| descriptor params bulk -> SW cold-start index | If params were indexed/stored, the index would bloat and blow the cold-start budget at full scale |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-11 | Tampering (build integrity) | a silent subdir-drop (Pitfall 1): descriptors emitted to a subdir readJsonDir does not descend -> empty shipped catalog, eval passes over nothing | mitigate | The inline-shape test asserts the emitted todoist slugs ARE present in the Plan-01-reconciled DATA.descriptors (Task 1); flat-emit (Plan 01) keeps them at top-level where readJsonDir reads them |
| T-36-12 | Denial of Service (SW cold-start) | params bulk indexed into the search index -> oversized index, blown cold-start budget at scale | mitigate | INDEX_OPTIONS indexes searchable text only (params NOT indexed/stored, schema-on-hit, VERIFIED); the serialized-size (<50KB) + loadJSON+first-search (<10ms) asserts (Task 2) prove the data-layout holds before breadth lands |
| T-36-13 | Elevation (scope creep -> MV3 ban) | the head sprawling into imperative handlers at breadth scale (cloning the imperative model) | mitigate | The HEAD_HANDLER_MODULES cap assertion (Task 1) fails CI if the head exceeds ~30; Phase 36 adds zero head handlers (breadth = descriptors-only) |
| T-36-14 | Tampering | INV-01 drift: a generator change altering the IIFE/djb2 shape | mitigate | The inline-shape test locks the IIFE wrapper + dual-export tail + deterministic catalogVersion + idempotent re-run; the generation code is unchanged (descriptors only grow the DATA literal; Plan 01 owns the regen, this plan verifies) |
</threat_model>

<verification>
- `node tests/catalog-inline-shape.test.js` exits 0 (VERIFIES the Plan-01-reconciled snapshot: IIFE/djb2 byte-stable shape; emitted todoist slugs inlined; deterministic catalogVersion; idempotent re-run — no pre-emit baseline asserted).
- `node tests/head-handler-cap.test.js` exits 0 (HEAD_HANDLER_MODULES.length <= 30).
- `node tests/capability-search-eval.test.js` exits 0 (recall@5>=0.9 + wrong-invoke==0 on the smoke category; index<50KB; loadJSON+first-search<10ms).
- `node scripts/validate-extension.mjs` / `npm run validate:extension` exits 0 (no regression; Plan 01 already reconciled the snapshot; this plan adds only non-shipping _fixtures/ so the staleness check stays green; a same-corpus regen is idempotent).
</verification>

<success_criteria>
CGEN-04 satisfied: the generated catalog committed by Plan 01 is inlined by `package-extension.mjs` via the
existing `readJsonDir` path with a stable `catalogVersion`; this plan VERIFIES the IIFE shape and djb2 hashing
are unchanged (INV-01), a same-corpus regen is idempotent, and the todoist slugs are present; on the todoist
smoke category the existing eval harness re-passes (recall@5>=0.9, wrong-invoke==0) and the SW cold-start parse
stays within budget (serialized index < ~50KB, loadJSON+first-search < ~10ms); HEAD_HANDLER_MODULES stays capped
(breadth is descriptors-only). Plan 04 owns no shipped descriptors and no snapshot regen — Plan 01 reconciled it
in Wave 1; this plan is the verifier.
</success_criteria>

<output>
Create `.planning/phases/36-codegen-pipeline-no-dead-entry-resolution/36-04-SUMMARY.md` when done.
</output>
