---
phase: 41-depth-2-remaining-hand-ports-guarded-writes
plan: 04
type: execute
wave: 3
depends_on: ["41-01", "41-02", "41-03"]
files_modified:
  - catalog/handlers/slack.js
  - tests/sensitive-write-import-gate.test.js
  - .planning/phases/41-depth-2-remaining-hand-ports-guarded-writes/41-HUMAN-UAT.md
autonomous: true
requirements: [DEPTH-02]

must_haves:
  truths:
    - "slack.send_message registers as a T1a write head on https://app.slack.com, resolves dom->T1a, and fails closed to RECIPE_DOM_FALLBACK_PENDING (distinct from the already-executable slack.chat.postMessage)"
    - "A T1a-write on the SENSITIVE app.slack.com origin through the LIVE committed roster returns RECIPE_CONSENT_MUTATING_REQUIRED (dual-field) WITHOUT the mutating flag; allows WITH setOriginMutating; a READ on the same origin allows under Auto"
    - "41-HUMAN-UAT.md carries the notion + slack guarded-write live-UAT rows (consolidated with the gitlab rows from Plan 02)"
  artifacts:
    - path: "catalog/handlers/slack.js"
      provides: "The slack.send_message guarded write head (fail-closed, distinct slug)"
      contains: "slack.send_message"
    - path: "tests/sensitive-write-import-gate.test.js"
      provides: "The SC2 Phase-41 block: a T1a-write on the sensitive slack origin honors the mutating opt-in"
      contains: "slack.send_message"
    - path: ".planning/phases/41-depth-2-remaining-hand-ports-guarded-writes/41-HUMAN-UAT.md"
      provides: "The consolidated live-UAT manifest (gitlab + notion + slack rows)"
      contains: "slack.send_message"
  key_links:
    - from: "tests/sensitive-write-import-gate.test.js"
      to: "extension/config/service-denylist.json"
      via: "classify('https://app.slack.com').sensitive === true through the live roster"
      pattern: "app\\.slack\\.com"
    - from: "catalog/handlers/slack.js"
      to: "catalog/descriptors/opentabs__slack__send_message.json"
      via: "slug-exact dom->T1a upgrade"
      pattern: "slack\\.send_message"
---

<objective>
Two deliverables + the UAT consolidation:

1. The `slack.send_message` guarded WRITE head (catalog/handlers/slack.js) on the verified
   same-origin app https://app.slack.com. CRITICAL: `slack.send_message` is the EXACT
   opentabs breadth write slug (verified: write/dom) and is DISTINCT from the already-
   executable hand-head `slack.chat.postMessage` (which has NO opentabs descriptor — it is a
   29-03 hand slug). So the new head UPGRADES the breadth descriptor dom->T1a WITHOUT
   colliding with chat.postMessage. It ships FAIL-CLOSED (returns RECIPE_DOM_FALLBACK_PENDING,
   NEVER calls callSlackMethod/ctx.executeBoundSpec) — the new write is fail-closed even though
   chat.postMessage is the live-proven executable exception.

2. The SC2 proof: extend tests/sensitive-write-import-gate.test.js with a Phase-41 block that
   routes a T1a WRITE on the SENSITIVE app.slack.com origin through the LIVE committed roster
   (the same live-roster pattern the discord/amazon blocks use): WITHOUT the per-origin
   mutating flag → dual-field RECIPE_CONSENT_MUTATING_REQUIRED (byte-exact code===errorCode
   ===error, INV-03); WITH setOriginMutating(origin,true) → allow; a READ on app.slack.com →
   allow under Auto. app.slack.com is sensitive via `https://*.slack.com` in the committed
   denylist (verified) — so slack.send_message is the natural SHIPPED sensitive-origin proof,
   and NO new risky social/payment write head is shipped just to prove the gate.

3. Consolidate the live-UAT manifest: append the notion (Plan 03) + slack guarded-write rows
   to 41-HUMAN-UAT.md (which Plan 02 created with the gitlab rows).

Purpose: SC2 — a sensitive-origin write honors the DENY-04 mutating opt-in; the gate runs
BEFORE tier dispatch (capability-router.js:720 before :743), so a T1a write is gated
IDENTICALLY to the proven DOM writes. The slack write is INERT (fail-closed) — the gate-allow
and the handler-fail-close are DISTINCT concerns (the gate proves consent posture; the handler
proves no-mutation-without-capture).
Output: 1 guarded write head + the SC2 block + consolidated UAT.

This wave delivers SC1 + SC2 because: slack.send_message fails closed (SC1) AND, routed
through the live roster, a sensitive-origin write returns RECIPE_CONSENT_MUTATING_REQUIRED
without the flag and allows with it while reads stay open under Auto (SC2).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-depth-2-remaining-hand-ports-guarded-writes/41-CONTEXT.md

# The module to EXTEND (it ALREADY has slack.chat.postMessage executable + callSlackMethod;
# the new slack.send_message is fail-closed and must NOT call callSlackMethod):
@catalog/handlers/slack.js

# The SC2 test to EXTEND (the discord + amazon end-to-end blocks are the EXACT shape to
# mirror for slack — installChromeStorageStub, load the live denylist, route through the
# real gate; the byte-equality asserts at :177-182 + :271-276):
@tests/sensitive-write-import-gate.test.js

# The fail-closed write template (github.issues.create, :111-123):
@catalog/handlers/github.js

<interfaces>
<!-- Already present in slack.js (reuse, do NOT redefine):
       SLACK_ORIGIN = 'https://app.slack.com'
       POST_MESSAGE_PARAMS (channel + text, additionalProperties:false) — reusable for
         send_message, OR add a SEND_MESSAGE_PARAMS that also allows thread_ts (the opentabs
         descriptor props: channel + text + optional thread_ts)
       callSlackMethod(slug, method, fields, ctx) — the EXECUTABLE path; the guarded
         send_message MUST NOT call it (fail-closed)
       typedRecipeError(code, extra) -> { success:false, code, errorCode, error, ...extra }
       the registration loop (descriptor.service 'app.slack.com', sideEffectClass carried). -->

<!-- The write descriptor already exists (verified): opentabs__slack__send_message.json,
     slug slack.send_message, service slack.com, write/dom, props channel+text+thread_ts.
     There is NO opentabs__slack__chat*.json — slack.chat.postMessage is hand-only, so
     slack.send_message does NOT collide (distinct slugs). -->

<!-- SC2 live-roster facts (verified): extension/config/service-denylist.json sensitiveOrigins
     includes "https://*.slack.com" -> classify('https://app.slack.com').sensitive === true,
     denied === false. The gate's posture-B re-gate (capability-router.js:476-496) fires on
     mutating + sensitive + !mutatingAllowed -> RECIPE_CONSENT_MUTATING_REQUIRED. -->

<!-- The SC2 test vehicle (mirror the discord block at sensitive-write-import-gate.test.js
     :125-192): load the REAL committed roster (await Denylist.load(); globalThis.
     FsbServiceDenylist = Denylist), assert classify('https://app.slack.com').sensitive, then
     Gate.evaluate({ origin:'https://app.slack.com', slug:'slack.send_message',
       entry:{ tier:'T1a', descriptor:{ sideEffectClass:'write' } } }) with mode auto, flag
     false -> decision !== 'allow' + error.code/errorCode/error === RECIPE_CONSENT_MUTATING_
     REQUIRED; then setOriginMutating(origin,true) -> allow; and a read entry -> allow.
     This is a T1a-handler-entry proof (the SC2 nuance: the gate sees tier:'T1a', the discord
     block used tier:'T3') so it proves the gate gates a T1a write identically. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Append the slack.send_message guarded write head (fail-closed, distinct slug)</name>
  <files>catalog/handlers/slack.js</files>
  <behavior>
    - Catalog.resolve('slack.send_message', 'https://app.slack.com').tier === 'T1a' (was T3);
      descriptor.slug byte-exact; descriptor.sideEffectClass === 'write'.
    - Catalog.resolve('slack.chat.postMessage', ...) STILL resolves the existing executable
      head (no collision, no regression — distinct slug).
    - handle({}, ctx) for slack.send_message returns code === errorCode === error ===
      'RECIPE_DOM_FALLBACK_PENDING', success:false, fellBackToDom:true; the recording
      ctx.executeBoundSpec recorder stays EMPTY (it never calls callSlackMethod).
    - SEND_MESSAGE_PARAMS has additionalProperties:false, required channel + text, optional
      thread_ts (mirrored from the opentabs__slack__send_message.json props).
  </behavior>
  <action>EDIT catalog/handlers/slack.js: inside `var handlers = { ... }`, AFTER
slack.chat.postMessage, add a SEND_MESSAGE_PARAMS schema (channel + text required, thread_ts
optional, additionalProperties:false) and the slack.send_message write entry: { tier:'T1a',
origin:SLACK_ORIGIN, sideEffectClass:'write', params:SEND_MESSAGE_PARAMS, async handle(args,
ctx){ return typedRecipeError('RECIPE_DOM_FALLBACK_PENDING', { slug:'slack.send_message',
reason:'unverified-slack-send-message-mutation', fellBackToDom:true }); } } — copy the
github.issues.create shape EXACTLY (it does NOT call callSlackMethod or ctx.executeBoundSpec).
Add a block comment: slack.send_message is the BREADTH write slug (upgrades
opentabs__slack__send_message dom->T1a) and is DISTINCT from the live-proven executable
slack.chat.postMessage (a 29-03 hand slug with no opentabs descriptor); the new write ships
FAIL-CLOSED until 41-HUMAN-UAT.md confirms its body, even though chat.postMessage is the
executable exception. Update the module header docblock. The registration loop carries
sideEffectClass through. Per D: same-origin verified app + the guarded-WRITE fail-closed
contract; do NOT make slack.send_message executable.</action>
  <verify>
    <automated>node tests/guarded-write-failclosed.test.js 2>&1 | grep "slack.send_message" | grep -v FAIL && node tests/head-handler-upgrade.test.js 2>&1 | grep "slack.send_message" | grep -q "T1a" && node tests/head-handler-cap.test.js && node tests/capability-head-handlers.test.js</automated>
  </verify>
  <done>slack.send_message passes the fail-closed harness, resolves dom->T1a (write class), does NOT collide with slack.chat.postMessage, head cap ≤30 (still 4 modules), head-handlers suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The SC2 proof — a T1a write on the sensitive slack origin honors the mutating opt-in</name>
  <files>tests/sensitive-write-import-gate.test.js</files>
  <behavior>
    - Through the LIVE committed roster: classify('https://app.slack.com').sensitive === true,
      denied === false.
    - Gate.evaluate({ origin:'https://app.slack.com', slug:'slack.send_message', entry:{
      tier:'T1a', descriptor:{ sideEffectClass:'write' } } }) under mode auto, flag false →
      decision !== 'allow' AND error.code === error.errorCode === error.error ===
      'RECIPE_CONSENT_MUTATING_REQUIRED' (INV-03 dual-field byte-exact).
    - After setOriginMutating('https://app.slack.com', true) → decision === 'allow'.
    - A READ entry on app.slack.com (e.g. slack.list_channels, sideEffectClass read) under
      Auto → decision === 'allow' (reads stay open).
  </behavior>
  <action>EDIT tests/sensitive-write-import-gate.test.js: after the amazon block (before the
final summary), add a Phase-41 SC2 block mirroring the discord block EXACTLY but for slack.
Reuse the already-loaded Denylist + Gate + Store (the file already does
installChromeStorageStub + await Denylist.load() + globalThis.FsbServiceDenylist = Denylist).
Define SLACK_ORIGIN='https://app.slack.com'; assert classify(SLACK_ORIGIN).sensitive===true &&
denied===false. Store._reset(); setOriginMode(SLACK_ORIGIN,'auto'). Evaluate the READ first
(slug 'slack.list_channels', entry tier:'T1a' descriptor sideEffectClass:'read') → allow.
Then evaluate the WRITE (slug 'slack.send_message', entry tier:'T1a' descriptor
sideEffectClass:'write') with flag false → assert decision !== 'allow' AND the three byte-equal
fields === 'RECIPE_CONSENT_MUTATING_REQUIRED'. Then setOriginMutating(SLACK_ORIGIN,true) and
re-evaluate the write → allow. Add a comment: this is the SC2 T1a-handler proof — the gate
sees tier:'T1a' (the discord/amazon blocks used tier:'T3'), proving a hand-ported T1a write is
gated IDENTICALLY to a DOM write because the gate runs BEFORE tier dispatch; the handler then
fail-closes on [ASSUMED] (distinct concern). Per D (SC2): prove it through the LIVE committed
roster, default to the shipped descriptor + the gate (no new risky write head).</action>
  <verify>
    <automated>node tests/sensitive-write-import-gate.test.js 2>&1 | grep -iE "slack" | grep -i "MUTATING_REQUIRED" && node tests/sensitive-write-import-gate.test.js</automated>
  </verify>
  <done>The SC2 slack block passes: the T1a write on the sensitive app.slack.com origin returns dual-field RECIPE_CONSENT_MUTATING_REQUIRED without the flag, allows with setOriginMutating, and the read allows under Auto. The whole suite exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: Consolidate the notion + slack guarded-write live-UAT rows</name>
  <files>.planning/phases/41-depth-2-remaining-hand-ports-guarded-writes/41-HUMAN-UAT.md</files>
  <action>EDIT .planning/phases/41-depth-2-remaining-hand-ports-guarded-writes/41-HUMAN-UAT.md
(created by Plan 02 with the gitlab rows): append rows for the notion writes (notion.create_page
/ update_page / create_database_item — Procedure: capture the real /api/v3 submitTransaction
RPC body on an authenticated www.notion.so tab) and the slack write (slack.send_message —
Procedure: capture the real chat.postMessage form-body incl the xoxc-in-body placement on an
authenticated app.slack.com tab). Keep the SAME table columns (ID | Service | op | Procedure |
Expected | Status), each Status = human_needed (writes ship FAIL-CLOSED). Add a one-line note
that slack.send_message is ALSO the SC2 sensitive-origin proof vehicle (its gate behavior is
CI-proven; only its mutation body is human_needed). Per D (Live-UAT recording): recorded, NOT
fabricated; non-blocking for CI.</action>
  <verify>
    <automated>UAT=.planning/phases/41-depth-2-remaining-hand-ports-guarded-writes/41-HUMAN-UAT.md; grep -q "notion.create_page" "$UAT" && grep -q "slack.send_message" "$UAT" && grep -q "gitlab.create_issue" "$UAT" && echo "all 7 guarded-write UAT rows consolidated"</automated>
  </verify>
  <done>41-HUMAN-UAT.md carries all 7 guarded-write rows (3 gitlab + 3 notion + 1 slack), each human_needed + non-blocking, slack.send_message noted as the SC2 vehicle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI/router -> slack write handler | a write to app.slack.com posts a message on the user's workspace (a sensitive-origin mutation) |
| consent gate -> tier dispatch | the gate (capability-router.js:720) runs BEFORE tier dispatch (:743); a sensitive-origin T1a write must be re-gated here identically to a DOM write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-10 | Elevation | a T1a slack write executing under Auto without the mutating opt-in -> a message posted on the user's account | mitigate | the posture-B re-gate (capability-router.js:476-496) returns RECIPE_CONSENT_MUTATING_REQUIRED for a sensitive-origin write without the flag — PROVEN end-to-end through the live roster on the SHIPPED slack.send_message descriptor (SC2 block) |
| T-41-11 | Spoofing | slack.send_message stamping success for an unverified mutation | mitigate | fail-closed: handle() returns RECIPE_DOM_FALLBACK_PENDING and never calls callSlackMethod/executeBoundSpec (proven by guarded-write-failclosed.test.js) |
| T-41-12 | Tampering | slack.send_message colliding with / silently re-tiering the executable slack.chat.postMessage | mitigate | distinct slugs (send_message has an opentabs descriptor; chat.postMessage is hand-only) — the upgrade harness asserts send_message resolves T1a byte-exact while the head-handlers suite confirms chat.postMessage still resolves |
| T-41-SC | Tampering | npm/pip/cargo installs | accept | NO new packages this phase; no install task exists |
</threat_model>

<verification>
- `node tests/guarded-write-failclosed.test.js` — slack.send_message GREEN.
- `node tests/sensitive-write-import-gate.test.js` — the Phase-41 slack SC2 block passes; suite exits 0.
- `node tests/head-handler-upgrade.test.js` — slack.send_message resolves T1a + write class.
- `node tests/head-handler-cap.test.js` + `node tests/capability-head-handlers.test.js` — green.
- 41-HUMAN-UAT.md carries all 7 guarded-write rows.
</verification>

<success_criteria>
- slack.send_message is a guarded write head (fail-closed, distinct from chat.postMessage), upgrades its opentabs descriptor dom->T1a.
- SC2 PROVEN: a T1a write on the sensitive app.slack.com origin returns dual-field RECIPE_CONSENT_MUTATING_REQUIRED without the flag, allows with setOriginMutating, reads allow under Auto — through the LIVE roster.
- The live-UAT manifest consolidates all 7 guarded-write rows. Head cap unchanged (4 modules).
</success_criteria>

<output>
Create `.planning/phases/41-depth-2-remaining-hand-ports-guarded-writes/41-04-SUMMARY.md` when done
</output>
