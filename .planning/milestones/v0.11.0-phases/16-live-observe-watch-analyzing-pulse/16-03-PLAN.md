---
phase: 16-live-observe-watch-analyzing-pulse
plan: 03
type: execute
wave: 2
depends_on: ["16-01", "16-02"]
files_modified:
  - extension/content/messaging.js
autonomous: true
requirements: [WATCH-01, VIS-01, VIS-03]
tags: [trigger, messaging, content-router, pulse]

must_haves:
  truths:
    - "messaging.js router has triggerObserveStart → FSB.triggerObserve.start(trigger_id, selector, extract, attrName) and triggerObserveStop → FSB.triggerObserve.stop(trigger_id)"
    - "messaging.js router has triggerPulseStart → resolve selector + FSB.actionGlowOverlay.showPulse(el) and triggerPulseStop → FSB.actionGlowOverlay.clearPulse()"
    - "messaging.js router has triggerRead → resolve + readValue + sendResponse({value}) for the test-arm / watchdog read path"
    - "Every new case follows the (async()=>{...})(); return true; async-sendResponse pattern with try/catch → sendResponse({success:false, error})"
    - "showPulse is gated so it does not fight an active run_task glow (one overlay owner per tab)"
  artifacts:
    - path: "extension/content/messaging.js"
      provides: "Additive router cases wiring the Plan 01 observer API and the Plan 02 pulse API into the content message router"
      contains: "triggerObserveStart"
  key_links:
    - from: "messaging.js triggerObserveStart case"
      to: "FSB.triggerObserve.start"
      via: "content router dispatch"
      pattern: "triggerObserve\\.start"
    - from: "messaging.js triggerPulseStart case"
      to: "FSB.actionGlowOverlay.showPulse"
      via: "content router dispatch"
      pattern: "showPulse"
---

<objective>
Wire the Plan 01 observer API (`FSB.triggerObserve`) and the Plan 02 pulse API (`ActionGlowOverlay.showPulse`/`clearPulse`) into the content message router `extension/content/messaging.js` by adding five additive `switch` cases — `triggerObserveStart`, `triggerObserveStop`, `triggerRead`, `triggerPulseStart`, `triggerPulseStop` — each mirroring the shipping `highlightElement` precedent (selector resolve → action → `sendResponse`, `return true` for async). This is the content-side dispatch half of WATCH-01 (arm/stop the observer) and the pulse drive of VIS-01/VIS-03 (start/clear the pulse on SW command).

Purpose: the SW (Plan 04) sends `triggerObserveStart{selector,extract,trigger_id}` + `triggerPulseStart{selector}` on arm and `triggerObserveStop`/`triggerPulseStop` on fire/stop. The router translates those messages into the module calls. The pulse is a STATE of the single `actionGlowOverlay` (one overlay owner per tab, D-10) — gated against an active `run_task` glow, never a second overlay.
Output: five additive router cases in `messaging.js`. No new file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/16-live-observe-watch-analyzing-pulse/16-CONTEXT.md
@.planning/phases/16-live-observe-watch-analyzing-pulse/16-RESEARCH.md
@.planning/phases/16-live-observe-watch-analyzing-pulse/16-PATTERNS.md
@.planning/phases/16-live-observe-watch-analyzing-pulse/16-01-SUMMARY.md
@.planning/phases/16-live-observe-watch-analyzing-pulse/16-02-SUMMARY.md

<interfaces>
<!-- Contracts from Plans 01 and 02 (read their SUMMARYs for final signatures). -->

From Plan 01 (extension/content/trigger-observe.js):
  FSB.triggerObserve.start(triggerId, selector, extract, attrName) -> { ok, reason?, already? }
  FSB.triggerObserve.stop(triggerId)
  FSB.triggerObserve.readValue(leaf, extract, attrName) -> { text, attributes? }   // for the triggerRead path

From Plan 02 (extension/content/visual-feedback.js):
  FSB.actionGlowOverlay.showPulse(element)   // adds 'trigger-pulse' to the single overlay
  FSB.actionGlowOverlay.clearPulse()         // removes it + destroys

Existing messaging.js shapes to mirror:
  case 'highlightElement' (:1229-1243) — async resolve->action->sendResponse, return true
  FSB.querySelectorWithShadow(request.selector) — the resolve call
  switch default + return true (:1424-1428); listener chrome.runtime.onMessage.addListener(handleBackgroundMessage) (:1432)
  single-overlay watchdog (:1199-1209) + actionGlowOverlay.hide() gate (:1196) — the pulse is a STATE of this overlay
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the five additive trigger router cases to messaging.js (observe start/stop, read, pulse start/stop)</name>
  <files>extension/content/messaging.js</files>
  <read_first>
    - extension/content/messaging.js:1196-1209 (single-overlay watchdog + actionGlowOverlay.hide gate), 1229-1243 (highlightElement case — THE async-sendResponse precedent to mirror), 1424-1432 (switch default + return true + named-listener registration) — THE ANALOG (itself)
    - extension/content/trigger-observe.js — the Plan 01 module the cases call (FSB.triggerObserve.start/stop/readValue)
    - extension/content/visual-feedback.js (ActionGlowOverlay.showPulse/clearPulse from Plan 02) — the pulse API the cases call
    - extension/content/selectors.js:497 — FSB.querySelectorWithShadow for triggerRead/triggerPulseStart resolution
    - 16-RESEARCH.md (Pattern: messaging router cases) + 16-PATTERNS.md:136-165 — the messaging.js file->analog map with the exact case wiring + the one-overlay gate note
    - 16-CONTEXT.md D-01, D-10 — additive router cases only; pulse is a STATE of the single overlay
    - .planning/phases/16-live-observe-watch-analyzing-pulse/16-01-SUMMARY.md + 16-02-SUMMARY.md — final API signatures + the chosen action-string names if the upstream plans fixed them
  </read_first>
  <action>
    Inside the `handleBackgroundMessage` switch in `messaging.js` (the switch whose default is at :1424, registered at :1432), ADD five cases, each mirroring the `highlightElement` `(async()=>{ try { ... sendResponse(...) } catch(e){ sendResponse({success:false, error:e.message}) } })(); return true;` shape (:1229-1243). Action-string names follow the `domStream*`/`highlightElement` naming convention (Claude's discretion per D-04/CONTEXT; use the names recorded in the Plan 01 SUMMARY for the observe/report wire so the SW value-report case in Plan 04 matches):

    - `triggerObserveStart`: call `FSB.triggerObserve.start(request.trigger_id, request.selector, request.extract, request.attrName)`; `sendResponse(result)`.
    - `triggerObserveStop`: call `FSB.triggerObserve.stop(request.trigger_id)`; `sendResponse({ success:true })`.
    - `triggerRead`: `leaf = FSB.querySelectorWithShadow(request.selector)`; `value = FSB.triggerObserve.readValue(leaf, request.extract, request.attrName)`; `sendResponse({ value })` (the synchronous read path the test-arm / SW watchdog uses).
    - `triggerPulseStart`: `el = FSB.querySelectorWithShadow(request.selector)`; if `el`, call `FSB.actionGlowOverlay.showPulse(el)` and `sendResponse({ success:true })`, else `sendResponse({ success:false, error:'Element not found' })`. GATE against an active `run_task` glow so the pulse does not fight it — the pulse is a STATE of the single `actionGlowOverlay` (D-10, one overlay owner per tab; respect the :1196 hide-gate / :1199-1209 watchdog discipline).
    - `triggerPulseStop`: call `FSB.actionGlowOverlay.clearPulse()`; `sendResponse({ success:true })`.

    Guard each case with `typeof FSB.triggerObserve !== 'undefined'` / `FSB.actionGlowOverlay` existence defensively (the modules load before messaging.js per the CONTENT_SCRIPT_FILES order set in Plan 04, but a missing module must `sendResponse` an error, not throw). No fenced code in this action — mirror the `highlightElement` block in `<read_first>`.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('extension/content/messaging.js','utf8'); const need=['triggerObserveStart','triggerObserveStop','triggerRead','triggerPulseStart','triggerPulseStop','showPulse','clearPulse']; const miss=need.filter(n=>!s.includes(n)); if(miss.length){console.error('MISSING:',miss);process.exit(1)} if(!/triggerObserve\.start/.test(s)){console.error('MISSING triggerObserve.start call');process.exit(1)} console.log('OK: messaging router cases present')"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `extension/content/messaging.js` contains all five case labels (`triggerObserveStart`, `triggerObserveStop`, `triggerRead`, `triggerPulseStart`, `triggerPulseStop`), a `FSB.triggerObserve.start` call, and `showPulse`/`clearPulse` calls.
    - SOURCE: each new case ends with `return true` (async sendResponse) and is wrapped in a `try/catch` that calls `sendResponse` with an error on failure (mirror highlightElement).
    - `npm test` remains green (no regression to the existing messaging-touching tests).
  </acceptance_criteria>
  <done>The content router dispatches the five trigger messages to the Plan 01 observer API and the Plan 02 pulse API; the pulse is gated as a state of the single overlay; every case uses the async-sendResponse + try/catch contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service worker -> content router | The SW sends observe/pulse/read commands; these arrive at the content router via `chrome.runtime.onMessage`. The router calls only in-extension `FSB.*` module APIs — it never evals message content or injects it into the DOM. |
| content router -> page DOM | `triggerPulseStart`/`triggerRead` resolve a selector against the page; resolution goes through the sanitizing `FSB.querySelectorWithShadow`, never a raw `eval`/`innerHTML`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03-INJECT | Tampering | selector handling in the new cases | mitigate | `triggerRead`/`triggerPulseStart` resolve via `FSB.querySelectorWithShadow` (selectors.js — sanitizes input, no eval); the router never `innerHTML`s or evals `request` fields. The reported value is the typed scalar produced by Plan 01 `readValue`. |
| T-16-03-OVERLAY | Denial of Service (UX) | pulse vs run_task glow collision | mitigate | The pulse is a STATE of the single `actionGlowOverlay` (one overlay owner per tab, D-10); `triggerPulseStart` is gated against an active run_task glow (respects the :1196 hide-gate / :1199-1209 single-overlay watchdog), never spawning a second overlay. |
| T-16-03-THROW | Denial of Service | missing-module / resolution failure | mitigate | Each case is wrapped in try/catch → `sendResponse({success:false, error})`, and guards `typeof FSB.triggerObserve/actionGlowOverlay` defensively so a load-order or resolution failure returns an error instead of throwing in the listener (mirror highlightElement). |
| T-16-03-SC | Tampering | npm/pip/cargo installs | mitigate | Phase 16 installs ZERO packages (16-RESEARCH.md § Package Legitimacy Audit: not applicable). No install task in this plan. |
</threat_model>

<verification>
- The `node -e` source check confirms all five case labels + the observer/pulse API calls are present.
- `npm test` green (Wave 1's `trigger-observe.test.js` + `trigger-observe-pulse.test.js` already in the chain; no regression).
- `INV-04` untouched (no `agent-loop.js` edit). `INV-01` held (no MCP tool-schema change — `messaging.js` is the content router, not a tool definition).
</verification>

<success_criteria>
- WATCH-01 (content dispatch half): `triggerObserveStart`/`triggerObserveStop`/`triggerRead` route to `FSB.triggerObserve` — verified.
- VIS-01/VIS-03 (pulse drive): `triggerPulseStart`/`triggerPulseStop` route to `showPulse`/`clearPulse`, gated as a single-overlay state — verified.
- All five cases use the async-sendResponse + try/catch contract.
- `npm test` passes.
</success_criteria>

<output>
Create `.planning/phases/16-live-observe-watch-analyzing-pulse/16-03-SUMMARY.md` when done. Record the final action-string names for all five cases so the Plan 04 SW-side `chrome.tabs.sendMessage`/`triggerObserveStart`/`triggerPulseStart` sends use the exact same strings.
</output>
