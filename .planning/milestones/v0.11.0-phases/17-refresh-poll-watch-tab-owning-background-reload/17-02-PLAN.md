---
phase: 17-refresh-poll-watch-tab-owning-background-reload
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - extension/content/messaging.js
  - tests/trigger-observe.test.js
autonomous: true
requirements: [WATCH-02]
user_setup: []

must_haves:
  truths:
    - "triggerRead distinguishes a missing watched element from a legitimate empty string"
    - "triggerRead never calls FSB.triggerObserve.readValue(null, ...) for missing selectors"
    - "Successful triggerRead responses preserve the existing { text, attributes? } value shape"
    - "The content-side missing-element outcome is typed as ELEMENT_NOT_FOUND for service-worker handling"
  artifacts:
    - path: "extension/content/messaging.js"
      provides: "triggerRead explicit missing-element response"
      contains: "ELEMENT_NOT_FOUND"
    - path: "tests/trigger-observe.test.js"
      provides: "Node/source coverage that triggerRead has a missing-element branch before readValue"
      contains: "ELEMENT_NOT_FOUND"
  key_links:
    - from: "extension/content/messaging.js triggerRead"
      to: "extension/content/trigger-observe.js readValue"
      via: "only call readValue when querySelectorWithShadow returns a leaf"
      pattern: "ELEMENT_NOT_FOUND"
---

<objective>
Make the shared `triggerRead` content route return an explicit missing-element outcome before refresh-poll starts using it after reloads.

Purpose: WATCH-02 needs to distinguish value-unchanged, value-changed, and element-not-found. The current `readValue(null) -> { text:'' }` behavior is valid for the live-observe utility but unsafe for refresh-poll because a missing element can be evaluated as empty text or the wrong page.
Output: an additive `triggerRead` missing-element response and deterministic coverage in `tests/trigger-observe.test.js`.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-refresh-poll-watch-tab-owning-background-reload/17-CONTEXT.md
@.planning/phases/17-refresh-poll-watch-tab-owning-background-reload/17-RESEARCH.md
@.planning/phases/17-refresh-poll-watch-tab-owning-background-reload/17-PATTERNS.md
@extension/content/messaging.js
@extension/content/trigger-observe.js
@tests/trigger-observe.test.js

<interfaces>
Current content route:
```javascript
case 'triggerRead':
  const leaf = FSB.querySelectorWithShadow(request.selector);
  const value = FSB.triggerObserve.readValue(leaf, request.extract, request.attrName || request.attribute);
  sendResponse({ value });
```

Required response shapes:
```javascript
// Success
{ success: true, ok: true, value: { text: '<trimmed>', attributes?: { [name]: '<trimmed>' } } }

// Missing element
{ success: false, ok: false, code: 'ELEMENT_NOT_FOUND', reason: 'element_not_found', selector: request.selector }
```

Do not change `FSB.triggerObserve.readValue(null)` in `trigger-observe.js`; the safer path is a `triggerRead` branch before calling it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add an explicit ELEMENT_NOT_FOUND branch to triggerRead before readValue</name>
  <files>extension/content/messaging.js</files>
  <read_first>
    - extension/content/messaging.js (file being modified)
    - extension/content/trigger-observe.js readValue and resolve patterns
    - .planning/phases/17-refresh-poll-watch-tab-owning-background-reload/17-CONTEXT.md D-10, D-11
    - .planning/phases/17-refresh-poll-watch-tab-owning-background-reload/17-RESEARCH.md Pitfall 3 and Pattern 3
    - .planning/phases/17-refresh-poll-watch-tab-owning-background-reload/17-PATTERNS.md extension/content/messaging.js section
  </read_first>
  <behavior>
    - If `request.selector` is missing or `FSB.querySelectorWithShadow(request.selector)` returns null, `triggerRead` sends `{ success:false, ok:false, code:'ELEMENT_NOT_FOUND', reason:'element_not_found', selector }`.
    - In the missing case, `FSB.triggerObserve.readValue` is not called.
    - In the success case, `triggerRead` still uses `FSB.querySelectorWithShadow` and `FSB.triggerObserve.readValue(leaf, request.extract, request.attrName || request.attribute)`.
  </behavior>
  <action>
    Modify only the `case 'triggerRead':` block in `extension/content/messaging.js`. Keep the async IIFE, `try/catch`, `sendResponse`, and `return true` shape. After the existing missing-module guard, read `const selector = request.selector;` and `const leaf = selector ? FSB.querySelectorWithShadow(selector) : null;`. If `!leaf`, send exactly `{ success: false, ok: false, code: 'ELEMENT_NOT_FOUND', reason: 'element_not_found', selector }` and return before calling `FSB.triggerObserve.readValue`. On success, keep the current shared value path and send `{ success: true, ok: true, value }`.

    Do not modify `extension/content/trigger-observe.js` in this task. Do not alter `triggerObserveStart`, `triggerPulseStart`, or any action-glow ownership gates.
  </action>
  <verify>
    <automated>node --check extension/content/messaging.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check extension/content/messaging.js` exits 0.
    - `grep -c "ELEMENT_NOT_FOUND" extension/content/messaging.js` is at least 1.
    - `grep -n "case 'triggerRead'" -A25 extension/content/messaging.js` shows `ELEMENT_NOT_FOUND` before `readValue`.
    - `grep -c "readValue(null" extension/content/messaging.js` returns 0.
  </acceptance_criteria>
  <done>`triggerRead` has a typed missing-element branch before value extraction and successful reads retain the locked `{ text, attributes? }` value shape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add deterministic coverage for triggerRead missing-element semantics</name>
  <files>tests/trigger-observe.test.js</files>
  <read_first>
    - tests/trigger-observe.test.js (file being modified)
    - extension/content/messaging.js (source under test)
    - extension/content/trigger-observe.js (readValue contract source)
    - .planning/phases/17-refresh-poll-watch-tab-owning-background-reload/17-VALIDATION.md Wave 0 Requirements
    - tests/dom-stream-perf.test.js source-grep assertion style
  </read_first>
  <behavior>
    - The test suite proves `extension/content/messaging.js` contains an `ELEMENT_NOT_FOUND` branch in the `triggerRead` case.
    - The test suite proves the `ELEMENT_NOT_FOUND` branch appears before the `readValue(...)` call inside that case.
    - Existing trigger-observe readValue shape tests remain green.
  </behavior>
  <action>
    Extend `tests/trigger-observe.test.js` with a source-invariant block that reads `extension/content/messaging.js`, extracts the `case 'triggerRead'` block through the following `return true;`, and asserts:
    `ELEMENT_NOT_FOUND` is present; `reason:'element_not_found'` or `reason: 'element_not_found'` is present; `readValue` is present; and the first index of `ELEMENT_NOT_FOUND` is lower than the first index of `readValue`. Also assert the block includes `success: true` and `ok: true` on the success path so the service worker can consume a typed success result while preserving `value`.

    Keep the existing trigger-observe VM tests unchanged, especially the locked `readValue` shape assertions. Do not add a new test file for this task; the validation strategy explicitly allows extending `tests/trigger-observe.test.js`.
  </action>
  <verify>
    <automated>node tests/trigger-observe.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/trigger-observe.test.js` exits 0.
    - `grep -c "ELEMENT_NOT_FOUND" tests/trigger-observe.test.js` is at least 1.
    - Existing locked shape assertions remain in the file: `grep -c "readValue emits the locked text and attribute shapes" tests/trigger-observe.test.js` returns 1.
  </acceptance_criteria>
  <done>The test suite now protects the missing-element branch and confirms it runs before `readValue`, closing the validation item for stale/missing selectors before refresh-poll consumes `triggerRead`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page DOM -> content script triggerRead | A selector may resolve to no element after reload, SPA replacement, auth redirect, or challenge page. |
| content script -> service worker | The content route response crosses into SW refresh-poll handling; it must distinguish missing element from legitimate empty text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | Tampering | stale selector / missing element treated as empty text | mitigate | Add `ELEMENT_NOT_FOUND` in `triggerRead` before `readValue`; tests assert `ELEMENT_NOT_FOUND` appears before `readValue` in the case block. Plan 17-03 persists this as `needs_attention` without evaluating. |
| T-17-02 | Spoofing / Tampering | challenge/login/CAPTCHA page false fire | transfer | This plan only reports missing element. Plan 17-04 classifies blocked pages and prevents challenge content evaluation. |
| T-17-01 | Tampering / Denial of Service | cross-agent tab reload before ownership validation | transfer | This plan performs no reload. Plan 17-03 owns ownership validation before reload. |
| T-17-03 | Denial of Service | sub-floor refresh-poll reload hammering | transfer | Plan 17-01 owns interval rejection and floor-safe scheduling. |
</threat_model>

<verification>
Run:
`node --check extension/content/messaging.js && node tests/trigger-observe.test.js`
</verification>

<success_criteria>
- `triggerRead` no longer collapses missing elements into `{ text:'' }`.
- `ELEMENT_NOT_FOUND` is typed and test-protected.
- Successful reads still return the shared `{ text, attributes? }` value shape.
</success_criteria>

<output>
After completion, create `.planning/phases/17-refresh-poll-watch-tab-owning-background-reload/17-02-SUMMARY.md`.
</output>
