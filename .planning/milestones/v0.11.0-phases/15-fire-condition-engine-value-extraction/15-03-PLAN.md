---
phase: 15-fire-condition-engine-value-extraction
plan: 03
type: execute
wave: 3
depends_on: [15-01, 15-02]
files_modified:
  - extension/utils/trigger-lifecycle.js
  - extension/background.js
  - tests/trigger-lifecycle.test.js
autonomous: true
requirements: [TRIG-02, TRIG-03, TRIG-04, TRIG-05, TRIG-06, TRIG-07, EXTRACT-01, EXTRACT-02, EXTRACT-03, EXTRACT-04, LIFE-04]
user_setup: []

must_haves:
  truths:
    - "When evaluate() returns outcome 'fired', the SEAM writes status:'fired' + fired_at to storage ATOMICALLY and clears the alarm (disarm)"
    - "When evaluate() returns no_fire/parse_error/pattern_error, the SEAM merges next_state (last_value, was_satisfied, last_evaluated_at) and the snapshot STAYS armed"
    - "A parse_error outcome NEVER writes status:'fired' (never fires)"
    - "A duplicate alarm tick after a fire sees status:'fired' via the existing noop_terminal guard and no-ops (no double fire across re-read)"
    - "value-extractor.js and trigger-manager.js are loaded in background.js importScripts in the correct order (extractor before store; manager after store, before lifecycle)"
    - "D-02: the trigger-lifecycle SEAM owns the storage re-read + atomic fired write-back around the pure evaluate()"
    - "D-07: atomic disarm + dedupe live in the SEAM write-back, guarded by the existing noop_terminal terminal check"
  artifacts:
    - path: "extension/utils/trigger-lifecycle.js"
      provides: "the evaluated_noop SEAM replaced with evaluate() + atomic fired write-back + edge-state persist"
      contains: "FsbTriggerManager"
    - path: "extension/background.js"
      provides: "two additive importScripts lines (value-extractor.js, trigger-manager.js)"
      contains: "value-extractor.js"
    - path: "tests/trigger-lifecycle.test.js"
      provides: "SEAM integration cases: fired -> atomic write-back + disarm; parse_error -> no fire; dedupe holds"
      contains: "evaluate"
  key_links:
    - from: "extension/utils/trigger-lifecycle.js"
      to: "extension/utils/trigger-manager.js"
      via: "FsbTriggerManager.evaluate(snap, reportedValue, now) at the SEAM (replacing evaluated_noop)"
      pattern: "FsbTriggerManager.evaluate"
    - from: "extension/background.js"
      to: "extension/utils/trigger-manager.js"
      via: "importScripts load order (after trigger-store, before trigger-lifecycle)"
      pattern: "importScripts.*trigger-manager"
---

<objective>
Wire Plan 02's pure `evaluate()` into the Phase-14 survivability scaffold. This plan MODIFIES three files: (1) replace the `evaluated_noop` SEAM in `trigger-lifecycle.js` (the comment-marked return at ~:282-290) with a call to `FsbTriggerManager.evaluate(...)` plus the ATOMIC `fired` write-back + disarm (on fire) or the `next_state` merge + stay-armed (on non-fire); (2) add the two additive `importScripts` lines in `background.js` so the SW loads `value-extractor.js` + `trigger-manager.js` in the correct order; (3) extend `tests/trigger-lifecycle.test.js` with the SEAM integration cases.

Purpose: This closes the "exactly one fire across SW eviction" guarantee. `evaluate()` is pure and never writes; the SEAM (storage-of-truth side) owns the terminal write + disarm, backstopped by the existing `noop_terminal` guard. This is the integration point where TRIG-02..07 / EXTRACT-* become live in the running service worker. INV-04 (agent-loop.js byte-frozen) and INV-01 (MCP schemas byte-identical) hold - no tool registration here (that is Phase 18).

Output: the SEAM edit, the two importScripts lines, and the extended lifecycle test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-fire-condition-engine-value-extraction/15-CONTEXT.md
@.planning/phases/15-fire-condition-engine-value-extraction/15-RESEARCH.md
@.planning/phases/15-fire-condition-engine-value-extraction/15-PATTERNS.md
@.planning/phases/15-fire-condition-engine-value-extraction/15-02-SUMMARY.md
@extension/utils/trigger-lifecycle.js
@extension/utils/trigger-manager.js

<interfaces>
<!-- The exact SEAM region and the manager surface the SEAM calls. -->

The SEAM to REPLACE (extension/utils/trigger-lifecycle.js, the current placeholder at ~:282-290):
```javascript
    // ----------------------------------------------------------------------
    // Phase 15 SEAM: plug the evaluate-and-fire step in HERE.
    // ----------------------------------------------------------------------
    return { ok: true, action: 'evaluated_noop' };
```
At this point in handleTriggerAlarm: `snap` is the fresh storage re-read (:259), `store` is in scope (:251), `now` is set (:271), `triggerId` is set (:246), the TTL reap (:276-279) and the `noop_terminal` guard (:267-269) have ALREADY run above. In-module helper: `clearAlarm(name)` (:120-129). `alarm.name` is the `fsbTrigger:<id>` name.

The existing dedupe guard ABOVE the SEAM (PRESERVE, do NOT remove) - trigger-lifecycle.js:267-269:
```javascript
if (snap.status === 'fired' || snap.status === 'stopped') {
  return { ok: true, action: 'noop_terminal' };
}
```

The manager surface the SEAM calls (Plan 02 output):
```javascript
// FsbTriggerManager.evaluate(snapshot, reportedValue, now) -> { outcome, matched_condition?, old_value, new_value, next_state }
//   outcome in 'fired' | 'no_fire' | 'parse_error' | 'pattern_error'
//   next_state = { last_value, was_satisfied, last_evaluated_at }
global.FsbTriggerManager = { evaluate, armTrigger, getCap, setCap, ... };
```

The background.js importScripts region to mirror (extension/background.js:34-42, VERIFIED):
```javascript
try { importScripts('utils/trigger-store.js'); } catch (e) { console.error('[FSB] Failed to load trigger-store.js:', e.message); }
try { importScripts('utils/trigger-lifecycle.js'); } catch (e) { console.error('[FSB] Failed to load trigger-lifecycle.js:', e.message); }
```

reportedValue source note: In Phase 15 the watch layer (Phase 16/17) does not exist yet, so the SEAM has no LIVE DOM value to pass. The reportedValue is the INPUT the future watch layer will supply. For Phase 15 the SEAM constructs the reportedValue from what the snapshot already carries (there is no scrape) - pass `{ text: snap.reported_value != null ? snap.reported_value : snap.last_value }` so the wiring is exercised and the contract is concrete; the live value arrives in Phase 16/17. Document this as the consumer contract (see output).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend trigger-lifecycle.test.js with SEAM integration cases (fired -> atomic write-back + disarm; parse_error -> no fire; dedupe holds), RED</name>
  <files>tests/trigger-lifecycle.test.js</files>
  <read_first>
    - .planning/phases/15-fire-condition-engine-value-extraction/15-VALIDATION.md (the P3 row in the Per-Task Verification Map: "SEAM: evaluate() result drives atomic fired write-back; noop_terminal dedupe holds across re-read; no fire on parse_error")
    - tests/trigger-lifecycle.test.js (the existing harness: createStorageArea()/createChromeMock()/setupHarness() :66-151; Case C noop_terminal double-call; Case E evaluated_noop; Case F eviction-between-reads :271-286 - the templates to extend)
    - extension/utils/trigger-lifecycle.js (the handleTriggerAlarm outcomes :226-290; the SEAM region)
    - extension/utils/trigger-manager.js (Plan 02 - the evaluate() surface the SEAM now calls; the test must install global.FsbTriggerManager so handleTriggerAlarm can reach it)
  </read_first>
  <behavior>
    These cases run handleTriggerAlarm(alarm) through the new SEAM. The harness must install a real-or-stub global.FsbTriggerManager (require the real Plan-02 module via the dual-export, OR a controllable stub whose evaluate() returns a scripted outcome) AND a real FsbTriggerStore over the session mock, then seed an armed snapshot.
    - FIRED path: seed an armed snapshot with a condition + baseline + last_value such that evaluate() returns outcome 'fired' (or stub evaluate -> 'fired' with a next_state). Call handleTriggerAlarm. Assert: the snapshot in storage now has status==='fired' and a fired_at set; the alarm was cleared (_cleared() shows the alarm name); the handler returned an ok result whose action is NOT 'evaluated_noop'.
    - NO-FIRE path: evaluate() returns 'no_fire' with next_state. Call handleTriggerAlarm. Assert: the snapshot stays status==='armed'; last_value and was_satisfied were merged from next_state; the alarm was NOT cleared.
    - PARSE_ERROR-NEVER-FIRES: evaluate() returns 'parse_error'. Call handleTriggerAlarm. Assert: the snapshot stays status==='armed' (NOT 'fired'); no fired_at; the alarm stays armed. This is the load-bearing EXTRACT-04 integration assertion.
    - DEDUPE across re-read (mirror existing Case C + F): after a fired write-back, call handleTriggerAlarm AGAIN for the same alarm; assert it returns action 'noop_terminal' (the existing :267-269 guard sees status:'fired') and _cleared() did NOT double-fire (the disarm is idempotent). This proves no double fire across an eviction-style re-read.
  </behavior>
  <action>
    Extend `tests/trigger-lifecycle.test.js` (append new cases; do NOT rewrite existing ones - the Phase-14 cases A-N must still pass). Reuse the existing createStorageArea()/createChromeMock()/setupHarness() (:66-151). In setupHarness (or a new local helper) ALSO install `global.FsbTriggerManager`: prefer requiring the REAL Plan-02 module (`require('../extension/utils/trigger-manager.js')` after the chrome mock is installed so its lazy resolvers bind) so the integration is end-to-end; if scripting a deterministic outcome is cleaner for a given case, install a stub `globalThis.FsbTriggerManager = { evaluate: function(){ return scriptedOutcome; } }` and restore it after the case. For each case, seed the armed snapshot via the store's writeSnapshot, build the alarm object `{ name: 'fsbTrigger:<id>' }`, call `await handleTriggerAlarm(alarm)`, then assert against the re-read snapshot (`await store.readSnapshot(id)`) and the alarm mock's `_cleared()` history. Add the four cases from the behavior block with clear check() messages. This is RED before the SEAM edit lands: with the current `evaluated_noop` SEAM the FIRED/NO-FIRE/PARSE_ERROR assertions fail (the handler still returns evaluated_noop and never writes status).
  </action>
  <verify>
    <automated>node tests/trigger-lifecycle.test.js; test $? -ne 0 && echo "RED-OK: SEAM cases fail before edit" || echo "UNEXPECTED-GREEN (seam may already be wired)"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/trigger-lifecycle.test.js` contains new cases referencing `FsbTriggerManager` and a `status` transition to `fired` (verify: `grep -c "FsbTriggerManager" tests/trigger-lifecycle.test.js` >= 1 AND `grep -c "parse_error" tests/trigger-lifecycle.test.js` >= 1).
    - The Phase-14 cases are preserved (no deletion): `grep -c "noop_terminal" tests/trigger-lifecycle.test.js` >= 1.
    - Running the test exits non-zero (RED: the new SEAM assertions fail because the SEAM still returns evaluated_noop).
    - File ASCII-clean: `LC_ALL=C grep -nP '[\x80-\xFF]' tests/trigger-lifecycle.test.js && echo DIRTY || echo CLEAN` prints CLEAN.
  </acceptance_criteria>
  <done>The lifecycle test is extended with the fired/no-fire/parse_error/dedupe SEAM integration cases (Phase-14 cases preserved) and fails (RED) because the evaluated_noop SEAM is not yet replaced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace the evaluated_noop SEAM in trigger-lifecycle.js with evaluate() + atomic fired write-back + edge-state persist, GREEN</name>
  <files>extension/utils/trigger-lifecycle.js</files>
  <read_first>
    - .planning/phases/15-fire-condition-engine-value-extraction/15-PATTERNS.md ("trigger-lifecycle.js (MODIFY)" :150-182 - the exact swap shape + the LANDMINE that the SEAM is the SOLE owner of storage I/O)
    - .planning/phases/15-fire-condition-engine-value-extraction/15-RESEARCH.md (lines 156-164 the system diagram = the replacement spec; lines 249-254 evaluate does NOT set status:fired)
    - extension/utils/trigger-lifecycle.js (the SEAM region :282-290; the noop_terminal guard :267-269 to PRESERVE; clearAlarm :120-129; the in-scope snap/store/now/triggerId)
    - tests/trigger-lifecycle.test.js (the Task-1 cases to satisfy)
  </read_first>
  <action>
    In `extension/utils/trigger-lifecycle.js`, REPLACE only the `evaluated_noop` return block (the comment-marked region ~:282-290) - leave everything above it (the noop_terminal guard :267-269, the TTL reap :276-279, the storage re-read :259) UNCHANGED. The replacement (15-RESEARCH.md:156-164 is the spec):
    (1) Resolve the manager defensively: `if (typeof FsbTriggerManager === 'undefined' || !FsbTriggerManager || typeof FsbTriggerManager.evaluate !== 'function') { return { ok: true, action: 'evaluated_noop' }; }` (mirror the `typeof FsbTriggerLifecycle` guards already used in background.js - a missing manager at boot degrades to the old no-op, never a throw out of the SW glue).
    (2) Construct the reportedValue from the snapshot (Phase 15 has no live scrape; the watch layer supplies this in Phase 16/17): `var reportedValue = { text: (snap.reported_value != null ? snap.reported_value : snap.last_value) };` (carry attributes if a reserved field exists). Document this as the consumer contract.
    (3) `var outcome = FsbTriggerManager.evaluate(snap, reportedValue, now);`
    (4) If `outcome && outcome.outcome === 'fired'`: set `snap.status = 'fired'; snap.fired_at = now;` then merge `outcome.next_state` (last_value/was_satisfied/last_evaluated_at) into snap, then `await store.writeSnapshot(triggerId, snap);` (the ATOMIC terminal write-back) then `await clearAlarm(alarm.name);` (disarm) and `return { ok: true, action: 'fired', outcome: outcome };`.
    (5) Else (no_fire / parse_error / pattern_error): merge `outcome.next_state` into snap (last_value, was_satisfied, last_evaluated_at) WITHOUT touching status, then `await store.writeSnapshot(triggerId, snap);` (persist edge state, STAY armed) and `return { ok: true, action: outcome.outcome, outcome: outcome };`. LANDMINE: a parse_error/pattern_error MUST NOT set status:'fired' - it stays armed and never fires (EXTRACT-04).
    Defensive shape: if `outcome` is null/malformed (manager returned nothing), fall back to `return { ok: true, action: 'evaluated_noop' };` rather than throwing. LANDMINE (the load-bearing rule): the SEAM is the SOLE owner of storage I/O for the fire path. Do NOT add comparison operators in this module (they live in trigger-manager). The terminal write + the existing noop_terminal guard both living HERE on the storage-of-truth side are what make "exactly one fire across SW eviction" hold. NO EMOJIS, ASCII-clean.
  </action>
  <verify>
    <automated>node tests/trigger-lifecycle.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/trigger-lifecycle.test.js` exits 0 (the new SEAM cases AND all Phase-14 cases A-N pass).
    - `extension/utils/trigger-lifecycle.js` calls the manager (verify: `grep -c "FsbTriggerManager.evaluate" extension/utils/trigger-lifecycle.js` >= 1).
    - The noop_terminal guard is preserved (verify: `grep -c "noop_terminal" extension/utils/trigger-lifecycle.js` >= 1).
    - The fired path writes status:'fired' (verify: `grep -c "status = 'fired'" extension/utils/trigger-lifecycle.js` >= 1) and the SEAM region calls `clearAlarm`.
    - INV-04 held: `extension/ai/agent-loop.js` is byte-untouched (verify: `git diff --quiet extension/ai/agent-loop.js && echo UNTOUCHED || echo CHANGED` prints UNTOUCHED).
    - Source ASCII-clean: `LC_ALL=C grep -nP '[\x80-\xFF]' extension/utils/trigger-lifecycle.js && echo DIRTY || echo CLEAN` prints CLEAN.
  </acceptance_criteria>
  <done>The SEAM now calls evaluate() and owns the atomic fired write-back + disarm (or the next_state merge + stay-armed on non-fire); parse_error never fires; the noop_terminal dedupe holds; all lifecycle tests GREEN; agent-loop.js untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Add the two importScripts lines in background.js (value-extractor + trigger-manager) in correct load order, then run the full Phase-15 gate</name>
  <files>extension/background.js</files>
  <read_first>
    - .planning/phases/15-fire-condition-engine-value-extraction/15-PATTERNS.md ("extension/background.js (MODIFY)" :186-208 - the exact two lines + the LOAD-ORDER LANDMINE)
    - extension/background.js (the Phase-14 trigger glue region :34-42 - the try/catch+console.error importScripts pattern to mirror; the alarm fan-out branch that already calls handleTriggerAlarm so no listener change is needed)
    - .planning/phases/15-fire-condition-engine-value-extraction/15-VALIDATION.md ("Manual-Only Verifications" - empty for Phase 15: all behaviors automated, no live-Chrome step required)
  </read_first>
  <action>
    In `extension/background.js`, add EXACTLY two additive `importScripts` lines in the Phase-14 trigger glue region (around :34-42), each following the existing `try { importScripts('utils/X.js'); } catch (e) { console.error('[FSB] Failed to load X.js:', e.message); }` pattern. LOAD ORDER (the LANDMINE - load-bearing): `value-extractor.js` must load BEFORE `trigger-store.js` (it is pure, no deps, and the manager needs it on the global); `trigger-manager.js` must load AFTER `trigger-store.js` (the cap resolves FsbTriggerStore) and BEFORE `trigger-lifecycle.js` (the SEAM calls FsbTriggerManager). Concretely: insert `try { importScripts('utils/value-extractor.js'); } catch (e) { console.error('[FSB] Failed to load value-extractor.js:', e.message); }` just BEFORE the existing `importScripts('utils/trigger-store.js')` line, and insert `try { importScripts('utils/trigger-manager.js'); } catch (e) { console.error('[FSB] Failed to load trigger-manager.js:', e.message); }` AFTER the `trigger-store.js` line and BEFORE the `trigger-lifecycle.js` line. Make NO other change to background.js. The existing alarm fan-out branch already calls `FsbTriggerLifecycle.handleTriggerAlarm(alarm)` (the SEAM edit lives inside that helper), so the onAlarm listener needs no change. Do NOT touch agent-loop.js (INV-04). Do NOT change any MCP tool schema (INV-01). After the edit, run the full Phase-15 gate (the verify command below): the three new test files + the Phase-14 pair, then the full `npm test`. Phase 15 has NO manual-only / live-Chrome verification (15-VALIDATION.md): the element value is injected as a test input, so the entire phase is deterministically covered browser-free. Real-fire MV3-survival UAT is milestone-end scope (the same deferral Phase 14 used); do NOT add a live-Chrome checkpoint here.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('extension/background.js','utf8');const ve=s.indexOf('value-extractor.js'),ts=s.indexOf('utils/trigger-store.js'),tm=s.indexOf('trigger-manager.js'),tl=s.indexOf('utils/trigger-lifecycle.js');process.exit((ve>-1&&tm>-1&&ve<ts&&ts<tm&&tm<tl)?0:1)" && node tests/value-extractor.test.js && node tests/trigger-manager.test.js && node tests/trigger-cap.test.js && node tests/trigger-store.test.js && node tests/trigger-lifecycle.test.js</automated>
  </verify>
  <acceptance_criteria>
    - background.js loads both new modules in the correct order (the node one-liner: value-extractor < trigger-store < trigger-manager < trigger-lifecycle, exit 0).
    - Exactly two new importScripts lines added (verify: `grep -c "importScripts('utils/value-extractor.js')" extension/background.js` == 1 AND `grep -c "importScripts('utils/trigger-manager.js')" extension/background.js` == 1).
    - All five trigger test files green: `node tests/value-extractor.test.js && node tests/trigger-manager.test.js && node tests/trigger-cap.test.js && node tests/trigger-store.test.js && node tests/trigger-lifecycle.test.js` exits 0.
    - Full suite green: `npm test` exits 0.
    - INV-04 held: `git diff --quiet extension/ai/agent-loop.js && echo UNTOUCHED || echo CHANGED` prints UNTOUCHED.
    - INV-01 held: no MCP tool schema changed - `node scripts/validate-extension.mjs` exits 0 and no `mcp/`/`tool-definitions.*` file is in this plan's diff.
  </acceptance_criteria>
  <done>background.js loads value-extractor.js and trigger-manager.js via importScripts in the load-bearing order (extractor->store->manager->lifecycle); the full Phase-15 test set and npm test are green; INV-01/INV-04 hold; no live-Chrome checkpoint added (Phase 15 is fully automated per VALIDATION.md).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evaluate() outcome -> SEAM storage write | The pure evaluate() result crosses into the storage-of-truth side; a wrong write-back (e.g. firing on parse_error, or a non-atomic write) would break the exactly-once / never-on-NaN guarantees. |
| chrome.alarms tick -> handleTriggerAlarm | A duplicate or replayed alarm tick (e.g. after SW eviction) crosses into the fire path; without the dedupe it could double-fire. |
| SW boot module load order -> SEAM call | If trigger-manager.js is not yet on the global when the SEAM runs, the call must degrade safely, not throw out of the SW glue. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-11 | Tampering / Integrity | firing on a parse_error / pattern_error outcome | mitigate | The SEAM sets `status:'fired'` ONLY when `outcome.outcome === 'fired'`; parse_error/pattern_error merge next_state and STAY armed (never write fired). The PARSE_ERROR-NEVER-FIRES test locks this at the integration level (EXTRACT-04). |
| T-15-12 | Tampering / Denial of Service | double fire across SW eviction (non-atomic fire decision + durable write) | mitigate | evaluate() is pure (no write); the SEAM does the single atomic `writeSnapshot(status:'fired')` then `clearAlarm` (disarm); a duplicate tick after the write hits the existing `noop_terminal` guard (:267-269) and no-ops. The dedupe test (re-call after fire -> noop_terminal, no double _cleared) proves it. |
| T-15-13 | Denial of Service | SEAM throws out of the SW glue when the manager is absent at boot | mitigate | Defensive `typeof FsbTriggerManager === 'undefined'` guard degrades to the old `evaluated_noop` (mirrors background.js's existing typeof guards); a null/malformed outcome also falls back to evaluated_noop rather than throwing. The load-order fix (extractor->store->manager->lifecycle) removes even the transient window. |
| T-15-14 | Elevation of Privilege / Contract break | a change leaking into agent-loop.js or an MCP tool schema | mitigate | INV-04: `git diff --quiet extension/ai/agent-loop.js` asserted UNTOUCHED. INV-01: no `mcp/`/`tool-definitions.*` file in the diff; `node scripts/validate-extension.mjs` exits 0. Tool registration is explicitly Phase 18, not here. |
| T-15-SC | Tampering | npm/pip/cargo installs | mitigate | Phase 15 installs ZERO packages (zero-dep mandate D-03). No dependency, no `package-lock.json` change, no slopcheck surface. The only edits are two importScripts lines + the SEAM swap + test extension. INV-06 untouched. |
</threat_model>

<verification>
- All five trigger test files green: `node tests/value-extractor.test.js && node tests/trigger-manager.test.js && node tests/trigger-cap.test.js && node tests/trigger-store.test.js && node tests/trigger-lifecycle.test.js`.
- Full suite green: `npm test`.
- INV-04: `git diff --quiet extension/ai/agent-loop.js` (untouched).
- INV-01: no MCP schema / tool-definitions change; `node scripts/validate-extension.mjs` exits 0.
- background.js load order: value-extractor < trigger-store < trigger-manager < trigger-lifecycle.
- Phase 15 is fully automated (15-VALIDATION.md "Manual-Only Verifications" is empty); no live-Chrome gate. Real-fire MV3-survival UAT is milestone-end scope.
</verification>

<success_criteria>
- The SEAM drives the atomic fired write-back + disarm on a fire; merges next_state + stays armed on no_fire/parse_error/pattern_error.
- A parse_error NEVER writes status:'fired' (EXTRACT-04 holds at the integration level).
- The noop_terminal dedupe holds across a re-read (no double fire after eviction).
- value-extractor.js + trigger-manager.js load in the correct order in background.js so the SEAM can reach the manager in the SW.
- INV-01 and INV-04 hold; the full test suite is green.
</success_criteria>

<output>
Create `.planning/phases/15-fire-condition-engine-value-extraction/15-03-SUMMARY.md` when done. Record: the reportedValue consumer contract the SEAM now expects (`{ text, attributes? }`, sourced from `snap.reported_value || snap.last_value` until Phase 16/17 supplies a live value) so the Phase 16/17 watch-layer planners conform to it; that the SEAM owns all fire-path storage I/O (evaluate stays pure); the confirmed importScripts load order; and that Phase 15 closed with full automated coverage and no live-Chrome UAT (milestone-end Chrome MV3 UAT covers real-fire survival, consistent with the Phase-14 deferral).
</output>
