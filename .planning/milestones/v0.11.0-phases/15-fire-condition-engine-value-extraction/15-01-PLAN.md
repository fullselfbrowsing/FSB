---
phase: 15-fire-condition-engine-value-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extension/utils/value-extractor.js
  - tests/value-extractor.test.js
  - package.json
autonomous: true
requirements: [EXTRACT-01, EXTRACT-02, EXTRACT-03, EXTRACT-04]
user_setup: []

must_haves:
  truths:
    - "parseLocaleNumber('$1,234.56', {locale:'en-US'}) returns value 1234.56"
    - "parseLocaleNumber('1.234,56', {locale:'de-DE'}) returns value 1234.56"
    - "parseLocaleNumber of a French NBSP-grouped string returns value 1234.56 (both U+00A0 and U+202F)"
    - "parseLocaleNumber of an accounting-parenthesized en-US value returns -1234.56"
    - "parseLocaleNumber of a German trailing-percent value returns value 12.5 with isPercent true (NOT 0.125)"
    - "parseLocaleNumber('abc' | '' | null) returns { error: 'parse_error' } and NEVER a numeric 0"
    - "extractValue selects text vs attribute source per the extract descriptor"
    - "D-01: value-extractor.js is one of the two new SW modules; pure dual-export IIFE (no _getChrome)"
    - "D-03: numeric parse uses Intl.NumberFormat formatToParts separator discovery + literal split/join; NaN -> parse_error, never 0"
    - "D-04: a per-trigger locale/decimal_separator override is honored in the schema (decimal_separator wins over locale)"
    - "D-05: raw text for changed/contains; locale-parsed number for threshold/equals/percent_change; extract override selects the source"
  artifacts:
    - path: "extension/utils/value-extractor.js"
      provides: "parseLocaleNumber + extractValue (pure, dual-export IIFE, no _getChrome)"
      contains: "function parseLocaleNumber"
      min_lines: 60
    - path: "tests/value-extractor.test.js"
      provides: "locale matrix + extract override + parse_error coverage"
      contains: "parse_error"
  key_links:
    - from: "tests/value-extractor.test.js"
      to: "extension/utils/value-extractor.js"
      via: "delete require.cache + require (fresh-require, no chrome mock)"
      pattern: "require.resolve.*value-extractor"
    - from: "package.json"
      to: "tests/value-extractor.test.js"
      via: "scripts.test && node chain append"
      pattern: "node tests/value-extractor.test.js"
---

<objective>
Create `extension/utils/value-extractor.js`: a pure, DOM-free, zero-dependency value-extraction module. It does locale-aware numeric parsing (the `Intl.NumberFormat().formatToParts()` separator-discovery recipe, D-03) and value-source selection (`text | number | attribute`, EXTRACT-03). This is the leaf module Plan 02's `trigger-manager` consumes for every numeric condition, so it is built and tested first.

Purpose: A silent 1000x numeric mis-parse on a EUR/INR page is the failure this phase exists to prevent (EXTRACT-01). A failed parse must yield a distinct `parse_error` and NEVER fire (EXTRACT-04). This module is the single chokepoint where both guarantees live.

Output: `extension/utils/value-extractor.js`, `tests/value-extractor.test.js` (full locale matrix), and the `package.json` test-chain append.

NOTE TO EXECUTOR: this plan file is intentionally pure ASCII. The locale test inputs (NBSP separators U+00A0 / U+202F, currency and crypto glyphs like the bitcoin sign U+20BF) MUST be built in the test source with JS string escapes (write the backslash-u form: backslash u 0 0 A 0, etc.), never pasted as literal invisible/multibyte characters. The authoritative input/expected matrix is 15-RESEARCH.md lines 640-657 - read it directly for the exact glyphs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-fire-condition-engine-value-extraction/15-CONTEXT.md
@.planning/phases/15-fire-condition-engine-value-extraction/15-RESEARCH.md
@.planning/phases/15-fire-condition-engine-value-extraction/15-PATTERNS.md

<interfaces>
<!-- The dual-export IIFE shell to mirror. value-extractor.js is PURE: it needs -->
<!-- NO _getChrome() resolver (no chrome access at all). Keep ONLY the dual-export -->
<!-- wrapper so the Node test can re-require it. Source: trigger-store.js:1-2,185-200. -->

Dual-export shell (open + close), from extension/utils/trigger-store.js:
```javascript
(function(global) {
  'use strict';
  // ... pure logic ...
  var exportsObj = { /* parseLocaleNumber, extractValue, ... */ };
  global.FsbValueExtractor = exportsObj;            // SW importScripts consumer
  if (typeof module !== 'undefined' && module.exports) {
    module.exports = exportsObj;                    // Node test consumer
  }
})(typeof globalThis !== 'undefined' ? globalThis : this);
```

The verified parse recipe lives in 15-RESEARCH.md lines 438-501 (the "Locale numeric parse (the verified recipe)" + "Value source selection" Code Examples). That recipe block - `_discoverSeparators`, `parseLocaleNumber`, `extractValue` - is the source of truth to transcribe (it is NOT a codebase file; it was verified empirically in Node v25.9.0). Read it before writing.

The required locale test matrix (every row a required case) is in 15-RESEARCH.md lines 640-657 and 15-VALIDATION.md "Sampling Rate".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 - create the value-extractor test (locale matrix + extract + parse_error) RED, and wire it into package.json</name>
  <files>tests/value-extractor.test.js, package.json</files>
  <read_first>
    - .planning/phases/15-fire-condition-engine-value-extraction/15-RESEARCH.md (lines 438-501 the verified recipe; lines 640-657 the locale matrix; lines 612-621 test framework)
    - .planning/phases/15-fire-condition-engine-value-extraction/15-VALIDATION.md ("Sampling Rate", "Wave 0 Requirements")
    - tests/trigger-store.test.js (the fresh-require harness: require.resolve + delete require.cache at :41-46; the check()/pass-fail counter + process.exit(failed>0?1:0) at :28-39,213-220)
    - package.json (the scripts.test serial && chain at :16 - note the tail ends with `node tests/trigger-lifecycle.test.js`)
  </read_first>
  <behavior>
    Every NBSP/multibyte input below is named by its code point. In the test source build it with a JS escape (backslash-u form), never a literal character. The exact glyphs and expected values are in 15-RESEARCH.md:640-657.
    - parseLocaleNumber('$1,234.56', {locale:'en-US'}) -> { value: 1234.56, isPercent: false }
    - parseLocaleNumber('1.234,56', {locale:'de-DE'}) -> value 1234.56
    - parseLocaleNumber('1[U+00A0]234,56', {locale:'fr-FR'}) -> value 1234.56 (regular NBSP group)
    - parseLocaleNumber('1[U+202F]234,56', {locale:'fr-FR'}) -> value 1234.56 (narrow NBSP group)
    - parseLocaleNumber('12,34,567.89', {locale:'en-IN'}) -> value 1234567.89 (Indian multi-group)
    - parseLocaleNumber('($1,234.56)', {locale:'en-US'}) -> value -1234.56
    - parseLocaleNumber('-1.234,56', {locale:'de-DE'}) -> value -1234.56
    - parseLocaleNumber('12,5[U+00A0]%', {locale:'de-DE'}) -> { value: 12.5, isPercent: true } (NOT 0.125)
    - parseLocaleNumber('45%', {locale:'en-US'}) -> { value: 45, isPercent: true }
    - parseLocaleNumber("CHF 1'234.50", {locale:'de-CH'}) -> value 1234.50 (apostrophe group stripped by the generic non-[0-9.] strip)
    - parseLocaleNumber('1.234', {locale:'de-DE'}) -> value 1234 (de-DE: '.' is the GROUP separator, stripped -> 1234)
    - parseLocaleNumber('1.234', {locale:'en-US'}) -> value 1.234 (en-US: '.' is the DECIMAL separator -> 1.234. Same string, different locale, different number - this pair proves D-04 is mathematically necessary.)
    - parseLocaleNumber('1.234', {decimal_separator: '.'}) -> value 1.234 (explicit decimal_separator override WINS: '.' forced as decimal regardless of locale; D-04 precedence proof)
    - parseLocaleNumber('[U+20BF]0.5', {locale:'en-US'}) -> value 0.5 (bitcoin-sign crypto glyph generic-stripped)
    - parseLocaleNumber('abc') / parseLocaleNumber('') / parseLocaleNumber(null) -> { error: 'parse_error' } (NEVER value 0, NEVER NaN leaking out)
    - extractValue({ text: '  $42  ' }, { extract: 'text' }) -> '$42' (trimmed)
    - extractValue({ attributes: { 'data-price': '19.99' } }, { extract: 'attribute', attribute: 'data-price' }) -> '19.99'
    - extractValue({ text: '50' }, { extract: 'number' }) -> '50' (returns the raw text; numeric parse happens at the compare site, not here)
  </behavior>
  <action>
    Create `tests/value-extractor.test.js` as a plain Node script (NO Jest/Mocha; Node built-in `assert` plus a local `check(cond,msg)` pass/fail counter; end with `process.exit(failed>0?1:0)` - copy that scaffold shape from tests/trigger-store.test.js:28-39,213-220). The extractor is PURE so NO chrome mock is needed: use the fresh-require pattern `const EXTRACTOR_PATH = require.resolve('../extension/utils/value-extractor.js'); function freshRequire(){ delete require.cache[EXTRACTOR_PATH]; return require(EXTRACTOR_PATH); }`. Write one assertion per row of the behavior block above (these correspond 1:1 to the 15-RESEARCH.md:640-657 matrix - every row is mandatory for EXTRACT-01). Build every NBSP-bearing or non-ASCII input with JS string escapes (backslash-u 00A0, backslash-u 202F, backslash-u 20BF) concatenated into the input string - do NOT paste literal invisible/multibyte characters into the test source. Group cases: the locale-parse matrix (EXTRACT-01); the THREE override cases that prove D-04 - `'1.234'` under de-DE -> 1234, the SAME `'1.234'` under en-US -> 1.234, and `'1.234'` with explicit `decimal_separator:'.'` -> 1.234 (override wins); the `extract: text|number|attribute` source-selection cases (EXTRACT-03); and the `parse_error` cases for `abc`/`''`/`null` asserting the return has `error === 'parse_error'` and NO `value` of 0 (EXTRACT-04). For percent cases assert BOTH `value` and `isPercent === true` and that the value is the raw number (12.5, not 0.125). Then append the new test to `package.json` scripts.test: at the very END of the `&&`-joined chain (after `node tests/trigger-lifecycle.test.js`), append ` && node tests/value-extractor.test.js` - do NOT reorder any existing entry. This is the Wave 0 RED step: the test will fail because value-extractor.js does not exist yet.
  </action>
  <verify>
    <automated>node tests/value-extractor.test.js; test $? -ne 0 && echo "RED-OK: fails before impl exists" || echo "UNEXPECTED-GREEN"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/value-extractor.test.js` exists and is a runnable Node script (`node tests/value-extractor.test.js` runs without a syntax error; it exits non-zero because the module under test is absent - this is the expected RED state).
    - `grep -c "parse_error" tests/value-extractor.test.js` returns >= 3 (the abc/''/null cases).
    - `grep -c "isPercent" tests/value-extractor.test.js` returns >= 2.
    - The narrow-NBSP case is present as a string escape, not a literal: `grep -c 'u202F' tests/value-extractor.test.js` returns >= 1.
    - The test source contains NO raw invisible/multibyte bytes: `LC_ALL=C grep -nP '[\x80-\xFF]' tests/value-extractor.test.js && echo DIRTY || echo CLEAN` prints CLEAN.
    - `package.json` scripts.test contains the exact substring `node tests/value-extractor.test.js`, positioned AFTER `node tests/trigger-lifecycle.test.js` (verify with: `node -e "const t=require('./package.json').scripts.test; process.exit(t.indexOf('trigger-lifecycle.test.js') < t.indexOf('value-extractor.test.js') ? 0 : 1)"`).
  </acceptance_criteria>
  <done>The test file encodes the full locale matrix + extract override + parse_error cases (NBSP inputs as JS backslash-u escapes), is wired into the npm test chain after the Phase-14 tests, and fails (RED) because value-extractor.js is not yet implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement value-extractor.js (locale parse recipe + extractValue) to GREEN</name>
  <files>extension/utils/value-extractor.js</files>
  <read_first>
    - .planning/phases/15-fire-condition-engine-value-extraction/15-RESEARCH.md (lines 438-501 - the verified `_discoverSeparators`/`parseLocaleNumber`/`extractValue` recipe; lines 369-371 anti-patterns)
    - .planning/phases/15-fire-condition-engine-value-extraction/15-PATTERNS.md ("value-extractor.js" section :29-67 - the LANDMINES list)
    - extension/utils/trigger-store.js (lines 1-2 and 185-200 - the dual-export IIFE shell to mirror; note value-extractor omits _getChrome because it is pure)
    - tests/value-extractor.test.js (the assertions written in Task 1 - implement to satisfy them)
  </read_first>
  <action>
    Create `extension/utils/value-extractor.js` as a dual-export IIFE (mirror trigger-store.js:1-2,185-200) exporting `global.FsbValueExtractor = { parseLocaleNumber, extractValue }` and the Node `module.exports` mirror. OMIT `_getChrome()` entirely - this module is pure `Intl`/string math and touches no chrome API. Transcribe the verified recipe from 15-RESEARCH.md:438-501:
    (1) `var _sepCache = new Map();` - memoize discovered separators per locale (the `Intl.NumberFormat` constructor is expensive and runs per mutation tick; LANDMINE: un-memoized this is a hot-path cost).
    (2) `_discoverSeparators(locale)` - construct `new Intl.NumberFormat(locale).formatToParts(11000.1)`, read the part whose `type === 'group'` and the part whose `type === 'decimal'`, default to `{ group: ',', decimal: '.' }` on any throw, cache and return.
    (3) `parseLocaleNumber(raw, opts)` performing, IN ORDER: reject non-string/empty -> `{ error:'parse_error', isPercent:false }`; trim; (1) strip a parentheses-negative wrapper and record `sign=-1`; (2) detect `%` and record `isPercent` (LANDMINE: keep `%` RAW - do NOT divide by 100; the comparison layer decides meaning), detect a leading `-` as sign; resolve `groupSep`/`decimalSep` where `opts.decimal_separator` WINS over the locale-discovered decimal (D-04); (3) remove the group separator with LITERAL `s.split(groupSep).join('')` (LANDMINE: NEVER `new RegExp(groupSep)` - `.` is a regex metacharacter; this is the #1 footgun in this file); (4) swap the decimal separator to `.` via `s.split(decimalSep).join('.')` when it is not already `.`; (5) strip everything non-`[0-9.]` with `s.replace(/[^0-9.]/g, '')` (this generically kills the dollar sign, the euro/pound/yen glyphs, letter currencies CHF/kr/zl, the CHF apostrophe group, and crypto glyphs with no symbol list to maintain); (6) `parseFloat`; if `!Number.isFinite(n)` return `{ error:'parse_error', isPercent }` (LANDMINE: NaN -> `parse_error`, NEVER `0`, NEVER `parseFloat(...) || 0` - EXTRACT-04); else return `{ value: sign * n, isPercent }`.
    (4) `extractValue(reportedValue, descriptor)` selecting the source: `mode = (descriptor && descriptor.extract) || 'text'`; for `'attribute'` read `descriptor.attribute` from `reportedValue.attributes[name]` and return it trimmed (or `''` if absent); for `'text'` and `'number'` return `reportedValue.text` trimmed (the numeric parse for `'number'` happens later at the compare site, not here - D-05).
    NO EMOJIS anywhere in the file (code, comments). Keep the source ASCII-clean: where a comment must reference a separator code point, NAME it (e.g. "U+202F narrow NBSP") rather than embedding the literal character. Use ASCII-only block comments documenting each numbered normalize step.
  </action>
  <verify>
    <automated>node tests/value-extractor.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/value-extractor.test.js` exits 0 (all locale-matrix + extract + parse_error assertions pass).
    - `extension/utils/value-extractor.js` contains `function parseLocaleNumber` and `function extractValue` and `_sepCache` (memoization present).
    - The file contains NO occurrence of `new RegExp` (verify: `grep -c "new RegExp" extension/utils/value-extractor.js` returns 0 - the literal split/join rule is the load-bearing footgun guard).
    - The file contains NO `|| 0` fallback on a parse (verify: `grep -c "parseFloat" extension/utils/value-extractor.js` >= 1 AND `grep -c "|| 0" extension/utils/value-extractor.js` returns 0).
    - The file exports `global.FsbValueExtractor` and contains NO `_getChrome` (verify: `grep -c "FsbValueExtractor" extension/utils/value-extractor.js` >= 1 AND `grep -c "_getChrome" extension/utils/value-extractor.js` returns 0).
    - Source is emoji/literal-multibyte-free: `LC_ALL=C grep -nP '[\x80-\xFF]' extension/utils/value-extractor.js && echo DIRTY || echo CLEAN` prints CLEAN.
  </acceptance_criteria>
  <done>value-extractor.js implements the locale-aware parse + extract override, all Task-1 assertions pass (GREEN), and the anti-pattern guards (no `new RegExp`, no `|| 0`, no `_getChrome`, ASCII-clean) hold.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page text -> value-extractor | Untrusted page text (currency strings, attribute values) crosses into `parseLocaleNumber` / `extractValue`. The watch layer (Phase 16/17) reports it; this module must never crash or silently mis-fire on hostile input. |
| caller condition spec -> value-extractor | Caller-supplied `locale` / `decimal_separator` / `attribute` name crosses in via the descriptor. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | `parseLocaleNumber` integrity of the parsed number | mitigate | Locale normalize-then-parse via `formatToParts` separator discovery; literal `split/join` (never `new RegExp(sep)` which would mis-strip on `.`); the mandatory `decimal_separator`/`locale` override (D-04) resolves bare-string ambiguity (`1.234`) that would otherwise be a 1000x mis-parse. Asserted by the full locale matrix incl. the de-DE-vs-en-US `1.234` pair. |
| T-15-02 | Tampering / Denial of Service | NaN propagation from a failed parse | mitigate | `!Number.isFinite(n)` -> `{ error:'parse_error' }`; NEVER `0`, NEVER `|| 0`. The `abc`/`''`/`null` test cases lock this. A `parse_error` propagates upward and (in Plan 02) never fires. |
| T-15-03 | Denial of Service | unbounded `_sepCache` growth | accept | Cache is keyed by locale strings (a small bounded set in practice). Acceptable for a per-extension SW; an LRU is unnecessary at this scale (15-RESEARCH.md Security Domain). |
| T-15-04 | Tampering | hostile `attribute` name in the extract descriptor | mitigate | `extractValue` reads `reportedValue.attributes[name]` only (a plain object lookup on the reported payload); it does NOT touch the DOM and cannot read arbitrary page state - Phase 15 never scrapes (D-02). Missing attribute returns `''`, not a throw. |
| T-15-SC | Tampering | npm/pip/cargo installs | mitigate | Phase 15 installs ZERO packages (zero-dep mandate D-03; STACK.md:99). No new dependency, no `package-lock.json` change, no slopcheck surface. INV-06 (Lattice pin) untouched. |
</threat_model>

<verification>
- `node tests/value-extractor.test.js` exits 0.
- The Phase-14 tests still pass (no regression from the package.json edit): `node tests/trigger-store.test.js && node tests/trigger-lifecycle.test.js`.
- `extension/utils/value-extractor.js` exists, is pure (no `_getChrome`, no chrome access), and contains no `new RegExp` and no `|| 0` parse fallback.
</verification>

<success_criteria>
- EXTRACT-01: the full locale matrix (US/DE/FR-NBSP-U+00A0/FR-narrow-U+202F/IN-multigroup/parens-neg/percent/CHF-apostrophe/crypto/override) parses to the correct Number.
- EXTRACT-02: raw trimmed text is returned for the `text` extract mode (no numeric coercion).
- EXTRACT-03: `extract: text | number | attribute` (+ attribute name) selects the correct source.
- EXTRACT-04: a failed parse yields `{ error: 'parse_error' }` and never a numeric 0 / NaN.
- The module is a pure dual-export IIFE that unit-tests browser-free, and is wired into `npm test`.
</success_criteria>

<output>
Create `.planning/phases/15-fire-condition-engine-value-extraction/15-01-SUMMARY.md` when done. Note in it: the exact `extractValue(reportedValue, descriptor)` input shape you implemented (`{ text, attributes? }`) - Plan 03 documents this as the consumer contract the Phase 16/17 watch layer must conform to.
</output>
