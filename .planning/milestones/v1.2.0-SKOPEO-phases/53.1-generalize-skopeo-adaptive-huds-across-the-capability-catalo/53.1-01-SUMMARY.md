---
phase: 53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo
plan: "01"
subsystem: catalog-profile-contract
tags: [chrome-extension, catalog, schema-validation, code-generation, readiness]

requires:
  - phase: 53-drive-context-router-semantic-anchors
    provides: Exact-origin context evidence, semantic identity boundaries, and the shared Skopeo shell contract
provides:
  - Closed schemaVersion 1 data-only app profile registry with bounded allowlisted semantics
  - Deterministic full-corpus profile/readiness artifact for 2,314 descriptors, 128 stems, and 129 services
  - Exactly-once semantic and byte-stability gates covering origin, profile, catalog, and readiness drift
affects: [53.1-02-current-service-projection, 53.1-03-context-resolver, 53.1-05-genre-adapters]

tech-stack:
  added: []
  patterns: [classic-script/CommonJS dual export, deterministic generated artifact, exact stem-service-origin join]

key-files:
  created:
    - catalog/skopeo/app-profiles.json
    - extension/utils/skopeo-profile-schema.js
    - scripts/generate-skopeo-profile-index.mjs
    - scripts/verify-skopeo-profile-coverage.mjs
    - extension/catalog/skopeo-profile-index.generated.js
    - tests/skopeo-profile-schema.test.js
    - tests/skopeo-profile-coverage.test.js
  modified: []

key-decisions:
  - "Keep appStem, service, exact HTTPS origin, profile identity, and profile version as separate generated fields and join descriptors by the exact stem/service pair."
  - "Give multi-stem services an explicit ambiguous-stem disposition; atlassian.net never guesses between Jira and Confluence."
  - "Compute executability only from canonical source readiness plus terminal evidence; source executionEnabled and invocable bits are never trusted."

patterns-established:
  - "Closed profile data: authored profiles select only bounded vocabulary, capability groups, attention ceilings, and allowlisted adapter/renderer IDs."
  - "Generated coverage gate: semantic set checks and byte-for-byte regeneration must both pass before the runtime artifact is current."

requirements-completed: [ADAPT-01, ADAPT-04, ADAPT-05, ADAPT-08, ADAPT-10]

duration: 20 min
completed: 2026-07-16
---

# Phase 53.1 Plan 01: Catalog Profile Contract Summary

**A closed data-only profile registry and byte-stable runtime index now cover all 2,314 catalog descriptors without collapsing stems, services, origins, or readiness evidence**

## Performance

- **Duration:** 20 min
- **Started:** 2026-07-16T17:32:45Z
- **Completed:** 2026-07-16T17:52:56Z
- **Tasks:** 3
- **Files modified:** 7

## Accomplishments

- Added RED-first schema and coverage oracles that exercise hostile profile fields, exact-origin spoofing, set drift, ambiguous joins, nondeterministic output, all nine presentation dispositions, and forged execution flags.
- Added a schemaVersion 1 authored registry with safe generic inheritance, representative profiles for every approved genre, and separate Drive/Docs deep-pack entries.
- Generated and gated a deeply frozen 130-pair runtime index covering 2,314 descriptors, 128 stems, and 129 services, including an explicit fail-quiet disposition for the shared Atlassian service.

## Task Commits

Each task was committed atomically:

1. **Task 1: Land RED-first profile schema and full-corpus security oracles** - `93220056` (test)
2. **Task 2: Implement the closed authored profile registry and validator** - `5ca4e5df` (feat)
3. **Task 3: Generate and gate the complete deterministic runtime index** - `a166338e` (feat)

## Files Created/Modified

- `tests/skopeo-profile-schema.test.js` - Exact-key, bounded-data, hostile-value, enum, and normalization security oracle.
- `tests/skopeo-profile-coverage.test.js` - Full-corpus baselines, join mutations, readiness precedence, and forged-boolean oracle.
- `catalog/skopeo/app-profiles.json` - Sole authored safe-default and audited representative profile registry.
- `extension/utils/skopeo-profile-schema.js` - Closed validator/normalizer with classic-script and CommonJS exports.
- `scripts/generate-skopeo-profile-index.mjs` - Canonical readiness join, deterministic generator, renderer, and stale-artifact check.
- `scripts/verify-skopeo-profile-coverage.mjs` - Exactly-once semantic, origin, disposition, baseline, ordering, and byte-equivalence verifier.
- `extension/catalog/skopeo-profile-index.generated.js` - Deeply frozen data-only runtime profile/readiness index.

## Decisions Made

- Model service disposition separately from pair-level runtime profiles so the 129-service completeness contract and the 130 distinct stem/service pairs are both explicit.
- Treat `atlassian.net` as `ambiguous-stem` with no profile, adapter, renderer, or executable capability guess; Jira and Confluence remain separate pair identities.
- Use code-point ordering instead of locale-aware sorting so generated bytes are stable across environments and camelCase catalog slugs remain valid after the lowercase app stem.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Accepted existing camelCase descriptor action segments**
- **Found during:** Task 3 (Generate and gate the complete deterministic runtime index)
- **Issue:** The first validator draft required every slug character to be lowercase, but the committed corpus includes stable camelCase action segments such as `notion.loadPage`.
- **Fix:** Kept the leading app-stem contract lowercase while allowing uppercase characters after it.
- **Files modified:** `scripts/generate-skopeo-profile-index.mjs`
- **Verification:** Generator completed across all 2,314 descriptors and the coverage suite passed.
- **Committed in:** `a166338e`

**2. [Rule 1 - Bug] Replaced locale-aware ordering with deterministic code-point ordering**
- **Found during:** Task 3 (Generate and gate the complete deterministic runtime index)
- **Issue:** `localeCompare` produced an order that disagreed with the verifier's byte-order oracle for mixed-case slugs.
- **Fix:** Added one explicit code-point comparator and used it for descriptor, pair, service, and origin ordering.
- **Files modified:** `scripts/generate-skopeo-profile-index.mjs`
- **Verification:** Mutation tests, generator `--check`, and byte-for-byte verifier all passed.
- **Committed in:** `a166338e`

---

**Total deviations:** 2 auto-fixed bugs
**Impact on plan:** Both fixes were required for compatibility with the committed catalog and cross-environment determinism; no scope was added.

## Issues Encountered

None beyond the two Task 3 implementation corrections documented above.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 53.1-02 can consume one exact-origin, generation-owned current-service slice from the committed profile index without receiving the full catalog.
- The generated artifact exposes separate raw readiness and terminal evidence plus canonical presentation dispositions; later projection code must continue to derive executability rather than trust caller booleans.

## Self-Check: PASSED

- All seven planned artifacts and this summary exist.
- Task commits `93220056`, `5ca4e5df`, and `a166338e` are present in history.
- Schema, full-corpus mutation, stale-artifact, and semantic coverage gates pass with 2,314 descriptors, 128 stems, 129 services, and 130 pairs.

---
*Phase: 53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo*
*Completed: 2026-07-16*
