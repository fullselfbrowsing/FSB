---
phase: 53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo
plan: "05"
type: execute
wave: 5
depends_on:
  - "53.1-04"
files_modified:
  - extension/content/skopeo-adapter-registry.js
  - extension/content/skopeo-renderer-registry.js
  - tests/fixtures/skopeo-catalog/genre-matrix.js
  - tests/skopeo-genre-adapters.test.js
autonomous: true
requirements:
  - ADAPT-02
  - ADAPT-04
  - ADAPT-09
  - ADAPT-10
must_haves:
  truths:
    - "Only bundled allowlisted adapter and renderer IDs can resolve; profiles cannot execute code or create chrome."
    - "D-11: Every approved genre has a deterministic representative composition fixture while automated catalog proof, local real-Chrome mechanics, and authorized live-host approval remain separate evidence states."
    - "Drive/Docs delegates to the existing Phase 53 router and anchor semantics without generalizing or replacing that deep adapter."
  artifacts:
    - path: "extension/content/skopeo-adapter-registry.js"
      provides: "Closed bundled genre/stable-evidence adapters and Drive delegation"
      exports: ["ADAPTER_IDS", "resolve"]
    - path: "extension/content/skopeo-renderer-registry.js"
      provides: "Closed result renderers producing only approved atoms"
      exports: ["RENDERER_IDS", "render"]
    - path: "tests/fixtures/skopeo-catalog/genre-matrix.js"
      provides: "Distinct deterministic genre/browser mechanics fixtures"
    - path: "tests/skopeo-genre-adapters.test.js"
      provides: "Allowlist, semantic evidence, renderer, and Drive regression oracle"
  key_links:
    - from: "extension/content/skopeo-adapter-registry.js"
      to: "extension/content/skopeo-context-router.js"
      via: "drive-docs-v1 exact delegation"
      pattern: "FSBSkopeoContextRouter"
    - from: "extension/content/skopeo-renderer-registry.js"
      to: "extension/content/skopeo-adaptive-composer.js"
      via: "closed atom output consumed by render model"
      pattern: "section-heading|status-row|fact-list"
---

<objective>
Ship the bounded adapter/renderer registries and representative cross-genre proof while preserving Drive/Docs as an unchanged deep pack.

Purpose: Complete D-04, D-05, and D-11 without custom app runtimes or arbitrary profile-supplied UI.
Output: Bundled registries, full genre fixture matrix, and delegation/security regression tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.2.0-SKOPEO-ROADMAP.md
@.planning/milestones/v1.2.0-SKOPEO-REQUIREMENTS.md
@.planning/milestones/v1.2.0-SKOPEO-STATE-SNAPSHOT.md
@.planning/milestones/v1.2.0-SKOPEO-phases/53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo/53.1-CONTEXT.md
@.planning/milestones/v1.2.0-SKOPEO-phases/53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo/53.1-RESEARCH.md
@.planning/milestones/v1.2.0-SKOPEO-phases/53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo/53.1-UI-SPEC.md
@.planning/milestones/v1.2.0-SKOPEO-phases/53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo/53.1-VALIDATION.md
@.planning/milestones/v1.2.0-SKOPEO-phases/53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo/53.1-04-SUMMARY.md

<interfaces>
Plan 03 calls an injected adapter seam:
```javascript
resolveAdapter(adapterId, closedInput) ->
  { status, genre, semanticEntity?, anchorDescriptor?, evidence };
```

Existing deep router (do not modify):
```javascript
FSBSkopeoContextRouter.createRouter({ generation }).route(input);
```

Plan 04 renderer output accepts only:
`section-heading`, `status-row`, `capability-row`, `fact-list`, `item-list`,
`compact-table`, `timeline`, `diff`, and `notice`.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Profile adapter/renderer ID → bundled registry | IDs are data and must resolve only through exact allowlists. |
| Candidate page evidence → semantic adapter | Evidence is untrusted until the selected bundled adapter validates stable identifiers. |
| Capability result → renderer atoms | Typed results may contain hostile strings but may not define structure or executable content. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan | Automated negative proof |
|-----------|----------|----------|-----------|-------------|-----------------|--------------------------|
| T-53.1-04 | High | Spoofing | adapter registry | mitigate | Exact allowlist lookup and adapter-owned stable evidence; generic/path/text evidence remains unanchored. | `node tests/skopeo-genre-adapters.test.js` rejects unknown/forged IDs and unstable evidence for every genre. |
| T-53.1-07 | High | Tampering | Drive/Docs delegation | mitigate | Call the existing router once with its exact closed input and pass through its closed result; no local identity reinterpretation. | The focused test compares delegated recognized/uncertain/unsupported tuples and existing Drive tests remain green. |
| T-53.1-09 | High | Information disclosure | renderer registry | mitigate | Exact result schemas, bounded rows/cells/text, approved atoms only, and text values rather than HTML/URLs/host nodes. | The focused test supplies script/HTML/Unicode-control/functions/remote URLs/oversized tables and requires rejection or bounded text output. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create the complete genre fixture matrix and RED registry oracle</name>
  <files>tests/fixtures/skopeo-catalog/genre-matrix.js, tests/skopeo-genre-adapters.test.js</files>
  <read_first>
    - tests/fixtures/skopeo-catalog/genre-matrix.js (confirm absent before creating)
    - tests/skopeo-genre-adapters.test.js (confirm absent before creating)
    - tests/fixtures/skopeo-semantic-anchor-fixture.js
    - tests/skopeo-context-router.test.js
    - tests/skopeo-anchor-registry.test.js
    - tests/skopeo-adaptive-composer.test.js
    - extension/content/skopeo-context-router.js
    - extension/content/skopeo-adaptive-composer.js
    - extension/utils/skopeo-profile-schema.js
    - .planning/milestones/v1.2.0-SKOPEO-phases/53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo/53.1-UI-SPEC.md
  </read_first>
  <action>
Create deterministic fixtures for all nine UI-SPEC genre rows: reader/knowledge, communication, document/editor, worklist/record, dashboard/admin, transactional, media/feed, generic app, and Drive/Docs deep delegation. Each fixture has separate exact projection, requested lens, optional stable adapter evidence, ready/non-ready capabilities, risk, expected first attention/primitives, expected atoms/copy, normal-width state, below-480 state, withdrawal, and Off expectations. Keep fixture/browser mechanics distinct from live-host approval; label fixtures `automated-fixture`, never `live-approved`. The test must require schema allowlist parity, unknown-ID failure, generic unanchored behavior, stable evidence validation, app-native noun propagation, exact Drive router delegation, closed renderer atoms, table-to-facts narrow behavior, bounds, hostile text-only values, and no functions/DOM/remote assets. Run now and retain RED only for absent registries.
  </action>
  <acceptance_criteria>
    - `test -f tests/fixtures/skopeo-catalog/genre-matrix.js && test -f tests/skopeo-genre-adapters.test.js` succeeds.
    - `rg -n "reader-knowledge|communication|document-editor|worklist-record|dashboard-admin|transactional|media-feed|generic-app|drive-docs-deep-pack" tests/fixtures/skopeo-catalog/genre-matrix.js` finds all nine rows.
    - `rg -n "automated-fixture|live-approved|unknown.*adapter|unknown.*renderer|HTML|remote|narrow" tests/skopeo-genre-adapters.test.js tests/fixtures/skopeo-catalog/genre-matrix.js` finds evidence separation and negatives.
    - Controlled RED names only `skopeo-adapter-registry` or `skopeo-renderer-registry`.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set +e; out=$(node tests/skopeo-genre-adapters.test.js 2&gt;&amp;1); code=$?; set -e; test $code -ne 0; printf "%s\n" "$out" | rg "skopeo-(adapter|renderer)-registry"'</automated>
  </verify>
  <done>All genre and Drive delegation contracts exist as deterministic RED fixtures without implying live-host approval.</done>
</task>

<task type="auto">
  <name>Task 2: Implement allowlisted adapters with generic fail-quiet and Drive delegation</name>
  <files>extension/content/skopeo-adapter-registry.js</files>
  <read_first>
    - extension/content/skopeo-adapter-registry.js (confirm absent before creating)
    - tests/skopeo-genre-adapters.test.js
    - tests/fixtures/skopeo-catalog/genre-matrix.js
    - extension/utils/skopeo-profile-schema.js
    - extension/content/skopeo-app-context-resolver.js
    - extension/content/skopeo-context-router.js
    - extension/content/skopeo-anchor-registry.js
    - .planning/milestones/v1.2.0-SKOPEO-phases/53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo/53.1-CONTEXT.md
  </read_first>
  <action>
Implement dual-export `FSBSkopeoAdapterRegistry` with a frozen ID set exactly equal to `FsbSkopeoProfileSchema.ADAPTER_IDS` and `resolve(adapterId, input)`. `generic-unanchored-v1` always returns app-level recognized context with no entity/anchor. Genre adapters validate only closed candidate evidence from their bundled recognizer contract: stable ID kind/value plus exact profile/origin/generation; they never infer identity from path, visible label, class, selector, node order, or DOM shape. Without stable evidence they return the same unanchored app-level context, not an error-looking guessed entity. `drive-docs-v1` creates/uses `FSBSkopeoContextRouter` and passes its exact recognized/uncertain/unsupported result through once, with no rewritten identity or fallback. Bound evidence to 8 items and stable values to 512 characters, matching the deep router. Do not add observers, page listeners, remote calls, callbacks in profile data, or an alternate anchor registry.
  </action>
  <acceptance_criteria>
    - `node --check extension/content/skopeo-adapter-registry.js` exits 0.
    - Adapter-only cases in `node tests/skopeo-genre-adapters.test.js --adapters-only` exit 0.
    - `rg -n "generic-unanchored-v1|drive-docs-v1|FSBSkopeoContextRouter|ADAPTER_IDS" extension/content/skopeo-adapter-registry.js` finds required seams.
    - `rg -n "querySelector|MutationObserver|innerHTML|fetch\\(|XMLHttpRequest" extension/content/skopeo-adapter-registry.js` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>node --check extension/content/skopeo-adapter-registry.js &amp;&amp; node tests/skopeo-genre-adapters.test.js --adapters-only &amp;&amp; node tests/skopeo-context-router.test.js &amp;&amp; node tests/skopeo-anchor-registry.test.js</automated>
  </verify>
  <done>Bundled adapters are allowlisted and evidence-bound; generic contexts remain unanchored and Drive/Docs uses the unchanged deep router.</done>
</task>

<task type="auto">
  <name>Task 3: Implement the closed renderer atom registry</name>
  <files>extension/content/skopeo-renderer-registry.js</files>
  <read_first>
    - extension/content/skopeo-renderer-registry.js (confirm absent before creating)
    - tests/skopeo-genre-adapters.test.js
    - tests/fixtures/skopeo-catalog/genre-matrix.js
    - extension/utils/skopeo-profile-schema.js
    - extension/content/skopeo-adaptive-composer.js
    - extension/content/skopeo-shell.js
    - .planning/milestones/v1.2.0-SKOPEO-phases/53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo/53.1-UI-SPEC.md
  </read_first>
  <action>
Implement dual-export `FSBSkopeoRendererRegistry` with a frozen ID set exactly equal to schema `RENDERER_IDS`, `render(rendererId, typedResult, options)`, and `validateAtoms`. Provide shared status/list/fact/table/timeline/diff/notice renderers and a Drive/Docs result adapter only as allowlisted functions bundled in this file. Output recursively frozen arrays of the nine Plan 04 atoms; cap sections at 12, items/rows at 50, columns at 8, and text at 512 characters. At width below 480, return labelled `fact-list` rows instead of `compact-table`. Treat every result value as text/data, drop unsupported structure, and return a bounded typed notice for malformed results. Never emit HTML, Markdown execution, forms, image/remote URLs, SVG, iframe, style, host nodes, callbacks, or a live region. Result/error copy must follow UI-SPEC exact action-name and safe-recovery grammar without raw exceptions/secrets.
  </action>
  <acceptance_criteria>
    - `node --check extension/content/skopeo-renderer-registry.js` exits 0.
    - `node tests/skopeo-genre-adapters.test.js && node tests/skopeo-adaptive-composer.test.js` exits 0.
    - `rg -n "compact-table|fact-list|timeline|diff|notice|validateAtoms|RENDERER_IDS" extension/content/skopeo-renderer-registry.js` finds closed atoms/seams.
    - `rg -n "innerHTML|Markdown|iframe|svg|createElement|querySelector|fetch\\(" extension/content/skopeo-renderer-registry.js` returns no executable/DOM rendering paths.
  </acceptance_criteria>
  <verify>
    <automated>node --check extension/content/skopeo-renderer-registry.js &amp;&amp; node tests/skopeo-genre-adapters.test.js &amp;&amp; node tests/skopeo-adaptive-composer.test.js</automated>
  </verify>
  <done>All representative results normalize to bounded approved atoms, including narrow behavior, without profile-supplied chrome or executable content.</done>
</task>

</tasks>

<verification>
Run `node tests/skopeo-genre-adapters.test.js && node tests/skopeo-adaptive-composer.test.js && node tests/skopeo-context-router.test.js && node tests/skopeo-anchor-registry.test.js`.
</verification>

<success_criteria>
- Every approved genre has deterministic distinct automated evidence and honest generic fallback.
- Adapter/renderer IDs are exact allowlists shared with the profile schema.
- Generic profiles never invent entities; stable adapters require bounded evidence.
- Drive/Docs delegates to the existing deep router unchanged and all Phase 53 regressions pass.
- Renderers emit only closed bounded text/data atoms and never imply live-host approval.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.2.0-SKOPEO-phases/53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo/53.1-05-SUMMARY.md`.
</output>
