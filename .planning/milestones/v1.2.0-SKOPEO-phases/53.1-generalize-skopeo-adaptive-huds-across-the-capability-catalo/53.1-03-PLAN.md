---
phase: 53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo
plan: "03"
type: execute
wave: 3
depends_on:
  - "53.1-02"
files_modified:
  - extension/content/skopeo-app-context-resolver.js
  - tests/skopeo-app-context-resolver.test.js
autonomous: true
requirements:
  - ADAPT-02
  - ADAPT-04
  - ADAPT-10
must_haves:
  truths:
    - "App identity, page genre, semantic entity evidence, requested lens, capability surface, and risk remain separate resolver inputs."
    - "D-05: A generic profile can recognize the exact app and remain useful at app level without inventing a semantic entity or anchor from unstable page evidence."
    - "D-04: The catalog-wide resolver surrounds Skopeo and delegates Drive/Docs through an injected deep-adapter seam while the existing Phase 53 router remains unchanged."
  artifacts:
    - path: "extension/content/skopeo-app-context-resolver.js"
      provides: "Catalog-wide closed app/genre/context result union"
      exports: ["STATUS", "REASON", "createResolver", "validateResult"]
    - path: "tests/skopeo-app-context-resolver.test.js"
      provides: "Generic fail-quiet, epoch, entity-evidence, and delegation oracle"
      contains: "no-stable-entity"
  key_links:
    - from: "extension/content/skopeo-app-context-resolver.js"
      to: "extension/utils/skopeo-capability-projector.js"
      via: "validated frozen current-service projection input"
      pattern: "profileVersion"
    - from: "extension/content/skopeo-app-context-resolver.js"
      to: "extension/content/skopeo-adapter-registry.js"
      via: "injected allowlisted resolveAdapter callback implemented in Plan 05"
      pattern: "resolveAdapter"
---

<objective>
Define the catalog-wide app/genre/context contract that safely surrounds the existing Drive/Docs deep router.

Purpose: Implement D-04 and D-05 as a closed resolver layer where generic profiles fail quietly and semantic identity requires bundled stable evidence.
Output: Pure classic-script resolver and hostile-input/delegation oracle.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.2.0-SKOPEO-ROADMAP.md
@.planning/milestones/v1.2.0-SKOPEO-REQUIREMENTS.md
@.planning/milestones/v1.2.0-SKOPEO-STATE-SNAPSHOT.md
@.planning/milestones/v1.2.0-SKOPEO-phases/53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo/53.1-CONTEXT.md
@.planning/milestones/v1.2.0-SKOPEO-phases/53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo/53.1-RESEARCH.md
@.planning/milestones/v1.2.0-SKOPEO-phases/53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo/53.1-UI-SPEC.md
@.planning/milestones/v1.2.0-SKOPEO-phases/53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo/53.1-VALIDATION.md
@.planning/milestones/v1.2.0-SKOPEO-phases/53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo/53.1-02-SUMMARY.md

<interfaces>
From Plan 02:
```javascript
FsbSkopeoCapabilityProjector.validateProjection(value);
// recognized projection carries exactOrigin, service, appStem, profileId,
// profileVersion, catalogVersion, profile, and current-service groups.
```

Existing deep adapter, preserved unchanged:
```javascript
FSBSkopeoContextRouter.createRouter({ generation }).route({
  url, contextKind, semanticIdentity, evidence
});
// -> recognized | uncertain | unsupported with monotonic contextEpoch
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Projection + current URL → context resolver | Projected app authority is trusted only for its exact tuple; current page data remains untrusted. |
| Adapter evidence → semantic entity | Only an allowlisted bundled adapter may supply stable evidence. |
| Resolver result → composer/anchor registry | Result must be closed, bounded, frozen, and epoch-owned before UI use. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan | Automated negative proof |
|-----------|----------|----------|-----------|-------------|-----------------|--------------------------|
| T-53.1-04 | High | Spoofing | semantic entity resolution | mitigate | Generic/path/text/class/list-position inputs always produce `semanticEntity:null`; only an allowlisted adapter result with stable evidence can create identity. | `node tests/skopeo-app-context-resolver.test.js` feeds URL paths, visible labels, CSS selectors, recycled positions, and forged adapter IDs and asserts no entity. |
| T-53.1-07 | High | Tampering | context epoch/result authority | mitigate | Advance contextEpoch on every attempt, recursively freeze results, and reject a result whose generation/origin/profile tuple differs. | The focused test replays stale generation/profile/origin/epoch results and requires `uncertain` or `unsupported`. |
| T-53.1-03 | Medium | Information disclosure | context result fields | mitigate | Exact-key bounded result excludes raw page copy, raw URL, DOM nodes, selectors, and callbacks. | The focused test walks serialized results and rejects forbidden keys/host objects. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Write the RED closed-context and no-invented-entity oracle</name>
  <files>tests/skopeo-app-context-resolver.test.js</files>
  <read_first>
    - tests/skopeo-app-context-resolver.test.js (confirm absent before creating)
    - tests/skopeo-context-router.test.js
    - tests/skopeo-anchor-registry.test.js
    - tests/skopeo-capability-projection.test.js
    - extension/content/skopeo-context-router.js
    - extension/content/skopeo-anchor-registry.js
    - extension/utils/skopeo-capability-projector.js
    - .planning/milestones/v1.2.0-SKOPEO-phases/53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo/53.1-UI-SPEC.md
  </read_first>
  <action>
Create a Node oracle for a `createResolver({generation, projection, resolveAdapter})` contract. Require a monotonic positive `contextEpoch` on every recognized, uncertain, unsupported, and post-disposal attempt. Exercise separate inputs for exact app projection, current URL, requested lens/task, adapter evidence, capabilities, and risk. Prove a generic profile returns exact app identity/default genre/current groups but `semanticEntity:null`, `anchorDescriptor:null`, and an explicit `no-stable-entity` reason when the only evidence is path, heading/label text, CSS class/selector, DOM shape, or list position. Prove forged adapter IDs, raw DOM nodes, functions, oversized copy, mismatched origin/profile/generation, and adapter exceptions fail quiet. Define the callback seam for later Drive/Docs delegation without modifying or mocking away `FSBSkopeoContextRouter`; include a delegation spy that must receive the closed input and whose closed recognized result may pass. Run now and retain RED only for the absent new resolver.
  </action>
  <acceptance_criteria>
    - `test -f tests/skopeo-app-context-resolver.test.js` succeeds.
    - `rg -n "path|visible|selector|class|list.position|semanticEntity|null|contextEpoch|dispose|drive" tests/skopeo-app-context-resolver.test.js` finds all required cases.
    - The controlled RED names `skopeo-app-context-resolver.js` and not a malformed fixture.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set +e; out=$(node tests/skopeo-app-context-resolver.test.js 2&gt;&amp;1); code=$?; set -e; test $code -ne 0; printf "%s\n" "$out" | rg "skopeo-app-context-resolver"'</automated>
  </verify>
  <done>The resolver boundary is pinned before implementation, including non-vacuous no-entity and stale-tuple negatives.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the catalog-wide resolver and generic fail-quiet contract</name>
  <files>extension/content/skopeo-app-context-resolver.js</files>
  <read_first>
    - extension/content/skopeo-app-context-resolver.js (confirm absent before creating)
    - tests/skopeo-app-context-resolver.test.js
    - extension/content/skopeo-context-router.js
    - extension/content/skopeo-anchor-registry.js
    - extension/utils/skopeo-capability-projector.js
    - extension/utils/skopeo-profile-schema.js
    - .planning/milestones/v1.2.0-SKOPEO-phases/53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo/53.1-CONTEXT.md
    - .planning/milestones/v1.2.0-SKOPEO-phases/53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo/53.1-UI-SPEC.md
  </read_first>
  <action>
Implement a dual-export `FSBSkopeoAppContextResolver` with frozen `STATUS` (`recognized|uncertain|unsupported`), bounded `REASON`, `createResolver`, and `validateResult`. Validate the Plan 02 projection once, but recheck generation/exactOrigin/profileVersion on every `resolve({url,requestedLens,adapterEvidence})`; increment contextEpoch before every branch. Parse current URL and require exact origin equality. Default to the profile genre and app-level task lens. Call `resolveAdapter(adapterId, closedInput)` only for the profile's allowlisted adapter ID; accept only exact-key frozen adapter results with bounded evidence and stable identity kinds. A `generic-unanchored-v1` adapter or absent stable evidence yields a recognized app-level result with no entity/anchor, never a guessed one. Preserve `drive-docs-deep-pack` as a delegation result kind for Plan 05; do not copy or rewrite the Phase 53 router. Results contain only generation, exactOrigin, profile identity/version, contextEpoch, app/genre/lens, optional semantic entity/anchor reference, current groups, and closed reasons/evidence. Exclude raw URL, page copy, DOM/Range nodes, selectors, callbacks, and executable data. `dispose()` makes later attempts unsupported while still advancing the epoch.
  </action>
  <acceptance_criteria>
    - `node --check extension/content/skopeo-app-context-resolver.js` exits 0.
    - `node tests/skopeo-app-context-resolver.test.js` exits 0.
    - `rg -n "FSBSkopeoAppContextResolver|createResolver|contextEpoch|no-stable-entity|drive-docs-deep-pack" extension/content/skopeo-app-context-resolver.js` finds the contract.
    - `rg -n "querySelector|MutationObserver|innerHTML|outerHTML|eval\\(|new Function" extension/content/skopeo-app-context-resolver.js` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>node --check extension/content/skopeo-app-context-resolver.js &amp;&amp; node tests/skopeo-app-context-resolver.test.js &amp;&amp; node tests/skopeo-context-router.test.js</automated>
  </verify>
  <done>Catalog-wide context resolution is closed and epoch-owned; generic apps stay usefully app-level and unanchored, while the Drive deep adapter remains a delegation seam.</done>
</task>

</tasks>

<verification>
Run `node tests/skopeo-app-context-resolver.test.js && node tests/skopeo-context-router.test.js && node tests/skopeo-anchor-registry.test.js`.
</verification>

<success_criteria>
- Resolver inputs remain separate and exact; no URL/text/DOM heuristic manufactures semantic identity.
- Generic profiles recognize the app but stay unanchored without bundled stable evidence.
- Every attempt advances contextEpoch and stale tuple results fail quiet.
- Existing Drive/Docs routing behavior remains byte-untouched and green.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.2.0-SKOPEO-phases/53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo/53.1-03-SUMMARY.md`.
</output>
