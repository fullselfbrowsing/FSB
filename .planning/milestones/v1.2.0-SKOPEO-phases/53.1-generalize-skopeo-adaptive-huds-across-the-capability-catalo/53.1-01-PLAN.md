---
phase: 53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - catalog/skopeo/app-profiles.json
  - extension/utils/skopeo-profile-schema.js
  - scripts/generate-skopeo-profile-index.mjs
  - scripts/verify-skopeo-profile-coverage.mjs
  - extension/catalog/skopeo-profile-index.generated.js
  - tests/skopeo-profile-schema.test.js
  - tests/skopeo-profile-coverage.test.js
autonomous: true
requirements:
  - ADAPT-01
  - ADAPT-04
  - ADAPT-05
  - ADAPT-08
  - ADAPT-10
must_haves:
  truths:
    - "D-02: App stem, catalog service, exact admitted origin, and profile version remain distinct keys, and every committed descriptor/service joins exactly once without substring identity or cross-service merging."
    - "D-01: Profile data can select only bounded vocabulary, capability groups, attention defaults, and allowlisted adapter/renderer IDs; it cannot contain executable presentation behavior."
    - "D-10: The current 2,314 descriptor / 128 stem / 129 service counts are pinned as regression baselines while completeness is derived from deterministic set joins."
  artifacts:
    - path: "extension/utils/skopeo-profile-schema.js"
      provides: "Closed schemaVersion 1 profile validator and normalizer"
      exports: ["SCHEMA_VERSION", "GENRES", "ADAPTER_IDS", "RENDERER_IDS", "validateRegistry", "normalizeProfile"]
    - path: "extension/catalog/skopeo-profile-index.generated.js"
      provides: "Deterministic data-only runtime profile/readiness index"
      contains: "FsbSkopeoProfileIndex"
    - path: "scripts/verify-skopeo-profile-coverage.mjs"
      provides: "Durable full-corpus exactly-once coverage gate"
      exports: ["verifySkopeoProfileCoverage"]
  key_links:
    - from: "scripts/generate-skopeo-profile-index.mjs"
      to: "extension/catalog/recipe-index.generated.js"
      via: "descriptor/stem/service set join"
      pattern: "recipe-index\\.generated"
    - from: "scripts/generate-skopeo-profile-index.mjs"
      to: "scripts/report-t1-terminal-states.mjs"
      via: "canonical generated readiness surface"
      pattern: "buildTerminalStateReport"
    - from: "scripts/verify-skopeo-profile-coverage.mjs"
      to: "extension/catalog/skopeo-profile-index.generated.js"
      via: "byte-stable regeneration plus exactly-once validation"
      pattern: "skopeo-profile-index\\.generated"
---

<objective>
Create the one versioned, data-only Skopeo profile contract and its deterministic complete-catalog artifact.

Purpose: Establish D-01 through D-03 and D-10 before any current-tab projection or UI composition can trust profile data.
Output: Closed profile schema, authored profile registry, deterministic generator/runtime index, full-corpus gate, and RED-first schema/coverage oracles.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.2.0-SKOPEO-PROJECT-SNAPSHOT.md
@.planning/milestones/v1.2.0-SKOPEO-ROADMAP.md
@.planning/milestones/v1.2.0-SKOPEO-REQUIREMENTS.md
@.planning/milestones/v1.2.0-SKOPEO-STATE-SNAPSHOT.md
@.planning/milestones/v1.2.0-SKOPEO-phases/53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo/53.1-CONTEXT.md
@.planning/milestones/v1.2.0-SKOPEO-phases/53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo/53.1-RESEARCH.md
@.planning/milestones/v1.2.0-SKOPEO-phases/53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo/53.1-UI-SPEC.md
@.planning/milestones/v1.2.0-SKOPEO-phases/53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo/53.1-VALIDATION.md

<interfaces>
Existing catalog input from `extension/catalog/recipe-index.generated.js`:
```javascript
module.exports = { recipes, descriptors };
// descriptor: { slug, service, intentSynonyms, description,
//               actionVerb, sideEffectClass, backing, params? }
```

Canonical richer readiness source from `scripts/report-t1-terminal-states.mjs`:
```javascript
export function buildTerminalStateReport(opts = {});
// Preserve both row.readiness (source readiness) and terminal evidence
// row.surfaceStatus/row.executionEnabled. The generated presentationDisposition
// is one of: t1-ready, guarded-fail-closed, blocked, bridge-needed, uat-needed,
// learn-pending, discovery-pending, degraded, unsupported.
// Canonical fail-closed precedence:
//   1. terminal guarded-fail-closed|blocked|bridge-needed|uat-needed -> same value;
//   2. source learn-pending -> learn-pending;
//   3. source discovery-pending + terminal degraded-discovery-pending -> degraded;
//   4. remaining source discovery-pending -> discovery-pending;
//   5. source t1-ready + terminal t1-ready + executionEnabled === true -> t1-ready;
//   6. every missing, unknown, contradictory, or unsupported combination -> unsupported.
// executionEnabled/invocable are true only when presentationDisposition === t1-ready.
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authored profile JSON → generator | Repository data is untrusted until exact-key and bound validation succeeds. |
| Descriptor/readiness corpus → generated runtime index | Catalog rows must join deterministically without merging stem, service, or origin identities. |
| Generated index → later background/content consumers | Only frozen data and allowlisted IDs may cross into runtime. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan | Automated negative proof |
|-----------|----------|----------|-----------|-------------|-----------------|--------------------------|
| T-53.1-01 | High | Spoofing | exact service/origin join | mitigate | Parse origins, use exact equality, keep appStem/service/exactOrigin/profileVersion as separate fields, and fail ambiguous service/stem joins. | `node tests/skopeo-profile-coverage.test.js` rejects substring host `docs.google.com.evil.example`, duplicate origins, and the shared-service ambiguity mutation. |
| T-53.1-02 | High | Tampering | profile generator | mitigate | Require exactly one profile or explicit disposition per service; reject missing, stale, duplicate, and nondeterministic output. | `node tests/skopeo-profile-coverage.test.js` mutates each failure class and requires nonzero validation failures. |
| T-53.1-03 | High | Elevation of privilege | profile schema | mitigate | Exact keys, bounded counts/strings, text-only values, and closed adapter/renderer/genre enums; reject code, selectors, HTML/CSS, prompts, callbacks, and remote assets. | `node tests/skopeo-profile-schema.test.js` supplies every forbidden field/value class and proves rejection before generation. |
| T-53.1-05 | High | Elevation of privilege | generated readiness disposition | mitigate | Derive a closed presentation disposition from both source readiness and terminal evidence using the documented fail-closed precedence; retain both source fields for audit and make only `t1-ready` executable. | `node tests/skopeo-profile-coverage.test.js` exercises all nine dispositions plus missing, contradictory, and forged-enabled mutations and proves no state collapses or escalates to ready. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Land RED-first profile schema and full-corpus security oracles</name>
  <files>tests/skopeo-profile-schema.test.js, tests/skopeo-profile-coverage.test.js</files>
  <read_first>
    - tests/skopeo-profile-schema.test.js (confirm absent before creating)
    - tests/skopeo-profile-coverage.test.js (confirm absent before creating)
    - tests/capability-recipe-schema.test.js
    - tests/coverage-report.test.js
    - tests/t1-terminal-states.test.js
    - extension/catalog/recipe-index.generated.js
    - .planning/milestones/v1.2.0-SKOPEO-phases/53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo/53.1-CONTEXT.md
    - .planning/milestones/v1.2.0-SKOPEO-phases/53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo/53.1-VALIDATION.md
  </read_first>
  <action>
Create standalone `node:assert` tests before production code. The schema oracle must require exact root/profile keys; `schemaVersion === 1`; profileVersion as a bounded identifier; closed genres (`reader-knowledge`, `communication`, `document-editor`, `worklist-record`, `dashboard-admin`, `transactional`, `media-feed`, `generic-app`, `drive-docs-deep-pack`); attention ceiling only `ambient|anchored`; at most 12 groups, 32 slug prefixes per group, 80-character display text, and 32-character entity nouns. It must reject functions and the field/value classes `selector`, `html`, `css`, `script`, `callback`, `prompt`, remote asset URL, markup, bidi controls, unknown adapter/renderer IDs, unknown keys, and excessive arrays/text per D-01 and T-53.1-03. The coverage oracle must derive descriptor, stem (`slug` prefix), service, and `(stem, service)` sets from the committed catalog; pin 2,314/128/129; require exactly one profile/disposition per distinct service without treating stems as services; and mutate missing, stale, duplicate, ambiguous-origin, nondeterministic, and substring-origin cases per D-02/D-10. It must also synthesize source-readiness/terminal-evidence combinations for all nine presentation dispositions, assert the documented fail-closed precedence, and reject missing/unknown/contradictory inputs plus forged `executionEnabled`/`invocable` bits per T-53.1-05. Exported pure generator/validator seams are the test targets; do not copy production logic into the tests. Run both now and retain a controlled RED caused only by the missing new modules.
  </action>
  <acceptance_criteria>
    - `test -f tests/skopeo-profile-schema.test.js && test -f tests/skopeo-profile-coverage.test.js` succeeds.
    - `rg -n "docs\\.google\\.com\\.evil\\.example|selector|callback|remote|bidi" tests/skopeo-profile-schema.test.js tests/skopeo-profile-coverage.test.js` finds hostile cases.
    - `rg -n "2314|128|129|missing|stale|duplicate|ambiguous|determin" tests/skopeo-profile-coverage.test.js` finds every baseline and failure class.
    - `rg -n "presentationDisposition|guarded-fail-closed|blocked|bridge-needed|uat-needed|learn-pending|discovery-pending|degraded|unsupported|contradict" tests/skopeo-profile-coverage.test.js` finds all nine readiness outcomes and fail-closed mutations.
    - The controlled RED output names only the not-yet-created profile schema/generator seam, not a syntax error in the tests.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set +e; out=$(node tests/skopeo-profile-schema.test.js 2&gt;&amp;1; node tests/skopeo-profile-coverage.test.js 2&gt;&amp;1); code=$?; set -e; test $code -ne 0; printf "%s\n" "$out" | rg "skopeo-profile-(schema|index)|generate-skopeo-profile"'</automated>
  </verify>
  <done>Both security contracts exist, are syntactically executable, and fail only because their production counterpart is intentionally absent.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the closed authored profile registry and validator</name>
  <files>catalog/skopeo/app-profiles.json, extension/utils/skopeo-profile-schema.js</files>
  <read_first>
    - catalog/skopeo/app-profiles.json (confirm absent before creating)
    - extension/utils/skopeo-profile-schema.js (confirm absent before creating)
    - tests/skopeo-profile-schema.test.js
    - extension/content/skopeo-shell.js
    - extension/content/skopeo-context-router.js
    - .planning/milestones/v1.2.0-SKOPEO-phases/53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo/53.1-UI-SPEC.md
    - .planning/milestones/v1.2.0-SKOPEO-phases/53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo/53.1-CONTEXT.md
  </read_first>
  <action>
Implement a classic-script/CommonJS dual export named `FsbSkopeoProfileSchema` with `SCHEMA_VERSION`, frozen genre/adapter/renderer sets, `validateRegistry(value)`, and `normalizeProfile(value, derivedIdentity)`. The authored JSON is the sole editable registry: `{schemaVersion, profileVersion, defaults, profiles}`. Keep `appStem`, `service`, and exact HTTPS origins separate; profile IDs are bounded stable identifiers, not host substrings. Profiles contain only displayName, defaultGenre, pageNoun, entityVocabulary, capabilityGroups, attentionCeiling, adapterId, and rendererId. Seed audited representative overrides for every approved genre plus Drive/Docs delegation; all other catalog rows inherit the declared `generic-app`/`generic-unanchored-v1` safe default during generation. Use the exact UI-SPEC vocabulary/copy constraints: no host logos/colors, no enum strings as visible labels, no focused/interstitial default, and no profile-defined chrome. Unknown schema versions fail with `SKOPEO_PROFILE_SCHEMA_UNSUPPORTED`; malformed/forbidden data fails with `SKOPEO_PROFILE_INVALID` and a bounded path/reason, never raw input.
  </action>
  <acceptance_criteria>
    - `node --check extension/utils/skopeo-profile-schema.js` exits 0.
    - `node tests/skopeo-profile-schema.test.js` exits 0 and exercises all T-53.1-03 negative rows.
    - `node -e "const s=require('./extension/utils/skopeo-profile-schema.js'); const r=require('./catalog/skopeo/app-profiles.json'); if(!s.validateRegistry(r).ok) process.exit(1)"` exits 0.
    - `rg -n 'focused|interstitial' catalog/skopeo/app-profiles.json` finds no attention-ceiling value using either term.
    - `rg -n 'selector|html|css|callback|prompt|remoteAsset' catalog/skopeo/app-profiles.json` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>node --check extension/utils/skopeo-profile-schema.js &amp;&amp; node tests/skopeo-profile-schema.test.js</automated>
  </verify>
  <done>The sole authored registry validates under the closed versioned contract and cannot carry executable or arbitrary UI behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Generate and gate the complete deterministic runtime index</name>
  <files>scripts/generate-skopeo-profile-index.mjs, scripts/verify-skopeo-profile-coverage.mjs, extension/catalog/skopeo-profile-index.generated.js</files>
  <read_first>
    - scripts/generate-skopeo-profile-index.mjs (confirm absent before creating)
    - scripts/verify-skopeo-profile-coverage.mjs (confirm absent before creating)
    - extension/catalog/skopeo-profile-index.generated.js (confirm absent before creating)
    - catalog/skopeo/app-profiles.json
    - extension/utils/skopeo-profile-schema.js
    - tests/skopeo-profile-coverage.test.js
    - scripts/report-t1-terminal-states.mjs
    - scripts/coverage-report.mjs
    - scripts/package-extension.mjs
    - extension/catalog/recipe-index.generated.js
  </read_first>
  <action>
Create a deterministic ESM generator with exported `buildSkopeoProfileIndex({descriptors, terminalReport, authoredRegistry})`, `presentationDispositionFor(row)`, `renderSkopeoProfileIndex(index)`, and CLI modes write/default and `--check`. Join descriptors by `(appStem, service)` while retaining separate service and exact-origin fields; never merge `notion.so` with `app.notion.com`, `slack.com` with `app.slack.com`, or Jira with Confluence on `atlassian.net`. An unambiguous new service inherits the safe generic profile; a multi-stem/shared-origin or otherwise ambiguous row receives one explicit fail-quiet disposition rather than a guessed profile. Include only bounded current-service-ready data needed later: profile identity/version, catalog hash, exact origin index, service/stem keys, genre/vocabulary/groups, allowlisted IDs, canonical action label/effect, side-effect class, descriptor parameter schema/summary, raw `sourceReadiness`, raw `sourceTerminalState`/`surfaceStatus`, canonical `presentationDisposition`, and `executionEnabled`/`invocable`. Implement exactly the interface precedence above so terminal guarded/blocked/bridge/UAT evidence wins, learn and discovery remain distinguishable, `degraded-discovery-pending` becomes `degraded` only with discovery source evidence, readiness requires matching ready source and terminal evidence, and all other combinations become `unsupported`. Compute both booleans locally as `presentationDisposition === 't1-ready'`; never trust source booleans. Derive all fields from `buildTerminalStateReport` and the descriptor schema; do not hand-maintain a second readiness list or copy content-supplied consequence text. Render a frozen classic-script IIFE publishing `globalThis.FsbSkopeoProfileIndex` plus CommonJS export. The verifier recomputes all joins and dispositions, asserts the current 2,314/128/129 baselines, exactly-once sets, no stale/duplicate/ambiguous mappings, valid IDs, and byte equality with the committed artifact. It must exit nonzero on every T-53.1-01/T-53.1-02/T-53.1-05 mutation.
  </action>
  <acceptance_criteria>
    - `node scripts/generate-skopeo-profile-index.mjs && node scripts/generate-skopeo-profile-index.mjs --check` exits 0 without a second-file diff.
    - `node scripts/verify-skopeo-profile-coverage.mjs` reports 2,314 descriptors, 128 stems, 129 services, and zero coverage failures.
    - `node tests/skopeo-profile-schema.test.js && node tests/skopeo-profile-coverage.test.js` exits 0.
    - `rg -n "FsbSkopeoProfileIndex|catalogVersion|profileVersion|sourceReadiness|sourceTerminalState|presentationDisposition|executionEnabled|invocable|actionLabel|effect" extension/catalog/skopeo-profile-index.generated.js` finds all runtime fields.
    - `rg -n "guarded-fail-closed|blocked|bridge-needed|uat-needed|learn-pending|discovery-pending|degraded|unsupported" scripts/generate-skopeo-profile-index.mjs` finds every canonical presentation disposition.
    - `rg -n "buildTerminalStateReport" scripts/generate-skopeo-profile-index.mjs` proves readiness is generated from the canonical report seam.
  </acceptance_criteria>
  <verify>
    <automated>node tests/skopeo-profile-schema.test.js &amp;&amp; node tests/skopeo-profile-coverage.test.js &amp;&amp; node scripts/generate-skopeo-profile-index.mjs --check &amp;&amp; node scripts/verify-skopeo-profile-coverage.mjs</automated>
  </verify>
  <done>The committed runtime data is byte-stable, complete across the full corpus, and fails closed under profile/origin/catalog drift.</done>
</task>

</tasks>

<verification>
Run `node tests/skopeo-profile-schema.test.js && node tests/skopeo-profile-coverage.test.js && node scripts/generate-skopeo-profile-index.mjs --check && node scripts/verify-skopeo-profile-coverage.mjs`. Confirm generated output is unchanged on a second run and every High threat's negative mutation is non-vacuous.
</verification>

<success_criteria>
- All 2,314 descriptors, 128 stems, and 129 services are represented by exactly one profile/disposition join without collapsing stem, service, or origin.
- Profiles are schemaVersion 1, data-only, bounded, text-safe, and select only allowlisted adapters/renderers.
- Readiness rows preserve source readiness and terminal evidence, deterministically distinguish all nine presentation dispositions, and mark only evidence-matched `t1-ready` rows executable/invocable.
- Missing, stale, duplicate, ambiguous-origin, hostile-field, and nondeterministic inputs fail automated checks.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.2.0-SKOPEO-phases/53.1-generalize-skopeo-adaptive-huds-across-the-capability-catalo/53.1-01-SUMMARY.md`.
</output>
