---
phase: 52-on-demand-hud-lifecycle-primitive-shell
plan: 11
type: execute
wave: 9
depends_on:
  - 09
files_modified:
  - extension/content/skopeo-shell.js
  - tests/skopeo-shell-contract.test.js
  - tests/skopeo-browser-contract.test.js
autonomous: true
gap_closure: true
requirements:
  - HUD-03
  - HUD-04
  - HUD-05
user_setup: []

must_haves:
  truths:
    - "While Focused or Interstitial is committed, scroll, visual-viewport motion, and layout-only required-control movement revoke stale geometry without requiring a synthetic window resize; the shell synchronously preserves, unwinds, or requests terminal unsafe-layout cleanup."
    - "A suspended Anchored scope is never restored with an obsolete rail side: Back applies the current placement to the exact retained nodes before they become active."
    - "Geometry invalidation owns a bounded set of listeners and at most one rich-state animation frame, releases that ownership when the rich scope/session ends, preserves scope identity, and destroys to the exact eleven-key zero certificate."
  artifacts:
    - path: "extension/content/skopeo-shell.js"
      provides: "Owned lifetime geometry invalidation and current-placement scope restoration"
      contains: "_revalidateGeometry"
      min_lines: 3100
    - path: "tests/skopeo-shell-contract.test.js"
      provides: "Deterministic non-resize motion, invalidation ownership, and Back-placement regressions"
      contains: "testRichGeometryRevalidation"
      min_lines: 1923
    - path: "tests/skopeo-browser-contract.test.js"
      provides: "Local-Chrome Focused/Gate motion and restored-rail placement evidence"
      contains: "runRichResizeContract"
      min_lines: 1449
  key_links:
    - from: "extension/content/skopeo-shell.js"
      to: "tests/skopeo-shell-contract.test.js"
      via: "mock scroll/visual-viewport signals and the owned frame advance exercise the production revalidation path"
      pattern: "scroll|visualViewport|requestAnimationFrame|_revalidateGeometry"
    - from: "extension/content/skopeo-shell.js"
      to: "tests/skopeo-browser-contract.test.js"
      via: "real control motion and safe placement-side changes are observed through the production shell without a synthetic resize for WR-07"
      pattern: "focused|interstitial|back|placement"
---

<objective>
Close WR-07 and WR-10 by keeping rich-surface geometry revocable for its whole lifetime and making restored scopes consume current placement authority.

Purpose: Prevent a moving host control or a stale suspended rail from becoming covered after a formerly safe Focused/Gate transition, while preserving the exact one-level Back, focus, identity, and teardown contracts.

Output: One bounded owned invalidation mechanism, current-placement restoration for retained Anchored scopes, and mock plus local-Chrome regression evidence.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.2.0-SKOPEO-PROJECT-SNAPSHOT.md
@.planning/milestones/v1.2.0-SKOPEO-ROADMAP.md
@.planning/milestones/v1.2.0-SKOPEO-STATE-SNAPSHOT.md
@.planning/milestones/v1.2.0-SKOPEO-REQUIREMENTS.md
@.planning/milestones/v1.2.0-SKOPEO-phases/52-on-demand-hud-lifecycle-primitive-shell/52-CONTEXT.md
@.planning/milestones/v1.2.0-SKOPEO-phases/52-on-demand-hud-lifecycle-primitive-shell/52-RESEARCH.md
@.planning/milestones/v1.2.0-SKOPEO-phases/52-on-demand-hud-lifecycle-primitive-shell/52-PATTERNS.md
@.planning/milestones/v1.2.0-SKOPEO-phases/52-on-demand-hud-lifecycle-primitive-shell/52-UI-SPEC.md
@.planning/milestones/v1.2.0-SKOPEO-phases/52-on-demand-hud-lifecycle-primitive-shell/52-VERIFICATION.md
@.planning/milestones/v1.2.0-SKOPEO-phases/52-on-demand-hud-lifecycle-primitive-shell/52-07-SUMMARY.md
@.planning/milestones/v1.2.0-SKOPEO-phases/52-on-demand-hud-lifecycle-primitive-shell/52-09-SUMMARY.md
@extension/content/skopeo-shell.js
@tests/skopeo-shell-contract.test.js
@tests/skopeo-browser-contract.test.js

<gap_coverage>
Plans 52-01 through 52-10 retain coverage of the Phase 52 goal, all seven HUD requirements, and D-01 through D-14. This plan claims only the HUD-03/HUD-04/HUD-05 repairs needed for WR-07 and WR-10. Plan 52-12 owns WR-09 and may execute in the same wave because its production and test files are disjoint. Drive/Docs routing, corpus work, Graphify/contracts, extra capability packs, and live UAT remain excluded; `52-UAT.md` stays read-only with zero live PASS rows.

## Must Haves: Decision Preservation Coverage

| Locked decision | Existing executed implementation | What 52-11 preserves or re-verifies |
|---|---|---|
| D-04: | Plans 52-02 and 52-07 established one six-primitive registry and retained attention scopes rather than pack-specific chrome. | Keep the registry, node identities, and public shell API unchanged while the shell and local-Chrome contracts re-verify that geometry invalidation/restoration neither replaces nor adds primitives. |
| D-05: | Plans 52-02 and 52-03 made ordinary committed entry Ambient-only and kept richer grammar behind the isolated controlled fixture. | Add no invocation/render path; re-run the production shell/runtime-adjacent gates so motion handling and restored placement cannot expose the richer fixture during normal entry. |
| D-06: | Plans 52-02 and 52-07 implemented the Ambient/Anchored/Focused/Interstitial policy, scarce halo/ghost/gate combinations, and attention-owned resource plateaus. | Preserve the attention allowlists and exact active/suspended scopes while Node and local-Chrome cases re-verify Focused/Gate preservation or bounded unwind without changing primitive scarcity. |
| D-07: | Plans 52-02 and 52-03 implemented one-level Back/close/Escape behavior, Ambient-to-off routing, and exact focus-target restoration; Plan 52-09 reused that path for unsafe geometry unwind. | Route restoration through the existing single-level `back()`/scope mechanics and re-verify Gate -> Focused -> Anchored identity, focus, and one-level-per-Back behavior after placement changes. |
| D-10: | Plans 52-02 through 52-04 established one lifecycle-owned Shadow shell, dynamically injected only as the dedicated shell/runtime pair. | Change only private geometry ownership inside that shell; assert one root/owner throughout and exact zero after destroy, with no injection-list, runtime-envelope, or public-API change. |
| D-11: | Plans 52-02 and 52-07 established the viewport-fixed/top-layer-capable pointer-transparent host, control-only pointer surfaces, overlay geometry, and unchanged host snapshots; Plan 52-09 added fresh/revocable placement. | Extend that same geometry certificate to scroll, visual-viewport, and layout motion, re-verifying 8px host-control clearance, pointer/resource stability, and no host mutation in Node and local Chrome. |
| D-12: | Plans 52-02 and 52-07 implemented no focus theft in Ambient/Anchored plus deliberate deep-Shadow focus and exact restoration in Focused/Interstitial; Plan 52-09 preserved focus during safe/unwind geometry changes. | Reuse the retained scope/focus identities and re-run accessibility plus browser contracts proving safe invalidation is focus-stable and Back restores the original declared control without forcing body focus. |
| D-13: | Plans 52-02, 52-05, and 52-07 implemented hidden-node removal, names/roles, keyboard and screen-reader behavior, visible focus, zoom/narrow layout, forced colors, and reduced motion. | Do not change semantic renderers or preference CSS; keep `skopeo-accessibility.test.js`, narrow local-Chrome geometry, and adjacent overlay gates blocking while restored scopes expose only their already-supported visible nodes. |
| D-14: | Plans 52-01 through 52-03 implemented idempotent terminal cleanup, and Plans 52-07/52-08 proved attention/runtime ownership and exact post-cleanup eleven-key zero certificates. | Ledger every added listener/frame, release it on scope exit and destroy, keep unsafe-layout one-shot, and re-verify repeated exact-zero teardown without touching Drive/Docs controls, unrelated FSB state, or deferred live UAT. |
</gap_coverage>

<interfaces>
Keep the public `FSBSkopeoShell` API, exact prepare token, one Shadow owner, six primitives, four attention levels, single-level public `back()`, and `unsafe-layout` kill envelope unchanged. Extend only private shell ownership:

```javascript
_installGeometryInvalidation() {
  // One stable synchronous callback for window resize, capture-phase document
  // scroll, and visualViewport resize/scroll when that EventTarget exists.
}

_syncRichGeometryFrame() {
  // While attention is Focused or Interstitial, own at most one pending RAF.
  // Its callback releases the fired handle, calls _revalidateGeometry(), and
  // rearms only when the same live shell still owns a safe rich surface.
}

_cancelRichGeometryFrame() {
  // Cancel/release on rich-scope exit and session destroy.
}

_applyPlacement(placement, scope) {
  // Apply lens/rail/entity-row placement to either the active surface or an
  // explicitly retained detached scope without replacing node identities.
}

_restoreSurfaceScope(scope) {
  // Before exposing Ambient/Anchored retained nodes, apply _currentPlacement;
  // an absent/unapplied placement fails closed through unsafe-layout.
}
```

The motion watch is bounded to one frame only while rich attention is active; it is not an interval, unbounded observer, page mutation, site selector, server, daemon, dependency, or new runtime protocol. Event callbacks invoke `_revalidateGeometry()` in the same dispatch; layout-only movement is caught on the next owned animation frame. Preserve D-07, D-09, D-10, D-11, D-12, and D-14 exactly.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Own bounded non-resize invalidation for Focused and Gate geometry</name>
  <read_first>
    - extension/content/skopeo-shell.js (modified file; read constructor ownership fields, `_installResizeListener`, `_createSurfaceScope`, `_disposeSurfaceScope`, `_commitSurfaceScope`, `_currentRichGeometry`, `_revalidateGeometry`, `render`, `back`, and `destroy`)
    - tests/skopeo-shell-contract.test.js (modified file; read `MockClock`, `MockWindow`, `createHarness`, `testAttentionResourcePlateaus`, and `testRichGeometryRevalidation`)
    - tests/skopeo-browser-contract.test.js (modified file; read `createRichShell`, `runRichResizeContract`, the outer async fixture assembly, and its rich-geometry assertions)
    - tests/helpers/skopeo-resource-ledger.js (read-only exact eleven-category acquire/release and zero oracle)
    - .planning/milestones/v1.2.0-SKOPEO-phases/52-on-demand-hud-lifecycle-primitive-shell/52-VERIFICATION.md (WR-07 reproduction and repair criteria)
    - .planning/milestones/v1.2.0-SKOPEO-phases/52-on-demand-hud-lifecycle-primitive-shell/52-09-SUMMARY.md (existing resize-only unwind, focus, scope, and resource behavior that must remain intact)
    - .planning/milestones/v1.2.0-SKOPEO-phases/52-on-demand-hud-lifecycle-primitive-shell/52-UI-SPEC.md (16px viewport inset, 8px required-control clearance, focus restoration, and unsafe-layout copy)
  </read_first>
  <files>extension/content/skopeo-shell.js, tests/skopeo-shell-contract.test.js, tests/skopeo-browser-contract.test.js</files>
  <action>
    Add red-first Node cases against the production shell for both Focused and Interstitial. Move the exact `_requiredHostControl()` rectangle into the active `.skopeo-focused-card` or `.skopeo-gate` without dispatching `window.resize`; advance the mock clock by one owned animation frame and prove the former implementation leaves the unsafe rich level committed. Add separate signal cases showing capture-phase scroll and available `visualViewport.scroll`/`visualViewport.resize` call the same geometry revalidation synchronously. Each unsafe case must restore the nearest currently measured-safe retained scope or, when placement/restoration is impossible, record one terminal `{generation, reason:'unsafe-layout', state}` request.

    Replace the resize-only installer with one session-owned geometry invalidation boundary. Register one stable callback on `window.resize`, capture-phase document scroll, and both visual-viewport events only when `window.visualViewport` supports matching add/remove methods. Every registered listener gets its own ledger handle and exact same target/type/options/function removal. The callback exits for disposed/unmounted shells and otherwise invokes `_revalidateGeometry()` before returning; do not debounce a safety decision behind a timer.

    Add one rich-attention layout-motion watch using `window.requestAnimationFrame`. Own no more than one pending frame and one `animationFrames` ledger entry. When a frame fires, clear and release that exact handle before synchronous `_revalidateGeometry()`; rearm only if the shell remains mounted, has not requested unsafe-layout, and its active attention is still Focused or Interstitial. Arm after a rich scope commits/restores, cancel when attention reaches Anchored/Ambient or the active rich scope is disposed, and cancel/release again idempotently during `destroy()`. Make `_requestUnsafeLayoutKill()` one-shot for the shell generation so event/frame convergence cannot emit repeated terminal requests while runtime teardown is in flight.

    Update the mock visual-viewport/EventTarget seam and plateau assertions so session listeners remain a stable count, rich attention owns exactly one animation frame, Anchored/Ambient own zero frames, repeated safe frames do not grow any category, and all destroy paths return exact zero. Preserve safe-rich attention, nodes, focus, live copy, required-control identity, and one-root state across a revalidation frame.

    Extend the local-Chrome fixture as an awaited contract. At normal and 420 CSS-pixel widths, move the real fixed required control into Focused and Gate geometry, await the owned frame rather than dispatching a synthetic resize, and record the same nearest-safe unwind/terminal outcome, node/scope identity, focus, nonintersection, stable resource plateau, and exact-zero destroy. Also exercise a real scroll or visual-viewport signal with safe geometry and prove immediate preservation. Keep the zero-dependency Chrome lookup, virtual-time result node, and temporary-profile cleanup.
  </action>
  <verify>
    <automated>node tests/helpers/skopeo-resource-ledger.js --self-test &amp;&amp; node tests/skopeo-shell-contract.test.js &amp;&amp; node tests/skopeo-accessibility.test.js &amp;&amp; node tests/skopeo-browser-contract.test.js &amp;&amp; node --check extension/content/skopeo-shell.js</automated>
  </verify>
  <acceptance_criteria>
    - Production owns resize, capture-scroll, and available visual-viewport resize/scroll listeners with exact matching cleanup and routes each signal through synchronous `_revalidateGeometry()`.
    - Focused and Gate each own at most one pending animation frame; layout-only required-control motion is handled on the next frame without any synthetic window resize.
    - Unsafe Focused never remains Focused and unsafe Gate never remains Gate; the nearest measured-safe scope is restored or exactly one terminal unsafe-layout request is emitted.
    - Safe signal/frame handling preserves attention, active/suspended identities, deep focus, live copy, one root, and the complete resource snapshot except for the stable one-frame ownership itself.
    - Leaving rich attention releases the frame immediately, repeated frames do not accumulate handles, and repeated destroy reports exact eleven-key zero in Node and local Chrome.
  </acceptance_criteria>
  <done>Rich geometry has owned lifetime invalidation for scroll, visual viewport, and otherwise silent layout motion, closing WR-07 without polling intervals or unbounded observers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply current placement when retained Anchored scopes return</name>
  <read_first>
    - extension/content/skopeo-shell.js (modified file; read `_scopeQuery`, `_applyPlacement`, `_restoreSurfaceScope`, `_revalidateGeometry`, `render`, and both rich branches of `back` after Task 1)
    - tests/skopeo-shell-contract.test.js (modified file; read `setupAnchoredFixture`, `captureRichState`, `assertSafeRichResizePreserved`, and the active-Anchored rail placement case)
    - tests/skopeo-browser-contract.test.js (modified file; read real rectangle helpers and the Focused/Gate safe-resize fixture before extending its Back sequence)
    - .planning/milestones/v1.2.0-SKOPEO-phases/52-on-demand-hud-lifecycle-primitive-shell/52-VERIFICATION.md (WR-10 right-rail reproduction and exact repair matrix)
    - .planning/milestones/v1.2.0-SKOPEO-phases/52-on-demand-hud-lifecycle-primitive-shell/52-07-SUMMARY.md (identity-preserving suspended-scope and one-level focus contract)
    - .planning/milestones/v1.2.0-SKOPEO-phases/52-on-demand-hud-lifecycle-primitive-shell/52-09-SUMMARY.md (fresh `_currentPlacement` authority and safe-rich early-return behavior)
  </read_first>
  <files>extension/content/skopeo-shell.js, tests/skopeo-shell-contract.test.js, tests/skopeo-browser-contract.test.js</files>
  <action>
    Add red-first Node regressions for the exact WR-10 sequences. Begin with a right-side Anchored rail, enter Focused, insert a visible blocker over the current right placement while leaving the Focused card safe, and dispatch resize so `_currentPlacement.corner` changes to a left corner. Assert ordinary `back()` currently restores the exact Anchored scope with stale `right:16px`. Repeat through Anchored -> Focused -> Interstitial, keep Gate safe while forcing the side change, then call Back twice. Capture placement, blocker rectangle, active/suspended scope identities, node arrays, focus targets, all eleven resource values, root count, and final teardown before each sequence.

    Refactor `_applyPlacement` to accept an optional scope and locate lens, rail, and adaptive entity-row nodes through `_scopeQuery` when the scope is detached; retain the active-surface behavior when no scope is supplied. Before `_restoreSurfaceScope` exposes an Ambient or Anchored scope, apply the current `_currentPlacement` to those exact retained nodes. Do not clone, rebuild, or replace the scope. If current placement is absent or cannot be applied, fail closed and request the shell's one-shot terminal unsafe-layout cleanup rather than exposing stale geometry. Focused restoration remains identity-only, and Gate -> Focused -> Anchored still consumes exactly one level per Back.

    Complete both Node sequences and their local-Chrome equivalents. For Focused-safe-resize -> Back, require the restored Anchored rail to use the new left side, not intersect the blocker at 8px clearance, equal the original Anchored scope/node identities, restore the original anchor focus, keep one root, and equal the Anchored resource plateau after Task 1's frame is released. For Gate-safe-resize -> Back -> Back, first prove exact Focused restoration and then the same current-placement Anchored result. The safe rich resize itself must not mutate the suspended scope early unless the chosen implementation deliberately updates it through the same scoped placement helper; either way, only current placement may be visible on restoration. Destroy and repeated destroy must be exact zero.

    Preserve the active Ambient/Anchored placement update cases, semantic/adaptive Anchored placement, staged collision rollback, and all Task 1 non-resize invalidation cases. Add no new public method or runtime message.
  </action>
  <verify>
    <automated>node tests/skopeo-shell-contract.test.js &amp;&amp; node tests/skopeo-accessibility.test.js &amp;&amp; node tests/skopeo-browser-contract.test.js &amp;&amp; node tests/overlay-stability-cadence.test.js &amp;&amp; node tests/overlay-content-audit.test.js &amp;&amp; node tests/skopeo-session-lifecycle.test.js &amp;&amp; npm run validate:extension</automated>
  </verify>
  <acceptance_criteria>
    - Focused-safe-resize -> Back applies the current side to the exact retained Anchored rail and leaves no 8px-clearance intersection with the blocker that forced the change.
    - Gate-safe-resize -> Back -> Back restores the exact Focused then Anchored identities, and the final Anchored rail uses the current placement rather than its pre-Gate side.
    - Restoring current placement performs no node replacement, extra focus operation, root creation, or resource growth; the Anchored plateau is exact after the rich frame is released.
    - Missing/unapplied placement never exposes a stale retained scope and produces no more than one terminal unsafe-layout request.
    - Node and required local-Chrome regressions end with exact eleven-key zero, and `git hash-object .planning/milestones/v1.2.0-SKOPEO-phases/52-on-demand-hud-lifecycle-primitive-shell/52-UAT.md` remains `a9fa6926c909d322fe45d8d959d37a24f4cafd80`.
  </acceptance_criteria>
  <done>Every retained Anchored scope consumes current placement authority when restored, closing WR-10 while preserving exact Back identity and teardown.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Committed rich shell to moving host page | Scroll, visual viewport, CSS/layout motion, or a moving required control can revoke a previously safe rectangle. |
| Owned invalidation callback to terminal lifecycle | Concurrent event/frame signals must not create repeated kill requests or outlive their scope/session. |
| Current placement to retained detached scope | Detached nodes retain identity but may hold obsolete physical placement until restored. |
| Automated Chrome mechanics to live evidence | Local deterministic mechanics do not constitute deferred Drive/Docs, VoiceOver, shortcut, or MV3 live approval. |

## ASVS L1 Threat Register

| Threat | Severity | Disposition | Concrete mitigation and blocking evidence |
|--------|----------|-------------|-------------------------------------------|
| Host interaction interception | High | mitigate in Tasks 1-2 | Same-dispatch event revalidation, one-frame layout watch, 8px clearance, nearest-safe unwind, and restored-rail nonintersection in Node/Chrome. |
| Focus denial or trap | High | preserve in Tasks 1-2 | Existing one-level Back and deep-focus targets are reused; safe invalidation is zero-write and unwind restores exact declared controls. |
| Stale resurrection / unsafe continuation | High | mitigate in Task 1 | One-shot unsafe-layout terminal request plus frame/listener ownership prevents stale callbacks from continuing after teardown. |
| Resource accumulation | High | mitigate in Task 1 | Stable session listeners, at most one ledgered frame only during rich attention, plateau equality, and repeated exact-zero destroy. |
| Wrong-tab spoofing | High | preserve / Plan 52-12 boundary | Shell accepts no tab identity and changes no worker/side-panel protocol; registered lifecycle and authority suites remain green. |
</threat_model>

<verification>
Run both focused task commands, then confirm `tests/skopeo-shell-contract.test.js` and `tests/skopeo-browser-contract.test.js` each occur exactly once in `package.json`'s `test` script, run `npm test`, and run `git diff --check -- extension/content/skopeo-shell.js tests/skopeo-shell-contract.test.js tests/skopeo-browser-contract.test.js`. WR-07/WR-10, an unsafe committed rich surface, stale restored rail placement, repeated terminal requests, listener/frame leakage, identity/focus loss, skipped local Chrome, or any `52-UAT.md` mutation is blocking. Inspect and restore only known generated date-stamp noise if the registered full chain produces it; do not alter unrelated user changes.
</verification>

<success_criteria>
- Scroll and visual-viewport signals revalidate synchronously, and layout-only required-control motion is caught by one bounded rich-state frame without synthetic resize.
- Unsafe Focused/Gate geometry restores the nearest measured-safe retained scope or requests one terminal unsafe-layout cleanup.
- Restored Anchored nodes always use current placement and remain clear of the blocker that forced a side change.
- Scope/node/focus identity, one-root ownership, resource plateaus, registered tests, and repeated exact-zero teardown remain intact.
- D-01 through D-14 and deferred scope remain unchanged; no dependency, site-specific selector, server, daemon, semantic router, Graphify work, or live-UAT claim is introduced.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.2.0-SKOPEO-phases/52-on-demand-hud-lifecycle-primitive-shell/52-11-SUMMARY.md` with the owned signal/frame inventory, Focused/Gate non-resize matrix, restored-placement Back matrix, identity/resource/zero evidence, executed Chrome path, registered test results, and explicit statement that L01-L15 remain deferred.
</output>
