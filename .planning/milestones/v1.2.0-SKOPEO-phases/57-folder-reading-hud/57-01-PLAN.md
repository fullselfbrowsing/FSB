---
phase: 57-folder-reading-hud
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - extension/utils/skopeo-hud-schema.js
  - extension/utils/skopeo-hud-projector.js
  - tests/skopeo-hud-schema.test.js
  - tests/skopeo-hud-projector.test.js
autonomous: true
requirements:
  - VIEW-01
  - VIEW-02
  - VIEW-03
must_haves:
  truths:
    - "D-01: A trusted caller can turn complete current corpus, graph, truth, and vendor-label inputs into one bounded, recursively frozen folder or reading projection without storage, Chrome, DOM, clock, locale, network, or host-page access."
    - "D-17: Every vendor projection carries owner, document/index state, governing state, one explicitly typed next material date, a separate consequence, memo evidence, first-class neutral policy/memo-requirement/notification slots, and evidence-backed gaps with explicit overflow."
    - "D-14: Notice deadline, renewal, termination, and expiration remain distinct through validation, ordering, and projection, consequences remain separate, and a missing notice deadline is never replaced by another date type."
    - "D-15: Incomplete, ambiguous, inaccessible, stale, or over-cap input cannot prove absence or publish a usable truth prefix; D-16: current final, scan, indexing, owner, and version-conflict states require evidence while later-phase policy and memo obligations remain unasserted."
  artifacts:
    - path: "extension/utils/skopeo-hud-schema.js"
      provides: "Closed descriptor-safe Phase 57 projection vocabulary, exact parsers, enums, finite caps, and recursive freezing"
      contains: "FsbSkopeoHudSchema"
    - path: "extension/utils/skopeo-hud-projector.js"
      provides: "Pure deterministic corpus/graph/truth-to-HUD projection and vendor/date/gap aggregation"
      contains: "FsbSkopeoHudProjector"
    - path: "tests/skopeo-hud-schema.test.js"
      provides: "Exact-key, hostile-shape, cap, safe-text, date, neutral-slot, and deep-freeze contract"
      contains: "skopeo hud schema contract"
    - path: "tests/skopeo-hud-projector.test.js"
      provides: "Permutation-invariant vendor/family/date/gap projection oracle"
      contains: "skopeo hud projector contract"
  key_links:
    - from: "extension/utils/skopeo-hud-projector.js"
      to: "extension/utils/skopeo-hud-schema.js"
      via: "every candidate projection is reparsed by the closed schema before return"
      pattern: "parseProjection"
    - from: "extension/utils/skopeo-hud-projector.js"
      to: "extension/utils/skopeo-corpus-schema.js"
      via: "vendor grouping uses only certified vendorScopeFileId/source-state input"
      pattern: "vendorScopeFileId"
    - from: "extension/utils/skopeo-hud-projector.js"
      to: "Phase 56 truth display snapshots"
      via: "governing, typed-date, consequence, conflict, and absence states come only from accepted current proof fields"
      pattern: "authorizedSetDigest|evaluationContext|blockerCodes"
---

<objective>
Fix the complete closed semantic language for Phase 57 and implement the pure folder/reading projector before background or DOM integration depends on it.

Purpose: Make trusted contract state displayable without allowing presentation code, labels, filenames, list order, incomplete sets, or Phase 58/59 concepts to manufacture authority.
Output: A frozen exact-key HUD schema, a pure deterministic projector, and focused schema/projector tests covering hostile input, all required vendor/date/gap states, caps, overflow, and permutation invariance.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.2.0-SKOPEO-PROJECT-SNAPSHOT.md
@.planning/milestones/v1.2.0-SKOPEO-ROADMAP.md
@.planning/milestones/v1.2.0-SKOPEO-REQUIREMENTS.md
@.planning/milestones/v1.2.0-SKOPEO-STATE-SNAPSHOT.md
@.planning/milestones/v1.2.0-SKOPEO-phases/57-folder-reading-hud/57-CONTEXT.md
@.planning/milestones/v1.2.0-SKOPEO-phases/57-folder-reading-hud/57-RESEARCH.md
@.planning/milestones/v1.2.0-SKOPEO-phases/57-folder-reading-hud/57-PATTERNS.md
@.planning/milestones/v1.2.0-SKOPEO-phases/57-folder-reading-hud/57-UI-SPEC.md
@.planning/milestones/v1.2.0-SKOPEO-phases/57-folder-reading-hud/57-VALIDATION.md
@extension/utils/skopeo-corpus-schema.js
@extension/utils/skopeo-capability-projector.js
@extension/utils/skopeo-graph-schema.js
@extension/utils/skopeo-truth-schema.js
@tests/skopeo-corpus-schema.test.js
@tests/skopeo-capability-projection.test.js

<interfaces>
Create the frozen classic-script/CommonJS global `FsbSkopeoHudSchema` with exact version `skopeo-hud-projection/1`. Its public surface contains only `VERSION`, the closed enum arrays, `LIMITS`, and `parseProjection(value)`. The three projection modes are exactly `folder`, `reading`, and `contract-closed`.

Fix these presentation bounds from the approved UI contract: 32 projected vendors, 3 next-material-date summary rows, 4 urgent-gap summary rows, 3 gaps per vendor card, 10 reading facts, 6 reading conflicts/gaps, and a 64 KiB serialized projection ceiling. Lists at their exact maximum parse; maximum-plus-one either returns a closed whole-result failure or carries the schema-authorized explicit overflow count. Silent truncation is invalid.

Create the frozen classic-script/CommonJS global `FsbSkopeoHudProjector` with exact `VERSION: 'skopeo-hud-projector/1'` and `createProjection(input)`. The input is one exact descriptor-safe record containing a background-derived mode, current complete/partial manifest projection, complete exact graph snapshot, current truth display snapshot, freshly certified vendor-label map, evaluation-context date, and authority envelope. It performs no I/O and returns only a schema-parsed recursively frozen Phase 57 projection.

The common content-safe envelope contains the projection version, exact generation/origin/profile-version/context-epoch/semantic-entity token, original request action token, a new opaque projection token, mode, currentness/result state, and bounded semantic body. It never contains tab/account/corpus/root/file/source/graph/truth/storage IDs, resource keys, certificates, provider data, raw URLs, raw records, or source text.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Corpus/graph/truth inputs → projector | Stable IDs and complete-set/currentness proof are authoritative; all labels and values remain untrusted bounded data. |
| Projector → content model | Only closed semantic enums, bounded display values, explicit overflow, and opaque tokens may cross; raw authority is forbidden. |
| Incomplete/over-cap inputs → visible state | A partial set can show explicitly partial manifest status but cannot yield governing, absence, or date conclusions. |
| Phase 57 → Phase 58/59 ownership | Policy/memo obligation and notification delivery have visible reserved slots but no invented outcome or control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-57-01 | Tampering | schema text and nested input parsing | mitigate | Tasks 1-3 reflect own keys/descriptors before reads; reject accessors, symbols, sparse arrays, custom prototypes, control text, hidden fields, and unsafe authority fields; clone then deep-freeze. |
| T-57-02 | Spoofing / Tampering | absence, governing, date, and gap derivation | mitigate | Tasks 1-3 require complete graph/truth/evaluation context and accepted Phase 56 states; incomplete authority maps to exact blockers or neutral states, never inferred absence. |
| T-57-03 | Spoofing | projection authority envelope | mitigate | Tasks 1-2 validate every exact tuple field and token as data; downstream plans rederive sender authority and recheck it before paint. |
| T-57-05 | Tampering / Denial of service | list/byte caps and partial publication | mitigate | Tasks 1-3 add exact max/max-plus-one tests, whole-result closure for truth, explicit authorized overflow only, deterministic bounded ordering, and a 64 KiB ceiling. |
| T-57-06 | Information disclosure / Elevation of privilege | content-safe projection shape | mitigate | Tasks 1-3 use exact-key allowlists that reject source IDs, URLs, locators, storage keys, provider data, graph/truth records, certificates, and facades. |
| T-57-08 | Elevation of privilege | later-phase states and controls | mitigate | Tasks 1-3 expose closed neutral slots only and reject ask, draft, send, approval, Document 10 decision, memo-obligation result, notification outcome, recipient, schedule, and delivery authority. |
| T-57-SC | Tampering | package supply chain | accept | This plan installs no package and changes no package or lockfile; all modules use repository-native classic JavaScript and Node assertions. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write controlled RED contracts for the closed schema and pure projector</name>
  <files>tests/skopeo-hud-schema.test.js, tests/skopeo-hud-projector.test.js</files>
  <read_first>
    - tests/skopeo-hud-schema.test.js (create)
    - tests/skopeo-hud-projector.test.js (create)
    - .planning/milestones/v1.2.0-SKOPEO-phases/57-folder-reading-hud/57-CONTEXT.md
    - .planning/milestones/v1.2.0-SKOPEO-phases/57-folder-reading-hud/57-UI-SPEC.md
    - .planning/milestones/v1.2.0-SKOPEO-phases/57-folder-reading-hud/57-VALIDATION.md
    - extension/utils/skopeo-corpus-schema.js
    - extension/utils/skopeo-capability-projector.js
    - tests/skopeo-corpus-schema.test.js
    - tests/skopeo-capability-projection.test.js
  </read_first>
  <behavior>
    - "Exact folder, reading, and contract-closed shapes accept every approved enum/cap and reject missing, extra, accessor, symbol, sparse, custom-prototype, cyclic, URL/ID/authority, and oversized fields without executing hostile getters."
    - "The projector groups only certified vendorScopeFileId and exact truth source bindings, never a label, filename, row position, recency hint, or page string."
    - "All four material-date types and separate consequences survive projection; earliest accepted future civil date wins with tie order notice deadline, termination, expiration, renewal."
    - "Complete absence and incomplete authority remain distinct; memo requirement and notification delivery remain first-class neutral values."
    - "Every successful projection is a fresh recursively frozen value and is invariant to input record, relation, family, citation, and source order."
  </behavior>
  <action>
Create two Node `node:assert/strict` classic/CommonJS contract tests following the exact VM/global pattern in the analogs. The tests must pin the interfaces above and all UI-SPEC enum/copy-neutral semantic values without implementing production behavior.

In the schema oracle, cover ordinary and null prototypes, exact own enumerable data properties, getter non-execution, symbols, sparse arrays, duplicate opaque IDs, bounded control-free text, typed civil dates, every governing/source/index/trust/date/gap/downstream state, recursive freeze, exact caps and max-plus-one, explicit overflow arithmetic, and forbidden raw authority markers. Require caller inputs to remain unfrozen and unmodified.

In the projector oracle, build production-shaped complete/partial corpus, exact graph, truth display, vendor-label, evaluation-context, and authority fixtures. Cover the 20-case inventory from 57-RESEARCH.md where applicable at this pure layer, including two vendors plus root policy evidence, active/historical/partial/review-required families, unreadable and download-blocked sources, complete owner absence versus incomplete graph, proven missing final versus unknown execution, all date-order cases, over-32 truth, vendor-scope ambiguity, neutral downstream slots, hostile labels, and at least six input permutations.

Support exact controlled-RED environment switches `SKOPEO_HUD_EXPECT_SCHEMA_RED=1` and `SKOPEO_HUD_EXPECT_PROJECTOR_RED=1`: each switch succeeds only when its named production interface is absent, emits exactly one stable `skopeo hud schema contract: RED` or `skopeo hud projector contract: RED` line, and fails on parser/harness errors or an unexpectedly present implementation. Normal invocation must remain red until Task 2.
  </action>
  <acceptance_criteria>
    - Both test files syntax-check and load all analog/source fixtures without a network, Chrome, storage, DOM, clock, or locale dependency.
    - Each controlled RED command's stdout/stderr and exit code are captured independently; it succeeds only for its intended missing schema/projector interface and emits its own exact marker once.
    - The schema matrix includes every fixed UI cap, closed enum, neutral downstream slot, forbidden authority field, and hostile descriptor class.
    - The projector matrix distinguishes complete absence from incomplete authority and covers all four date meanings, separate consequence, root-level evidence, over-cap closure, and deterministic permutations.
    - `git diff --check -- tests/skopeo-hud-schema.test.js tests/skopeo-hud-projector.test.js` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>schema_red_output=$(SKOPEO_HUD_EXPECT_SCHEMA_RED=1 node tests/skopeo-hud-schema.test.js 2&gt;&amp;1); schema_red_status=$?; projector_red_output=$(SKOPEO_HUD_EXPECT_PROJECTOR_RED=1 node tests/skopeo-hud-projector.test.js 2&gt;&amp;1); projector_red_status=$?; test "$schema_red_status" -eq 0 &amp;&amp; test "$(printf '%s\n' "$schema_red_output" | grep -Fxc 'skopeo hud schema contract: RED')" -eq 1 &amp;&amp; test "$projector_red_status" -eq 0 &amp;&amp; test "$(printf '%s\n' "$projector_red_output" | grep -Fxc 'skopeo hud projector contract: RED')" -eq 1 &amp;&amp; printf '%s\n%s\n' "$schema_red_output" "$projector_red_output" &amp;&amp; node --check tests/skopeo-hud-schema.test.js &amp;&amp; node --check tests/skopeo-hud-projector.test.js</automated>
  </verify>
  <done>The Wave 0 tests fix the full closed projection contract, each controlled mode returns success only for its intended absent interface, and each independently captured output contains its unique RED marker exactly once.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement exact frozen projection schemas and the pure projector kernel</name>
  <files>extension/utils/skopeo-hud-schema.js, extension/utils/skopeo-hud-projector.js, tests/skopeo-hud-schema.test.js, tests/skopeo-hud-projector.test.js</files>
  <read_first>
    - extension/utils/skopeo-hud-schema.js (create)
    - extension/utils/skopeo-hud-projector.js (create)
    - tests/skopeo-hud-schema.test.js
    - tests/skopeo-hud-projector.test.js
    - extension/utils/skopeo-corpus-schema.js
    - extension/utils/skopeo-capability-projector.js
    - extension/utils/skopeo-graph-schema.js
    - extension/utils/skopeo-truth-schema.js
    - .planning/milestones/v1.2.0-SKOPEO-phases/57-folder-reading-hud/57-PATTERNS.md
    - .planning/milestones/v1.2.0-SKOPEO-phases/57-folder-reading-hud/57-UI-SPEC.md
  </read_first>
  <action>
Implement the two frozen classic-script/CommonJS modules from `<interfaces>` using two-space indentation, single quotes, exact-own-data parsing, null-prototype output records, dense arrays, finite integer/count validation, bounded control-free text, and clone-then-deep-freeze. Do not mutate or freeze caller-owned objects. This is the one semantic contract per D-01 and D-03; neither module may read storage, Chrome APIs, Drive, graph/truth facades, the DOM, a clock, locale, network, or global host state.

Fix closed enums for folder governing states (`governing`, `partially-governing`, `review-required`, `not-evaluated`), reading states (add `historical`, `superseded`, `access-unavailable`), date types (`notice-deadline`, `renewal`, `termination`, `expiration`), fact/trust/source/index states, the gap/result vocabulary from UI-SPEC, and downstream policy/memo/notification neutral states. Schema parsing must reject any caller-supplied heading, instruction, HTML, action label, URL, source/file/graph/truth/storage ID, certificate, resource key, provider data, callback, or executable value. Actions are opaque bounded IDs only.

Implement `createProjection(input)` as a pure switch over the background-derived exact mode. Validate the authority envelope and every input before use, group sources only by certified `vendorScopeFileId`, join truth only through exact source bindings, retain root-level sources as corpus-level evidence, and require a complete exact graph/truth/evaluation context before any definitive governing, date, conflict, owner-absence, or final-absence result. Per D-04, accept only the bounded truth-family display shape needed for folder/reading projection; no generic record or raw facade shape is admitted.

For over-cap/incomplete/stale/ambiguous input per D-05, return one `contract-closed` projection or explicitly partial manifest rows whose governing/date/absence fields are `not-evaluated`; never return a truth prefix. Preserve exact overflow counts only when total/count authority is certified. Generate no label or identity from a filename, row order, recency, similarity, visible text, URL, or color.
  </action>
  <acceptance_criteria>
    - Both production modules syntax-check, expose only the exact frozen interfaces, and have no Chrome/storage/DOM/network/clock/locale/package dependency.
    - `parseProjection` accepts all three exact modes at every exact cap, rejects every max-plus-one/hostile/authority-leak fixture as specified, and returns a fresh recursively frozen value.
    - `createProjection` validates before publication, is permutation-invariant, groups only stable certified identities, and emits no raw source/graph/truth/storage/provider authority.
    - Incomplete or over-cap truth never yields governing/date/absence conclusions; authorized partial manifest status is labelled partial with exact overflow.
    - `node tests/skopeo-hud-schema.test.js &amp;&amp; node tests/skopeo-hud-projector.test.js` exits 0 within the 30-second focused target.
  </acceptance_criteria>
  <verify>
    <automated>node --check extension/utils/skopeo-hud-schema.js &amp;&amp; node --check extension/utils/skopeo-hud-projector.js &amp;&amp; node tests/skopeo-hud-schema.test.js &amp;&amp; node tests/skopeo-hud-projector.test.js</automated>
  </verify>
  <done>The Phase 57 semantic schema and pure projection kernel are green, exact, bounded, immutable, and free of runtime or presentation authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Complete vendor ordering, typed-date selection, and evidence-only gap semantics</name>
  <files>extension/utils/skopeo-hud-projector.js, tests/skopeo-hud-projector.test.js</files>
  <read_first>
    - extension/utils/skopeo-hud-projector.js
    - tests/skopeo-hud-projector.test.js
    - extension/utils/skopeo-hud-schema.js
    - .planning/milestones/v1.2.0-SKOPEO-phases/57-folder-reading-hud/57-CONTEXT.md
    - .planning/milestones/v1.2.0-SKOPEO-phases/57-folder-reading-hud/57-RESEARCH.md
    - .planning/milestones/v1.2.0-SKOPEO-phases/57-folder-reading-hud/57-UI-SPEC.md
    - .planning/milestones/v1.2.0-SKOPEO-phases/57-folder-reading-hud/57-VALIDATION.md
  </read_first>
  <action>
Finish the projector's outcome semantics per D-07 and D-14 through D-17. Each vendor row must contain all eleven UI-SPEC slots: bounded vendor label/token, owner, document/index aggregate with distinct source states/counts, governing status, next typed material date, separate consequence, memo evidence, policy-document evidence, memo-requirement neutral state, notification-delivery neutral state, and bounded urgent gaps with overflow.

Select a next material date only from current accepted future civil dates relative to the supplied evaluation date. Sort civil date ascending, then `notice-deadline`, `termination`, `expiration`, `renewal`, then opaque vendor identity. Retain the date discriminator and separate consequence; never substitute renewal/termination/expiration for a notice deadline. If comparison authority is missing, emit all schema-bounded typed dates or `not-evaluated` exactly as the closed contract permits—never guess.

Map gaps using one closed evidence table per D-15/D-16: `missing-final` only from complete authoritative executed-final absence; `unreadable-scan` from current unreadable source; `incomplete-indexing` from a current source/graph blocker; `owner-gap` only from a complete owner relation set; `version-conflict` from current truth conflict/review state; `policy-document-missing` only from exact configured identity plus complete evidence. Memo evidence may be `on-file` when proven, but memo requirement remains `not-evaluated`; notification delivery remains `not-available` until its owning authority exists. Missing data is not automatically urgent; only an upstream authoritative priority enters the urgent summary.

Sort vendors exactly as UI-SPEC specifies: review-required or confirmed urgent gap first; then accepted future material date earliest first; then normalized bounded label; then opaque identity tie-breaker. Content must not need to resort. Emit summary caps 3/4 and vendor gap cap 3 with exact overflow; zero-result copy eligibility is a semantic complete/empty flag, never inferred from an empty partial array.
  </action>
  <acceptance_criteria>
    - Every vendor fixture contains all required slots; no row silently drops owner/index/governing/date/consequence/memo/policy/memo-requirement/notification/gap state.
    - The exact date-order and tie-order assertions pass for notice, termination, expiration, and renewal, with consequences separate and no notice substitution.
    - Complete owner/final/policy absence and incomplete/unknown input produce different closed results; memo obligation and notification remain neutral.
    - Vendor, date, and urgent-gap ordering is byte-identical across at least six input permutations, with exact summary/card overflow counts.
    - Focused schema/projector tests and `git diff --check` exit 0 within the 30-second target.
  </acceptance_criteria>
  <verify>
    <automated>node --check extension/utils/skopeo-hud-projector.js &amp;&amp; node tests/skopeo-hud-schema.test.js &amp;&amp; node tests/skopeo-hud-projector.test.js &amp;&amp; git diff --check -- extension/utils/skopeo-hud-projector.js tests/skopeo-hud-projector.test.js</automated>
  </verify>
  <done>The pure projector produces complete deterministic vendor, date, consequence, gap, and neutral downstream semantics for later trusted/background and content consumers.</done>
</task>

</tasks>

<verification>
- After every task commit, run that task's focused command and `node --check` for every changed JavaScript file; focused feedback must remain under 30 seconds.
- Wave 1 gate: `node tests/skopeo-hud-schema.test.js &amp;&amp; node tests/skopeo-hud-projector.test.js`.
- Regression gate: `node tests/skopeo-corpus-schema.test.js &amp;&amp; node tests/skopeo-capability-projection.test.js &amp;&amp; node tests/skopeo-truth-schema.test.js`.
- Static review: neither production module contains `chrome.`, `localStorage`, `sessionStorage`, `indexedDB`, `fetch(`, `Date.parse`, `new Date`, `innerHTML`, `eval(`, `Function(`, or a content/MCP/global facade export beyond its named module factory.
- Source audit: VIEW-01 vendor fields, VIEW-02 typed dates/consequence, VIEW-03 all first-class gap/downstream states, D-01/D-03/D-04/D-05/D-07/D-14..D-17, research caps/ordering, UI-SPEC copy semantics, and T-57-01/02/05/06/08 have executable coverage in this plan; no deferred Phase 58/59 control is present.
</verification>

<success_criteria>
- Closed schema and projector tests pass at exact maximum and reject maximum-plus-one, hostile descriptors, authority leakage, and silent truncation.
- Folder and reading projections are deterministic, recursively frozen, minimized, and entirely derived from injected current proof objects.
- No guessed identity, absence, governing status, date, consequence, obligation, urgency, or notification outcome can enter a valid projection.
- No package, lockfile, manifest, runtime, shell, or external dependency changes occur in this plan.
</success_criteria>

<source_coverage>
- GOAL: Defines the frozen bounded data contract and pure projector required for the Folder &amp; Reading HUD.
- REQ: Covers VIEW-01 vendor/folder projection, VIEW-02 typed dates and consequences, and VIEW-03 reading gaps/downstream states.
- RESEARCH: Applies the closed schema, caps, deterministic ordering, byte ceiling, non-inference rules, and background-only projection boundary.
- CONTEXT: Implements D-01, D-03, D-04, D-05, D-07, and D-14 through D-17; deferred Phase 58/59 controls remain excluded.
</source_coverage>

<output>
Create `.planning/milestones/v1.2.0-SKOPEO-phases/57-folder-reading-hud/57-01-SUMMARY.md` when done.
</output>
