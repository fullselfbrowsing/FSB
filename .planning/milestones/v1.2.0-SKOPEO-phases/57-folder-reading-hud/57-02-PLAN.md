---
phase: 57-folder-reading-hud
plan: "02"
type: execute
wave: 2
depends_on:
  - "57-01"
files_modified:
  - extension/utils/skopeo-truth-engine.js
  - extension/config/config.js
  - extension/background.js
  - tests/skopeo-truth-store.test.js
  - tests/skopeo-truth-runtime.test.js
  - tests/skopeo-hud-runtime.test.js
autonomous: true
requirements:
  - VIEW-01
  - VIEW-02
  - VIEW-03
  - VIEW-05
must_haves:
  truths:
    - "D-04: One private background truth display snapshot provides only the smallest bounded complete-family overview required by the HUD projector, validates every family/source/version/digest/evaluation-context binding, and creates no generic listing or storage bridge; D-05: a missing, corrupt, changed, stale, inaccessible, incomplete, tuple-mismatched, or over-cap member returns no prefix and withdraws stale influence before replacement."
    - "The current civil date is derived only from an injected clock under an explicitly configured, schema-valid IANA governing timezone; missing or stale timezone/calendar configuration yields a typed neutral blocker with no browser-locale or UTC fallback."
    - "An explicitly invoked current controller may call the private ensureCurrentHudTruthDisplaySnapshot(exactTuple, { evaluationContext }) helper to inspect current truth, deduplicate at most one recompute for the exact tuple plus evaluation-context digest, then reinspect; passive navigation, boot, hidden cadence, and unrelated renders cannot start recomputation."
    - "The truth facade remains background-private and bounded to 64 KiB; content, page scripts, MCP, UI, and global consumers receive neither the facade nor raw corpus/graph/truth/provider/storage authority."
  artifacts:
    - path: "extension/utils/skopeo-truth-engine.js"
      provides: "Eighth private facade method inspectDisplaySnapshot with complete-generation/currentness validation"
      contains: "inspectDisplaySnapshot"
    - path: "extension/config/config.js"
      provides: "Default-closed readable keys for explicit truth timezone binding and immutable calendars"
      contains: "skopeoTruthTimezoneBinding"
    - path: "extension/background.js"
      provides: "Private evaluation-context builder, named ensureCurrentHudTruthDisplaySnapshot orchestration helper, exact recompute dedupe, abort/currentness fencing, and closure-held truth facade"
      contains: "fsbSkopeoTruthEngineFacade"
    - path: "tests/skopeo-hud-runtime.test.js"
      provides: "Phase 57 truth-context/recompute/currentness integration harness consumed by later controller/content plans"
      contains: "skopeo hud runtime contract"
  key_links:
    - from: "extension/utils/skopeo-truth-engine.js"
      to: "extension/utils/skopeo-truth-store.js"
      via: "inspectMetadata → bounded readActiveFamily set → identical inspectMetadata recheck"
      pattern: "inspectMetadata|readActiveFamily"
    - from: "extension/background.js"
      to: "extension/utils/skopeo-truth-engine.js"
      via: "ensureCurrentHudTruthDisplaySnapshot(exactTuple, { evaluationContext }) calls inspectDisplaySnapshot with that exact signature, conditionally recomputes once, reinspects, and final-checks the same tuple and evaluation context"
      pattern: "ensureCurrentHudTruthDisplaySnapshot|inspectDisplaySnapshot|recompute"
    - from: "extension/background.js"
      to: "extension/config/config.js"
      via: "configured timezone/calendar keys are parsed into an explicit evaluation context"
      pattern: "skopeoTruthTimezoneBinding|skopeoTruthCalendars"
---

<objective>
Create the private complete truth-display snapshot and the trusted evaluation-context/recompute caller that make Phase 57 projections current without exposing or duplicating truth authority.

Purpose: Resolve the two production prerequisites identified by research—complete family enumeration and an explicit governing-date context—while preserving exact-set, revocation, provenance, MV3, and explicit-invocation boundaries.
Output: `inspectDisplaySnapshot`, default-closed configuration keys, an injected-clock evaluation builder, exact request deduplication/cancellation, and focused truth/HUD runtime tests.

This plan intentionally touches the shared truth engine and background boundary together: the new facade method is unusable without the exact context builder and explicit caller, while the caller cannot be safely specified without the facade's complete-generation/currentness contract. Tasks remain separated into RED contract, truth/store implementation, and background context/orchestration wiring.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.2.0-SKOPEO-PROJECT-SNAPSHOT.md
@.planning/milestones/v1.2.0-SKOPEO-ROADMAP.md
@.planning/milestones/v1.2.0-SKOPEO-REQUIREMENTS.md
@.planning/milestones/v1.2.0-SKOPEO-STATE-SNAPSHOT.md
@.planning/milestones/v1.2.0-SKOPEO-phases/57-folder-reading-hud/57-CONTEXT.md
@.planning/milestones/v1.2.0-SKOPEO-phases/57-folder-reading-hud/57-RESEARCH.md
@.planning/milestones/v1.2.0-SKOPEO-phases/57-folder-reading-hud/57-PATTERNS.md
@.planning/milestones/v1.2.0-SKOPEO-phases/57-folder-reading-hud/57-UI-SPEC.md
@.planning/milestones/v1.2.0-SKOPEO-phases/57-folder-reading-hud/57-VALIDATION.md
@.planning/milestones/v1.2.0-SKOPEO-phases/57-folder-reading-hud/57-01-SUMMARY.md
@extension/utils/skopeo-truth-store.js
@extension/utils/skopeo-truth-engine.js
@extension/utils/skopeo-truth-schema.js
@extension/config/config.js
@extension/background.js
@tests/skopeo-truth-store.test.js
@tests/skopeo-truth-runtime.test.js
@tests/skopeo-corpus-runtime.test.js

<interfaces>
The current truth store already exposes `inspectMetadata({ partitionKey })` and `readActiveFamily({ partitionKey, familyId })`. Use that smallest existing private surface per D-04: inspect metadata, reject more than 32 families, read every listed family, validate exact metadata/snapshot membership, then inspect metadata again and require canonical equality. Do not add a generic store listing, content bridge, callback, query language, or caller-selected fields.

Extend the returned frozen `FsbSkopeoTruthEngine.create(...)` facade from seven to exactly eight methods by adding `inspectDisplaySnapshot(exactTuple, { evaluationContext })`. It uses `requestContext(..., false)`, fresh visible/exact-graph authority, complete active-generation membership, identical authorized-set digest and evaluation context, source/record/relation/candidate/schema/prompt/adjudication/deadline/calendar versions, and final authority/context rechecks. It returns one recursively frozen 64 KiB-bounded background-only value with output generation, authorized-set/context digests, and sorted minimized family summaries needed by the Plan 01 projector; no family or byte prefix is valid.

Name the private background orchestration helper exactly `ensureCurrentHudTruthDisplaySnapshot(exactTuple, { evaluationContext })`. This helper is the only projection-controller entry to truth readiness: it must call `inspectDisplaySnapshot(exactTuple, { evaluationContext })`, conditionally share one exact-key recompute only for an admitted missing/stale result, call `inspectDisplaySnapshot(exactTuple, { evaluationContext })` again, then return only after final equality checks for the exact tuple, evaluation context/context digest, controller/entity, and exact source set. Keep both the facade and helper lexical to background scope.

Add config defaults `skopeoTruthTimezoneBinding: null` and `skopeoTruthCalendars: []`. A configured binding must parse as exact `{ kind: 'configured', timezone, configurationId, configurationVersion }`; calendars must each pass `FsbSkopeoTruthSchema.parseBusinessCalendar`. No settings UI is added.

Create a private background evaluation-context builder with injected `{ now, readSettings, truthSchema, IntlDateTimeFormat }`. It returns either exact parsed `{ asOfCivilDate, governingTimezoneBinding, calendars }` or closed blocker codes. It obtains year/month/day only through `Intl.DateTimeFormat(..., { timeZone, year: 'numeric', month: '2-digit', day: '2-digit' }).formatToParts(now())`; it never parses a locale string, calls `Date.parse`, chooses browser timezone, or defaults to UTC.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Durable truth generation → display snapshot | Metadata and families can change between awaits; completeness requires identical pointer/generation membership before and after all reads. |
| Trusted configuration/clock → evaluation context | Locale, browser timezone, malformed settings, stale calendars, or a caller date can silently change deadline meaning unless exact and revalidated. |
| Explicit controller request → provider-backed recompute | Duplicate/racing/passive work can waste provider calls or publish after teardown unless keyed, abortable, and currentness-fenced. |
| Truth facade → later background projection | Internal IDs/proofs are allowed only inside this private join; content/MCP/global exposure is forbidden. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-57-02 | Spoofing / Tampering | complete truth and evaluation-context admission | mitigate | Tasks 1-3 reparse context, require complete metadata/family/digest/version equality, perform before/after authority validation, and emit exact closed blockers. |
| T-57-03 | Spoofing | controller/context freshness | mitigate | Tasks 1 and 3 bind the dedupe key to tab/generation/origin/profile/context/entity plus context digest; abort and final currentness checks make stale results inert. |
| T-57-05 | Tampering / Denial of service | family and byte caps | mitigate | Tasks 1-3 reject the 33rd family and 64 KiB max-plus-one with no prefix, using sorted bounded reads and one shared in-flight promise. |
| T-57-06 | Information disclosure / Elevation of privilege | private facade and raw proof objects | mitigate | Tasks 1-3 keep the facade in background lexical state, return minimized frozen summaries, add global/content/MCP/static negative assertions, and omit raw provider/store capabilities. |
| T-57-08 | Elevation of privilege | recompute trigger and downstream ownership | mitigate | Tasks 1 and 3 expose an explicit-request helper only, with no passive cadence, UI settings flow, ask/policy/notification state, alarm, or delivery effect. |
| T-57-SC | Tampering | package supply chain | accept | No package install, dependency, lockfile, manifest permission, remote asset, or new runtime is introduced. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write controlled RED complete-snapshot and evaluation-context runtime contracts</name>
  <files>tests/skopeo-truth-store.test.js, tests/skopeo-truth-runtime.test.js, tests/skopeo-hud-runtime.test.js</files>
  <read_first>
    - tests/skopeo-truth-store.test.js
    - tests/skopeo-truth-runtime.test.js
    - tests/skopeo-hud-runtime.test.js (create)
    - extension/utils/skopeo-truth-store.js
    - extension/utils/skopeo-truth-engine.js
    - extension/utils/skopeo-truth-schema.js
    - extension/background.js
    - extension/config/config.js
    - tests/skopeo-corpus-runtime.test.js
    - .planning/milestones/v1.2.0-SKOPEO-phases/57-folder-reading-hud/57-VALIDATION.md
  </read_first>
  <behavior>
    - "Display inspection accepts only one complete active generation with <=32 current families and identical source/version/digest/evaluation bindings before and after all reads."
    - "Missing/corrupt/replaced members, 33 families, 64 KiB max-plus-one, access drift, graph drift, or evaluation-context drift returns no family prefix and withdraws stale influence where applicable."
    - "Configured IANA timezone plus injected clock yields exact YYYY-MM-DD; missing/malformed/stale timezone or calendars yields typed blockers and zero implicit browser/UTC/locale fallback."
    - "Concurrent exact requests share one recompute, different context digests do not, teardown aborts work, and passive boot/navigation causes zero inspection, Drive, graph, provider, or recompute calls."
    - "The truth facade is exact/frozen/private and no content, page, MCP, or global reference can obtain it."
  </behavior>
  <action>
Extend the existing truth store/runtime oracles and create `tests/skopeo-hud-runtime.test.js` using the marked background-controller extraction pattern. Pin the exact eighth facade method, complete-generation read choreography, 32-family and 64 KiB boundaries, source/record/relation/version/digest/context equality, before/after metadata equality, stale withdrawal, and no-prefix semantics.

Add production-shaped configuration and injected-clock fixtures for valid configured timezone binding, calendar sorting, civil-date rollover in multiple IANA zones, missing binding, invalid zone, malformed/stale calendars, and a clock/context change between initial and final currentness checks. Assert no `Date.parse`, locale-string parsing, browser timezone, implicit UTC, or content-supplied date can enter the builder.

Specify the exact private `ensureCurrentHudTruthDisplaySnapshot(exactTuple, { evaluationContext })` helper and a controller-local in-flight registry keyed by the full current tuple/entity plus evaluation-context digest. Pin its inspect → conditional recompute → reinspect order, both exact `inspectDisplaySnapshot(exactTuple, { evaluationContext })` calls, and its final same-tuple/same-context checks. Exercise two folder/reading consumers sharing one promise, a different digest starting separate work, controller replacement, abort, navigation, provider failure, exact-set blocker, and late completion. Recompute may occur only after an explicit helper call and only for absent/stale current generation when all source/context prerequisites are exact; every other blocker stays closed without provider work.

Add controlled RED switch `SKOPEO_HUD_EXPECT_TRUTH_RED=1`. It must exit 0 only when `inspectDisplaySnapshot` and the trusted background context/orchestration helpers are absent, emit exactly `skopeo hud truth runtime contract: RED`, and reject unrelated harness/parser failures. Normal new assertions remain red until Tasks 2-3.
  </action>
  <acceptance_criteria>
    - Existing truth-store/truth-runtime coverage remains intact while the new RED cases fail only at the intended Phase 57 seams.
    - Tests cover exact active generation, 32/33 family and 64 KiB boundaries, corruption/replacement/drift, and zero-prefix output.
    - Tests cover IANA civil-date rollover, config/currentness failure, zero implicit timezone/UTC/locale path, exact dedupe, abort, and zero passive calls.
    - Boundary probes assert the facade is absent from globalThis, content messages, MCP dispatch, and serialized responses.
    - The controlled RED command's stdout/stderr and exit code are captured independently; it returns success only for the intended absent truth/runtime interfaces, emits the exact marker once, and all three test files syntax-check.
  </acceptance_criteria>
  <verify>
    <automated>truth_red_output=$(SKOPEO_HUD_EXPECT_TRUTH_RED=1 node tests/skopeo-hud-runtime.test.js 2&gt;&amp;1); truth_red_status=$?; test "$truth_red_status" -eq 0 &amp;&amp; test "$(printf '%s\n' "$truth_red_output" | grep -Fxc 'skopeo hud truth runtime contract: RED')" -eq 1 &amp;&amp; printf '%s\n' "$truth_red_output" &amp;&amp; node --check tests/skopeo-hud-runtime.test.js &amp;&amp; node --check tests/skopeo-truth-runtime.test.js &amp;&amp; node --check tests/skopeo-truth-store.test.js</automated>
  </verify>
  <done>The RED contract fixes complete truth-display inspection, explicit evaluation context, deduplicated explicit recomputation, cancellation, and private-facade invariants; controlled mode succeeds only for the intended absent interfaces and its independently captured output contains the unique marker exactly once.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the private complete truth display snapshot</name>
  <files>extension/utils/skopeo-truth-engine.js, tests/skopeo-truth-store.test.js, tests/skopeo-truth-runtime.test.js</files>
  <read_first>
    - extension/utils/skopeo-truth-engine.js
    - tests/skopeo-truth-runtime.test.js
    - tests/skopeo-truth-store.test.js
    - extension/utils/skopeo-truth-store.js
    - extension/utils/skopeo-truth-schema.js
    - extension/utils/skopeo-graph-engine.js
    - .planning/milestones/v1.2.0-SKOPEO-phases/56-governing-lineage-evidence-deadline-engine/56-04-SUMMARY.md
    - .planning/milestones/v1.2.0-SKOPEO-phases/56-governing-lineage-evidence-deadline-engine/56-05-SUMMARY.md
    - .planning/milestones/v1.2.0-SKOPEO-phases/57-folder-reading-hud/57-PATTERNS.md
  </read_first>
  <action>
Add `inspectDisplaySnapshot` to the frozen truth facade per D-01/D-04, validating `truthStore.inspectMetadata` in `create(...)` and using the existing store interface rather than creating a generic listing method. Call `requestContext(request, false)`, derive fresh visible/exact graph authority, validate evaluation context, read active metadata, and return `fact-missing` when no current generation exists.

Reject metadata with more than 32 families before any family read. For each sorted metadata member, call `readActiveFamily`, require its exact family/snapshot/output-generation/authorized-set/source-binding/evaluation-context and all schema/rule/provider/record/relation/candidate versions to match the current metadata/graph/context, and build only the fields the Plan 01 projector needs. Re-read `inspectMetadata`, visible sources, exact graph snapshot, and authoritative evaluation context after all awaits; require canonical equality. On stale source/graph/context/proof, use the existing invalidation path before returning `snapshot-stale` or the exact evaluation blocker per D-05.

Run the result through the existing safe clone and `MAX_MINIMIZED_RESULT_BYTES` boundary. If a family is missing/corrupt, any equality fails, or serialized bytes exceed 64 KiB, return one closed blocker with no prior family array. Keep accepted historical facts/citations distinct from governing assertions and carry explicit blocker/conflict/access/trust states; do not perform presentation sorting/copy, policy adjudication, or navigation here.

Update truth store tests only to prove the existing `inspectMetadata` plus `readActiveFamily` composition: corrupt or changed generation members cannot be read as a complete set, before/after metadata detects replacement, and no store API expansion occurred. Update the exact truth-facade surface assertion from seven to eight methods and prove the instantiated facade remains lexical/background-only.
  </action>
  <acceptance_criteria>
    - The truth facade exposes exactly eight frozen methods and the store public surface remains unchanged.
    - `inspectDisplaySnapshot` returns complete sorted current families only when metadata, every proof, exact graph/source set, all versions, and evaluation context agree before and after reads.
    - Missing/corrupt/replaced/33-family/64 KiB-plus-one inputs return a single blocker and no usable family prefix.
    - Stale influence is withdrawn before a stale result returns, and historical versus governing evidence remains separate.
    - Truth store/runtime tests and production syntax check pass within the focused 30-second target.
  </acceptance_criteria>
  <verify>
    <automated>node --check extension/utils/skopeo-truth-engine.js &amp;&amp; node tests/skopeo-truth-store.test.js &amp;&amp; node tests/skopeo-truth-runtime.test.js</automated>
  </verify>
  <done>The private truth facade can enumerate one complete current display snapshot without widening the truth store or exposing a partial/raw family listing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build exact evaluation context and deduplicated explicit recomputation</name>
  <files>extension/config/config.js, extension/background.js, tests/skopeo-hud-runtime.test.js, tests/skopeo-truth-runtime.test.js</files>
  <read_first>
    - extension/config/config.js
    - extension/background.js
    - tests/skopeo-hud-runtime.test.js
    - tests/skopeo-truth-runtime.test.js
    - extension/utils/skopeo-truth-schema.js
    - extension/utils/skopeo-truth-engine.js
    - extension/utils/skopeo-corpus-controller.js
    - tests/skopeo-corpus-runtime.test.js
    - .planning/milestones/v1.2.0-SKOPEO-phases/57-folder-reading-hud/57-RESEARCH.md
    - .planning/milestones/v1.2.0-SKOPEO-phases/57-folder-reading-hud/57-VALIDATION.md
  </read_first>
  <action>
Add the two default-closed config keys from `<interfaces>` so `Config.loadFromStorage()` can read them. Do not add settings UI, migrations that invent values, UTC/browser defaults, or another storage owner. Parse every read afresh at consequential use; cached config does not authorize a stale context.

Lift the created truth-engine facade from its current function-local variable into a background module-lexical `fsbSkopeoTruthEngineFacade` that is assigned only after trusted recovery and reset to null on closed boot. Never place it on `globalThis`, the corpus boundary state, controller exports, content messaging, MCP, diagnostics, or test serialization. Preserve existing graph/corpus/truth boot and recovery order.

Implement the injected evaluation-context builder exactly as `<interfaces>` describes. Validate the configured timezone binding first, call the injected clock once, derive civil components with `formatToParts` in that exact IANA timezone, sort and parse immutable calendars, then reparse the full context with the truth schema. Missing/malformed/stale input returns `timezone-missing`, `business-calendar-missing`, `evaluation-context-mismatch`, or `evaluation-context-stale` as applicable and starts no truth/provider work.

Inside the existing Skopeo controller closure, add one private in-flight Map and the exact helper `ensureCurrentHudTruthDisplaySnapshot(exactTuple, { evaluationContext })`. Its key canonicalizes current sender-derived tab/generation/exact-origin/profile/profile-version/context-epoch/semantic-entity plus the context digest. The helper first calls `fsbSkopeoTruthEngineFacade.inspectDisplaySnapshot(exactTuple, { evaluationContext })`; only `fact-missing` or a freshly withdrawn `snapshot-stale` result may trigger one shared `recompute`, and only after current source/exact-set/context prerequisites are admitted. It must then call `fsbSkopeoTruthEngineFacade.inspectDisplaySnapshot(exactTuple, { evaluationContext })` again and final-check the same exact tuple, evaluation context/context digest, controller/entity, and exact source set before return. Source unreadable/inaccessible, incomplete/over-cap, timezone/calendar, provider, access, or currentness blockers return typed closed data without retry loops. Delete the entry in `finally`; abort it on controller teardown/replacement. No call occurs on worker boot, passive navigation, hidden timer, route classification, hover, render, or polling.

Keep this helper private for Plan 03's exact projection message; do not expose a content action yet. Per D-02, every eventual display use must still pass through `runSkopeoCorpusOperation('display', ...)`; this task supplies truth readiness only and does not bypass corpus operation authority.
  </action>
  <acceptance_criteria>
    - Config loads exact nullable timezone binding and empty calendars without adding UI/default authority or changing existing settings.
    - Valid injected-clock/config fixtures yield the expected civil date in at least three IANA zones and around midnight; missing/malformed/stale input yields exact blockers and no truth/provider call.
    - Two identical explicit requests through `ensureCurrentHudTruthDisplaySnapshot(exactTuple, { evaluationContext })` share one recompute; differing tuple/entity/context digest does not; both inspections use the exact facade signature; final tuple/context drift closes the result; teardown aborts and late results remain inert; passive lifecycle produces zero calls.
    - The truth facade remains lexical/private and existing boot/recovery/currentness tests stay green.
    - Focused HUD/truth tests, truth aggregate, storage-boundary verifier, and JavaScript syntax checks exit 0.
  </acceptance_criteria>
  <verify>
    <automated>node --check extension/config/config.js &amp;&amp; node --check extension/background.js &amp;&amp; node tests/skopeo-truth-store.test.js &amp;&amp; node tests/skopeo-truth-runtime.test.js &amp;&amp; node tests/skopeo-hud-runtime.test.js &amp;&amp; npm run test:skopeo-truth-evals &amp;&amp; node scripts/verify-skopeo-storage-boundary.mjs</automated>
  </verify>
  <done>An explicit current controller can obtain or recompute one complete current truth display snapshot under an exact configured civil-date context, with no passive work or authority escape.</done>
</task>

</tasks>

<verification>
- After every task commit, run the directly owned test command plus `node --check` for each changed JavaScript file; focused feedback target is under 30 seconds.
- Wave 2 gate: `node tests/skopeo-truth-store.test.js &amp;&amp; node tests/skopeo-truth-runtime.test.js &amp;&amp; node tests/skopeo-hud-runtime.test.js &amp;&amp; npm run test:skopeo-truth-evals`.
- Security regression: `node scripts/verify-skopeo-storage-boundary.mjs &amp;&amp; node tests/skopeo-corpus-runtime.test.js`.
- Static source assertions require zero `globalThis.fsbSkopeoTruthEngineFacade`, zero new content/MCP action, zero `Date.parse`/locale-string parse/implicit timezone fallback, zero passive recompute trigger, and no package/manifest/lockfile change.
- Preserve Phase 56 evidence status: deterministic structural/security may pass, provisional regression remains `pass (not gold)`, and domain/live citation/authorized Chrome evidence remains `human_needed`.
</verification>

<success_criteria>
- Complete truth display inspection is current, bounded, frozen, all-or-nothing, and private.
- Exact configured timezone/calendar authority—not browser or content state—determines the evaluation civil date.
- Recompute runs only after explicit current invocation, deduplicates by the complete tuple/context key, aborts on teardown, and cannot repaint or escape after drift.
- No store listing bridge, second runtime, detached UI, ask/policy/notification behavior, external dependency, or raw authority surface is introduced.
</success_criteria>

<source_coverage>
- GOAL: Supplies the private complete truth snapshot and exact evaluation context needed by the Folder &amp; Reading HUD.
- REQ: Covers VIEW-01 through VIEW-03 truth-backed display semantics and VIEW-05 currentness/least privilege.
- RESEARCH: Applies complete-family reads, before/after metadata equality, 32-family and 64 KiB closure, configured civil-time context, and controller-local deduplication.
- CONTEXT: Implements D-01, D-02, D-04, D-05, D-10, D-11, and D-14 through D-17; deferred Phase 58/59 controls remain excluded.
</source_coverage>

<output>
Create `.planning/milestones/v1.2.0-SKOPEO-phases/57-folder-reading-hud/57-02-SUMMARY.md` when done.
</output>
