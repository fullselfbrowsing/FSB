---
phase: 56-governing-lineage-evidence-deadline-engine
plan: "05"
type: execute
wave: 4
depends_on:
  - "56-02"
  - "56-03"
  - "56-04"
files_modified:
  - tests/skopeo-truth-runtime.test.js
  - extension/utils/skopeo-truth-engine.js
  - extension/background.js
  - scripts/verify-skopeo-storage-boundary.mjs
  - tests/skopeo-graph-runtime.test.js
  - tests/skopeo-corpus-runtime.test.js
  - tests/lattice-provider-bridge-smoke.test.js
  - tests/fixtures/skopeo-truth-evals/manifest.json
  - tests/fixtures/skopeo-truth-evals/cases.json
  - tests/skopeo-truth-evals.test.js
  - package.json
autonomous: true
requirements:
  - TRUTH-02
  - TRUTH-03
  - TRUTH-04
  - TRUTH-06
  - TRUTH-07
  - TRUTH-08
  - TRUTH-09
  - TRUTH-11
must_haves:
  truths:
    - "D-01/D-02/D-03/D-04: Runtime recomputation uses the complete current exact set and source-local candidate extraction, then the pure four-axis/overlay adjudicator; no label parsing, filename/recency/similarity/order tie-break, second model pass, or generic query language is introduced."
    - "D-05/D-06/D-07/D-08: Facade outputs retain nine separate typed facts, exact version/range citations, separate access/currentness and trust, and complete conflict members, while returning no raw source/provider/graph/store data."
    - "D-09/D-10/D-11/D-12: Runtime publishes only deterministic civil-date proofs and eligible/ineligible data with exact blockers; `counts`/`alerts`, chrome.alarms, notifications, recipients, schedules, delivery states, and alert ledgers remain absent."
    - "D-13: Truth recomputation consumes only the fresh frozen Phase 54-authorized exact-set graph facade plus current background manifest authority; search results, graph storage, and partial/max-prefix projections never become complete truth input."
    - "D-14: Background constructs the immutable truth store before participant registration and preserves complete pointer-last family publication, then orders corpus recovery → graph recovery → graph facade → truth recovery → truth facade."
    - "D-15: Background registers graph invalidation before recovery and binds `citations` to the real truth owner while `counts`/`alerts` remain exact empty owners, so source or graph change withdraws affected truth before newer publication."
    - "D-16: One separate private truth facade boots only after graph recovery and exposes bounded frozen lineage/fact/conflict/citation/deadline/status projections; content, MCP, UI, and raw graph/truth storage never receive authority."
    - "Every runtime call supplies a closed evaluation context explicitly; the trusted seam validates cited/configured timezone and immutable calendar currentness before adjudication and again before publish/read, with no `now`, locale, or UTC default."
    - "Automated fixtures report deterministic structural/security and provisional regression independently from `domain_fidelity: human_needed`; no synthetic/redacted case, configured-provider run, or automated judge fabricates expert legal approval."
  artifacts:
    - path: "extension/utils/skopeo-truth-engine.js"
      provides: "Fresh-authority exact-set recompute/read orchestration and one minimized frozen background facade"
      contains: "FsbSkopeoTruthEngine"
    - path: "extension/background.js"
      provides: "Exact six-module truth import, construction, invalidator/participant registration, recovery, and private-facade order"
      contains: "fsbSkopeoTruthEngineFacade"
    - path: "scripts/verify-skopeo-storage-boundary.mjs"
      provides: "Truth-module closure, date/runtime prohibition, participant/boot order, and private-surface static gate"
      contains: "TRUTH_MODULE_PATHS"
    - path: "tests/skopeo-truth-runtime.test.js"
      provides: "Controlled RED/GREEN production boot, authority, stale withdrawal, facade, privacy, and phase-fence oracle"
      contains: "skopeo truth runtime contract"
    - path: "tests/skopeo-truth-evals.test.js"
      provides: "24-case deterministic/security and provisional evaluation with honest human domain status"
      contains: "domain_fidelity"
    - path: "package.json"
      provides: "One truth aggregate placed once after the existing graph aggregate in normal test execution"
      contains: "test:skopeo-truth-evals"
  key_links:
    - from: "extension/background.js"
      to: "extension/utils/skopeo-truth-store.js"
      via: "real citations binder, graph invalidator registration, and truth recovery occur inside the marked trusted boundary"
      pattern: "registerTruthInvalidator|getPurgeParticipant\\(['\"]citations['\"]\\)|truthStore\\.recover"
    - from: "extension/utils/skopeo-graph-engine.js"
      to: "extension/utils/skopeo-truth-engine.js"
      via: "only the frozen snapshotExactSet facade method supplies complete graph input"
      pattern: "snapshotExactSet"
    - from: "extension/utils/skopeo-truth-extractor.js"
      to: "extension/utils/skopeo-truth-engine.js"
      via: "fresh Phase 54 source-local provider-no-storage steps yield normalized candidate generations"
      pattern: "prepareSource|nextBatch|finalize|discard"
    - from: "extension/utils/skopeo-lineage-adjudicator.js"
      to: "extension/utils/skopeo-truth-engine.js"
      via: "pure family proofs are rechecked and persisted under a fresh complete-set operation"
      pattern: "adjudicateExactSet"
    - from: "extension/utils/skopeo-truth-store.js"
      to: "extension/utils/skopeo-truth-engine.js"
      via: "opaque guarded pointer-last publication and fresh active-snapshot reads"
      pattern: "beginFamilyReplacement|publishFamilySnapshot|readActiveFamily"
    - from: "package.json"
      to: "tests/skopeo-truth-evals.test.js"
      via: "stable network-free aggregate after the existing graph aggregate"
      pattern: "test:skopeo-truth-evals"
---

<objective>
Integrate the exact-set, extraction, deterministic adjudication, deadline, and persistence contracts into one trusted background-only runtime and close Phase 56 with static, runtime, fixture, and package gates.

Purpose: Make governing truth safely usable by later phases through minimized fresh-authority projections while preserving source withdrawal, MV3 recovery, privacy, and honest human evidence boundaries.
Output: One private truth facade, locked boot/purge/recovery order, strengthened static verifier, 24-case deterministic corpus, and once-only package wiring.

Atomic integration/evaluation closure rationale: this final plan intentionally owns 11 files because the runtime factory, background import/boot wiring, literal-boundary verifier, existing runtime/import-count regressions, production evaluation aggregate, fixtures, and once-only package entry are one inseparable release gate. Moving only the package/evaluation files earlier would reference a runtime that does not yet exist; moving background/verifier/runtime files later would require a sixth plan or overlapping concurrent edits to the same authority boundary. The three tasks remain strictly separated as RED contract, runtime/background/static implementation, and fixture/package evaluation, and execute sequentially within this one final owner.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.2.0-SKOPEO-PROJECT-SNAPSHOT.md
@.planning/milestones/v1.2.0-SKOPEO-ROADMAP.md
@.planning/milestones/v1.2.0-SKOPEO-REQUIREMENTS.md
@.planning/milestones/v1.2.0-SKOPEO-STATE-SNAPSHOT.md
@.planning/milestones/v1.2.0-SKOPEO-phases/56-governing-lineage-evidence-deadline-engine/56-CONTEXT.md
@.planning/milestones/v1.2.0-SKOPEO-phases/56-governing-lineage-evidence-deadline-engine/56-RESEARCH.md
@.planning/milestones/v1.2.0-SKOPEO-phases/56-governing-lineage-evidence-deadline-engine/56-PATTERNS.md
@.planning/milestones/v1.2.0-SKOPEO-phases/56-governing-lineage-evidence-deadline-engine/56-AI-SPEC.md
@.planning/milestones/v1.2.0-SKOPEO-phases/56-governing-lineage-evidence-deadline-engine/56-VALIDATION.md
@.planning/milestones/v1.2.0-SKOPEO-phases/55-chrome-local-graph-incremental-truth-foundation/55-05-PLAN.md
@.planning/milestones/v1.2.0-SKOPEO-phases/55-chrome-local-graph-incremental-truth-foundation/55-05-SUMMARY.md
@.planning/milestones/v1.2.0-SKOPEO-phases/55-chrome-local-graph-incremental-truth-foundation/55-VERIFICATION.md
@.planning/milestones/v1.2.0-SKOPEO-phases/55-chrome-local-graph-incremental-truth-foundation/55-HUMAN-UAT.md
@extension/background.js
@scripts/verify-skopeo-storage-boundary.mjs
@tests/skopeo-graph-runtime.test.js
@tests/skopeo-corpus-runtime.test.js
@tests/skopeo-graph-evals.test.js
@tests/fixtures/skopeo-graph-evals/manifest.json
@tests/fixtures/skopeo-graph-evals/cases.json
@package.json

<interfaces>
Create `globalThis.FsbSkopeoTruthEngine` with exact `VERSION: 'skopeo-truth-engine/1'` and `create({ truthSchema, truthStore, truthExtractor, lineageAdjudicator, deadlineEngine, graphFacade, corpusTransport, runCorpusOperation, readVisibleSourceSet, validateEvaluationContext, readSettings, providerFactory, byteLength })`.

The returned frozen background facade exposes exactly `recompute`, `inspectLineage`, `inspectFacts`, `inspectConflicts`, `inspectCitations`, `inspectDeadline`, and `inspectStatus`. Each method accepts an exact live Skopeo tuple and a closed family/status request that must explicitly carry a Plan 01 `evaluationContext`; it derives/rechecks the current visible source set internally and returns a new recursively frozen projection capped at 64 KiB. Missing context returns `evaluation-context-missing`; stale authoritative bindings return `evaluation-context-stale`; a seam response/digest or persisted-context mismatch returns `evaluation-context-mismatch`. No method accepts a graph object, source text, storage key, arbitrary expression/query, callback, URL, filename, scheduling instruction, clock-derived date, locale, or default timezone.

`validateEvaluationContext({ exactTuple, evaluationContext, graphSnapshot, signal })` is the only authoritative context-currentness seam. The engine first reparses the caller-supplied context with `truthSchema.parseEvaluationContext`, locally computes `contextDigest` as native SHA-256 over the truth-schema canonical context tuple, then requires this seam to return exact frozen `{ ok: true, contextDigest }` with the same 64-hex digest. The seam verifies cited timezone citations against the exact current graph snapshot, or the configured timezone ID/version against the current trusted configuration, and verifies every supplied calendar ID/version/weekend/holiday payload against the current immutable calendar registry. It may return exact `{ ok: false, blockerCodes }` only from the closed context blocker set and may never supply, repair, convert, or default the civil date, timezone, or calendar. The engine calls it before adjudication and again immediately before every publication or read projection.

Import exactly six truth modules once after the five graph modules in this order: `skopeo-truth-schema.js`, `skopeo-truth-extractor.js`, `skopeo-lineage-adjudicator.js`, `skopeo-deadline-engine.js`, `skopeo-truth-store.js`, `skopeo-truth-engine.js`.

The trusted boot order is exact: validate dependencies → construct corpus store and graph store/query/extractor → construct truth store/extractor/adjudicator dependencies → register truth invalidator → register exactly seven purge participants (four graph, real truth `citations`, empty `counts`, empty `alerts`) → corpus recover → graph recover → graph facade → truth recover → truth facade → ready state. Neither recovery hydrates a graph query or fetches Drive/provider content.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Background visible manifest → exact recompute set | A stale, duplicate, partial, non-ready, or over-32 source set can silently omit operative paper unless reread and re-certified before publication/read. |
| Graph/extractor/pure modules → durable publication | Individually valid results can become stale between awaits; no module output may publish without final exact tuple, set, generation, rule, and provider binding checks. |
| Durable truth → background facade | A raw page, stale snapshot, mutable object, oversized projection, or internal capability can escape unless every read reauthorizes and minimizes. |
| Boot/participant order → revocation safety | Empty citations ownership, late invalidator registration, or truth facade creation before recovery can leave stale conclusions reachable after source change/restart. |
| Truth runtime → content/MCP/later phases | A generic message/query, raw graph/store/provider access, HUD/ask/policy implementation, or scheduling effect would expand authority beyond Phase 56. |
| Synthetic evaluation → domain claim | Passing deterministic fixtures can be mislabeled as expert-approved governing/legal fidelity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T56-01 | Tampering / Elevation of privilege | runtime extraction admission | mitigate in Tasks 1-3 | Production extractor/schema, configured provider, issued handles, fresh operations, no second pass, forbidden-field fixture matrix. |
| T56-02 | Spoofing | runtime lineage input | mitigate in Tasks 1-3 | Complete exact-set facade only, pure adjudicator, label/order/recency permutations, and no direct graph store/search authority. |
| T56-03 | Spoofing / Tampering | citation projection/currentness | mitigate in Tasks 1-3 | Fresh graph/source binding comparison on recompute/read, local citation IDs, stale withdrawal before return, exact minimized metadata. |
| T56-04 | Tampering | runtime deadline result | mitigate in Tasks 1-3 | Inject only pure closed engine, exact context/calendars, blockers, no Date/model arithmetic/scheduling. |
| T56-05 | Spoofing / Denial of service | exact source set and caps | mitigate in Tasks 1-3 | Background manifest discovery, unique sorted <=32 set, fresh before/after certification, whole-result caps, no prefix publication. |
| T56-06 | Tampering | clearance/conflicts | mitigate in Tasks 1-3 | Preserve pure proof/conflicts, never publish eligible with blockers, fixture assertions for unresolved input. |
| T56-07 | Tampering / Repudiation | boot/purge/recovery/invalidation | mitigate in Tasks 1-3 | Exact construction/registration/recovery order, real citations owner, graph invalidator, pointer-last store, stale-read withdrawal. |
| T56-08 | Information disclosure / Elevation of privilege | facade/static boundary | mitigate in Tasks 1-3 | Frozen minimized surface, literal closure verifier, forbidden-marker probes, no content/MCP/UI/policy/alarm/notification/recipient/ledger path. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the controlled RED trusted truth-runtime contract</name>
  <files>tests/skopeo-truth-runtime.test.js, tests/skopeo-graph-runtime.test.js, tests/skopeo-corpus-runtime.test.js</files>
  <read_first>
    - tests/skopeo-truth-runtime.test.js
    - extension/utils/skopeo-truth-engine.js
    - tests/skopeo-graph-runtime.test.js
    - tests/skopeo-corpus-runtime.test.js
    - extension/background.js
    - extension/utils/skopeo-truth-schema.js
    - extension/utils/skopeo-truth-extractor.js
    - extension/utils/skopeo-lineage-adjudicator.js
    - extension/utils/skopeo-deadline-engine.js
    - extension/utils/skopeo-truth-store.js
    - extension/utils/skopeo-graph-engine.js
    - scripts/verify-skopeo-storage-boundary.mjs
    - .planning/milestones/v1.2.0-SKOPEO-phases/56-governing-lineage-evidence-deadline-engine/56-VALIDATION.md
  </read_first>
  <behavior>
    - "Background imports six truth modules exactly once/in order and remains closed unless dependencies, invalidator registration, seven participant bindings, corpus/graph/truth recovery, and both facades succeed in exact order."
    - "The `citations` participant is the real truth binder, while only `counts` and `alerts` use the exact empty binder; graph still owns fragments/indexes/relationships/result-cache."
    - "Recompute derives one sorted current visible set capped at 32, obtains one complete graph snapshot, extracts one source at a time under fresh operations, adjudicates locally, rechecks set/digest/currentness, and publishes pointer-last."
    - "Every facade request supplies an explicit schema-parsed as-of civil date, cited/configured governing-timezone binding, and immutable calendar IDs/versions/data; the authoritative context seam validates and revalidates the exact digest, and no clock/locale/default UTC path can create or repair it."
    - "Every read repeats fresh authority and exact input-digest comparison; mismatch synchronously withdraws stale truth before returning a closed/review-required result."
    - "The facade is frozen/minimized and exposes no source text, graph/store/query/cache, candidates/prompts/provider output, storage keys/guards, filename/URL, mutable object, generic query, content/MCP route, or scheduling capability."
  </behavior>
  <action>
Create `tests/skopeo-truth-runtime.test.js` using production truth modules plus the established fake-Chrome/VM background extraction pattern from graph/corpus runtime tests. Fix the exact engine factory dependency set/API, source-set discovery, fresh operation traces, provider-no-storage choreography, graph snapshot/currentness digest, pure adjudication invocation, truth mutation order, restart, and seven facade projections.

Extend graph/corpus runtime tests only where needed to prove one-time truth invalidator registration, exact real/empty participant ownership, and unchanged Phase 54/55 behavior. Assert the exact six import order, no truth module in manifest/`CONTENT_SCRIPT_FILES`/`SKOPEO_INJECTION_FILES`, construction before participant registration, invalidator before mutations/recovery, corpus → graph → graph facade → truth → truth facade recovery order, zero boot hydration/source/provider calls, and closed boot on each missing/rejected dependency.

Run adversarial operation traces for manifest/set drift; source state/fingerprint/fragment/overlay drift; provider/model change; cancellation during source read/provider/adjudication/deadline/stage/publish; missing evaluation context; cited-timezone citation drift; configured-timezone ID/version drift; calendar ID/version/data drift; civil-date/context-digest mismatch; context currentness changing between initial validation and commit/read; source purge; graph mutation; MV3 recreation; corrupt truth recovery; projection max-plus-one; and stale first read. Require every missing/stale/mismatched context to return its closed context blocker and publish nothing. Require old active pointer withdrawal before any changed-source provider call/recompute and before any stale read can return.

Probe all facade results for recursive freezing and exact minimized fields. Seed unique markers in raw source, filenames, URLs, graph labels, facts, dates, addresses, consequences, locators, prompts, responses, errors, storage keys, guards, and cross-partition sources; allow typed fact/date/address/consequence values only in their expressly authorized `inspectFacts`/`inspectDeadline` projection locations and require every raw/internal marker absent. Require no generic runtime action/MCP tool/content injection/HUD/ask/policy/alarm/notification/recipient/ledger effect.

Confirm controlled RED independently: the truth-runtime test must fail nonzero with exact marker `skopeo truth runtime contract`, the graph-runtime test with `skopeo graph runtime truth integration contract`, and the corpus-runtime test with `skopeo corpus runtime truth integration contract`. A pass, wrong failure, or missing marker in any test fails the gate even when another test fails as expected. Do not stub the engine/background integration.
  </action>
  <acceptance_criteria>
    - Runtime fixtures load all production dependencies and reach intended missing-engine/import/boot assertions without breaking existing graph/corpus harnesses.
    - RED traces cover successful recompute/read/restart plus every authority/provider/graph/rule/storage/evaluation-context drift and cancellation boundary in the action.
    - Exact assertions require four graph owners, one real citations owner, two empty owners, invalidator registration, and the complete recovery/facade order.
    - Boundary probes test all seven facade methods and every forbidden content/MCP/UI/ask/policy/scheduling/raw-internal path.
    - The controlled RED command exits 0 only after each of the three tests independently exits nonzero and emits its own exact stable missing-contract marker.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -e; check_red() { label="$1"; marker="$2"; file="$3"; set +e; out=$(node "$file" 2&gt;&amp;1); code=$?; set -e; test "$code" -ne 0 || { printf "%s unexpectedly passed\n" "$label" &gt;&amp;2; exit 1; }; printf "%s\n" "$out" | rg -F -q "$marker" || { printf "%s wrong RED failure\n%s\n" "$label" "$out" &gt;&amp;2; exit 1; }; }; check_red truth-runtime "skopeo truth runtime contract" tests/skopeo-truth-runtime.test.js; check_red graph-runtime "skopeo graph runtime truth integration contract" tests/skopeo-graph-runtime.test.js; check_red corpus-runtime "skopeo corpus runtime truth integration contract" tests/skopeo-corpus-runtime.test.js'</automated>
  </verify>
  <done>The RED oracle fixes exact trusted boot, participant/recovery order, fresh recompute/read choreography, stale withdrawal, minimized projections, and phase-boundary closure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the truth engine, trusted background boot, and static private-boundary gate</name>
  <files>extension/utils/skopeo-truth-engine.js, tests/skopeo-truth-runtime.test.js, extension/background.js, scripts/verify-skopeo-storage-boundary.mjs, tests/skopeo-graph-runtime.test.js, tests/skopeo-corpus-runtime.test.js, tests/lattice-provider-bridge-smoke.test.js</files>
  <read_first>
    - extension/utils/skopeo-truth-engine.js
    - tests/skopeo-truth-runtime.test.js
    - extension/background.js
    - scripts/verify-skopeo-storage-boundary.mjs
    - tests/skopeo-graph-runtime.test.js
    - tests/skopeo-corpus-runtime.test.js
    - tests/lattice-provider-bridge-smoke.test.js
    - extension/utils/skopeo-graph-engine.js
    - extension/utils/skopeo-truth-store.js
    - extension/utils/skopeo-truth-extractor.js
    - extension/utils/skopeo-lineage-adjudicator.js
    - extension/utils/skopeo-deadline-engine.js
    - .planning/milestones/v1.2.0-SKOPEO-phases/56-governing-lineage-evidence-deadline-engine/56-PATTERNS.md
    - .planning/milestones/v1.2.0-SKOPEO-phases/56-governing-lineage-evidence-deadline-engine/56-AI-SPEC.md
  </read_first>
  <action>
Implement `FsbSkopeoTruthEngine.create` with exact descriptor-safe dependency validation and the frozen facade in `<interfaces>`. `readVisibleSourceSet(exactTuple)` is a background-owned narrow seam: after live tuple/boundary/claim validation it reads the one visible manifest, returns only a sorted unique nonempty set of exact source IDs/states/fingerprint bindings capped at 32, and is reread before every publication/read result. It exposes no store or manifest object and returns exact `exact-set-over-cap`/closed status rather than the first 32.

Parse the request's `evaluationContext` before any graph, provider, adjudication, store, or read effect. The request is the sole source of `asOfCivilDate`, governing timezone binding, and calendar data; do not call `now`, `Date`, browser locale, host timezone, or UTC defaults to construct any part of it. After obtaining the exact graph snapshot, call `validateEvaluationContext` and require exact current citation/configuration/calendar bindings plus a matching canonical digest. Missing context returns `evaluation-context-missing`; stale authoritative bindings return `evaluation-context-stale`; malformed/mixed/mismatched context or digest returns `evaluation-context-mismatch`. Each case returns no family proof and stages/publishes nothing.

`recompute` must:

1. Discover the complete visible set and reject non-ready/incomplete/over-cap state without a partial family.
2. Call only `graphFacade.snapshotExactSet` under its fresh Phase 54 query authority and require exact source membership/digest.
3. For each source in sorted order, run truth-extractor preparation/content/provider/optional repair/finalization through distinct fresh Phase 54 ingestion operations, exact invariant bindings, operation signal, and provider-no-storage acknowledgement. Never retain a certificate; always discard the opaque extractor session in `finally`.
4. Pass only the complete frozen snapshot/candidate generations and the parsed, authoritatively validated evaluation context to `adjudicateExactSet`; require each returned family to pass `parseSemanticFamilyProof` and contain no page/hash/manifest/`sts1:` field.
5. Reread the visible set and graph digest, recheck provider/rule versions and the live tuple, then call `validateEvaluationContext` again and require the identical current context digest immediately before each commit. Publish each semantic family proof through `beginFamilyReplacement` → store-owned `stageFamilySnapshot` (deterministic pages/hashes/manifest/`sts1:` construction) → `publishFamilySnapshot` under a fresh exact-set ingestion effect. Missing/stale/mismatched context publishes nothing; no late/cancelled/stale result retains a mutation guard.
6. Return only minimized status/family IDs and blockers, never raw candidates/snapshot/store state.

Each inspect method requires and reparses the caller's explicit evaluation context, discovers/re-authorizes the current exact set, obtains a fresh graph snapshot/digest, validates the supplied context against that snapshot, reads one active family, compares every persisted input/rule/evaluation-context/manifest binding, revalidates context currentness immediately before return, and only then projects its named domain. On graph/input/context mismatch it returns no stale projection, enters a separate fresh ingestion effect to withdraw the family synchronously, and returns the exact `snapshot-stale` or evaluation-context blocker with review-required status. Projection fields are closed: lineage axes/path/overlays/inheritance; typed facts/trust/access/citation IDs; complete conflict members; citation ID plus exact source fingerprint/fragment/record/optional relation/locator/range metadata; deadline derivation/eligibility/blockers; or metadata-only status. Each is a new recursively frozen value at most 64 KiB. Citation metadata is not a URL/access grant; future navigation must use fresh `citation-open` authority.

Integrate background imports and exact boot order from `<interfaces>`. Construct the narrow `validateEvaluationContext` seam inside the trusted boundary from existing fresh citation/current-configuration authority plus an immutable versioned calendar registry; inject only that function into the truth engine, never its backing stores/config objects. The seam validates caller data and currentness but never supplies a civil date, timezone, or calendar default. Construct truth store before participant registration; register `truthStore.graphInvalidator` with graph store once; bind graph's four participants, truth's real `citations`, and exact empty `counts`/`alerts`; recover corpus then graph; create graph facade; recover truth with its issued mutation guard; create the frozen truth facade last. Keep truth facade referenced only inside the marked boundary with no global generic message/content/MCP/UI route. Update the Lattice import-count pin only to exact counts observed after adding six imports; do not guess or weaken the test.

Extend `verify-skopeo-storage-boundary.mjs` with exact `TRUTH_MODULE_PATHS`/import order and reuse its literal closure/runtime scans. For truth modules additionally reject direct storage outside truth store, graph-store/query injection into truth engine, `Date.parse`/implicit `new Date(string)`/locale date parsing, eval/Function/dynamic import, remote hosts/fetch outside extractor's injected provider, raw source/provider access outside extractor, generic expressions/query languages, embeddings/vector/RAG/agents, content/HUD/ask/policy/MCP/server/daemon/process/database/Python, alarms/notifications/recipients/schedules/alert ledgers, injection-root reachability, missing/duplicate/reordered imports, truth store after participant registration, absent invalidator, empty citations, nonempty counts/alerts, wrong recovery/facade order, boot hydration, or truth facade reference outside the marked boundary. Add `sourceOverrides` negative mutations for every class.
  </action>
  <acceptance_criteria>
    - `node tests/skopeo-truth-runtime.test.js`, graph/corpus runtime regressions, and `node scripts/verify-skopeo-storage-boundary.mjs` all exit 0.
    - Background imports exactly six truth modules once/in order and executes the exact construction → invalidator → seven participants → corpus recover → graph recover → graph facade → truth recover → truth facade order with zero boot hydration/provider/Drive read.
    - Real `citations` owns all truth influence; only `counts`/`alerts` use empty binders; graph still owns exactly four categories.
    - Recompute uses the complete current visible set, fresh exact graph snapshots, fresh source-local provider operations, explicit authoritatively validated evaluation context, pure semantic-proof adjudication, final digest/set/context recheck, and store-owned manifest/pointer-last publication with zero partial/late state.
    - Missing/stale/mismatched evaluation context, timezone binding, or calendar data returns the exact context blocker and publishes nothing; context currentness changing before commit is caught by the final seam call.
    - All seven reads require explicit context, use fresh authority/context currentness, and synchronously withdraw mismatch before returning no stale data; every output is frozen, exact-key, minimized, and within 64 KiB.
    - Static/runtime negative mutations detect direct graph/storage/raw/provider/content/MCP/UI/ask/policy/date-default/scheduling/notification paths and private-facade escape.
    - Lattice provider-bridge counts are updated from observed exact import tokens/call sites and its focused test remains green.
  </acceptance_criteria>
  <verify>
    <automated>node --check extension/utils/skopeo-truth-engine.js &amp;&amp; node --check extension/background.js &amp;&amp; node tests/skopeo-truth-runtime.test.js &amp;&amp; node tests/skopeo-graph-runtime.test.js &amp;&amp; node tests/skopeo-corpus-runtime.test.js &amp;&amp; node scripts/verify-skopeo-storage-boundary.mjs &amp;&amp; node tests/lattice-provider-bridge-smoke.test.js</automated>
  </verify>
  <done>One recovered background-only truth facade now recomputes and reads exact cited governing/deadline state under fresh authority without exposing internals or implementing later phases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Land the 24-case truth evaluation, once-only package gate, and honest human evidence status</name>
  <files>tests/fixtures/skopeo-truth-evals/manifest.json, tests/fixtures/skopeo-truth-evals/cases.json, tests/skopeo-truth-evals.test.js, tests/skopeo-truth-runtime.test.js, package.json</files>
  <read_first>
    - tests/fixtures/skopeo-truth-evals/manifest.json
    - tests/fixtures/skopeo-truth-evals/cases.json
    - tests/skopeo-truth-evals.test.js
    - tests/skopeo-truth-runtime.test.js
    - package.json
    - tests/fixtures/skopeo-graph-evals/manifest.json
    - tests/fixtures/skopeo-graph-evals/cases.json
    - tests/skopeo-graph-evals.test.js
    - .planning/milestones/v1.2.0-SKOPEO-phases/56-governing-lineage-evidence-deadline-engine/56-AI-SPEC.md
    - .planning/milestones/v1.2.0-SKOPEO-phases/56-governing-lineage-evidence-deadline-engine/56-VALIDATION.md
    - .planning/milestones/v1.2.0-SKOPEO-phases/55-chrome-local-graph-incremental-truth-foundation/55-VERIFICATION.md
    - .planning/milestones/v1.2.0-SKOPEO-phases/55-chrome-local-graph-incremental-truth-foundation/55-HUMAN-UAT.md
  </read_first>
  <action>
Create an ordered immutable synthetic/irreversibly-redacted manifest and exactly 24 case objects in `cases.json`, with stable category/ID counts 6 governing (`G01`–`G06`), 6 fact/evidence (`F01`–`F06`), 6 deadline (`D01`–`D06`), and 6 runtime/security (`R01`–`R06`):

- G01 executed/effective active base; G02 newer unsigned draft cannot govern; G03 clause partial amendment plus inherited base clause; G04 explicit full replacement; G05 conflicting/unorderable amendments; G06 missing/conflicting execution plus dangling/cyclic lineage abstention.
- F01 unreadable/inaccessible required source; F02 all nine typed assertions; F03 conflicting lifecycle/notice facts; F04 forged/clipped/stale citation; F05 access/currentness separate from five-state trust; F06 cross-account/corpus/family isolation.
- D01 calendar-day leap/month/year boundaries; D02 business-day missing calendar; D03 explicit immutable business calendar plus caller-supplied context currentness; D04 missing/stale/mismatched evaluation context, cited/configured timezone, boundary, or consequence; D05 unsupported operator/model arithmetic/clock-default rejection; D06 direct-versus-derived deadline conflict and eligibility blockers.
- R01 filename/label/recency/order/confidence/prompt-injection spoof; R02 source revocation/cancellation during computation; R03 authorized-set/fragment/record/relation/candidate-overlay/evaluation-context drift; R04 semantic-proof → store-owned page/hash/manifest → pointer-last publication and reverse-dependency crash recovery; R05 exact limits including 2,048 unique family citations accepted and 2,049 rejected; R06 raw-data/diagnostic/content/MCP/UI/policy/scheduling boundary closure.

Each case binds fixture/schema/prompt/extractor/adjudicator/deadline/calendar/store versions, exact input authority transitions, recorded candidate response where applicable, expected proof/assertion/conflict/derivation/blocker/visibility or absence, forbidden-marker probes, `label_version`, nullable `gold_label_version`, `review_status: 'pending'|'approved'`, `required_reviewer_roles`, `approved_reviewer_roles`, and nullable nonidentifying `review_record_ref`. Initialize every case pending with null gold/review reference and no approved roles. Required roles by applicable case are selected from exact codes `commercial-contracts-counsel`, `legal-operations`, `source-system-steward`, `privacy-security`, and `evaluation-lead`; automated code cannot add approval.

Implement the network-free harness against production schema, extractor, exact-set graph facade, adjudicator, deadline, truth store, and truth engine with fake Phase 54 authority/storage/provider. Enforce zero averaging: every Critical structural/security case must pass, every pending expected output is compared only as `provisional_regression`, and every conflict/blocker/absence/privacy/scheduling condition is exact. Enumerate all 24 IDs/counts and fail on missing/duplicate/extra/version/reviewer metadata drift. Report exactly:

- `deterministic_structural_security: pass|fail`
- `provisional_regression: pass|fail (not gold)`
- `domain_fidelity: human_needed|approved|rejected`

Keep `domain_fidelity: human_needed` until genuine representative approvals name all required roles, matching label/gold versions, and valid review records. No LLM judge, configured-provider run, deterministic comparison, or synthetic fixture can promote it. Preserve Phase 55's pending expert/live Chrome/full-suite startup items; do not represent them as resolved.

Add `test:skopeo-truth-evals` with the stable new-test order `skopeo-truth-schema` → `skopeo-deadline-engine` → `skopeo-truth-extractor` → `skopeo-lineage-adjudicator` → `skopeo-truth-store` → `skopeo-truth-runtime` → `skopeo-truth-evals`. Keep the existing `test:skopeo-graph-evals` unchanged as the sole owner of shared graph schema/store/query/runtime tests, so normal execution does not duplicate them. Insert the truth aggregate exactly once immediately after the existing graph aggregate in normal `npm test`; add no dependency/runner/watch mode. The full Phase 56 gate explicitly runs both aggregates.

Run the truth aggregate, graph aggregate, storage verifier, and extension validation. Then run full `npm test`. If the known Chrome DevTools startup timeout recurs, record the truth/graph focused passes separately and the repository-wide gate as not green; an isolated browser pass does not override it. Do not weaken/remove the browser test or fabricate live Chrome/Drive/citation/domain approval.
  </action>
  <acceptance_criteria>
    - Manifest/harness enumerate exactly 24 unique IDs with category counts 6/6/6/6, all required scenario coverage, exact version bindings, and valid pending reviewer metadata.
    - `npm run test:skopeo-truth-evals` exits 0, remains network-free, completes within the focused 30-second target, and reports deterministic pass, provisional pass `(not gold)`, and `domain_fidelity: human_needed`.
    - `npm run test:skopeo-graph-evals && npm run test:skopeo-truth-evals && node scripts/verify-skopeo-storage-boundary.mjs && npm run validate:extension` exits 0.
    - `package.json` invokes each aggregate once in normal `npm test`, with shared graph tests owned only by the graph aggregate and every new truth test owned only by the truth aggregate; no package/lockfile/provider/host/MCP change occurs.
    - Full `npm test` exits 0, or any recurrence of the documented downstream Chrome startup timeout is reported exactly as a non-green repository gate while all focused results remain separately stated.
    - Human-only domain adjudication, live citation navigation, and Chrome MV3 lifecycle checks remain pending/`human_needed`; no fixture or optional provider output is called expert-approved or live-approved.
    - Fixture/runtime/static scans find no real customer text/identifier, external trace/eval service, raw prompt/response persistence, content/MCP truth surface, HUD/ask/policy implementation, alarm/notification/recipient/schedule/delivery/alert-ledger behavior.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:skopeo-truth-evals &amp;&amp; node scripts/verify-skopeo-storage-boundary.mjs &amp;&amp; npm run validate:extension</automated>
  </verify>
  <done>The complete deterministic Phase 56 corpus, runtime/static gates, and once-only package integration prove structural/security behavior while expert legal/domain and live Chrome evidence remain honestly human-needed.</done>
</task>

</tasks>

<verification>
Run `npm run test:skopeo-graph-evals && npm run test:skopeo-truth-evals && node scripts/verify-skopeo-storage-boundary.mjs && npm run validate:extension && npm test`. Confirm no unexpected generated-file or package-lock change remains. Report deterministic structural/security, provisional regression, domain fidelity, live Chrome/citation status, and the full-suite Chrome startup result as separate evidence dimensions.
</verification>

<success_criteria>
- A trusted Chrome-local runtime turns the complete current authorized graph set into immutable cited governing/fact/conflict/deadline proofs and withdraws stale truth before any read or recompute.
- One frozen minimized background facade is available only after corpus, graph, and truth recovery; no content, MCP, UI, policy, storage, raw graph/source/provider, or generic-query authority escapes.
- The real citations participant and graph invalidator remove every affected multi-source family before source/graph changes; counts and alerts remain exact empty later-phase owners.
- All 24 deterministic structural/security fixtures and provisional regressions pass, while real legal/domain and live Chrome/Drive/citation evidence remains `human_needed` until genuine approval.
- No Phase 57 HUD, Phase 58 ask/Document-10/memo policy, or Phase 59 alarms/notifications/recipients/scheduling/delivery/alert ledger is implemented.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.2.0-SKOPEO-phases/56-governing-lineage-evidence-deadline-engine/56-05-SUMMARY.md` with separate deterministic structural/security, provisional regression, domain-fidelity, live-UAT, and full-suite statuses; write `human_needed` for unapproved human evidence.
</output>
