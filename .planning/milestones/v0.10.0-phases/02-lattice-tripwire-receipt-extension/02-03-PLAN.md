---
phase: 02
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - lattice/packages/lattice/src/index.ts
  - lattice/packages/lattice/dist/index.js
  - lattice/packages/lattice/dist/index.d.ts
  - lattice/docs/fsb-integration-gaps.md
autonomous: true
requirements: [LSDK-04, LSDK-05, LSDK-06, LSDK-07, LSDK-08, LSDK-02, LSDK-03]
requirements_addressed: [LSDK-04, LSDK-05, LSDK-06, LSDK-07, LSDK-08, LSDK-02, LSDK-03]
decisions_addressed: [D-14, D-15, D-17, CD-03]

must_haves:
  truths:
    - "lattice/packages/lattice/src/index.ts re-exports createHookPipeline (value) + HookPipeline (type) + HookLifecycleEvent (type) from ./contract/bands.js"
    - "Re-export inserted alphabetically (between artifact and contract per RESEARCH.md Pitfall 7)"
    - "dist/index.js and dist/index.d.ts rebuilt via pnpm build; createHookPipeline reachable in dist bundle"
    - "FSB-side bare-specifier `import('lattice')` exposes lattice.createHookPipeline as a function (verified via Node probe)"
    - "lattice/docs/fsb-integration-gaps.md Receipts rows 2 + 3 flipped to Covered with backlink SHA from Plan 02-01"
    - "lattice/docs/fsb-integration-gaps.md Tripwires/hooks rows 2 + 3 + 4 + 5 flipped to Covered with backlink SHA from Plan 02-02"
    - "lattice/docs/fsb-integration-gaps.md gains one additive Tripwires/hooks row for HookLifecycleEvent union (Covered) with backlink SHA from Plan 02-02"
    - "Two Lattice commits land on fsb-integration-experiments: (a) feat(api): re-export tripwire pipeline + lifecycle events; (b) docs(fsb-integration): close Phase 2 audit rows. Both carry Ref: FSB v0.10.0-attempt-2 Phase 2 footer."
    - "No git push (D-15)"
  artifacts:
    - path: "lattice/packages/lattice/src/index.ts"
      provides: "Public surface re-exports createHookPipeline + HookPipeline + HookLifecycleEvent"
      contains: "createHookPipeline"
    - path: "lattice/packages/lattice/dist/index.js"
      provides: "Built bundle containing createHookPipeline"
      contains: "createHookPipeline"
    - path: "lattice/packages/lattice/dist/index.d.ts"
      provides: "Type declaration containing HookPipeline + HookLifecycleEvent"
      contains: "HookLifecycleEvent"
    - path: "lattice/docs/fsb-integration-gaps.md"
      provides: "Audit doc with Phase 2 rows flipped to Covered + new lifecycle row"
      contains: "Phase 2 (FSB v0.10.0-attempt-2)"
  key_links:
    - from: "lattice/packages/lattice/src/index.ts"
      to: "lattice/packages/lattice/src/contract/bands.ts"
      via: "Re-export of createHookPipeline, type HookPipeline, type HookLifecycleEvent"
      pattern: "export.*createHookPipeline.*from.*bands"
    - from: "lattice/packages/lattice/dist/index.js"
      to: "lattice/packages/lattice/src/index.ts"
      via: "tsdown build (clean: true) -- the same build flow that ran in Phase 1 Plan 01-01"
      pattern: "createHookPipeline"
---

<objective>
Land the public-surface re-export of Phase 2's new band-pipeline primitive on Lattice's `src/index.ts` so FSB can `import { createHookPipeline, HookPipeline, HookLifecycleEvent } from 'lattice'` via the bare specifier. Rebuild `dist/` via `pnpm build` so the symbol reaches the bundled ESM + type declarations. Then flip the closed Phase 2 audit-doc rows in `lattice/docs/fsb-integration-gaps.md` from `Needs extension` / `Needs addition` to `Covered` with backlink SHAs to the receipts (Plan 02-01) and bands (Plan 02-02) commits, and add one new row capturing the HookLifecycleEvent union.

Purpose: Make Phase 2's Lattice-side surface consumable by FSB (Plan 02-04's smoke depends on `lattice.createHookPipeline` resolving via the bare specifier). Close audit-doc rows per D-17 + RESEARCH.md "Audit-Doc Row Closure Map".

Output: Two Lattice-side commits on fsb-integration-experiments:
1. `feat(api): re-export tripwire pipeline + lifecycle events from public surface` -- modifies `src/index.ts` and rebuilds `dist/`.
2. `docs(fsb-integration): close Phase 2 audit rows (receipts + tripwires/hooks)` -- modifies `docs/fsb-integration-gaps.md`.

Both carry `Ref: FSB v0.10.0-attempt-2 Phase 2` body footer (D-14). No git push (D-15).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/LATTICE-PIN.md

@.planning/phases/02-lattice-tripwire-receipt-extension/02-CONTEXT.md
@.planning/phases/02-lattice-tripwire-receipt-extension/02-RESEARCH.md
@.planning/phases/02-lattice-tripwire-receipt-extension/02-VALIDATION.md
@.planning/phases/02-lattice-tripwire-receipt-extension/02-01-SUMMARY.md
@.planning/phases/02-lattice-tripwire-receipt-extension/02-02-SUMMARY.md
@.planning/phases/02-lattice-tripwire-receipt-extension/02-01-LATTICE-SHA.txt
@.planning/phases/02-lattice-tripwire-receipt-extension/02-02-LATTICE-SHA.txt

@.planning/phases/01-lattice-gap-survey-scaffold/01-01-SUMMARY.md
@lattice/docs/fsb-integration-gaps.md

<interfaces>
Existing index.ts re-export pattern (lattice/packages/lattice/src/index.ts, current state from Plan 01-01 + 01-02):

```typescript
export { artifact } from "./artifacts/artifact.js";
export { contract } from "./contract/contract.js";
export { inv } from "./contract/invariants.js";
export { defaultPiiDetectors } from "./contract/pii-detectors.js";
export {
  estimateRouteCost,
  evaluateContractAgainstRoute,
} from "./contract/preflight.js";
export { evaluateTripwires } from "./contract/tripwire.js";
// ... continues with outputs, receipts, results, providers, replay, runtime, sessions, storage, tools ...
// ... then type re-export block (CapabilityReceiptBody and others) ...
```

Alphabetical insertion point (per RESEARCH.md Pitfall 7): between `artifact` and `contract` (the `bands.ts` re-export). Note: the existing line `export { artifact } from "./artifacts/artifact.js";` is the alphabetically-FIRST entry. The new bands line follows immediately.

Phase 1 build flow (from 01-01-SUMMARY.md, verified):
```bash
cd lattice && pnpm install   # idempotent
cd lattice && pnpm build      # tsdown produces dist/index.js + dist/index.d.ts (clean: true)
```

dist/ files (lattice/packages/lattice/dist/): gitignored on Lattice's side. Rebuilt per-developer per phase. Phase 1 already produced ~107KB dist/index.js + ~53KB dist/index.d.ts.

Current audit-doc state (lattice/docs/fsb-integration-gaps.md):
- Receipts row 2 ("Step-transition fields ...") -- Status: `Needs extension`, Severity: `Blocker` (line 22)
- Receipts row 3 ("sessionId field ...") -- Status: `Needs extension`, Severity: `Blocker` (line 23)
- Tripwires/hooks row 2 ("Priority bands ...") -- Status: `Needs addition`, Severity: `Blocker` (line 33)
- Tripwires/hooks row 3 ("Per-handler matcher regex + race-with-log budget ...") -- Status: `Needs addition`, Severity: `Blocker` (line 34)
- Tripwires/hooks row 4 ("Frozen contexts ...") -- Status: `Needs addition`, Severity: `Important` (line 35)
- Tripwires/hooks row 5 ("Mid-session registration freeze") -- Status: `Needs addition`, Severity: `Important` (line 36)

Lattice SHAs from prior plans:
- Plan 02-01 SHA: file `.planning/phases/02-lattice-tripwire-receipt-extension/02-01-LATTICE-SHA.txt`
- Plan 02-02 SHA: file `.planning/phases/02-lattice-tripwire-receipt-extension/02-02-LATTICE-SHA.txt`

These SHAs will be the backlinks in the row-flip Notes column.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add createHookPipeline + types to Lattice public surface re-exports; rebuild dist/</name>
  <files>lattice/packages/lattice/src/index.ts, lattice/packages/lattice/dist/index.js, lattice/packages/lattice/dist/index.d.ts</files>
  <read_first>
    - lattice/packages/lattice/src/index.ts (full -- 117 lines, current re-export surface)
    - lattice/packages/lattice/src/contract/bands.ts (just-created -- contains createHookPipeline + HookPipeline + HookLifecycleEvent exports)
    - .planning/phases/01-lattice-gap-survey-scaffold/01-01-SUMMARY.md ("dist/ ownership" section + tsdown clean: true reference)
    - .planning/phases/02-lattice-tripwire-receipt-extension/02-RESEARCH.md (section: "Public-surface re-export (exact diff for index.ts)", "Open Questions" #1, "Pitfall 7", "Pitfall 8")
    - lattice/packages/lattice/tsdown.config.ts (full -- entry point + clean flag)
  </read_first>
  <behavior>
    - Test 1: After edit, `src/index.ts` contains a new line near the top (alphabetically between artifact and contract entries): `export { createHookPipeline, type HookPipeline, type HookLifecycleEvent } from "./contract/bands.js";`.
    - Test 2: All existing exports remain byte-identical (no reordering of other entries; no duplicate exports).
    - Test 3: `cd lattice && pnpm --filter lattice typecheck` exits 0.
    - Test 4: `cd lattice && pnpm build` exits 0 and regenerates dist/index.js + dist/index.d.ts.
    - Test 5: `grep "createHookPipeline" lattice/packages/lattice/dist/index.js` returns >= 1 match.
    - Test 6: `grep "HookLifecycleEvent" lattice/packages/lattice/dist/index.d.ts` returns >= 1 match.
    - Test 7: `grep "HookPipeline" lattice/packages/lattice/dist/index.d.ts` returns >= 1 match.
    - Test 8: Node-side bare-specifier probe from FSB root resolves createHookPipeline as a function.
    - Test 9: Phase 1's `tests/lattice-smoke.test.js` continues to pass post-rebuild (no Phase 1 breakage).
  </behavior>
  <action>
**Step 1.1 -- Pre-flight catalog drift check (RESEARCH.md Pitfall 8).**

Run: `cd /Users/lakshmanturlapati/Desktop/FSB/automation/lattice && grep -c "catalog:" packages/lattice/package.json`

Expected output: `0`. If non-zero, the catalog-fix from Phase 1 (commit `22bf986`) has been undone by a `pnpm install --refresh-lockfile` somewhere. STOP and resolve before proceeding -- npm 11 cannot install with `catalog:` specifiers and FSB's `file:` dep would break.

**Step 1.2 -- Modify `lattice/packages/lattice/src/index.ts`.**

The current file (as of Phase 1 / Plan 01-01) has the following layout (verified at research time):

```
line 1:  export { artifact } from "./artifacts/artifact.js";
line 2:  export { contract } from "./contract/contract.js";
line 3:  export { inv } from "./contract/invariants.js";
... (continues to ~line 117)
```

Insert ONE new line immediately after line 1 (which is `export { artifact } ...`) and immediately before the current line 2 (which is `export { contract } ...`). The alphabetical insertion point puts `bands` after `artifacts` and before `contract` (and `inv`, `pii-detectors`, `preflight`, `tripwire` -- all of which live under `./contract/`).

NEW line to insert:

```typescript
export { createHookPipeline, type HookPipeline, type HookLifecycleEvent } from "./contract/bands.js";
```

Use the Edit/Write tool. The result MUST keep all other lines byte-identical -- only ONE line added, at the correct alphabetical position.

**Step 1.3 -- Run Lattice typecheck.**

Run: `cd /Users/lakshmanturlapati/Desktop/FSB/automation/lattice && pnpm --filter lattice typecheck`

Expected: exit 0.

**Step 1.4 -- Run Lattice full test suite (regression gate).**

Run: `cd /Users/lakshmanturlapati/Desktop/FSB/automation/lattice && pnpm --filter lattice test`

Expected: exit 0. Existing 451+ tests + Plan 02-01 receipts tests + Plan 02-02 bands tests all green.

**Step 1.5 -- Rebuild Lattice dist/.**

Run: `cd /Users/lakshmanturlapati/Desktop/FSB/automation/lattice && pnpm build`

Expected: exit 0. tsdown produces fresh dist/index.js + dist/index.d.ts (clean: true wipes the prior bundle first).

**Step 1.6 -- Verify the new symbol reaches the built bundle.**

Run:
```bash
grep -c "createHookPipeline" /Users/lakshmanturlapati/Desktop/FSB/automation/lattice/packages/lattice/dist/index.js
grep -c "HookLifecycleEvent\|HookPipeline" /Users/lakshmanturlapati/Desktop/FSB/automation/lattice/packages/lattice/dist/index.d.ts
```

Both must return >= 1.

**Step 1.7 -- FSB-side Node bare-specifier probe (regression-free import).**

From FSB root (NOT inside `lattice/`):

```bash
cd /Users/lakshmanturlapati/Desktop/FSB/automation && node -e "
import('lattice').then(l => {
  const probe = {
    createReceipt: typeof l.createReceipt,
    verifyReceipt: typeof l.verifyReceipt,
    createInMemorySigner: typeof l.createInMemorySigner,
    generateEd25519KeyPairJwk: typeof l.generateEd25519KeyPairJwk,
    createMemoryKeySet: typeof l.createMemoryKeySet,
    createHookPipeline: typeof l.createHookPipeline,
  };
  console.log(JSON.stringify(probe, null, 2));
  if (probe.createHookPipeline !== 'function') process.exit(1);
});
"
```

Expected: All 6 entries log `'function'`; exit 0. The presence of `createHookPipeline: 'function'` is the Phase 2 surface-presence proof. The 5 prior entries are Phase 1 carryforward (must remain present).

**Step 1.8 -- Phase 1 smoke regression gate.**

Run: `cd /Users/lakshmanturlapati/Desktop/FSB/automation && node tests/lattice-smoke.test.js`

Expected: exit 0 with 29 PASS / 0 FAIL. Phase 2's index.ts edit + dist rebuild MUST NOT break Phase 1's mint+verify round-trip.

**Step 1.9 -- Stage src/index.ts and commit (D-14 ceremony).**

dist/ is gitignored on Lattice's side -- DO NOT attempt to stage it. Only stage the source file:

```bash
cd /Users/lakshmanturlapati/Desktop/FSB/automation/lattice && git add packages/lattice/src/index.ts
```

Verify the staged diff is a SINGLE-line addition:

```bash
cd /Users/lakshmanturlapati/Desktop/FSB/automation/lattice && git diff --cached --stat
```

Expected output: `packages/lattice/src/index.ts | 1 +` (one line added; no removals). If the output shows multiple line changes, STOP and inspect -- this plan only adds one line.

Then commit using HEREDOC for the message (D-14 ceremony):

```bash
cd /Users/lakshmanturlapati/Desktop/FSB/automation/lattice && git commit -m "$(cat <<'EOF'
feat(api): re-export tripwire pipeline + lifecycle events from public surface

Add one re-export line to packages/lattice/src/index.ts so FSB and other
Lattice consumers can import the Phase 2 band-pipeline primitive via the
bare specifier "lattice":

  export { createHookPipeline, type HookPipeline, type HookLifecycleEvent } from "./contract/bands.js";

Inserted alphabetically (between artifacts and contract entries) per
existing convention. tsdown rebuild (clean: true) regenerates dist/index.js
and dist/index.d.ts; the new symbol reaches both the ESM bundle and the
TypeScript declaration aggregate.

Surface presence check (Node probe from FSB root):
  typeof lattice.createHookPipeline === "function"

Phase 1 carryforward (createReceipt, verifyReceipt, createInMemorySigner,
generateEd25519KeyPairJwk, createMemoryKeySet) all remain reachable.

Ref: FSB v0.10.0-attempt-2 Phase 2
EOF
)"
```

**Step 1.10 -- Confirm NO push (D-15).**

Run: `cd /Users/lakshmanturlapati/Desktop/FSB/automation/lattice && git reflog -10 | grep -c push`

Expected output: `0`.

**Step 1.11 -- Capture SHA for downstream.**

Run: `cd /Users/lakshmanturlapati/Desktop/FSB/automation/lattice && git rev-parse HEAD > /Users/lakshmanturlapati/Desktop/FSB/automation/.planning/phases/02-lattice-tripwire-receipt-extension/02-03a-LATTICE-SHA.txt`
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Desktop/FSB/automation/lattice && pnpm --filter lattice typecheck && pnpm --filter lattice test && pnpm build && grep -q "createHookPipeline" packages/lattice/src/index.ts && grep -q "createHookPipeline" packages/lattice/dist/index.js && grep -q "HookLifecycleEvent" packages/lattice/dist/index.d.ts && cd /Users/lakshmanturlapati/Desktop/FSB/automation && node tests/lattice-smoke.test.js && cd /Users/lakshmanturlapati/Desktop/FSB/automation/lattice && git log -1 --format="%s" | grep -F "feat(api): re-export tripwire pipeline + lifecycle events from public surface" && git log -1 --format="%B" | grep -F "Ref: FSB v0.10.0-attempt-2 Phase 2" && test -f /Users/lakshmanturlapati/Desktop/FSB/automation/.planning/phases/02-lattice-tripwire-receipt-extension/02-03a-LATTICE-SHA.txt</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "createHookPipeline" lattice/packages/lattice/src/index.ts` returns 1.
    - `grep -c "HookPipeline\|HookLifecycleEvent" lattice/packages/lattice/src/index.ts` returns >= 2 (in the same line as the new export plus the type re-export block if present in the prior shape).
    - `cd lattice && git diff HEAD~1 HEAD --stat` shows `packages/lattice/src/index.ts | 1 +` (one line added).
    - `cd lattice && pnpm --filter lattice typecheck` exits 0.
    - `cd lattice && pnpm --filter lattice test` exits 0.
    - `cd lattice && pnpm build` exits 0.
    - `grep -c "createHookPipeline" lattice/packages/lattice/dist/index.js` returns >= 1.
    - `grep -c "HookLifecycleEvent" lattice/packages/lattice/dist/index.d.ts` returns >= 1.
    - `node -e "import('lattice').then(l => { if (typeof l.createHookPipeline !== 'function') process.exit(1); })"` (run from FSB root) exits 0.
    - `node tests/lattice-smoke.test.js` (Phase 1 smoke) exits 0 with 29 PASS / 0 FAIL.
    - `cd lattice && git log -1 --format="%s"` returns exactly `feat(api): re-export tripwire pipeline + lifecycle events from public surface`.
    - `cd lattice && git log -1 --format="%B" | grep -c "Ref: FSB v0.10.0-attempt-2 Phase 2"` returns 1.
    - `cd lattice && git reflog -10 | grep -c push` returns 0.
    - `.planning/phases/02-lattice-tripwire-receipt-extension/02-03a-LATTICE-SHA.txt` exists with 40 hex chars.
    - No emojis in modified file or commit message.
  </acceptance_criteria>
  <done>One Lattice commit lands with the index.ts re-export; dist/ rebuilt; FSB bare-specifier probe passes; Phase 1 smoke unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Flip closed Phase 2 audit-doc rows in lattice/docs/fsb-integration-gaps.md + add lifecycle row</name>
  <files>lattice/docs/fsb-integration-gaps.md</files>
  <read_first>
    - lattice/docs/fsb-integration-gaps.md (full -- 91 lines; rows to be flipped)
    - .planning/phases/02-lattice-tripwire-receipt-extension/02-01-LATTICE-SHA.txt (the receipts commit SHA from Plan 02-01)
    - .planning/phases/02-lattice-tripwire-receipt-extension/02-02-LATTICE-SHA.txt (the bands commit SHA from Plan 02-02)
    - .planning/phases/02-lattice-tripwire-receipt-extension/02-RESEARCH.md (section: "lattice/docs/fsb-integration-gaps.md row-flip diff" -- exact wording for each row)
  </read_first>
  <behavior>
    - Test 1: Receipts row 2 ("Step-transition fields ...") Status flips from `Needs extension` to `Covered`, Severity flips from `Blocker` to `n/a`, Notes column gets a Phase 2 narrative ending with the 7-or-8-char short SHA from Plan 02-01.
    - Test 2: Receipts row 3 ("sessionId field ...") flips same way, same SHA backlink.
    - Test 3: Tripwires/hooks rows 2, 3, 4, 5 flip from Needs addition (Blocker/Important) to Covered (n/a), with backlink to Plan 02-02 SHA.
    - Test 4: ONE new row appended to the Tripwires/hooks table: lifecycle event union (BEFORE_PROVIDER / AFTER_PROVIDER / BEFORE_TOOL / AFTER_TOOL), separate from RunEventKind. Status: Covered. Severity: n/a. Notes backlink Plan 02-02 SHA.
    - Test 5: Other doc sections (Providers, Delegation, MV3-survivability, Observability/step-markers, "How this doc gets used" tail) are byte-unchanged.
    - Test 6: One Lattice commit lands with subject `docs(fsb-integration): close Phase 2 audit rows (receipts + tripwires/hooks)` + Ref footer.
  </behavior>
  <action>
**Step 2.1 -- Load the two backlink SHAs from Plans 02-01 and 02-02.**

Capture both SHAs as shell variables for use in the row text:

```bash
RECEIPTS_SHA=$(cat /Users/lakshmanturlapati/Desktop/FSB/automation/.planning/phases/02-lattice-tripwire-receipt-extension/02-01-LATTICE-SHA.txt)
BANDS_SHA=$(cat /Users/lakshmanturlapati/Desktop/FSB/automation/.planning/phases/02-lattice-tripwire-receipt-extension/02-02-LATTICE-SHA.txt)
echo "Receipts: $RECEIPTS_SHA"
echo "Bands:    $BANDS_SHA"
```

Use the 7-character short SHA prefix in the doc text (matches Lattice's existing commit-reference style: `ab6c1f6`, `195e5ae`, `22bf986` -- all 7 chars).

**Step 2.2 -- Modify `lattice/docs/fsb-integration-gaps.md`.**

The current Receipts section (lines 19-25) has this shape:

```markdown
| Receipts | Capability Receipt mint + verify round-trip via Ed25519 (DSSE v1.0 envelope + JCS canonical form) | Covered | n/a | v1.1 ships this surface end-to-end; FSB's Phase 1 smoke proves the round-trip from a Node-side consumer. |
| Receipts | Step-transition fields on the receipt body (stepName, stepIndex, parentStepName, previousStepName, timestamp per FSB attempt-1 inspector envelope) | Needs extension | Blocker | FSB autopilot emits 12 step markers per iteration (attempt-1 02-state-inspectability-carve-out). Without these fields on the receipt body, every step-marker emission must carry the metadata out-of-band, defeating the "Lattice receipt is the inspector envelope" thesis (INV-06). Phase 2 candidate. |
| Receipts | sessionId field on the receipt body (FSB ties step markers to a persistent session across SW eviction) | Needs extension | Blocker | Phase 2 candidate. |
| Receipts | createReceipt is reachable via the public `lattice` bare specifier | Covered | n/a | Resolved in Phase 1 by re-exporting from `src/index.ts` (D-13 narrowed). |
```

Replace rows 2 and 3 (the two "Needs extension | Blocker" rows). Row 1 (the Covered round-trip row) and row 4 (the Covered createReceipt row) STAY BYTE-IDENTICAL.

New row 2:

```markdown
| Receipts | Step-transition fields on the receipt body (stepName, stepIndex, parentStepName, previousStepName, timestamp per FSB attempt-1 inspector envelope) | Covered | n/a | Phase 2 (FSB v0.10.0-attempt-2) added stepName, stepIndex, parentStepName, previousStepName, timestamp as optional top-level fields on CapabilityReceiptBody; version bumped via literal-union `"lattice-receipt/v1" | "lattice-receipt/v1.1"`. JCS round-trip unchanged. Redaction policy unchanged (step-marker fields are stable identifiers, not user content). Lattice commit `<RECEIPTS_SHORT>`. |
```

New row 3:

```markdown
| Receipts | sessionId field on the receipt body (FSB ties step markers to a persistent session across SW eviction) | Covered | n/a | Phase 2 (FSB v0.10.0-attempt-2) added sessionId as optional top-level field on CapabilityReceiptBody, same commit. Lattice commit `<RECEIPTS_SHORT>`. |
```

Replace `<RECEIPTS_SHORT>` with the first 7 characters of `$RECEIPTS_SHA`.

The current Tripwires/hooks section (lines 30-36) has this shape:

```markdown
| Tripwires/hooks | Pure tripwire evaluator over invariant set | Covered | n/a | v1.1 ships `evaluateTripwires` -- typed result, never throws. |
| Tripwires/hooks | Priority bands (SAFETY > OBSERVABILITY > EXTENSION) for hook ordering | Needs addition | Blocker | FSB attempt-1 Phase 1 (hooks-foundation) built this inside FSB. Per INV-06 this lives in Lattice. Phase 3 candidate. |
| Tripwires/hooks | Per-handler matcher regex + race-with-log budget so a slow handler cannot stall the safety band | Needs addition | Blocker | FSB attempt-1 pattern; required for autopilot reliability. Phase 3 candidate. |
| Tripwires/hooks | Frozen contexts (handler cannot mutate the band-set after registration window closes) | Needs addition | Important | Closes attempt-1 duplication. Phase 3 candidate. |
| Tripwires/hooks | Mid-session registration freeze | Needs addition | Important | Phase 3 candidate. |
```

Replace rows 2, 3, 4, 5 (the four Needs-addition rows). Row 1 (Covered "pure tripwire evaluator") STAYS BYTE-IDENTICAL.

Note: the existing rows mention "Phase 3 candidate" -- that wording was written in Phase 1 when tripwires were tentatively scoped to Phase 3. The Phase 2 CONTEXT.md re-pulled them into Phase 2. The row-flip text overwrites the stale "Phase 3 candidate" mention with the Phase 2 closure narrative.

New row 2:

```markdown
| Tripwires/hooks | Priority bands (SAFETY > OBSERVABILITY > EXTENSION) for hook ordering | Covered | n/a | Phase 2 (FSB v0.10.0-attempt-2) added `lattice/packages/lattice/src/contract/bands.ts` exporting createHookPipeline factory. Bands: SAFETY=0, OBSERVABILITY=1, EXTENSION=2. Lower number runs first; within-band registration order preserved. Lattice commit `<BANDS_SHORT>`. |
```

New row 3:

```markdown
| Tripwires/hooks | Per-handler matcher regex + race-with-log budget so a slow handler cannot stall the safety band | Covered | n/a | Phase 2 -- bands.ts RegisterOptions.matcher (optional per-handler regex) + RegisterOptions.budgetMs (default 100ms). Timeout emits HOOK_TIMEOUT event via TracerLike (no-abort `Promise.race`; CPU-leak risk explicitly accepted per CONTEXT.md D-09). Lattice commit `<BANDS_SHORT>`. |
```

New row 4:

```markdown
| Tripwires/hooks | Frozen contexts (handler cannot mutate the band-set after registration window closes) | Covered | n/a | Phase 2 -- bands.ts pipeline.run() wraps each handler's context in structuredClone + Object.freeze. Handler mutations do not leak. Lattice commit `<BANDS_SHORT>`. |
```

New row 5:

```markdown
| Tripwires/hooks | Mid-session registration freeze | Covered | n/a | Phase 2 -- bands.ts pipeline.freeze() is irreversible; subsequent register() throws Error(name === "PIPELINE_FROZEN"). Lattice commit `<BANDS_SHORT>`. |
```

Plus ONE additional row appended to the Tripwires/hooks table (after the existing 5th row):

```markdown
| Tripwires/hooks | Typed lifecycle event union (BEFORE_PROVIDER, AFTER_PROVIDER, BEFORE_TOOL, AFTER_TOOL) separate from RunEventKind | Covered | n/a | Phase 2 -- bands.ts HookLifecycleEvent union. Separate vocabulary from tracing.ts RunEventKind by design (CONTEXT.md D-12). Phase 3 (observability) can extend either independently. Lattice commit `<BANDS_SHORT>`. |
```

Replace `<BANDS_SHORT>` with the first 7 characters of `$BANDS_SHA`.

Do NOT touch the Receipts section's row 1 + row 4. Do NOT touch the Tripwires/hooks section's row 1. Do NOT touch any row in the Providers, Delegation, MV3-survivability, Observability/step-markers sections. Do NOT touch the "How this doc gets used" tail (lines 87-91 -- last paragraph).

Use the Edit tool to perform 6 row replacements + 1 row addition (or do it as a single multi-line replacement that captures the entire Receipts + Tripwires/hooks tables).

**Step 2.3 -- Verify the doc changes look right.**

Run:

```bash
grep -c "Phase 2 (FSB v0.10.0-attempt-2)" /Users/lakshmanturlapati/Desktop/FSB/automation/lattice/docs/fsb-integration-gaps.md
```

Expected: >= 2 (Receipts row 2 + Receipts row 3 reference Phase 2 in the doc-narrative SHA prefix).

Run:

```bash
grep -c "Phase 2 --" /Users/lakshmanturlapati/Desktop/FSB/automation/lattice/docs/fsb-integration-gaps.md
```

Expected: >= 5 (Tripwires/hooks rows 2-5 + lifecycle row each reference "Phase 2 --").

Run:

```bash
grep -c "Needs addition\|Needs extension" /Users/lakshmanturlapati/Desktop/FSB/automation/lattice/docs/fsb-integration-gaps.md
```

Expected: between 9 and 13 (depending on what existed pre-Phase-2 minus what Phase 2 closed). Phase 2 closes 6 rows: 2 Receipts ("Needs extension") + 4 Tripwires/hooks ("Needs addition"). The remaining "Needs addition" + "Needs extension" rows belong to Providers, MV3-survivability, and Observability/step-markers sections -- they remain UNCHANGED by Phase 2.

**Step 2.4 -- Stage and commit on Lattice's fsb-integration-experiments branch.**

Confirm branch:

```bash
cd /Users/lakshmanturlapati/Desktop/FSB/automation/lattice && git rev-parse --abbrev-ref HEAD
```

Expected: `fsb-integration-experiments`.

Stage the single modified file:

```bash
cd /Users/lakshmanturlapati/Desktop/FSB/automation/lattice && git add docs/fsb-integration-gaps.md
```

Then commit:

```bash
cd /Users/lakshmanturlapati/Desktop/FSB/automation/lattice && git commit -m "$(cat <<'EOF'
docs(fsb-integration): close Phase 2 audit rows (receipts + tripwires/hooks)

Flip 6 rows in docs/fsb-integration-gaps.md from Needs extension / Needs
addition to Covered, with Lattice commit backlinks to Phase 2's two
feat commits:

  Receipts row 2 (step-transition fields)         -> feat(receipts) Phase 2-01
  Receipts row 3 (sessionId field)                -> feat(receipts) Phase 2-01
  Tripwires/hooks row 2 (priority bands)          -> feat(contract) Phase 2-02
  Tripwires/hooks row 3 (matcher + race-with-log) -> feat(contract) Phase 2-02
  Tripwires/hooks row 4 (frozen contexts)         -> feat(contract) Phase 2-02
  Tripwires/hooks row 5 (mid-session freeze)      -> feat(contract) Phase 2-02

Plus one additive Tripwires/hooks row: HookLifecycleEvent typed union
(BEFORE_PROVIDER, AFTER_PROVIDER, BEFORE_TOOL, AFTER_TOOL) -- separate
vocabulary from tracing.ts RunEventKind by design (CONTEXT.md D-12).

Other sections (Providers, Delegation, MV3-survivability,
Observability/step-markers) are byte-unchanged by Phase 2 -- they
remain on the queue for Phases 3-6.

Ref: FSB v0.10.0-attempt-2 Phase 2
EOF
)"
```

**Step 2.5 -- Confirm NO push (D-15).**

```bash
cd /Users/lakshmanturlapati/Desktop/FSB/automation/lattice && git reflog -10 | grep -c push
```

Expected: `0`.

**Step 2.6 -- Capture the final Lattice HEAD SHA for downstream plans.**

```bash
cd /Users/lakshmanturlapati/Desktop/FSB/automation/lattice && git rev-parse HEAD > /Users/lakshmanturlapati/Desktop/FSB/automation/.planning/phases/02-lattice-tripwire-receipt-extension/02-03-LATTICE-SHA.txt
```

This is now Lattice's HEAD after ALL Phase 2 Lattice commits land. Plan 02-05 consumes this for the LATTICE-PIN.md bump.
  </action>
  <verify>
    <automated>grep -c "Phase 2 (FSB v0.10.0-attempt-2)" /Users/lakshmanturlapati/Desktop/FSB/automation/lattice/docs/fsb-integration-gaps.md && grep -c "Phase 2 --" /Users/lakshmanturlapati/Desktop/FSB/automation/lattice/docs/fsb-integration-gaps.md && cd /Users/lakshmanturlapati/Desktop/FSB/automation/lattice && git log -1 --format="%s" | grep -F "docs(fsb-integration): close Phase 2 audit rows (receipts + tripwires/hooks)" && git log -1 --format="%B" | grep -F "Ref: FSB v0.10.0-attempt-2 Phase 2" && git reflog -10 | grep -c push | grep -q "^0$" && test -f /Users/lakshmanturlapati/Desktop/FSB/automation/.planning/phases/02-lattice-tripwire-receipt-extension/02-03-LATTICE-SHA.txt</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 2 (FSB v0.10.0-attempt-2)" lattice/docs/fsb-integration-gaps.md` returns >= 2.
    - `grep -c "Phase 2 --" lattice/docs/fsb-integration-gaps.md` returns >= 5.
    - `grep -c "HookLifecycleEvent" lattice/docs/fsb-integration-gaps.md` returns 1 (new row).
    - `grep -c "createHookPipeline" lattice/docs/fsb-integration-gaps.md` returns 1 (in the bands.ts narrative).
    - Receipts row 2 and row 3 no longer match the regex `Needs extension\|Blocker`. (Verify by grepping the Receipts section.)
    - Tripwires/hooks rows 2-5 no longer match `Needs addition`.
    - Doc still has all 6 domain headers: `grep -c "^## " lattice/docs/fsb-integration-gaps.md` returns 7 (6 domain headers + 1 tail header).
    - `cd lattice && git diff HEAD~1 HEAD --name-only` returns exactly `docs/fsb-integration-gaps.md` (one file).
    - `cd lattice && git log -1 --format="%s"` returns exactly `docs(fsb-integration): close Phase 2 audit rows (receipts + tripwires/hooks)`.
    - `cd lattice && git log -1 --format="%B" | grep -c "Ref: FSB v0.10.0-attempt-2 Phase 2"` returns 1.
    - `cd lattice && git reflog -10 | grep -c push` returns 0.
    - `.planning/phases/02-lattice-tripwire-receipt-extension/02-03-LATTICE-SHA.txt` exists, 40 hex chars.
    - No emojis in the doc or the commit message.
  </acceptance_criteria>
  <done>Six closed rows flipped + one new lifecycle row added in audit doc; one Lattice commit lands with conventional subject + Ref footer; no push; SHA captured for Plan 02-05.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| src/index.ts re-export -> dist bundle | tsdown clean: true rebuild; bundle bytes change only with the new re-export entry. |
| dist/index.js -> FSB Node consumer | Bare-specifier resolution via npm file: dep symlink. |
| Audit doc text -> Lattice repo audit trail | Markdown row content travels in Lattice's local-only fsb-integration-experiments branch. |
| Lattice commit message footer | Public-ish identifier "FSB v0.10.0-attempt-2 Phase 2"; local-only this milestone. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | T (Tampering) | dist/ stale after src/index.ts edit | mitigate | tsdown is configured with `clean: true` (verified at lattice/packages/lattice/tsdown.config.ts in Phase 1). Step 1.5 explicitly runs `pnpm build`; Step 1.6 + 1.7 verify the new symbol reaches the bundle AND the FSB bare-specifier probe resolves it. |
| T-02-03-02 | T (Tampering) | catalog: literal drift (Pitfall 8) | mitigate | Step 1.1 grep returns 0 catalog: literals before proceeding. If non-zero, plan stops. |
| T-02-03-03 | I (Info disclosure) | Audit-doc row text contains FSB source code | accept | Row text contains only Lattice-side symbol names + Phase 2 narrative; no FSB source code, no PII. |
| T-02-03-04 | I (Info disclosure) | Ref: footer in Lattice commit | accept | Same as Plan 02-01 / 02-02. Public milestone identifier only. |
| T-02-03-05 | E (Elevation of privilege) | Accidental push to Lattice remote | mitigate | Step 1.10 and Step 2.5 verify `git reflog | grep -c push` returns 0 after each commit. Plan never invokes `git push`. |
| T-02-03-06 | T (Tampering) | Re-export at wrong alphabetical position (Pitfall 7) | mitigate | Step 1.2 specifies the EXACT insertion point: after `artifact` line, before `contract` line. Acceptance check via diff --stat confirms a single-line addition. |
| T-02-03-07 | T (Tampering) | Stale "Phase 3 candidate" mention in audit doc | mitigate | Step 2.2's row-flip text explicitly overwrites the stale text. Audit-doc consumers reading post-Phase-2 see the closed-row narrative, not the original "Phase 3 candidate" wording. |
| T-02-03-08 | T (Tampering) | Phase 1 smoke regression from rebuild | mitigate | Step 1.8 runs `node tests/lattice-smoke.test.js` as a regression gate. 29 PASS / 0 FAIL must hold. |

Block on: high.

</threat_model>

<verification>

After both tasks complete, verify at the plan boundary:

```bash
# index.ts has the new re-export
grep "createHookPipeline" /Users/lakshmanturlapati/Desktop/FSB/automation/lattice/packages/lattice/src/index.ts

# dist/ rebuilt and contains the new symbol
grep -c "createHookPipeline" /Users/lakshmanturlapati/Desktop/FSB/automation/lattice/packages/lattice/dist/index.js
grep -c "HookLifecycleEvent" /Users/lakshmanturlapati/Desktop/FSB/automation/lattice/packages/lattice/dist/index.d.ts

# FSB bare-specifier probe (the Plan 02-04 smoke depends on this resolving)
cd /Users/lakshmanturlapati/Desktop/FSB/automation && node -e "import('lattice').then(l => { console.log('createHookPipeline:', typeof l.createHookPipeline); if (typeof l.createHookPipeline !== 'function') process.exit(1); })"

# Phase 1 smoke regression
cd /Users/lakshmanturlapati/Desktop/FSB/automation && node tests/lattice-smoke.test.js

# Audit doc row flips
grep -c "Phase 2 (FSB v0.10.0-attempt-2)" /Users/lakshmanturlapati/Desktop/FSB/automation/lattice/docs/fsb-integration-gaps.md
grep -c "Phase 2 --" /Users/lakshmanturlapati/Desktop/FSB/automation/lattice/docs/fsb-integration-gaps.md
grep -c "HookLifecycleEvent" /Users/lakshmanturlapati/Desktop/FSB/automation/lattice/docs/fsb-integration-gaps.md

# Two Lattice commits landed
cd /Users/lakshmanturlapati/Desktop/FSB/automation/lattice && git log -3 --format="%s"
cd /Users/lakshmanturlapati/Desktop/FSB/automation/lattice && git log HEAD~2..HEAD --format="%B" | grep -c "Ref: FSB v0.10.0-attempt-2 Phase 2"
cd /Users/lakshmanturlapati/Desktop/FSB/automation/lattice && git reflog | grep -c push

# Captured SHAs
test -f /Users/lakshmanturlapati/Desktop/FSB/automation/.planning/phases/02-lattice-tripwire-receipt-extension/02-03a-LATTICE-SHA.txt
test -f /Users/lakshmanturlapati/Desktop/FSB/automation/.planning/phases/02-lattice-tripwire-receipt-extension/02-03-LATTICE-SHA.txt

# Phase 1 baseline + INV gates
grep -c "setTimeout" /Users/lakshmanturlapati/Desktop/FSB/automation/extension/ai/agent-loop.js  # MUST return 8
node /Users/lakshmanturlapati/Desktop/FSB/automation/tests/tool-definitions-parity.test.js     # MUST exit 0
git diff $(git merge-base origin/main HEAD)..HEAD --name-only | grep -E "^extension/" | wc -l  # MUST return 0
```

</verification>

<success_criteria>

- [ ] `lattice/packages/lattice/src/index.ts` has one new line at the alphabetically-correct position re-exporting createHookPipeline + types from `./contract/bands.js`.
- [ ] `lattice/packages/lattice/dist/index.js` contains `createHookPipeline` (rebuilt via tsdown clean: true).
- [ ] `lattice/packages/lattice/dist/index.d.ts` contains `HookLifecycleEvent` and `HookPipeline`.
- [ ] FSB bare-specifier probe resolves `lattice.createHookPipeline` as a function from FSB root.
- [ ] Phase 1's `tests/lattice-smoke.test.js` continues to pass (29/29 assertions).
- [ ] `lattice/docs/fsb-integration-gaps.md` Receipts rows 2 and 3 flipped to Covered with Plan 02-01 SHA backlink.
- [ ] Tripwires/hooks rows 2, 3, 4, 5 flipped to Covered with Plan 02-02 SHA backlink.
- [ ] One new Tripwires/hooks row added for HookLifecycleEvent union (Covered) with Plan 02-02 SHA backlink.
- [ ] Other doc sections (Providers, Delegation, MV3-survivability, Observability/step-markers, tail) are byte-unchanged.
- [ ] Two Lattice commits land in this plan, both with `Ref: FSB v0.10.0-attempt-2 Phase 2` footer.
- [ ] `cd lattice && git reflog | grep -c push` returns 0.
- [ ] `.planning/phases/02-lattice-tripwire-receipt-extension/02-03a-LATTICE-SHA.txt` and `02-03-LATTICE-SHA.txt` both exist.
- [ ] INV-01, INV-04 hold; zero FSB `extension/*` modifications.

</success_criteria>

<output>
After completion, create `.planning/phases/02-lattice-tripwire-receipt-extension/02-03-SUMMARY.md` capturing:
- Both Lattice commit SHAs (02-03a = re-export bump, 02-03 = audit doc flip)
- The 6 audit-doc rows flipped + 1 new row added (with old -> new diff scope)
- Verification output (FSB bare-specifier probe + Phase 1 smoke regression check)
- Confirmation of D-15 (no push), CD-03 resolution (which way the planner consolidated)
- LSDK-02..LSDK-08 marked Covered by the audit doc post-Phase 2
</output>
