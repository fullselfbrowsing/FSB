---
phase: 10-mcp-philosophy-parity-for-autopilot-driver-visual-session-li
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - extension/utils/mcp-metrics-recorder.js
  - extension/ai/agent-loop.js
  - tests/mcp-philosophy-parity-smoke.test.js
autonomous: true
requirements:
  - FINT-17
  - FINT-18
user_setup: []

must_haves:
  truths:
    - "D-04: `mcp-metrics-recorder.js` accepts `dispatcher_route: 'autopilot'` while preserving existing `tool` and `message` routes and coercing unknown routes to null."
    - "D-04: `recordDispatch` rows carry an optional top-level `drivingModel` field when supplied and leave MCP rows unchanged when absent."
    - "D-03: Autopilot records one metrics dispatch after real tool execution, after `toolResults.push(...)` and before the next iteration proceeds."
    - "D-04: Driving model identity is sourced from `session.providerConfig.providerKey` and canonical `session.providerConfig.model`."
    - "D-04: xAI reasoning tokens are copied from `response.usage.completion_tokens_details.reasoning_tokens` only when provider is `xai`; `0` remains `0`."
    - "Plan 10-01 visual-session behavior and smoke assertions stay intact."
    - "D-07: INV-04 and INV-06 remain byte-frozen."
  artifacts:
    - path: "extension/utils/mcp-metrics-recorder.js"
      provides: "autopilot dispatcher route and optional drivingModel row field"
      contains: "drivingModel"
    - path: "extension/ai/agent-loop.js"
      provides: "autopilot `recordDispatch` call site after tool execution"
      contains: "fsbMcpMetricsRecorder.recordDispatch"
    - path: "tests/mcp-philosophy-parity-smoke.test.js"
      provides: "Parts 5-8 recorder, dispatcher route, xAI reasoning token, and MCP non-regression assertions"
      contains: "Part 7"
  key_links:
    - from: "agent-loop tool result"
      to: "globalThis.fsbMcpMetricsRecorder.recordDispatch"
      via: "guarded post-toolResults.push call"
      pattern: "dispatcher_route:\\s*'autopilot'"
    - from: "session.providerConfig"
      to: "recordDispatch drivingModel"
      via: "providerKey + model + xAI-only reasoning_tokens"
      pattern: "providerConfig\\.model"
---

<objective>
Ship the metrics recorder and driving-model attribution side of MCP-philosophy parity for the autopilot driver. The executor extends recorder routing, stores an optional `drivingModel` field, emits an autopilot `recordDispatch` after real tool execution, and fills the smoke harness sections covering recorder schema, dispatcher route, xAI reasoning token behavior, and MCP non-regression.

Purpose: close FINT-17 and FINT-18 by making autopilot tool calls appear in `fsbUsageData` with the same metrics recorder path MCP uses, plus explicit provider/model attribution for the model that drove each dispatch.

Backfill note: this plan was reconstructed on 2026-06-15 from existing Phase 10 execution artifacts because 10-02-SUMMARY.md, 10-REVIEW.md, and 10-VERIFICATION.md existed but the PLAN.md artifact was absent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/LATTICE-PIN.md
@.planning/phases/10-mcp-philosophy-parity-for-autopilot-driver-visual-session-li/10-CONTEXT.md
@.planning/phases/10-mcp-philosophy-parity-for-autopilot-driver-visual-session-li/10-01-SUMMARY.md
@extension/utils/mcp-metrics-recorder.js
@extension/ai/agent-loop.js
@tests/mcp-philosophy-parity-smoke.test.js
@package.json

<interfaces>
- `extension/utils/mcp-metrics-recorder.js` owns `recordDispatch`, dispatcher route normalization, and the row schema written to `fsbUsageData`.
- `extension/ai/agent-loop.js` has a canonical successful tool-result path where `toolResults.push(...)` occurs. Insert the metrics dispatch after that push; do not instrument permission-denied/no-execution paths as successful executions.
- `session.providerConfig.providerKey` and `session.providerConfig.model` are the canonical fields for provider/model identity. Do not use stale planning shorthand `modelName`.
- The provider response is available as `response`; xAI reasoning tokens, when present, live at `response.usage.completion_tokens_details.reasoning_tokens`.
- Phase 10 preserves INV-01, INV-04, and INV-06. Do not modify MCP tool definitions, deferred-iterator callback bodies, or any file under `lattice/`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend recorder route allowlist and row schema</name>
  <files>extension/utils/mcp-metrics-recorder.js, tests/mcp-philosophy-parity-smoke.test.js</files>
  <read_first>
    - extension/utils/mcp-metrics-recorder.js `recordDispatch`
    - 10-CONTEXT.md D-04
    - 10-01-SUMMARY.md carryforward for Plan 10-02
  </read_first>
  <behavior>
    - Extend route allowlist to accept exactly `tool`, `message`, and `autopilot`.
    - Keep unknown routes coerced to null.
    - Add optional top-level `drivingModel` pass-through field to the row schema.
    - Coerce missing or non-object `input.drivingModel` to undefined so MCP rows remain clean.
    - Add smoke assertions for row schema, dispatcher route acceptance, unknown-route coercion, and MCP row non-regression.
  </behavior>
  <verify>
    <automated>grep -c "input.dispatcher_route === 'autopilot'" extension/utils/mcp-metrics-recorder.js && grep -c "drivingModel:" extension/utils/mcp-metrics-recorder.js && node tests/mcp-philosophy-parity-smoke.test.js</automated>
  </verify>
  <done>
    - Recorder accepts `dispatcher_route: 'autopilot'`.
    - Rows preserve optional `drivingModel`.
    - MCP rows have `drivingModel === undefined` when absent.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit autopilot recordDispatch after tool execution</name>
  <files>extension/ai/agent-loop.js, tests/mcp-philosophy-parity-smoke.test.js</files>
  <read_first>
    - extension/ai/agent-loop.js successful `toolResults.push(...)` path
    - extension/ai/agent-loop.js `session.providerConfig` setup and refresh logic
    - 10-CONTEXT.md D-03 and D-04
  </read_first>
  <behavior>
    - Insert a guarded `globalThis.fsbMcpMetricsRecorder.recordDispatch({...})` block after the successful tool-result push.
    - Payload includes `client: 'FSB Autopilot'`, `tool: call.name`, `requestPayload: call.args`, `success: result.success`, `dispatcher_route: 'autopilot'`, and `drivingModel`.
    - `drivingModel.provider` comes from `session.providerConfig.providerKey`.
    - `drivingModel.model_id` comes from `session.providerConfig.model`.
    - `drivingModel.reasoning_tokens` is set only for xAI responses with `completion_tokens_details.reasoning_tokens`; preserve numeric `0`.
    - Wrap the entire recorder call defensively so recorder failures cannot affect tool execution.
  </behavior>
  <verify>
    <automated>grep -c "fsbMcpMetricsRecorder.recordDispatch" extension/ai/agent-loop.js && grep -c "providerConfig.model" extension/ai/agent-loop.js && node tests/mcp-philosophy-parity-smoke.test.js</automated>
  </verify>
  <done>
    - Autopilot metrics row emits after real tool execution.
    - Provider/model/reasoning token attribution follows the canonical fields.
    - No denied/skipped tool path is mislabeled as a real dispatch.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fill smoke Parts 5-8</name>
  <files>tests/mcp-philosophy-parity-smoke.test.js</files>
  <read_first>
    - tests/mcp-philosophy-parity-smoke.test.js Parts 1-4 and placeholders
    - 10-01-SUMMARY.md carryforward notes
  </read_first>
  <behavior>
    - Fill Part 5 for `recordDispatch` row schema and `drivingModel`.
    - Fill Part 6 for `dispatcher_route: 'autopilot'` acceptance and unknown-route rejection.
    - Fill Part 7 for xAI reasoning token edge cases, including missing details, non-xAI provider, and zero-token preservation.
    - Fill Part 8 to prove MCP rows do not carry `drivingModel`.
    - Preserve Parts 1-4 and leave Parts 9-10 for Plan 10-03.
  </behavior>
  <verify>
    <automated>node tests/mcp-philosophy-parity-smoke.test.js && node tests/lattice-step-emitter-smoke.test.js && npm test && npm run build</automated>
  </verify>
  <done>
    - Smoke exits 0 with at least 30 PASS / 0 FAIL.
    - Full npm test chain exits 0.
    - Build exits 0.
  </done>
</task>

</tasks>

<verification>
- `node tests/mcp-philosophy-parity-smoke.test.js`
- `node tests/lattice-step-emitter-smoke.test.js`
- `grep -c "setTimeout" extension/ai/agent-loop.js` must return `8`.
- awk/source scan confirms neither `recordVisualSessionTick` nor `recordDispatch` lives inside a deferred-iterator callback body.
- `cd lattice && git rev-parse HEAD` remains the pinned SHA recorded in `.planning/LATTICE-PIN.md`.
- `npm test`
- `npm run build`
</verification>

<success_criteria>
- FINT-17 and FINT-18 behavior is implemented and covered by smoke tests.
- Autopilot tool calls produce recorder rows with `dispatcher_route: 'autopilot'`.
- Recorder rows carry `drivingModel` only where supplied.
- xAI `reasoning_tokens` attribution is xAI-only and preserves zero.
- Plan 10-01 behavior remains green.
- 10-02-SUMMARY.md can truthfully report Plan 10-02 complete.
</success_criteria>
