---
phase: 07-archive-fsb-custom-provider-stack
plan: 04
type: execute
wave: 4
depends_on:
  - 07-01-PLAN.md
  - 07-02-PLAN.md
  - 07-03-PLAN.md
files_modified:
  - .planning/v0.10.0-MILESTONE-AUDIT.md
  - .planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md
  - .planning/LATTICE-PIN.md
autonomous: false
requirements:
  - FINT-09

must_haves:
  truths:
    - "User has executed UAT-1 per `.planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md` Human Verification section AND reported back one of: `UAT-1 PASS`, `UAT-1 PARTIAL <details>`, `UAT-1 FAIL <details>`. The checkpoint Task 1 captures the verdict from the user."
    - "If verdict is `UAT-1 PASS`: `.planning/v0.10.0-MILESTONE-AUDIT.md` UAT-1 section UPDATED — `scores.uat_consolidated` flipped from `1 task — deferred to Phase 7 end` to `1 task — executed (passed) 2026-05-27 via Plan 07-04`; milestone-level `status` frontmatter flipped from `in_progress` to `passed`; `status_history` appended with a new entry `{date: 2026-05-27, verdict: passed, note: 'UAT-1 user-confirmed green (all 6 sub-assertions + xAI Test-Connection + autopilot iteration PASS). Plan 07-04 ceremony.'}`."
    - "If verdict is `UAT-1 PARTIAL` or `UAT-1 FAIL`: `.planning/v0.10.0-MILESTONE-AUDIT.md` `status` frontmatter STAYS `in_progress`; `status_history` appended with `{date: 2026-05-27, verdict: human_needed|gaps_found, note: '<user-reported-details>'}`; UAT-1 status flipped to `pending_execution` (PARTIAL) or `failed` (FAIL); the failing sub-assertions are added to `gaps` section + a gap-closure plan is proposed in the SUMMARY for human review."
    - "`.planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md` frontmatter `verdict` is updated to: `passed` (UAT-1 PASS), `gaps_found` (UAT-1 PARTIAL), or `failed` (UAT-1 FAIL). The body gets an appended `## UAT-1 Execution Record` section capturing: date executed; user verdict; user-reported observations (verbatim from the user's reply); screenshots referenced (file paths only — actual files outside scope of this plan)."
    - "`.planning/LATTICE-PIN.md` Phase 7 row Notes cell PLACEHOLDERS (`<07-02-sha>` for Plan 07-02 commit, `<07-03-sha>` for Plan 07-03 commit, `<07-04-sha>` for THIS commit) are BACKFILLED with the actual short SHAs captured from `git log --oneline | head -20`. Frontmatter `last_updated` bumped if it isn't already today's date. `current_lattice_sha` UNCHANGED at `e95067bfa87ed1b75838fc3b3ef217a3b01acbd3` (INV-06 holds across the entire Phase 7)."
    - "Plan 07-04 lands as ONE FSB commit (preferred) OR two FSB commits (one for the user-verdict capture + audit/verification update + LATTICE-PIN backfill; one for the SUMMARY) per executor preference. The first commit message reflects the verdict: PASS verdict commit message = `docs(07-04): UAT-1 PASS — flip v0.10.0 milestone status to passed + backfill LATTICE-PIN Phase 7 SHAs`; PARTIAL/FAIL commit message = `docs(07-04): UAT-1 <PARTIAL|FAIL> — record verdict + gaps + retain in_progress milestone status`."
    - "ZERO production code touched in this plan."
    - "ZERO Lattice-side commits (INV-06 holds; current_lattice_sha UNCHANGED)."
  artifacts:
    - path: ".planning/v0.10.0-MILESTONE-AUDIT.md"
      provides: "UAT-1 status flipped per user verdict; milestone status flipped to passed only on UAT-1 PASS; status_history appended with the new entry"
      contains: "UAT-1"
    - path: ".planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md"
      provides: "frontmatter verdict updated; body appended with UAT-1 Execution Record section capturing user verdict + observations"
      contains: "UAT-1 Execution Record"
    - path: ".planning/LATTICE-PIN.md"
      provides: "Phase 7 row Notes cell SHA placeholders backfilled with actual short SHAs; last_updated bumped"
      contains: "Phase 7"
  key_links:
    - from: "User UAT-1 verdict (chat reply)"
      to: ".planning/v0.10.0-MILESTONE-AUDIT.md status_history"
      via: "Task 1 checkpoint captures verdict; Task 2 records it"
      pattern: "UAT-1"
    - from: ".planning/LATTICE-PIN.md Phase 7 row"
      to: "Plan 07-02 + 07-03 + 07-04 SHAs from git log"
      via: "backfill placeholders with actual SHAs"
      pattern: "07-0[234]-sha"
---

<objective>
Wave 4 (final wave) of Phase 7: post-UAT ceremony. Captures the user's UAT-1 verdict (PASS / PARTIAL / FAIL), updates `.planning/v0.10.0-MILESTONE-AUDIT.md` accordingly (flips milestone status to passed ONLY on UAT-1 PASS), updates `.planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md` with the UAT-1 Execution Record, and backfills the LATTICE-PIN.md Phase 7 row Notes cell with the actual short SHAs for Plans 07-02, 07-03, 07-04.

Task 1 is a `checkpoint:human-verify` gate that waits for the user to report the verdict. Subsequent tasks are autonomous given the verdict.

This plan does NOT touch production code OR Lattice-side files.

Output: 3 modified files (v0.10.0-MILESTONE-AUDIT.md, 07-VERIFICATION.md, LATTICE-PIN.md). 1-2 FSB commits. Milestone-end gate closed (on UAT-1 PASS) or held open with recorded gap (on PARTIAL/FAIL).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/LATTICE-PIN.md
@.planning/v0.10.0-MILESTONE-AUDIT.md
@.planning/phases/07-archive-fsb-custom-provider-stack/07-CONTEXT.md
@.planning/phases/07-archive-fsb-custom-provider-stack/07-01-SUMMARY.md
@.planning/phases/07-archive-fsb-custom-provider-stack/07-02-SUMMARY.md
@.planning/phases/07-archive-fsb-custom-provider-stack/07-03-SUMMARY.md
@.planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md

<interfaces>
<!-- v0.10.0-MILESTONE-AUDIT.md frontmatter current state (lines 1-20). -->

```yaml
# (yaml document delimiter removed for parser safety)
milestone: v0.10.0
milestone_name: Autopilot via Lattice SDK (attempt 2)
audited: 2026-05-26
last_revised: 2026-05-27
status: in_progress
status_history:
  - date: 2026-05-26
    verdict: tech_debt
    note: "All 5 phases passed; 35/35 in-scope requirements satisfied; 3 documented carryforward integration gaps per D-22; 1 consolidated UAT-1 outstanding."
  - date: 2026-05-27
    verdict: in_progress
    note: "Milestone re-opened via /gsd-autonomous discussion. ... UAT-1 deferred to Phase 7 end. Phase 8 (delegation primitive) renumbered + parked for v0.11.0+."
scores:
  ...
  uat_consolidated: 1 task — deferred to Phase 7 end
...
# (yaml document delimiter removed for parser safety)
```

<!-- TARGET after UAT-1 PASS verdict: -->

```yaml
# (yaml document delimiter removed for parser safety)
milestone: v0.10.0
milestone_name: Autopilot via Lattice SDK (attempt 2)
audited: 2026-05-26
last_revised: 2026-05-27
status: passed                        # was: in_progress
status_history:
  - {Phase 5 row preserved}
  - {Phase 6-7 in_progress row preserved}
  - date: 2026-05-27
    verdict: passed                   # NEW entry on UAT-1 PASS
    note: "UAT-1 user-confirmed green (all 6 sub-assertions + xAI Test-Connection sub-test + autopilot iteration sub-test PASS). Plan 07-04 ceremony flipped milestone status."
scores:
  ...
  uat_consolidated: 1 task — executed (passed) 2026-05-27 via Plan 07-04   # was: 1 task — deferred to Phase 7 end
...
# (yaml document delimiter removed for parser safety)
```

<!-- TARGET after UAT-1 PARTIAL or FAIL: -->

```yaml
# (yaml document delimiter removed for parser safety)
milestone: v0.10.0
milestone_name: Autopilot via Lattice SDK (attempt 2)
audited: 2026-05-26
last_revised: 2026-05-27
status: in_progress                   # UNCHANGED
status_history:
  - {prior rows preserved}
  - date: 2026-05-27
    verdict: <human_needed | gaps_found>   # human_needed (PARTIAL) or gaps_found (FAIL)
    note: "UAT-1 <PARTIAL|FAIL>: <user-reported-details verbatim>. Plan 07-04 records verdict; gap-closure plan recommended in 07-04-SUMMARY.md."
scores:
  ...
  uat_consolidated: 1 task — <pending_execution | failed> (Plan 07-04 ceremony recorded verdict)
...
gaps:
  uat:                                # NEW gaps subsection on PARTIAL/FAIL
    - id: UAT-1-<a..f>
      description: "<which sub-assertion failed + user observation>"
      severity: <blocker|major|minor>
...
# (yaml document delimiter removed for parser safety)
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1 (checkpoint): User runs UAT-1 per 07-VERIFICATION.md Human Verification section and reports verdict</name>
  <what-built>
    Wave 1-3 are all complete:
    - Plan 07-01 stripped `FSB_LATTICE_PROVIDER_BRIDGE_ENABLED` from 4 files (agent-loop.js + bridge.js + smoke + regression test); bridge unconditional; `npm test` exit 0
    - Plan 07-02 flipped REQUIREMENTS.md FINT-09 to Complete (Strategy B form) + appended Phase 7 row to LATTICE-PIN.md (current_lattice_sha UNCHANGED at `e95067bf...`)
    - Plan 07-03 generated the consolidated UAT-1 6-sub-assertion procedure into `.planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md` Human Verification section (>= 80 lines, includes preparation + Chrome reload steps + 6 sub-assertions table + xAI Test-Connection sub-test + autopilot iteration sub-test + 3-verdict reporting protocol)

    Phase 7 production code + documentation are GREEN end-to-end. The milestone-end gate (UAT-1 single Chrome MV3 reload session) is the ONLY remaining check.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md` in your editor.
    2. Follow the `## Human Verification (UAT-1 — milestone-end gate)` section step by step in Chrome (or Brave / Edge):
       - **Preparation**: No `npm run build` needed for Phase 7 (per the doc).
       - **Chrome MV3 reload procedure**: load unpacked at `/Users/lakshmanturlapati/Desktop/FSB/automation/extension/` (or click reload on the FSB card if already loaded); open SW DevTools console; open offscreen page DevTools console.
       - **6 sub-assertions table**: verify each of a-f per the Expected / What to Look For / What to Avoid columns.
       - **xAI Test-Connection sub-test**: paste fresh xAI API key; click Test Connection; expect success in ~5s; expect NO `xa***cy` masked-echo error.
       - **Autopilot iteration sub-test**: load a simple page; start autopilot with `say 'hello' once and stop`; expect >= 1 iteration step; observe SW console for the `lattice-provider-execute` envelope flow.
    3. Determine the verdict:
       - **`UAT-1 PASS`**: all 6 sub-assertions pass + both sub-tests green.
       - **`UAT-1 PARTIAL <details>`**: some sub-assertions pass, others fail. List which failed + what you observed.
       - **`UAT-1 FAIL <details>`**: one or more critical sub-assertions fail blocking the milestone. Describe what broke (which assertion, what error, which page/UI).
    4. Reply in this conversation with the verdict + details. Optional: paste screenshots (sidepanel iteration history, SW console bridge envelope flow).
  </how-to-verify>
  <resume-signal>
    Reply with: `UAT-1 PASS` OR `UAT-1 PARTIAL <details>` OR `UAT-1 FAIL <details>`. The verdict + details will be captured verbatim into the next task's edits to v0.10.0-MILESTONE-AUDIT.md + 07-VERIFICATION.md.
  </resume-signal>
  <files>(none — checkpoint task; no file writes in this step. Subsequent Tasks 2-4 record the user verdict into v0.10.0-MILESTONE-AUDIT.md + 07-VERIFICATION.md + LATTICE-PIN.md)</files>
  <action>
    HALT execution and present the `<what-built>` + `<how-to-verify>` blocks above to the user. WAIT for the user to reply with one of the three verdict strings (`UAT-1 PASS` / `UAT-1 PARTIAL <details>` / `UAT-1 FAIL <details>`) as specified in `<resume-signal>`. Do NOT proceed to Task 2 until the verdict is captured. Do NOT attempt to run the UAT-1 procedure programmatically — it requires manual Chrome interaction per CONTEXT.md `<decisions>` Q2 lock.
  </action>
  <verify>
    <automated>echo "MANUAL — checkpoint awaits user verdict from UAT-1 execution per 07-VERIFICATION.md Human Verification section"</automated>
  </verify>
  <done>
    User has replied with `UAT-1 PASS`, `UAT-1 PARTIAL <details>`, or `UAT-1 FAIL <details>`. The verdict string + any details are captured verbatim and ready for Task 2 to write into v0.10.0-MILESTONE-AUDIT.md status_history + 07-VERIFICATION.md UAT-1 Execution Record.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update .planning/v0.10.0-MILESTONE-AUDIT.md per user verdict — flip milestone status to passed (UAT-1 PASS only) OR record gap (UAT-1 PARTIAL/FAIL); append status_history entry; update scores.uat_consolidated</name>
  <read_first>
    - .planning/v0.10.0-MILESTONE-AUDIT.md entire file (focus on frontmatter lines 1-117: status + status_history + scores + gaps)
    - User verdict captured in Task 1 (verbatim)
    - .planning/phases/07-archive-fsb-custom-provider-stack/07-03-SUMMARY.md (confirm UAT-1 procedure was the one user executed)
  </read_first>
  <files>.planning/v0.10.0-MILESTONE-AUDIT.md</files>
  <behavior>
    - **On UAT-1 PASS:**
      - frontmatter `status:` flipped `in_progress` -> `passed`
      - frontmatter `last_revised:` bumped to 2026-05-27 (or actual commit date)
      - `status_history:` appended with `- date: 2026-05-27; verdict: passed; note: "UAT-1 user-confirmed green (all 6 sub-assertions + xAI Test-Connection sub-test + autopilot iteration sub-test PASS). Plan 07-04 ceremony flipped milestone status."`
      - `scores.uat_consolidated:` updated from `1 task — deferred to Phase 7 end` to `1 task — executed (passed) 2026-05-27 via Plan 07-04`
      - `scores.requirements_in_scope_phases_6_7:` updated from `0/3 (pending — FINT-07/08/09)` to `3/3 (FINT-07, FINT-08, FINT-09 all Complete)`
      - `scores.phases:` updated from `5/7 complete (Phases 6 + 7 pending discuss → plan → execute)` to `7/7 complete`
    - **On UAT-1 PARTIAL:**
      - frontmatter `status:` STAYS `in_progress`
      - `status_history:` appended with `- date: 2026-05-27; verdict: human_needed; note: "UAT-1 PARTIAL: <user-reported-details verbatim>. Plan 07-04 records verdict; partial sub-assertions tracked in gaps.uat for follow-on gap-closure plan."`
      - `scores.uat_consolidated:` updated to `1 task — pending_execution (Plan 07-04 recorded PARTIAL verdict)`
      - `gaps.uat:` subsection ADDED (if not present) with one entry per failing sub-assertion: `{id: UAT-1-<a..f>, description: <which sub-assertion + user observation>, severity: major}`
    - **On UAT-1 FAIL:**
      - frontmatter `status:` STAYS `in_progress`
      - `status_history:` appended with `- date: 2026-05-27; verdict: gaps_found; note: "UAT-1 FAIL: <user-reported-details verbatim>. Plan 07-04 records verdict; milestone status NOT flipped. Recommended action: rollback Plan 07-01 (restore flag wrapper + legacy fallback) OR escalate to debug session."`
      - `scores.uat_consolidated:` updated to `1 task — failed (Plan 07-04 recorded FAIL verdict)`
      - `gaps.uat:` subsection ADDED with entries per failing assertion + `severity: blocker`
    - All OTHER frontmatter sections + body BYTE-FROZEN
  </behavior>
  <action>
    Step 1 — Read `.planning/v0.10.0-MILESTONE-AUDIT.md` frontmatter (lines 1-117). Capture the verbatim user verdict from Task 1's checkpoint resume.

    Step 2 — Branch on the verdict:

    **If `UAT-1 PASS`:**
    - Edit frontmatter line 6: `status: in_progress` -> `status: passed`
    - Edit frontmatter line 5: `last_revised: 2026-05-27` (bump if needed)
    - Append a new entry to `status_history:` (immediately after the existing 2026-05-27 in_progress entry):
      ```
        - date: 2026-05-27
          verdict: passed
          note: "UAT-1 user-confirmed green (all 6 sub-assertions + xAI Test-Connection sub-test + autopilot iteration sub-test PASS). Plan 07-04 ceremony flipped milestone status."
      ```
    - Edit `scores.uat_consolidated:` to `1 task — executed (passed) 2026-05-27 via Plan 07-04`
    - Edit `scores.requirements_in_scope_phases_6_7:` to `3/3 (FINT-07, FINT-08, FINT-09 all Complete)`
    - Edit `scores.phases:` to `7/7 complete`

    **If `UAT-1 PARTIAL <details>`:**
    - Leave `status: in_progress` UNCHANGED
    - Append `status_history:` with verdict `human_needed` + the verbatim user details in the note
    - Edit `scores.uat_consolidated:` to `1 task — pending_execution (Plan 07-04 recorded PARTIAL verdict; see gaps.uat for failing sub-assertions)`
    - Add `gaps.uat:` subsection (under the existing `gaps:` block) with one entry per failing sub-assertion. Use the user's reported sub-assertion IDs (e.g., UAT-1-a, UAT-1-d).

    **If `UAT-1 FAIL <details>`:**
    - Same as PARTIAL but verdict = `gaps_found`; gaps entries get severity `blocker`; note explicitly recommends rollback or escalation.

    Step 3 — In all 3 branches: do NOT modify any other frontmatter section (audited, milestone, milestone_name) or any of the body sections (Requirements Coverage, Phase-by-Phase Verification Status, Cross-Phase Integration, End-to-End Flows, Invariant Compliance, Nyquist Validation Coverage, UAT Carryforwards, Verdict). Body sections are PURELY narrative; the frontmatter is the audit source of truth.

    Step 4 — Verify with the verification commands.
  </action>
  <verify>
    <automated>grep "^status:" .planning/v0.10.0-MILESTONE-AUDIT.md; grep -c "uat_consolidated:" .planning/v0.10.0-MILESTONE-AUDIT.md; grep -c "Plan 07-04" .planning/v0.10.0-MILESTONE-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - On UAT-1 PASS: `grep "^status:" .planning/v0.10.0-MILESTONE-AUDIT.md` returns `status: passed`
    - On UAT-1 PARTIAL or FAIL: `grep "^status:" .planning/v0.10.0-MILESTONE-AUDIT.md` returns `status: in_progress` (UNCHANGED)
    - `grep -c "Plan 07-04" .planning/v0.10.0-MILESTONE-AUDIT.md` returns `>= 1` (status_history + scores reference Plan 07-04)
    - `grep -c "uat_consolidated:" .planning/v0.10.0-MILESTONE-AUDIT.md` returns `1`
    - On UAT-1 PASS: `grep -c "executed (passed)" .planning/v0.10.0-MILESTONE-AUDIT.md` returns `>= 1`
    - On UAT-1 PARTIAL: `grep -c "pending_execution" .planning/v0.10.0-MILESTONE-AUDIT.md` returns `>= 1`
    - On UAT-1 FAIL: `grep -c "failed" .planning/v0.10.0-MILESTONE-AUDIT.md` returns `>= 1` (in the uat_consolidated score)
    - Pre-existing status_history entries preserved: `grep -c "2026-05-26" .planning/v0.10.0-MILESTONE-AUDIT.md` returns `>= 1`; `grep -c "tech_debt" .planning/v0.10.0-MILESTONE-AUDIT.md` returns `>= 1`
    - Pre-existing gaps + tech_debt + nyquist + orphaned_requirements subsections BYTE-FROZEN: `grep -c "G1\\|G2\\|G3" .planning/v0.10.0-MILESTONE-AUDIT.md` returns `>= 1`; `grep -c "LSDK-LL..P" .planning/v0.10.0-MILESTONE-AUDIT.md` returns `>= 1`
  </acceptance_criteria>
  <done>
    .planning/v0.10.0-MILESTONE-AUDIT.md frontmatter updated per user verdict; status_history appended; scores.uat_consolidated updated; on UAT-1 PASS milestone status flipped to passed + scores updated to 3/3 + 7/7; on UAT-1 PARTIAL or FAIL milestone status stays in_progress + gaps recorded; body sections + other frontmatter BYTE-FROZEN.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update .planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md with UAT-1 Execution Record section + frontmatter verdict update</name>
  <read_first>
    - .planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md current state (created by Plan 07-03)
    - User verdict + observations from Task 1 checkpoint
  </read_first>
  <files>.planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md</files>
  <behavior>
    - Frontmatter `verdict:` updated:
      - UAT-1 PASS -> `verdict: passed`
      - UAT-1 PARTIAL -> `verdict: gaps_found`
      - UAT-1 FAIL -> `verdict: failed`
    - Frontmatter `gated_on:` updated: `"UAT-1 user execution + verdict report"` -> `"UAT-1 executed 2026-05-27; verdict: <PASS|PARTIAL|FAIL>"`
    - Body APPENDED with `## UAT-1 Execution Record` section containing:
      - `**Date executed:** 2026-05-27`
      - `**Verdict:** UAT-1 <PASS|PARTIAL|FAIL>`
      - `**User observations:** <verbatim from user reply; quoted block>`
      - `**Screenshots:** (referenced if any; file paths only — actual capture is user's responsibility)`
      - `**Per-assertion results:** (table — only if PARTIAL or FAIL; lists which a-f sub-assertions passed/failed + per-failure observation)`
      - `**Next action:** (PASS -> Plan 07-04 Task 4 backfills LATTICE-PIN SHAs; PARTIAL -> recommend follow-on gap-closure plan; FAIL -> recommend rollback OR escalation)`
    - All OTHER sections (Phase Summary, Automated Verification, Cross-Phase Invariants, Human Verification, User Verdict Reporting, Post-UAT Ceremony) BYTE-FROZEN
  </behavior>
  <action>
    Step 1 — Edit the frontmatter `verdict:` field per the verdict branch.

    Step 2 — Edit the frontmatter `gated_on:` field per the verdict branch.

    Step 3 — Append the `## UAT-1 Execution Record` section to the end of the file. Use a markdown sub-section structure:

    ```markdown
    ## UAT-1 Execution Record

    **Date executed:** 2026-05-27 (or actual user-reported date)

    **Verdict:** UAT-1 <PASS | PARTIAL | FAIL>

    **User observations (verbatim from user reply):**

    > <user-reported-details verbatim, quoted>

    **Screenshots:** <list any user-mentioned screenshot file paths; otherwise note "none captured">

    **Per-assertion results:** <only on PARTIAL or FAIL — markdown table mapping each of a-f to PASS / FAIL + observation>

    **Next action:** <PASS: "Plan 07-04 Task 4 backfills LATTICE-PIN.md Phase 7 row Notes cell SHAs; milestone is closed."; PARTIAL: "Recommend follow-on gap-closure plan addressing the failing sub-assertions: <list>. Run `/gsd-plan-phase --gaps 7` to scope."; FAIL: "Recommend either (1) rollback Plan 07-01 via `git revert <07-01-sha>` restoring the flag wrapper + legacy fallback, OR (2) escalate to a debug session investigating the failure root cause. Milestone status remains `in_progress` until resolution.">
    ```

    Step 4 — Verify with the verification commands.
  </action>
  <verify>
    <automated>grep "^verdict:" .planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md; grep -c "## UAT-1 Execution Record" .planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md; grep -c "Plan 07-04" .planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "^verdict:" .planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md` returns one of: `verdict: passed`, `verdict: gaps_found`, `verdict: failed`
    - `grep -c "## UAT-1 Execution Record" .planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md` returns `1`
    - `grep -c "Date executed:" .planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md` returns `>= 1`
    - `grep -c "Verdict: UAT-1" .planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md` returns `>= 1`
    - `grep -c "User observations" .planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md` returns `>= 1`
    - Plan 07-03 generated sections preserved: `grep -c "## Phase Summary" .planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md` returns `1`; `grep -c "## Human Verification" .planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md` returns `1`
  </acceptance_criteria>
  <done>
    07-VERIFICATION.md frontmatter verdict updated per user verdict; UAT-1 Execution Record appended with date + verdict + observations + per-assertion table (if PARTIAL/FAIL) + next action; all Plan 07-03-generated sections BYTE-FROZEN.
  </done>
</task>

<task type="auto">
  <name>Task 4: Backfill .planning/LATTICE-PIN.md Phase 7 row Notes cell SHA placeholders (07-02-sha, 07-03-sha, 07-04-sha) with actual short SHAs; bump last_updated</name>
  <read_first>
    - .planning/LATTICE-PIN.md current state (Phase 7 row at line ~30 with placeholders)
    - Capture from `git log --oneline --all | head -30`: the Plan 07-02 commit SHA (from `docs(07): finalize FINT-09 + bump LATTICE-PIN ...`); the Plan 07-03 commit SHA (from `docs(07-03): generate UAT-1 ...`); the Plan 07-04 commit SHA (this commit — captured AFTER Task 2 + Task 3 commit; backfill BEFORE THIS commit OR via amend if executor is comfortable; preferred: this Task 4 is its own commit so it can reference the prior commits)
  </read_first>
  <files>.planning/LATTICE-PIN.md</files>
  <behavior>
    - Frontmatter `current_lattice_sha:` UNCHANGED at `e95067bfa87ed1b75838fc3b3ef217a3b01acbd3` (INV-06)
    - Frontmatter `last_updated:` BUMPED to today's date (commit date)
    - Phase 7 row Notes cell: 3 placeholders BACKFILLED with actual short SHAs:
      - `<07-02-sha>` -> actual SHA (e.g., `a1b2c3d4`)
      - `<07-03-sha>` -> actual SHA (e.g., `b2c3d4e5`)
      - `<07-04-sha>` -> actual SHA of Task 2 + 3 commit (e.g., `c3d4e5f6`)
    - Phase 1-6 rows + schema sections BYTE-FROZEN
    - If verdict is UAT-1 PASS, the Phase 7 row Notes cell narrative ALSO gets a new sentence appended: `UAT-1 milestone-end gate PASSED 2026-05-27 (user-confirmed; v0.10.0-MILESTONE-AUDIT.md status flipped in_progress -> passed via Plan 07-04 Task 2).` Otherwise (PARTIAL/FAIL): `UAT-1 milestone-end gate <PARTIAL|FAIL> 2026-05-27 (user-reported; v0.10.0-MILESTONE-AUDIT.md status stays in_progress; gap-closure plan or rollback recommended per 07-VERIFICATION.md UAT-1 Execution Record).`
  </behavior>
  <action>
    Step 1 — Run `git log --oneline | head -20` (NOT the actual `git` tool inside this plan — the executor's bash environment) to capture the recent commit SHAs. Identify:
    - Plan 07-02's commit SHA (commit message `docs(07): finalize FINT-09 + bump LATTICE-PIN for Phase 7 flag removal`)
    - Plan 07-03's commit SHA (commit message `docs(07-03): generate UAT-1 6-sub-assertion procedure into 07-VERIFICATION.md`)
    - Plan 07-04 Task 2 + Task 3's commit SHA (commit message from Task 2 that captured the verdict — PASS / PARTIAL / FAIL variant)

    Step 2 — Read `.planning/LATTICE-PIN.md` and locate the Phase 7 row's Notes cell containing the 3 `<07-0X-sha>` placeholders.

    Step 3 — Apply Edit operations replacing each placeholder with the actual short SHA. Edit also bumps `last_updated:` in the frontmatter.

    Step 4 — On UAT-1 PASS: append the success sentence to the Phase 7 row Notes cell. On UAT-1 PARTIAL/FAIL: append the corresponding sentence per the `<behavior>` block.

    Step 5 — Do NOT modify any other line. `current_lattice_sha:` STAYS at `e95067bfa87ed1b75838fc3b3ef217a3b01acbd3` (INV-06 hard-gated by Plan 06-05's INV byte-freeze regression smoke).

    Step 6 — Verify with the verification commands.
  </action>
  <verify>
    <automated>grep -c "<07-0[1234]-sha>" .planning/LATTICE-PIN.md; grep "current_lattice_sha:" .planning/LATTICE-PIN.md; grep -c "UAT-1 milestone-end gate" .planning/LATTICE-PIN.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<07-0[1234]-sha>" .planning/LATTICE-PIN.md` returns `0` (all placeholders backfilled — including the 07-01-sha that Plan 07-02 already backfilled if any remained)
    - `grep "current_lattice_sha:" .planning/LATTICE-PIN.md` shows `e95067bfa87ed1b75838fc3b3ef217a3b01acbd3` (UNCHANGED — INV-06)
    - `grep -c "UAT-1 milestone-end gate" .planning/LATTICE-PIN.md` returns `>= 1` (Phase 7 row narrative captures the milestone-end gate outcome)
    - Phase 1-6 rows BYTE-FROZEN: `grep -c "^| Phase [1-6]" .planning/LATTICE-PIN.md` returns `6`
    - Phase 7 row preserved: `grep -c "^| Phase 7" .planning/LATTICE-PIN.md` returns `1`
    - `npm test` exits 0 (Plan 06-05 INV-06 byte-freeze regression smoke asserts current_lattice_sha == e95067bf... from inside the smoke; bumping it would fail; we did not bump it; smoke stays green)
  </acceptance_criteria>
  <done>
    LATTICE-PIN.md Phase 7 row Notes cell SHA placeholders backfilled with actual short SHAs for Plans 07-02, 07-03, 07-04; UAT-1 milestone-end gate outcome sentence appended; current_lattice_sha UNCHANGED at e95067bf...; last_updated bumped; Phase 1-6 rows + schema sections + frontmatter (except last_updated) BYTE-FROZEN; `npm test` exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan ↔ user verdict | The checkpoint Task 1 captures the user verdict verbatim. If the user mis-reports the verdict (e.g., says PASS when an assertion actually failed), the audit-trail records the user's claim; future re-runs can re-verify. |
| Plan ↔ INV-06 frontmatter byte-freeze | `current_lattice_sha` is the hard-gated invariant Plan 06-05 INV byte-freeze regression smoke asserts from inside `npm test`. Task 4 MUST NOT touch it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Tampering | user verdict recording accuracy | accept | Verdict is captured verbatim in status_history note + 07-VERIFICATION.md UAT-1 Execution Record. Future audit can compare against observed behaviour. |
| T-07-04-02 | Repudiation | premature milestone status flip without UAT-1 PASS | mitigate | Task 2's branch logic explicitly gates `status: passed` flip on verdict == PASS. Acceptance criterion grep enforces `status: passed` ONLY on PASS path. |
| T-07-04-03 | Tampering | accidental `current_lattice_sha` bump | mitigate | Task 4 acceptance criterion grep enforces `current_lattice_sha:` == `e95067bf...`; Plan 06-05 INV-06 byte-freeze smoke catches drift from inside `npm test`. |
| T-07-04-04 | Denial of service | UAT-1 PARTIAL/FAIL blocks milestone close indefinitely | accept | This is by design — milestone status stays `in_progress` until UAT-1 passes. The recommended next action (gap-closure plan or rollback) is documented in 07-VERIFICATION.md Next Action subsection per Task 3. |
</threat_model>

<verification>
Plan 07-04 deliverable verification (run after user verdict captured):

```bash
# v0.10.0-MILESTONE-AUDIT.md status reflects verdict
grep "^status:" .planning/v0.10.0-MILESTONE-AUDIT.md
# On UAT-1 PASS: status: passed
# On UAT-1 PARTIAL/FAIL: status: in_progress

# UAT-1 Execution Record captured
grep -c "## UAT-1 Execution Record" .planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md   # 1
grep "^verdict:" .planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md

# LATTICE-PIN backfilled + INV-06 holds
grep -c "<07-0[1234]-sha>" .planning/LATTICE-PIN.md                                                            # 0
grep "current_lattice_sha:" .planning/LATTICE-PIN.md                                                           # e95067bf... (UNCHANGED)

# Zero production code touched
git status --porcelain extension/ tests/                                                                       # empty

# Smoke chain still green (INV-06 byte-freeze smoke asserts current_lattice_sha from inside)
npm test                                                                                                       # exit 0
```
</verification>

<success_criteria>
- User has executed UAT-1 in Chrome per 07-VERIFICATION.md Human Verification section and reported a verdict
- `.planning/v0.10.0-MILESTONE-AUDIT.md` reflects the verdict: on PASS status flips `in_progress` -> `passed` + scores updated to 3/3 + 7/7 + UAT-1 executed-passed; on PARTIAL/FAIL status stays `in_progress` + gaps recorded
- `.planning/phases/07-archive-fsb-custom-provider-stack/07-VERIFICATION.md` frontmatter verdict updated + UAT-1 Execution Record appended with verbatim user observations + per-assertion table (if PARTIAL/FAIL) + next action
- `.planning/LATTICE-PIN.md` Phase 7 row SHA placeholders backfilled; `current_lattice_sha` UNCHANGED at `e95067bf...` (INV-06 holds); `last_updated` bumped
- ZERO production code touched
- ZERO Lattice-side commits (INV-06 holds)
- 1-2 FSB commits (preferred: 1 atomic commit covering Tasks 2 + 3 + 4; OR 1 commit for Tasks 2+3 then 1 commit for Task 4 backfill)
- On UAT-1 PASS: v0.10.0 milestone CLOSED end-to-end; ready for `/gsd-audit-milestone v0.10.0` re-aggregation if desired
- On UAT-1 PARTIAL/FAIL: milestone CLOSURE BLOCKED with recorded gap; 07-VERIFICATION.md UAT-1 Execution Record Next Action specifies follow-on plan
</success_criteria>

<output>
After completion, create `.planning/phases/07-archive-fsb-custom-provider-stack/07-04-SUMMARY.md` per the template at $HOME/.claude/get-shit-done/templates/summary.md.

Include:
- Captured user verdict + verbatim details from Task 1 checkpoint
- v0.10.0-MILESTONE-AUDIT.md changes summary (status flip vs unchanged; status_history entry appended; scores updates)
- 07-VERIFICATION.md changes summary (frontmatter verdict + UAT-1 Execution Record appended)
- LATTICE-PIN.md changes summary (placeholders backfilled with actual short SHAs; current_lattice_sha UNCHANGED)
- Phase 7 closure signal: GREEN end-to-end (on PASS) OR HELD OPEN with recorded gap (on PARTIAL/FAIL)
- Milestone closure signal: v0.10.0 PASSED (on UAT-1 PASS) OR v0.10.0 STILL IN PROGRESS (on PARTIAL/FAIL)
- Next user action: `/gsd-audit-milestone v0.10.0` (on PASS, re-aggregate) OR follow-on gap-closure plan / rollback (on PARTIAL/FAIL per 07-VERIFICATION.md Next Action)
</output>
