---
phase: 05-mv3-survivability-bundler
plan: 06
type: execute
wave: 4
depends_on:
  - 05-03-PLAN.md
  - 05-04-PLAN.md
  - 05-05-PLAN.md
files_modified:
  - .planning/LATTICE-PIN.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - LSDK-19
  - LSDK-20
  - LSDK-21
  - LSDK-22
  - FINT-03
  - FINT-04
  - FINT-05
  - FINT-06
requirements_addressed:
  - LSDK-19
  - LSDK-20
  - LSDK-21
  - LSDK-22
  - FINT-03
  - FINT-04
  - FINT-05
  - FINT-06

must_haves:
  truths:
    - "`.planning/LATTICE-PIN.md` frontmatter `current_lattice_sha` advances from Phase 4 HEAD `f1c943bd9398daeda2ccf92a3d0c2bc004a0379f` to the new Phase 5 Lattice HEAD (captured from `cd lattice && git rev-parse fsb-integration-experiments` after Plan 05-03 commits)."
    - "`.planning/LATTICE-PIN.md` per-phase log gains a NEW Phase 5 row referencing all 3 Phase 5 Lattice commits: Plan 05-02 (`feat(runtime): add MV3-survivability adapter contract + noop reference impl`) + Plan 05-03 re-export + Plan 05-03 audit-doc closure."
    - "`.planning/REQUIREMENTS.md` LSDK section gains entries for LSDK-19 (SurvivabilityAdapter interface), LSDK-20 (createNoopSurvivabilityAdapter ref impl), LSDK-21 (public-surface re-export), LSDK-22 (audit-doc closure) -- each marked `[x] Complete (Phase 05 Plan ...)` with Lattice commit short-SHAs."
    - "`.planning/REQUIREMENTS.md` FINT section gains entries for FINT-03 (esbuild bundler infra), FINT-04 (offscreen Lattice host), FINT-05 (FSB standalone runtime adapter), FINT-06 (Node-side survivability smoke) -- each marked Complete with FSB commit short-SHAs."
    - "`.planning/REQUIREMENTS.md` Traceability table gains 8 new rows (one per new REQ-ID); total v1 count advances from 21 (Phase 4) to 29 (+8)."
    - "Plan 05-06 lands as ONE FSB commit `docs(05): bump LATTICE-PIN + finalize LSDK + FINT traceability` with Ref footer + plan-ID. NO Lattice commits in this plan (all Lattice work landed in Plans 05-02 + 05-03)."
    - "Phase 1+2+3+4 FSB smokes BYTE-FROZEN through this plan (no test chain modification)."
  artifacts:
    - path: ".planning/LATTICE-PIN.md"
      provides: "Phase 5 SHA bump + Phase 5 row in per-phase log with 3 Lattice commit short-SHAs (Plan 05-02 + Plan 05-03 re-export + Plan 05-03 audit-doc)"
      contains: "Phase 5"
    - path: ".planning/REQUIREMENTS.md"
      provides: "LSDK-19..22 + FINT-03..06 entries + 8 new traceability rows; total v1 = 29"
      contains: "LSDK-19"
  key_links:
    - from: ".planning/LATTICE-PIN.md"
      to: "Lattice fsb-integration-experiments HEAD"
      via: "current_lattice_sha frontmatter field"
      pattern: "current_lattice_sha"
    - from: ".planning/REQUIREMENTS.md"
      to: "Phase 5 Plan 05-02 + 05-03 + 05-04 + 05-05 commits"
      via: "REQ-ID -> commit short-SHA citations in section + traceability table"
      pattern: "LSDK-19"
---

<objective>
Wave 4 of Phase 5 (FSB-side ceremony closure, depends on all prior plans): finalize the Phase 5 cross-repo audit trail by (a) advancing `.planning/LATTICE-PIN.md`'s frontmatter SHA to the Phase 5 Lattice HEAD + appending a Phase 5 row to the per-phase log referencing the 3 new Lattice commits (Plan 05-02 + 2 from Plan 05-03), and (b) populating `.planning/REQUIREMENTS.md` with 8 new requirement entries (LSDK-19, LSDK-20, LSDK-21, LSDK-22, FINT-03, FINT-04, FINT-05, FINT-06) + their traceability rows.

NO Lattice repo modifications in this plan -- all Lattice-side work landed in Plans 05-02 + 05-03 + audit-doc. NO new code, no new tests, no new functionality. Pure documentation closure mirroring the Phase 1+2+3+4 ceremony pattern.

Output: 2 files modified (`LATTICE-PIN.md` + `REQUIREMENTS.md`). ONE FSB commit. Phase 1-4 smokes byte-frozen. INV-04 setTimeout = 8.

Per CONTEXT.md D-23 + D-24: Plan 05-06 is the FINAL plan in Phase 5; depends_on transitively covers Wave 3 (Plans 05-04 + 05-05) which depended on Wave 2 (Plan 05-03) which depended on Wave 1 Plan 05-02. The dependency chain ensures the Lattice HEAD captured here is exactly the Plan 05-03 audit-doc commit (which is the last Lattice commit Phase 5 produces; Wave 3 + Wave 4 are FSB-side only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/LATTICE-PIN.md
@.planning/phases/05-mv3-survivability-bundler/05-CONTEXT.md
@.planning/phases/05-mv3-survivability-bundler/05-01-PLAN.md
@.planning/phases/05-mv3-survivability-bundler/05-02-PLAN.md
@.planning/phases/05-mv3-survivability-bundler/05-03-PLAN.md
@.planning/phases/05-mv3-survivability-bundler/05-04-PLAN.md
@.planning/phases/05-mv3-survivability-bundler/05-05-PLAN.md

<interfaces>
<!-- File-row schemas to update. Existing structure documented. -->

.planning/LATTICE-PIN.md (current state per Phase 4 close):
  Frontmatter:
    current_lattice_sha: f1c943bd9398daeda2ccf92a3d0c2bc004a0379f
    current_branch: fsb-integration-experiments
    last_updated: 2026-05-24
    schema_version: 1
  Body: per-phase log table with 4 rows (Phase 1 / Phase 2 / Phase 3 / Phase 4).
  Phase 5 row appends as the 5th row.

.planning/REQUIREMENTS.md (current state per Phase 4 close):
  LSDK section: LSDK-01..LSDK-18 all populated (21 occurrences in grep).
  FINT section: FINT-01 only populated; FINT-NN..M placeholders for the
                future agent-loop integration phases.
  Traceability table: 21 concrete rows + 2 MCP cross-cutting rows.
  Total v1 count: "21 concrete so far".

Phase 5 captured short-SHAs (collected from `cd lattice && git log` after
Plans 05-02 + 05-03 commits):
  - PLAN_02_SHA: from Plan 05-02 ("feat(runtime): add MV3-survivability...")
  - REEXPORT_SHA: from Plan 05-03 Task 1 ("feat(api): re-export...")
  - AUDIT_DOC_SHA: from Plan 05-03 Task 2 ("docs(fsb-integration): close...")
  - PHASE_5_LATTICE_HEAD: full 40-char SHA of the audit-doc commit (== AUDIT_DOC_SHA expanded)

Phase 5 FSB commit short-SHAs (from `git log automation`):
  - PLAN_01_FSB_SHA: from Plan 05-01 ("feat(05-01): add esbuild bundler infra...")
  - PLAN_04_FSB_SHA: from Plan 05-04 ("feat(05-04): hybrid offscreen Lattice host...")
  - PLAN_05_FSB_SHA: from Plan 05-05 ("feat(05-05): FSB standalone MV3-survivability adapter...")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump LATTICE-PIN.md frontmatter SHA + append Phase 5 per-phase log row</name>
  <files>.planning/LATTICE-PIN.md</files>
  <action>
First, capture all relevant SHAs:

```
cd lattice
PHASE_5_LATTICE_HEAD=$(git rev-parse fsb-integration-experiments)
echo "PHASE_5_LATTICE_HEAD=$PHASE_5_LATTICE_HEAD"

PLAN_02_SHA=$(git log --format=%h --all -- packages/lattice/src/runtime/survivability.ts | tail -1)
REEXPORT_SHA=$(git log --format=%h --all -- packages/lattice/src/index.ts | head -1)
AUDIT_DOC_SHA=$(git log --format=%h --all -- docs/fsb-integration-gaps.md | head -1)
echo "PLAN_02_SHA=$PLAN_02_SHA"
echo "REEXPORT_SHA=$REEXPORT_SHA"
echo "AUDIT_DOC_SHA=$AUDIT_DOC_SHA"

# Sanity: Phase 5 produced 3 Lattice commits total
PHASE_5_COMMITS=$(git log f1c943b..HEAD --format=%h --oneline | wc -l | tr -d ' ')
echo "PHASE_5_COMMITS=$PHASE_5_COMMITS (expected: 3)"

cd /Users/lakshmanturlapati/Desktop/FSB/automation
```

Capture FSB commit SHAs:
```
PLAN_01_FSB_SHA=$(git log --format=%h --all --grep="^feat(05-01): add esbuild" | head -1)
PLAN_04_FSB_SHA=$(git log --format=%h --all --grep="^feat(05-04): hybrid offscreen" | head -1)
PLAN_05_FSB_SHA=$(git log --format=%h --all --grep="^feat(05-05): FSB standalone MV3" | head -1)
echo "PLAN_01_FSB_SHA=$PLAN_01_FSB_SHA"
echo "PLAN_04_FSB_SHA=$PLAN_04_FSB_SHA"
echo "PLAN_05_FSB_SHA=$PLAN_05_FSB_SHA"
```

Read the current `.planning/LATTICE-PIN.md`. Apply two surgical edits.

**Edit A: frontmatter `current_lattice_sha` + `last_updated`.**

Before:
```
current_lattice_sha: f1c943bd9398daeda2ccf92a3d0c2bc004a0379f
current_branch: fsb-integration-experiments
last_updated: 2026-05-24
schema_version: 1
```

After (substitute `<PHASE_5_LATTICE_HEAD>` with the full 40-char SHA captured above; substitute `<TODAY>` with the current date 2026-05-24 OR whatever the actual execution date is):
```
current_lattice_sha: <PHASE_5_LATTICE_HEAD>
current_branch: fsb-integration-experiments
last_updated: <TODAY>
schema_version: 1
```

**Edit B: append Phase 5 row to the per-phase log table.**

The current table body ends with the Phase 4 row. Append a new row IMMEDIATELY after the Phase 4 row. The row schema (column-for-column matches Phase 1-4 rows):

```
| Phase 5   | <TODAY> | `<PHASE_5_LATTICE_HEAD>` | `fsb-integration-experiments`   | (1) `packages/lattice/src/runtime/survivability.ts` + `survivability.test.ts` (`<PLAN_02_SHA>`) -- NEW Lattice module shipping `SurvivabilityAdapter<TState>` interface (4 methods: serialize, deserialize, onEviction, resume) + `createNoopSurvivabilityAdapter()` reference impl + ResumePolicy literal-union (SAFE | RECOVERY_AMBIGUOUS | ON_ERROR_SW_EVICTION_MID_REQUEST | ON_ERROR_SW_EVICTION_MID_TOOL_DISPATCH; CD-E carries forward from FSB attempt-1 02-04-PLAN.md taxonomy) + SerializedSnapshot + EvictionHook<TState> + UnsubscribeFn. 12+ vitest cases covering shape conformance + JSON round-trip + composition with v1.1 ReceiptEnvelope (Test 12 exercises DSSE+JCS round-trip with real ephemeral Ed25519 keypair; no mocks for Lattice primitives). Composition conventions documented in JSDoc (not enforced): D-09 onEviction hooks SHOULD register in BAND.SAFETY (Phase 2); D-10 SerializedSnapshot.payload MAY embed v1.1 ReceiptEnvelope from Phase 3 createCheckpointHook. CONSERVATIVE recovery dispatcher EXPLICITLY OUT OF SCOPE per CONTEXT.md D-22 (deferred to follow-on milestone). (2) `packages/lattice/src/index.ts` (`<REEXPORT_SHA>`) -- re-export `createNoopSurvivabilityAdapter` value + 5 type-only re-exports (SurvivabilityAdapter, SerializedSnapshot, EvictionHook, ResumePolicy, UnsubscribeFn) from `./runtime/survivability.js`. dist/ rebuilt locally via tsdown clean:true (dist/ gitignored). (3) `docs/fsb-integration-gaps.md` (`<AUDIT_DOC_SHA>`) -- 2 MV3-survivability Blocker rows flipped to `Covered` with backlink SHAs (Adapter contract row + Cross-process resumption row). | Phase 5 = MV3-survivability adapter contract + bundler infra + hybrid offscreen Lattice host. Lattice vitest 397 (Phase 4 baseline) -> >= 409 PASS (Plan 05-02 additive). FSB consumes via the existing `file:` dep; Plan 05-04 ships the FIRST in-extension Lattice consumption surface (hybrid offscreen path; background.js BYTE-FROZEN; classic SW + 153 importScripts chain untouched per D-17); Plan 05-05 ships the FSB-side standalone runtime adapter (`extension/ai/lattice-runtime-adapter.js`; standalone per D-19; feature-flag gated default-off per D-20 + CD-D; LRU bound documented in JSDoc per threat-model row 3, enforcement deferred) + Node-side smoke (`tests/lattice-survivability-smoke.test.js`, >= 25 PASS real-runtime end-to-end). Phase 1-4 byte-frozen baseline preserved: receipts/types.ts (post-Phase-2), contract/bands.ts + checkpoint.ts (Phase 2+3), tracing/tracing.ts (post-Phase-3), providers/*.ts (post-Phase-4) all untouched. All 3 Phase 5 Lattice commits carry `Ref: FSB v0.10.0-attempt-2 Phase 5` footer (D-25); none pushed (D-19 NO-push carryforward; `git reflog \| grep -c push` = 0). FSB-side: 4 commits this phase (Plan 05-01 `<PLAN_01_FSB_SHA>` esbuild bundler infra; Plan 05-04 `<PLAN_04_FSB_SHA>` offscreen Lattice host + manifest WAR entry; Plan 05-05 `<PLAN_05_FSB_SHA>` standalone adapter + Node smoke; THIS commit ceremony closure). |
```

(Substitute all `<...>` placeholders with the captured SHA values. The row is a single physical line in the markdown table -- the formatting renders as a wide cell.)

Verify the edits:
```
grep -E "^current_lattice_sha:" .planning/LATTICE-PIN.md
```
Expected: `current_lattice_sha: <PHASE_5_LATTICE_HEAD>` (the 40-char SHA captured).

```
grep -c "Phase 5" .planning/LATTICE-PIN.md
```
Expected: >= 1 (the new row's first column).

```
grep -c "PHASE_5_LATTICE_HEAD" .planning/LATTICE-PIN.md
```
Expected: 0 (the placeholder must be substituted; if grep finds the literal string, the substitution failed).

Verify Phase 1-4 rows still present (no row regression):
```
[ "$(grep -c '^| Phase ' .planning/LATTICE-PIN.md)" = "5" ] && echo "PASS: 5 phase rows total (Phase 1..5)"
```

Verify SHA matches `cd lattice && git rev-parse fsb-integration-experiments`:
```
EXPECTED=$(cd lattice && git rev-parse fsb-integration-experiments)
ACTUAL=$(grep -E "^current_lattice_sha:" .planning/LATTICE-PIN.md | awk '{print $2}')
[ "$EXPECTED" = "$ACTUAL" ] && echo "PASS: LATTICE-PIN frontmatter SHA matches Lattice HEAD"
```
  </action>
  <verify>
    <automated>EXPECTED=$(cd lattice && git rev-parse fsb-integration-experiments) && ACTUAL=$(grep -E "^current_lattice_sha:" .planning/LATTICE-PIN.md | awk '{print $2}') && [ "$EXPECTED" = "$ACTUAL" ] && echo "PASS: LATTICE-PIN SHA matches Lattice HEAD" && [ "$(grep -c '^| Phase ' .planning/LATTICE-PIN.md)" = "5" ] && echo "PASS: 5 phase rows" && grep -q "Phase 5" .planning/LATTICE-PIN.md && echo "PASS: Phase 5 row present" && [ "$(grep -c 'PHASE_5_LATTICE_HEAD' .planning/LATTICE-PIN.md)" = "0" ] && echo "PASS: placeholders substituted"</automated>
  </verify>
  <done>
- `.planning/LATTICE-PIN.md` frontmatter `current_lattice_sha` matches `cd lattice && git rev-parse fsb-integration-experiments` exactly.
- Per-phase log gains a Phase 5 row with all 3 Phase 5 Lattice commit short-SHAs cited (Plan 05-02 + Plan 05-03 re-export + Plan 05-03 audit-doc).
- The Phase 5 row's narrative summarizes the 3 commits + cross-references the 4 FSB-side commits (Plan 05-01 + 05-04 + 05-05 + this ceremony commit).
- Phase 1-4 rows preserved untouched (5 phase rows total).
- No placeholder strings remaining in the file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Populate REQUIREMENTS.md with LSDK-19..22 + FINT-03..06 + 8 traceability rows</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Read the current `.planning/REQUIREMENTS.md`. Apply three surgical edits.

**Edit A: Add LSDK-19, LSDK-20, LSDK-21, LSDK-22 entries to the LSDK section.**

The LSDK section currently ends with LSDK-18 (LM Studio adapter). Insert FOUR new entries immediately after LSDK-18. The two existing placeholder rows (`LSDK-LL..P (TBD)` for delegation + `LSDK-PP..Q (TBD)` for MV3-survivability) must be REPLACED by the new concrete entries -- the MV3-survivability placeholder (`LSDK-PP..Q`) is the placeholder that Phase 5 fulfills.

Before (the current LSDK tail at lines ~60-61):
```
- [ ] **LSDK-LL..P (TBD):** Task-delegation primitive shape (if Lattice opens multi-agent policy; otherwise design as a Lattice-adjacent FSB-side primitive that consumes Lattice's receipt + tripwire surface).
- [ ] **LSDK-PP..Q (TBD):** MV3-survivability adapter contract -- documented interface for runtimes whose execution context can be evicted mid-flow.
```

After (delete the MV3-survivability placeholder + insert 4 concrete entries above the still-pending delegation placeholder):
```
- [x] **LSDK-19 -- DONE <TODAY> (Phase 05 Plan 05-02 + Plan 05-03):** `SurvivabilityAdapter<TState>` interface + ResumePolicy + SerializedSnapshot + EvictionHook + UnsubscribeFn shipped at `lattice/packages/lattice/src/runtime/survivability.ts`. 4 contract methods (serialize, deserialize, onEviction, resume). Documented interface for runtimes whose execution context can be evicted mid-flow (Chrome MV3 SW, Cloudflare Workers, AWS Lambda). ResumePolicy literal-union: `SAFE | RECOVERY_AMBIGUOUS | ON_ERROR_SW_EVICTION_MID_REQUEST | ON_ERROR_SW_EVICTION_MID_TOOL_DISPATCH` (CD-E carries forward FSB attempt-1 02-04-PLAN.md CONSERVATIVE recovery taxonomy). Composition conventions documented in JSDoc (not enforced): D-09 onEviction hooks SHOULD register in BAND.SAFETY; D-10 SerializedSnapshot.payload MAY embed v1.1 ReceiptEnvelope. CONSERVATIVE recovery dispatcher EXPLICITLY OUT OF SCOPE per CONTEXT.md D-22. Lattice commit `<PLAN_02_SHA>` (interface + ref impl) + `<REEXPORT_SHA>` (public surface re-export) + `<AUDIT_DOC_SHA>` (audit-doc closure).
- [x] **LSDK-20 -- DONE <TODAY> (Phase 05 Plan 05-02):** `createNoopSurvivabilityAdapter()` reference implementation shipped at `lattice/packages/lattice/src/runtime/survivability.ts` (~190 lines including the interface). Analog to `createFakeProvider` -- records eviction events via Set<EvictionHook> + round-trips state via JSON.stringify/parse; default resume policy = `SAFE`. Companion `survivability.test.ts` (~280 lines) ships 12+ vitest cases covering factory identity, serialize/deserialize round-trip, onEviction lifecycle, resume() ResumePolicy contract, type-level discriminated union, integration with real v1.1 Capability Receipt envelope (Test 12 exercises DSSE+JCS round-trip with ephemeral Ed25519 keypair from `generateEd25519KeyPairJwk()` + `createInMemorySigner` + `verifyReceipt` + `createMemoryKeySet`). Lattice vitest 397 -> >= 409 PASS / 0 FAIL. Lattice commit `<PLAN_02_SHA>`.
- [x] **LSDK-21 -- DONE <TODAY> (Phase 05 Plan 05-03):** Public-surface re-export at `lattice/packages/lattice/src/index.ts` -- 1 value export (`createNoopSurvivabilityAdapter`) + 5 type-only re-exports (`SurvivabilityAdapter`, `SerializedSnapshot`, `EvictionHook`, `ResumePolicy`, `UnsubscribeFn`) from `./runtime/survivability.js`. Bare-specifier `await import('lattice')` from FSB root resolves `createNoopSurvivabilityAdapter` as `function`; 11 Phase 1-4 carryforward exports preserved. dist/ rebuilt locally via tsdown clean:true. Lattice commit `<REEXPORT_SHA>`.
- [x] **LSDK-22 -- DONE <TODAY> (Phase 05 Plan 05-03):** Audit-doc closure at `lattice/docs/fsb-integration-gaps.md` -- 2 MV3-survivability Blocker rows flipped to `Covered` with backlink SHAs ("Adapter contract for runtimes whose execution context can be evicted mid-flow" + "Cross-process resumption from persisted state envelope"). Phase 1-4 Covered rows preserved untouched (Phase 2 = 6 rows, Phase 3 = 2 rows, Phase 4 = 5 rows; grep probes verify counts). After this commit: 7 of 8 audit-doc Blocker rows closed across Phases 1-5; 1 Out-of-scope (Delegation primitive contingent on Lattice multi-agent policy; Phase 6 if Lattice opens). Lattice commit `<AUDIT_DOC_SHA>`.
- [ ] **LSDK-LL..P (TBD):** Task-delegation primitive shape (if Lattice opens multi-agent policy; otherwise design as a Lattice-adjacent FSB-side primitive that consumes Lattice's receipt + tripwire surface).
```

(Substitute `<TODAY>` with the execution date 2026-05-24; substitute the SHA placeholders with the captured short-SHAs from Task 1's pre-execution script.)

**Edit B: Add FINT-03, FINT-04, FINT-05, FINT-06 entries to the FINT section.**

The FINT section currently has FINT-01 populated + 5 placeholder rows (`FINT-NN..M`, `FINT-MM..K`, etc.). Insert FOUR new concrete entries IMMEDIATELY after FINT-01 (preserving the 5 placeholder rows that describe future agent-loop integration phases).

Before (the FINT section structure):
```
- [x] **FINT-01 (path:dep wiring + Node smoke) -- DONE 2026-05-24 (Phase 01 Plan 01-02):** ... [existing text] ...
- [ ] **FINT-NN..M (TBD):** Adapt FSB's `runAgentLoop` to emit step transitions through Lattice's receipt API ...
```

After (insert 4 new concrete entries between FINT-01 and the existing placeholders):
```
- [x] **FINT-01 (path:dep wiring + Node smoke) -- DONE 2026-05-24 (Phase 01 Plan 01-02):** ... [unchanged] ...
- [x] **FINT-03 (esbuild bundler infrastructure) -- DONE <TODAY> (Phase 05 Plan 05-01):** Per-entrypoint esbuild bundler config at `esbuild.config.js` (~120 lines) -- offscreen + content-script + sidepanel entries -- emits to `extension/dist/` (CD-A: single tree). `npm run build` invokes (D-05). esbuild ^0.24.0 in devDependencies (no plugins per Phase 5 threat-model row 1). Sourcemap strategy per D-04: external .js.map for offscreen + sidepanel; sourcemaps disabled for content-script bundles (lean injection payload). Bundler-installed-not-wired per D-06: ZERO `manifest.json` change in Plan 05-01; ZERO behavior diff (production still loads classic `extension/background.js` + 153 importScripts chain unchanged). `extension/dist/` added to `.gitignore`. FSB commit `<PLAN_01_FSB_SHA>`.
- [x] **FINT-04 (hybrid offscreen Lattice host) -- DONE <TODAY> (Phase 05 Plan 05-04):** FIRST in-extension Lattice consumption surface in this milestone (Option B carryforward from Phase 1 ends here). New offscreen document `extension/offscreen/lattice-host.html` declaring `<script type="module" src="lattice-host.js">` + ESM bundle-input `extension/offscreen/lattice-host.js` (~150 lines). Imports `createCheckpointHook` + `createHookPipeline` + `createInMemorySigner` + `generateEd25519KeyPairJwk` + `createNoopSurvivabilityAdapter` + 2 constants from the `'lattice'` bare specifier. Listens on `chrome.runtime.onMessage` for `{type: 'lattice-step-transition'}` envelopes per D-16; composes Phase 2 `createHookPipeline` + Phase 3 `createCheckpointHook` to mint v1.1 receipts; posts back `{type: 'lattice-receipt-minted'}` (or `{type: 'lattice-receipt-mint-failed'}` per D-07 best-effort mint). Cross-extension messages rejected via `sender.id === chrome.runtime.id` check. `extension/manifest.json` appends `'offscreen/lattice-host.html'` to `web_accessible_resources.resources` (CD-C); existing `"offscreen"` permission reused. `background.js` BYTE-FROZEN per D-17 (153 importScripts chain untouched). Bundler emits `extension/dist/offscreen/lattice-host.js` (ESM, ~50-500KB) with the `'lattice'` bare specifier rewritten (zero unresolved references in dist bundle). FSB commit `<PLAN_04_FSB_SHA>`.
- [x] **FINT-05 (FSB standalone MV3-survivability adapter) -- DONE <TODAY> (Phase 05 Plan 05-05):** Standalone module `extension/ai/lattice-runtime-adapter.js` (~200 lines; CJS-compatible for classic SW importScripts + Node tests) implementing Lattice's `SurvivabilityAdapter<TState>` contract over `chrome.storage.session`. 4 contract methods (serialize, deserialize, onEviction, resume) + FSB-specific `fireEvictionHooks(state)` helper. Per D-19 STANDALONE: does NOT modify `agent-loop.js`, `runAgentLoop`, or any existing iterator (INV-04 setTimeout iterator at lines 1824/2418/2487/2497 stays byte-frozen). Per D-20 + CD-D feature-flag gated: `globalThis.FSB_LATTICE_RUNTIME_ADAPTER_ENABLED` defaults `false`; uniform check pattern `if (typeof FSB_LATTICE_RUNTIME_ADAPTER_ENABLED !== "undefined" && FSB_LATTICE_RUNTIME_ADAPTER_ENABLED) { ... }` gates every production write to `chrome.storage.session`. ResumePolicy dispatch over FSB attempt-1 marker vocabulary (CD-E): `BEFORE_API_REQUEST` -> `ON_ERROR_SW_EVICTION_MID_REQUEST`; `BEFORE_TOOL_EXECUTION` -> `ON_ERROR_SW_EVICTION_MID_TOOL_DISPATCH`; `BEFORE_ITERATION` / `BEFORE_NEXT_ITERATION_SCHEDULE` / undefined -> `SAFE`; unknown -> `RECOVERY_AMBIGUOUS`. LRU bound documented in JSDoc (default cap = 50 per sessionId); enforcement deferred per threat-model row 3 (Phase 5 is contract-shipping; LRU is follow-on). CONSERVATIVE recovery dispatcher (the actual rewiring into runAgentLoop's restored-mode branches) EXPLICITLY OUT OF SCOPE per D-22; deferred to follow-on milestone. FSB commit `<PLAN_05_FSB_SHA>`.
- [x] **FINT-06 (Node-side survivability smoke + ceremony) -- DONE <TODAY> (Phase 05 Plan 05-05):** Real-runtime Node smoke `tests/lattice-survivability-smoke.test.js` (~240 lines; >= 25 PASS / 0 FAIL real-runtime end-to-end). Mocks only `chrome.storage.session` (in-memory) + `chrome.runtime` (no-op) because those Chrome APIs are unavailable in Node; uses real `createReceipt` + `verifyReceipt` + ephemeral Ed25519 keypair (no Lattice mocks; mirrors Phase 1-4 smoke convention "Real runtime tests, not static-text"). 5-part coverage: Part 1 surface presence (Plan 05-03 re-export + Phase 1-4 carryforward); Part 2 adapter 4 contract methods + ResumePolicy dispatch over FSB attempt-1 marker vocabulary; Part 3 real v1.1 receipt embedded in adapter state + serialize/deserialize + verifyReceipt round-trip (proves DSSE+JCS preserved through JSON round-trip); Part 4 feature flag default-off (zero writes) + flag-on (writes recorded in mock storage) + onEviction lifecycle (UnsubscribeFn idempotency); Part 5 Phase 1-4 Lattice surface carryforward. Appended to `package.json` `scripts.test` chain as the FINAL entry (after `lattice-providers-smoke.test.js`). Phase 1+2+3+4 chain entries BYTE-FROZEN. FSB commit `<PLAN_05_FSB_SHA>` (combined with FINT-05).
- [ ] **FINT-NN..M (TBD):** Adapt FSB's `runAgentLoop` to emit step transitions through Lattice's receipt API ...
```

(Substitute placeholders identically to Edit A.)

**Edit C: Add 8 new rows to the Traceability table + bump total v1 count.**

The current Traceability table ends after the Phase 4 LSDK-14..18 rows + `(more) | (TBD) | Pending`. Insert 8 new rows immediately before `(more) | (TBD)`:

```
| LSDK-19 | 05 | Complete (Phase 05 Plan 05-02 + Plan 05-03: SurvivabilityAdapter interface + ResumePolicy + 4 supporting types; Lattice commits `<PLAN_02_SHA>` + `<REEXPORT_SHA>` + `<AUDIT_DOC_SHA>`) |
| LSDK-20 | 05 | Complete (Phase 05 Plan 05-02: createNoopSurvivabilityAdapter ref impl + 12+ vitest cases; Lattice commit `<PLAN_02_SHA>`) |
| LSDK-21 | 05 | Complete (Phase 05 Plan 05-03: src/index.ts re-export of factory + 5 types; Lattice commit `<REEXPORT_SHA>`) |
| LSDK-22 | 05 | Complete (Phase 05 Plan 05-03: 2 MV3-survivability audit-doc rows flipped Blocker -> Covered with backlink SHAs; Lattice commit `<AUDIT_DOC_SHA>`) |
| FINT-03 | 05 | Complete (Phase 05 Plan 05-01: esbuild bundler infra + scripts.build + extension/dist/; FSB commit `<PLAN_01_FSB_SHA>`) |
| FINT-04 | 05 | Complete (Phase 05 Plan 05-04: hybrid offscreen Lattice host + manifest WAR entry + bundler emits offscreen ESM bundle; FSB commit `<PLAN_04_FSB_SHA>`) |
| FINT-05 | 05 | Complete (Phase 05 Plan 05-05: standalone lattice-runtime-adapter.js over chrome.storage.session; feature-flag default-off; FSB commit `<PLAN_05_FSB_SHA>`) |
| FINT-06 | 05 | Complete (Phase 05 Plan 05-05: Node smoke lattice-survivability-smoke.test.js >= 25 PASS; package.json scripts.test chain extended; FSB commit `<PLAN_05_FSB_SHA>`) |
```

Update the total count line at the bottom. Before:
```
**Total v1 requirements:** TBD (21 concrete so far; remaining LSDK/FINT/MCP/PRV IDs populated during downstream phase planning)
```

After:
```
**Total v1 requirements:** TBD (29 concrete so far; remaining FINT/MCP/PRV IDs populated during downstream phase planning)
```

Verify the edits:
```
grep -c "LSDK-19" .planning/REQUIREMENTS.md
```
Expected: 2 (1 section entry + 1 traceability row).

```
grep -c "LSDK-20" .planning/REQUIREMENTS.md
```
Expected: 2.

```
grep -c "LSDK-21" .planning/REQUIREMENTS.md
```
Expected: 2.

```
grep -c "LSDK-22" .planning/REQUIREMENTS.md
```
Expected: 2.

```
grep -c "FINT-03" .planning/REQUIREMENTS.md
```
Expected: 2.

```
grep -c "FINT-04" .planning/REQUIREMENTS.md
```
Expected: 2.

```
grep -c "FINT-05" .planning/REQUIREMENTS.md
```
Expected: 2.

```
grep -c "FINT-06" .planning/REQUIREMENTS.md
```
Expected: 2.

```
grep "29 concrete" .planning/REQUIREMENTS.md
```
Expected: 1 line matching.

Verify the Phase 1-4 baseline counts preserved (no row regression):
```
grep -c "LSDK-14" .planning/REQUIREMENTS.md
```
Expected: 2 (1 section entry + 1 traceability row; Phase 4 baseline preserved).

```
grep -c "FINT-01" .planning/REQUIREMENTS.md
```
Expected: 2 (1 section entry + 1 traceability row; Phase 1 baseline preserved).

Verify NO placeholder strings remaining:
```
grep -cE "(PLAN_02_SHA|REEXPORT_SHA|AUDIT_DOC_SHA|PLAN_01_FSB_SHA|PLAN_04_FSB_SHA|PLAN_05_FSB_SHA|<TODAY>)" .planning/REQUIREMENTS.md
```
Expected: 0 (all placeholders substituted with the captured SHAs + date).
  </action>
  <verify>
    <automated>[ "$(grep -c 'LSDK-19' .planning/REQUIREMENTS.md)" = "2" ] && [ "$(grep -c 'LSDK-20' .planning/REQUIREMENTS.md)" = "2" ] && [ "$(grep -c 'LSDK-21' .planning/REQUIREMENTS.md)" = "2" ] && [ "$(grep -c 'LSDK-22' .planning/REQUIREMENTS.md)" = "2" ] && echo "PASS: 4 LSDK entries + 4 traceability rows" && [ "$(grep -c 'FINT-03' .planning/REQUIREMENTS.md)" = "2" ] && [ "$(grep -c 'FINT-04' .planning/REQUIREMENTS.md)" = "2" ] && [ "$(grep -c 'FINT-05' .planning/REQUIREMENTS.md)" = "2" ] && [ "$(grep -c 'FINT-06' .planning/REQUIREMENTS.md)" = "2" ] && echo "PASS: 4 FINT entries + 4 traceability rows" && grep -q "29 concrete" .planning/REQUIREMENTS.md && echo "PASS: total count bumped 21->29" && [ "$(grep -c 'LSDK-14' .planning/REQUIREMENTS.md)" = "2" ] && [ "$(grep -c 'FINT-01' .planning/REQUIREMENTS.md)" = "2" ] && echo "PASS: Phase 1-4 baseline preserved" && [ "$(grep -cE '(PLAN_02_SHA|REEXPORT_SHA|AUDIT_DOC_SHA|PLAN_01_FSB_SHA|PLAN_04_FSB_SHA|PLAN_05_FSB_SHA|<TODAY>)' .planning/REQUIREMENTS.md)" = "0" ] && echo "PASS: placeholders substituted"</automated>
  </verify>
  <done>
- `.planning/REQUIREMENTS.md` LSDK section has 4 new Complete entries (LSDK-19, LSDK-20, LSDK-21, LSDK-22).
- FINT section has 4 new Complete entries (FINT-03, FINT-04, FINT-05, FINT-06).
- Traceability table has 8 new rows.
- Total v1 count bumped from 21 to 29.
- Phase 1-4 baseline entries preserved (LSDK-01..18 + FINT-01).
- No placeholder strings remaining (all SHAs + date substituted).
  </done>
</task>

<task type="auto">
  <name>Task 3: Final byte-freeze verification + Plan 05-06 atomic commit</name>
  <files>(no file edits -- verification + commit only)</files>
  <action>
Run the comprehensive Phase 5 closure verification before committing.

1. INV-01 MCP wire parity:
   ```
   node tests/tool-definitions-parity.test.js
   ```
   Expected: `=== Results: 142 passed, 0 failed ===`. Exit 0.

2. INV-04 setTimeout iterator preserved:
   ```
   grep -c "setTimeout" extension/ai/agent-loop.js
   ```
   Expected: `8` exact.

3. Phase 1+2+3+4 FSB smokes byte-frozen:
   ```
   node tests/lattice-smoke.test.js | grep "passed: 29"
   node tests/lattice-tripwire-smoke.test.js | grep "passed: 39"
   node tests/lattice-checkpoint-smoke.test.js | grep "passed: 72"
   node tests/lattice-providers-smoke.test.js | grep "passed: 47"
   ```
   All 4 lines must print.

4. Phase 5 survivability smoke (Plan 05-05) passes:
   ```
   node tests/lattice-survivability-smoke.test.js | tail -3 | grep "failed: 0"
   ```

5. Full FSB test chain:
   ```
   npm test 2>&1 | tail -5 | grep "failed: 0"
   ```

6. Lattice vitest still green at the Phase 5 HEAD:
   ```
   cd lattice && pnpm --filter lattice test 2>&1 | tail -3 | grep -E "Tests +[4-9][0-9][0-9] passed"
   cd /Users/lakshmanturlapati/Desktop/FSB/automation
   ```

7. LATTICE-PIN.md frontmatter SHA matches Lattice HEAD:
   ```
   [ "$(cd lattice && git rev-parse fsb-integration-experiments)" = "$(grep -E '^current_lattice_sha:' .planning/LATTICE-PIN.md | awk '{print $2}')" ] && echo "PASS"
   ```

8. D-19 NO-push contract on Lattice:
   ```
   cd lattice && git reflog -50 | grep -c push
   cd /Users/lakshmanturlapati/Desktop/FSB/automation
   ```
   Expected: 0.

9. Zero extension/* modifications since pivot (carries forward Phase 1-4 baseline; verifies Phase 5 only touched the allowed new files):
   ```
   git diff --name-only 51bdbb36 HEAD -- "extension/*" "mcp/*" | grep -vE "^extension/(offscreen/lattice-host\.(html|js)|ai/lattice-runtime-adapter\.js|manifest\.json|dist/)" | head -5
   ```
   Expected: empty (only the Phase 5 allowed-list files appear in the diff; manifest.json is the one byte-frozen-with-exception per D-18 minimal change; offscreen/lattice-host.* + ai/lattice-runtime-adapter.js are NEW Phase 5 files; extension/dist/ is gitignored so won't appear).

10. Phase 5 file modification scope (only Phase 5-allowed paths):
    ```
    git diff --name-only HEAD~4 HEAD | sort -u
    ```
    Expected output (10 paths -- 4 commits' worth):
    - .gitignore (Plan 05-01)
    - .planning/LATTICE-PIN.md (Plan 05-06)
    - .planning/REQUIREMENTS.md (Plan 05-06)
    - esbuild.config.js (Plan 05-01 + 05-04 entry-flag drop)
    - extension/ai/lattice-runtime-adapter.js (Plan 05-05)
    - extension/manifest.json (Plan 05-04 WAR entry)
    - extension/offscreen/lattice-host.html (Plan 05-04)
    - extension/offscreen/lattice-host.js (Plan 05-04)
    - package-lock.json (Plan 05-01 esbuild install)
    - package.json (Plan 05-01 + Plan 05-05)
    - tests/lattice-survivability-smoke.test.js (Plan 05-05)

    11 paths total (or 10 if package-lock.json is unchanged in some npm versions). Acceptable: any subset of the above.

If ALL checks pass, commit Plan 05-06 atomically:

```
git add .planning/LATTICE-PIN.md .planning/REQUIREMENTS.md && git commit -m "$(cat <<'EOF'
docs(05): bump LATTICE-PIN + finalize LSDK + FINT traceability

Wave 4 of Phase 5 (FSB v0.10.0-attempt-2) -- ceremony closure. Pure
documentation update; no code, no tests, no functionality change.

Files:
  - .planning/LATTICE-PIN.md: frontmatter current_lattice_sha advances
    from f1c943bd (Phase 4 HEAD) to the new Phase 5 Lattice HEAD; per-
    phase log gains a Phase 5 row citing all 3 Phase 5 Lattice commits
    (Plan 05-02 adapter + Plan 05-03 re-export + Plan 05-03 audit-doc
    closure) + cross-referencing the 4 FSB-side Phase 5 commits.
  - .planning/REQUIREMENTS.md: LSDK section gains LSDK-19 + LSDK-20 +
    LSDK-21 + LSDK-22 entries (each marked Complete with Lattice commit
    short-SHAs); FINT section gains FINT-03 + FINT-04 + FINT-05 +
    FINT-06 entries (each marked Complete with FSB commit short-SHAs).
    Traceability table gains 8 new rows. Total v1 count: 21 -> 29.

Phase 5 deliverables (cumulative across all 6 plans):
  - Plan 05-01: esbuild bundler infrastructure (behavior-free per D-06)
  - Plan 05-02: Lattice-side SurvivabilityAdapter<TState> contract +
    createNoopSurvivabilityAdapter ref impl + 12+ vitest cases
  - Plan 05-03: Lattice src/index.ts public-surface re-export +
    audit-doc closure (2 MV3-survivability Blocker rows flipped)
  - Plan 05-04: FSB hybrid offscreen Lattice host (HTML + ESM JS
    module + bundler entry + manifest WAR entry); background.js
    byte-frozen
  - Plan 05-05: FSB standalone lattice-runtime-adapter.js
    (SurvivabilityAdapter over chrome.storage.session; feature-flag
    default-off per D-20; CONSERVATIVE recovery dispatcher OUT OF
    SCOPE per D-22) + Node-side survivability smoke (>= 25 PASS)
  - Plan 05-06 (THIS commit): LATTICE-PIN bump + REQUIREMENTS
    traceability

Phase 5 Lattice commits (3 total; all on fsb-integration-experiments;
none pushed per D-19; all carry Ref: FSB v0.10.0-attempt-2 Phase 5
footer per D-25):
  - Plan 05-02: feat(runtime): add MV3-survivability adapter contract
    + noop reference impl
  - Plan 05-03 a: feat(api): re-export survivability adapter contract
  - Plan 05-03 b: docs(fsb-integration): close Phase 5 audit rows
    (MV3-survivability)

Phase 5 FSB commits (4 total on automation branch; this commit is the
4th):
  - Plan 05-01: feat(05-01): add esbuild bundler infra (behavior-free)
  - Plan 05-04: feat(05-04): hybrid offscreen Lattice host (bundled
    via esbuild)
  - Plan 05-05: feat(05-05): FSB standalone MV3-survivability adapter
    + Node smoke
  - Plan 05-06 (THIS commit): docs(05): bump LATTICE-PIN + finalize
    LSDK + FINT traceability

Phase 5 baseline preserved:
  - INV-01 MCP wire 142 PASS
  - INV-04 setTimeout count = 8 (agent-loop.js byte-frozen)
  - INV-05 deprecated modules untouched
  - INV-06 Lattice SurvivabilityAdapter contract lives in Lattice;
    FSB adapter is glue
  - Phase 1+2+3+4 smokes byte-frozen: 29 + 39 + 72 + 47 PASS
  - extension/background.js + agent-loop.js + tool-definitions.js
    BYTE-FROZEN through all 6 Phase 5 plans
  - extension/manifest.json: single surgical edit per D-18 (CD-C
    web_accessible_resources entry for offscreen/lattice-host.html);
    all other fields byte-identical

Phase 5 deferred / out-of-scope (carries forward to follow-on milestones):
  - SW classic-to-module migration (153 importScripts -> ES imports)
  - CONSERVATIVE recovery dispatcher wiring into runAgentLoop's
    restored-mode branches (D-22)
  - Feature flag default-on flip (post-UAT)
  - LRU enforcement on chrome.storage.session snapshot accumulation
    (threat-model row 3)
  - chrome.storage.local override for runtime-switchable feature flag
    (CD-D)
  - SW-side wiring of the SW <-> offscreen message bus
  - Mainline PR back into Lattice (v0.11.0+)
  - Delegation primitive (Phase 6 CONTINGENT)

Phase 5 + milestone HUMAN-UAT carryforward: Phase 1's manual MV3 sanity
reload remains the SINGLE deferred-pending-UAT item from milestone
start per user directive ("continue all phases with GSD autonomous;
UAT will be at the end"). Phase 5 introduces ZERO new UAT items beyond
what Phase 1 already deferred (Option B carryforward ends with Phase 5
shipping the offscreen Lattice host; the existing Phase 1 MV3 reload
procedure remains the milestone-end verification).

Closes LSDK-19, LSDK-20, LSDK-21, LSDK-22.
Closes FINT-03, FINT-04, FINT-05, FINT-06.

Ref: FSB v0.10.0-attempt-2 Phase 5 Plan 05-06
EOF
)"
```

Verify the commit:
```
git log -1 --pretty=%s | grep -q "^docs(05): bump LATTICE-PIN"
```
Expected: exit 0.

Final Phase 5 summary count:
```
git log --grep="Phase 5" --grep="^feat(05-" --grep="^docs(05" --all-match --oneline | wc -l
```
Or simpler:
```
git log --oneline --grep="^feat(05-" automation
git log --oneline --grep="^docs(05" automation
```

Expected output (per branch grep): 3 `feat(05-XX)` commits (Plans 05-01 + 05-04 + 05-05) + 1 `docs(05)` commit (Plan 05-06) = 4 FSB commits total for Phase 5.

DO NOT run `git push` -- D-19 NO-push carryforward applies.

Phase 5 is now complete.
  </action>
  <verify>
    <automated>node tests/tool-definitions-parity.test.js 2>&1 | grep -q "142 passed" && echo "PASS: MCP parity" && [ "$(grep -c 'setTimeout' extension/ai/agent-loop.js)" = "8" ] && echo "PASS: INV-04" && node tests/lattice-smoke.test.js 2>&1 | grep -q "passed: 29" && echo "PASS: Phase 1 smoke" && node tests/lattice-tripwire-smoke.test.js 2>&1 | grep -q "passed: 39" && echo "PASS: Phase 2 smoke" && node tests/lattice-checkpoint-smoke.test.js 2>&1 | grep -q "passed: 72" && echo "PASS: Phase 3 smoke" && node tests/lattice-providers-smoke.test.js 2>&1 | grep -q "passed: 47" && echo "PASS: Phase 4 smoke" && node tests/lattice-survivability-smoke.test.js 2>&1 | tail -3 | grep -q "failed: 0" && echo "PASS: Phase 5 survivability smoke" && [ "$(cd lattice && git rev-parse fsb-integration-experiments)" = "$(grep -E '^current_lattice_sha:' .planning/LATTICE-PIN.md | awk '{print $2}')" ] && echo "PASS: LATTICE-PIN SHA matches" && [ "$(cd lattice && git reflog -50 | grep -c push)" = "0" ] && echo "PASS: D-19 NO-push" && [ -z "$(git diff --name-only HEAD -- extension/background.js extension/ai/agent-loop.js extension/ai/tool-definitions.js)" ] && echo "PASS: byte-frozen extension files" && git log -1 --pretty=%s | grep -q "^docs(05): bump LATTICE-PIN" && echo "PASS: Plan 05-06 commit landed"</automated>
  </verify>
  <done>
- All 7 byte-freeze verifiers pass: MCP wire (142), INV-04 (8), Phase 1 (29), Phase 2 (39), Phase 3 (72), Phase 4 (47), Phase 5 survivability (>= 25; 0 FAIL).
- LATTICE-PIN.md frontmatter SHA matches `cd lattice && git rev-parse fsb-integration-experiments`.
- `cd lattice && git reflog | grep -c push` = 0 (D-19 NO-push contract holds across all Phase 5 Lattice commits).
- extension/background.js + agent-loop.js + tool-definitions.js BYTE-FROZEN through Phase 5.
- Phase 5 = 4 FSB commits (05-01, 05-04, 05-05, 05-06) + 3 Lattice commits (05-02, 05-03 re-export, 05-03 audit-doc).
- ONE atomic FSB commit `docs(05): bump LATTICE-PIN + finalize LSDK + FINT traceability` with Ref footer + plan-ID.
- Phase 5 is COMPLETE.
- D-23 + D-24 + D-25 ceremony honored.
- LSDK-19..22 + FINT-03..06 (8 requirements) all covered + traceable.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LATTICE-PIN.md frontmatter -> Lattice HEAD | The SHA is the authoritative pin; threat = drift if the captured SHA differs from `cd lattice && git rev-parse fsb-integration-experiments` |
| REQUIREMENTS.md traceability rows -> commit SHAs | Each row cites short-SHAs; threat = inaccurate citations break the audit chain |
| Ceremony commit messages -> downstream auditors | The commit body is the primary cross-repo trace; threat = missing Ref footer or missing decision references |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | Tampering | LATTICE-PIN.md frontmatter SHA drift | mitigate | Task 3 explicit verifier: `cd lattice && git rev-parse fsb-integration-experiments` MUST match the frontmatter `current_lattice_sha` exact; the verifier shell command is part of the automated pass criteria |
| T-05-06-02 | Information Disclosure | Short-SHA collisions | accept | Git short-SHAs are 7 chars; collision probability in a small repo is negligible. The commit messages cite both short + full where ambiguity matters; the Lattice repo has < 1000 commits so short-SHAs are unique |
| T-05-06-03 | Repudiation | Ref footer absence on Plan 05-06 commit | mitigate | The HEREDOC commit message includes `Ref: FSB v0.10.0-attempt-2 Phase 5 Plan 05-06`; verifier greps for the exact subject line |
| T-05-06-04 | Tampering | REQUIREMENTS.md baseline regression | mitigate | Task 2 explicit verifiers: LSDK-14 + FINT-01 + Phase 1-4 LSDK rows all preserved at count = 2 (1 section + 1 traceability row each); Phase 5 only ADDS, never removes or modifies prior entries |
| T-05-06-05 | Spoofing | Plan 05-06 commit subject naming | mitigate | The commit subject `docs(05): bump LATTICE-PIN + finalize LSDK + FINT traceability` mirrors the Phase 4 closure commit naming convention (e.g., Phase 4 used `docs(04): bump LATTICE-PIN + finalize LSDK-14..18 traceability`) |
| T-05-06-06 | Denial of Service | npm test regression introduced by Plan 05-06 | accept | Task 3 runs the full test chain before commit; if any smoke regresses, the verifier returns non-zero and the commit does not land. This plan modifies only .planning/ files which are not in the test path; regression is functionally impossible from .md edits |
</threat_model>

<verification>
1. `.planning/LATTICE-PIN.md` frontmatter `current_lattice_sha` matches `cd lattice && git rev-parse fsb-integration-experiments`.
2. `.planning/LATTICE-PIN.md` per-phase log has 5 phase rows total (Phase 1..5).
3. `.planning/REQUIREMENTS.md` LSDK section has LSDK-19, 20, 21, 22 entries (each Complete).
4. `.planning/REQUIREMENTS.md` FINT section has FINT-03, 04, 05, 06 entries (each Complete).
5. `.planning/REQUIREMENTS.md` Traceability table has 8 new rows (LSDK-19..22 + FINT-03..06).
6. `.planning/REQUIREMENTS.md` total v1 count line reads "29 concrete so far".
7. No placeholder strings remaining in either file (all SHAs + date substituted).
8. Phase 1-4 REQUIREMENTS.md baseline preserved (LSDK-01..18 + FINT-01 entries unchanged; counts 2 each).
9. INV-04 setTimeout = 8.
10. Phase 1-5 smokes all PASS (29 + 39 + 72 + 47 + >=25).
11. extension/background.js + agent-loop.js + tool-definitions.js BYTE-FROZEN.
12. `cd lattice && git reflog | grep -c push` = 0 (D-19).
13. ONE FSB commit `docs(05): bump LATTICE-PIN + finalize LSDK + FINT traceability` with Ref footer + plan-ID.
</verification>

<success_criteria>
- LSDK-19, LSDK-20, LSDK-21, LSDK-22 traced.
- FINT-03, FINT-04, FINT-05, FINT-06 traced.
- Cross-repo audit chain extended to Phase 5 (Lattice HEAD pinned + per-phase log row appended).
- Total v1 count bumped 21 -> 29.
- ONE atomic FSB commit; no Lattice repo modifications; no git push.
- D-23 + D-24 + D-25 ceremony honored.
- Phase 5 is COMPLETE -- ready for `/gsd-verify-phase 5`.
</success_criteria>

<output>
After completion, create `.planning/phases/05-mv3-survivability-bundler/05-06-SUMMARY.md` documenting:
- Captured SHAs (all 6: 3 Lattice + 3 FSB Plan 05-0[145])
- LATTICE-PIN frontmatter diff (Phase 4 SHA -> Phase 5 SHA)
- LATTICE-PIN per-phase log: row count 4 -> 5
- REQUIREMENTS.md: 8 new entries + 8 traceability rows + count bump 21 -> 29
- Final Phase 5 commit roster (4 FSB + 3 Lattice; all with Ref footers; none pushed)
- 13 verification probe outputs
- Phase 5 final state: ready for `/gsd-verify-phase 5`
</output>
</content>
</invoke>