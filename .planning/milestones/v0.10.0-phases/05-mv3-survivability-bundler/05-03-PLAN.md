---
phase: 05-mv3-survivability-bundler
plan: 03
type: execute
wave: 2
depends_on:
  - 05-02-PLAN.md
files_modified:
  - lattice/packages/lattice/src/index.ts
  - lattice/docs/fsb-integration-gaps.md
autonomous: true
requirements:
  - LSDK-21
  - LSDK-22
requirements_addressed:
  - LSDK-21
  - LSDK-22

must_haves:
  truths:
    - "Lattice's public surface at `lattice/packages/lattice/src/index.ts` re-exports `createNoopSurvivabilityAdapter` (value) + 5 type names (`SurvivabilityAdapter`, `SerializedSnapshot`, `EvictionHook`, `ResumePolicy`, `UnsubscribeFn`) per D-13."
    - "Lattice dist/ rebuilt locally via `pnpm build`; bare-specifier probe `await import('lattice')` from FSB's working directory resolves `lattice.createNoopSurvivabilityAdapter` as `function`."
    - "Two MV3-survivability audit-doc rows in `lattice/docs/fsb-integration-gaps.md` (currently 'Adapter contract for runtimes whose execution context can be evicted mid-flow' = Blocker; 'Cross-process resumption from persisted state envelope' = Blocker) flip to `Covered` with backlink SHAs to Plan 05-02 commit + this plan's commits (D-14)."
    - "Lattice vitest 0 regressions: still >= 409 PASS / 0 FAIL after the re-export + audit-doc commits."
    - "Phase 1-4 audit-doc rows already Covered stay Covered (no row regressions); only the 2 MV3-survivability rows flip in this plan."
    - "Plan 05-03 lands as TWO Lattice commits on `fsb-integration-experiments` -- one for the re-export (`feat(api): re-export survivability adapter contract`) + one for the audit-doc closure (`docs(fsb-integration): close Phase 5 audit rows (MV3-survivability)`). Both carry `Ref: FSB v0.10.0-attempt-2 Phase 5` footer (D-25). NO `git push` (D-19)."
  artifacts:
    - path: "lattice/packages/lattice/src/index.ts"
      provides: "Public-surface re-exports for createNoopSurvivabilityAdapter + 5 type names from ./runtime/survivability.js"
      contains: "createNoopSurvivabilityAdapter"
    - path: "lattice/docs/fsb-integration-gaps.md"
      provides: "2 MV3-survivability rows flipped Blocker -> Covered with Plan 05-02 + Plan 05-03 backlink SHAs"
      contains: "Phase 5 (FSB v0.10.0-attempt-2)"
  key_links:
    - from: "lattice/packages/lattice/src/index.ts"
      to: "lattice/packages/lattice/src/runtime/survivability.ts"
      via: "export { createNoopSurvivabilityAdapter } from './runtime/survivability.js'"
      pattern: "from \"./runtime/survivability.js\""
    - from: "lattice/packages/lattice/dist/index.js"
      to: "lattice/packages/lattice/src/index.ts"
      via: "tsdown build"
      pattern: "createNoopSurvivabilityAdapter"
---

<objective>
Wave 2 of Phase 5 (Lattice-side, depends on Plan 05-02): publish Plan 05-02's survivability primitive on Lattice's public surface so FSB-side code can consume it via the existing `'lattice'` bare specifier. Two commits land on Lattice's `fsb-integration-experiments` branch:

1. **`feat(api): re-export survivability adapter contract`** -- adds 1 value re-export + 5 type re-exports to `lattice/packages/lattice/src/index.ts`; rebuilds `dist/` via `pnpm build` (dist/ is gitignored but rebuilt locally per Phase 2/3/4 ceremony).

2. **`docs(fsb-integration): close Phase 5 audit rows (MV3-survivability)`** -- flips the 2 MV3-survivability Blocker rows in `lattice/docs/fsb-integration-gaps.md` (lines ~70 + ~72) to `Covered` with backlink SHAs referencing Plan 05-02 + the re-export commit from step 1 above.

Purpose: this plan unblocks Wave 3. Plan 05-04 (offscreen Lattice host) and Plan 05-05 (FSB standalone adapter) both import `createNoopSurvivabilityAdapter` (or the types) from `'lattice'` via the bare specifier; without the re-export they cannot reach the new surface. The audit-doc closure preserves the cross-repo trace from Phase 1's audit doc -> Phase 5's actual closure, mirroring the Phase 2/3/4 pattern (each phase flipped specific row(s) with backlink SHAs in the Notes column).

Output: 2 Lattice files modified (`src/index.ts` + `docs/fsb-integration-gaps.md`). 2 Lattice commits with Ref footers. NO push. NO FSB-side modifications in this plan (Plan 05-06 W4 handles FSB-side LATTICE-PIN.md bump that records both Plan 05-02 + Plan 05-03 + Plan 05-04 Lattice work).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/LATTICE-PIN.md
@.planning/phases/05-mv3-survivability-bundler/05-CONTEXT.md
@.planning/phases/05-mv3-survivability-bundler/05-02-PLAN.md
@lattice/packages/lattice/src/index.ts
@lattice/docs/fsb-integration-gaps.md

<interfaces>
<!-- The exact line-ranges + insertion points in the two files this plan modifies. -->

lattice/packages/lattice/src/index.ts (134 lines, Phase 4 HEAD):
  - Existing structure: value exports block (lines 1-58) + type-only exports block (lines 59-134).
  - Insertion point for value export: between line 52 (`export { createAI } from "./runtime/create-ai.js";`) and line 53 (`export { createMemorySessionStore } from "./sessions/session.js";`) -- alphabetical-runtime-module ordering keeps createAI -> createNoopSurvivabilityAdapter -> createMemorySessionStore.
  - Insertion point for type exports: append after line 134 (`} from "./runtime/public-types.js";`) -- a new type-export block from `./runtime/survivability.js`.

lattice/docs/fsb-integration-gaps.md (93 lines, Phase 4 HEAD):
  - MV3-survivability section: lines 63-72.
  - Row 1 (line ~70): "Adapter contract for runtimes whose execution context can be evicted mid-flow (resume-from-checkpoint, conservative recovery, idle-kill survival)" = Status "Needs addition" / Severity "Blocker".
  - Row 2 (line ~72): "Cross-process resumption from persisted state envelope" = Status "Needs addition" / Severity "Blocker".
  - Each row flips: Status -> "Covered" + Severity -> "n/a" + Notes cell rewritten to cite Plan 05-02 short-SHA + Plan 05-03 re-export commit short-SHA + Plan 05-03 audit-doc commit short-SHA. Mirrors Phase 2/3/4 audit-doc closure prose.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-export survivability surface from src/index.ts + rebuild dist/</name>
  <files>lattice/packages/lattice/src/index.ts</files>
  <action>
First, capture the Plan 05-02 Lattice short-SHA for the commit body footer:
```
cd lattice && git log -1 --format=%h packages/lattice/src/runtime/survivability.ts
```
Record the short-SHA (e.g., `abc1234`) -- needed for the commit body in Task 2 below AND for the audit-doc Notes column in this task's audit-doc commit (which happens AFTER this task, but the SHA is also captured here for the SUMMARY).

Read the current `lattice/packages/lattice/src/index.ts` (it should be 134 lines; line 52 is `export { createAI } from "./runtime/create-ai.js";`). Apply two surgical insertions:

**Insertion 1: value export.** Between line 52 (`createAI`) and line 53 (`createMemorySessionStore`), insert:
```typescript
export { createNoopSurvivabilityAdapter } from "./runtime/survivability.js";
```

**Insertion 2: type-only re-exports.** After line 134 (the closing `} from "./runtime/public-types.js";`), append a new type-export block:
```typescript
export type {
  EvictionHook,
  ResumePolicy,
  SerializedSnapshot,
  SurvivabilityAdapter,
  UnsubscribeFn,
} from "./runtime/survivability.js";
```

The resulting file has 134 + 1 + 7 = 142 lines (1 line for the value export + 7 lines for the type-only block including opening/closing braces).

NOTE on alphabetical ordering: Lattice's existing src/index.ts has alphabetical-ish runtime-module ordering for the value exports (createAI -> createMemorySessionStore via the "runtime" prefix grouping). `createNoopSurvivabilityAdapter` slots in between because `createN` > `createAI` lexically (Phase 5 follows the same convention Phase 4 used when it added `createXaiProvider` at line 41 alphabetically among the providers block).

NOTE on type-only ordering: the appended type-export block stays SEPARATE from the existing big type-export block (lines 60-134 -- "public-types") because survivability types live in their own module under runtime/. This mirrors how Phase 4 added a separate type-export block for provider option types (lines 33, 36, 38, 40, 42 -- each adapter's *Options type re-exported alongside its factory). The alternative -- merging into the public-types.ts re-export block -- would require modifying public-types.ts which is BYTE-FROZEN through Phase 4. Keeping the survivability types in a separate block honors the byte-freeze.

Verify the edits:
```
grep -c "createNoopSurvivabilityAdapter" lattice/packages/lattice/src/index.ts
grep -c "SurvivabilityAdapter" lattice/packages/lattice/src/index.ts
grep -c "ResumePolicy" lattice/packages/lattice/src/index.ts
```
Expected: 1 + 1 + 1 (single occurrence in src/index.ts each).

Rebuild Lattice's dist/ (the local build is the only consumption surface for FSB; the file: dep resolves the package's `exports` map to dist/index.js):
```
cd lattice && pnpm --filter lattice build 2>&1 | tail -5
```
Expected: tsdown emits dist/ cleanly; no errors. The dist/ directory is gitignored in Lattice (per Lattice AGENTS.md / .gitignore convention) but rebuilt locally per Phase 2/3/4 ceremony.

Verify dist/index.js carries the new export:
```
grep -c "createNoopSurvivabilityAdapter" lattice/packages/lattice/dist/index.js
```
Expected: >= 1.

Bare-specifier reachability probe from FSB's working directory (the file: dep resolves `'lattice'` -> `lattice/packages/lattice/dist/index.js`):
```
cd /Users/lakshmanturlapati/Desktop/FSB/automation && node -e "import('lattice').then(m => { console.log('createNoopSurvivabilityAdapter:', typeof m.createNoopSurvivabilityAdapter); if (typeof m.createNoopSurvivabilityAdapter !== 'function') { process.exit(1); } })"
```
Expected: `createNoopSurvivabilityAdapter: function`. Exit 0.

Phase 1-4 carryforward surface still reachable:
```
cd /Users/lakshmanturlapati/Desktop/FSB/automation && node -e "import('lattice').then(m => { const required = ['createReceipt','verifyReceipt','createHookPipeline','createCheckpointHook','createAnthropicProvider','createGeminiProvider','createXaiProvider','createOpenRouterProvider','createLmStudioProvider','STEP_TRANSITION_EVENT_NAME','DEFAULT_CHECKPOINT_BAND']; for (const name of required) { if (m[name] === undefined) { console.error('FAIL: missing', name); process.exit(1); } } console.log('PASS: 11 carryforward exports present'); })"
```
Expected: `PASS: 11 carryforward exports present`. Exit 0.

Now commit on Lattice's `fsb-integration-experiments` branch (D-25 ceremony):
```
cd lattice && git checkout fsb-integration-experiments && git add packages/lattice/src/index.ts && git commit -m "$(cat <<'EOF'
feat(api): re-export survivability adapter contract

Phase 5 (FSB v0.10.0-attempt-2) -- expose Plan 05-02's
MV3-survivability adapter contract on Lattice's public surface.

Adds:
  - createNoopSurvivabilityAdapter (value re-export)
  - SurvivabilityAdapter<TState>, SerializedSnapshot, EvictionHook<TState>,
    UnsubscribeFn, ResumePolicy (type-only re-exports)

Resolution path:
  consumer -> 'lattice' -> dist/index.js -> ./runtime/survivability.js

Lattice vitest unchanged (re-export adds zero tests). FSB consumes via the
existing file:./lattice/packages/lattice path dep (Phase 1 wiring). Bare-
specifier probe from FSB root confirms function-type reachability for
createNoopSurvivabilityAdapter; 11 Phase 1-4 carryforward exports still
reachable (createReceipt, verifyReceipt, createHookPipeline,
createCheckpointHook, 5 provider factories, 2 step-marker constants).

Phase 1-4 public-surface BYTE-FROZEN at line-range level outside the two
narrow insertions documented in the Phase 5 Plan 05-03 plan:
  - Line 52->53: createAI -> createNoopSurvivabilityAdapter (alphabetical
    runtime-module ordering)
  - End-of-file: separate type-only block from ./runtime/survivability.js
    (survivability types live in their own module, not in public-types.ts)

Ref: FSB v0.10.0-attempt-2 Phase 5
EOF
)"
```

Verify the commit landed:
```
cd lattice && git log -1 --pretty=%s
```
Expected: `feat(api): re-export survivability adapter contract`.

Capture this commit's short-SHA for Task 2 (the audit-doc Notes column cites it):
```
cd lattice && git log -1 --format=%h
```
Record (e.g., `def5678`) -- referred to as `REEXPORT_SHA` below.
  </action>
  <verify>
    <automated>[ "$(grep -c 'createNoopSurvivabilityAdapter' lattice/packages/lattice/src/index.ts)" = "1" ] && echo "PASS: createNoopSurvivabilityAdapter re-export present" && [ "$(grep -c 'SurvivabilityAdapter' lattice/packages/lattice/src/index.ts)" = "1" ] && echo "PASS: SurvivabilityAdapter type re-export" && [ "$(grep -c 'ResumePolicy' lattice/packages/lattice/src/index.ts)" = "1" ] && echo "PASS: ResumePolicy type re-export" && (cd lattice && pnpm --filter lattice build 2>&1 | tail -2) && grep -q "createNoopSurvivabilityAdapter" lattice/packages/lattice/dist/index.js && echo "PASS: dist rebuilt" && node -e "import('lattice').then(m => { if (typeof m.createNoopSurvivabilityAdapter !== 'function') { process.exit(1); } })" && echo "PASS: bare-specifier reachability" && (cd lattice && git log -1 --pretty=%s | grep -q "feat(api): re-export survivability adapter contract") && echo "PASS: re-export commit landed" && [ "$(cd lattice && git log -1 --format=%B | grep -c 'Ref: FSB v0.10.0-attempt-2 Phase 5')" = "1" ] && echo "PASS: Ref footer"</automated>
  </verify>
  <done>
- `lattice/packages/lattice/src/index.ts` has `createNoopSurvivabilityAdapter` value re-export + 5 type-only re-exports.
- `lattice/packages/lattice/dist/index.js` carries the new export (tsdown rebuilt cleanly).
- `await import('lattice')` from FSB root resolves `createNoopSurvivabilityAdapter` as `function`.
- 11 Phase 1-4 carryforward exports still reachable (regression-free).
- ONE Lattice commit `feat(api): re-export survivability adapter contract` on `fsb-integration-experiments` with Ref footer. No push.
- D-13 honored (public surface re-exports).
  </done>
</task>

<task type="auto">
  <name>Task 2: Flip 2 MV3-survivability audit-doc rows to Covered (D-14)</name>
  <files>lattice/docs/fsb-integration-gaps.md</files>
  <action>
First, capture the relevant short-SHAs for the Notes column citations:

```
cd lattice
PLAN_02_SHA=$(git log --format=%h --all -- packages/lattice/src/runtime/survivability.ts | tail -1)
REEXPORT_SHA=$(git log -1 --format=%h packages/lattice/src/index.ts)
echo "PLAN_02_SHA=$PLAN_02_SHA"
echo "REEXPORT_SHA=$REEXPORT_SHA"
```

These two SHAs are cited verbatim in the audit-doc row Notes (mirrors Phase 2/3/4 audit-doc closure prose pattern).

Read the current `lattice/docs/fsb-integration-gaps.md`. The MV3-survivability section spans lines 63-72. There are 4 rows in that section:

- Row 1 (line ~68): "Single-invocation runtime facade (createAI)" -- ALREADY Covered, DO NOT modify.
- Row 2 (line ~70): "Adapter contract for runtimes whose execution context can be evicted mid-flow (resume-from-checkpoint, conservative recovery, idle-kill survival)" -- currently `Needs addition` / `Blocker`. FLIP to `Covered` / `n/a`.
- Row 3 (line ~71): "Memory session store sufficient for in-process run history" -- ALREADY Covered, DO NOT modify.
- Row 4 (line ~72): "Cross-process resumption from persisted state envelope" -- currently `Needs addition` / `Blocker`. FLIP to `Covered` / `n/a`.

Apply exactly two row rewrites (no other lines modified). Mirror the Phase 4 prose tone (e.g., the Anthropic row at line 46 reads "Phase 4 (FSB v0.10.0-attempt-2) added ... Lattice commits cf31d82 + e5659a8 + f9c7ef4").

**Row 2 rewrite** (the "Adapter contract" row at line ~70). Before:
```
| MV3-survivability | Adapter contract for runtimes whose execution context can be evicted mid-flow (resume-from-checkpoint, conservative recovery, idle-kill survival) | Needs addition | Blocker | FSB attempt-1 _al_handleRestoredMode is exactly this. Per INV-06 the adapter contract lives in Lattice; the MV3 implementation is FSB-side glue. Phase candidate. |
```

After:
```
| MV3-survivability | Adapter contract for runtimes whose execution context can be evicted mid-flow (resume-from-checkpoint, conservative recovery, idle-kill survival) | Covered | n/a | Phase 5 (FSB v0.10.0-attempt-2) added `lattice/packages/lattice/src/runtime/survivability.ts` -- `SurvivabilityAdapter<TState>` interface (4 methods: serialize, deserialize, onEviction, resume) + `createNoopSurvivabilityAdapter()` reference impl + 12+ vitest cases covering shape conformance + JSON round-trip + composition with v1.1 ReceiptEnvelope (Test 12 exercises DSSE+JCS round-trip with real ephemeral Ed25519 keypair). ResumePolicy literal-union: SAFE | RECOVERY_AMBIGUOUS | ON_ERROR_SW_EVICTION_MID_REQUEST | ON_ERROR_SW_EVICTION_MID_TOOL_DISPATCH (carries forward FSB attempt-1 02-04-PLAN.md CONSERVATIVE recovery taxonomy per CONTEXT.md CD-E). Composition conventions documented in JSDoc (not enforced): D-09 onEviction hooks SHOULD register in BAND.SAFETY (Phase 2); D-10 SerializedSnapshot.payload MAY embed v1.1 ReceiptEnvelope from Phase 3 createCheckpointHook. CONSERVATIVE recovery dispatcher EXPLICITLY OUT OF SCOPE per D-22 (deferred to follow-on milestone; agent-loop.js byte-frozen in Phase 5). Lattice commits `<PLAN_02_SHA>` (adapter + tests) + `<REEXPORT_SHA>` (public surface re-export). FSB-side standalone adapter (chrome.storage.session backed) lands in Plan 05-05; hybrid offscreen Lattice host lands in Plan 05-04. |
```

(Replace `<PLAN_02_SHA>` and `<REEXPORT_SHA>` with the captured short-SHAs from the script above.)

**Row 4 rewrite** (the "Cross-process resumption" row at line ~72). Before:
```
| MV3-survivability | Cross-process resumption from persisted state envelope | Needs addition | Blocker | Phase candidate (same phase as eviction adapter contract). |
```

After:
```
| MV3-survivability | Cross-process resumption from persisted state envelope | Covered | n/a | Phase 5 (FSB v0.10.0-attempt-2) -- the SurvivabilityAdapter `serialize` / `deserialize` methods (Plan 05-02) carry the cross-process state envelope; `SerializedSnapshot` is a string-encodable opaque wrapper (`kind: "survivability-snapshot"`, `version: "lattice-survivability/v1"`, `payload: string`, `capturedAt: ISO-8601`) that survives MV3 SW eviction + Cloudflare Worker freeze + Lambda thaw. Test 5 of survivability.test.ts proves byte-equal JSON round-trip; Test 12 proves embedded ReceiptEnvelope DSSE+JCS round-trip under verifyReceipt. The actual MV3 implementation (chrome.storage.session-backed) lands in Plan 05-05 as FSB-side glue per INV-06. Lattice commits `<PLAN_02_SHA>` (interface + ref impl) + `<REEXPORT_SHA>` (public surface re-export). |
```

(Same SHA substitution.)

Verify the diff is exactly 2 rows changed (no other audit-doc rows touched -- Phase 1-4 Covered rows stay Covered):
```
cd lattice && git diff docs/fsb-integration-gaps.md | head -30
```
Visually confirm only the two MV3-survivability rows mutated.

Word-count smell check (audit-doc rows are dense but not paragraph-length):
```
grep -c "Phase 5 (FSB v0.10.0-attempt-2)" lattice/docs/fsb-integration-gaps.md
```
Expected: 2 (one per flipped row).

Confirm no Phase 1-4 Covered rows regressed:
```
grep -c "Phase 4 (FSB v0.10.0-attempt-2) added" lattice/docs/fsb-integration-gaps.md
```
Expected: 5 (5 Phase 4 Provider rows still Covered).

```
grep -c "Phase 2 (FSB v0.10.0-attempt-2)" lattice/docs/fsb-integration-gaps.md
```
Expected: 6 (5 closed Phase 2 rows + 1 added lifecycle row).

```
grep -c "Phase 3 (FSB v0.10.0-attempt-2)" lattice/docs/fsb-integration-gaps.md
```
Expected: 2 (2 Phase 3 Observability rows closed).

If any count differs from expected, the audit-doc has been over-modified -- revert + retry the surgical row rewrites.

Now commit the second Lattice commit of Plan 05-03 (D-25 ceremony):
```
cd lattice && git add docs/fsb-integration-gaps.md && git commit -m "$(cat <<'EOF'
docs(fsb-integration): close Phase 5 audit rows (MV3-survivability)

Phase 5 (FSB v0.10.0-attempt-2) -- flip 2 MV3-survivability Blocker rows
in lattice/docs/fsb-integration-gaps.md from "Needs addition" to "Covered"
with backlink SHAs to Plan 05-02 (adapter contract + ref impl + 12+ tests)
and Plan 05-03 task 1 (public surface re-export + dist/ rebuild).

Rows flipped:
  - "Adapter contract for runtimes whose execution context can be evicted
    mid-flow (resume-from-checkpoint, conservative recovery, idle-kill
    survival)" -- Blocker -> Covered
  - "Cross-process resumption from persisted state envelope" -- Blocker
    -> Covered

Both rows cite the SurvivabilityAdapter<TState> 4-method interface,
ResumePolicy literal-union (SAFE | RECOVERY_AMBIGUOUS |
ON_ERROR_SW_EVICTION_MID_REQUEST | ON_ERROR_SW_EVICTION_MID_TOOL_DISPATCH
per CONTEXT.md CD-E), and the SerializedSnapshot opaque-payload envelope.
CONSERVATIVE recovery dispatcher EXPLICITLY OUT OF SCOPE per D-22.

Phase 1-4 Covered rows preserved untouched:
  - Receipts: 4 rows still Covered (Phase 1 + Phase 2)
  - Tripwires/hooks: 6 rows still Covered (Phase 1 + Phase 2 + Phase 2
    lifecycle addition)
  - Providers: 7 rows still Covered (Phase 1 OpenAI-compat + Phase 4
    5 native adapters + Phase 1 custom OpenAI-compat)
  - Delegation: 2 Covered, 1 Out-of-scope unchanged
  - Observability: 5 rows preserved (Phase 1 + Phase 3 + Phase 3 envelope)
  - MV3-survivability: 2 rows already Covered (Phase 1 createAI + memory
    session store) preserved; 2 rows now flipped by THIS commit

After this commit:
  - 7 of 8 audit-doc Blocker rows closed across Phases 1-5
  - 1 Out-of-scope row remains (Delegation primitive contingent on
    Lattice multi-agent policy; addressed in Phase 6 if Lattice opens
    the policy)
  - 1 Important + 1 Nice-to-have row remain (Observability recovery-
    markers + OTEL exporter; deferred per ROADMAP)

Ref: FSB v0.10.0-attempt-2 Phase 5
EOF
)"
```

Verify the second commit landed:
```
cd lattice && git log -2 --pretty=oneline
```
Expected: two commits, top one = `docs(fsb-integration): close Phase 5 audit rows (MV3-survivability)`, second one = `feat(api): re-export survivability adapter contract`.

Verify Ref footer:
```
cd lattice && git log -1 --format=%B | grep -c "Ref: FSB v0.10.0-attempt-2 Phase 5"
```
Expected: 1.

Verify D-19 NO-push contract:
```
cd lattice && git reflog -20 | grep -c push
```
Expected: 0.

Capture new Lattice HEAD SHA for Plan 05-04 + Plan 05-06:
```
cd lattice && git rev-parse HEAD
```
Record full SHA + short SHA. Plan 05-04 imports from `'lattice'` (which now reaches the new surface via this plan's re-export); Plan 05-06 records this SHA in `.planning/LATTICE-PIN.md`'s frontmatter.

Return to FSB working directory:
```
cd /Users/lakshmanturlapati/Desktop/FSB/automation
```
  </action>
  <verify>
    <automated>[ "$(grep -c 'Phase 5 (FSB v0.10.0-attempt-2)' lattice/docs/fsb-integration-gaps.md)" = "2" ] && echo "PASS: 2 Phase 5 rows flipped" && [ "$(grep -c 'Phase 4 (FSB v0.10.0-attempt-2) added' lattice/docs/fsb-integration-gaps.md)" = "5" ] && echo "PASS: Phase 4 5 Provider rows preserved" && [ "$(grep -c 'Phase 2 (FSB v0.10.0-attempt-2)' lattice/docs/fsb-integration-gaps.md)" = "6" ] && echo "PASS: Phase 2 6 rows preserved" && [ "$(grep -c 'Phase 3 (FSB v0.10.0-attempt-2)' lattice/docs/fsb-integration-gaps.md)" = "2" ] && echo "PASS: Phase 3 2 rows preserved" && (cd lattice && git log -1 --pretty=%s | grep -q "docs(fsb-integration): close Phase 5 audit rows") && echo "PASS: docs commit landed" && [ "$(cd lattice && git log -2 --format=%B | grep -c 'Ref: FSB v0.10.0-attempt-2 Phase 5')" = "2" ] && echo "PASS: 2 Ref footers (both Plan 05-03 commits)" && [ "$(cd lattice && git reflog -20 | grep -c push)" = "0" ] && echo "PASS: D-19 NO-push"</automated>
  </verify>
  <done>
- `lattice/docs/fsb-integration-gaps.md` has exactly 2 MV3-survivability rows flipped Blocker -> Covered with backlink SHAs to Plan 05-02 + Plan 05-03 re-export commit.
- Phase 1-4 Covered rows preserved untouched (grep probes verify counts).
- ONE Lattice commit `docs(fsb-integration): close Phase 5 audit rows (MV3-survivability)` on `fsb-integration-experiments` with Ref footer.
- Across Plan 05-02 + Plan 05-03 total: 3 Lattice commits, all with Ref footers, all on `fsb-integration-experiments`, none pushed.
- D-14 + D-25 honored.
- LSDK-21 (public surface re-export) + LSDK-22 (audit-doc closure) covered.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lattice public surface (src/index.ts) -> FSB consumer | The re-export becomes part of Lattice's public API surface; threat = breaking change for existing Lattice consumers |
| Audit-doc Notes column -> downstream auditors | The Notes cells become primary cross-repo trace; threat = inaccurate SHA references break the audit chain |
| dist/index.js -> bare-specifier import | Threat = stale dist not rebuilt locally so FSB consumes the pre-Plan-05-02 surface (missing the new export) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Tampering | src/index.ts re-export ordering | mitigate | Alphabetical-runtime-module ordering documented (createAI -> createNoopSurvivabilityAdapter -> createMemorySessionStore); type-only block kept SEPARATE from existing public-types.ts re-export block (preserves public-types.ts byte-freeze through Phase 4) |
| T-05-03-02 | Information Disclosure | Notes column SHA references | mitigate | Both SHAs captured from `git log` in the same shell session before the commit body is composed; commit body shows the captured values; manual visual verify before commit |
| T-05-03-03 | Repudiation | dist/ stale | mitigate | `pnpm --filter lattice build` runs after src/index.ts edit + bare-specifier probe asserts createNoopSurvivabilityAdapter is function (would fail if dist not rebuilt) |
| T-05-03-04 | Tampering | Phase 1-4 audit-doc Covered rows | mitigate | 4 grep probes assert Phase 2 + Phase 3 + Phase 4 row counts unchanged after Plan 05-03 (Phase 4 = 5 rows; Phase 2 = 6 rows including additive lifecycle; Phase 3 = 2 rows) |
| T-05-03-05 | Spoofing | Ref footer absence | mitigate | `git log -1 --format=%B \| grep -c "Ref: FSB v0.10.0-attempt-2 Phase 5"` returns 1 after each commit; D-25 ceremony enforced |
| T-05-03-06 | Denial of Service | tsdown build failure | accept | Lattice's existing tsdown config is the authoritative builder; if it fails, the failure is upstream of FSB's Plan 05-03 scope and would have been caught in Plan 05-02 (which also ran typecheck) |
</threat_model>

<verification>
1. `lattice/packages/lattice/src/index.ts` has createNoopSurvivabilityAdapter value re-export + 5 type re-exports.
2. `lattice/packages/lattice/dist/index.js` carries createNoopSurvivabilityAdapter (tsdown rebuilt).
3. `await import('lattice')` from FSB root resolves createNoopSurvivabilityAdapter as `function`.
4. 11 Phase 1-4 carryforward exports still reachable.
5. `lattice/docs/fsb-integration-gaps.md` has 2 MV3-survivability rows flipped Blocker -> Covered.
6. Phase 1-4 audit-doc Covered rows preserved (grep counts: Phase 2 = 6, Phase 3 = 2, Phase 4 = 5).
7. TWO Lattice commits on `fsb-integration-experiments` (re-export + audit-doc) both with Ref footers.
8. `cd lattice && git reflog | grep -c push` = 0 (D-19).
9. Lattice vitest unchanged (no test files modified; same >= 409 PASS baseline).
</verification>

<success_criteria>
- D-13 (public surface re-export) + D-14 (audit-doc closure) + D-25 (Lattice commit ceremony) honored.
- LSDK-21 + LSDK-22 covered.
- Wave 3 plans unblocked: Plan 05-04 can `import` from `'lattice'` and reach createNoopSurvivabilityAdapter; Plan 05-05 can do the same.
- ZERO FSB-side modifications in this plan.
- Cross-repo audit chain extended (Phase 5 row counts: 2 closures + 0 regressions).
</success_criteria>

<output>
After completion, create `.planning/phases/05-mv3-survivability-bundler/05-03-SUMMARY.md` documenting:
- Plan 05-02 short-SHA + Plan 05-03 re-export short-SHA + Plan 05-03 audit-doc short-SHA (all captured from `cd lattice && git log`)
- src/index.ts pre/post line counts (134 -> 142)
- Bare-specifier reachability probe output (createNoopSurvivabilityAdapter = function)
- 11 carryforward exports still reachable (verification listing)
- Audit-doc row count probes (Phase 2 = 6, Phase 3 = 2, Phase 4 = 5, Phase 5 = 2)
- Ref footer count = 2 (one per commit)
- New Lattice HEAD SHA (Plan 05-04 + Plan 05-06 consume)
</output>
</content>
</invoke>