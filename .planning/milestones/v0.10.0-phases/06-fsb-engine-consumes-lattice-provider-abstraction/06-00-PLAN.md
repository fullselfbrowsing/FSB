---
phase: 06-fsb-engine-consumes-lattice-provider-abstraction
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/lattice-provider-bridge-smoke.test.js
  - package.json
autonomous: true
requirements:
  - FINT-07
  - FINT-08
requirements_addressed:
  - FINT-07
  - FINT-08

must_haves:
  truths:
    - "A new test file `tests/lattice-provider-bridge-smoke.test.js` exists as the per-task verification harness for every Phase 6 downstream plan. Per RESEARCH Section 15 + VALIDATION Wave 0 Requirements."
    - "The harness contains 6 Part placeholders (Part 1 surface presence; Part 2 per-provider round-trip; Part 3 error envelope; Part 4 abort propagation; Part 5 flag/trim/options grep; Part 6 INV byte-freeze). At least Part 0 placeholder (smoke harness boot) PASSes; Parts 1-6 are inert markers that PASS-when-empty so downstream plans can wire assertions incrementally without flipping the suite red."
    - "The harness includes inline mocks for `chrome.runtime` (sendMessage + onMessage; emulates Chrome 105+ rejection behavior when no listener) AND `chrome.offscreen` (hasDocument + createDocument with idempotency) per RESEARCH Section 8 + Section 9. Mocks DO NOT mock `lattice` itself -- real `await import('lattice')` per FSB convention 'Real runtime tests, not static-text'."
    - "`package.json` `scripts.test` chain gains the new smoke as the FINAL entry (after `tests/lattice-survivability-smoke.test.js`). Phase 1+2+3+4+5 entries BYTE-FROZEN order."
    - "Plan 06-00 lands as ONE FSB commit `test(06-00): scaffold lattice-provider-bridge smoke harness (Wave 0)` with Ref footer + plan-ID. Test file present + executable + green (>= 1 PASS so the chain stays green)."
  artifacts:
    - path: "tests/lattice-provider-bridge-smoke.test.js"
      provides: "Wave 0 smoke scaffold: 6 Part placeholders + chrome.runtime mock + chrome.offscreen mock + real lattice import; pattern matches Phase 5 lattice-survivability-smoke.test.js conventions"
      min_lines: 180
      contains: "lattice-provider-bridge"
    - path: "package.json"
      provides: "scripts.test chain extended with the new smoke as the LAST entry; Phase 1-5 entries byte-frozen"
      contains: "lattice-provider-bridge-smoke"
  key_links:
    - from: "tests/lattice-provider-bridge-smoke.test.js"
      to: "lattice (bare specifier)"
      via: "await import('lattice')"
      pattern: "await import"
    - from: "package.json scripts.test"
      to: "tests/lattice-provider-bridge-smoke.test.js"
      via: "&& chain append (LAST entry)"
      pattern: "lattice-provider-bridge-smoke.test.js"
---

<objective>
Wave 0 of Phase 6: scaffold the per-task verification harness at `tests/lattice-provider-bridge-smoke.test.js` BEFORE any Phase 6 plan that depends on the bridge runs. Per Nyquist validation contract (06-VALIDATION.md): every downstream Phase 6 task references a Part of this smoke; the file MUST exist with placeholder Parts wired in so executors can incrementally fill them without flipping the suite red.

Per RESEARCH Section 8 + Section 15 + Section 9: the harness follows Phase 5 `tests/lattice-survivability-smoke.test.js` conventions (CJS `'use strict'`, `passAssert` / `passAssertEqual` counters, `process.exit(failed > 0 ? 1 : 0)`, 6-Part structure, real `await import('lattice')`, inline mocks ONLY for chrome.* APIs that don't exist in Node). The harness is the canonical place where every downstream plan asserts its observable behaviour; this plan ships it empty-but-green so the &&-chain stays green.

Per CONTEXT.md scope-locks: no Lattice-side code touched (INV-06). No `extension/*` source touched (Wave 0 is test-scaffolding only). No fixtures added (smoke is self-contained per RESEARCH Section 8). No new framework installed (Node built-in only).

Output: 1 new file (`tests/lattice-provider-bridge-smoke.test.js`) + 1 surgical edit (`package.json` scripts.test chain append). ONE FSB commit. Phase 1-5 smokes byte-frozen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/LATTICE-PIN.md
@.planning/phases/06-fsb-engine-consumes-lattice-provider-abstraction/06-CONTEXT.md
@.planning/phases/06-fsb-engine-consumes-lattice-provider-abstraction/06-RESEARCH.md
@.planning/phases/06-fsb-engine-consumes-lattice-provider-abstraction/06-VALIDATION.md
@package.json
@tests/lattice-survivability-smoke.test.js
@tests/lattice-providers-smoke.test.js

<interfaces>
<!-- FSB test convention (extracted from Phase 5 smoke at tests/lattice-survivability-smoke.test.js:38-93). -->

Test harness shape:
  - 'use strict' at top of file
  - let passed = 0; let failed = 0;
  - function passAssert(cond, msg)
  - function passAssertEqual(actual, expected, msg)
  - top-level async IIFE wrapping all Parts
  - process.exit(failed > 0 ? 1 : 0)
  - PASS / FAIL prefix in console.log lines

Real lattice import (no mocks):
  - const lattice = await import('lattice');   // dynamic ESM-from-CJS

chrome.runtime mock pattern (NEW for Phase 6; extension of Phase 5 pattern):
  function createChromeRuntimeMock(handler) {
    const listeners = handler ? [handler] : [];
    return {
      id: 'fsb-test-extension-id',
      onMessage: { addListener(fn) { listeners.push(fn); } },
      sendMessage(message) {
        return new Promise((resolve, reject) => {
          if (listeners.length === 0) {
            reject(new Error('Could not establish connection. Receiving end does not exist.'));
            return;
          }
          const sender = { id: 'fsb-test-extension-id' };
          let responded = false;
          const sendResponse = (envelope) => { if (!responded) { responded = true; resolve(envelope); } };
          let kept = false;
          for (const l of listeners) {
            const ret = l(message, sender, sendResponse);
            if (ret === true) { kept = true; break; }
          }
          if (!kept && !responded) resolve(undefined);
        });
      }
    };
  }

chrome.offscreen mock pattern:
  function createChromeOffscreenMock() {
    let docOpen = false;
    let createCount = 0;
    return {
      async hasDocument() { return docOpen; },
      async createDocument(opts) {
        if (docOpen) throw new Error('Only a single offscreen document may be created.');
        docOpen = true;
        createCount++;
        return undefined;
      },
      async closeDocument() { docOpen = false; },
      _createCount() { return createCount; }
    };
  }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the Wave 0 smoke scaffold with all 6 Part placeholders + chrome.* mocks</name>
  <read_first>
    - tests/lattice-survivability-smoke.test.js (entire file -- canonical Phase 5 smoke pattern; copy harness shape verbatim)
    - tests/lattice-providers-smoke.test.js (Phase 4 surface-presence pattern; informative)
    - .planning/phases/06-fsb-engine-consumes-lattice-provider-abstraction/06-RESEARCH.md sections 8, 9, 15 (test framework + chrome.* mocks + 6-Part structure)
    - .planning/phases/06-fsb-engine-consumes-lattice-provider-abstraction/06-VALIDATION.md (per-task verification map showing which Part each downstream task asserts into)
    - package.json (scripts.test chain -- copy the trailing `&& node tests/lattice-survivability-smoke.test.js` pattern as the model for the append)
  </read_first>
  <files>tests/lattice-provider-bridge-smoke.test.js</files>
  <behavior>
    - File compiles + runs under `node tests/lattice-provider-bridge-smoke.test.js`
    - Test passes (>= 1 PASS, 0 FAIL) when nothing downstream is implemented (placeholders are inert; not "not implemented" FAILs)
    - `await import('lattice')` succeeds and `lattice.createXaiProvider` / `lattice.createOpenAIProvider` / `lattice.createAnthropicProvider` / `lattice.createGeminiProvider` / `lattice.createOpenRouterProvider` / `lattice.createLmStudioProvider` / `lattice.createOpenAICompatibleProvider` are all `typeof === 'function'` (Part 1 surface presence -- the 7 factory functions Phase 6 will dispatch to)
    - chrome.runtime mock + chrome.offscreen mock helpers are defined at module top (re-usable by downstream Part fills) and exported via a `module.exports` or test-internal scope -- NOT polluting `globalThis` until each Part needs to (downstream Parts will set `globalThis.chrome = ...` inside their IIFE)
    - 6 Part headers printed via `console.log('\n--- Part N: <name> ---')` so executors of downstream plans can see which Parts they need to fill
  </behavior>
  <action>
    Create `tests/lattice-provider-bridge-smoke.test.js` (~200-250 lines). Skeleton:

    1. Top-of-file JSDoc block (~25 lines) documenting Phase 6 Plan 06-00 Wave 0 purpose; mention coverage map (Part 1-6 -> FINT-07a/b, FINT-08a/b/c/d/e, INV-04). Mirror the Phase 5 JSDoc block style at tests/lattice-survivability-smoke.test.js:3-36.

    2. `'use strict';` then `let passed = 0; let failed = 0;` then `function passAssert(cond, msg)` + `function passAssertEqual(actual, expected, msg)` -- VERBATIM from tests/lattice-survivability-smoke.test.js:38-56.

    3. Helper `function createChromeRuntimeMock(initialHandlers)` -- copy the pattern from <interfaces> block above. Default `initialHandlers = []`. `sendMessage` returns Promise that rejects with `'Could not establish connection. Receiving end does not exist.'` when listeners empty (Chrome 105+ behaviour per RESEARCH Section 6); resolves with sendResponse envelope when a listener returns true; resolves with `undefined` if no listener returns true.

    4. Helper `function createChromeOffscreenMock()` -- copy verbatim from <interfaces> block above. Tracks `createCount` for idempotency assertions (Part 5).

    5. Helper `async function loadOffscreenHandlerSource()` -- placeholder for Plan 06-01 to fill; returns the offscreen handler module's listener function. For Wave 0 returns `null` (downstream plans will mock the require/import).

    6. Top-level async IIFE that:
       a. Imports lattice: `const lattice = await import('lattice');` with try/catch + helpful error message on failure (mirror tests/lattice-survivability-smoke.test.js:99-106).
       b. Prints `console.log('\n--- Lattice Phase 6 provider-bridge smoke ---');`
       c. Runs all 6 Parts in sequence (each Part is a `console.log` header + the inert assertions described below). NO Part throws.
       d. Final summary: `console.log('\nSummary: ' + passed + ' PASS / ' + failed + ' FAIL');` then `process.exit(failed > 0 ? 1 : 0);`

    7. Part 1 (Surface presence) placeholder -- ACTUAL PASSes (these are real Wave 0 deliverables; downstream Plans 06-01 + 06-03 add more):
       - `passAssertEqual(typeof lattice.createXaiProvider, 'function', 'lattice.createXaiProvider reachable')`
       - `passAssertEqual(typeof lattice.createOpenAIProvider, 'function', 'lattice.createOpenAIProvider reachable')`
       - `passAssertEqual(typeof lattice.createAnthropicProvider, 'function', 'lattice.createAnthropicProvider reachable')`
       - `passAssertEqual(typeof lattice.createGeminiProvider, 'function', 'lattice.createGeminiProvider reachable')`
       - `passAssertEqual(typeof lattice.createOpenRouterProvider, 'function', 'lattice.createOpenRouterProvider reachable')`
       - `passAssertEqual(typeof lattice.createLmStudioProvider, 'function', 'lattice.createLmStudioProvider reachable')`
       - `passAssertEqual(typeof lattice.createOpenAICompatibleProvider, 'function', 'lattice.createOpenAICompatibleProvider reachable')`
       - Mark with code comment `// Plan 06-01 + 06-03 add executeViaBridge + offscreen handler presence checks here.`

    8. Parts 2-6 (per-provider round-trip; error envelope; abort; flag/trim/options; INV byte-freeze) -- INERT placeholders for Wave 0. Each Part prints its `--- Part N ---` header, then includes a single placeholder PASS:
       ```js
       passAssert(true, 'Plan 06-00 Wave 0 placeholder -- downstream plan fills this Part');
       ```
       Mark each Part with a `// Plan 06-XX will populate:` code comment indicating which downstream plan owns the fills (per VALIDATION.md per-task map):
       - Part 2: Plan 06-01 (per-provider message-bus round-trip via mock fetch x 7)
       - Part 3: Plan 06-01 + Plan 06-03 (error envelope: adapter_error / host_unreachable / invalid_provider)
       - Part 4: Plan 06-01 (AbortController propagation, 2 PASS)
       - Part 5: Plan 06-02 + Plan 06-03 + Plan 06-04 (importScripts insertion grep + flag-on/off + saveSettings trim grep + checkApiConnection field-read grep)
       - Part 6: Plan 06-05 (INV-04 setTimeout iterator byte-freeze + INV-01/02 tool-definitions-parity + INV-05 deprecated module absence + INV-06 Lattice byte-freeze)

    9. Export the helpers (`createChromeRuntimeMock`, `createChromeOffscreenMock`) via `module.exports` AT THE END (outside the IIFE; the IIFE only runs when the file is invoked directly via `node tests/...`). Detection pattern: `if (require.main === module) (async () => {...})();` OR keep the IIFE and ALSO export helpers; latter is simpler. Use the simpler pattern (always-run IIFE + module.exports helpers) -- downstream plans will require() the helpers from this file AND independently re-run their own `node tests/...` invocations.

    Reference Phase 5 smoke verbatim for:
    - Line 1-36: JSDoc block style
    - Line 38-56: passAssert + passAssertEqual definitions
    - Line 63-93: chrome.storage.session mock pattern (we adapt for chrome.runtime / chrome.offscreen)
    - Line 95-106: top-level async IIFE + lattice import + helpful error handling
  </action>
  <verify>
    <automated>node tests/lattice-provider-bridge-smoke.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/lattice-provider-bridge-smoke.test.js` exits 0 with `>= 7 PASS / 0 FAIL` (the 7 surface-presence checks + 5 placeholder PASSes = 12 total minimum; some plans may carry more)
    - `grep -c "passAssert" tests/lattice-provider-bridge-smoke.test.js` returns >= 12 (the 7 surface checks + 5 part placeholders + helper definitions)
    - `grep -c "Part" tests/lattice-provider-bridge-smoke.test.js` returns >= 6 (one per Part header)
    - `grep -c "createChromeRuntimeMock" tests/lattice-provider-bridge-smoke.test.js` returns >= 1 (helper defined)
    - `grep -c "createChromeOffscreenMock" tests/lattice-provider-bridge-smoke.test.js` returns >= 1 (helper defined)
    - `grep -c "await import('lattice')" tests/lattice-provider-bridge-smoke.test.js` returns 1 (real Lattice import)
    - `grep -c "module.exports" tests/lattice-provider-bridge-smoke.test.js` returns 1 (helpers exported for downstream re-use)
    - File contains `'use strict';` on line 1 (after JSDoc / shebang)
    - File contains `process.exit(failed > 0 ? 1 : 0)` exactly once
    - File DOES NOT contain `vitest`, `jest`, `mocha`, or `describe(` -- Node built-in only
    - File DOES NOT contain `import.meta` -- CJS-only
  </acceptance_criteria>
  <done>
    `tests/lattice-provider-bridge-smoke.test.js` exists; `node tests/lattice-provider-bridge-smoke.test.js` exits 0 with the placeholder + surface-presence PASSes; 6 Part headers printed; chrome.* mock helpers defined + exported.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append the new smoke to package.json scripts.test chain as the LAST entry (after Phase 5 smoke)</name>
  <read_first>
    - package.json (specifically the scripts.test entry on line 16 -- one long &&-chain; the LAST entry is `node tests/lattice-survivability-smoke.test.js`)
    - .planning/phases/05-mv3-survivability-bundler/05-05-PLAN.md (Task that appended Phase 5 smoke -- reference pattern)
  </read_first>
  <files>package.json</files>
  <action>
    Edit `package.json` line 16 (the long `scripts.test` &&-chain). Append `&& node tests/lattice-provider-bridge-smoke.test.js` to the very end (AFTER `&& node tests/lattice-survivability-smoke.test.js`). The Phase 1+2+3+4+5 smokes (lattice-smoke, lattice-tripwire-smoke, lattice-checkpoint-smoke, lattice-providers-smoke, lattice-survivability-smoke) MUST stay in EXACT same order BYTE-FROZEN; the new entry is purely appended.

    Do NOT modify any other `scripts.test` entry. Do NOT reformat the line. Do NOT change indentation. Do NOT touch any other `package.json` field.

    Exact diff (one location, one append):
    OLD trailing portion:  `... && node tests/lattice-survivability-smoke.test.js"`
    NEW trailing portion:  `... && node tests/lattice-survivability-smoke.test.js && node tests/lattice-provider-bridge-smoke.test.js"`
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "lattice-provider-bridge-smoke" package.json` returns 1 (new entry appended)
    - `grep -c "lattice-survivability-smoke" package.json` returns 1 (Phase 5 entry preserved)
    - `grep -c "lattice-providers-smoke" package.json` returns 1 (Phase 4 entry preserved)
    - `grep -c "lattice-checkpoint-smoke" package.json` returns 1 (Phase 3 entry preserved)
    - `grep -c "lattice-tripwire-smoke" package.json` returns 1 (Phase 2 entry preserved)
    - `grep -c "lattice-smoke.test.js" package.json` returns 1 (Phase 1 entry preserved)
    - The order in `package.json` scripts.test: lattice-smoke -> lattice-tripwire-smoke -> lattice-checkpoint-smoke -> lattice-providers-smoke -> lattice-survivability-smoke -> lattice-provider-bridge-smoke (verify via grep + sort or by reading the line)
    - `npm test` exits 0 (full chain green; the new smoke is now the LAST entry)
  </acceptance_criteria>
  <done>
    `package.json` `scripts.test` ends with `&& node tests/lattice-provider-bridge-smoke.test.js`; Phase 1-5 smoke entries byte-frozen in position; `npm test` exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test ↔ Lattice | Real `await import('lattice')` -- Lattice is treated as the authority for its surface; smoke does NOT re-implement Lattice primitives |
| Test ↔ chrome.* | chrome.runtime + chrome.offscreen mocked in-memory; no real Chrome APIs invoked (Node test env) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-00-01 | Tampering | tests/lattice-provider-bridge-smoke.test.js | accept | Placeholder Parts are inert PASS-when-empty; downstream plans cannot regress green status by failing to populate (they would only ADD assertions). Acceptable because Wave 0 = scaffolding only. |
| T-06-00-02 | Information Disclosure | chrome.runtime mock | mitigate | Mocks do not log message contents. Downstream Parts that exercise real API keys MUST use placeholder values (e.g. 'fsb-fake-test-key') -- enforced at Part-fill time per RESEARCH Security Domain row 'API key leakage via logging'. |
| T-06-00-03 | DoS | npm test chain | accept | Adding one entry to a 110+-step chain has negligible cost (~3s); chain stays under the documented ~3-5min runtime. |
</threat_model>

<verification>
Wave 0 deliverable verification:

```bash
# Smoke runs clean
node tests/lattice-provider-bridge-smoke.test.js

# Full chain still green (Phase 1-5 byte-frozen + new entry green)
npm test

# Surface presence: all 7 Lattice factories reachable from the smoke
node -e "(async()=>{const l=await import('lattice'); for (const f of ['createXaiProvider','createOpenAIProvider','createAnthropicProvider','createGeminiProvider','createOpenRouterProvider','createLmStudioProvider','createOpenAICompatibleProvider']) console.log(f,'=',typeof l[f]);})()"

# Confirm helpers exported for downstream re-use
node -e "const {createChromeRuntimeMock, createChromeOffscreenMock} = require('./tests/lattice-provider-bridge-smoke.test.js'); console.log('Runtime mock fn:', typeof createChromeRuntimeMock, '| Offscreen mock fn:', typeof createChromeOffscreenMock);"
```

INV-04 setTimeout count BYTE-FROZEN baseline established:
```bash
grep -c "setTimeout" extension/ai/agent-loop.js  # MUST be 8 (Phase 5 baseline)
```
</verification>

<success_criteria>
- `tests/lattice-provider-bridge-smoke.test.js` exists with all 6 Part headers + 7 surface-presence PASSes + 5 placeholder PASSes (>= 12 PASS / 0 FAIL minimum)
- `package.json` `scripts.test` has the new smoke as the FINAL entry; Phase 1-5 entries BYTE-FROZEN
- `npm test` exits 0 (full chain green)
- `chrome.runtime` + `chrome.offscreen` mock helpers defined AND exported via `module.exports` so Plans 06-01 / 06-02 / 06-03 / 06-04 / 06-05 can `require()` them
- ZERO extension/* files modified (Wave 0 = test scaffolding only)
- ONE FSB commit: `test(06-00): scaffold lattice-provider-bridge smoke harness (Wave 0)` with `Ref: FSB v0.10.0-attempt-2 Phase 6 Plan 06-00`
- INV-04 setTimeout count in agent-loop.js still = 8 (no production code touched)
</success_criteria>

<output>
After completion, create `.planning/phases/06-fsb-engine-consumes-lattice-provider-abstraction/06-00-SUMMARY.md` per the template at $HOME/.claude/get-shit-done/templates/summary.md.

Include:
- Smoke file path + line count + PASS count
- Helpers exported (createChromeRuntimeMock + createChromeOffscreenMock)
- Per-Part placeholder marker showing which Plan fills which Part (downstream traceability)
- Confirmation Phase 1-5 smoke chain entries byte-frozen
- Confirmation INV-04 setTimeout count = 8 unchanged
- Wave 0 -> Wave 1 handoff signal: downstream Plan 06-01 may now begin
</output>
