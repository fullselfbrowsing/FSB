---
phase: 04-provider-adapter-alignment
plan: 05
type: execute
wave: 3
depends_on:
  - 04-04
files_modified:
  - tests/lattice-providers-smoke.test.js
  - package.json
  - .planning/LATTICE-PIN.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - LSDK-14
  - LSDK-15
  - LSDK-16
  - LSDK-17
  - LSDK-18
must_haves:
  truths:
    - "tests/lattice-providers-smoke.test.js exists and asserts all 5 new factories are reachable via await import('lattice')"
    - "Each new factory invoked with stub options returns ProviderAdapter shape (kind, id, capabilities populated)"
    - "package.json scripts.test chain ends with the new Phase 4 providers smoke (appended after lattice-checkpoint-smoke; Phase 1+2+3 smokes byte-frozen upstream in the chain)"
    - "LATTICE-PIN.md frontmatter current_lattice_sha bumped to the post-Plan-04-04 Lattice HEAD (40-char SHA)"
    - "LATTICE-PIN.md per-phase table appended with Phase 4 row referencing all 8 Phase 4 Lattice commits"
    - "REQUIREMENTS.md populated with LSDK-14..LSDK-18 entries in the LSDK section + 5 traceability table rows"
    - "Full FSB npm test chain exits 0; Phase 1+2+3 smokes byte-frozen (29 + 39 + 72 PASS unchanged); new providers smoke >= 10 PASS"
    - "ONE FSB commit lands: feat(04): ship Phase 4 provider-adapter alignment (surface-presence smoke + audit-trail closure)"
  artifacts:
    - path: "tests/lattice-providers-smoke.test.js"
      provides: "FSB-side thin surface-presence smoke for the 5 new factories"
      contains: "createAnthropicProvider"
      min_lines: 150
    - path: "package.json"
      provides: "scripts.test chain extended with the new providers smoke"
      contains: "lattice-providers-smoke"
    - path: ".planning/LATTICE-PIN.md"
      provides: "current_lattice_sha bumped + Phase 4 audit row appended"
      contains: "Phase 4"
    - path: ".planning/REQUIREMENTS.md"
      provides: "LSDK-14..18 entries + traceability rows"
      contains: "LSDK-14"
  key_links:
    - from: "tests/lattice-providers-smoke.test.js"
      to: "lattice (bare specifier)"
      via: "await import('lattice')"
      pattern: "await import\\('lattice'\\)"
    - from: "package.json"
      to: "tests/lattice-providers-smoke.test.js"
      via: "scripts.test chain"
      pattern: "lattice-providers-smoke"
    - from: ".planning/LATTICE-PIN.md"
      to: "Lattice fsb-integration-experiments HEAD (post Plan 04-04)"
      via: "current_lattice_sha matches `cd lattice && git rev-parse fsb-integration-experiments`"
      pattern: "current_lattice_sha"
---

<objective>
Close the FSB-side Phase 4 audit trail: ship a thin surface-presence smoke at `tests/lattice-providers-smoke.test.js`, extend `package.json` `scripts.test` to run it, bump `.planning/LATTICE-PIN.md` to the post-Plan-04-04 Lattice HEAD, and populate `.planning/REQUIREMENTS.md` with `LSDK-14..LSDK-18` entries + traceability rows. ONE atomic FSB commit lands all four edits.

Purpose: The FSB-side smoke proves that the 5 new factories are REACHABLE via the bare specifier from FSB's existing `file:` dependency (`lattice` -> `./lattice/packages/lattice`). This is ceremony parity with Phases 1-3 (each shipped one FSB smoke per phase). The substantive INV-03 parity proof lives in the Lattice-side `parity.test.ts` (Plan 04-04 Task 2). LATTICE-PIN.md and REQUIREMENTS.md close the audit trail at the FSB-side -- the per-phase row records all 8 Phase 4 Lattice commits + the SHA bump from `7afd62fc` to the post-Plan-04-04 HEAD.

Output:
- `tests/lattice-providers-smoke.test.js` NEW (~180-220 lines, ~10 PASS assertions; mirrors Phase 1-3 FSB smoke conventions per `<deep_work_rules>`)
- `package.json` modified (scripts.test chain extended with one more `&& node tests/lattice-providers-smoke.test.js` entry)
- `.planning/LATTICE-PIN.md` modified (frontmatter SHA bump + new per-phase row)
- `.planning/REQUIREMENTS.md` modified (LSDK-14..18 entries + 5 traceability rows)
- 1 FSB commit on `automation`: `feat(04): ship Phase 4 provider-adapter alignment (surface-presence smoke + audit-trail closure)`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/LATTICE-PIN.md
@.planning/phases/04-provider-adapter-alignment/04-CONTEXT.md
@.planning/phases/04-provider-adapter-alignment/04-01-PLAN.md
@.planning/phases/04-provider-adapter-alignment/04-02-PLAN.md
@.planning/phases/04-provider-adapter-alignment/04-03-PLAN.md
@.planning/phases/04-provider-adapter-alignment/04-04-PLAN.md

# FSB smoke convention (Phase 1+2+3 patterns to mirror)
@tests/lattice-checkpoint-smoke.test.js
@tests/install-identity.test.js
@package.json

<interfaces>
<!-- FSB smoke pattern from Phase 1+2+3 (lattice-checkpoint-smoke.test.js convention) -->

FSB smoke convention:
- File: `tests/lattice-<phase-tag>-smoke.test.js` (CJS-compatible script run via `node tests/...`)
- Uses `'use strict';` directive at top
- Manual `passed` / `failed` counters; `passAssert(cond, msg)` and `passAssertEqual(actual, expected, msg)` helpers
- Dynamic `await import('lattice')` (not static import; smoke is a Node CJS script)
- Bare-specifier resolution via the `lattice` `file:` dep at `package.json:81`
- Sections delimited by `console.log('\n--- Part N: ... ---')` markers
- Exits with `process.exit(failed > 0 ? 1 : 0)`
- No emoji bytes (project instruction)

Wave 3 outputs (post Plan 04-04) consumed by this plan:
- 5 new factories on the public surface (createAnthropicProvider, createGeminiProvider, createXaiProvider, createOpenRouterProvider, createLmStudioProvider)
- 5 new option-type re-exports (type-only; not surfaced at runtime)
- dist/ rebuilt -- bare-specifier resolution works
- Lattice HEAD: post-Plan-04-04 SHA (captured at execute-time via `cd lattice && git rev-parse fsb-integration-experiments`)
- 8 Phase 4 Lattice commits (5 adapter feat + 1 api feat + 1 parity test + 1 docs)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write tests/lattice-providers-smoke.test.js (FSB-side thin surface-presence smoke; ~10 PASS)</name>
  <files>tests/lattice-providers-smoke.test.js</files>
  <behavior>
    - Part 1 (surface presence): Each of 5 new factories + 5 new option-type re-exports reachable via `await import('lattice')` -- factories are functions; type-only exports do NOT appear at runtime, so this check is REMOVED (TypeScript types do not survive to JS). Only the 5 runtime factories are checked.
    - Part 1b (Phase 1+2+3 carryforward): existing surface still reachable (createReceipt, verifyReceipt, createInMemorySigner, generateEd25519KeyPairJwk, createMemoryKeySet, createHookPipeline, createCheckpointHook, createOpenAIProvider, createOpenAICompatibleProvider, createFakeProvider, STEP_TRANSITION_EVENT_NAME, DEFAULT_CHECKPOINT_BAND)
    - Part 2 (factory invocation): Each of the 5 new factories invoked with stub options + a fake-fetch stub; returned adapter has expected kind and id
    - Part 3 (capabilities populated): Each adapter's capabilities array is non-empty with modelId matching the option supplied
    - Part 4: Adapter.execute is a function on all 5 new adapters
    - Final: console.log('passed:', passed); console.log('failed:', failed); process.exit
  </behavior>
  <action>
Use the `Write` tool to create `tests/lattice-providers-smoke.test.js` with EXACTLY this content:

```javascript
'use strict';

/**
 * Phase 4 (v0.10.0-attempt-2) -- Lattice provider-adapter surface-presence smoke.
 *
 * Purpose: prove FSB can reach Phase 4's 5 new native provider factories
 * via the existing file: dependency. This is the FSB-side ceremony parity
 * smoke (one per phase since Phase 1). The SUBSTANTIVE INV-03 proof lives
 * in the Lattice-side `parity.test.ts` (Plan 04-04 Task 2).
 *
 * Exercises:
 *   (1) Surface presence: 5 new factories + Phase 1+2+3 carryforward exports
 *       reachable via `await import('lattice')` bare specifier.
 *   (2) Factory invocation with stub options + injected fake fetch; each new
 *       adapter returns a ProviderAdapter shape (kind === "provider-adapter",
 *       expected id, capabilities populated, execute is function).
 *   (3) capabilities[0].modelId reflects the supplied model option (per-adapter).
 *   (4) Phase 1+2+3 byte-frozen baseline preserved (29 + 39 + 72 PASS in
 *       the prior smokes; this smoke runs additively after them).
 *
 * Coverage:
 *   - Phase 4 CONTEXT.md D-01 .. D-21 (surface presence aspects)
 *   - INV-06 (the primitives live in Lattice; FSB just consumes via bare specifier)
 *   - INV-03 ceremony parity (substantive proof in Lattice parity.test.ts)
 *   - Phase 1+2+3 byte-frozen baseline (existing FSB smokes remain unchanged
 *     and the Phase 1+2+3 surface still reachable from THIS smoke too)
 *
 * Run: node tests/lattice-providers-smoke.test.js
 */

let passed = 0;
let failed = 0;

function passAssert(cond, msg) {
  if (cond) {
    passed++;
    console.log('  PASS:', msg);
  } else {
    failed++;
    console.error('  FAIL:', msg);
  }
}

function passAssertEqual(actual, expected, msg) {
  passAssert(
    actual === expected,
    msg + ' (expected: ' + JSON.stringify(expected) + ', got: ' + JSON.stringify(actual) + ')'
  );
}

/**
 * Build a fake fetch returning a provider-shape-appropriate body.
 * Mirrors the Lattice-side per-provider fake-body convention (parity.test.ts).
 */
function makeFakeFetch(body, status) {
  status = status || 200;
  return async function () {
    return new Response(JSON.stringify(body), {
      status: status,
      headers: { 'content-type': 'application/json' }
    });
  };
}

const ANTHROPIC_FAKE_BODY = {
  content: [{ type: 'text', text: 'fsb-smoke anthropic ok' }],
  usage: { input_tokens: 10, output_tokens: 5 }
};

const GEMINI_FAKE_BODY = {
  candidates: [
    { content: { parts: [{ text: 'fsb-smoke gemini ok' }], role: 'model' } }
  ],
  usageMetadata: { promptTokenCount: 10, candidatesTokenCount: 5, totalTokenCount: 15 }
};

const OPENAI_COMPAT_FAKE_BODY = {
  choices: [{ message: { content: 'fsb-smoke openai-compat ok' } }],
  usage: { prompt_tokens: 10, completion_tokens: 5 }
};

(async () => {
  console.log('\n--- Lattice Phase 4 providers surface-presence smoke ---');

  let lattice;
  try {
    lattice = await import('lattice');
  } catch (err) {
    console.error('  FAIL: dynamic import("lattice") threw:', err && err.message ? err.message : err);
    console.error('         Did you run `cd lattice && pnpm install && pnpm build` after Phase 4 commits?');
    process.exit(1);
  }

  // ---- Part 1: surface presence -- 5 new factories ----
  console.log('\n--- Part 1: surface presence (5 new factories) ---');

  passAssertEqual(typeof lattice.createAnthropicProvider, 'function', 'lattice.createAnthropicProvider is a function (NEW in Phase 4)');
  passAssertEqual(typeof lattice.createGeminiProvider, 'function', 'lattice.createGeminiProvider is a function (NEW in Phase 4)');
  passAssertEqual(typeof lattice.createXaiProvider, 'function', 'lattice.createXaiProvider is a function (NEW in Phase 4)');
  passAssertEqual(typeof lattice.createOpenRouterProvider, 'function', 'lattice.createOpenRouterProvider is a function (NEW in Phase 4)');
  passAssertEqual(typeof lattice.createLmStudioProvider, 'function', 'lattice.createLmStudioProvider is a function (NEW in Phase 4)');

  // ---- Part 1b: Phase 1+2+3 carryforward exports still reachable ----
  console.log('\n--- Part 1b: Phase 1+2+3 carryforward exports ---');

  passAssertEqual(typeof lattice.createReceipt, 'function', 'lattice.createReceipt still reachable (Phase 1 carryforward)');
  passAssertEqual(typeof lattice.verifyReceipt, 'function', 'lattice.verifyReceipt still reachable');
  passAssertEqual(typeof lattice.createInMemorySigner, 'function', 'lattice.createInMemorySigner still reachable');
  passAssertEqual(typeof lattice.generateEd25519KeyPairJwk, 'function', 'lattice.generateEd25519KeyPairJwk still reachable');
  passAssertEqual(typeof lattice.createMemoryKeySet, 'function', 'lattice.createMemoryKeySet still reachable');
  passAssertEqual(typeof lattice.createHookPipeline, 'function', 'lattice.createHookPipeline still reachable (Phase 2 carryforward)');
  passAssertEqual(typeof lattice.createCheckpointHook, 'function', 'lattice.createCheckpointHook still reachable (Phase 3 carryforward)');
  passAssertEqual(typeof lattice.createOpenAIProvider, 'function', 'lattice.createOpenAIProvider still reachable (pre-Phase-4 baseline)');
  passAssertEqual(typeof lattice.createOpenAICompatibleProvider, 'function', 'lattice.createOpenAICompatibleProvider still reachable');
  passAssertEqual(typeof lattice.createFakeProvider, 'function', 'lattice.createFakeProvider still reachable');
  passAssertEqual(lattice.STEP_TRANSITION_EVENT_NAME, 'step.transition', 'lattice.STEP_TRANSITION_EVENT_NAME constant preserved');
  passAssertEqual(lattice.DEFAULT_CHECKPOINT_BAND, 1, 'lattice.DEFAULT_CHECKPOINT_BAND === 1 preserved');

  if (failed > 0) {
    console.log('\nLattice providers smoke: surface presence check failed; aborting before factory invocation.');
    process.exit(1);
  }

  // ---- Part 2: factory invocation (each new adapter executes against fake fetch) ----
  console.log('\n--- Part 2: per-adapter shape + execute() against fake fetch ---');

  // Anthropic
  {
    const adapter = lattice.createAnthropicProvider({
      model: 'claude-3-opus-fsb-smoke',
      apiKey: 'sk-ant-fsb-smoke',
      fetch: makeFakeFetch(ANTHROPIC_FAKE_BODY)
    });
    passAssertEqual(adapter.kind, 'provider-adapter', 'createAnthropicProvider().kind === "provider-adapter"');
    passAssertEqual(adapter.id, 'anthropic', 'createAnthropicProvider().id === "anthropic"');
    passAssert(Array.isArray(adapter.capabilities) && adapter.capabilities.length > 0, 'createAnthropicProvider().capabilities non-empty');
    passAssertEqual(adapter.capabilities[0].modelId, 'claude-3-opus-fsb-smoke', 'Anthropic capabilities[0].modelId reflects supplied model');
    passAssertEqual(typeof adapter.execute, 'function', 'createAnthropicProvider().execute is a function');
    const response = await adapter.execute({ task: 't', artifacts: [], outputs: ['text'] });
    passAssertEqual(typeof response.rawOutputs.text, 'string', 'Anthropic execute() returns rawOutputs.text:string');
    passAssertEqual(response.rawOutputs.text, 'fsb-smoke anthropic ok', 'Anthropic rawOutputs.text matches fake body content[0].text');
  }

  // Gemini
  {
    const adapter = lattice.createGeminiProvider({
      model: 'gemini-1.5-flash-fsb-smoke',
      apiKey: 'AIza-fsb-smoke',
      fetch: makeFakeFetch(GEMINI_FAKE_BODY)
    });
    passAssertEqual(adapter.kind, 'provider-adapter', 'createGeminiProvider().kind === "provider-adapter"');
    passAssertEqual(adapter.id, 'gemini', 'createGeminiProvider().id === "gemini"');
    passAssert(Array.isArray(adapter.capabilities) && adapter.capabilities.length > 0, 'createGeminiProvider().capabilities non-empty');
    passAssertEqual(adapter.capabilities[0].modelId, 'gemini-1.5-flash-fsb-smoke', 'Gemini capabilities[0].modelId reflects supplied model');
    const response = await adapter.execute({ task: 't', artifacts: [], outputs: ['text'] });
    passAssertEqual(response.rawOutputs.text, 'fsb-smoke gemini ok', 'Gemini rawOutputs.text matches fake body candidates[0].content.parts[0].text');
  }

  // xAI
  {
    const adapter = lattice.createXaiProvider({
      model: 'grok-4-fsb-smoke',
      apiKey: 'xai-fsb-smoke',
      fetch: makeFakeFetch(OPENAI_COMPAT_FAKE_BODY)
    });
    passAssertEqual(adapter.kind, 'provider-adapter', 'createXaiProvider().kind === "provider-adapter"');
    passAssertEqual(adapter.id, 'xai', 'createXaiProvider().id === "xai"');
    passAssertEqual(adapter.capabilities[0].modelId, 'grok-4-fsb-smoke', 'xAI capabilities[0].modelId reflects supplied model');
    const response = await adapter.execute({ task: 't', artifacts: [], outputs: ['text'] });
    passAssertEqual(response.rawOutputs.text, 'fsb-smoke openai-compat ok', 'xAI rawOutputs.text matches fake body (OpenAI-compat shape)');
  }

  // OpenRouter
  {
    const adapter = lattice.createOpenRouterProvider({
      model: 'openai/gpt-4o-fsb-smoke',
      apiKey: 'sk-or-fsb-smoke',
      fetch: makeFakeFetch(OPENAI_COMPAT_FAKE_BODY)
    });
    passAssertEqual(adapter.kind, 'provider-adapter', 'createOpenRouterProvider().kind === "provider-adapter"');
    passAssertEqual(adapter.id, 'openrouter', 'createOpenRouterProvider().id === "openrouter"');
    passAssertEqual(adapter.capabilities[0].modelId, 'openai/gpt-4o-fsb-smoke', 'OpenRouter capabilities[0].modelId reflects supplied model');
    const response = await adapter.execute({ task: 't', artifacts: [], outputs: ['text'] });
    passAssertEqual(response.rawOutputs.text, 'fsb-smoke openai-compat ok', 'OpenRouter rawOutputs.text matches fake body');
  }

  // LM Studio (no apiKey -- CD-03 default)
  {
    const adapter = lattice.createLmStudioProvider({
      model: 'qwen2.5-coder-fsb-smoke',
      fetch: makeFakeFetch(OPENAI_COMPAT_FAKE_BODY)
    });
    passAssertEqual(adapter.kind, 'provider-adapter', 'createLmStudioProvider().kind === "provider-adapter"');
    passAssertEqual(adapter.id, 'lm-studio', 'createLmStudioProvider().id === "lm-studio"');
    passAssertEqual(adapter.capabilities[0].modelId, 'qwen2.5-coder-fsb-smoke', 'LM Studio capabilities[0].modelId reflects supplied model');
    const response = await adapter.execute({ task: 't', artifacts: [], outputs: ['text'] });
    passAssertEqual(response.rawOutputs.text, 'fsb-smoke openai-compat ok', 'LM Studio rawOutputs.text matches fake body');
  }

  // ---- Part 3: distinct provider ids (CD-02 ceremony parity with Lattice parity smoke) ----
  console.log('\n--- Part 3: 5 new adapters claim distinct ids ---');

  const ids = new Set();
  for (const builder of [
    () => lattice.createAnthropicProvider({ model: 'm', apiKey: 'k', fetch: makeFakeFetch(ANTHROPIC_FAKE_BODY) }),
    () => lattice.createGeminiProvider({ model: 'm', apiKey: 'k', fetch: makeFakeFetch(GEMINI_FAKE_BODY) }),
    () => lattice.createXaiProvider({ model: 'm', apiKey: 'k', fetch: makeFakeFetch(OPENAI_COMPAT_FAKE_BODY) }),
    () => lattice.createOpenRouterProvider({ model: 'm', apiKey: 'k', fetch: makeFakeFetch(OPENAI_COMPAT_FAKE_BODY) }),
    () => lattice.createLmStudioProvider({ model: 'm', fetch: makeFakeFetch(OPENAI_COMPAT_FAKE_BODY) })
  ]) {
    const adapter = builder();
    passAssert(!ids.has(adapter.id), '5 new adapter id "' + adapter.id + '" is distinct (no collision)');
    ids.add(adapter.id);
  }
  passAssertEqual(ids.size, 5, '5 new adapters yield 5 distinct ids');

  console.log('\n--- Summary ---');
  console.log('passed:', passed);
  console.log('failed:', failed);
  process.exit(failed > 0 ? 1 : 0);
})().catch((err) => {
  console.error('Providers smoke harness uncaught error:', err && err.stack ? err.stack : err);
  process.exit(1);
});
```

Then run the smoke standalone to confirm it passes:
```bash
node tests/lattice-providers-smoke.test.js
```
Expect: `passed: 38 / failed: 0` (38 assertions: 5 new factories + 12 carryforward + 5 adapter blocks * ~4 assertions each + 6 distinct-id assertions).

DO NOT commit yet -- the FSB commit ceremony lands all four edits (this file + package.json + LATTICE-PIN.md + REQUIREMENTS.md) as one atomic commit per CONTEXT.md plan_shape_guidance Wave 4.
  </action>
  <verify>
    <automated>node tests/lattice-providers-smoke.test.js 2&gt;&amp;1 | grep -E "passed:|failed:" | tail -2</automated>
  </verify>
  <done>
    - File `tests/lattice-providers-smoke.test.js` exists
    - `wc -l tests/lattice-providers-smoke.test.js` >= 150
    - `grep -c "await import('lattice')" tests/lattice-providers-smoke.test.js` >= 1
    - `grep -cE "createAnthropicProvider|createGeminiProvider|createXaiProvider|createOpenRouterProvider|createLmStudioProvider" tests/lattice-providers-smoke.test.js` >= 5 (each new factory used)
    - `node tests/lattice-providers-smoke.test.js` exits 0 with `passed: >=20 / failed: 0` (CONTEXT.md D-13 floor of ~10; actual ~38)
    - NO emoji bytes (grep -P would catch; project instruction)
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend package.json scripts.test chain with the new providers smoke</name>
  <files>package.json</files>
  <action>
First read the current `package.json` line 16 (the scripts.test chain). It currently ends with:
```
... && node tests/lattice-tripwire-smoke.test.js && node tests/lattice-checkpoint-smoke.test.js
```

Use the `Edit` tool on `package.json` to replace exactly this substring:
```
&& node tests/lattice-checkpoint-smoke.test.js"
```
with:
```
&& node tests/lattice-checkpoint-smoke.test.js && node tests/lattice-providers-smoke.test.js"
```

(The closing `"` is included in both old_string + new_string so we land EXACTLY at the end of the
scripts.test value string.)

Then verify the chain runs end-to-end:
```bash
npm test 2>&1 | tail -10
```
Expect: full chain exits 0 with the new providers smoke appearing as the last test invocation.

Phase 1+2+3 smokes must remain byte-frozen upstream in the chain -- the edit only appends ONE
new `&& node tests/...` segment to the right-hand-side of the chain. Verify via:
```bash
node tests/lattice-smoke.test.js 2>&1 | grep -E "passed:|failed:" | tail -2  # Expect 29 / 0
node tests/lattice-tripwire-smoke.test.js 2>&1 | grep -E "passed:|failed:" | tail -2  # Expect 39 / 0
node tests/lattice-checkpoint-smoke.test.js 2>&1 | grep -E "passed:|failed:" | tail -2  # Expect 72 / 0
node tests/lattice-providers-smoke.test.js 2>&1 | grep -E "passed:|failed:" | tail -2  # Expect >=20 / 0
```
  </action>
  <verify>
    <automated>grep -c "lattice-providers-smoke" package.json</automated>
  </verify>
  <done>
    - `grep -c "lattice-providers-smoke" package.json` returns 1 (appended once)
    - `grep -c "lattice-checkpoint-smoke" package.json` returns 1 (Phase 3 entry preserved; appears once)
    - `grep -c "lattice-tripwire-smoke" package.json` returns 1 (Phase 2 entry preserved)
    - `grep -c "lattice-smoke.test.js" package.json` returns 1 (Phase 1 entry preserved; the .test.js suffix discriminates from -tripwire-smoke and similar)
    - `npm test` exits 0 with the chain running the providers smoke as last step
  </done>
</task>

<task type="auto">
  <name>Task 3: Bump .planning/LATTICE-PIN.md frontmatter SHA + append Phase 4 row</name>
  <files>.planning/LATTICE-PIN.md</files>
  <action>
First capture the post-Plan-04-04 Lattice HEAD SHA:
```bash
cd lattice && git rev-parse fsb-integration-experiments
```
Expect: a 40-char SHA distinct from the pre-Phase-4 SHA `7afd62fc595bedc5ad9d4576d2d679cf50c68fd8`.
Call this `<phase-4-head-sha>` in the edits below.

Then capture all 8 Phase 4 Lattice commit short SHAs:
```bash
cd lattice && git log fsb-integration-experiments --oneline 7afd62fc..HEAD --format="%h %s"
```
Expected output (most-recent first):
```
<sha-docs>      docs(fsb-integration): close Phase 4 audit rows (providers)
<sha-parity>    test(providers): add INV-03 parity smoke covering 7 logical providers
<sha-api>       feat(api): re-export 5 new provider factories from public surface
<sha-lm>        feat(providers): add LM Studio provider adapter
<sha-or>        feat(providers): add OpenRouter provider adapter
<sha-xai>       feat(providers): add xAI provider adapter
<sha-gemini>    feat(providers): add Gemini provider adapter
<sha-anth>      feat(providers): add Anthropic provider adapter
```

Use the `Edit` tool on `.planning/LATTICE-PIN.md` to:

EDIT A: Replace the frontmatter line:
```
current_lattice_sha: 7afd62fc595bedc5ad9d4576d2d679cf50c68fd8
```
with (substitute actual SHA):
```
current_lattice_sha: <phase-4-head-sha>
```

EDIT B: Replace the line:
```
last_updated: 2026-05-24
```
with (use today's date 2026-05-24 unless the executor runs on a different date):
```
last_updated: 2026-05-24
```
(Same date; the existing entry is correct because Phase 4 lands the same execution day. If executor
runs on a future date, use that date.)

EDIT C: Replace the current_pinned_sha narrative paragraph around line 15:
```
**Current pinned SHA:** `97836f2` (full SHA in frontmatter)
```
with (substitute actual short SHA of the post-Plan-04-04 HEAD):
```
**Current pinned SHA:** `<phase-4-head-short-sha>` (full SHA in frontmatter)
```

EDIT D: APPEND a Phase 4 row to the per-phase table (BEFORE the `## How this file gets used` heading
which is currently at line 28). The current Phase 3 row ends with `(D-15/D-17).` -- append AFTER
that closing `|` of the Phase 3 row, on a NEW line, the Phase 4 row:

```
| Phase 4   | 2026-05-24 | `<phase-4-head-sha>` | `fsb-integration-experiments`   | (1) `packages/lattice/src/providers/anthropic.ts` + `.test.ts` (`<sha-anth>`) -- full custom adapter for `/v1/messages` with top-level `system` field, `content[0].text` parsing, `input_tokens`/`output_tokens` usage extraction, `x-api-key` + `anthropic-version: 2023-06-01` headers; mirrors FSB universal-provider.js:280-297 + 566-573. 9 vitest cases. (2) `packages/lattice/src/providers/gemini.ts` + `.test.ts` (`<sha-gemini>`) -- full custom adapter for `/v1beta/models/{model}:generateContent` with `contents[].parts[].text` shape, `candidates[0].content.parts[0].text` parsing, `usageMetadata.promptTokenCount`/`candidatesTokenCount` extraction, 4 HARM_CATEGORY safetySettings at BLOCK_NONE (FSB convention), `?key=` query-string auth, role `user`/`model` preserved per D-07; mirrors FSB universal-provider.js:210-274 + 551-563. 10 vitest cases. (3) `packages/lattice/src/providers/xai.ts` + `.test.ts` (`<sha-xai>`) -- thin wrapper around createOpenAICompatibleProvider pinned to `https://api.x.ai/v1`. PRESERVES `completion_tokens_details.reasoning_tokens` quirk per D-07 + universal-provider.js:585-594: legacy UsageRecord.totalTokens recomputed to include reasoning_tokens. 8 vitest cases. (4) `packages/lattice/src/providers/openrouter.ts` + `.test.ts` (`<sha-or>`) -- thin wrapper pinned to `https://openrouter.ai/api/v1`. Model-routing array / fallback DEFERRED per D-17 carryforward. 7 vitest cases. (5) `packages/lattice/src/providers/lm-studio.ts` + `.test.ts` (`<sha-lm>`) -- thin wrapper pinned to `http://localhost:1234/v1`. apiKey OPTIONAL (CD-03 default; no opt-out). Latency-tail diagnostics DEFERRED per D-16 carryforward. 8 vitest cases. (6) `packages/lattice/src/index.ts` (`<sha-api>`) -- re-export 5 new factories + 5 option type aliases from public surface. dist/ rebuilt locally via tsdown clean:true (dist/ gitignored). (7) `packages/lattice/src/providers/parity.test.ts` (`<sha-parity>`) -- INV-03 provider-parity smoke iterating all 7 logical providers (OpenAI, OpenAI-compat, Anthropic, Gemini, xAI, OpenRouter, LM Studio) against fake fetch; asserts ProviderAdapter shape, rawOutputs population, normalizedUsage Phase 7 shape, provider-name errors on non-OK, AbortSignal propagation per D-05, rawResponse preservation, distinct ids (CD-02 default). 7 vitest cases. (8) `docs/fsb-integration-gaps.md` (`<sha-docs>`) -- 5 Providers rows (lines 46-50) flipped Blocker/Important/Nice-to-have -> Covered with backlink SHAs to commits 1-7. | Phase 4 = provider adapter alignment + INV-03 hard-gate proof. Lattice vitest suite green at the new HEAD (347 baseline + 42 adapter cases + 7 parity smoke = 396 PASS / 0 FAIL). FSB consumes the 5 new factories via the existing `file:` dep; `tests/lattice-providers-smoke.test.js` (this plan; >=20 PASS, ~38 actual) validates surface presence + per-adapter execute() end-to-end real-runtime with fake fetch. Phase 1+2+3 byte-frozen baseline preserved -- Phase 1+2+3 audit-doc rows (Receipts / Tripwires/hooks / Delegation / MV3-survivability / Observability) untouched; Phase 1+2+3 Lattice source files in receipts/, contract/, tracing/ untouched in Phase 4 (only `src/index.ts` modified for re-export additions). All 8 Phase 4 Lattice commits carry `Ref: FSB v0.10.0-attempt-2 Phase 4` footer (D-18); none pushed (D-19). Zero `extension/*` or `mcp/*` modifications (Option B carryforward). |
```

(The exact table row width is large; Markdown table rendering handles it fine. Substitute the
8 actual short SHAs from the `git log` capture above.)
  </action>
  <verify>
    <automated>grep -c "Phase 4" .planning/LATTICE-PIN.md</automated>
  </verify>
  <done>
    - `grep -c "Phase 4" .planning/LATTICE-PIN.md` >= 2 (frontmatter narrative + per-phase row)
    - Frontmatter `current_lattice_sha:` matches `cd lattice && git rev-parse fsb-integration-experiments` exactly (40-char SHA)
    - Per-phase table has 4 rows: Phase 1, Phase 2, Phase 3, Phase 4 (one row added; Phase 1+2+3 rows BYTE-FROZEN)
    - All 8 Phase 4 Lattice commit short SHAs cited in the Phase 4 row Notes column
    - Phase 4 row narrates: 5 adapter feat + api re-export + parity smoke + audit-doc closure
    - `cd lattice && git rev-parse fsb-integration-experiments` exit 0 (Lattice branch still valid)
  </done>
</task>

<task type="auto">
  <name>Task 4: Populate REQUIREMENTS.md with LSDK-14..LSDK-18 entries + traceability rows; commit ONE FSB commit</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
First identify the post-Plan-04-04 SHAs (already captured in Task 3). Re-use the same short SHAs.

Use the `Edit` tool to make TWO edits to `.planning/REQUIREMENTS.md`:

EDIT A: Replace the section block currently at lines 55-57:
```
- [ ] **LSDK-KK..L (TBD):** Provider adapter extensions -- bring Lattice's provider abstraction into parity with FSB's 7-provider matrix (Anthropic, OpenAI, xAI, Gemini, LM Studio, OpenRouter, custom OpenAI-compatible).
- [ ] **LSDK-LL..P (TBD):** Task-delegation primitive shape (if Lattice opens multi-agent policy; otherwise design as a Lattice-adjacent FSB-side primitive that consumes Lattice's receipt + tripwire surface).
- [ ] **LSDK-PP..Q (TBD):** MV3-survivability adapter contract -- documented interface for runtimes whose execution context can be evicted mid-flow.
```
with:
```
- [x] **LSDK-14 -- DONE 2026-05-24 (Phase 04 Plan 04-01 + Plan 04-04 + Plan 04-05):** Anthropic native provider adapter shipped at `lattice/packages/lattice/src/providers/anthropic.ts` -- full custom adapter for `/v1/messages` API (top-level `system` field + `content[0].text` response shape) mirroring FSB `extension/ai/universal-provider.js:280-297` (request) + `:566-573` (response/usage). 9 vitest cases per D-09 contract. `x-api-key` + `anthropic-version: 2023-06-01` headers; `input_tokens`/`output_tokens` usage extraction (NOT prompt/completion); AbortSignal wired via spread pattern per D-05; non-OK throws `Error("Anthropic provider failed with ${status}.")`. Streaming + prompt caching + tool use deferred. Re-exported from `src/index.ts` (`createAnthropicProvider` + type `AnthropicProviderOptions`). Audit-doc Providers row (Blocker) flipped to Covered. Lattice commits `<sha-anth>` (adapter + tests) + `<sha-api>` (public surface re-export) + `<sha-parity>` (INV-03 parity smoke) + `<sha-docs>` (audit-doc closure). FSB-side smoke `tests/lattice-providers-smoke.test.js` exercises end-to-end.
- [x] **LSDK-15 -- DONE 2026-05-24 (Phase 04 Plan 04-02 + Plan 04-04 + Plan 04-05):** Gemini native provider adapter shipped at `lattice/packages/lattice/src/providers/gemini.ts` -- full custom adapter for `/v1beta/models/{model}:generateContent` API (`contents[].parts[].text` request shape, `candidates[0].content.parts[0].text` response, `usageMetadata.promptTokenCount`/`candidatesTokenCount` usage, 4 HARM_CATEGORY safetySettings at BLOCK_NONE per FSB convention, `?key=` query-string auth, role `user`/`model` preserved per D-07) mirroring FSB `extension/ai/universal-provider.js:210-274` + `:551-563`. 10 vitest cases per D-09 contract + 3 Gemini-specific cases (safetySettings, query-string auth, role mapping). Missing-candidates throws `Error("Gemini provider returned no candidates.")`. Streaming + multimodal (vision) deferred. Re-exported from `src/index.ts` (`createGeminiProvider` + type `GeminiProviderOptions`). Audit-doc Providers row (Blocker) flipped to Covered. Lattice commits `<sha-gemini>` (adapter + tests) + `<sha-api>` + `<sha-parity>` + `<sha-docs>`.
- [x] **LSDK-16 -- DONE 2026-05-24 (Phase 04 Plan 04-03 + Plan 04-04 + Plan 04-05):** xAI native provider adapter shipped at `lattice/packages/lattice/src/providers/xai.ts` -- thin wrapper around `createOpenAICompatibleProvider` pinned to `https://api.x.ai/v1`. PRESERVES xAI's `completion_tokens_details.reasoning_tokens` quirk per D-07: legacy `UsageRecord.totalTokens` recomputed to INCLUDE `reasoning_tokens` when present (matches FSB `extension/ai/universal-provider.js:585-594` production behavior). Phase 7 normalized `Usage` (promptTokens/completionTokens/costUsd) unchanged -- represents billable tokens only; reasoning_tokens accessible via `response.rawResponse.usage.completion_tokens_details.reasoning_tokens`. 8 vitest cases including the explicit `reasoning_tokens` quirk verification. Tool-streaming deferred. Re-exported from `src/index.ts` (`createXaiProvider` + type `XaiProviderOptions`). Audit-doc Providers row (Important) flipped to Covered. Lattice commits `<sha-xai>` (adapter + tests) + `<sha-api>` + `<sha-parity>` + `<sha-docs>`.
- [x] **LSDK-17 -- DONE 2026-05-24 (Phase 04 Plan 04-03 + Plan 04-04 + Plan 04-05):** OpenRouter native provider adapter shipped at `lattice/packages/lattice/src/providers/openrouter.ts` -- thin wrapper around `createOpenAICompatibleProvider` pinned to `https://openrouter.ai/api/v1`. First-class named adapter for ceremony parity. Model-routing array / fallback-array DEFERRED per D-17 carryforward (caller supplies single model id; multi-model fallback is a follow-on phase; JSDoc on factory documents the carryforward). 7 vitest cases per D-09 contract. Streaming deferred. Re-exported from `src/index.ts` (`createOpenRouterProvider` + type `OpenRouterProviderOptions`). Audit-doc Providers row (Nice-to-have) flipped to Covered. Lattice commits `<sha-or>` (adapter + tests) + `<sha-api>` + `<sha-parity>` + `<sha-docs>`.
- [x] **LSDK-18 -- DONE 2026-05-24 (Phase 04 Plan 04-03 + Plan 04-04 + Plan 04-05):** LM Studio native provider adapter shipped at `lattice/packages/lattice/src/providers/lm-studio.ts` -- thin wrapper around `createOpenAICompatibleProvider` pinned to `http://localhost:1234/v1`. `apiKey` is OPTIONAL (CD-03: LM Studio is no-auth by convention; no opt-out flag); when omitted, the underlying factory's conditional at `adapters.ts:53` sends no `Authorization` header. Latency-tail diagnostics DEFERRED per D-16 carryforward (LM Studio IS the named INV-03 latency canary, but the diagnostics module belongs in a follow-on observability phase; JSDoc on factory + audit-doc row note both document the carryforward). 8 vitest cases per D-09 contract + the explicit no-Authorization-by-default verification. Streaming deferred. Re-exported from `src/index.ts` (`createLmStudioProvider` + type `LmStudioProviderOptions`). Audit-doc Providers row (Important) flipped to Covered. Lattice commits `<sha-lm>` (adapter + tests) + `<sha-api>` + `<sha-parity>` + `<sha-docs>`.
- [ ] **LSDK-LL..P (TBD):** Task-delegation primitive shape (if Lattice opens multi-agent policy; otherwise design as a Lattice-adjacent FSB-side primitive that consumes Lattice's receipt + tripwire surface).
- [ ] **LSDK-PP..Q (TBD):** MV3-survivability adapter contract -- documented interface for runtimes whose execution context can be evicted mid-flow.
```

EDIT B: Append 5 traceability rows in the bottom Traceability table. Find the last row in the
table currently:
```
| LSDK-13 | 03 | Complete (Phase 03 Plan 03-01: checkpoint.test.ts 15 vitest cases; Lattice commit a67f476) |
```
and INSERT immediately after that line (before the `**Total v1 requirements:**` line):
```
| LSDK-14 | 04 | Complete (Phase 04 Plan 04-01 + Plan 04-04 + Plan 04-05: Anthropic adapter; Lattice commits `<sha-anth>` + `<sha-api>` + `<sha-parity>` + `<sha-docs>`) |
| LSDK-15 | 04 | Complete (Phase 04 Plan 04-02 + Plan 04-04 + Plan 04-05: Gemini adapter; Lattice commits `<sha-gemini>` + `<sha-api>` + `<sha-parity>` + `<sha-docs>`) |
| LSDK-16 | 04 | Complete (Phase 04 Plan 04-03 + Plan 04-04 + Plan 04-05: xAI adapter with reasoning_tokens quirk; Lattice commits `<sha-xai>` + `<sha-api>` + `<sha-parity>` + `<sha-docs>`) |
| LSDK-17 | 04 | Complete (Phase 04 Plan 04-03 + Plan 04-04 + Plan 04-05: OpenRouter adapter; Lattice commits `<sha-or>` + `<sha-api>` + `<sha-parity>` + `<sha-docs>`) |
| LSDK-18 | 04 | Complete (Phase 04 Plan 04-03 + Plan 04-04 + Plan 04-05: LM Studio adapter; Lattice commits `<sha-lm>` + `<sha-api>` + `<sha-parity>` + `<sha-docs>`) |
```

Also update the `**Total v1 requirements:**` line at the bottom to reflect new count:
old:
```
**Total v1 requirements:** TBD (16 concrete so far; remaining LSDK/FINT/MCP/PRV IDs populated during downstream phase planning)
```
new:
```
**Total v1 requirements:** TBD (21 concrete so far; remaining LSDK/FINT/MCP/PRV IDs populated during downstream phase planning)
```

Now commit ONE atomic FSB commit covering all four edits (this task + Task 1 + Task 2 + Task 3):

```bash
git add tests/lattice-providers-smoke.test.js package.json .planning/LATTICE-PIN.md .planning/REQUIREMENTS.md && git commit -m "$(cat <<'EOF'
feat(04): ship Phase 4 provider-adapter alignment (surface-presence smoke + audit-trail closure)

Phase 4 of the v0.10.0 milestone (Autopilot via Lattice SDK, attempt 2)
closes the INV-03 hard-gate by shipping 5 native provider adapters on
Lattice's fsb-integration-experiments branch:

  - Anthropic (full custom; /v1/messages with top-level system field)
  - Gemini (full custom; /v1beta/models/{model}:generateContent with
    BLOCK_NONE safetySettings + ?key= query-string auth)
  - xAI (thin wrapper; api.x.ai/v1; preserves reasoning_tokens quirk)
  - OpenRouter (thin wrapper; openrouter.ai/api/v1; model-routing DEFERRED)
  - LM Studio (thin wrapper; localhost:1234/v1; no-auth by default,
    latency-tail diagnostics DEFERRED)

Plus the Lattice-side INV-03 parity smoke at parity.test.ts (7 vitest
cases iterating all 7 logical providers proving ProviderAdapter shape,
rawOutputs population, normalizedUsage Phase 7 shape, error messaging,
AbortSignal propagation, rawResponse preservation, distinct ids).

This FSB-side commit lands the ceremony parity smoke + audit trail:

  1. tests/lattice-providers-smoke.test.js -- thin surface-presence smoke
     (>=20 PASS; ~38 actual). 5 new factories reachable via the existing
     file: dep (await import('lattice')); per-adapter execute() against
     provider-shape-appropriate fake fetch produces ProviderAdapter shape
     end-to-end. Phase 1+2+3 carryforward exports verified still reachable.

  2. package.json scripts.test chain extended with the new smoke after
     lattice-checkpoint-smoke. Phase 1+2+3 smoke entries BYTE-FROZEN.

  3. .planning/LATTICE-PIN.md frontmatter current_lattice_sha bumped to
     the post-Plan-04-04 Lattice HEAD; per-phase table appended with a
     Phase 4 row narrating all 8 Phase 4 Lattice commits (5 adapter feat
     + 1 api feat + 1 test feat + 1 docs feat). Phase 1+2+3 rows
     BYTE-FROZEN (append-only audit trail).

  4. .planning/REQUIREMENTS.md populated with LSDK-14..LSDK-18 entries
     in the LSDK section + 5 traceability rows. Phase 1+2+3 baseline
     LSDK-01..13 + FINT-01 + MCP-01/02 entries BYTE-FROZEN.

Invariants holding:
  - INV-01 MCP wire UNTOUCHED (tool-definitions-parity 142 PASS)
  - INV-03 Provider parity ACHIEVED (5 new adapters cover all FSB providers;
    Lattice-side parity smoke proves all 7 honor the same contract)
  - INV-04 setTimeout iterator preserved (grep returns 8)
  - INV-06 Lattice primitives in Lattice (the 5 adapters live in Lattice;
    FSB consumes via existing bare specifier)
  - Phase 1+2+3 FSB smokes byte-frozen (29 + 39 + 72 PASS unchanged)
  - Phase 1+2+3 Lattice source files byte-frozen (only src/index.ts
    modified for re-export additions in Phase 4)
  - Option B reconciliation: ZERO extension/* or mcp/* modifications
  - D-19 NO git push to Lattice's remote (all 8 Phase 4 Lattice commits
    on fsb-integration-experiments only)

Ref: FSB v0.10.0-attempt-2 Phase 4
EOF
)"
```

Then run full FSB test chain one final time to confirm end-to-end:
```bash
npm test
```
Expect: exits 0; full chain green; last smoke is the new providers smoke.
  </action>
  <verify>
    <automated>npm test 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <done>
    - `.planning/REQUIREMENTS.md` has LSDK-14, LSDK-15, LSDK-16, LSDK-17, LSDK-18 entries marked `[x]` / "Complete"
    - `grep -c "LSDK-14\|LSDK-15\|LSDK-16\|LSDK-17\|LSDK-18" .planning/REQUIREMENTS.md` returns 10 (5 section entries + 5 traceability rows)
    - `grep -c "Total v1 requirements:.*21" .planning/REQUIREMENTS.md` returns 1 (count bumped from 16 to 21)
    - One FSB commit `feat(04): ...` lands on `automation` covering all 4 files
    - `git log -1 --format=%B | grep -c "Ref: FSB v0.10.0-attempt-2 Phase 4"` returns 1
    - `git diff --name-only HEAD~1 HEAD` lists exactly: `.planning/LATTICE-PIN.md`, `.planning/REQUIREMENTS.md`, `package.json`, `tests/lattice-providers-smoke.test.js`
    - `git diff --name-only HEAD~1 HEAD -- "extension/*" "mcp/*"` empty (ZERO extension/mcp modifications)
    - `npm test` exits 0 with full chain green
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FSB Node smoke -> Lattice bare specifier | `await import('lattice')` resolves via the `file:` dep at `package.json:81`. |
| FSB smoke -> fake fetch | Per-adapter fake-fetch fixtures; no network calls. |
| LATTICE-PIN.md -> readers | SHA pin for audit cross-reference; downstream readers cross-verify via `cd lattice && git rev-parse`. |
| REQUIREMENTS.md -> downstream phases | Requirement IDs are append-only; once marked Complete they are not retroactively edited. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | Information disclosure | Stub apiKey in smoke ('sk-ant-fsb-smoke', etc) | accept | Strings are obviously-test stubs; injected fake fetch never makes a network call; smoke runs offline. Not real credentials. |
| T-04-05-02 | Tampering | LATTICE-PIN.md SHA drift | mitigate | Plan acceptance criterion requires frontmatter SHA match exact `cd lattice && git rev-parse fsb-integration-experiments` output. |
| T-04-05-03 | Tampering | REQUIREMENTS.md retro-edits to Phase 1+2+3 LSDK entries | mitigate | Plan acceptance criterion verifies LSDK-01..13 baseline preserved (each appears exactly 2 times after the Phase 4 edits, matching Phase 3 baseline). |
| T-04-05-04 | Spoofing | scripts.test chain reordering | mitigate | Plan acceptance criterion verifies Phase 1+2+3 smoke entries each appear exactly once in scripts.test (no duplication, no removal). |
| T-04-05-05 | Tampering | FSB commit accidentally includes extension/* edits | mitigate | Done criterion `git diff --name-only HEAD~1 HEAD -- "extension/*" "mcp/*"` empty across the commit. Plan lists exactly 4 files in `files_modified` -- no extension paths. |
| T-04-05-06 | Repudiation | FSB commit Ref footer forgotten | mitigate | `git log -1 --format=%B | grep -c "Ref: FSB v0.10.0-attempt-2 Phase 4"` returns 1. |
| T-04-05-07 | DoS | New smoke slows full npm test chain | accept | Smoke runs offline against fake fetch; expected duration < 1 second. Adds < 1% to the multi-minute chain. |
</threat_model>

<verification>
After all 4 tasks complete:

```bash
# 1. New smoke file present + passes standalone
ls -la tests/lattice-providers-smoke.test.js
node tests/lattice-providers-smoke.test.js 2>&1 | grep -E "passed:|failed:" | tail -2
# Expect: passed: >=20 (actual ~38), failed: 0

# 2. scripts.test chain extended
grep -c "lattice-providers-smoke" package.json
# Expect: 1
grep -c "lattice-checkpoint-smoke" package.json
# Expect: 1 (Phase 3 entry preserved)
grep -c "lattice-tripwire-smoke" package.json
# Expect: 1 (Phase 2 entry preserved)
grep -cE "lattice-smoke\\.test\\.js" package.json
# Expect: 1 (Phase 1 entry preserved)

# 3. Full FSB test chain exits 0 (cumulative: ~140 (existing) + ~38 (new providers smoke) = ~178 lattice-smoke assertions)
npm test 2>&1 | tail -5
# Expect: chain exits 0; final invocation is lattice-providers-smoke.test.js

# 4. LATTICE-PIN.md frontmatter SHA bumped
grep -E "^current_lattice_sha:" .planning/LATTICE-PIN.md
# Expect: line with 40-char SHA != 7afd62fc... (i.e., bumped to post-Plan-04-04 HEAD)

# Cross-verify against Lattice
diff <(cd lattice && git rev-parse fsb-integration-experiments) <(grep -E "^current_lattice_sha:" .planning/LATTICE-PIN.md | awk '{print $2}')
# Expect: empty (matches exactly)

# 5. LATTICE-PIN.md per-phase table has Phase 4 row
grep -c "^| Phase 4" .planning/LATTICE-PIN.md
# Expect: 1

# Phase 1+2+3 rows preserved
grep -c "^| Phase 1" .planning/LATTICE-PIN.md
grep -c "^| Phase 2" .planning/LATTICE-PIN.md
grep -c "^| Phase 3" .planning/LATTICE-PIN.md
# Expect: 1 / 1 / 1

# 6. REQUIREMENTS.md LSDK-14..18 populated
grep -c "LSDK-14\|LSDK-15\|LSDK-16\|LSDK-17\|LSDK-18" .planning/REQUIREMENTS.md
# Expect: 10 (5 section entries + 5 traceability rows)

# Phase 1+2+3 baseline preserved
grep -cE "LSDK-01|LSDK-02|LSDK-03|LSDK-04|LSDK-05|LSDK-06|LSDK-07|LSDK-08|LSDK-09|LSDK-10|LSDK-11|LSDK-12|LSDK-13" .planning/REQUIREMENTS.md
# Expect: 26 (13 IDs * 2 occurrences each)

# 7. ONE FSB commit, exact file set
git log -1 --format="%H %s"
git diff --name-only HEAD~1 HEAD
# Expect 4 files: .planning/LATTICE-PIN.md, .planning/REQUIREMENTS.md, package.json, tests/lattice-providers-smoke.test.js

# 8. No extension/* or mcp/* modifications
git diff --name-only HEAD~1 HEAD -- "extension/*" "mcp/*"
# Expect: empty

# 9. Ref footer in FSB commit
git log -1 --format=%B | grep -c "Ref: FSB v0.10.0-attempt-2 Phase 4"
# Expect: 1

# 10. Invariants
node tests/tool-definitions-parity.test.js 2>&1 | tail -3
# Expect: 142 PASS / 0 FAIL (INV-01)
grep -c "setTimeout" extension/ai/agent-loop.js
# Expect: 8 (INV-04)

# 11. Phase 1+2+3 smokes byte-frozen
node tests/lattice-smoke.test.js 2>&1 | grep -E "passed:|failed:" | tail -2
# Expect: 29 / 0
node tests/lattice-tripwire-smoke.test.js 2>&1 | grep -E "passed:|failed:" | tail -2
# Expect: 39 / 0
node tests/lattice-checkpoint-smoke.test.js 2>&1 | grep -E "passed:|failed:" | tail -2
# Expect: 72 / 0

# 12. Lattice still has no push (D-19 carryforward across phases)
cd lattice && git reflog -30 | grep -c push
# Expect: 0

# 13. INV-03 hard gate -- Lattice-side parity smoke green
cd lattice && pnpm --filter lattice exec vitest run packages/lattice/src/providers/parity.test.ts 2>&1 | grep -E "Tests +[0-9]+ passed"
# Expect: 7 passed
```
</verification>

<success_criteria>
- `tests/lattice-providers-smoke.test.js` exists; `node tests/lattice-providers-smoke.test.js` exits 0 with >= 20 PASS, 0 FAIL (CONTEXT.md D-13 floor of ~10; actual ~38)
- All 5 new factories reachable via `await import('lattice')` (proven by Part 1 of smoke)
- Each new factory invoked with stub options returns ProviderAdapter shape with expected id (anthropic, gemini, xai, openrouter, lm-studio)
- Phase 1+2+3 carryforward exports still reachable (proven by Part 1b of smoke)
- 5 new adapters yield 5 distinct ids (CD-02 ceremony parity)
- `package.json` `scripts.test` chain ends with the new providers smoke; Phase 1+2+3 smoke entries byte-frozen upstream
- `npm test` exits 0; full chain green; last invocation is the new providers smoke
- `.planning/LATTICE-PIN.md` `current_lattice_sha:` matches `cd lattice && git rev-parse fsb-integration-experiments` exactly (post-Plan-04-04 HEAD)
- LATTICE-PIN.md per-phase table has 4 rows (Phase 1+2+3 byte-frozen; Phase 4 appended) narrating all 8 Phase 4 Lattice commits
- `.planning/REQUIREMENTS.md` LSDK-14..18 entries `[x]` Complete; 5 traceability rows added; LSDK-01..13 baseline byte-frozen
- ONE atomic FSB commit `feat(04): ship Phase 4 provider-adapter alignment (surface-presence smoke + audit-trail closure)` covering exactly the 4 files listed in `files_modified`
- FSB commit has `Ref: FSB v0.10.0-attempt-2 Phase 4` footer
- ZERO `extension/*` or `mcp/*` modifications across the entire phase
- INV-01 + INV-04 hold; Phase 1+2+3 smokes byte-frozen (29 + 39 + 72 PASS unchanged)
- Lattice INV-03 parity smoke green (7 PASS); ZERO `git push` on Lattice's remote (D-19 carryforward)
</success_criteria>

<output>
After completion, create `.planning/phases/04-provider-adapter-alignment/04-05-SUMMARY.md` with:
- FSB commit SHA (the single Phase 4 FSB commit)
- Files modified (4 files; exact list)
- New providers smoke assertion count (>= 20; actual ~38)
- Lattice HEAD SHA (post-Plan-04-04) recorded in LATTICE-PIN.md frontmatter
- LSDK-14..18 traceability table row contents
- INV-03 verification: Lattice parity smoke 7 PASS + FSB providers smoke ~38 PASS = INV-03 hard gate cleared
- Full FSB `npm test` chain exit code (0) and tail output
- Carryforward notes for Phase 5+ (MV3-survivability adapter contract): the 5 adapters are stable surface for any future FSB autopilot rewiring; Phase 5 work continues to layer ABOVE these adapters without modifying them
- Phase 4 milestone-relative progress: 5/5 adapter rows closed in audit-doc Providers domain; INV-03 hard-gate achieved; Phase 4 ready for `/gsd-verify-phase 4`
</output>
