---
phase: 21-package-intake-contract-mapping
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - .planning/PHANTOMSTREAM-PIN.md
  - tests/helpers/phantom-stream-public-pin.js
  - tests/phantom-stream-public-package.test.js
autonomous: true
requirements: ["PKG-01", "PKG-03"]
user_setup: []
must_haves:
  truths:
    - "FSB pins the published npm package `@full-self-browsing/phantom-stream@0.1.0`, not the stale `@fullselfbrowsing/phantom-stream` package name."
    - "The root package-lock records the registry integrity `sha512-Hf6K0bjAT5M9dUs7Xw1NB2Cb8hkmiMz7KDO0rq5mRkDKmQnLY1sTqTXwIX2r5gjLKVkl3TCemr3hSucVc1k69g==`."
    - "A planning pin file and helper test cross-check package.json, package-lock.json, and installed package metadata."
  artifacts:
    - path: ".planning/PHANTOMSTREAM-PIN.md"
      provides: "Approved package source and provenance record"
      contains: "@full-self-browsing/phantom-stream"
    - path: "tests/helpers/phantom-stream-public-pin.js"
      provides: "Reusable pin validator"
      contains: "EXPECTED_PUBLIC_PHANTOM_STREAM"
    - path: "tests/phantom-stream-public-package.test.js"
      provides: "Package pin smoke"
      contains: "validatePublicPhantomStreamPin"
---

<objective>
Approve and pin the PhantomStream npm package source before any production stream migration starts.

Purpose: Phase 21 discovered that the old unhyphenated package name is stale. This plan installs and records the correct published package as the immutable source for later phases.
Output: `package.json`, `package-lock.json`, `.planning/PHANTOMSTREAM-PIN.md`, and a focused pin smoke test.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/PHANTOMSTREAM-PACKAGE.md
@.planning/phases/21-package-intake-contract-mapping/21-CONTEXT.md
@package.json
@package-lock.json
@.planning/LATTICE-PIN.md
@tests/helpers/lattice-public-pin.js
@tests/lattice-public-package.test.js

<interfaces>
Approved registry package:
```text
name: @full-self-browsing/phantom-stream
version: 0.1.0
integrity: sha512-Hf6K0bjAT5M9dUs7Xw1NB2Cb8hkmiMz7KDO0rq5mRkDKmQnLY1sTqTXwIX2r5gjLKVkl3TCemr3hSucVc1k69g==
tarball: https://registry.npmjs.org/@full-self-browsing/phantom-stream/-/phantom-stream-0.1.0.tgz
```

Use the Lattice pin helper/test as the nearest local pattern, but do not alias PhantomStream to a different import name.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install the exact PhantomStream package</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json
    - package-lock.json
    - .planning/phases/21-package-intake-contract-mapping/21-CONTEXT.md
  </read_first>
  <action>Run `npm install @full-self-browsing/phantom-stream@0.1.0 --save-exact`. Inspect the resulting `package.json` and `package-lock.json` to confirm the dependency is exact, the lockfile root points to `0.1.0`, and `node_modules/@full-self-browsing/phantom-stream` resolves with the expected name/version/integrity. Do not add the old unhyphenated package name anywhere.</action>
  <acceptance_criteria>
    - `package.json` has dependency `"@full-self-browsing/phantom-stream": "0.1.0"`.
    - `package-lock.json` includes `node_modules/@full-self-browsing/phantom-stream` with version `0.1.0`.
    - The lockfile integrity matches the approved registry integrity.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const p=require('./package.json'); if (p.dependencies['@full-self-browsing/phantom-stream'] !== '0.1.0') process.exit(1)"</automated>
  </verify>
  <done>The root package metadata and lockfile pin PhantomStream exactly.</done>
</task>

<task type="auto">
  <name>Task 2: Record package provenance</name>
  <files>.planning/PHANTOMSTREAM-PIN.md</files>
  <read_first>
    - .planning/LATTICE-PIN.md
    - .planning/research/PHANTOMSTREAM-PACKAGE.md
  </read_first>
  <action>Create `.planning/PHANTOMSTREAM-PIN.md` with YAML frontmatter recording source `npm`, package `@full-self-browsing/phantom-stream`, version `0.1.0`, tarball URL, integrity, old package name status `E404`, checked date `2026-06-17`, and decision that npm is the approved source unless smoke tests fail. Include a short body explaining why no GitHub/tarball fallback is currently authorized.</action>
  <acceptance_criteria>
    - The pin file records exact source, version, tarball, and integrity.
    - The stale package name is documented as a rejected source.
    - The decision does not claim browser/runtime parity yet.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "@full-self-browsing/phantom-stream\\|sha512-Hf6K0bj" .planning/PHANTOMSTREAM-PIN.md</automated>
  </verify>
  <done>Package provenance is recorded in `.planning/PHANTOMSTREAM-PIN.md`.</done>
</task>

<task type="auto">
  <name>Task 3: Add pin validator and smoke test</name>
  <files>tests/helpers/phantom-stream-public-pin.js, tests/phantom-stream-public-package.test.js</files>
  <read_first>
    - tests/helpers/lattice-public-pin.js
    - tests/lattice-public-package.test.js
  </read_first>
  <action>Create a PhantomStream pin helper and package smoke test using the Lattice package test as the pattern. The helper must parse `.planning/PHANTOMSTREAM-PIN.md`, read `package.json` and `package-lock.json`, and return all pin mismatches. The smoke test must assert the helper passes, read the installed package metadata from `node_modules/@full-self-browsing/phantom-stream/package.json`, and check name, version, module type, and exported subpaths. Keep this test focused on pin/provenance; Plan 21-02 owns runtime export symbol smoke.</action>
  <acceptance_criteria>
    - The helper exports `EXPECTED_PUBLIC_PHANTOM_STREAM` and `validatePublicPhantomStreamPin`.
    - The smoke test fails if package.json, package-lock, installed metadata, or PHANTOMSTREAM-PIN disagree.
    - The test uses Node built-in `assert`/`assert/strict` and no test framework dependency.
  </acceptance_criteria>
  <verify>
    <automated>node tests/phantom-stream-public-package.test.js</automated>
  </verify>
  <done>The pin smoke passes and protects the approved source decision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> FSB dependency graph | Third-party package bytes enter FSB through package-lock integrity. |
| planning docs -> migration plans | Later phases depend on the recorded source decision. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01-01 | Tampering | `package-lock.json` | mitigate | Pin exact version and assert registry integrity in helper/test. |
| T-21-01-02 | Spoofing | package name | mitigate | Reject stale `@fullselfbrowsing/phantom-stream` and require `@full-self-browsing/phantom-stream`. |
| T-21-01-03 | Repudiation | source decision | mitigate | Record source, date, tarball, and fallback decision in `.planning/PHANTOMSTREAM-PIN.md`. |
</threat_model>

<verification>
Run:

```bash
node tests/phantom-stream-public-package.test.js
git diff --check
```
</verification>

<success_criteria>
- PKG-01 has exact npm source/provenance evidence.
- PKG-03 is resolved by the corrected package name and documented stale-name rejection.
- No production stream code imports PhantomStream in this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/21-package-intake-contract-mapping/21-01-SUMMARY.md`.
</output>
